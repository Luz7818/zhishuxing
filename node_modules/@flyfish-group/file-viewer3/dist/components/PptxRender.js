var xf = Object.defineProperty, _f = Object.defineProperties;
var yf = Object.getOwnPropertyDescriptors;
var As = Object.getOwnPropertySymbols;
var mf = Object.prototype.hasOwnProperty, bf = Object.prototype.propertyIsEnumerable;
var Ss = Math.pow, Cs = (e, t, n) => t in e ? xf(e, t, { enumerable: !0, configurable: !0, writable: !0, value: n }) : e[t] = n, Qi = (e, t) => {
  for (var n in t || (t = {}))
    mf.call(t, n) && Cs(e, n, t[n]);
  if (As)
    for (var n of As(t))
      bf.call(t, n) && Cs(e, n, t[n]);
  return e;
}, ks = (e, t) => _f(e, yf(t));
import { defineComponent as Tf, ref as wf, onMounted as $f, openBlock as Af, createElementBlock as Cf } from "vue";
import { c as Sf, g as kf } from "./_commonjsHelpers.js";
import { r as Ef } from "./worker-ref.js";
import { _ as Rf } from "./index.js";
var zu = { exports: {} };
/*!
 * jQuery JavaScript Library v3.7.1
 * https://jquery.com/
 *
 * Copyright OpenJS Foundation and other contributors
 * Released under the MIT license
 * https://jquery.org/license
 *
 * Date: 2023-08-28T13:37Z
 */
(function(e) {
  (function(t, n) {
    e.exports = t.document ? n(t, !0) : function(i) {
      if (!i.document)
        throw new Error("jQuery requires a window with a document");
      return n(i);
    };
  })(typeof window != "undefined" ? window : Sf, function(t, n) {
    var i = [], r = Object.getPrototypeOf, a = i.slice, o = i.flat ? function(u) {
      return i.flat.call(u);
    } : function(u) {
      return i.concat.apply([], u);
    }, s = i.push, l = i.indexOf, c = {}, f = c.toString, h = c.hasOwnProperty, g = h.toString, x = g.call(Object), y = {}, T = function(d) {
      return typeof d == "function" && typeof d.nodeType != "number" && typeof d.item != "function";
    }, A = function(d) {
      return d != null && d === d.window;
    }, w = t.document, C = {
      type: !0,
      src: !0,
      nonce: !0,
      noModule: !0
    };
    function R(u, d, v) {
      v = v || w;
      var _, m, b = v.createElement("script");
      if (b.text = u, d)
        for (_ in C)
          m = d[_] || d.getAttribute && d.getAttribute(_), m && b.setAttribute(_, m);
      v.head.appendChild(b).parentNode.removeChild(b);
    }
    function S(u) {
      return u == null ? u + "" : typeof u == "object" || typeof u == "function" ? c[f.call(u)] || "object" : typeof u;
    }
    var I = "3.7.1", L = /HTML$/i, p = function(u, d) {
      return new p.fn.init(u, d);
    };
    p.fn = p.prototype = {
      // The current version of jQuery being used
      jquery: I,
      constructor: p,
      // The default length of a jQuery object is 0
      length: 0,
      toArray: function() {
        return a.call(this);
      },
      // Get the Nth element in the matched element set OR
      // Get the whole matched element set as a clean array
      get: function(u) {
        return u == null ? a.call(this) : u < 0 ? this[u + this.length] : this[u];
      },
      // Take an array of elements and push it onto the stack
      // (returning the new matched element set)
      pushStack: function(u) {
        var d = p.merge(this.constructor(), u);
        return d.prevObject = this, d;
      },
      // Execute a callback for every element in the matched set.
      each: function(u) {
        return p.each(this, u);
      },
      map: function(u) {
        return this.pushStack(p.map(this, function(d, v) {
          return u.call(d, v, d);
        }));
      },
      slice: function() {
        return this.pushStack(a.apply(this, arguments));
      },
      first: function() {
        return this.eq(0);
      },
      last: function() {
        return this.eq(-1);
      },
      even: function() {
        return this.pushStack(p.grep(this, function(u, d) {
          return (d + 1) % 2;
        }));
      },
      odd: function() {
        return this.pushStack(p.grep(this, function(u, d) {
          return d % 2;
        }));
      },
      eq: function(u) {
        var d = this.length, v = +u + (u < 0 ? d : 0);
        return this.pushStack(v >= 0 && v < d ? [this[v]] : []);
      },
      end: function() {
        return this.prevObject || this.constructor();
      },
      // For internal use only.
      // Behaves like an Array's method, not like a jQuery method.
      push: s,
      sort: i.sort,
      splice: i.splice
    }, p.extend = p.fn.extend = function() {
      var u, d, v, _, m, b, $ = arguments[0] || {}, M = 1, E = arguments.length, N = !1;
      for (typeof $ == "boolean" && (N = $, $ = arguments[M] || {}, M++), typeof $ != "object" && !T($) && ($ = {}), M === E && ($ = this, M--); M < E; M++)
        if ((u = arguments[M]) != null)
          for (d in u)
            _ = u[d], !(d === "__proto__" || $ === _) && (N && _ && (p.isPlainObject(_) || (m = Array.isArray(_))) ? (v = $[d], m && !Array.isArray(v) ? b = [] : !m && !p.isPlainObject(v) ? b = {} : b = v, m = !1, $[d] = p.extend(N, b, _)) : _ !== void 0 && ($[d] = _));
      return $;
    }, p.extend({
      // Unique for each copy of jQuery on the page
      expando: "jQuery" + (I + Math.random()).replace(/\D/g, ""),
      // Assume jQuery is ready without the ready module
      isReady: !0,
      error: function(u) {
        throw new Error(u);
      },
      noop: function() {
      },
      isPlainObject: function(u) {
        var d, v;
        return !u || f.call(u) !== "[object Object]" ? !1 : (d = r(u), d ? (v = h.call(d, "constructor") && d.constructor, typeof v == "function" && g.call(v) === x) : !0);
      },
      isEmptyObject: function(u) {
        var d;
        for (d in u)
          return !1;
        return !0;
      },
      // Evaluates a script in a provided context; falls back to the global one
      // if not specified.
      globalEval: function(u, d, v) {
        R(u, { nonce: d && d.nonce }, v);
      },
      each: function(u, d) {
        var v, _ = 0;
        if (U(u))
          for (v = u.length; _ < v && d.call(u[_], _, u[_]) !== !1; _++)
            ;
        else
          for (_ in u)
            if (d.call(u[_], _, u[_]) === !1)
              break;
        return u;
      },
      // Retrieve the text value of an array of DOM nodes
      text: function(u) {
        var d, v = "", _ = 0, m = u.nodeType;
        if (!m)
          for (; d = u[_++]; )
            v += p.text(d);
        return m === 1 || m === 11 ? u.textContent : m === 9 ? u.documentElement.textContent : m === 3 || m === 4 ? u.nodeValue : v;
      },
      // results is for internal usage only
      makeArray: function(u, d) {
        var v = d || [];
        return u != null && (U(Object(u)) ? p.merge(
          v,
          typeof u == "string" ? [u] : u
        ) : s.call(v, u)), v;
      },
      inArray: function(u, d, v) {
        return d == null ? -1 : l.call(d, u, v);
      },
      isXMLDoc: function(u) {
        var d = u && u.namespaceURI, v = u && (u.ownerDocument || u).documentElement;
        return !L.test(d || v && v.nodeName || "HTML");
      },
      // Support: Android <=4.0 only, PhantomJS 1 only
      // push.apply(_, arraylike) throws on ancient WebKit
      merge: function(u, d) {
        for (var v = +d.length, _ = 0, m = u.length; _ < v; _++)
          u[m++] = d[_];
        return u.length = m, u;
      },
      grep: function(u, d, v) {
        for (var _, m = [], b = 0, $ = u.length, M = !v; b < $; b++)
          _ = !d(u[b], b), _ !== M && m.push(u[b]);
        return m;
      },
      // arg is for internal usage only
      map: function(u, d, v) {
        var _, m, b = 0, $ = [];
        if (U(u))
          for (_ = u.length; b < _; b++)
            m = d(u[b], b, v), m != null && $.push(m);
        else
          for (b in u)
            m = d(u[b], b, v), m != null && $.push(m);
        return o($);
      },
      // A global GUID counter for objects
      guid: 1,
      // jQuery.support is not used in Core but other projects attach their
      // properties to it so it needs to exist.
      support: y
    }), typeof Symbol == "function" && (p.fn[Symbol.iterator] = i[Symbol.iterator]), p.each(
      "Boolean Number String Function Array Date RegExp Object Error Symbol".split(" "),
      function(u, d) {
        c["[object " + d + "]"] = d.toLowerCase();
      }
    );
    function U(u) {
      var d = !!u && "length" in u && u.length, v = S(u);
      return T(u) || A(u) ? !1 : v === "array" || d === 0 || typeof d == "number" && d > 0 && d - 1 in u;
    }
    function H(u, d) {
      return u.nodeName && u.nodeName.toLowerCase() === d.toLowerCase();
    }
    var q = i.pop, X = i.sort, j = i.splice, z = "[\\x20\\t\\r\\n\\f]", G = new RegExp(
      "^" + z + "+|((?:^|[^\\\\])(?:\\\\.)*)" + z + "+$",
      "g"
    );
    p.contains = function(u, d) {
      var v = d && d.parentNode;
      return u === v || !!(v && v.nodeType === 1 && // Support: IE 9 - 11+
      // IE doesn't have `contains` on SVG.
      (u.contains ? u.contains(v) : u.compareDocumentPosition && u.compareDocumentPosition(v) & 16));
    };
    var _e = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\x80-\uFFFF\w-]/g;
    function Ue(u, d) {
      return d ? u === "\0" ? "�" : u.slice(0, -1) + "\\" + u.charCodeAt(u.length - 1).toString(16) + " " : "\\" + u;
    }
    p.escapeSelector = function(u) {
      return (u + "").replace(_e, Ue);
    };
    var te = w, re = s;
    (function() {
      var u, d, v, _, m, b = re, $, M, E, N, B, Y = p.expando, O = 0, Z = 0, de = Wi(), $e = Wi(), xe = Wi(), ut = Wi(), tt = function(k, D) {
        return k === D && (m = !0), 0;
      }, Vt = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped", Yt = "(?:\\\\[\\da-fA-F]{1,6}" + z + "?|\\\\[^\\r\\n\\f]|[\\w-]|[^\0-\\x7f])+", Te = "\\[" + z + "*(" + Yt + ")(?:" + z + // Operator (capture 2)
      "*([*^$|!~]?=)" + z + // "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
      `*(?:'((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)"|(` + Yt + "))|)" + z + "*\\]", An = ":(" + Yt + `)(?:\\((('((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)")|((?:\\\\.|[^\\\\()[\\]]|` + Te + ")*)|.*)\\)|)", Se = new RegExp(z + "+", "g"), We = new RegExp("^" + z + "*," + z + "*"), _i = new RegExp("^" + z + "*([>+~]|" + z + ")" + z + "*"), da = new RegExp(z + "|>"), Ut = new RegExp(An), yi = new RegExp("^" + Yt + "$"), jt = {
        ID: new RegExp("^#(" + Yt + ")"),
        CLASS: new RegExp("^\\.(" + Yt + ")"),
        TAG: new RegExp("^(" + Yt + "|[*])"),
        ATTR: new RegExp("^" + Te),
        PSEUDO: new RegExp("^" + An),
        CHILD: new RegExp(
          "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + z + "*(even|odd|(([+-]|)(\\d*)n|)" + z + "*(?:([+-]|)" + z + "*(\\d+)|))" + z + "*\\)|)",
          "i"
        ),
        bool: new RegExp("^(?:" + Vt + ")$", "i"),
        // For use in libraries implementing .is()
        // We use this for POS matching in `select`
        needsContext: new RegExp("^" + z + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + z + "*((?:-\\d)?\\d*)" + z + "*\\)|)(?=[^-]|$)", "i")
      }, gn = /^(?:input|select|textarea|button)$/i, pn = /^h\d$/i, Rt = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, ha = /[+~]/, rn = new RegExp("\\\\[\\da-fA-F]{1,6}" + z + "?|\\\\([^\\r\\n\\f])", "g"), an = function(k, D) {
        var P = "0x" + k.slice(1) - 65536;
        return D || (P < 0 ? String.fromCharCode(P + 65536) : String.fromCharCode(P >> 10 | 55296, P & 1023 | 56320));
      }, cf = function() {
        vn();
      }, ff = Zi(
        function(k) {
          return k.disabled === !0 && H(k, "fieldset");
        },
        { dir: "parentNode", next: "legend" }
      );
      function df() {
        try {
          return $.activeElement;
        } catch (k) {
        }
      }
      try {
        b.apply(
          i = a.call(te.childNodes),
          te.childNodes
        ), i[te.childNodes.length].nodeType;
      } catch (k) {
        b = {
          apply: function(D, P) {
            re.apply(D, a.call(P));
          },
          call: function(D) {
            re.apply(D, a.call(arguments, 1));
          }
        };
      }
      function De(k, D, P, F) {
        var V, Q, J, ne, ee, me, ce, pe = D && D.ownerDocument, be = D ? D.nodeType : 9;
        if (P = P || [], typeof k != "string" || !k || be !== 1 && be !== 9 && be !== 11)
          return P;
        if (!F && (vn(D), D = D || $, E)) {
          if (be !== 11 && (ee = Rt.exec(k)))
            if (V = ee[1]) {
              if (be === 9)
                if (J = D.getElementById(V)) {
                  if (J.id === V)
                    return b.call(P, J), P;
                } else
                  return P;
              else if (pe && (J = pe.getElementById(V)) && De.contains(D, J) && J.id === V)
                return b.call(P, J), P;
            } else {
              if (ee[2])
                return b.apply(P, D.getElementsByTagName(k)), P;
              if ((V = ee[3]) && D.getElementsByClassName)
                return b.apply(P, D.getElementsByClassName(V)), P;
            }
          if (!ut[k + " "] && (!N || !N.test(k))) {
            if (ce = k, pe = D, be === 1 && (da.test(k) || _i.test(k))) {
              for (pe = ha.test(k) && ga(D.parentNode) || D, (pe != D || !y.scope) && ((ne = D.getAttribute("id")) ? ne = p.escapeSelector(ne) : D.setAttribute("id", ne = Y)), me = mi(k), Q = me.length; Q--; )
                me[Q] = (ne ? "#" + ne : ":scope") + " " + qi(me[Q]);
              ce = me.join(",");
            }
            try {
              return b.apply(
                P,
                pe.querySelectorAll(ce)
              ), P;
            } catch (se) {
              ut(k, !0);
            } finally {
              ne === Y && D.removeAttribute("id");
            }
          }
        }
        return $s(k.replace(G, "$1"), D, P, F);
      }
      function Wi() {
        var k = [];
        function D(P, F) {
          return k.push(P + " ") > d.cacheLength && delete D[k.shift()], D[P + " "] = F;
        }
        return D;
      }
      function Nt(k) {
        return k[Y] = !0, k;
      }
      function Un(k) {
        var D = $.createElement("fieldset");
        try {
          return !!k(D);
        } catch (P) {
          return !1;
        } finally {
          D.parentNode && D.parentNode.removeChild(D), D = null;
        }
      }
      function hf(k) {
        return function(D) {
          return H(D, "input") && D.type === k;
        };
      }
      function gf(k) {
        return function(D) {
          return (H(D, "input") || H(D, "button")) && D.type === k;
        };
      }
      function Ts(k) {
        return function(D) {
          return "form" in D ? D.parentNode && D.disabled === !1 ? "label" in D ? "label" in D.parentNode ? D.parentNode.disabled === k : D.disabled === k : D.isDisabled === k || // Where there is no isDisabled, check manually
          D.isDisabled !== !k && ff(D) === k : D.disabled === k : "label" in D ? D.disabled === k : !1;
        };
      }
      function Cn(k) {
        return Nt(function(D) {
          return D = +D, Nt(function(P, F) {
            for (var V, Q = k([], P.length, D), J = Q.length; J--; )
              P[V = Q[J]] && (P[V] = !(F[V] = P[V]));
          });
        });
      }
      function ga(k) {
        return k && typeof k.getElementsByTagName != "undefined" && k;
      }
      function vn(k) {
        var D, P = k ? k.ownerDocument || k : te;
        return P == $ || P.nodeType !== 9 || !P.documentElement || ($ = P, M = $.documentElement, E = !p.isXMLDoc($), B = M.matches || M.webkitMatchesSelector || M.msMatchesSelector, M.msMatchesSelector && // Support: IE 11+, Edge 17 - 18+
        // IE/Edge sometimes throw a "Permission denied" error when strict-comparing
        // two documents; shallow comparisons work.
        // eslint-disable-next-line eqeqeq
        te != $ && (D = $.defaultView) && D.top !== D && D.addEventListener("unload", cf), y.getById = Un(function(F) {
          return M.appendChild(F).id = p.expando, !$.getElementsByName || !$.getElementsByName(p.expando).length;
        }), y.disconnectedMatch = Un(function(F) {
          return B.call(F, "*");
        }), y.scope = Un(function() {
          return $.querySelectorAll(":scope");
        }), y.cssHas = Un(function() {
          try {
            return $.querySelector(":has(*,:jqfake)"), !1;
          } catch (F) {
            return !0;
          }
        }), y.getById ? (d.filter.ID = function(F) {
          var V = F.replace(rn, an);
          return function(Q) {
            return Q.getAttribute("id") === V;
          };
        }, d.find.ID = function(F, V) {
          if (typeof V.getElementById != "undefined" && E) {
            var Q = V.getElementById(F);
            return Q ? [Q] : [];
          }
        }) : (d.filter.ID = function(F) {
          var V = F.replace(rn, an);
          return function(Q) {
            var J = typeof Q.getAttributeNode != "undefined" && Q.getAttributeNode("id");
            return J && J.value === V;
          };
        }, d.find.ID = function(F, V) {
          if (typeof V.getElementById != "undefined" && E) {
            var Q, J, ne, ee = V.getElementById(F);
            if (ee) {
              if (Q = ee.getAttributeNode("id"), Q && Q.value === F)
                return [ee];
              for (ne = V.getElementsByName(F), J = 0; ee = ne[J++]; )
                if (Q = ee.getAttributeNode("id"), Q && Q.value === F)
                  return [ee];
            }
            return [];
          }
        }), d.find.TAG = function(F, V) {
          return typeof V.getElementsByTagName != "undefined" ? V.getElementsByTagName(F) : V.querySelectorAll(F);
        }, d.find.CLASS = function(F, V) {
          if (typeof V.getElementsByClassName != "undefined" && E)
            return V.getElementsByClassName(F);
        }, N = [], Un(function(F) {
          var V;
          M.appendChild(F).innerHTML = "<a id='" + Y + "' href='' disabled='disabled'></a><select id='" + Y + "-\r\\' disabled='disabled'><option selected=''></option></select>", F.querySelectorAll("[selected]").length || N.push("\\[" + z + "*(?:value|" + Vt + ")"), F.querySelectorAll("[id~=" + Y + "-]").length || N.push("~="), F.querySelectorAll("a#" + Y + "+*").length || N.push(".#.+[+~]"), F.querySelectorAll(":checked").length || N.push(":checked"), V = $.createElement("input"), V.setAttribute("type", "hidden"), F.appendChild(V).setAttribute("name", "D"), M.appendChild(F).disabled = !0, F.querySelectorAll(":disabled").length !== 2 && N.push(":enabled", ":disabled"), V = $.createElement("input"), V.setAttribute("name", ""), F.appendChild(V), F.querySelectorAll("[name='']").length || N.push("\\[" + z + "*name" + z + "*=" + z + `*(?:''|"")`);
        }), y.cssHas || N.push(":has"), N = N.length && new RegExp(N.join("|")), tt = function(F, V) {
          if (F === V)
            return m = !0, 0;
          var Q = !F.compareDocumentPosition - !V.compareDocumentPosition;
          return Q || (Q = (F.ownerDocument || F) == (V.ownerDocument || V) ? F.compareDocumentPosition(V) : (
            // Otherwise we know they are disconnected
            1
          ), Q & 1 || !y.sortDetached && V.compareDocumentPosition(F) === Q ? F === $ || F.ownerDocument == te && De.contains(te, F) ? -1 : V === $ || V.ownerDocument == te && De.contains(te, V) ? 1 : _ ? l.call(_, F) - l.call(_, V) : 0 : Q & 4 ? -1 : 1);
        }), $;
      }
      De.matches = function(k, D) {
        return De(k, null, null, D);
      }, De.matchesSelector = function(k, D) {
        if (vn(k), E && !ut[D + " "] && (!N || !N.test(D)))
          try {
            var P = B.call(k, D);
            if (P || y.disconnectedMatch || // As well, disconnected nodes are said to be in a document
            // fragment in IE 9
            k.document && k.document.nodeType !== 11)
              return P;
          } catch (F) {
            ut(D, !0);
          }
        return De(D, $, null, [k]).length > 0;
      }, De.contains = function(k, D) {
        return (k.ownerDocument || k) != $ && vn(k), p.contains(k, D);
      }, De.attr = function(k, D) {
        (k.ownerDocument || k) != $ && vn(k);
        var P = d.attrHandle[D.toLowerCase()], F = P && h.call(d.attrHandle, D.toLowerCase()) ? P(k, D, !E) : void 0;
        return F !== void 0 ? F : k.getAttribute(D);
      }, De.error = function(k) {
        throw new Error("Syntax error, unrecognized expression: " + k);
      }, p.uniqueSort = function(k) {
        var D, P = [], F = 0, V = 0;
        if (m = !y.sortStable, _ = !y.sortStable && a.call(k, 0), X.call(k, tt), m) {
          for (; D = k[V++]; )
            D === k[V] && (F = P.push(V));
          for (; F--; )
            j.call(k, P[F], 1);
        }
        return _ = null, k;
      }, p.fn.uniqueSort = function() {
        return this.pushStack(p.uniqueSort(a.apply(this)));
      }, d = p.expr = {
        // Can be adjusted by the user
        cacheLength: 50,
        createPseudo: Nt,
        match: jt,
        attrHandle: {},
        find: {},
        relative: {
          ">": { dir: "parentNode", first: !0 },
          " ": { dir: "parentNode" },
          "+": { dir: "previousSibling", first: !0 },
          "~": { dir: "previousSibling" }
        },
        preFilter: {
          ATTR: function(k) {
            return k[1] = k[1].replace(rn, an), k[3] = (k[3] || k[4] || k[5] || "").replace(rn, an), k[2] === "~=" && (k[3] = " " + k[3] + " "), k.slice(0, 4);
          },
          CHILD: function(k) {
            return k[1] = k[1].toLowerCase(), k[1].slice(0, 3) === "nth" ? (k[3] || De.error(k[0]), k[4] = +(k[4] ? k[5] + (k[6] || 1) : 2 * (k[3] === "even" || k[3] === "odd")), k[5] = +(k[7] + k[8] || k[3] === "odd")) : k[3] && De.error(k[0]), k;
          },
          PSEUDO: function(k) {
            var D, P = !k[6] && k[2];
            return jt.CHILD.test(k[0]) ? null : (k[3] ? k[2] = k[4] || k[5] || "" : P && Ut.test(P) && // Get excess from tokenize (recursively)
            (D = mi(P, !0)) && // advance to the next closing parenthesis
            (D = P.indexOf(")", P.length - D) - P.length) && (k[0] = k[0].slice(0, D), k[2] = P.slice(0, D)), k.slice(0, 3));
          }
        },
        filter: {
          TAG: function(k) {
            var D = k.replace(rn, an).toLowerCase();
            return k === "*" ? function() {
              return !0;
            } : function(P) {
              return H(P, D);
            };
          },
          CLASS: function(k) {
            var D = de[k + " "];
            return D || (D = new RegExp("(^|" + z + ")" + k + "(" + z + "|$)")) && de(k, function(P) {
              return D.test(
                typeof P.className == "string" && P.className || typeof P.getAttribute != "undefined" && P.getAttribute("class") || ""
              );
            });
          },
          ATTR: function(k, D, P) {
            return function(F) {
              var V = De.attr(F, k);
              return V == null ? D === "!=" : D ? (V += "", D === "=" ? V === P : D === "!=" ? V !== P : D === "^=" ? P && V.indexOf(P) === 0 : D === "*=" ? P && V.indexOf(P) > -1 : D === "$=" ? P && V.slice(-P.length) === P : D === "~=" ? (" " + V.replace(Se, " ") + " ").indexOf(P) > -1 : D === "|=" ? V === P || V.slice(0, P.length + 1) === P + "-" : !1) : !0;
            };
          },
          CHILD: function(k, D, P, F, V) {
            var Q = k.slice(0, 3) !== "nth", J = k.slice(-4) !== "last", ne = D === "of-type";
            return F === 1 && V === 0 ? (
              // Shortcut for :nth-*(n)
              function(ee) {
                return !!ee.parentNode;
              }
            ) : function(ee, me, ce) {
              var pe, be, se, Fe, $t, dt = Q !== J ? "nextSibling" : "previousSibling", Mt = ee.parentNode, Gt = ne && ee.nodeName.toLowerCase(), jn = !ce && !ne, _t = !1;
              if (Mt) {
                if (Q) {
                  for (; dt; ) {
                    for (se = ee; se = se[dt]; )
                      if (ne ? H(se, Gt) : se.nodeType === 1)
                        return !1;
                    $t = dt = k === "only" && !$t && "nextSibling";
                  }
                  return !0;
                }
                if ($t = [J ? Mt.firstChild : Mt.lastChild], J && jn) {
                  for (be = Mt[Y] || (Mt[Y] = {}), pe = be[k] || [], Fe = pe[0] === O && pe[1], _t = Fe && pe[2], se = Fe && Mt.childNodes[Fe]; se = ++Fe && se && se[dt] || // Fallback to seeking `elem` from the start
                  (_t = Fe = 0) || $t.pop(); )
                    if (se.nodeType === 1 && ++_t && se === ee) {
                      be[k] = [O, Fe, _t];
                      break;
                    }
                } else if (jn && (be = ee[Y] || (ee[Y] = {}), pe = be[k] || [], Fe = pe[0] === O && pe[1], _t = Fe), _t === !1)
                  for (; (se = ++Fe && se && se[dt] || (_t = Fe = 0) || $t.pop()) && !((ne ? H(se, Gt) : se.nodeType === 1) && ++_t && (jn && (be = se[Y] || (se[Y] = {}), be[k] = [O, _t]), se === ee)); )
                    ;
                return _t -= V, _t === F || _t % F === 0 && _t / F >= 0;
              }
            };
          },
          PSEUDO: function(k, D) {
            var P, F = d.pseudos[k] || d.setFilters[k.toLowerCase()] || De.error("unsupported pseudo: " + k);
            return F[Y] ? F(D) : F.length > 1 ? (P = [k, k, "", D], d.setFilters.hasOwnProperty(k.toLowerCase()) ? Nt(function(V, Q) {
              for (var J, ne = F(V, D), ee = ne.length; ee--; )
                J = l.call(V, ne[ee]), V[J] = !(Q[J] = ne[ee]);
            }) : function(V) {
              return F(V, 0, P);
            }) : F;
          }
        },
        pseudos: {
          // Potentially complex pseudos
          not: Nt(function(k) {
            var D = [], P = [], F = _a(k.replace(G, "$1"));
            return F[Y] ? Nt(function(V, Q, J, ne) {
              for (var ee, me = F(V, null, ne, []), ce = V.length; ce--; )
                (ee = me[ce]) && (V[ce] = !(Q[ce] = ee));
            }) : function(V, Q, J) {
              return D[0] = V, F(D, null, J, P), D[0] = null, !P.pop();
            };
          }),
          has: Nt(function(k) {
            return function(D) {
              return De(k, D).length > 0;
            };
          }),
          contains: Nt(function(k) {
            return k = k.replace(rn, an), function(D) {
              return (D.textContent || p.text(D)).indexOf(k) > -1;
            };
          }),
          // "Whether an element is represented by a :lang() selector
          // is based solely on the element's language value
          // being equal to the identifier C,
          // or beginning with the identifier C immediately followed by "-".
          // The matching of C against the element's language value is performed case-insensitively.
          // The identifier C does not have to be a valid language name."
          // https://www.w3.org/TR/selectors/#lang-pseudo
          lang: Nt(function(k) {
            return yi.test(k || "") || De.error("unsupported lang: " + k), k = k.replace(rn, an).toLowerCase(), function(D) {
              var P;
              do
                if (P = E ? D.lang : D.getAttribute("xml:lang") || D.getAttribute("lang"))
                  return P = P.toLowerCase(), P === k || P.indexOf(k + "-") === 0;
              while ((D = D.parentNode) && D.nodeType === 1);
              return !1;
            };
          }),
          // Miscellaneous
          target: function(k) {
            var D = t.location && t.location.hash;
            return D && D.slice(1) === k.id;
          },
          root: function(k) {
            return k === M;
          },
          focus: function(k) {
            return k === df() && $.hasFocus() && !!(k.type || k.href || ~k.tabIndex);
          },
          // Boolean properties
          enabled: Ts(!1),
          disabled: Ts(!0),
          checked: function(k) {
            return H(k, "input") && !!k.checked || H(k, "option") && !!k.selected;
          },
          selected: function(k) {
            return k.parentNode && k.parentNode.selectedIndex, k.selected === !0;
          },
          // Contents
          empty: function(k) {
            for (k = k.firstChild; k; k = k.nextSibling)
              if (k.nodeType < 6)
                return !1;
            return !0;
          },
          parent: function(k) {
            return !d.pseudos.empty(k);
          },
          // Element/input types
          header: function(k) {
            return pn.test(k.nodeName);
          },
          input: function(k) {
            return gn.test(k.nodeName);
          },
          button: function(k) {
            return H(k, "input") && k.type === "button" || H(k, "button");
          },
          text: function(k) {
            var D;
            return H(k, "input") && k.type === "text" && // Support: IE <10 only
            // New HTML5 attribute values (e.g., "search") appear
            // with elem.type === "text"
            ((D = k.getAttribute("type")) == null || D.toLowerCase() === "text");
          },
          // Position-in-collection
          first: Cn(function() {
            return [0];
          }),
          last: Cn(function(k, D) {
            return [D - 1];
          }),
          eq: Cn(function(k, D, P) {
            return [P < 0 ? P + D : P];
          }),
          even: Cn(function(k, D) {
            for (var P = 0; P < D; P += 2)
              k.push(P);
            return k;
          }),
          odd: Cn(function(k, D) {
            for (var P = 1; P < D; P += 2)
              k.push(P);
            return k;
          }),
          lt: Cn(function(k, D, P) {
            var F;
            for (P < 0 ? F = P + D : P > D ? F = D : F = P; --F >= 0; )
              k.push(F);
            return k;
          }),
          gt: Cn(function(k, D, P) {
            for (var F = P < 0 ? P + D : P; ++F < D; )
              k.push(F);
            return k;
          })
        }
      }, d.pseudos.nth = d.pseudos.eq;
      for (u in { radio: !0, checkbox: !0, file: !0, password: !0, image: !0 })
        d.pseudos[u] = hf(u);
      for (u in { submit: !0, reset: !0 })
        d.pseudos[u] = gf(u);
      function ws() {
      }
      ws.prototype = d.filters = d.pseudos, d.setFilters = new ws();
      function mi(k, D) {
        var P, F, V, Q, J, ne, ee, me = $e[k + " "];
        if (me)
          return D ? 0 : me.slice(0);
        for (J = k, ne = [], ee = d.preFilter; J; ) {
          (!P || (F = We.exec(J))) && (F && (J = J.slice(F[0].length) || J), ne.push(V = [])), P = !1, (F = _i.exec(J)) && (P = F.shift(), V.push({
            value: P,
            // Cast descendant combinators to space
            type: F[0].replace(G, " ")
          }), J = J.slice(P.length));
          for (Q in d.filter)
            (F = jt[Q].exec(J)) && (!ee[Q] || (F = ee[Q](F))) && (P = F.shift(), V.push({
              value: P,
              type: Q,
              matches: F
            }), J = J.slice(P.length));
          if (!P)
            break;
        }
        return D ? J.length : J ? De.error(k) : (
          // Cache the tokens
          $e(k, ne).slice(0)
        );
      }
      function qi(k) {
        for (var D = 0, P = k.length, F = ""; D < P; D++)
          F += k[D].value;
        return F;
      }
      function Zi(k, D, P) {
        var F = D.dir, V = D.next, Q = V || F, J = P && Q === "parentNode", ne = Z++;
        return D.first ? (
          // Check against closest ancestor/preceding element
          function(ee, me, ce) {
            for (; ee = ee[F]; )
              if (ee.nodeType === 1 || J)
                return k(ee, me, ce);
            return !1;
          }
        ) : (
          // Check against all ancestor/preceding elements
          function(ee, me, ce) {
            var pe, be, se = [O, ne];
            if (ce) {
              for (; ee = ee[F]; )
                if ((ee.nodeType === 1 || J) && k(ee, me, ce))
                  return !0;
            } else
              for (; ee = ee[F]; )
                if (ee.nodeType === 1 || J)
                  if (be = ee[Y] || (ee[Y] = {}), V && H(ee, V))
                    ee = ee[F] || ee;
                  else {
                    if ((pe = be[Q]) && pe[0] === O && pe[1] === ne)
                      return se[2] = pe[2];
                    if (be[Q] = se, se[2] = k(ee, me, ce))
                      return !0;
                  }
            return !1;
          }
        );
      }
      function pa(k) {
        return k.length > 1 ? function(D, P, F) {
          for (var V = k.length; V--; )
            if (!k[V](D, P, F))
              return !1;
          return !0;
        } : k[0];
      }
      function pf(k, D, P) {
        for (var F = 0, V = D.length; F < V; F++)
          De(k, D[F], P);
        return P;
      }
      function Ki(k, D, P, F, V) {
        for (var Q, J = [], ne = 0, ee = k.length, me = D != null; ne < ee; ne++)
          (Q = k[ne]) && (!P || P(Q, F, V)) && (J.push(Q), me && D.push(ne));
        return J;
      }
      function va(k, D, P, F, V, Q) {
        return F && !F[Y] && (F = va(F)), V && !V[Y] && (V = va(V, Q)), Nt(function(J, ne, ee, me) {
          var ce, pe, be, se, Fe = [], $t = [], dt = ne.length, Mt = J || pf(
            D || "*",
            ee.nodeType ? [ee] : ee,
            []
          ), Gt = k && (J || !D) ? Ki(Mt, Fe, k, ee, me) : Mt;
          if (P ? (se = V || (J ? k : dt || F) ? (
            // ...intermediate processing is necessary
            []
          ) : (
            // ...otherwise use results directly
            ne
          ), P(Gt, se, ee, me)) : se = Gt, F)
            for (ce = Ki(se, $t), F(ce, [], ee, me), pe = ce.length; pe--; )
              (be = ce[pe]) && (se[$t[pe]] = !(Gt[$t[pe]] = be));
          if (J) {
            if (V || k) {
              if (V) {
                for (ce = [], pe = se.length; pe--; )
                  (be = se[pe]) && ce.push(Gt[pe] = be);
                V(null, se = [], ce, me);
              }
              for (pe = se.length; pe--; )
                (be = se[pe]) && (ce = V ? l.call(J, be) : Fe[pe]) > -1 && (J[ce] = !(ne[ce] = be));
            }
          } else
            se = Ki(
              se === ne ? se.splice(dt, se.length) : se
            ), V ? V(null, ne, se, me) : b.apply(ne, se);
        });
      }
      function xa(k) {
        for (var D, P, F, V = k.length, Q = d.relative[k[0].type], J = Q || d.relative[" "], ne = Q ? 1 : 0, ee = Zi(function(pe) {
          return pe === D;
        }, J, !0), me = Zi(function(pe) {
          return l.call(D, pe) > -1;
        }, J, !0), ce = [function(pe, be, se) {
          var Fe = !Q && (se || be != v) || ((D = be).nodeType ? ee(pe, be, se) : me(pe, be, se));
          return D = null, Fe;
        }]; ne < V; ne++)
          if (P = d.relative[k[ne].type])
            ce = [Zi(pa(ce), P)];
          else {
            if (P = d.filter[k[ne].type].apply(null, k[ne].matches), P[Y]) {
              for (F = ++ne; F < V && !d.relative[k[F].type]; F++)
                ;
              return va(
                ne > 1 && pa(ce),
                ne > 1 && qi(
                  // If the preceding token was a descendant combinator, insert an implicit any-element `*`
                  k.slice(0, ne - 1).concat({ value: k[ne - 2].type === " " ? "*" : "" })
                ).replace(G, "$1"),
                P,
                ne < F && xa(k.slice(ne, F)),
                F < V && xa(k = k.slice(F)),
                F < V && qi(k)
              );
            }
            ce.push(P);
          }
        return pa(ce);
      }
      function vf(k, D) {
        var P = D.length > 0, F = k.length > 0, V = function(Q, J, ne, ee, me) {
          var ce, pe, be, se = 0, Fe = "0", $t = Q && [], dt = [], Mt = v, Gt = Q || F && d.find.TAG("*", me), jn = O += Mt == null ? 1 : Math.random() || 0.1, _t = Gt.length;
          for (me && (v = J == $ || J || me); Fe !== _t && (ce = Gt[Fe]) != null; Fe++) {
            if (F && ce) {
              for (pe = 0, !J && ce.ownerDocument != $ && (vn(ce), ne = !E); be = k[pe++]; )
                if (be(ce, J || $, ne)) {
                  b.call(ee, ce);
                  break;
                }
              me && (O = jn);
            }
            P && ((ce = !be && ce) && se--, Q && $t.push(ce));
          }
          if (se += Fe, P && Fe !== se) {
            for (pe = 0; be = D[pe++]; )
              be($t, dt, J, ne);
            if (Q) {
              if (se > 0)
                for (; Fe--; )
                  $t[Fe] || dt[Fe] || (dt[Fe] = q.call(ee));
              dt = Ki(dt);
            }
            b.apply(ee, dt), me && !Q && dt.length > 0 && se + D.length > 1 && p.uniqueSort(ee);
          }
          return me && (O = jn, v = Mt), $t;
        };
        return P ? Nt(V) : V;
      }
      function _a(k, D) {
        var P, F = [], V = [], Q = xe[k + " "];
        if (!Q) {
          for (D || (D = mi(k)), P = D.length; P--; )
            Q = xa(D[P]), Q[Y] ? F.push(Q) : V.push(Q);
          Q = xe(
            k,
            vf(V, F)
          ), Q.selector = k;
        }
        return Q;
      }
      function $s(k, D, P, F) {
        var V, Q, J, ne, ee, me = typeof k == "function" && k, ce = !F && mi(k = me.selector || k);
        if (P = P || [], ce.length === 1) {
          if (Q = ce[0] = ce[0].slice(0), Q.length > 2 && (J = Q[0]).type === "ID" && D.nodeType === 9 && E && d.relative[Q[1].type]) {
            if (D = (d.find.ID(
              J.matches[0].replace(rn, an),
              D
            ) || [])[0], D)
              me && (D = D.parentNode);
            else
              return P;
            k = k.slice(Q.shift().value.length);
          }
          for (V = jt.needsContext.test(k) ? 0 : Q.length; V-- && (J = Q[V], !d.relative[ne = J.type]); )
            if ((ee = d.find[ne]) && (F = ee(
              J.matches[0].replace(rn, an),
              ha.test(Q[0].type) && ga(D.parentNode) || D
            ))) {
              if (Q.splice(V, 1), k = F.length && qi(Q), !k)
                return b.apply(P, F), P;
              break;
            }
        }
        return (me || _a(k, ce))(
          F,
          D,
          !E,
          P,
          !D || ha.test(k) && ga(D.parentNode) || D
        ), P;
      }
      y.sortStable = Y.split("").sort(tt).join("") === Y, vn(), y.sortDetached = Un(function(k) {
        return k.compareDocumentPosition($.createElement("fieldset")) & 1;
      }), p.find = De, p.expr[":"] = p.expr.pseudos, p.unique = p.uniqueSort, De.compile = _a, De.select = $s, De.setDocument = vn, De.tokenize = mi, De.escape = p.escapeSelector, De.getText = p.text, De.isXML = p.isXMLDoc, De.selectors = p.expr, De.support = p.support, De.uniqueSort = p.uniqueSort;
    })();
    var ue = function(u, d, v) {
      for (var _ = [], m = v !== void 0; (u = u[d]) && u.nodeType !== 9; )
        if (u.nodeType === 1) {
          if (m && p(u).is(v))
            break;
          _.push(u);
        }
      return _;
    }, we = function(u, d) {
      for (var v = []; u; u = u.nextSibling)
        u.nodeType === 1 && u !== d && v.push(u);
      return v;
    }, st = p.expr.match.needsContext, Je = /^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i;
    function ft(u, d, v) {
      return T(d) ? p.grep(u, function(_, m) {
        return !!d.call(_, m, _) !== v;
      }) : d.nodeType ? p.grep(u, function(_) {
        return _ === d !== v;
      }) : typeof d != "string" ? p.grep(u, function(_) {
        return l.call(d, _) > -1 !== v;
      }) : p.filter(d, u, v);
    }
    p.filter = function(u, d, v) {
      var _ = d[0];
      return v && (u = ":not(" + u + ")"), d.length === 1 && _.nodeType === 1 ? p.find.matchesSelector(_, u) ? [_] : [] : p.find.matches(u, p.grep(d, function(m) {
        return m.nodeType === 1;
      }));
    }, p.fn.extend({
      find: function(u) {
        var d, v, _ = this.length, m = this;
        if (typeof u != "string")
          return this.pushStack(p(u).filter(function() {
            for (d = 0; d < _; d++)
              if (p.contains(m[d], this))
                return !0;
          }));
        for (v = this.pushStack([]), d = 0; d < _; d++)
          p.find(u, m[d], v);
        return _ > 1 ? p.uniqueSort(v) : v;
      },
      filter: function(u) {
        return this.pushStack(ft(this, u || [], !1));
      },
      not: function(u) {
        return this.pushStack(ft(this, u || [], !0));
      },
      is: function(u) {
        return !!ft(
          this,
          // If this is a positional/relative selector, check membership in the returned set
          // so $("p:first").is("p:last") won't return true for a doc with two "p".
          typeof u == "string" && st.test(u) ? p(u) : u || [],
          !1
        ).length;
      }
    });
    var vt, et = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/, oe = p.fn.init = function(u, d, v) {
      var _, m;
      if (!u)
        return this;
      if (v = v || vt, typeof u == "string")
        if (u[0] === "<" && u[u.length - 1] === ">" && u.length >= 3 ? _ = [null, u, null] : _ = et.exec(u), _ && (_[1] || !d))
          if (_[1]) {
            if (d = d instanceof p ? d[0] : d, p.merge(this, p.parseHTML(
              _[1],
              d && d.nodeType ? d.ownerDocument || d : w,
              !0
            )), Je.test(_[1]) && p.isPlainObject(d))
              for (_ in d)
                T(this[_]) ? this[_](d[_]) : this.attr(_, d[_]);
            return this;
          } else
            return m = w.getElementById(_[2]), m && (this[0] = m, this.length = 1), this;
        else
          return !d || d.jquery ? (d || v).find(u) : this.constructor(d).find(u);
      else {
        if (u.nodeType)
          return this[0] = u, this.length = 1, this;
        if (T(u))
          return v.ready !== void 0 ? v.ready(u) : (
            // Execute immediately if ready is not present
            u(p)
          );
      }
      return p.makeArray(u, this);
    };
    oe.prototype = p.fn, vt = p(w);
    var He = /^(?:parents|prev(?:Until|All))/, Oe = {
      children: !0,
      contents: !0,
      next: !0,
      prev: !0
    };
    p.fn.extend({
      has: function(u) {
        var d = p(u, this), v = d.length;
        return this.filter(function() {
          for (var _ = 0; _ < v; _++)
            if (p.contains(this, d[_]))
              return !0;
        });
      },
      closest: function(u, d) {
        var v, _ = 0, m = this.length, b = [], $ = typeof u != "string" && p(u);
        if (!st.test(u)) {
          for (; _ < m; _++)
            for (v = this[_]; v && v !== d; v = v.parentNode)
              if (v.nodeType < 11 && ($ ? $.index(v) > -1 : (
                // Don't pass non-elements to jQuery#find
                v.nodeType === 1 && p.find.matchesSelector(v, u)
              ))) {
                b.push(v);
                break;
              }
        }
        return this.pushStack(b.length > 1 ? p.uniqueSort(b) : b);
      },
      // Determine the position of an element within the set
      index: function(u) {
        return u ? typeof u == "string" ? l.call(p(u), this[0]) : l.call(
          this,
          // If it receives a jQuery object, the first element is used
          u.jquery ? u[0] : u
        ) : this[0] && this[0].parentNode ? this.first().prevAll().length : -1;
      },
      add: function(u, d) {
        return this.pushStack(
          p.uniqueSort(
            p.merge(this.get(), p(u, d))
          )
        );
      },
      addBack: function(u) {
        return this.add(
          u == null ? this.prevObject : this.prevObject.filter(u)
        );
      }
    });
    function K(u, d) {
      for (; (u = u[d]) && u.nodeType !== 1; )
        ;
      return u;
    }
    p.each({
      parent: function(u) {
        var d = u.parentNode;
        return d && d.nodeType !== 11 ? d : null;
      },
      parents: function(u) {
        return ue(u, "parentNode");
      },
      parentsUntil: function(u, d, v) {
        return ue(u, "parentNode", v);
      },
      next: function(u) {
        return K(u, "nextSibling");
      },
      prev: function(u) {
        return K(u, "previousSibling");
      },
      nextAll: function(u) {
        return ue(u, "nextSibling");
      },
      prevAll: function(u) {
        return ue(u, "previousSibling");
      },
      nextUntil: function(u, d, v) {
        return ue(u, "nextSibling", v);
      },
      prevUntil: function(u, d, v) {
        return ue(u, "previousSibling", v);
      },
      siblings: function(u) {
        return we((u.parentNode || {}).firstChild, u);
      },
      children: function(u) {
        return we(u.firstChild);
      },
      contents: function(u) {
        return u.contentDocument != null && // Support: IE 11+
        // <object> elements with no `data` attribute has an object
        // `contentDocument` with a `null` prototype.
        r(u.contentDocument) ? u.contentDocument : (H(u, "template") && (u = u.content || u), p.merge([], u.childNodes));
      }
    }, function(u, d) {
      p.fn[u] = function(v, _) {
        var m = p.map(this, d, v);
        return u.slice(-5) !== "Until" && (_ = v), _ && typeof _ == "string" && (m = p.filter(_, m)), this.length > 1 && (Oe[u] || p.uniqueSort(m), He.test(u) && m.reverse()), this.pushStack(m);
      };
    });
    var ie = /[^\x20\t\r\n\f]+/g;
    function fe(u) {
      var d = {};
      return p.each(u.match(ie) || [], function(v, _) {
        d[_] = !0;
      }), d;
    }
    p.Callbacks = function(u) {
      u = typeof u == "string" ? fe(u) : p.extend({}, u);
      var d, v, _, m, b = [], $ = [], M = -1, E = function() {
        for (m = m || u.once, _ = d = !0; $.length; M = -1)
          for (v = $.shift(); ++M < b.length; )
            b[M].apply(v[0], v[1]) === !1 && u.stopOnFalse && (M = b.length, v = !1);
        u.memory || (v = !1), d = !1, m && (v ? b = [] : b = "");
      }, N = {
        // Add a callback or a collection of callbacks to the list
        add: function() {
          return b && (v && !d && (M = b.length - 1, $.push(v)), function B(Y) {
            p.each(Y, function(O, Z) {
              T(Z) ? (!u.unique || !N.has(Z)) && b.push(Z) : Z && Z.length && S(Z) !== "string" && B(Z);
            });
          }(arguments), v && !d && E()), this;
        },
        // Remove a callback from the list
        remove: function() {
          return p.each(arguments, function(B, Y) {
            for (var O; (O = p.inArray(Y, b, O)) > -1; )
              b.splice(O, 1), O <= M && M--;
          }), this;
        },
        // Check if a given callback is in the list.
        // If no argument is given, return whether or not list has callbacks attached.
        has: function(B) {
          return B ? p.inArray(B, b) > -1 : b.length > 0;
        },
        // Remove all callbacks from the list
        empty: function() {
          return b && (b = []), this;
        },
        // Disable .fire and .add
        // Abort any current/pending executions
        // Clear all callbacks and values
        disable: function() {
          return m = $ = [], b = v = "", this;
        },
        disabled: function() {
          return !b;
        },
        // Disable .fire
        // Also disable .add unless we have memory (since it would have no effect)
        // Abort any pending executions
        lock: function() {
          return m = $ = [], !v && !d && (b = v = ""), this;
        },
        locked: function() {
          return !!m;
        },
        // Call all callbacks with the given context and arguments
        fireWith: function(B, Y) {
          return m || (Y = Y || [], Y = [B, Y.slice ? Y.slice() : Y], $.push(Y), d || E()), this;
        },
        // Call all the callbacks with the given arguments
        fire: function() {
          return N.fireWith(this, arguments), this;
        },
        // To know if the callbacks have already been called at least once
        fired: function() {
          return !!_;
        }
      };
      return N;
    };
    function W(u) {
      return u;
    }
    function Le(u) {
      throw u;
    }
    function Ce(u, d, v, _) {
      var m;
      try {
        u && T(m = u.promise) ? m.call(u).done(d).fail(v) : u && T(m = u.then) ? m.call(u, d, v) : d.apply(void 0, [u].slice(_));
      } catch (b) {
        v.apply(void 0, [b]);
      }
    }
    p.extend({
      Deferred: function(u) {
        var d = [
          // action, add listener, callbacks,
          // ... .then handlers, argument index, [final state]
          [
            "notify",
            "progress",
            p.Callbacks("memory"),
            p.Callbacks("memory"),
            2
          ],
          [
            "resolve",
            "done",
            p.Callbacks("once memory"),
            p.Callbacks("once memory"),
            0,
            "resolved"
          ],
          [
            "reject",
            "fail",
            p.Callbacks("once memory"),
            p.Callbacks("once memory"),
            1,
            "rejected"
          ]
        ], v = "pending", _ = {
          state: function() {
            return v;
          },
          always: function() {
            return m.done(arguments).fail(arguments), this;
          },
          catch: function(b) {
            return _.then(null, b);
          },
          // Keep pipe for back-compat
          pipe: function() {
            var b = arguments;
            return p.Deferred(function($) {
              p.each(d, function(M, E) {
                var N = T(b[E[4]]) && b[E[4]];
                m[E[1]](function() {
                  var B = N && N.apply(this, arguments);
                  B && T(B.promise) ? B.promise().progress($.notify).done($.resolve).fail($.reject) : $[E[0] + "With"](
                    this,
                    N ? [B] : arguments
                  );
                });
              }), b = null;
            }).promise();
          },
          then: function(b, $, M) {
            var E = 0;
            function N(B, Y, O, Z) {
              return function() {
                var de = this, $e = arguments, xe = function() {
                  var tt, Vt;
                  if (!(B < E)) {
                    if (tt = O.apply(de, $e), tt === Y.promise())
                      throw new TypeError("Thenable self-resolution");
                    Vt = tt && // Support: Promises/A+ section 2.3.4
                    // https://promisesaplus.com/#point-64
                    // Only check objects and functions for thenability
                    (typeof tt == "object" || typeof tt == "function") && tt.then, T(Vt) ? Z ? Vt.call(
                      tt,
                      N(E, Y, W, Z),
                      N(E, Y, Le, Z)
                    ) : (E++, Vt.call(
                      tt,
                      N(E, Y, W, Z),
                      N(E, Y, Le, Z),
                      N(
                        E,
                        Y,
                        W,
                        Y.notifyWith
                      )
                    )) : (O !== W && (de = void 0, $e = [tt]), (Z || Y.resolveWith)(de, $e));
                  }
                }, ut = Z ? xe : function() {
                  try {
                    xe();
                  } catch (tt) {
                    p.Deferred.exceptionHook && p.Deferred.exceptionHook(
                      tt,
                      ut.error
                    ), B + 1 >= E && (O !== Le && (de = void 0, $e = [tt]), Y.rejectWith(de, $e));
                  }
                };
                B ? ut() : (p.Deferred.getErrorHook ? ut.error = p.Deferred.getErrorHook() : p.Deferred.getStackHook && (ut.error = p.Deferred.getStackHook()), t.setTimeout(ut));
              };
            }
            return p.Deferred(function(B) {
              d[0][3].add(
                N(
                  0,
                  B,
                  T(M) ? M : W,
                  B.notifyWith
                )
              ), d[1][3].add(
                N(
                  0,
                  B,
                  T(b) ? b : W
                )
              ), d[2][3].add(
                N(
                  0,
                  B,
                  T($) ? $ : Le
                )
              );
            }).promise();
          },
          // Get a promise for this deferred
          // If obj is provided, the promise aspect is added to the object
          promise: function(b) {
            return b != null ? p.extend(b, _) : _;
          }
        }, m = {};
        return p.each(d, function(b, $) {
          var M = $[2], E = $[5];
          _[$[1]] = M.add, E && M.add(
            function() {
              v = E;
            },
            // rejected_callbacks.disable
            // fulfilled_callbacks.disable
            d[3 - b][2].disable,
            // rejected_handlers.disable
            // fulfilled_handlers.disable
            d[3 - b][3].disable,
            // progress_callbacks.lock
            d[0][2].lock,
            // progress_handlers.lock
            d[0][3].lock
          ), M.add($[3].fire), m[$[0]] = function() {
            return m[$[0] + "With"](this === m ? void 0 : this, arguments), this;
          }, m[$[0] + "With"] = M.fireWith;
        }), _.promise(m), u && u.call(m, m), m;
      },
      // Deferred helper
      when: function(u) {
        var d = arguments.length, v = d, _ = Array(v), m = a.call(arguments), b = p.Deferred(), $ = function(M) {
          return function(E) {
            _[M] = this, m[M] = arguments.length > 1 ? a.call(arguments) : E, --d || b.resolveWith(_, m);
          };
        };
        if (d <= 1 && (Ce(
          u,
          b.done($(v)).resolve,
          b.reject,
          !d
        ), b.state() === "pending" || T(m[v] && m[v].then)))
          return b.then();
        for (; v--; )
          Ce(m[v], $(v), b.reject);
        return b.promise();
      }
    });
    var Ge = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;
    p.Deferred.exceptionHook = function(u, d) {
      t.console && t.console.warn && u && Ge.test(u.name) && t.console.warn(
        "jQuery.Deferred exception: " + u.message,
        u.stack,
        d
      );
    }, p.readyException = function(u) {
      t.setTimeout(function() {
        throw u;
      });
    };
    var Ve = p.Deferred();
    p.fn.ready = function(u) {
      return Ve.then(u).catch(function(d) {
        p.readyException(d);
      }), this;
    }, p.extend({
      // Is the DOM ready to be used? Set to true once it occurs.
      isReady: !1,
      // A counter to track how many items to wait for before
      // the ready event fires. See trac-6781
      readyWait: 1,
      // Handle when the DOM is ready
      ready: function(u) {
        (u === !0 ? --p.readyWait : p.isReady) || (p.isReady = !0, !(u !== !0 && --p.readyWait > 0) && Ve.resolveWith(w, [p]));
      }
    }), p.ready.then = Ve.then;
    function xt() {
      w.removeEventListener("DOMContentLoaded", xt), t.removeEventListener("load", xt), p.ready();
    }
    w.readyState === "complete" || w.readyState !== "loading" && !w.documentElement.doScroll ? t.setTimeout(p.ready) : (w.addEventListener("DOMContentLoaded", xt), t.addEventListener("load", xt));
    var mt = function(u, d, v, _, m, b, $) {
      var M = 0, E = u.length, N = v == null;
      if (S(v) === "object") {
        m = !0;
        for (M in v)
          mt(u, d, M, v[M], !0, b, $);
      } else if (_ !== void 0 && (m = !0, T(_) || ($ = !0), N && ($ ? (d.call(u, _), d = null) : (N = d, d = function(B, Y, O) {
        return N.call(p(B), O);
      })), d))
        for (; M < E; M++)
          d(
            u[M],
            v,
            $ ? _ : _.call(u[M], M, d(u[M], v))
          );
      return m ? u : N ? d.call(u) : E ? d(u[0], v) : b;
    }, bc = /^-ms-/, Tc = /-([a-z])/g;
    function wc(u, d) {
      return d.toUpperCase();
    }
    function Ht(u) {
      return u.replace(bc, "ms-").replace(Tc, wc);
    }
    var ci = function(u) {
      return u.nodeType === 1 || u.nodeType === 9 || !+u.nodeType;
    };
    function fi() {
      this.expando = p.expando + fi.uid++;
    }
    fi.uid = 1, fi.prototype = {
      cache: function(u) {
        var d = u[this.expando];
        return d || (d = {}, ci(u) && (u.nodeType ? u[this.expando] = d : Object.defineProperty(u, this.expando, {
          value: d,
          configurable: !0
        }))), d;
      },
      set: function(u, d, v) {
        var _, m = this.cache(u);
        if (typeof d == "string")
          m[Ht(d)] = v;
        else
          for (_ in d)
            m[Ht(_)] = d[_];
        return m;
      },
      get: function(u, d) {
        return d === void 0 ? this.cache(u) : (
          // Always use camelCase key (gh-2257)
          u[this.expando] && u[this.expando][Ht(d)]
        );
      },
      access: function(u, d, v) {
        return d === void 0 || d && typeof d == "string" && v === void 0 ? this.get(u, d) : (this.set(u, d, v), v !== void 0 ? v : d);
      },
      remove: function(u, d) {
        var v, _ = u[this.expando];
        if (_ !== void 0) {
          if (d !== void 0)
            for (Array.isArray(d) ? d = d.map(Ht) : (d = Ht(d), d = d in _ ? [d] : d.match(ie) || []), v = d.length; v--; )
              delete _[d[v]];
          (d === void 0 || p.isEmptyObject(_)) && (u.nodeType ? u[this.expando] = void 0 : delete u[this.expando]);
        }
      },
      hasData: function(u) {
        var d = u[this.expando];
        return d !== void 0 && !p.isEmptyObject(d);
      }
    };
    var ae = new fi(), bt = new fi(), $c = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/, Ac = /[A-Z]/g;
    function Cc(u) {
      return u === "true" ? !0 : u === "false" ? !1 : u === "null" ? null : u === +u + "" ? +u : $c.test(u) ? JSON.parse(u) : u;
    }
    function Uo(u, d, v) {
      var _;
      if (v === void 0 && u.nodeType === 1)
        if (_ = "data-" + d.replace(Ac, "-$&").toLowerCase(), v = u.getAttribute(_), typeof v == "string") {
          try {
            v = Cc(v);
          } catch (m) {
          }
          bt.set(u, d, v);
        } else
          v = void 0;
      return v;
    }
    p.extend({
      hasData: function(u) {
        return bt.hasData(u) || ae.hasData(u);
      },
      data: function(u, d, v) {
        return bt.access(u, d, v);
      },
      removeData: function(u, d) {
        bt.remove(u, d);
      },
      // TODO: Now that all calls to _data and _removeData have been replaced
      // with direct calls to dataPriv methods, these can be deprecated.
      _data: function(u, d, v) {
        return ae.access(u, d, v);
      },
      _removeData: function(u, d) {
        ae.remove(u, d);
      }
    }), p.fn.extend({
      data: function(u, d) {
        var v, _, m, b = this[0], $ = b && b.attributes;
        if (u === void 0) {
          if (this.length && (m = bt.get(b), b.nodeType === 1 && !ae.get(b, "hasDataAttrs"))) {
            for (v = $.length; v--; )
              $[v] && (_ = $[v].name, _.indexOf("data-") === 0 && (_ = Ht(_.slice(5)), Uo(b, _, m[_])));
            ae.set(b, "hasDataAttrs", !0);
          }
          return m;
        }
        return typeof u == "object" ? this.each(function() {
          bt.set(this, u);
        }) : mt(this, function(M) {
          var E;
          if (b && M === void 0)
            return E = bt.get(b, u), E !== void 0 || (E = Uo(b, u), E !== void 0) ? E : void 0;
          this.each(function() {
            bt.set(this, u, M);
          });
        }, null, d, arguments.length > 1, null, !0);
      },
      removeData: function(u) {
        return this.each(function() {
          bt.remove(this, u);
        });
      }
    }), p.extend({
      queue: function(u, d, v) {
        var _;
        if (u)
          return d = (d || "fx") + "queue", _ = ae.get(u, d), v && (!_ || Array.isArray(v) ? _ = ae.access(u, d, p.makeArray(v)) : _.push(v)), _ || [];
      },
      dequeue: function(u, d) {
        d = d || "fx";
        var v = p.queue(u, d), _ = v.length, m = v.shift(), b = p._queueHooks(u, d), $ = function() {
          p.dequeue(u, d);
        };
        m === "inprogress" && (m = v.shift(), _--), m && (d === "fx" && v.unshift("inprogress"), delete b.stop, m.call(u, $, b)), !_ && b && b.empty.fire();
      },
      // Not public - generate a queueHooks object, or return the current one
      _queueHooks: function(u, d) {
        var v = d + "queueHooks";
        return ae.get(u, v) || ae.access(u, v, {
          empty: p.Callbacks("once memory").add(function() {
            ae.remove(u, [d + "queue", v]);
          })
        });
      }
    }), p.fn.extend({
      queue: function(u, d) {
        var v = 2;
        return typeof u != "string" && (d = u, u = "fx", v--), arguments.length < v ? p.queue(this[0], u) : d === void 0 ? this : this.each(function() {
          var _ = p.queue(this, u, d);
          p._queueHooks(this, u), u === "fx" && _[0] !== "inprogress" && p.dequeue(this, u);
        });
      },
      dequeue: function(u) {
        return this.each(function() {
          p.dequeue(this, u);
        });
      },
      clearQueue: function(u) {
        return this.queue(u || "fx", []);
      },
      // Get a promise resolved when queues of a certain type
      // are emptied (fx is the type by default)
      promise: function(u, d) {
        var v, _ = 1, m = p.Deferred(), b = this, $ = this.length, M = function() {
          --_ || m.resolveWith(b, [b]);
        };
        for (typeof u != "string" && (d = u, u = void 0), u = u || "fx"; $--; )
          v = ae.get(b[$], u + "queueHooks"), v && v.empty && (_++, v.empty.add(M));
        return M(), m.promise(d);
      }
    });
    var jo = /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/.source, di = new RegExp("^(?:([+-])=|)(" + jo + ")([a-z%]*)$", "i"), nn = ["Top", "Right", "Bottom", "Left"], Tn = w.documentElement, Bn = function(u) {
      return p.contains(u.ownerDocument, u);
    }, Sc = { composed: !0 };
    Tn.getRootNode && (Bn = function(u) {
      return p.contains(u.ownerDocument, u) || u.getRootNode(Sc) === u.ownerDocument;
    });
    var Vi = function(u, d) {
      return u = d || u, u.style.display === "none" || u.style.display === "" && // Otherwise, check computed style
      // Support: Firefox <=43 - 45
      // Disconnected elements can have computed display: none, so first confirm that elem is
      // in the document.
      Bn(u) && p.css(u, "display") === "none";
    };
    function Go(u, d, v, _) {
      var m, b, $ = 20, M = _ ? function() {
        return _.cur();
      } : function() {
        return p.css(u, d, "");
      }, E = M(), N = v && v[3] || (p.cssNumber[d] ? "" : "px"), B = u.nodeType && (p.cssNumber[d] || N !== "px" && +E) && di.exec(p.css(u, d));
      if (B && B[3] !== N) {
        for (E = E / 2, N = N || B[3], B = +E || 1; $--; )
          p.style(u, d, B + N), (1 - b) * (1 - (b = M() / E || 0.5)) <= 0 && ($ = 0), B = B / b;
        B = B * 2, p.style(u, d, B + N), v = v || [];
      }
      return v && (B = +B || +E || 0, m = v[1] ? B + (v[1] + 1) * v[2] : +v[2], _ && (_.unit = N, _.start = B, _.end = m)), m;
    }
    var Wo = {};
    function kc(u) {
      var d, v = u.ownerDocument, _ = u.nodeName, m = Wo[_];
      return m || (d = v.body.appendChild(v.createElement(_)), m = p.css(d, "display"), d.parentNode.removeChild(d), m === "none" && (m = "block"), Wo[_] = m, m);
    }
    function Xn(u, d) {
      for (var v, _, m = [], b = 0, $ = u.length; b < $; b++)
        _ = u[b], _.style && (v = _.style.display, d ? (v === "none" && (m[b] = ae.get(_, "display") || null, m[b] || (_.style.display = "")), _.style.display === "" && Vi(_) && (m[b] = kc(_))) : v !== "none" && (m[b] = "none", ae.set(_, "display", v)));
      for (b = 0; b < $; b++)
        m[b] != null && (u[b].style.display = m[b]);
      return u;
    }
    p.fn.extend({
      show: function() {
        return Xn(this, !0);
      },
      hide: function() {
        return Xn(this);
      },
      toggle: function(u) {
        return typeof u == "boolean" ? u ? this.show() : this.hide() : this.each(function() {
          Vi(this) ? p(this).show() : p(this).hide();
        });
      }
    });
    var hi = /^(?:checkbox|radio)$/i, qo = /<([a-z][^\/\0>\x20\t\r\n\f]*)/i, Zo = /^$|^module$|\/(?:java|ecma)script/i;
    (function() {
      var u = w.createDocumentFragment(), d = u.appendChild(w.createElement("div")), v = w.createElement("input");
      v.setAttribute("type", "radio"), v.setAttribute("checked", "checked"), v.setAttribute("name", "t"), d.appendChild(v), y.checkClone = d.cloneNode(!0).cloneNode(!0).lastChild.checked, d.innerHTML = "<textarea>x</textarea>", y.noCloneChecked = !!d.cloneNode(!0).lastChild.defaultValue, d.innerHTML = "<option></option>", y.option = !!d.lastChild;
    })();
    var Et = {
      // XHTML parsers do not magically insert elements in the
      // same way that tag soup parsers do. So we cannot shorten
      // this by omitting <tbody> or other required elements.
      thead: [1, "<table>", "</table>"],
      col: [2, "<table><colgroup>", "</colgroup></table>"],
      tr: [2, "<table><tbody>", "</tbody></table>"],
      td: [3, "<table><tbody><tr>", "</tr></tbody></table>"],
      _default: [0, "", ""]
    };
    Et.tbody = Et.tfoot = Et.colgroup = Et.caption = Et.thead, Et.th = Et.td, y.option || (Et.optgroup = Et.option = [1, "<select multiple='multiple'>", "</select>"]);
    function Tt(u, d) {
      var v;
      return typeof u.getElementsByTagName != "undefined" ? v = u.getElementsByTagName(d || "*") : typeof u.querySelectorAll != "undefined" ? v = u.querySelectorAll(d || "*") : v = [], d === void 0 || d && H(u, d) ? p.merge([u], v) : v;
    }
    function Qr(u, d) {
      for (var v = 0, _ = u.length; v < _; v++)
        ae.set(
          u[v],
          "globalEval",
          !d || ae.get(d[v], "globalEval")
        );
    }
    var Ec = /<|&#?\w+;/;
    function Ko(u, d, v, _, m) {
      for (var b, $, M, E, N, B, Y = d.createDocumentFragment(), O = [], Z = 0, de = u.length; Z < de; Z++)
        if (b = u[Z], b || b === 0)
          if (S(b) === "object")
            p.merge(O, b.nodeType ? [b] : b);
          else if (!Ec.test(b))
            O.push(d.createTextNode(b));
          else {
            for ($ = $ || Y.appendChild(d.createElement("div")), M = (qo.exec(b) || ["", ""])[1].toLowerCase(), E = Et[M] || Et._default, $.innerHTML = E[1] + p.htmlPrefilter(b) + E[2], B = E[0]; B--; )
              $ = $.lastChild;
            p.merge(O, $.childNodes), $ = Y.firstChild, $.textContent = "";
          }
      for (Y.textContent = "", Z = 0; b = O[Z++]; ) {
        if (_ && p.inArray(b, _) > -1) {
          m && m.push(b);
          continue;
        }
        if (N = Bn(b), $ = Tt(Y.appendChild(b), "script"), N && Qr($), v)
          for (B = 0; b = $[B++]; )
            Zo.test(b.type || "") && v.push(b);
      }
      return Y;
    }
    var Qo = /^([^.]*)(?:\.(.+)|)/;
    function zn() {
      return !0;
    }
    function Hn() {
      return !1;
    }
    function Jr(u, d, v, _, m, b) {
      var $, M;
      if (typeof d == "object") {
        typeof v != "string" && (_ = _ || v, v = void 0);
        for (M in d)
          Jr(u, M, v, _, d[M], b);
        return u;
      }
      if (_ == null && m == null ? (m = v, _ = v = void 0) : m == null && (typeof v == "string" ? (m = _, _ = void 0) : (m = _, _ = v, v = void 0)), m === !1)
        m = Hn;
      else if (!m)
        return u;
      return b === 1 && ($ = m, m = function(E) {
        return p().off(E), $.apply(this, arguments);
      }, m.guid = $.guid || ($.guid = p.guid++)), u.each(function() {
        p.event.add(this, d, m, _, v);
      });
    }
    p.event = {
      global: {},
      add: function(u, d, v, _, m) {
        var b, $, M, E, N, B, Y, O, Z, de, $e, xe = ae.get(u);
        if (ci(u))
          for (v.handler && (b = v, v = b.handler, m = b.selector), m && p.find.matchesSelector(Tn, m), v.guid || (v.guid = p.guid++), (E = xe.events) || (E = xe.events = /* @__PURE__ */ Object.create(null)), ($ = xe.handle) || ($ = xe.handle = function(ut) {
            return typeof p != "undefined" && p.event.triggered !== ut.type ? p.event.dispatch.apply(u, arguments) : void 0;
          }), d = (d || "").match(ie) || [""], N = d.length; N--; )
            M = Qo.exec(d[N]) || [], Z = $e = M[1], de = (M[2] || "").split(".").sort(), Z && (Y = p.event.special[Z] || {}, Z = (m ? Y.delegateType : Y.bindType) || Z, Y = p.event.special[Z] || {}, B = p.extend({
              type: Z,
              origType: $e,
              data: _,
              handler: v,
              guid: v.guid,
              selector: m,
              needsContext: m && p.expr.match.needsContext.test(m),
              namespace: de.join(".")
            }, b), (O = E[Z]) || (O = E[Z] = [], O.delegateCount = 0, (!Y.setup || Y.setup.call(u, _, de, $) === !1) && u.addEventListener && u.addEventListener(Z, $)), Y.add && (Y.add.call(u, B), B.handler.guid || (B.handler.guid = v.guid)), m ? O.splice(O.delegateCount++, 0, B) : O.push(B), p.event.global[Z] = !0);
      },
      // Detach an event or set of events from an element
      remove: function(u, d, v, _, m) {
        var b, $, M, E, N, B, Y, O, Z, de, $e, xe = ae.hasData(u) && ae.get(u);
        if (!(!xe || !(E = xe.events))) {
          for (d = (d || "").match(ie) || [""], N = d.length; N--; ) {
            if (M = Qo.exec(d[N]) || [], Z = $e = M[1], de = (M[2] || "").split(".").sort(), !Z) {
              for (Z in E)
                p.event.remove(u, Z + d[N], v, _, !0);
              continue;
            }
            for (Y = p.event.special[Z] || {}, Z = (_ ? Y.delegateType : Y.bindType) || Z, O = E[Z] || [], M = M[2] && new RegExp("(^|\\.)" + de.join("\\.(?:.*\\.|)") + "(\\.|$)"), $ = b = O.length; b--; )
              B = O[b], (m || $e === B.origType) && (!v || v.guid === B.guid) && (!M || M.test(B.namespace)) && (!_ || _ === B.selector || _ === "**" && B.selector) && (O.splice(b, 1), B.selector && O.delegateCount--, Y.remove && Y.remove.call(u, B));
            $ && !O.length && ((!Y.teardown || Y.teardown.call(u, de, xe.handle) === !1) && p.removeEvent(u, Z, xe.handle), delete E[Z]);
          }
          p.isEmptyObject(E) && ae.remove(u, "handle events");
        }
      },
      dispatch: function(u) {
        var d, v, _, m, b, $, M = new Array(arguments.length), E = p.event.fix(u), N = (ae.get(this, "events") || /* @__PURE__ */ Object.create(null))[E.type] || [], B = p.event.special[E.type] || {};
        for (M[0] = E, d = 1; d < arguments.length; d++)
          M[d] = arguments[d];
        if (E.delegateTarget = this, !(B.preDispatch && B.preDispatch.call(this, E) === !1)) {
          for ($ = p.event.handlers.call(this, E, N), d = 0; (m = $[d++]) && !E.isPropagationStopped(); )
            for (E.currentTarget = m.elem, v = 0; (b = m.handlers[v++]) && !E.isImmediatePropagationStopped(); )
              (!E.rnamespace || b.namespace === !1 || E.rnamespace.test(b.namespace)) && (E.handleObj = b, E.data = b.data, _ = ((p.event.special[b.origType] || {}).handle || b.handler).apply(m.elem, M), _ !== void 0 && (E.result = _) === !1 && (E.preventDefault(), E.stopPropagation()));
          return B.postDispatch && B.postDispatch.call(this, E), E.result;
        }
      },
      handlers: function(u, d) {
        var v, _, m, b, $, M = [], E = d.delegateCount, N = u.target;
        if (E && // Support: IE <=9
        // Black-hole SVG <use> instance trees (trac-13180)
        N.nodeType && // Support: Firefox <=42
        // Suppress spec-violating clicks indicating a non-primary pointer button (trac-3861)
        // https://www.w3.org/TR/DOM-Level-3-Events/#event-type-click
        // Support: IE 11 only
        // ...but not arrow key "clicks" of radio inputs, which can have `button` -1 (gh-2343)
        !(u.type === "click" && u.button >= 1)) {
          for (; N !== this; N = N.parentNode || this)
            if (N.nodeType === 1 && !(u.type === "click" && N.disabled === !0)) {
              for (b = [], $ = {}, v = 0; v < E; v++)
                _ = d[v], m = _.selector + " ", $[m] === void 0 && ($[m] = _.needsContext ? p(m, this).index(N) > -1 : p.find(m, this, null, [N]).length), $[m] && b.push(_);
              b.length && M.push({ elem: N, handlers: b });
            }
        }
        return N = this, E < d.length && M.push({ elem: N, handlers: d.slice(E) }), M;
      },
      addProp: function(u, d) {
        Object.defineProperty(p.Event.prototype, u, {
          enumerable: !0,
          configurable: !0,
          get: T(d) ? function() {
            if (this.originalEvent)
              return d(this.originalEvent);
          } : function() {
            if (this.originalEvent)
              return this.originalEvent[u];
          },
          set: function(v) {
            Object.defineProperty(this, u, {
              enumerable: !0,
              configurable: !0,
              writable: !0,
              value: v
            });
          }
        });
      },
      fix: function(u) {
        return u[p.expando] ? u : new p.Event(u);
      },
      special: {
        load: {
          // Prevent triggered image.load events from bubbling to window.load
          noBubble: !0
        },
        click: {
          // Utilize native event to ensure correct state for checkable inputs
          setup: function(u) {
            var d = this || u;
            return hi.test(d.type) && d.click && H(d, "input") && Yi(d, "click", !0), !1;
          },
          trigger: function(u) {
            var d = this || u;
            return hi.test(d.type) && d.click && H(d, "input") && Yi(d, "click"), !0;
          },
          // For cross-browser consistency, suppress native .click() on links
          // Also prevent it if we're currently inside a leveraged native-event stack
          _default: function(u) {
            var d = u.target;
            return hi.test(d.type) && d.click && H(d, "input") && ae.get(d, "click") || H(d, "a");
          }
        },
        beforeunload: {
          postDispatch: function(u) {
            u.result !== void 0 && u.originalEvent && (u.originalEvent.returnValue = u.result);
          }
        }
      }
    };
    function Yi(u, d, v) {
      if (!v) {
        ae.get(u, d) === void 0 && p.event.add(u, d, zn);
        return;
      }
      ae.set(u, d, !1), p.event.add(u, d, {
        namespace: !1,
        handler: function(_) {
          var m, b = ae.get(this, d);
          if (_.isTrigger & 1 && this[d]) {
            if (b)
              (p.event.special[d] || {}).delegateType && _.stopPropagation();
            else if (b = a.call(arguments), ae.set(this, d, b), this[d](), m = ae.get(this, d), ae.set(this, d, !1), b !== m)
              return _.stopImmediatePropagation(), _.preventDefault(), m;
          } else
            b && (ae.set(this, d, p.event.trigger(
              b[0],
              b.slice(1),
              this
            )), _.stopPropagation(), _.isImmediatePropagationStopped = zn);
        }
      });
    }
    p.removeEvent = function(u, d, v) {
      u.removeEventListener && u.removeEventListener(d, v);
    }, p.Event = function(u, d) {
      if (!(this instanceof p.Event))
        return new p.Event(u, d);
      u && u.type ? (this.originalEvent = u, this.type = u.type, this.isDefaultPrevented = u.defaultPrevented || u.defaultPrevented === void 0 && // Support: Android <=2.3 only
      u.returnValue === !1 ? zn : Hn, this.target = u.target && u.target.nodeType === 3 ? u.target.parentNode : u.target, this.currentTarget = u.currentTarget, this.relatedTarget = u.relatedTarget) : this.type = u, d && p.extend(this, d), this.timeStamp = u && u.timeStamp || Date.now(), this[p.expando] = !0;
    }, p.Event.prototype = {
      constructor: p.Event,
      isDefaultPrevented: Hn,
      isPropagationStopped: Hn,
      isImmediatePropagationStopped: Hn,
      isSimulated: !1,
      preventDefault: function() {
        var u = this.originalEvent;
        this.isDefaultPrevented = zn, u && !this.isSimulated && u.preventDefault();
      },
      stopPropagation: function() {
        var u = this.originalEvent;
        this.isPropagationStopped = zn, u && !this.isSimulated && u.stopPropagation();
      },
      stopImmediatePropagation: function() {
        var u = this.originalEvent;
        this.isImmediatePropagationStopped = zn, u && !this.isSimulated && u.stopImmediatePropagation(), this.stopPropagation();
      }
    }, p.each({
      altKey: !0,
      bubbles: !0,
      cancelable: !0,
      changedTouches: !0,
      ctrlKey: !0,
      detail: !0,
      eventPhase: !0,
      metaKey: !0,
      pageX: !0,
      pageY: !0,
      shiftKey: !0,
      view: !0,
      char: !0,
      code: !0,
      charCode: !0,
      key: !0,
      keyCode: !0,
      button: !0,
      buttons: !0,
      clientX: !0,
      clientY: !0,
      offsetX: !0,
      offsetY: !0,
      pointerId: !0,
      pointerType: !0,
      screenX: !0,
      screenY: !0,
      targetTouches: !0,
      toElement: !0,
      touches: !0,
      which: !0
    }, p.event.addProp), p.each({ focus: "focusin", blur: "focusout" }, function(u, d) {
      function v(_) {
        if (w.documentMode) {
          var m = ae.get(this, "handle"), b = p.event.fix(_);
          b.type = _.type === "focusin" ? "focus" : "blur", b.isSimulated = !0, m(_), b.target === b.currentTarget && m(b);
        } else
          p.event.simulate(
            d,
            _.target,
            p.event.fix(_)
          );
      }
      p.event.special[u] = {
        // Utilize native event if possible so blur/focus sequence is correct
        setup: function() {
          var _;
          if (Yi(this, u, !0), w.documentMode)
            _ = ae.get(this, d), _ || this.addEventListener(d, v), ae.set(this, d, (_ || 0) + 1);
          else
            return !1;
        },
        trigger: function() {
          return Yi(this, u), !0;
        },
        teardown: function() {
          var _;
          if (w.documentMode)
            _ = ae.get(this, d) - 1, _ ? ae.set(this, d, _) : (this.removeEventListener(d, v), ae.remove(this, d));
          else
            return !1;
        },
        // Suppress native focus or blur if we're currently inside
        // a leveraged native-event stack
        _default: function(_) {
          return ae.get(_.target, u);
        },
        delegateType: d
      }, p.event.special[d] = {
        setup: function() {
          var _ = this.ownerDocument || this.document || this, m = w.documentMode ? this : _, b = ae.get(m, d);
          b || (w.documentMode ? this.addEventListener(d, v) : _.addEventListener(u, v, !0)), ae.set(m, d, (b || 0) + 1);
        },
        teardown: function() {
          var _ = this.ownerDocument || this.document || this, m = w.documentMode ? this : _, b = ae.get(m, d) - 1;
          b ? ae.set(m, d, b) : (w.documentMode ? this.removeEventListener(d, v) : _.removeEventListener(u, v, !0), ae.remove(m, d));
        }
      };
    }), p.each({
      mouseenter: "mouseover",
      mouseleave: "mouseout",
      pointerenter: "pointerover",
      pointerleave: "pointerout"
    }, function(u, d) {
      p.event.special[u] = {
        delegateType: d,
        bindType: d,
        handle: function(v) {
          var _, m = this, b = v.relatedTarget, $ = v.handleObj;
          return (!b || b !== m && !p.contains(m, b)) && (v.type = $.origType, _ = $.handler.apply(this, arguments), v.type = d), _;
        }
      };
    }), p.fn.extend({
      on: function(u, d, v, _) {
        return Jr(this, u, d, v, _);
      },
      one: function(u, d, v, _) {
        return Jr(this, u, d, v, _, 1);
      },
      off: function(u, d, v) {
        var _, m;
        if (u && u.preventDefault && u.handleObj)
          return _ = u.handleObj, p(u.delegateTarget).off(
            _.namespace ? _.origType + "." + _.namespace : _.origType,
            _.selector,
            _.handler
          ), this;
        if (typeof u == "object") {
          for (m in u)
            this.off(m, d, u[m]);
          return this;
        }
        return (d === !1 || typeof d == "function") && (v = d, d = void 0), v === !1 && (v = Hn), this.each(function() {
          p.event.remove(this, u, v, d);
        });
      }
    });
    var Rc = /<script|<style|<link/i, Mc = /checked\s*(?:[^=]|=\s*.checked.)/i, Lc = /^\s*<!\[CDATA\[|\]\]>\s*$/g;
    function Jo(u, d) {
      return H(u, "table") && H(d.nodeType !== 11 ? d : d.firstChild, "tr") && p(u).children("tbody")[0] || u;
    }
    function Dc(u) {
      return u.type = (u.getAttribute("type") !== null) + "/" + u.type, u;
    }
    function Ic(u) {
      return (u.type || "").slice(0, 5) === "true/" ? u.type = u.type.slice(5) : u.removeAttribute("type"), u;
    }
    function es(u, d) {
      var v, _, m, b, $, M, E;
      if (d.nodeType === 1) {
        if (ae.hasData(u) && (b = ae.get(u), E = b.events, E)) {
          ae.remove(d, "handle events");
          for (m in E)
            for (v = 0, _ = E[m].length; v < _; v++)
              p.event.add(d, m, E[m][v]);
        }
        bt.hasData(u) && ($ = bt.access(u), M = p.extend({}, $), bt.set(d, M));
      }
    }
    function Nc(u, d) {
      var v = d.nodeName.toLowerCase();
      v === "input" && hi.test(u.type) ? d.checked = u.checked : (v === "input" || v === "textarea") && (d.defaultValue = u.defaultValue);
    }
    function Vn(u, d, v, _) {
      d = o(d);
      var m, b, $, M, E, N, B = 0, Y = u.length, O = Y - 1, Z = d[0], de = T(Z);
      if (de || Y > 1 && typeof Z == "string" && !y.checkClone && Mc.test(Z))
        return u.each(function($e) {
          var xe = u.eq($e);
          de && (d[0] = Z.call(this, $e, xe.html())), Vn(xe, d, v, _);
        });
      if (Y && (m = Ko(d, u[0].ownerDocument, !1, u, _), b = m.firstChild, m.childNodes.length === 1 && (m = b), b || _)) {
        for ($ = p.map(Tt(m, "script"), Dc), M = $.length; B < Y; B++)
          E = m, B !== O && (E = p.clone(E, !0, !0), M && p.merge($, Tt(E, "script"))), v.call(u[B], E, B);
        if (M)
          for (N = $[$.length - 1].ownerDocument, p.map($, Ic), B = 0; B < M; B++)
            E = $[B], Zo.test(E.type || "") && !ae.access(E, "globalEval") && p.contains(N, E) && (E.src && (E.type || "").toLowerCase() !== "module" ? p._evalUrl && !E.noModule && p._evalUrl(E.src, {
              nonce: E.nonce || E.getAttribute("nonce")
            }, N) : R(E.textContent.replace(Lc, ""), E, N));
      }
      return u;
    }
    function ts(u, d, v) {
      for (var _, m = d ? p.filter(d, u) : u, b = 0; (_ = m[b]) != null; b++)
        !v && _.nodeType === 1 && p.cleanData(Tt(_)), _.parentNode && (v && Bn(_) && Qr(Tt(_, "script")), _.parentNode.removeChild(_));
      return u;
    }
    p.extend({
      htmlPrefilter: function(u) {
        return u;
      },
      clone: function(u, d, v) {
        var _, m, b, $, M = u.cloneNode(!0), E = Bn(u);
        if (!y.noCloneChecked && (u.nodeType === 1 || u.nodeType === 11) && !p.isXMLDoc(u))
          for ($ = Tt(M), b = Tt(u), _ = 0, m = b.length; _ < m; _++)
            Nc(b[_], $[_]);
        if (d)
          if (v)
            for (b = b || Tt(u), $ = $ || Tt(M), _ = 0, m = b.length; _ < m; _++)
              es(b[_], $[_]);
          else
            es(u, M);
        return $ = Tt(M, "script"), $.length > 0 && Qr($, !E && Tt(u, "script")), M;
      },
      cleanData: function(u) {
        for (var d, v, _, m = p.event.special, b = 0; (v = u[b]) !== void 0; b++)
          if (ci(v)) {
            if (d = v[ae.expando]) {
              if (d.events)
                for (_ in d.events)
                  m[_] ? p.event.remove(v, _) : p.removeEvent(v, _, d.handle);
              v[ae.expando] = void 0;
            }
            v[bt.expando] && (v[bt.expando] = void 0);
          }
      }
    }), p.fn.extend({
      detach: function(u) {
        return ts(this, u, !0);
      },
      remove: function(u) {
        return ts(this, u);
      },
      text: function(u) {
        return mt(this, function(d) {
          return d === void 0 ? p.text(this) : this.empty().each(function() {
            (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) && (this.textContent = d);
          });
        }, null, u, arguments.length);
      },
      append: function() {
        return Vn(this, arguments, function(u) {
          if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
            var d = Jo(this, u);
            d.appendChild(u);
          }
        });
      },
      prepend: function() {
        return Vn(this, arguments, function(u) {
          if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
            var d = Jo(this, u);
            d.insertBefore(u, d.firstChild);
          }
        });
      },
      before: function() {
        return Vn(this, arguments, function(u) {
          this.parentNode && this.parentNode.insertBefore(u, this);
        });
      },
      after: function() {
        return Vn(this, arguments, function(u) {
          this.parentNode && this.parentNode.insertBefore(u, this.nextSibling);
        });
      },
      empty: function() {
        for (var u, d = 0; (u = this[d]) != null; d++)
          u.nodeType === 1 && (p.cleanData(Tt(u, !1)), u.textContent = "");
        return this;
      },
      clone: function(u, d) {
        return u = u == null ? !1 : u, d = d == null ? u : d, this.map(function() {
          return p.clone(this, u, d);
        });
      },
      html: function(u) {
        return mt(this, function(d) {
          var v = this[0] || {}, _ = 0, m = this.length;
          if (d === void 0 && v.nodeType === 1)
            return v.innerHTML;
          if (typeof d == "string" && !Rc.test(d) && !Et[(qo.exec(d) || ["", ""])[1].toLowerCase()]) {
            d = p.htmlPrefilter(d);
            try {
              for (; _ < m; _++)
                v = this[_] || {}, v.nodeType === 1 && (p.cleanData(Tt(v, !1)), v.innerHTML = d);
              v = 0;
            } catch (b) {
            }
          }
          v && this.empty().append(d);
        }, null, u, arguments.length);
      },
      replaceWith: function() {
        var u = [];
        return Vn(this, arguments, function(d) {
          var v = this.parentNode;
          p.inArray(this, u) < 0 && (p.cleanData(Tt(this)), v && v.replaceChild(d, this));
        }, u);
      }
    }), p.each({
      appendTo: "append",
      prependTo: "prepend",
      insertBefore: "before",
      insertAfter: "after",
      replaceAll: "replaceWith"
    }, function(u, d) {
      p.fn[u] = function(v) {
        for (var _, m = [], b = p(v), $ = b.length - 1, M = 0; M <= $; M++)
          _ = M === $ ? this : this.clone(!0), p(b[M])[d](_), s.apply(m, _.get());
        return this.pushStack(m);
      };
    });
    var ea = new RegExp("^(" + jo + ")(?!px)[a-z%]+$", "i"), ta = /^--/, Ui = function(u) {
      var d = u.ownerDocument.defaultView;
      return (!d || !d.opener) && (d = t), d.getComputedStyle(u);
    }, ns = function(u, d, v) {
      var _, m, b = {};
      for (m in d)
        b[m] = u.style[m], u.style[m] = d[m];
      _ = v.call(u);
      for (m in d)
        u.style[m] = b[m];
      return _;
    }, Pc = new RegExp(nn.join("|"), "i");
    (function() {
      function u() {
        if (N) {
          E.style.cssText = "position:absolute;left:-11111px;width:60px;margin-top:1px;padding:0;border:0", N.style.cssText = "position:relative;display:block;box-sizing:border-box;overflow:scroll;margin:auto;border:1px;padding:1px;width:60%;top:1%", Tn.appendChild(E).appendChild(N);
          var B = t.getComputedStyle(N);
          v = B.top !== "1%", M = d(B.marginLeft) === 12, N.style.right = "60%", b = d(B.right) === 36, _ = d(B.width) === 36, N.style.position = "absolute", m = d(N.offsetWidth / 3) === 12, Tn.removeChild(E), N = null;
        }
      }
      function d(B) {
        return Math.round(parseFloat(B));
      }
      var v, _, m, b, $, M, E = w.createElement("div"), N = w.createElement("div");
      N.style && (N.style.backgroundClip = "content-box", N.cloneNode(!0).style.backgroundClip = "", y.clearCloneStyle = N.style.backgroundClip === "content-box", p.extend(y, {
        boxSizingReliable: function() {
          return u(), _;
        },
        pixelBoxStyles: function() {
          return u(), b;
        },
        pixelPosition: function() {
          return u(), v;
        },
        reliableMarginLeft: function() {
          return u(), M;
        },
        scrollboxSize: function() {
          return u(), m;
        },
        // Support: IE 9 - 11+, Edge 15 - 18+
        // IE/Edge misreport `getComputedStyle` of table rows with width/height
        // set in CSS while `offset*` properties report correct values.
        // Behavior in IE 9 is more subtle than in newer versions & it passes
        // some versions of this test; make sure not to make it pass there!
        //
        // Support: Firefox 70+
        // Only Firefox includes border widths
        // in computed dimensions. (gh-4529)
        reliableTrDimensions: function() {
          var B, Y, O, Z;
          return $ == null && (B = w.createElement("table"), Y = w.createElement("tr"), O = w.createElement("div"), B.style.cssText = "position:absolute;left:-11111px;border-collapse:separate", Y.style.cssText = "box-sizing:content-box;border:1px solid", Y.style.height = "1px", O.style.height = "9px", O.style.display = "block", Tn.appendChild(B).appendChild(Y).appendChild(O), Z = t.getComputedStyle(Y), $ = parseInt(Z.height, 10) + parseInt(Z.borderTopWidth, 10) + parseInt(Z.borderBottomWidth, 10) === Y.offsetHeight, Tn.removeChild(B)), $;
        }
      }));
    })();
    function gi(u, d, v) {
      var _, m, b, $, M = ta.test(d), E = u.style;
      return v = v || Ui(u), v && ($ = v.getPropertyValue(d) || v[d], M && $ && ($ = $.replace(G, "$1") || void 0), $ === "" && !Bn(u) && ($ = p.style(u, d)), !y.pixelBoxStyles() && ea.test($) && Pc.test(d) && (_ = E.width, m = E.minWidth, b = E.maxWidth, E.minWidth = E.maxWidth = E.width = $, $ = v.width, E.width = _, E.minWidth = m, E.maxWidth = b)), $ !== void 0 ? (
        // Support: IE <=9 - 11 only
        // IE returns zIndex value as an integer.
        $ + ""
      ) : $;
    }
    function is(u, d) {
      return {
        get: function() {
          if (u()) {
            delete this.get;
            return;
          }
          return (this.get = d).apply(this, arguments);
        }
      };
    }
    var rs = ["Webkit", "Moz", "ms"], as = w.createElement("div").style, os = {};
    function Oc(u) {
      for (var d = u[0].toUpperCase() + u.slice(1), v = rs.length; v--; )
        if (u = rs[v] + d, u in as)
          return u;
    }
    function na(u) {
      var d = p.cssProps[u] || os[u];
      return d || (u in as ? u : os[u] = Oc(u) || u);
    }
    var Fc = /^(none|table(?!-c[ea]).+)/, Bc = { position: "absolute", visibility: "hidden", display: "block" }, ss = {
      letterSpacing: "0",
      fontWeight: "400"
    };
    function us(u, d, v) {
      var _ = di.exec(d);
      return _ ? (
        // Guard against undefined "subtract", e.g., when used as in cssHooks
        Math.max(0, _[2] - (v || 0)) + (_[3] || "px")
      ) : d;
    }
    function ia(u, d, v, _, m, b) {
      var $ = d === "width" ? 1 : 0, M = 0, E = 0, N = 0;
      if (v === (_ ? "border" : "content"))
        return 0;
      for (; $ < 4; $ += 2)
        v === "margin" && (N += p.css(u, v + nn[$], !0, m)), _ ? (v === "content" && (E -= p.css(u, "padding" + nn[$], !0, m)), v !== "margin" && (E -= p.css(u, "border" + nn[$] + "Width", !0, m))) : (E += p.css(u, "padding" + nn[$], !0, m), v !== "padding" ? E += p.css(u, "border" + nn[$] + "Width", !0, m) : M += p.css(u, "border" + nn[$] + "Width", !0, m));
      return !_ && b >= 0 && (E += Math.max(0, Math.ceil(
        u["offset" + d[0].toUpperCase() + d.slice(1)] - b - E - M - 0.5
        // If offsetWidth/offsetHeight is unknown, then we can't determine content-box scroll gutter
        // Use an explicit zero to avoid NaN (gh-3964)
      )) || 0), E + N;
    }
    function ls(u, d, v) {
      var _ = Ui(u), m = !y.boxSizingReliable() || v, b = m && p.css(u, "boxSizing", !1, _) === "border-box", $ = b, M = gi(u, d, _), E = "offset" + d[0].toUpperCase() + d.slice(1);
      if (ea.test(M)) {
        if (!v)
          return M;
        M = "auto";
      }
      return (!y.boxSizingReliable() && b || // Support: IE 10 - 11+, Edge 15 - 18+
      // IE/Edge misreport `getComputedStyle` of table rows with width/height
      // set in CSS while `offset*` properties report correct values.
      // Interestingly, in some cases IE 9 doesn't suffer from this issue.
      !y.reliableTrDimensions() && H(u, "tr") || // Fall back to offsetWidth/offsetHeight when value is "auto"
      // This happens for inline elements with no explicit setting (gh-3571)
      M === "auto" || // Support: Android <=4.1 - 4.3 only
      // Also use offsetWidth/offsetHeight for misreported inline dimensions (gh-3602)
      !parseFloat(M) && p.css(u, "display", !1, _) === "inline") && // Make sure the element is visible & connected
      u.getClientRects().length && (b = p.css(u, "boxSizing", !1, _) === "border-box", $ = E in u, $ && (M = u[E])), M = parseFloat(M) || 0, M + ia(
        u,
        d,
        v || (b ? "border" : "content"),
        $,
        _,
        // Provide the current computed size to request scroll gutter calculation (gh-3589)
        M
      ) + "px";
    }
    p.extend({
      // Add in style property hooks for overriding the default
      // behavior of getting and setting a style property
      cssHooks: {
        opacity: {
          get: function(u, d) {
            if (d) {
              var v = gi(u, "opacity");
              return v === "" ? "1" : v;
            }
          }
        }
      },
      // Don't automatically add "px" to these possibly-unitless properties
      cssNumber: {
        animationIterationCount: !0,
        aspectRatio: !0,
        borderImageSlice: !0,
        columnCount: !0,
        flexGrow: !0,
        flexShrink: !0,
        fontWeight: !0,
        gridArea: !0,
        gridColumn: !0,
        gridColumnEnd: !0,
        gridColumnStart: !0,
        gridRow: !0,
        gridRowEnd: !0,
        gridRowStart: !0,
        lineHeight: !0,
        opacity: !0,
        order: !0,
        orphans: !0,
        scale: !0,
        widows: !0,
        zIndex: !0,
        zoom: !0,
        // SVG-related
        fillOpacity: !0,
        floodOpacity: !0,
        stopOpacity: !0,
        strokeMiterlimit: !0,
        strokeOpacity: !0
      },
      // Add in properties whose names you wish to fix before
      // setting or getting the value
      cssProps: {},
      // Get and set the style property on a DOM Node
      style: function(u, d, v, _) {
        if (!(!u || u.nodeType === 3 || u.nodeType === 8 || !u.style)) {
          var m, b, $, M = Ht(d), E = ta.test(d), N = u.style;
          if (E || (d = na(M)), $ = p.cssHooks[d] || p.cssHooks[M], v !== void 0) {
            if (b = typeof v, b === "string" && (m = di.exec(v)) && m[1] && (v = Go(u, d, m), b = "number"), v == null || v !== v)
              return;
            b === "number" && !E && (v += m && m[3] || (p.cssNumber[M] ? "" : "px")), !y.clearCloneStyle && v === "" && d.indexOf("background") === 0 && (N[d] = "inherit"), (!$ || !("set" in $) || (v = $.set(u, v, _)) !== void 0) && (E ? N.setProperty(d, v) : N[d] = v);
          } else
            return $ && "get" in $ && (m = $.get(u, !1, _)) !== void 0 ? m : N[d];
        }
      },
      css: function(u, d, v, _) {
        var m, b, $, M = Ht(d), E = ta.test(d);
        return E || (d = na(M)), $ = p.cssHooks[d] || p.cssHooks[M], $ && "get" in $ && (m = $.get(u, !0, v)), m === void 0 && (m = gi(u, d, _)), m === "normal" && d in ss && (m = ss[d]), v === "" || v ? (b = parseFloat(m), v === !0 || isFinite(b) ? b || 0 : m) : m;
      }
    }), p.each(["height", "width"], function(u, d) {
      p.cssHooks[d] = {
        get: function(v, _, m) {
          if (_)
            return Fc.test(p.css(v, "display")) && // Support: Safari 8+
            // Table columns in Safari have non-zero offsetWidth & zero
            // getBoundingClientRect().width unless display is changed.
            // Support: IE <=11 only
            // Running getBoundingClientRect on a disconnected node
            // in IE throws an error.
            (!v.getClientRects().length || !v.getBoundingClientRect().width) ? ns(v, Bc, function() {
              return ls(v, d, m);
            }) : ls(v, d, m);
        },
        set: function(v, _, m) {
          var b, $ = Ui(v), M = !y.scrollboxSize() && $.position === "absolute", E = M || m, N = E && p.css(v, "boxSizing", !1, $) === "border-box", B = m ? ia(
            v,
            d,
            m,
            N,
            $
          ) : 0;
          return N && M && (B -= Math.ceil(
            v["offset" + d[0].toUpperCase() + d.slice(1)] - parseFloat($[d]) - ia(v, d, "border", !1, $) - 0.5
          )), B && (b = di.exec(_)) && (b[3] || "px") !== "px" && (v.style[d] = _, _ = p.css(v, d)), us(v, _, B);
        }
      };
    }), p.cssHooks.marginLeft = is(
      y.reliableMarginLeft,
      function(u, d) {
        if (d)
          return (parseFloat(gi(u, "marginLeft")) || u.getBoundingClientRect().left - ns(u, { marginLeft: 0 }, function() {
            return u.getBoundingClientRect().left;
          })) + "px";
      }
    ), p.each({
      margin: "",
      padding: "",
      border: "Width"
    }, function(u, d) {
      p.cssHooks[u + d] = {
        expand: function(v) {
          for (var _ = 0, m = {}, b = typeof v == "string" ? v.split(" ") : [v]; _ < 4; _++)
            m[u + nn[_] + d] = b[_] || b[_ - 2] || b[0];
          return m;
        }
      }, u !== "margin" && (p.cssHooks[u + d].set = us);
    }), p.fn.extend({
      css: function(u, d) {
        return mt(this, function(v, _, m) {
          var b, $, M = {}, E = 0;
          if (Array.isArray(_)) {
            for (b = Ui(v), $ = _.length; E < $; E++)
              M[_[E]] = p.css(v, _[E], !1, b);
            return M;
          }
          return m !== void 0 ? p.style(v, _, m) : p.css(v, _);
        }, u, d, arguments.length > 1);
      }
    });
    function wt(u, d, v, _, m) {
      return new wt.prototype.init(u, d, v, _, m);
    }
    p.Tween = wt, wt.prototype = {
      constructor: wt,
      init: function(u, d, v, _, m, b) {
        this.elem = u, this.prop = v, this.easing = m || p.easing._default, this.options = d, this.start = this.now = this.cur(), this.end = _, this.unit = b || (p.cssNumber[v] ? "" : "px");
      },
      cur: function() {
        var u = wt.propHooks[this.prop];
        return u && u.get ? u.get(this) : wt.propHooks._default.get(this);
      },
      run: function(u) {
        var d, v = wt.propHooks[this.prop];
        return this.options.duration ? this.pos = d = p.easing[this.easing](
          u,
          this.options.duration * u,
          0,
          1,
          this.options.duration
        ) : this.pos = d = u, this.now = (this.end - this.start) * d + this.start, this.options.step && this.options.step.call(this.elem, this.now, this), v && v.set ? v.set(this) : wt.propHooks._default.set(this), this;
      }
    }, wt.prototype.init.prototype = wt.prototype, wt.propHooks = {
      _default: {
        get: function(u) {
          var d;
          return u.elem.nodeType !== 1 || u.elem[u.prop] != null && u.elem.style[u.prop] == null ? u.elem[u.prop] : (d = p.css(u.elem, u.prop, ""), !d || d === "auto" ? 0 : d);
        },
        set: function(u) {
          p.fx.step[u.prop] ? p.fx.step[u.prop](u) : u.elem.nodeType === 1 && (p.cssHooks[u.prop] || u.elem.style[na(u.prop)] != null) ? p.style(u.elem, u.prop, u.now + u.unit) : u.elem[u.prop] = u.now;
        }
      }
    }, wt.propHooks.scrollTop = wt.propHooks.scrollLeft = {
      set: function(u) {
        u.elem.nodeType && u.elem.parentNode && (u.elem[u.prop] = u.now);
      }
    }, p.easing = {
      linear: function(u) {
        return u;
      },
      swing: function(u) {
        return 0.5 - Math.cos(u * Math.PI) / 2;
      },
      _default: "swing"
    }, p.fx = wt.prototype.init, p.fx.step = {};
    var Yn, ji, Xc = /^(?:toggle|show|hide)$/, zc = /queueHooks$/;
    function ra() {
      ji && (w.hidden === !1 && t.requestAnimationFrame ? t.requestAnimationFrame(ra) : t.setTimeout(ra, p.fx.interval), p.fx.tick());
    }
    function cs() {
      return t.setTimeout(function() {
        Yn = void 0;
      }), Yn = Date.now();
    }
    function Gi(u, d) {
      var v, _ = 0, m = { height: u };
      for (d = d ? 1 : 0; _ < 4; _ += 2 - d)
        v = nn[_], m["margin" + v] = m["padding" + v] = u;
      return d && (m.opacity = m.width = u), m;
    }
    function fs(u, d, v) {
      for (var _, m = (It.tweeners[d] || []).concat(It.tweeners["*"]), b = 0, $ = m.length; b < $; b++)
        if (_ = m[b].call(v, d, u))
          return _;
    }
    function Hc(u, d, v) {
      var _, m, b, $, M, E, N, B, Y = "width" in d || "height" in d, O = this, Z = {}, de = u.style, $e = u.nodeType && Vi(u), xe = ae.get(u, "fxshow");
      v.queue || ($ = p._queueHooks(u, "fx"), $.unqueued == null && ($.unqueued = 0, M = $.empty.fire, $.empty.fire = function() {
        $.unqueued || M();
      }), $.unqueued++, O.always(function() {
        O.always(function() {
          $.unqueued--, p.queue(u, "fx").length || $.empty.fire();
        });
      }));
      for (_ in d)
        if (m = d[_], Xc.test(m)) {
          if (delete d[_], b = b || m === "toggle", m === ($e ? "hide" : "show"))
            if (m === "show" && xe && xe[_] !== void 0)
              $e = !0;
            else
              continue;
          Z[_] = xe && xe[_] || p.style(u, _);
        }
      if (E = !p.isEmptyObject(d), !(!E && p.isEmptyObject(Z))) {
        Y && u.nodeType === 1 && (v.overflow = [de.overflow, de.overflowX, de.overflowY], N = xe && xe.display, N == null && (N = ae.get(u, "display")), B = p.css(u, "display"), B === "none" && (N ? B = N : (Xn([u], !0), N = u.style.display || N, B = p.css(u, "display"), Xn([u]))), (B === "inline" || B === "inline-block" && N != null) && p.css(u, "float") === "none" && (E || (O.done(function() {
          de.display = N;
        }), N == null && (B = de.display, N = B === "none" ? "" : B)), de.display = "inline-block")), v.overflow && (de.overflow = "hidden", O.always(function() {
          de.overflow = v.overflow[0], de.overflowX = v.overflow[1], de.overflowY = v.overflow[2];
        })), E = !1;
        for (_ in Z)
          E || (xe ? "hidden" in xe && ($e = xe.hidden) : xe = ae.access(u, "fxshow", { display: N }), b && (xe.hidden = !$e), $e && Xn([u], !0), O.done(function() {
            $e || Xn([u]), ae.remove(u, "fxshow");
            for (_ in Z)
              p.style(u, _, Z[_]);
          })), E = fs($e ? xe[_] : 0, _, O), _ in xe || (xe[_] = E.start, $e && (E.end = E.start, E.start = 0));
      }
    }
    function Vc(u, d) {
      var v, _, m, b, $;
      for (v in u)
        if (_ = Ht(v), m = d[_], b = u[v], Array.isArray(b) && (m = b[1], b = u[v] = b[0]), v !== _ && (u[_] = b, delete u[v]), $ = p.cssHooks[_], $ && "expand" in $) {
          b = $.expand(b), delete u[_];
          for (v in b)
            v in u || (u[v] = b[v], d[v] = m);
        } else
          d[_] = m;
    }
    function It(u, d, v) {
      var _, m, b = 0, $ = It.prefilters.length, M = p.Deferred().always(function() {
        delete E.elem;
      }), E = function() {
        if (m)
          return !1;
        for (var Y = Yn || cs(), O = Math.max(0, N.startTime + N.duration - Y), Z = O / N.duration || 0, de = 1 - Z, $e = 0, xe = N.tweens.length; $e < xe; $e++)
          N.tweens[$e].run(de);
        return M.notifyWith(u, [N, de, O]), de < 1 && xe ? O : (xe || M.notifyWith(u, [N, 1, 0]), M.resolveWith(u, [N]), !1);
      }, N = M.promise({
        elem: u,
        props: p.extend({}, d),
        opts: p.extend(!0, {
          specialEasing: {},
          easing: p.easing._default
        }, v),
        originalProperties: d,
        originalOptions: v,
        startTime: Yn || cs(),
        duration: v.duration,
        tweens: [],
        createTween: function(Y, O) {
          var Z = p.Tween(
            u,
            N.opts,
            Y,
            O,
            N.opts.specialEasing[Y] || N.opts.easing
          );
          return N.tweens.push(Z), Z;
        },
        stop: function(Y) {
          var O = 0, Z = Y ? N.tweens.length : 0;
          if (m)
            return this;
          for (m = !0; O < Z; O++)
            N.tweens[O].run(1);
          return Y ? (M.notifyWith(u, [N, 1, 0]), M.resolveWith(u, [N, Y])) : M.rejectWith(u, [N, Y]), this;
        }
      }), B = N.props;
      for (Vc(B, N.opts.specialEasing); b < $; b++)
        if (_ = It.prefilters[b].call(N, u, B, N.opts), _)
          return T(_.stop) && (p._queueHooks(N.elem, N.opts.queue).stop = _.stop.bind(_)), _;
      return p.map(B, fs, N), T(N.opts.start) && N.opts.start.call(u, N), N.progress(N.opts.progress).done(N.opts.done, N.opts.complete).fail(N.opts.fail).always(N.opts.always), p.fx.timer(
        p.extend(E, {
          elem: u,
          anim: N,
          queue: N.opts.queue
        })
      ), N;
    }
    p.Animation = p.extend(It, {
      tweeners: {
        "*": [function(u, d) {
          var v = this.createTween(u, d);
          return Go(v.elem, u, di.exec(d), v), v;
        }]
      },
      tweener: function(u, d) {
        T(u) ? (d = u, u = ["*"]) : u = u.match(ie);
        for (var v, _ = 0, m = u.length; _ < m; _++)
          v = u[_], It.tweeners[v] = It.tweeners[v] || [], It.tweeners[v].unshift(d);
      },
      prefilters: [Hc],
      prefilter: function(u, d) {
        d ? It.prefilters.unshift(u) : It.prefilters.push(u);
      }
    }), p.speed = function(u, d, v) {
      var _ = u && typeof u == "object" ? p.extend({}, u) : {
        complete: v || !v && d || T(u) && u,
        duration: u,
        easing: v && d || d && !T(d) && d
      };
      return p.fx.off ? _.duration = 0 : typeof _.duration != "number" && (_.duration in p.fx.speeds ? _.duration = p.fx.speeds[_.duration] : _.duration = p.fx.speeds._default), (_.queue == null || _.queue === !0) && (_.queue = "fx"), _.old = _.complete, _.complete = function() {
        T(_.old) && _.old.call(this), _.queue && p.dequeue(this, _.queue);
      }, _;
    }, p.fn.extend({
      fadeTo: function(u, d, v, _) {
        return this.filter(Vi).css("opacity", 0).show().end().animate({ opacity: d }, u, v, _);
      },
      animate: function(u, d, v, _) {
        var m = p.isEmptyObject(u), b = p.speed(d, v, _), $ = function() {
          var M = It(this, p.extend({}, u), b);
          (m || ae.get(this, "finish")) && M.stop(!0);
        };
        return $.finish = $, m || b.queue === !1 ? this.each($) : this.queue(b.queue, $);
      },
      stop: function(u, d, v) {
        var _ = function(m) {
          var b = m.stop;
          delete m.stop, b(v);
        };
        return typeof u != "string" && (v = d, d = u, u = void 0), d && this.queue(u || "fx", []), this.each(function() {
          var m = !0, b = u != null && u + "queueHooks", $ = p.timers, M = ae.get(this);
          if (b)
            M[b] && M[b].stop && _(M[b]);
          else
            for (b in M)
              M[b] && M[b].stop && zc.test(b) && _(M[b]);
          for (b = $.length; b--; )
            $[b].elem === this && (u == null || $[b].queue === u) && ($[b].anim.stop(v), m = !1, $.splice(b, 1));
          (m || !v) && p.dequeue(this, u);
        });
      },
      finish: function(u) {
        return u !== !1 && (u = u || "fx"), this.each(function() {
          var d, v = ae.get(this), _ = v[u + "queue"], m = v[u + "queueHooks"], b = p.timers, $ = _ ? _.length : 0;
          for (v.finish = !0, p.queue(this, u, []), m && m.stop && m.stop.call(this, !0), d = b.length; d--; )
            b[d].elem === this && b[d].queue === u && (b[d].anim.stop(!0), b.splice(d, 1));
          for (d = 0; d < $; d++)
            _[d] && _[d].finish && _[d].finish.call(this);
          delete v.finish;
        });
      }
    }), p.each(["toggle", "show", "hide"], function(u, d) {
      var v = p.fn[d];
      p.fn[d] = function(_, m, b) {
        return _ == null || typeof _ == "boolean" ? v.apply(this, arguments) : this.animate(Gi(d, !0), _, m, b);
      };
    }), p.each({
      slideDown: Gi("show"),
      slideUp: Gi("hide"),
      slideToggle: Gi("toggle"),
      fadeIn: { opacity: "show" },
      fadeOut: { opacity: "hide" },
      fadeToggle: { opacity: "toggle" }
    }, function(u, d) {
      p.fn[u] = function(v, _, m) {
        return this.animate(d, v, _, m);
      };
    }), p.timers = [], p.fx.tick = function() {
      var u, d = 0, v = p.timers;
      for (Yn = Date.now(); d < v.length; d++)
        u = v[d], !u() && v[d] === u && v.splice(d--, 1);
      v.length || p.fx.stop(), Yn = void 0;
    }, p.fx.timer = function(u) {
      p.timers.push(u), p.fx.start();
    }, p.fx.interval = 13, p.fx.start = function() {
      ji || (ji = !0, ra());
    }, p.fx.stop = function() {
      ji = null;
    }, p.fx.speeds = {
      slow: 600,
      fast: 200,
      // Default speed
      _default: 400
    }, p.fn.delay = function(u, d) {
      return u = p.fx && p.fx.speeds[u] || u, d = d || "fx", this.queue(d, function(v, _) {
        var m = t.setTimeout(v, u);
        _.stop = function() {
          t.clearTimeout(m);
        };
      });
    }, function() {
      var u = w.createElement("input"), d = w.createElement("select"), v = d.appendChild(w.createElement("option"));
      u.type = "checkbox", y.checkOn = u.value !== "", y.optSelected = v.selected, u = w.createElement("input"), u.value = "t", u.type = "radio", y.radioValue = u.value === "t";
    }();
    var ds, pi = p.expr.attrHandle;
    p.fn.extend({
      attr: function(u, d) {
        return mt(this, p.attr, u, d, arguments.length > 1);
      },
      removeAttr: function(u) {
        return this.each(function() {
          p.removeAttr(this, u);
        });
      }
    }), p.extend({
      attr: function(u, d, v) {
        var _, m, b = u.nodeType;
        if (!(b === 3 || b === 8 || b === 2)) {
          if (typeof u.getAttribute == "undefined")
            return p.prop(u, d, v);
          if ((b !== 1 || !p.isXMLDoc(u)) && (m = p.attrHooks[d.toLowerCase()] || (p.expr.match.bool.test(d) ? ds : void 0)), v !== void 0) {
            if (v === null) {
              p.removeAttr(u, d);
              return;
            }
            return m && "set" in m && (_ = m.set(u, v, d)) !== void 0 ? _ : (u.setAttribute(d, v + ""), v);
          }
          return m && "get" in m && (_ = m.get(u, d)) !== null ? _ : (_ = p.find.attr(u, d), _ == null ? void 0 : _);
        }
      },
      attrHooks: {
        type: {
          set: function(u, d) {
            if (!y.radioValue && d === "radio" && H(u, "input")) {
              var v = u.value;
              return u.setAttribute("type", d), v && (u.value = v), d;
            }
          }
        }
      },
      removeAttr: function(u, d) {
        var v, _ = 0, m = d && d.match(ie);
        if (m && u.nodeType === 1)
          for (; v = m[_++]; )
            u.removeAttribute(v);
      }
    }), ds = {
      set: function(u, d, v) {
        return d === !1 ? p.removeAttr(u, v) : u.setAttribute(v, v), v;
      }
    }, p.each(p.expr.match.bool.source.match(/\w+/g), function(u, d) {
      var v = pi[d] || p.find.attr;
      pi[d] = function(_, m, b) {
        var $, M, E = m.toLowerCase();
        return b || (M = pi[E], pi[E] = $, $ = v(_, m, b) != null ? E : null, pi[E] = M), $;
      };
    });
    var Yc = /^(?:input|select|textarea|button)$/i, Uc = /^(?:a|area)$/i;
    p.fn.extend({
      prop: function(u, d) {
        return mt(this, p.prop, u, d, arguments.length > 1);
      },
      removeProp: function(u) {
        return this.each(function() {
          delete this[p.propFix[u] || u];
        });
      }
    }), p.extend({
      prop: function(u, d, v) {
        var _, m, b = u.nodeType;
        if (!(b === 3 || b === 8 || b === 2))
          return (b !== 1 || !p.isXMLDoc(u)) && (d = p.propFix[d] || d, m = p.propHooks[d]), v !== void 0 ? m && "set" in m && (_ = m.set(u, v, d)) !== void 0 ? _ : u[d] = v : m && "get" in m && (_ = m.get(u, d)) !== null ? _ : u[d];
      },
      propHooks: {
        tabIndex: {
          get: function(u) {
            var d = p.find.attr(u, "tabindex");
            return d ? parseInt(d, 10) : Yc.test(u.nodeName) || Uc.test(u.nodeName) && u.href ? 0 : -1;
          }
        }
      },
      propFix: {
        for: "htmlFor",
        class: "className"
      }
    }), y.optSelected || (p.propHooks.selected = {
      get: function(u) {
        var d = u.parentNode;
        return d && d.parentNode && d.parentNode.selectedIndex, null;
      },
      set: function(u) {
        var d = u.parentNode;
        d && (d.selectedIndex, d.parentNode && d.parentNode.selectedIndex);
      }
    }), p.each([
      "tabIndex",
      "readOnly",
      "maxLength",
      "cellSpacing",
      "cellPadding",
      "rowSpan",
      "colSpan",
      "useMap",
      "frameBorder",
      "contentEditable"
    ], function() {
      p.propFix[this.toLowerCase()] = this;
    });
    function wn(u) {
      var d = u.match(ie) || [];
      return d.join(" ");
    }
    function $n(u) {
      return u.getAttribute && u.getAttribute("class") || "";
    }
    function aa(u) {
      return Array.isArray(u) ? u : typeof u == "string" ? u.match(ie) || [] : [];
    }
    p.fn.extend({
      addClass: function(u) {
        var d, v, _, m, b, $;
        return T(u) ? this.each(function(M) {
          p(this).addClass(u.call(this, M, $n(this)));
        }) : (d = aa(u), d.length ? this.each(function() {
          if (_ = $n(this), v = this.nodeType === 1 && " " + wn(_) + " ", v) {
            for (b = 0; b < d.length; b++)
              m = d[b], v.indexOf(" " + m + " ") < 0 && (v += m + " ");
            $ = wn(v), _ !== $ && this.setAttribute("class", $);
          }
        }) : this);
      },
      removeClass: function(u) {
        var d, v, _, m, b, $;
        return T(u) ? this.each(function(M) {
          p(this).removeClass(u.call(this, M, $n(this)));
        }) : arguments.length ? (d = aa(u), d.length ? this.each(function() {
          if (_ = $n(this), v = this.nodeType === 1 && " " + wn(_) + " ", v) {
            for (b = 0; b < d.length; b++)
              for (m = d[b]; v.indexOf(" " + m + " ") > -1; )
                v = v.replace(" " + m + " ", " ");
            $ = wn(v), _ !== $ && this.setAttribute("class", $);
          }
        }) : this) : this.attr("class", "");
      },
      toggleClass: function(u, d) {
        var v, _, m, b, $ = typeof u, M = $ === "string" || Array.isArray(u);
        return T(u) ? this.each(function(E) {
          p(this).toggleClass(
            u.call(this, E, $n(this), d),
            d
          );
        }) : typeof d == "boolean" && M ? d ? this.addClass(u) : this.removeClass(u) : (v = aa(u), this.each(function() {
          if (M)
            for (b = p(this), m = 0; m < v.length; m++)
              _ = v[m], b.hasClass(_) ? b.removeClass(_) : b.addClass(_);
          else
            (u === void 0 || $ === "boolean") && (_ = $n(this), _ && ae.set(this, "__className__", _), this.setAttribute && this.setAttribute(
              "class",
              _ || u === !1 ? "" : ae.get(this, "__className__") || ""
            ));
        }));
      },
      hasClass: function(u) {
        var d, v, _ = 0;
        for (d = " " + u + " "; v = this[_++]; )
          if (v.nodeType === 1 && (" " + wn($n(v)) + " ").indexOf(d) > -1)
            return !0;
        return !1;
      }
    });
    var jc = /\r/g;
    p.fn.extend({
      val: function(u) {
        var d, v, _, m = this[0];
        return arguments.length ? (_ = T(u), this.each(function(b) {
          var $;
          this.nodeType === 1 && (_ ? $ = u.call(this, b, p(this).val()) : $ = u, $ == null ? $ = "" : typeof $ == "number" ? $ += "" : Array.isArray($) && ($ = p.map($, function(M) {
            return M == null ? "" : M + "";
          })), d = p.valHooks[this.type] || p.valHooks[this.nodeName.toLowerCase()], (!d || !("set" in d) || d.set(this, $, "value") === void 0) && (this.value = $));
        })) : m ? (d = p.valHooks[m.type] || p.valHooks[m.nodeName.toLowerCase()], d && "get" in d && (v = d.get(m, "value")) !== void 0 ? v : (v = m.value, typeof v == "string" ? v.replace(jc, "") : v == null ? "" : v)) : void 0;
      }
    }), p.extend({
      valHooks: {
        option: {
          get: function(u) {
            var d = p.find.attr(u, "value");
            return d != null ? d : (
              // Support: IE <=10 - 11 only
              // option.text throws exceptions (trac-14686, trac-14858)
              // Strip and collapse whitespace
              // https://html.spec.whatwg.org/#strip-and-collapse-whitespace
              wn(p.text(u))
            );
          }
        },
        select: {
          get: function(u) {
            var d, v, _, m = u.options, b = u.selectedIndex, $ = u.type === "select-one", M = $ ? null : [], E = $ ? b + 1 : m.length;
            for (b < 0 ? _ = E : _ = $ ? b : 0; _ < E; _++)
              if (v = m[_], (v.selected || _ === b) && // Don't return options that are disabled or in a disabled optgroup
              !v.disabled && (!v.parentNode.disabled || !H(v.parentNode, "optgroup"))) {
                if (d = p(v).val(), $)
                  return d;
                M.push(d);
              }
            return M;
          },
          set: function(u, d) {
            for (var v, _, m = u.options, b = p.makeArray(d), $ = m.length; $--; )
              _ = m[$], (_.selected = p.inArray(p.valHooks.option.get(_), b) > -1) && (v = !0);
            return v || (u.selectedIndex = -1), b;
          }
        }
      }
    }), p.each(["radio", "checkbox"], function() {
      p.valHooks[this] = {
        set: function(u, d) {
          if (Array.isArray(d))
            return u.checked = p.inArray(p(u).val(), d) > -1;
        }
      }, y.checkOn || (p.valHooks[this].get = function(u) {
        return u.getAttribute("value") === null ? "on" : u.value;
      });
    });
    var vi = t.location, hs = { guid: Date.now() }, oa = /\?/;
    p.parseXML = function(u) {
      var d, v;
      if (!u || typeof u != "string")
        return null;
      try {
        d = new t.DOMParser().parseFromString(u, "text/xml");
      } catch (_) {
      }
      return v = d && d.getElementsByTagName("parsererror")[0], (!d || v) && p.error("Invalid XML: " + (v ? p.map(v.childNodes, function(_) {
        return _.textContent;
      }).join(`
`) : u)), d;
    };
    var gs = /^(?:focusinfocus|focusoutblur)$/, ps = function(u) {
      u.stopPropagation();
    };
    p.extend(p.event, {
      trigger: function(u, d, v, _) {
        var m, b, $, M, E, N, B, Y, O = [v || w], Z = h.call(u, "type") ? u.type : u, de = h.call(u, "namespace") ? u.namespace.split(".") : [];
        if (b = Y = $ = v = v || w, !(v.nodeType === 3 || v.nodeType === 8) && !gs.test(Z + p.event.triggered) && (Z.indexOf(".") > -1 && (de = Z.split("."), Z = de.shift(), de.sort()), E = Z.indexOf(":") < 0 && "on" + Z, u = u[p.expando] ? u : new p.Event(Z, typeof u == "object" && u), u.isTrigger = _ ? 2 : 3, u.namespace = de.join("."), u.rnamespace = u.namespace ? new RegExp("(^|\\.)" + de.join("\\.(?:.*\\.|)") + "(\\.|$)") : null, u.result = void 0, u.target || (u.target = v), d = d == null ? [u] : p.makeArray(d, [u]), B = p.event.special[Z] || {}, !(!_ && B.trigger && B.trigger.apply(v, d) === !1))) {
          if (!_ && !B.noBubble && !A(v)) {
            for (M = B.delegateType || Z, gs.test(M + Z) || (b = b.parentNode); b; b = b.parentNode)
              O.push(b), $ = b;
            $ === (v.ownerDocument || w) && O.push($.defaultView || $.parentWindow || t);
          }
          for (m = 0; (b = O[m++]) && !u.isPropagationStopped(); )
            Y = b, u.type = m > 1 ? M : B.bindType || Z, N = (ae.get(b, "events") || /* @__PURE__ */ Object.create(null))[u.type] && ae.get(b, "handle"), N && N.apply(b, d), N = E && b[E], N && N.apply && ci(b) && (u.result = N.apply(b, d), u.result === !1 && u.preventDefault());
          return u.type = Z, !_ && !u.isDefaultPrevented() && (!B._default || B._default.apply(O.pop(), d) === !1) && ci(v) && E && T(v[Z]) && !A(v) && ($ = v[E], $ && (v[E] = null), p.event.triggered = Z, u.isPropagationStopped() && Y.addEventListener(Z, ps), v[Z](), u.isPropagationStopped() && Y.removeEventListener(Z, ps), p.event.triggered = void 0, $ && (v[E] = $)), u.result;
        }
      },
      // Piggyback on a donor event to simulate a different one
      // Used only for `focus(in | out)` events
      simulate: function(u, d, v) {
        var _ = p.extend(
          new p.Event(),
          v,
          {
            type: u,
            isSimulated: !0
          }
        );
        p.event.trigger(_, null, d);
      }
    }), p.fn.extend({
      trigger: function(u, d) {
        return this.each(function() {
          p.event.trigger(u, d, this);
        });
      },
      triggerHandler: function(u, d) {
        var v = this[0];
        if (v)
          return p.event.trigger(u, d, v, !0);
      }
    });
    var Gc = /\[\]$/, vs = /\r?\n/g, Wc = /^(?:submit|button|image|reset|file)$/i, qc = /^(?:input|select|textarea|keygen)/i;
    function sa(u, d, v, _) {
      var m;
      if (Array.isArray(d))
        p.each(d, function(b, $) {
          v || Gc.test(u) ? _(u, $) : sa(
            u + "[" + (typeof $ == "object" && $ != null ? b : "") + "]",
            $,
            v,
            _
          );
        });
      else if (!v && S(d) === "object")
        for (m in d)
          sa(u + "[" + m + "]", d[m], v, _);
      else
        _(u, d);
    }
    p.param = function(u, d) {
      var v, _ = [], m = function(b, $) {
        var M = T($) ? $() : $;
        _[_.length] = encodeURIComponent(b) + "=" + encodeURIComponent(M == null ? "" : M);
      };
      if (u == null)
        return "";
      if (Array.isArray(u) || u.jquery && !p.isPlainObject(u))
        p.each(u, function() {
          m(this.name, this.value);
        });
      else
        for (v in u)
          sa(v, u[v], d, m);
      return _.join("&");
    }, p.fn.extend({
      serialize: function() {
        return p.param(this.serializeArray());
      },
      serializeArray: function() {
        return this.map(function() {
          var u = p.prop(this, "elements");
          return u ? p.makeArray(u) : this;
        }).filter(function() {
          var u = this.type;
          return this.name && !p(this).is(":disabled") && qc.test(this.nodeName) && !Wc.test(u) && (this.checked || !hi.test(u));
        }).map(function(u, d) {
          var v = p(this).val();
          return v == null ? null : Array.isArray(v) ? p.map(v, function(_) {
            return { name: d.name, value: _.replace(vs, `\r
`) };
          }) : { name: d.name, value: v.replace(vs, `\r
`) };
        }).get();
      }
    });
    var Zc = /%20/g, Kc = /#.*$/, Qc = /([?&])_=[^&]*/, Jc = /^(.*?):[ \t]*([^\r\n]*)$/mg, ef = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/, tf = /^(?:GET|HEAD)$/, nf = /^\/\//, xs = {}, ua = {}, _s = "*/".concat("*"), la = w.createElement("a");
    la.href = vi.href;
    function ys(u) {
      return function(d, v) {
        typeof d != "string" && (v = d, d = "*");
        var _, m = 0, b = d.toLowerCase().match(ie) || [];
        if (T(v))
          for (; _ = b[m++]; )
            _[0] === "+" ? (_ = _.slice(1) || "*", (u[_] = u[_] || []).unshift(v)) : (u[_] = u[_] || []).push(v);
      };
    }
    function ms(u, d, v, _) {
      var m = {}, b = u === ua;
      function $(M) {
        var E;
        return m[M] = !0, p.each(u[M] || [], function(N, B) {
          var Y = B(d, v, _);
          if (typeof Y == "string" && !b && !m[Y])
            return d.dataTypes.unshift(Y), $(Y), !1;
          if (b)
            return !(E = Y);
        }), E;
      }
      return $(d.dataTypes[0]) || !m["*"] && $("*");
    }
    function ca(u, d) {
      var v, _, m = p.ajaxSettings.flatOptions || {};
      for (v in d)
        d[v] !== void 0 && ((m[v] ? u : _ || (_ = {}))[v] = d[v]);
      return _ && p.extend(!0, u, _), u;
    }
    function rf(u, d, v) {
      for (var _, m, b, $, M = u.contents, E = u.dataTypes; E[0] === "*"; )
        E.shift(), _ === void 0 && (_ = u.mimeType || d.getResponseHeader("Content-Type"));
      if (_) {
        for (m in M)
          if (M[m] && M[m].test(_)) {
            E.unshift(m);
            break;
          }
      }
      if (E[0] in v)
        b = E[0];
      else {
        for (m in v) {
          if (!E[0] || u.converters[m + " " + E[0]]) {
            b = m;
            break;
          }
          $ || ($ = m);
        }
        b = b || $;
      }
      if (b)
        return b !== E[0] && E.unshift(b), v[b];
    }
    function af(u, d, v, _) {
      var m, b, $, M, E, N = {}, B = u.dataTypes.slice();
      if (B[1])
        for ($ in u.converters)
          N[$.toLowerCase()] = u.converters[$];
      for (b = B.shift(); b; )
        if (u.responseFields[b] && (v[u.responseFields[b]] = d), !E && _ && u.dataFilter && (d = u.dataFilter(d, u.dataType)), E = b, b = B.shift(), b) {
          if (b === "*")
            b = E;
          else if (E !== "*" && E !== b) {
            if ($ = N[E + " " + b] || N["* " + b], !$) {
              for (m in N)
                if (M = m.split(" "), M[1] === b && ($ = N[E + " " + M[0]] || N["* " + M[0]], $)) {
                  $ === !0 ? $ = N[m] : N[m] !== !0 && (b = M[0], B.unshift(M[1]));
                  break;
                }
            }
            if ($ !== !0)
              if ($ && u.throws)
                d = $(d);
              else
                try {
                  d = $(d);
                } catch (Y) {
                  return {
                    state: "parsererror",
                    error: $ ? Y : "No conversion from " + E + " to " + b
                  };
                }
          }
        }
      return { state: "success", data: d };
    }
    p.extend({
      // Counter for holding the number of active queries
      active: 0,
      // Last-Modified header cache for next request
      lastModified: {},
      etag: {},
      ajaxSettings: {
        url: vi.href,
        type: "GET",
        isLocal: ef.test(vi.protocol),
        global: !0,
        processData: !0,
        async: !0,
        contentType: "application/x-www-form-urlencoded; charset=UTF-8",
        /*
        timeout: 0,
        data: null,
        dataType: null,
        username: null,
        password: null,
        cache: null,
        throws: false,
        traditional: false,
        headers: {},
        */
        accepts: {
          "*": _s,
          text: "text/plain",
          html: "text/html",
          xml: "application/xml, text/xml",
          json: "application/json, text/javascript"
        },
        contents: {
          xml: /\bxml\b/,
          html: /\bhtml/,
          json: /\bjson\b/
        },
        responseFields: {
          xml: "responseXML",
          text: "responseText",
          json: "responseJSON"
        },
        // Data converters
        // Keys separate source (or catchall "*") and destination types with a single space
        converters: {
          // Convert anything to text
          "* text": String,
          // Text to html (true = no transformation)
          "text html": !0,
          // Evaluate text as a json expression
          "text json": JSON.parse,
          // Parse text as xml
          "text xml": p.parseXML
        },
        // For options that shouldn't be deep extended:
        // you can add your own custom options here if
        // and when you create one that shouldn't be
        // deep extended (see ajaxExtend)
        flatOptions: {
          url: !0,
          context: !0
        }
      },
      // Creates a full fledged settings object into target
      // with both ajaxSettings and settings fields.
      // If target is omitted, writes into ajaxSettings.
      ajaxSetup: function(u, d) {
        return d ? (
          // Building a settings object
          ca(ca(u, p.ajaxSettings), d)
        ) : (
          // Extending ajaxSettings
          ca(p.ajaxSettings, u)
        );
      },
      ajaxPrefilter: ys(xs),
      ajaxTransport: ys(ua),
      // Main method
      ajax: function(u, d) {
        typeof u == "object" && (d = u, u = void 0), d = d || {};
        var v, _, m, b, $, M, E, N, B, Y, O = p.ajaxSetup({}, d), Z = O.context || O, de = O.context && (Z.nodeType || Z.jquery) ? p(Z) : p.event, $e = p.Deferred(), xe = p.Callbacks("once memory"), ut = O.statusCode || {}, tt = {}, Vt = {}, Yt = "canceled", Te = {
          readyState: 0,
          // Builds headers hashtable if needed
          getResponseHeader: function(Se) {
            var We;
            if (E) {
              if (!b)
                for (b = {}; We = Jc.exec(m); )
                  b[We[1].toLowerCase() + " "] = (b[We[1].toLowerCase() + " "] || []).concat(We[2]);
              We = b[Se.toLowerCase() + " "];
            }
            return We == null ? null : We.join(", ");
          },
          // Raw string
          getAllResponseHeaders: function() {
            return E ? m : null;
          },
          // Caches the header
          setRequestHeader: function(Se, We) {
            return E == null && (Se = Vt[Se.toLowerCase()] = Vt[Se.toLowerCase()] || Se, tt[Se] = We), this;
          },
          // Overrides response content-type header
          overrideMimeType: function(Se) {
            return E == null && (O.mimeType = Se), this;
          },
          // Status-dependent callbacks
          statusCode: function(Se) {
            var We;
            if (Se)
              if (E)
                Te.always(Se[Te.status]);
              else
                for (We in Se)
                  ut[We] = [ut[We], Se[We]];
            return this;
          },
          // Cancel the request
          abort: function(Se) {
            var We = Se || Yt;
            return v && v.abort(We), An(0, We), this;
          }
        };
        if ($e.promise(Te), O.url = ((u || O.url || vi.href) + "").replace(nf, vi.protocol + "//"), O.type = d.method || d.type || O.method || O.type, O.dataTypes = (O.dataType || "*").toLowerCase().match(ie) || [""], O.crossDomain == null) {
          M = w.createElement("a");
          try {
            M.href = O.url, M.href = M.href, O.crossDomain = la.protocol + "//" + la.host != M.protocol + "//" + M.host;
          } catch (Se) {
            O.crossDomain = !0;
          }
        }
        if (O.data && O.processData && typeof O.data != "string" && (O.data = p.param(O.data, O.traditional)), ms(xs, O, d, Te), E)
          return Te;
        N = p.event && O.global, N && p.active++ === 0 && p.event.trigger("ajaxStart"), O.type = O.type.toUpperCase(), O.hasContent = !tf.test(O.type), _ = O.url.replace(Kc, ""), O.hasContent ? O.data && O.processData && (O.contentType || "").indexOf("application/x-www-form-urlencoded") === 0 && (O.data = O.data.replace(Zc, "+")) : (Y = O.url.slice(_.length), O.data && (O.processData || typeof O.data == "string") && (_ += (oa.test(_) ? "&" : "?") + O.data, delete O.data), O.cache === !1 && (_ = _.replace(Qc, "$1"), Y = (oa.test(_) ? "&" : "?") + "_=" + hs.guid++ + Y), O.url = _ + Y), O.ifModified && (p.lastModified[_] && Te.setRequestHeader("If-Modified-Since", p.lastModified[_]), p.etag[_] && Te.setRequestHeader("If-None-Match", p.etag[_])), (O.data && O.hasContent && O.contentType !== !1 || d.contentType) && Te.setRequestHeader("Content-Type", O.contentType), Te.setRequestHeader(
          "Accept",
          O.dataTypes[0] && O.accepts[O.dataTypes[0]] ? O.accepts[O.dataTypes[0]] + (O.dataTypes[0] !== "*" ? ", " + _s + "; q=0.01" : "") : O.accepts["*"]
        );
        for (B in O.headers)
          Te.setRequestHeader(B, O.headers[B]);
        if (O.beforeSend && (O.beforeSend.call(Z, Te, O) === !1 || E))
          return Te.abort();
        if (Yt = "abort", xe.add(O.complete), Te.done(O.success), Te.fail(O.error), v = ms(ua, O, d, Te), !v)
          An(-1, "No Transport");
        else {
          if (Te.readyState = 1, N && de.trigger("ajaxSend", [Te, O]), E)
            return Te;
          O.async && O.timeout > 0 && ($ = t.setTimeout(function() {
            Te.abort("timeout");
          }, O.timeout));
          try {
            E = !1, v.send(tt, An);
          } catch (Se) {
            if (E)
              throw Se;
            An(-1, Se);
          }
        }
        function An(Se, We, _i, da) {
          var Ut, yi, jt, gn, pn, Rt = We;
          E || (E = !0, $ && t.clearTimeout($), v = void 0, m = da || "", Te.readyState = Se > 0 ? 4 : 0, Ut = Se >= 200 && Se < 300 || Se === 304, _i && (gn = rf(O, Te, _i)), !Ut && p.inArray("script", O.dataTypes) > -1 && p.inArray("json", O.dataTypes) < 0 && (O.converters["text script"] = function() {
          }), gn = af(O, gn, Te, Ut), Ut ? (O.ifModified && (pn = Te.getResponseHeader("Last-Modified"), pn && (p.lastModified[_] = pn), pn = Te.getResponseHeader("etag"), pn && (p.etag[_] = pn)), Se === 204 || O.type === "HEAD" ? Rt = "nocontent" : Se === 304 ? Rt = "notmodified" : (Rt = gn.state, yi = gn.data, jt = gn.error, Ut = !jt)) : (jt = Rt, (Se || !Rt) && (Rt = "error", Se < 0 && (Se = 0))), Te.status = Se, Te.statusText = (We || Rt) + "", Ut ? $e.resolveWith(Z, [yi, Rt, Te]) : $e.rejectWith(Z, [Te, Rt, jt]), Te.statusCode(ut), ut = void 0, N && de.trigger(
            Ut ? "ajaxSuccess" : "ajaxError",
            [Te, O, Ut ? yi : jt]
          ), xe.fireWith(Z, [Te, Rt]), N && (de.trigger("ajaxComplete", [Te, O]), --p.active || p.event.trigger("ajaxStop")));
        }
        return Te;
      },
      getJSON: function(u, d, v) {
        return p.get(u, d, v, "json");
      },
      getScript: function(u, d) {
        return p.get(u, void 0, d, "script");
      }
    }), p.each(["get", "post"], function(u, d) {
      p[d] = function(v, _, m, b) {
        return T(_) && (b = b || m, m = _, _ = void 0), p.ajax(p.extend({
          url: v,
          type: d,
          dataType: b,
          data: _,
          success: m
        }, p.isPlainObject(v) && v));
      };
    }), p.ajaxPrefilter(function(u) {
      var d;
      for (d in u.headers)
        d.toLowerCase() === "content-type" && (u.contentType = u.headers[d] || "");
    }), p._evalUrl = function(u, d, v) {
      return p.ajax({
        url: u,
        // Make this explicit, since user can override this through ajaxSetup (trac-11264)
        type: "GET",
        dataType: "script",
        cache: !0,
        async: !1,
        global: !1,
        // Only evaluate the response if it is successful (gh-4126)
        // dataFilter is not invoked for failure responses, so using it instead
        // of the default converter is kludgy but it works.
        converters: {
          "text script": function() {
          }
        },
        dataFilter: function(_) {
          p.globalEval(_, d, v);
        }
      });
    }, p.fn.extend({
      wrapAll: function(u) {
        var d;
        return this[0] && (T(u) && (u = u.call(this[0])), d = p(u, this[0].ownerDocument).eq(0).clone(!0), this[0].parentNode && d.insertBefore(this[0]), d.map(function() {
          for (var v = this; v.firstElementChild; )
            v = v.firstElementChild;
          return v;
        }).append(this)), this;
      },
      wrapInner: function(u) {
        return T(u) ? this.each(function(d) {
          p(this).wrapInner(u.call(this, d));
        }) : this.each(function() {
          var d = p(this), v = d.contents();
          v.length ? v.wrapAll(u) : d.append(u);
        });
      },
      wrap: function(u) {
        var d = T(u);
        return this.each(function(v) {
          p(this).wrapAll(d ? u.call(this, v) : u);
        });
      },
      unwrap: function(u) {
        return this.parent(u).not("body").each(function() {
          p(this).replaceWith(this.childNodes);
        }), this;
      }
    }), p.expr.pseudos.hidden = function(u) {
      return !p.expr.pseudos.visible(u);
    }, p.expr.pseudos.visible = function(u) {
      return !!(u.offsetWidth || u.offsetHeight || u.getClientRects().length);
    }, p.ajaxSettings.xhr = function() {
      try {
        return new t.XMLHttpRequest();
      } catch (u) {
      }
    };
    var of = {
      // File protocol always yields status code 0, assume 200
      0: 200,
      // Support: IE <=9 only
      // trac-1450: sometimes IE returns 1223 when it should be 204
      1223: 204
    }, xi = p.ajaxSettings.xhr();
    y.cors = !!xi && "withCredentials" in xi, y.ajax = xi = !!xi, p.ajaxTransport(function(u) {
      var d, v;
      if (y.cors || xi && !u.crossDomain)
        return {
          send: function(_, m) {
            var b, $ = u.xhr();
            if ($.open(
              u.type,
              u.url,
              u.async,
              u.username,
              u.password
            ), u.xhrFields)
              for (b in u.xhrFields)
                $[b] = u.xhrFields[b];
            u.mimeType && $.overrideMimeType && $.overrideMimeType(u.mimeType), !u.crossDomain && !_["X-Requested-With"] && (_["X-Requested-With"] = "XMLHttpRequest");
            for (b in _)
              $.setRequestHeader(b, _[b]);
            d = function(M) {
              return function() {
                d && (d = v = $.onload = $.onerror = $.onabort = $.ontimeout = $.onreadystatechange = null, M === "abort" ? $.abort() : M === "error" ? typeof $.status != "number" ? m(0, "error") : m(
                  // File: protocol always yields status 0; see trac-8605, trac-14207
                  $.status,
                  $.statusText
                ) : m(
                  of[$.status] || $.status,
                  $.statusText,
                  // Support: IE <=9 only
                  // IE9 has no XHR2 but throws on binary (trac-11426)
                  // For XHR2 non-text, let the caller handle it (gh-2498)
                  ($.responseType || "text") !== "text" || typeof $.responseText != "string" ? { binary: $.response } : { text: $.responseText },
                  $.getAllResponseHeaders()
                ));
              };
            }, $.onload = d(), v = $.onerror = $.ontimeout = d("error"), $.onabort !== void 0 ? $.onabort = v : $.onreadystatechange = function() {
              $.readyState === 4 && t.setTimeout(function() {
                d && v();
              });
            }, d = d("abort");
            try {
              $.send(u.hasContent && u.data || null);
            } catch (M) {
              if (d)
                throw M;
            }
          },
          abort: function() {
            d && d();
          }
        };
    }), p.ajaxPrefilter(function(u) {
      u.crossDomain && (u.contents.script = !1);
    }), p.ajaxSetup({
      accepts: {
        script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
      },
      contents: {
        script: /\b(?:java|ecma)script\b/
      },
      converters: {
        "text script": function(u) {
          return p.globalEval(u), u;
        }
      }
    }), p.ajaxPrefilter("script", function(u) {
      u.cache === void 0 && (u.cache = !1), u.crossDomain && (u.type = "GET");
    }), p.ajaxTransport("script", function(u) {
      if (u.crossDomain || u.scriptAttrs) {
        var d, v;
        return {
          send: function(_, m) {
            d = p("<script>").attr(u.scriptAttrs || {}).prop({ charset: u.scriptCharset, src: u.url }).on("load error", v = function(b) {
              d.remove(), v = null, b && m(b.type === "error" ? 404 : 200, b.type);
            }), w.head.appendChild(d[0]);
          },
          abort: function() {
            v && v();
          }
        };
      }
    });
    var bs = [], fa = /(=)\?(?=&|$)|\?\?/;
    p.ajaxSetup({
      jsonp: "callback",
      jsonpCallback: function() {
        var u = bs.pop() || p.expando + "_" + hs.guid++;
        return this[u] = !0, u;
      }
    }), p.ajaxPrefilter("json jsonp", function(u, d, v) {
      var _, m, b, $ = u.jsonp !== !1 && (fa.test(u.url) ? "url" : typeof u.data == "string" && (u.contentType || "").indexOf("application/x-www-form-urlencoded") === 0 && fa.test(u.data) && "data");
      if ($ || u.dataTypes[0] === "jsonp")
        return _ = u.jsonpCallback = T(u.jsonpCallback) ? u.jsonpCallback() : u.jsonpCallback, $ ? u[$] = u[$].replace(fa, "$1" + _) : u.jsonp !== !1 && (u.url += (oa.test(u.url) ? "&" : "?") + u.jsonp + "=" + _), u.converters["script json"] = function() {
          return b || p.error(_ + " was not called"), b[0];
        }, u.dataTypes[0] = "json", m = t[_], t[_] = function() {
          b = arguments;
        }, v.always(function() {
          m === void 0 ? p(t).removeProp(_) : t[_] = m, u[_] && (u.jsonpCallback = d.jsonpCallback, bs.push(_)), b && T(m) && m(b[0]), b = m = void 0;
        }), "script";
    }), y.createHTMLDocument = function() {
      var u = w.implementation.createHTMLDocument("").body;
      return u.innerHTML = "<form></form><form></form>", u.childNodes.length === 2;
    }(), p.parseHTML = function(u, d, v) {
      if (typeof u != "string")
        return [];
      typeof d == "boolean" && (v = d, d = !1);
      var _, m, b;
      return d || (y.createHTMLDocument ? (d = w.implementation.createHTMLDocument(""), _ = d.createElement("base"), _.href = w.location.href, d.head.appendChild(_)) : d = w), m = Je.exec(u), b = !v && [], m ? [d.createElement(m[1])] : (m = Ko([u], d, b), b && b.length && p(b).remove(), p.merge([], m.childNodes));
    }, p.fn.load = function(u, d, v) {
      var _, m, b, $ = this, M = u.indexOf(" ");
      return M > -1 && (_ = wn(u.slice(M)), u = u.slice(0, M)), T(d) ? (v = d, d = void 0) : d && typeof d == "object" && (m = "POST"), $.length > 0 && p.ajax({
        url: u,
        // If "type" variable is undefined, then "GET" method will be used.
        // Make value of this field explicit since
        // user can override it through ajaxSetup method
        type: m || "GET",
        dataType: "html",
        data: d
      }).done(function(E) {
        b = arguments, $.html(_ ? (
          // If a selector was specified, locate the right elements in a dummy div
          // Exclude scripts to avoid IE 'Permission Denied' errors
          p("<div>").append(p.parseHTML(E)).find(_)
        ) : (
          // Otherwise use the full result
          E
        ));
      }).always(v && function(E, N) {
        $.each(function() {
          v.apply(this, b || [E.responseText, N, E]);
        });
      }), this;
    }, p.expr.pseudos.animated = function(u) {
      return p.grep(p.timers, function(d) {
        return u === d.elem;
      }).length;
    }, p.offset = {
      setOffset: function(u, d, v) {
        var _, m, b, $, M, E, N, B = p.css(u, "position"), Y = p(u), O = {};
        B === "static" && (u.style.position = "relative"), M = Y.offset(), b = p.css(u, "top"), E = p.css(u, "left"), N = (B === "absolute" || B === "fixed") && (b + E).indexOf("auto") > -1, N ? (_ = Y.position(), $ = _.top, m = _.left) : ($ = parseFloat(b) || 0, m = parseFloat(E) || 0), T(d) && (d = d.call(u, v, p.extend({}, M))), d.top != null && (O.top = d.top - M.top + $), d.left != null && (O.left = d.left - M.left + m), "using" in d ? d.using.call(u, O) : Y.css(O);
      }
    }, p.fn.extend({
      // offset() relates an element's border box to the document origin
      offset: function(u) {
        if (arguments.length)
          return u === void 0 ? this : this.each(function(m) {
            p.offset.setOffset(this, u, m);
          });
        var d, v, _ = this[0];
        if (_)
          return _.getClientRects().length ? (d = _.getBoundingClientRect(), v = _.ownerDocument.defaultView, {
            top: d.top + v.pageYOffset,
            left: d.left + v.pageXOffset
          }) : { top: 0, left: 0 };
      },
      // position() relates an element's margin box to its offset parent's padding box
      // This corresponds to the behavior of CSS absolute positioning
      position: function() {
        if (this[0]) {
          var u, d, v, _ = this[0], m = { top: 0, left: 0 };
          if (p.css(_, "position") === "fixed")
            d = _.getBoundingClientRect();
          else {
            for (d = this.offset(), v = _.ownerDocument, u = _.offsetParent || v.documentElement; u && (u === v.body || u === v.documentElement) && p.css(u, "position") === "static"; )
              u = u.parentNode;
            u && u !== _ && u.nodeType === 1 && (m = p(u).offset(), m.top += p.css(u, "borderTopWidth", !0), m.left += p.css(u, "borderLeftWidth", !0));
          }
          return {
            top: d.top - m.top - p.css(_, "marginTop", !0),
            left: d.left - m.left - p.css(_, "marginLeft", !0)
          };
        }
      },
      // This method will return documentElement in the following cases:
      // 1) For the element inside the iframe without offsetParent, this method will return
      //    documentElement of the parent window
      // 2) For the hidden or detached element
      // 3) For body or html element, i.e. in case of the html node - it will return itself
      //
      // but those exceptions were never presented as a real life use-cases
      // and might be considered as more preferable results.
      //
      // This logic, however, is not guaranteed and can change at any point in the future
      offsetParent: function() {
        return this.map(function() {
          for (var u = this.offsetParent; u && p.css(u, "position") === "static"; )
            u = u.offsetParent;
          return u || Tn;
        });
      }
    }), p.each({ scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function(u, d) {
      var v = d === "pageYOffset";
      p.fn[u] = function(_) {
        return mt(this, function(m, b, $) {
          var M;
          if (A(m) ? M = m : m.nodeType === 9 && (M = m.defaultView), $ === void 0)
            return M ? M[d] : m[b];
          M ? M.scrollTo(
            v ? M.pageXOffset : $,
            v ? $ : M.pageYOffset
          ) : m[b] = $;
        }, u, _, arguments.length);
      };
    }), p.each(["top", "left"], function(u, d) {
      p.cssHooks[d] = is(
        y.pixelPosition,
        function(v, _) {
          if (_)
            return _ = gi(v, d), ea.test(_) ? p(v).position()[d] + "px" : _;
        }
      );
    }), p.each({ Height: "height", Width: "width" }, function(u, d) {
      p.each({
        padding: "inner" + u,
        content: d,
        "": "outer" + u
      }, function(v, _) {
        p.fn[_] = function(m, b) {
          var $ = arguments.length && (v || typeof m != "boolean"), M = v || (m === !0 || b === !0 ? "margin" : "border");
          return mt(this, function(E, N, B) {
            var Y;
            return A(E) ? _.indexOf("outer") === 0 ? E["inner" + u] : E.document.documentElement["client" + u] : E.nodeType === 9 ? (Y = E.documentElement, Math.max(
              E.body["scroll" + u],
              Y["scroll" + u],
              E.body["offset" + u],
              Y["offset" + u],
              Y["client" + u]
            )) : B === void 0 ? (
              // Get width or height on the element, requesting but not forcing parseFloat
              p.css(E, N, M)
            ) : (
              // Set width or height on the element
              p.style(E, N, B, M)
            );
          }, d, $ ? m : void 0, $);
        };
      });
    }), p.each([
      "ajaxStart",
      "ajaxStop",
      "ajaxComplete",
      "ajaxError",
      "ajaxSuccess",
      "ajaxSend"
    ], function(u, d) {
      p.fn[d] = function(v) {
        return this.on(d, v);
      };
    }), p.fn.extend({
      bind: function(u, d, v) {
        return this.on(u, null, d, v);
      },
      unbind: function(u, d) {
        return this.off(u, null, d);
      },
      delegate: function(u, d, v, _) {
        return this.on(d, u, v, _);
      },
      undelegate: function(u, d, v) {
        return arguments.length === 1 ? this.off(u, "**") : this.off(d, u || "**", v);
      },
      hover: function(u, d) {
        return this.on("mouseenter", u).on("mouseleave", d || u);
      }
    }), p.each(
      "blur focus focusin focusout resize scroll click dblclick mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave change select submit keydown keypress keyup contextmenu".split(" "),
      function(u, d) {
        p.fn[d] = function(v, _) {
          return arguments.length > 0 ? this.on(d, null, v, _) : this.trigger(d);
        };
      }
    );
    var sf = /^[\s\uFEFF\xA0]+|([^\s\uFEFF\xA0])[\s\uFEFF\xA0]+$/g;
    p.proxy = function(u, d) {
      var v, _, m;
      if (typeof d == "string" && (v = u[d], d = u, u = v), !!T(u))
        return _ = a.call(arguments, 2), m = function() {
          return u.apply(d || this, _.concat(a.call(arguments)));
        }, m.guid = u.guid = u.guid || p.guid++, m;
    }, p.holdReady = function(u) {
      u ? p.readyWait++ : p.ready(!0);
    }, p.isArray = Array.isArray, p.parseJSON = JSON.parse, p.nodeName = H, p.isFunction = T, p.isWindow = A, p.camelCase = Ht, p.type = S, p.now = Date.now, p.isNumeric = function(u) {
      var d = p.type(u);
      return (d === "number" || d === "string") && // parseFloat NaNs numeric-cast false positives ("")
      // ...but misinterprets leading-number strings, particularly hex literals ("0x...")
      // subtraction forces infinities to NaN
      !isNaN(u - parseFloat(u));
    }, p.trim = function(u) {
      return u == null ? "" : (u + "").replace(sf, "$1");
    };
    var uf = t.jQuery, lf = t.$;
    return p.noConflict = function(u) {
      return t.$ === p && (t.$ = lf), u && t.jQuery === p && (t.jQuery = uf), p;
    }, typeof n == "undefined" && (t.jQuery = t.$ = p), p;
  });
})(zu);
var Mf = zu.exports;
const Zt = /* @__PURE__ */ kf(Mf), Lf = () => ({
  // These are the defaults.
  // pptxFileUrl: "",
  // fileInputId: "",
  slidesScale: "",
  //Change Slides scale by percent
  slideMode: !1,
  /** true,false*/
  slideType: "divs2slidesjs",
  /*'divs2slidesjs' (default) , 'revealjs'(https://revealjs.com)  -TODO*/
  revealjsPath: "",
  /*path to js file of revealjs - TODO*/
  keyBoardShortCut: !1,
  /** true,false ,condition: slideMode: true XXXXX - need to remove - this is doublcated*/
  mediaProcess: !0,
  /** true,false: if true then process video and audio files */
  jsZipV2: !1,
  themeProcess: !0,
  /*true (default) , false, "colorsAndImageOnly"*/
  incSlide: {
    width: 0,
    height: 0
  },
  slideModeConfig: {
    first: 1,
    nav: !0,
    /** true,false : show or not nav buttons*/
    navTxtColor: "black",
    /** color */
    keyBoardShortCut: !0,
    /** true,false ,condition: */
    showSlideNum: !0,
    /** true,false */
    showTotalSlideNum: !0,
    /** true,false */
    autoSlide: !0,
    /** false or seconds , F8 to active ,keyBoardShortCut: true */
    randomAutoSlide: !1,
    /** true,false ,autoSlide:true */
    loop: !1,
    /** true,false */
    background: !1,
    /** false or color*/
    transition: "default",
    /** transition type: "slid","fade","default","random" , to show transition efects :transitionTime > 0.5 */
    transitionTime: 1
    /** transition time between slides in seconds */
  },
  revealjsConfig: {}
});
function ur(e, t) {
  return e == null || t == null ? NaN : e < t ? -1 : e > t ? 1 : e >= t ? 0 : NaN;
}
function Df(e, t) {
  return e == null || t == null ? NaN : t < e ? -1 : t > e ? 1 : t >= e ? 0 : NaN;
}
function fo(e) {
  let t, n, i;
  e.length !== 2 ? (t = ur, n = (s, l) => ur(e(s), l), i = (s, l) => e(s) - l) : (t = e === ur || e === Df ? e : If, n = e, i = e);
  function r(s, l, c = 0, f = s.length) {
    if (c < f) {
      if (t(l, l) !== 0)
        return f;
      do {
        const h = c + f >>> 1;
        n(s[h], l) < 0 ? c = h + 1 : f = h;
      } while (c < f);
    }
    return c;
  }
  function a(s, l, c = 0, f = s.length) {
    if (c < f) {
      if (t(l, l) !== 0)
        return f;
      do {
        const h = c + f >>> 1;
        n(s[h], l) <= 0 ? c = h + 1 : f = h;
      } while (c < f);
    }
    return c;
  }
  function o(s, l, c = 0, f = s.length) {
    const h = r(s, l, c, f - 1);
    return h > c && i(s[h - 1], l) > -i(s[h], l) ? h - 1 : h;
  }
  return { left: r, center: o, right: a };
}
function If() {
  return 0;
}
function Nf(e) {
  return e === null ? NaN : +e;
}
const Pf = fo(ur), Of = Pf.right;
fo(Nf).center;
const Ff = Of;
class Es extends Map {
  constructor(t, n = zf) {
    if (super(), Object.defineProperties(this, { _intern: { value: /* @__PURE__ */ new Map() }, _key: { value: n } }), t != null)
      for (const [i, r] of t)
        this.set(i, r);
  }
  get(t) {
    return super.get(Rs(this, t));
  }
  has(t) {
    return super.has(Rs(this, t));
  }
  set(t, n) {
    return super.set(Bf(this, t), n);
  }
  delete(t) {
    return super.delete(Xf(this, t));
  }
}
function Rs({ _intern: e, _key: t }, n) {
  const i = t(n);
  return e.has(i) ? e.get(i) : n;
}
function Bf({ _intern: e, _key: t }, n) {
  const i = t(n);
  return e.has(i) ? e.get(i) : (e.set(i, n), n);
}
function Xf({ _intern: e, _key: t }, n) {
  const i = t(n);
  return e.has(i) && (n = e.get(i), e.delete(i)), n;
}
function zf(e) {
  return e !== null && typeof e == "object" ? e.valueOf() : e;
}
const Hf = Math.sqrt(50), Vf = Math.sqrt(10), Yf = Math.sqrt(2);
function xr(e, t, n) {
  const i = (t - e) / Math.max(0, n), r = Math.floor(Math.log10(i)), a = i / Math.pow(10, r), o = a >= Hf ? 10 : a >= Vf ? 5 : a >= Yf ? 2 : 1;
  let s, l, c;
  return r < 0 ? (c = Math.pow(10, -r) / o, s = Math.round(e * c), l = Math.round(t * c), s / c < e && ++s, l / c > t && --l, c = -c) : (c = Math.pow(10, r) * o, s = Math.round(e / c), l = Math.round(t / c), s * c < e && ++s, l * c > t && --l), l < s && 0.5 <= n && n < 2 ? xr(e, t, n * 2) : [s, l, c];
}
function za(e, t, n) {
  if (t = +t, e = +e, n = +n, !(n > 0))
    return [];
  if (e === t)
    return [e];
  const i = t < e, [r, a, o] = i ? xr(t, e, n) : xr(e, t, n);
  if (!(a >= r))
    return [];
  const s = a - r + 1, l = new Array(s);
  if (i)
    if (o < 0)
      for (let c = 0; c < s; ++c)
        l[c] = (a - c) / -o;
    else
      for (let c = 0; c < s; ++c)
        l[c] = (a - c) * o;
  else if (o < 0)
    for (let c = 0; c < s; ++c)
      l[c] = (r + c) / -o;
  else
    for (let c = 0; c < s; ++c)
      l[c] = (r + c) * o;
  return l;
}
function Ha(e, t, n) {
  return t = +t, e = +e, n = +n, xr(e, t, n)[2];
}
function Va(e, t, n) {
  t = +t, e = +e, n = +n;
  const i = t < e, r = i ? Ha(t, e, n) : Ha(e, t, n);
  return (i ? -1 : 1) * (r < 0 ? 1 / -r : r);
}
function Uf(e) {
  return e;
}
var lr = 1, cr = 2, Ya = 3, Ci = 4, Ms = 1e-6;
function jf(e) {
  return "translate(" + e + ",0)";
}
function Gf(e) {
  return "translate(0," + e + ")";
}
function Wf(e) {
  return (t) => +e(t);
}
function qf(e, t) {
  return t = Math.max(0, e.bandwidth() - t * 2) / 2, e.round() && (t = Math.round(t)), (n) => +e(n) + t;
}
function Zf() {
  return !this.__axis;
}
function Fr(e, t) {
  var n = [], i = null, r = null, a = 6, o = 6, s = 3, l = typeof window != "undefined" && window.devicePixelRatio > 1 ? 0 : 0.5, c = e === lr || e === Ci ? -1 : 1, f = e === Ci || e === cr ? "x" : "y", h = e === lr || e === Ya ? jf : Gf;
  function g(x) {
    var y = i == null ? t.ticks ? t.ticks.apply(t, n) : t.domain() : i, T = r == null ? t.tickFormat ? t.tickFormat.apply(t, n) : Uf : r, A = Math.max(a, 0) + s, w = t.range(), C = +w[0] + l, R = +w[w.length - 1] + l, S = (t.bandwidth ? qf : Wf)(t.copy(), l), I = x.selection ? x.selection() : x, L = I.selectAll(".domain").data([null]), p = I.selectAll(".tick").data(y, t).order(), U = p.exit(), H = p.enter().append("g").attr("class", "tick"), q = p.select("line"), X = p.select("text");
    L = L.merge(L.enter().insert("path", ".tick").attr("class", "domain").attr("stroke", "currentColor")), p = p.merge(H), q = q.merge(H.append("line").attr("stroke", "currentColor").attr(f + "2", c * a)), X = X.merge(H.append("text").attr("fill", "currentColor").attr(f, c * A).attr("dy", e === lr ? "0em" : e === Ya ? "0.71em" : "0.32em")), x !== I && (L = L.transition(x), p = p.transition(x), q = q.transition(x), X = X.transition(x), U = U.transition(x).attr("opacity", Ms).attr("transform", function(j) {
      return isFinite(j = S(j)) ? h(j + l) : this.getAttribute("transform");
    }), H.attr("opacity", Ms).attr("transform", function(j) {
      var z = this.parentNode.__axis;
      return h((z && isFinite(z = z(j)) ? z : S(j)) + l);
    })), U.remove(), L.attr("d", e === Ci || e === cr ? o ? "M" + c * o + "," + C + "H" + l + "V" + R + "H" + c * o : "M" + l + "," + C + "V" + R : o ? "M" + C + "," + c * o + "V" + l + "H" + R + "V" + c * o : "M" + C + "," + l + "H" + R), p.attr("opacity", 1).attr("transform", function(j) {
      return h(S(j) + l);
    }), q.attr(f + "2", c * a), X.attr(f, c * A).text(T), I.filter(Zf).attr("fill", "none").attr("font-size", 10).attr("font-family", "sans-serif").attr("text-anchor", e === cr ? "start" : e === Ci ? "end" : "middle"), I.each(function() {
      this.__axis = S;
    });
  }
  return g.scale = function(x) {
    return arguments.length ? (t = x, g) : t;
  }, g.ticks = function() {
    return n = Array.from(arguments), g;
  }, g.tickArguments = function(x) {
    return arguments.length ? (n = x == null ? [] : Array.from(x), g) : n.slice();
  }, g.tickValues = function(x) {
    return arguments.length ? (i = x == null ? null : Array.from(x), g) : i && i.slice();
  }, g.tickFormat = function(x) {
    return arguments.length ? (r = x, g) : r;
  }, g.tickSize = function(x) {
    return arguments.length ? (a = o = +x, g) : a;
  }, g.tickSizeInner = function(x) {
    return arguments.length ? (a = +x, g) : a;
  }, g.tickSizeOuter = function(x) {
    return arguments.length ? (o = +x, g) : o;
  }, g.tickPadding = function(x) {
    return arguments.length ? (s = +x, g) : s;
  }, g.offset = function(x) {
    return arguments.length ? (l = +x, g) : l;
  }, g;
}
function Kf(e) {
  return Fr(lr, e);
}
function Qf(e) {
  return Fr(cr, e);
}
function Ls(e) {
  return Fr(Ya, e);
}
function Ds(e) {
  return Fr(Ci, e);
}
var Jf = { value: () => {
} };
function ho() {
  for (var e = 0, t = arguments.length, n = {}, i; e < t; ++e) {
    if (!(i = arguments[e] + "") || i in n || /[\s.]/.test(i))
      throw new Error("illegal type: " + i);
    n[i] = [];
  }
  return new fr(n);
}
function fr(e) {
  this._ = e;
}
function ed(e, t) {
  return e.trim().split(/^|\s+/).map(function(n) {
    var i = "", r = n.indexOf(".");
    if (r >= 0 && (i = n.slice(r + 1), n = n.slice(0, r)), n && !t.hasOwnProperty(n))
      throw new Error("unknown type: " + n);
    return { type: n, name: i };
  });
}
fr.prototype = ho.prototype = {
  constructor: fr,
  on: function(e, t) {
    var n = this._, i = ed(e + "", n), r, a = -1, o = i.length;
    if (arguments.length < 2) {
      for (; ++a < o; )
        if ((r = (e = i[a]).type) && (r = td(n[r], e.name)))
          return r;
      return;
    }
    if (t != null && typeof t != "function")
      throw new Error("invalid callback: " + t);
    for (; ++a < o; )
      if (r = (e = i[a]).type)
        n[r] = Is(n[r], e.name, t);
      else if (t == null)
        for (r in n)
          n[r] = Is(n[r], e.name, null);
    return this;
  },
  copy: function() {
    var e = {}, t = this._;
    for (var n in t)
      e[n] = t[n].slice();
    return new fr(e);
  },
  call: function(e, t) {
    if ((r = arguments.length - 2) > 0)
      for (var n = new Array(r), i = 0, r, a; i < r; ++i)
        n[i] = arguments[i + 2];
    if (!this._.hasOwnProperty(e))
      throw new Error("unknown type: " + e);
    for (a = this._[e], i = 0, r = a.length; i < r; ++i)
      a[i].value.apply(t, n);
  },
  apply: function(e, t, n) {
    if (!this._.hasOwnProperty(e))
      throw new Error("unknown type: " + e);
    for (var i = this._[e], r = 0, a = i.length; r < a; ++r)
      i[r].value.apply(t, n);
  }
};
function td(e, t) {
  for (var n = 0, i = e.length, r; n < i; ++n)
    if ((r = e[n]).name === t)
      return r.value;
}
function Is(e, t, n) {
  for (var i = 0, r = e.length; i < r; ++i)
    if (e[i].name === t) {
      e[i] = Jf, e = e.slice(0, i).concat(e.slice(i + 1));
      break;
    }
  return n != null && e.push({ name: t, value: n }), e;
}
var Ua = "http://www.w3.org/1999/xhtml";
const Jn = {
  svg: "http://www.w3.org/2000/svg",
  xhtml: Ua,
  xlink: "http://www.w3.org/1999/xlink",
  xml: "http://www.w3.org/XML/1998/namespace",
  xmlns: "http://www.w3.org/2000/xmlns/"
};
function Br(e) {
  var t = e += "", n = t.indexOf(":");
  return n >= 0 && (t = e.slice(0, n)) !== "xmlns" && (e = e.slice(n + 1)), Jn.hasOwnProperty(t) ? { space: Jn[t], local: e } : e;
}
function nd(e) {
  return function() {
    var t = this.ownerDocument, n = this.namespaceURI;
    return n === Ua && t.documentElement.namespaceURI === Ua ? t.createElement(e) : t.createElementNS(n, e);
  };
}
function id(e) {
  return function() {
    return this.ownerDocument.createElementNS(e.space, e.local);
  };
}
function Hu(e) {
  var t = Br(e);
  return (t.local ? id : nd)(t);
}
function rd() {
}
function go(e) {
  return e == null ? rd : function() {
    return this.querySelector(e);
  };
}
function ad(e) {
  typeof e != "function" && (e = go(e));
  for (var t = this._groups, n = t.length, i = new Array(n), r = 0; r < n; ++r)
    for (var a = t[r], o = a.length, s = i[r] = new Array(o), l, c, f = 0; f < o; ++f)
      (l = a[f]) && (c = e.call(l, l.__data__, f, a)) && ("__data__" in l && (c.__data__ = l.__data__), s[f] = c);
  return new yt(i, this._parents);
}
function Vu(e) {
  return e == null ? [] : Array.isArray(e) ? e : Array.from(e);
}
function od() {
  return [];
}
function Yu(e) {
  return e == null ? od : function() {
    return this.querySelectorAll(e);
  };
}
function sd(e) {
  return function() {
    return Vu(e.apply(this, arguments));
  };
}
function ud(e) {
  typeof e == "function" ? e = sd(e) : e = Yu(e);
  for (var t = this._groups, n = t.length, i = [], r = [], a = 0; a < n; ++a)
    for (var o = t[a], s = o.length, l, c = 0; c < s; ++c)
      (l = o[c]) && (i.push(e.call(l, l.__data__, c, o)), r.push(l));
  return new yt(i, r);
}
function Uu(e) {
  return function() {
    return this.matches(e);
  };
}
function ju(e) {
  return function(t) {
    return t.matches(e);
  };
}
var ld = Array.prototype.find;
function cd(e) {
  return function() {
    return ld.call(this.children, e);
  };
}
function fd() {
  return this.firstElementChild;
}
function dd(e) {
  return this.select(e == null ? fd : cd(typeof e == "function" ? e : ju(e)));
}
var hd = Array.prototype.filter;
function gd() {
  return Array.from(this.children);
}
function pd(e) {
  return function() {
    return hd.call(this.children, e);
  };
}
function vd(e) {
  return this.selectAll(e == null ? gd : pd(typeof e == "function" ? e : ju(e)));
}
function xd(e) {
  typeof e != "function" && (e = Uu(e));
  for (var t = this._groups, n = t.length, i = new Array(n), r = 0; r < n; ++r)
    for (var a = t[r], o = a.length, s = i[r] = [], l, c = 0; c < o; ++c)
      (l = a[c]) && e.call(l, l.__data__, c, a) && s.push(l);
  return new yt(i, this._parents);
}
function Gu(e) {
  return new Array(e.length);
}
function _d() {
  return new yt(this._enter || this._groups.map(Gu), this._parents);
}
function _r(e, t) {
  this.ownerDocument = e.ownerDocument, this.namespaceURI = e.namespaceURI, this._next = null, this._parent = e, this.__data__ = t;
}
_r.prototype = {
  constructor: _r,
  appendChild: function(e) {
    return this._parent.insertBefore(e, this._next);
  },
  insertBefore: function(e, t) {
    return this._parent.insertBefore(e, t);
  },
  querySelector: function(e) {
    return this._parent.querySelector(e);
  },
  querySelectorAll: function(e) {
    return this._parent.querySelectorAll(e);
  }
};
function yd(e) {
  return function() {
    return e;
  };
}
function md(e, t, n, i, r, a) {
  for (var o = 0, s, l = t.length, c = a.length; o < c; ++o)
    (s = t[o]) ? (s.__data__ = a[o], i[o] = s) : n[o] = new _r(e, a[o]);
  for (; o < l; ++o)
    (s = t[o]) && (r[o] = s);
}
function bd(e, t, n, i, r, a, o) {
  var s, l, c = /* @__PURE__ */ new Map(), f = t.length, h = a.length, g = new Array(f), x;
  for (s = 0; s < f; ++s)
    (l = t[s]) && (g[s] = x = o.call(l, l.__data__, s, t) + "", c.has(x) ? r[s] = l : c.set(x, l));
  for (s = 0; s < h; ++s)
    x = o.call(e, a[s], s, a) + "", (l = c.get(x)) ? (i[s] = l, l.__data__ = a[s], c.delete(x)) : n[s] = new _r(e, a[s]);
  for (s = 0; s < f; ++s)
    (l = t[s]) && c.get(g[s]) === l && (r[s] = l);
}
function Td(e) {
  return e.__data__;
}
function wd(e, t) {
  if (!arguments.length)
    return Array.from(this, Td);
  var n = t ? bd : md, i = this._parents, r = this._groups;
  typeof e != "function" && (e = yd(e));
  for (var a = r.length, o = new Array(a), s = new Array(a), l = new Array(a), c = 0; c < a; ++c) {
    var f = i[c], h = r[c], g = h.length, x = $d(e.call(f, f && f.__data__, c, i)), y = x.length, T = s[c] = new Array(y), A = o[c] = new Array(y), w = l[c] = new Array(g);
    n(f, h, T, A, w, x, t);
    for (var C = 0, R = 0, S, I; C < y; ++C)
      if (S = T[C]) {
        for (C >= R && (R = C + 1); !(I = A[R]) && ++R < y; )
          ;
        S._next = I || null;
      }
  }
  return o = new yt(o, i), o._enter = s, o._exit = l, o;
}
function $d(e) {
  return typeof e == "object" && "length" in e ? e : Array.from(e);
}
function Ad() {
  return new yt(this._exit || this._groups.map(Gu), this._parents);
}
function Cd(e, t, n) {
  var i = this.enter(), r = this, a = this.exit();
  return typeof e == "function" ? (i = e(i), i && (i = i.selection())) : i = i.append(e + ""), t != null && (r = t(r), r && (r = r.selection())), n == null ? a.remove() : n(a), i && r ? i.merge(r).order() : r;
}
function Sd(e) {
  for (var t = e.selection ? e.selection() : e, n = this._groups, i = t._groups, r = n.length, a = i.length, o = Math.min(r, a), s = new Array(r), l = 0; l < o; ++l)
    for (var c = n[l], f = i[l], h = c.length, g = s[l] = new Array(h), x, y = 0; y < h; ++y)
      (x = c[y] || f[y]) && (g[y] = x);
  for (; l < r; ++l)
    s[l] = n[l];
  return new yt(s, this._parents);
}
function kd() {
  for (var e = this._groups, t = -1, n = e.length; ++t < n; )
    for (var i = e[t], r = i.length - 1, a = i[r], o; --r >= 0; )
      (o = i[r]) && (a && o.compareDocumentPosition(a) ^ 4 && a.parentNode.insertBefore(o, a), a = o);
  return this;
}
function Ed(e) {
  e || (e = Rd);
  function t(h, g) {
    return h && g ? e(h.__data__, g.__data__) : !h - !g;
  }
  for (var n = this._groups, i = n.length, r = new Array(i), a = 0; a < i; ++a) {
    for (var o = n[a], s = o.length, l = r[a] = new Array(s), c, f = 0; f < s; ++f)
      (c = o[f]) && (l[f] = c);
    l.sort(t);
  }
  return new yt(r, this._parents).order();
}
function Rd(e, t) {
  return e < t ? -1 : e > t ? 1 : e >= t ? 0 : NaN;
}
function Md() {
  var e = arguments[0];
  return arguments[0] = this, e.apply(null, arguments), this;
}
function Ld() {
  return Array.from(this);
}
function Dd() {
  for (var e = this._groups, t = 0, n = e.length; t < n; ++t)
    for (var i = e[t], r = 0, a = i.length; r < a; ++r) {
      var o = i[r];
      if (o)
        return o;
    }
  return null;
}
function Id() {
  let e = 0;
  for (const t of this)
    ++e;
  return e;
}
function Nd() {
  return !this.node();
}
function Pd(e) {
  for (var t = this._groups, n = 0, i = t.length; n < i; ++n)
    for (var r = t[n], a = 0, o = r.length, s; a < o; ++a)
      (s = r[a]) && e.call(s, s.__data__, a, r);
  return this;
}
function Od(e) {
  return function() {
    this.removeAttribute(e);
  };
}
function Fd(e) {
  return function() {
    this.removeAttributeNS(e.space, e.local);
  };
}
function Bd(e, t) {
  return function() {
    this.setAttribute(e, t);
  };
}
function Xd(e, t) {
  return function() {
    this.setAttributeNS(e.space, e.local, t);
  };
}
function zd(e, t) {
  return function() {
    var n = t.apply(this, arguments);
    n == null ? this.removeAttribute(e) : this.setAttribute(e, n);
  };
}
function Hd(e, t) {
  return function() {
    var n = t.apply(this, arguments);
    n == null ? this.removeAttributeNS(e.space, e.local) : this.setAttributeNS(e.space, e.local, n);
  };
}
function Vd(e, t) {
  var n = Br(e);
  if (arguments.length < 2) {
    var i = this.node();
    return n.local ? i.getAttributeNS(n.space, n.local) : i.getAttribute(n);
  }
  return this.each((t == null ? n.local ? Fd : Od : typeof t == "function" ? n.local ? Hd : zd : n.local ? Xd : Bd)(n, t));
}
function Wu(e) {
  return e.ownerDocument && e.ownerDocument.defaultView || e.document && e || e.defaultView;
}
function Yd(e) {
  return function() {
    this.style.removeProperty(e);
  };
}
function Ud(e, t, n) {
  return function() {
    this.style.setProperty(e, t, n);
  };
}
function jd(e, t, n) {
  return function() {
    var i = t.apply(this, arguments);
    i == null ? this.style.removeProperty(e) : this.style.setProperty(e, i, n);
  };
}
function Gd(e, t, n) {
  return arguments.length > 1 ? this.each((t == null ? Yd : typeof t == "function" ? jd : Ud)(e, t, n == null ? "" : n)) : ei(this.node(), e);
}
function ei(e, t) {
  return e.style.getPropertyValue(t) || Wu(e).getComputedStyle(e, null).getPropertyValue(t);
}
function Wd(e) {
  return function() {
    delete this[e];
  };
}
function qd(e, t) {
  return function() {
    this[e] = t;
  };
}
function Zd(e, t) {
  return function() {
    var n = t.apply(this, arguments);
    n == null ? delete this[e] : this[e] = n;
  };
}
function Kd(e, t) {
  return arguments.length > 1 ? this.each((t == null ? Wd : typeof t == "function" ? Zd : qd)(e, t)) : this.node()[e];
}
function qu(e) {
  return e.trim().split(/^|\s+/);
}
function po(e) {
  return e.classList || new Zu(e);
}
function Zu(e) {
  this._node = e, this._names = qu(e.getAttribute("class") || "");
}
Zu.prototype = {
  add: function(e) {
    var t = this._names.indexOf(e);
    t < 0 && (this._names.push(e), this._node.setAttribute("class", this._names.join(" ")));
  },
  remove: function(e) {
    var t = this._names.indexOf(e);
    t >= 0 && (this._names.splice(t, 1), this._node.setAttribute("class", this._names.join(" ")));
  },
  contains: function(e) {
    return this._names.indexOf(e) >= 0;
  }
};
function Ku(e, t) {
  for (var n = po(e), i = -1, r = t.length; ++i < r; )
    n.add(t[i]);
}
function Qu(e, t) {
  for (var n = po(e), i = -1, r = t.length; ++i < r; )
    n.remove(t[i]);
}
function Qd(e) {
  return function() {
    Ku(this, e);
  };
}
function Jd(e) {
  return function() {
    Qu(this, e);
  };
}
function eh(e, t) {
  return function() {
    (t.apply(this, arguments) ? Ku : Qu)(this, e);
  };
}
function th(e, t) {
  var n = qu(e + "");
  if (arguments.length < 2) {
    for (var i = po(this.node()), r = -1, a = n.length; ++r < a; )
      if (!i.contains(n[r]))
        return !1;
    return !0;
  }
  return this.each((typeof t == "function" ? eh : t ? Qd : Jd)(n, t));
}
function nh() {
  this.textContent = "";
}
function ih(e) {
  return function() {
    this.textContent = e;
  };
}
function rh(e) {
  return function() {
    var t = e.apply(this, arguments);
    this.textContent = t == null ? "" : t;
  };
}
function ah(e) {
  return arguments.length ? this.each(e == null ? nh : (typeof e == "function" ? rh : ih)(e)) : this.node().textContent;
}
function oh() {
  this.innerHTML = "";
}
function sh(e) {
  return function() {
    this.innerHTML = e;
  };
}
function uh(e) {
  return function() {
    var t = e.apply(this, arguments);
    this.innerHTML = t == null ? "" : t;
  };
}
function lh(e) {
  return arguments.length ? this.each(e == null ? oh : (typeof e == "function" ? uh : sh)(e)) : this.node().innerHTML;
}
function ch() {
  this.nextSibling && this.parentNode.appendChild(this);
}
function fh() {
  return this.each(ch);
}
function dh() {
  this.previousSibling && this.parentNode.insertBefore(this, this.parentNode.firstChild);
}
function hh() {
  return this.each(dh);
}
function gh(e) {
  var t = typeof e == "function" ? e : Hu(e);
  return this.select(function() {
    return this.appendChild(t.apply(this, arguments));
  });
}
function ph() {
  return null;
}
function vh(e, t) {
  var n = typeof e == "function" ? e : Hu(e), i = t == null ? ph : typeof t == "function" ? t : go(t);
  return this.select(function() {
    return this.insertBefore(n.apply(this, arguments), i.apply(this, arguments) || null);
  });
}
function xh() {
  var e = this.parentNode;
  e && e.removeChild(this);
}
function _h() {
  return this.each(xh);
}
function yh() {
  var e = this.cloneNode(!1), t = this.parentNode;
  return t ? t.insertBefore(e, this.nextSibling) : e;
}
function mh() {
  var e = this.cloneNode(!0), t = this.parentNode;
  return t ? t.insertBefore(e, this.nextSibling) : e;
}
function bh(e) {
  return this.select(e ? mh : yh);
}
function Th(e) {
  return arguments.length ? this.property("__data__", e) : this.node().__data__;
}
function wh(e) {
  return function(t) {
    e.call(this, t, this.__data__);
  };
}
function $h(e) {
  return e.trim().split(/^|\s+/).map(function(t) {
    var n = "", i = t.indexOf(".");
    return i >= 0 && (n = t.slice(i + 1), t = t.slice(0, i)), { type: t, name: n };
  });
}
function Ah(e) {
  return function() {
    var t = this.__on;
    if (t) {
      for (var n = 0, i = -1, r = t.length, a; n < r; ++n)
        a = t[n], (!e.type || a.type === e.type) && a.name === e.name ? this.removeEventListener(a.type, a.listener, a.options) : t[++i] = a;
      ++i ? t.length = i : delete this.__on;
    }
  };
}
function Ch(e, t, n) {
  return function() {
    var i = this.__on, r, a = wh(t);
    if (i) {
      for (var o = 0, s = i.length; o < s; ++o)
        if ((r = i[o]).type === e.type && r.name === e.name) {
          this.removeEventListener(r.type, r.listener, r.options), this.addEventListener(r.type, r.listener = a, r.options = n), r.value = t;
          return;
        }
    }
    this.addEventListener(e.type, a, n), r = { type: e.type, name: e.name, value: t, listener: a, options: n }, i ? i.push(r) : this.__on = [r];
  };
}
function Sh(e, t, n) {
  var i = $h(e + ""), r, a = i.length, o;
  if (arguments.length < 2) {
    var s = this.node().__on;
    if (s) {
      for (var l = 0, c = s.length, f; l < c; ++l)
        for (r = 0, f = s[l]; r < a; ++r)
          if ((o = i[r]).type === f.type && o.name === f.name)
            return f.value;
    }
    return;
  }
  for (s = t ? Ch : Ah, r = 0; r < a; ++r)
    this.each(s(i[r], t, n));
  return this;
}
function Ju(e, t, n) {
  var i = Wu(e), r = i.CustomEvent;
  typeof r == "function" ? r = new r(t, n) : (r = i.document.createEvent("Event"), n ? (r.initEvent(t, n.bubbles, n.cancelable), r.detail = n.detail) : r.initEvent(t, !1, !1)), e.dispatchEvent(r);
}
function kh(e, t) {
  return function() {
    return Ju(this, e, t);
  };
}
function Eh(e, t) {
  return function() {
    return Ju(this, e, t.apply(this, arguments));
  };
}
function Rh(e, t) {
  return this.each((typeof t == "function" ? Eh : kh)(e, t));
}
function* Mh() {
  for (var e = this._groups, t = 0, n = e.length; t < n; ++t)
    for (var i = e[t], r = 0, a = i.length, o; r < a; ++r)
      (o = i[r]) && (yield o);
}
var vo = [null];
function yt(e, t) {
  this._groups = e, this._parents = t;
}
function ui() {
  return new yt([[document.documentElement]], vo);
}
function Lh() {
  return this;
}
yt.prototype = ui.prototype = {
  constructor: yt,
  select: ad,
  selectAll: ud,
  selectChild: dd,
  selectChildren: vd,
  filter: xd,
  data: wd,
  enter: _d,
  exit: Ad,
  join: Cd,
  merge: Sd,
  selection: Lh,
  order: kd,
  sort: Ed,
  call: Md,
  nodes: Ld,
  node: Dd,
  size: Id,
  empty: Nd,
  each: Pd,
  attr: Vd,
  style: Gd,
  property: Kd,
  classed: th,
  text: ah,
  html: lh,
  raise: fh,
  lower: hh,
  append: gh,
  insert: vh,
  remove: _h,
  clone: bh,
  datum: Th,
  on: Sh,
  dispatch: Rh,
  [Symbol.iterator]: Mh
};
function le(e) {
  return typeof e == "string" ? new yt([[document.querySelector(e)]], [document.documentElement]) : new yt([[e]], vo);
}
function Dh(e) {
  let t;
  for (; t = e.sourceEvent; )
    e = t;
  return e;
}
function ja(e, t) {
  if (e = Dh(e), t === void 0 && (t = e.currentTarget), t) {
    var n = t.ownerSVGElement || t;
    if (n.createSVGPoint) {
      var i = n.createSVGPoint();
      return i.x = e.clientX, i.y = e.clientY, i = i.matrixTransform(t.getScreenCTM().inverse()), [i.x, i.y];
    }
    if (t.getBoundingClientRect) {
      var r = t.getBoundingClientRect();
      return [e.clientX - r.left - t.clientLeft, e.clientY - r.top - t.clientTop];
    }
  }
  return [e.pageX, e.pageY];
}
function el(e) {
  return typeof e == "string" ? new yt([document.querySelectorAll(e)], [document.documentElement]) : new yt([Vu(e)], vo);
}
const Ih = { passive: !1 }, Mi = { capture: !0, passive: !1 };
function ya(e) {
  e.stopImmediatePropagation();
}
function Kn(e) {
  e.preventDefault(), e.stopImmediatePropagation();
}
function Nh(e) {
  var t = e.document.documentElement, n = le(e).on("dragstart.drag", Kn, Mi);
  "onselectstart" in t ? n.on("selectstart.drag", Kn, Mi) : (t.__noselect = t.style.MozUserSelect, t.style.MozUserSelect = "none");
}
function Ph(e, t) {
  var n = e.document.documentElement, i = le(e).on("dragstart.drag", null);
  t && (i.on("click.drag", Kn, Mi), setTimeout(function() {
    i.on("click.drag", null);
  }, 0)), "onselectstart" in n ? i.on("selectstart.drag", null) : (n.style.MozUserSelect = n.__noselect, delete n.__noselect);
}
const Ji = (e) => () => e;
function Ga(e, {
  sourceEvent: t,
  subject: n,
  target: i,
  identifier: r,
  active: a,
  x: o,
  y: s,
  dx: l,
  dy: c,
  dispatch: f
}) {
  Object.defineProperties(this, {
    type: { value: e, enumerable: !0, configurable: !0 },
    sourceEvent: { value: t, enumerable: !0, configurable: !0 },
    subject: { value: n, enumerable: !0, configurable: !0 },
    target: { value: i, enumerable: !0, configurable: !0 },
    identifier: { value: r, enumerable: !0, configurable: !0 },
    active: { value: a, enumerable: !0, configurable: !0 },
    x: { value: o, enumerable: !0, configurable: !0 },
    y: { value: s, enumerable: !0, configurable: !0 },
    dx: { value: l, enumerable: !0, configurable: !0 },
    dy: { value: c, enumerable: !0, configurable: !0 },
    _: { value: f }
  });
}
Ga.prototype.on = function() {
  var e = this._.on.apply(this._, arguments);
  return e === this._ ? this : e;
};
function Oh(e) {
  return !e.ctrlKey && !e.button;
}
function Fh() {
  return this.parentNode;
}
function Bh(e, t) {
  return t == null ? { x: e.x, y: e.y } : t;
}
function Xh() {
  return navigator.maxTouchPoints || "ontouchstart" in this;
}
function zh() {
  var e = Oh, t = Fh, n = Bh, i = Xh, r = {}, a = ho("start", "drag", "end"), o = 0, s, l, c, f, h = 0;
  function g(S) {
    S.on("mousedown.drag", x).filter(i).on("touchstart.drag", A).on("touchmove.drag", w, Ih).on("touchend.drag touchcancel.drag", C).style("touch-action", "none").style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
  }
  function x(S, I) {
    if (!(f || !e.call(this, S, I))) {
      var L = R(this, t.call(this, S, I), S, I, "mouse");
      L && (le(S.view).on("mousemove.drag", y, Mi).on("mouseup.drag", T, Mi), Nh(S.view), ya(S), c = !1, s = S.clientX, l = S.clientY, L("start", S));
    }
  }
  function y(S) {
    if (Kn(S), !c) {
      var I = S.clientX - s, L = S.clientY - l;
      c = I * I + L * L > h;
    }
    r.mouse("drag", S);
  }
  function T(S) {
    le(S.view).on("mousemove.drag mouseup.drag", null), Ph(S.view, c), Kn(S), r.mouse("end", S);
  }
  function A(S, I) {
    if (e.call(this, S, I)) {
      var L = S.changedTouches, p = t.call(this, S, I), U = L.length, H, q;
      for (H = 0; H < U; ++H)
        (q = R(this, p, S, I, L[H].identifier, L[H])) && (ya(S), q("start", S, L[H]));
    }
  }
  function w(S) {
    var I = S.changedTouches, L = I.length, p, U;
    for (p = 0; p < L; ++p)
      (U = r[I[p].identifier]) && (Kn(S), U("drag", S, I[p]));
  }
  function C(S) {
    var I = S.changedTouches, L = I.length, p, U;
    for (f && clearTimeout(f), f = setTimeout(function() {
      f = null;
    }, 500), p = 0; p < L; ++p)
      (U = r[I[p].identifier]) && (ya(S), U("end", S, I[p]));
  }
  function R(S, I, L, p, U, H) {
    var q = a.copy(), X = ja(H || L, I), j, z, G;
    if ((G = n.call(S, new Ga("beforestart", {
      sourceEvent: L,
      target: g,
      identifier: U,
      active: o,
      x: X[0],
      y: X[1],
      dx: 0,
      dy: 0,
      dispatch: q
    }), p)) != null)
      return j = G.x - X[0] || 0, z = G.y - X[1] || 0, function _e(Ue, te, re) {
        var ue = X, we;
        switch (Ue) {
          case "start":
            r[U] = _e, we = o++;
            break;
          case "end":
            delete r[U], --o;
          case "drag":
            X = ja(re || te, I), we = o;
            break;
        }
        q.call(
          Ue,
          S,
          new Ga(Ue, {
            sourceEvent: te,
            subject: G,
            target: g,
            identifier: U,
            active: we,
            x: X[0] + j,
            y: X[1] + z,
            dx: X[0] - ue[0],
            dy: X[1] - ue[1],
            dispatch: q
          }),
          p
        );
      };
  }
  return g.filter = function(S) {
    return arguments.length ? (e = typeof S == "function" ? S : Ji(!!S), g) : e;
  }, g.container = function(S) {
    return arguments.length ? (t = typeof S == "function" ? S : Ji(S), g) : t;
  }, g.subject = function(S) {
    return arguments.length ? (n = typeof S == "function" ? S : Ji(S), g) : n;
  }, g.touchable = function(S) {
    return arguments.length ? (i = typeof S == "function" ? S : Ji(!!S), g) : i;
  }, g.on = function() {
    var S = a.on.apply(a, arguments);
    return S === a ? g : S;
  }, g.clickDistance = function(S) {
    return arguments.length ? (h = (S = +S) * S, g) : Math.sqrt(h);
  }, g;
}
function xo(e, t, n) {
  e.prototype = t.prototype = n, n.constructor = e;
}
function tl(e, t) {
  var n = Object.create(e.prototype);
  for (var i in t)
    n[i] = t[i];
  return n;
}
function Bi() {
}
var Li = 0.7, yr = 1 / Li, Qn = "\\s*([+-]?\\d+)\\s*", Di = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*", Kt = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*", Hh = /^#([0-9a-f]{3,8})$/, Vh = new RegExp(`^rgb\\(${Qn},${Qn},${Qn}\\)$`), Yh = new RegExp(`^rgb\\(${Kt},${Kt},${Kt}\\)$`), Uh = new RegExp(`^rgba\\(${Qn},${Qn},${Qn},${Di}\\)$`), jh = new RegExp(`^rgba\\(${Kt},${Kt},${Kt},${Di}\\)$`), Gh = new RegExp(`^hsl\\(${Di},${Kt},${Kt}\\)$`), Wh = new RegExp(`^hsla\\(${Di},${Kt},${Kt},${Di}\\)$`), Ns = {
  aliceblue: 15792383,
  antiquewhite: 16444375,
  aqua: 65535,
  aquamarine: 8388564,
  azure: 15794175,
  beige: 16119260,
  bisque: 16770244,
  black: 0,
  blanchedalmond: 16772045,
  blue: 255,
  blueviolet: 9055202,
  brown: 10824234,
  burlywood: 14596231,
  cadetblue: 6266528,
  chartreuse: 8388352,
  chocolate: 13789470,
  coral: 16744272,
  cornflowerblue: 6591981,
  cornsilk: 16775388,
  crimson: 14423100,
  cyan: 65535,
  darkblue: 139,
  darkcyan: 35723,
  darkgoldenrod: 12092939,
  darkgray: 11119017,
  darkgreen: 25600,
  darkgrey: 11119017,
  darkkhaki: 12433259,
  darkmagenta: 9109643,
  darkolivegreen: 5597999,
  darkorange: 16747520,
  darkorchid: 10040012,
  darkred: 9109504,
  darksalmon: 15308410,
  darkseagreen: 9419919,
  darkslateblue: 4734347,
  darkslategray: 3100495,
  darkslategrey: 3100495,
  darkturquoise: 52945,
  darkviolet: 9699539,
  deeppink: 16716947,
  deepskyblue: 49151,
  dimgray: 6908265,
  dimgrey: 6908265,
  dodgerblue: 2003199,
  firebrick: 11674146,
  floralwhite: 16775920,
  forestgreen: 2263842,
  fuchsia: 16711935,
  gainsboro: 14474460,
  ghostwhite: 16316671,
  gold: 16766720,
  goldenrod: 14329120,
  gray: 8421504,
  green: 32768,
  greenyellow: 11403055,
  grey: 8421504,
  honeydew: 15794160,
  hotpink: 16738740,
  indianred: 13458524,
  indigo: 4915330,
  ivory: 16777200,
  khaki: 15787660,
  lavender: 15132410,
  lavenderblush: 16773365,
  lawngreen: 8190976,
  lemonchiffon: 16775885,
  lightblue: 11393254,
  lightcoral: 15761536,
  lightcyan: 14745599,
  lightgoldenrodyellow: 16448210,
  lightgray: 13882323,
  lightgreen: 9498256,
  lightgrey: 13882323,
  lightpink: 16758465,
  lightsalmon: 16752762,
  lightseagreen: 2142890,
  lightskyblue: 8900346,
  lightslategray: 7833753,
  lightslategrey: 7833753,
  lightsteelblue: 11584734,
  lightyellow: 16777184,
  lime: 65280,
  limegreen: 3329330,
  linen: 16445670,
  magenta: 16711935,
  maroon: 8388608,
  mediumaquamarine: 6737322,
  mediumblue: 205,
  mediumorchid: 12211667,
  mediumpurple: 9662683,
  mediumseagreen: 3978097,
  mediumslateblue: 8087790,
  mediumspringgreen: 64154,
  mediumturquoise: 4772300,
  mediumvioletred: 13047173,
  midnightblue: 1644912,
  mintcream: 16121850,
  mistyrose: 16770273,
  moccasin: 16770229,
  navajowhite: 16768685,
  navy: 128,
  oldlace: 16643558,
  olive: 8421376,
  olivedrab: 7048739,
  orange: 16753920,
  orangered: 16729344,
  orchid: 14315734,
  palegoldenrod: 15657130,
  palegreen: 10025880,
  paleturquoise: 11529966,
  palevioletred: 14381203,
  papayawhip: 16773077,
  peachpuff: 16767673,
  peru: 13468991,
  pink: 16761035,
  plum: 14524637,
  powderblue: 11591910,
  purple: 8388736,
  rebeccapurple: 6697881,
  red: 16711680,
  rosybrown: 12357519,
  royalblue: 4286945,
  saddlebrown: 9127187,
  salmon: 16416882,
  sandybrown: 16032864,
  seagreen: 3050327,
  seashell: 16774638,
  sienna: 10506797,
  silver: 12632256,
  skyblue: 8900331,
  slateblue: 6970061,
  slategray: 7372944,
  slategrey: 7372944,
  snow: 16775930,
  springgreen: 65407,
  steelblue: 4620980,
  tan: 13808780,
  teal: 32896,
  thistle: 14204888,
  tomato: 16737095,
  turquoise: 4251856,
  violet: 15631086,
  wheat: 16113331,
  white: 16777215,
  whitesmoke: 16119285,
  yellow: 16776960,
  yellowgreen: 10145074
};
xo(Bi, Dn, {
  copy(e) {
    return Object.assign(new this.constructor(), this, e);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: Ps,
  // Deprecated! Use color.formatHex.
  formatHex: Ps,
  formatHex8: qh,
  formatHsl: Zh,
  formatRgb: Os,
  toString: Os
});
function Ps() {
  return this.rgb().formatHex();
}
function qh() {
  return this.rgb().formatHex8();
}
function Zh() {
  return nl(this).formatHsl();
}
function Os() {
  return this.rgb().formatRgb();
}
function Dn(e) {
  var t, n;
  return e = (e + "").trim().toLowerCase(), (t = Hh.exec(e)) ? (n = t[1].length, t = parseInt(t[1], 16), n === 6 ? Fs(t) : n === 3 ? new St(t >> 8 & 15 | t >> 4 & 240, t >> 4 & 15 | t & 240, (t & 15) << 4 | t & 15, 1) : n === 8 ? er(t >> 24 & 255, t >> 16 & 255, t >> 8 & 255, (t & 255) / 255) : n === 4 ? er(t >> 12 & 15 | t >> 8 & 240, t >> 8 & 15 | t >> 4 & 240, t >> 4 & 15 | t & 240, ((t & 15) << 4 | t & 15) / 255) : null) : (t = Vh.exec(e)) ? new St(t[1], t[2], t[3], 1) : (t = Yh.exec(e)) ? new St(t[1] * 255 / 100, t[2] * 255 / 100, t[3] * 255 / 100, 1) : (t = Uh.exec(e)) ? er(t[1], t[2], t[3], t[4]) : (t = jh.exec(e)) ? er(t[1] * 255 / 100, t[2] * 255 / 100, t[3] * 255 / 100, t[4]) : (t = Gh.exec(e)) ? zs(t[1], t[2] / 100, t[3] / 100, 1) : (t = Wh.exec(e)) ? zs(t[1], t[2] / 100, t[3] / 100, t[4]) : Ns.hasOwnProperty(e) ? Fs(Ns[e]) : e === "transparent" ? new St(NaN, NaN, NaN, 0) : null;
}
function Fs(e) {
  return new St(e >> 16 & 255, e >> 8 & 255, e & 255, 1);
}
function er(e, t, n, i) {
  return i <= 0 && (e = t = n = NaN), new St(e, t, n, i);
}
function Kh(e) {
  return e instanceof Bi || (e = Dn(e)), e ? (e = e.rgb(), new St(e.r, e.g, e.b, e.opacity)) : new St();
}
function Wa(e, t, n, i) {
  return arguments.length === 1 ? Kh(e) : new St(e, t, n, i == null ? 1 : i);
}
function St(e, t, n, i) {
  this.r = +e, this.g = +t, this.b = +n, this.opacity = +i;
}
xo(St, Wa, tl(Bi, {
  brighter(e) {
    return e = e == null ? yr : Math.pow(yr, e), new St(this.r * e, this.g * e, this.b * e, this.opacity);
  },
  darker(e) {
    return e = e == null ? Li : Math.pow(Li, e), new St(this.r * e, this.g * e, this.b * e, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new St(Ln(this.r), Ln(this.g), Ln(this.b), mr(this.opacity));
  },
  displayable() {
    return -0.5 <= this.r && this.r < 255.5 && -0.5 <= this.g && this.g < 255.5 && -0.5 <= this.b && this.b < 255.5 && 0 <= this.opacity && this.opacity <= 1;
  },
  hex: Bs,
  // Deprecated! Use color.formatHex.
  formatHex: Bs,
  formatHex8: Qh,
  formatRgb: Xs,
  toString: Xs
}));
function Bs() {
  return `#${Rn(this.r)}${Rn(this.g)}${Rn(this.b)}`;
}
function Qh() {
  return `#${Rn(this.r)}${Rn(this.g)}${Rn(this.b)}${Rn((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}
function Xs() {
  const e = mr(this.opacity);
  return `${e === 1 ? "rgb(" : "rgba("}${Ln(this.r)}, ${Ln(this.g)}, ${Ln(this.b)}${e === 1 ? ")" : `, ${e})`}`;
}
function mr(e) {
  return isNaN(e) ? 1 : Math.max(0, Math.min(1, e));
}
function Ln(e) {
  return Math.max(0, Math.min(255, Math.round(e) || 0));
}
function Rn(e) {
  return e = Ln(e), (e < 16 ? "0" : "") + e.toString(16);
}
function zs(e, t, n, i) {
  return i <= 0 ? e = t = n = NaN : n <= 0 || n >= 1 ? e = t = NaN : t <= 0 && (e = NaN), new Bt(e, t, n, i);
}
function nl(e) {
  if (e instanceof Bt)
    return new Bt(e.h, e.s, e.l, e.opacity);
  if (e instanceof Bi || (e = Dn(e)), !e)
    return new Bt();
  if (e instanceof Bt)
    return e;
  e = e.rgb();
  var t = e.r / 255, n = e.g / 255, i = e.b / 255, r = Math.min(t, n, i), a = Math.max(t, n, i), o = NaN, s = a - r, l = (a + r) / 2;
  return s ? (t === a ? o = (n - i) / s + (n < i) * 6 : n === a ? o = (i - t) / s + 2 : o = (t - n) / s + 4, s /= l < 0.5 ? a + r : 2 - a - r, o *= 60) : s = l > 0 && l < 1 ? 0 : o, new Bt(o, s, l, e.opacity);
}
function Jh(e, t, n, i) {
  return arguments.length === 1 ? nl(e) : new Bt(e, t, n, i == null ? 1 : i);
}
function Bt(e, t, n, i) {
  this.h = +e, this.s = +t, this.l = +n, this.opacity = +i;
}
xo(Bt, Jh, tl(Bi, {
  brighter(e) {
    return e = e == null ? yr : Math.pow(yr, e), new Bt(this.h, this.s, this.l * e, this.opacity);
  },
  darker(e) {
    return e = e == null ? Li : Math.pow(Li, e), new Bt(this.h, this.s, this.l * e, this.opacity);
  },
  rgb() {
    var e = this.h % 360 + (this.h < 0) * 360, t = isNaN(e) || isNaN(this.s) ? 0 : this.s, n = this.l, i = n + (n < 0.5 ? n : 1 - n) * t, r = 2 * n - i;
    return new St(
      ma(e >= 240 ? e - 240 : e + 120, r, i),
      ma(e, r, i),
      ma(e < 120 ? e + 240 : e - 120, r, i),
      this.opacity
    );
  },
  clamp() {
    return new Bt(Hs(this.h), tr(this.s), tr(this.l), mr(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && 0 <= this.l && this.l <= 1 && 0 <= this.opacity && this.opacity <= 1;
  },
  formatHsl() {
    const e = mr(this.opacity);
    return `${e === 1 ? "hsl(" : "hsla("}${Hs(this.h)}, ${tr(this.s) * 100}%, ${tr(this.l) * 100}%${e === 1 ? ")" : `, ${e})`}`;
  }
}));
function Hs(e) {
  return e = (e || 0) % 360, e < 0 ? e + 360 : e;
}
function tr(e) {
  return Math.max(0, Math.min(1, e || 0));
}
function ma(e, t, n) {
  return (e < 60 ? t + (n - t) * e / 60 : e < 180 ? n : e < 240 ? t + (n - t) * (240 - e) / 60 : t) * 255;
}
const _o = (e) => () => e;
function eg(e, t) {
  return function(n) {
    return e + n * t;
  };
}
function tg(e, t, n) {
  return e = Math.pow(e, n), t = Math.pow(t, n) - e, n = 1 / n, function(i) {
    return Math.pow(e + i * t, n);
  };
}
function ng(e) {
  return (e = +e) == 1 ? il : function(t, n) {
    return n - t ? tg(t, n, e) : _o(isNaN(t) ? n : t);
  };
}
function il(e, t) {
  var n = t - e;
  return n ? eg(e, n) : _o(isNaN(e) ? t : e);
}
const br = function e(t) {
  var n = ng(t);
  function i(r, a) {
    var o = n((r = Wa(r)).r, (a = Wa(a)).r), s = n(r.g, a.g), l = n(r.b, a.b), c = il(r.opacity, a.opacity);
    return function(f) {
      return r.r = o(f), r.g = s(f), r.b = l(f), r.opacity = c(f), r + "";
    };
  }
  return i.gamma = e, i;
}(1);
function ig(e, t) {
  t || (t = []);
  var n = e ? Math.min(t.length, e.length) : 0, i = t.slice(), r;
  return function(a) {
    for (r = 0; r < n; ++r)
      i[r] = e[r] * (1 - a) + t[r] * a;
    return i;
  };
}
function rg(e) {
  return ArrayBuffer.isView(e) && !(e instanceof DataView);
}
function ag(e, t) {
  var n = t ? t.length : 0, i = e ? Math.min(n, e.length) : 0, r = new Array(i), a = new Array(n), o;
  for (o = 0; o < i; ++o)
    r[o] = Xi(e[o], t[o]);
  for (; o < n; ++o)
    a[o] = t[o];
  return function(s) {
    for (o = 0; o < i; ++o)
      a[o] = r[o](s);
    return a;
  };
}
function og(e, t) {
  var n = /* @__PURE__ */ new Date();
  return e = +e, t = +t, function(i) {
    return n.setTime(e * (1 - i) + t * i), n;
  };
}
function Ft(e, t) {
  return e = +e, t = +t, function(n) {
    return e * (1 - n) + t * n;
  };
}
function sg(e, t) {
  var n = {}, i = {}, r;
  (e === null || typeof e != "object") && (e = {}), (t === null || typeof t != "object") && (t = {});
  for (r in t)
    r in e ? n[r] = Xi(e[r], t[r]) : i[r] = t[r];
  return function(a) {
    for (r in n)
      i[r] = n[r](a);
    return i;
  };
}
var qa = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g, ba = new RegExp(qa.source, "g");
function ug(e) {
  return function() {
    return e;
  };
}
function lg(e) {
  return function(t) {
    return e(t) + "";
  };
}
function rl(e, t) {
  var n = qa.lastIndex = ba.lastIndex = 0, i, r, a, o = -1, s = [], l = [];
  for (e = e + "", t = t + ""; (i = qa.exec(e)) && (r = ba.exec(t)); )
    (a = r.index) > n && (a = t.slice(n, a), s[o] ? s[o] += a : s[++o] = a), (i = i[0]) === (r = r[0]) ? s[o] ? s[o] += r : s[++o] = r : (s[++o] = null, l.push({ i: o, x: Ft(i, r) })), n = ba.lastIndex;
  return n < t.length && (a = t.slice(n), s[o] ? s[o] += a : s[++o] = a), s.length < 2 ? l[0] ? lg(l[0].x) : ug(t) : (t = l.length, function(c) {
    for (var f = 0, h; f < t; ++f)
      s[(h = l[f]).i] = h.x(c);
    return s.join("");
  });
}
function Xi(e, t) {
  var n = typeof t, i;
  return t == null || n === "boolean" ? _o(t) : (n === "number" ? Ft : n === "string" ? (i = Dn(t)) ? (t = i, br) : rl : t instanceof Dn ? br : t instanceof Date ? og : rg(t) ? ig : Array.isArray(t) ? ag : typeof t.valueOf != "function" && typeof t.toString != "function" || isNaN(t) ? sg : Ft)(e, t);
}
function cg(e, t) {
  return e = +e, t = +t, function(n) {
    return Math.round(e * (1 - n) + t * n);
  };
}
var Vs = 180 / Math.PI, Za = {
  translateX: 0,
  translateY: 0,
  rotate: 0,
  skewX: 0,
  scaleX: 1,
  scaleY: 1
};
function al(e, t, n, i, r, a) {
  var o, s, l;
  return (o = Math.sqrt(e * e + t * t)) && (e /= o, t /= o), (l = e * n + t * i) && (n -= e * l, i -= t * l), (s = Math.sqrt(n * n + i * i)) && (n /= s, i /= s, l /= s), e * i < t * n && (e = -e, t = -t, l = -l, o = -o), {
    translateX: r,
    translateY: a,
    rotate: Math.atan2(t, e) * Vs,
    skewX: Math.atan(l) * Vs,
    scaleX: o,
    scaleY: s
  };
}
var nr;
function fg(e) {
  const t = new (typeof DOMMatrix == "function" ? DOMMatrix : WebKitCSSMatrix)(e + "");
  return t.isIdentity ? Za : al(t.a, t.b, t.c, t.d, t.e, t.f);
}
function dg(e) {
  return e == null || (nr || (nr = document.createElementNS("http://www.w3.org/2000/svg", "g")), nr.setAttribute("transform", e), !(e = nr.transform.baseVal.consolidate())) ? Za : (e = e.matrix, al(e.a, e.b, e.c, e.d, e.e, e.f));
}
function ol(e, t, n, i) {
  function r(c) {
    return c.length ? c.pop() + " " : "";
  }
  function a(c, f, h, g, x, y) {
    if (c !== h || f !== g) {
      var T = x.push("translate(", null, t, null, n);
      y.push({ i: T - 4, x: Ft(c, h) }, { i: T - 2, x: Ft(f, g) });
    } else
      (h || g) && x.push("translate(" + h + t + g + n);
  }
  function o(c, f, h, g) {
    c !== f ? (c - f > 180 ? f += 360 : f - c > 180 && (c += 360), g.push({ i: h.push(r(h) + "rotate(", null, i) - 2, x: Ft(c, f) })) : f && h.push(r(h) + "rotate(" + f + i);
  }
  function s(c, f, h, g) {
    c !== f ? g.push({ i: h.push(r(h) + "skewX(", null, i) - 2, x: Ft(c, f) }) : f && h.push(r(h) + "skewX(" + f + i);
  }
  function l(c, f, h, g, x, y) {
    if (c !== h || f !== g) {
      var T = x.push(r(x) + "scale(", null, ",", null, ")");
      y.push({ i: T - 4, x: Ft(c, h) }, { i: T - 2, x: Ft(f, g) });
    } else
      (h !== 1 || g !== 1) && x.push(r(x) + "scale(" + h + "," + g + ")");
  }
  return function(c, f) {
    var h = [], g = [];
    return c = e(c), f = e(f), a(c.translateX, c.translateY, f.translateX, f.translateY, h, g), o(c.rotate, f.rotate, h, g), s(c.skewX, f.skewX, h, g), l(c.scaleX, c.scaleY, f.scaleX, f.scaleY, h, g), c = f = null, function(x) {
      for (var y = -1, T = g.length, A; ++y < T; )
        h[(A = g[y]).i] = A.x(x);
      return h.join("");
    };
  };
}
var hg = ol(fg, "px, ", "px)", "deg)"), gg = ol(dg, ", ", ")", ")"), ti = 0, Si = 0, bi = 0, sl = 1e3, Tr, ki, wr = 0, In = 0, Xr = 0, Ii = typeof performance == "object" && performance.now ? performance : Date, ul = typeof window == "object" && window.requestAnimationFrame ? window.requestAnimationFrame.bind(window) : function(e) {
  setTimeout(e, 17);
};
function yo() {
  return In || (ul(pg), In = Ii.now() + Xr);
}
function pg() {
  In = 0;
}
function $r() {
  this._call = this._time = this._next = null;
}
$r.prototype = ll.prototype = {
  constructor: $r,
  restart: function(e, t, n) {
    if (typeof e != "function")
      throw new TypeError("callback is not a function");
    n = (n == null ? yo() : +n) + (t == null ? 0 : +t), !this._next && ki !== this && (ki ? ki._next = this : Tr = this, ki = this), this._call = e, this._time = n, Ka();
  },
  stop: function() {
    this._call && (this._call = null, this._time = 1 / 0, Ka());
  }
};
function ll(e, t, n) {
  var i = new $r();
  return i.restart(e, t, n), i;
}
function vg() {
  yo(), ++ti;
  for (var e = Tr, t; e; )
    (t = In - e._time) >= 0 && e._call.call(void 0, t), e = e._next;
  --ti;
}
function Ys() {
  In = (wr = Ii.now()) + Xr, ti = Si = 0;
  try {
    vg();
  } finally {
    ti = 0, _g(), In = 0;
  }
}
function xg() {
  var e = Ii.now(), t = e - wr;
  t > sl && (Xr -= t, wr = e);
}
function _g() {
  for (var e, t = Tr, n, i = 1 / 0; t; )
    t._call ? (i > t._time && (i = t._time), e = t, t = t._next) : (n = t._next, t._next = null, t = e ? e._next = n : Tr = n);
  ki = e, Ka(i);
}
function Ka(e) {
  if (!ti) {
    Si && (Si = clearTimeout(Si));
    var t = e - In;
    t > 24 ? (e < 1 / 0 && (Si = setTimeout(Ys, e - Ii.now() - Xr)), bi && (bi = clearInterval(bi))) : (bi || (wr = Ii.now(), bi = setInterval(xg, sl)), ti = 1, ul(Ys));
  }
}
function Us(e, t, n) {
  var i = new $r();
  return t = t == null ? 0 : +t, i.restart((r) => {
    i.stop(), e(r + t);
  }, t, n), i;
}
var yg = ho("start", "end", "cancel", "interrupt"), mg = [], cl = 0, js = 1, Qa = 2, dr = 3, Gs = 4, Ja = 5, hr = 6;
function zr(e, t, n, i, r, a) {
  var o = e.__transition;
  if (!o)
    e.__transition = {};
  else if (n in o)
    return;
  bg(e, n, {
    name: t,
    index: i,
    // For context during callback.
    group: r,
    // For context during callback.
    on: yg,
    tween: mg,
    time: a.time,
    delay: a.delay,
    duration: a.duration,
    ease: a.ease,
    timer: null,
    state: cl
  });
}
function mo(e, t) {
  var n = zt(e, t);
  if (n.state > cl)
    throw new Error("too late; already scheduled");
  return n;
}
function tn(e, t) {
  var n = zt(e, t);
  if (n.state > dr)
    throw new Error("too late; already running");
  return n;
}
function zt(e, t) {
  var n = e.__transition;
  if (!n || !(n = n[t]))
    throw new Error("transition not found");
  return n;
}
function bg(e, t, n) {
  var i = e.__transition, r;
  i[t] = n, n.timer = ll(a, 0, n.time);
  function a(c) {
    n.state = js, n.timer.restart(o, n.delay, n.time), n.delay <= c && o(c - n.delay);
  }
  function o(c) {
    var f, h, g, x;
    if (n.state !== js)
      return l();
    for (f in i)
      if (x = i[f], x.name === n.name) {
        if (x.state === dr)
          return Us(o);
        x.state === Gs ? (x.state = hr, x.timer.stop(), x.on.call("interrupt", e, e.__data__, x.index, x.group), delete i[f]) : +f < t && (x.state = hr, x.timer.stop(), x.on.call("cancel", e, e.__data__, x.index, x.group), delete i[f]);
      }
    if (Us(function() {
      n.state === dr && (n.state = Gs, n.timer.restart(s, n.delay, n.time), s(c));
    }), n.state = Qa, n.on.call("start", e, e.__data__, n.index, n.group), n.state === Qa) {
      for (n.state = dr, r = new Array(g = n.tween.length), f = 0, h = -1; f < g; ++f)
        (x = n.tween[f].value.call(e, e.__data__, n.index, n.group)) && (r[++h] = x);
      r.length = h + 1;
    }
  }
  function s(c) {
    for (var f = c < n.duration ? n.ease.call(null, c / n.duration) : (n.timer.restart(l), n.state = Ja, 1), h = -1, g = r.length; ++h < g; )
      r[h].call(e, f);
    n.state === Ja && (n.on.call("end", e, e.__data__, n.index, n.group), l());
  }
  function l() {
    n.state = hr, n.timer.stop(), delete i[t];
    for (var c in i)
      return;
    delete e.__transition;
  }
}
function Tg(e, t) {
  var n = e.__transition, i, r, a = !0, o;
  if (n) {
    t = t == null ? null : t + "";
    for (o in n) {
      if ((i = n[o]).name !== t) {
        a = !1;
        continue;
      }
      r = i.state > Qa && i.state < Ja, i.state = hr, i.timer.stop(), i.on.call(r ? "interrupt" : "cancel", e, e.__data__, i.index, i.group), delete n[o];
    }
    a && delete e.__transition;
  }
}
function wg(e) {
  return this.each(function() {
    Tg(this, e);
  });
}
function $g(e, t) {
  var n, i;
  return function() {
    var r = tn(this, e), a = r.tween;
    if (a !== n) {
      i = n = a;
      for (var o = 0, s = i.length; o < s; ++o)
        if (i[o].name === t) {
          i = i.slice(), i.splice(o, 1);
          break;
        }
    }
    r.tween = i;
  };
}
function Ag(e, t, n) {
  var i, r;
  if (typeof n != "function")
    throw new Error();
  return function() {
    var a = tn(this, e), o = a.tween;
    if (o !== i) {
      r = (i = o).slice();
      for (var s = { name: t, value: n }, l = 0, c = r.length; l < c; ++l)
        if (r[l].name === t) {
          r[l] = s;
          break;
        }
      l === c && r.push(s);
    }
    a.tween = r;
  };
}
function Cg(e, t) {
  var n = this._id;
  if (e += "", arguments.length < 2) {
    for (var i = zt(this.node(), n).tween, r = 0, a = i.length, o; r < a; ++r)
      if ((o = i[r]).name === e)
        return o.value;
    return null;
  }
  return this.each((t == null ? $g : Ag)(n, e, t));
}
function bo(e, t, n) {
  var i = e._id;
  return e.each(function() {
    var r = tn(this, i);
    (r.value || (r.value = {}))[t] = n.apply(this, arguments);
  }), function(r) {
    return zt(r, i).value[t];
  };
}
function fl(e, t) {
  var n;
  return (typeof t == "number" ? Ft : t instanceof Dn ? br : (n = Dn(t)) ? (t = n, br) : rl)(e, t);
}
function Sg(e) {
  return function() {
    this.removeAttribute(e);
  };
}
function kg(e) {
  return function() {
    this.removeAttributeNS(e.space, e.local);
  };
}
function Eg(e, t, n) {
  var i, r = n + "", a;
  return function() {
    var o = this.getAttribute(e);
    return o === r ? null : o === i ? a : a = t(i = o, n);
  };
}
function Rg(e, t, n) {
  var i, r = n + "", a;
  return function() {
    var o = this.getAttributeNS(e.space, e.local);
    return o === r ? null : o === i ? a : a = t(i = o, n);
  };
}
function Mg(e, t, n) {
  var i, r, a;
  return function() {
    var o, s = n(this), l;
    return s == null ? void this.removeAttribute(e) : (o = this.getAttribute(e), l = s + "", o === l ? null : o === i && l === r ? a : (r = l, a = t(i = o, s)));
  };
}
function Lg(e, t, n) {
  var i, r, a;
  return function() {
    var o, s = n(this), l;
    return s == null ? void this.removeAttributeNS(e.space, e.local) : (o = this.getAttributeNS(e.space, e.local), l = s + "", o === l ? null : o === i && l === r ? a : (r = l, a = t(i = o, s)));
  };
}
function Dg(e, t) {
  var n = Br(e), i = n === "transform" ? gg : fl;
  return this.attrTween(e, typeof t == "function" ? (n.local ? Lg : Mg)(n, i, bo(this, "attr." + e, t)) : t == null ? (n.local ? kg : Sg)(n) : (n.local ? Rg : Eg)(n, i, t));
}
function Ig(e, t) {
  return function(n) {
    this.setAttribute(e, t.call(this, n));
  };
}
function Ng(e, t) {
  return function(n) {
    this.setAttributeNS(e.space, e.local, t.call(this, n));
  };
}
function Pg(e, t) {
  var n, i;
  function r() {
    var a = t.apply(this, arguments);
    return a !== i && (n = (i = a) && Ng(e, a)), n;
  }
  return r._value = t, r;
}
function Og(e, t) {
  var n, i;
  function r() {
    var a = t.apply(this, arguments);
    return a !== i && (n = (i = a) && Ig(e, a)), n;
  }
  return r._value = t, r;
}
function Fg(e, t) {
  var n = "attr." + e;
  if (arguments.length < 2)
    return (n = this.tween(n)) && n._value;
  if (t == null)
    return this.tween(n, null);
  if (typeof t != "function")
    throw new Error();
  var i = Br(e);
  return this.tween(n, (i.local ? Pg : Og)(i, t));
}
function Bg(e, t) {
  return function() {
    mo(this, e).delay = +t.apply(this, arguments);
  };
}
function Xg(e, t) {
  return t = +t, function() {
    mo(this, e).delay = t;
  };
}
function zg(e) {
  var t = this._id;
  return arguments.length ? this.each((typeof e == "function" ? Bg : Xg)(t, e)) : zt(this.node(), t).delay;
}
function Hg(e, t) {
  return function() {
    tn(this, e).duration = +t.apply(this, arguments);
  };
}
function Vg(e, t) {
  return t = +t, function() {
    tn(this, e).duration = t;
  };
}
function Yg(e) {
  var t = this._id;
  return arguments.length ? this.each((typeof e == "function" ? Hg : Vg)(t, e)) : zt(this.node(), t).duration;
}
function Ug(e, t) {
  if (typeof t != "function")
    throw new Error();
  return function() {
    tn(this, e).ease = t;
  };
}
function jg(e) {
  var t = this._id;
  return arguments.length ? this.each(Ug(t, e)) : zt(this.node(), t).ease;
}
function Gg(e, t) {
  return function() {
    var n = t.apply(this, arguments);
    if (typeof n != "function")
      throw new Error();
    tn(this, e).ease = n;
  };
}
function Wg(e) {
  if (typeof e != "function")
    throw new Error();
  return this.each(Gg(this._id, e));
}
function qg(e) {
  typeof e != "function" && (e = Uu(e));
  for (var t = this._groups, n = t.length, i = new Array(n), r = 0; r < n; ++r)
    for (var a = t[r], o = a.length, s = i[r] = [], l, c = 0; c < o; ++c)
      (l = a[c]) && e.call(l, l.__data__, c, a) && s.push(l);
  return new ln(i, this._parents, this._name, this._id);
}
function Zg(e) {
  if (e._id !== this._id)
    throw new Error();
  for (var t = this._groups, n = e._groups, i = t.length, r = n.length, a = Math.min(i, r), o = new Array(i), s = 0; s < a; ++s)
    for (var l = t[s], c = n[s], f = l.length, h = o[s] = new Array(f), g, x = 0; x < f; ++x)
      (g = l[x] || c[x]) && (h[x] = g);
  for (; s < i; ++s)
    o[s] = t[s];
  return new ln(o, this._parents, this._name, this._id);
}
function Kg(e) {
  return (e + "").trim().split(/^|\s+/).every(function(t) {
    var n = t.indexOf(".");
    return n >= 0 && (t = t.slice(0, n)), !t || t === "start";
  });
}
function Qg(e, t, n) {
  var i, r, a = Kg(t) ? mo : tn;
  return function() {
    var o = a(this, e), s = o.on;
    s !== i && (r = (i = s).copy()).on(t, n), o.on = r;
  };
}
function Jg(e, t) {
  var n = this._id;
  return arguments.length < 2 ? zt(this.node(), n).on.on(e) : this.each(Qg(n, e, t));
}
function ep(e) {
  return function() {
    var t = this.parentNode;
    for (var n in this.__transition)
      if (+n !== e)
        return;
    t && t.removeChild(this);
  };
}
function tp() {
  return this.on("end.remove", ep(this._id));
}
function np(e) {
  var t = this._name, n = this._id;
  typeof e != "function" && (e = go(e));
  for (var i = this._groups, r = i.length, a = new Array(r), o = 0; o < r; ++o)
    for (var s = i[o], l = s.length, c = a[o] = new Array(l), f, h, g = 0; g < l; ++g)
      (f = s[g]) && (h = e.call(f, f.__data__, g, s)) && ("__data__" in f && (h.__data__ = f.__data__), c[g] = h, zr(c[g], t, n, g, c, zt(f, n)));
  return new ln(a, this._parents, t, n);
}
function ip(e) {
  var t = this._name, n = this._id;
  typeof e != "function" && (e = Yu(e));
  for (var i = this._groups, r = i.length, a = [], o = [], s = 0; s < r; ++s)
    for (var l = i[s], c = l.length, f, h = 0; h < c; ++h)
      if (f = l[h]) {
        for (var g = e.call(f, f.__data__, h, l), x, y = zt(f, n), T = 0, A = g.length; T < A; ++T)
          (x = g[T]) && zr(x, t, n, T, g, y);
        a.push(g), o.push(f);
      }
  return new ln(a, o, t, n);
}
var rp = ui.prototype.constructor;
function ap() {
  return new rp(this._groups, this._parents);
}
function op(e, t) {
  var n, i, r;
  return function() {
    var a = ei(this, e), o = (this.style.removeProperty(e), ei(this, e));
    return a === o ? null : a === n && o === i ? r : r = t(n = a, i = o);
  };
}
function dl(e) {
  return function() {
    this.style.removeProperty(e);
  };
}
function sp(e, t, n) {
  var i, r = n + "", a;
  return function() {
    var o = ei(this, e);
    return o === r ? null : o === i ? a : a = t(i = o, n);
  };
}
function up(e, t, n) {
  var i, r, a;
  return function() {
    var o = ei(this, e), s = n(this), l = s + "";
    return s == null && (l = s = (this.style.removeProperty(e), ei(this, e))), o === l ? null : o === i && l === r ? a : (r = l, a = t(i = o, s));
  };
}
function lp(e, t) {
  var n, i, r, a = "style." + t, o = "end." + a, s;
  return function() {
    var l = tn(this, e), c = l.on, f = l.value[a] == null ? s || (s = dl(t)) : void 0;
    (c !== n || r !== f) && (i = (n = c).copy()).on(o, r = f), l.on = i;
  };
}
function cp(e, t, n) {
  var i = (e += "") == "transform" ? hg : fl;
  return t == null ? this.styleTween(e, op(e, i)).on("end.style." + e, dl(e)) : typeof t == "function" ? this.styleTween(e, up(e, i, bo(this, "style." + e, t))).each(lp(this._id, e)) : this.styleTween(e, sp(e, i, t), n).on("end.style." + e, null);
}
function fp(e, t, n) {
  return function(i) {
    this.style.setProperty(e, t.call(this, i), n);
  };
}
function dp(e, t, n) {
  var i, r;
  function a() {
    var o = t.apply(this, arguments);
    return o !== r && (i = (r = o) && fp(e, o, n)), i;
  }
  return a._value = t, a;
}
function hp(e, t, n) {
  var i = "style." + (e += "");
  if (arguments.length < 2)
    return (i = this.tween(i)) && i._value;
  if (t == null)
    return this.tween(i, null);
  if (typeof t != "function")
    throw new Error();
  return this.tween(i, dp(e, t, n == null ? "" : n));
}
function gp(e) {
  return function() {
    this.textContent = e;
  };
}
function pp(e) {
  return function() {
    var t = e(this);
    this.textContent = t == null ? "" : t;
  };
}
function vp(e) {
  return this.tween("text", typeof e == "function" ? pp(bo(this, "text", e)) : gp(e == null ? "" : e + ""));
}
function xp(e) {
  return function(t) {
    this.textContent = e.call(this, t);
  };
}
function _p(e) {
  var t, n;
  function i() {
    var r = e.apply(this, arguments);
    return r !== n && (t = (n = r) && xp(r)), t;
  }
  return i._value = e, i;
}
function yp(e) {
  var t = "text";
  if (arguments.length < 1)
    return (t = this.tween(t)) && t._value;
  if (e == null)
    return this.tween(t, null);
  if (typeof e != "function")
    throw new Error();
  return this.tween(t, _p(e));
}
function mp() {
  for (var e = this._name, t = this._id, n = gl(), i = this._groups, r = i.length, a = 0; a < r; ++a)
    for (var o = i[a], s = o.length, l, c = 0; c < s; ++c)
      if (l = o[c]) {
        var f = zt(l, t);
        zr(l, e, n, c, o, {
          time: f.time + f.delay + f.duration,
          delay: 0,
          duration: f.duration,
          ease: f.ease
        });
      }
  return new ln(i, this._parents, e, n);
}
function bp() {
  var e, t, n = this, i = n._id, r = n.size();
  return new Promise(function(a, o) {
    var s = { value: o }, l = { value: function() {
      --r === 0 && a();
    } };
    n.each(function() {
      var c = tn(this, i), f = c.on;
      f !== e && (t = (e = f).copy(), t._.cancel.push(s), t._.interrupt.push(s), t._.end.push(l)), c.on = t;
    }), r === 0 && a();
  });
}
var Tp = 0;
function ln(e, t, n, i) {
  this._groups = e, this._parents = t, this._name = n, this._id = i;
}
function hl(e) {
  return ui().transition(e);
}
function gl() {
  return ++Tp;
}
var on = ui.prototype;
ln.prototype = hl.prototype = {
  constructor: ln,
  select: np,
  selectAll: ip,
  selectChild: on.selectChild,
  selectChildren: on.selectChildren,
  filter: qg,
  merge: Zg,
  selection: ap,
  transition: mp,
  call: on.call,
  nodes: on.nodes,
  node: on.node,
  size: on.size,
  empty: on.empty,
  each: on.each,
  on: Jg,
  attr: Dg,
  attrTween: Fg,
  style: cp,
  styleTween: hp,
  text: vp,
  textTween: yp,
  remove: tp,
  tween: Cg,
  delay: zg,
  duration: Yg,
  ease: jg,
  easeVarying: Wg,
  end: bp,
  [Symbol.iterator]: on[Symbol.iterator]
};
const wp = (e) => +e;
function $p(e) {
  return ((e *= 2) <= 1 ? e * e * e : (e -= 2) * e * e + 2) / 2;
}
var Ap = {
  time: null,
  // Set on use.
  delay: 0,
  duration: 250,
  ease: $p
};
function Cp(e, t) {
  for (var n; !(n = e.__transition) || !(n = n[t]); )
    if (!(e = e.parentNode))
      throw new Error(`transition ${t} not found`);
  return n;
}
function Sp(e) {
  var t, n;
  e instanceof ln ? (t = e._id, e = e._name) : (t = gl(), (n = Ap).time = yo(), e = e == null ? null : e + "");
  for (var i = this._groups, r = i.length, a = 0; a < r; ++a)
    for (var o = i[a], s = o.length, l, c = 0; c < s; ++c)
      (l = o[c]) && zr(l, e, t, c, o, n || Cp(l, t));
  return new ln(i, this._parents, e, t);
}
ui.prototype.interrupt = wg;
ui.prototype.transition = Sp;
function kp(e) {
  var t = e.__brush;
  return t ? t.dim.output(t.selection) : null;
}
const eo = Math.PI, to = 2 * eo, kn = 1e-6, Ep = to - kn;
function pl(e) {
  this._ += e[0];
  for (let t = 1, n = e.length; t < n; ++t)
    this._ += arguments[t] + e[t];
}
function Rp(e) {
  let t = Math.floor(e);
  if (!(t >= 0))
    throw new Error(`invalid digits: ${e}`);
  if (t > 15)
    return pl;
  const n = Ss(10, t);
  return function(i) {
    this._ += i[0];
    for (let r = 1, a = i.length; r < a; ++r)
      this._ += Math.round(arguments[r] * n) / n + i[r];
  };
}
class Mp {
  constructor(t) {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null, this._ = "", this._append = t == null ? pl : Rp(t);
  }
  moveTo(t, n) {
    this._append`M${this._x0 = this._x1 = +t},${this._y0 = this._y1 = +n}`;
  }
  closePath() {
    this._x1 !== null && (this._x1 = this._x0, this._y1 = this._y0, this._append`Z`);
  }
  lineTo(t, n) {
    this._append`L${this._x1 = +t},${this._y1 = +n}`;
  }
  quadraticCurveTo(t, n, i, r) {
    this._append`Q${+t},${+n},${this._x1 = +i},${this._y1 = +r}`;
  }
  bezierCurveTo(t, n, i, r, a, o) {
    this._append`C${+t},${+n},${+i},${+r},${this._x1 = +a},${this._y1 = +o}`;
  }
  arcTo(t, n, i, r, a) {
    if (t = +t, n = +n, i = +i, r = +r, a = +a, a < 0)
      throw new Error(`negative radius: ${a}`);
    let o = this._x1, s = this._y1, l = i - t, c = r - n, f = o - t, h = s - n, g = f * f + h * h;
    if (this._x1 === null)
      this._append`M${this._x1 = t},${this._y1 = n}`;
    else if (g > kn)
      if (!(Math.abs(h * l - c * f) > kn) || !a)
        this._append`L${this._x1 = t},${this._y1 = n}`;
      else {
        let x = i - o, y = r - s, T = l * l + c * c, A = x * x + y * y, w = Math.sqrt(T), C = Math.sqrt(g), R = a * Math.tan((eo - Math.acos((T + g - A) / (2 * w * C))) / 2), S = R / C, I = R / w;
        Math.abs(S - 1) > kn && this._append`L${t + S * f},${n + S * h}`, this._append`A${a},${a},0,0,${+(h * x > f * y)},${this._x1 = t + I * l},${this._y1 = n + I * c}`;
      }
  }
  arc(t, n, i, r, a, o) {
    if (t = +t, n = +n, i = +i, o = !!o, i < 0)
      throw new Error(`negative radius: ${i}`);
    let s = i * Math.cos(r), l = i * Math.sin(r), c = t + s, f = n + l, h = 1 ^ o, g = o ? r - a : a - r;
    this._x1 === null ? this._append`M${c},${f}` : (Math.abs(this._x1 - c) > kn || Math.abs(this._y1 - f) > kn) && this._append`L${c},${f}`, i && (g < 0 && (g = g % to + to), g > Ep ? this._append`A${i},${i},0,1,${h},${t - s},${n - l}A${i},${i},0,1,${h},${this._x1 = c},${this._y1 = f}` : g > kn && this._append`A${i},${i},0,${+(g >= eo)},${h},${this._x1 = t + i * Math.cos(a)},${this._y1 = n + i * Math.sin(a)}`);
  }
  rect(t, n, i, r) {
    this._append`M${this._x0 = this._x1 = +t},${this._y0 = this._y1 = +n}h${i = +i}v${+r}h${-i}Z`;
  }
  toString() {
    return this._;
  }
}
var Ws = {}, Ta = {}, wa = 34, Ti = 10, $a = 13;
function vl(e) {
  return new Function("d", "return {" + e.map(function(t, n) {
    return JSON.stringify(t) + ": d[" + n + '] || ""';
  }).join(",") + "}");
}
function Lp(e, t) {
  var n = vl(e);
  return function(i, r) {
    return t(n(i), r, e);
  };
}
function qs(e) {
  var t = /* @__PURE__ */ Object.create(null), n = [];
  return e.forEach(function(i) {
    for (var r in i)
      r in t || n.push(t[r] = r);
  }), n;
}
function At(e, t) {
  var n = e + "", i = n.length;
  return i < t ? new Array(t - i + 1).join(0) + n : n;
}
function Dp(e) {
  return e < 0 ? "-" + At(-e, 6) : e > 9999 ? "+" + At(e, 6) : At(e, 4);
}
function Ip(e) {
  var t = e.getUTCHours(), n = e.getUTCMinutes(), i = e.getUTCSeconds(), r = e.getUTCMilliseconds();
  return isNaN(e) ? "Invalid Date" : Dp(e.getUTCFullYear()) + "-" + At(e.getUTCMonth() + 1, 2) + "-" + At(e.getUTCDate(), 2) + (r ? "T" + At(t, 2) + ":" + At(n, 2) + ":" + At(i, 2) + "." + At(r, 3) + "Z" : i ? "T" + At(t, 2) + ":" + At(n, 2) + ":" + At(i, 2) + "Z" : n || t ? "T" + At(t, 2) + ":" + At(n, 2) + "Z" : "");
}
function xl(e) {
  var t = new RegExp('["' + e + `
\r]`), n = e.charCodeAt(0);
  function i(h, g) {
    var x, y, T = r(h, function(A, w) {
      if (x)
        return x(A, w - 1);
      y = A, x = g ? Lp(A, g) : vl(A);
    });
    return T.columns = y || [], T;
  }
  function r(h, g) {
    var x = [], y = h.length, T = 0, A = 0, w, C = y <= 0, R = !1;
    h.charCodeAt(y - 1) === Ti && --y, h.charCodeAt(y - 1) === $a && --y;
    function S() {
      if (C)
        return Ta;
      if (R)
        return R = !1, Ws;
      var L, p = T, U;
      if (h.charCodeAt(p) === wa) {
        for (; T++ < y && h.charCodeAt(T) !== wa || h.charCodeAt(++T) === wa; )
          ;
        return (L = T) >= y ? C = !0 : (U = h.charCodeAt(T++)) === Ti ? R = !0 : U === $a && (R = !0, h.charCodeAt(T) === Ti && ++T), h.slice(p + 1, L - 1).replace(/""/g, '"');
      }
      for (; T < y; ) {
        if ((U = h.charCodeAt(L = T++)) === Ti)
          R = !0;
        else if (U === $a)
          R = !0, h.charCodeAt(T) === Ti && ++T;
        else if (U !== n)
          continue;
        return h.slice(p, L);
      }
      return C = !0, h.slice(p, y);
    }
    for (; (w = S()) !== Ta; ) {
      for (var I = []; w !== Ws && w !== Ta; )
        I.push(w), w = S();
      g && (I = g(I, A++)) == null || x.push(I);
    }
    return x;
  }
  function a(h, g) {
    return h.map(function(x) {
      return g.map(function(y) {
        return f(x[y]);
      }).join(e);
    });
  }
  function o(h, g) {
    return g == null && (g = qs(h)), [g.map(f).join(e)].concat(a(h, g)).join(`
`);
  }
  function s(h, g) {
    return g == null && (g = qs(h)), a(h, g).join(`
`);
  }
  function l(h) {
    return h.map(c).join(`
`);
  }
  function c(h) {
    return h.map(f).join(e);
  }
  function f(h) {
    return h == null ? "" : h instanceof Date ? Ip(h) : t.test(h += "") ? '"' + h.replace(/"/g, '""') + '"' : h;
  }
  return {
    parse: i,
    parseRows: r,
    format: o,
    formatBody: s,
    formatRows: l,
    formatRow: c,
    formatValue: f
  };
}
var _l = xl(","), Np = _l.parse, Pp = _l.parseRows, yl = xl("	"), Op = yl.parse, Fp = yl.parseRows;
function Bp(e) {
  return Math.abs(e = Math.round(e)) >= 1e21 ? e.toLocaleString("en").replace(/,/g, "") : e.toString(10);
}
function Ar(e, t) {
  if ((n = (e = t ? e.toExponential(t - 1) : e.toExponential()).indexOf("e")) < 0)
    return null;
  var n, i = e.slice(0, n);
  return [
    i.length > 1 ? i[0] + i.slice(2) : i,
    +e.slice(n + 1)
  ];
}
function ni(e) {
  return e = Ar(Math.abs(e)), e ? e[1] : NaN;
}
function Xp(e, t) {
  return function(n, i) {
    for (var r = n.length, a = [], o = 0, s = e[0], l = 0; r > 0 && s > 0 && (l + s + 1 > i && (s = Math.max(1, i - l)), a.push(n.substring(r -= s, r + s)), !((l += s + 1) > i)); )
      s = e[o = (o + 1) % e.length];
    return a.reverse().join(t);
  };
}
function zp(e) {
  return function(t) {
    return t.replace(/[0-9]/g, function(n) {
      return e[+n];
    });
  };
}
var Hp = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;
function Ni(e) {
  if (!(t = Hp.exec(e)))
    throw new Error("invalid format: " + e);
  var t;
  return new To({
    fill: t[1],
    align: t[2],
    sign: t[3],
    symbol: t[4],
    zero: t[5],
    width: t[6],
    comma: t[7],
    precision: t[8] && t[8].slice(1),
    trim: t[9],
    type: t[10]
  });
}
Ni.prototype = To.prototype;
function To(e) {
  this.fill = e.fill === void 0 ? " " : e.fill + "", this.align = e.align === void 0 ? ">" : e.align + "", this.sign = e.sign === void 0 ? "-" : e.sign + "", this.symbol = e.symbol === void 0 ? "" : e.symbol + "", this.zero = !!e.zero, this.width = e.width === void 0 ? void 0 : +e.width, this.comma = !!e.comma, this.precision = e.precision === void 0 ? void 0 : +e.precision, this.trim = !!e.trim, this.type = e.type === void 0 ? "" : e.type + "";
}
To.prototype.toString = function() {
  return this.fill + this.align + this.sign + this.symbol + (this.zero ? "0" : "") + (this.width === void 0 ? "" : Math.max(1, this.width | 0)) + (this.comma ? "," : "") + (this.precision === void 0 ? "" : "." + Math.max(0, this.precision | 0)) + (this.trim ? "~" : "") + this.type;
};
function Vp(e) {
  e:
    for (var t = e.length, n = 1, i = -1, r; n < t; ++n)
      switch (e[n]) {
        case ".":
          i = r = n;
          break;
        case "0":
          i === 0 && (i = n), r = n;
          break;
        default:
          if (!+e[n])
            break e;
          i > 0 && (i = 0);
          break;
      }
  return i > 0 ? e.slice(0, i) + e.slice(r + 1) : e;
}
var ml;
function Yp(e, t) {
  var n = Ar(e, t);
  if (!n)
    return e + "";
  var i = n[0], r = n[1], a = r - (ml = Math.max(-8, Math.min(8, Math.floor(r / 3))) * 3) + 1, o = i.length;
  return a === o ? i : a > o ? i + new Array(a - o + 1).join("0") : a > 0 ? i.slice(0, a) + "." + i.slice(a) : "0." + new Array(1 - a).join("0") + Ar(e, Math.max(0, t + a - 1))[0];
}
function Zs(e, t) {
  var n = Ar(e, t);
  if (!n)
    return e + "";
  var i = n[0], r = n[1];
  return r < 0 ? "0." + new Array(-r).join("0") + i : i.length > r + 1 ? i.slice(0, r + 1) + "." + i.slice(r + 1) : i + new Array(r - i.length + 2).join("0");
}
const Ks = {
  "%": (e, t) => (e * 100).toFixed(t),
  b: (e) => Math.round(e).toString(2),
  c: (e) => e + "",
  d: Bp,
  e: (e, t) => e.toExponential(t),
  f: (e, t) => e.toFixed(t),
  g: (e, t) => e.toPrecision(t),
  o: (e) => Math.round(e).toString(8),
  p: (e, t) => Zs(e * 100, t),
  r: Zs,
  s: Yp,
  X: (e) => Math.round(e).toString(16).toUpperCase(),
  x: (e) => Math.round(e).toString(16)
};
function Qs(e) {
  return e;
}
var Js = Array.prototype.map, eu = ["y", "z", "a", "f", "p", "n", "µ", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y"];
function Up(e) {
  var t = e.grouping === void 0 || e.thousands === void 0 ? Qs : Xp(Js.call(e.grouping, Number), e.thousands + ""), n = e.currency === void 0 ? "" : e.currency[0] + "", i = e.currency === void 0 ? "" : e.currency[1] + "", r = e.decimal === void 0 ? "." : e.decimal + "", a = e.numerals === void 0 ? Qs : zp(Js.call(e.numerals, String)), o = e.percent === void 0 ? "%" : e.percent + "", s = e.minus === void 0 ? "−" : e.minus + "", l = e.nan === void 0 ? "NaN" : e.nan + "";
  function c(h) {
    h = Ni(h);
    var g = h.fill, x = h.align, y = h.sign, T = h.symbol, A = h.zero, w = h.width, C = h.comma, R = h.precision, S = h.trim, I = h.type;
    I === "n" ? (C = !0, I = "g") : Ks[I] || (R === void 0 && (R = 12), S = !0, I = "g"), (A || g === "0" && x === "=") && (A = !0, g = "0", x = "=");
    var L = T === "$" ? n : T === "#" && /[boxX]/.test(I) ? "0" + I.toLowerCase() : "", p = T === "$" ? i : /[%p]/.test(I) ? o : "", U = Ks[I], H = /[defgprs%]/.test(I);
    R = R === void 0 ? 6 : /[gprs]/.test(I) ? Math.max(1, Math.min(21, R)) : Math.max(0, Math.min(20, R));
    function q(X) {
      var j = L, z = p, G, _e, Ue;
      if (I === "c")
        z = U(X) + z, X = "";
      else {
        X = +X;
        var te = X < 0 || 1 / X < 0;
        if (X = isNaN(X) ? l : U(Math.abs(X), R), S && (X = Vp(X)), te && +X == 0 && y !== "+" && (te = !1), j = (te ? y === "(" ? y : s : y === "-" || y === "(" ? "" : y) + j, z = (I === "s" ? eu[8 + ml / 3] : "") + z + (te && y === "(" ? ")" : ""), H) {
          for (G = -1, _e = X.length; ++G < _e; )
            if (Ue = X.charCodeAt(G), 48 > Ue || Ue > 57) {
              z = (Ue === 46 ? r + X.slice(G + 1) : X.slice(G)) + z, X = X.slice(0, G);
              break;
            }
        }
      }
      C && !A && (X = t(X, 1 / 0));
      var re = j.length + X.length + z.length, ue = re < w ? new Array(w - re + 1).join(g) : "";
      switch (C && A && (X = t(ue + X, ue.length ? w - z.length : 1 / 0), ue = ""), x) {
        case "<":
          X = j + X + z + ue;
          break;
        case "=":
          X = j + ue + X + z;
          break;
        case "^":
          X = ue.slice(0, re = ue.length >> 1) + j + X + z + ue.slice(re);
          break;
        default:
          X = ue + j + X + z;
          break;
      }
      return a(X);
    }
    return q.toString = function() {
      return h + "";
    }, q;
  }
  function f(h, g) {
    var x = c((h = Ni(h), h.type = "f", h)), y = Math.max(-8, Math.min(8, Math.floor(ni(g) / 3))) * 3, T = Math.pow(10, -y), A = eu[8 + y / 3];
    return function(w) {
      return x(T * w) + A;
    };
  }
  return {
    format: c,
    formatPrefix: f
  };
}
var ir, Pi, bl;
jp({
  thousands: ",",
  grouping: [3],
  currency: ["$", ""]
});
function jp(e) {
  return ir = Up(e), Pi = ir.format, bl = ir.formatPrefix, ir;
}
function Gp(e) {
  return Math.max(0, -ni(Math.abs(e)));
}
function Wp(e, t) {
  return Math.max(0, Math.max(-8, Math.min(8, Math.floor(ni(t) / 3))) * 3 - ni(Math.abs(e)));
}
function qp(e, t) {
  return e = Math.abs(e), t = Math.abs(t) - e, Math.max(0, ni(t) - ni(e)) + 1;
}
function li(e, t) {
  switch (arguments.length) {
    case 0:
      break;
    case 1:
      this.range(e);
      break;
    default:
      this.range(t).domain(e);
      break;
  }
  return this;
}
const tu = Symbol("implicit");
function Tl() {
  var e = new Es(), t = [], n = [], i = tu;
  function r(a) {
    let o = e.get(a);
    if (o === void 0) {
      if (i !== tu)
        return i;
      e.set(a, o = t.push(a) - 1);
    }
    return n[o % n.length];
  }
  return r.domain = function(a) {
    if (!arguments.length)
      return t.slice();
    t = [], e = new Es();
    for (const o of a)
      e.has(o) || e.set(o, t.push(o) - 1);
    return r;
  }, r.range = function(a) {
    return arguments.length ? (n = Array.from(a), r) : n.slice();
  }, r.unknown = function(a) {
    return arguments.length ? (i = a, r) : i;
  }, r.copy = function() {
    return Tl(t, n).unknown(i);
  }, li.apply(r, arguments), r;
}
function Zp(e) {
  return function() {
    return e;
  };
}
function Kp(e) {
  return +e;
}
var nu = [0, 1];
function qn(e) {
  return e;
}
function no(e, t) {
  return (t -= e = +e) ? function(n) {
    return (n - e) / t;
  } : Zp(isNaN(t) ? NaN : 0.5);
}
function Qp(e, t) {
  var n;
  return e > t && (n = e, e = t, t = n), function(i) {
    return Math.max(e, Math.min(t, i));
  };
}
function Jp(e, t, n) {
  var i = e[0], r = e[1], a = t[0], o = t[1];
  return r < i ? (i = no(r, i), a = n(o, a)) : (i = no(i, r), a = n(a, o)), function(s) {
    return a(i(s));
  };
}
function ev(e, t, n) {
  var i = Math.min(e.length, t.length) - 1, r = new Array(i), a = new Array(i), o = -1;
  for (e[i] < e[0] && (e = e.slice().reverse(), t = t.slice().reverse()); ++o < i; )
    r[o] = no(e[o], e[o + 1]), a[o] = n(t[o], t[o + 1]);
  return function(s) {
    var l = Ff(e, s, 1, i) - 1;
    return a[l](r[l](s));
  };
}
function Hr(e, t) {
  return t.domain(e.domain()).range(e.range()).interpolate(e.interpolate()).clamp(e.clamp()).unknown(e.unknown());
}
function wo() {
  var e = nu, t = nu, n = Xi, i, r, a, o = qn, s, l, c;
  function f() {
    var g = Math.min(e.length, t.length);
    return o !== qn && (o = Qp(e[0], e[g - 1])), s = g > 2 ? ev : Jp, l = c = null, h;
  }
  function h(g) {
    return g == null || isNaN(g = +g) ? a : (l || (l = s(e.map(i), t, n)))(i(o(g)));
  }
  return h.invert = function(g) {
    return o(r((c || (c = s(t, e.map(i), Ft)))(g)));
  }, h.domain = function(g) {
    return arguments.length ? (e = Array.from(g, Kp), f()) : e.slice();
  }, h.range = function(g) {
    return arguments.length ? (t = Array.from(g), f()) : t.slice();
  }, h.rangeRound = function(g) {
    return t = Array.from(g), n = cg, f();
  }, h.clamp = function(g) {
    return arguments.length ? (o = g ? !0 : qn, f()) : o !== qn;
  }, h.interpolate = function(g) {
    return arguments.length ? (n = g, f()) : n;
  }, h.unknown = function(g) {
    return arguments.length ? (a = g, h) : a;
  }, function(g, x) {
    return i = g, r = x, f();
  };
}
function wl() {
  return wo()(qn, qn);
}
function tv(e, t, n, i) {
  var r = Va(e, t, n), a;
  switch (i = Ni(i == null ? ",f" : i), i.type) {
    case "s": {
      var o = Math.max(Math.abs(e), Math.abs(t));
      return i.precision == null && !isNaN(a = Wp(r, o)) && (i.precision = a), bl(i, o);
    }
    case "":
    case "e":
    case "g":
    case "p":
    case "r": {
      i.precision == null && !isNaN(a = qp(r, Math.max(Math.abs(e), Math.abs(t)))) && (i.precision = a - (i.type === "e"));
      break;
    }
    case "f":
    case "%": {
      i.precision == null && !isNaN(a = Gp(r)) && (i.precision = a - (i.type === "%") * 2);
      break;
    }
  }
  return Pi(i);
}
function $l(e) {
  var t = e.domain;
  return e.ticks = function(n) {
    var i = t();
    return za(i[0], i[i.length - 1], n == null ? 10 : n);
  }, e.tickFormat = function(n, i) {
    var r = t();
    return tv(r[0], r[r.length - 1], n == null ? 10 : n, i);
  }, e.nice = function(n) {
    n == null && (n = 10);
    var i = t(), r = 0, a = i.length - 1, o = i[r], s = i[a], l, c, f = 10;
    for (s < o && (c = o, o = s, s = c, c = r, r = a, a = c); f-- > 0; ) {
      if (c = Ha(o, s, n), c === l)
        return i[r] = o, i[a] = s, t(i);
      if (c > 0)
        o = Math.floor(o / c) * c, s = Math.ceil(s / c) * c;
      else if (c < 0)
        o = Math.ceil(o * c) / c, s = Math.floor(s * c) / c;
      else
        break;
      l = c;
    }
    return e;
  }, e;
}
function Cr() {
  var e = wl();
  return e.copy = function() {
    return Hr(e, Cr());
  }, li.apply(e, arguments), $l(e);
}
function Al(e, t) {
  e = e.slice();
  var n = 0, i = e.length - 1, r = e[n], a = e[i], o;
  return a < r && (o = n, n = i, i = o, o = r, r = a, a = o), e[n] = t.floor(r), e[i] = t.ceil(a), e;
}
function iu(e) {
  return Math.log(e);
}
function ru(e) {
  return Math.exp(e);
}
function nv(e) {
  return -Math.log(-e);
}
function iv(e) {
  return -Math.exp(-e);
}
function rv(e) {
  return isFinite(e) ? +("1e" + e) : e < 0 ? 0 : e;
}
function av(e) {
  return e === 10 ? rv : e === Math.E ? Math.exp : (t) => Math.pow(e, t);
}
function ov(e) {
  return e === Math.E ? Math.log : e === 10 && Math.log10 || e === 2 && Math.log2 || (e = Math.log(e), (t) => Math.log(t) / e);
}
function au(e) {
  return (t, n) => -e(-t, n);
}
function sv(e) {
  const t = e(iu, ru), n = t.domain;
  let i = 10, r, a;
  function o() {
    return r = ov(i), a = av(i), n()[0] < 0 ? (r = au(r), a = au(a), e(nv, iv)) : e(iu, ru), t;
  }
  return t.base = function(s) {
    return arguments.length ? (i = +s, o()) : i;
  }, t.domain = function(s) {
    return arguments.length ? (n(s), o()) : n();
  }, t.ticks = (s) => {
    const l = n();
    let c = l[0], f = l[l.length - 1];
    const h = f < c;
    h && ([c, f] = [f, c]);
    let g = r(c), x = r(f), y, T;
    const A = s == null ? 10 : +s;
    let w = [];
    if (!(i % 1) && x - g < A) {
      if (g = Math.floor(g), x = Math.ceil(x), c > 0) {
        for (; g <= x; ++g)
          for (y = 1; y < i; ++y)
            if (T = g < 0 ? y / a(-g) : y * a(g), !(T < c)) {
              if (T > f)
                break;
              w.push(T);
            }
      } else
        for (; g <= x; ++g)
          for (y = i - 1; y >= 1; --y)
            if (T = g > 0 ? y / a(-g) : y * a(g), !(T < c)) {
              if (T > f)
                break;
              w.push(T);
            }
      w.length * 2 < A && (w = za(c, f, A));
    } else
      w = za(g, x, Math.min(x - g, A)).map(a);
    return h ? w.reverse() : w;
  }, t.tickFormat = (s, l) => {
    if (s == null && (s = 10), l == null && (l = i === 10 ? "s" : ","), typeof l != "function" && (!(i % 1) && (l = Ni(l)).precision == null && (l.trim = !0), l = Pi(l)), s === 1 / 0)
      return l;
    const c = Math.max(1, i * s / t.ticks().length);
    return (f) => {
      let h = f / a(Math.round(r(f)));
      return h * i < i - 0.5 && (h *= i), h <= c ? l(f) : "";
    };
  }, t.nice = () => n(Al(n(), {
    floor: (s) => a(Math.floor(r(s))),
    ceil: (s) => a(Math.ceil(r(s)))
  })), t;
}
function Cl() {
  const e = sv(wo()).domain([1, 10]);
  return e.copy = () => Hr(e, Cl()).base(e.base()), li.apply(e, arguments), e;
}
function ou(e) {
  return function(t) {
    return Math.sign(t) * Math.log1p(Math.abs(t / e));
  };
}
function su(e) {
  return function(t) {
    return Math.sign(t) * Math.expm1(Math.abs(t)) * e;
  };
}
function uv(e) {
  var t = 1, n = e(ou(t), su(t));
  return n.constant = function(i) {
    return arguments.length ? e(ou(t = +i), su(t)) : t;
  }, $l(n);
}
function Sl() {
  var e = uv(wo());
  return e.copy = function() {
    return Hr(e, Sl()).constant(e.constant());
  }, li.apply(e, arguments);
}
const Aa = /* @__PURE__ */ new Date(), Ca = /* @__PURE__ */ new Date();
function rt(e, t, n, i) {
  function r(a) {
    return e(a = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+a)), a;
  }
  return r.floor = (a) => (e(a = /* @__PURE__ */ new Date(+a)), a), r.ceil = (a) => (e(a = new Date(a - 1)), t(a, 1), e(a), a), r.round = (a) => {
    const o = r(a), s = r.ceil(a);
    return a - o < s - a ? o : s;
  }, r.offset = (a, o) => (t(a = /* @__PURE__ */ new Date(+a), o == null ? 1 : Math.floor(o)), a), r.range = (a, o, s) => {
    const l = [];
    if (a = r.ceil(a), s = s == null ? 1 : Math.floor(s), !(a < o) || !(s > 0))
      return l;
    let c;
    do
      l.push(c = /* @__PURE__ */ new Date(+a)), t(a, s), e(a);
    while (c < a && a < o);
    return l;
  }, r.filter = (a) => rt((o) => {
    if (o >= o)
      for (; e(o), !a(o); )
        o.setTime(o - 1);
  }, (o, s) => {
    if (o >= o)
      if (s < 0)
        for (; ++s <= 0; )
          for (; t(o, -1), !a(o); )
            ;
      else
        for (; --s >= 0; )
          for (; t(o, 1), !a(o); )
            ;
  }), n && (r.count = (a, o) => (Aa.setTime(+a), Ca.setTime(+o), e(Aa), e(Ca), Math.floor(n(Aa, Ca))), r.every = (a) => (a = Math.floor(a), !isFinite(a) || !(a > 0) ? null : a > 1 ? r.filter(i ? (o) => i(o) % a === 0 : (o) => r.count(0, o) % a === 0) : r)), r;
}
const Sr = rt(() => {
}, (e, t) => {
  e.setTime(+e + t);
}, (e, t) => t - e);
Sr.every = (e) => (e = Math.floor(e), !isFinite(e) || !(e > 0) ? null : e > 1 ? rt((t) => {
  t.setTime(Math.floor(t / e) * e);
}, (t, n) => {
  t.setTime(+t + n * e);
}, (t, n) => (n - t) / e) : Sr);
Sr.range;
const sn = 1e3, Lt = sn * 60, un = Lt * 60, cn = un * 24, $o = cn * 7, uu = cn * 30, Sa = cn * 365, Mn = rt((e) => {
  e.setTime(e - e.getMilliseconds());
}, (e, t) => {
  e.setTime(+e + t * sn);
}, (e, t) => (t - e) / sn, (e) => e.getUTCSeconds());
Mn.range;
const Ao = rt((e) => {
  e.setTime(e - e.getMilliseconds() - e.getSeconds() * sn);
}, (e, t) => {
  e.setTime(+e + t * Lt);
}, (e, t) => (t - e) / Lt, (e) => e.getMinutes());
Ao.range;
const Co = rt((e) => {
  e.setUTCSeconds(0, 0);
}, (e, t) => {
  e.setTime(+e + t * Lt);
}, (e, t) => (t - e) / Lt, (e) => e.getUTCMinutes());
Co.range;
const So = rt((e) => {
  e.setTime(e - e.getMilliseconds() - e.getSeconds() * sn - e.getMinutes() * Lt);
}, (e, t) => {
  e.setTime(+e + t * un);
}, (e, t) => (t - e) / un, (e) => e.getHours());
So.range;
const ko = rt((e) => {
  e.setUTCMinutes(0, 0, 0);
}, (e, t) => {
  e.setTime(+e + t * un);
}, (e, t) => (t - e) / un, (e) => e.getUTCHours());
ko.range;
const zi = rt(
  (e) => e.setHours(0, 0, 0, 0),
  (e, t) => e.setDate(e.getDate() + t),
  (e, t) => (t - e - (t.getTimezoneOffset() - e.getTimezoneOffset()) * Lt) / cn,
  (e) => e.getDate() - 1
);
zi.range;
const Vr = rt((e) => {
  e.setUTCHours(0, 0, 0, 0);
}, (e, t) => {
  e.setUTCDate(e.getUTCDate() + t);
}, (e, t) => (t - e) / cn, (e) => e.getUTCDate() - 1);
Vr.range;
const kl = rt((e) => {
  e.setUTCHours(0, 0, 0, 0);
}, (e, t) => {
  e.setUTCDate(e.getUTCDate() + t);
}, (e, t) => (t - e) / cn, (e) => Math.floor(e / cn));
kl.range;
function Pn(e) {
  return rt((t) => {
    t.setDate(t.getDate() - (t.getDay() + 7 - e) % 7), t.setHours(0, 0, 0, 0);
  }, (t, n) => {
    t.setDate(t.getDate() + n * 7);
  }, (t, n) => (n - t - (n.getTimezoneOffset() - t.getTimezoneOffset()) * Lt) / $o);
}
const Yr = Pn(0), kr = Pn(1), lv = Pn(2), cv = Pn(3), ii = Pn(4), fv = Pn(5), dv = Pn(6);
Yr.range;
kr.range;
lv.range;
cv.range;
ii.range;
fv.range;
dv.range;
function On(e) {
  return rt((t) => {
    t.setUTCDate(t.getUTCDate() - (t.getUTCDay() + 7 - e) % 7), t.setUTCHours(0, 0, 0, 0);
  }, (t, n) => {
    t.setUTCDate(t.getUTCDate() + n * 7);
  }, (t, n) => (n - t) / $o);
}
const Ur = On(0), Er = On(1), hv = On(2), gv = On(3), ri = On(4), pv = On(5), vv = On(6);
Ur.range;
Er.range;
hv.range;
gv.range;
ri.range;
pv.range;
vv.range;
const Eo = rt((e) => {
  e.setDate(1), e.setHours(0, 0, 0, 0);
}, (e, t) => {
  e.setMonth(e.getMonth() + t);
}, (e, t) => t.getMonth() - e.getMonth() + (t.getFullYear() - e.getFullYear()) * 12, (e) => e.getMonth());
Eo.range;
const Ro = rt((e) => {
  e.setUTCDate(1), e.setUTCHours(0, 0, 0, 0);
}, (e, t) => {
  e.setUTCMonth(e.getUTCMonth() + t);
}, (e, t) => t.getUTCMonth() - e.getUTCMonth() + (t.getUTCFullYear() - e.getUTCFullYear()) * 12, (e) => e.getUTCMonth());
Ro.range;
const fn = rt((e) => {
  e.setMonth(0, 1), e.setHours(0, 0, 0, 0);
}, (e, t) => {
  e.setFullYear(e.getFullYear() + t);
}, (e, t) => t.getFullYear() - e.getFullYear(), (e) => e.getFullYear());
fn.every = (e) => !isFinite(e = Math.floor(e)) || !(e > 0) ? null : rt((t) => {
  t.setFullYear(Math.floor(t.getFullYear() / e) * e), t.setMonth(0, 1), t.setHours(0, 0, 0, 0);
}, (t, n) => {
  t.setFullYear(t.getFullYear() + n * e);
});
fn.range;
const dn = rt((e) => {
  e.setUTCMonth(0, 1), e.setUTCHours(0, 0, 0, 0);
}, (e, t) => {
  e.setUTCFullYear(e.getUTCFullYear() + t);
}, (e, t) => t.getUTCFullYear() - e.getUTCFullYear(), (e) => e.getUTCFullYear());
dn.every = (e) => !isFinite(e = Math.floor(e)) || !(e > 0) ? null : rt((t) => {
  t.setUTCFullYear(Math.floor(t.getUTCFullYear() / e) * e), t.setUTCMonth(0, 1), t.setUTCHours(0, 0, 0, 0);
}, (t, n) => {
  t.setUTCFullYear(t.getUTCFullYear() + n * e);
});
dn.range;
function El(e, t, n, i, r, a) {
  const o = [
    [Mn, 1, sn],
    [Mn, 5, 5 * sn],
    [Mn, 15, 15 * sn],
    [Mn, 30, 30 * sn],
    [a, 1, Lt],
    [a, 5, 5 * Lt],
    [a, 15, 15 * Lt],
    [a, 30, 30 * Lt],
    [r, 1, un],
    [r, 3, 3 * un],
    [r, 6, 6 * un],
    [r, 12, 12 * un],
    [i, 1, cn],
    [i, 2, 2 * cn],
    [n, 1, $o],
    [t, 1, uu],
    [t, 3, 3 * uu],
    [e, 1, Sa]
  ];
  function s(c, f, h) {
    const g = f < c;
    g && ([c, f] = [f, c]);
    const x = h && typeof h.range == "function" ? h : l(c, f, h), y = x ? x.range(c, +f + 1) : [];
    return g ? y.reverse() : y;
  }
  function l(c, f, h) {
    const g = Math.abs(f - c) / h, x = fo(([, , A]) => A).right(o, g);
    if (x === o.length)
      return e.every(Va(c / Sa, f / Sa, h));
    if (x === 0)
      return Sr.every(Math.max(Va(c, f, h), 1));
    const [y, T] = o[g / o[x - 1][2] < o[x][2] / g ? x - 1 : x];
    return y.every(T);
  }
  return [s, l];
}
const [xv, _v] = El(dn, Ro, Ur, kl, ko, Co), [yv, mv] = El(fn, Eo, Yr, zi, So, Ao);
function ka(e) {
  if (0 <= e.y && e.y < 100) {
    var t = new Date(-1, e.m, e.d, e.H, e.M, e.S, e.L);
    return t.setFullYear(e.y), t;
  }
  return new Date(e.y, e.m, e.d, e.H, e.M, e.S, e.L);
}
function Ea(e) {
  if (0 <= e.y && e.y < 100) {
    var t = new Date(Date.UTC(-1, e.m, e.d, e.H, e.M, e.S, e.L));
    return t.setUTCFullYear(e.y), t;
  }
  return new Date(Date.UTC(e.y, e.m, e.d, e.H, e.M, e.S, e.L));
}
function wi(e, t, n) {
  return { y: e, m: t, d: n, H: 0, M: 0, S: 0, L: 0 };
}
function bv(e) {
  var t = e.dateTime, n = e.date, i = e.time, r = e.periods, a = e.days, o = e.shortDays, s = e.months, l = e.shortMonths, c = $i(r), f = Ai(r), h = $i(a), g = Ai(a), x = $i(o), y = Ai(o), T = $i(s), A = Ai(s), w = $i(l), C = Ai(l), R = {
    a: te,
    A: re,
    b: ue,
    B: we,
    c: null,
    d: gu,
    e: gu,
    f: Yv,
    g: ex,
    G: nx,
    H: zv,
    I: Hv,
    j: Vv,
    L: Rl,
    m: Uv,
    M: jv,
    p: st,
    q: Je,
    Q: xu,
    s: _u,
    S: Gv,
    u: Wv,
    U: qv,
    V: Zv,
    w: Kv,
    W: Qv,
    x: null,
    X: null,
    y: Jv,
    Y: tx,
    Z: ix,
    "%": vu
  }, S = {
    a: ft,
    A: vt,
    b: et,
    B: oe,
    c: null,
    d: pu,
    e: pu,
    f: sx,
    g: xx,
    G: yx,
    H: rx,
    I: ax,
    j: ox,
    L: Ll,
    m: ux,
    M: lx,
    p: He,
    q: Oe,
    Q: xu,
    s: _u,
    S: cx,
    u: fx,
    U: dx,
    V: hx,
    w: gx,
    W: px,
    x: null,
    X: null,
    y: vx,
    Y: _x,
    Z: mx,
    "%": vu
  }, I = {
    a: q,
    A: X,
    b: j,
    B: z,
    c: G,
    d: du,
    e: du,
    f: Ov,
    g: fu,
    G: cu,
    H: hu,
    I: hu,
    j: Dv,
    L: Pv,
    m: Lv,
    M: Iv,
    p: H,
    q: Mv,
    Q: Bv,
    s: Xv,
    S: Nv,
    u: Cv,
    U: Sv,
    V: kv,
    w: Av,
    W: Ev,
    x: _e,
    X: Ue,
    y: fu,
    Y: cu,
    Z: Rv,
    "%": Fv
  };
  R.x = L(n, R), R.X = L(i, R), R.c = L(t, R), S.x = L(n, S), S.X = L(i, S), S.c = L(t, S);
  function L(K, ie) {
    return function(fe) {
      var W = [], Le = -1, Ce = 0, Ge = K.length, Ve, xt, mt;
      for (fe instanceof Date || (fe = /* @__PURE__ */ new Date(+fe)); ++Le < Ge; )
        K.charCodeAt(Le) === 37 && (W.push(K.slice(Ce, Le)), (xt = lu[Ve = K.charAt(++Le)]) != null ? Ve = K.charAt(++Le) : xt = Ve === "e" ? " " : "0", (mt = ie[Ve]) && (Ve = mt(fe, xt)), W.push(Ve), Ce = Le + 1);
      return W.push(K.slice(Ce, Le)), W.join("");
    };
  }
  function p(K, ie) {
    return function(fe) {
      var W = wi(1900, void 0, 1), Le = U(W, K, fe += "", 0), Ce, Ge;
      if (Le != fe.length)
        return null;
      if ("Q" in W)
        return new Date(W.Q);
      if ("s" in W)
        return new Date(W.s * 1e3 + ("L" in W ? W.L : 0));
      if (ie && !("Z" in W) && (W.Z = 0), "p" in W && (W.H = W.H % 12 + W.p * 12), W.m === void 0 && (W.m = "q" in W ? W.q : 0), "V" in W) {
        if (W.V < 1 || W.V > 53)
          return null;
        "w" in W || (W.w = 1), "Z" in W ? (Ce = Ea(wi(W.y, 0, 1)), Ge = Ce.getUTCDay(), Ce = Ge > 4 || Ge === 0 ? Er.ceil(Ce) : Er(Ce), Ce = Vr.offset(Ce, (W.V - 1) * 7), W.y = Ce.getUTCFullYear(), W.m = Ce.getUTCMonth(), W.d = Ce.getUTCDate() + (W.w + 6) % 7) : (Ce = ka(wi(W.y, 0, 1)), Ge = Ce.getDay(), Ce = Ge > 4 || Ge === 0 ? kr.ceil(Ce) : kr(Ce), Ce = zi.offset(Ce, (W.V - 1) * 7), W.y = Ce.getFullYear(), W.m = Ce.getMonth(), W.d = Ce.getDate() + (W.w + 6) % 7);
      } else
        ("W" in W || "U" in W) && ("w" in W || (W.w = "u" in W ? W.u % 7 : "W" in W ? 1 : 0), Ge = "Z" in W ? Ea(wi(W.y, 0, 1)).getUTCDay() : ka(wi(W.y, 0, 1)).getDay(), W.m = 0, W.d = "W" in W ? (W.w + 6) % 7 + W.W * 7 - (Ge + 5) % 7 : W.w + W.U * 7 - (Ge + 6) % 7);
      return "Z" in W ? (W.H += W.Z / 100 | 0, W.M += W.Z % 100, Ea(W)) : ka(W);
    };
  }
  function U(K, ie, fe, W) {
    for (var Le = 0, Ce = ie.length, Ge = fe.length, Ve, xt; Le < Ce; ) {
      if (W >= Ge)
        return -1;
      if (Ve = ie.charCodeAt(Le++), Ve === 37) {
        if (Ve = ie.charAt(Le++), xt = I[Ve in lu ? ie.charAt(Le++) : Ve], !xt || (W = xt(K, fe, W)) < 0)
          return -1;
      } else if (Ve != fe.charCodeAt(W++))
        return -1;
    }
    return W;
  }
  function H(K, ie, fe) {
    var W = c.exec(ie.slice(fe));
    return W ? (K.p = f.get(W[0].toLowerCase()), fe + W[0].length) : -1;
  }
  function q(K, ie, fe) {
    var W = x.exec(ie.slice(fe));
    return W ? (K.w = y.get(W[0].toLowerCase()), fe + W[0].length) : -1;
  }
  function X(K, ie, fe) {
    var W = h.exec(ie.slice(fe));
    return W ? (K.w = g.get(W[0].toLowerCase()), fe + W[0].length) : -1;
  }
  function j(K, ie, fe) {
    var W = w.exec(ie.slice(fe));
    return W ? (K.m = C.get(W[0].toLowerCase()), fe + W[0].length) : -1;
  }
  function z(K, ie, fe) {
    var W = T.exec(ie.slice(fe));
    return W ? (K.m = A.get(W[0].toLowerCase()), fe + W[0].length) : -1;
  }
  function G(K, ie, fe) {
    return U(K, t, ie, fe);
  }
  function _e(K, ie, fe) {
    return U(K, n, ie, fe);
  }
  function Ue(K, ie, fe) {
    return U(K, i, ie, fe);
  }
  function te(K) {
    return o[K.getDay()];
  }
  function re(K) {
    return a[K.getDay()];
  }
  function ue(K) {
    return l[K.getMonth()];
  }
  function we(K) {
    return s[K.getMonth()];
  }
  function st(K) {
    return r[+(K.getHours() >= 12)];
  }
  function Je(K) {
    return 1 + ~~(K.getMonth() / 3);
  }
  function ft(K) {
    return o[K.getUTCDay()];
  }
  function vt(K) {
    return a[K.getUTCDay()];
  }
  function et(K) {
    return l[K.getUTCMonth()];
  }
  function oe(K) {
    return s[K.getUTCMonth()];
  }
  function He(K) {
    return r[+(K.getUTCHours() >= 12)];
  }
  function Oe(K) {
    return 1 + ~~(K.getUTCMonth() / 3);
  }
  return {
    format: function(K) {
      var ie = L(K += "", R);
      return ie.toString = function() {
        return K;
      }, ie;
    },
    parse: function(K) {
      var ie = p(K += "", !1);
      return ie.toString = function() {
        return K;
      }, ie;
    },
    utcFormat: function(K) {
      var ie = L(K += "", S);
      return ie.toString = function() {
        return K;
      }, ie;
    },
    utcParse: function(K) {
      var ie = p(K += "", !0);
      return ie.toString = function() {
        return K;
      }, ie;
    }
  };
}
var lu = { "-": "", _: " ", 0: "0" }, ot = /^\s*\d+/, Tv = /^%/, wv = /[\\^$*+?|[\]().{}]/g;
function Re(e, t, n) {
  var i = e < 0 ? "-" : "", r = (i ? -e : e) + "", a = r.length;
  return i + (a < n ? new Array(n - a + 1).join(t) + r : r);
}
function $v(e) {
  return e.replace(wv, "\\$&");
}
function $i(e) {
  return new RegExp("^(?:" + e.map($v).join("|") + ")", "i");
}
function Ai(e) {
  return new Map(e.map((t, n) => [t.toLowerCase(), n]));
}
function Av(e, t, n) {
  var i = ot.exec(t.slice(n, n + 1));
  return i ? (e.w = +i[0], n + i[0].length) : -1;
}
function Cv(e, t, n) {
  var i = ot.exec(t.slice(n, n + 1));
  return i ? (e.u = +i[0], n + i[0].length) : -1;
}
function Sv(e, t, n) {
  var i = ot.exec(t.slice(n, n + 2));
  return i ? (e.U = +i[0], n + i[0].length) : -1;
}
function kv(e, t, n) {
  var i = ot.exec(t.slice(n, n + 2));
  return i ? (e.V = +i[0], n + i[0].length) : -1;
}
function Ev(e, t, n) {
  var i = ot.exec(t.slice(n, n + 2));
  return i ? (e.W = +i[0], n + i[0].length) : -1;
}
function cu(e, t, n) {
  var i = ot.exec(t.slice(n, n + 4));
  return i ? (e.y = +i[0], n + i[0].length) : -1;
}
function fu(e, t, n) {
  var i = ot.exec(t.slice(n, n + 2));
  return i ? (e.y = +i[0] + (+i[0] > 68 ? 1900 : 2e3), n + i[0].length) : -1;
}
function Rv(e, t, n) {
  var i = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(t.slice(n, n + 6));
  return i ? (e.Z = i[1] ? 0 : -(i[2] + (i[3] || "00")), n + i[0].length) : -1;
}
function Mv(e, t, n) {
  var i = ot.exec(t.slice(n, n + 1));
  return i ? (e.q = i[0] * 3 - 3, n + i[0].length) : -1;
}
function Lv(e, t, n) {
  var i = ot.exec(t.slice(n, n + 2));
  return i ? (e.m = i[0] - 1, n + i[0].length) : -1;
}
function du(e, t, n) {
  var i = ot.exec(t.slice(n, n + 2));
  return i ? (e.d = +i[0], n + i[0].length) : -1;
}
function Dv(e, t, n) {
  var i = ot.exec(t.slice(n, n + 3));
  return i ? (e.m = 0, e.d = +i[0], n + i[0].length) : -1;
}
function hu(e, t, n) {
  var i = ot.exec(t.slice(n, n + 2));
  return i ? (e.H = +i[0], n + i[0].length) : -1;
}
function Iv(e, t, n) {
  var i = ot.exec(t.slice(n, n + 2));
  return i ? (e.M = +i[0], n + i[0].length) : -1;
}
function Nv(e, t, n) {
  var i = ot.exec(t.slice(n, n + 2));
  return i ? (e.S = +i[0], n + i[0].length) : -1;
}
function Pv(e, t, n) {
  var i = ot.exec(t.slice(n, n + 3));
  return i ? (e.L = +i[0], n + i[0].length) : -1;
}
function Ov(e, t, n) {
  var i = ot.exec(t.slice(n, n + 6));
  return i ? (e.L = Math.floor(i[0] / 1e3), n + i[0].length) : -1;
}
function Fv(e, t, n) {
  var i = Tv.exec(t.slice(n, n + 1));
  return i ? n + i[0].length : -1;
}
function Bv(e, t, n) {
  var i = ot.exec(t.slice(n));
  return i ? (e.Q = +i[0], n + i[0].length) : -1;
}
function Xv(e, t, n) {
  var i = ot.exec(t.slice(n));
  return i ? (e.s = +i[0], n + i[0].length) : -1;
}
function gu(e, t) {
  return Re(e.getDate(), t, 2);
}
function zv(e, t) {
  return Re(e.getHours(), t, 2);
}
function Hv(e, t) {
  return Re(e.getHours() % 12 || 12, t, 2);
}
function Vv(e, t) {
  return Re(1 + zi.count(fn(e), e), t, 3);
}
function Rl(e, t) {
  return Re(e.getMilliseconds(), t, 3);
}
function Yv(e, t) {
  return Rl(e, t) + "000";
}
function Uv(e, t) {
  return Re(e.getMonth() + 1, t, 2);
}
function jv(e, t) {
  return Re(e.getMinutes(), t, 2);
}
function Gv(e, t) {
  return Re(e.getSeconds(), t, 2);
}
function Wv(e) {
  var t = e.getDay();
  return t === 0 ? 7 : t;
}
function qv(e, t) {
  return Re(Yr.count(fn(e) - 1, e), t, 2);
}
function Ml(e) {
  var t = e.getDay();
  return t >= 4 || t === 0 ? ii(e) : ii.ceil(e);
}
function Zv(e, t) {
  return e = Ml(e), Re(ii.count(fn(e), e) + (fn(e).getDay() === 4), t, 2);
}
function Kv(e) {
  return e.getDay();
}
function Qv(e, t) {
  return Re(kr.count(fn(e) - 1, e), t, 2);
}
function Jv(e, t) {
  return Re(e.getFullYear() % 100, t, 2);
}
function ex(e, t) {
  return e = Ml(e), Re(e.getFullYear() % 100, t, 2);
}
function tx(e, t) {
  return Re(e.getFullYear() % 1e4, t, 4);
}
function nx(e, t) {
  var n = e.getDay();
  return e = n >= 4 || n === 0 ? ii(e) : ii.ceil(e), Re(e.getFullYear() % 1e4, t, 4);
}
function ix(e) {
  var t = e.getTimezoneOffset();
  return (t > 0 ? "-" : (t *= -1, "+")) + Re(t / 60 | 0, "0", 2) + Re(t % 60, "0", 2);
}
function pu(e, t) {
  return Re(e.getUTCDate(), t, 2);
}
function rx(e, t) {
  return Re(e.getUTCHours(), t, 2);
}
function ax(e, t) {
  return Re(e.getUTCHours() % 12 || 12, t, 2);
}
function ox(e, t) {
  return Re(1 + Vr.count(dn(e), e), t, 3);
}
function Ll(e, t) {
  return Re(e.getUTCMilliseconds(), t, 3);
}
function sx(e, t) {
  return Ll(e, t) + "000";
}
function ux(e, t) {
  return Re(e.getUTCMonth() + 1, t, 2);
}
function lx(e, t) {
  return Re(e.getUTCMinutes(), t, 2);
}
function cx(e, t) {
  return Re(e.getUTCSeconds(), t, 2);
}
function fx(e) {
  var t = e.getUTCDay();
  return t === 0 ? 7 : t;
}
function dx(e, t) {
  return Re(Ur.count(dn(e) - 1, e), t, 2);
}
function Dl(e) {
  var t = e.getUTCDay();
  return t >= 4 || t === 0 ? ri(e) : ri.ceil(e);
}
function hx(e, t) {
  return e = Dl(e), Re(ri.count(dn(e), e) + (dn(e).getUTCDay() === 4), t, 2);
}
function gx(e) {
  return e.getUTCDay();
}
function px(e, t) {
  return Re(Er.count(dn(e) - 1, e), t, 2);
}
function vx(e, t) {
  return Re(e.getUTCFullYear() % 100, t, 2);
}
function xx(e, t) {
  return e = Dl(e), Re(e.getUTCFullYear() % 100, t, 2);
}
function _x(e, t) {
  return Re(e.getUTCFullYear() % 1e4, t, 4);
}
function yx(e, t) {
  var n = e.getUTCDay();
  return e = n >= 4 || n === 0 ? ri(e) : ri.ceil(e), Re(e.getUTCFullYear() % 1e4, t, 4);
}
function mx() {
  return "+0000";
}
function vu() {
  return "%";
}
function xu(e) {
  return +e;
}
function _u(e) {
  return Math.floor(+e / 1e3);
}
var Gn, Mo, Il, Lo, Nl;
bx({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function bx(e) {
  return Gn = bv(e), Mo = Gn.format, Il = Gn.parse, Lo = Gn.utcFormat, Nl = Gn.utcParse, Gn;
}
function Tx(e) {
  return new Date(e);
}
function wx(e) {
  return e instanceof Date ? +e : +/* @__PURE__ */ new Date(+e);
}
function Do(e, t, n, i, r, a, o, s, l, c) {
  var f = wl(), h = f.invert, g = f.domain, x = c(".%L"), y = c(":%S"), T = c("%I:%M"), A = c("%I %p"), w = c("%a %d"), C = c("%b %d"), R = c("%B"), S = c("%Y");
  function I(L) {
    return (l(L) < L ? x : s(L) < L ? y : o(L) < L ? T : a(L) < L ? A : i(L) < L ? r(L) < L ? w : C : n(L) < L ? R : S)(L);
  }
  return f.invert = function(L) {
    return new Date(h(L));
  }, f.domain = function(L) {
    return arguments.length ? g(Array.from(L, wx)) : g().map(Tx);
  }, f.ticks = function(L) {
    var p = g();
    return e(p[0], p[p.length - 1], L == null ? 10 : L);
  }, f.tickFormat = function(L, p) {
    return p == null ? I : c(p);
  }, f.nice = function(L) {
    var p = g();
    return (!L || typeof L.range != "function") && (L = t(p[0], p[p.length - 1], L == null ? 10 : L)), L ? g(Al(p, L)) : f;
  }, f.copy = function() {
    return Hr(f, Do(e, t, n, i, r, a, o, s, l, c));
  }, f;
}
function $x() {
  return li.apply(Do(yv, mv, fn, Eo, Yr, zi, So, Ao, Mn, Mo).domain([new Date(2e3, 0, 1), new Date(2e3, 0, 2)]), arguments);
}
function Ax() {
  return li.apply(Do(xv, _v, dn, Ro, Ur, Vr, ko, Co, Mn, Lo).domain([Date.UTC(2e3, 0, 1), Date.UTC(2e3, 0, 2)]), arguments);
}
function ke(e) {
  return function() {
    return e;
  };
}
const yu = Math.abs, ht = Math.atan2, Sn = Math.cos, Cx = Math.max, Ra = Math.min, Wt = Math.sin, Zn = Math.sqrt, gt = 1e-12, Oi = Math.PI, Rr = Oi / 2, gr = 2 * Oi;
function Sx(e) {
  return e > 1 ? 0 : e < -1 ? Oi : Math.acos(e);
}
function mu(e) {
  return e >= 1 ? Rr : e <= -1 ? -Rr : Math.asin(e);
}
function Io(e) {
  let t = 3;
  return e.digits = function(n) {
    if (!arguments.length)
      return t;
    if (n == null)
      t = null;
    else {
      const i = Math.floor(n);
      if (!(i >= 0))
        throw new RangeError(`invalid digits: ${n}`);
      t = i;
    }
    return e;
  }, () => new Mp(t);
}
function kx(e) {
  return e.innerRadius;
}
function Ex(e) {
  return e.outerRadius;
}
function Rx(e) {
  return e.startAngle;
}
function Mx(e) {
  return e.endAngle;
}
function Lx(e) {
  return e && e.padAngle;
}
function Dx(e, t, n, i, r, a, o, s) {
  var l = n - e, c = i - t, f = o - r, h = s - a, g = h * l - f * c;
  if (!(g * g < gt))
    return g = (f * (t - a) - h * (e - r)) / g, [e + g * l, t + g * c];
}
function rr(e, t, n, i, r, a, o) {
  var s = e - n, l = t - i, c = (o ? a : -a) / Zn(s * s + l * l), f = c * l, h = -c * s, g = e + f, x = t + h, y = n + f, T = i + h, A = (g + y) / 2, w = (x + T) / 2, C = y - g, R = T - x, S = C * C + R * R, I = r - a, L = g * T - y * x, p = (R < 0 ? -1 : 1) * Zn(Cx(0, I * I * S - L * L)), U = (L * R - C * p) / S, H = (-L * C - R * p) / S, q = (L * R + C * p) / S, X = (-L * C + R * p) / S, j = U - A, z = H - w, G = q - A, _e = X - w;
  return j * j + z * z > G * G + _e * _e && (U = q, H = X), {
    cx: U,
    cy: H,
    x01: -f,
    y01: -h,
    x11: U * (r / I - 1),
    y11: H * (r / I - 1)
  };
}
function bu() {
  var e = kx, t = Ex, n = ke(0), i = null, r = Rx, a = Mx, o = Lx, s = null, l = Io(c);
  function c() {
    var f, h, g = +e.apply(this, arguments), x = +t.apply(this, arguments), y = r.apply(this, arguments) - Rr, T = a.apply(this, arguments) - Rr, A = yu(T - y), w = T > y;
    if (s || (s = f = l()), x < g && (h = x, x = g, g = h), !(x > gt))
      s.moveTo(0, 0);
    else if (A > gr - gt)
      s.moveTo(x * Sn(y), x * Wt(y)), s.arc(0, 0, x, y, T, !w), g > gt && (s.moveTo(g * Sn(T), g * Wt(T)), s.arc(0, 0, g, T, y, w));
    else {
      var C = y, R = T, S = y, I = T, L = A, p = A, U = o.apply(this, arguments) / 2, H = U > gt && (i ? +i.apply(this, arguments) : Zn(g * g + x * x)), q = Ra(yu(x - g) / 2, +n.apply(this, arguments)), X = q, j = q, z, G;
      if (H > gt) {
        var _e = mu(H / g * Wt(U)), Ue = mu(H / x * Wt(U));
        (L -= _e * 2) > gt ? (_e *= w ? 1 : -1, S += _e, I -= _e) : (L = 0, S = I = (y + T) / 2), (p -= Ue * 2) > gt ? (Ue *= w ? 1 : -1, C += Ue, R -= Ue) : (p = 0, C = R = (y + T) / 2);
      }
      var te = x * Sn(C), re = x * Wt(C), ue = g * Sn(I), we = g * Wt(I);
      if (q > gt) {
        var st = x * Sn(R), Je = x * Wt(R), ft = g * Sn(S), vt = g * Wt(S), et;
        if (A < Oi)
          if (et = Dx(te, re, ft, vt, st, Je, ue, we)) {
            var oe = te - et[0], He = re - et[1], Oe = st - et[0], K = Je - et[1], ie = 1 / Wt(Sx((oe * Oe + He * K) / (Zn(oe * oe + He * He) * Zn(Oe * Oe + K * K))) / 2), fe = Zn(et[0] * et[0] + et[1] * et[1]);
            X = Ra(q, (g - fe) / (ie - 1)), j = Ra(q, (x - fe) / (ie + 1));
          } else
            X = j = 0;
      }
      p > gt ? j > gt ? (z = rr(ft, vt, te, re, x, j, w), G = rr(st, Je, ue, we, x, j, w), s.moveTo(z.cx + z.x01, z.cy + z.y01), j < q ? s.arc(z.cx, z.cy, j, ht(z.y01, z.x01), ht(G.y01, G.x01), !w) : (s.arc(z.cx, z.cy, j, ht(z.y01, z.x01), ht(z.y11, z.x11), !w), s.arc(0, 0, x, ht(z.cy + z.y11, z.cx + z.x11), ht(G.cy + G.y11, G.cx + G.x11), !w), s.arc(G.cx, G.cy, j, ht(G.y11, G.x11), ht(G.y01, G.x01), !w))) : (s.moveTo(te, re), s.arc(0, 0, x, C, R, !w)) : s.moveTo(te, re), !(g > gt) || !(L > gt) ? s.lineTo(ue, we) : X > gt ? (z = rr(ue, we, st, Je, g, -X, w), G = rr(te, re, ft, vt, g, -X, w), s.lineTo(z.cx + z.x01, z.cy + z.y01), X < q ? s.arc(z.cx, z.cy, X, ht(z.y01, z.x01), ht(G.y01, G.x01), !w) : (s.arc(z.cx, z.cy, X, ht(z.y01, z.x01), ht(z.y11, z.x11), !w), s.arc(0, 0, g, ht(z.cy + z.y11, z.cx + z.x11), ht(G.cy + G.y11, G.cx + G.x11), w), s.arc(G.cx, G.cy, X, ht(G.y11, G.x11), ht(G.y01, G.x01), !w))) : s.arc(0, 0, g, I, S, w);
    }
    if (s.closePath(), f)
      return s = null, f + "" || null;
  }
  return c.centroid = function() {
    var f = (+e.apply(this, arguments) + +t.apply(this, arguments)) / 2, h = (+r.apply(this, arguments) + +a.apply(this, arguments)) / 2 - Oi / 2;
    return [Sn(h) * f, Wt(h) * f];
  }, c.innerRadius = function(f) {
    return arguments.length ? (e = typeof f == "function" ? f : ke(+f), c) : e;
  }, c.outerRadius = function(f) {
    return arguments.length ? (t = typeof f == "function" ? f : ke(+f), c) : t;
  }, c.cornerRadius = function(f) {
    return arguments.length ? (n = typeof f == "function" ? f : ke(+f), c) : n;
  }, c.padRadius = function(f) {
    return arguments.length ? (i = f == null ? null : typeof f == "function" ? f : ke(+f), c) : i;
  }, c.startAngle = function(f) {
    return arguments.length ? (r = typeof f == "function" ? f : ke(+f), c) : r;
  }, c.endAngle = function(f) {
    return arguments.length ? (a = typeof f == "function" ? f : ke(+f), c) : a;
  }, c.padAngle = function(f) {
    return arguments.length ? (o = typeof f == "function" ? f : ke(+f), c) : o;
  }, c.context = function(f) {
    return arguments.length ? (s = f == null ? null : f, c) : s;
  }, c;
}
function No(e) {
  return typeof e == "object" && "length" in e ? e : Array.from(e);
}
function Pl(e) {
  this._context = e;
}
Pl.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
        break;
      case 1:
        this._point = 2;
      default:
        this._context.lineTo(e, t);
        break;
    }
  }
};
function Po(e) {
  return new Pl(e);
}
function Ol(e) {
  return e[0];
}
function Fl(e) {
  return e[1];
}
function Bl(e, t) {
  var n = ke(!0), i = null, r = Po, a = null, o = Io(s);
  e = typeof e == "function" ? e : e === void 0 ? Ol : ke(e), t = typeof t == "function" ? t : t === void 0 ? Fl : ke(t);
  function s(l) {
    var c, f = (l = No(l)).length, h, g = !1, x;
    for (i == null && (a = r(x = o())), c = 0; c <= f; ++c)
      !(c < f && n(h = l[c], c, l)) === g && ((g = !g) ? a.lineStart() : a.lineEnd()), g && a.point(+e(h, c, l), +t(h, c, l));
    if (x)
      return a = null, x + "" || null;
  }
  return s.x = function(l) {
    return arguments.length ? (e = typeof l == "function" ? l : ke(+l), s) : e;
  }, s.y = function(l) {
    return arguments.length ? (t = typeof l == "function" ? l : ke(+l), s) : t;
  }, s.defined = function(l) {
    return arguments.length ? (n = typeof l == "function" ? l : ke(!!l), s) : n;
  }, s.curve = function(l) {
    return arguments.length ? (r = l, i != null && (a = r(i)), s) : r;
  }, s.context = function(l) {
    return arguments.length ? (l == null ? i = a = null : a = r(i = l), s) : i;
  }, s;
}
function Ix(e, t, n) {
  var i = null, r = ke(!0), a = null, o = Po, s = null, l = Io(c);
  e = typeof e == "function" ? e : e === void 0 ? Ol : ke(+e), t = typeof t == "function" ? t : ke(t === void 0 ? 0 : +t), n = typeof n == "function" ? n : n === void 0 ? Fl : ke(+n);
  function c(h) {
    var g, x, y, T = (h = No(h)).length, A, w = !1, C, R = new Array(T), S = new Array(T);
    for (a == null && (s = o(C = l())), g = 0; g <= T; ++g) {
      if (!(g < T && r(A = h[g], g, h)) === w)
        if (w = !w)
          x = g, s.areaStart(), s.lineStart();
        else {
          for (s.lineEnd(), s.lineStart(), y = g - 1; y >= x; --y)
            s.point(R[y], S[y]);
          s.lineEnd(), s.areaEnd();
        }
      w && (R[g] = +e(A, g, h), S[g] = +t(A, g, h), s.point(i ? +i(A, g, h) : R[g], n ? +n(A, g, h) : S[g]));
    }
    if (C)
      return s = null, C + "" || null;
  }
  function f() {
    return Bl().defined(r).curve(o).context(a);
  }
  return c.x = function(h) {
    return arguments.length ? (e = typeof h == "function" ? h : ke(+h), i = null, c) : e;
  }, c.x0 = function(h) {
    return arguments.length ? (e = typeof h == "function" ? h : ke(+h), c) : e;
  }, c.x1 = function(h) {
    return arguments.length ? (i = h == null ? null : typeof h == "function" ? h : ke(+h), c) : i;
  }, c.y = function(h) {
    return arguments.length ? (t = typeof h == "function" ? h : ke(+h), n = null, c) : t;
  }, c.y0 = function(h) {
    return arguments.length ? (t = typeof h == "function" ? h : ke(+h), c) : t;
  }, c.y1 = function(h) {
    return arguments.length ? (n = h == null ? null : typeof h == "function" ? h : ke(+h), c) : n;
  }, c.lineX0 = c.lineY0 = function() {
    return f().x(e).y(t);
  }, c.lineY1 = function() {
    return f().x(e).y(n);
  }, c.lineX1 = function() {
    return f().x(i).y(t);
  }, c.defined = function(h) {
    return arguments.length ? (r = typeof h == "function" ? h : ke(!!h), c) : r;
  }, c.curve = function(h) {
    return arguments.length ? (o = h, a != null && (s = o(a)), c) : o;
  }, c.context = function(h) {
    return arguments.length ? (h == null ? a = s = null : s = o(a = h), c) : a;
  }, c;
}
function Nx(e, t) {
  return t < e ? -1 : t > e ? 1 : t >= e ? 0 : NaN;
}
function Px(e) {
  return e;
}
function Ox() {
  var e = Px, t = Nx, n = null, i = ke(0), r = ke(gr), a = ke(0);
  function o(s) {
    var l, c = (s = No(s)).length, f, h, g = 0, x = new Array(c), y = new Array(c), T = +i.apply(this, arguments), A = Math.min(gr, Math.max(-gr, r.apply(this, arguments) - T)), w, C = Math.min(Math.abs(A) / c, a.apply(this, arguments)), R = C * (A < 0 ? -1 : 1), S;
    for (l = 0; l < c; ++l)
      (S = y[x[l] = l] = +e(s[l], l, s)) > 0 && (g += S);
    for (t != null ? x.sort(function(I, L) {
      return t(y[I], y[L]);
    }) : n != null && x.sort(function(I, L) {
      return n(s[I], s[L]);
    }), l = 0, h = g ? (A - c * R) / g : 0; l < c; ++l, T = w)
      f = x[l], S = y[f], w = T + (S > 0 ? S * h : 0) + R, y[f] = {
        data: s[f],
        index: l,
        value: S,
        startAngle: T,
        endAngle: w,
        padAngle: C
      };
    return y;
  }
  return o.value = function(s) {
    return arguments.length ? (e = typeof s == "function" ? s : ke(+s), o) : e;
  }, o.sortValues = function(s) {
    return arguments.length ? (t = s, n = null, o) : t;
  }, o.sort = function(s) {
    return arguments.length ? (n = s, t = null, o) : n;
  }, o.startAngle = function(s) {
    return arguments.length ? (i = typeof s == "function" ? s : ke(+s), o) : i;
  }, o.endAngle = function(s) {
    return arguments.length ? (r = typeof s == "function" ? s : ke(+s), o) : r;
  }, o.padAngle = function(s) {
    return arguments.length ? (a = typeof s == "function" ? s : ke(+s), o) : a;
  }, o;
}
function bn() {
}
function Mr(e, t, n) {
  e._context.bezierCurveTo(
    (2 * e._x0 + e._x1) / 3,
    (2 * e._y0 + e._y1) / 3,
    (e._x0 + 2 * e._x1) / 3,
    (e._y0 + 2 * e._y1) / 3,
    (e._x0 + 4 * e._x1 + t) / 6,
    (e._y0 + 4 * e._y1 + n) / 6
  );
}
function jr(e) {
  this._context = e;
}
jr.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN, this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 3:
        Mr(this, this._x1, this._y1);
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
    }
    (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3, this._context.lineTo((5 * this._x0 + this._x1) / 6, (5 * this._y0 + this._y1) / 6);
      default:
        Mr(this, e, t);
        break;
    }
    this._x0 = this._x1, this._x1 = e, this._y0 = this._y1, this._y1 = t;
  }
};
function Fx(e) {
  return new jr(e);
}
function Xl(e) {
  this._context = e;
}
Xl.prototype = {
  areaStart: bn,
  areaEnd: bn,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = NaN, this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x2, this._y2), this._context.closePath();
        break;
      }
      case 2: {
        this._context.moveTo((this._x2 + 2 * this._x3) / 3, (this._y2 + 2 * this._y3) / 3), this._context.lineTo((this._x3 + 2 * this._x2) / 3, (this._y3 + 2 * this._y2) / 3), this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x2, this._y2), this.point(this._x3, this._y3), this.point(this._x4, this._y4);
        break;
      }
    }
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._x2 = e, this._y2 = t;
        break;
      case 1:
        this._point = 2, this._x3 = e, this._y3 = t;
        break;
      case 2:
        this._point = 3, this._x4 = e, this._y4 = t, this._context.moveTo((this._x0 + 4 * this._x1 + e) / 6, (this._y0 + 4 * this._y1 + t) / 6);
        break;
      default:
        Mr(this, e, t);
        break;
    }
    this._x0 = this._x1, this._x1 = e, this._y0 = this._y1, this._y1 = t;
  }
};
function Bx(e) {
  return new Xl(e);
}
function zl(e) {
  this._context = e;
}
zl.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN, this._point = 0;
  },
  lineEnd: function() {
    (this._line || this._line !== 0 && this._point === 3) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        var n = (this._x0 + 4 * this._x1 + e) / 6, i = (this._y0 + 4 * this._y1 + t) / 6;
        this._line ? this._context.lineTo(n, i) : this._context.moveTo(n, i);
        break;
      case 3:
        this._point = 4;
      default:
        Mr(this, e, t);
        break;
    }
    this._x0 = this._x1, this._x1 = e, this._y0 = this._y1, this._y1 = t;
  }
};
function Xx(e) {
  return new zl(e);
}
function Hl(e, t) {
  this._basis = new jr(e), this._beta = t;
}
Hl.prototype = {
  lineStart: function() {
    this._x = [], this._y = [], this._basis.lineStart();
  },
  lineEnd: function() {
    var e = this._x, t = this._y, n = e.length - 1;
    if (n > 0)
      for (var i = e[0], r = t[0], a = e[n] - i, o = t[n] - r, s = -1, l; ++s <= n; )
        l = s / n, this._basis.point(
          this._beta * e[s] + (1 - this._beta) * (i + l * a),
          this._beta * t[s] + (1 - this._beta) * (r + l * o)
        );
    this._x = this._y = null, this._basis.lineEnd();
  },
  point: function(e, t) {
    this._x.push(+e), this._y.push(+t);
  }
};
const zx = function e(t) {
  function n(i) {
    return t === 1 ? new jr(i) : new Hl(i, t);
  }
  return n.beta = function(i) {
    return e(+i);
  }, n;
}(0.85);
function Lr(e, t, n) {
  e._context.bezierCurveTo(
    e._x1 + e._k * (e._x2 - e._x0),
    e._y1 + e._k * (e._y2 - e._y0),
    e._x2 + e._k * (e._x1 - t),
    e._y2 + e._k * (e._y1 - n),
    e._x2,
    e._y2
  );
}
function Oo(e, t) {
  this._context = e, this._k = (1 - t) / 6;
}
Oo.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN, this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        Lr(this, this._x1, this._y1);
        break;
    }
    (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
        break;
      case 1:
        this._point = 2, this._x1 = e, this._y1 = t;
        break;
      case 2:
        this._point = 3;
      default:
        Lr(this, e, t);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = e, this._y0 = this._y1, this._y1 = this._y2, this._y2 = t;
  }
};
const Hx = function e(t) {
  function n(i) {
    return new Oo(i, t);
  }
  return n.tension = function(i) {
    return e(+i);
  }, n;
}(0);
function Fo(e, t) {
  this._context = e, this._k = (1 - t) / 6;
}
Fo.prototype = {
  areaStart: bn,
  areaEnd: bn,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN, this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3), this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3), this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3), this.point(this._x4, this._y4), this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._x3 = e, this._y3 = t;
        break;
      case 1:
        this._point = 2, this._context.moveTo(this._x4 = e, this._y4 = t);
        break;
      case 2:
        this._point = 3, this._x5 = e, this._y5 = t;
        break;
      default:
        Lr(this, e, t);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = e, this._y0 = this._y1, this._y1 = this._y2, this._y2 = t;
  }
};
const Vx = function e(t) {
  function n(i) {
    return new Fo(i, t);
  }
  return n.tension = function(i) {
    return e(+i);
  }, n;
}(0);
function Bo(e, t) {
  this._context = e, this._k = (1 - t) / 6;
}
Bo.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN, this._point = 0;
  },
  lineEnd: function() {
    (this._line || this._line !== 0 && this._point === 3) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3, this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      default:
        Lr(this, e, t);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = e, this._y0 = this._y1, this._y1 = this._y2, this._y2 = t;
  }
};
const Yx = function e(t) {
  function n(i) {
    return new Bo(i, t);
  }
  return n.tension = function(i) {
    return e(+i);
  }, n;
}(0);
function Xo(e, t, n) {
  var i = e._x1, r = e._y1, a = e._x2, o = e._y2;
  if (e._l01_a > gt) {
    var s = 2 * e._l01_2a + 3 * e._l01_a * e._l12_a + e._l12_2a, l = 3 * e._l01_a * (e._l01_a + e._l12_a);
    i = (i * s - e._x0 * e._l12_2a + e._x2 * e._l01_2a) / l, r = (r * s - e._y0 * e._l12_2a + e._y2 * e._l01_2a) / l;
  }
  if (e._l23_a > gt) {
    var c = 2 * e._l23_2a + 3 * e._l23_a * e._l12_a + e._l12_2a, f = 3 * e._l23_a * (e._l23_a + e._l12_a);
    a = (a * c + e._x1 * e._l23_2a - t * e._l12_2a) / f, o = (o * c + e._y1 * e._l23_2a - n * e._l12_2a) / f;
  }
  e._context.bezierCurveTo(i, r, a, o, e._x2, e._y2);
}
function Vl(e, t) {
  this._context = e, this._alpha = t;
}
Vl.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN, this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        this.point(this._x2, this._y2);
        break;
    }
    (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    if (e = +e, t = +t, this._point) {
      var n = this._x2 - e, i = this._y2 - t;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(n * n + i * i, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
      default:
        Xo(this, e, t);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a, this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a, this._x0 = this._x1, this._x1 = this._x2, this._x2 = e, this._y0 = this._y1, this._y1 = this._y2, this._y2 = t;
  }
};
const Ux = function e(t) {
  function n(i) {
    return t ? new Vl(i, t) : new Oo(i, 0);
  }
  return n.alpha = function(i) {
    return e(+i);
  }, n;
}(0.5);
function Yl(e, t) {
  this._context = e, this._alpha = t;
}
Yl.prototype = {
  areaStart: bn,
  areaEnd: bn,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN, this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3), this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3), this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3), this.point(this._x4, this._y4), this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(e, t) {
    if (e = +e, t = +t, this._point) {
      var n = this._x2 - e, i = this._y2 - t;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(n * n + i * i, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1, this._x3 = e, this._y3 = t;
        break;
      case 1:
        this._point = 2, this._context.moveTo(this._x4 = e, this._y4 = t);
        break;
      case 2:
        this._point = 3, this._x5 = e, this._y5 = t;
        break;
      default:
        Xo(this, e, t);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a, this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a, this._x0 = this._x1, this._x1 = this._x2, this._x2 = e, this._y0 = this._y1, this._y1 = this._y2, this._y2 = t;
  }
};
const jx = function e(t) {
  function n(i) {
    return t ? new Yl(i, t) : new Fo(i, 0);
  }
  return n.alpha = function(i) {
    return e(+i);
  }, n;
}(0.5);
function Ul(e, t) {
  this._context = e, this._alpha = t;
}
Ul.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN, this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    (this._line || this._line !== 0 && this._point === 3) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    if (e = +e, t = +t, this._point) {
      var n = this._x2 - e, i = this._y2 - t;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(n * n + i * i, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3, this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      default:
        Xo(this, e, t);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a, this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a, this._x0 = this._x1, this._x1 = this._x2, this._x2 = e, this._y0 = this._y1, this._y1 = this._y2, this._y2 = t;
  }
};
const Gx = function e(t) {
  function n(i) {
    return t ? new Ul(i, t) : new Bo(i, 0);
  }
  return n.alpha = function(i) {
    return e(+i);
  }, n;
}(0.5);
function jl(e) {
  this._context = e;
}
jl.prototype = {
  areaStart: bn,
  areaEnd: bn,
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    this._point && this._context.closePath();
  },
  point: function(e, t) {
    e = +e, t = +t, this._point ? this._context.lineTo(e, t) : (this._point = 1, this._context.moveTo(e, t));
  }
};
function Wx(e) {
  return new jl(e);
}
function Tu(e) {
  return e < 0 ? -1 : 1;
}
function wu(e, t, n) {
  var i = e._x1 - e._x0, r = t - e._x1, a = (e._y1 - e._y0) / (i || r < 0 && -0), o = (n - e._y1) / (r || i < 0 && -0), s = (a * r + o * i) / (i + r);
  return (Tu(a) + Tu(o)) * Math.min(Math.abs(a), Math.abs(o), 0.5 * Math.abs(s)) || 0;
}
function $u(e, t) {
  var n = e._x1 - e._x0;
  return n ? (3 * (e._y1 - e._y0) / n - t) / 2 : t;
}
function Ma(e, t, n) {
  var i = e._x0, r = e._y0, a = e._x1, o = e._y1, s = (a - i) / 3;
  e._context.bezierCurveTo(i + s, r + s * t, a - s, o - s * n, a, o);
}
function Dr(e) {
  this._context = e;
}
Dr.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = this._t0 = NaN, this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
      case 3:
        Ma(this, this._t0, $u(this, this._t0));
        break;
    }
    (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    var n = NaN;
    if (e = +e, t = +t, !(e === this._x1 && t === this._y1)) {
      switch (this._point) {
        case 0:
          this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
          break;
        case 1:
          this._point = 2;
          break;
        case 2:
          this._point = 3, Ma(this, $u(this, n = wu(this, e, t)), n);
          break;
        default:
          Ma(this, this._t0, n = wu(this, e, t));
          break;
      }
      this._x0 = this._x1, this._x1 = e, this._y0 = this._y1, this._y1 = t, this._t0 = n;
    }
  }
};
function Gl(e) {
  this._context = new Wl(e);
}
(Gl.prototype = Object.create(Dr.prototype)).point = function(e, t) {
  Dr.prototype.point.call(this, t, e);
};
function Wl(e) {
  this._context = e;
}
Wl.prototype = {
  moveTo: function(e, t) {
    this._context.moveTo(t, e);
  },
  closePath: function() {
    this._context.closePath();
  },
  lineTo: function(e, t) {
    this._context.lineTo(t, e);
  },
  bezierCurveTo: function(e, t, n, i, r, a) {
    this._context.bezierCurveTo(t, e, i, n, a, r);
  }
};
function qx(e) {
  return new Dr(e);
}
function Zx(e) {
  return new Gl(e);
}
function ql(e) {
  this._context = e;
}
ql.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = [], this._y = [];
  },
  lineEnd: function() {
    var e = this._x, t = this._y, n = e.length;
    if (n)
      if (this._line ? this._context.lineTo(e[0], t[0]) : this._context.moveTo(e[0], t[0]), n === 2)
        this._context.lineTo(e[1], t[1]);
      else
        for (var i = Au(e), r = Au(t), a = 0, o = 1; o < n; ++a, ++o)
          this._context.bezierCurveTo(i[0][a], r[0][a], i[1][a], r[1][a], e[o], t[o]);
    (this._line || this._line !== 0 && n === 1) && this._context.closePath(), this._line = 1 - this._line, this._x = this._y = null;
  },
  point: function(e, t) {
    this._x.push(+e), this._y.push(+t);
  }
};
function Au(e) {
  var t, n = e.length - 1, i, r = new Array(n), a = new Array(n), o = new Array(n);
  for (r[0] = 0, a[0] = 2, o[0] = e[0] + 2 * e[1], t = 1; t < n - 1; ++t)
    r[t] = 1, a[t] = 4, o[t] = 4 * e[t] + 2 * e[t + 1];
  for (r[n - 1] = 2, a[n - 1] = 7, o[n - 1] = 8 * e[n - 1] + e[n], t = 1; t < n; ++t)
    i = r[t] / a[t - 1], a[t] -= i, o[t] -= i * o[t - 1];
  for (r[n - 1] = o[n - 1] / a[n - 1], t = n - 2; t >= 0; --t)
    r[t] = (o[t] - r[t + 1]) / a[t];
  for (a[n - 1] = (e[n] + r[n - 1]) / 2, t = 0; t < n - 1; ++t)
    a[t] = 2 * e[t + 1] - r[t + 1];
  return [r, a];
}
function Kx(e) {
  return new ql(e);
}
function Gr(e, t) {
  this._context = e, this._t = t;
}
Gr.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = this._y = NaN, this._point = 0;
  },
  lineEnd: function() {
    0 < this._t && this._t < 1 && this._point === 2 && this._context.lineTo(this._x, this._y), (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line >= 0 && (this._t = 1 - this._t, this._line = 1 - this._line);
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
        break;
      case 1:
        this._point = 2;
      default: {
        if (this._t <= 0)
          this._context.lineTo(this._x, t), this._context.lineTo(e, t);
        else {
          var n = this._x * (1 - this._t) + e * this._t;
          this._context.lineTo(n, this._y), this._context.lineTo(n, t);
        }
        break;
      }
    }
    this._x = e, this._y = t;
  }
};
function Qx(e) {
  return new Gr(e, 0.5);
}
function Jx(e) {
  return new Gr(e, 0);
}
function e_(e) {
  return new Gr(e, 1);
}
function Ei(e, t, n) {
  this.k = e, this.x = t, this.y = n;
}
Ei.prototype = {
  constructor: Ei,
  scale: function(e) {
    return e === 1 ? this : new Ei(this.k * e, this.x, this.y);
  },
  translate: function(e, t) {
    return e === 0 & t === 0 ? this : new Ei(this.k, this.x + this.k * e, this.y + this.k * t);
  },
  apply: function(e) {
    return [e[0] * this.k + this.x, e[1] * this.k + this.y];
  },
  applyX: function(e) {
    return e * this.k + this.x;
  },
  applyY: function(e) {
    return e * this.k + this.y;
  },
  invert: function(e) {
    return [(e[0] - this.x) / this.k, (e[1] - this.y) / this.k];
  },
  invertX: function(e) {
    return (e - this.x) / this.k;
  },
  invertY: function(e) {
    return (e - this.y) / this.k;
  },
  rescaleX: function(e) {
    return e.copy().domain(e.range().map(this.invertX, this).map(e.invert, e));
  },
  rescaleY: function(e) {
    return e.copy().domain(e.range().map(this.invertY, this).map(e.invert, e));
  },
  toString: function() {
    return "translate(" + this.x + "," + this.y + ") scale(" + this.k + ")";
  }
};
Ei.prototype;
/*!
* Copyright (c) 2017 ~ present NAVER Corp.
 * billboard.js project is licensed under the MIT license
 * 
 * billboard.js, JavaScript chart library
 * https://naver.github.io/billboard.js/
 * 
 * @version 3.14.3
*/
var ye = function() {
  return ye = Object.assign || function(t) {
    for (var n, i = 1, r = arguments.length; i < r; i++) {
      n = arguments[i];
      for (var a in n)
        Object.prototype.hasOwnProperty.call(n, a) && (t[a] = n[a]);
    }
    return t;
  }, ye.apply(this, arguments);
};
function Ke(e, t, n) {
  if (n || arguments.length === 2)
    for (var i = 0, r = t.length, a; i < r; i++)
      (a || !(i in t)) && (a || (a = Array.prototype.slice.call(t, 0, i)), a[i] = t[i]);
  return e.concat(a || Array.prototype.slice.call(t));
}
var Ee = {
  button: "bb-button",
  chart: "bb-chart",
  empty: "bb-empty",
  main: "bb-main",
  target: "bb-target",
  EXPANDED: "_expanded_"
}, Xe = {
  arc: "bb-arc",
  arcLabelLine: "bb-arc-label-line",
  arcRange: "bb-arc-range",
  arcs: "bb-arcs",
  chartArc: "bb-chart-arc",
  chartArcs: "bb-chart-arcs",
  chartArcsBackground: "bb-chart-arcs-background",
  chartArcsTitle: "bb-chart-arcs-title",
  needle: "bb-needle"
}, io = {
  area: "bb-area",
  areas: "bb-areas"
}, nt = {
  axis: "bb-axis",
  axisX: "bb-axis-x",
  axisXLabel: "bb-axis-x-label",
  axisY: "bb-axis-y",
  axisY2: "bb-axis-y2",
  axisY2Label: "bb-axis-y2-label",
  axisYLabel: "bb-axis-y-label",
  axisXTooltip: "bb-axis-x-tooltip",
  axisYTooltip: "bb-axis-y-tooltip",
  axisY2Tooltip: "bb-axis-y2-tooltip"
}, Ot = {
  bar: "bb-bar",
  bars: "bb-bars",
  chartBar: "bb-chart-bar",
  chartBars: "bb-chart-bars"
}, ro = {
  candlestick: "bb-candlestick",
  candlesticks: "bb-candlesticks",
  chartCandlestick: "bb-chart-candlestick",
  chartCandlesticks: "bb-chart-candlesticks",
  valueDown: "bb-value-down",
  valueUp: "bb-value-up"
}, pt = {
  chartCircles: "bb-chart-circles",
  circle: "bb-circle",
  circles: "bb-circles"
}, ao = {
  colorPattern: "bb-color-pattern",
  colorScale: "bb-colorscale"
}, En = {
  dragarea: "bb-dragarea",
  INCLUDED: "_included_"
}, t_ = {
  funnel: "bb-funnel",
  chartFunnel: "bb-chart-funnel",
  chartFunnels: "bb-chart-funnels",
  funnelBackground: "bb-funnel-background"
}, qt = {
  chartArcsGaugeMax: "bb-chart-arcs-gauge-max",
  chartArcsGaugeMin: "bb-chart-arcs-gauge-min",
  chartArcsGaugeUnit: "bb-chart-arcs-gauge-unit",
  chartArcsGaugeTitle: "bb-chart-arcs-gauge-title",
  gaugeValue: "bb-gauge-value"
}, Pe = {
  legend: "bb-legend",
  legendBackground: "bb-legend-background",
  legendItem: "bb-legend-item",
  legendItemEvent: "bb-legend-item-event",
  legendItemHidden: "bb-legend-item-hidden",
  legendItemPoint: "bb-legend-item-point",
  legendItemTile: "bb-legend-item-tile"
}, _n = {
  chartLine: "bb-chart-line",
  chartLines: "bb-chart-lines",
  line: "bb-line",
  lines: "bb-lines"
}, Pt = {
  eventRect: "bb-event-rect",
  eventRects: "bb-event-rects",
  eventRectsMultiple: "bb-event-rects-multiple",
  eventRectsSingle: "bb-event-rects-single"
}, ze = {
  focused: "bb-focused",
  defocused: "bb-defocused",
  legendItemFocused: "bb-legend-item-focused",
  xgridFocus: "bb-xgrid-focus",
  ygridFocus: "bb-ygrid-focus"
}, qe = {
  grid: "bb-grid",
  gridLines: "bb-grid-lines",
  xgrid: "bb-xgrid",
  xgridLine: "bb-xgrid-line",
  xgridLines: "bb-xgrid-lines",
  xgrids: "bb-xgrids",
  ygrid: "bb-ygrid",
  ygridLine: "bb-ygrid-line",
  ygridLines: "bb-ygrid-lines",
  ygrids: "bb-ygrids"
}, n_ = {
  chartRadar: "bb-chart-radar",
  chartRadars: "bb-chart-radars"
}, Fi = {
  region: "bb-region",
  regions: "bb-regions"
}, it = {
  selectedCircle: "bb-selected-circle",
  selectedCircles: "bb-selected-circles",
  SELECTED: "_selected_"
}, ct = {
  shape: "bb-shape",
  shapes: "bb-shapes"
}, Zl = {
  brush: "bb-brush",
  subchart: "bb-subchart"
}, Ct = {
  chartText: "bb-chart-text",
  chartTexts: "bb-chart-texts",
  text: "bb-text",
  texts: "bb-texts",
  title: "bb-title",
  TextOverlapping: "text-overlapping"
}, pr = {
  tooltip: "bb-tooltip",
  tooltipContainer: "bb-tooltip-container",
  tooltipName: "bb-tooltip-name"
}, i_ = {
  treemap: "bb-treemap",
  chartTreemap: "bb-chart-treemap",
  chartTreemaps: "bb-chart-treemaps"
}, r_ = {
  buttonZoomReset: "bb-zoom-reset",
  zoomBrush: "bb-zoom-brush"
}, lt = ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye(ye({}, Ee), Xe), io), nt), Ot), ro), pt), ao), En), qt), Pe), _n), Pt), ze), t_), qe), n_), Fi), it), ct), Zl), Ct), pr), i_), r_), a_ = {
  /**
   * Set boost options
   * @name boost
   * @memberof Options
   * @type {object}
   * @property {object} boost boost object
   * @property {boolean} [boost.useCssRule=false] Avoid setting inline styles for each shape elements.
   * - **NOTE:**
   *   - Will append &lt;style> to the head tag and will add shpes' CSS rules dynamically.
   *   - For now, covers colors related properties (fill, stroke, etc.) only.
   * @property {boolean} [boost.useWorker=false] Use Web Worker as possible for processing.
   * - **NOTE:**
   *   - For now, only applies for data conversion at the initial time.
   *   - As of Web Worker's async nature, handling chart instance synchrously is not recommended.
   * @example
   *  boost: {
   *      useCssRule: true,
   *      useWorker: false
   *  }
   */
  boost_useCssRule: !1,
  boost_useWorker: !1
}, o_ = {
  /**
   * Set color of the data values
   * @name color
   * @memberof Options
   * @type {object}
   * @property {object} color color object
   * @property {string|object|Function} [color.onover] Set the color value for each data point when mouse/touch onover event occurs.
   * @property {Array|null} [color.pattern=[]] Set custom color pattern. Passing `null` will not set a color for these elements, which requires the usage of custom CSS-based theming to work.
   * @property {Function} [color.tiles] if defined, allows use svg's patterns to fill data area. It should return an array of [SVGPatternElement](https://developer.mozilla.org/en-US/docs/Web/API/SVGPatternElement).
   *  - **NOTE:** The pattern element's id will be defined as `bb-colorize-pattern-$COLOR-VALUE`.<br>
   *    ex. When color pattern value is `['red', '#fff']` and defined 2 patterns,then ids for pattern elements are:<br>
   *    - `bb-colorize-pattern-red`
   *    - `bb-colorize-pattern-fff`
   * @property {object} [color.threshold] color threshold for gauge and tooltip color
   * @property {string} [color.threshold.unit] If set to `value`, the threshold will be based on the data value. Otherwise it'll be based on equation of the `threshold.max` option value.
   * @property {Array} [color.threshold.values] Threshold values for each steps
   * @property {number} [color.threshold.max=100] The base value to determine threshold step value condition. When the given value is 15 and max 10, then the value for threshold is `15*100/10`.
   * @example
   *  color: {
   *      pattern: ["#1f77b4", "#aec7e8", ...],
   *
   *      // Set colors' patterns
   *      // it should return an array of SVGPatternElement
   *      tiles: function() {
   *         var pattern = document.createElementNS("http://www.w3.org/2000/svg", "pattern");
   *         var g = document.createElementNS("http://www.w3.org/2000/svg", "g");
   *         var circle1 = document.createElementNS("http://www.w3.org/2000/svg", "circle");
   *
   *         pattern.setAttribute("patternUnits", "userSpaceOnUse");
   *         pattern.setAttribute("width", "32");
   *         pattern.setAttribute("height", "32");
   *
   *         g.style.fill = "#000";
   *         g.style.opacity = "0.2";
   *
   *         circle1.setAttribute("cx", "3");
   *         circle1.setAttribute("cy", "3");
   *         circle1.setAttribute("r", "3");
   *
   *         g.appendChild(circle1);
   *         pattern.appendChild(g);
   *
   *         return [pattern];
   *      },
   *
   *      // for threshold usage, pattern values should be set for each steps
   *      pattern: ["grey", "green", "yellow", "orange", "red"],
   *      threshold: {
   *          unit: "value",
   *
   *          // when value is 20 => 'green', value is 40 => 'orange' will be set.
   *          values: [10, 20, 30, 40, 50],
   *
   *          // the equation for max:
   *          // - unit == 'value': max => 30
   *          // - unit != 'value': max => value*100/30
   *          max: 30
   *      },
   *
   *      // set all data to 'red'
   *      onover: "red",
   *
   *      // set different color for data
   *      onover: {
   *          data1: "red",
   *          data2: "yellow"
   *      },
   *
   *      // will pass data object to the callback
   *      onover: function(d) {
   *          return d.id === "data1" ? "red" : "green";
   *      }
   *  }
   */
  color_pattern: [],
  color_tiles: void 0,
  color_threshold: {},
  color_onover: void 0
}, s_ = {
  /**
   * Legend options
   * @name legend
   * @memberof Options
   * @type {object}
   * @property {object} legend Legend object
   * @property {boolean} [legend.show=true] Show or hide legend.
   * @property {boolean} [legend.hide=false] Hide legend
   *  If true given, all legend will be hidden. If string or array given, only the legend that has the id will be hidden.
   * @property {string|HTMLElement} [legend.contents.bindto=undefined] Set CSS selector or element reference to bind legend items.
   * - **NOTE:** Should be used along with `legend.contents.template`.
   * @property {string|Function} [legend.contents.template="<span style='color:#fff;padding:5px;background-color:{=COLOR}'>{=TITLE}</span>"] Set item's template.<br>
   *  - If set `string` value, within template the 'color' and 'title' can be replaced using template-like syntax string:
   *    - {=COLOR}: data color value
   *    - {=TITLE}: data title value
   *  - If set `function` value, will pass following arguments to the given function:
   *   - title {string}: data's id value
   *   - color {string}: color string
   *   - data {Array}: data array
   * @property {string} [legend.position=bottom] Change the position of legend.<br>
   *  Available values are: `bottom`, `right` and `inset` are supported.
   * @property {object} [legend.inset={anchor: 'top-left',x: 10,y: 0,step: undefined}] Change inset legend attributes.<br>
   *  This option accepts object that has the keys `anchor`, `x`, `y` and `step`.
   *  - **anchor** decides the position of the legend:
   *   - top-left
   *   - top-right
   *   - bottom-left
   *   - bottom-right
   *  - **x** and **y**:
   *   - set the position of the legend based on the anchor.
   *  - **step**:
   *   - defines the max step the legend has (e.g. If 2 set and legend has 3 legend item, the legend 2 columns).
   * @property {boolean} [legend.equally=false] Set to all items have same width size.
   * @property {number} [legend.padding=0] Set padding value
   * @property {boolean} [legend.item.interaction=true] Set legend item interaction.
   *  - **NOTE:**
   *    - This setting will not have effect on `.toggle()` method.
   *    - `legend.item.onXXX` listener options will work if set, regardless of this option value.
   * @property {boolean} [legend.item.interaction.dblclick=false] Set legend item to interact on double click.
   *  - **NOTE:**
   *    - Double clicking will make focused clicked dataseries only, hiding all others.
   *      - for single click case, `click + altKey(Win)/optionKey(Mac OS)` to have same effect.
   *    - To return initial state(which all dataseries are showing), double click current focused legend item again.
   *      - for single click case, `click + altKey(Win)/optionKey(Mac OS)` to have same effect.
   *    - In this case, default `click` interaction will be disabled.
   * @property {Function} [legend.item.onclick=undefined] Set click event handler to the legend item.
   *  - **NOTE:**
   *    - When set, default `click` interaction will be disabled.
   *    - When `interaction.dblclick=true` is set, will be called on double click.
   * @property {Function} [legend.item.onover=undefined] Set mouse/touch over event handler to the legend item.
   *  - **NOTE:** When set, default `mouseover` interaction will be disabled.
   * @property {Function} [legend.item.onout=undefined] Set mouse/touch out event handler to the legend item.
   *  - **NOTE:** When set, default `mouseout` interaction will be disabled.
   * @property {number} [legend.item.tile.width=10] Set width for 'rectangle' legend item tile element.
   * @property {number} [legend.item.tile.height=10] Set height for 'rectangle' legend item tile element.
   * @property {number} [legend.item.tile.r=5] Set the radius for 'circle' legend item tile type.
   * @property {string} [legend.item.tile.type="rectangle"] Set legend item shape type.<br>
   * - **Available Values:**
   *   - circle
   *   - rectangle
   * @property {boolean} [legend.format] Set formatter function for legend text.
   * The argument:<br>
   *  - `id`: Legend text value. When `data.names` is specified, will pass from it, otherwise will pass data id.
   *  - `dataId`: When `data.names` specified, will pass the original data id. Otherwise will be undefined.
   * @property {boolean} [legend.tooltip=false] Show full legend text value using system tooltip(via `<title>` element).
   * @property {boolean} [legend.usePoint=false] Whether to use custom points in legend.
   * @see [Demo: format](https://naver.github.io/billboard.js/demo/#Legend.LegendFormat)
   * @see [Demo: item.interaction](https://naver.github.io/billboard.js/demo/#Legend.LegendItemInteraction)
   * @see [Demo: item.tile.type](https://naver.github.io/billboard.js/demo/#Legend.LegendItemTileType)
   * @see [Demo: position](https://naver.github.io/billboard.js/demo/#Legend.LegendPosition)
   * @see [Demo: contents.template](https://naver.github.io/billboard.js/demo/#Legend.LegendTemplate1)
   * @see [Demo: usePoint](https://naver.github.io/billboard.js/demo/#Legend.usePoint)
   * @example
   *  legend: {
   *      show: true,
   *      hide: true,
   *      //or hide: "data1"
   *      //or hide: ["data1", "data2"]
   *      contents: {
   *          bindto: "#legend",   // <ul id='legend'></ul>
   *
   *          // will be as: <li style='background-color:#1f77b4'>data1</li>
   *          template: "<li style='background-color:{=COLOR}'>{=TITLE}</li>"
   *
   *          // or using function
   *          template: function(id, color, data) {
   *               // if you want omit some legend, return falsy value
   *               if (id !== "data1") {
   *                    return "<li style='background-color:"+ color +">"+ id +"</li>";
   *               }
   *          }
   *      },
   *      position: "bottom",  // bottom, right, inset
   *      inset: {
   *          anchor: "top-right"  // top-left, top-right, bottom-left, bottom-right
   *          x: 20,
   *          y: 10,
   *          step: 2
   *      },
   *      equally: false,
   *      padding: 10,
   *      item: {
   *          // will disable default interaction
   *          interaction: false,
   *
   *          // set legend interact on double click
   *          // by double clicking, will make focused clicked dataseries only, hiding all others.
   *          interaction: {
   *            dblclick: true
   *          }
   *
   *          // when set below callback, will disable corresponding default interactions
   *          onclick: function(id, visible) {
   *           	// toggle based on the data visibility
   *           	this[visible ? "hide" : "show"](id);
   *          },
   *          onover: function(id, visible) { ... },
   *          onout: function(id, visible) { ... },
   *
   *          // set tile's size
   *          tile: {
   *              // set tile type
   *              type: "circle"  // or "rectangle" (default)
   *
   *              // width & height, are only applicable for 'rectangle' legend type
   *              width: 15,
   *              height: 15
   *
   *              // radis is only applicable for 'circle' legend type
   *              r: 10
   *          }
   *      },
   *      format: function(id, dataId) {
   *          // set ellipsis string when length is > 5
   *          // to get full legend value, combine with 'legend.tooltip=true'
   *          if (id.length > 5) {
   *            	id = id.substr(0, 5) + "...";
   *          }
   *
   *          return id;
   *      },
   *      tooltip: true,
   *      usePoint: true
   *  }
   */
  legend_contents_bindto: void 0,
  legend_contents_template: "<span style='color:#fff;padding:5px;background-color:{=COLOR}'>{=TITLE}</span>",
  legend_equally: !1,
  legend_hide: !1,
  legend_inset_anchor: "top-left",
  legend_inset_x: 10,
  legend_inset_y: 0,
  legend_inset_step: void 0,
  legend_item_interaction: !0,
  legend_item_dblclick: !1,
  legend_item_onclick: void 0,
  legend_item_onover: void 0,
  legend_item_onout: void 0,
  legend_item_tile_width: 10,
  legend_item_tile_height: 10,
  legend_item_tile_r: 5,
  legend_item_tile_type: "rectangle",
  legend_format: void 0,
  legend_padding: 0,
  legend_position: "bottom",
  legend_show: !0,
  legend_tooltip: !1,
  legend_usePoint: !1
}, u_ = {
  /**
   * Specify the CSS selector or the element which the chart will be set to. D3 selection object can be specified also.<br>
   * If other chart is set already, it will be replaced with the new one (only one chart can be set in one element).
   * - **NOTE:** In case of element doesn't exist or not specified, will add a `<div>` element to the body.
   * @name bindto
   * @memberof Options
   * @property {string|HTMLElement|d3.selection|object} [bindto="#chart"] Specify the element where chart will be drawn.
   * @property {string|HTMLElement|d3.selection} bindto.element="#chart" Specify the element where chart will be drawn.
   * @property {string} [bindto.classname=bb] Specify the class name of bind element.<br>
   *     **NOTE:** When class name isn't `bb`, then you also need to update the default CSS to be rendered correctly.
   * @default #chart
   * @example
   * bindto: "#myContainer"
   *
   * // or HTMLElement
   * bindto: document.getElementById("myContainer")
   *
   * // or D3 selection object
   * bindto: d3.select("#myContainer")
   *
   * // or to change default classname
   * bindto: {
   *    element: "#chart",
   *    classname: "bill-board"  // ex) <div id='chart' class='bill-board'>
   * }
   */
  bindto: "#chart",
  /**
   * Set chart background.
   * @name background
   * @memberof Options
   * @property {object} background background object
   * @property {string} background.class Specify the class name for background element.
   * @property {string} background.color Specify the fill color for background element.<br>**NOTE:** Will be ignored if `imgUrl` option is set.
   * @property {string} background.imgUrl Specify the image url string for background.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.Background)
   * @example
   * background: {
   *    class: "myClass",
   *    color: "red",
   *
   *    // Set image url for background.
   *    // If specified, 'color' option will be ignored.
   *    imgUrl: "https://naver.github.io/billboard.js/img/logo/billboard.js.svg",
   * }
   */
  background: {},
  /**
   * Set 'clip-path' attribute for chart element
   * - **NOTE:**
   *  > When is false, chart node element is positioned after the axis node in DOM tree hierarchy.
   *  > Is to make chart element positioned over axis element.
   * @name clipPath
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.clipPath)
   * @example
   * // don't set 'clip-path' attribute
   * clipPath: false
   */
  clipPath: !0,
  /**
   * Set svg element's class name
   * @name svg
   * @memberof Options
   * @type {object}
   * @property {object} [svg] svg object
   * @property {string} [svg.classname] class name for svg element
   * @example
   * svg: {
   *   classname: "test_class"
   * }
   */
  svg_classname: void 0,
  /**
   * The desired size of the chart element.
   * If value is not specified, the width of the chart will be calculated by the size of the parent element it's appended to.
   * @name size
   * @memberof Options
   * @type {object}
   * @property {object} [size] size object
   * @property {number} [size.width] width of the chart element
   * @property {number} [size.height] height of the chart element
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.ChartSize)
   * @example
   * size: {
   *   width: 640,
   *   height: 480
   * }
   */
  size_width: void 0,
  size_height: void 0,
  /**
   * The padding of the chart element.
   * - **NOTE:** for more information, see the "[`Understanding padding`](https://github.com/naver/billboard.js/wiki/Understanding-padding)"" wiki documentaion.
   * @name padding
   * @memberof Options
   * @type {object}
   * @property {object|boolean} [padding=true] Set padding of chart, and accepts object or boolean type.
   * - `Object`: Specify each side's padding.
   * - `false`: Remove padding completely and make shape to fully occupy the container element.
   *   - In this case, axes and subchart will be hidden.
   *   - To adjust some padding from this state, use `axis.[x|y].padding` option.
   * @property {string} [padding.mode] padding mode
   * - `"fit"`: Reduce padding as much as possible to make chart fit to the container element for chart types w/axis.<br>When specified, all padding values will be relative from fitted value.
   * @property {number} [padding.top] padding on the top of chart
   * @property {number} [padding.right] padding on the right of chart
   * @property {number} [padding.bottom] padding on the bottom of chart
   * @property {number} [padding.left] padding on the left of chart
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.Padding)
   * @see [Demo: Fit padding](https://naver.github.io/billboard.js/demo/#ChartOptions.FitPadding)
   * @example
   * // remove padding completely.
   * padding: false,
   *
   * padding: {
   *   // specifying mode value, will reduce padding and make fit to the container element.
   *   mode: "fit"
   *
   *   // when mode is "fit", all padding values will be relative from fitted value.
   *   // so, 0 will be initial fitted value.
   *   top: 20,
   *   right: 20,
   *   bottom: 20,
   *   left: 20
   * }
   *
   * // or specify padding value for each side
   * padding: {
   *   top: 20,
   *   right: 20,
   *   bottom: 20,
   *   left: 20
   * }
   */
  padding: !0,
  padding_mode: void 0,
  padding_left: void 0,
  padding_right: void 0,
  padding_top: void 0,
  padding_bottom: void 0,
  /**
   * Set chart resize options
   * @name resize
   * @memberof Options
   * @type {object}
   * @property {object} [resize] resize object
   * @property {boolean|string} [resize.auto=true] Set chart resize automatically on viewport changes.
   * - **NOTE:** Available options
   *   - true: Enables automatic resize.
   *   - false: Disables automatic resize.
   *   - "viewBox": Enables automatic resize, and size will be fixed based on the viewbox.
   * @property {boolean|number} [resize.timer=true] Set resize timer option.
   * - **NOTE:** Available options
   *   - The resize function will be called using:
   *     - true: `setTimeout()`
   *     - false: `requestIdleCallback()`
   *   - Given number(delay in ms) value, resize function will be triggered using `setTimeout()` with given delay.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.resizeViewBox)
   * @example
   *  resize: {
   *      auto: false,
   *
   *      // set resize based on viewBox value
   *      auto: "viewBox",
   *
   *      // set resize function will be triggered using `setTimeout()`
   *      timer: true,
   *
   *      // set resize function will be triggered using `requestIdleCallback()`
   *      timer: false,
   *
   *      // set resize function will be triggered using `setTimeout()` with a delay of `100ms`.
   *      timer: 100
   *  }
   */
  resize_auto: !0,
  resize_timer: !0,
  /**
   * Set a callback to execute when the chart is clicked.
   * @name onclick
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onclick: function(event) {
   *   this; // chart instance itself
   *   event; // native event object
   *   ...
   * }
   */
  onclick: void 0,
  /**
   * Set a callback to execute when mouse/touch enters the chart.
   * @name onover
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onover: function(event) {
   *   this; // chart instance itself
   *   event; // native event object
   *   ...
   * }
   */
  onover: void 0,
  /**
   * Set a callback to execute when mouse/touch leaves the chart.
   * @name onout
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onout: function(event) {
   *   this; // chart instance itself
   *   event; // native event object
   *   ...
   * }
   */
  onout: void 0,
  /**
   * Set a callback to execute when user resizes the screen.
   * @name onresize
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onresize: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onresize: void 0,
  /**
   * Set a callback to execute when screen resize finished.
   * @name onresized
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onresized: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onresized: void 0,
  /**
   * Set a callback to execute before the chart is initialized
   * @name onbeforeinit
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onbeforeinit: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onbeforeinit: void 0,
  /**
   * Set a callback to execute when the chart is initialized.
   * @name oninit
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * oninit: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  oninit: void 0,
  /**
   * Set a callback to execute after the chart is initialized
   * @name onafterinit
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onafterinit: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onafterinit: void 0,
  /**
   * Set a callback which is executed when the chart is rendered. Basically, this callback will be called in each time when the chart is redrawed.
   * @name onrendered
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onrendered: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onrendered: void 0,
  /**
   * Set duration of transition (in milliseconds) for chart animation.<br><br>
   * - **NOTE:** If `0 `or `null` set, transition will be skipped. So, this makes initial rendering faster especially in case you have a lot of data.
   * @name transition
   * @memberof Options
   * @type {object}
   * @property {object} [transition] transition object
   * @property {number} [transition.duration=350] duration in milliseconds
   * @example
   * transition: {
   *    duration: 500
   * }
   */
  transition_duration: 250,
  /**
   * Set plugins
   * @name plugins
   * @memberof Options
   * @type {Array}
   * @example
   *  plugins: [
   *    new bb.plugin.stanford({ ... }),
   *    new PluginA(),
   *    ...
   * ]
   */
  plugins: [],
  /**
   * Control the render timing
   * @name render
   * @memberof Options
   * @type {object}
   * @property {object} [render] render object
   * @property {boolean} [render.lazy=true] Make to not render at initialization.
   * - **NOTE**:
   *   - Enabled by default when bind element's visibility is hidden.
   *   - When set to `false`, will initialize the chart regardless the bind element's visibility state, but in this case chart can't be guaranteed to be rendered properly.
   * @property {boolean} [render.observe=true] Observe bind element's visibility(`display` or `visiblity` inline css property or class value) & render when is visible automatically (for IEs, only works IE11+). When set to **false**, call [`.flush()`](./Chart.html#flush) to render.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.LazyRender)
   * @example
   *  render: {
   *    lazy: true,
   *    observe: true
   * }
   *
   * @example
   * 	// <!-- render.lazy will detect visibility defined -->
   *  // (a) <div id='chart' class='hide'></div>
   *  // (b) <div id='chart' style='display:none'></div>
   *
   *  // render.lazy enabled by default when element is hidden
   *  var chart = bb.generate({ ... });
   *
   *  // chart will be rendered automatically when element's visibility changes
   *  // Note: works only for inlined css property or class attribute changes
   *  document.getElementById('chart').classList.remove('hide')  // (a)
   *  document.getElementById('chart').style.display = 'block';  // (b)
   *
   * @example
   * 	// chart won't be rendered and not observing bind element's visiblity changes
   *  var chart = bb.generate({
   *     render: {
   *          lazy: true,
   *          observe: false
   *     }
   *  });
   *
   *  // call at any point when you want to render
   *  chart.flush();
   */
  render: {},
  /**
   * Show rectangles inside the chart.<br><br>
   * This option accepts array including object that has axis, start, end and class.
   * The keys start, end and class are optional.
   * axis must be x, y or y2. start and end should be the value where regions start and end.
   * If not specified, the edge values will be used.
   * If timeseries x axis, date string, Date object and unixtime integer can be used.
   * If class is set, the region element will have it as class.
   * @name regions
   * @memberof Options
   * @type {Array}
   * @default []
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Region.RegionLabel)
   * @example
   *  regions: [
   *    {
   *      axis: "x",
   *      start: 1,
   *      end: 4,
   *      class: "region-1-4",
   *      label: {
   *      	text: "Region Text",
   *      	x: 5,  // position relative of the initial x coordinate
   *      	y: 5,  // position relative of the initial y coordinate
   *      	color: "red",  // color string
   *      	rotated: true  // make text to show in vertical or horizontal
   *      }
   *    }
   *  ]
   */
  regions: []
}, l_ = {
  /**
   * Set title options
   * @name title
   * @memberof Options
   * @type {object}
   * @property {object} title Title object
   * @property {string} [title.text] Title text. If contains `\n`, it's used as line break allowing multiline title.
   * @property {number} [title.padding.top=0] Top padding value.
   * @property {number} [title.padding.right=0] Right padding value.
   * @property {number} [title.padding.bottom=0] Bottom padding value.
   * @property {number} [title.padding.left=0] Left padding value.
   * @property {string} [title.position=center] Available values are: 'center', 'right' and 'left'.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Title.MultilinedTitle)
   * @example
   *  title: {
   *      text: "Title Text",
   *
   *      // or Multiline title text
   *      text: "Main title text\nSub title text",
   *
   *      padding: {
   *          top: 10,
   *          right: 10,
   *          bottom: 10,
   *          left: 10
   *      },
   *      position: "center"
   *  }
   */
  title_text: void 0,
  title_padding: {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  },
  title_position: "center"
}, c_ = {
  /**
   * Tooltip options
   * @name tooltip
   * @memberof Options
   * @type {object}
   * @property {object} tooltip Tooltip object
   * @property {boolean} [tooltip.show=true] Show or hide tooltip.
   * @property {boolean} [tooltip.doNotHide=false] Make tooltip keep showing not hiding on interaction.
   * @property {boolean} [tooltip.grouped=true] Set if tooltip is grouped or not for the data points.
   *   - **NOTE:** The overlapped data points will be displayed as grouped even if set false.
   * @property {boolean} [tooltip.linked=false] Set if tooltips on all visible charts with like x points are shown together when one is shown.
   * @property {string} [tooltip.linked.name=""] Groping name for linked tooltip.<br>If specified, linked tooltip will be groped interacting to be worked only with the same name.
   * @property {Function} [tooltip.format.title] Set format for the title of tooltip.<br>
   *  Specified function receives x of the data point to show.
   * @property {Function} [tooltip.format.name] Set format for the name of each data in tooltip.<br>
   *  Specified function receives name, ratio, id and index of the data point to show. ratio will be undefined if the chart is not donut/pie/gauge.
   * @property {Function} [tooltip.format.value] Set format for the value of each data value in tooltip. If undefined returned, the row of that value will be skipped to be called.
   *  - Will pass following arguments to the given function:
   *    - `value {string}`: Value of the data point. If data row contains multiple or ranged(ex. candlestick, area range, etc.) value, formatter will be called as value length.
   *    - `ratio {number}`: Ratio of the data point in the `pie/donut/gauge` and `area/bar` when contains grouped data. Otherwise is `undefined`.
   *    - `id {string}`: id of the data point
   *    - `index {number}`: Index of the data point
   * @property {Function} [tooltip.position] Set custom position function for the tooltip.<br>
   *  This option can be used to modify the tooltip position by returning object that has top and left.
   *  - Will pass following arguments to the given function:
   *    - `data {Array}`: Current selected data array object.
   *    - `width {number}`: Width of tooltip.
   *    - `height {number}`: Height of tooltip.
   *    - `element {SVGElement}`: Tooltip event bound element
   *    - `pos {object}`: Current position of the tooltip.
   * @property {Function|object} [tooltip.contents] Set custom HTML for the tooltip.<br>
   *  If tooltip.grouped is true, data includes multiple data points.<br><br>
   *  Specified function receives `data` array and `defaultTitleFormat`, `defaultValueFormat` and `color` functions of the data point to show.
   *  - **Note:**
   *    - defaultTitleFormat:
   *      - if `axis.x.tick.format` option will be used if set.
   *      - otherwise, will return function based on tick format type(category, timeseries).
   *    - defaultValueFormat:
   * 	    - for Arc type (except gauge, radar), the function will return value from `(ratio * 100).toFixed(1)`.
   * 	    - for Axis based types, will be used `axis.[y|y2].tick.format` option value if is set.
   * 	    - otherwise, will parse value and return as number.
   * @property {string|HTMLElement} [tooltip.contents.bindto=undefined] Set CSS selector or element reference to bind tooltip.
   *  - **NOTE:** When is specified, will not be updating tooltip's position.
   * @property {string} [tooltip.contents.template=undefined] Set tooltip's template.<br><br>
   *  Within template, below syntax will be replaced using template-like syntax string:
   *    - **{{ ... }}**: the doubly curly brackets indicate loop block for data rows.
   *    - **{=CLASS_TOOLTIP}**: default tooltip class name `bb-tooltip`.
   *    - **{=CLASS_TOOLTIP_NAME}**: default tooltip data class name (ex. `bb-tooltip-name-data1`)
   *    - **{=TITLE}**: title value.
   *    - **{=COLOR}**: data color.
   *    - **{=NAME}**: data id value.
   *    - **{=VALUE}**: data value.
   * @property {object} [tooltip.contents.text=undefined] Set additional text content within data loop, using template syntax.
   *  - **NOTE:** It should contain `{ key: Array, ... }` value
   *    - 'key' name is used as substitution within template as '{=KEY}'
   *    - The value array length should match with the data length
   * @property {boolean} [tooltip.init.show=false] Show tooltip at the initialization.
   * @property {number} [tooltip.init.x=0] Set x Axis index(or index for Arc(donut, gauge, pie) types) to be shown at the initialization.
   * @property {object} [tooltip.init.position] Set the position of tooltip at the initialization.
   * @property {Function} [tooltip.onshow] Set a callback that will be invoked before the tooltip is shown.
   * @property {Function} [tooltip.onhide] Set a callback that will be invoked before the tooltip is hidden.
   * @property {Function} [tooltip.onshown] Set a callback that will be invoked after the tooltip is shown
   * @property {Function} [tooltip.onhidden] Set a callback that will be invoked after the tooltip is hidden.
   * @property {string|Function|null} [tooltip.order=null] Set tooltip data display order.<br><br>
   *  **Available Values:**
   *  - `desc`: In descending data value order
   *  - `asc`: In ascending data value order
   *  - `null`: It keeps the data display order<br>
   *     **NOTE:** When `data.groups` is set, the order will follow as the stacked graph order.<br>
   *      If want to order as data bound, set any value rather than asc, desc or null. (ex. empty string "")
   *  - `function(data1, data2) { ... }`: [Array.sort compareFunction](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/sort#Parameters)
   * @see [Demo: Hide Tooltip](https://naver.github.io/billboard.js/demo/#Tooltip.HideTooltip)
   * @see [Demo: Tooltip Grouping](https://naver.github.io/billboard.js/demo/#Tooltip.TooltipGrouping)
   * @see [Demo: Tooltip Format](https://naver.github.io/billboard.js/demo/#Tooltip.TooltipFormat)
   * @see [Demo: Linked Tooltip](https://naver.github.io/billboard.js/demo/#Tooltip.LinkedTooltips)
   * @see [Demo: Tooltip Position](https://naver.github.io/billboard.js/demo/#Tooltip.TooltipPosition)
   * @see [Demo: Tooltip Template](https://naver.github.io/billboard.js/demo/#Tooltip.TooltipTemplate)
   * @example
   *  tooltip: {
   *      show: true,
   *      doNotHide: true,
   *      grouped: false,
   *      format: {
   *          title: function(x) { return "Data " + x; },
   *          name: function(name, ratio, id, index) { return name; },
   *
   *          // If data row contains multiple or ranged(ex. candlestick, area range, etc.) value,
   *          // formatter will be called as value length times.
   *          value: function(value, ratio, id, index) { return ratio; }
   *      },
   *      position: function(data, width, height, element, pos) {
   *          // data: [{x, index, id, name, value}, ...]
   *          // width: Tooltip width
   *          // height: Tooltip height
   *          // element: Tooltip event bound element
   *          // pos: {
   *          //   x: Current mouse event x position,
   *          //   y: Current mouse event y position,
   *          //   xAxis: Current x Axis position (the value is given for axis based chart type only)
   *          //   yAxis: Current y Axis position value or function(the value is given for axis based chart type only)
   *          // }
   *
   *          // yAxis will work differently per data lenghts
   *          // - a) Single data: `yAxis` will return `number` value
   *          // - b) Multiple data: `yAxis` will return a function with property value
   *
   *          // a) Single data:
   *          // Get y coordinate
   *          pos.yAxis; // y axis coordinate value of current data point
   *
   *          // b) Multiple data:
   *          // Get y coordinate of value 500, where 'data1' scales(y or y2).
   *          // When 'data.axes' option is used, data can bound to different axes.
   *          // - when "data.axes={data1: 'y'}", wil return y value from y axis scale.
   *          // - when "data.axes={data1: 'y2'}", wil return y value from y2 axis scale.
   *          pos.yAxis(500, "data1"); // will return y coordinate value of data1
   *
   *          pos.yAxis(500); // get y coordinate with value of 500, using y axis scale
   *          pos.yAxis(500, null, "y2"); // get y coordinate with value of 500, using y2 axis scale
   *
   *          return {
   *            top: 0,
   *            left: 0
   *          }
   *      },
   *
   *      contents: function(d, defaultTitleFormat, defaultValueFormat, color) {
   *          return ... // formatted html as you want
   *      },
   *
   *       // specify tooltip contents using template
   *       // - example of HTML returned:
   *       // <ul class="bb-tooltip">
   *       //   <li class="bb-tooltip-name-data1"><span>250</span><br><span style="color:#00c73c">data1</span></li>
   *       //   <li class="bb-tooltip-name-data2"><span>50</span><br><span style="color:#fa7171">data2</span></li>
   *       // </ul>
   *       contents: {
   *      	bindto: "#tooltip",
   *      	template: '<ul class={=CLASS_TOOLTIP}>{{' +
   *      			'<li class="{=CLASS_TOOLTIP_NAME}"><span>{=VALUE}</span><br>' +
   *      			'<span style=color:{=COLOR}>{=NAME}</span></li>' +
   *      		'}}</ul>'
   *      }
   *
   *       // with additional text value
   *       // - example of HTML returned:
   *       // <ul class="bb-tooltip">
   *       //   <li class="bb-tooltip-name-data1"><span>250</span><br>comment1<span style="color:#00c73c">data1</span>text1</li>
   *       //   <li class="bb-tooltip-name-data2"><span>50</span><br>comment2<span style="color:#fa7171">data2</span>text2</li>
   *       // </ul>
   *       contents: {
   *      	bindto: "#tooltip",
   *      	text: {
   *      		// a) 'key' name is used as substitution within template as '{=KEY}'
   *      		// b) the length should match with the data length
   *      		VAR1: ["text1", "text2"],
   *      		VAR2: ["comment1", "comment2"],
   *      	},
   *      	template: '<ul class={=CLASS_TOOLTIP}>{{' +
   *      			'<li class="{=CLASS_TOOLTIP_NAME}"><span>{=VALUE}</span>{=VAR2}<br>' +
   *      			'<span style=color:{=COLOR}>{=NAME}</span>{=VAR1}</li>' +
   *      		'}}</ul>'
   *      }
   *
   *      // sort tooltip data value display in ascending order
   *      order: "asc",
   *
   *      // specifying sort function
   *      order: function(a, b) {
   *         // param data passed format
   *         {x: 5, value: 250, id: "data1", index: 5, name: "data1"}
   *           ...
   *      },
   *
   *      // show at the initialization
   *      init: {
   *          show: true,
   *          x: 2, // x Axis index (or index for Arc(donut, gauge, pie) types)
   *          position: {
   *              top: "150px",  // specify as number or as string with 'px' unit string
   *              left: 250  // specify as number or as string with 'px' unit string
   *          }
   *      },
   *
   *      // fires prior tooltip is shown
   *      onshow: function(selectedData) {
   *      	// current dataset selected
   *      	// ==> [{x: 4, value: 150, id: "data2", index: 4, name: "data2"}, ...]
   *      	selectedData;
   *      },
   *
   *      // fires prior tooltip is hidden
   *      onhide: function(selectedData) {
   *      	// current dataset selected
   *      	// ==> [{x: 4, value: 150, id: "data2", index: 4, name: "data2"}, ...]
   *      	selectedData;
   *      },
   *
   *      // fires after tooltip is shown
   *      onshown: function(selectedData) {
   *      	// current dataset selected
   *      	// ==> [{x: 4, value: 150, id: "data2", index: 4, name: "data2"}, ...]
   *      	selectedData;
   *      },
   *
   *      // fires after tooltip is hidden
   *      onhidden: function(selectedData) {
   *      	// current dataset selected
   *      	// ==> [{x: 4, value: 150, id: "data2", index: 4, name: "data2"}, ...]
   *      	selectedData;
   *      },
   *
   *      // Link any tooltips when multiple charts are on the screen where same x coordinates are available
   *      // Useful for timeseries correlation
   *      linked: true,
   *
   *      // Specify name to interact those with the same name only.
   *      linked: {
   *          name: "some-group"
   *      }
   *  }
   */
  tooltip_show: !0,
  tooltip_doNotHide: !1,
  tooltip_grouped: !0,
  tooltip_format_title: void 0,
  tooltip_format_name: void 0,
  tooltip_format_value: void 0,
  tooltip_position: void 0,
  tooltip_contents: {},
  tooltip_init_show: !1,
  tooltip_init_x: 0,
  tooltip_init_position: void 0,
  tooltip_linked: !1,
  tooltip_linked_name: "",
  tooltip_onshow: function() {
  },
  tooltip_onhide: function() {
  },
  tooltip_onshown: function() {
  },
  tooltip_onhidden: function() {
  },
  tooltip_order: null
}, f_ = {
  /**
   * Specify the key of x values in the data.<br><br>
   * We can show the data with non-index x values by this option. This option is required when the type of x axis is timeseries. If this option is set on category axis, the values of the data on the key will be used for category names.
   * @name data․x
   * @memberof Options
   * @type {string}
   * @default undefined
   * @example
   * data: {
   *   x: "date"
   * }
   */
  data_x: void 0,
  /**
   * Converts data id value
   * @name data․idConverter
   * @memberof Options
   * @type {Function}
   * @default function(id) { return id; }
   * @example
   * data: {
   *    idConverter: function(id) {
   *       // when id is 'data1', converts to be 'data2'
   *       // 'data2' should be given as the initial data value
   *       if (id === "data1") {
   *          return "data2";
   *       } else {
   *          return id;
   *       }
   *    }
   * }
   */
  data_idConverter: function(e) {
    return e;
  },
  /**
   * Set custom data name.
   * If a name is set to `null`, the series is omitted from the legend.
   * @name data․names
   * @memberof Options
   * @type {object}
   * @default {}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataName)
   * @example
   * data: {
   *   names: {
   *     data1: "Data Name 1",
   *     data2: "Data Name 2"
   *   }
   * }
   */
  data_names: {},
  /**
   * Set custom data class.<br><br>
   * If this option is specified, the element g for the data has an additional class that has the prefix 'bb-target-' (eg. bb-target-additional-data1-class).
   * @name data․classes
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   classes: {
   *     data1: "additional-data1-class",
   *     data2: "additional-data2-class"
   *   }
   * }
   */
  data_classes: {},
  /**
   * Set chart type at once.<br><br>
   * If this option is specified, the type will be applied to every data. This setting can be overwritten by data.types.<br><br>
   * **Available Values:**
   * - area
   * - area-line-range
   * - area-spline
   * - area-spline-range
   * - area-step
   * - area-step-range
   * - bar
   * - bubble
   * - candlestick
   * - donut
   * - funnel
   * - gauge
   * - line
   * - pie
   * - polar
   * - radar
   * - scatter
   * - spline
   * - step
   * - treemap
   * @name data․type
   * @memberof Options
   * @type {string}
   * @default "line"<br>NOTE: When importing shapes by ESM, `line()` should be specified for type.
   * @example
   * data: {
   *    type: "bar"
   * }
   * @example
   * // Generate chart by importing ESM
   * // Import types to be used only, where this will make smaller bundle size.
   * import bb, {
   *   area,
   *   areaLineRange,
   *   areaSpline,
   *   areaSplineRange,
   *   areaStep,
   *   areaStepRange,
   *   bar,
   *   bubble,
   *   candlestick,
   *   donut,
   *   funnel,
   *   gauge,
   *   line,
   *   pie,
   *   polar,
   *   radar,
   *   scatter,
   *   spline,
   *   step,
   *   treemap
   * }
   *
   * bb.generate({
   *   ...,
   *   data: {
   *     type: bar()
   *   }
   * });
   */
  data_type: void 0,
  /**
   * Set chart type for each data.<br>
   * This setting overwrites data.type setting.
   * - **NOTE:** `radar` and `treemap` type can't be combined with other types.
   * @name data․types
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   types: {
   *     data1: "bar",
   *     data2: "spline"
   *   }
   * }
   * @example
   * // Generate chart by importing ESM
   * // Import types to be used only, where this will make smaller bundle size.
   * import bb, {
   *   area,
   *   areaLineRange,
   *   areaSpline,
   *   areaSplineRange,
   *   areaStep,
   *   areaStepRange,
   *   bar,
   *   bubble,
   *   candlestick,
   *   donut,
   *   funnel,
   *   gauge,
   *   line,
   *   pie,
   *   polar,
   *   radar,
   *   scatter,
   *   spline,
   *   step,
   *   treemap
   * }
   *
   * bb.generate({
   *   ...,
   *   data: {
   *     types: {
   *       data1: bar(),
   *       data1: spline()
   *     }
   *   }
   * });
   */
  data_types: {},
  /**
   *  This option changes the order of stacking data and pieces of pie/donut.
   *  - If `null` specified, it will be the order the data loaded.
   *  - If function specified, it will be used as [Array.sort compareFunction](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/sort#Parameters)<br><br>
   *
   *  **Available Values:**
   *  - `desc`: In descending order
   *  - `asc`: In ascending order
   *  - `null`: It keeps the data load order
   *  - `function(data1, data2) { ... }`: Array.sort compareFunction
   *
   *  **NOTE**: order function, only works for Axis based types & Arc types, except `Radar` type.
   * @name data․order
   * @memberof Options
   * @type {string|Function|null}
   * @default desc
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataOrder)
   * @example
   * data: {
   *   // in descending order (default)
   *   order: "desc"
   *
   *   // in ascending order
   *   order: "asc"
   *
   *   // keeps data input order
   *   order: null
   *
   *   // specifying sort function
   *   order: function(a, b) {
   *       // param data passed format
   *       // {
   *       //   id: "data1", id_org: "data1", values: [
   *       //      {x: 5, value: 250, id: "data1", index: 5, name: "data1"},
   *       //       ...
   *       //   ]
   *       // }
   *
   *       const reducer = (p, c) => p + Math.abs(c.value);
   *       const aSum = a.values.reduce(reducer, 0);
   *       const bSum = b.values.reduce(reducer, 0);
   *
   *       // ascending order
   *       return aSum - bSum;
   *
   *       // descending order
   *       // return bSum - aSum;
   *   }
   * }
   */
  data_order: "desc",
  /**
   * Set groups for the data for stacking.
   * @name data․groups
   * @memberof Options
   * @type {Array}
   * @default []
   * @example
   * data: {
   *   groups: [
   *     ["data1", "data2"],
   *     ["data3"]
   *   ]
   * }
   */
  data_groups: [],
  /**
   * Set how zero value will be treated on groups.<br>
   * Possible values:
   * - `zero`: 0 will be positioned at absolute axis zero point.
   * - `positive`: 0 will be positioned at the top of a stack.
   * - `negative`: 0 will be positioned at the bottom of a stack.
   * @name data․groupsZeroAs
   * @memberof Options
   * @type {string}
   * @default "positive"
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.Groups)
   * @example
   * data: {
   *   groupsZeroAs: "zero" // "positive" or "negative"
   * }
   */
  data_groupsZeroAs: "positive",
  /**
   * Set color converter function.<br><br>
   * This option should a function and the specified function receives color (e.g. '#ff0000') and d that has data parameters like id, value, index, etc. And it must return a string that represents color (e.g. '#00ff00').
   * @name data․color
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataColor)
   * @example
   * data: {
   *   color: function(color, d) { ... }
   * }
   */
  data_color: void 0,
  /**
   * Set color for each data.
   * @name data․colors
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   colors: {
   *     data1: "#ff0000",
   *     data2: function(d) {
   *        return "#000";
   *     }
   *     ...
   *   }
   * }
   */
  data_colors: {},
  /**
   * Set labels options
   * @name data․labels
   * @memberof Options
   * @type {object}
   * @property {object} data Data object
   * @property {boolean} [data.labels=false] Show or hide labels on each data points
   * @property {boolean} [data.labels.centered=false] Centerize labels on `bar` shape. (**NOTE:** works only for 'bar' type)
   * @property {Function} [data.labels.format] Set formatter function for data labels.<br>
   * The formatter function receives 4 arguments such as `v, id, i, texts` and it **must return a string** (`\n` character will be used as line break) that will be shown as the label.<br><br>
   * The arguments are:<br>
   *  - `v` is the value of the data point where the label is shown.
   *  - `id` is the id of the data where the label is shown.
   *  - `i` is the index of the data series point where the label is shown.
   *  - `texts` is the array of whole corresponding data series' text labels.<br><br>
   * Formatter function can be defined for each data by specifying as an object and D3 formatter function can be set (ex. d3.format('$'))
   * @property {string|object} [data.labels.backgroundColors] Set label text background colors.
   * @property {string|object|Function} [data.labels.colors] Set label text colors.
   * @property {object|Function} [data.labels.position] Set each dataset position, relative the original.<br><br>
   * When function is specified, will receives 5 arguments such as `type, v, id, i, texts` and it must return a position number.<br><br>
   * The arguments are:<br>
   *  - `type` coordinate type string, which will be 'x' or 'y'.
   *  - `v` is the value of the data point where the label is shown.
   *  - `id` is the id of the data where the label is shown.
   *  - `i` is the index of the data series point where the label is shown.
   *  - `texts` is the array of whole corresponding data series' text labels.<br><br>
   * @property {number} [data.labels.position.x=0] x coordinate position, relative the original.
   * @property {number} [data.labels.position.y=0] y coordinate position, relative the original.
   * @property {object} [data.labels.rotate] Rotate label text. Specify degree value in a range of `0 ~ 360`.
   * - **NOTE:** Depend on rotate value, text position need to be adjusted manually(using `data.labels.position` option) to be shown nicely.
   * @memberof Options
   * @type {object}
   * @default {}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataLabel)
   * @see [Demo: label colors](https://naver.github.io/billboard.js/demo/#Data.DataLabelColors)
   * @see [Demo: label format](https://naver.github.io/billboard.js/demo/#Data.DataLabelFormat)
   * @see [Demo: label multiline](https://naver.github.io/billboard.js/demo/#Data.DataLabelMultiline)
   * @see [Demo: label overlap](https://naver.github.io/billboard.js/demo/#Data.DataLabelOverlap)
   * @see [Demo: label position](https://naver.github.io/billboard.js/demo/#Data.DataLabelPosition)
   * @see [Demo: label rotate](https://naver.github.io/billboard.js/demo/#Data.DataLabelRotate)
   * @example
   * data: {
   *   labels: true,
   *
   *   // or set specific options
   *   labels: {
   *     format: function(v, id, i, texts) {
   *         ...
   *         // to multiline, return with '\n' character
   *         return "Line1\nLine2";
   *     },
   *
   *     // it's possible to set for each data
   *     format: {
   *         data1: function(v, id, i, texts) { ... },
   *         ...
   *     },
   *
   *     // align text to center of the 'bar' shape (works only for 'bar' type)
   *     centered: true,
   *
   *     // apply backgound color for label texts
   *     backgroundColors: "red",
   *
   *     // set differenct backround colors per dataset
   *     backgroundColors: {
   *          data1: "green",
   *          data2: "yellow"
   *     }
   *
   *     // apply for all label texts
   *     colors: "red",
   *
   *     // set different colors per dataset
   *     // for not specified dataset, will have the default color value
   *     colors: {
   *        data1: "yellow",
   *        data3: "green"
   *     },
   *
   *     // call back for label text color
   *     colors: function(color, d) {
   *         // color: the default data label color string
   *         // data: ex) {x: 0, value: 200, id: "data3", index: 0}
   *         ....
   *         return d.value > 200 ? "cyan" : color;
   *     },
   *
   *     // return x, y coordinate position
   *     // apt to handle each text position manually
   *     position: function(type, v, id, i, texts) {
   *         ...
   *         return type == "x" ? 10 : 20;
   *     },
   *
   *     // set x, y coordinate position
   *     position: {
   *        x: -10,
   *        y: 10
   *     },
   *
   *     // or set x, y coordinate position by each dataset
   *     position: {
   *        data1: {x: 5, y: 5},
   *        data2: {x: 10, y: -20}
   *     },
   *
   * 	   // rotate degree for label text
   *     rotate: 90
   *   }
   * }
   */
  data_labels: {},
  data_labels_backgroundColors: void 0,
  data_labels_colors: void 0,
  data_labels_position: {},
  /**
   * Hide each data when the chart appears.<br><br>
   * If true specified, all of data will be hidden. If multiple ids specified as an array, those will be hidden.
   * @name data․hide
   * @memberof Options
   * @type {boolean|Array}
   * @default false
   * @example
   * data: {
   *   // all of data will be hidden
   *   hide: true
   *
   *   // specified data will be hidden
   *   hide: ["data1", ...]
   * }
   */
  data_hide: !1,
  /**
   * Filter values to be shown
   * The data value is the same as the returned by `.data()`.
   * @see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/filter
   * @name data․filter
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * data: {
   *   // filter for id value
   *   filter: function(v) {
   *      // v: [{id: "data1", id_org: "data1", values: [
   *      //      {x: 0, value: 130, id: "data2", index: 0}, ...]
   *      //    }, ...]
   *      return v.id !== "data1";
   *   }
   */
  data_filter: void 0,
  /**
   * Set a callback for click event on each data point.<br><br>
   * This callback will be called when each data point clicked and will receive `d` and element as the arguments.
   * - `d` is the data clicked and element is the element clicked.
   * - `element` is the current interacting svg element.
   * - In this callback, `this` will be the Chart object.
   * @name data․onclick
   * @memberof Options
   * @type {Function}
   * @default function() {}
   * @example
   * data: {
   *     onclick: function(d, element) {
   *        // d - ex) {x: 4, value: 150, id: "data1", index: 4, name: "data1"}
   *        // element - <circle>
   *        ...
   *     }
   * }
   */
  data_onclick: function() {
  },
  /**
   * Set a callback for mouse/touch over event on each data point.<br><br>
   * This callback will be called when mouse cursor or via touch moves onto each data point and will receive `d` and `element` as the argument.
   * - `d` is the data where mouse cursor moves onto.
   * - `element` is the current interacting svg element.
   * - In this callback, `this` will be the Chart object.
   * @name data․onover
   * @memberof Options
   * @type {Function}
   * @default function() {}
   * @example
   * data: {
   *     onover: function(d, element) {
   *        // d - ex) {x: 4, value: 150, id: "data1", index: 4}
   *        // element - <circle>
   *        ...
   *     }
   * }
   */
  data_onover: function() {
  },
  /**
   * Set a callback for mouse/touch out event on each data point.<br><br>
   * This callback will be called when mouse cursor or via touch moves out each data point and will receive `d` as the argument.
   * - `d` is the data where mouse cursor moves out.
   * - `element` is the current interacting svg element.
   * - In this callback, `this` will be the Chart object.
   * @name data․onout
   * @memberof Options
   * @type {Function}
   * @default function() {}
   * @example
   * data: {
   *     onout: function(d, element) {
   *        // d - ex) {x: 4, value: 150, id: "data1", index: 4}
   *        // element - <circle>
   *        ...
   *     }
   * }
   */
  data_onout: function() {
  },
  /**
   * Set a callback for when data is shown.<br>
   * The callback will receive shown data ids in array.
   * @name data․onshown
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   *  data: {
   *    onshown: function(ids) {
   *      // ids - ["data1", "data2", ...]
   *      ...
   *    }
   *  }
   */
  data_onshown: void 0,
  /**
   * Set a callback for when data is hidden.<br>
   * The callback will receive hidden data ids in array.
   * @name data․onhidden
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   *  data: {
   *    onhidden: function(ids) {
   *      // ids - ["data1", "data2", ...]
   *      ...
   *    }
   *  }
   */
  data_onhidden: void 0,
  /**
   * Set a callback for minimum data
   * - **NOTE:** For 'area-line-range', 'area-step-range' and 'area-spline-range', `mid` data will be taken for the comparison
   * @name data․onmin
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.OnMinMaxCallback)
   * @example
   *  onmin: function(data) {
   *    // data - ex) [{x: 3, value: 400, id: "data1", index: 3}, ... ]
   *    ...
   *  }
   */
  data_onmin: void 0,
  /**
   * Set a callback for maximum data
   * - **NOTE:** For 'area-line-range', 'area-step-range' and 'area-spline-range', `mid` data will be taken for the comparison
   * @name data․onmax
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.OnMinMaxCallback)
   * @example
   *  onmax: function(data) {
   *    // data - ex) [{x: 3, value: 400, id: "data1", index: 3}, ... ]
   *    ...
   *  }
   */
  data_onmax: void 0,
  /**
   * Load a CSV or JSON file from a URL. NOTE that this will not work if loading via the "file://" protocol as the most browsers will block XMLHTTPRequests.
   * @name data․url
   * @memberof Options
   * @type {string}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.LoadData)
   * @example
   * data: {
   *     url: "/data/test.csv"
   * }
   */
  data_url: void 0,
  /**
   * XHR header value
   * - **NOTE:** Should be used with `data.url` option
   * @name data․headers
   * @memberof Options
   * @type {string}
   * @default undefined
   * @see https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequest/setRequestHeader
   * @example
   * data: {
   *     url: "/data/test.csv",
   *     headers: {
   *        "Content-Type": "text/xml",
   *        ...
   *     }
   * }
   */
  data_headers: void 0,
  /**
   * Parse a JSON object for data. See also data.keys.
   * @name data․json
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @see [data․keys](#.data%25E2%2580%25A4keys)
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.JSONData)
   * @example
   * data: {
   *     json: [
   *       {name: "www.site1.com", upload: 200, download: 200, total: 400},
   *       {name: "www.site2.com", upload: 100, download: 300, total: 400},
   *       {name: "www.site3.com", upload: 300, download: 200, total: 500},
   *       {name: "www.site4.com", upload: 400, download: 100, total: 500}
   *     ],
   *     keys: {
   *       // case 1: specify 'x' key for category axis
   *       x: "name", // 'name' key will be used as category x axis values
   *       value: ["upload", "download"]
   *
   *       // case 2: without 'x' key for non-category axis
   *       value: ["upload", "download"]
   *     }
   * }
   */
  data_json: void 0,
  /**
   * Load data from a multidimensional array, with the first element containing the data names, the following containing related data in that order.
   * @name data․rows
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.RowOrientedData)
   * @example
   * data: {
   *   rows: [
   *     ["A", "B", "C"],
   *     [90, 120, 300],
   *     [40, 160, 240],
   *     [50, 200, 290],
   *     [120, 160, 230],
   *     [80, 130, 300],
   *     [90, 220, 320]
   *   ]
   * }
   *
   * // for 'bar' type, data can contain:
   * // - an array of [start, end] data following the order
   * data: {
   *   rows: [
   *      ["data1", "data2"],
   *      [[100, 150], 120],
   *      [[200, 300], 55],
   *      [[-400, 500], 60]
   *   ],
   *   type: "bar"
   * }
   *
   * // for 'range' types('area-line-range' or 'area-step-range' or 'area-spline-range'), data should contain:
   * // - an array of [high, mid, low] data following the order
   * // - or an object with 'high', 'mid' and 'low' key value
   * data: {
   *   rows: [
   *      ["data1", "data2"],
   *      [
   *        // or {high:150, mid: 140, low: 110}, 120
   *        [150, 140, 110], 120
   *      ],
   *      [[155, 130, 115], 55],
   *      [[160, 135, 120], 60]
   *   ],
   *   types: {
   *       data1: "area-line-range",
   *       data2: "line"
   *   }
   * }
   *
   * // for 'bubble' type, data can contain dimension value:
   * // - an array of [y, z] data following the order
   * // - or an object with 'y' and 'z' key value
   * // 'y' is for y axis coordination and 'z' is the bubble radius value
   * data: {
   *   rows: [
   *      ["data1", "data2"],
   *      [
   *        // or {y:10, z: 140}, 120
   *        [10, 140], 120
   *      ],
   *      [[100, 30], 55],
   *      [[50, 100], 60]
   *   ],
   *   types: {
   *       data1: "bubble",
   *       data2: "line"
   *   }
   * }
   *
   * // for 'canlestick' type, data should contain:
   * // - an array of [open, high, low, close, volume(optional)] data following the order
   * // - or an object with 'open', 'high', 'low', 'close' and 'value'(optional) key value
   * data: {
   *   rows: [
   *      ["data1", "data2"],
   * 		[
   * 			// open, high, low, close, volume (optional)
   * 			{open: 1300, high: 1369, low: 1200, close: 1339, volume: 100},
   * 			[1000, 1100, 850, 870]
   * 		],
   * 		[
   * 			{open: 1348, high: 1371, low: 1271, close: 1320},
   * 			[870, 1250, 830, 1200, 50]
   * 		]
   *   ],
   *   type: "candlestick"
   * }
   */
  data_rows: void 0,
  /**
   * Load data from a multidimensional array, with each element containing an array consisting of a datum name and associated data values.
   * @name data․columns
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.ColumnOrientedData)
   * @example
   * data: {
   *   columns: [
   *      ["data1", 30, 20, 50, 40, 60, 50],
   *      ["data2", 200, 130, 90, 240, 130, 220],
   *      ["data3", 300, 200, 160, 400, 250, 250]
   *   ]
   * }
   *
   * // for 'bar' type, data can contain:
   * // - an array of [start, end] data following the order
   * data: {
   *   columns: [
   *     ["data1", -100, 50, [100, 200], [200, 300]],
   *     ["data2", -200, 300, [-100, 100], [-50, -30]],
   *   ],
   *   type: "bar"
   * }
   *
   * // for 'range' types('area-line-range' or 'area-step-range' or 'area-spline-range'), data should contain:
   * // - an array of [high, mid, low] data following the order
   * // - or an object with 'high', 'mid' and 'low' key value
   * data: {
   *   columns: [
   *      ["data1",
   *          [150, 140, 110],  // or {high:150, mid: 140, low: 110}
   *          [150, 140, 110],
   *          [150, 140, 110]
   *      ]
   *   ],
   *   type: "area-line-range"
   * }
   *
   * // for 'bubble' type, data can contain dimension value:
   * // - an array of [y, z] data following the order
   * // - or an object with 'y' and 'z' key value
   * // 'y' is for y axis coordination and 'z' is the bubble radius value
   * data: {
   *   columns: [
   *      ["data1",
   *          [10, 140],  // or {y:10, z: 140}
   *          [100, 30],
   *          [50, 100]
   *      ]
   *   ],
   *   type: "bubble"
   * }
   *
   * // for 'canlestick' type, data should contain:
   * // - an array of [open, high, low, close, volume(optional)] data following the order
   * // - or an object with 'open', 'high', 'low', 'close' and 'value'(optional) key value
   * data: {
   *   columns: [
   *      ["data1",
   *          [1000, 1100, 850, 870, 100],  // or {open:1000, high: 1100, low: 870, volume: 100}
   *          [870, 1250, 830, 1200]  // 'volume' can be omitted
   *      ]
   *   ],
   *   type: "candlestick"
   * }
   */
  data_columns: void 0,
  /**
   * Used if loading JSON via data.url.
   * - **Available Values:**
   *   - json
   *   - csv
   *   - tsv
   * @name data․mimeType
   * @memberof Options
   * @type {string}
   * @default csv
   * @example
   * data: {
   *     mimeType: "json"
   * }
   */
  data_mimeType: "csv",
  /**
   * Choose which JSON object keys correspond to desired data.
   * - **NOTE:** Only for JSON object given as array.
   * @name data․keys
   * @memberof Options
   * @type {string}
   * @default undefined
   * @example
   * data: {
   *     json: [
   *       {name: "www.site1.com", upload: 200, download: 200, total: 400},
   *       {name: "www.site2.com", upload: 100, download: 300, total: 400},
   *       {name: "www.site3.com", upload: 300, download: 200, total: 500},
   *       {name: "www.site4.com", upload: 400, download: 100, total: 500}
   *     ],
   *     keys: {
   *       // case 1: specify 'x' key for category axis
   *       x: "name", // 'name' key will be used as category x axis values
   *       value: ["upload", "download"]
   *
   *       // case 2: without 'x' key for non-category axis
   *       value: ["upload", "download"]
   *     }
   * }
   */
  data_keys: void 0,
  /**
   * Set text label to be displayed when there's no data to show.
   * - ex. Toggling all visible data to not be shown, unloading all current data, etc.
   * @name data․empty․label․text
   * @memberof Options
   * @type {string}
   * @default ""
   * @example
   * data: {
   *   empty: {
   *     label: {
   *       text: "No Data"
   *     }
   *   }
   * }
   */
  data_empty_label_text: ""
}, d_ = {
  /**
   * Interaction options
   * @name interaction
   * @memberof Options
   * @type {object}
   * @property {object} interaction Intersection object
   * @property {boolean} [interaction.enabled=true] Indicate if the chart should have interactions.<br>
   *     If `false` is set, all of interactions (showing/hiding tooltip, selection, mouse events, etc) will be disabled.
   * @property {boolean} [interaction.brighten=true] Make brighter for the selected area (ex. 'pie' type data selected area)
   * @property {boolean} [interaction.inputType.mouse=true] enable or disable mouse interaction
   * @property {boolean} [interaction.inputType.touch=true] enable or disable  touch interaction
   * @property {boolean|number} [interaction.inputType.touch.preventDefault=false] enable or disable to call event.preventDefault on touchstart & touchmove event. It's usually used to prevent document scrolling.
   * @property {boolean} [interaction.onout=true] Enable or disable "onout" event.<br>
   * 		When is disabled, defocus(hiding tooltip, focused gridline, etc.) event won't work.
   * @see [Demo: touch.preventDefault](https://naver.github.io/billboard.js/demo/#Interaction.PreventScrollOnTouch)
   * @example
   * interaction: {
   *    enabled: false,
   *    brighten: false,
   *    inputType: {
   *        mouse: true,
   *        touch: false
   *
   *        // or declare preventDefault explicitly.
   *        // In this case touch inputType is enabled by default
   *        touch: {
   *            preventDefault: true
   *
   *            // or threshold pixel value (pixel moved from touchstart to touchmove)
   *            preventDefault: 5
   *        }
   *    },
   *
   *    // disable "onout" event
   *    onout: false
   * }
   */
  interaction_enabled: !0,
  interaction_brighten: !0,
  interaction_inputType_mouse: !0,
  interaction_inputType_touch: {},
  interaction_onout: !0
};
function h_() {
  return typeof globalThis == "object" && globalThis !== null && globalThis.Object === Object && globalThis || typeof global == "object" && global !== null && global.Object === Object && global || typeof self == "object" && self !== null && self.Object === Object && self || Function("return this")();
}
function g_(e) {
  var t = typeof (e == null ? void 0 : e.requestAnimationFrame) == "function" && typeof (e == null ? void 0 : e.cancelAnimationFrame) == "function", n = typeof (e == null ? void 0 : e.requestIdleCallback) == "function" && typeof (e == null ? void 0 : e.cancelIdleCallback) == "function", i = function(a) {
    return setTimeout(a, 1);
  }, r = function(a) {
    return clearTimeout(a);
  };
  return [
    t ? e.requestAnimationFrame : i,
    t ? e.cancelAnimationFrame : r,
    n ? e.requestIdleCallback : i,
    n ? e.cancelIdleCallback : r
  ];
}
var Me = h_(), je = Me == null ? void 0 : Me.document, Kl = g_(Me), p_ = Kl[0], Ql = Kl[2], Ae = function(e) {
  return e || e === 0;
}, he = function(e) {
  return typeof e == "function";
}, Ne = function(e) {
  return typeof e == "string";
}, ge = function(e) {
  return typeof e == "number";
}, at = function(e) {
  return typeof e == "undefined";
}, Ye = function(e) {
  return typeof e != "undefined";
}, oo = function(e) {
  return typeof e == "boolean";
}, v_ = function(e) {
  return Math.ceil(e / 10) * 10;
}, ar = function(e) {
  return Math.ceil(e) + 0.5;
}, Ri = function(e) {
  return e[1] - e[0];
}, Jt = function(e) {
  return typeof e == "object";
}, en = function(e) {
  return at(e) || e === null || Ne(e) && e.length === 0 || Jt(e) && !(e instanceof Date) && Object.keys(e).length === 0 || ge(e) && isNaN(e);
}, Qe = function(e) {
  return !en(e);
}, Ie = function(e) {
  return Array.isArray(e);
}, Be = function(e) {
  return e && !(e != null && e.nodeType) && Jt(e) && !Ie(e);
};
function mn(e, t, n) {
  return Ye(e[t]) ? e[t] : n;
}
function x_(e, t) {
  var n = !1;
  return Object.keys(e).forEach(function(i) {
    return e[i] === t && (n = !0);
  }), n;
}
function Ze(e, t) {
  for (var n = [], i = 2; i < arguments.length; i++)
    n[i - 2] = arguments[i];
  var r = he(e);
  return r && e.call.apply(e, Ke([t], n, !1)), r;
}
function Ir(e, t) {
  var n = 0, i = function() {
    for (var r = [], a = 0; a < arguments.length; a++)
      r[a] = arguments[a];
    !--n && t.apply.apply(t, Ke([this], r, !1));
  };
  "duration" in e ? e.each(function() {
    return ++n;
  }).on("end", i) : (++n, e.call(i));
}
function La(e) {
  return Ne(e) ? e.replace(/<(script|img)?/ig, "&lt;").replace(/(script)?>/ig, "&gt;") : e;
}
function Nr(e, t, n, i) {
  if (n === void 0 && (n = [-1, 1]), i === void 0 && (i = !1), !(!e || !Ne(t)))
    if (t.indexOf(`
`) === -1)
      e.text(t);
    else {
      var r = [e.text(), t].map(function(s) {
        return s.replace(/[\s\n]/g, "");
      });
      if (r[0] !== r[1]) {
        var a = t.split(`
`), o = i ? a.length - 1 : 1;
        e.html(""), a.forEach(function(s, l) {
          e.append("tspan").attr("x", 0).attr("dy", "".concat(l === 0 ? n[0] * o : n[1], "em")).text(s);
        });
      }
    }
}
function Jl(e) {
  var t = e.getBBox(), n = t.x, i = t.y, r = t.width, a = t.height;
  return [
    { x: n, y: i + a },
    // seg0
    { x: n, y: i },
    // seg1
    { x: n + r, y: i },
    // seg2
    { x: n + r, y: i + a }
    // seg3
  ];
}
function __(e) {
  var t = e.getBoundingClientRect(), n = t.width, i = t.height, r = Jl(e), a = r[0].x, o = Math.min(r[0].y, r[1].y);
  return {
    x: a,
    y: o,
    width: n,
    height: i
  };
}
function Qt(e, t) {
  var n, i = e && ((n = e.touches || e.sourceEvent && e.sourceEvent.touches) === null || n === void 0 ? void 0 : n[0]), r = [0, 0];
  try {
    r = ja(i || e, t);
  } catch (a) {
  }
  return r.map(function(a) {
    return isNaN(a) ? 0 : a;
  });
}
function ec(e) {
  var t = e.event, n = e.$el, i = n.subchart.main || n.main, r;
  return t && t.type === "brush" ? r = t.selection : i && (r = i.select(".bb-brush").node()) && (r = kp(r)), r;
}
function vr(e) {
  var t = !("rect" in e) || "rect" in e && e.hasAttribute("width") && e.rect.width !== +e.getAttribute("width");
  return t ? e.rect = e.getBoundingClientRect() : e.rect;
}
function Fn(e, t, n) {
  e === void 0 && (e = !0), t === void 0 && (t = 0), n === void 0 && (n = 1e4);
  var i = Me.crypto || Me.msCrypto, r = i ? t + i.getRandomValues(new Uint32Array(1))[0] % (n - t + 1) : Math.floor(Math.random() * (n - t) + t);
  return e ? String(r) : r;
}
function so(e, t, n, i, r) {
  if (n > i)
    return -1;
  var a = Math.floor((n + i) / 2), o = e[a], s = o.x, l = o.w, c = l === void 0 ? 0 : l;
  return r && (s = e[a].y, c = e[a].h), t >= s && t <= s + c ? a : t < s ? so(e, t, n, a - 1, r) : so(e, t, a + 1, i, r);
}
function y_(e) {
  var t = ec(e);
  return t ? t[0] === t[1] : !0;
}
function m_() {
  for (var e = [], t = 0; t < arguments.length; t++)
    e[t] = arguments[t];
  var n = function(i) {
    if (Be(i) && i.constructor) {
      var r = new i.constructor();
      for (var a in i)
        r[a] = n(i[a]);
      return r;
    }
    return i;
  };
  return e.map(function(i) {
    return n(i);
  }).reduce(function(i, r) {
    return ye(ye({}, i), r);
  });
}
function Dt(e, t) {
  e === void 0 && (e = {}), Ie(t) && t.forEach(function(i) {
    return Dt(e, i);
  });
  for (var n in t)
    /^\d+$/.test(n) || n in e || (e[n] = t[n]);
  return e;
}
var hn = function(e) {
  return e.charAt(0).toUpperCase() + e.slice(1);
};
function b_(e, t) {
  return t === void 0 && (t = "-"), e.split(t).map(function(n, i) {
    return i ? n.charAt(0).toUpperCase() + n.slice(1).toLowerCase() : n.toLowerCase();
  }).join("");
}
var ai = function(e) {
  return [].slice.call(e);
};
function T_(e, t, n) {
  var i = e.rootSelector, r = i === void 0 ? "" : i, a = e.sheet, o = function(l) {
    return l.replace(/\s?(bb-)/g, ".$1").replace(/\.+/g, ".");
  }, s = "".concat(r, " ").concat(o(t), " {").concat(n.join(";"), "}");
  return a[a.insertRule ? "insertRule" : "addRule"](s, a.cssRules.length);
}
function w_(e) {
  var t = [];
  return e.forEach(function(n) {
    var i;
    try {
      n.cssRules && n.cssRules.length && (t = t.concat(ai(n.cssRules)));
    } catch (r) {
      (i = Me.console) === null || i === void 0 || i.warn("Error while reading rules from ".concat(n.href, ": ").concat(r.toString()));
    }
  }), t;
}
function tc(e) {
  var t, n, i, r, a, o;
  return {
    x: ((n = (t = Me.pageXOffset) !== null && t !== void 0 ? t : Me.scrollX) !== null && n !== void 0 ? n : 0) + ((i = e.scrollLeft) !== null && i !== void 0 ? i : 0),
    y: ((a = (r = Me.pageYOffset) !== null && r !== void 0 ? r : Me.scrollY) !== null && a !== void 0 ? a : 0) + ((o = e.scrollTop) !== null && o !== void 0 ? o : 0)
  };
}
function Pr(e, t, n, i) {
  t === void 0 && (t = 0), n === void 0 && (n = 0), i === void 0 && (i = !0);
  var r = new DOMPoint(t, n), a = e.getScreenCTM(), o = r.matrixTransform(i ? a == null ? void 0 : a.inverse() : a);
  if (i === !1) {
    var s = e.getBoundingClientRect();
    o.x -= s.x, o.y -= s.y;
  }
  return o;
}
function Cu(e) {
  var t = e ? e.transform : null, n = t && t.baseVal;
  return n && n.numberOfItems ? n.getItem(0).matrix : { a: 0, b: 0, c: 0, d: 0, e: 0, f: 0 };
}
function uo(e) {
  var t = e[0] instanceof Date, n = (t ? e.map(Number) : e).filter(function(i, r, a) {
    return a.indexOf(i) === r;
  });
  return t ? n.map(function(i) {
    return new Date(i);
  }) : n;
}
function Da(e) {
  return e && e.length ? e.reduce(function(t, n) {
    return t.concat(n);
  }) : [];
}
function oi(e) {
  for (var t = [], n = 1; n < arguments.length; n++)
    t[n - 1] = arguments[n];
  if (!t.length || t.length === 1 && !t[0])
    return e;
  var i = t.shift();
  return Be(e) && Be(i) && Object.keys(i).forEach(function(r) {
    var a = i[r];
    Be(a) ? (!e[r] && (e[r] = {}), e[r] = oi(e[r], a)) : e[r] = Ie(a) ? a.concat() : a;
  }), oi.apply(void 0, Ke([e], t, !1));
}
function si(e, t) {
  t === void 0 && (t = !0);
  var n;
  return e[0] instanceof Date ? n = t ? function(i, r) {
    return i - r;
  } : function(i, r) {
    return r - i;
  } : t && !e.every(isNaN) ? n = function(i, r) {
    return i - r;
  } : t || (n = function(i, r) {
    return i > r && -1 || i < r && 1 || i === r && 0;
  }), e.concat().sort(n);
}
function yn(e, t) {
  var n = t.filter(function(i) {
    return Qe(i);
  });
  return n.length ? ge(n[0]) ? n = Math[e].apply(Math, n) : n[0] instanceof Date && (n = si(n, e === "min")[0]) : n = void 0, n;
}
var $_ = {
  mouse: function() {
    var e = function() {
      return {
        bubbles: !1,
        cancelable: !1,
        screenX: 0,
        screenY: 0,
        clientX: 0,
        clientY: 0
      };
    };
    try {
      return new MouseEvent("t"), function(t, n, i) {
        i === void 0 && (i = e()), t.dispatchEvent(new MouseEvent(n, i));
      };
    } catch (t) {
      return function(n, i, r) {
        r === void 0 && (r = e());
        var a = je.createEvent("MouseEvent");
        a.initMouseEvent(
          i,
          r.bubbles,
          r.cancelable,
          Me,
          0,
          // the event's mouse click count
          r.screenX,
          r.screenY,
          r.clientX,
          r.clientY,
          !1,
          !1,
          !1,
          !1,
          0,
          null
        ), n.dispatchEvent(a);
      };
    }
  }(),
  touch: function(e, t, n) {
    var i = new Touch(oi({
      identifier: Date.now(),
      target: e,
      radiusX: 2.5,
      radiusY: 2.5,
      rotationAngle: 10,
      force: 0.5
    }, n));
    e.dispatchEvent(new TouchEvent(t, {
      cancelable: !0,
      bubbles: !0,
      shiftKey: !0,
      touches: [i],
      targetTouches: [],
      changedTouches: [i]
    }));
  }
};
function Or(e, t) {
  var n = e;
  for (var i in t)
    n = n.replace(new RegExp("{=".concat(i, "}"), "g"), t[i]);
  return n;
}
function kt(e) {
  var t, n;
  if (e instanceof Date)
    n = e;
  else if (Ne(e)) {
    var i = this, r = i.config, a = i.format;
    n = (t = a.dataTime(r.data_xFormat)(e)) !== null && t !== void 0 ? t : new Date(e);
  } else
    ge(e) && !isNaN(e) && (n = /* @__PURE__ */ new Date(+e));
  return (!n || isNaN(+n)) && console && console.error && console.error("Failed to parse x '".concat(e, "' to Date object")), n;
}
function zo(e) {
  var t = e.attr("viewBox");
  return t ? /(\d+(\.\d+)?){3}/.test(t) : !1;
}
function A_(e, t, n) {
  n === void 0 && (n = !1);
  for (var i = !!e.node, r = !1, a = 0, o = Object.entries(t); a < o.length; a++) {
    var s = o[a], l = s[0], c = s[1];
    if (r = i ? e.style(l) === c : e.style[l] === c, n === !1 && r)
      break;
  }
  return r;
}
function Hi() {
  return (je == null ? void 0 : je.hidden) === !1 || (je == null ? void 0 : je.visibilityState) === "visible";
}
function C_(e, t) {
  var n = Me.DocumentTouch, i = Me.matchMedia, r = Me.navigator, a = i == null ? void 0 : i("(pointer:coarse)").matches, o = !1;
  if (t)
    if (r && "maxTouchPoints" in r)
      o = r.maxTouchPoints > 0;
    else if ("ontouchmove" in Me || n && je instanceof n)
      o = !0;
    else if (a)
      o = !0;
    else {
      var s = r.userAgent;
      o = /\b(BlackBerry|webOS|iPhone|IEMobile)\b/i.test(s) || /\b(Android|Windows Phone|iPad|iPod)\b/i.test(s);
    }
  var l = e && !a && (i == null ? void 0 : i("(pointer:fine)").matches);
  return l && "mouse" || o && "touch" || "mouse";
}
function nc(e, t) {
  t() === !1 ? p_(function() {
    return nc(e, t);
  }) : e();
}
var Wr = (
  /** @class */
  function() {
    function e() {
      return m_(u_, a_, f_, o_, d_, s_, l_, c_, e.data);
    }
    return e.setOptions = function(t) {
      this.data = t.reduce(function(n, i) {
        return ye(ye({}, n), i);
      }, this.data);
    }, e.data = {}, e;
  }()
), S_ = (
  /** @class */
  function() {
    function e() {
      var t = {
        chart: null,
        main: null,
        svg: null,
        axis: {
          x: null,
          y: null,
          y2: null,
          subX: null
        },
        axisTooltip: {
          x: null,
          y: null,
          y2: null
        },
        defs: null,
        tooltip: null,
        legend: null,
        title: null,
        subchart: {
          main: null,
          // $$.context
          bar: null,
          // $$.contextBar
          line: null,
          // $$.contextLine
          area: null
          // $$.contextArea
        },
        arcs: null,
        bar: null,
        // mainBar,
        candlestick: null,
        line: null,
        // mainLine,
        area: null,
        // mainArea,
        circle: null,
        // mainCircle,
        radar: null,
        text: null,
        // mainText,
        grid: {
          main: null,
          // grid (also focus)
          x: null,
          // xgrid,
          y: null
          // ygrid,
        },
        gridLines: {
          main: null,
          // gridLines
          x: null,
          // xgridLines,
          y: null
          // ygridLines
        },
        region: {
          main: null,
          // region
          list: null
          // mainRegion
        },
        eventRect: null,
        zoomResetBtn: null
        // drag zoom reset button
      };
      return t;
    }
    return e;
  }()
), k_ = (
  /** @class */
  function() {
    function e() {
      return {
        // chart drawn area dimension, excluding axes
        width: 0,
        width2: 0,
        height: 0,
        height2: 0,
        margin: {
          top: 0,
          bottom: 0,
          left: 0,
          right: 0
        },
        margin2: {
          top: 0,
          bottom: 0,
          left: 0,
          right: 0
        },
        margin3: {
          top: 0,
          bottom: 0,
          left: 0,
          right: 0
        },
        arcWidth: 0,
        arcHeight: 0,
        xAxisHeight: 0,
        hasAxis: !1,
        hasFunnel: !1,
        hasRadar: !1,
        hasTreemap: !1,
        // for data CSS rule index (used when boost.useCssRule is true)
        cssRule: {},
        current: {
          // current domain value. Assigned when is zoom is called
          domain: void 0,
          // chart whole dimension
          width: 0,
          height: 0,
          dataMax: 0,
          maxTickSize: {
            x: {
              width: 0,
              height: 0,
              ticks: [],
              clipPath: 0,
              domain: ""
            },
            y: { width: 0, height: 0, domain: "" },
            y2: { width: 0, height: 0, domain: "" }
          },
          // current used chart type list
          types: [],
          needle: void 0
          // arc needle current value
        },
        // legend
        isLegendRight: !1,
        isLegendInset: !1,
        isLegendTop: !1,
        isLegendLeft: !1,
        legendStep: 0,
        legendItemWidth: 0,
        legendItemHeight: 0,
        legendHasRendered: !1,
        eventReceiver: {
          currentIdx: -1,
          // current event interaction index
          rect: {},
          // event rect's clientBoundingRect
          data: [],
          // event data bound of previoous eventRect
          coords: []
          // coordination value of previous eventRect
        },
        axis: {
          x: {
            padding: { left: 0, right: 0 },
            tickCount: 0
          }
        },
        rotatedPadding: {
          left: 30,
          right: 0,
          top: 5
        },
        withoutFadeIn: {},
        inputType: "",
        datetimeId: "",
        // clip id string
        clip: {
          id: "",
          idXAxis: "",
          idYAxis: "",
          idXAxisTickTexts: "",
          idGrid: "",
          idSubchart: "",
          // clipIdForSubchart
          path: "",
          pathXAxis: "",
          pathYAxis: "",
          pathXAxisTickTexts: "",
          pathGrid: ""
        },
        // state
        event: null,
        // event object
        dragStart: null,
        dragging: !1,
        flowing: !1,
        cancelClick: !1,
        mouseover: !1,
        rendered: !1,
        transiting: !1,
        redrawing: !1,
        // if redraw() is on process
        resizing: !1,
        // resize event called
        toggling: !1,
        // legend toggle
        zooming: !1,
        hasNegativeValue: !1,
        hasPositiveValue: !0,
        orgAreaOpacity: "0.2",
        orgConfig: {},
        // user original genration config
        // ID strings
        hiddenTargetIds: [],
        hiddenLegendIds: [],
        focusedTargetIds: [],
        defocusedTargetIds: [],
        // value for Arc
        radius: 0,
        innerRadius: 0,
        outerRadius: void 0,
        innerRadiusRatio: 0,
        gaugeArcWidth: 0,
        radiusExpanded: 0,
        // xgrid attribute
        xgridAttr: {
          x1: null,
          x2: null,
          y1: null,
          y2: null
        }
      };
    }
    return e;
  }()
), Su = {
  element: S_,
  state: k_
}, E_ = (
  /** @class */
  function() {
    function e() {
      var t = this;
      Object.keys(Su).forEach(function(n) {
        t[n] = new Su[n]();
      });
    }
    return e.prototype.getStore = function(t) {
      return this[t];
    }, e;
  }()
), Xt = {
  bubbleBaseLength: "$baseLength",
  colorPattern: "__colorPattern__",
  dataMinMax: "$dataMinMax",
  dataTotalSum: "$dataTotalSum",
  dataTotalPerIndex: "$totalPerIndex",
  legendItemTextBox: "legendItemTextBox",
  radarPoints: "$radarPoints",
  radarTextWidth: "$radarTextWidth",
  setOverOut: "setOverOut",
  callOverOutForTouch: "callOverOutForTouch",
  textRect: "textRect"
}, R_ = (
  /** @class */
  function() {
    function e() {
      this.cache = {};
    }
    return e.prototype.add = function(t, n, i) {
      return i === void 0 && (i = !1), this.cache[t] = i ? this.cloneTarget(n) : n, this.cache[t];
    }, e.prototype.remove = function(t) {
      var n = this;
      (Ne(t) ? [t] : t).forEach(function(i) {
        return delete n.cache[i];
      });
    }, e.prototype.get = function(t, n) {
      if (n === void 0 && (n = !1), n && Array.isArray(t)) {
        for (var i = [], r = 0, a = void 0; a = t[r]; r++)
          a in this.cache && i.push(this.cloneTarget(this.cache[a]));
        return i;
      } else {
        var o = this.cache[t];
        return Ae(o) ? o : null;
      }
    }, e.prototype.reset = function(t) {
      var n = this;
      for (var i in n.cache)
        (t || /^\$/.test(i)) && (n.cache[i] = null);
    }, e.prototype.cloneTarget = function(t) {
      return {
        id: t.id,
        id_org: t.id_org,
        values: t.values.map(function(n) {
          return { x: n.x, value: n.value, id: n.id };
        })
      };
    }, e;
  }()
), ve = {
  AREA: "area",
  AREA_LINE_RANGE: "area-line-range",
  AREA_SPLINE: "area-spline",
  AREA_SPLINE_RANGE: "area-spline-range",
  AREA_STEP: "area-step",
  AREA_STEP_RANGE: "area-step-range",
  BAR: "bar",
  BUBBLE: "bubble",
  CANDLESTICK: "candlestick",
  DONUT: "donut",
  FUNNEL: "funnel",
  GAUGE: "gauge",
  LINE: "line",
  PIE: "pie",
  POLAR: "polar",
  RADAR: "radar",
  SCATTER: "scatter",
  SPLINE: "spline",
  STEP: "step",
  TREEMAP: "treemap"
}, Ia = {
  AREA: "initArea",
  AREA_LINE_RANGE: "initArea",
  AREA_SPLINE: "initArea",
  AREA_SPLINE_RANGE: "initArea",
  AREA_STEP: "initArea",
  AREA_STEP_RANGE: "initArea",
  BAR: "initBar",
  BUBBLE: "initCircle",
  CANDLESTICK: "initCandlestick",
  DONUT: "initArc",
  FUNNEL: "initFunnel",
  GAUGE: "initArc",
  LINE: "initLine",
  PIE: "initArc",
  POLAR: "initPolar",
  RADAR: "initCircle",
  SCATTER: "initCircle",
  SPLINE: "initLine",
  STEP: "initLine",
  TREEMAP: "initTreemap"
}, xn = {
  Area: [
    ve.AREA,
    ve.AREA_SPLINE,
    ve.AREA_SPLINE_RANGE,
    ve.AREA_LINE_RANGE,
    ve.AREA_STEP,
    ve.AREA_STEP_RANGE
  ],
  AreaRange: [
    ve.AREA_SPLINE_RANGE,
    ve.AREA_LINE_RANGE,
    ve.AREA_STEP_RANGE
  ],
  Arc: [
    ve.PIE,
    ve.DONUT,
    ve.GAUGE,
    ve.POLAR,
    ve.RADAR
  ],
  Line: [
    ve.LINE,
    ve.SPLINE,
    ve.AREA,
    ve.AREA_SPLINE,
    ve.AREA_SPLINE_RANGE,
    ve.AREA_LINE_RANGE,
    ve.STEP,
    ve.AREA_STEP,
    ve.AREA_STEP_RANGE
  ],
  Step: [
    ve.STEP,
    ve.AREA_STEP,
    ve.AREA_STEP_RANGE
  ],
  Spline: [
    ve.SPLINE,
    ve.AREA_SPLINE,
    ve.AREA_SPLINE_RANGE
  ]
};
function M_(e) {
  var t = e, n = t.config, i = "";
  if (en(n.data_type || n.data_types) && !t[Ia.LINE])
    i = "line";
  else
    for (var r in Ia) {
      var a = ve[r];
      if (t.hasType(a) && !t[Ia[r]]) {
        i = a;
        break;
      }
    }
  i && L_("Please, make sure if %c".concat(b_(i)), "module has been imported and specified correctly.", "https://github.com/naver/billboard.js/wiki/CHANGELOG-v2#modularization-by-its-functionality");
}
function L_(e, t, n) {
  var i, r = "[billboard.js]", a = (i = Me.console) === null || i === void 0 ? void 0 : i.error;
  if (a) {
    var o = ["background:red;color:white;display:block;font-size:15px", t];
    console.error.apply(console, Ke(["❌ ".concat(r, " ").concat(e), "background:red;color:white;display:block;font-size:15px"], o, !1)), console.info("%cℹ️", "font-size:15px", n);
  }
  throw Error("".concat(r, " ").concat(e.replace(/\%c([a-z-]+)/i, "'$1' "), " ").concat(t));
}
var D_ = Me.setTimeout, I_ = Me.clearTimeout;
function N_(e) {
  var t = [], n, i = function() {
    i.clear(), e === !1 ? Ql(function() {
      t.forEach(function(r) {
        return r();
      });
    }, { timeout: 200 }) : n = D_(function() {
      t.forEach(function(r) {
        return r();
      });
    }, ge(e) ? e : 200);
  };
  return i.clear = function() {
    n && (I_(n), n = null);
  }, i.add = function(r) {
    return t.push(r);
  }, i.remove = function(r) {
    return t.splice(t.indexOf(r), 1);
  }, i;
}
function ic() {
  var e = [], t = function(n, i) {
    function r() {
      for (var a, o = 0, s = 0, l = void 0; l = e[s]; s++) {
        if (l === !0 || !((a = l.empty) === null || a === void 0) && a.call(l)) {
          o++;
          continue;
        }
        if (Hi() === !1) {
          o = e.length;
          break;
        }
        try {
          l.transition();
        } catch (c) {
          o++;
        }
      }
      return o === e.length;
    }
    nc(function() {
      i == null || i();
    }, r);
  };
  return t.add = function(n) {
    Ie(n) ? e = e.concat(n) : e.push(n);
  }, t;
}
var Na = {};
function P_(e, t) {
  var n, i = e.toString(), r = i.replace(/(function|[\s\W\n])/g, "").substring(0, 15);
  return r in Na || (Na[r] = new Me.Blob([
    "".concat((n = t == null ? void 0 : t.map(String).join(";")) !== null && n !== void 0 ? n : "", `

			self.onmessage=function({data}) {
				const result = (`).concat(i, `).apply(null, data);
				self.postMessage(result);
			};`)
  ], {
    type: "text/javascript"
  })), Me.URL.createObjectURL(Na[r]);
}
function O_(e) {
  var t = new Me.Worker(e);
  return t.onerror = function(n) {
    console.error ? console.error(n) : console.log(n);
  }, t;
}
function Pa(e, t, n, i) {
  e === void 0 && (e = !0);
  var r = function() {
    for (var s = [], l = 0; l < arguments.length; l++)
      s[l] = arguments[l];
    var c = t.apply(void 0, s);
    n(c);
  };
  if (Me.Worker && e) {
    var a = P_(t, i), o = O_(a);
    r = function() {
      for (var s = [], l = 0; l < arguments.length; l++)
        s[l] = arguments[l];
      o.postMessage(s), o.onmessage = function(c) {
        return Me.URL.revokeObjectURL(a), n(c.data);
      };
    };
  }
  return r;
}
function lo(e) {
  var t = [];
  return e.forEach(function(n, i) {
    var r = n[0];
    n.forEach(function(a, o) {
      if (o > 0) {
        if (typeof t[o - 1] == "undefined" && (t[o - 1] = {}), typeof a == "undefined")
          throw new Error("Source data is missing a component at (".concat(i, ", ").concat(o, ")!"));
        t[o - 1][r] = a;
      }
    });
  }), t;
}
function co(e) {
  var t = e[0], n = [];
  return e.forEach(function(i, r) {
    if (r > 0) {
      var a = {};
      i.forEach(function(o, s) {
        if (typeof o == "undefined")
          throw new Error("Source data is missing a component at (".concat(r, ", ").concat(s, ")!"));
        a[t[s]] = o;
      }), n.push(a);
    }
  }), n;
}
function rc(e, t) {
  var n = [], i, r;
  if (Array.isArray(e)) {
    var a = function(o, s) {
      if (o[s] !== void 0)
        return o[s];
      var l = s.replace(/\[(\w+)\]/g, ".$1"), c = l.replace(/^\./, "").split("."), f = o;
      return c.some(function(h) {
        return !(f = f && h in f ? f[h] : void 0);
      }), f;
    };
    t.x ? i = t.value.concat(t.x) : i = t.value, n.push(i), e.forEach(function(o) {
      var s = i.map(function(l) {
        var c = a(o, l);
        return typeof c == "undefined" && (c = null), c;
      });
      n.push(s);
    }), r = co(n);
  } else
    Object.keys(e).forEach(function(o) {
      var s, l = e[o].concat();
      (s = l.unshift) === null || s === void 0 || s.call(l, o), n.push(l);
    }), r = lo(n);
  return r;
}
function F_(e, t, n, i, r) {
  t === void 0 && (t = "csv");
  var a = new XMLHttpRequest(), o = { csv: B_, tsv: X_, json: rc };
  a.open("GET", e), n && Object.keys(n).forEach(function(s) {
    a.setRequestHeader(s, n[s]);
  }), a.onreadystatechange = function() {
    if (a.readyState === 4)
      if (a.status === 200) {
        var s = a.responseText;
        s && r.call(this, o[t](t === "json" ? JSON.parse(s) : s, i));
      } else
        throw new Error("".concat(e, ": Something went wrong loading!"));
  }, a.send();
}
function ac(e, t) {
  var n = e.rows(t), i;
  return n.length === 1 ? (i = [{}], n[0].forEach(function(r) {
    i[0][r] = null;
  })) : i = e.parse(t), i;
}
function B_(e) {
  return ac({
    rows: Pp,
    parse: Np
  }, e);
}
function X_(e) {
  return ac({
    rows: Fp,
    parse: Op
  }, e);
}
function ku(e, t) {
  var n = e || (t == null ? void 0 : t.data_keys);
  return n != null && n.x && (t.data_x = n.x), n;
}
var z_ = {
  /**
   * Convert data according its type
   * @param {object} args data object
   * @param {Function} [callback] callback for url(XHR) type loading
   * @private
   */
  convertData: function(e, t) {
    var n = this.config, i = n.boost_useWorker, r = e;
    if (e.bindto && (r = {}, ["url", "mimeType", "headers", "keys", "json", "keys", "rows", "columns"].forEach(function(a) {
      var o = "data_".concat(a);
      o in e && (r[a] = e[o]);
    })), r.url && t)
      F_(r.url, r.mimeType, r.headers, ku(r.keys, n), t);
    else if (r.json)
      Pa(i, rc, t, [lo, co])(r.json, ku(r.keys, n));
    else if (r.rows)
      Pa(i, co, t)(r.rows);
    else if (r.columns)
      Pa(i, lo, t)(r.columns);
    else if (e.bindto)
      throw Error("url or json or rows or columns is required.");
  },
  convertDataToTargets: function(e, t) {
    var n = this, i = this, r = i.axis, a = i.config, o = i.state, s = a.data_type, l = !1, c = !1, f = !1;
    r && (l = r.isCategorized(), c = r.isTimeSeries(), f = r.isCustomX());
    var h = Object.keys(e[0] || {}), g = h.length ? h.filter(i.isNotX, i) : [], x = h.length ? h.filter(i.isX, i) : [], y;
    g.forEach(function(w) {
      var C = n.getXKey(w);
      f || c ? x.indexOf(C) >= 0 ? y = (t && i.data.xs[w] || []).concat(e.map(function(R) {
        return R[C];
      }).filter(Ae).map(function(R, S) {
        return i.generateTargetX(R, w, S);
      })) : a.data_x ? y = n.getOtherTargetXs() : Qe(a.data_xs) && (y = i.getXValuesOfXKey(C, i.data.targets)) : y = e.map(function(R, S) {
        return S;
      }), y && (n.data.xs[w] = y);
    }), g.forEach(function(w) {
      if (!n.data.xs[w])
        throw new Error('x is not defined for id = "'.concat(w, '".'));
    });
    var T = g.map(function(w, C) {
      var R = a.data_idConverter.bind(i.api)(w), S = i.getXKey(w), I = f && l, L = I && e.map(function(H) {
        return H.x;
      }).every(function(H) {
        return a.axis_x_categories.indexOf(H) > -1;
      }), p = e.__append__, U = S === null && p ? i.api.data.values(w).length : 0;
      return {
        id: R,
        id_org: w,
        values: e.map(function(H, q) {
          var X = H[S], j = H[w], z;
          return j = j !== null && !isNaN(j) && !Be(j) ? +j : Ie(j) || Be(j) ? j : null, (I || o.hasRadar) && C === 0 && !at(X) ? (!L && C === 0 && q === 0 && !p && (a.axis_x_categories = []), z = a.axis_x_categories.indexOf(X), z === -1 && (z = a.axis_x_categories.length, a.axis_x_categories.push(X))) : z = i.generateTargetX(X, w, U + q), (at(j) || i.data.xs[w].length <= q) && (z = void 0), {
            x: z,
            value: j,
            id: R,
            index: -1
          };
        }).filter(function(H) {
          return Ye(H.x);
        })
      };
    });
    if (T.forEach(function(w) {
      var C;
      a.data_xSort && (w.values = w.values.sort(function(R, S) {
        var I = R.x || R.x === 0 ? R.x : 1 / 0, L = S.x || S.x === 0 ? S.x : 1 / 0;
        return I - L;
      })), w.values.forEach(function(R, S) {
        return R.index = S;
      }), (C = i.data.xs[w.id]) === null || C === void 0 || C.sort(function(R, S) {
        return R - S;
      });
    }), o.hasNegativeValue = i.hasNegativeValueInTargets(T), o.hasPositiveValue = i.hasPositiveValueInTargets(T), s && i.isValidChartType(s)) {
      var A = i.mapToIds(T).filter(function(w) {
        return !(w in a.data_types) || !i.isValidChartType(a.data_types[w]);
      });
      i.setTargetType(A, s);
    }
    return T.forEach(function(w) {
      return i.cache.add(w.id_org, w, !0);
    }), T;
  }
}, H_ = {
  isX: function(e) {
    var t = this, n = t.config, i = n.data_x && e === n.data_x, r = Qe(n.data_xs) && x_(n.data_xs, e);
    return i || r;
  },
  isNotX: function(e) {
    return !this.isX(e);
  },
  isStackNormalized: function() {
    var e = this.config;
    return !!(e.data_stack_normalize && e.data_groups.length);
  },
  /**
   * Check if given id is grouped data or has grouped data
   * @param {string} id Data id value
   * @returns {boolean} is grouped data or has grouped data
   * @private
   */
  isGrouped: function(e) {
    var t = this.config.data_groups;
    return e ? t.some(function(n) {
      return n.indexOf(e) >= 0 && n.length > 1;
    }) : t.length > 0;
  },
  getXKey: function(e) {
    var t = this, n = t.config;
    return n.data_x ? n.data_x : Qe(n.data_xs) ? n.data_xs[e] : null;
  },
  getXValuesOfXKey: function(e, t) {
    var n = this, i = t && Qe(t) ? n.mapToIds(t) : [], r;
    return i.forEach(function(a) {
      n.getXKey(a) === e && (r = n.data.xs[a]);
    }), r;
  },
  /**
   * Get index number based on given x Axis value
   * @param {Date|number|string} x x Axis to be compared
   * @param {Array} basedX x Axis list to be based on
   * @returns {number} index number
   * @private
   */
  getIndexByX: function(e, t) {
    var n = this;
    return t ? t.indexOf(Ne(e) ? e : +e) : (n.filterByX(n.data.targets, e)[0] || { index: null }).index;
  },
  getXValue: function(e, t) {
    var n = this;
    return e in n.data.xs && n.data.xs[e] && Ae(n.data.xs[e][t]) ? n.data.xs[e][t] : t;
  },
  getOtherTargetXs: function() {
    var e = this, t = Object.keys(e.data.xs);
    return t.length ? e.data.xs[t[0]] : null;
  },
  getOtherTargetX: function(e) {
    var t = this.getOtherTargetXs();
    return t && e < t.length ? t[e] : null;
  },
  addXs: function(e) {
    var t = this, n = t.config;
    Object.keys(e).forEach(function(i) {
      n.data_xs[i] = e[i];
    });
  },
  /**
   * Determine if x axis is multiple
   * @returns {boolean} true: multiple, false: single
   * @private
   */
  isMultipleX: function() {
    return !this.config.axis_x_forceAsSingle && (Qe(this.config.data_xs) || this.hasType("bubble") || this.hasType("scatter"));
  },
  addName: function(e) {
    var t = this, n = t.config, i;
    return e && (i = n.data_names[e.id], e.name = i !== void 0 ? i : e.id), e;
  },
  /**
   * Get all values on given index
   * @param {number} index Index
   * @param {boolean} filterNull Filter nullish value
   * @returns {Array}
   * @private
   */
  getAllValuesOnIndex: function(e, t) {
    t === void 0 && (t = !1);
    var n = this, i = n.filterTargetsToShow(n.data.targets).map(function(r) {
      return n.addName(n.getValueOnIndex(r.values, e));
    });
    return t && (i = i.filter(function(r) {
      return r && "value" in r && Ae(r.value);
    })), i;
  },
  getValueOnIndex: function(e, t) {
    var n = e.filter(function(i) {
      return i.index === t;
    });
    return n.length ? n[0] : null;
  },
  updateTargetX: function(e, t) {
    var n = this;
    e.forEach(function(i) {
      i.values.forEach(function(r, a) {
        r.x = n.generateTargetX(t[a], i.id, a);
      }), n.data.xs[i.id] = t;
    });
  },
  updateTargetXs: function(e, t) {
    var n = this;
    e.forEach(function(i) {
      t[i.id] && n.updateTargetX([i], t[i.id]);
    });
  },
  generateTargetX: function(e, t, n) {
    var i = this, r = i.axis, a = r != null && r.isCategorized() ? n : e || n;
    if (r != null && r.isTimeSeries()) {
      var o = kt.bind(i);
      a = o(e || i.getXValue(t, n));
    } else
      r != null && r.isCustomX() && !(r != null && r.isCategorized()) && (a = Ae(e) ? +e : i.getXValue(t, n));
    return a;
  },
  updateXs: function(e) {
    e.length && (this.axis.xs = e.map(function(t) {
      return t.x;
    }));
  },
  getPrevX: function(e) {
    var t = this.axis.xs[e - 1];
    return Ye(t) ? t : null;
  },
  getNextX: function(e) {
    var t = this.axis.xs[e + 1];
    return Ye(t) ? t : null;
  },
  /**
   * Get base value isAreaRangeType
   * @param {object} data Data object
   * @returns {number}
   * @private
   */
  getBaseValue: function(e) {
    var t = this, n = t.state.hasAxis, i = e.value;
    return i && n && (t.isAreaRangeType(e) ? i = t.getRangedData(e, "mid") : t.isBubbleZType(e) && (i = t.getBubbleZData(i, "y"))), i;
  },
  /**
   * Get min/max value from the data
   * @private
   * @param {Array} data array data to be evaluated
   * @returns {{min: {number}, max: {number}}}
   */
  getMinMaxValue: function(e) {
    var t = this.getBaseValue.bind(this), n, i;
    return (e || this.data.targets.map(function(r) {
      return r.values;
    })).forEach(function(r, a) {
      var o = r.map(t).filter(ge);
      n = Math.min.apply(Math, Ke([a ? n : 1 / 0], o, !1)), i = Math.max.apply(Math, Ke([a ? i : -1 / 0], o, !1));
    }), { min: n, max: i };
  },
  /**
   * Get the min/max data
   * @private
   * @returns {{min: Array, max: Array}}
   */
  getMinMaxData: function() {
    var e = this, t = Xt.dataMinMax, n = e.cache.get(t);
    if (!n) {
      var i = e.data.targets.map(function(s) {
        return s.values;
      }), r = e.getMinMaxValue(i), a = [], o = [];
      i.forEach(function(s) {
        var l = e.getFilteredDataByValue(s, r.min), c = e.getFilteredDataByValue(s, r.max);
        l.length && (a = a.concat(l)), c.length && (o = o.concat(c));
      }), e.cache.add(t, n = { min: a, max: o });
    }
    return n;
  },
  /**
   * Get sum of data per index
   * @private
   * @returns {Array}
   */
  getTotalPerIndex: function() {
    var e = this, t = Xt.dataTotalPerIndex, n = e.cache.get(t);
    return (e.config.data_groups.length || e.isStackNormalized()) && !n && (n = [], e.data.targets.forEach(function(i) {
      i.values.forEach(function(r, a) {
        n[a] || (n[a] = 0), n[a] += ge(r.value) ? r.value : 0;
      });
    })), n;
  },
  /**
   * Get total data sum
   * @param {boolean} subtractHidden Subtract hidden data from total
   * @returns {number}
   * @private
   */
  getTotalDataSum: function(e) {
    var t = this, n = Xt.dataTotalSum, i = t.cache.get(n);
    if (!ge(i)) {
      var r = Da(t.data.targets.map(function(a) {
        return a.values;
      })).map(function(a) {
        return a.value;
      });
      i = r.length ? r.reduce(function(a, o) {
        return a + o;
      }) : 0, t.cache.add(n, i);
    }
    return e && (i -= t.getHiddenTotalDataSum()), i;
  },
  /**
   * Get total hidden data sum
   * @returns {number}
   * @private
   */
  getHiddenTotalDataSum: function() {
    var e = this, t = e.api, n = e.state.hiddenTargetIds, i = 0;
    return n.length && (i = t.data.values.bind(t)(n).reduce(function(r, a) {
      return r + a;
    })), i;
  },
  /**
   * Get filtered data by value
   * @param {object} data Data
   * @param {number} value Value to be filtered
   * @returns {Array} filtered array data
   * @private
   */
  getFilteredDataByValue: function(e, t) {
    var n = this;
    return e.filter(function(i) {
      return n.getBaseValue(i) === t;
    });
  },
  /**
   * Return the max length of the data
   * @returns {number} max data length
   * @private
   */
  getMaxDataCount: function() {
    return Math.max.apply(Math, Ke(Ke([], this.data.targets.map(function(e) {
      return e.values.length;
    }), !1), [0], !1));
  },
  getMaxDataCountTarget: function() {
    var e = this.filterTargetsToShow() || [], t = e.length, n = this.config.axis_x_inverted;
    return t > 1 ? (e = e.map(function(i) {
      return i.values;
    }).reduce(function(i, r) {
      return i.concat(r);
    }).map(function(i) {
      return i.x;
    }), e = si(uo(e)).map(function(i, r, a) {
      return {
        x: i,
        index: n ? a.length - r - 1 : r
      };
    })) : t && (e = e[0].values.concat()), e;
  },
  mapToIds: function(e) {
    return e.map(function(t) {
      return t.id;
    });
  },
  mapToTargetIds: function(e) {
    var t = this;
    return e ? Ie(e) ? e.concat() : [e] : t.mapToIds(t.data.targets);
  },
  hasTarget: function(e, t) {
    for (var n = this.mapToIds(e), i = 0, r = void 0; r = n[i]; i++)
      if (r === t)
        return !0;
    return !1;
  },
  isTargetToShow: function(e) {
    return this.state.hiddenTargetIds.indexOf(e) < 0;
  },
  isLegendToShow: function(e) {
    return this.state.hiddenLegendIds.indexOf(e) < 0;
  },
  filterTargetsToShow: function(e) {
    var t = this;
    return (e || t.data.targets).filter(function(n) {
      return t.isTargetToShow(n.id);
    });
  },
  mapTargetsToUniqueXs: function(e) {
    var t = this, n = t.axis, i = [];
    return e != null && e.length && (i = uo(Da(e.map(function(r) {
      return r.values.map(function(a) {
        return +a.x;
      });
    }))), i = n != null && n.isTimeSeries() ? i.map(function(r) {
      return /* @__PURE__ */ new Date(+r);
    }) : i.map(Number)), si(i);
  },
  /**
   * Add to the state target Ids
   * @param {string} type State's prop name
   * @param {Array|string} targetIds Target ids array
   * @private
   */
  addTargetIds: function(e, t) {
    var n = this.state, i = Ie(t) ? t : [t];
    i.forEach(function(r) {
      n[e].indexOf(r) < 0 && n[e].push(r);
    });
  },
  /**
   * Remove from the state target Ids
   * @param {string} type State's prop name
   * @param {Array|string} targetIds Target ids array
   * @private
   */
  removeTargetIds: function(e, t) {
    var n = this.state, i = Ie(t) ? t : [t];
    i.forEach(function(r) {
      var a = n[e].indexOf(r);
      a >= 0 && n[e].splice(a, 1);
    });
  },
  addHiddenTargetIds: function(e) {
    this.addTargetIds("hiddenTargetIds", e);
  },
  removeHiddenTargetIds: function(e) {
    this.removeTargetIds("hiddenTargetIds", e);
  },
  addHiddenLegendIds: function(e) {
    this.addTargetIds("hiddenLegendIds", e);
  },
  removeHiddenLegendIds: function(e) {
    this.removeTargetIds("hiddenLegendIds", e);
  },
  getValuesAsIdKeyed: function(e) {
    var t = this, n = t.state.hasAxis, i = {}, r = t.isMultipleX(), a = r ? t.mapTargetsToUniqueXs(e).map(function(o) {
      return Ne(o) ? o : +o;
    }) : null;
    return e.forEach(function(o) {
      var s = [];
      o.values.filter(function(l) {
        var c = l.value;
        return Ae(c) || c === null;
      }).forEach(function(l) {
        var c = l.value;
        c !== null && t.isCandlestickType(l) && (c = Ie(c) ? c.slice(0, 4) : [c.open, c.high, c.low, c.close]), Ie(c) ? s.push.apply(s, c) : Be(c) && "high" in c ? s.push.apply(s, Object.values(c)) : t.isBubbleZType(l) ? s.push(n && t.getBubbleZData(c, "y")) : r ? s[t.getIndexByX(l.x, a)] = c : s.push(c);
      }), i[o.id] = s;
    }), i;
  },
  checkValueInTargets: function(e, t) {
    for (var n = Object.keys(e), i, r = 0; r < n.length; r++) {
      i = e[n[r]].values;
      for (var a = 0; a < i.length; a++)
        if (t(i[a].value))
          return !0;
    }
    return !1;
  },
  hasMultiTargets: function() {
    return this.filterTargetsToShow().length > 1;
  },
  hasNegativeValueInTargets: function(e) {
    return this.checkValueInTargets(e, function(t) {
      return t < 0;
    });
  },
  hasPositiveValueInTargets: function(e) {
    return this.checkValueInTargets(e, function(t) {
      return t > 0;
    });
  },
  /**
   * Sort targets data
   * Note: For stacked bar, will sort from the total sum of data series, not for each stacked bar
   * @param {Array} targetsValue Target value
   * @returns {Array}
   * @private
   */
  orderTargets: function(e) {
    var t = this, n = Ke([], e, !0), i = t.getSortCompareFn();
    return i && n.sort(i), n;
  },
  /**
   * Get data.order compare function
   * @param {boolean} isReversed for Arc & Treemap type sort order needs to be reversed
   * @returns {Function} compare function
   * @private
   */
  getSortCompareFn: function(e) {
    e === void 0 && (e = !1);
    var t = this, n = t.config, i = n.data_order, r = /asc/i.test(i), a = /desc/i.test(i), o;
    if (r || a) {
      var s = function(c, f) {
        return c + Math.abs(f.value);
      }, l = function(c) {
        return ge(c) ? c : "values" in c ? c.values.reduce(s, 0) : c.value;
      };
      o = function(c, f) {
        var h = l(c), g = l(f);
        return e ? r ? h - g : g - h : r ? g - h : h - g;
      };
    } else
      he(i) && (o = i.bind(t.api));
    return o || null;
  },
  filterByX: function(e, t) {
    return Da(e.map(function(n) {
      return n.values;
    })).filter(function(n) {
      return n.x - t === 0;
    });
  },
  filterRemoveNull: function(e) {
    var t = this;
    return e.filter(function(n) {
      return Ae(t.getBaseValue(n));
    });
  },
  filterByXDomain: function(e, t) {
    return e.map(function(n) {
      return {
        id: n.id,
        id_org: n.id_org,
        values: n.values.filter(function(i) {
          return t[0] <= i.x && i.x <= t[1];
        })
      };
    });
  },
  hasDataLabel: function() {
    var e = this.config.data_labels;
    return oo(e) && e || Jt(e) && Qe(e);
  },
  /**
   * Determine if has null value
   * @param {Array} targets Data array to be evaluated
   * @returns {boolean}
   * @private
   */
  hasNullDataValue: function(e) {
    return e.some(function(t) {
      var n = t.value;
      return n === null;
    });
  },
  /**
   * Get data index from the event coodinates
   * @param {Event} event Event object
   * @returns {number}
   * @private
   */
  getDataIndexFromEvent: function(e) {
    var t = this, n = t.$el, i = t.config, r = t.state, a = r.hasRadar, o = r.inputType, s = r.eventReceiver, l = s.coords, c = s.rect, f;
    if (a) {
      var h = e.target;
      /tspan/i.test(h.tagName) && (h = h.parentNode);
      var g = le(h).datum();
      f = g && Object.keys(g).length === 1 ? g.index : void 0;
    } else {
      var x = i.axis_rotated, y = tc(n.chart.node()), T = o === "touch" && e.changedTouches ? e.changedTouches[0] : e, A = x ? T.clientY + y.y : T.clientX + y.x;
      if (zo(n.svg)) {
        var w = [A, 0];
        x && w.reverse(), A = Pr.apply(void 0, Ke([n.eventRect.node()], w, !1))[x ? "y" : "x"];
      } else
        A -= x ? c.top : c.left;
      f = so(l, A, 0, l.length - 1, x);
    }
    return f;
  },
  getDataLabelLength: function(e, t, n) {
    var i = this, r = [0, 0], a = 1.3;
    return i.$el.chart.select("svg").selectAll(".dummy").data([e, t]).enter().append("text").text(function(o) {
      return i.dataLabelFormat(o.id)(o);
    }).each(function(o, s) {
      r[s] = this.getBoundingClientRect()[n] * a;
    }).remove(), r;
  },
  isNoneArc: function(e) {
    return this.hasTarget(this.data.targets, e.id);
  },
  isArc: function(e) {
    return "data" in e && this.hasTarget(this.data.targets, e.data.id);
  },
  findSameXOfValues: function(e, t) {
    var n = e[t].x, i = [], r;
    for (r = t - 1; r >= 0 && n === e[r].x; r--)
      i.push(e[r]);
    for (r = t; r < e.length && n === e[r].x; r++)
      i.push(e[r]);
    return i;
  },
  findClosestFromTargets: function(e, t) {
    var n = this, i = e.map(function(r) {
      return n.findClosest(r.values, t);
    });
    return n.findClosest(i, t);
  },
  findClosest: function(e, t) {
    var n = this, i = n.$el.main, r = e.filter(function(s) {
      return s && Ae(s.value);
    }), a, o;
    return r.filter(function(s) {
      return n.isBarType(s.id) || n.isCandlestickType(s.id);
    }).forEach(function(s) {
      var l = n.isBarType(s.id) ? ".".concat(Ot.chartBar, ".").concat(Ee.target).concat(n.getTargetSelectorSuffix(s.id), " .").concat(Ot.bar, "-").concat(s.index) : ".".concat(ro.chartCandlestick, ".").concat(Ee.target).concat(n.getTargetSelectorSuffix(s.id), " .").concat(ro.candlestick, "-").concat(s.index, " path");
      !o && n.isWithinBar(i.select(l).node()) && (o = s);
    }), r.filter(function(s) {
      return !n.isBarType(s.id) && !n.isCandlestickType(s.id);
    }).forEach(function(s) {
      var l = n.dist(s, t);
      a = n.getPointSensitivity(s), l < a && (a = l, o = s);
    }), o;
  },
  dist: function(e, t) {
    var n = this, i = n.config.axis_rotated, r = n.scale, a = +i, o = +!i, s = n.circleY(e, e.index), l = (r.zoom || r.x)(e.x);
    return Math.sqrt(Math.pow(l - t[a], 2) + Math.pow(s - t[o], 2));
  },
  /**
   * Convert data for step type
   * @param {Array} values Object data values
   * @returns {Array}
   * @private
   */
  convertValuesToStep: function(e) {
    var t = this, n = t.axis, i = t.config, r = i.line_step_type, a = n ? n.isCategorized() : !1, o = Ie(e) ? e.concat() : [e];
    if (!(a || /step\-(after|before)/.test(r)))
      return e;
    if (o.length) {
      var s = o[0], l = o[o.length - 1], c = s.id, f = s.x;
      o.unshift({ x: --f, value: s.value, id: c }), a && r === "step-after" && o.unshift({ x: --f, value: s.value, id: c }), f = l.x, o.push({ x: ++f, value: l.value, id: c }), a && r === "step-before" && o.push({ x: ++f, value: l.value, id: c });
    }
    return o;
  },
  convertValuesToRange: function(e) {
    var t = Ie(e) ? e.concat() : [e], n = [];
    return t.forEach(function(i) {
      var r = i.x, a = i.id;
      n.push({
        x: r,
        id: a,
        value: i.value[0]
      }), n.push({
        x: r,
        id: a,
        value: i.value[2]
      });
    }), n;
  },
  updateDataAttributes: function(e, t) {
    var n = this, i = n.config, r = i["data_".concat(e)];
    return at(t) || (Object.keys(t).forEach(function(a) {
      r[a] = t[a];
    }), n.redraw({ withLegend: !0 })), r;
  },
  getRangedData: function(e, t, n) {
    t === void 0 && (t = ""), n === void 0 && (n = "areaRange");
    var i = e == null ? void 0 : e.value;
    if (Ie(i)) {
      if (n === "bar")
        return i.reduce(function(a, o) {
          return o - a;
        });
      var r = {
        areaRange: ["high", "mid", "low"],
        candlestick: ["open", "high", "low", "close", "volume"]
      }[n].indexOf(t);
      return r >= 0 && i ? i[r] : void 0;
    } else if (i && t)
      return i[t];
    return i;
  },
  /**
   * Set ratio for grouped data
   * @param {Array} data Data array
   * @private
   */
  setRatioForGroupedData: function(e) {
    var t = this, n = t.config;
    if (n.data_groups.length && e.some(function(r) {
      return t.isGrouped(r.id);
    })) {
      var i = function(r) {
        return t.getRatio("index", r, !0);
      };
      e.forEach(function(r) {
        "values" in r ? r.values.forEach(i) : i(r);
      });
    }
  },
  /**
   * Get ratio value
   * @param {string} type Ratio for given type
   * @param {object} d Data value object
   * @param {boolean} asPercent Convert the return as percent or not
   * @returns {number} Ratio value
   * @private
   */
  getRatio: function(e, t, n) {
    n === void 0 && (n = !1);
    var i = this, r = i.config, a = i.state, o = i.api, s = 0;
    if (t && o.data.shown().length)
      if (s = t.ratio || t.value, e === "arc")
        if (i.pie.padAngle()())
          s = t.value / i.getTotalDataSum(!0);
        else {
          var l = r.gauge_fullCircle ? i.getArcLength() : i.getStartingAngle() * -2, c = i.hasType("gauge") ? l : Math.PI * 2;
          s = (t.endAngle - t.startAngle) / c;
        }
      else if (e === "index") {
        var f = o.data.values.bind(o), h = this.getTotalPerIndex();
        if (a.hiddenTargetIds.length) {
          var g = f(a.hiddenTargetIds, !1);
          g.length && (g = g.reduce(function(A, w) {
            return A.map(function(C, R) {
              return (ge(C) ? C : 0) + w[R];
            });
          }), h = h.map(function(A, w) {
            return A - g[w];
          }));
        }
        var x = h[t.index];
        t.ratio = ge(t.value) && h && x ? t.value / x : 0, s = t.ratio;
      } else if (e === "radar")
        s = parseFloat(String(Math.max(t.value, 0))) / a.current.dataMax * r.radar_size_ratio;
      else if (e === "bar") {
        var y = i.getYScaleById.bind(i)(t.id), T = y.domain().reduce(function(A, w) {
          return w - A;
        });
        s = T === 0 ? 0 : Math.abs(i.getRangedData(t, null, e) / T);
      } else
        e === "treemap" && (s /= i.getTotalDataSum(!0));
    return n && s ? s * 100 : s;
  },
  /**
   * Sort data index to be aligned with x axis.
   * @param {Array} tickValues Tick array values
   * @private
   */
  updateDataIndexByX: function(e) {
    var t = this, n = e.reduce(function(i, r, a) {
      return i[Number(r.x)] = a, i;
    }, {});
    t.data.targets.forEach(function(i) {
      i.values.forEach(function(r, a) {
        var o = n[Number(r.x)];
        o === void 0 && (o = a), r.index = o;
      });
    });
  },
  /**
   * Determine if bubble has dimension data
   * @param {object|Array} d data value
   * @returns {boolean}
   * @private
   */
  isBubbleZType: function(e) {
    var t = this;
    return t.isBubbleType(e) && (Be(e.value) && ("z" in e.value || "y" in e.value) || Ie(e.value) && e.value.length >= 2);
  },
  /**
   * Determine if bar has ranged data
   * @param {Array} d data value
   * @returns {boolean}
   * @private
   */
  isBarRangeType: function(e) {
    var t = this, n = e.value;
    return t.isBarType(e) && Ie(n) && n.length >= 2 && n.every(function(i) {
      return ge(i);
    });
  },
  /**
   * Get data object by id
   * @param {string} id data id
   * @returns {object}
   * @private
   */
  getDataById: function(e) {
    var t, n = this.cache.get(e) || this.api.data(e);
    return (t = n == null ? void 0 : n[0]) !== null && t !== void 0 ? t : n;
  }
};
function oc(e, t) {
  t === void 0 && (t = !1);
  var n = this, i = n.api;
  t && n.api.flush(!0), e == null || e.call(i);
}
var V_ = {
  load: function(e, t) {
    var n = this, i = n.axis, r = n.data, a = n.org, o = n.scale, s = t.append, l = {
      domain: null,
      currentDomain: null,
      x: null
    }, c = e;
    c && (t.filter && (c = c.filter(t.filter)), (t.type || t.types) && c.forEach(function(f) {
      var h, g = ((h = t.types) === null || h === void 0 ? void 0 : h[f.id]) || t.type;
      n.setTargetType(f.id, g);
    }), r.targets.forEach(function(f) {
      for (var h = 0; h < c.length; h++)
        if (f.id === c[h].id) {
          f.values = s ? f.values.concat(c[h].values) : c[h].values, c.splice(h, 1);
          break;
        }
    }), r.targets = r.targets.concat(c)), n.updateTargets(r.targets), o.zoom && (l.x = i.isCategorized() ? o.x.orgScale() : (a.xScale || o.x).copy(), l.domain = n.getXDomain(r.targets), l.x.domain(l.domain), l.currentDomain = n.zoom.getDomain(), n.withinRange(l.currentDomain, void 0, l.domain) || (o.x.domain(l.domain), o.zoom = null, n.$el.eventRect.property("__zoom", null))), n.redraw({
      withUpdateOrgXDomain: !0,
      withUpdateXDomain: !0,
      withLegend: !0
    }), o.zoom ? (a.xDomain = l.domain, a.xScale = l.x, i.isCategorized() && (l.currentDomain = n.getZoomDomainValue(l.currentDomain), a.xDomain = n.getZoomDomainValue(a.xDomain), a.xScale = l.x.domain(a.xDomain)), n.updateCurrentZoomTransform(l.x, l.currentDomain)) : a.xScale && a.xScale.domain(a.xDomain), n.updateTypesElements(), oc.call(n, t.done, t.resizeAfter);
  },
  loadFromArgs: function(e) {
    var t = this;
    t.config && (t.cache.reset(), t.convertData(e, function(n) {
      var i = e.data || n;
      e.append && (i.__append__ = !0), i && t.load(t.convertDataToTargets(i), e);
    }));
  },
  unload: function(e, t) {
    var n, i = this, r = i.state, a = i.$el, o = i.$T, s = !!(!((n = i.hasLegendDefsPoint) === null || n === void 0) && n.call(i)), l = t, c = e;
    if (i.cache.reset(), l || (l = function() {
    }), c = c.filter(function(h) {
      return i.hasTarget(i.data.targets, h);
    }), !c || c.length === 0) {
      l();
      return;
    }
    var f = a.svg.selectAll(c.map(function(h) {
      return i.selectorTarget(h);
    }));
    o(f).style("opacity", "0").remove().call(Ir, l), c.forEach(function(h) {
      var g, x = i.getTargetSelectorSuffix(h);
      r.withoutFadeIn[h] = !1, a.legend && a.legend.selectAll(".".concat(Pe.legendItem).concat(x)).remove(), i.data.targets = i.data.targets.filter(function(y) {
        return y.id !== h;
      }), s && ((g = a.defs) === null || g === void 0 || g.select("#".concat(i.getDefsPointId(x))).remove());
    }), r.hasFunnel && i.updateFunnel(i.data.targets), r.hasTreemap && i.updateTargetsForTreemap(i.data.targets), i.updateTypesElements();
  }
}, Y_ = {
  /**
   * Expand data shape/point
   * @param {number} index Index number
   * @param {string} id Data id
   * @param {boolean} reset Reset expand state
   * @private
   */
  setExpand: function(e, t, n) {
    var i = this, r = i.config, a = i.$el.circle;
    a && r.point_focus_expand_enabled && i.expandCircles(e, t, n), i.expandBarTypeShapes(!0, e, t, n);
  },
  /**
   * Expand/Unexpand bar type shapes
   * @param {boolean} expand Expand or unexpand
   * @param {number} i Shape index
   * @param {string} id Data id
   * @param {boolean} reset Reset expand style
   * @private
   */
  expandBarTypeShapes: function(e, t, n, i) {
    e === void 0 && (e = !0);
    var r = this;
    ["bar", "candlestick"].filter(function(a) {
      return r.$el[a];
    }).forEach(function(a) {
      i && r.$el[a].classed(Ee.EXPANDED, !1), r.getShapeByIndex(a, t, n).classed(Ee.EXPANDED, e);
    });
  },
  /**
   * Handle data.onover/out callback options
   * @param {boolean} isOver Over or not
   * @param {number|object} d data object
   * @private
   */
  setOverOut: function(e, t) {
    var n = this, i = n.config, r = n.state, a = r.hasFunnel, o = r.hasRadar, s = r.hasTreemap, l = n.$el.main, c = Be(t);
    if (c || t !== -1) {
      var f = i[e ? "data_onover" : "data_onout"].bind(n.api);
      if (i.color_onover && n.setOverColor(e, t, c), c) {
        var h = n.getTargetSelectorSuffix(t.id), g = a || s ? "".concat(Ee.target + h, " .").concat(ct.shape) : Xe.arc + h;
        f(t, l.select(".".concat(g)).node());
      } else if (i.tooltip_grouped)
        e && (o && n.isPointFocusOnly() ? n.showCircleFocus(n.getAllValuesOnIndex(t, !0)) : n.setExpand(t, null, !0)), !n.isMultipleX() && l.selectAll(".".concat(ct.shape, "-").concat(t)).each(function(w) {
          f(w, this);
        });
      else {
        var x = n.cache.get(Xt.setOverOut) || [], y = l.selectAll(".".concat(ct.shape, "-").concat(t)).filter(function(w) {
          return n.isWithinShape(this, w);
        }), T = y.filter(function() {
          var w = this;
          return x.every(function(C) {
            return C !== w;
          });
        });
        if (!e || y.empty() || x.length === T.size() && T.nodes().every(function(w, C) {
          return w !== x[C];
        }))
          for (; x.length; ) {
            var A = x.pop();
            i.data_onout.bind(n.api)(le(A).datum(), A);
          }
        T.each(function() {
          e && (f(le(this).datum(), this), x.push(this));
        }), n.cache.add(Xt.setOverOut, x);
      }
    }
  },
  /**
   * Call data.onover/out callback for touch event
   * @param {number|object} d target index or data object for Arc type
   * @private
   */
  callOverOutForTouch: function(e) {
    var t = this, n = t.cache.get(Xt.callOverOutForTouch);
    (Be(e) && n ? e.id !== n.id : e !== n) && ((n || ge(n)) && t.setOverOut(!1, n), (e || ge(e)) && t.setOverOut(!0, e), t.cache.add(Xt.callOverOutForTouch, e));
  },
  /**
   * Return draggable selection function
   * @returns {Function}
   * @private
   */
  getDraggableSelection: function() {
    var e = this, t = e.config, n = e.state;
    return t.interaction_enabled && t.data_selection_draggable && e.drag ? zh().on("drag", function(i) {
      n.event = i, e.drag(Qt(i, this));
    }).on("start", function(i) {
      n.event = i, e.dragstart(Qt(i, this));
    }).on("end", function(i) {
      n.event = i, e.dragend();
    }) : function() {
    };
  },
  /**
   * Dispatch a mouse event.
   * @private
   * @param {string} type event type
   * @param {number} index Index of eventRect
   * @param {Array} mouse x and y coordinate value
   */
  dispatchEvent: function(e, t, n) {
    var i, r, a = this, o = a.config, s = a.state, l = s.eventReceiver, c = s.hasAxis, f = s.hasFunnel, h = s.hasRadar, g = s.hasTreemap, x = a.$el, y = x.eventRect, T = x.funnel, A = x.radar, w = x.svg, C = x.treemap, R = (r = (f || g) && l.rect || h && A.axes.select(".".concat(nt.axis, "-").concat(t, " text")) || y || ((i = a.getArcElementByIdOrIndex) === null || i === void 0 ? void 0 : i.call(a, t))) === null || r === void 0 ? void 0 : r.node();
    if (R) {
      var S = a.isMultipleX(), I = o.axis_rotated, L = R.getBoundingClientRect(), p = L.width, U = L.left, H = L.top;
      if (c && !h && !S) {
        var q = l.coords[t];
        q ? (p = q.w, U += q.x, H += q.y) : (p = 0, U = 0, H = 0);
      }
      var X = U + (n ? n[0] : 0) + (S || I ? 0 : p / 2), j = H + (n ? n[1] : 0) + (I ? 4 : 0);
      if (zo(w)) {
        var z = Pr(a.$el.eventRect.node(), X, j, !1);
        X = z.x, j = z.y;
      }
      var G = {
        screenX: X,
        screenY: j,
        clientX: X,
        clientY: j,
        bubbles: h
        // radar type needs to bubble up event
      };
      (f || g) && (R = (T != null ? T : C).node()), $_[/^(mouse|click)/.test(e) ? "mouse" : "touch"](R, e, G);
    }
  },
  setDragStatus: function(e) {
    this.state.dragging = e;
  },
  /**
   * Unbind zoom events
   * @private
   */
  unbindZoomEvent: function() {
    var e = this, t = e.$el, n = t.eventRect, i = t.zoomResetBtn;
    n == null || n.on(".zoom wheel.zoom .drag", null), i == null || i.on("click", null).style("display", "none");
  },
  /**
   * Unbind all attached events
   * @private
   */
  unbindAllEvents: function() {
    var e, t = this, n = t.$el, i = n.arcs, r = n.eventRect, a = n.legend, o = n.region, s = n.svg, l = n.treemap, c = t.brush, f = [
      "wheel",
      "click",
      "mouseover",
      "mousemove",
      "mouseout",
      "touchstart",
      "touchmove",
      "touchend",
      "touchstart.eventRect",
      "touchmove.eventRect",
      "touchend.eventRect",
      ".brush",
      ".drag",
      ".zoom",
      "wheel.zoom",
      "dblclick.zoom"
    ].join(" ");
    [
      s,
      r,
      o == null ? void 0 : o.list,
      c == null ? void 0 : c.getSelection(),
      i == null ? void 0 : i.selectAll("path"),
      a == null ? void 0 : a.selectAll("g"),
      l
    ].forEach(function(h) {
      return h == null ? void 0 : h.on(f, null);
    }), (e = t.unbindZoomEvent) === null || e === void 0 || e.call(t);
  }
}, U_ = {
  /**
   * Category Name
   * @param {number} i Index number
   * @returns {string} category Name
   * @private
   */
  categoryName: function(e) {
    var t, n = this.config.axis_x_categories;
    return (t = n == null ? void 0 : n[e]) !== null && t !== void 0 ? t : e;
  }
}, j_ = {
  generateClass: function(e, t) {
    return " ".concat(e, " ").concat(e + this.getTargetSelectorSuffix(t));
  },
  /**
   * Get class string
   * @param {string} type Shape type
   * @param {boolean} withShape Get with shape prefix
   * @returns {string} Class string
   * @private
   */
  getClass: function(e, t) {
    var n = this, i = /s$/.test(e), r = /^(area|arc|line|funnel|treemap)s?$/.test(e), a = i ? "id" : "index";
    return function(o) {
      var s = o.data || o, l = (t ? n.generateClass(lt[i ? "shapes" : "shape"], s[a]) : "") + n.generateClass(lt[e], s[r ? "id" : a]);
      return l.trim();
    };
  },
  /**
   * Get chart class string
   * @param {string} type Shape type
   * @returns {string} Class string
   * @private
   */
  getChartClass: function(e) {
    var t = this;
    return function(n) {
      return lt["chart".concat(e)] + t.classTarget((n.data ? n.data : n).id);
    };
  },
  generateExtraLineClass: function() {
    var e = this, t = e.config.line_classes || [], n = [];
    return function(i) {
      var r, a = i.id || ((r = i.data) === null || r === void 0 ? void 0 : r.id) || i;
      return n.indexOf(a) < 0 && n.push(a), t[n.indexOf(a) % t.length];
    };
  },
  classRegion: function(e, t) {
    return "".concat(this.generateClass(lt.region, t), " ").concat("class" in e ? e.class : "");
  },
  classTarget: function(e) {
    var t = this.config.data_classes[e], n = "";
    return t && (n = " ".concat(lt.target, "-").concat(t)), this.generateClass(lt.target, e) + n;
  },
  classFocus: function(e) {
    return this.classFocused(e) + this.classDefocused(e);
  },
  classFocused: function(e) {
    return " ".concat(this.state.focusedTargetIds.indexOf(e.id) >= 0 ? lt.focused : "");
  },
  classDefocused: function(e) {
    return " ".concat(this.state.defocusedTargetIds.indexOf(e.id) >= 0 ? lt.defocused : "");
  },
  getTargetSelectorSuffix: function(e) {
    var t = e || e === 0 ? "-".concat(e) : "";
    return t.replace(/[\x00-\x20\x7F-\xA0\s?!@#$%^&*()_=+,.<>'":;\[\]\/|~`{}\\]/g, "-");
  },
  selectorTarget: function(e, t, n) {
    t === void 0 && (t = ""), n === void 0 && (n = "");
    var i = this.getTargetSelectorSuffix(e);
    return "".concat(t, ".").concat(lt.target + i, " ").concat(n, ", ").concat(t, ".").concat(lt.circles + i, " ").concat(n);
  },
  selectorTargets: function(e, t) {
    var n = this, i = e || [];
    return i.length ? i.map(function(r) {
      return n.selectorTarget(r, t);
    }) : null;
  },
  selectorLegend: function(e) {
    return ".".concat(lt.legendItem + this.getTargetSelectorSuffix(e));
  },
  selectorLegends: function(e) {
    var t = this;
    return e != null && e.length ? e.map(function(n) {
      return t.selectorLegend(n);
    }) : null;
  }
}, G_ = function(e, t, n) {
  var i = le(e.cloneNode(!0));
  return i.attr("id", n).insert("rect", ":first-child").attr("width", i.attr("width")).attr("height", i.attr("height")).style("fill", t), {
    id: n,
    node: i.node()
  };
};
function W_(e) {
  var t = Xt.colorPattern, n = je.body, i = n[t];
  if (!i) {
    var r = ";", a = e.classed(ao.colorPattern, !0).style("background-image");
    e.classed(ao.colorPattern, !1), a.indexOf(r) > -1 && (i = a.replace(/url[^#]*|["'()]|(\s|%20)/g, "").split(r).map(function(o) {
      return o.trim().replace(/[\"'\s]/g, "");
    }).filter(Boolean), n[t] = i);
  }
  return i;
}
var q_ = [
  "#1f77b4",
  "#ff7f0e",
  "#2ca02c",
  "#d62728",
  "#9467bd",
  "#8c564b",
  "#e377c2",
  "#7f7f7f",
  "#bcbd22",
  "#17becf"
], Z_ = {
  generateColor: function() {
    var e = this, t = e.$el, n = e.config, i = n.data_colors, r = n.data_color, a = [], o = Qe(n.color_pattern) ? n.color_pattern : Tl(W_(t.chart) || q_).range(), s = o;
    if (he(n.color_tiles)) {
      var l = n.color_tiles.bind(e.api)(), c = o.map(function(f, h) {
        var g = f.replace(/[#\(\)\s,]/g, ""), x = "".concat(e.state.datetimeId, "-pattern-").concat(g, "-").concat(h);
        return G_(l[h % l.length], f, x);
      });
      o = c.map(function(f) {
        return "url(#".concat(f.id, ")");
      }), e.patterns = c;
    }
    return function(f) {
      var h, g = f.id || ((h = f.data) === null || h === void 0 ? void 0 : h.id) || f, x = e.isTypeOf(g, ["line", "spline", "step"]) || !n.data_types[g], y;
      return he(i[g]) ? y = i[g].bind(e.api)(f) : i[g] ? y = i[g] : (a.indexOf(g) < 0 && a.push(g), y = x ? s[a.indexOf(g) % s.length] : o[a.indexOf(g) % o.length], i[g] = y), he(r) ? r.bind(e.api)(y, f) : y;
    };
  },
  generateLevelColor: function() {
    var e = this, t = e.config, n = t.color_pattern, i = t.color_threshold, r = i.unit === "value", a = i.max || 100, o = i.values && i.values.length ? i.values : [];
    return Qe(i) ? function(s) {
      for (var l = r ? s : s * 100 / a, c = n[n.length - 1], f = 0, h = o.length; f < h; f++)
        if (l <= o[f]) {
          c = n[f];
          break;
        }
      return c;
    } : null;
  },
  /**
   * Append data backgound color filter definition
   * @param {string|object} color Color string
   * @param {object} attr filter attribute
   * @private
   */
  generateTextBGColorFilter: function(e, t) {
    t === void 0 && (t = {
      x: 0,
      y: 0,
      width: 1,
      height: 1
    });
    var n = this, i = n.$el, r = n.state;
    if (e) {
      var a = [];
      Ne(e) ? a.push("") : Be(e) && (a = Object.keys(e)), a.forEach(function(o) {
        var s = "".concat(r.datetimeId, "-labels-bg").concat(n.getTargetSelectorSuffix(o)).concat(Ne(e) ? n.getTargetSelectorSuffix(e) : "");
        i.defs.append("filter").attr("x", t.x).attr("y", t.y).attr("width", t.width).attr("height", t.height).attr("id", s).html('<feFlood flood-color="'.concat(o === "" ? e : e[o], `" />
						<feComposite in="SourceGraphic" />`));
      });
    }
  },
  /**
   * Get data gradient color url
   * @param {string} id Data id
   * @returns {string}
   * @private
   */
  getGradienColortUrl: function(e) {
    return "url(#".concat(this.state.datetimeId, "-gradient").concat(this.getTargetSelectorSuffix(e), ")");
  },
  /**
   * Update linear/radial gradient definition
   * - linear: area & bar only
   * - radial: type which has data points only
   * @private
   */
  updateLinearGradient: function() {
    var e = this, t = e.config, n = e.data.targets, i = e.state.datetimeId, r = e.$el.defs;
    n.forEach(function(a) {
      var o = "".concat(i, "-gradient").concat(e.getTargetSelectorSuffix(a.id)), s = e.hasPointType() && t.point_radialGradient, l = e.isAreaType(a) && "area" || e.isBarType(a) && "bar";
      if ((s || l) && r.select("#".concat(o)).empty()) {
        var c = e.color(a), f = {
          defs: null,
          stops: []
        };
        if (s) {
          var h = s.cx, g = h === void 0 ? 0.3 : h, x = s.cy, y = x === void 0 ? 0.3 : x, T = s.r, A = T === void 0 ? 0.7 : T, w = s.stops, C = w === void 0 ? [[0.1, c, 0], [0.9, c, 1]] : w;
          f.stops = C, f.defs = r.append("radialGradient").attr("id", "".concat(o)).attr("cx", g).attr("cy", y).attr("r", A);
        } else {
          var R = t.axis_rotated, S = t["".concat(l, "_linearGradient")], I = S.x, L = I === void 0 ? R ? [1, 0] : [0, 0] : I, p = S.y, U = p === void 0 ? R ? [0, 0] : [0, 1] : p, H = S.stops, C = H === void 0 ? [[0, c, 1], [1, c, 0]] : H;
          f.stops = C, f.defs = r.append("linearGradient").attr("id", "".concat(o)).attr("x1", L[0]).attr("x2", L[1]).attr("y1", U[0]).attr("y2", U[1]);
        }
        f.stops.forEach(function(q) {
          var X = q[0], j = q[1], z = q[2], G = he(j) ? j.bind(e.api)(a.id) : j;
          f.defs && f.defs.append("stop").attr("offset", X).attr("stop-color", G || c).attr("stop-opacity", z);
        });
      }
    });
  },
  /**
   * Set the data over color.
   * When is out, will restate in its previous color value
   * @param {boolean} isOver true: set overed color, false: restore
   * @param {number|object} d target index or data object for Arc type
   * @private
   */
  setOverColor: function(e, t) {
    var n = this, i = n.config, r = n.$el.main, a = i.color_onover, o = e ? a : n.color;
    Be(o) ? o = function(s) {
      var l = s.id;
      return l in a ? a[l] : n.color(l);
    } : Ne(o) ? o = function() {
      return a;
    } : he(a) && (o = o.bind(n.api)), r.selectAll(Be(t) ? (
      // when is Arc type
      ".".concat(Xe.arc).concat(n.getTargetSelectorSuffix(t.id))
    ) : ".".concat(ct.shape, "-").concat(t)).style("fill", o);
  }
}, K_ = {
  getYDomainMinMax: function(e, t) {
    var n = this, i = n.axis, r = n.config, a = t === "min", o = r.data_groups, s = n.mapToIds(e), l = n.getValuesAsIdKeyed(e);
    if (o.length > 0) {
      var c = n["has".concat(a ? "Negative" : "Positive", "ValueInTargets")](e);
      o.forEach(function(f) {
        var h = f.filter(function(y) {
          return s.indexOf(y) >= 0;
        });
        if (h.length) {
          var g = h[0], x = i.getId(g);
          c && l[g] && (l[g] = l[g].map(function(y) {
            return (a ? y < 0 : y > 0) ? y : 0;
          })), h.filter(function(y, T) {
            return T > 0;
          }).forEach(function(y) {
            if (l[y]) {
              var T = i.getId(y);
              l[y].forEach(function(A, w) {
                var C = +A, R = a ? C > 0 : C < 0;
                T === x && !(c && R) && (l[g][w] += C);
              });
            }
          });
        }
      });
    }
    return yn(t, Object.keys(l).map(function(f) {
      return yn(t, l[f]);
    }));
  },
  /**
   * Check if hidden targets bound to the given axis id
   * @param {string} id ID to be checked
   * @returns {boolean}
   * @private
   */
  isHiddenTargetWithYDomain: function(e) {
    var t = this;
    return t.state.hiddenTargetIds.some(function(n) {
      return t.axis.getId(n) === e;
    });
  },
  getYDomain: function(e, t, n) {
    var i = this, r = i.axis, a = i.config, o = i.scale, s = "axis_".concat(t);
    if (i.isStackNormalized())
      return [0, 100];
    var l = (o == null ? void 0 : o[t]) && o[t].type === "log", c = e.filter(function(G) {
      return r.getId(G.id) === t;
    }), f = n ? i.filterByXDomain(c, n) : c;
    if (f.length === 0)
      return i.isHiddenTargetWithYDomain(t) ? o[t].domain() : t === "y2" ? o.y.domain() : (
        // When all data bounds to y2, y Axis domain is called prior y2.
        // So, it needs to call to get y2 domain here
        i.getYDomain(e, "y2", n)
      );
    var h = a["".concat(s, "_min")], g = a["".concat(s, "_max")], x = a["".concat(s, "_center")], y = a["".concat(s, "_inverted")], T = i.hasDataLabel() && a.axis_rotated, A = i.hasDataLabel() && !a.axis_rotated, w = i.getYDomainMinMax(f, "min"), C = i.getYDomainMinMax(f, "max"), R = Ke([ve.BAR, ve.BUBBLE, ve.SCATTER], xn.Line, !0).some(function(G) {
      var _e = G.indexOf("area") > -1 ? "area" : G;
      return i.hasType(G, f, !0) && a["".concat(_e, "_zerobased")];
    });
    w = Ae(h) ? h : Ae(g) ? w <= g ? w : g - 10 : w, C = Ae(g) ? g : Ae(h) ? h <= C ? C : h + 10 : C, isNaN(w) && (w = 0), isNaN(C) && (C = w), w === C && (w < 0 ? C = 0 : w = 0);
    var S = w >= 0 && C >= 0, I = w <= 0 && C <= 0;
    (Ae(h) && S || Ae(g) && I) && (R = !1), R && (S && (w = 0), I && (C = 0));
    var L = Math.abs(C - w), p = { top: L * 0.1, bottom: L * 0.1 };
    if (Ye(x)) {
      var U = Math.max(Math.abs(w), Math.abs(C));
      C = x + U, w = x - U;
    }
    if (T) {
      var H = Ri(o.y.range()), q = i.getDataLabelLength(w, C, "width").map(function(G) {
        return G / H;
      });
      ["bottom", "top"].forEach(function(G, _e) {
        p[G] += L * (q[_e] / (1 - q[0] - q[1]));
      });
    } else if (A) {
      var X = i.getDataLabelLength(w, C, "height");
      ["bottom", "top"].forEach(function(G, _e) {
        p[G] += i.convertPixelToScale("y", X[_e], L);
      });
    }
    p = i.getResettedPadding(p);
    var j = a["".concat(s, "_padding")];
    Qe(j) && ["bottom", "top"].forEach(function(G) {
      p[G] = r.getPadding(j, G, p[G], L);
    }), R && (S && (p.bottom = w), I && (p.top = -C));
    var z = l ? [w, C].map(function(G) {
      return G < 0 ? 0 : G;
    }) : [w - p.bottom, C + p.top];
    return y ? z.reverse() : z;
  },
  getXDomainMinMax: function(e, t) {
    var n, i = this, r = i.config["axis_x_".concat(t)], a = yn(t, e.map(function(s) {
      return yn(t, s.values.map(function(l) {
        return l.x;
      }));
    })), o = Be(r) ? r.value : r;
    return o = Ye(o) && (!((n = i.axis) === null || n === void 0) && n.isTimeSeries()) ? kt.bind(this)(o) : o, Be(r) && r.fit && (t === "min" && o < a || t === "max" && o > a) && (o = void 0), Ye(o) ? o : a;
  },
  /**
   * Get x Axis padding
   * @param {Array} domain x Axis domain
   * @param {number} tickCount Tick count
   * @returns {object} Padding object values with 'left' & 'right' key
   * @private
   */
  getXDomainPadding: function(e, t) {
    var n = this, i = n.axis, r = n.config, a = r.axis_x_padding, o = i.isTimeSeries() && t, s = Ri(e), l;
    if (i.isCategorized() || o)
      l = 0;
    else if (n.hasType("bar")) {
      var c = n.getMaxDataCount();
      l = c > 1 ? s / (c - 1) / 2 : 0.5;
    } else
      l = n.getResettedPadding(s * 0.01);
    var f = ge(a) ? { left: a, right: a } : a, h = f.left, g = h === void 0 ? l : h, x = f.right, y = x === void 0 ? l : x;
    if (a.unit === "px") {
      var T = Math.abs(s + s * 0.2);
      g = i.getPadding(a, "left", l, T), y = i.getPadding(a, "right", l, T);
    } else {
      var A = s + g + y;
      if (o && A) {
        var w = s / t / A;
        g = g / A / w, y = y / A / w;
      }
    }
    return { left: g, right: y };
  },
  /**
   * Get x Axis domain
   * @param {Array} targets targets
   * @returns {Array} x Axis domain
   * @private
   */
  getXDomain: function(e) {
    var t = this, n = t.axis, i = t.config, r = t.scale.x, a = i.axis_x_inverted, o = [
      t.getXDomainMinMax(e, "min"),
      t.getXDomainMinMax(e, "max")
    ], s = o[0], l = s === void 0 ? 0 : s, c = o[1], f = c === void 0 ? 0 : c;
    if (r.type !== "log") {
      var h = n.isCategorized(), g = n.isTimeSeries(), x = t.getXDomainPadding(o), y = o[0], T = o[1];
      y - T === 0 && !h && (g ? (y = new Date(y.getTime() * 0.5), T = new Date(T.getTime() * 1.5)) : (y = y === 0 ? 1 : y * 0.5, T = T === 0 ? -1 : T * 1.5)), (y || y === 0) && (l = g ? new Date(y.getTime() - x.left) : y - x.left), (T || T === 0) && (f = g ? new Date(T.getTime() + x.right) : T + x.right);
    }
    return a ? [f, l] : [l, f];
  },
  updateXDomain: function(e, t, n, i, r) {
    var a, o = this, s = o.config, l = o.org, c = o.scale, f = c.x, h = c.subX, g = s.zoom_enabled;
    if (n && (f.domain(r || si(o.getXDomain(e), !s.axis_x_inverted)), l.xDomain = f.domain(), h.domain(f.domain()), (a = o.brush) === null || a === void 0 || a.scale(h)), t) {
      var x = r || !o.brush || y_(o) ? l.xDomain : ec(o).map(h.invert);
      f.domain(x);
    }
    return (n || t) && g && o.zoom.updateScaleExtent(), i && f.domain(o.trimXDomain(f.orgDomain())), f.domain();
  },
  /**
   * Trim x domain when given domain surpasses the range
   * @param {Array} domain Domain value
   * @returns {Array} Trimed domain if given domain is out of range
   * @private
   */
  trimXDomain: function(e) {
    var t = this, n = t.config.axis_x_inverted, i = t.getZoomDomain(), r = i[0], a = i[1];
    return (n ? e[0] >= r : e[0] <= r) && (e[1] = +e[1] + (r - e[0]), e[0] = r), (n ? e[1] <= a : e[1] >= a) && (e[0] = +e[0] - (e[1] - a), e[1] = a), e;
  },
  /**
   * Get subchart/zoom domain
   * @param {string} type "subX" or "zoom"
   * @param {boolean} getCurrent Get current domain if true
   * @returns {Array} zoom domain
   * @private
   */
  getZoomDomain: function(e, t) {
    e === void 0 && (e = "zoom"), t === void 0 && (t = !1);
    var n = this, i = n.config, r = n.scale, a = n.org, o = t && r[e] ? r[e].domain() : a.xDomain, s = o[0], l = o[1];
    return e === "zoom" && (Ye(i.zoom_x_min) && (s = yn("min", [s, i.zoom_x_min])), Ye(i.zoom_x_max) && (l = yn("max", [l, i.zoom_x_max]))), [s, l];
  },
  /**
   * Return zoom domain from given domain
   * - 'category' type need to add offset to original value
   * @param {Array} domainValue domain value
   * @returns {Array} Zoom domain
   * @private
   */
  getZoomDomainValue: function(e) {
    var t = this, n = t.config, i = t.axis;
    if (i.isCategorized() && Array.isArray(e)) {
      var r = n.axis_x_inverted, a = e.map(function(o, s) {
        return Number(o) + (s === 0 ? +r : +!r);
      });
      return a;
    }
    return e;
  },
  /**
   * Converts pixels to axis' scale values
   * @param {string} type Axis type
   * @param {number} pixels Pixels
   * @param {number} domainLength Domain length
   * @returns {number}
   * @private
   */
  convertPixelToScale: function(e, t, n) {
    var i = this, r = i.config, a = i.state, o = r.axis_rotated, s;
    return e === "x" ? s = o ? "height" : "width" : s = o ? "width" : "height", n * (t / a[s]);
  },
  /**
   * Check if the given domain is within subchart/zoom range
   * @param {Array} domain Target domain value
   * @param {Array} current Current subchart/zoom domain value
   * @param {Array} range subchart/zoom range value
   * @returns {boolean}
   * @private
   */
  withinRange: function(e, t, n) {
    t === void 0 && (t = [0, 0]);
    var i = this, r = i.config.axis_x_inverted, a = n, o = a[0], s = a[1];
    if (Array.isArray(e)) {
      var l = Ke([], e, !0);
      if (r && l.reverse(), l[0] < l[1])
        return e.every(function(c, f) {
          return (f === 0 ? r ? +c <= o : +c >= o : r ? +c >= s : +c <= s) && !e.every(function(h, g) {
            return h === t[g];
          });
        });
    }
    return !1;
  }
};
function Eu(e, t, n) {
  var i = e.config, r = "axis_".concat(t, "_tick_format"), a = i[r] ? i[r] : e.defaultValueFormat;
  return a.call(e.api, n);
}
var Q_ = {
  yFormat: function(e) {
    return Eu(this, "y", e);
  },
  y2Format: function(e) {
    return Eu(this, "y2", e);
  },
  /**
   * Get default value format function
   * @returns {Function} formatter function
   * @private
   */
  getDefaultValueFormat: function() {
    var e = this, t = e.defaultArcValueFormat, n = e.yFormat, i = e.y2Format, r = e.hasArcType(null, ["gauge", "polar", "radar"]);
    return function(a, o, s) {
      var l = r ? t : e.axis && e.axis.getId(s) === "y2" ? i : n;
      return l.call(e, a, o);
    };
  },
  defaultValueFormat: function(e) {
    return Ie(e) ? e.join("~") : Ae(e) ? +e : "";
  },
  defaultArcValueFormat: function(e, t) {
    return "".concat((t * 100).toFixed(1), "%");
  },
  defaultPolarValueFormat: function(e) {
    return "".concat(e);
  },
  dataLabelFormat: function(e) {
    var t = this, n = t.config.data_labels, i = function(a) {
      var o = "~", s = a;
      return Ie(a) ? s = a.join(o) : Be(a) && (s = Object.values(a).join(o)), s;
    }, r = i;
    return he(n.format) ? r = n.format : Jt(n.format) && (n.format[e] ? r = n.format[e] === !0 ? i : n.format[e] : r = function() {
      return "";
    }), r.bind(t.api);
  }
};
function or(e) {
  var t = this, n = t.getDataById(e), i = t.levelColor ? t.levelColor(n.values[0].value) : t.color(n);
  return i;
}
function Oa(e, t) {
  var n;
  t === void 0 && (t = !0);
  var i = this.config, r = (n = i.data_names[e]) !== null && n !== void 0 ? n : e;
  return t && he(i.legend_format) && (r = i.legend_format(r, e !== r ? e : void 0)), r;
}
var J_ = {
  /**
   * Initialize the legend.
   * @private
   */
  initLegend: function() {
    var e = this, t = e.config, n = e.$el;
    e.legendItemTextBox = {}, e.state.legendHasRendered = !1, t.legend_show ? (t.legend_contents_bindto || (n.legend = e.$el.svg.append("g").classed(Pe.legend, !0).attr("transform", e.getTranslate("legend"))), e.updateLegend()) : e.state.hiddenLegendIds = e.mapToIds(e.data.targets);
  },
  /**
   * Update legend element
   * @param {Array} targetIds ID's of target
   * @param {object} options withTransform : Whether to use the transform property / withTransitionForTransform: Whether transition is used when using the transform property / withTransition : whether or not to transition.
   * @param {object} transitions Return value of the generateTransitions
   * @private
   */
  updateLegend: function(e, t, n) {
    var i, r = this, a = r.config, o = r.state, s = r.scale, l = r.$el, c = t || {
      withTransform: !1,
      withTransitionForTransform: !1,
      withTransition: !1
    };
    c.withTransition = mn(c, "withTransition", !0), c.withTransitionForTransform = mn(c, "withTransitionForTransform", !0), a.legend_contents_bindto && a.legend_contents_template ? r.updateLegendTemplate() : o.hasTreemap || r.updateLegendElement(e || r.mapToIds(r.data.targets), c, n), (i = l.legend) === null || i === void 0 || i.selectAll(".".concat(Pe.legendItem)).classed(Pe.legendItemHidden, function(f) {
      var h = !r.isTargetToShow(f);
      return h && (this.style.opacity = null), h;
    }), r.updateScales(!1, !s.zoom), r.updateSvgSize(), r.transformAll(c.withTransitionForTransform, n), o.legendHasRendered = !0;
  },
  /**
   * Update legend using template option
   * @private
   */
  updateLegendTemplate: function() {
    var e = this, t = e.config, n = e.$el, i = le(t.legend_contents_bindto), r = t.legend_contents_template;
    if (!i.empty()) {
      var a = e.mapToIds(e.data.targets), o = [], s = "";
      a.forEach(function(c) {
        var f = he(r) ? r.bind(e.api)(c, e.color(c), e.api.data(c)[0].values) : Or(r, {
          COLOR: e.color(c),
          TITLE: c
        });
        f && (o.push(c), s += f);
      });
      var l = i.html(s).selectAll(function() {
        return this.childNodes;
      }).data(o);
      e.setLegendItem(l), n.legend = i;
    }
  },
  /**
   * Update the size of the legend.
   * @param {Obejct} size Size object
   * @private
   */
  updateSizeForLegend: function(e) {
    var t = this, n = t.config, i = t.state, r = i.isLegendTop, a = i.isLegendLeft, o = i.isLegendRight, s = i.isLegendInset, l = i.current, c = e.width, f = e.height, h = {
      top: r ? t.getCurrentPaddingByDirection("top") + n.legend_inset_y + 5.5 : l.height - f - t.getCurrentPaddingByDirection("bottom") - n.legend_inset_y,
      left: a ? t.getCurrentPaddingByDirection("left") + n.legend_inset_x + 0.5 : l.width - c - t.getCurrentPaddingByDirection("right") - n.legend_inset_x + 0.5
    };
    t.state.margin3 = {
      top: o ? 0 : s ? h.top : l.height - f,
      right: NaN,
      bottom: 0,
      left: o ? l.width - c : s ? h.left : 0
    };
  },
  /**
   * Transform Legend
   * @param {boolean} withTransition whether or not to transition.
   * @private
   */
  transformLegend: function(e) {
    var t = this, n = t.$el.legend, i = t.$T;
    i(n, e).attr("transform", t.getTranslate("legend"));
  },
  /**
   * Update the legend step
   * @param {number} step Step value
   * @private
   */
  updateLegendStep: function(e) {
    this.state.legendStep = e;
  },
  /**
   * Update legend item width
   * @param {number} width Width value
   * @private
   */
  updateLegendItemWidth: function(e) {
    this.state.legendItemWidth = e;
  },
  /**
   * Update legend item height
   * @param {number} height Height value
   * @private
   */
  updateLegendItemHeight: function(e) {
    this.state.legendItemHeight = e;
  },
  /**
   * Update legend item color
   * @param {string} id Corresponding data ID value
   * @param {string} color Color value
   * @private
   */
  updateLegendItemColor: function(e, t) {
    var n = this.$el.legend;
    n && n.select(".".concat(Pe.legendItem, "-").concat(e, " line")).style("stroke", t);
  },
  /**
   * Get the width of the legend
   * @returns {number} width
   * @private
   */
  getLegendWidth: function() {
    var e = this, t = e.state, n = t.current.width, i = t.isLegendRight, r = t.isLegendInset, a = t.legendItemWidth, o = t.legendStep;
    return e.config.legend_show ? i || r ? a * (o + 1) : n : 0;
  },
  /**
   * Get the height of the legend
   * @returns {number} height
   * @private
   */
  getLegendHeight: function() {
    var e, t = this, n = t.state, i = n.current, r = n.isLegendRight, a = n.legendItemHeight, o = n.legendStep, s = ((e = t.config.padding) === null || e === void 0 ? void 0 : e.mode) === "fit", l = t.config.legend_show ? r ? i.height : Math.max(s ? 10 : 20, a) * (o + 1) : 0;
    return l;
  },
  /**
   * Get the opacity of the legend that is unfocused
   * @param {d3.selection} legendItem Legend item node
   * @returns {string|null} opacity
   * @private
   */
  opacityForUnfocusedLegend: function(e) {
    return e.classed(Pe.legendItemHidden) ? null : "0.3";
  },
  /**
   * Toggles the focus of the legend
   * @param {Array} targetIds ID's of target
   * @param {boolean} focus whether or not to focus.
   * @private
   */
  toggleFocusLegend: function(e, t) {
    var n = this, i = n.$el.legend, r = n.$T, a = n.mapToTargetIds(e);
    i && r(i.selectAll(".".concat(Pe.legendItem)).filter(function(o) {
      return a.indexOf(o) >= 0;
    }).classed(ze.legendItemFocused, t)).style("opacity", function() {
      return t ? null : n.opacityForUnfocusedLegend.call(n, le(this));
    });
  },
  /**
   * Revert the legend to its default state
   * @private
   */
  revertLegend: function() {
    var e = this, t = e.$el.legend, n = e.$T;
    t && n(t.selectAll(".".concat(Pe.legendItem)).classed(ze.legendItemFocused, !1)).style("opacity", null);
  },
  /**
   * Shows the legend
   * @param {Array} targetIds ID's of target
   * @private
   */
  showLegend: function(e) {
    var t = this, n = t.config, i = t.$el, r = t.$T;
    n.legend_show || (n.legend_show = !0, i.legend ? i.legend.style("visibility", null) : t.initLegend(), !t.state.legendHasRendered && t.updateLegend()), t.removeHiddenLegendIds(e), r(i.legend.selectAll(t.selectorLegends(e)).style("visibility", null)).style("opacity", null);
  },
  /**
   * Hide the legend
   * @param {Array} targetIds ID's of target
   * @private
   */
  hideLegend: function(e) {
    var t = this, n = t.config, i = t.$el.legend;
    n.legend_show && en(e) && (n.legend_show = !1, i.style("visibility", "hidden")), t.addHiddenLegendIds(e), i.selectAll(t.selectorLegends(e)).style("opacity", "0").style("visibility", "hidden");
  },
  /**
   * Get legend item textbox dimension
   * @param {string} id Data ID
   * @param {HTMLElement|d3.selection} textElement Text node element
   * @returns {object} Bounding rect
   * @private
   */
  getLegendItemTextBox: function(e, t) {
    var n = this, i = n.cache, r = n.state, a, o = Xt.legendItemTextBox;
    return e && (a = !r.redrawing && i.get(o) || {}, a[e] || (a[e] = n.getTextRect(t, Pe.legendItem), i.add(o, a)), a = a[e]), a;
  },
  /**
   * Set legend item style & bind events
   * @param {d3.selection} item Item node
   * @private
   */
  setLegendItem: function(e) {
    var t = this, n = t.$el, i = t.api, r = t.config, a = t.state, o = a.inputType === "touch", s = t.hasType("gauge"), l = r.boost_useCssRule, c = r.legend_item_interaction;
    e.attr("class", function(f) {
      var h = le(this), g = !h.empty() && h.attr("class") || "";
      return g + t.generateClass(Pe.legendItem, f);
    }).style("visibility", function(f) {
      return t.isLegendToShow(f) ? null : "hidden";
    }), r.interaction_enabled && (l && [
      [".".concat(Pe.legendItem), "cursor:pointer"],
      [".".concat(Pe.legendItem, " text"), "pointer-events:none"],
      [".".concat(Pe.legendItemPoint, " text"), "pointer-events:none"],
      [".".concat(Pe.legendItemTile), "pointer-events:none"],
      [".".concat(Pe.legendItemEvent), "fill-opacity:0"]
    ].forEach(function(f) {
      var h = f[0], g = f[1];
      t.setCssRule(!1, h, [g])(n.legend);
    }), e.on(c.dblclick ? "dblclick" : "click", c || he(r.legend_item_onclick) ? function(f, h) {
      if (!Ze(r.legend_item_onclick, i, h, !a.hiddenTargetIds.includes(h))) {
        var g = f.altKey, x = f.target, y = f.type;
        y === "dblclick" || g ? a.hiddenTargetIds.length && x.parentNode.getAttribute("class").indexOf(Pe.legendItemHidden) === -1 ? i.show() : (i.hide(), i.show(h)) : (i.toggle(h), le(this).classed(ze.legendItemFocused, !1));
      }
      o && t.hideTooltip();
    } : null), !o && e.on("mouseout", c || he(r.legend_item_onout) ? function(f, h) {
      Ze(r.legend_item_onout, i, h, !a.hiddenTargetIds.includes(h)) || (le(this).classed(ze.legendItemFocused, !1), s && t.undoMarkOverlapped(t, ".".concat(qt.gaugeValue)), t.api.revert());
    } : null).on("mouseover", c || he(r.legend_item_onover) ? function(f, h) {
      Ze(r.legend_item_onover, i, h, !a.hiddenTargetIds.includes(h)) || (le(this).classed(ze.legendItemFocused, !0), s && t.markOverlapped(h, t, ".".concat(qt.gaugeValue)), !a.transiting && t.isTargetToShow(h) && i.focus(h));
    } : null), !e.empty() && e.on("click mouseout mouseover") && e.style("cursor", t.getStylePropValue("pointer")));
  },
  /**
   * Update the legend
   * @param {Array} targetIds ID's of target
   * @param {object} options withTransform : Whether to use the transform property / withTransitionForTransform: Whether transition is used when using the transform property / withTransition : whether or not to transition.
   * @private
   */
  updateLegendElement: function(e, t) {
    var n = this, i = n.config, r = n.state, a = n.$el.legend, o = n.$T, s = i.legend_item_tile_type, l = s !== "circle", c = i.legend_item_tile_r, f = {
      width: l ? i.legend_item_tile_width : c * 2,
      height: l ? i.legend_item_tile_height : c * 2
    }, h = {
      padding: {
        top: 4,
        right: 10
      },
      max: {
        width: 0,
        height: 0
      },
      posMin: 10,
      step: 0,
      tileWidth: f.width + 5,
      totalLength: 0
    }, g = {
      offsets: {},
      widths: {},
      heights: {},
      margins: [0],
      steps: {}
    }, x, y, T, A = e.filter(function(L) {
      return !Ye(i.data_names[L]) || i.data_names[L] !== null;
    }), w = t.withTransition, C = n.getUpdateLegendPositions(A, h, g);
    r.isLegendInset && (h.step = i.legend_inset_step ? i.legend_inset_step : A.length, n.updateLegendStep(h.step)), r.isLegendRight ? (x = function(L) {
      return h.max.width * g.steps[L];
    }, y = function(L) {
      return g.margins[g.steps[L]] + g.offsets[L];
    }) : r.isLegendInset ? (x = function(L) {
      return h.max.width * g.steps[L] + 10;
    }, y = function(L) {
      return g.margins[g.steps[L]] + g.offsets[L];
    }) : (x = function(L) {
      return g.margins[g.steps[L]] + g.offsets[L];
    }, y = function(L) {
      return h.max.height * g.steps[L];
    });
    var R = {
      xText: function(L, p) {
        return x(L, p) + 4 + f.width;
      },
      xRect: function(L, p) {
        return x(L, p);
      },
      x1Tile: function(L, p) {
        return x(L, p) - 2;
      },
      x2Tile: function(L, p) {
        return x(L, p) - 2 + f.width;
      },
      yText: function(L, p) {
        return y(L, p) + 9;
      },
      yRect: function(L, p) {
        return y(L, p) - 5;
      },
      yTile: function(L, p) {
        return y(L, p) + 4;
      }
    };
    n.generateLegendItem(A, f, C, R), T = a.select(".".concat(Pe.legendBackground, " rect")), r.isLegendInset && h.max.width > 0 && T.size() === 0 && (T = a.insert("g", ".".concat(Pe.legendItem)).attr("class", Pe.legendBackground).append("rect")), i.legend_tooltip && a.selectAll("title").data(A).text(function(L) {
      return Oa.bind(n)(L, !1);
    });
    var S = a.selectAll("text").data(A).text(function(L) {
      return Oa.bind(n)(L);
    }).each(function(L, p) {
      C(this, L, p);
    });
    o(S, w).attr("x", R.xText).attr("y", R.yText);
    var I = a.selectAll("rect.".concat(Pe.legendItemEvent)).data(A);
    o(I, w).attr("width", function(L) {
      return g.widths[L];
    }).attr("height", function(L) {
      return g.heights[L];
    }).attr("x", R.xRect).attr("y", R.yRect), n.updateLegendItemPos(A, w, R), T && o(T, w).attr("height", n.getLegendHeight() - 12).attr("width", h.max.width * (h.step + 1) + 10), n.updateLegendItemWidth(h.max.width), n.updateLegendItemHeight(h.max.height), n.updateLegendStep(h.step);
  },
  /**
   * Get position update function
   * @param {Array} targetIdz Data ids
   * @param {object} dimension Dimension object
   * @param {object} sizes Size object
   * @returns {Function} Update position function
   * @private
   */
  getUpdateLegendPositions: function(e, t, n) {
    var i = this, r = i.config, a = i.state, o = a.isLegendRight || a.isLegendInset;
    return function(s, l, c) {
      var f = c === 0, h = c === e.length - 1, g = i.getLegendItemTextBox(l, s), x = g.width + t.tileWidth + (h && !o ? 0 : t.padding.right) + r.legend_padding, y = g.height + t.padding.top, T = o ? y : x, A = o ? i.getLegendHeight() : i.getLegendWidth(), w, C = function(S, I) {
        I || (w = (A - t.totalLength - T) / 2, w < t.posMin && (w = (A - T) / 2, t.totalLength = 0, t.step++)), n.steps[S] = t.step, n.margins[t.step] = a.isLegendInset ? 10 : w, n.offsets[S] = t.totalLength, t.totalLength += T;
      };
      if (f && (t.totalLength = 0, t.step = 0, t.max.width = 0, t.max.height = 0), r.legend_show && !i.isLegendToShow(l)) {
        n.widths[l] = 0, n.heights[l] = 0, n.steps[l] = 0, n.offsets[l] = 0;
        return;
      }
      n.widths[l] = x, n.heights[l] = y, (!t.max.width || x >= t.max.width) && (t.max.width = x), (!t.max.height || y >= t.max.height) && (t.max.height = y);
      var R = o ? t.max.height : t.max.width;
      r.legend_equally ? (Object.keys(n.widths).forEach(function(S) {
        return n.widths[S] = t.max.width;
      }), Object.keys(n.heights).forEach(function(S) {
        return n.heights[S] = t.max.height;
      }), w = (A - R * e.length) / 2, w < t.posMin ? (t.totalLength = 0, t.step = 0, e.forEach(function(S) {
        return C(S);
      })) : C(l, !0)) : C(l);
    };
  },
  /**
   * Generate legend item elements
   * @param {Array} targetIdz Data ids
   * @param {object} itemTileSize Item tile size {width, height}
   * @param {Function} updatePositions Update position function
   * @param {object} posFn Position functions
   * @private
   */
  generateLegendItem: function(e, t, n, i) {
    var r = this, a = r.config, o = r.state, s = r.$el.legend, l = a.legend_usePoint, c = a.legend_item_tile_r, f = a.legend_item_tile_type, h = f !== "circle", g = o.isLegendRight || o.isLegendInset, x = -200, y = s.selectAll(".".concat(Pe.legendItem)).data(e).enter().append("g");
    if (r.setLegendItem(y), a.legend_tooltip && y.append("title").text(function(A) {
      return A;
    }), y.append("text").text(function(A) {
      return Oa.bind(r)(A);
    }).each(function(A, w) {
      n(this, A, w);
    }).style("pointer-events", r.getStylePropValue("none")).attr("x", g ? i.xText : x).attr("y", g ? x : i.yText), y.append("rect").attr("class", Pe.legendItemEvent).style("fill-opacity", r.getStylePropValue("0")).attr("x", g ? i.xRect : x).attr("y", g ? x : i.yRect), l) {
      var T = [];
      y.append(function(A) {
        var w = Qe(a.point_pattern) ? a.point_pattern : [a.point_type];
        T.indexOf(A) === -1 && T.push(A);
        var C = w[T.indexOf(A) % w.length];
        return C === "rectangle" && (C = "rect"), je.createElementNS(Jn.svg, "hasValidPointType" in r && r.hasValidPointType(C) ? C : "use");
      }).attr("class", Pe.legendItemPoint).style("fill", or.bind(r)).style("pointer-events", r.getStylePropValue("none")).attr("href", function(A, w, C) {
        var R = C[w], S = R.nodeName.toLowerCase(), I = r.getTargetSelectorSuffix(A);
        return S === "use" ? "#".concat(o.datetimeId, "-point").concat(I) : void 0;
      });
    } else
      y.append(h ? "line" : f).attr("class", Pe.legendItemTile).style("stroke", or.bind(r)).style("pointer-events", r.getStylePropValue("none")).call(function(A) {
        f === "circle" ? A.attr("r", c).style("fill", or.bind(r)).attr("cx", g ? i.x2Tile : x).attr("cy", g ? x : i.yTile) : h && A.attr("stroke-width", t.height).attr("x1", g ? i.x1Tile : x).attr("y1", g ? x : i.yTile).attr("x2", g ? i.x2Tile : x).attr("y2", g ? x : i.yTile);
      });
  },
  /**
   * Update legend item position
   * @param {Array} targetIdz Data ids
   * @param {boolean} withTransition Whether or not to apply transition
   * @param {object} posFn Position functions
   * @private
   */
  updateLegendItemPos: function(e, t, n) {
    var i = this, r = i.config, a = i.$el.legend, o = i.$T, s = r.legend_usePoint, l = r.legend_item_tile_type, c = l !== "circle";
    if (s) {
      var f = a.selectAll(".".concat(Pe.legendItemPoint)).data(e);
      o(f, t).each(function() {
        var h = this.nodeName.toLowerCase(), g = r.point_r, x = "x", y = "y", T = 2, A = 2.5, w = null, C = null, R = null;
        if (h === "circle") {
          var S = g * 0.2;
          x = "cx", y = "cy", w = g + S, T = g * 2, A = -S;
        } else if (h === "rect") {
          var S = g * 2.5;
          C = S, R = S, A = 3;
        }
        le(this).attr(x, function(I) {
          return n.x1Tile(I) + T;
        }).attr(y, function(I) {
          return n.yTile(I) - A;
        }).attr("r", w).attr("width", C).attr("height", R);
      });
    } else {
      var f = a.selectAll(".".concat(Pe.legendItemTile)).data(e);
      o(f, t).style("stroke", or.bind(i)).call(function(g) {
        l === "circle" ? g.attr("cx", function(x) {
          var y = n.x2Tile(x);
          return y - (y - n.x1Tile(x)) / 2;
        }).attr("cy", n.yTile) : c && g.attr("x1", n.x1Tile).attr("y1", n.yTile).attr("x2", n.x2Tile).attr("y2", n.yTile);
      });
    }
  }
}, ey = {
  redraw: function(e) {
    var t, n, i, r;
    e === void 0 && (e = {});
    var a = this, o = a.config, s = a.state, l = a.$el, c = l.main, f = l.treemap;
    s.redrawing = !0;
    var h = a.filterTargetsToShow(a.data.targets), g = e.flow, x = e.initializing, y = a.getWithOption(e), T = y.Transition ? o.transition_duration : 0, A = y.TransitionForExit ? T : 0, w = y.TransitionForAxis ? T : 0, C = (t = a.axis) === null || t === void 0 ? void 0 : t.generateTransitions(w);
    a.updateSizes(x), y.Legend && o.legend_show ? (e.withTransition = !!T, !f && a.updateLegend(a.mapToIds(a.data.targets), e, C)) : y.Dimension && a.updateDimension(!0), o.data_empty_label_text && c.select("text.".concat(Ct.text, ".").concat(Ee.empty)).attr("x", s.width / 2).attr("y", s.height / 2).text(o.data_empty_label_text).style("display", h.length ? "none" : null), s.hasAxis ? (a.axis.redrawAxis(h, y, C, g, x), a.hasGrid() && a.updateGrid(), o.regions.length && a.updateRegion(), ["bar", "candlestick", "line", "area"].forEach(function(R) {
      var S = hn(R);
      (/^(line|area)$/.test(R) && a.hasTypeOf(S) || a.hasType(R)) && a["update".concat(S)](y.TransitionForExit);
    }), l.text && c.selectAll(".".concat(it.selectedCircles)).filter(a.isBarType.bind(a)).selectAll("circle").remove(), o.interaction_enabled && !g && y.EventRect && (a.redrawEventRect(), (n = a.bindZoomEvent) === null || n === void 0 || n.call(a))) : (l.arcs && a.redrawArc(T, A, y.Transform), l.radar && a.redrawRadar(), l.polar && a.redrawPolar(), l.funnel && a.redrawFunnel(), f && a.updateTreemap(A)), !s.resizing && !f && (a.hasPointType() || s.hasRadar) ? a.updateCircle() : !((i = a.hasLegendDefsPoint) === null || i === void 0) && i.call(a) && a.data.targets.forEach(a.point("create", this)), a.hasDataLabel() && !a.hasArcType(null, ["radar"]) && a.updateText(), (r = a.redrawTitle) === null || r === void 0 || r.call(a), x && a.updateTypesElements(), a.generateRedrawList(h, g, T, y.Subchart), a.updateTooltipOnRedraw(), a.callPluginHook("$redraw", e, T);
  },
  /**
   * Generate redraw list
   * @param {object} targets targets data to be shown
   * @param {object} flow flow object
   * @param {number} duration duration value
   * @param {boolean} withSubchart whether or not to show subchart
   * @private
   */
  generateRedrawList: function(e, t, n, i) {
    var r = this, a = r.config, o = r.state, s = r.getDrawShape();
    o.hasAxis && a.subchart_show && r.redrawSubchart(i, n, s);
    var l = t && r.generateFlow({
      targets: e,
      flow: t,
      duration: t.duration,
      shape: s,
      xv: r.xv.bind(r)
    }), c = (n || l) && Hi(), f = r.getRedrawList(s, t, l, c), h = function() {
      l && l(), o.redrawing = !1, Ze(a.onrendered, r.api);
    };
    if (c && f.length) {
      var g = ic();
      hl().duration(n).each(function() {
        f.reduce(function(x, y) {
          return x.concat(y);
        }, []).forEach(function(x) {
          return g.add(x);
        });
      }).call(g, h);
    } else
      o.transiting || h();
    r.mapToIds(r.data.targets).forEach(function(x) {
      o.withoutFadeIn[x] = !0;
    });
  },
  getRedrawList: function(e, t, n, i) {
    var r = this, a = r.config, o = r.state, s = o.hasAxis, l = o.hasRadar, c = o.hasTreemap, f = r.$el.grid, h = e.pos, g = h.cx, x = h.cy, y = h.xForText, T = h.yForText, A = [];
    return s && ((a.grid_x_lines.length || a.grid_y_lines.length) && A.push(r.redrawGrid(i)), a.regions.length && A.push(r.redrawRegion(i)), Object.keys(e.type).forEach(function(w) {
      var C = hn(w), R = e.type[w];
      (/^(area|line)$/.test(w) && r.hasTypeOf(C) || r.hasType(w)) && A.push(r["redraw".concat(C)](R, i));
    }), !t && f.main && A.push(r.updateGridFocus())), (!r.hasArcType() || l) && Qe(a.data_labels) && a.data_labels !== !1 && A.push(r.redrawText(y, T, t, i)), (r.hasPointType() || l) && !r.isPointFocusOnly() && r.redrawCircle && A.push(r.redrawCircle(g, x, i, n)), c && A.push(r.redrawTreemap(i)), A;
  },
  updateAndRedraw: function(e) {
    e === void 0 && (e = {});
    var t = this, n = t.config, i = t.state, r;
    e.withTransition = mn(e, "withTransition", !0), e.withTransform = mn(e, "withTransform", !1), e.withLegend = mn(e, "withLegend", !1), e.withUpdateXDomain = !0, e.withUpdateOrgXDomain = !0, e.withTransitionForExit = !1, e.withTransitionForTransform = mn(e, "withTransitionForTransform", e.withTransition), e.withLegend && n.legend_show || (i.hasAxis && (r = t.axis.generateTransitions(e.withTransitionForAxis ? n.transition_duration : 0)), t.updateScales(), t.updateSvgSize(), t.transformAll(e.withTransitionForTransform, r)), t.redraw(e, r);
  }
};
function Nn(e, t, n) {
  e === void 0 && (e = "linear");
  var i = {
    linear: Cr,
    log: Sl,
    _log: Cl,
    time: $x,
    utc: Ax
  }[e]();
  return i.type = e, /_?log/.test(e) && i.clamp(!0), i.range([t != null ? t : 0, n != null ? n : 1]);
}
var ty = {
  /**
   * Get x Axis scale function
   * @param {number} min Min range value
   * @param {number} max Max range value
   * @param {Array} domain Domain value
   * @param {Function} offset The offset getter to be sum
   * @returns {Function} scale
   * @private
   */
  getXScale: function(e, t, n, i) {
    var r = this, a = r.state.loading !== "append" && r.scale.zoom || Nn(r.axis.getAxisType("x"), e, t);
    return r.getCustomizedXScale(n ? a.domain(n) : a, i);
  },
  /**
   * Get y Axis scale function
   * @param {string} id Axis id: 'y' or 'y2'
   * @param {number} min Min value
   * @param {number} max Max value
   * @param {Array} domain Domain value
   * @returns {Function} Scale function
   * @private
   */
  getYScale: function(e, t, n, i) {
    var r = this, a = Nn(r.axis.getAxisType(e), t, n);
    return i && a.domain(i), a;
  },
  /**
   * Get y Axis scale
   * @param {string} id Axis id
   * @param {boolean} isSub Weather is sub Axis
   * @returns {Function} Scale function
   * @private
   */
  getYScaleById: function(e, t) {
    var n;
    t === void 0 && (t = !1);
    var i = ((n = this.axis) === null || n === void 0 ? void 0 : n.getId(e)) === "y2", r = t ? i ? "subY2" : "subY" : i ? "y2" : "y";
    return this.scale[r];
  },
  /**
   * Get customized x axis scale
   * @param {d3.scaleLinear|d3.scaleTime} scaleValue Scale function
   * @param {Function} offsetValue Offset getter to be sum
   * @returns {Function} Scale function
   * @private
   */
  getCustomizedXScale: function(e, t) {
    var n = this, i = t || function() {
      return n.axis.x.tickOffset();
    }, r = n.config.axis_x_inverted, a = function(s) {
      return e(s) + i();
    };
    for (var o in e)
      a[o] = e[o];
    return a.orgDomain = function() {
      return e.domain();
    }, a.orgScale = function() {
      return e;
    }, n.axis.isCategorized() && (a.domain = function(s) {
      var l = s;
      return arguments.length ? (e.domain(l), a) : (l = this.orgDomain(), r ? [l[0] + 1, l[1]] : [l[0], l[1] + 1]);
    }), a;
  },
  /**
   * Update scale
   * @param {boolean} isInit Param is given at the init rendering
   * @param {boolean} updateXDomain If update x domain
   * @private
   */
  updateScales: function(e, t) {
    var n, i;
    t === void 0 && (t = !0);
    var r = this, a = r.axis, o = r.config, s = r.format, l = r.org, c = r.scale, f = r.state, h = f.current, g = f.width, x = f.height, y = f.width2, T = f.height2, A = f.hasAxis, w = f.hasTreemap;
    if (A) {
      var C = o.axis_rotated, R = r.getResettedPadding(1), S = {
        x: C ? R : 0,
        y: C ? 0 : x,
        subX: C ? 1 : 0,
        subY: C ? 0 : T
      }, I = {
        x: C ? x : g,
        y: C ? g : R,
        subX: C ? x : g,
        subY: C ? y : 1
      }, L = t && ((n = c.x) === null || n === void 0 ? void 0 : n.orgDomain()), p = t && l.xDomain;
      c.x = r.getXScale(S.x, I.x, L, function() {
        return a.x.tickOffset();
      }), c.subX = r.getXScale(S.x, I.x, p, function(H) {
        var q;
        return H % 1 ? 0 : ((q = a.subX) !== null && q !== void 0 ? q : a.x).tickOffset();
      }), s.xAxisTick = a.getXAxisTickFormat(), s.subXAxisTick = a.getXAxisTickFormat(!0), a.setAxis("x", c.x, o.axis_x_tick_outer, e), o.subchart_show && a.setAxis("subX", c.subX, o.axis_x_tick_outer, e), c.y = r.getYScale("y", S.y, I.y, c.y ? c.y.domain() : o.axis_y_default), c.subY = r.getYScale("y", S.subY, I.subY, c.subY ? c.subY.domain() : o.axis_y_default), a.setAxis("y", c.y, o.axis_y_tick_outer, e), o.axis_y2_show && (c.y2 = r.getYScale("y2", S.y, I.y, c.y2 ? c.y2.domain() : o.axis_y2_default), c.subY2 = r.getYScale("y2", S.subY, I.subY, c.subY2 ? c.subY2.domain() : o.axis_y2_default), a.setAxis("y2", c.y2, o.axis_y2_tick_outer, e));
    } else if (w) {
      var U = r.getCurrentPadding();
      c.x = Cr().rangeRound([U.left, h.width - U.right]), c.y = Cr().rangeRound([U.top, h.height - U.bottom]);
    } else
      (i = r.updateArc) === null || i === void 0 || i.call(r);
  },
  /**
   * Get the zoom or unzoomed scaled value
   * @param {Date|number|object} d Data value
   * @returns {number|null}
   * @private
   */
  xx: function(e) {
    var t = this, n = t.config, i = t.scale, r = i.x, a = i.zoom, o = n.zoom_enabled && a ? a : r;
    return e ? o(Ae(e.x) ? e.x : e) : null;
  },
  xv: function(e) {
    var t = this, n = t.axis, i = t.config, r = t.scale, a = r.x, o = r.zoom, s = i.zoom_enabled && o ? o : a, l = t.getBaseValue(e);
    return n.isTimeSeries() ? l = kt.call(t, l) : n.isCategorized() && Ne(l) && (l = i.axis_x_categories.indexOf(l)), s(l);
  },
  yv: function(e) {
    var t = this, n = t.scale, i = n.y, r = n.y2, a = e.axis && e.axis === "y2" ? r : i;
    return a(t.getBaseValue(e));
  },
  subxx: function(e) {
    return e ? this.scale.subX(e.x) : null;
  }
}, ny = {
  /**
   * Update container size
   * @private
   */
  setContainerSize: function() {
    var e = this, t = e.state;
    t.current.width = e.getCurrentWidth(), t.current.height = e.getCurrentHeight();
  },
  getCurrentWidth: function() {
    var e = this;
    return e.config.size_width || e.getParentWidth();
  },
  getCurrentHeight: function() {
    var e = this, t = e.config, n = t.size_height || e.getParentHeight();
    return n > 0 ? n : 320 / (e.hasType("gauge") && !t.gauge_fullCircle ? 2 : 1);
  },
  /**
   * Get the parent rect element's size
   * @param {string} key property/attribute name
   * @returns {number}
   * @private
   */
  getParentRectValue: function(e) {
    for (var t = "offset".concat(hn(e)), n = this.$el.chart.node(), i = 0; i < 30 && n && n.tagName !== "BODY"; ) {
      try {
        i = n.getBoundingClientRect()[e];
      } catch (a) {
        t in n && (i = n[t]);
      }
      n = n.parentNode;
    }
    var r = je.body[t];
    return i > r && (i = r), i;
  },
  getParentWidth: function() {
    return this.getParentRectValue("width");
  },
  getParentHeight: function() {
    var e = this.$el.chart.style("height"), t = 0;
    return e && (t = /px$/.test(e) ? parseInt(e, 10) : this.getParentRectValue("height")), t;
  },
  getSvgLeft: function(e) {
    var t = this, n = t.config, i = t.state.hasAxis, r = t.$el, a = n.axis_rotated, o = a || !a && !n.axis_y_inner, s = a ? nt.axisX : nt.axisY, l = r.main.select(".".concat(s)).node(), c = i && n["axis_".concat(a ? "x" : "y", "_label")], f = 0;
    if (i && (Ne(c) || Ne(c.text) || /^inner-/.test(c == null ? void 0 : c.position))) {
      var h = r.main.select(".".concat(s, "-label"));
      h.empty() || (f = h.node().getBoundingClientRect().left);
    }
    var g = l && o ? l.getBoundingClientRect() : { right: 0 }, x = r.chart.node().getBoundingClientRect().left + f, y = t.hasArcType(), T = g.right - x - (y ? 0 : t.getCurrentPaddingByDirection("left", e));
    return T > 0 ? T : 0;
  },
  updateDimension: function(e) {
    var t, n = this, i = n.config, r = n.state.hasAxis, a = n.$el;
    r && !e && n.axis.x && i.axis_rotated && ((t = n.axis.subX) === null || t === void 0 || t.create(a.axis.subX)), n.updateScales(e), n.updateSvgSize(), n.transformAll(!1);
  },
  updateSvgSize: function() {
    var e = this, t = e.config, n = e.state, i = n.clip, r = n.current, a = n.hasAxis, o = n.width, s = n.height, l = e.$el.svg;
    if (t.resize_auto === "viewBox" ? l.attr("viewBox", "0 0 ".concat(r.width, " ").concat(r.height)) : l.attr("width", r.width).attr("height", r.height), a) {
      var c = l.select(".".concat(Zl.brush, " .overlay")), f = { width: 0, height: 0 };
      c.size() && (f.width = +c.attr("width"), f.height = +c.attr("height")), l.selectAll(["#".concat(i.id), "#".concat(i.idGrid)]).select("rect").attr("width", o).attr("height", s), l.select("#".concat(i.idXAxis)).select("rect").call(e.setXAxisClipPath.bind(e)), l.select("#".concat(i.idYAxis)).select("rect").call(e.setYAxisClipPath.bind(e)), i.idSubchart && l.select("#".concat(i.idSubchart)).select("rect").attr("width", o).attr("height", f.height);
    }
  },
  /**
   * Get padding by the direction.
   * @param {string} type "top" | "bottom" | "left" | "right"
   * @param {boolean} [withoutRecompute=false] If set true, do not recompute the padding value.
   * @param {boolean} [withXAxisTickTextOverflow=false] If set true, calculate x axis tick text overflow.
   * @returns {number} padding value
   * @private
   */
  getCurrentPaddingByDirection: function(e, t, n) {
    var i;
    t === void 0 && (t = !1), n === void 0 && (n = !1);
    var r = this, a = r.config, o = r.$el, s = r.state.hasAxis, l = a.axis_rotated, c = ((i = a.padding) === null || i === void 0 ? void 0 : i.mode) === "fit", f = ge(a["padding_".concat(e)]) ? a["padding_".concat(e)] : void 0, h = s ? {
      top: l ? "y2" : null,
      bottom: l ? "y" : "x",
      left: l ? "x" : "y",
      right: l ? null : "y2"
    }[e] : null, g = /^(left|right)$/.test(e), x = h && a["axis_".concat(h, "_inner")], y = h && a["axis_".concat(h, "_show")], T = h ? a["axis_".concat(h, "_axes")].length : 0, A = h ? g ? r.getAxisWidthByAxisId(h, t) : r.getHorizontalAxisHeight(h) : 0, w = 20, C = 0;
    !c && g && (A = v_(A));
    var R = s && g && (x || at(f) && !y) ? 0 : c ? (y ? A : 0) + (f != null ? f : 0) : at(f) ? A : f;
    return g && s ? (h && (c || x) && a["axis_".concat(h, "_label")].text && (R += r.axis.getAxisLabelPosition(h).isOuter ? w : 0), e === "right" ? (R += l ? !c && at(f) ? 10 : 2 : !y || x ? c ? 2 : 1 : 0, R += n ? r.axis.getXAxisTickTextY2Overflow(w) : 0) : e === "left" && l && at(f) && (R = a.axis_x_show ? c ? A : Math.max(A, 40) : 1)) : e === "top" ? (o.title && o.title.node() && (R += r.getTitlePadding()), C = l && !x ? T : 0) : e === "bottom" && s && l && !y && (R += 1), R + A * T - C;
  },
  getCurrentPadding: function(e) {
    e === void 0 && (e = !1);
    var t = this, n = ["top", "bottom", "left", "right"].map(function(s) {
      return t.getCurrentPaddingByDirection(s, null, e);
    }), i = n[0], r = n[1], a = n[2], o = n[3];
    return { top: i, bottom: r, left: a, right: o };
  },
  /**
   * Get resetted padding values when 'padding=false' option is set
   * https://github.com/naver/billboard.js/issues/2367
   * @param {number|object} v Padding values to be resetted
   * @returns {number|object} Padding value
   * @private
   */
  getResettedPadding: function(e) {
    var t = this, n = t.config, i = ge(e), r = i ? 0 : {};
    return n.padding === !1 ? !i && Object.keys(e).forEach(function(a) {
      r[a] = !en(n.data_labels) && n.data_labels !== !1 && a === "top" ? e[a] : 0;
    }) : r = e, r;
  },
  /**
   * Update size values
   * @param {boolean} isInit If is called at initialization
   * @private
   */
  updateSizes: function(e) {
    var t, n, i, r, a, o = this, s = o.config, l = o.state, c = o.$el.legend, f = s.axis_rotated, h = o.hasArcType() || l.hasFunnel || l.hasTreemap, g = ((t = s.padding) === null || t === void 0 ? void 0 : t.mode) === "fit";
    !e && o.setContainerSize();
    var x = {
      width: c ? o.getLegendWidth() : 0,
      height: c ? o.getLegendHeight() : 0
    };
    !h && s.axis_x_show && s.axis_x_tick_autorotate && o.updateXAxisTickClip();
    var y = {
      right: s.legend_show && l.isLegendRight ? o.getLegendWidth() + (g ? 0 : 20) : 0,
      bottom: !s.legend_show || l.isLegendRight || l.isLegendInset ? 0 : x.height
    }, T = f || h ? 0 : o.getHorizontalAxisHeight("x"), A = s.subchart_axis_x_show && s.subchart_axis_x_tick_text_show ? T : 30, w = s.subchart_show && !h ? s.subchart_size_height + A : 0, C = o.hasType("gauge") && s.arc_needle_show && !s.gauge_fullCircle && !s.gauge_label_show ? 10 : 0, R = o.getCurrentPadding(!0);
    if (l.margin = !h && f ? {
      top: R.top,
      right: h ? 0 : R.right + y.right,
      bottom: y.bottom + R.bottom,
      left: w + (h ? 0 : R.left)
    } : {
      top: (g ? 0 : 4) + R.top,
      // for top tick text
      right: h ? 0 : R.right + y.right,
      bottom: C + w + y.bottom + R.bottom,
      left: h ? 0 : R.left
    }, l.margin = o.getResettedPadding(l.margin), l.margin2 = f ? {
      top: l.margin.top,
      right: NaN,
      bottom: 20 + y.bottom,
      left: o.state.rotatedPadding.left
    } : {
      top: l.current.height - w - y.bottom,
      right: NaN,
      bottom: A + y.bottom,
      left: l.margin.left
    }, l.margin3 = {
      top: 0,
      right: NaN,
      bottom: 0,
      left: 0
    }, (n = o.updateSizeForLegend) === null || n === void 0 || n.call(o, x), l.width = l.current.width - l.margin.left - l.margin.right, l.height = l.current.height - l.margin.top - l.margin.bottom, l.width < 0 && (l.width = 0), l.height < 0 && (l.height = 0), l.width2 = f ? l.margin.left - l.rotatedPadding.left - l.rotatedPadding.right : l.width, l.height2 = f ? l.height : l.current.height - l.margin2.top - l.margin2.bottom, l.width2 < 0 && (l.width2 = 0), l.height2 < 0 && (l.height2 = 0), o.hasArcType()) {
      var S = o.hasType("gauge"), I = s.legend_show && l.isLegendRight, L = (i = l.hasRadar && o.cache.get(Xt.radarTextWidth)) !== null && i !== void 0 ? i : 0;
      l.arcWidth = l.width - (I ? x.width + 10 : 0) - L, l.arcHeight = l.height - (I && !S ? 0 : 10), !((r = s.arc_rangeText_values) === null || r === void 0) && r.length && (S ? (l.arcWidth -= 25, l.arcHeight -= 10, l.margin.left += 10) : (l.arcHeight -= 20, l.margin.top += 10)), S && !s.gauge_fullCircle && (l.arcHeight += l.height - o.getPaddingBottomForGauge()), (a = o.updateRadius) === null || a === void 0 || a.call(o);
    }
    l.isLegendRight && h && (l.margin3.left = l.arcWidth / 2 + l.radiusExpanded * 1.1);
  }
}, iy = {
  /**
   * Add props color css rule to given selector
   * @param {boolean} withShape Set shpes' prefix class
   * @param {string} selector CSS selector
   * @param {Array} props CSS props list
   * @param {Function} propsFn Function to retrieve value or determine for props
   * @returns {Function}
   * @private
   */
  setCssRule: function(e, t, n, i) {
    var r = this, a = r.config, o = r.state, s = o.cssRule, l = o.style;
    return a.boost_useCssRule ? function(c) {
      c.each(function(f) {
        var h = i && (i == null ? void 0 : i.call(r, f)), g = "".concat(e ? ".".concat(ct.shapes + r.getTargetSelectorSuffix(f.id)) : "").concat(t);
        t in s && l.sheet.deleteRule(s[g]), r.state.cssRule[g] = T_(l, g, n.filter(Boolean).map(function(x) {
          return Ne(h) && x.indexOf(":") === -1 ? "".concat(x, ": ").concat(h) : x || "";
        }));
      });
    } : function() {
    };
  },
  /**
   * Get style prop value
   * @param {Function|string} v Value
   * @returns {string|null}
   * @private
   */
  getStylePropValue: function(e) {
    var t = this.config.boost_useCssRule;
    return t ? null : he(e) ? e.bind(this) : e;
  }
};
function ry(e) {
  var t = "middle";
  return e > 0 && e <= 170 ? t = "end" : e > 190 && e <= 360 && (t = "start"), t;
}
function ay(e, t, n, i, r) {
  var a, o = this, s = e.value, l = o.isCandlestickType(e), c = ge(s) && s < 0 || l && !(!((a = o.getCandlestickData(e)) === null || a === void 0) && a._isUp), f = t.x, h = t.y, g = 4, x = g * 2;
  return i ? n === "start" ? (f += c ? 0 : x, h += g) : n === "middle" ? (f += x, h -= x) : n === "end" && (c && (f -= x), h += g) : (n === "start" ? (f += g, c && (h += x * 2)) : n === "middle" ? h -= x : n === "end" && (f -= g, c && (h += x * 2)), r && (h += c ? -17 : l ? 13 : 7)), { x: f, y: h };
}
function Ru(e, t) {
  var n, i = this.config.data_labels_position, r = e.id, a = e.index, o = e.value;
  return (n = he(i) ? i.bind(this.api)(t, o, r, a, this.$el.text) : (r in i ? i[r] : i)[t]) !== null && n !== void 0 ? n : 0;
}
var oy = {
  opacityForText: function(e) {
    var t = this;
    return t.isBarType(e) && !t.meetsLabelThreshold(Math.abs(t.getRatio("bar", e)), "bar") ? "0" : t.hasDataLabel ? null : "0";
  },
  /**
   * Initializes the text
   * @private
   */
  initText: function() {
    var e = this.$el;
    e.main.select(".".concat(Ee.chart)).append("g").attr("class", Ct.chartTexts).style("pointer-events", e.funnel || e.treemap ? "none" : null);
  },
  /**
   * Update chartText
   * @param {object} targets $$.data.targets
   * @private
   */
  updateTargetsForText: function(e) {
    var t = this, n = t.getChartClass("Text"), i = t.getClass("texts", "id"), r = t.classFocus.bind(t), a = t.$el.main.select(".".concat(Ct.chartTexts)).selectAll(".".concat(Ct.chartText)).data(e).attr("class", function(s) {
      return "".concat(n(s)).concat(r(s)).trim();
    }), o = a.enter().append("g").style("opacity", "0").attr("class", n).call(t.setCssRule(!0, " .".concat(Ct.text), ["fill", "pointer-events:none"], t.updateTextColor));
    o.append("g").attr("class", i);
  },
  /**
   * Update text
   * @private
   */
  updateText: function() {
    var e = this, t = e.$el, n = e.$T, i = e.config, r = e.axis, a = e.getClass("text", "index"), o = i.data_labels.centered, s = t.main.selectAll(".".concat(Ct.texts)).selectAll(".".concat(Ct.text)).data(e.labelishData.bind(e));
    n(s.exit()).style("fill-opacity", "0").remove(), t.text = s.enter().append("text").merge(s).attr("class", a).attr("text-anchor", function(l) {
      var c = i["axis_".concat(r == null ? void 0 : r.getId(l.id), "_inverted")], f = c ? l.value > 0 : l.value < 0;
      if (e.isCandlestickType(l)) {
        var h = e.getCandlestickData(l);
        f = !(h != null && h._isUp);
      } else if (e.isTreemapType(l))
        return o ? "middle" : "start";
      return i.axis_rotated ? f ? "end" : "start" : "middle";
    }).style("fill", e.getStylePropValue(e.updateTextColor)).style("fill-opacity", "0").each(function(l, c, f) {
      var h = le(this), g = l.value;
      if (e.isBubbleZType(l))
        g = e.getBubbleZData(g, "z");
      else if (e.isCandlestickType(l)) {
        var x = e.getCandlestickData(l);
        x && (g = x.close);
      }
      g = e.isTreemapType(l) ? e.treemapDataLabelFormat(l)(h) : e.dataLabelFormat(l.id)(g, l.id, l.index, f), ge(g) ? this.textContent = g : Nr(h, g);
    });
  },
  updateTextColor: function(e) {
    var t = this, n = t.config, i = n.data_labels_colors, r = t.isArcType(e) && !t.isRadarType(e) || t.isFunnelType(e) || t.isTreemapType(e) ? null : t.color(e), a;
    if (Ne(i))
      a = i;
    else if (Be(i)) {
      var o = (e.data || e).id;
      a = i[o];
    } else
      he(i) && (a = i.bind(t.api)(r, e));
    if (t.isCandlestickType(e) && !he(i)) {
      var s = t.getCandlestickData(e);
      if (!(s != null && s._isUp)) {
        var l = n.candlestick_color_down;
        a = Be(l) ? l[e.id] : l;
      }
    }
    return a || r;
  },
  /**
   * Update data label text background color
   * @param {object} d Data object
   * @param {object|string} option option object
   * @returns {string|null}
   * @private
   */
  updateTextBGColor: function(e, t) {
    var n = this, i = n.$el, r = "";
    if (Ne(t) || Be(t)) {
      var a = Ne(t) ? "" : n.getTargetSelectorSuffix("id" in e ? e.id : e.data.id), o = i.defs.select(["filter[id*='labels-bg", "']"].join(a));
      o.size() && (r = "url(#".concat(o.attr("id"), ")"));
    }
    return r || null;
  },
  /**
   * Redraw chartText
   * @param {Function} getX Positioning function for x
   * @param {Function} getY Positioning function for y
   * @param {boolean} forFlow Weather is flow
   * @param {boolean} withTransition transition is enabled
   * @returns {Array}
   * @private
   */
  redrawText: function(e, t, n, i) {
    var r = this, a = r.$T, o = r.axis, s = r.config, l = r.state.hasTreemap, c = Fn(!0), f = s.axis_rotated, h = s.data_labels.rotate, g = ry(h), x = h ? "rotate(".concat(h, ")") : "";
    return r.$el.text.style("fill", r.getStylePropValue(r.updateTextColor)).attr("filter", function(y) {
      return r.updateTextBGColor.bind(r)(y, s.data_labels_backgroundColors);
    }).style("fill-opacity", n ? 0 : r.opacityForText.bind(r)).each(function(y, T) {
      var A = a(l && this.childElementCount ? this.parentNode : this, !!(i && this.getAttribute("x")), c), w = s["axis_".concat(o == null ? void 0 : o.getId(y.id), "_inverted")], C = {
        x: e.bind(this)(y, T),
        y: t.bind(this)(y, T)
      };
      h && (C = ay.bind(r)(y, C, g, f, w), A.attr("text-anchor", g)), this.childElementCount || h ? A.attr("transform", "translate(".concat(C.x, " ").concat(C.y, ") ").concat(x)) : A.attr("x", C.x).attr("y", C.y);
    }), !0;
  },
  /**
   * Gets the getBoundingClientRect value of the element
   * @param {HTMLElement|d3.selection} element Target element
   * @param {string} className Class name
   * @returns {object} value of element.getBoundingClientRect()
   * @private
   */
  getTextRect: function(e, t) {
    var n = this, i = e.node ? e.node() : e;
    /text/i.test(i.tagName) || (i = i.querySelector("text"));
    var r = i.textContent, a = "".concat(Xt.textRect, "-").concat(r.replace(/\W/g, "_")), o = n.cache.get(a);
    return o || (n.$el.svg.append("text").style("visibility", "hidden").style("font", le(i).style("font")).classed(t, !0).text(r).call(function(s) {
      o = vr(s.node());
    }).remove(), n.cache.add(a, o)), o;
  },
  /**
   * Gets the x or y coordinate of the text
   * @param {object} indices Indices values
   * @param {boolean} forX whether or not to x
   * @returns {number} coordinates
   * @private
   */
  generateXYForText: function(e, t) {
    var n = this, i = n.state, r = i.hasRadar, a = i.hasFunnel, o = i.hasTreemap, s = Object.keys(e), l = {}, c = t ? n.getXForText : n.getYForText;
    return a && s.push("funnel"), r && s.push("radar"), o && s.push("treemap"), s.forEach(function(f) {
      l[f] = n["generateGet".concat(hn(f), "Points")](e[f], !1);
    }), function(f, h) {
      var g = n.isAreaType(f) && "area" || n.isBarType(f) && "bar" || n.isCandlestickType(f) && "candlestick" || n.isFunnelType(f) && "funnel" || n.isRadarType(f) && "radar" || n.isTreemapType(f) && "treemap" || "line";
      return c.call(n, l[g](f, h), f, this);
    };
  },
  /**
   * Get centerized text position for bar type data.label.text
   * @param {object} d Data object
   * @param {Array} points Data points position
   * @param {HTMLElement} textElement Data label text element
   * @param {string} type 'x' or 'y'
   * @returns {number} Position value
   * @private
   */
  getCenteredTextPos: function(e, t, n, i) {
    var r = this, a = r.config, o = a.axis_rotated, s = r.isBarType(e), l = r.isTreemapType(e);
    if (a.data_labels.centered && (s || l)) {
      var c = vr(n);
      if (s) {
        var f = r.getRangedData(e, null, "bar") >= 0;
        if (o) {
          var h = (f ? t[1][1] - t[0][1] : t[0][1] - t[1][1]) / 2 + c.width / 2;
          return f ? -h - 3 : h + 2;
        } else {
          var g = (f ? t[0][1] - t[1][1] : t[1][1] - t[0][1]) / 2 + c.height / 2;
          return f ? g : -g - 2;
        }
      } else if (l)
        return i === "x" ? (t[1][0] - t[0][0]) / 2 : (t[1][1] - t[0][1]) / 2 + c.height / 2;
    }
    return 0;
  },
  /**
   * Gets the x coordinate of the text
   * @param {object} points Data points position
   * @param {object} d Data object
   * @param {HTMLElement} textElement Data label text element
   * @returns {number} x coordinate
   * @private
   */
  getXForText: function(e, t, n) {
    var i, r = this, a = r.config, o = a.axis_rotated, s = r.isFunnelType(t), l = r.isTreemapType(t), c = e ? e[0][0] : 0;
    if (r.isCandlestickType(t))
      o ? c = !((i = r.getCandlestickData(t)) === null || i === void 0) && i._isUp ? e[2][2] + 4 : e[2][1] - 4 : c += (e[1][0] - c) / 2;
    else if (s)
      c += r.state.current.width / 2;
    else if (l)
      c += a.data_labels.centered ? 0 : 5;
    else if (o) {
      var f = a["axis_".concat(r.axis.getId(t.id), "_inverted")], h = r.isBarType(t) ? 4 : 6, g = t.value;
      c = e[2][1], f ? c -= h * (g > 0 ? 1 : -1) : c += h * (g < 0 ? -1 : 1);
    } else
      c = r.hasType("bar") ? (e[2][0] + e[0][0]) / 2 : c;
    return (o || l) && (c += r.getCenteredTextPos(t, e, n, "x")), c + Ru.call(this, t, "x");
  },
  /**
   * Gets the y coordinate of the text
   * @param {object} points Data points position
   * @param {object} d Data object
   * @param {HTMLElement} textElement Data label text element
   * @returns {number} y coordinate
   * @private
   */
  getYForText: function(e, t, n) {
    var i = this, r = i.axis, a = i.config, o = i.state, s = a.axis_rotated, l = a["axis_".concat(r == null ? void 0 : r.getId(t.id), "_inverted")], c = i.isBarType(t), f = i.isFunnelType(t), h = i.isTreemapType(t), g = a.point_r, x = vr(n), y = t.value, T = 3, A;
    if (i.isCandlestickType(t))
      y = i.getCandlestickData(t), s ? (A = e[0][0], A += (e[1][0] - A) / 2 + T) : (A = y && y._isUp ? e[2][2] - T : e[2][1] + T * 4, l && (A += 15 * (y._isUp ? 1 : -1)));
    else if (f)
      A = e ? e[0][1] + (e[1][1] - e[0][1]) / 2 + x.height / 2 - 3 : 0;
    else if (h)
      A = e[0][1] + (a.data_labels.centered ? 0 : x.height + 5);
    else if (s)
      A = (e[0][0] + e[2][0] + x.height * 0.6) / 2;
    else if (A = e[2][1], ge(g) && g > 5 && (i.isLineType(t) || i.isScatterType(t)) && (T += a.point_r / 2.3), y < 0 || y === 0 && !o.hasPositiveValue && o.hasNegativeValue)
      A += l ? c ? -3 : -5 : x.height + (c ? -T : T);
    else {
      var w = -T * 2;
      c ? w = -T : i.isBubbleType(t) && (w = T), l && (w = c ? 10 : 15), A += w;
    }
    return (!s || h) && (A += i.getCenteredTextPos(t, e, n, "y")), A + Ru.call(this, t, "y");
  },
  /**
   * Calculate if two or more text nodes are overlapping
   * Mark overlapping text nodes with "text-overlapping" class
   * @param {string} id Axis id
   * @param {ChartInternal} $$ ChartInternal context
   * @param {string} selector Selector string
   * @private
   */
  markOverlapped: function(e, t, n) {
    var i = t.$el.arcs.selectAll(n), r = i.filter(function(l) {
      return l.data.id !== e;
    }), a = i.filter(function(l) {
      return l.data.id === e;
    }), o = Cu(a.node()), s = function(l, c) {
      return Math.sqrt(Math.pow(l, 2) + Math.pow(c, 2));
    };
    a.node() && r.each(function() {
      var l = Cu(this), c = le(this), f = s(o.e, o.f) > s(l.e, l.f) ? a : c, h = Math.ceil(Math.abs(o.e - l.e)) < Math.ceil(f.node().getComputedTextLength()), g = Math.ceil(Math.abs(o.f - l.f)) < parseInt(a.style("font-size"), 10);
      c.classed(Ct.TextOverlapping, h && g);
    });
  },
  /**
   * Calculate if two or more text nodes are overlapping
   * Remove "text-overlapping" class on selected text nodes
   * @param {ChartInternal} $$ ChartInternal context
   * @param {string} selector Selector string
   * @private
   */
  undoMarkOverlapped: function(e, t) {
    e.$el.arcs.selectAll(t).each(function() {
      el([this, this.previousSibling]).classed(Ct.TextOverlapping, !1);
    });
  },
  /**
   * Check if meets the ratio to show data label text
   * @param {number} ratio ratio to meet
   * @param {string} type chart type
   * @returns {boolean}
   * @private
   */
  meetsLabelThreshold: function(e, t) {
    e === void 0 && (e = 0);
    var n = this, i = n.config, r = i["".concat(t, "_label_threshold")] || 0;
    return e >= r;
  }
};
function Mu(e, t) {
  e === void 0 && (e = "left");
  var n = ge(t), i;
  return e.indexOf("center") > -1 ? i = n ? t / 2 : "middle" : e.indexOf("right") > -1 ? i = n ? t : "end" : i = n ? 0 : "start", i;
}
var sy = {
  /**
   * Initializes the title
   * @private
   */
  initTitle: function() {
    var e = this, t = e.config, n = e.$el;
    if (t.title_text) {
      n.title = n.svg.append("g");
      var i = n.title.append("text").style("text-anchor", Mu(t.title_position)).attr("class", Ct.title);
      Nr(i, t.title_text, [0.3, 1.5]);
    }
  },
  /**
   * Redraw title
   * @private
   */
  redrawTitle: function() {
    var e = this, t = e.config, n = e.state.current, i = e.$el.title;
    if (i) {
      var r = Mu(t.title_position, n.width), a = (t.title_padding.top || 0) + e.getTextRect(e.$el.title, Ct.title).height;
      i.attr("transform", "translate(".concat(r, ", ").concat(a, ")"));
    }
  },
  /**
   * Get title padding
   * @returns {number} padding value
   * @private
   */
  getTitlePadding: function() {
    var e = this, t = e.$el.title, n = e.config;
    return (n.title_padding.top || 0) + (t ? e.getTextRect(t, Ct.title).height : 0) + (n.title_padding.bottom || 0);
  }
}, uy = {
  /**
   * Initializes the tooltip
   * @private
   */
  initTooltip: function() {
    var e = this, t = e.config, n = e.$el;
    n.tooltip = le(t.tooltip_contents.bindto), n.tooltip.empty() && (n.tooltip = n.chart.append("div").attr("class", pr.tooltipContainer).style("position", "absolute").style("pointer-events", "none").style("display", "none")), e.bindTooltipResizePos();
  },
  /**
   * Show tooltip at initialization.
   * Is called only when tooltip.init.show=true option is set
   * @private
   */
  initShowTooltip: function() {
    var e, t, n = this, i = n.config, r = n.$el, a = n.state, o = a.hasAxis, s = a.hasRadar;
    if (i.tooltip_init_show) {
      var l = !(o || s);
      !((t = n.axis) === null || t === void 0) && t.isTimeSeries() && Ne(i.tooltip_init_x) && (i.tooltip_init_x = kt.call(n, i.tooltip_init_x)), n.api.tooltip.show({
        data: (e = {}, e[l ? "index" : "x"] = i.tooltip_init_x, e)
      });
      var c = i.tooltip_init_position;
      if (!i.tooltip_contents.bindto && !en(c)) {
        var f = c.top, h = f === void 0 ? 0 : f, g = c.left, x = g === void 0 ? 50 : g;
        r.tooltip.style("top", Ne(h) ? h : "".concat(h, "px")).style("left", Ne(x) ? x : "".concat(x, "px")).style("display", null);
      }
    }
  },
  /**
   * Get the tooltip HTML string
   * @param  {Array} args Arguments
   * @returns {string} Formatted HTML string
   * @private
   */
  getTooltipHTML: function() {
    for (var e = [], t = 0; t < arguments.length; t++)
      e[t] = arguments[t];
    var n = this, i = n.api, r = n.config;
    return he(r.tooltip_contents) ? r.tooltip_contents.bind(i).apply(void 0, e) : n.getTooltipContent.apply(n, e);
  },
  /**
   * Returns the tooltip content(HTML string)
   * @param {object} d data
   * @param {Function} defaultTitleFormat Default title format
   * @param {Function} defaultValueFormat Default format for each data value in the tooltip.
   * @param {Function} color Color function
   * @returns {string} html
   * @private
   */
  getTooltipContent: function(e, t, n, i) {
    var r, a = this, o = a.api, s = a.config, l = a.state, c = a.$el, f = ["title", "name", "value"].map(function(te) {
      var re = s["tooltip_format_".concat(te)];
      return he(re) ? re.bind(o) : re;
    }), h = f[0], g = f[1], x = f[2], y = function() {
      for (var te = [], re = 0; re < arguments.length; re++)
        te[re] = arguments[re];
      return La((h || t).apply(void 0, te));
    }, T = function() {
      for (var te = [], re = 0; re < arguments.length; re++)
        te[re] = arguments[re];
      return La((g || function(ue) {
        return ue;
      }).apply(void 0, te));
    }, A = function() {
      for (var te = [], re = 0; re < arguments.length; re++)
        te[re] = arguments[re];
      var ue = x || (l.hasTreemap || a.isStackNormalized() ? function(we, st) {
        return "".concat((st * 100).toFixed(2), "%");
      } : n);
      return La(ue.apply(void 0, te));
    }, w = s.tooltip_order, C = function(te) {
      return a.axis && a.isBubbleZType(te) ? a.getBubbleZData(te.value, "z") : a.getBaseValue(te);
    }, R = a.levelColor ? function(te) {
      return a.levelColor(te.value);
    } : function(te) {
      return i(te);
    }, S = s.tooltip_contents, I = S.template, L = a.mapToTargetIds();
    if (w === null && s.data_groups.length) {
      var p = a.orderTargets(a.data.targets).map(function(te) {
        return te.id;
      }).reverse();
      e.sort(function(te, re) {
        var ue = te ? te.value : null, we = re ? re.value : null;
        return ue > 0 && we > 0 && (ue = te.id ? p.indexOf(te.id) : null, we = re.id ? p.indexOf(re.id) : null), ue - we;
      });
    } else if (/^(asc|desc)$/.test(w)) {
      var U = w === "asc";
      e.sort(function(te, re) {
        var ue = te ? C(te) : null, we = re ? C(re) : null;
        return U ? ue - we : we - ue;
      });
    } else
      he(w) && e.sort(w.bind(o));
    var H = a.getTooltipContentTemplate(I), q = e.length, X, j, z, G, _e, Ue = function() {
      if (j = e[_e], !j || !(C(j) || C(j) === 0))
        return "continue";
      if (at(X)) {
        var te = (l.hasAxis || l.hasRadar) && y(j.x);
        X = Or(H[0], {
          CLASS_TOOLTIP: pr.tooltip,
          TITLE: Ae(te) ? I ? te : '<tr><th colspan="2">'.concat(te, "</th></tr>") : ""
        });
      }
      if (!j.ratio && c.arcs && (z = ["arc", a.$el.arcs.select("path.".concat(Xe.arc, "-").concat(j.id)).data()[0]], j.ratio = a.getRatio.apply(a, z)), z = [j.ratio, j.id, j.index], a.isAreaRangeType(j)) {
        var re = ["high", "low"].map(function(Le) {
          return A.apply(void 0, Ke([a.getRangedData(j, Le)], z, !1));
        }), ue = re[0], we = re[1], st = A.apply(void 0, Ke([C(j)], z, !1));
        G = "<b>Mid:</b> ".concat(st, " <b>High:</b> ").concat(ue, " <b>Low:</b> ").concat(we);
      } else if (a.isCandlestickType(j)) {
        var Je = ["open", "high", "low", "close", "volume"].map(function(Ge) {
          var Ve = a.getRangedData(j, Ge, "candlestick");
          return Ve ? A.apply(void 0, Ke([a.getRangedData(j, Ge, "candlestick")], z, !1)) : void 0;
        }), ft = Je[0], ue = Je[1], we = Je[2], vt = Je[3], et = Je[4];
        G = "<b>Open:</b> ".concat(ft, " <b>High:</b> ").concat(ue, " <b>Low:</b> ").concat(we, " <b>Close:</b> ").concat(vt).concat(et ? " <b>Volume:</b> ".concat(et) : "");
      } else if (a.isBarRangeType(j)) {
        var oe = j.value, He = j.id, Oe = j.index;
        G = "".concat(A(oe, void 0, He, Oe));
      } else
        G = A.apply(void 0, Ke([C(j)], z, !1));
      if (G !== void 0) {
        if (j.name === null)
          return "continue";
        var K = T.apply(void 0, Ke([(r = j.name) !== null && r !== void 0 ? r : j.id], z, !1)), ie = R(j), fe = {
          CLASS_TOOLTIP_NAME: pr.tooltipName + a.getTargetSelectorSuffix(j.id),
          COLOR: I || !a.patterns ? ie : '<svg><rect style="fill:'.concat(ie, '" width="10" height="10"></rect></svg>'),
          NAME: K,
          VALUE: G
        };
        if (I && Be(S.text)) {
          var W = L.indexOf(j.id);
          Object.keys(S.text).forEach(function(Le) {
            fe[Le] = S.text[Le][W];
          });
        }
        X += Or(H[1], fe);
      }
    };
    for (_e = 0; _e < q; _e++)
      Ue();
    return "".concat(X, "</table>");
  },
  /**
   * Get the content template string
   * @param {string} tplStr Tempalte string
   * @returns {Array} Template string
   * @private
   */
  getTooltipContentTemplate: function(e) {
    return (e || `<table class="{=CLASS_TOOLTIP}"><tbody>
				{=TITLE}
				{{<tr class="{=CLASS_TOOLTIP_NAME}">
					<td class="name">`.concat(this.patterns ? "{=COLOR}" : '<span style="background-color:{=COLOR}"></span>', `{=NAME}</td>
					<td class="value">{=VALUE}</td>
				</tr>}}
			</tbody></table>`)).replace(/(\r?\n|\t)/g, "").split(/{{(.*)}}/);
  },
  /**
   * Update tooltip position coordinate
   * @param {object} dataToShow Data object
   * @param {SVGElement} eventTarget Event element
   * @private
   */
  setTooltipPosition: function(e, t) {
    var n, i, r = this, a = r.config, o = r.scale, s = r.state, l = r.$el, c = l.eventRect, f = l.tooltip, h = l.svg, g = a.tooltip_contents.bindto, x = a.axis_rotated, y = f == null ? void 0 : f.datum();
    if (!g && y) {
      var T = e != null ? e : JSON.parse(y.current), A = Qt(s.event, t != null ? t : c == null ? void 0 : c.node()), w = A[0], C = A[1], R = { x: w, y: C };
      if (s.hasAxis && o.x && y && "x" in y) {
        var S = function(q, X, j) {
          var z;
          q === void 0 && (q = 0), j === void 0 && (j = "y");
          var G = o[X ? (z = r.axis) === null || z === void 0 ? void 0 : z.getId(X) : j];
          return G ? G(q) + (x ? s.margin.left : s.margin.top) : 0;
        };
        R.xAxis = o.x(y.x) + // add margin only when user specified tooltip.position function
        (a.tooltip_position ? x ? s.margin.top : s.margin.left : 0), T.length === 1 ? R.yAxis = S(T[0].value, T[0].id) : R.yAxis = S;
      }
      var I = y.width, L = I === void 0 ? 0 : I, p = y.height, U = p === void 0 ? 0 : p, H = (i = (n = a.tooltip_position) === null || n === void 0 ? void 0 : n.bind(r.api)(T, L, U, c == null ? void 0 : c.node(), R)) !== null && i !== void 0 ? i : zo(h) ? r.getTooltipPositionViewBox.bind(r)(L, U, R) : r.getTooltipPosition.bind(r)(L, U, R);
      ["top", "left"].forEach(function(q) {
        var X = H[q];
        f.style(q, "".concat(X, "px")), q === "left" && !y.xPosInPercent && (y.xPosInPercent = X / s.current.width * 100);
      });
    }
  },
  /**
   * Get tooltip position when svg has vieBox attribute
   * @param {number} tWidth Tooltip width value
   * @param {number} tHeight Tooltip height value
   * @param {object} currPos Current event position value from SVG coordinate
   * @returns {object} top, left value
   */
  getTooltipPositionViewBox: function(e, t, n) {
    var i, r, a = this, o = a.$el, s = o.eventRect, l = o.svg, c = a.config, f = a.state, h = c.axis_rotated, g = a.hasArcType() || f.hasFunnel || f.hasTreemap, x = (r = (i = g ? l : s) === null || i === void 0 ? void 0 : i.node()) !== null && r !== void 0 ? r : f.event.target, y = n.x, T = n.y;
    f.hasAxis && (y = h ? y : n.xAxis, T = h ? n.xAxis : T);
    var A = Pr(x, y, T, !1), w = x.getBoundingClientRect(), C = Pr(x, 20, 0, !1).x, R = A.y, S = A.x + e / 2 + C;
    return g && (f.hasFunnel || f.hasTreemap || f.hasRadar ? (S -= e / 2 + C, R += t) : (R += w.height / 2, S += w.width / 2 - (e - C))), S + e > w.width && (S = w.width - e - C), R + t > w.height && (R -= t * 2), {
      top: R,
      left: S
    };
  },
  /**
   * Returns the position of the tooltip
   * @param {string} tWidth Width value of tooltip element
   * @param {string} tHeight Height value of tooltip element
   * @param {object} currPos Current mouse position
   * @returns {object} top, left value
   * @private
   */
  getTooltipPosition: function(e, t, n) {
    var i, r, a, o = this, s = o.config, l = o.scale, c = o.state, f = c.width, h = c.height, g = c.current, x = c.hasFunnel, y = c.hasRadar, T = c.hasTreemap, A = c.isLegendRight, w = c.inputType, C = o.hasType("gauge") && !s.gauge_fullCircle, R = s.axis_rotated, S = o.hasArcType(), I = o.getSvgLeft(!0), L = I + g.width - o.getCurrentPaddingByDirection("right"), p = 20, U = n.x, H = n.y;
    if (y)
      U += U >= f / 2 ? 15 : -(e + 15), H += 15;
    else if (S) {
      var q = w !== "touch";
      if (q) {
        var X = (r = (i = o.getTitlePadding) === null || i === void 0 ? void 0 : i.call(o)) !== null && r !== void 0 ? r : 0;
        X && C && (!((a = s.arc_rangeText_values) === null || a === void 0) && a.length) && (X += 10), U += (f - (A ? o.getLegendWidth() : 0)) / 2, H += (C ? h : h / 2 + t) + X;
      }
    } else if (x || T)
      H += t;
    else {
      var j = {
        top: o.getCurrentPaddingByDirection("top", !0),
        left: o.getCurrentPaddingByDirection("left", !0)
      };
      R ? (U += I + j.left + p, H = j.top + n.xAxis + p, L -= I) : (U = I + j.left + p + (l.zoom ? U : n.xAxis), H += j.top - 5);
    }
    if (U + e + 15 > L && (U -= e + (x || T || S ? 0 : R ? p * 2 : 38)), H + t > g.height) {
      var z = T ? t + 10 : 30;
      H -= C ? t * 1.5 : t + z;
    }
    var G = { top: H, left: U };
    return Object.keys(G).forEach(function(_e) {
      G[_e] < 0 && (G[_e] = 0);
    }), G;
  },
  /**
   * Show the tooltip
   * @param {object} selectedData Data object
   * @param {SVGElement} eventTarget Event element
   * @private
   */
  showTooltip: function(e, t) {
    var n = this, i = n.config, r = n.$el.tooltip, a = e.filter(function(h) {
      return h && Ae(n.getBaseValue(h));
    });
    if (!(!r || a.length === 0 || !i.tooltip_show)) {
      var o = r.datum(), s = JSON.stringify(e);
      if (!o || o.current !== s) {
        var l = e.concat().sort()[0], c = l.index, f = l.x;
        Ze(i.tooltip_onshow, n.api, e), r.html(n.getTooltipHTML(
          e,
          // data
          n.axis ? n.axis.getXAxisTickFormat() : n.categoryName.bind(n),
          // defaultTitleFormat
          n.getDefaultValueFormat(),
          // defaultValueFormat
          n.color
          // color
        )).style("display", null).style("visibility", null).datum(o = {
          index: c,
          x: f,
          current: s,
          width: r.property("offsetWidth"),
          height: r.property("offsetHeight")
        }), Ze(i.tooltip_onshown, n.api, e), n._handleLinkedCharts(!0, c);
      }
      n.setTooltipPosition(a, t);
    }
  },
  /**
   * Adjust tooltip position on resize event
   * @private
   */
  bindTooltipResizePos: function() {
    var e = this, t = e.resizeFunction, n = e.state, i = e.$el.tooltip;
    t.add(function() {
      if (i.style("display") === "block") {
        var r = n.current, a = i.datum(), o = a.width, s = a.xPosInPercent, l = r.width / 100 * s, c = r.width - (l + o);
        c < 0 && (l += c), i.style("left", "".concat(l, "px"));
      }
    });
  },
  /**
   * Hide the tooltip
   * @param {boolean} force Force to hide
   * @private
   */
  hideTooltip: function(e) {
    var t, n = this, i = n.api, r = n.config, a = n.$el.tooltip;
    if (a && a.style("display") !== "none" && (!r.tooltip_doNotHide || e)) {
      var o = JSON.parse((t = a.datum().current) !== null && t !== void 0 ? t : {});
      Ze(r.tooltip_onhide, i, o), a.style("display", "none").style("visibility", "hidden").datum(null), Ze(r.tooltip_onhidden, i, o);
    }
  },
  /**
   * Toggle display for linked chart instances
   * @param {boolean} show true: show, false: hide
   * @param {number} index x Axis index
   * @private
   */
  _handleLinkedCharts: function(e, t) {
    var n = this, i = n.charts, r = n.config, a = n.state.event;
    if (a != null && a.isTrusted && r.tooltip_linked && i.length > 1) {
      var o = r.tooltip_linked_name;
      i.filter(function(s) {
        return s !== n.api;
      }).forEach(function(s) {
        var l = s.internal, c = l.config, f = l.$el, h = c.tooltip_linked, g = c.tooltip_linked_name, x = je.body.contains(f.chart.node());
        if (h && o === g && x) {
          var y = f.tooltip.data()[0], T = t !== (y == null ? void 0 : y.index);
          try {
            s.tooltip[e && T ? "show" : "hide"]({ index: t });
          } catch (A) {
          }
        }
      });
    }
  },
  /**
   * Update tooltip content on redraw
   * - In a situation where tooltip is displayed and data load happens, it should reflect loaded data to tooltip
   * @param {d3Selection} context Event rect element
   * @param {number} index Data index
   * @private
   */
  updateTooltipOnRedraw: function(e, t) {
    var n, i = this, r = i.config, a = i.$el, o = a.eventRect, s = a.svg, l = a.tooltip, c = i.state, f = c.event, h = c.hasAxis, g = c.hasRadar, x = c.hasTreemap;
    if ((l == null ? void 0 : l.style("display")) === "block" && f) {
      var y = e != null ? e : (n = g ? s : o) === null || n === void 0 ? void 0 : n.node();
      if (h || g)
        if (i.isMultipleX())
          i.selectRectForMultipleXs(y, !1);
        else {
          var T = t != null ? t : i.getDataIndexFromEvent(f);
          t === -1 ? i.api.tooltip.hide() : (i.selectRectForSingle(y, T), i.setExpand(T, null, !0));
        }
      else {
        var A = f.clientX, w = f.clientY;
        setTimeout(function() {
          var C = [A, w].every(Number.isFinite) && je.elementFromPoint(A, w), R = C && le(C).datum();
          if (R) {
            var S = i.hasArcType() ? i.convertToArcData(i.updateAngle(R)) : R == null ? void 0 : R.data;
            x && (C = s.node()), S && i.showTooltip([S], C);
          } else
            i.api.tooltip.hide();
        }, r.transition_duration);
      }
    }
  }
}, ly = {
  getTranslate: function(e, t) {
    var n;
    t === void 0 && (t = 0);
    var i = this, r = i.config, a = i.state, o = r.axis_rotated, s = 0, l, c;
    if (t && /^(x|y2?)$/.test(e) && (s = i.getAxisSize(e) * t), e === "main")
      l = ar(a.margin.left), c = ar(a.margin.top);
    else if (e === "context")
      l = ar(a.margin2.left), c = ar(a.margin2.top);
    else if (e === "legend")
      l = a.margin3.left, c = a.margin3.top;
    else if (e === "x")
      l = o ? -s : 0, c = o ? 0 : a.height + s;
    else if (e === "y")
      l = o ? 0 : -s, c = o ? a.height + s : 0;
    else if (e === "y2")
      l = o ? 0 : a.width + s, c = o ? -s - 1 : 0;
    else if (e === "subX")
      l = 0, c = o ? 0 : a.height2;
    else if (e === "arc")
      l = a.arcWidth / 2, c = a.arcHeight / 2, !((n = r.arc_rangeText_values) === null || n === void 0) && n.length && (c += 5 + (i.hasType("gauge") && r.title_text ? 10 : 0));
    else if (e === "polar")
      l = a.arcWidth / 2, c = a.arcHeight / 2;
    else if (e === "radar") {
      var f = i.getRadarSize(), h = f[0], g = f[1];
      l = a.width / 2 - h, c = a.height / 2 - g;
    }
    return "translate(".concat(l, ", ").concat(c, ")");
  },
  transformMain: function(e, t) {
    var n = this, i = n.$el.main, r = n.$T, a = t != null && t.axisX ? t.axisX : r(i.select(".".concat(nt.axisX)), e), o = t != null && t.axisY ? t.axisY : r(i.select(".".concat(nt.axisY)), e), s = t != null && t.axisY2 ? t.axisY2 : r(i.select(".".concat(nt.axisY2)), e);
    r(i, e).attr("transform", n.getTranslate("main")), a.attr("transform", n.getTranslate("x")), o.attr("transform", n.getTranslate("y")), s.attr("transform", n.getTranslate("y2")), i.select(".".concat(Xe.chartArcs)).attr("transform", n.getTranslate("arc"));
  },
  transformAll: function(e, t) {
    var n = this, i = n.config, r = n.state, a = r.hasAxis, o = r.hasFunnel, s = r.hasTreemap, l = n.$el;
    !o && !s && n.transformMain(e, t), a && i.subchart_show && n.transformContext(e, t), l.legend && n.transformLegend(e);
  }
}, cy = {
  /**
   * Check if the given chart type is valid
   * @param {string} type Chart type string
   * @returns {boolean}
   * @private
   */
  isValidChartType: function(e) {
    return !!(e && Object.values(ve).indexOf(e) > -1);
  },
  setTargetType: function(e, t) {
    var n = this, i = n.config, r = n.state.withoutFadeIn;
    n.mapToTargetIds(e).forEach(function(a) {
      r[a] = t === i.data_types[a], i.data_types[a] = t;
    }), e || (i.data_type = t);
  },
  /**
   * Updte current used chart types
   * @private
   */
  updateTypesElements: function() {
    var e = this, t = e.state.current;
    Object.keys(ve).forEach(function(n) {
      var i = ve[n], r = e.hasType(i, null, !0), a = t.types.indexOf(i);
      a === -1 && r ? t.types.push(i) : a > -1 && !r && t.types.splice(a, 1);
    }), e.setChartElements();
  },
  /**
   * Check if given chart types exists
   * @param {string} type Chart type
   * @param {Array} targetsValue Data array
   * @param {boolean} checkFromData Force to check type cotains from data targets
   * @returns {boolean}
   * @private
   */
  hasType: function(e, t, n) {
    var i;
    n === void 0 && (n = !1);
    var r = this, a = r.config, o = r.state.current, s = a.data_types, l = t || r.data.targets, c = !1;
    return !n && ((i = o.types) === null || i === void 0 ? void 0 : i.indexOf(e)) > -1 ? c = !0 : l != null && l.length ? l.forEach(function(f) {
      var h = s[f.id];
      (h === e || !h && e === "line") && (c = !0);
    }) : Object.keys(s).length ? Object.keys(s).forEach(function(f) {
      s[f] === e && (c = !0);
    }) : c = a.data_type === e, c;
  },
  /**
   * Check if contains given chart types
   * @param {string} type Type key
   * @param {object} targets Target data
   * @param {Array} exclude Excluded types
   * @returns {boolean}
   * @private
   */
  hasTypeOf: function(e, t, n) {
    var i = this;
    return n === void 0 && (n = []), e in xn ? !xn[e].filter(function(r) {
      return n.indexOf(r) === -1;
    }).every(function(r) {
      return !i.hasType(r, t);
    }) : !1;
  },
  /**
   * Check if given data is certain chart type
   * @param {object} d Data object
   * @param {string|Array} type chart type
   * @returns {boolean}
   * @private
   */
  isTypeOf: function(e, t) {
    var n, i = Ne(e) ? e : e.id, r = this.config && (((n = this.config.data_types) === null || n === void 0 ? void 0 : n[i]) || this.config.data_type);
    return Ie(t) ? t.indexOf(r) >= 0 : r === t;
  },
  hasPointType: function() {
    var e = this;
    return e.hasTypeOf("Line") || e.hasType("bubble") || e.hasType("scatter");
  },
  /**
   * Check if contains arc types chart
   * @param {object} targets Target data
   * @param {Array} exclude Excluded types
   * @returns {boolean}
   * @private
   */
  hasArcType: function(e, t) {
    return this.hasTypeOf("Arc", e, t);
  },
  hasMultiArcGauge: function() {
    return this.hasType("gauge") && this.config.gauge_type === "multi";
  },
  isLineType: function(e) {
    var t = Ne(e) ? e : e.id;
    return !this.config.data_types[t] || this.isTypeOf(t, xn.Line);
  },
  isStepType: function(e) {
    return this.isTypeOf(e, xn.Step);
  },
  isSplineType: function(e) {
    return this.isTypeOf(e, xn.Spline);
  },
  isAreaType: function(e) {
    return this.isTypeOf(e, xn.Area);
  },
  isAreaRangeType: function(e) {
    return this.isTypeOf(e, xn.AreaRange);
  },
  isBarType: function(e) {
    return this.isTypeOf(e, "bar");
  },
  isBubbleType: function(e) {
    return this.isTypeOf(e, "bubble");
  },
  isCandlestickType: function(e) {
    return this.isTypeOf(e, "candlestick");
  },
  isScatterType: function(e) {
    return this.isTypeOf(e, "scatter");
  },
  isTreemapType: function(e) {
    return this.isTypeOf(e, "treemap");
  },
  isPieType: function(e) {
    return this.isTypeOf(e, "pie");
  },
  isFunnelType: function(e) {
    return this.isTypeOf(e, "funnel");
  },
  isGaugeType: function(e) {
    return this.isTypeOf(e, "gauge");
  },
  isDonutType: function(e) {
    return this.isTypeOf(e, "donut");
  },
  isPolarType: function(e) {
    return this.isTypeOf(e, "polar");
  },
  isRadarType: function(e) {
    return this.isTypeOf(e, "radar");
  },
  isArcType: function(e) {
    return this.isPieType(e) || this.isDonutType(e) || this.isGaugeType(e) || this.isPolarType(e) || this.isRadarType(e);
  },
  // determine if is 'circle' data point
  isCirclePoint: function(e) {
    var t = this.config, n = t.point_pattern, i = !1;
    return (e == null ? void 0 : e.tagName) === "circle" ? i = !0 : i = t.point_type === "circle" && (!n || Ie(n) && n.length === 0), i;
  },
  lineData: function(e) {
    return this.isLineType(e) ? [e] : [];
  },
  arcData: function(e) {
    return this.isArcType(e.data) ? [e] : [];
  },
  /**
   * Get data adapt for data label showing
   * @param {object} d Data object
   * @returns {Array}
   * @private
   */
  labelishData: function(e) {
    return this.isBarType(e) || this.isLineType(e) || this.isScatterType(e) || this.isBubbleType(e) || this.isCandlestickType(e) || this.isFunnelType(e) || this.isRadarType(e) || this.isTreemapType(e) ? e.values.filter(function(t) {
      return ge(t.value) || !!t.value;
    }) : [];
  },
  barLineBubbleData: function(e) {
    return this.isBarType(e) || this.isLineType(e) || this.isBubbleType(e) ? e.values : [];
  },
  // https://github.com/d3/d3-shape#curves
  isInterpolationType: function(e) {
    return [
      "basis",
      "basis-closed",
      "basis-open",
      "bundle",
      "cardinal",
      "cardinal-closed",
      "cardinal-open",
      "catmull-rom",
      "catmull-rom-closed",
      "catmull-rom-open",
      "linear",
      "linear-closed",
      "monotone-x",
      "monotone-y",
      "natural"
    ].indexOf(e) >= 0;
  }
};
function fy(e) {
  var t = this, n;
  return t.isLineType(e) ? n = t.generateGetLinePoints(t.getShapeIndices(t.isLineType)) : t.isBarType(e) && (n = t.generateGetBarPoints(t.getShapeIndices(t.isBarType))), n;
}
var dy = {
  /**
   * Get the shape draw function
   * @returns {object}
   * @private
   */
  getDrawShape: function() {
    var e = this, t = e.config.axis_rotated, n = e.state, i = n.hasRadar, r = n.hasTreemap, a = { type: {}, indices: {}, pos: {} };
    if (!r && ["bar", "candlestick", "line", "area"].forEach(function(l) {
      var c = hn(/^(bubble|scatter)$/.test(l) ? "line" : l);
      if (e.hasType(l) || e.hasTypeOf(c) || l === "line" && (e.hasType("bubble") || e.hasType("scatter"))) {
        var f = e.getShapeIndices(e["is".concat(c, "Type")]), h = e["generateDraw".concat(c)];
        a.indices[l] = f, a.type[l] = h ? h.bind(e)(f, !1) : void 0;
      }
    }), !e.hasArcType() || i || r) {
      var o = void 0, s = void 0;
      r || (o = i ? e.radarCircleX : t ? e.circleY : e.circleX, s = i ? e.radarCircleY : t ? e.circleX : e.circleY), a.pos = {
        xForText: e.generateXYForText(a.indices, !0),
        yForText: e.generateXYForText(a.indices, !1),
        cx: (o || function() {
        }).bind(e),
        cy: (s || function() {
        }).bind(e)
      };
    }
    return a;
  },
  /**
   * Get shape's indices according it's position within each axis tick.
   *
   * From the below example, indices will be:
   * ==> {data1: 0, data2: 0, data3: 1, data4: 1, __max__: 1}
   *
   * 	data1 data3   data1 data3
   * 	data2 data4   data2 data4
   * 	-------------------------
   * 		 0             1
   * @param {Function} typeFilter Chart type filter function
   * @returns {object} Indices object with its position
   */
  getShapeIndices: function(e) {
    var t = this, n = t.config, i = n.data_xs, r = Qe(i), a = {}, o = r ? {} : 0;
    return r && uo(Object.keys(i).map(function(s) {
      return i[s];
    })).forEach(function(s) {
      o[s] = 0, a[s] = {};
    }), t.filterTargetsToShow(t.data.targets.filter(e, t)).forEach(function(s) {
      for (var l, c = (s.id in i) ? i[s.id] : "", f = c ? a[c] : a, h = 0, g = void 0; g = n.data_groups[h]; h++)
        if (!(g.indexOf(s.id) < 0))
          for (var x = 0, y = void 0; y = g[x]; x++) {
            if (y in f) {
              f[s.id] = f[y];
              break;
            }
            s.id !== y && c && (f[y] = (l = f[s.id]) !== null && l !== void 0 ? l : o[c]);
          }
      at(f[s.id]) && (f[s.id] = c ? o[c]++ : o++, f.__max__ = (c ? o[c] : o) - 1);
    }), a;
  },
  /**
   * Get indices value based on data ID value
   * @param {object} indices Indices object
   * @param {object} d Data row
   * @param {string} caller Caller function name (Used only for 'sparkline' plugin)
   * @returns {object} Indices object
   * @private
   */
  getIndices: function(e, t, n) {
    var i = this, r = i.config, a = r.data_xs, o = r.bar_indices_removeNull, s = t.id, l = t.index;
    if (i.isBarType(s) && o) {
      var c = {};
      return i.getAllValuesOnIndex(l, !0).forEach(function(f, h) {
        c[f.id] = h, c.__max__ = h;
      }), c;
    }
    return Qe(a) ? e[a[s]] : e;
  },
  /**
   * Get indices max number
   * @param {object} indices Indices object
   * @returns {number} Max number
   * @private
   */
  getIndicesMax: function(e) {
    return Qe(this.config.data_xs) ? (
      // if is multiple xs, return total sum of xs' __max__ value
      Object.keys(e).map(function(t) {
        return e[t].__max__ || 0;
      }).reduce(function(t, n) {
        return t + n;
      })
    ) : e.__max__;
  },
  getShapeX: function(e, t, n) {
    var i = this, r = i.config, a = i.scale, o = n ? a.subX : a.zoom || a.x, s = r.bar_overlap, l = r.bar_padding, c = function(h, g) {
      return h + g;
    }, f = Jt(e) && (e._$total.length ? e._$total.reduce(c) / 2 : 0);
    return function(h) {
      var g = i.getIndices(t, h, "getShapeX"), x = h.id in g ? g[h.id] : 0, y = (g.__max__ || 0) + 1, T = 0;
      if (Qe(h.x)) {
        var A = o(h.x, !0);
        if (f) {
          var w = e[h.id] || e._$width;
          T = s ? A - w / 2 : A - w + e._$total.slice(0, x + 1).reduce(c) - f;
        } else
          T = A - (ge(e) ? e : e._$width) * (y / 2 - (s ? 1 : x));
      }
      return e && T && y > 1 && l && (x && (T += l * x), y > 2 ? T -= (y - 1) * l / 2 : y === 2 && (T -= l / 2)), T;
    };
  },
  getShapeY: function(e) {
    var t = this, n = t.isStackNormalized();
    return function(i) {
      var r = i.value;
      return ge(i) ? r = i : t.isAreaRangeType(i) ? r = t.getBaseValue(i, "mid") : n ? r = t.getRatio("index", i, !0) : t.isBubbleZType(i) ? r = t.getBubbleZData(i.value, "y") : t.isBarRangeType(i) && (r = r[1]), t.getYScaleById(i.id, e)(r);
    };
  },
  /**
   * Get shape based y Axis min value
   * @param {string} id Data id
   * @returns {number}
   * @private
   */
  getShapeYMin: function(e) {
    var t = this, n = t.axis.getId(e), i = t.scale[n], r = i.domain()[0], a = t.config["axis_".concat(n, "_inverted")];
    return !t.isGrouped(e) && !a && r > 0 ? r : 0;
  },
  /**
   * Get Shape's offset data
   * @param {Function} typeFilter Type filter function
   * @returns {object}
   * @private
   */
  getShapeOffsetData: function(e) {
    var t = this, n = t.orderTargets(t.filterTargetsToShow(t.data.targets.filter(e, t))), i = t.isStackNormalized(), r = n.map(function(o) {
      var s = o.values, l = {};
      t.isStepType(o) && (s = t.convertValuesToStep(s));
      var c = s.reduce(function(f, h) {
        var g = Number(h.x);
        return f[g] = h, l[g] = i ? t.getRatio("index", h, !0) : h.value, f;
      }, {});
      return {
        id: o.id,
        rowValues: s,
        rowValueMapByXValue: c,
        values: l
      };
    }), a = n.reduce(function(o, s, l) {
      var c = s.id;
      return o[c] = l, o;
    }, {});
    return { indexMapByTargetId: a, shapeOffsetTargets: r };
  },
  getShapeOffset: function(e, t, n) {
    var i = this, r = i.getShapeOffsetData(e), a = r.shapeOffsetTargets, o = r.indexMapByTargetId, s = i.config.data_groupsZeroAs;
    return function(l, c) {
      var f = l.id, h = l.value, g = l.x, x = i.getIndices(t, l), y = i.getYScaleById(f, n);
      if (i.isBarRangeType(l))
        return y(h[0]);
      var T = Number(g), A = y(s === "zero" ? 0 : i.getShapeYMin(f)), w = A;
      return a.filter(function(C) {
        return C.id !== f && x[C.id] === x[f];
      }).forEach(function(C) {
        var R = C.id, S = C.rowValueMapByXValue, I = C.rowValues, L = C.values;
        if (o[R] < o[f]) {
          var p = L[T], U = I[c];
          if ((!U || Number(U.x) !== T) && (U = S[T]), (U == null ? void 0 : U.value) * h >= 0 && ge(p)) {
            var H = h === 0 ? s === "positive" && p > 0 || s === "negative" && p < 0 : !0;
            H && (w += y(p) - A);
          }
        }
      }), w;
    };
  },
  /**
   * Get data's y coordinate
   * @param {object} d Target data
   * @param {number} i Index number
   * @returns {number} y coordinate
   * @private
   */
  circleY: function(e, t) {
    var n = this, i = e.id, r;
    return n.isGrouped(i) && (r = fy.bind(n)(e)), r ? r(e, t)[0][1] : n.getYScaleById(i)(n.getBaseValue(e));
  },
  getBarW: function(e, t, n) {
    var i, r, a, o, s, l = this, c = l.config, f = l.org, h = l.scale, g = l.state, x = l.getMaxDataCount(), y = e === "bar" && ((i = c.data_groups) === null || i === void 0 ? void 0 : i.length), T = "".concat(e, "_width"), A = ((a = (r = l.getZoomTransform) === null || r === void 0 ? void 0 : r.call(l)) !== null && a !== void 0 ? a : { k: 1 }).k, w = [
      (o = c.axis_x_min) !== null && o !== void 0 ? o : f.xDomain[0],
      (s = c.axis_x_max) !== null && s !== void 0 ? s : f.xDomain[1]
    ].map(l.axis.isTimeSeries() ? kt.bind(l) : Number), C = t.tickInterval(x);
    if (h.zoom && !l.axis.isCategorized() && A > 1) {
      var R = w.every(function(L, p) {
        return L === f.xDomain[p];
      });
      C = f.xDomain.map(function(L, p) {
        var U = R ? L : L - Math.abs(w[p]);
        return h.zoom(U);
      }).reduce(function(L, p) {
        return Math.abs(L) + p;
      }) / x;
    }
    var S = function(L) {
      var p = L ? c[T][L] : c[T], U = L ? p.ratio : c["".concat(T, "_ratio")], H = L ? p.max : c["".concat(T, "_max")], q = ge(p) ? p : he(p) ? p.call(l, g.width, n, x) : n ? C * U / n : 0;
      return H && q > H ? H : q;
    }, I = S();
    return !y && Jt(c[T]) && (I = { _$width: I, _$total: [] }, l.filterTargetsToShow(l.data.targets).forEach(function(L) {
      c[T][L.id] && (I[L.id] = S(L.id), I._$total.push(I[L.id] || I._$width));
    })), I;
  },
  /**
   * Get shape element
   * @param {string} shapeName Shape string
   * @param {number} i Index number
   * @param {string} id Data series id
   * @returns {d3Selection}
   * @private
   */
  getShapeByIndex: function(e, t, n) {
    var i = this, r = i.$el, a = Ae(t) ? "-".concat(t) : "", o = r[e];
    return o && !o.empty() ? o = o.filter(function(s) {
      return n ? s.id === n : !0;
    }).filter(function(s) {
      return Ae(t) ? s.index === t : !0;
    }) : o = (n ? r.main.selectAll(".".concat(lt["".concat(e, "s")]).concat(i.getTargetSelectorSuffix(n))) : r.main).selectAll(".".concat(lt[e]).concat(a)), o;
  },
  isWithinShape: function(e, t) {
    var n, i = this, r = le(e), a;
    return i.isTargetToShow(t.id) ? !((n = i.hasValidPointType) === null || n === void 0) && n.call(i, e.nodeName) ? a = i.isStepType(t) ? i.isWithinStep(e, i.getYScaleById(t.id)(i.getBaseValue(t))) : i.isWithinCircle(e, i.isBubbleType(t) ? i.pointSelectR(t) * 1.5 : 0) : e.nodeName === "path" && (a = r.classed(lt.bar) ? i.isWithinBar(e) : !0) : a = !1, a;
  },
  getInterpolate: function(e) {
    var t = this, n = t.getInterpolateType(e);
    return {
      basis: Fx,
      "basis-closed": Bx,
      "basis-open": Xx,
      bundle: zx,
      cardinal: Hx,
      "cardinal-closed": Vx,
      "cardinal-open": Yx,
      "catmull-rom": Ux,
      "catmull-rom-closed": jx,
      "catmull-rom-open": Gx,
      "monotone-x": qx,
      "monotone-y": Zx,
      natural: Kx,
      "linear-closed": Wx,
      linear: Po,
      step: Qx,
      "step-after": e_,
      "step-before": Jx
    }[n];
  },
  getInterpolateType: function(e) {
    var t = this, n = t.config, i = n.spline_interpolation_type, r = t.isInterpolationType(i) ? i : "cardinal";
    return t.isSplineType(e) ? r : t.isStepType(e) ? n.line_step_type : "linear";
  },
  isWithinBar: function(e) {
    var t = Qt(this.state.event, e), n = Jl(e), i = n[0], r = n[1], a = Math.min(i.x, r.x), o = Math.min(i.y, r.y), s = this.config.bar_sensitivity, l = e.getBBox(), c = l.width, f = l.height, h = a - s, g = a + c + s, x = o + f + s, y = o - s, T = h < t[0] && t[0] < g && y < t[1] && t[1] < x;
    return T;
  }
}, qr = (
  /** @class */
  function() {
    function e(t) {
      this.data = {
        xs: {},
        targets: []
      }, this.scale = {
        x: null,
        y: null,
        y2: null,
        subX: null,
        subY: null,
        subY2: null,
        zoom: null
      }, this.org = {
        xScale: null,
        xDomain: null
      }, this.format = {
        extraLineClasses: null,
        xAxisTick: null,
        dataTime: null,
        // dataTimeFormat
        defaultAxisTime: null,
        // defaultAxisTimeFormat
        axisTime: null
        // axisTimeFormat
      };
      var n = this;
      n.api = t, n.config = new Wr(), n.cache = new R_();
      var i = new E_();
      n.$el = i.getStore("element"), n.state = i.getStore("state"), n.$T = n.$T.bind(n);
    }
    return e.prototype.$T = function(t, n, i) {
      var r = this, a = r.config, o = r.state, s = a.transition_duration, l = a.subchart_show, c = t;
      if (c) {
        "tagName" in c && (c = le(c));
        var f = (n !== !1 && s || n) && (!o.zooming || o.dragging) && !o.resizing && o.rendered && !l;
        c = f ? c.transition(i).duration(s) : c;
      }
      return c;
    }, e.prototype.beforeInit = function() {
      var t = this;
      t.callPluginHook("$beforeInit"), Ze(t.config.onbeforeinit, t.api);
    }, e.prototype.afterInit = function() {
      var t = this;
      t.callPluginHook("$afterInit"), Ze(t.config.onafterinit, t.api);
    }, e.prototype.init = function() {
      var t = this, n = t.config, i = t.state, r = t.$el, a = n.boost_useCssRule;
      if (M_(t), i.hasRadar = !i.hasAxis && t.hasType("radar"), i.hasFunnel = !i.hasAxis && t.hasType("funnel"), i.hasTreemap = !i.hasAxis && t.hasType("treemap"), i.hasAxis = !t.hasArcType() && !i.hasFunnel && !i.hasTreemap, i.datetimeId = "bb-".concat(+/* @__PURE__ */ new Date() * Fn()), a) {
        var o = je.createElement("style");
        o.type = "text/css", je.head.appendChild(o), i.style = {
          rootSelctor: ".".concat(i.datetimeId),
          sheet: o.sheet
        }, r.style = o;
      }
      var s = {
        element: n.bindto,
        classname: "bb"
      };
      Be(n.bindto) && (s.element = n.bindto.element || "#chart", s.classname = n.bindto.classname || s.classname), r.chart = he(s.element.node) ? n.bindto.element : le(s.element || []), r.chart.empty() && (r.chart = le(je.body.appendChild(je.createElement("div")))), r.chart.html("").classed(s.classname, !0).classed(i.datetimeId, a).style("position", "relative"), t.initParams(), t.initToRender();
    }, e.prototype.initToRender = function(t) {
      var n = this, i = n.config, r = n.state, a = n.$el.chart, o = function() {
        return A_(a, { display: "none", visibility: "hidden" });
      }, s = i.render.lazy === !1 ? !1 : i.render.lazy || o(), l = Me.MutationObserver;
      s && l && i.render.observe !== !1 && !t && new l(function(c, f) {
        o() || (f.disconnect(), !r.rendered && n.initToRender(!0));
      }).observe(a.node(), {
        attributes: !0,
        attributeFilter: ["class", "style"]
      }), (!s || t) && n.convertData(i, function(c) {
        n.initWithData(c), n.afterInit();
      });
    }, e.prototype.initParams = function() {
      var t, n = this, i = n.config, r = n.format, a = n.state, o = i.axis_rotated;
      if (n.color = n.generateColor(), n.levelColor = n.generateLevelColor(), i.padding === !1 && (i.axis_x_show = !1, i.axis_y_show = !1, i.axis_y2_show = !1, i.subchart_show = !1), (n.hasPointType() || !((t = n.hasLegendDefsPoint) === null || t === void 0) && t.call(n)) && (n.point = n.generatePoint()), a.hasAxis) {
        n.initClip(), r.extraLineClasses = n.generateExtraLineClass(), r.dataTime = i.data_xLocaltime ? Il : Nl, r.axisTime = i.axis_x_localtime ? Mo : Lo;
        var s = n.config.zoom_enabled && n.config.zoom_type === "drag";
        r.defaultAxisTime = function(l) {
          var c = n.scale, f = c.x, h = c.zoom, g = s ? h : h && f.orgDomain().toString() !== h.domain().toString(), x = l.getMilliseconds() && ".%L" || l.getSeconds() && ".:%S" || l.getMinutes() && "%I:%M" || l.getHours() && "%I %p" || l.getDate() !== 1 && "%b %d" || g && l.getDate() === 1 && "%b'%y" || l.getMonth() && "%-m/%-d" || "%Y";
          return r.axisTime(x)(l);
        };
      }
      a.isLegendRight = i.legend_position === "right", a.isLegendInset = i.legend_position === "inset", a.isLegendTop = i.legend_inset_anchor === "top-left" || i.legend_inset_anchor === "top-right", a.isLegendLeft = i.legend_inset_anchor === "top-left" || i.legend_inset_anchor === "bottom-left", a.rotatedPadding.top = n.getResettedPadding(a.rotatedPadding.top), a.rotatedPadding.right = o && !i.axis_x_show ? 0 : 30, a.inputType = C_(i.interaction_inputType_mouse, i.interaction_inputType_touch);
    }, e.prototype.initWithData = function(t) {
      var n, i, r, a = this, o = a.config, s = a.scale, l = a.state, c = a.$el, f = a.org, h = l.hasAxis, g = l.hasFunnel, x = l.hasTreemap, y = o.interaction_enabled, T = a.hasType("polar"), A = o.data_labels_backgroundColors;
      if (h && (a.axis = a.getAxisInstance(), o.zoom_enabled && a.initZoom()), a.data.xs = {}, a.data.targets = a.convertDataToTargets(t), o.data_filter && (a.data.targets = a.data.targets.filter(o.data_filter.bind(a.api))), o.data_hide && a.addHiddenTargetIds(o.data_hide === !0 ? a.mapToIds(a.data.targets) : o.data_hide), o.legend_hide && a.addHiddenLegendIds(o.legend_hide === !0 ? a.mapToIds(a.data.targets) : o.legend_hide), a.updateSizes(), a.updateScales(!0), h) {
        var w = s.x, C = s.y, R = s.y2, S = s.subX, I = s.subY, L = s.subY2;
        w && (w.domain(si(a.getXDomain(a.data.targets), !o.axis_x_inverted)), S.domain(w.domain()), f.xDomain = w.domain()), C && (C.domain(a.getYDomain(a.data.targets, "y")), I.domain(C.domain())), R && (R.domain(a.getYDomain(a.data.targets, "y2")), L && L.domain(R.domain()));
      }
      if (c.svg = c.chart.append("svg").style("overflow", "hidden").style("display", "block"), y && l.inputType) {
        var p = l.inputType === "touch", U = o.onclick, H = o.onover, q = o.onout;
        c.svg.on("click", (U == null ? void 0 : U.bind(a.api)) || null).on(p ? "touchstart" : "mouseenter", (H == null ? void 0 : H.bind(a.api)) || null).on(p ? "touchend" : "mouseleave", (q == null ? void 0 : q.bind(a.api)) || null);
      }
      o.svg_classname && c.svg.attr("class", o.svg_classname);
      var X = he(o.color_tiles) && a.patterns;
      (h || X || T || x || A || !((n = a.hasLegendDefsPoint) === null || n === void 0) && n.call(a)) && (c.defs = c.svg.append("defs"), h && ["id", "idXAxis", "idYAxis", "idGrid"].forEach(function(G) {
        a.appendClip(c.defs, l.clip[G]);
      }), a.generateTextBGColorFilter(A), X && a.patterns.forEach(function(G) {
        return c.defs.append(function() {
          return G.node;
        });
      })), a.updateSvgSize(), a.bindResize();
      var j = c.svg.append("g").classed(Ee.main, !0).attr("transform", g || x ? null : a.getTranslate("main"));
      if (c.main = j, o.subchart_show && a.initSubchart(), o.tooltip_show && a.initTooltip(), o.title_text && a.initTitle(), !x && o.legend_show && a.initLegend(), o.data_empty_label_text && j.append("text").attr("class", "".concat(Ct.text, " ").concat(Ee.empty)).attr("text-anchor", "middle").attr("dominant-baseline", "middle"), h && (o.regions.length && a.initRegion(), !o.clipPath && a.axis.init()), j.append("g").classed(Ee.chart, !0).attr("clip-path", h ? l.clip.path : null), a.callPluginHook("$init"), a.initChartElements(), h && (y && ((i = a.initEventRect) === null || i === void 0 || i.call(a)), a.initGrid(), o.clipPath && ((r = a.axis) === null || r === void 0 || r.init())), a.updateTargets(a.data.targets), a.updateDimension(), Ze(o.oninit, a.api), a.setBackground(), a.redraw({
        withTransition: !1,
        withTransform: !0,
        withUpdateXDomain: !0,
        withUpdateOrgXDomain: !0,
        withTransitionForAxis: !1,
        initializing: !0
      }), o.data_onmin || o.data_onmax) {
        var z = a.getMinMaxData();
        Ze(o.data_onmin, a.api, z.min), Ze(o.data_onmax, a.api, z.max);
      }
      o.tooltip_show && a.initShowTooltip(), l.rendered = !0;
    }, e.prototype.initChartElements = function() {
      var t = this, n = t.state, i = n.hasAxis, r = n.hasRadar, a = n.hasTreemap, o = [];
      if (i) {
        var s = ["bar", "bubble", "candlestick", "line"];
        t.config.bar_front && s.push(s.shift()), s.forEach(function(c) {
          var f = hn(c);
          (c === "line" && t.hasTypeOf(f) || t.hasType(c)) && o.push(f);
        });
      } else if (a)
        o.push("Treemap");
      else if (t.hasType("funnel"))
        o.push("Funnel");
      else {
        var l = t.hasType("polar");
        r || o.push("Arc", "Pie"), t.hasType("gauge") ? o.push("Gauge") : r ? o.push("Radar") : l && o.push("Polar");
      }
      o.forEach(function(c) {
        t["init".concat(c)]();
      }), Qe(t.config.data_labels) && !t.hasArcType(null, ["radar"]) && t.initText();
    }, e.prototype.setChartElements = function() {
      var t = this, n = t.$el, i = n.chart, r = n.svg, a = n.defs, o = n.main, s = n.tooltip, l = n.legend, c = n.title, f = n.grid, h = n.needle, g = n.arcs, x = n.circle, y = n.bar, T = n.candlestick, A = n.line, w = n.area, C = n.text;
      t.api.$ = {
        chart: i,
        svg: r,
        defs: a,
        main: o,
        tooltip: s,
        legend: l,
        title: c,
        grid: f,
        arc: g,
        circles: x,
        bar: { bars: y },
        candlestick: T,
        line: { lines: A, areas: w },
        needle: h,
        text: { texts: C }
      };
    }, e.prototype.setBackground = function() {
      var t = this, n = t.config.background, i = t.state, r = t.$el.svg;
      if (Qe(n)) {
        var a = r.select("g").insert(n.imgUrl ? "image" : "rect", ":first-child");
        n.imgUrl ? a.attr("href", n.imgUrl) : n.color && a.style("fill", n.color).attr("clip-path", i.clip.path), a.attr("class", n.class || null).attr("width", "100%").attr("height", "100%");
      }
    }, e.prototype.updateTargets = function(t) {
      var n, i = this, r = i.state, a = r.hasAxis, o = r.hasFunnel, s = r.hasRadar, l = r.hasTreemap, c = function(g) {
        return i["updateTargetsFor".concat(g)](t.filter(i["is".concat(g, "Type")].bind(i)));
      };
      if (i.updateTargetsForText(t), a)
        ["bar", "candlestick", "line"].forEach(function(g) {
          var x = hn(g);
          (g === "line" && i.hasTypeOf(x) || i.hasType(g)) && c(x);
        }), i.updateTargetsForSubchart && i.updateTargetsForSubchart(t);
      else if (i.hasArcType(t)) {
        var f = "Arc";
        s ? f = "Radar" : i.hasType("polar") && (f = "Polar"), c(f);
      } else
        o ? c("Funnel") : l && c("Treemap");
      var h = i.hasType("bubble") || i.hasType("scatter");
      h && ((n = i.updateTargetForCircle) === null || n === void 0 || n.call(i)), i.filterTargetsToShowAtInit(h);
    }, e.prototype.filterTargetsToShowAtInit = function(t) {
      t === void 0 && (t = !1);
      var n = this, i = n.$el.svg, r = n.$T, a = ".".concat(Ee.target);
      t && (a += ", .".concat(pt.chartCircles, " > .").concat(pt.circles)), r(i.selectAll(a).filter(function(o) {
        return n.isTargetToShow(o.id);
      })).style("opacity", null);
    }, e.prototype.getWithOption = function(t) {
      var n = {
        Dimension: !0,
        EventRect: !0,
        Legend: !1,
        Subchart: !0,
        Transform: !1,
        Transition: !0,
        TrimXDomain: !0,
        UpdateXAxis: "UpdateXDomain",
        UpdateXDomain: !1,
        UpdateOrgXDomain: !1,
        TransitionForExit: "Transition",
        TransitionForAxis: "Transition",
        Y: !0
      };
      return Object.keys(n).forEach(function(i) {
        var r = n[i];
        Ne(r) && (r = n[r]), n[i] = mn(t, "with".concat(i), r);
      }), n;
    }, e.prototype.initialOpacity = function(t) {
      var n = this, i = n.state.withoutFadeIn, r = n.getBaseValue(t) !== null && i[t.id] ? null : "0";
      return r;
    }, e.prototype.bindResize = function() {
      var t = this, n = t.config, i = t.state, r = N_(n.resize_timer), a = [];
      a.push(function() {
        return Ze(n.onresize, t.api);
      }), n.resize_auto === !0 && a.push(function() {
        i.resizing = !0, n.legend_show && (t.updateSizes(), t.updateLegend()), t.api.flush(!1);
      }), a.push(function() {
        Ze(n.onresized, t.api), i.resizing = !1;
      }), a.forEach(function(o) {
        return r.add(o);
      }), t.resizeFunction = r, Me.addEventListener("resize", t.resizeFunction = r);
    }, e.prototype.callPluginHook = function(t) {
      for (var n = this, i = [], r = 1; r < arguments.length; r++)
        i[r - 1] = arguments[r];
      this.config.plugins.forEach(function(a) {
        t === "$beforeInit" && (a.$$ = n, n.api.plugins.push(a)), a[t].apply(a, i);
      });
    }, e;
  }()
);
Dt(qr.prototype, [
  // common
  z_,
  H_,
  V_,
  U_,
  j_,
  Z_,
  K_,
  Y_,
  Q_,
  J_,
  ey,
  ty,
  dy,
  ny,
  iy,
  oy,
  sy,
  uy,
  ly,
  cy
]);
function hy(e) {
  var t = this.config, n, i, r, a = function() {
    var o = i.shift();
    if (o && n && Jt(n) && o in n)
      return n = n[o], a();
    if (!o)
      return n;
  };
  Object.keys(t).forEach(function(o) {
    n = e, i = o.split("_"), r = a(), Ye(r) && (t[o] = r);
  }), this.api && (this.state.orgConfig = e);
}
var gy = {
  /**
   * Resize the chart.
   * @function resize
   * @instance
   * @memberof Chart
   * @param {object} size This argument should include width and height in pixels.
   * @param {number} [size.width] width value
   * @param {number} [size.height] height value
   * @example
   * // Resize to 640x480
   * chart.resize({
   *    width: 640,
   *    height: 480
   * });
   */
  resize: function(e) {
    var t = this.internal, n = t.config, i = t.state;
    i.rendered && (n.size_width = e ? e.width : null, n.size_height = e ? e.height : null, i.resizing = !0, this.flush(!1), t.resizeFunction());
  },
  /**
   * Force to redraw.
   * - **NOTE:** When zoom/subchart is used, the zoomed state will be resetted.
   * @function flush
   * @instance
   * @memberof Chart
   * @param {boolean} [soft] For soft redraw.
   * @example
   * chart.flush();
   *
   * // for soft redraw
   * chart.flush(true);
   */
  flush: function(e) {
    var t, n, i = this.internal, r = i.state, a = i.$el.zoomResetBtn;
    r.rendered ? (r.resizing ? (t = i.brush) === null || t === void 0 || t.updateResize() : (n = i.axis) === null || n === void 0 || n.setOrient(), a == null || a.style("display", "none"), i.scale.zoom = null, e ? i.redraw({
      withTransform: !0,
      withUpdateXDomain: !0,
      withUpdateOrgXDomain: !0,
      withLegend: !0
    }) : i.updateAndRedraw({
      withLegend: !0,
      withTransition: !1,
      withTransitionForTransform: !1
    }), !r.resizing && i.brush && (i.brush.getSelection().call(i.brush.move), i.unselectRect())) : i.initToRender(!0);
  },
  /**
   * Reset the chart object and remove element and events completely.
   * @function destroy
   * @instance
   * @memberof Chart
   * @returns {null}
   * @example
   * chart.destroy();
   */
  destroy: function() {
    var e = this, t = this.internal, n = t.$el, i = n.chart, r = n.style, a = n.svg;
    if (Qe(t)) {
      t.callPluginHook("$willDestroy"), t.charts.splice(t.charts.indexOf(this), 1), t.unbindAllEvents(), a.select("*").interrupt(), t.resizeFunction.clear(), Me.removeEventListener("resize", t.resizeFunction), i.classed("bb", !1).style("position", null).selectChildren().remove(), r && r.parentNode.removeChild(r), Object.keys(this).forEach(function(s) {
        s === "internal" && Object.keys(t).forEach(function(l) {
          t[l] = null;
        }), e[s] = null, delete e[s];
      });
      for (var o in this)
        this[o] = function() {
        };
    }
    return null;
  },
  /**
   * Get or set config option value.
   * - **NOTE**
   *  - The option key name must be specified as the last level.
   *  - when no argument is given, will return all specified generation options object only. (will exclude any other options not specified at the initialization)
   * @function config
   * @instance
   * @memberof Chart
   * @param {string} name The option key name.
   * @param {*} [value] The value accepted for indicated option.
   * @param {boolean} [redraw] Set to redraw with the new option changes.
   * - **NOTE:** Doesn't guarantee work in all circumstances. It can be applied for limited options only.
   * @returns {*}
   * @example
   *
   * // Getter
   * chart.config("gauge.max");
   *
   * // Getter specified with top level key name will not work.
   * // The option key name must be specified as the last level.
   * // chart.config("gauge"); // will not work
   *
   * // without any arguments, it returns generation config object
   * chart.config();  // {data: { ... }, axis: { ... }, ...}
   *
   * // Setter
   * chart.config("gauge.max", 100);
   *
   * // Setter specified with top level key name will not work.
   * // The option key name must be specified as the last level.
   * // chart.config("gauge", {min: 10, max: 20}); // will not work
   *
   * // Setter & redraw with the new option
   * chart.config("gauge.max", 100, true);
   */
  config: function(e, t, n) {
    var i = this.internal, r = i.config, a = i.state, o = e == null ? void 0 : e.replace(/\./g, "_"), s;
    return e && o in r ? Ye(t) ? (r[o] = t, s = t, n && this.flush()) : s = r[o] : (arguments.length === 0 || en(e)) && (s = a.orgConfig), s;
  }
}, py = {
  /**
   * Get the color
   * @function color
   * @instance
   * @memberof Chart
   * @param {string} id id to get the color
   * @returns {string}
   * @example
   * chart.color("data1");
   */
  color: function(e) {
    return this.internal.color(e);
  }
}, sc = function(e) {
  var t = this.internal.data.targets;
  if (!at(e)) {
    var n = Ie(e) ? e : [e];
    return t.filter(function(i) {
      return n.some(function(r) {
        return r === i.id;
      });
    });
  }
  return t;
};
Dt(sc, {
  /**
   * Get data shown in the chart.
   * @function data․shown
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIds If this argument is given, this API filters the data with specified target ids. If this argument is not given, all shown data will be returned.
   * @returns {Array} Data objects
   * @example
   * // Get shown data by filtering to include only data1 data
   * chart.data.shown("data1");
   * // --> [{id: "data1", id_org: "data1", values: Array(6)}, ...]
   *
   * // Get shown data by filtering to include data1 and data2 data
   * chart.data.shown(["data1", "data2"]);
   *
   * // Get all shown data
   * chart.data.shown();
   */
  shown: function(e) {
    return this.internal.filterTargetsToShow(this.data(e));
  },
  /**
   * Get values of the data loaded in the chart.
   * @function data․values
   * @instance
   * @memberof Chart
   * @param {string|Array|null} targetIds This API returns the values of specified target. If this argument is not given, null will be retruned
   * @param {boolean} [flat=true] Get flatten values
   * @returns {Array} Data values
   * @example
   * // Get data1 values
   * chart.data.values("data1");
   * // --> [10, 20, 30, 40]
   */
  values: function(e, t) {
    t === void 0 && (t = !0);
    var n = null;
    if (e) {
      var i = this.data(e);
      Ie(i) && (n = [], i.forEach(function(r) {
        var a = r.values.map(function(o) {
          return o.value;
        });
        t ? n = n.concat(a) : n.push(a);
      }));
    }
    return n;
  },
  /**
   * Get and set names of the data loaded in the chart.
   * @function data․names
   * @instance
   * @memberof Chart
   * @param {object} names If this argument is given, the names of data will be updated. If not given, the current names will be returned. The format of this argument is the same as [data.names](./Options.html#.data%25E2%2580%25A4names).
   * @returns {object} Corresponding names according its key value, if specified names values.
   * @example
   * // Get current names
   * chart.data.names();
   * // --> {data1: "test1", data2: "test2"}
   *
   * // Update names
   * chart.data.names({
   *  data1: "New Name 1",
   *  data2: "New Name 2"
   * });
   */
  names: function(e) {
    var t = this.internal;
    return t.updateDataAttributes("names", e);
  },
  /**
   * Get and set colors of the data loaded in the chart.
   * @function data․colors
   * @instance
   * @memberof Chart
   * @param {object} colors If this argument is given, the colors of data will be updated. If not given, the current colors will be returned. The format of this argument is the same as [data.colors](./Options.html#.data%25E2%2580%25A4colors).
   * @returns {object} Corresponding data color value according its key value.
   * @example
   * // Get current colors
   * chart.data.colors();
   * // --> {data1: "#00c73c", data2: "#fa7171"}
   *
   * // Update colors
   * chart.data.colors({
   *  data1: "#FFFFFF",
   *  data2: "#000000"
   * });
   */
  colors: function(e) {
    return this.internal.updateDataAttributes("colors", e);
  },
  /**
   * Get and set axes of the data loaded in the chart.
   * - **NOTE:** If all data is related to one of the axes, the domain of axis without related data will be replaced by the domain from the axis with related data
   * @function data․axes
   * @instance
   * @memberof Chart
   * @param {object} axes If this argument is given, the axes of data will be updated. If not given, the current axes will be returned. The format of this argument is the same as
   * @returns {object} Corresponding axes value for data, if specified axes value.
   * @example
   * // Get current axes
   * chart.data.axes();
   * // --> {data1: "y"}
   *
   * // Update axes
   * chart.data.axes({
   *  data1: "y",
   *  data2: "y2"
   * });
   */
  axes: function(e) {
    return this.internal.updateDataAttributes("axes", e);
  },
  /**
   * Get the minimum data value bound to the chart
   * @function data․min
   * @instance
   * @memberof Chart
   * @returns {Array} Data objects
   * @example
   * // Get current axes
   * chart.data.min();
   * // --> [{x: 0, value: 30, id: "data1", index: 0}, ...]
   */
  min: function() {
    return this.internal.getMinMaxData().min;
  },
  /**
   * Get the maximum data value bound to the chart
   * @function data․max
   * @instance
   * @memberof Chart
   * @returns {Array} Data objects
   * @example
   * // Get current axes
   * chart.data.max();
   * // --> [{x: 3, value: 400, id: "data1", index: 3}, ...]
   */
  max: function() {
    return this.internal.getMinMaxData().max;
  }
});
var vy = { data: sc }, xy = function(e) {
  var t;
  return (t = Me.btoa) === null || t === void 0 ? void 0 : t.call(Me, encodeURIComponent(e).replace(/%([0-9A-F]{2})/g, function(n, i) {
    return String.fromCharCode(Number("0x".concat(i)));
  }));
};
function _y(e, t, n) {
  var i = t || n, r = i.width, a = i.height, o = new XMLSerializer(), s = e.cloneNode(!0), l = w_(ai(je.styleSheets)).filter(function(x) {
    return x.cssText;
  }).map(function(x) {
    return x.cssText;
  });
  s.setAttribute("xmlns", Jn.xhtml), s.style.margin = "0", s.style.padding = "0", t.preserveFontStyle && s.querySelectorAll("text").forEach(function(x) {
    x.innerHTML = "";
  });
  var c = o.serializeToString(s), f = je.createElement("style");
  f.appendChild(je.createTextNode(l.join(`
`)));
  var h = o.serializeToString(f), g = '<svg xmlns="'.concat(Jn.svg, '" width="').concat(r, '" height="').concat(a, `" 
		viewBox="0 0 `).concat(n.width, " ").concat(n.height, `" 
		preserveAspectRatio="`).concat((t == null ? void 0 : t.preserveAspectRatio) === !1 ? "none" : "xMinYMid meet", `">
			<foreignObject width="100%" height="100%">
				`).concat(h, `
				`).concat(c.replace(/(url\()[^#]+/g, "$1"), `
			</foreignObject></svg>`);
  return "data:image/svg+xml;base64,".concat(xy(g));
}
function yy(e, t) {
  var n = t.top, i = t.left, r = e.getBBox(), a = r.x, o = r.y, s = e.getScreenCTM(), l = s.a, c = s.b, f = s.c, h = s.d, g = s.e, x = s.f, y = e.getBoundingClientRect(), T = y.width, A = y.height;
  return {
    x: l * a + f * o + g - i,
    y: c * a + h * o + x - n + (A - Math.round(A / 4)),
    width: T,
    height: A
  };
}
function my(e) {
  var t = e.getBoundingClientRect(), n = t.left, i = t.top, r = function(o) {
    return o.textContent || o.childElementCount;
  }, a = [];
  return ai(e.querySelectorAll("text")).filter(r).forEach(function(o) {
    var s = function(c) {
      var f, h = Me.getComputedStyle(c), g = h.fill, x = h.fontFamily, y = h.fontSize, T = h.textAnchor, A = h.transform, w = yy(c, { left: n, top: i }), C = w.x, R = w.y, S = w.width, I = w.height;
      return f = {}, f[c.textContent] = {
        x: C,
        y: R,
        width: S,
        height: I,
        fill: g,
        fontFamily: x,
        fontSize: y,
        textAnchor: T,
        transform: A
      }, f;
    };
    if (o.childElementCount > 1) {
      var l = [];
      return ai(o.querySelectorAll("tspan")).filter(r).forEach(function(c) {
        a.push(s(c));
      }), l;
    } else
      a.push(s(o));
  }), a;
}
function by(e, t) {
  t.forEach(function(n) {
    Object.keys(n).forEach(function(i) {
      var r = n[i], a = r.x, o = r.y, s = r.width, l = r.height, c = r.fill, f = r.fontFamily, h = r.fontSize, g = r.transform;
      if (e.save(), e.font = "".concat(h, " ").concat(f), e.fillStyle = c, g === "none")
        e.fillText(i, a, o);
      else {
        var x = g.replace(/(matrix|\(|\))/g, "").split(",");
        x.splice(4).every(function(y) {
          return +y == 0;
        }) ? (x.push(a + s - s / 4), x.push(o - l + l / 3)) : (x.push(a), x.push(o)), e.transform.apply(e, x), e.fillText(i, 0, 0);
      }
      e.restore();
    });
  });
}
var Ty = {
  /**
   * Export chart as an image.
   * - **NOTE:**
   *   - IE11 and below not work properly due to the lack of the feature(<a href="https://msdn.microsoft.com/en-us/library/hh834675(v=vs.85).aspx">foreignObject</a>) support
   *   - Every style applied to the chart & the basic CSS file(ex. billboard.css) should be at same domain as API call context to get correct styled export image.
   * @function export
   * @instance
   * @memberof Chart
   * @param {object} option Export option
   * @param {string} [option.mimeType="image/png"] The desired output image format. (ex. 'image/png' for png, 'image/jpeg' for jpeg format)
   * @param {number} [option.width={currentWidth}] width
   * @param {number} [option.height={currentHeigth}] height
   * @param {boolean} [option.preserveAspectRatio=true] Preserve aspect ratio on given size
   * @param {boolean} [option.preserveFontStyle=false] Preserve font style(font-family).<br>
   * **NOTE:**
   *   - This option is useful when outlink web font style's `font-family` are applied to chart's text element.
   *   - Text element's position(especially "transformed") can't be preserved correctly according the page's layout condition.
   *   - If need to preserve accurate text position, embed the web font data within to the page and set `preserveFontStyle=false`.
   *     - Checkout the embed example: <a href="https://stackblitz.com/edit/zfbya9-8nf9nn?file=index.html">https://stackblitz.com/edit/zfbya9-8nf9nn?file=index.html</a>
   * @param {Function} [callback] The callback to be invoked when export is ready.
   * @returns {string} dataURI
   * @example
   *  chart.export();
   *  // --> "data:image/svg+xml;base64,PHN..."
   *
   *  // Initialize the download automatically
   *  chart.export({mimeType: "image/png"}, dataUrl => {
   *     const link = document.createElement("a");
   *
   *     link.download = `${Date.now()}.png`;
   *     link.href = dataUrl;
   *     link.innerHTML = "Download chart as image";
   *
   *     document.body.appendChild(link);
   *  });
   *
   *  // Resize the exported image
   *  chart.export(
   *    {
   *      width: 800,
   *      height: 600,
   *      preserveAspectRatio: false,
   *      preserveFontStyle: false,
   *      mimeType: "image/png"
   *    },
   *    dataUrl => { ... }
   *  );
   */
  export: function(e, t) {
    var n = this, i = this.internal, r = i.state, a = i.$el, o = a.chart, s = a.svg, l = r.current, c = l.width, f = l.height, h = oi({
      width: c,
      height: f,
      preserveAspectRatio: !0,
      preserveFontStyle: !1,
      mimeType: "image/png"
    }, e), g = _y(o.node(), h, { width: c, height: f }), x = h.preserveFontStyle ? my(s.node()) : [];
    if (t && he(t)) {
      var y = new Image();
      y.crossOrigin = "Anonymous", y.onload = function() {
        var T = je.createElement("canvas"), A = T.getContext("2d");
        T.width = h.width || c, T.height = h.height || f, A.drawImage(y, 0, 0), x.length && (by(A, x), x.length = 0), t.bind(n)(T.toDataURL(h.mimeType));
      }, y.src = g;
    }
    return g;
  }
}, wy = {
  /**
   * This API highlights specified targets and fade out the others.<br><br>
   * You can specify multiple targets by giving an array that includes id as String. If no argument is given, all of targets will be highlighted.
   * @function focus
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIdsValue Target ids to be highlighted.
   * @example
   *  // data1 will be highlighted and the others will be faded out
   *  chart.focus("data1");
   *
   * // data1 and data2 will be highlighted and the others will be faded out
   * chart.focus(["data1", "data2"]);
   *
   * // all targets will be highlighted
   * chart.focus();
   */
  focus: function(e) {
    var t = this.internal, n = t.state, i = t.mapToTargetIds(e), r = t.$el.svg.selectAll(t.selectorTargets(i.filter(t.isTargetToShow, t)));
    this.revert(), this.defocus(), r.classed(ze.focused, !0).classed(ze.defocused, !1), t.hasArcType() && !n.hasRadar && (t.expandArc(i), t.hasType("gauge") && t.markOverlapped(e, t, ".".concat(qt.gaugeValue))), t.toggleFocusLegend(i, !0), n.focusedTargetIds = i, n.defocusedTargetIds = n.defocusedTargetIds.filter(function(a) {
      return i.indexOf(a) < 0;
    });
  },
  /**
   * This API fades out specified targets and reverts the others.<br><br>
   * You can specify multiple targets by giving an array that includes id as String. If no argument is given, all of targets will be faded out.
   * @function defocus
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIdsValue Target ids to be faded out.
   * @example
   * // data1 will be faded out and the others will be reverted.
   * chart.defocus("data1");
   *
   * // data1 and data2 will be faded out and the others will be reverted.
   * chart.defocus(["data1", "data2"]);
   *
   * // all targets will be faded out.
   * chart.defocus();
   */
  defocus: function(e) {
    var t = this.internal, n = t.state, i = t.mapToTargetIds(e), r = t.$el.svg.selectAll(t.selectorTargets(i.filter(t.isTargetToShow, t)));
    r.classed(ze.focused, !1).classed(ze.defocused, !0), t.hasArcType(null, ["polar"]) && (t.unexpandArc(i), t.hasType("gauge") && t.undoMarkOverlapped(t, ".".concat(qt.gaugeValue))), t.toggleFocusLegend(i, !1), n.focusedTargetIds = n.focusedTargetIds.filter(function(a) {
      return i.indexOf(a) < 0;
    }), n.defocusedTargetIds = i;
  },
  /**
   * Revert focused or defocused state to initial state.<br><br>
   * You can specify multiple targets by giving an array that includes id as string. If no argument is given, all of targets will be reverted.
   * @function revert
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIdsValue Target ids to be reverted
   * @example
   * // 'data1' will be reverted.
   * chart.revert("data1");
   *
   * // 'data1' and 'data2' will be reverted.
   * chart.revert(["data1", "data2"]);
   *
   * // all targets will be reverted.
   * chart.revert();
   */
  revert: function(e) {
    var t = this.internal, n = t.config, i = t.state, r = t.$el, a = t.mapToTargetIds(e), o = r.svg.selectAll(t.selectorTargets(a));
    o.classed(ze.focused, !1).classed(ze.defocused, !1), t.hasArcType(null, ["polar"]) && t.unexpandArc(a), n.legend_show && (t.showLegend(a.filter(t.isLegendToShow.bind(t))), r.legend.selectAll(t.selectorLegends(a)).filter(function() {
      return le(this).classed(ze.legendItemFocused);
    }).classed(ze.legendItemFocused, !1)), i.focusedTargetIds = [], i.defocusedTargetIds = [];
  }
}, $y = {
  /**
   * Show legend for each target.
   * - **NOTE:** Legend APIs aren't supported for `treemap` type.
   * @function legend․show
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIds
   * - If targetIds is given, specified target's legend will be shown.
   * - If only one target is the candidate, String can be passed.
   * - If no argument is given, all of target's legend will be shown.
   * @example
   * // Show legend for data1.
   * chart.legend.show("data1");
   *
   * // Show legend for data1 and data2.
   * chart.legend.show(["data1", "data2"]);
   *
   * // Show all legend.
   * chart.legend.show();
   */
  show: function(e) {
    var t = this.internal;
    t.showLegend(t.mapToTargetIds(e)), t.updateAndRedraw({ withLegend: !0 });
  },
  /**
   * Hide legend for each target.
   * @function legend․hide
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIds
   * - If targetIds is given, specified target's legend will be hidden.
   * - If only one target is the candidate, String can be passed.
   * - If no argument is given, all of target's legend will be hidden.
   * @example
   * // Hide legend for data1.
   * chart.legend.hide("data1");
   *
   * // Hide legend for data1 and data2.
   * chart.legend.hide(["data1", "data2"]);
   *
   * // Hide all legend.
   * chart.legend.hide();
   */
  hide: function(e) {
    var t = this.internal;
    t.hideLegend(t.mapToTargetIds(e)), t.updateAndRedraw({ withLegend: !0 });
  }
}, Ay = { legend: $y }, Cy = {
  /**
   * Load data to the chart.<br><br>
   * You can specify multiple targets by giving an array that includes id as String. If no argument is given, all of targets will be toggles.
   * - <b>Note:</b>
   *   - unload should be used if some data needs to be unloaded simultaneously.
   *     If you call unload API soon after/before load instead of unload param, chart will not be rendered properly because of cancel of animation.<br>
   *   - done will be called after data loaded, but it's not after rendering.
   *     It's because rendering will finish after some transition and there is some time lag between loading and rendering
   * @function load
   * @instance
   * @memberof Chart
   * @param {object} args The object can consist with following members:<br>
   *
   *    | Key | Type | Description |
   *    | --- | --- | --- |
   *    | columns | Array | The `columns` data will be loaded. If data that has the same target id is given, the chart will be updated. Otherwise, new target will be added |
   *    | json | Array | The `json` data will be loaded. If data that has the same target id is given, the chart will be updated. Otherwise, new target will be added |
   *    | rows | Array | The `rows` data will be loaded. If data that has the same target id is given, the chart will be updated. Otherwise, new target will be added |
   *    | url | string | The data from `url` will be loaded. If data that has the same target id is given, the chart will be updated. Otherwise, new target will be added |
   *    | &nbsp; | | |
   *    | append | boolean | Load data appending it to the current dataseries.<br>If the existing chart has`x` value, should provide with corresponding `x` value for newly loaded data.  |
   *    | axes | Object | The axes specified by data.axes will be updated. axes must be Object that has target id as keys. |
   *    | categories | Array | The categories specified by axis.x.categories or data.x will be updated. categories must be Array. |
   *    | classes | Object | The classes specified by data.classes will be updated. classes must be Object that has target id as keys. |
   *    | colors | Object | The colors specified by data.colors will be updated. colors must be Object that has target id as keys. |
   *    | data | Obejct | Data objects to be loaded. Checkout the example. |
   *    | done | Function | The specified function will be called after data loaded.|
   *    | headers | string |  Set request header if loading via `data.url`.<br>@see [data․headers](Options.html#.data%25E2%2580%25A4headers) |
   *    | keys | Object |  Choose which JSON objects keys correspond to desired data.<br>**NOTE:** Only for JSON object given as array.<br>@see [data․keys](Options.html#.data%25E2%2580%25A4keys) |
   *    | mimeType | string |  Set 'json' if loading JSON via url.<br>@see [data․mimeType](Options.html#.data%25E2%2580%25A4mimeType) |
   *    | names | Object | Same as data.names() |
   *    | resizeAfter | boolean | Resize after the load. Default value is `false`.<br>- This option won't call `onresize` neither `onresized`.<br>- When set to 'true', will call `.flush(true)` at the end of load. |
   *    | type | string | The type of targets will be updated. |
   *    | types | Object | The types of targets will be updated. |
   *    | unload | Array | Specify the data will be unloaded before loading new data. If true given, all of data will be unloaded. If target ids given as String or Array, specified targets will be unloaded. If absent or false given, unload will not occur. |
   *    | xs | string | Same as data.xs option  |
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataFromURL)
   * @example
   * // Load data1 and unload data2 and data3
   * chart.load({
   *     columns: [
   *        ["data1", 100, 200, 150, ...],
   *        ...
   *    ],
   *    unload: ["data2", "data3"],
   *    url: "...",
   *    done: function() { ... }
   *    resizeAfter: true  // will resize after load
   * });
   * @example
   * const chart = bb.generate({
   *   data: {
   *     columns: [
   *       ["data1", 20, 30, 40]
   *     ]
   *   }
   * });
   *
   * chart.load({
   *    columns: [
   *        // with 'append' option, the 'data1' will have `[20,30,40,50,60]`.
   *        ["data1", 50, 60]
   *    ],
   *    append: true
   * });
   * @example
   * const chart = bb.generate({
   *   data: {
   *     x: "x",
   *     xFormat: "%Y-%m-%dT%H:%M:%S",
   *     columns: [
   *       ["x", "2021-01-03T03:00:00", "2021-01-04T12:00:00", "2021-01-05T21:00:00"],
   *       ["data1", 36, 30, 24]
   *     ]
   *   },
   *   axis: {
   *     x: {
   *       type: "timeseries"
   *     }
   *   }
   * };
   *
   * chart.load({
   *   columns: [
   *     // when existing chart has `x` value, should provide correponding 'x' value.
   *     // with 'append' option, the 'data1' will have `[36,30,24,37]`.
   *     ["x", "2021-02-01T08:00:00"],
   *     ["data1", 37]
   *   ],
   *   append: true
   * });
   * @example
   * // myAPI.json
   * // {
   * //   "data1": [220, 240, 270, 250, 280],
   * //   "data2": [180, 150, 300, 70, 120]
   * // }
   *
   * chart.load({
   *     url: './data/myAPI.json',
   *     mimeType: "json",
   *
   *     // set request header if is needed
   *     headers: {
   *       "Content-Type": "text/json"
   *     }
   * });
   * @example
   * chart.load({
   *     data: [
   *       // equivalent as: columns: [["data1", 30, 200, 100]]
   *       {"data1": 30}, {"data1": 200}, {"data1": 100}
   *
   *       // or
   *       // equivalent as: columns: [["data1", 10, 20], ["data2", 13, 30]]
   *       // {"data1": 10, "data2": 13}, {"data1": 20, "data2": 30}}
   *     ]
   * });
   * @example
   * chart.load({
   *     json: [
   *          {name: "www.site1.com", upload: 800, download: 500, total: 400},
   *     ],
   *     keys: {
   *         x: "name",
   *         value: ["upload", "download"]
   *     }
   * });
   * @example
   * chart.load({
   *   json: {
   *       data1:[30, 20, 50, 40, 60, 50],
   *       data2:[200, 130, 90, 240, 130, 220],
   *   }
   * });
   */
  load: function(e) {
    var t = this.internal, n = t.config;
    e.xs && t.addXs(e.xs), "names" in e && this.data.names(e.names), "classes" in e && Object.keys(e.classes).forEach(function(i) {
      n.data_classes[i] = e.classes[i];
    }), "categories" in e && t.axis.isCategorized() && (n.axis_x_categories = e.categories), "axes" in e && Object.keys(e.axes).forEach(function(i) {
      n.data_axes[i] = e.axes[i];
    }), "colors" in e && Object.keys(e.colors).forEach(function(i) {
      n.data_colors[i] = e.colors[i];
    }), "unload" in e && e.unload !== !1 ? t.unload(t.mapToTargetIds(e.unload === !0 ? null : e.unload), function() {
      Ql(function() {
        return t.loadFromArgs(e);
      });
    }) : t.loadFromArgs(e);
  },
  /**
   * Unload data to the chart.<br><br>
   * You can specify multiple targets by giving an array that includes id as String. If no argument is given, all of targets will be toggles.
   * - <b>Note:</b>
   * If you call load API soon after/before unload, unload param of load should be used. Otherwise chart will not be rendered properly because of cancel of animation.<br>
   * `done` will be called after data loaded, but it's not after rendering. It's because rendering will finish after some transition and there is some time lag between loading and rendering.
   * @function unload
   * @instance
   * @memberof Chart
   * @param {object} argsValue
   *  | key | Type | Description |
   *  | --- | --- | --- |
   *  | ids | String &vert; Array | Target id data to be unloaded. If not given, all data will be unloaded. |
   *  | done | Fuction | Callback after data is unloaded. |
   *  | resizeAfter | boolean | Resize after the unload. Default value is `false`.<br>- This option won't call `onresize` neither `onresized`.<br>- When set to 'true', will call `.flush(true)` at the end of unload. |
   * @example
   *  // Unload data2 and data3
   *  chart.unload({
   *    ids: ["data2", "data3"],
   *    done: function() {
   *       // called after the unloaded
   *    },
   *    resizeAfter: true  // will resize after unload
   *  });
   */
  unload: function(e) {
    var t = this.internal, n = e || {};
    en(n) && this.tooltip.hide(), Ie(n) ? n = { ids: n } : Ne(n) && (n = { ids: [n] });
    var i = t.mapToTargetIds(n.ids);
    t.unload(i, function() {
      t.redraw({
        withUpdateOrgXDomain: !0,
        withUpdateXDomain: !0,
        withLegend: !0
      }), t.cache.remove(i), oc.call(t, n.done, n.resizeAfter);
    });
  }
};
function Lu(e, t, n) {
  var i = this, r = this.internal, a = r.mapToTargetIds(t), o = r.state.hiddenTargetIds.map(function(c) {
    return a.indexOf(c) > -1 && c;
  }).filter(Boolean);
  r.state.toggling = !0, r["".concat(e ? "remove" : "add", "HiddenTargetIds")](a);
  var s = r.$el.svg.selectAll(r.selectorTargets(a)), l = e ? null : "0";
  e && o.length && (s.style("display", null), Ze(r.config.data_onshown, this, o)), r.$T(s).style("opacity", l, "important").call(Ir, function() {
    var c;
    !e && o.length === 0 && (s.style("display", "none"), Ze((c = r.config) === null || c === void 0 ? void 0 : c.data_onhidden, i, a)), s.style("opacity", l);
  }), n.withLegend && r["".concat(e ? "show" : "hide", "Legend")](a), r.redraw({
    withUpdateOrgXDomain: !0,
    withUpdateXDomain: !0,
    withLegend: !0
  }), r.state.toggling = !1;
}
var Sy = {
  /**
   * Show data series on chart
   * @function show
   * @instance
   * @memberof Chart
   * @param {string|Array} [targetIdsValue] The target id value.
   * @param {object} [options] The object can consist with following members:<br>
   *
   *    | Key | Type | default | Description |
   *    | --- | --- | --- | --- |
   *    | withLegend | boolean | false | whether or not display legend |
   *
   * @example
   * // show 'data1'
   * chart.show("data1");
   *
   * // show 'data1' and 'data3'
   * chart.show(["data1", "data3"]);
   */
  show: function(e, t) {
    t === void 0 && (t = {}), Lu.call(this, !0, e, t);
  },
  /**
   * Hide data series from chart
   * @function hide
   * @instance
   * @memberof Chart
   * @param {string|Array} [targetIdsValue] The target id value.
   * @param {object} [options] The object can consist with following members:<br>
   *
   *    | Key | Type | default | Description |
   *    | --- | --- | --- | --- |
   *    | withLegend | boolean | false | whether or not display legend |
   *
   * @example
   * // hide 'data1'
   * chart.hide("data1");
   *
   * // hide 'data1' and 'data3'
   * chart.hide(["data1", "data3"]);
   */
  hide: function(e, t) {
    t === void 0 && (t = {}), Lu.call(this, !1, e, t);
  },
  /**
   * Toggle data series on chart. When target data is hidden, it will show. If is shown, it will hide in vice versa.
   * @function toggle
   * @instance
   * @memberof Chart
   * @param {string|Array} [targetIds] The target id value.
   * @param {object} [options] The object can consist with following members:<br>
   *
   *    | Key | Type | default | Description |
   *    | --- | --- | --- | --- |
   *    | withLegend | boolean | false | whether or not display legend |
   *
   * @example
   * // toggle 'data1'
   * chart.toggle("data1");
   *
   * // toggle 'data1' and 'data3'
   * chart.toggle(["data1", "data3"]);
   */
  toggle: function(e, t) {
    var n = this;
    t === void 0 && (t = {});
    var i = this.internal, r = { show: [], hide: [] };
    i.mapToTargetIds(e).forEach(function(a) {
      return r[i.isTargetToShow(a) ? "hide" : "show"].push(a);
    }), r.show.length && this.show(r.show, t), r.hide.length && setTimeout(function() {
      return n.hide(r.hide, t);
    }, 0);
  }
}, ky = {
  /**
   * Show tooltip
   * @function tooltip․show
   * @instance
   * @memberof Chart
   * @param {object} args The object can consist with following members:<br>
   *
   *    | Key | Type | Description |
   *    | --- | --- | --- |
   *    | index | Number | Determine focus by index |
   *    | x | Number &vert; Date | Determine focus by x Axis index |
   *    | mouse | Array | Determine x and y coordinate value relative the targeted '.bb-event-rect' x Axis.<br>It should be used along with `data`, `index` or `x` value. The default value is set as `[0,0]` |
   *    | data | Object | When [data.xs](Options.html#.data%25E2%2580%25A4xs) option is used or [tooltip.grouped](Options.html#.tooltip) set to 'false', `should be used giving this param`.<br><br>**Key:**<br>- x {number &verbar; Date}: x Axis value<br>- index {number}: x Axis index (useless for data.xs)<br>- id {string}: data id<br>- value {number}: The corresponding value for tooltip. |
   *
   * @example
   *  // show the 2nd x Axis coordinate tooltip
   *  // for Arc(gauge, donut & pie) and radar type, approch showing tooltip by using "index" number.
   *  chart.tooltip.show({
   *    index: 1
   *  });
   *
   *  // show tooltip for the 3rd x Axis in x:50 and y:100 coordinate of '.bb-event-rect' of the x Axis.
   *  chart.tooltip.show({
   *    x: 2,
   *    mouse: [50, 100]
   *  });
   *
   *  // show tooltip for timeseries x axis
   *  chart.tooltip.show({
   *    x: new Date("2018-01-02 00:00")
   *  });
   *
   *  // treemap type can be shown by using "id" only.
   *  chart.tooltip.show({
   *    data: {
   *        id: "data1"  // data id
   *    }
   *  });
   *
   *  // for Arc types, specify 'id' or 'index'
   *  chart.tooltip.show({ data: { id: "data2" }});
   *  chart.tooltip.show({ data: { index: 2 }});
   *
   *  // when data.xs is used
   *  chart.tooltip.show({
   *    data: {
   *        x: 3,  // x Axis value
   *        id: "data1",  // data id
   *        value: 500  // data value
   *    }
   *  });
   *
   *  // when data.xs isn't used, but tooltip.grouped=false is set
   *  chart.tooltip.show({
   *    data: {
   *        index: 3,  // or 'x' key value
   *        id: "data1",  // data id
   *        value: 500  // data value
   *    }
   *  });
   */
  show: function(e) {
    var t, n, i, r = this.internal, a = r.$el, o = r.config, s = r.state, l = s.eventReceiver, c = s.hasFunnel, f = s.hasTreemap, h = s.inputType, g, x;
    if (e.mouse && (x = e.mouse), e.data) {
      var y = e.data, T = (t = r.getYScaleById(y.id)) === null || t === void 0 ? void 0 : t(y.value);
      if ((c || f) && y.id) {
        var A = r.selectorTarget(y.id, void 0, ".".concat(ct.shape));
        l.rect = a.main.select(A);
      } else
        r.isMultipleX() ? x = [r.xx(y), T] : (o.tooltip_grouped || (x = [0, T]), g = (n = y.index) !== null && n !== void 0 ? n : r.hasArcType() && y.id ? (i = r.getArcElementByIdOrIndex(y.id)) === null || i === void 0 ? void 0 : i.datum().index : r.getIndexByX(y.x));
    } else
      Ye(e.x) ? g = r.getIndexByX(e.x) : Ye(e.index) && (g = e.index);
    (h === "mouse" ? ["mouseover", "mousemove"] : ["touchstart"]).forEach(function(w) {
      r.dispatchEvent(w, g, x);
    });
  },
  /**
   * Hide tooltip
   * @function tooltip․hide
   * @instance
   * @memberof Chart
   */
  hide: function() {
    var e, t, n, i = this.internal, r = i.state.inputType, a = i.$el.tooltip, o = a == null ? void 0 : a.datum();
    if (o) {
      var s = JSON.parse(o.current)[0].index;
      (r === "mouse" ? ["mouseout"] : ["touchend"]).forEach(function(l) {
        i.dispatchEvent(l, s);
      });
    }
    r === "touch" && i.callOverOutForTouch(), i.hideTooltip(!0), (e = i.hideGridFocus) === null || e === void 0 || e.call(i), (t = i.unexpandCircles) === null || t === void 0 || t.call(i), (n = i.expandBarTypeShapes) === null || n === void 0 || n.call(i, !1);
  }
}, Ey = { tooltip: ky }, Ho = (
  /** @class */
  function() {
    function e(t) {
      this.plugins = [];
      var n = new qr(this);
      this.internal = n, function i(r, a, o) {
        Object.keys(r).forEach(function(s) {
          var l = he(r[s]), c = a !== o, f = Qe(r[s]), h = f && Object.keys(r[s]).length > 0;
          l && (!c && h || c) ? a[s] = r[s].bind(o) : f && !l ? a[s] = {} : a[s] = r[s], h && i(r[s], a[s], o);
        });
      }(e.prototype, this, this), hy.call(n, t), n.beforeInit(), n.init();
    }
    return e;
  }()
);
Dt(Ho.prototype, [
  gy,
  py,
  vy,
  Ty,
  wy,
  Ay,
  Cy,
  Sy,
  Ey
]);
function Du(e, t, n) {
  var i = e.config, r = function(a, o) {
    var s = ge(o) ? o : o === !1 ? void 0 : null;
    s !== null && (i["axis_".concat(a, "_").concat(t)] = s);
  };
  Ye(n) && (Jt(n) ? Object.keys(n).forEach(function(a) {
    r(a, n[a]);
  }) : (ge(n) || n === !1) && ["y", "y2"].forEach(function(a) {
    r(a, n);
  }), e.redraw({
    withUpdateOrgXDomain: !0,
    withUpdateXDomain: !0
  }));
}
function Iu(e, t) {
  var n = e.config;
  return {
    x: n["axis_x_".concat(t)],
    y: n["axis_y_".concat(t)],
    y2: n["axis_y2_".concat(t)]
  };
}
var Ry = {
  /**
   * Get and set axis labels.
   * - **NOTE:** Only applicable for chart types which has x and y axes.
   * @function axis․labels
   * @instance
   * @memberof Chart
   * @param {object} labels specified axis' label to be updated.
   * @param {string} [labels.x] x Axis string
   * @param {string} [labels.y] y Axis string
   * @param {string} [labels.y2] y2 Axis string
   * @returns {object|undefined} axis labels text object
   * @example
   * // Update axis' label
   * chart.axis.labels({
   *   x: "New X Axis Label",
   *   y: "New Y Axis Label",
   *   y2: "New Y2 Axis Label"
   * });
   *
   * chart.axis.labels();
   * // --> {
   * //  x: "New X Axis Label",
   * //  y: "New Y Axis Label",
   * //  y2: "New Y2 Axis Label"
   * // }
   */
  labels: function(e) {
    var t = this.internal, n;
    return e && (Object.keys(e).forEach(function(i) {
      t.axis.setLabelText(i, e[i]);
    }), t.axis.updateLabels()), ["x", "y", "y2"].forEach(function(i) {
      var r = t.axis.getLabelText(i);
      r && (!n && (n = {}), n[i] = r);
    }), n;
  },
  /**
   * Get and set axis min value.
   * - **NOTE:** Only applicable for chart types which has x and y axes.
   * @function axis․min
   * @instance
   * @memberof Chart
   * @param {object} min If min is given, specified axis' min value will be updated.<br>
   *   If no argument is given, the min values set on generating option for each axis will be returned.
   *   If not set any min values on generation, it will return `undefined`.<br>
   *   To unset specific axis max, set `false` to each of them.
   * @returns {object|undefined}
   * @example
   * // Update axis' min
   * chart.axis.min({
   *   x: -10,
   *   y: 1000,
   *   y2: 100
   * });
   *
   * // To unset specific axis min, set false to each of them.
   * chart.axis.min({
   *   x: false,
   *   y: false,
   *   y2: false
   * });
   *
   * // shorthand (only affects y and y2 axis)
   * chart.axis.min(-50);
   * chart.axis.min(false);
   */
  min: function(e) {
    var t = this.internal;
    return Ae(e) || e === !1 ? Du(t, "min", e) : Iu(t, "min");
  },
  /**
   * Get and set axis max value.
   * - **NOTE:** Only applicable for chart types which has x and y axes.
   * @function axis․max
   * @instance
   * @memberof Chart
   * @param {object} max If max is given, specified axis' max value will be updated.<br>
   *   If no argument is given, the max values set on generating option for each axis will be returned.
   *   If not set any max values on generation, it will return `undefined`.<br>
   *   To unset specific axis max, set `false` to each of them.
   * @returns {object|undefined}
   * @example
   * // Update axis' label
   * chart.axis.max({
   *    x: 100,
   *    y: 1000,
   *    y2: 10000
   * });
   *
   * // To unset specific axis max, set false to each of them.
   * chart.axis.max({
   *   x: false,
   *   y: false,
   *   y2: false
   * });
   *
   * // shorthand (only affects y and y2 axis)
   * chart.axis.max(10);
   * chart.axis.max(false);
   */
  max: function(e) {
    var t = this.internal;
    return Ae(e) || e === !1 ? Du(t, "max", e) : Iu(t, "max");
  },
  /**
   * Get and set axis min and max value.
   * - **NOTE:** Only applicable for chart types which has x and y axes.
   * @function axis․range
   * @instance
   * @memberof Chart
   * @param {object} range If range is given, specified axis' min and max value will be updated.
   *   If no argument is given, the current min and max values for each axis will be returned.<br>
   *   To unset specific axis max, set `false` to each of them.
   * @returns {object|undefined}
   * @example
   * // Update axis' label
   * chart.axis.range({
   *   min: {
   *     x: -10,
   *     y: -1000,
   *     y2: -10000
   *   },
   *   max: {
   *     x: 100,
   *     y: 1000,
   *     y2: 10000
   *   },
   * });
   *
   * // To unset specific axis max, set false to each of them.
   * chart.axis.range({
   *   min: {
   *     x: false,
   *     y: false,
   *     y2: false
   *   },
   *   max: {
   *     x: false,
   *     y: false,
   *     y2: false
   *   },
   * });
   *
   * // shorthand (only affects y and y2 axis)
   * chart.axis.range({ min: -50, max: 1000 });
   * chart.axis.range({ min: false, max: false });
   */
  range: function(e) {
    var t = this.axis;
    if (arguments.length) {
      var n = e.min, i = e.max;
      Ye(i) && t.max(i), Ye(n) && t.min(n);
    } else
      return {
        max: t.max(),
        min: t.min()
      };
  }
}, My = { axis: Ry }, Ly = {
  /**
   * Set specified category name on category axis.
   * @function category
   * @instance
   * @memberof Chart
   * @param {number} i index of category to be changed
   * @param {string} category category value to be changed
   * @returns {string}
   * @example
   * chart.category(2, "Category 3");
   */
  category: function(e, t) {
    var n = this.internal, i = n.config;
    return arguments.length > 1 && (i.axis_x_categories[e] = t, n.redraw()), i.axis_x_categories[e];
  },
  /**
   * Set or get category names on category axis.
   * @function categories
   * @instance
   * @memberof Chart
   * @param {Array} categories This must be an array that includes category names in string. If category names are included in the date by data.x option, this is not required.
   * @returns {Array}
   * @example
   * chart.categories([
   *      "Category 1", "Category 2", ...
   * ]);
   */
  categories: function(e) {
    var t = this.internal, n = t.config;
    if (!e || !Array.isArray(e)) {
      var i = n.axis_x_categories;
      return en(i) ? Object.values(t.data.xs)[0] : i;
    }
    return n.axis_x_categories = e, t.redraw(), n.axis_x_categories;
  }
}, Dy = {
  /**
   * Flow data to the chart.<br><br>
   * By this API, you can append new data points to the chart.
   * @function flow
   * @instance
   * @memberof Chart
   * @param {object} args The object can consist with following members:<br>
   *
   *    | Key | Type | Description |
   *    | --- | --- | --- |
   *    | json | Object | Data as JSON format (@see [data․json](Options.html#.data%25E2%2580%25A4json)) |
   *    | rows | Array | Data in array as row format (@see [data․rows](Options.html#.data%25E2%2580%25A4json)) |
   *    | columns | Array | Data in array as column format (@see [data․columns](Options.html#.data%25E2%2580%25A4columns)) |
   *    | to | String | The lower x edge will move to that point. If not given, the lower x edge will move by the number of given data points |
   *    | length | Number | The lower x edge will move by the number of this argument |
   *    | duration | Number | The duration of the transition will be specified value. If not given, transition.duration will be used as default |
   *    | done | Function | The specified function will be called when flow ends |
   *
   * - **NOTE:**
   *   - If json, rows and columns given, the data will be loaded.
   *   - If data that has the same target id is given, the chart will be appended.
   *   - Otherwise, new target will be added. One of these is required when calling.
   *   - If json specified, keys is required as well as data.json.
   * 	 - If tab isn't visible(by evaluating `document.hidden`), will not be executed to prevent unnecessary work.
   * @example
   * // 2 data points will be apprended to the tail and popped from the head.
   * // After that, 4 data points will be appended and no data points will be poppoed.
   * chart.flow({
   *  columns: [
   *    ["x", "2018-01-11", "2018-01-21"],
   *    ["data1", 500, 200],
   *    ["data2", 100, 300],
   *    ["data3", 200, 120]
   *  ],
   *  to: "2013-01-11",
   *  done: function () {
   *    chart.flow({
   *      columns: [
   *        ["x", "2018-02-11", "2018-02-12", "2018-02-13", "2018-02-14"],
   *        ["data1", 200, 300, 100, 250],
   *        ["data2", 100, 90, 40, 120],
   *        ["data3", 100, 100, 300, 500]
   *      ],
   *      length: 2,
   *      duration: 1500
   *    });
   *  }
   * });
   */
  flow: function(e) {
    var t = this.internal, n;
    (e.json || e.rows || e.columns) && t.convertData(e, function(r) {
      n = r, i();
    });
    function i() {
      var r, a = 0, o = 0, s, l;
      if (!(t.state.redrawing || !n || !Hi())) {
        var c = [], f = t.getMaxDataCount(), h = t.convertDataToTargets(n, !0), g = t.axis.isTimeSeries();
        t.data.targets.forEach(function(T) {
          for (var A = !1, w = 0; w < h.length; w++)
            if (T.id === h[w].id) {
              A = !0, T.values[T.values.length - 1] && (o = T.values[T.values.length - 1].index + 1), a = h[w].values.length;
              for (var C = 0; C < a; C++)
                h[w].values[C].index = o + C, g || (h[w].values[C].x = o + C);
              T.values = T.values.concat(h[w].values), h.splice(w, 1);
              break;
            }
          !A && c.push(T.id);
        }), t.data.targets.forEach(function(T) {
          for (var A = 0; A < c.length; A++)
            if (T.id === c[A]) {
              o = T.values[T.values.length - 1].index + 1;
              for (var w = 0; w < a; w++)
                T.values.push({
                  id: T.id,
                  index: o + w,
                  x: g ? t.getOtherTargetX(o + w) : o + w,
                  value: null
                });
            }
        }), t.data.targets.length && h.forEach(function(T) {
          for (var A = [], w = t.data.targets[0].values[0].index; w < o; w++)
            A.push({
              id: T.id,
              index: w,
              x: g ? t.getOtherTargetX(w) : w,
              value: null
            });
          T.values.forEach(function(C) {
            C.index += o, g || (C.x += o);
          }), T.values = A.concat(T.values);
        }), t.data.targets = t.data.targets.concat(h);
        var x = t.data.targets[0], y = x.values[0];
        Ye(e.to) ? (a = 0, l = g ? kt.call(t, e.to) : e.to, x.values.forEach(function(T) {
          T.x < l && a++;
        })) : Ye(e.length) && (a = e.length), f ? f === 1 && g && (s = (x.values[x.values.length - 1].x - y.x) / 2, r = [new Date(+y.x - s), new Date(+y.x + s)]) : (g ? s = x.values.length > 1 ? x.values[x.values.length - 1].x - y.x : y.x - t.getXDomain(t.data.targets)[0] : s = 1, r = [y.x - s, y.x]), r && t.updateXDomain(null, !0, !0, !1, r), t.updateTargets(t.data.targets), t.redraw({
          flow: {
            index: y.index,
            length: a,
            duration: Ae(e.duration) ? e.duration : t.config.transition_duration,
            done: e.done,
            orgDataCount: f
          },
          withLegend: !0,
          withTransition: f > 1,
          withTrimXDomain: !1,
          withUpdateXAxis: !0
        });
      }
    }
  }
};
function Vo(e, t) {
  var n = this.internal, i = n.config, r = i.transition_duration && Hi(), a = "grid_".concat(t, "_lines");
  return e && (i[a] = e, n.updateGrid(), n.redrawGrid(r)), i[a];
}
function uc(e, t) {
  var n = "grid_".concat(t, "_lines");
  return Vo.bind(this)(this.internal.config[n].concat(e || []), t);
}
function lc(e, t) {
  this.internal.removeGridLines(e, t);
}
var cc = function(e) {
  return Vo.bind(this)(e, "x");
};
Dt(cc, {
  /**
   * Add x grid lines.<br>
   * This API adds new x grid lines instead of replacing like xgrids.
   * @function xgrids․add
   * @instance
   * @memberof Chart
   * @param {Array|object} grids New x grid lines will be added. The format of this argument is the same as grid.x.lines and it's possible to give an Object if only one line will be added.
   * @returns {Array}
   * @example
   *  // Add a new x grid line
   * chart.xgrids.add(
   *   {value: 4, text: "Label 4"}
   * );
   *
   * // Add new x grid lines
   * chart.xgrids.add([
   *   {value: 2, text: "Label 2"},
   *   {value: 4, text: "Label 4"}
   * ]);
   */
  add: function(e) {
    return uc.bind(this)(e, "x");
  },
  /**
   * Remove x grid lines.<br>
   * This API removes x grid lines.
   * @function xgrids․remove
   * @instance
   * @memberof Chart
   * @param {object} grids This argument should include value or class. If value is given, the x grid lines that have specified x value will be removed. If class is given, the x grid lines that have specified class will be removed. If args is not given, all of x grid lines will be removed.
   * @param {number} [grids.value] target value
   * @param {string} [grids.class] target class
   * @returns {void}
   * @example
   * // x grid line on x = 2 will be removed
   * chart.xgrids.remove({value: 2});
   *
   * // x grid lines that have 'grid-A' will be removed
   * chart.xgrids.remove({
   *   class: "grid-A"
   * });
   *
   * // all of x grid lines will be removed
   * chart.xgrids.remove();
   */
  remove: function(e) {
    return lc.bind(this)(e, !0);
  }
});
var fc = function(e) {
  return Vo.bind(this)(e, "y");
};
Dt(fc, {
  /**
   * Add y grid lines.<br>
   * This API adds new y grid lines instead of replacing like ygrids.
   * @function ygrids․add
   * @instance
   * @memberof Chart
   * @param {Array|object} grids New y grid lines will be added. The format of this argument is the same as grid.y.lines and it's possible to give an Object if only one line will be added.
   * @returns {object}
   * @example
   *  // Add a new x grid line
   * chart.ygrids.add(
   *   {value: 400, text: "Label 4"}
   * );
   *
   * // Add new x grid lines
   * chart.ygrids.add([
   *   {value: 200, text: "Label 2"},
   *   {value: 400, text: "Label 4"}
   * ]);
   */
  add: function(e) {
    return uc.bind(this)(e, "y");
  },
  /**
   * Remove y grid lines.<br>
   * This API removes x grid lines.
   * @function ygrids․remove
   * @instance
   * @memberof Chart
   * @param {object} grids This argument should include value or class. If value is given, the y grid lines that have specified y value will be removed. If class is given, the y grid lines that have specified class will be removed. If args is not given, all of y grid lines will be removed.
   * @param {number} [grids.value] target value
   * @param {string} [grids.class] target class
   * @returns {void}
   * @example
   * // y grid line on y = 200 will be removed
   * chart.ygrids.remove({value: 200});
   *
   * // y grid lines that have 'grid-A' will be removed
   * chart.ygrids.remove({
   *   class: "grid-A"
   * });
   *
   * // all of y grid lines will be removed
   * chart.ygrids.remove();
   */
  remove: function(e) {
    return lc.bind(this)(e, !1);
  }
});
var Iy = { xgrids: cc, ygrids: fc }, Ny = {
  /**
   * Update groups for the targets.
   * @function groups
   * @instance
   * @memberof Chart
   * @param {Array} groups This argument needs to be an Array that includes one or more Array that includes target ids to be grouped.
   * @returns {Array} Grouped data names array
   * @example
   *  // data1 and data2 will be a new group.
   *  chart.groups([
   *     ["data1", "data2"]
   *  ]);
   */
  groups: function(e) {
    var t = this.internal, n = t.config;
    return at(e) || (n.data_groups = e, t.redraw()), n.data_groups;
  }
};
function dc(e, t) {
  t === void 0 && (t = !1);
  var n = this.internal, i = n.config, r = i.transition_duration && Hi();
  return e ? (i.regions = t ? i.regions.concat(e) : e, n.updateRegion(), n.redrawRegion(r), t ? i.regions : e) : i.regions;
}
var hc = function(e) {
  return dc.bind(this)(e);
};
Dt(hc, {
  /**
   * Add new region.<br><br>
   * This API adds new region instead of replacing like regions.
   * @function regions․add
   * @instance
   * @memberof Chart
   * @param {Array|object} regions New region will be added. The format of this argument is the same as regions and it's possible to give an Object if only one region will be added.
   * @returns {Array} regions
   * @example
   * // Add a new region
   * chart.regions.add(
   *    {
   *      axis: "x", start: 5, class: "regionX",
   *      label: {
   *      	text: "Region Text",
   *      	color: "red"  // color string
   *      }
   *    }
   * );
   *
   * // Add new regions
   * chart.regions.add([
   *    {axis: "x", start: 5, class: "regionX"},
   *    {
   *      axis: "y", end: 50, class: "regionY",
   *      label: {
   *      	text: "Region Text",
   *      	x: 5,  // position relative of the initial x coordinate
   *      	y: 5,  // position relative of the initial y coordinate
   *      	color: "red",  // color string
   *      	rotated: true  // make text to show in vertical or horizontal
   *      }
   *    }
   * ]);
   */
  add: function(e) {
    return dc.bind(this)(e, !0);
  },
  /**
   * Remove regions.<br><br>
   * This API removes regions.
   * @function regions․remove
   * @instance
   * @memberof Chart
   * @param {object} optionsValue This argument should include classes. If classes is given, the regions that have one of the specified classes will be removed. If args is not given, all of regions will be removed.
   * @returns {Array} regions Removed regions
   * @example
   * // regions that have 'region-A' or 'region-B' will be removed.
   * chart.regions.remove({
   *   classes: [
   *     "region-A", "region-B"
   *   ]
   * });
   *
   * // all of regions will be removed.
   * chart.regions.remove();
   */
  remove: function(e) {
    var t = this.internal, n = t.config, i = t.$T, r = e || {}, a = mn(r, "classes", [Fi.region]), o = t.$el.main.select(".".concat(Fi.regions)).selectAll(a.map(function(s) {
      return ".".concat(s);
    }));
    return i(o).style("opacity", "0").remove(), o = n.regions, Object.keys(r).length ? (o = o.filter(function(s) {
      var l = !1;
      return s.class ? (s.class.split(" ").forEach(function(c) {
        a.indexOf(c) >= 0 && (l = !0);
      }), !l) : !0;
    }), n.regions = o) : n.regions = [], o;
  }
});
var Py = { regions: hc }, Oy = {
  /**
   * Get and set x values for the chart.
   * @function x
   * @instance
   * @memberof Chart
   * @param {Array} x If x is given, x values of every target will be updated. If no argument is given, current x values will be returned as an Object whose keys are the target ids.
   * @returns {object} xs
   * @example
   *  // Get current x values
   *  chart.x();
   *
   *  // Update x values for all targets
   *  chart.x([100, 200, 300, 400, ...]);
   */
  x: function(e) {
    var t = this.internal, n = t.axis, i = t.data, r = n.isCustomX() && n.isCategorized();
    return Ie(e) && (r ? this.categories(e) : (t.updateTargetX(i.targets, e), t.redraw({
      withUpdateOrgXDomain: !0,
      withUpdateXDomain: !0
    }))), r ? this.categories() : i.xs;
  },
  /**
   * Get and set x values for the chart.
   * @function xs
   * @instance
   * @memberof Chart
   * @param {Array} xs If xs is given, specified target's x values will be updated. If no argument is given, current x values will be returned as an Object whose keys are the target ids.
   * @returns {object} xs
   * @example
   *  // Get current x values
   *  chart.xs();
   *
   *  // Update x values for all targets
   *  chart.xs({
   *    data1: [10, 20, 30, 40, ...],
   *    data2: [100, 200, 300, 400, ...]
   *  });
   */
  xs: function(e) {
    var t = this.internal;
    return Be(e) && (t.updateTargetXs(t.data.targets, e), t.redraw({
      withUpdateOrgXDomain: !0,
      withUpdateXDomain: !0
    })), t.data.xs;
  }
}, Nu = (
  /** @class */
  function() {
    function e(t) {
      var n = Nn(), i = t.config, r = t.params;
      this.owner = t, this.config = i, this.scale = n, (i.noTransition || !r.config.transition_duration) && (i.withoutTransition = !0), i.range = this.scaleExtent((r.orgXScale || n).range());
    }
    return e.getSizeFor1Char = function(t, n) {
      n === void 0 && (n = !0);
      var i = {
        w: 5.5,
        h: 11.5
      };
      return !t.empty() && t.text("0").call(function(r) {
        try {
          var a = r.node().getBBox(), o = a.width, s = a.height;
          o && s && (i.w = o, i.h = s);
        } finally {
          r.text("");
        }
      }), n && (this.getSizeFor1Char = function() {
        return i;
      }), i;
    }, e.prototype.getTickTransformSetter = function(t) {
      var n = this.config, i = t === "x" ? function(r) {
        return "translate(".concat(r + n.tickOffset, ",0)");
      } : function(r) {
        return "translate(0,".concat(r, ")");
      };
      return function(r, a) {
        r.attr("transform", function(o) {
          var s = a(o);
          return Ae(o) ? i(s) : null;
        });
      };
    }, e.prototype.scaleExtent = function(t) {
      var n = t[0], i = t[t.length - 1];
      return n < i ? [n, i] : [i, n];
    }, e.prototype.generateTicks = function(t, n) {
      var i = this.owner.params.tickStepSize, r = t.domain(), a = r[0], o = r[1], s = [];
      if (n && i)
        for (var l = Math.round(a); l <= o; )
          s.push(l), l += i;
      else if (t.ticks) {
        var c = this.config.tickArguments;
        if (t.type === "log" && !c) {
          var f = Nn("_log").domain([a > 0 ? a : 1, o]).range(t.range());
          s = f.ticks();
          for (var h = o.toFixed().length; s.length > 15; h--)
            s = f.ticks(h);
          s.splice(0, 1, a), s.splice(s.length - 1, 1, o);
        } else
          s = t.ticks.apply(t, this.config.tickArguments || []);
        s = s.map(function(g) {
          var x = Ne(g) && ge(g) && !isNaN(g) && Math.round(g * 10) / 10 || g;
          return x;
        });
      }
      return s;
    }, e.prototype.copyScale = function() {
      var t = this.scale.copy();
      return t.domain().length || t.domain(this.scale.domain()), t.type = this.scale.type, t;
    }, e.prototype.textFormatted = function(t) {
      var n = this.config.tickFormat, i = /\d+\.\d+0{5,}\d$/.test(t) ? +String(t).replace(/0+\d$/, "") : t, r = n ? n(i) : i;
      return Ye(r) ? r : "";
    }, e.prototype.transitionise = function(t) {
      var n = this.config, i = t;
      if (n.withoutTransition)
        i = t.interrupt();
      else if (n.transition || !this.owner.params.noTransition)
        try {
          i = t.transition(n.transition);
        } catch (r) {
        }
      return i;
    }, e;
  }()
), Fy = (
  /** @class */
  function() {
    function e(t) {
      t === void 0 && (t = {});
      var n = {
        innerTickSize: 6,
        outerTickSize: t.outerTick ? 6 : 0,
        orient: "bottom",
        range: [],
        tickArguments: null,
        tickCentered: null,
        tickCulling: !0,
        tickFormat: null,
        tickLength: 9,
        tickOffset: 0,
        tickPadding: 3,
        tickValues: null,
        transition: null,
        noTransition: t.noTransition
      };
      n.tickLength = Math.max(n.innerTickSize, 0) + n.tickPadding, this.config = n, this.params = t, this.helper = new Nu(this);
    }
    return e.prototype.create = function(t) {
      var n = this, i = n.config, r = n.helper, a = n.params, o = r.scale, s = i.orient, l = this.splitTickText.bind(n), c = /^(left|right)$/.test(s), f = /^(top|bottom)$/.test(s), h = r.getTickTransformSetter(f ? "x" : "y"), g = h === r.axisX ? "y" : "x", x = /^(top|left)$/.test(s) ? -1 : 1, y = a.tickTextRotate;
      this.config.range = o.rangeExtent ? o.rangeExtent() : r.scaleExtent((a.orgXScale || o).range());
      var T = i.innerTickSize, A = i.tickLength, w = i.range, C = a.id, R = C && /^(x|y|y2)$/.test(C) ? a.config["axis_".concat(C, "_tick_text_position")] : { x: 0, y: 0 }, S = C === "subX" ? "subchart_axis_x" : "axis_".concat(C), I = a.config["".concat(S, "_show")], L = {
        tick: I ? a.config["".concat(S, "_tick_show")] : !1,
        text: I ? a.config["".concat(S, "_tick_text_show")] : !1
      }, p = a.config.axis_evalTextSize, U;
      t.each(function() {
        var H = le(this), q = this.__chart__ || o, X = r.copyScale();
        U = H, this.__chart__ = X, i.tickOffset = a.isCategory ? (X(1) - X(0)) / 2 : 0;
        var j = H.selectAll(".domain").data([0]);
        if (j.enter().append("path").attr("class", "domain").merge(j).attr("d", function() {
          var oe = i.outerTickSize * x;
          return f ? "M".concat(w[0], ",").concat(oe, "V0H").concat(w[1], "V").concat(oe) : "M".concat(oe, ",").concat(w[0], "H0V").concat(w[1], "H").concat(oe);
        }), L.tick || L.text) {
          var z = i.tickValues || r.generateTicks(X, c);
          n.generatedTicks = z;
          var G = H.selectAll(".tick").data(z, X), _e = G.enter().insert("g", ".domain").attr("class", "tick"), Ue = G.exit().remove();
          G = _e.merge(G), L.tick && _e.append("line"), L.text && _e.append("text");
          var te = G.select("text"), re = he(p) ? p.bind(n.params.owner.api)(te.node()) : Nu.getSizeFor1Char(te, p), ue = [], we = te.selectAll("tspan").data(function(oe, He) {
            var Oe = a.tickMultiline ? l(oe, X, z, c, re.w) : Ie(r.textFormatted(oe)) ? r.textFormatted(oe).concat() : [r.textFormatted(oe)];
            return ue[He] = Oe.length, Oe.map(function(K) {
              return { index: He, splitted: K };
            });
          });
          we.exit().remove(), we = we.enter().append("tspan").merge(we).text(function(oe) {
            return oe.splitted;
          }), we.attr("x", f ? 0 : A * x).attr("dx", function() {
            var oe = 0;
            return /(top|bottom)/.test(s) && y && (oe = 8 * Math.sin(Math.PI * (y / 180)) * (s === "top" ? -1 : 1)), oe + (R.x || 0);
          }()).attr("dy", function(oe, He) {
            var Oe = ".71em", K = 0;
            return s !== "top" && (K = re.h, He === 0 && (K = c ? -((ue[oe.index] - 1) * (re.h / 2) - 3) : R.y === 0 ? Oe : 0)), ge(K) && R.y ? K + R.y : K || Oe;
          });
          var st = G.select("line"), Je = G.select("text");
          if (_e.select("line").attr("".concat(g, "2"), T * x), _e.select("text").attr(g, A * x), n.setTickLineTextPosition(st, Je), a.tickTitle) {
            var ft = Je.select("title");
            (ft.empty() ? Je.append("title") : ft).text(function(oe) {
              return a.tickTitle[oe];
            });
          }
          if (X.bandwidth) {
            var vt = X, et = vt.bandwidth() / 2;
            q = function(oe) {
              return vt(oe) + et;
            }, X = q;
          } else
            q.bandwidth ? q = X : h(Ue, X);
          G = a.owner.state.flowing ? r.transitionise(G) : a.owner.$T(G), h(_e, q), h(G.style("opacity", null), X);
        }
      }), this.g = U;
    }, e.prototype.getGeneratedTicks = function(t) {
      var n, i = ((n = this.generatedTicks) === null || n === void 0 ? void 0 : n.length) - 1, r = this.generatedTicks;
      if (i > t) {
        var a = Math.round(i / t + 0.1);
        r = this.generatedTicks.map(function(o, s) {
          return s % a === 0 ? o : null;
        }).filter(function(o) {
          return o !== null;
        }).splice(0, t);
      }
      return r;
    }, e.prototype.getTickXY = function() {
      var t = this.config, n = { x: 0, y: 0 };
      return this.params.isCategory && (n.x = t.tickCentered ? 0 : t.tickOffset, n.y = t.tickCentered ? t.tickOffset : 0), n;
    }, e.prototype.getTickSize = function(t) {
      var n = this.helper.scale, i = this.config, r = i.innerTickSize, a = i.range, o = n(t) + (i.tickCentered ? 0 : i.tickOffset);
      return a[0] < o && o < a[1] ? r : 0;
    }, e.prototype.setTickLineTextPosition = function(t, n) {
      var i = this.getTickXY(), r = this.config, a = r.innerTickSize, o = r.orient, s = r.tickLength, l = r.tickOffset, c = this.params.tickTextRotate, f = function(A) {
        var w = ["start", "end"];
        return o === "top" && w.reverse(), A ? w[A > 0 ? 0 : 1] : "middle";
      }, h = function(A) {
        return A ? "rotate(".concat(A, ")") : null;
      }, g = function(A) {
        var w = A / (o === "bottom" ? 15 : 23);
        return A ? 11.5 - 2.5 * w * (A > 0 ? 1 : -1) : s;
      }, x = this.params.owner.config, y = x.axis_rotated, T = x.axis_x_tick_text_inner;
      switch (o) {
        case "bottom":
          t.attr("x1", i.x).attr("x2", i.x).attr("y2", this.getTickSize.bind(this)), n.attr("x", 0).attr("y", g(c)).style("text-anchor", f(c)).style("text-anchor", function(A, w, C) {
            var R = C.length;
            return !y && w === 0 && (T === !0 || T.first) ? "start" : !y && w === R - 1 && (T === !0 || T.last) ? "end" : f(c);
          }).attr("transform", h(c));
          break;
        case "top":
          t.attr("x2", 0).attr("y2", -a), n.attr("x", 0).attr("y", -g(c) * 2).style("text-anchor", f(c)).attr("transform", h(c));
          break;
        case "left":
          t.attr("x2", -a).attr("y1", i.y).attr("y2", i.y), n.attr("x", -s).attr("y", l).style("text-anchor", "end");
          break;
        case "right":
          t.attr("x2", a).attr("y2", 0), n.attr("x", s).attr("y", 0).style("text-anchor", "start");
      }
    }, e.prototype.splitTickText = function(t, n, i, r, a) {
      var o = this.params, s = this.helper.textFormatted(t), l = Ne(s) && s.indexOf(`
`) > -1 ? s.split(`
`) : [];
      if (l.length)
        return l;
      if (Ie(s))
        return s;
      var c = o.tickWidth;
      (!c || c <= 0) && (c = r ? 95 : o.isCategory ? (o.isInverted ? n(i[0]) - n(i[1]) : n(i[1]) - n(i[0])) - 12 : 110);
      function f(h, g) {
        for (var x, y, T, A = 1; A < g.length; A++)
          if (g.charAt(A) === " " && (y = A), x = g.substr(0, A + 1), T = a * x.length, c < T)
            return f(h.concat(g.substr(0, y || A)), g.slice(y ? y + 1 : A));
        return h.concat(g);
      }
      return f(l, String(s));
    }, e.prototype.scale = function(t) {
      return arguments.length ? (this.helper.scale = t, this) : this.helper.scale;
    }, e.prototype.orient = function(t) {
      return arguments.length ? (this.config.orient = t in {
        top: 1,
        right: 1,
        bottom: 1,
        left: 1
      } ? String(t) : "bottom", this) : this.config.orient;
    }, e.prototype.tickFormat = function(t) {
      var n = this.config;
      return arguments.length ? (n.tickFormat = t, this) : n.tickFormat;
    }, e.prototype.tickCentered = function(t) {
      var n = this.config;
      return arguments.length ? (n.tickCentered = t, this) : n.tickCentered;
    }, e.prototype.tickOffset = function() {
      return this.config.tickOffset;
    }, e.prototype.tickInterval = function(t) {
      var n, i = this.config, r = i.outerTickSize, a = i.tickOffset, o = i.tickValues, s;
      if (this.params.isCategory)
        s = a * 2;
      else {
        var l = (n = this.params.owner.scale.zoom) !== null && n !== void 0 ? n : this.helper.scale, c = this.g.select("path.domain").node().getTotalLength() - r * 2;
        s = c / (t || this.g.selectAll("line").size());
        var f = o ? o.map(function(h, g, x) {
          var y = g + 1;
          return y < x.length ? l(x[y]) - l(h) : null;
        }).filter(Boolean) : [];
        s = Math.min.apply(Math, Ke(Ke([], f, !1), [s], !1));
      }
      return s === 1 / 0 ? 0 : s;
    }, e.prototype.ticks = function() {
      for (var t = [], n = 0; n < arguments.length; n++)
        t[n] = arguments[n];
      var i = this.config;
      return t.length ? (i.tickArguments = ai(t), this) : i.tickArguments;
    }, e.prototype.tickCulling = function(t) {
      var n = this.config;
      return arguments.length ? (n.tickCulling = t, this) : n.tickCulling;
    }, e.prototype.tickValues = function(t) {
      var n = this, i = this.config;
      if (he(t))
        i.tickValues = function() {
          return t(n.helper.scale.domain());
        };
      else {
        if (!arguments.length)
          return i.tickValues;
        i.tickValues = t;
      }
      return this;
    }, e.prototype.setTransition = function(t) {
      return this.config.transition = t, this;
    }, e;
  }()
), By = {
  getAxisInstance: function() {
    return this.axis || new Xy(this);
  }
}, Xy = (
  /** @class */
  function() {
    function e(t) {
      this.axesList = {}, this.tick = {
        x: null,
        y: null,
        y2: null
      }, this.xs = [], this.orient = {
        x: "bottom",
        y: "left",
        y2: "right",
        subX: "bottom"
      }, this.owner = t, this.setOrient();
    }
    return e.prototype.getAxisClassName = function(t) {
      return "".concat(nt.axis, " ").concat(nt["axis".concat(hn(t))]);
    }, e.prototype.isHorizontal = function(t, n) {
      var i = t.config.axis_rotated;
      return n ? i : !i;
    }, e.prototype.isCategorized = function() {
      var t = this.owner, n = t.config, i = t.state;
      return n.axis_x_type.indexOf("category") >= 0 || i.hasRadar;
    }, e.prototype.isCustomX = function() {
      var t = this.owner.config;
      return !this.isTimeSeries() && (t.data_x || Qe(t.data_xs));
    }, e.prototype.isTimeSeries = function(t) {
      return t === void 0 && (t = "x"), this.owner.config["axis_".concat(t, "_type")] === "timeseries";
    }, e.prototype.isLog = function(t) {
      return t === void 0 && (t = "x"), this.owner.config["axis_".concat(t, "_type")] === "log";
    }, e.prototype.isTimeSeriesY = function() {
      return this.isTimeSeries("y");
    }, e.prototype.getAxisType = function(t) {
      t === void 0 && (t = "x");
      var n = "linear";
      return this.isTimeSeries(t) ? n = this.owner.config.axis_x_localtime ? "time" : "utc" : this.isLog(t) && (n = "log"), n;
    }, e.prototype.getExtent = function() {
      var t = this.owner, n = t.config, i = t.scale, r = n.axis_x_extent;
      if (r) {
        if (he(r))
          r = r.bind(t.api)(t.getXDomain(t.data.targets), i.subX);
        else if (this.isTimeSeries() && r.every(isNaN)) {
          var a = kt.bind(t);
          r = r.map(function(o) {
            return i.subX(a(o));
          });
        }
      }
      return r;
    }, e.prototype.init = function() {
      var t = this, n = this.owner, i = n.config, r = n.$el, a = r.main, o = r.axis, s = n.state.clip, l = i.axis_rotated, c = ["x", "y"];
      i.axis_y2_show && c.push("y2"), c.forEach(function(f) {
        var h = t.getAxisClassName(f), g = nt["axis".concat(f.toUpperCase(), "Label")];
        o[f] = a.append("g").attr("class", h).attr("clip-path", function() {
          var x = null;
          return f === "x" ? x = s.pathXAxis : f === "y" && (x = s.pathYAxis), x;
        }).attr("transform", n.getTranslate(f)).style("visibility", i["axis_".concat(f, "_show")] ? null : "hidden"), o[f].append("text").attr("class", g).attr("transform", ["rotate(-90)", null][f === "x" ? +!l : +l]).style("text-anchor", function() {
          return t.textAnchorForAxisLabel(f);
        }), t.generateAxes(f);
      }), i.axis_tooltip && this.setAxisTooltip();
    }, e.prototype.setOrient = function() {
      var t = this.owner, n = t.config, i = n.axis_rotated, r = n.axis_y_inner, a = n.axis_y2_inner;
      this.orient = {
        x: i ? "left" : "bottom",
        y: i ? r ? "top" : "bottom" : r ? "right" : "left",
        y2: i ? a ? "bottom" : "top" : a ? "left" : "right",
        subX: i ? "left" : "bottom"
      };
    }, e.prototype.generateAxes = function(t) {
      var n = this.owner, i = n.config, r = [], a = i["axis_".concat(t, "_axes")], o = i.axis_rotated, s;
      t === "x" ? s = o ? Ds : Ls : t === "y" ? s = o ? Ls : Ds : t === "y2" && (s = o ? Kf : Qf), a.length && a.forEach(function(l) {
        var c = l.tick || {}, f = n.scale[t].copy();
        l.domain && f.domain(l.domain), r.push(s(f).ticks(c.count).tickFormat(he(c.format) ? c.format.bind(n.api) : function(h) {
          return h;
        }).tickValues(c.values).tickSizeOuter(c.outer === !1 ? 0 : 6));
      }), this.axesList[t] = r;
    }, e.prototype.updateAxes = function() {
      var t = this, n = this.owner, i = n.config, r = n.$el.main, a = n.$T;
      Object.keys(this.axesList).forEach(function(o) {
        var s = i["axis_".concat(o, "_axes")], l = n.scale[o].copy(), c = l.range();
        t.axesList[o].forEach(function(f, h) {
          var g = f.scale().range();
          c.every(function(T, A) {
            return T === g[A];
          }) || f.scale().range(c);
          var x = "".concat(t.getAxisClassName(o), "-").concat(h + 1), y = r.select(".".concat(x.replace(/\s/, ".")));
          y.empty() ? y = r.append("g").attr("class", x).style("visibility", i["axis_".concat(o, "_show")] ? null : "hidden").call(f) : (s[h].domain && l.domain(s[h].domain), a(y).call(f.scale(l))), y.attr("transform", n.getTranslate(o, h + 1));
        });
      });
    }, e.prototype.setAxis = function(t, n, i, r) {
      var a = this.owner;
      t !== "subX" && (this.tick[t] = this.getTickValues(t)), this[t] = this.getAxis(
        t,
        n,
        i,
        // do not transit x Axis on zoom and resizing
        // https://github.com/naver/billboard.js/issues/1949
        t === "x" && (a.scale.zoom || a.config.subchart_show || a.state.resizing) ? !0 : r
      );
    }, e.prototype.getAxis = function(t, n, i, r, a) {
      var o = this.owner, s = o.config, l = /^(x|subX)$/.test(t), c = l ? "x" : t, f = l && this.isCategorized(), h = this.orient[t], g = a ? 0 : o.getAxisTickRotate(c), x;
      if (l)
        x = t === "subX" ? o.format.subXAxisTick : o.format.xAxisTick;
      else {
        var y = s["axis_".concat(t, "_tick_format")];
        he(y) && (x = y.bind(o.api));
      }
      var T = this.tick[c], A = oi({
        outerTick: i,
        noTransition: r,
        config: s,
        id: t,
        tickTextRotate: g,
        owner: o
      }, l && {
        isCategory: f,
        isInverted: s.axis_x_inverted,
        tickMultiline: s.axis_x_tick_multiline,
        tickWidth: s.axis_x_tick_width,
        tickTitle: f && s.axis_x_tick_tooltip && o.api.categories(),
        orgXScale: o.scale.x
      });
      l || (A.tickStepSize = s["axis_".concat(c, "_tick_stepSize")]);
      var w = new Fy(A).scale(l && o.scale.zoom || n).orient(h);
      if (l && this.isTimeSeries() && T && !he(T)) {
        var C = kt.bind(o);
        T = T.map(function(S) {
          return C(S);
        });
      } else
        !l && this.isTimeSeriesY() && (w.ticks(s.axis_y_tick_time_value), T = null);
      T && w.tickValues(T), w.tickFormat(x || !l && o.isStackNormalized() && function(S) {
        return "".concat(S, "%");
      }), f && (w.tickCentered(s.axis_x_tick_centered), en(s.axis_x_tick_culling) && (s.axis_x_tick_culling = !1));
      var R = s["axis_".concat(c, "_tick_count")];
      return R && w.ticks(R), w;
    }, e.prototype.updateXAxisTickValues = function(t, n) {
      var i, r = this.owner, a = r.config, o = a.axis_x_tick_fit, s = a.axis_x_tick_count, l;
      return (o || s && o) && (l = r.mapTargetsToUniqueXs(t), this.isCategorized() && s > l.length && (s = l.length), l = this.generateTickValues(l, s, this.isTimeSeries())), n ? n.tickValues(l) : this.x && (this.x.tickValues(l), (i = this.subX) === null || i === void 0 || i.tickValues(l)), l;
    }, e.prototype.getId = function(t) {
      var n = this.owner, i = n.config, r = n.scale, a = i.data_axes[t];
      return (!a || !r[a]) && (a = "y"), a;
    }, e.prototype.getXAxisTickFormat = function(t) {
      var n = this.owner, i = n.config, r = n.format, a = t && i.subchart_axis_x_tick_format || i.axis_x_tick_format, o = this.isTimeSeries(), s = this.isCategorized(), l;
      return a ? he(a) ? l = a.bind(n.api) : o && (l = function(c) {
        return c ? r.axisTime(a)(c) : "";
      }) : l = o ? r.defaultAxisTime : s ? n.categoryName : function(c) {
        return c < 0 ? c.toFixed(0) : c;
      }, he(l) ? function(c) {
        return l.apply(n, s ? [c, n.categoryName(c)] : [c]);
      } : l;
    }, e.prototype.getTickValues = function(t) {
      var n = this.owner, i = n.config["axis_".concat(t, "_tick_values")], r = n["".concat(t, "Axis")];
      return (he(i) ? i.call(n.api) : i) || (r ? r.tickValues() : void 0);
    }, e.prototype.getLabelOptionByAxisId = function(t) {
      return this.owner.config["axis_".concat(t, "_label")];
    }, e.prototype.getLabelText = function(t) {
      var n = this.getLabelOptionByAxisId(t);
      return Ne(n) ? n : n ? n.text : null;
    }, e.prototype.setLabelText = function(t, n) {
      var i = this.owner, r = i.config, a = this.getLabelOptionByAxisId(t);
      Ne(a) ? r["axis_".concat(t, "_label")] = n : a && (a.text = n);
    }, e.prototype.getLabelPosition = function(t, n) {
      var i = this.owner.config.axis_rotated, r = this.getLabelOptionByAxisId(t), a = Jt(r) && r.position ? r.position : n[+!i], o = function(s) {
        return !!~a.indexOf(s);
      };
      return {
        isInner: o("inner"),
        isOuter: o("outer"),
        isLeft: o("left"),
        isCenter: o("center"),
        isRight: o("right"),
        isTop: o("top"),
        isMiddle: o("middle"),
        isBottom: o("bottom")
      };
    }, e.prototype.getAxisLabelPosition = function(t) {
      return this.getLabelPosition(t, t === "x" ? ["inner-top", "inner-right"] : ["inner-right", "inner-top"]);
    }, e.prototype.getLabelPositionById = function(t) {
      return this.getAxisLabelPosition(t);
    }, e.prototype.xForAxisLabel = function(t) {
      var n = this.owner, i = n.state, r = i.width, a = i.height, o = this.getAxisLabelPosition(t), s = o.isMiddle ? -a / 2 : 0;
      return this.isHorizontal(n, t !== "x") ? s = o.isLeft ? 0 : o.isCenter ? r / 2 : r : o.isBottom && (s = -a), s;
    }, e.prototype.dxForAxisLabel = function(t) {
      var n = this.owner, i = this.getAxisLabelPosition(t), r = i.isBottom ? "0.5em" : "0";
      return this.isHorizontal(n, t !== "x") ? r = i.isLeft ? "0.5em" : i.isRight ? "-0.5em" : "0" : i.isTop && (r = "-0.5em"), r;
    }, e.prototype.textAnchorForAxisLabel = function(t) {
      var n = this.owner, i = this.getAxisLabelPosition(t), r = i.isMiddle ? "middle" : "end";
      return this.isHorizontal(n, t !== "x") ? r = i.isLeft ? "start" : i.isCenter ? "middle" : "end" : i.isBottom && (r = "start"), r;
    }, e.prototype.dyForAxisLabel = function(t) {
      var n = this.owner, i = n.config, r = i.axis_rotated, a = this.getAxisLabelPosition(t).isInner, o = i["axis_".concat(t, "_tick_rotate")] ? n.getHorizontalAxisHeight(t) : 0, s = this.getMaxTickSize(t).width, l;
      if (t === "x") {
        var c = i.axis_x_height;
        r ? l = a ? "1.2em" : -25 - s : a ? l = "-0.5em" : c ? l = c - 10 : o ? l = o - 10 : l = "3em";
      } else
        l = {
          y: ["-0.5em", 10, "3em", "1.2em", 10],
          y2: ["1.2em", -20, "-2.2em", "-0.5em", 15]
        }[t], r ? a ? l = l[0] : o ? l = o * (t === "y2" ? -1 : 1) - l[1] : l = l[2] : l = a ? l[3] : (l[4] + (i["axis_".concat(t, "_inner")] ? 0 : s + l[4])) * (t === "y" ? -1 : 1);
      return l;
    }, e.prototype.getMaxTickSize = function(t, n) {
      var i = this.owner, r = i.config, a = i.state.current, o = i.$el, s = o.svg, l = o.chart, c = a.maxTickSize[t], f = "axis_".concat(t), h = {
        width: 0,
        height: 0
      };
      if (n || !r["".concat(f, "_show")] || c.width > 0 && i.filterTargetsToShow().length === 0)
        return c;
      if (s) {
        var g = /^y2?$/.test(t), x = i.filterTargetsToShow(i.data.targets), y = i.scale[t].copy().domain(i["get".concat(g ? "Y" : "X", "Domain")](x, t)), T = y.domain(), A = T[0] === T[1] && T.every(function(p) {
          return p > 0;
        }), w = Ie(c.domain) && c.domain[0] === c.domain[1] && c.domain.every(function(p) {
          return p > 0;
        });
        if (A || w)
          return c.size;
        c.domain = T, g || c.ticks.splice(0);
        var C = this.getAxis(t, y, !1, !1, !0), R = r["".concat(f, "_tick_rotate")], S = r["".concat(f, "_tick_count")], I = r["".concat(f, "_tick_values")];
        !I && S && C.tickValues(this.generateTickValues(T, S, g ? this.isTimeSeriesY() : this.isTimeSeries())), !g && this.updateXAxisTickValues(x, C);
        var L = l.append("svg").style("visibility", "hidden").style("position", "fixed").style("top", "0").style("left", "0");
        C.create(L), L.selectAll("text").attr("transform", ge(R) ? "rotate(".concat(R, ")") : null).each(function(p, U) {
          var H = this.getBoundingClientRect(), q = H.width, X = H.height;
          h.width = Math.max(h.width, q), h.height = Math.max(h.height, X), g || (c.ticks[U] = q);
        }), L.remove();
      }
      return Object.keys(h).forEach(function(p) {
        h[p] > 0 && (c[p] = h[p]);
      }), c;
    }, e.prototype.getXAxisTickTextY2Overflow = function(t) {
      var n = this.owner, i = n.axis, r = n.config, a = n.state, o = a.current, s = a.isLegendRight, l = a.legendItemWidth, c = n.getAxisTickRotate("x"), f = c > 0 && c < 90;
      if ((i.isCategorized() || i.isTimeSeries()) && r.axis_x_tick_fit && (!r.axis_x_tick_culling || en(r.axis_x_tick_culling)) && !r.axis_x_tick_multiline && f) {
        var h = r.axis_y2_show && o.maxTickSize.y2.width || 0, g = s && l || 0, x = o.width - n.getCurrentPaddingByDirection("left"), y = this.getXAxisTickMaxOverflow(c, x - t) - h - g, T = Math.max(0, y) + t;
        return Math.min(T, x / 2);
      }
      return 0;
    }, e.prototype.getXAxisTickMaxOverflow = function(t, n) {
      for (var i = this.owner, r = i.axis, a = i.config, o = i.state, s = r.isTimeSeries(), l = o.current.maxTickSize.x.ticks, c = l.length, f = o.axis.x.padding, h = f.left, g = f.right, x = 0, y = c - (s && a.axis_x_tick_fit ? 0.5 : 0), T = 0; T < c; T++) {
        var A = T + 1, w = Math.cos(Math.PI * t / 180) * l[T], C = A - (s ? 1 : 0.5) + h;
        if (!(C <= 0)) {
          var R = n - w, S = R / C, I = y - A, L = g * S, p = I * S + L, U = w - S / 2 - p;
          x = Math.max(x, U);
        }
      }
      var H = i.filterTargetsToShow(i.data.targets), q = 0;
      if (!s && a.axis_x_tick_count <= H.length && H[0].values.length) {
        var X = Nn(i.axis.getAxisType("x"), 0, n - x).domain([
          h * -1,
          i.getXDomainMax(i.data.targets) + 1 + g
        ]);
        q = (X(1) - X(0)) / 2;
      }
      return x + q;
    }, e.prototype.updateLabels = function(t) {
      var n = this, i = this.owner, r = i.$el.main, a = i.$T, o = {
        x: r.select(".".concat(nt.axisX, " .").concat(nt.axisXLabel)),
        y: r.select(".".concat(nt.axisY, " .").concat(nt.axisYLabel)),
        y2: r.select(".".concat(nt.axisY2, " .").concat(nt.axisY2Label))
      };
      Object.keys(o).filter(function(s) {
        return !o[s].empty();
      }).forEach(function(s) {
        var l = o[s];
        a(l, t).attr("x", function() {
          return n.xForAxisLabel(s);
        }).attr("dx", function() {
          return n.dxForAxisLabel(s);
        }).attr("dy", function() {
          return n.dyForAxisLabel(s);
        }).text(function() {
          return n.getLabelText(s);
        });
      });
    }, e.prototype.getPadding = function(t, n, i, r) {
      var a = ge(t) ? t : t[n];
      return Ae(a) ? this.owner.convertPixelToScale(/(bottom|top)/.test(n) ? "y" : "x", a, r) : i;
    }, e.prototype.generateTickValues = function(t, n, i) {
      var r = t;
      if (n) {
        var a = he(n) ? n() : n;
        if (a === 1)
          r = [t[0]];
        else if (a === 2)
          r = [t[0], t[t.length - 1]];
        else if (a > 2) {
          var o = this.isCategorized(), s = a - 2, l = t[0], c = t[t.length - 1], f = (c - l) / (s + 1), h = void 0;
          r = [l];
          for (var g = 0; g < s; g++)
            h = +l + f * (g + 1), r.push(i ? new Date(h) : o ? Math.round(h) : h);
          r.push(c);
        }
      }
      return i || (r = r.sort(function(x, y) {
        return x - y;
      })), r;
    }, e.prototype.generateTransitions = function(t) {
      var n = this.owner, i = n.$el.axis, r = n.$T, a = ["x", "y", "y2", "subX"].map(function(f) {
        return r(i[f], t);
      }), o = a[0], s = a[1], l = a[2], c = a[3];
      return { axisX: o, axisY: s, axisY2: l, axisSubX: c };
    }, e.prototype.redraw = function(t, n, i) {
      var r = this, a = this.owner, o = a.config, s = a.$el, l = n ? "0" : null;
      ["x", "y", "y2", "subX"].forEach(function(c) {
        var f = r[c], h = s.axis[c];
        f && h && (!i && !o.transition_duration && (f.config.withoutTransition = !0), h.style("opacity", l), f.create(t["axis".concat(hn(c))]));
      }), this.updateAxes();
    }, e.prototype.redrawAxis = function(t, n, i, r, a) {
      var o = this, s, l, c, f = this.owner, h = f.config, g = f.scale, x = f.$el, y = !!g.zoom, T;
      !y && this.isCategorized() && t.length === 0 && g.x.domain([0, x.axis.x.selectAll(".tick").size()]), g.x && t.length ? (!y && f.updateXDomain(t, n.UpdateXDomain, n.UpdateOrgXDomain, n.TrimXDomain), h.axis_x_tick_values || this.updateXAxisTickValues(t)) : this.x && (this.x.tickValues([]), (s = this.subX) === null || s === void 0 || s.tickValues([])), h.zoom_rescale && !r && (T = g.x.orgDomain()), ["y", "y2"].forEach(function(A) {
        var w = "axis_".concat(A, "_"), C = g[A];
        if (C) {
          var R = h["".concat(w, "tick_values")], S = h["".concat(w, "tick_count")];
          if (C.domain(f.getYDomain(t, A, T)), !R && S) {
            var I = f.axis[A], L = C.domain();
            I.tickValues(o.generateTickValues(L, L.every(function(p) {
              return p === 0;
            }) ? 1 : S, o.isTimeSeriesY()));
          }
        }
      }), this.redraw(i, f.hasArcType(), a), this.updateLabels(n.Transition), (n.UpdateXDomain || n.UpdateXAxis || n.Y) && t.length && this.setCulling(), n.Y && ((l = g.subY) === null || l === void 0 || l.domain(f.getYDomain(t, "y")), (c = g.subY2) === null || c === void 0 || c.domain(f.getYDomain(t, "y2")));
    }, e.prototype.setCulling = function() {
      var t = this.owner, n = t.config, i = t.state, r = i.clip, a = i.current, o = t.$el;
      ["subX", "x", "y", "y2"].forEach(function(s) {
        var l = o.axis[s], c = s === "subX" ? "x" : s, f = "axis_".concat(c, "_tick_culling"), h = n[f];
        if (l && h) {
          var g = l.selectAll(".tick"), x = si(g.data()), y = x.length, T = n["".concat(f, "_max")], A = n["".concat(f, "_lines")], w;
          if (y) {
            for (var C = 1; C < y; C++)
              if (y / C < T) {
                w = C;
                break;
              }
            g.each(function(S) {
              var I = A ? this.querySelector("text") : this;
              I && (I.style.display = x.indexOf(S) % w ? "none" : null);
            });
          } else
            g.style("display", null);
          if (s === "x") {
            var R = a.maxTickSize.x.clipPath ? r.pathXAxisTickTexts : null;
            o.svg.selectAll(".".concat(nt.axisX, " .tick text")).attr("clip-path", R);
          }
        }
      });
    }, e.prototype.setAxisTooltip = function() {
      var t, n = this.owner, i = n.config, r = i.axis_rotated, a = i.axis_tooltip, o = n.$el, s = o.axis, l = o.axisTooltip, c = (t = a.backgroundColor) !== null && t !== void 0 ? t : "black";
      n.generateTextBGColorFilter(c, {
        x: -0.15,
        y: -0.2,
        width: 1.3,
        height: 1.3
      }), ["x", "y", "y2"].forEach(function(f) {
        var h, g, x;
        if (l[f] = (h = s[f]) === null || h === void 0 ? void 0 : h.append("text").classed(nt["axis".concat(f.toUpperCase(), "Tooltip")], !0).attr("filter", n.updateTextBGColor({ id: f }, c)), r) {
          var y = f === "x" ? "x" : "y", T = f === "y" ? "1.15em" : f === "x" ? "-0.3em" : "-0.4em";
          (g = l[f]) === null || g === void 0 || g.attr(y, T).attr("d".concat(f === "x" ? "y" : "x"), f === "x" ? "0.4em" : "-1.3em").style("text-anchor", f === "x" ? "end" : null);
        } else {
          var y = f === "x" ? "y" : "x", T = f === "x" ? "1.15em" : "".concat(f === "y" ? "-" : "", "0.4em");
          (x = l[f]) === null || x === void 0 || x.attr(y, T).attr("d".concat(f === "x" ? "x" : "y"), f === "x" ? "-1em" : "0.3em").style("text-anchor", f === "y" ? "end" : null);
        }
      });
    }, e;
  }()
), zy = {
  /**
   * Initialize the area that detects the event.
   * Add a container for the zone that detects the event.
   * @private
   */
  initEventRect: function() {
    var e = this;
    e.$el.main.select(".".concat(Ee.chart)).append("g").attr("class", Pt.eventRects).style("fill-opacity", "0");
  },
  /**
   * Redraws the area that detects the event.
   * @private
   */
  redrawEventRect: function() {
    var e, t = this, n = t.config, i = t.state, r = t.$el, a = t.isMultipleX(), o = n.axis_x_inverted;
    if (r.eventRect)
      t.updateEventRect(r.eventRect, !0);
    else if (t.data.targets.length) {
      var s = t.$el.main.select(".".concat(Pt.eventRects)).style("cursor", n.zoom_enabled && n.zoom_type !== "drag" ? n.axis_rotated ? "ns-resize" : "ew-resize" : null).classed(Pt.eventRectsMultiple, a).classed(Pt.eventRectsSingle, !a), l = s.selectAll(".".concat(Pt.eventRect)).data([0]).enter().append("rect");
      t.updateEventRect(l), t.updateEventType(l), l.call(t.getDraggableSelection()), r.eventRect = l, t.state.inputType === "touch" && !r.svg.on("touchstart.eventRect") && !t.hasArcType() && t.bindTouchOnEventRect(), i.rendered && t.updateEventRect(r.eventRect, !0);
    }
    if (!a) {
      var c = t.getMaxDataCountTarget();
      (!n.data_xSort || o) && c.sort(function(f, h) {
        return o ? h.x - f.x : f.x - h.x;
      }), t.updateDataIndexByX(c), t.updateXs(c), (e = t.updatePointClass) === null || e === void 0 || e.call(t, !0), i.eventReceiver.data = c;
    }
    t.updateEventRectData();
  },
  bindTouchOnEventRect: function() {
    var e = this, t = e.config, n = e.state, i = e.$el, r = i.eventRect, a = i.svg, o = function(x) {
      if (e.isMultipleX())
        e.selectRectForMultipleXs(x);
      else {
        var y = e.getDataIndexFromEvent(n.event);
        e.callOverOutForTouch(y), y === -1 ? e.unselectRect() : e.selectRectForSingle(x, y);
      }
    }, s = function() {
      e.unselectRect(), e.callOverOutForTouch();
    }, l = t.interaction_inputType_touch.preventDefault, c = oo(l) && l || !1, f = !isNaN(l) && l || null, h, g = function(x) {
      var y = x.type, T = x.changedTouches[0], A = T["client".concat(t.axis_rotated ? "Y" : "X")];
      y === "touchstart" ? c ? x.preventDefault() : f !== null && (h = A) : y === "touchmove" && (c || h === !0 || f !== null && Math.abs(h - A) >= f) && (h = !0, x.preventDefault());
    };
    r.on("touchstart", function(x) {
      n.event = x, e.updateEventRect();
    }).on("touchstart.eventRect touchmove.eventRect", function(x) {
      if (n.event = x, !r.empty() && r.classed(Pt.eventRect)) {
        if (n.dragging || n.flowing || e.hasArcType() || x.touches.length > 1)
          return;
        g(x), o(r.node());
      } else
        s();
    }, !0).on("touchend.eventRect", function(x) {
      n.event = x, !r.empty() && r.classed(Pt.eventRect) && (e.hasArcType() || !e.toggleShape || n.cancelClick) && n.cancelClick && (n.cancelClick = !1);
    }, !0), a.on("touchstart", function(x) {
      n.event = x;
      var y = x.target;
      y && y !== r.node() && s();
    });
  },
  /**
   * Update event rect size
   * @param {d3Selection} eventRect Event <rect> element
   * @param {boolean} force Force to update
   * @private
   */
  updateEventRect: function(e, t) {
    t === void 0 && (t = !1);
    var n = this, i = n.state, r = n.$el, a = i.eventReceiver, o = i.width, s = i.height, l = i.rendered, c = i.resizing, f = e || r.eventRect, h = function() {
      if (a) {
        var g = tc(r.chart.node());
        a.rect = f.node().getBoundingClientRect().toJSON(), a.rect.top += g.y, a.rect.left += g.x;
      }
    };
    (!l || c || t) && (f.attr("x", 0).attr("y", 0).attr("width", o).attr("height", s), (!l || t) && f.classed(Pt.eventRect, !0)), h();
  },
  /**
   * Update event type (single or multiple x)
   * @param {d3Selection | boolean} target Target element or boolean to rebind event
   */
  updateEventType: function(e) {
    var t = this, n = oo(e), i = n ? t.$el.eventRect : e, r = n ? e !== (i == null ? void 0 : i.datum().multipleX) : !1;
    i && (r && (i == null || i.on("mouseover mousemove mouseout click", null)), t.isMultipleX() ? t.generateEventRectsForMultipleXs(i) : t.generateEventRectsForSingleX(i));
  },
  /**
   * Updates the location and size of the eventRect.
   * @private
   */
  updateEventRectData: function() {
    var e = this, t = e.config, n = e.scale, i = e.state, r = n.zoom || n.x, a = t.axis_rotated, o = e.isMultipleX(), s, l, c, f;
    if (e.updateEventType(o), o)
      s = 0, l = 0, c = i.width, f = i.height;
    else {
      var h, g = void 0;
      if (e.axis.isCategorized())
        h = e.getEventRectWidth(), g = function(A) {
          return r(A.x) - h / 2;
        };
      else {
        var x = function(A) {
          var w = A.index;
          return {
            prev: e.getPrevX(w),
            next: e.getNextX(w)
          };
        };
        h = function(A) {
          var w = x(A), C = r.domain(), R;
          return w.prev === null && w.next === null ? R = a ? i.height : i.width : w.prev === null ? R = (r(w.next) + r(A.x)) / 2 : w.next === null ? R = r(C[1]) - (r(w.prev) + r(A.x)) / 2 : (Object.keys(w).forEach(function(S, I) {
            var L;
            w[S] = (L = w[S]) !== null && L !== void 0 ? L : C[I];
          }), R = Math.max(0, (r(w.next) - r(w.prev)) / 2)), R;
        }, g = function(A) {
          var w = x(A), C;
          return w.prev === null && w.next === null ? C = 0 : w.prev === null ? C = r(r.domain()[0]) : C = (r(A.x) + r(w.prev)) / 2, C;
        };
      }
      s = a ? 0 : g, l = a ? g : 0, c = a ? i.width : h, f = a ? h : i.height;
    }
    var y = i.eventReceiver, T = function(A, w) {
      return he(A) ? A(w) : A;
    };
    y.coords.splice(y.data.length), y.data.forEach(function(A, w) {
      y.coords[w] = {
        x: T(s, A),
        y: T(l, A),
        w: T(c, A),
        h: T(f, A)
      };
    });
  },
  /**
   * Seletct rect for single x value
   * @param {d3Selection} context Event rect element
   * @param {number} index x Axis index
   * @private
   */
  selectRectForSingle: function(e, t) {
    var n, i, r = this, a = r.config, o = r.$el, s = o.main, l = o.circle, c = a.data_selection_enabled, f = a.data_selection_grouped, h = a.data_selection_isselectable, g = a.tooltip_grouped, x = r.getAllValuesOnIndex(t);
    if (!(g && (r.showTooltip(x, e), (n = r.showGridFocus) === null || n === void 0 || n.call(r, x), !c || f))) {
      !l && s.selectAll(".".concat(Ee.EXPANDED, ":not(.").concat(ct.shape, "-").concat(t, ")")).classed(Ee.EXPANDED, !1);
      var y = s.selectAll(".".concat(ct.shape, "-").concat(t)).classed(Ee.EXPANDED, !0).style("cursor", h ? "pointer" : null).filter(function(T) {
        return r.isWithinShape(this, T);
      });
      y.empty() && !g && a.interaction_onout && ((i = r.hideGridFocus) === null || i === void 0 || i.call(r), r.hideTooltip(), !f && r.setExpand(t)), y.call(function(T) {
        var A, w, C = T.data();
        c && (f || h != null && h.bind(r.api)(C)) && (e.style.cursor = "pointer"), g || (r.showTooltip(C, e), (A = r.showGridFocus) === null || A === void 0 || A.call(r, C), (w = r.unexpandCircles) === null || w === void 0 || w.call(r), T.each(function(R) {
          return r.setExpand(t, R.id);
        }));
      });
    }
  },
  /**
   * Select rect for multiple x values
   * @param {d3Selection} context Event rect element
   * @param {boolean} [triggerEvent=true] Whether trigger event or not
   * @private
   */
  selectRectForMultipleXs: function(e, t) {
    t === void 0 && (t = !0);
    var n = this, i = n.config, r = n.state, a = n.filterTargetsToShow(n.data.targets);
    if (!(r.dragging || n.hasArcType(a))) {
      var o = Qt(r.event, e), s = n.findClosestFromTargets(a, o);
      if (t && r.mouseover && (!s || s.id !== r.mouseover.id) && (i.data_onout.call(n.api, r.mouseover), r.mouseover = void 0), !s) {
        n.unselectRect();
        return;
      }
      var l = n.isBubbleType(s) || n.isScatterType(s) || !i.tooltip_grouped ? [s] : n.filterByX(a, s.x), c = l.map(function(h) {
        return n.addName(h);
      });
      n.showTooltip(c, e), n.setExpand(s.index, s.id, !0), n.showGridFocus(c);
      var f = n.dist(s, o);
      (n.isBarType(s.id) || f < n.getPointSensitivity(s)) && (n.$el.svg.select(".".concat(Pt.eventRect)).style("cursor", "pointer"), t && !r.mouseover && (i.data_onover.call(n.api, s), r.mouseover = s));
    }
  },
  /**
   * Unselect EventRect.
   * @private
   */
  unselectRect: function() {
    var e = this, t = e.$el, n = t.circle, i = t.tooltip;
    e.$el.svg.select(".".concat(Pt.eventRect)).style("cursor", null), e.hideGridFocus(), i && (e.hideTooltip(), e._handleLinkedCharts(!1)), n && !e.isPointFocusOnly() && e.unexpandCircles(), e.expandBarTypeShapes(!1);
  },
  /**
   * Create eventRect for each data on the x-axis.
   * Register touch and drag events.
   * @param {object} eventRectEnter d3.select($EVENT.eventRects) object.
   * @returns {object} d3.select($EVENT.eventRects) object.
   * @private
   */
  generateEventRectsForSingleX: function(e) {
    var t = this, n = t.config, i = t.state, r = i.eventReceiver, a = e.style("cursor", n.data_selection_enabled && n.data_selection_grouped ? "pointer" : null).on("click", function(s) {
      i.event = s;
      var l = r.currentIdx, c = r.data, f = c[l === -1 ? t.getDataIndexFromEvent(s) : l];
      t.clickHandlerForSingleX.bind(this)(f, t);
    }).datum({ multipleX: !1 });
    if (i.inputType === "mouse") {
      var o = function(s) {
        var l = s ? t.getDataIndexFromEvent(s) : r.currentIdx;
        return l > -1 ? r.data[l] : null;
      };
      a.on("mouseover", function(s) {
        i.event = s, t.updateEventRect(), Object.values(t.$el.axisTooltip).forEach(function(l) {
          return l == null ? void 0 : l.style("display", null);
        });
      }).on("mousemove", function(s) {
        var l = o(s);
        if (i.event = s, !!l) {
          var c = l.index, f = n.line_step_type;
          if (n.line_step_tooltipMatch && t.hasType("step") && /^step\-(before|after)$/.test(f)) {
            var h = t.scale.zoom || t.scale.x, g = t.axis.xs[c], x = h.invert(Qt(s, this)[0]);
            f === "step-after" && x < g ? c -= 1 : f === "step-before" && x > g && (c += 1);
          }
          t.showAxisGridFocus();
          var y = n.tooltip_grouped && c === r.currentIdx;
          if (i.dragging || i.flowing || t.hasArcType() || y) {
            n.tooltip_show && y && t.setTooltipPosition();
            return;
          }
          c !== r.currentIdx && (t.setOverOut(!1, r.currentIdx), r.currentIdx = c), c === -1 ? t.unselectRect() : t.selectRectForSingle(this, c), t.setOverOut(c !== -1, c);
        }
      }).on("mouseout", function(s) {
        i.event = s, !(!n || t.hasArcType() || r.currentIdx === -1 || !n.interaction_onout) && (t.hideAxisGridFocus(), t.unselectRect(), t.setOverOut(!1, r.currentIdx), r.currentIdx = -1);
      });
    }
    return a;
  },
  clickHandlerForSingleX: function(e, t) {
    var n = t, i = n.config, r = n.state, a = n.$el.main;
    if (!e || n.hasArcType() || r.cancelClick) {
      r.cancelClick && (r.cancelClick = !1);
      return;
    }
    var o = e.index;
    a.selectAll(".".concat(ct.shape, "-").concat(o)).each(function(s) {
      var l;
      (i.data_selection_grouped || n.isWithinShape(this, s)) && ((l = n.toggleShape) === null || l === void 0 || l.call(n, this, s, o), i.data_onclick.bind(n.api)(s, this));
    });
  },
  /**
   * Create an eventRect,
   * Register touch and drag events.
   * @param {object} eventRectEnter d3.select($EVENT.eventRects) object.
   * @private
   */
  generateEventRectsForMultipleXs: function(e) {
    var t = this, n = t.config, i = t.state;
    e.on("click", function(r) {
      i.event = r, t.clickHandlerForMultipleXS.bind(this)(t);
    }).datum({ multipleX: !0 }), i.inputType === "mouse" && e.on("mouseover mousemove", function(r) {
      i.event = r, t.selectRectForMultipleXs(this);
    }).on("mouseout", function(r) {
      i.event = r, !(!t.config || t.hasArcType() || !n.interaction_onout) && t.unselectRect();
    });
  },
  clickHandlerForMultipleXS: function(e) {
    var t = e, n = t.config, i = t.state, r = t.filterTargetsToShow(t.data.targets);
    if (!t.hasArcType(r)) {
      var a = Qt(i.event, this), o = t.findClosestFromTargets(r, a), s = t.getPointSensitivity(o);
      o && (t.isBarType(o.id) || t.dist(o, a) < s) && t.$el.main.selectAll(".".concat(ct.shapes).concat(t.getTargetSelectorSuffix(o.id))).selectAll(".".concat(ct.shape, "-").concat(o.index)).each(function() {
        var l;
        (n.data_selection_grouped || t.isWithinShape(this, o)) && ((l = t.toggleShape) === null || l === void 0 || l.call(t, this, o, o.index), n.data_onclick.bind(t.api)(o, this));
      });
    }
  }
}, Hy = {
  /**
   * Generate flow
   * @param {object} args option object
   * @returns {Function}
   * @private
   */
  generateFlow: function(e) {
    var t = this, n = t.data, i = t.state, r = t.$el;
    return function() {
      var a = e.flow.length;
      i.flowing = !0, n.targets.forEach(function(s) {
        s.values.splice(0, a);
      }), t.updateXGrid && t.updateXGrid(!0);
      var o = {};
      [
        "axis.x",
        "grid.x",
        "gridLines.x",
        "region.list",
        "text",
        "bar",
        "line",
        "area",
        "circle"
      ].forEach(function(s) {
        var l = s.split("."), c = r[l[0]];
        c && l.length > 1 && (c = c[l[1]]), c != null && c.size() && (o[s] = c);
      }), t.hideGridFocus(), t.setFlowList(o, e);
    };
  },
  /**
   * Set flow list
   * @param {object} elements Target elements
   * @param {object} args option object
   * @private
   */
  setFlowList: function(e, t) {
    var n = this, i = t.flow, r = t.targets, a = i.duration, o = a === void 0 ? t.duration : a, s = i.index, l = i.length, c = i.orgDataCount, f = n.getFlowTransform(r, c, s, l), h = ic(), g;
    h.add(Object.keys(e).map(function(x) {
      return g = e[x].transition().ease(wp).duration(o), x === "axis.x" ? g = g.call(function(y) {
        n.axis.x.setTransition(y).create(y);
      }) : x === "region.list" ? g = g.filter(n.isRegionOnX).attr("transform", f) : g = g.attr("transform", f), g;
    })), g.call(h, function() {
      n.cleanUpFlow(e, t);
    });
  },
  /**
   * Clean up flow
   * @param {object} elements Target elements
   * @param {object} args option object
   * @private
   */
  cleanUpFlow: function(e, t) {
    var n = this, i = n.config, r = n.state, a = n.$el.svg, o = i.axis_rotated, s = t.flow, l = t.shape, c = t.xv, f = l.pos, h = f.cx, g = f.cy, x = f.xForText, y = f.yForText, T = s.done, A = T === void 0 ? function() {
    } : T, w = s.length;
    w && (["circle", "text", "shape", "eventRect"].forEach(function(C) {
      for (var R = [], S = 0; S < w; S++)
        R.push(".".concat(lt[C], "-").concat(S));
      a.selectAll(".".concat(lt["".concat(C, "s")])).selectAll(R).remove();
    }), a.select(".".concat(lt.xgrid)).remove()), Object.keys(e).forEach(function(C) {
      var R = e[C];
      if (C !== "axis.x" && R.attr("transform", null), C === "grid.x")
        R.attr(r.xgridAttr);
      else if (C === "gridLines.x")
        R.attr("x1", o ? 0 : c).attr("x2", o ? r.width : c), R.select("text").attr("x", o ? r.width : 0).attr("y", c);
      else if (/^(area|bar|line)$/.test(C))
        R.attr("d", l.type[C]);
      else if (C === "text")
        R.attr("x", x).attr("y", y).style("fill-opacity", n.opacityForText.bind(n));
      else if (C === "circle")
        if (n.isCirclePoint())
          R.attr("cx", h).attr("cy", g);
        else {
          var S = function(L) {
            return h(L) - i.point_r;
          }, I = function(L) {
            return g(L) - i.point_r;
          };
          R.attr("x", S).attr("y", I);
        }
      else
        C === "region.list" && R.select("rect").filter(n.isRegionOnX).attr("x", n.regionX.bind(n)).attr("width", n.regionWidth.bind(n));
    }), i.interaction_enabled && n.redrawEventRect(), A.call(n.api), r.flowing = !1;
  },
  /**
   * Get flow transform value
   * @param {object} targets target
   * @param {number} orgDataCount original data count
   * @param {number} flowIndex flow index
   * @param {number} flowLength flow length
   * @returns {string}
   * @private
   */
  getFlowTransform: function(e, t, n, i) {
    var r = this, a = r.data, o = r.scale.x, s = a.targets[0].values, l = r.getValueOnIndex(s, n), c = r.getValueOnIndex(s, n + i), f, h = o.domain(), g = r.updateXDomain(e, !0, !0);
    t ? t === 1 || (l == null ? void 0 : l.x) === (c == null ? void 0 : c.x) ? f = o(h[0]) - o(g[0]) : f = r.axis.isTimeSeries() ? o(h[0]) - o(g[0]) : o((l == null ? void 0 : l.x) || 0) - o(c.x) : s.length !== 1 ? f = o(h[0]) - o(g[0]) : r.axis.isTimeSeries() ? (l = r.getValueOnIndex(s, 0), c = r.getValueOnIndex(s, s.length - 1), f = o(l.x) - o(c.x)) : f = Ri(g) / 2;
    var x = Ri(h) / Ri(g);
    return "translate(".concat(f, ",0) scale(").concat(x, ",1)");
  }
}, Vy = {
  initClip: function() {
    var e = this, t = e.state, n = t.clip, i = t.datetimeId;
    n.id = "".concat(i, "-clip"), n.idXAxis = "".concat(n.id, "-xaxis"), n.idYAxis = "".concat(n.id, "-yaxis"), n.idGrid = "".concat(n.id, "-grid"), n.path = e.getClipPath(n.id), n.pathXAxis = e.getClipPath(n.idXAxis), n.pathYAxis = e.getClipPath(n.idYAxis), n.pathGrid = e.getClipPath(n.idGrid);
  },
  getClipPath: function(e) {
    var t = this, n = t.config;
    return !n.clipPath && /-clip$/.test(e) || !n.axis_x_clipPath && /-clip-xaxis$/.test(e) || !n.axis_y_clipPath && /-clip-yaxis$/.test(e) ? null : "url(#".concat(e, ")");
  },
  appendClip: function(e, t) {
    t && e.append("clipPath").attr("id", t).append("rect");
  },
  /**
   * Set x Axis clipPath dimension
   * @param {d3Selecton} node clipPath <rect> selection
   * @private
   */
  setXAxisClipPath: function(e) {
    var t = this, n = t.config, i = t.state, r = i.margin, a = i.width, o = i.height, s = n.axis_rotated, l = Math.max(30, r.left) - (s ? 0 : 20), c = (s ? r.top + o + 10 : r.bottom) + 20, f = s ? -(1 + l) : -(l - 1), h = -15, g = s ? r.left + 20 : a + 10 + l;
    e.attr("x", f).attr("y", h).attr("width", g).attr("height", c);
  },
  /**
   * Set y Axis clipPath dimension
   * @param {d3Selection} node clipPath <rect> selection
   * @private
   */
  setYAxisClipPath: function(e) {
    var t = this, n = t.config, i = t.state, r = i.margin, a = i.width, o = i.height, s = n.axis_rotated, l = Math.max(30, r.left) - (s ? 20 : 0), c = n.axis_y_inner, f = c && !s ? n.axis_y_label.text ? -20 : -1 : s ? -(1 + l) : -(l - 1), h = -(s ? 20 : r.top), g = (s ? a + 15 + l : r.left + 20) + (c ? 20 : 0), x = (s ? r.bottom + 10 : r.top + o) + 10;
    e.attr("x", f).attr("y", h).attr("width", g).attr("height", x);
  },
  updateXAxisTickClip: function() {
    var e = this, t = e.config, n = e.state, i = n.clip, r = n.xAxisHeight, a = e.$el.defs, o = e.getHorizontalAxisHeight("x");
    if (a && !i.idXAxisTickTexts) {
      var s = "".concat(i.id, "-xaxisticktexts");
      e.appendClip(a, s), i.pathXAxisTickTexts = e.getClipPath(i.idXAxisTickTexts), i.idXAxisTickTexts = s;
    }
    !t.axis_x_tick_multiline && e.getAxisTickRotate("x") && o !== r && (e.setXAxisTickClipWidth(), e.setXAxisTickTextClipPathWidth()), e.state.xAxisHeight = o;
  },
  setXAxisTickClipWidth: function() {
    var e = this, t = e.config, n = e.state.current.maxTickSize, i = e.getAxisTickRotate("x");
    if (!t.axis_x_tick_multiline && i) {
      var r = Math.sin(Math.PI / 180 * Math.abs(i));
      n.x.clipPath = (e.getHorizontalAxisHeight("x") - 20) / r;
    } else
      n.x.clipPath = null;
  },
  setXAxisTickTextClipPathWidth: function() {
    var e = this, t = e.state, n = t.clip, i = t.current, r = e.$el.svg;
    r && r.select("#".concat(n.idXAxisTickTexts, " rect")).attr("width", i.maxTickSize.x.clipPath).attr("height", 30);
  }
}, Yy = function(e) {
  return Ae(e.position) || "end";
}, Uy = function(e) {
  return e.position === "start" ? 4 : e.position === "middle" ? 0 : -4;
};
function Pu(e, t, n) {
  return function(i) {
    var r = e ? 0 : t;
    return i.position === "start" ? r = e ? -n : 0 : i.position === "middle" && (r = (e ? -n : t) / 2), r;
  };
}
function Ou(e, t) {
  e.each(function() {
    var n = le(this);
    ["x1", "x2", "y1", "y2"].forEach(function(i) {
      return n.attr(i, +n.attr(i));
    });
  });
}
var jy = {
  hasGrid: function() {
    var e = this.config;
    return ["x", "y"].some(function(t) {
      return e["grid_".concat(t, "_show")] || e["grid_".concat(t, "_lines")].length;
    });
  },
  initGrid: function() {
    var e = this;
    e.hasGrid() && e.initGridLines(), e.initFocusGrid();
  },
  initGridLines: function() {
    var e = this, t = e.config, n = e.state.clip, i = e.$el;
    (t.grid_x_lines.length || t.grid_y_lines.length) && (i.gridLines.main = i.main.insert("g", ".".concat(Ee.chart).concat(t.grid_lines_front ? " + *" : "")).attr("clip-path", n.pathGrid).attr("class", "".concat(qe.grid, " ").concat(qe.gridLines)), i.gridLines.main.append("g").attr("class", qe.xgridLines), i.gridLines.main.append("g").attr("class", qe.ygridLines), i.gridLines.x = el([]));
  },
  updateXGrid: function(e) {
    var t = this, n = t.config, i = t.scale, r = t.state, a = t.$el, o = a.main, s = a.grid, l = n.axis_rotated, c = t.generateGridData(n.grid_x_type, i.x), f = t.axis.isCategorized() ? t.axis.x.tickOffset() : 0, h = function(g) {
      return (i.zoom || i.x)(g) + f * (l ? -1 : 1);
    };
    r.xgridAttr = l ? {
      x1: 0,
      x2: r.width,
      y1: h,
      y2: h
    } : {
      x1: h,
      x2: h,
      y1: 0,
      y2: r.height
    }, s.x = o.select(".".concat(qe.xgrids)).selectAll(".".concat(qe.xgrid)).data(c), s.x.exit().remove(), s.x = s.x.enter().append("line").attr("class", qe.xgrid).merge(s.x), e || s.x.each(function() {
      var g = le(this);
      Object.keys(r.xgridAttr).forEach(function(x) {
        g.attr(x, r.xgridAttr[x]).style("opacity", function() {
          return g.attr(l ? "y1" : "x1") === (l ? r.height : 0) ? "0" : null;
        });
      });
    });
  },
  updateYGrid: function() {
    var e = this, t = e.axis, n = e.config, i = e.scale, r = e.state, a = e.$el, o = a.grid, s = a.main, l = n.axis_rotated, c = function(h) {
      return i.y(h);
    }, f = t.y.getGeneratedTicks(n.grid_y_ticks) || e.scale.y.ticks(n.grid_y_ticks);
    o.y = s.select(".".concat(qe.ygrids)).selectAll(".".concat(qe.ygrid)).data(f), o.y.exit().remove(), o.y = o.y.enter().append("line").attr("class", qe.ygrid).merge(o.y), o.y.attr("x1", l ? c : 0).attr("x2", l ? c : r.width).attr("y1", l ? 0 : c).attr("y2", l ? r.height : c), Ou(o.y);
  },
  updateGrid: function() {
    var e = this, t = e.$el, n = t.grid, i = t.gridLines;
    !i.main && e.initGridLines(), n.main.style("visibility", e.hasArcType() ? "hidden" : null), e.hideGridFocus(), e.updateGridLines("x"), e.updateGridLines("y");
  },
  /**
   * Update Grid lines
   * @param {string} type x | y
   * @private
   */
  updateGridLines: function(e) {
    var t = this, n = t.config, i = t.$el, r = i.gridLines, a = i.main, o = t.$T, s = n.axis_rotated, l = e === "x";
    n["grid_".concat(e, "_show")] && t["update".concat(e.toUpperCase(), "Grid")]();
    var c = a.select(".".concat(qe["".concat(e, "gridLines")])).selectAll(".".concat(qe["".concat(e, "gridLine")])).data(n["grid_".concat(e, "_lines")]);
    o(c.exit()).style("opacity", "0").remove();
    var f = c.enter().append("g");
    f.append("line").style("opacity", "0"), c = f.merge(c), c.each(function(h) {
      var g = le(this);
      g.select("text").empty() && h.text && g.append("text").style("opacity", "0");
    }), o(c.attr("class", function(h) {
      return "".concat(qe["".concat(e, "gridLine")], " ").concat(h.class || "").trim();
    }).select("text").attr("text-anchor", Yy).attr("transform", function() {
      return l ? s ? null : "rotate(-90)" : s ? "rotate(-90)" : null;
    }).attr("dx", Uy).attr("dy", -5)).text(function(h) {
      var g;
      return (g = h.text) !== null && g !== void 0 ? g : this.remove();
    }), r[e] = c;
  },
  redrawGrid: function(e) {
    var t = this, n = t.config.axis_rotated, i = t.state, r = i.width, a = i.height, o = t.$el.gridLines, s = t.$T, l = t.xv.bind(t), c = t.yv.bind(t), f = o.x.select("line"), h = o.x.select("text"), g = o.y.select("line"), x = o.y.select("text");
    return f = s(f, e).attr("x1", n ? 0 : l).attr("x2", n ? r : l).attr("y1", n ? l : 0).attr("y2", n ? l : a), h = s(h, e).attr("x", Pu(!n, r, a)).attr("y", l), g = s(g, e).attr("x1", n ? c : 0).attr("x2", n ? c : r).attr("y1", n ? 0 : c).attr("y2", n ? a : c), x = s(x, e).attr("x", Pu(n, r, a)).attr("y", c), [
      f.style("opacity", null),
      h.style("opacity", null),
      g.style("opacity", null),
      x.style("opacity", null)
    ];
  },
  initFocusGrid: function() {
    var e = this, t = e.config, n = e.state.clip, i = e.$el, r = t.grid_front, a = ".".concat(r && i.gridLines.main ? qe.gridLines : Ee.chart).concat(r ? " + *" : ""), o = i.main.insert("g", a).attr("clip-path", n.pathGrid).attr("class", qe.grid);
    if (i.grid.main = o, t.grid_x_show && o.append("g").attr("class", qe.xgrids), t.grid_y_show && o.append("g").attr("class", qe.ygrids), t.axis_tooltip) {
      var s = o.append("g").attr("class", "bb-axis-tooltip");
      s.append("line").attr("class", "bb-axis-tooltip-x"), s.append("line").attr("class", "bb-axis-tooltip-y");
    }
    t.interaction_enabled && t.grid_focus_show && !t.axis_tooltip && (o.append("g").attr("class", ze.xgridFocus).append("line").attr("class", ze.xgridFocus), t.grid_focus_y && !t.tooltip_grouped && o.append("g").attr("class", ze.ygridFocus).append("line").attr("class", ze.ygridFocus));
  },
  showAxisGridFocus: function() {
    for (var e, t, n = this, i = n.config, r = n.format, a = n.state, o = a.event, s = a.width, l = a.height, c = i.axis_rotated, f = Qt(o, (e = n.$el.eventRect) === null || e === void 0 ? void 0 : e.node()), h = f[0], g = f[1], x = { x: h, y: g }, y = 0, T = Object.entries(n.$el.axisTooltip); y < T.length; y++) {
      var A = T[y], w = A[0], C = A[1], R = w === "x" && !c || w !== "x" && c ? "x" : "y", S = x[R], I = (t = n.scale[w]) === null || t === void 0 ? void 0 : t.invert(S);
      I && (I = w === "x" && n.axis.isTimeSeries() ? r.xAxisTick(I) : I == null ? void 0 : I.toFixed(2), C == null || C.attr(R, S).text(I));
    }
    n.$el.main.selectAll("line.bb-axis-tooltip-x, line.bb-axis-tooltip-y").style("visibility", null).each(function(L, p) {
      var U = le(this);
      p === 0 ? U.attr("x1", h).attr("x2", h).attr("y1", p ? 0 : l).attr("y2", p ? l : 0) : U.attr("x1", p ? 0 : s).attr("x2", p ? s : 0).attr("y1", g).attr("y2", g);
    });
  },
  hideAxisGridFocus: function() {
    var e = this;
    e.$el.main.selectAll("line.bb-axis-tooltip-x, line.bb-axis-tooltip-y").style("visibility", "hidden"), Object.values(e.$el.axisTooltip).forEach(function(t) {
      return t == null ? void 0 : t.style("display", "none");
    });
  },
  /**
   * Show grid focus line
   * @param {Array} data Selected data
   * @private
   */
  showGridFocus: function(e) {
    var t, n = this, i = n.config, r = n.state, a = r.width, o = r.height, s = i.axis_rotated, l = n.$el.main.selectAll("line.".concat(ze.xgridFocus, ", line.").concat(ze.ygridFocus)), c = (e || [l.datum()]).filter(function(g) {
      return g && Ae(n.getBaseValue(g));
    });
    if (!(!i.tooltip_show || c.length === 0 || !i.axis_x_forceAsSingle && n.hasType("bubble") || n.hasArcType())) {
      var f = i.grid_focus_edge && !i.tooltip_grouped, h = n.xx.bind(n);
      l.style("visibility", null).data(c.concat(c)).each(function(g) {
        var x = le(this), y = {
          x: h(g),
          y: n.getYScaleById(g.id)(g.value)
        }, T;
        if (x.classed(ze.xgridFocus))
          T = s ? [
            null,
            // x1
            y.x,
            // y1
            f ? y.y : a,
            // x2
            y.x
            // y2
          ] : [
            y.x,
            f ? y.y : null,
            y.x,
            o
          ];
        else {
          var A = n.axis.getId(g.id) === "y2";
          T = s ? [
            y.y,
            // x1
            f && !A ? y.x : null,
            // y1
            y.y,
            // x2
            f && A ? y.x : o
            // y2
          ] : [
            f && A ? y.x : null,
            y.y,
            f && !A ? y.x : a,
            y.y
          ];
        }
        ["x1", "y1", "x2", "y2"].forEach(function(w, C) {
          return x.attr(w, T[C]);
        });
      }), Ou(l), (t = n.showCircleFocus) === null || t === void 0 || t.call(n, e);
    }
  },
  hideGridFocus: function() {
    var e, t = this, n = t.state, i = n.inputType, r = n.resizing, a = t.$el.main;
    (i === "mouse" || !r) && (a.selectAll("line.".concat(ze.xgridFocus, ", line.").concat(ze.ygridFocus)).style("visibility", "hidden"), (e = t.hideCircleFocus) === null || e === void 0 || e.call(t));
  },
  updateGridFocus: function() {
    var e, t = this, n = t.state, i = n.inputType, r = n.width, a = n.height, o = n.resizing, s = t.$el.grid, l = s.main.select("line.".concat(ze.xgridFocus));
    if (i === "touch")
      l.empty() ? o && ((e = t.showCircleFocus) === null || e === void 0 || e.call(t)) : t.showGridFocus();
    else {
      var c = t.config.axis_rotated;
      l.attr("x1", c ? 0 : -10).attr("x2", c ? r : -10).attr("y1", c ? -10 : 0).attr("y2", c ? -10 : a);
    }
    return !0;
  },
  generateGridData: function(e, t) {
    var n = this, i = n.$el.main.select(".".concat(nt.axisX)).selectAll(".tick").size(), r = [];
    if (e === "year")
      for (var a = n.getXDomain(), o = a.map(function(f) {
        return f.getFullYear();
      }), s = o[0], l = o[1], c = s; c <= l; c++)
        r.push(new Date("".concat(c, "-01-01 00:00:00")));
    else
      r = t.ticks(10), r.length > i && (r = r.filter(function(f) {
        return String(f).indexOf(".") < 0;
      }));
    return r;
  },
  getGridFilterToRemove: function(e) {
    return e ? function(t) {
      var n = !1;
      return (Ie(e) ? e.concat() : [e]).forEach(function(i) {
        ("value" in i && t.value === i.value || "class" in i && t.class === i.class) && (n = !0);
      }), n;
    } : function() {
      return !0;
    };
  },
  removeGridLines: function(e, t) {
    var n = this, i = n.config, r = n.$T, a = n.getGridFilterToRemove(e), o = function(f) {
      return !a(f);
    }, s = t ? qe.xgridLines : qe.ygridLines, l = t ? qe.xgridLine : qe.ygridLine;
    r(n.$el.main.select(".".concat(s)).selectAll(".".concat(l)).filter(a)).style("opacity", "0").remove();
    var c = "grid_".concat(t ? "x" : "y", "_lines");
    i[c] = i[c].filter(o);
  }
}, Gy = {
  initRegion: function() {
    var e = this, t = e.$el;
    t.region.main = t.main.insert("g", ":first-child").attr("clip-path", e.state.clip.path).attr("class", Fi.regions);
  },
  updateRegion: function() {
    var e = this, t = e.config, n = e.$el.region, i = e.$T;
    n.main || e.initRegion(), n.main.style("visibility", e.hasArcType() ? "hidden" : null);
    var r = n.main.selectAll(".".concat(Fi.region)).data(t.regions);
    i(r.exit()).style("opacity", "0").remove();
    var a = r.enter().append("g");
    a.append("rect").style("fill-opacity", "0"), n.list = a.merge(r).attr("class", e.classRegion.bind(e)), n.list.each(function(o) {
      var s, l = le(this);
      l.select("text").empty() && (!((s = o.label) === null || s === void 0) && s.text) && le(this).append("text").style("opacity", "0");
    });
  },
  redrawRegion: function(e) {
    var t = this, n = t.$el.region, i = t.$T, r = n.list.select("rect"), a = n.list.selectAll("text");
    return r = i(r, e).attr("x", t.regionX.bind(t)).attr("y", t.regionY.bind(t)).attr("width", t.regionWidth.bind(t)).attr("height", t.regionHeight.bind(t)), a = i(a, e).attr("transform", function(o) {
      var s, l = (s = o.label) !== null && s !== void 0 ? s : {}, c = l.x, f = c === void 0 ? 0 : c, h = l.y, g = h === void 0 ? 0 : h, x = l.rotated, y = x === void 0 ? !1 : x;
      return "translate(".concat(t.regionX.bind(t)(o) + f, ", ").concat(t.regionY.bind(t)(o) + g, ")").concat(y ? " rotate(-90)" : "");
    }).attr("text-anchor", function(o) {
      var s;
      return !((s = o.label) === null || s === void 0) && s.rotated ? "end" : null;
    }).attr("dy", "1em").style("fill", function(o) {
      var s, l;
      return (l = (s = o.label) === null || s === void 0 ? void 0 : s.color) !== null && l !== void 0 ? l : null;
    }).text(function(o) {
      var s;
      return (s = o.label) === null || s === void 0 ? void 0 : s.text;
    }), [
      r.style("fill-opacity", function(o) {
        return Ae(o.opacity) ? o.opacity : null;
      }).on("end", function() {
        le(this.parentNode).selectAll("rect:not([x])").remove();
      }),
      a.style("opacity", null)
    ];
  },
  getRegionXY: function(e, t) {
    var n = this, i = n.config, r = n.scale, a = i.axis_rotated, o = e === "x", s = "start", l, c = 0;
    return t.axis === "y" || t.axis === "y2" ? (o || (s = "end"), (o ? a : !a) && s in t && (l = r[t.axis], c = l(t[s]))) : (o ? !a : a) && s in t && (l = r.zoom || r.x, c = l(n.axis.isTimeSeries() ? kt.call(n, t[s]) : t[s])), c;
  },
  regionX: function(e) {
    return this.getRegionXY("x", e);
  },
  regionY: function(e) {
    return this.getRegionXY("y", e);
  },
  getRegionSize: function(e, t) {
    var n = this, i = n.config, r = n.scale, a = n.state, o = i.axis_rotated, s = e === "width", l = n[s ? "regionX" : "regionY"](t), c, f = "end", h = a[e];
    return t.axis === "y" || t.axis === "y2" ? (s || (f = "start"), (s ? o : !o) && f in t && (c = r[t.axis], h = c(t[f]))) : (s ? !o : o) && f in t && (c = r.zoom || r.x, h = c(n.axis.isTimeSeries() ? kt.call(n, t[f]) : t[f])), h < l ? 0 : h - l;
  },
  regionWidth: function(e) {
    return this.getRegionSize("width", e);
  },
  regionHeight: function(e) {
    return this.getRegionSize("height", e);
  },
  isRegionOnX: function(e) {
    return !e.axis || e.axis === "x";
  }
}, Wy = {
  /**
   * Get Axis size according its position
   * @param {string} id Axis id value - x, y or y2
   * @returns {number} size Axis size value
   * @private
   */
  getAxisSize: function(e) {
    var t = this, n = t.config.axis_rotated;
    return n && e === "x" || !n && /y2?/.test(e) ? t.getAxisWidthByAxisId(e, !0) : t.getHorizontalAxisHeight(e);
  },
  getAxisWidthByAxisId: function(e, t) {
    var n, i, r = this;
    if (r.axis) {
      var a = (n = r.axis) === null || n === void 0 ? void 0 : n.getLabelPositionById(e), o = r.axis.getMaxTickSize(e, t).width, s = o === 0 ? 0.5 : 0;
      return o + (((i = r.config.padding) === null || i === void 0 ? void 0 : i.mode) === "fit" ? a.isInner ? 10 + s : 10 : a.isInner ? 20 + s : 40);
    } else
      return 40;
  },
  getHorizontalAxisHeight: function(e) {
    var t, n, i = this, r = i.config, a = i.state, o = a.rotatedPadding, s = a.isLegendRight, l = a.isLegendInset, c = r.axis_rotated, f = ((t = r.padding) === null || t === void 0 ? void 0 : t.mode) === "fit", h = r["axis_".concat(e, "_inner")], g = r["axis_".concat(e, "_label")].text, x = 13, y = ((n = r.padding) === null || n === void 0 ? void 0 : n.mode) === "fit" ? h && !g ? e === "y" ? 1 : 0 : 20 : 30;
    if (e === "x" && !r.axis_x_show)
      return 8;
    if (e === "x" && ge(r.axis_x_height))
      return r.axis_x_height;
    if (e === "y" && !r.axis_y_show)
      return r.legend_show && !s && !l ? 10 : 1;
    if (e === "y2" && !r.axis_y2_show)
      return f ? 0 : o.top;
    var T = i.axis.getMaxTickSize(e), A = Math.abs(r.axis_x_tick_rotate) > 0 && (!r.axis_x_tick_autorotate || i.needToRotateXAxisTickTexts());
    return (r.axis_x_tick_multiline || A) && T.height > x && (y += T.height - x), y + (i.axis.getLabelPositionById(e).isInner ? 0 : 10) + (e === "y2" && !c ? -10 : 0);
  },
  getEventRectWidth: function() {
    var e = this, t = e.config, n = e.axis, i = t.axis_x_inverted, r = n.x.tickInterval();
    return Math.max(0, i ? Math.abs(r) : r);
  },
  /**
   * Get axis tick test rotate value
   * @param {string} id Axis id
   * @returns {number} rotate value
   * @private
   */
  getAxisTickRotate: function(e) {
    var t = this, n = t.axis, i = t.config, r = t.state, a = t.$el, o = i["axis_".concat(e, "_tick_rotate")];
    if (e === "x") {
      var s = n.isCategorized() || n.isTimeSeries();
      if (i.axis_x_tick_fit && s) {
        var l = i.axis_x_tick_count, c = r.current.maxTickSize.x.ticks.length, f = 0;
        if (l ? f = l > c ? c : l : c && (f = c), f !== r.axis.x.tickCount) {
          var h = t.data.targets;
          r.axis.x.padding = t.getXDomainPadding([
            t.getXDomainMinMax(h, "min"),
            t.getXDomainMinMax(h, "max")
          ], f);
        }
        r.axis.x.tickCount = f;
      }
      a.svg && i.axis_x_tick_autorotate && i.axis_x_tick_fit && !i.axis_x_tick_multiline && !i.axis_x_tick_culling && s && (o = t.needToRotateXAxisTickTexts() ? i.axis_x_tick_rotate : 0);
    }
    return o;
  },
  /**
   * Check weather axis tick text needs to be rotated
   * @returns {boolean}
   * @private
   */
  needToRotateXAxisTickTexts: function() {
    var e = this, t = e.state, n = t.axis, i = t.current, r = t.isLegendRight, a = t.legendItemWidth, o = r && a, s = i.width - o - e.getCurrentPaddingByDirection("left") - e.getCurrentPaddingByDirection("right"), l = n.x.tickCount + n.x.padding.left + n.x.padding.right, c = e.axis.getMaxTickSize("x").width, f = l ? s / l : 0;
    return c > f;
  }
}, qy = {
  /**
   * Set clip-path attribute for x axis element
   * @name axis․x․clipPath
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo]()
   * @example
   * // don't set 'clip-path' attribute
   * clipPath: false
   */
  axis_x_clipPath: !0,
  /**
   * Show or hide x axis.
   * @name axis․x․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   x: {
   *     show: false
   *   }
   * }
   */
  axis_x_show: !0,
  /**
   * Force the x axis to interact as single rather than multiple x axes.
   * - **NOTE:** The tooltip event will be triggered nearing each data points(for multiple xs) rather than x axis based(as single x does) in below condition:
   *   - for `bubble` & `scatter` type
   *   - when `data.xs` is set
   *   - when `tooltip.grouped=false` is set
   *     - `tooltip.grouped` options will take precedence over `axis.forceSingleX` option.
   * @name axis․x․forceAsSingle
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.ForceAsSingle)
   * @example
   * axis: {
   *   x: {
   *      // will work as single x axis
   *      forceAsSingle: true
   *   }
   * }
   */
  axis_x_forceAsSingle: !1,
  /**
   * Set type of x axis.<br><br>
   * **Available Values:**
   * - category
   * - indexed
   * - log
   * - timeseries
   *
   * **NOTE:**<br>
   * - **log** type:
   *   - the x values specified by [`data.x`](#.data%25E2%2580%25A4x)(or by any equivalent option), must be exclusively-positive.
   *   - x axis min value should be >= 0.
   *   - for 'category' type, `data.xs` option isn't supported.
   * @name axis․x․type
   * @memberof Options
   * @type {string}
   * @default indexed
   * @see [Demo: indexed](https://naver.github.io/billboard.js/demo/#Chart.AreaChart)
   * @see [Demo: timeseries](https://naver.github.io/billboard.js/demo/#Chart.TimeseriesChart)
   * @see [Demo: category](https://naver.github.io/billboard.js/demo/#Data.CategoryData)
   * @see [Demo: log](https://naver.github.io/billboard.js/demo/#Axis.LogScales)
   * @example
   * axis: {
   *   x: {
   *     type: "timeseries"
   *   }
   * }
   */
  axis_x_type: "indexed",
  /**
   * Set how to treat the timezone of x values.<br>
   * If true, treat x value as localtime. If false, convert to UTC internally.
   * @name axis․x․localtime
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   x: {
   *     localtime: false
   *   }
   * }
   */
  axis_x_localtime: !0,
  /**
   * Set category names on category axis.
   * This must be an array that includes category names in string. If category names are included in the date by data.x option, this is not required.
   * @name axis․x․categories
   * @memberof Options
   * @type {Array}
   * @default []
   * @example
   * axis: {
   *   x: {
   *     categories: ["Category 1", "Category 2", ...]
   *   }
   * }
   */
  axis_x_categories: [],
  /**
   * centerize ticks on category axis.
   * @name axis․x․tick․centered
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       centered: true
   *     }
   *   }
   * }
   */
  axis_x_tick_centered: !1,
  /**
   * A function to format tick value. Format string is also available for timeseries data.
   * @name axis․x․tick․format
   * @memberof Options
   * @type {Function|string}
   * @default undefined
   * @see [D3's time specifier](https://d3js.org/d3-time-format#locale_format)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *        // for timeseries, a 'datetime' object is given as parameter
   *       format: function(x) {
   *           return x.getFullYear();
   *       }
   *
   *       // for category, index(Number) and categoryName(String) are given as parameter
   *       format: function(index, categoryName) {
   *           return categoryName.substr(0, 10);
   *       },
   *
   *        // for timeseries format specifier
   *        format: "%Y-%m-%d %H:%M:%S"
   *     }
   *   }
   * }
   */
  axis_x_tick_format: void 0,
  /**
   * Setting for culling ticks.
   * - `true`: the ticks will be culled, then only limited tick text will be shown.<br>
   *   This option does not hide the tick lines by default, if want to hide tick lines, set `axis.x.tick.culling.lines=false`.
   * - `false`: all of ticks will be shown.<br><br>
   * The number of ticks to be shown can be chaned by `axis.x.tick.culling.max`.
   * @name axis․x․tick․culling
   * @memberof Options
   * @type {boolean}
   * @default
   * `true` for indexed axis and timeseries axis, `false` for category axis
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       culling: false
   *     }
   *   }
   * }
   */
  axis_x_tick_culling: {},
  /**
   * The number of tick texts will be adjusted to less than this value.
   * @name axis․x․tick․culling․max
   * @memberof Options
   * @type {number}
   * @default 10
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       culling: {
   *           max: 5
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_culling_max: 10,
  /**
   * Control visibility of tick lines within culling option, along with tick text.
   * @name axis․x․tick․culling․lines
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       culling: {
   *           lines: false,
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_culling_lines: !0,
  /**
   * The number of x axis ticks to show.<br><br>
   * This option hides tick lines together with tick text. If this option is used on timeseries axis, the ticks position will be determined precisely and not nicely positioned (e.g. it will have rough second value).
   * @name axis․x․tick․count
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       count: 5
   *     }
   *   }
   * }
   */
  axis_x_tick_count: void 0,
  /**
   * Show or hide x axis tick line.
   * @name axis․x․tick․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       show: false
   *     }
   *   }
   * }
   */
  axis_x_tick_show: !0,
  /**
   * Show or hide x axis tick text.
   * @name axis․x․tick․text․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       text: {
   *           show: false
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_text_show: !0,
  /**
   * Set the first/last axis tick text to be positioned inside of the chart on non-rotated axis.
   * @name axis․x․tick․text․inner
   * @memberof Options
   * @type {boolean|object}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickInner)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       text: {
   *          inner: true,
   *
   *          // or specify each position of the first and last tick text
   *          inner: {
   *       	   first: true,
   *       	   last: true
   *       	}
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_text_inner: !1,
  /**
   * Set the x Axis tick text's position relatively its original position
   * @name axis․x․tick․text․position
   * @memberof Options
   * @type {object}
   * @default {x: 0, y:0}
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       text: {
   *         position: {
   *           x: 10,
   *           y: 10
   *         }
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_text_position: { x: 0, y: 0 },
  /**
   * Fit x axis ticks.
   * - **true**: ticks will be shown according to x value of the data points.
   * - **false**: ticks will be shown as to have same intervals.
   * @name axis․x․tick․fit
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickFitting)
   * @see [Demo: for timeseries zoom](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickTimeseries)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       fit: false
   *     }
   *   }
   * }
   */
  axis_x_tick_fit: !0,
  /**
   * Set the x values of ticks manually.<br><br>
   * If this option is provided, the position of the ticks will be determined based on those values.<br>
   * This option works with `timeseries` data and the x values will be parsed accoding to the type of the value and data.xFormat option.
   * @name axis․x․tick․values
   * @memberof Options
   * @type {Array|Function}
   * @default null
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       values: [1, 2, 4, 8, 16, 32, ...],
   *
   *       // an Array value should be returned
   *       values: function() {
   *       	return [ ... ];
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_values: null,
  /**
   * Rotate x axis tick text if there is not enough space for 'category' and 'timeseries' type axis.
   * - **NOTE:** The conditions where `autorotate` is enabled are:
   *   - axis.x.type='category' or 'timeseries
   *   - axis.x.tick.multiline=false
   *   - axis.x.tick.culling=false
   *   - axis.x.tick.fit=true
   * - **NOTE:** axis.x.tick.clippath=false is necessary for calculating the overflow padding between the end of x axis and the width of the SVG
   * @name axis․x․tick․autorotate
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickAutorotate)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       rotate: 15,
   *       autorotate: true,
   *       multiline: false,
   *       culling: false,
   *       fit: true
   *     },
   *     clipPath: false
   *   }
   * }
   */
  axis_x_tick_autorotate: !1,
  /**
   * Rotate x axis tick text.
   * - If you set negative value, it will rotate to opposite direction.
   * - Applied when [`axis.rotated`](#.axis%25E2%2580%25A4rotated) option is `false`.
   * - As long as `axis_x_tick_fit` is set to `true` it will calculate an overflow for the y2 axis and add this value to the right padding.
   * @name axis․x․tick․rotate
   * @memberof Options
   * @type {number}
   * @default 0
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.RotateXAxisTickText)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       rotate: 60
   *     }
   *   }
   * }
   */
  axis_x_tick_rotate: 0,
  /**
   * Show x axis outer tick.
   * @name axis․x․tick․outer
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       outer: false
   *     }
   *   }
   * }
   */
  axis_x_tick_outer: !0,
  /**
   * Set tick text to be multiline
   * - **NOTE:**
   *  > When x tick text contains `\n`, it's used as line break and 'axis.x.tick.width' option is ignored.
   * @name axis․x․tick․multiline
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickMultiline)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       multiline: false
   *     }
   *   }
   * }
   * @example
   * // example of line break with '\n'
   * // In this case, 'axis.x.tick.width' is ignored
   * data: {
   *    x: "x",
   *    columns: [
   *        ["x", "long\ntext", "Another\nLong\nText"],
   *        ...
   *    ],
   * }
   */
  axis_x_tick_multiline: !0,
  /**
   * Set tick width
   * - **NOTE:**
   *  > When x tick text contains `\n`, this option is ignored.
   * @name axis․x․tick․width
   * @memberof Options
   * @type {number}
   * @default null
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       width: 50
   *     }
   *   }
   * }
   */
  axis_x_tick_width: null,
  /**
   * Set to display system tooltip(via `<title>` element) for tick text
   * - **NOTE:** Only available for category axis type (`axis.x.type='category'`)
   * @name axis․x․tick․tooltip
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       tooltip: true
   *     }
   *   }
   * }
   */
  axis_x_tick_tooltip: !1,
  /**
   * Set max value of x axis range.
   * @name axis․x․max
   * @memberof Options
   * @property {number} max Set the max value
   * @property {boolean} [max.fit=false] When specified `max.value` is greater than the bound data value, setting `true` will make x axis max to be fitted to the bound data max value.
   * - **NOTE:** If the bound data max value is greater than the `max.value`, the x axis max will be limited as the given `max.value`.
   * @property {number} [max.value] Set the max value
   * @example
   * axis: {
   *   x: {
   *     max: 100,
   *
   *     max: {
   *       // 'fit=true' will make x axis max to be limited as the bound data value max when 'max.value' is greater.
   *       // - when bound data max is '10' and max.value: '100' ==>  x axis max will be '10'
   *       // - when bound data max is '1000' and max.value: '100' ==> x axis max will be '100'
   *       fit: true,
   *       value: 100
   *     }
   *   }
   * }
   */
  axis_x_max: void 0,
  /**
   * Set min value of x axis range.
   * @name axis․x․min
   * @memberof Options
   * @property {number} min Set the min value
   * @property {boolean} [min.fit=false] When specified `min.value` is lower than the bound data value, setting `true` will make x axis min to be fitted to the bound data min value.
   * - **NOTE:** If the bound data min value is lower than the `min.value`, the x axis min will be limited as the given `min.value`.
   * @property {number} [min.value] Set the min value
   * @example
   * axis: {
   *   x: {
   *     min: -100,
   *
   *     min: {
   *       // 'fit=true' will make x axis min to be limited as the bound data value min when 'min.value' is lower.
   *       // - when bound data min is '-10' and min.value: '-100' ==>  x axis min will be '-10'
   *       // - when bound data min is '-1000' and min.value: '-100' ==> x axis min will be '-100'
   *       fit: true,
   *       value: -100
   *     }
   *   }
   * }
   */
  axis_x_min: void 0,
  /**
   * Change the direction of x axis.<br><br>
   * If true set, the direction will be `right -> left`.
   * @name axis․x․inverted
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.InvertedAxis)
   * @example
   * axis: {
   *   x: {
   *     inverted: true
   *   }
   * }
   */
  axis_x_inverted: !1,
  /**
   * Set padding for x axis.<br><br>
   * If this option is set, the range of x axis will increase/decrease according to the values.
   * If no padding is needed in the rage of x axis, 0 should be set.
   * By default, left/right padding are set depending on x axis type or chart types.
   * - **NOTE:**
   *   - The meaning of padding values, differs according axis types:<br>
   *     - **category/indexed:** The unit of tick value
   *       ex. the given value `1`, is same as the width of 1 tick width
   *     - **timeseries:** Numeric time value
   *       ex. the given value `1000*60*60*24`, which is numeric time equivalent of a day, is same as the width of 1 tick width
   *   - If want values to be treated as pixels, specify `unit:"px"`.
   *     - The pixel value will be convered based on the scale values. Hence can not reflect accurate padding result.
   * @name axis․x․padding
   * @memberof Options
   * @type {object|number}
   * @default {}
   * @example
   * axis: {
   *   x: {
   *     padding: {
   *       // when axis type is 'category'
   *       left: 1,  // set left padding width of equivalent value of a tick's width
   *       right: 0.5  // set right padding width as half of equivalent value of tick's width
   *
   *       // when axis type is 'timeseries'
   *       left: 1000*60*60*24,  // set left padding width of equivalent value of a day tick's width
   *       right: 1000*60*60*12   // set right padding width as half of equivalent value of a day tick's width
   *     },
   *
   *     // or set both values at once.
   *     padding: 10,
   *
   *     // or set padding values as pixel unit.
   *     padding: {
   *       left: 100,
   *       right: 50,
   *       unit: "px"
   *     },
   *   }
   * }
   */
  axis_x_padding: {},
  /**
   * Set height of x axis.<br><br>
   * The height of x axis can be set manually by this option. If you need more space for x axis, please use this option for that. The unit is pixel.
   * @name axis․x․height
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   x: {
   *     height: 20
   *   }
   * }
   */
  axis_x_height: void 0,
  /**
   * Set extent for subchart and zoom(drag only). This can be an array or function that returns an array.
   * - **NOTE:** Specifying value, will limit the zoom scope selection within.
   * @name axis․x․extent
   * @memberof Options
   * @type {Array|Function}
   * @default undefined
   * @example
   * axis: {
   *   x: {
   *     // extent range as a pixel value
   *     extent: [0, 200],
   *
   *     // when axis is 'timeseries', parsable datetime string
   *     extent: ["2019-03-01", "2019-03-05"],
   *
   *     // return extent value
   *     extent: function(domain, scale) {
   *    	 var extent = domain.map(function(v) {
   *     	    return scale(v);
   *     	 });
   *
   *   	 // it should return a format of array
   *   	 // ex) [0, 584]
   *     	 return extent;
   *     }
   *   }
   * }
   */
  axis_x_extent: void 0,
  /**
   * Set label on x axis.<br><br>
   * You can set x axis label and change its position by this option.
   * `string` and `object` can be passed and we can change the poisiton by passing object that has position key.<br>
   * Available position differs according to the axis direction (vertical or horizontal).
   * If string set, the position will be the default.
   *
   *  - **If it's horizontal axis:**
   *    - inner-right [default]
   *    - inner-center
   *    - inner-left
   *    - outer-right
   *    - outer-center
   *    - outer-left
   *  - **If it's vertical axis:**
   *    - inner-top [default]
   *    - inner-middle
   *    - inner-bottom
   *    - outer-top
   *    - outer-middle
   *    - outer-bottom
   * @name axis․x․label
   * @memberof Options
   * @type {string|object}
   * @default undefined
   * @example
   * axis: {
   *   x: {
   *     label: "Your X Axis"
   *   }
   * }
   *
   * axis: {
   *   x: {
   *     label: {
   *        text: "Your X Axis",
   *        position: "outer-center"
   *     }
   *   }
   * }
   */
  axis_x_label: {},
  /**
   * Set additional axes for x Axis.
   * - **NOTE:** Axis' scale is based on x Axis value if domain option isn't set.
   *
   * Each axis object should consist with following options:
   *
   * | Name | Type | Default | Description |
   * | --- | --- | --- | --- |
   * | domain | Array | - | Set the domain value |
   * | tick.outer | boolean | true | Show outer tick |
   * | tick.format | Function | - | Set formatter for tick text |
   * | tick.count | Number | - | Set the number of y axis ticks |
   * | tick.values | Array | - | Set tick values manually |
   * @name axis․x․axes
   * @memberof Options
   * @type {Array}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.MultiAxes)
   * @see [Demo: Domain](https://naver.github.io/billboard.js/demo/#Axis.MultiAxesDomain)
   * @example
   * x: {
   *    axes: [
   *      {
   *        // if set, will not be correlated with the main x Axis domain value
   *        domain: [0, 1000],
   *        tick: {
   *          outer: false,
   *          format: function(x) {
   *             return x + "%";
   *          },
   *          count: 2,
   *          values: [10, 20, 30]
   *        }
   *      },
   *      ...
   *    ]
   * }
   */
  axis_x_axes: []
}, Zy = {
  /**
   * Set clip-path attribute for y axis element
   * - **NOTE**: `clip-path` attribute for y Axis is set only when `axis.y.inner` option is true.
   * @name axis․y․clipPath
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * // don't set 'clip-path' attribute
   * clipPath: false
   */
  axis_y_clipPath: !0,
  /**
   * Show or hide y axis.
   * @name axis․y․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y: {
   *     show: false
   *   }
   * }
   */
  axis_y_show: !0,
  /**
   * Set type of y axis.<br><br>
   * **Available Values:**
   *  - indexed
   *  - log
   *  - timeseries
   *
   * **NOTE:**<br>
   * - **log** type:
   *   - the bound data values must be exclusively-positive.
   *   - y axis min value should be >= 0.
   *   - [`data.groups`](#.data%25E2%2580%25A4groups)(stacked data) option aren't supported.
   *
   * @name axis․y․type
   * @memberof Options
   * @type {string}
   * @default "indexed"
   * @see [Demo: log](https://naver.github.io/billboard.js/demo/#Axis.LogScales)
   * @example
   * axis: {
   *   y: {
   *     type: "log"
   *   }
   * }
   */
  axis_y_type: "indexed",
  /**
   * Set max value of y axis.
   * - **NOTE:** Padding will be added based on this value, so if you don't need the padding, please set axis.y.padding to disable it (e.g. axis.y.padding = 0).
   * @name axis․y․max
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     max: 1000
   *   }
   * }
   */
  axis_y_max: void 0,
  /**
   * Set min value of y axis.
   * - **NOTE:**
   *   Padding will be added based on this value, so if you don't need the padding, please set axis.y.padding to disable it (e.g. axis.y.padding = 0).
   * @name axis․y․min
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     min: 1000
   *   }
   * }
   */
  axis_y_min: void 0,
  /**
   * Change the direction of y axis.<br><br>
   * If true set, the direction will be `top -> bottom`.
   * @name axis․y․inverted
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.InvertedAxis)
   * @example
   * axis: {
   *   y: {
   *     inverted: true
   *   }
   * }
   */
  axis_y_inverted: !1,
  /**
   * Set center value of y axis.
   * @name axis․y․center
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     center: 0
   *   }
   * }
   */
  axis_y_center: void 0,
  /**
   * Show y axis inside of the chart.
   * @name axis․y․inner
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y: {
   *     inner: true
   *   }
   * }
   */
  axis_y_inner: !1,
  /**
   * Set label on y axis.<br><br>
   * You can set y axis label and change its position by this option. This option works in the same way as [axis.x.label](#.axis%25E2%2580%25A4x%25E2%2580%25A4label).
   * @name axis․y․label
   * @memberof Options
   * @type {string|object}
   * @default {}
   * @see [axis.x.label](#.axis%25E2%2580%25A4x%25E2%2580%25A4label) for position string value.
   * @example
   * axis: {
   *   y: {
   *     label: "Your Y Axis"
   *   }
   * }
   *
   * axis: {
   *   y: {
   *     label: {
   *        text: "Your Y Axis",
   *        position: "outer-middle"
   *     }
   *   }
   * }
   */
  axis_y_label: {},
  /**
   * Set formatter for y axis tick text.<br><br>
   * This option accepts d3.format object as well as a function you define.
   * @name axis․y․tick․format
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       format: function(x) {
   *           return x.getFullYear();
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_format: void 0,
  /**
   * Setting for culling ticks.
   * - `true`: the ticks will be culled, then only limited tick text will be shown.<br>
   *   This option does not hide the tick lines by default, if want to hide tick lines, set `axis.y.tick.culling.lines=false`.
   * - `false`: all of ticks will be shown.<br><br>
   * The number of ticks to be shown can be chaned by `axis.y.tick.culling.max`.
   * @name axis․y․tick․culling
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       culling: false
   *     }
   *   }
   * }
   */
  axis_y_tick_culling: !1,
  /**
   * The number of tick texts will be adjusted to less than this value.
   * @name axis․y․tick․culling․max
   * @memberof Options
   * @type {number}
   * @default 5
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       culling: {
   *           max: 5
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_culling_max: 5,
  /**
   * Control visibility of tick lines within culling option, along with tick text.
   * @name axis․y․tick․culling․lines
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       culling: {
   *           lines: false,
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_culling_lines: !0,
  /**
   * Show y axis outer tick.
   * @name axis․y․tick․outer
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       outer: false
   *     }
   *   }
   * }
   */
  axis_y_tick_outer: !0,
  /**
   * Set y axis tick values manually.
   * @name axis․y․tick․values
   * @memberof Options
   * @type {Array|Function}
   * @default null
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       values: [100, 1000, 10000],
   *
   *       // an Array value should be returned
   *       values: function() {
   *       	return [ ... ];
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_values: null,
  /**
   * Rotate y axis tick text.
   * - If you set negative value, it will rotate to opposite direction.
   * - Applied when [`axis.rotated`](#.axis%25E2%2580%25A4rotated) option is `true`.
   * @name axis․y․tick․rotate
   * @memberof Options
   * @type {number}
   * @default 0
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       rotate: 60
   *     }
   *   }
   * }
   */
  axis_y_tick_rotate: 0,
  /**
   * Set the number of y axis ticks.<br><br>
   * - **NOTE:** The position of the ticks will be calculated precisely, so the values on the ticks will not be rounded nicely. In the case, axis.y.tick.format or axis.y.tick.values will be helpful.
   * @name axis․y․tick․count
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       count: 5
   *     }
   *   }
   * }
   */
  axis_y_tick_count: void 0,
  /**
   * Show or hide y axis tick line.
   * @name axis․y․tick․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       show: false
   *     }
   *   }
   * }
   */
  axis_y_tick_show: !0,
  /**
   * Set axis tick step(interval) size.
   * - **NOTE:** Will be ignored if `axis.y.tick.count` or `axis.y.tick.values` options are set.
   * @name axis․y․tick․stepSize
   * @memberof Options
   * @type {number}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.StepSizeForYAxis)
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       // tick value will step as indicated interval value.
   *       // ex) 'stepSize=15' ==> [0, 15, 30, 45, 60]
   *       stepSize: 15
   *     }
   *   }
   * }
   */
  axis_y_tick_stepSize: null,
  /**
   * Show or hide y axis tick text.
   * @name axis․y․tick․text․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       text: {
   *           show: false
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_text_show: !0,
  /**
   * Set the y Axis tick text's position relatively its original position
   * @name axis․y․tick․text․position
   * @memberof Options
   * @type {object}
   * @default {x: 0, y:0}
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       text: {
   *         position: {
   *           x: 10,
   *           y: 10
   *         }
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_text_position: { x: 0, y: 0 },
  /**
   * Set the number of y axis ticks.<br><br>
   * - **NOTE:** The position of the ticks will be calculated precisely, so the values on the ticks will not be rounded nicely. In the case, axis.y.tick.format or axis.y.tick.values will be helpful.
   * @name axis․y․tick․time
   * @memberof Options
   * @private
   * @type {object}
   * @property {object} time time object
   * @property {Function} [time.value] D3's time interval function (https://github.com/d3/d3-time#intervals)
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       time: {
   *          // ticks at 15-minute intervals
   *          // https://github.com/d3/d3-scale/blob/master/README.md#time_ticks
   *          value: d3.timeMinute.every(15)
   *       }
   *     }
   *   }
   * }
   */
  // @TODO: not fully implemented yet
  axis_y_tick_time_value: void 0,
  /**
   * Set padding for y axis.<br><br>
   * You can set padding for y axis to create more space on the edge of the axis.
   * This option accepts object and it can include top and bottom. top, bottom will be treated as pixels.
   *
   * - **NOTE:**
   *   - Given values are translated relative to the y Axis domain value for padding
   *   - For area and bar type charts, [area.zerobased](#.area) or [bar.zerobased](#.bar) options should be set to 'false` to get padded bottom.
   * @name axis․y․padding
   * @memberof Options
   * @type {object|number}
   * @default {}
   * @example
   * axis: {
   *   y: {
   *     padding: {
   *       top: 0,
   *       bottom: 0
   *     },
   *
   *     // or set both values at once.
   *     padding: 10
   *   }
   * }
   */
  axis_y_padding: {},
  /**
   * Set default range of y axis.<br><br>
   * This option set the default value for y axis when there is no data on init.
   * @name axis․y․default
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     default: [0, 1000]
   *   }
   * }
   */
  axis_y_default: void 0,
  /**
   * Set additional axes for y Axis.
   * - **NOTE:** Axis' scale is based on y Axis value if domain option isn't set.
   *
   * Each axis object should consist with following options:
   *
   * | Name | Type | Default | Description |
   * | --- | --- | --- | --- |
   * | domain | Array | - | Set the domain value |
   * | tick.outer | boolean | true | Show outer tick |
   * | tick.format | Function | - | Set formatter for tick text |
   * | tick.count | Number | - | Set the number of y axis ticks |
   * | tick.values | Array | - | Set tick values manually |
   * @name axis․y․axes
   * @memberof Options
   * @type {Array}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.MultiAxes)
   * @see [Demo: Domain](https://naver.github.io/billboard.js/demo/#Axis.MultiAxesDomain)
   * @example
   * y: {
   *    axes: [
   *      {
   *        // if set, will not be correlated with the main y Axis domain value
   *        domain: [0, 1000],
   *        tick: {
   *          outer: false,
   *          format: function(x) {
   *             return x + "%";
   *          },
   *          count: 2,
   *          values: [10, 20, 30]
   *        }
   *      },
   *      ...
   *    ]
   * }
   */
  axis_y_axes: []
}, Ky = {
  /**
   * Show or hide y2 axis.
   * - **NOTE**:
   *   - When set to `false` will not generate y2 axis node. In this case, all 'y2' axis related functionality won't work properly.
   *   - If need to use 'y2' related options while y2 isn't visible, set the value `true` and control visibility by css display property.
   * @name axis․y2․show
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y2: {
   *     show: true
   *   }
   * }
   */
  axis_y2_show: !1,
  /**
   * Set type of y2 axis.<br><br>
   * **Available Values:**
   *  - indexed
   *  - log
   *  - timeseries
   *
   * **NOTE:**<br>
   * - **log** type:
   *   - the bound data values must be exclusively-positive.
   *   - y2 axis min value should be >= 0.
   *   - [`data.groups`](#.data%25E2%2580%25A4groups)(stacked data) option aren't supported.
   *
   * @name axis․y2․type
   * @memberof Options
   * @type {string}
   * @default "indexed"
   * @see [Demo: log](https://naver.github.io/billboard.js/demo/#Axis.LogScales)
   * @example
   * axis: {
   *   y2: {
   *     type: "indexed"
   *   }
   * }
   */
  axis_y2_type: "indexed",
  /**
   * Set max value of y2 axis.
   * @name axis․y2․max
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     max: 1000
   *   }
   * }
   */
  axis_y2_max: void 0,
  /**
   * Set min value of y2 axis.
   * @name axis․y2․min
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     min: -1000
   *   }
   * }
   */
  axis_y2_min: void 0,
  /**
   * Change the direction of y2 axis.<br><br>
   * If true set, the direction will be `top -> bottom`.
   * @name axis․y2․inverted
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.InvertedAxis)
   * @example
   * axis: {
   *   y2: {
   *     inverted: true
   *   }
   * }
   */
  axis_y2_inverted: !1,
  /**
   * Set center value of y2 axis.
   * @name axis․y2․center
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     center: 0
   *   }
   * }
   */
  axis_y2_center: void 0,
  /**
   * Show y2 axis inside of the chart.
   * @name axis․y2․inner
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y2: {
   *     inner: true
   *   }
   * }
   */
  axis_y2_inner: !1,
  /**
   * Set label on y2 axis.<br><br>
   * You can set y2 axis label and change its position by this option. This option works in the same way as [axis.x.label](#.axis%25E2%2580%25A4x%25E2%2580%25A4label).
   * @name axis․y2․label
   * @memberof Options
   * @type {string|object}
   * @default {}
   * @see [axis.x.label](#.axis%25E2%2580%25A4x%25E2%2580%25A4label) for position string value.
   * @example
   * axis: {
   *   y2: {
   *     label: "Your Y2 Axis"
   *   }
   * }
   *
   * axis: {
   *   y2: {
   *     label: {
   *        text: "Your Y2 Axis",
   *        position: "outer-middle"
   *     }
   *   }
   * }
   */
  axis_y2_label: {},
  /**
   * Set formatter for y2 axis tick text.<br><br>
   * This option works in the same way as axis.y.format.
   * @name axis․y2․tick․format
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       format: d3.format("$,")
   *       //or format: function(d) { return "$" + d; }
   *     }
   *   }
   * }
   */
  axis_y2_tick_format: void 0,
  /**
   * Setting for culling ticks.
   * - `true`: the ticks will be culled, then only limited tick text will be shown.<br>
   *   This option does not hide the tick lines by default, if want to hide tick lines, set `axis.y2.tick.culling.lines=false`.
   * - `false`: all of ticks will be shown.<br><br>
   * The number of ticks to be shown can be chaned by `axis.y2.tick.culling.max`.
   * @name axis․y2․tick․culling
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       culling: false
   *     }
   *   }
   * }
   */
  axis_y2_tick_culling: !1,
  /**
   * The number of tick texts will be adjusted to less than this value.
   * @name axis․y2․tick․culling․max
   * @memberof Options
   * @type {number}
   * @default 5
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       culling: {
   *           max: 5
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_culling_max: 5,
  /**
   * Control visibility of tick lines within culling option, along with tick text.
   * @name axis․y2․tick․culling․lines
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       culling: {
   *           lines: false,
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_culling_lines: !0,
  /**
   * Show or hide y2 axis outer tick.
   * @name axis․y2․tick․outer
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       outer: false
   *     }
   *   }
   * }
   */
  axis_y2_tick_outer: !0,
  /**
   * Set y2 axis tick values manually.
   * @name axis․y2․tick․values
   * @memberof Options
   * @type {Array|Function}
   * @default null
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       values: [100, 1000, 10000],
   *
   *       // an Array value should be returned
   *       values: function() {
   *       	return [ ... ];
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_values: null,
  /**
   * Rotate y2 axis tick text.
   * - If you set negative value, it will rotate to opposite direction.
   * - Applied when [`axis.rotated`](#.axis%25E2%2580%25A4rotated) option is `true`.
   * @name axis․y2․tick․rotate
   * @memberof Options
   * @type {number}
   * @default 0
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       rotate: 60
   *     }
   *   }
   * }
   */
  axis_y2_tick_rotate: 0,
  /**
   * Set the number of y2 axis ticks.
   * - **NOTE:** This works in the same way as axis.y.tick.count.
   * @name axis․y2․tick․count
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       count: 5
   *     }
   *   }
   * }
   */
  axis_y2_tick_count: void 0,
  /**
   * Show or hide y2 axis tick line.
   * @name axis․y2․tick․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       show: false
   *     }
   *   }
   * }
   */
  axis_y2_tick_show: !0,
  /**
   * Set axis tick step(interval) size.
   * - **NOTE:** Will be ignored if `axis.y2.tick.count` or `axis.y2.tick.values` options are set.
   * @name axis․y2․tick․stepSize
   * @memberof Options
   * @type {number}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.StepSizeForYAxis)
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       // tick value will step as indicated interval value.
   *       // ex) 'stepSize=15' ==> [0, 15, 30, 45, 60]
   *       stepSize: 15
   *     }
   *   }
   * }
   */
  axis_y2_tick_stepSize: null,
  /**
   * Show or hide y2 axis tick text.
   * @name axis․y2․tick․text․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       text: {
   *           show: false
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_text_show: !0,
  /**
   * Set the y2 Axis tick text's position relatively its original position
   * @name axis․y2․tick․text․position
   * @memberof Options
   * @type {object}
   * @default {x: 0, y:0}
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       text: {
   *         position: {
   *           x: 10,
   *           y: 10
   *         }
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_text_position: { x: 0, y: 0 },
  /**
   * Set padding for y2 axis.<br><br>
   * You can set padding for y2 axis to create more space on the edge of the axis.
   * This option accepts object and it can include top and bottom. top, bottom will be treated as pixels.
   *
   * - **NOTE:**
   *   - Given values are translated relative to the y2 Axis domain value for padding
   *   - For area and bar type charts, [area.zerobased](#.area) or [bar.zerobased](#.bar) options should be set to 'false` to get padded bottom.
   * @name axis․y2․padding
   * @memberof Options
   * @type {object|number}
   * @default {}
   * @example
   * axis: {
   *   y2: {
   *     padding: {
   *       top: 100,
   *       bottom: 100
   *     }
   *
   *     // or set both values at once.
   *     padding: 10
   * }
   */
  axis_y2_padding: {},
  /**
   * Set default range of y2 axis.<br><br>
   * This option set the default value for y2 axis when there is no data on init.
   * @name axis․y2․default
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     default: [0, 1000]
   *   }
   * }
   */
  axis_y2_default: void 0,
  /**
   * Set additional axes for y2 Axis.
   * - **NOTE:** Axis' scale is based on y2 Axis value if domain option isn't set.
   *
   * Each axis object should consist with following options:
   *
   * | Name | Type | Default | Description |
   * | --- | --- | --- | --- |
   * | domain | Array | - | Set the domain value |
   * | tick.outer | boolean | true | Show outer tick |
   * | tick.format | Function | - | Set formatter for tick text |
   * | tick.count | Number | - | Set the number of y axis ticks |
   * | tick.values | Array | - | Set tick values manually |
   * @name axis․y2․axes
   * @memberof Options
   * @type {Array}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.MultiAxes)
   * @see [Demo: Domain](https://naver.github.io/billboard.js/demo/#Axis.MultiAxesDomain)
   * @example
   * y2: {
   *    axes: [
   *      {
   *        // if set, will not be correlated with the main y2 Axis domain value
   *        domain: [0, 1000],
   *        tick: {
   *          outer: false,
   *          format: function(x) {
   *             return x + "%";
   *          },
   *          count: 2,
   *          values: [10, 20, 30]
   *        }
   *      },
   *      ...
   *    ]
   * }
   */
  axis_y2_axes: []
}, Qy = ye(ye(ye({
  /**
   * Setup the way to evaluate tick text size.
   * - **NOTE:**
   *   - Setting `false` or custom evaluator, highly recommended to memoize evaluated text dimension value to not degrade performance.
   * @name axis․evalTextSize
   * @memberof Options
   * @type {boolean|Function}
   * @default true
   * @example
   * axis: {
   *   // will evaluate getting text size every time.
   *   evalTextSize: false.
   *
   *   // set a custom evaluator
   *   evalTextSize: function(textElement) {
   *     // set some character to be evaluated
   *     text.textContent = "0";
   *
   *     // get the size
   *      const box = text.getBBox();
   *
   *     // clear text
   *     text.textContent = "";
   *
   *     return { w: 7, h: 12};
   *   }
   * }
   */
  axis_evalTextSize: !0,
  /**
   * Switch x and y axis position.
   * @name axis․rotated
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   rotated: true
   * }
   */
  axis_rotated: !1,
  /**
   * Set axis tooltip.
   * - **NOTE:**
   *   - When enabled, will disable default focus grid line.
   *   - For `timeseries` x Axis, tootlip will be formatted using x Axis' tick format.
   *   - For `category` x Axis, tootlip will be displaying scales' value text.
   * @name axis․tooltip
   * @memberof Options
   * @type {boolean}
   * @default false
   * @property {object} axis Axis object
   * @property {boolean} [axis.tooltip=false] Show tooltip or not.
   * @property {string|object} [axis.tooltip.backgroundColor] Set axis tooltip text background colors.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.AxisTooltip)
   * @example
   * axis: {
   *     tooltip: true, // default background color is
   *
   *     // set backgound color for axis tooltip texts
   *     tooltip: {
   *          backgroundColor: "red",
   *
   *          // set differenct backround colors per axes
   *          // NOTE: In this case, only specified axes tooltip will appear.
   *          backgroundColor: {
   *               x: "green",
   *               y: "yellow",
   *               y2: "red"
   *          }
   *     }
   * }
   */
  axis_tooltip: !1
}, qy), Zy), Ky), Jy = {
  /**
   * Set related options
   * @name grid
   * @memberof Options
   * @type {object}
   * @property {boolean} [front=false] Set 'grid & focus lines' to be positioned over grid lines and chart elements.
   * @property {object} x Grid x object
   * @property {boolean} [x.show=false] Show grids along x axis.
   * @property {Array} [x.lines=[]] Show additional grid lines along x axis.<br>
   *  This option accepts array including object that has value, text, position and class. text, position and class are optional. For position, start, middle and end (default) are available.
   *  If x axis is category axis, value can be category name. If x axis is timeseries axis, value can be date string, Date object and unixtime integer.
   * @property {object} y Grid y object
   * @property {boolean} [y.show=false] Show grids along x axis.
   * @property {Array} [y.lines=[]] Show additional grid lines along y axis.<br>
   *  This option accepts array including object that has value, text, position and class.
   * @property {number} [y.ticks=undefined] Number of y grids to be shown.
   * @property {object} focus Grid focus object
   * @property {boolean} [focus.edge=false] Show edged focus grid line.<br>**NOTE:** Available when [`tooltip.grouped=false`](#.tooltip) option is set.
   * @property {boolean} [focus.show=true] Show grid line when focus.
   * @property {boolean} [focus.y=false] Show y coordinate focus grid line.<br>**NOTE:** Available when [`tooltip.grouped=false`](#.tooltip) option is set.
   * @property {object} lines Grid lines object
   * @property {boolean} [lines.front=true] Set grid lines to be positioned over chart elements.
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Grid.GridLines)
   * @see [Demo: X Grid Lines](https://naver.github.io/billboard.js/demo/#Grid.OptionalXGridLines)
   * @see [Demo: Y Grid Lines](https://naver.github.io/billboard.js/demo/#Grid.OptionalYGridLines)
   * @example
   * grid: {
   *   x: {
   *     show: true,
   *     lines: [
   *       {value: 2, text: "Label on 2"},
   *       {value: 5, text: "Label on 5", class: "label-5"},
   *       {value: 6, text: "Label on 6", position: "start"}
   *     ]
   *   },
   *   y: {
   *     show: true,
   *     lines: [
   *       {value: 100, text: "Label on 100"},
   *       {value: 200, text: "Label on 200", class: "label-200"},
   *       {value: 300, text: "Label on 300", position: 'middle'}
   *     ],
   *     ticks: 5
   *   },
   *   front: true,
   *   focus: {
   *      show: false,
   *
   *      // Below options are available when 'tooltip.grouped=false' option is set
   *      edge: true,
   *      y: true
   *   },
   *   lines: {
   *      front: false
   *   }
   * }
   */
  grid_x_show: !1,
  grid_x_type: "tick",
  grid_x_lines: [],
  grid_y_show: !1,
  grid_y_lines: [],
  grid_y_ticks: void 0,
  grid_focus_edge: !1,
  grid_focus_show: !0,
  grid_focus_y: !1,
  grid_front: !1,
  grid_lines_front: !0
}, em = {
  /**
   * Specify the keys of the x values for each data.<br><br>
   * This option can be used if we want to show the data that has different x values.
   * @name data․xs
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   xs: {
   *      data1: "x1",
   *      data2: "x2"
   *   }
   * }
   */
  data_xs: {},
  /**
   * Set a format specifier to parse string specifed as x.
   * @name data․xFormat
   * @memberof Options
   * @type {string}
   * @default %Y-%m-%d
   * @example
   * data: {
   *    x: "x",
   *    columns: [
   *        ["x", "01012019", "02012019", "03012019"],
   *        ["data1", 30, 200, 100]
   *    ],
   *    // Format specifier to parse as datetime for given 'x' string value
   *    xFormat: "%m%d%Y"
   * },
   * axis: {
   *    x: {
   *        type: "timeseries"
   *    }
   * }
   * @see [D3's time specifier](https://d3js.org/d3-time-format#locale_format)
   */
  data_xFormat: "%Y-%m-%d",
  /**
   * Set localtime format to parse x axis.
   * @name data․xLocaltime
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * data: {
   *   xLocaltime: false
   * }
   */
  data_xLocaltime: !0,
  /**
   * Sort on x axis.
   * - **NOTE:** This option works for lineish(area/line/spline/step) types only.
   * @name data․xSort
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataXSort)
   * @example
   * data: {
   *   xSort: false,
   *   x: "x",
   *   columns: [
   *     // The line graph will start to be drawn following the x axis sequence
   *     // Below data, wil start drawing x=1: 200, x=2: 300, x=3: 100
   *     ["x", 3, 1, 2],
   *     ["data1", 100, 200, 300]
   *   ]
   * }
   */
  data_xSort: !0,
  /**
   * Set y axis the data related to. y and y2 can be used.
   * - **NOTE:** If all data is related to one of the axes, the domain of axis without related data will be replaced by the domain from the axis with related data
   * @name data․axes
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   axes: {
   *     data1: "y",
   *     data2: "y2"
   *   }
   * }
   */
  data_axes: {},
  /**
   * Define regions for each data.<br>
   * The values must be an array for each data and it should include an object that has `start`, `end` and `style`.
   * - The object type should be as:
   *   - start {number}: Start data point number. If not set, the start will be the first data point.
   *   - [end] {number}: End data point number. If not set, the end will be the last data point.
   *   - [style.dasharray="2 2"] {string}: The first number specifies a distance for the filled area, and the second a distance for the unfilled area.
   * - **NOTE:**
   *   - Supports only line type.
   *   - `start` and `end` values should be in the exact x value range.
   *   - Dashes will be applied using `stroke-dasharray` css property when data doesn't contain nullish value(or nullish value with `line.connectNull=true` set).
   *   - Dashes will be applied via path command when data contains nullish value.
   * @name data․regions
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   regions: {
   *     data1: [{
   *         start: 1,
   *         end: 2,
   *         style: {
   *             dasharray: "5 2"
   *         }
   *     }, {
   *         start: 3
   *     }],
   *     ...
   *   }
   * }
   */
  data_regions: {},
  /**
   * Set the stacking to be normalized
   * - **NOTE:**
   *   - For stacking, '[data.groups](#.data%25E2%2580%25A4groups)' option should be set
   *   - y Axis will be set in percentage value (0 ~ 100%)
   *   - Must have postive values
   * @name data․stack․normalize
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataStackNormalized)
   * @example
   * data: {
   *   stack: {
   *      normalize: true
   *   }
   * }
   */
  data_stack_normalize: !1
}, tm = [
  My,
  Ly,
  Dy,
  Iy,
  Ny,
  Py,
  Oy
], nm = {
  axis: By,
  clip: Vy,
  eventrect: zy,
  flow: Hy,
  grid: jy,
  region: Gy,
  sizeAxis: Wy
}, im = {
  optDataAxis: em,
  optAxis: Qy,
  optGrid: Jy
};
function Fu(e) {
  e === void 0 && (e = 0);
  var t = this, n = t.config, i = t.state, r = t.hasMultiArcGauge(), a = i.gaugeArcWidth / t.filterTargetsToShow(t.data.targets).length, o = e ? Math.min(i.radiusExpanded * e - i.radius, a * 0.8 - (1 - e) * 100) : 0;
  return {
    /**
     * Getter of arc innerRadius value
     * @param {IArcData} d Data object
     * @returns {number} innerRadius value
     * @private
     */
    inner: function(s) {
      var l = t.getRadius(s).innerRadius;
      return r ? i.radius - a * (s.index + 1) : ge(l) ? l : 0;
    },
    /**
     * Getter of arc outerRadius value
     * @param {IArcData} d Data object
     * @returns {number} outerRadius value
     * @private
     */
    outer: function(s) {
      var l = t.getRadius(s).outerRadius, c;
      if (r)
        c = i.radius - a * s.index + o;
      else if (t.hasType("polar") && !e)
        c = t.getPolarOuterRadius(s, l);
      else if (c = l, e) {
        var f = i.radiusExpanded;
        i.radius !== l && (f -= Math.abs(i.radius - l)), c = f * e;
      }
      return c;
    },
    /**
     * Getter of arc cornerRadius value
     * @param {IArcData} d Data object
     * @param {number} outerRadius outer radius value
     * @returns {number} cornerRadius value
     * @private
     */
    corner: function(s, l) {
      var c = n.arc_cornerRadius_ratio, f = c === void 0 ? 0 : c, h = n.arc_cornerRadius, g = h === void 0 ? 0 : h, x = s.data.id, y = s.value, T = 0;
      return f ? T = f * l : T = ge(g) ? g : g.call(t.api, x, y, l), T;
    }
  };
}
function Fa(e) {
  return function(t) {
    var n = function(r) {
      var a = r.startAngle, o = a === void 0 ? 0 : a, s = r.endAngle, l = s === void 0 ? 0 : s, c = r.padAngle, f = c === void 0 ? 0 : c;
      return {
        startAngle: o,
        endAngle: l,
        padAngle: f
      };
    }, i = Xi(n(this._current), n(t));
    return this._current = t, function(r) {
      var a = i(r), o = t.data, s = t.index, l = t.value;
      return e(ye(ye({}, a), { data: o, index: s, value: l }));
    };
  };
}
var rm = {
  initPie: function() {
    var e = this, t = e.config, n = t.data_type, i = t["".concat(n, "_padding")], r = t["".concat(n, "_startingAngle")] || 0, a = (i ? i * 0.01 : t["".concat(n, "_padAngle")]) || 0;
    e.pie = Ox().startAngle(r).endAngle(r + 2 * Math.PI).padAngle(a).value(function(o) {
      var s, l;
      return (l = (s = o.values) === null || s === void 0 ? void 0 : s.reduce(function(c, f) {
        return c + f.value;
      }, 0)) !== null && l !== void 0 ? l : o;
    }).sort(e.getSortCompareFn.bind(e)(!0));
  },
  updateRadius: function() {
    var e = this, t = e.config, n = e.state, i = t.data_type, r = t["".concat(i, "_padding")], a = t.gauge_width || t.donut_width, o = e.filterTargetsToShow(e.data.targets).length * t.gauge_arcs_minWidth;
    n.radiusExpanded = Math.min(n.arcWidth, n.arcHeight) / 2 * (e.hasMultiArcGauge() && t.gauge_label_show ? 0.85 : 1), n.radius = n.radiusExpanded * 0.95, n.innerRadiusRatio = a ? (n.radius - a) / n.radius : 0.6, n.gaugeArcWidth = a || (o <= n.radius - n.innerRadius ? n.radius - n.innerRadius : o <= n.radius ? o : n.radius);
    var s = t.pie_innerRadius || (r ? r * (n.innerRadiusRatio + 0.1) : 0);
    n.outerRadius = t.pie_outerRadius, n.innerRadius = e.hasType("donut") || e.hasType("gauge") ? n.radius * n.innerRadiusRatio : s;
  },
  /**
   * Get pie's inner & outer radius value
   * @param {object|undefined} d Data object
   * @returns {object}
   * @private
   */
  getRadius: function(e) {
    var t = this, n = e == null ? void 0 : e.data, i = t.state, r = i.innerRadius, a = i.outerRadius;
    return !ge(r) && n && (r = r[n.id] || 0), Be(a) && n && n.id in a ? a = a[n.id] : ge(a) || (a = t.state.radius), { innerRadius: r, outerRadius: a };
  },
  updateArc: function() {
    var e = this;
    e.updateRadius(), e.svgArc = e.getSvgArc(), e.svgArcExpanded = e.getSvgArcExpanded();
  },
  getArcLength: function() {
    var e = this, t = e.config, n = t.gauge_arcLength * 3.6, i = 2 * (n / 360);
    return n < -360 ? i = -2 : n > 360 && (i = 2), i * Math.PI;
  },
  getStartingAngle: function() {
    var e = this, t = e.config, n = t.data_type, i = e.hasType("gauge") ? t.gauge_fullCircle : !1, r = -1 * Math.PI / 2, a = Math.PI / 2, o = t["".concat(n, "_startingAngle")] || 0;
    return !i && o <= r ? o = r : !i && o >= a ? o = a : (o > Math.PI || o < -1 * Math.PI) && (o = Math.PI), o;
  },
  /**
   * Update angle data
   * @param {object} dValue Data object
   * @param {boolean} forRange Weather is for ranged text option(arc.rangeText.values)
   * @returns {object|null} Updated angle data
   * @private
   */
  updateAngle: function(e, t) {
    var n;
    t === void 0 && (t = !1);
    var i = this, r = i.config, a = i.state, o = t && i.hasType("gauge"), s = i.pie, l = e, c = !1;
    if (!r)
      return null;
    var f = i.getStartingAngle(), h = r.gauge_fullCircle || t && !o ? i.getArcLength() : f * -2;
    if (l.data && i.isGaugeType(l.data) && !i.hasMultiArcGauge()) {
      var g = r.gauge_min, x = r.gauge_max, y = i.getTotalDataSum(a.rendered), T = h * ((y - g) / (x - g));
      s = s.startAngle(f).endAngle(T + f);
    }
    if (t === !1 && s(i.filterTargetsToShow()).forEach(function(S, I) {
      var L;
      !c && S.data.id === ((L = l.data) === null || L === void 0 ? void 0 : L.id) && (c = !0, l = S, l.index = I);
    }), isNaN(l.startAngle) && (l.startAngle = 0), isNaN(l.endAngle) && (l.endAngle = l.startAngle), t || l.data && (r.gauge_enforceMinMax || i.hasMultiArcGauge())) {
      var g = r.gauge_min, x = r.gauge_max, A = t && !o ? i.getTotalDataSum(a.rendered) : x, w = h / (A - g), C = (n = l.value) !== null && n !== void 0 ? n : 0, R = C < g ? 0 : C < A ? C - g : A - g;
      l.startAngle = f, l.endAngle = f + w * R;
    }
    return c || t ? l : null;
  },
  getSvgArc: function() {
    var e = this, t = Fu.call(e), n = t.inner, i = t.outer, r = t.corner, a = bu().innerRadius(n).outerRadius(i), o = function(s, l) {
      var c, f = "M 0 0";
      if (s.value || s.data) {
        var h = l ? s : (c = e.updateAngle(s)) !== null && c !== void 0 ? c : null;
        h && (f = a.cornerRadius(r(h, i(h)))(h));
      }
      return f;
    };
    return o.centroid = a.centroid, o;
  },
  /**
   * Get expanded arc path function
   * @param {number} rate Expand rate
   * @returns {Function} Expanded arc path getter function
   * @private
   */
  getSvgArcExpanded: function(e) {
    e === void 0 && (e = 1);
    var t = this, n = Fu.call(t, e), i = n.inner, r = n.outer, a = n.corner, o = bu().innerRadius(i).outerRadius(r);
    return function(s) {
      var l = t.updateAngle(s), c = r(l), f = 0;
      return l && (f = a(l, c)), l ? o.cornerRadius(f)(l) : "M 0 0";
    };
  },
  getArc: function(e, t, n) {
    return n || this.isArcType(e.data) ? this.svgArc(e, t) : "M 0 0";
  },
  /**
   * Render range value text
   * @private
   */
  redrawArcRangeText: function() {
    var e = this, t = e.config, n = e.$el.arcs, i = e.state, r = e.$T, a = t.arc_rangeText_format, o = e.hasType("gauge") && t.arc_rangeText_fixed, s = t.arc_rangeText_values;
    if (s != null && s.length) {
      var l = t.arc_rangeText_unit === "%", c = e.getTotalDataSum(i.rendered);
      l && (s = s.map(function(g) {
        return c / 100 * g;
      }));
      var f = e.pie(s).map(function(g, x) {
        return g.index = x, g;
      }), h = n.selectAll(".".concat(Xe.arcRange)).data(s);
      h.exit(), h = r(h.enter().append("text").attr("class", Xe.arcRange).style("text-anchor", "middle").style("pointer-events", "none").style("opacity", "0").text(function(g) {
        var x = l ? g / c * 100 : g;
        return he(a) ? a(x) : "".concat(x).concat(l ? "%" : "");
      }).merge(h)), (!i.rendered || i.rendered && !o) && c > 0 && h.attr("transform", function(g, x) {
        return e.transformForArcLabel(f[x], !0);
      }), h.style("opacity", function(g) {
        return !o && (g > c || c === 0) ? "0" : null;
      });
    }
  },
  /**
   * Set transform attributes to arc label text
   * @param {object} d Data object
   * @param {boolean} forRange Weather is for ranged text option(arc.rangeText.values)
   * @returns {string} Translate attribute string
   * @private
   */
  transformForArcLabel: function(e, t) {
    var n, i, r;
    t === void 0 && (t = !1);
    var a = this, o = a.config, s = a.state.radiusExpanded, l = a.updateAngle(e, t), c = "";
    if (l) {
      if (t || a.hasMultiArcGauge()) {
        var f = Math.sin(l.endAngle - Math.PI / 2), h = o.arc_rangeText_position, g = Math.cos(l.endAngle - Math.PI / 2) * (s + (t ? 5 : 25)), x = f * (s + 15 - Math.abs(f * 10)) + 3;
        if (t && h) {
          var y = o.arc_rangeText_values, T = he(h) ? h(y[e.index]) : h;
          g += (n = T == null ? void 0 : T.x) !== null && n !== void 0 ? n : 0, x += (i = T == null ? void 0 : T.y) !== null && i !== void 0 ? i : 0;
        }
        c = "translate(".concat(g, ",").concat(x, ")");
      } else if (!a.hasType("gauge") || a.data.targets.length > 1) {
        var A = a.getRadius(e).outerRadius;
        a.hasType("polar") && (A = a.getPolarOuterRadius(e, A));
        var w = this.svgArc.centroid(l), C = w.map(function(p) {
          return isNaN(p) ? 0 : p;
        }), g = C[0], x = C[1], R = Math.sqrt(g * g + x * x), S = (r = ["donut", "gauge", "pie", "polar"].filter(a.hasType.bind(a)).map(function(p) {
          return o["".concat(p, "_label_ratio")];
        })) === null || r === void 0 ? void 0 : r[0];
        S ? S = he(S) ? S.bind(a.api)(e, A, R) : S : S = A && (R ? (36 / A > 0.375 ? 1.175 - 36 / A : 0.8) * A / R : 0), c = "translate(".concat(g * S, ",").concat(x * S, ")");
      }
    }
    return c;
  },
  convertToArcData: function(e) {
    return this.addName({
      id: "data" in e ? e.data.id : e.id,
      value: e.value,
      ratio: this.getRatio("arc", e),
      index: e.index
    });
  },
  textForArcLabel: function(e) {
    var t = this, n = t.hasType("gauge");
    t.shouldShowArcLabel() && e.style("fill", t.updateTextColor.bind(t)).attr("filter", function(i) {
      return t.updateTextBGColor.bind(t)(i, t.config.data_labels_backgroundColors);
    }).each(function(i) {
      var r, a = le(this), o = t.updateAngle(i), s = t.getRatio("arc", o), l = t.meetsLabelThreshold(s, (r = ["donut", "gauge", "pie", "polar"].filter(t.hasType.bind(t))) === null || r === void 0 ? void 0 : r[0]);
      if (l) {
        var c = (o || i).value, f = (t.getArcLabelFormat() || t.defaultArcValueFormat)(c, s, i.data.id).toString();
        Nr(a, f, [-1, 1], n);
      } else
        a.text("");
    });
  },
  expandArc: function(e) {
    var t = this, n = t.state.transiting, i = t.$el;
    if (n) {
      var r = setInterval(function() {
        n || (clearInterval(r), i.legend.selectAll(".".concat(ze.legendItemFocused)).size() > 0 && t.expandArc(e));
      }, 10);
      return;
    }
    var a = t.mapToTargetIds(e);
    i.svg.selectAll(t.selectorTargets(a, ".".concat(Xe.chartArc))).each(function(o) {
      if (t.shouldExpand(o.data.id)) {
        var s = t.getExpandConfig(o.data.id, "duration"), l = t.getSvgArcExpanded(t.getExpandConfig(o.data.id, "rate"));
        le(this).selectAll("path").transition().duration(s).attrTween("d", Fa(t.svgArcExpanded.bind(t))).transition().duration(s * 2).attrTween("d", Fa(l.bind(t)));
      }
    });
  },
  unexpandArc: function(e) {
    var t = this, n = t.state.transiting, i = t.$el.svg;
    if (!n) {
      var r = t.mapToTargetIds(e);
      i.selectAll(t.selectorTargets(r, ".".concat(Xe.chartArc))).selectAll("path").transition().duration(function(a) {
        return t.getExpandConfig(a.data.id, "duration");
      }).attrTween("d", Fa(t.svgArc.bind(t))), i.selectAll("".concat(Xe.arc)).style("opacity", null);
    }
  },
  /**
   * Get expand config value
   * @param {string} id data ID
   * @param {string} key config key: 'duration | rate'
   * @returns {number}
   * @private
   */
  getExpandConfig: function(e, t) {
    var n = this, i = n.config, r = {
      duration: 50,
      rate: 0.98
    }, a;
    return n.isDonutType(e) ? a = "donut" : n.isGaugeType(e) ? a = "gauge" : n.isPieType(e) && (a = "pie"), a ? i["".concat(a, "_expand_").concat(t)] : r[t];
  },
  shouldExpand: function(e) {
    var t = this, n = t.config;
    return t.isDonutType(e) && n.donut_expand || t.isGaugeType(e) && n.gauge_expand || t.isPieType(e) && n.pie_expand;
  },
  shouldShowArcLabel: function() {
    var e = this, t = e.config;
    return ["donut", "gauge", "pie", "polar"].some(function(n) {
      return e.hasType(n) && t["".concat(n, "_label_show")];
    });
  },
  getArcLabelFormat: function() {
    var e = this, t = e.config, n = function(i) {
      return i;
    };
    return ["donut", "gauge", "pie", "polar"].filter(e.hasType.bind(e)).forEach(function(i) {
      n = t["".concat(i, "_label_format")];
    }), he(n) ? n.bind(e.api) : n;
  },
  updateTargetsForArc: function(e) {
    var t = this, n = t.$el, i = t.hasType("gauge"), r = t.getChartClass("Arc"), a = t.getClass("arcs", !0), o = t.classFocus.bind(t), s = n.main.select(".".concat(Xe.chartArcs)), l = s.selectAll(".".concat(Xe.chartArc)).data(t.pie(e)).attr("class", function(f) {
      return r(f) + o(f.data);
    }), c = l.enter().append("g").attr("class", r).call(this.setCssRule(!1, ".".concat(Xe.chartArcs, " text"), [
      "pointer-events:none",
      "text-anchor:middle"
    ]));
    c.append("g").attr("class", a).merge(l), c.append("text").attr("dy", i && !t.hasMultiTargets() ? "-.1em" : ".35em").style("opacity", "0").style("text-anchor", t.getStylePropValue("middle")).style("pointer-events", t.getStylePropValue("none")), n.text = s.selectAll(".".concat(Ee.target, " text"));
  },
  initArc: function() {
    var e = this, t = e.$el;
    t.arcs = t.main.select(".".concat(Ee.chart)).append("g").attr("class", Xe.chartArcs).attr("transform", e.getTranslate("arc")), e.setArcTitle();
  },
  /**
   * Set arc title text
   * @param {string} str Title text
   * @private
   */
  setArcTitle: function(e) {
    var t = this, n = e || t.getArcTitle(), i = t.hasType("gauge");
    if (n) {
      var r = i ? qt.chartArcsGaugeTitle : Xe.chartArcsTitle, a = t.$el.arcs.select(".".concat(r));
      a.empty() && (a = t.$el.arcs.append("text").attr("class", r).style("text-anchor", "middle")), i && a.attr("dy", "-0.3em"), Nr(a, n, i ? void 0 : [-0.6, 1.35], !0);
    }
  },
  /**
   * Return arc title text
   * @returns {string} Arc title text
   * @private
   */
  getArcTitle: function() {
    var e = this, t = e.hasType("donut") && "donut" || e.hasType("gauge") && "gauge";
    return t ? e.config["".concat(t, "_title")] : "";
  },
  /**
   * Get arc title text with needle value
   * @returns {string|boolean} When title contains needle template string will return processed string, otherwise false
   * @private
   */
  getArcTitleWithNeedleValue: function() {
    var e = this, t = e.config, n = e.state, i = e.getArcTitle();
    if (i && e.config.arc_needle_show && /{=[A-Z_]+}/.test(i)) {
      var r = n.current.needle;
      return ge(r) || (r = t.arc_needle_value), Or(i, {
        NEEDLE_VALUE: ge(r) ? r : 0
      });
    }
    return !1;
  },
  redrawArc: function(e, t, n) {
    var i = this, r = i.config, a = i.state, o = i.$el.main, s = r.interaction_enabled, l = s && r.data_selection_isselectable, c = o.selectAll(".".concat(Xe.arcs)).selectAll(".".concat(Xe.arc)).data(i.arcData.bind(i));
    c.exit().transition().duration(t).style("opacity", "0").remove(), c = c.enter().append("path").attr("class", i.getClass("arc", !0)).style("fill", function(f) {
      return i.color(f.data);
    }).style("cursor", function(f) {
      var h;
      return !((h = l == null ? void 0 : l.bind) === null || h === void 0) && h.call(l, i.api)(f) ? "pointer" : null;
    }).style("opacity", "0").each(function(f) {
      i.isGaugeType(f.data) && (f.startAngle = r.gauge_startingAngle, f.endAngle = r.gauge_startingAngle), this._current = f;
    }).merge(c), i.hasType("gauge") && (i.updateGaugeMax(), i.hasMultiArcGauge() && i.redrawArcGaugeLine()), c.attr("transform", function(f) {
      return !i.isGaugeType(f.data) && n ? "scale(0)" : "";
    }).style("opacity", function(f) {
      return f === this._current ? "0" : null;
    }).each(function() {
      a.transiting = !0;
    }).transition().duration(e).attrTween("d", function(f) {
      var h = i.updateAngle(f);
      if (!h)
        return function() {
          return "M 0 0";
        };
      isNaN(this._current.startAngle) && (this._current.startAngle = 0), isNaN(this._current.endAngle) && (this._current.endAngle = this._current.startAngle);
      var g = Xi(this._current, h);
      return this._current = g(0), function(x) {
        var y = g(x);
        return y.data = f.data, i.getArc(y, !0);
      };
    }).attr("transform", n ? "scale(1)" : "").style("fill", function(f) {
      var h;
      return i.levelColor ? (h = i.levelColor(f.data.values[0].value), r.data_colors[f.data.id] = h) : h = i.color(f.data), h;
    }).style("opacity", null).call(Ir, function() {
      if (i.levelColor) {
        var f = le(this), h = f.datum(this._current);
        i.updateLegendItemColor(h.data.id, f.style("fill"));
      }
      a.transiting = !1, Ze(r.onrendered, i.api);
    }), s && i.bindArcEvent(c), i.hasType("polar") && i.redrawPolar(), i.hasType("gauge") && i.redrawBackgroundArcs(), r.arc_needle_show && i.redrawNeedle(), i.redrawArcText(e), i.redrawArcRangeText();
  },
  /**
   * Update needle element
   * @private
   */
  redrawNeedle: function() {
    var e = this, t = e.$el, n = e.config, i = e.state, r = i.hiddenTargetIds, a = i.radius, o = (a - 1) / 100 * n.arc_needle_length, s = r.length !== e.data.targets.length, l = e.$el.arcs.select(".".concat(Xe.needle)), c = n.arc_needle_path, f = n.arc_needle_bottom_width / 2, h = n.arc_needle_top_width / 2, g = n.arc_needle_top_rx, x = n.arc_needle_top_ry, y = n.arc_needle_bottom_len, T = n.arc_needle_bottom_rx, A = n.arc_needle_bottom_ry, w = e.getNeedleAngle(), C = function() {
      var S = e.getArcTitleWithNeedleValue();
      S && e.setArcTitle(S);
    };
    if (C(), l.empty() && (l = t.arcs.append("path").classed(Xe.needle, !0), t.needle = l, t.needle.updateHelper = function(S, I) {
      I === void 0 && (I = !1), t.needle.style("display") !== "none" && e.$T(t.needle).style("transform", "rotate(".concat(e.getNeedleAngle(S), "deg)")).call(Ir, function() {
        I && (n.arc_needle_value = S), C();
      });
    }), s) {
      var R = he(c) ? c.call(e, o) : "M-".concat(f, " ").concat(y, " A").concat(T, " ").concat(A, " 0 0 0 ").concat(f, " ").concat(y, " L").concat(h, " -").concat(o, " A").concat(g, " ").concat(x, " 0 0 0 -").concat(h, " -").concat(o, " L-").concat(f, " ").concat(y, " Z");
      e.$T(l).attr("d", R).style("fill", n.arc_needle_color).style("display", null).style("transform", "rotate(".concat(w, "deg)"));
    } else
      l.style("display", "none");
  },
  /**
   * Get needle angle value relative given value
   * @param {number} v Value to be calculated angle
   * @returns {number} angle value
   * @private
   */
  getNeedleAngle: function(e) {
    var t = this, n = t.config, i = t.state, r = t.getArcLength(), a = t.hasType("gauge"), o = t.getTotalDataSum(!0), s = Ye(e) ? e : n.arc_needle_value, l = n["".concat(n.data_type, "_startingAngle")] || 0, c = 0;
    if (ge(s) || (s = a && t.data.targets.length === 1 ? o : 0), i.current.needle = s, a) {
      l = t.getStartingAngle();
      var f = n.gauge_fullCircle ? r : l * -2, h = n.gauge_min, g = n.gauge_max;
      c = f * ((s - h) / (g - h));
    } else
      c = r * (s / o);
    return (l + c) * (180 / Math.PI);
  },
  redrawBackgroundArcs: function() {
    var e = this, t = e.config, n = e.state, i = e.hasMultiArcGauge(), r = t.gauge_fullCircle, a = e.filterTargetsToShow(e.data.targets).length === 0 && !!t.data_empty_label_text, o = e.getStartingAngle(), s = r ? o + e.getArcLength() : o * -1, l = e.$el.arcs.select("".concat(i ? "g" : "", ".").concat(Xe.chartArcsBackground));
    if (i) {
      var c = 0;
      l = l.selectAll("path.".concat(Xe.chartArcsBackground)).data(e.data.targets), l.enter().append("path").attr("class", function(f, h) {
        return "".concat(Xe.chartArcsBackground, " ").concat(Xe.chartArcsBackground, "-").concat(h);
      }).merge(l).style("fill", t.gauge_background || null).attr("d", function(f) {
        var h = f.id;
        if (a || n.hiddenTargetIds.indexOf(h) >= 0)
          return "M 0 0";
        var g = {
          data: [{ value: t.gauge_max }],
          startAngle: o,
          endAngle: s,
          index: c++
        };
        return e.getArc(g, !0, !0);
      }), l.exit().remove();
    } else
      l.attr("d", a ? "M 0 0" : function() {
        var f = {
          data: [{ value: t.gauge_max }],
          startAngle: o,
          endAngle: s
        };
        return e.getArc(f, !0, !0);
      });
  },
  bindArcEvent: function(e) {
    var t = this, n = t.config, i = t.state, r = i.inputType === "touch", a = i.inputType === "mouse";
    function o(c, f, h) {
      t.expandArc(h), t.api.focus(h), t.toggleFocusLegend(h, !0), t.showTooltip([f], c);
    }
    function s(c) {
      var f = void 0;
      t.unexpandArc(f), t.api.revert(), t.revertLegend(), t.hideTooltip();
    }
    if (e.on("click", function(c, f, h) {
      var g, x = t.updateAngle(f), y;
      x && (y = t.convertToArcData(x), (g = t.toggleShape) === null || g === void 0 || g.call(t, this, y, h), n.data_onclick.bind(t.api)(y, this));
    }), a && e.on("mouseover", function(c, f) {
      if (!i.transiting) {
        i.event = c;
        var h = t.updateAngle(f), g = h ? t.convertToArcData(h) : null, x = (g == null ? void 0 : g.id) || void 0;
        o(this, g, x), t.setOverOut(!0, g);
      }
    }).on("mouseout", function(c, f) {
      if (!(i.transiting || !n.interaction_onout)) {
        i.event = c;
        var h = t.updateAngle(f), g = h ? t.convertToArcData(h) : null;
        s(), t.setOverOut(!1, g);
      }
    }).on("mousemove", function(c, f) {
      var h = t.updateAngle(f), g = h ? t.convertToArcData(h) : null;
      i.event = c, t.showTooltip([g], this);
    }), r && t.hasArcType() && !t.radars) {
      var l = function(c) {
        var f, h, g = (h = (f = c.changedTouches) === null || f === void 0 ? void 0 : f[0]) !== null && h !== void 0 ? h : { clientX: 0, clientY: 0 }, x = g.clientX, y = g.clientY, T = le(je.elementFromPoint(x, y));
        return T;
      };
      t.$el.svg.on("touchstart touchmove", function(c) {
        if (!i.transiting) {
          i.event = c;
          var f = l(c), h = f.datum(), g = h != null && h.data && h.data.id ? t.updateAngle(h) : null, x = g ? t.convertToArcData(g) : null, y = (x == null ? void 0 : x.id) || void 0;
          t.callOverOutForTouch(x), at(y) ? s() : o(this, x, y);
        }
      });
    }
  },
  redrawArcText: function(e) {
    var t = this, n = t.config, i = t.state, r = t.$el, a = r.main, o = r.arcs, s = t.hasType("gauge"), l = t.hasMultiArcGauge(), c;
    if (s && t.data.targets.length === 1 && n.gauge_title || (c = a.selectAll(".".concat(Xe.chartArc)).select("text").style("opacity", "0").attr("class", function(h) {
      return t.isGaugeType(h.data) ? qt.gaugeValue : null;
    }).call(t.textForArcLabel.bind(t)).attr("transform", function(h) {
      return t.transformForArcLabel.bind(t)(h);
    }).style("font-size", function(h) {
      return t.isGaugeType(h.data) && t.data.targets.length === 1 && !l ? "".concat(Math.round(i.radius / 5), "px") : null;
    }).transition().duration(e).style("opacity", function(h) {
      return t.isTargetToShow(h.data.id) && t.isArcType(h.data) ? null : "0";
    }), l && c.attr("dy", "-.1em")), a.select(".".concat(Xe.chartArcsTitle)).style("opacity", t.hasType("donut") || s ? null : "0"), s) {
      var f = n.gauge_fullCircle;
      f && (c == null || c.attr("dy", "".concat(l ? 0 : Math.round(i.radius / 14)))), n.gauge_label_show && (o.select(".".concat(qt.chartArcsGaugeUnit)).attr("dy", "".concat(f ? 1.5 : 0.75, "em")).text(n.gauge_units), o.select(".".concat(qt.chartArcsGaugeMin)).attr("dx", "".concat(-1 * (i.innerRadius + (i.radius - i.innerRadius) / (f ? 1 : 2)), "px")).attr("dy", "1.2em").text(t.textForGaugeMinMax(n.gauge_min, !1)), !f && o.select(".".concat(qt.chartArcsGaugeMax)).attr("dx", "".concat(i.innerRadius + (i.radius - i.innerRadius) / 2, "px")).attr("dy", "1.2em").text(t.textForGaugeMinMax(n.gauge_max, !0)));
    }
  },
  /**
   * Get Arc element by id or index
   * @param {string|number} value id or index of Arc
   * @returns {d3Selection} Arc path element
   * @private
   */
  getArcElementByIdOrIndex: function(e) {
    var t = this, n = t.$el.arcs, i = ge(e) ? function(r) {
      return r.index === e;
    } : function(r) {
      return r.data.id === e;
    };
    return n == null ? void 0 : n.selectAll(".".concat(Ee.target, " path")).filter(i);
  }
}, am = {
  initArea: function(e) {
    var t = this, n = t.config;
    e.insert("g", ".".concat(n.area_front ? pt.circles : _n.lines)).attr("class", t.getClass("areas", !0));
  },
  /**
   * Update area color
   * @param {object} d Data object
   * @returns {string} Color string
   * @private
   */
  updateAreaColor: function(e) {
    var t = this;
    return t.config.area_linearGradient ? t.getGradienColortUrl(e.id) : t.color(e);
  },
  /**
   * Generate/Update elements
   * @param {boolean} withTransition Transition for exit elements
   * @param {boolean} isSub Subchart draw
   * @private
   */
  updateArea: function(e, t) {
    t === void 0 && (t = !1);
    var n = this, i = n.config, r = n.state, a = n.$el, o = n.$T, s = t ? a.subchart : a;
    i.area_linearGradient && n.updateLinearGradient();
    var l = s.main.selectAll(".".concat(io.areas)).selectAll(".".concat(io.area)).data(n.lineData.bind(n));
    o(l.exit(), e).style("opacity", "0").remove(), s.area = l.enter().append("path").attr("class", n.getClass("area", !0)).style("fill", n.updateAreaColor.bind(n)).style("opacity", function() {
      return r.orgAreaOpacity = le(this).style("opacity"), "0";
    }).merge(l), l.style("opacity", r.orgAreaOpacity), n.setRatioForGroupedData(s.area.data());
  },
  /**
   * Redraw function
   * @param {Function} drawFn Retuned functino from .generateDrawCandlestick()
   * @param {boolean} withTransition With or without transition
   * @param {boolean} isSub Subchart draw
   * @returns {Array}
   */
  redrawArea: function(e, t, n) {
    n === void 0 && (n = !1);
    var i = this, r = (n ? this.$el.subchart : this.$el).area, a = i.state.orgAreaOpacity;
    return [
      i.$T(r, t, Fn()).attr("d", e).style("fill", i.updateAreaColor.bind(i)).style("opacity", function(o) {
        return String(i.isAreaRangeType(o) ? a / 1.75 : a);
      })
    ];
  },
  /**
   * Generate area path data
   * @param {object} areaIndices Indices
   * @param {boolean} isSub Weather is sub axis
   * @returns {Function}
   * @private
   */
  generateDrawArea: function(e, t) {
    var n = this, i = n.config, r = i.line_connectNull, a = i.axis_rotated, o = n.generateGetAreaPoints(e, t), s = n.getYScaleById.bind(n), l = function(h) {
      return (t ? n.subxx : n.xx).call(n, h);
    }, c = function(h, g) {
      return n.isGrouped(h.id) ? o(h, g)[0][1] : s(h.id, t)(n.isAreaRangeType(h) ? n.getRangedData(h, "high") : n.getShapeYMin(h.id));
    }, f = function(h, g) {
      return n.isGrouped(h.id) ? o(h, g)[1][1] : s(h.id, t)(n.isAreaRangeType(h) ? n.getRangedData(h, "low") : h.value);
    };
    return function(h) {
      var g = r ? n.filterRemoveNull(h.values) : h.values, x = 0, y = 0, T;
      if (n.isAreaType(h)) {
        var A = Ix();
        A = a ? A.y(l).x0(c).x1(f) : A.x(l).y0(i.area_above ? 0 : i.area_below ? n.state.height : c).y1(f), r || (A = A.defined(function(w) {
          return n.getBaseValue(w) !== null;
        })), n.isStepType(h) && (g = n.convertValuesToStep(g)), T = A.curve(n.getCurve(h))(g);
      } else
        g[0] && (x = n.scale.x(g[0].x), y = n.getYScaleById(h.id)(g[0].value)), T = a ? "M ".concat(y, " ").concat(x) : "M ".concat(x, " ").concat(y);
      return T || "M 0 0";
    };
  },
  generateGetAreaPoints: function(e, t) {
    var n = this, i = n.config, r = n.getShapeX(0, e, t), a = n.getShapeY(!!t), o = n.getShapeOffset(n.isAreaType, e, t), s = n.getYScaleById.bind(n);
    return function(l, c) {
      var f = s.call(n, l.id, t)(n.getShapeYMin(l.id)), h = o(l, c) || f, g = r(l), x = l.value, y = a(l);
      return i.axis_rotated && (x > 0 && y < f || x < 0 && f < y) && (y = f), [
        [g, h],
        [g, y - (f - h)],
        [g, y - (f - h)],
        // needed for compatibility
        [g, h]
        // needed for compatibility
      ];
    };
  }
}, om = {
  initBar: function() {
    var e = this, t = e.$el, n = e.config, i = e.state.clip;
    t.bar = t.main.select(".".concat(Ee.chart)), t.bar = n.bar_front ? t.bar.append("g") : t.bar.insert("g", ":first-child"), t.bar.attr("class", Ot.chartBars).call(this.setCssRule(!1, ".".concat(Ot.chartBars), ["pointer-events:none"])), n.clipPath === !1 && (n.bar_radius || n.bar_radius_ratio) && t.bar.attr("clip-path", i.pathXAxis.replace(/#[^)]*/, "#".concat(i.id)));
  },
  updateTargetsForBar: function(e) {
    var t = this, n = t.config, i = t.$el, r = t.getChartClass("Bar"), a = t.getClass("bars", !0), o = t.classFocus.bind(t), s = n.interaction_enabled && n.data_selection_isselectable;
    i.bar || t.initBar();
    var l = i.main.select(".".concat(Ot.chartBars)).selectAll(".".concat(Ot.chartBar)).data(
      // remove
      e.filter(function(f) {
        return f.values.some(function(h) {
          return ge(h.value) || t.isBarRangeType(h);
        });
      })
    ).attr("class", function(f) {
      return r(f) + o(f);
    }), c = l.enter().append("g").attr("class", r).style("opacity", "0").style("pointer-events", t.getStylePropValue("none"));
    c.append("g").attr("class", a).style("cursor", function(f) {
      var h;
      return !((h = s == null ? void 0 : s.bind) === null || h === void 0) && h.call(s, t.api)(f) ? "pointer" : null;
    }).call(t.setCssRule(!0, " .".concat(Ot.bar), ["fill"], t.color));
  },
  /**
   * Generate/Update elements
   * @param {boolean} withTransition Transition for exit elements
   * @param {boolean} isSub Subchart draw
   * @private
   */
  updateBar: function(e, t) {
    t === void 0 && (t = !1);
    var n = this, i = n.config, r = n.$el, a = n.$T, o = t ? r.subchart : r, s = n.getClass("bar", !0), l = n.initialOpacity.bind(n);
    i.bar_linearGradient && n.updateLinearGradient();
    var c = o.main.selectAll(".".concat(Ot.bars)).selectAll(".".concat(Ot.bar)).data(n.labelishData.bind(n));
    a(c.exit(), e).style("opacity", "0").remove(), o.bar = c.enter().append("path").attr("class", s).style("fill", n.updateBarColor.bind(n)).merge(c).style("opacity", l), n.setRatioForGroupedData(o.bar.data());
  },
  /**
   * Update bar color
   * @param {object} d Data object
   * @returns {string} Color string
   * @private
   */
  updateBarColor: function(e) {
    var t = this, n = t.getStylePropValue(t.color);
    return t.config.bar_linearGradient ? t.getGradienColortUrl(e.id) : n ? n(e) : null;
  },
  /**
   * Redraw function
   * @param {Function} drawFn Retuned function from .getDrawShape() => .generateDrawBar()
   * @param {boolean} withTransition With or without transition
   * @param {boolean} isSub Subchart draw
   * @returns {Array}
   * @private
   */
  redrawBar: function(e, t, n) {
    n === void 0 && (n = !1);
    var i = this, r = (n ? i.$el.subchart : i.$el).bar;
    return [
      i.$T(r, t, Fn()).attr("d", function(a) {
        return (ge(a.value) || i.isBarRangeType(a)) && e(a);
      }).style("fill", i.updateBarColor.bind(i)).style("clip-path", function(a) {
        return a.clipPath;
      }).style("opacity", null)
    ];
  },
  /**
   * Generate draw function
   * @param {object} barIndices data order within x axis.
   * barIndices ==> {data1: 0, data2: 0, data3: 1, data4: 1, __max__: 1}
   *
   * When gropus given as:
   *  groups: [
   * 		["data1", "data2"],
   * 		["data3", "data4"]
   * 	],
   *
   * Will be rendered as:
   * 		data1 data3   data1 data3
   * 		data2 data4   data2 data4
   * 		-------------------------
   * 			 0             1
   * @param {boolean} isSub If is for subchart
   * @returns {Function}
   * @private
   */
  generateDrawBar: function(e, t) {
    var n = this, i = n.config, r = n.generateGetBarPoints(e, t), a = i.axis_rotated, o = i.bar_radius, s = i.bar_radius_ratio, l = ge(o) && o > 0 ? function() {
      return o;
    } : ge(s) ? function(c) {
      return c * s;
    } : null;
    return function(c, f) {
      var h = r(c, f), g = +a, x = +!g, y = c.value < 0, T = i["axis_".concat(n.axis.getId(c.id), "_inverted")], A = !T && y || T && !y, w = ["", ""], C = n.isGrouped(c.id), R = l && C ? n.isStackingRadiusData(c) : !1, S = [
        h[0][g],
        h[0][x]
      ], I = 0;
      if (c.clipPath = null, l) {
        var L = a ? x : g, p = h[2][L] - h[0][L];
        I = !C || R ? l(p) : 0;
        var U = "a".concat(I, " ").concat(I, " ").concat(A ? "1 0 0" : "0 0 1", " ");
        w[+!a] = "".concat(U).concat(I, ",").concat(I), w[+a] = "".concat(U).concat([-I, I][a ? "sort" : "reverse"]()), A && w.reverse();
      }
      var H = a ? h[1][g] + (A ? I : -I) : h[1][x] + (A ? -I : I);
      if (I) {
        var q = "";
        a ? A && S[0] < H ? q = "0 ".concat(H - S[0], "px 0 0") : !A && S[0] > H && (q = "0 0 0 ".concat(S[0] - H, "px")) : A && S[1] > H ? q = "".concat(S[1] - H, "px 0 0 0") : !A && S[1] < H && (q = "0 0 ".concat(H - S[1], "px 0")), q && (c.clipPath = "inset(".concat(q, ")"));
      }
      var X = a ? "H".concat(H, " ").concat(w[0], "V").concat(h[2][x] - I, " ").concat(w[1], "H").concat(h[3][g]) : "V".concat(H, " ").concat(w[0], "H").concat(h[2][g] - I, " ").concat(w[1], "V").concat(h[3][x]);
      return "M".concat(h[0][g], ",").concat(h[0][x]).concat(X, "z");
    };
  },
  /**
   * Determine if given stacking bar data is radius type
   * @param {object} d Data row
   * @returns {boolean}
   */
  isStackingRadiusData: function(e) {
    var t = this, n = t.$el, i = t.config, r = t.data, a = t.state, o = e.id, s = e.index, l = e.value;
    if (a.hiddenTargetIds.indexOf(o) > -1) {
      var c = n.bar.filter(function(x) {
        return x.id === o && x.value === l;
      });
      return !c.empty() && /a\d+/i.test(c.attr("d"));
    }
    var f = i.data_groups.find(function(x) {
      return x.indexOf(o) > -1;
    }), h = t.orderTargets(t.filterTargetsToShow(r.targets.filter(t.isBarType, t))).filter(function(x) {
      return f.indexOf(x.id) > -1;
    }), g = h.map(function(x) {
      return x.values.filter(function(y) {
        return y.index === s && (ge(l) && l > 0 ? y.value > 0 : y.value < 0);
      })[0];
    }).filter(Boolean).map(function(x) {
      return x.id;
    });
    return l !== 0 && g.indexOf(o) === g.length - 1;
  },
  /**
   * Generate bar coordinate points data
   * @param {object} barIndices Data order within x axis.
   * @param {boolean} isSub If is for subchart
   * @returns {Array} Array of coordinate points
   * @private
   */
  generateGetBarPoints: function(e, t) {
    var n = this, i = n.config, r = t ? n.axis.subX : n.axis.x, a = n.getIndicesMax(e) + 1, o = n.getBarW("bar", r, a), s = n.getShapeX(o, e, !!t), l = n.getShapeY(!!t), c = n.getShapeOffset(n.isBarType, e, !!t), f = n.getYScaleById.bind(n);
    return function(h, g) {
      var x = h.id, y = f.call(n, x, t)(n.getShapeYMin(x)), T = c(h, g) || y, A = ge(o) ? o : o[h.id] || o._$width, w = i["axis_".concat(n.axis.getId(x), "_inverted")], C = h.value, R = s(h), S = l(h);
      i.axis_rotated && !w && (C > 0 && S < y || C < 0 && y < S) && (S = y), n.isBarRangeType(h) || (S -= y - T);
      var I = R + A;
      return [
        [R, T],
        [R, S],
        [I, S],
        [I, T]
      ];
    };
  }
};
function sm(e, t, n, i) {
  i === void 0 && (i = !1);
  for (var r = e ? [e, 0] : n, a = function(l) {
    n.forEach(function(c) {
      l + c <= t && r.push(c), l += c;
    }), o = l;
  }, o, s = e || n.reduce(function(l, c) {
    return l + c;
  }); s <= t; )
    a(s), s = o;
  return r.length % 2 !== 0 && r.push(i ? n[1] : 0), {
    dash: r.join(" "),
    length: r.reduce(function(l, c) {
      return l + c;
    }, 0)
  };
}
function um(e, t, n) {
  var i = this, r = [], a = "2 2";
  if (Ye(t))
    for (var o = function(g, x) {
      return at(g) ? x : n ? kt.call(i, g) : g;
    }, s = 0, l = void 0; l = t[s]; s++) {
      var c = o(l.start, e[0].x), f = o(l.end, e[e.length - 1].x), h = l.style || { dasharray: a };
      r[s] = { start: c, end: f, style: h };
    }
  return r;
}
var lm = {
  initLine: function() {
    var e = this.$el;
    e.line = e.main.select(".".concat(Ee.chart)).append("g").attr("class", _n.chartLines).call(this.setCssRule(!1, ".".concat(_n.chartLines), ["pointer-events:none"]));
  },
  updateTargetsForLine: function(e) {
    var t = this, n = t.$el, i = n.area, r = n.line, a = n.main, o = t.getChartClass("Line"), s = t.getClass("lines", !0), l = t.classFocus.bind(t);
    r || t.initLine();
    var c = e.filter(function(x) {
      return !(t.isScatterType(x) || t.isBubbleType(x));
    }), f = a.select(".".concat(_n.chartLines)).selectAll(".".concat(_n.chartLine)).data(c).attr("class", function(x) {
      return o(x) + l(x);
    }), h = f.enter().append("g").attr("class", o).style("opacity", "0").style("pointer-events", t.getStylePropValue("none"));
    if (h.append("g").attr("class", s), t.hasTypeOf("Area")) {
      var g = (!i && h.empty() ? f : h).filter(t.isAreaType.bind(t));
      t.initArea(g);
    }
    t.updateTargetForCircle(c, h);
  },
  /**
   * Generate/Update elements
   * @param {boolean} withTransition Transition for exit elements
   * @param {boolean} isSub Subchart draw
   * @private
   */
  updateLine: function(e, t) {
    t === void 0 && (t = !1);
    var n = this, i = n.format.extraLineClasses, r = n.$el, a = n.$T, o = t ? r.subchart : r, s = o.main.selectAll(".".concat(_n.lines)).selectAll(".".concat(_n.line)).data(n.lineData.bind(n));
    a(s.exit(), e).style("opacity", "0").remove(), o.line = s.enter().append("path").attr("class", function(l) {
      return "".concat(n.getClass("line", !0)(l), " ").concat(i(l) || "");
    }).style("stroke", n.color).merge(s).style("opacity", n.initialOpacity.bind(n)).attr("transform", null);
  },
  /**
   * Redraw function
   * @param {Function} drawFn Retuned functino from .generateDrawCandlestick()
   * @param {boolean} withTransition With or without transition
   * @param {boolean} isSub Subchart draw
   * @returns {Array}
   * @private
   */
  redrawLine: function(e, t, n) {
    n === void 0 && (n = !1);
    var i = this, r = i.$el, a = i.$T, o = (n ? r.subchart : r).line;
    return [
      a(o, t, Fn()).attr("d", e).style("stroke", this.color).style("opacity", null)
    ];
  },
  /**
   * Get the curve interpolate
   * @param {Array} d Data object
   * @returns {Function}
   * @private
   */
  getCurve: function(e) {
    var t = this, n = t.config.axis_rotated && t.isStepType(e);
    return n ? function(i) {
      var r = t.getInterpolate(e)(i);
      return r.orgPoint = r.point, r.pointRotated = function(a, o) {
        this._point === 1 && (this._point = 2);
        var s = this._y * (1 - this._t) + o * this._t;
        this._context.lineTo(this._x, s), this._context.lineTo(a, s), this._x = a, this._y = o;
      }, r.point = function(a, o) {
        this._point === 0 ? this.orgPoint(a, o) : this.pointRotated(a, o);
      }, r;
    } : t.getInterpolate(e);
  },
  generateDrawLine: function(e, t) {
    var n = this, i = n.config, r = n.scale, a = i.line_connectNull, o = i.axis_rotated, s = n.generateGetLinePoints(e, t), l = n.getYScaleById.bind(n), c = function(x) {
      return (t ? n.subxx : n.xx).call(n, x);
    }, f = function(x, y) {
      return n.isGrouped(x.id) ? s(x, y)[0][1] : l(x.id, t)(n.getBaseValue(x));
    }, h = Bl();
    h = o ? h.x(f).y(c) : h.x(c).y(f), a || (h = h.defined(function(x) {
      return n.getBaseValue(x) !== null;
    }));
    var g = t ? r.subX : r.x;
    return function(x) {
      var y = l(x.id, t), T = a ? n.filterRemoveNull(x.values) : x.values, A = 0, w = 0, C;
      if (n.isLineType(x)) {
        var R = i.data_regions[x.id];
        R ? C = n.lineWithRegions(T, r.zoom || g, y, R) : (n.isStepType(x) && (T = n.convertValuesToStep(T)), C = h.curve(n.getCurve(x))(T));
      } else
        T[0] && (A = g(T[0].x), w = y(T[0].value)), C = o ? "M ".concat(w, " ").concat(A) : "M ".concat(A, " ").concat(w);
      return C || "M 0 0";
    };
  },
  /**
   * Set regions dasharray and get path
   * @param {Array} d Data object
   * @param {Function} x x scale function
   * @param {Function} y y scale function
   * @param {object} _regions regions to be set
   * @returns {stirng} Path string
   * @private
   */
  lineWithRegions: function(e, t, n, i) {
    for (var r = this, a = r.config, o = a.axis_rotated, s = r.axis.isTimeSeries(), l = "2 2", c = um.bind(r)(e, i, s), f = r.hasNullDataValue(e), h, g, x, y, T = o ? function(oe) {
      return n(oe.value);
    } : function(oe) {
      return t(oe.x);
    }, A = o ? function(oe) {
      return t(oe.x);
    } : function(oe) {
      return n(oe.value);
    }, w = function(oe) {
      return "M".concat(oe[0][0], ",").concat(oe[0][1], "L").concat(oe[1][0], ",").concat(oe[1][1]);
    }, C = s ? function(oe, He, Oe, K) {
      var ie = oe.x.getTime(), fe = He.x - oe.x, W = new Date(ie + fe * Oe), Le = new Date(ie + fe * (Oe + K)), Ce = o ? [[n(g(Oe)), t(W)], [n(g(Oe + x)), t(Le)]] : [[t(W), n(g(Oe))], [t(Le), n(g(Oe + x))]];
      return w(Ce);
    } : function(oe, He, Oe, K) {
      var ie = t(He.x, !o), fe = n(He.value, o), W = Oe + K, Le = t(h(Oe), !o), Ce = n(g(Oe), o), Ge = t(h(W), !o), Ve = n(g(W), o);
      Ge > ie && (Ge = ie), oe.value > He.value && (o ? Ve < fe : Ve > fe) && (Ve = fe);
      var xt = [
        [Le, Ce],
        [Ge, Ve]
      ];
      return o && xt.forEach(function(mt) {
        return mt.reverse();
      }), w(xt);
    }, R = { x: r.axis.getAxisType("x"), y: r.axis.getAxisType("y") }, S = "", I = r.$el.line.filter(function(oe) {
      var He = oe.id;
      return He === e[0].id;
    }), L = I.clone().style("display", "none"), p = function(oe, He) {
      return oe.attr("d", He).node().getTotalLength();
    }, U = {
      dash: [],
      lastLength: 0
    }, H = !1, q = 0, X = void 0; X = e[q]; q++) {
      var j = e[q - 1], z = j && Ae(j.value), G = r.isWithinRegions(X.x, c);
      if (Ae(X.value)) {
        if (at(c) || !G || !z)
          S += "".concat(q && z ? "L" : "M").concat(T(X), ",").concat(A(X));
        else if (z)
          if (G = ((G == null ? void 0 : G.dasharray) || l).split(" ").map(Number), h = Nn(R.x, j.x, X.x), g = Nn(R.y, j.value, X.value), f) {
            var _e = t(X.x) - t(j.x), Ue = n(X.value) - n(j.value), te = Math.sqrt(Math.pow(_e, 2) + Math.pow(Ue, 2));
            x = G[0] / te, y = x * G[1];
            for (var re = x; re <= 1; re += y)
              S += C(j, X, re, x), re + y >= 1 && (S += C(j, X, 1, 0));
          } else {
            var ue = [];
            if (H = X.x === e[e.length - 1].x, s) {
              var we = +j.x, st = new Date(we), Je = new Date(we + (+X.x - we));
              ue = [
                [t(st), n(g(0))],
                // M
                [t(Je), n(g(1))]
                // L
              ];
            } else
              ue = [
                [t(h(0)), n(g(0))],
                // M
                [t(h(1)), n(g(1))]
                // L
              ];
            o && ue.forEach(function(oe) {
              return oe.reverse();
            });
            var ft = p(L, S), vt = p(L, S += "L".concat(ue[1].join(","))), et = sm(ft - U.lastLength, vt - U.lastLength, G, H);
            U.lastLength += et.length, U.dash.push(et.dash);
          }
      }
    }
    return U.dash.length && (!H && U.dash.push(p(L, S)), L.remove(), I.attr("stroke-dasharray", U.dash.join(" "))), S;
  },
  isWithinRegions: function(e, t) {
    for (var n = 0, i = void 0; i = t[n]; n++)
      if (i.start < e && e <= i.end)
        return i.style;
    return !1;
  },
  isWithinStep: function(e, t) {
    return Math.abs(t - Qt(this.state.event, e)[1]) < 30;
  },
  shouldDrawPointsForLine: function(e) {
    var t = this.config.line_point;
    return t === !0 || Ie(t) && t.indexOf(e.id) !== -1;
  }
}, Wn = function() {
  return Fn();
}, gc = {
  initialOpacityForCircle: function(e) {
    var t = this, n = t.config, i = t.state.withoutFadeIn, r = n.point_opacity;
    return at(r) && (r = this.getBaseValue(e) !== null && i[e.id] ? this.opacityForCircle(e) : "0"), r;
  },
  opacityForCircle: function(e) {
    var t, n = this.config, i = n.point_opacity;
    return at(i) && (i = n.point_show && !(!((t = this.isPointFocusOnly) === null || t === void 0) && t.call(this)) ? null : "0", i = Ae(this.getBaseValue(e)) ? this.isBubbleType(e) || this.isScatterType(e) ? "0.5" : i : "0"), i;
  },
  initCircle: function() {
    var e = this, t = e.$el.main;
    !e.point && (e.point = e.generatePoint()), (e.hasType("bubble") || e.hasType("scatter")) && t.select(".".concat(Ee.chart, " > .").concat(pt.chartCircles)).empty() && t.select(".".concat(Ee.chart)).append("g").attr("class", pt.chartCircles);
  },
  updateTargetForCircle: function(e, t) {
    var n = this, i = this, r = i.config, a = i.data, o = i.$el, s = r.interaction_enabled && r.data_selection_enabled, l = s && r.data_selection_isselectable, c = i.getClass("circles", !0);
    if (r.point_show) {
      i.initCircle();
      var f = e, h = t;
      if (!f) {
        f = a.targets.filter(function(x) {
          return n.isScatterType(x) || n.isBubbleType(x);
        });
        var g = o.main.select(".".concat(pt.chartCircles)).style("pointer-events", "none").selectAll(".".concat(pt.circles)).data(f);
        g.exit().remove(), h = g.enter();
      }
      s && h.append("g").attr("class", function(x) {
        return i.generateClass(it.selectedCircles, x.id);
      }), h.append("g").attr("class", c).call(function(x) {
        i.setCssRule(!0, ".".concat(pt.circles), ["cursor:pointer"], l)(x), i.setCssRule(!0, " .".concat(pt.circle), ["fill", "stroke"], i.color)(x);
      }).style("opacity", function() {
        var x = le(this.parentNode);
        return x.attr("class").indexOf(pt.chartCircles) > -1 ? "0" : null;
      }), s && f.forEach(function(x) {
        o.main.selectAll(".".concat(it.selectedCircles).concat(i.getTargetSelectorSuffix(x.id))).selectAll("".concat(it.selectedCircle)).each(function(y) {
          y.value = x.values[y.index].value;
        });
      });
    }
  },
  updateCircle: function(e) {
    e === void 0 && (e = !1);
    var t = this, n = t.config, i = t.state, r = t.$el, a = t.isPointFocusOnly(), o = e ? r.subchart : r;
    if (n.point_show && !i.toggling) {
      n.point_radialGradient && t.updateLinearGradient();
      var s = o.main.selectAll(".".concat(pt.circles)).selectAll(".".concat(pt.circle)).data(function(l) {
        return t.isLineType(l) && t.shouldDrawPointsForLine(l) || t.isBubbleType(l) || t.isRadarType(l) || t.isScatterType(l) ? a ? [l.values[0]] : l.values : [];
      });
      s.exit().remove(), s.enter().filter(Boolean).append(t.point("create", this, t.pointR.bind(t), t.updateCircleColor.bind(t))), o.circle = o.main.selectAll(".".concat(pt.circles, " .").concat(pt.circle)).style("stroke", t.getStylePropValue(t.color)).style("opacity", t.initialOpacityForCircle.bind(t));
    }
  },
  /**
   * Update circle color
   * @param {object} d Data object
   * @returns {string} Color string
   * @private
   */
  updateCircleColor: function(e) {
    var t = this, n = t.getStylePropValue(t.color);
    return t.config.point_radialGradient ? t.getGradienColortUrl(e.id) : n ? n(e) : null;
  },
  redrawCircle: function(e, t, n, i, r) {
    r === void 0 && (r = !1);
    var a = this, o = a.state.rendered, s = a.$el, l = a.$T, c = r ? s.subchart : s, f = c.main.selectAll(".".concat(it.selectedCircle));
    if (!a.config.point_show)
      return [];
    var h = a.point("update", a, e, t, a.updateCircleColor.bind(a), n, i, f), g = a.isCirclePoint() ? "c" : "", x = Fn(), y = a.opacityForCircle.bind(a), T = [];
    return c.circle.each(function(A) {
      var w = h.bind(this)(A);
      w = l(w, n || !o, x).style("opacity", y), T.push(w);
    }), [
      T,
      l(f, n).attr("".concat(g, "x"), e).attr("".concat(g, "y"), t)
    ];
  },
  /**
   * Show focused data point circle
   * @param {object} d Selected data
   * @private
   */
  showCircleFocus: function(e) {
    var t = this, n = t.state, i = n.hasRadar, r = n.resizing, a = n.toggling, o = n.transiting, s = t.$el, l = s.circle;
    if (o === !1 && l && t.isPointFocusOnly()) {
      var c = (i ? t.radarCircleX : t.circleX).bind(t), f = (i ? t.radarCircleY : t.circleY).bind(t), h = a || at(e), g = t.point("update", t, c, f, t.getStylePropValue(t.color), r ? !1 : h);
      e && (l = l.filter(function(x) {
        var y, T = (y = e.filter) === null || y === void 0 ? void 0 : y.call(e, function(A) {
          return A.id === x.id;
        });
        return T.length ? le(this).datum(T[0]) : !1;
      })), l.attr("class", this.updatePointClass.bind(this)).style("opacity", null).each(function(x) {
        var y = x.id, T = x.index, A = x.value, w = "hidden";
        Ae(A) && (g.bind(this)(x), t.expandCircles(T, y), w = ""), this.style.visibility = w;
      });
    }
  },
  /**
   * Hide focused data point circle
   * @private
   */
  hideCircleFocus: function() {
    var e = this, t = e.$el.circle;
    e.isPointFocusOnly() && t && (e.unexpandCircles(), t.style("visibility", "hidden"));
  },
  circleX: function(e) {
    return this.xx(e);
  },
  updateCircleY: function(e) {
    e === void 0 && (e = !1);
    var t = this, n = t.generateGetLinePoints(t.getShapeIndices(t.isLineType), e);
    return function(i, r) {
      var a = i.id;
      return t.isGrouped(a) ? n(i, r)[0][1] : t.getYScaleById(a, e)(t.getBaseValue(i));
    };
  },
  expandCircles: function(e, t, n) {
    var i = this, r = i.pointExpandedR.bind(i);
    n && i.unexpandCircles();
    var a = i.getShapeByIndex("circle", e, t).classed(Ee.EXPANDED, !0), o = r(a) / i.config.point_r, s = 1 - o;
    i.isCirclePoint() ? a.attr("r", r) : a.each(function() {
      var l = le(this);
      if (this.tagName === "circle")
        l.attr("r", r);
      else {
        var c = this.getBBox(), f = c.width, h = c.height, g = s * (+l.attr("x") + f / 2), x = s * (+l.attr("y") + h / 2);
        l.attr("transform", "translate(".concat(g, " ").concat(x, ") scale(").concat(o, ")"));
      }
    });
  },
  unexpandCircles: function(e) {
    var t = this, n = t.pointR.bind(t), i = t.getShapeByIndex("circle", e).filter(function() {
      return le(this).classed(Ee.EXPANDED);
    }).classed(Ee.EXPANDED, !1);
    if (i.attr("r", n), !t.isCirclePoint()) {
      var r = n(i) / t.config.point_r;
      i.attr("transform", r !== 1 ? "scale(".concat(r, ")") : null);
    }
  },
  pointR: function(e) {
    var t = this, n = t.config, i = n.point_r, r = i;
    return t.isBubbleType(e) ? r = t.getBubbleR(e) : he(i) && (r = i.bind(t.api)(e)), e.r = r, r;
  },
  pointExpandedR: function(e) {
    var t = this, n = t.config, i = t.isBubbleType(e) ? 1.15 : 1.75;
    return n.point_focus_expand_enabled ? n.point_focus_expand_r || t.pointR(e) * i : t.pointR(e);
  },
  pointSelectR: function(e) {
    var t = this, n = t.config.point_select_r;
    return he(n) ? n(e) : n || t.pointR(e) * 4;
  },
  /**
   * Check if point.focus.only option can be applied.
   * @returns {boolean}
   * @private
   */
  isPointFocusOnly: function() {
    var e = this;
    return e.config.point_focus_only && !e.hasType("bubble") && !e.hasType("scatter") && !e.hasArcType(null, ["radar"]);
  },
  isWithinCircle: function(e, t) {
    var n = this.state, i = Qt(n.event, e), r = le(e), a = this.isCirclePoint(e) ? "c" : "", o = this.getPointSensitivity(r == null ? void 0 : r.datum()), s = +r.attr("".concat(a, "x")), l = +r.attr("".concat(a, "y"));
    if (!(s || l) && e.nodeType === 1) {
      var c = vr(e), f = c.x, h = c.y;
      s = f, l = h;
    }
    return Math.sqrt(Math.pow(s - i[0], 2) + Math.pow(l - i[1], 2)) < (t || o);
  },
  /**
   * Get data point sensitivity radius
   * @param {object} d Data point object
   * @returns {number} return the sensitivity value
   */
  getPointSensitivity: function(e) {
    var t = this, n = t.config.point_sensitivity;
    if (e)
      he(n) ? n = n.call(t.api, e) : n === "radius" && (n = e.r);
    else
      return n;
    return n;
  },
  updatePointClass: function(e) {
    var t = this, n = t.$el.circle, i = !1;
    return (Be(e) || n) && (i = e === !0 ? n.each(function(r) {
      var a = t.getClass("circle", !0)(r);
      this.getAttribute("class").indexOf(Ee.EXPANDED) > -1 && (a += " ".concat(Ee.EXPANDED)), this.setAttribute("class", a);
    }) : t.getClass("circle", !0)(e)), i;
  },
  generateGetLinePoints: function(e, t) {
    var n = this, i = n.config, r = n.getShapeX(0, e, t), a = n.getShapeY(t), o = n.getShapeOffset(n.isLineType, e, t), s = n.getYScaleById.bind(n);
    return function(l, c) {
      var f = s.call(n, l.id, t)(n.getShapeYMin(l.id)), h = o(l, c) || f, g = r(l), x = a(l);
      i.axis_rotated && (l.value > 0 && x < f || l.value < 0 && f < x) && (x = f);
      var y = [g, x - (f - h)];
      return [
        y,
        y,
        // from here and below, needed for compatibility
        y,
        y
      ];
    };
  },
  custom: {
    create: function(e, t, n) {
      return e.append("use").attr("xlink:href", "#".concat(t)).attr("class", this.updatePointClass.bind(this)).style("fill", n).node();
    },
    update: function(e, t, n, i, r, a, o) {
      var s = this, l = e.node().getBBox(), c = l.width, f = l.height, h = function(y) {
        return Ae(y.value) ? t(y) - c / 2 : 0;
      }, g = function(y) {
        return Ae(y.value) ? n(y) - f / 2 : 0;
      }, x = e;
      return r && (a && x.attr("x", h), x = s.$T(x, r, Wn()), o && s.$T(o, r, Wn())), x.attr("x", h).attr("y", g).style("fill", i);
    }
  },
  // 'circle' data point
  circle: {
    create: function(e, t, n) {
      return e.append("circle").attr("class", this.updatePointClass.bind(this)).attr("r", t).style("fill", n).node();
    },
    update: function(e, t, n, i, r, a, o) {
      var s = this, l = e;
      return s.hasType("bubble") && l.attr("r", s.pointR.bind(s)), r && (a && l.attr("cx", t), l.attr("cx") && (l = s.$T(l, r, Wn())), o && s.$T(l, r, Wn())), l.attr("cx", t).attr("cy", n).style("fill", i);
    }
  },
  // 'rectangle' data point
  rectangle: {
    create: function(e, t, n) {
      var i = function(r) {
        return t(r) * 2;
      };
      return e.append("rect").attr("class", this.updatePointClass.bind(this)).attr("width", i).attr("height", i).style("fill", n).node();
    },
    update: function(e, t, n, i, r, a, o) {
      var s = this, l = s.config.point_r, c = function(g) {
        return t(g) - l;
      }, f = function(g) {
        return n(g) - l;
      }, h = e;
      return r && (a && h.attr("x", c), h = s.$T(h, r, Wn()), o && s.$T(o, r, Wn())), h.attr("x", c).attr("y", f).style("fill", i);
    }
  }
};
function cm(e) {
  return Jt(e) && he(e.create) && he(e.update);
}
function fm(e, t) {
  var n, i = this, r = function(c, f) {
    for (var h = c.attributes, g = 0, x; x = h[g]; g++)
      x = x.name, f.setAttribute(x, c.getAttribute(x));
  }, a = new DOMParser().parseFromString(e, "image/svg+xml"), o = a.documentElement, s = je.createElementNS(Jn.svg, o.nodeName.toLowerCase());
  if (s.id = t, s.style.fill = "inherit", s.style.stroke = "inherit", r(o, s), !((n = o.childNodes) === null || n === void 0) && n.length) {
    var l = le(s);
    "innerHTML" in s ? l.html(o.innerHTML) : ai(o.childNodes).forEach(function(c) {
      r(c, l.append(c.tagName).node());
    });
  }
  i.$el.defs.node().appendChild(s);
}
var Zr = {
  /**
   * Check if point type option is valid
   * @param {string} type point type
   * @returns {boolean}
   * @private
   */
  hasValidPointType: function(e) {
    return /^(circle|rect(angle)?|polygon|ellipse|use)$/i.test(e || this.config.point_type);
  },
  /**
   * Check if pattern point is set to be used on legend
   * @returns {boolean}
   * @private
   */
  hasLegendDefsPoint: function() {
    var e, t = this.config;
    return t.legend_show && ((e = t.point_pattern) === null || e === void 0 ? void 0 : e.length) && t.legend_usePoint;
  },
  getDefsPointId: function(e) {
    var t = this.state.datetimeId;
    return "".concat(t, "-point").concat(e);
  },
  /**
   * Get generate point function
   * @returns {Function}
   * @private
   */
  generatePoint: function() {
    var e = this, t = e.$el, n = e.config, i = [], r = Qe(n.point_pattern) ? n.point_pattern : [n.point_type];
    return function(a, o) {
      for (var s = [], l = 2; l < arguments.length; l++)
        s[l - 2] = arguments[l];
      return function(c) {
        var f, h, g, x, y = e.getTargetSelectorSuffix(c.id || ((f = c.data) === null || f === void 0 ? void 0 : f.id) || c), T = le(this);
        i.indexOf(y) < 0 && i.push(y);
        var A = r[i.indexOf(y) % r.length];
        if (e.hasValidPointType(A))
          A = e[A];
        else if (!cm(A || n.point_type)) {
          var w = e.getDefsPointId(y), C = t.defs.select("#".concat(w));
          if (C.size() < 1 && fm.bind(e)(A, w), a === "create")
            return (h = e.custom) === null || h === void 0 ? void 0 : h.create.bind(o).apply(void 0, Ke([T, w], s, !1));
          if (a === "update")
            return (g = e.custom) === null || g === void 0 ? void 0 : g.update.bind(o).apply(void 0, Ke([T], s, !1));
        }
        return (x = A[a]) === null || x === void 0 ? void 0 : x.bind(o).apply(void 0, Ke([T], s, !1));
      };
    };
  }
}, Kr = {
  /**
   * Set point options
   * @name point
   * @memberof Options
   * @type {object}
   * @property {object} point Point object
   * @property {boolean} [point.show=true] Whether to show each point in line.
   * @property {number|Function} [point.r=2.5] The radius size of each point.
   *  - **NOTE:** Disabled for 'bubble' type
   * @property {boolean|object} [point.radialGradient=false] Set the radial gradient on point.<br><br>
   * Or customize by giving below object value:
   *  - cx {number}: `cx` value (default: `0.3`)
   *  - cy {number}: `cy` value (default: `0.3`)
   *  - r {number}: `r` value (default: `0.7`)
   *  - stops {Array}: Each item should be having `[offset, stop-color, stop-opacity]` values.
   *    - (default: `[[0.1, $DATA_COLOR, 1], [0.9, $DATA_COLOR, 0]]`)
   * @property {boolean} [point.focus.expand.enabled=true] Whether to expand each point on focus.
   * @property {number} [point.focus.expand.r=point.r*1.75] The radius size of each point on focus.
   *  - **NOTE:** For 'bubble' type, the default is `bubbleSize*1.15`
   * @property {boolean} [point.focus.only=false] Show point only when is focused.
   * @property {number|null} [point.opacity=undefined] Set point opacity value.
   * - **NOTE:**
   * 	- `null` will make to not set inline 'opacity' css prop.
   * 	- when no value(or undefined) is set, it defaults to set opacity value according its chart types.
   * @property {number|string|Function} [point.sensitivity=10] The senstivity value for interaction boundary.
   * - **Available Values:**
   *   - {number}: Absolute sensitivity value which is the distance from the data point in pixel.
   *   - "radius": sensitivity based on point's radius
   *   - Function: callback for each point to determine the sensitivity<br>
   *    	```js
   *   	sensitivity: function(d) {
   * 	  // ex. of argument d:
   * 	  // ==> {x: 2, value: 55, id: 'data3', index: 2, r: 19.820624179302296}
   *
   * 	  // returning d.r, will make sensitivity same as point's radius value.
   *  	  return d.r;
   * 	}
   * 	```
   * @property {number} [point.select.r=point.r*4] The radius size of each point on selected.
   * @property {string} [point.type="circle"] The type of point to be drawn
   * - **NOTE:**
   *   - If chart has 'bubble' type, only circle can be used.
   *   - For IE, non circle point expansions are not supported due to lack of transform support.
   * - **Available Values:**
   *   - circle
   *   - rectangle
   * @property {Array} [point.pattern=[]] The type of point or svg shape as string, to be drawn for each line
   * - **NOTE:**
   *   - This is an `experimental` feature and can have some unexpected behaviors.
   *   - If chart has 'bubble' type, only circle can be used.
   *   - For IE, non circle point expansions are not supported due to lack of transform support.
   * - **Available Values:**
   *   - circle
   *   - rectangle
   *   - svg shape tag interpreted as string<br>
   *     (ex. `<polygon points='2.5 0 0 5 5 5'></polygon>`)
   * @see [Demo: point type](https://naver.github.io/billboard.js/demo/#Point.RectanglePoints)
   * @see [Demo: point focus only](https://naver.github.io/billboard.js/demo/#Point.FocusOnly)
   * @see [Demo: point radialGradient](https://naver.github.io/billboard.js/demo/#Point.RadialGradientPoint)
   * @see [Demo: point sensitivity](https://naver.github.io/billboard.js/demo/#Point.PointSensitivity)
   * @example
   *  point: {
   *      show: false,
   *      r: 5,
   *
   *      // or customize the radius
   *      r: function(d) {
   *          ...
   *          return r;
   *      },
   *
   *      // will generate follwing radialGradient:
   *      // for more info: https://developer.mozilla.org/en-US/docs/Web/SVG/Element/radialGradient
   *      // <radualGradient cx="0.3" cy="0.3" r="0.7">
   *      //    <stop offset="0.1" stop-color="$DATA_COLOR" stop-opacity="1"></stop>
   *      //    <stop offset="0.9" stop-color="$DATA_COLOR" stop-opacity="0"></stop>
   *      // </radialrGradient>
   *      radialGradient: true,
   *
   *      // Or customized gradient
   *      radialGradient: {
   *      	cx: 0.3,  // cx attributes
   *      	cy: 0.5,  // cy attributes
   *      	r: 0.7,  // r attributes
   *      	stops: [
   *      	  // offset, stop-color, stop-opacity
   *      	  [0, "#7cb5ec", 1],
   *
   *      	  // setting 'null' for stop-color, will set its original data color
   *      	  [0.5, null, 0],
   *
   *      	  // setting 'function' for stop-color, will pass data id as argument.
   *      	  // It should return color string or null value
   *      	  [1, function(id) { return id === "data1" ? "red" : "blue"; }, 0],
   *      	]
   *      },
   *
   *      focus: {
   *          expand: {
   *              enabled: true,
   *              r: 1
   *          },
   *          only: true
   *      },
   *
   *      // do not set inline 'opacity' css prop setting
   *      opacity: null,
   *
   *      // set every data point's opacity value
   *      opacity: 0.7,
   *
   *      select: {
   *          r: 3
   *      },
   *
   *      // having lower value, means how closer to be for interaction
   *      sensitivity: 3,
   *
   *      // sensitivity based on point's radius
   *      sensitivity: "radius",
   *
   *      // callback for each point to determine the sensitivity
   *      sensitivity: function(d) {
   * 	// ex. of argument d:
   * 	// ==> {x: 2, value: 55, id: 'data3', index: 2, r: 19.820624179302296}
   *
   * 	// returning d.r, will make sensitivity same as point's radius value.
   * 	return d.r;
   *      }
   *
   *      // valid values are "circle" or "rectangle"
   *      type: "rectangle",
   *
   *      // or indicate as pattern
   *      pattern: [
   *        "circle",
   *        "rectangle",
   *        "<polygon points='0 6 4 0 -4 0'></polygon>"
   *     ],
   *  }
   */
  point_show: !0,
  point_r: 2.5,
  point_radialGradient: !1,
  point_sensitivity: 10,
  point_focus_expand_enabled: !0,
  point_focus_expand_r: void 0,
  point_focus_only: !1,
  point_opacity: void 0,
  point_pattern: [],
  point_select_r: void 0,
  point_type: "circle"
}, dm = {
  /**
   * Set area options
   * @name area
   * @memberof Options
   * @type {object}
   * @property {object} area Area object
   * @property {boolean} [area.above=false] Set background area `above` the data chart line.
   * @property {boolean} [area.below=false] Set background area `below` the data chart line.
   *  - **NOTE**: Can't be used along with `above` option. When above & below options are set to true, `above` will be prioritized.
   * @property {boolean} [area.front=true] Set area node to be positioned over line node.
   * @property {boolean|object} [area.linearGradient=false] Set the linear gradient on area.<br><br>
   * Or customize by giving below object value:
   *  - x {Array}: `x1`, `x2` value (default: `[0, 0]`)
   *  - y {Array}: `y1`, `y2` value (default: `[0, 1]`)
   *  - stops {Array}: Each item should be having `[offset, stop-color, stop-opacity]` values.
   *    - (default: `[[0, $DATA_COLOR, 1], [1, $DATA_COLOR, 0]]`)
   * @property {boolean} [area.zerobased=true] Set if min or max value will be 0 on area chart.
   * @see [MDN's &lt;linearGradient>](https://developer.mozilla.org/en-US/docs/Web/SVG/Element/linearGradient), [&lt;stop>](https://developer.mozilla.org/en-US/docs/Web/SVG/Element/stop)
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Chart.AreaChart)
   * @see [Demo: above](https://naver.github.io/billboard.js/demo/#AreaChartOptions.Above)
   * @see [Demo: below](https://naver.github.io/billboard.js/demo/#AreaChartOptions.Below)
   * @see [Demo: linearGradient](https://naver.github.io/billboard.js/demo/#AreaChartOptions.LinearGradient)
   * @example
   *  area: {
   *      above: true,
   *      below: false,
   *      zerobased: false,
   *
   *      // <g class='bb-areas'> will be positioned behind the line <g class='bb-lines'> in stacking order
   *      front: false,
   *
   *      // will generate follwing linearGradient:
   *      // for more info: https://developer.mozilla.org/en-US/docs/Web/SVG/Element/linearGradient
   *      // <linearGradient x1="0" x2="0" y1="0" y2="1">
   *      //    <stop offset="0" stop-color="$DATA_COLOR" stop-opacity="1"></stop>
   *      //    <stop offset="1" stop-color="$DATA_COLOR" stop-opacity="0"></stop>
   *      // </linearGradient>
   *      linearGradient: true,
   *
   *      // Or customized gradient
   *      linearGradient: {
   *      	x: [0, 0],  // x1, x2 attributes
   *      	y: [0, 0],  // y1, y2 attributes
   *      	stops: [
   *      	  // offset, stop-color, stop-opacity
   *      	  [0, "#7cb5ec", 1],
   *
   *      	  // setting 'null' for stop-color, will set its original data color
   *      	  [0.5, null, 0],
   *
   *      	  // setting 'function' for stop-color, will pass data id as argument.
   *      	  // It should return color string or null value
   *      	  [1, function(id) { return id === "data1" ? "red" : "blue"; }, 0],
   *      	]
   *      }
   *  }
   */
  area_above: !1,
  area_below: !1,
  area_front: !0,
  area_linearGradient: !1,
  area_zerobased: !0
}, hm = {
  /**
   * Set bar options
   * @name bar
   * @memberof Options
   * @type {object}
   * @property {object} bar Bar object
   * @property {boolean} [bar.front=false] Set 'bar' to be positioned over(on the top) other shapes elements.
   * @property {number} [bar.indices.removeNull=false] Remove nullish data on bar indices positions.
   * @property {number} [bar.label.threshold=0] Set threshold ratio to show/hide labels.
   * @property {boolean|object} [bar.linearGradient=false] Set the linear gradient on bar.<br><br>
   * Or customize by giving below object value:
   *  - x {Array}: `x1`, `x2` value (default: `[0, 0]`)
   *  - y {Array}: `y1`, `y2` value (default: `[0, 1]`)
   *  - stops {Array}: Each item should be having `[offset, stop-color, stop-opacity]` values.
   *    - (default: `[[0, $DATA_COLOR, 1], [1, $DATA_COLOR, 0]]`)
   * @property {boolean} [bar.overlap=false] Bars will be rendered at same position, which will be overlapped each other. (for non-grouped bars only)
   * @property {number} [bar.padding=0] The padding pixel value between each bar.
   * @property {number} [bar.radius] Set the radius of bar edge in pixel.
   * @property {number} [bar.radius.ratio] Set the radius ratio of bar edge in relative the bar's width.
   * @property {number} [bar.sensitivity=2] The senstivity offset value for interaction boundary.
   * @property {number|Function|object} [bar.width] Change the width of bar chart.
   * @property {number} [bar.width.ratio=0.6] Change the width of bar chart by ratio.
   * - **NOTE:** Criteria for ratio.
   *   - When x ticks count is same with the data count, the baseline for ratio is the minimum interval value of x ticks.
   * 	   - ex. when timeseries x values are: [2024-01-01, 2024-02-01, 2024-03-01], the minimum interval will be `2024-02-01 ~ 2024-03-01`
   *     - if the minimum interval is 30px, then ratio=1 means 30px.
   *   - When x ticks count is lower than the data count, the baseline will be calculated as `chart width / data count`.
   * 	   - ex. when chart width is 500, data count is 5, then ratio=1 means 100px.
   * @property {number} [bar.width.max] The maximum width value for ratio.
   * @property {number} [bar.width.dataname] Change the width of bar for indicated dataset only.
   * @property {number} [bar.width.dataname.ratio=0.6] Change the width of bar chart by ratio.
   *  - **NOTE:**
   *   - Works only for non-stacked bar
   * @property {number} [bar.width.dataname.max] The maximum width value for ratio.
   * @property {boolean} [bar.zerobased=true] Set if min or max value will be 0 on bar chart.
   * @see [Demo: bar front](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarFront)
   * @see [Demo: bar indices](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarIndices)
   * @see [Demo: bar overlap](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarOverlap)
   * @see [Demo: bar padding](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarPadding)
   * @see [Demo: bar radius](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarRadius)
   * @see [Demo: bar width](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarWidth)
   * @see [Demo: bar width variant](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarWidthVariant)
   * @example
   *  bar: {
   *      // make bar shape to be positioned over the other shape elements
   *      front: true,
   *
   *      // remove nullish data on bar indices postions
   *      indices: {
   *          removeNull: true
   *      },
   *
   *      // will generate follwing linearGradient:
   *      // for more info: https://developer.mozilla.org/en-US/docs/Web/SVG/Element/linearGradient
   *      // <linearGradient x1="0" x2="0" y1="0" y2="1">
   *      //    <stop offset="0" stop-color="$DATA_COLOR" stop-opacity="1"></stop>
   *      //    <stop offset="1" stop-color="$DATA_COLOR" stop-opacity="0"></stop>
   *      // </linearGradient>
   *      linearGradient: true,
   *
   *      // Or customized gradient
   *      linearGradient: {
   *      	x: [0, 0],  // x1, x2 attributes
   *      	y: [0, 0],  // y1, y2 attributes
   *      	stops: [
   *      	  // offset, stop-color, stop-opacity
   *      	  [0, "#7cb5ec", 1],
   *
   *      	  // setting 'null' for stop-color, will set its original data color
   *      	  [0.5, null, 0],
   *
   *      	  // setting 'function' for stop-color, will pass data id as argument.
   *      	  // It should return color string or null value
   *      	  [1, function(id) { return id === "data1" ? "red" : "blue"; }, 0],
   *      	]
   *      },
   *
   *      // remove nullish da
   *      overlap: true,
   *
   *      padding: 1,
   *
   *      // bar radius
   *      radius: 10,
   *      // or
   *      radius: {
   *          ratio: 0.5
   *      }
   *
   *      label: {
   *          // 0.1(10%) ratio value means, the minimum ratio to show text label relative to the y Axis domain range value.
   *          // if data value is below than 0.1, text label will be hidden.
   *          threshold: 0.1,
   *      },
   *
   *      // will not have offset between each bar elements for interaction
   *      sensitivity: 0,
   *
   *      width: 10,
   *
   *      // or specify width callback. The callback will receive width, targetsNum, maxDataCount as arguments.
   *      // - width: chart area width
   *      // - targetsNum: number of targets
   *      // - maxDataCount: maximum data count among targets
   *      width: function(width, targetsNum, maxDataCount) {
   *            return width / (targetsNum * maxDataCount);
   *      }
   *
   *      // or specify ratio & max
   *      width: {
   *          ratio: 0.2,
   *          max: 20
   *      },
   *
   *      // or specify width per dataset
   *      width: {
   *          data1: 20,
   *          data2: {
   *              ratio: 0.2,
   *              max: 20
   *          }
   *      },
   *
   *      zerobased: false
   *  }
   */
  bar_front: !1,
  bar_indices_removeNull: !1,
  bar_label_threshold: 0,
  bar_linearGradient: !1,
  bar_overlap: !1,
  bar_padding: 0,
  bar_radius: void 0,
  bar_radius_ratio: void 0,
  bar_sensitivity: 2,
  bar_width: void 0,
  bar_width_ratio: 0.6,
  bar_width_max: void 0,
  bar_zerobased: !0
}, gm = {
  /**
   * Set line options
   * @name line
   * @memberof Options
   * @type {object}
   * @property {object} line Line object
   * @property {boolean} [line.connectNull=false] Set if null data point will be connected or not.<br>
   *  If true set, the region of null data will be connected without any data point. If false set, the region of null data will not be connected and get empty.
   * @property {Array}   [line.classes=undefined] If set, used to set a css class on each line.
   * @property {boolean} [line.step.type=step] Change step type for step chart.<br>
   * **Available values:**
   * - step
   * - step-before
   * - step-after
   * @property {boolean} [line.step.tooltipMatch=false] Set to `true` for `step-before` and `step-after` types to have cursor/tooltip match to hovered step's point instead of nearest point.
   * @property {boolean|Array} [line.point=true] Set to false to not draw points on linecharts. Or pass an array of line ids to draw points for.
   * @property {boolean} [line.zerobased=false] Set if min or max value will be 0 on line chart.
   * @example
   *  line: {
   *      connectNull: true,
   *      classes: [
   *          "line-class1",
   *          "line-class2"
   *      ],
   *      step: {
   *          type: "step-after",
   *
   *          // to have cursor/tooltip match to hovered step's point instead of nearest point.
   *          tooltipMatch: true
   *      },
   *
   *      // hide all data points ('point.show=false' also has similar effect)
   *      point: false,
   *
   *      // show data points for only indicated datas
   *      point: [
   *          "data1", "data3"
   *      ],
   *
   *      zerobased: false
   *  }
   */
  line_connectNull: !1,
  line_step_type: "step",
  line_step_tooltipMatch: !1,
  line_zerobased: !1,
  line_classes: void 0,
  line_point: !0
}, pm = {
  /**
   * Set scatter options
   * @name scatter
   * @memberof Options
   * @type {object}
   * @property {object} [scatter] scatter object
   * @property {boolean} [scatter.zerobased=false] Set if min or max value will be 0 on scatter chart.
   * @example
   *  scatter: {
   *      connectNull: true,
   *      step: {
   *          type: "step-after"
   *      },
   *
   *      // hide all data points ('point.show=false' also has similar effect)
   *      point: false,
   *
   *      // show data points for only indicated datas
   *      point: [
   *          "data1", "data3"
   *      ],
   *
   *      zerobased: false
   *  }
   */
  scatter_zerobased: !1
}, vm = {
  /**
   * Set arc options
   * @name arc
   * @memberof Options
   * @type {object}
   * @property {object} arc Arc object
   * @property {number|Function} [arc.cornerRadius=0] Set corner radius of Arc(donut/gauge/pie/polar) shape.
   *  - **NOTE:**
   * 	  - Corner radius can't surpass the `(outerRadius - innerRadius) /2` of indicated shape.
   * @property {number} [arc.cornerRadius.ratio=0] Set ratio relative of outer radius.
   * @property {object} [arc.needle] Set needle options.
   * @property {boolean} [arc.needle.show=false] Show or hide needle.
   * @property {string} [arc.needle.color] Set needle filled color.
   * @property {Function} [arc.needle.path] Set custom needle path function.
   *  - **NOTE:**
   *   - The path should be starting from 0,0 (which is center) to top center coordinate.
   *   - The function will receive, `length`{number} parameter which indicating the needle length in pixel relative to radius.
   * @property {number} [arc.needle.value] Set needle value.
   *  - **NOTE:**
   *   - For single gauge chart, needle will point the data value by default, otherwise will point 0(zero).
   * @property {number} [arc.needle.length=100] Set needle length in percentages relative to radius.
   * @property {object} [arc.needle.top] Set needle top options.
   * @property {number} [arc.needle.top.rx=0] Set needle top [rx radius value](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve).
   * @property {number} [arc.needle.top.ry=0] Set needle top [ry radius value](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve).
   * @property {number} [arc.needle.top.width=0] Set needle top width in pixel.
   * @property {object} [arc.needle.bottom] Set needle bottom options.
   * @property {number} [arc.needle.bottom.rx=1] Set needle bottom [rx radius value](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve).
   * @property {number} [arc.needle.bottom.ry=1] Set needle bottom [ry radius value](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve).
   * @property {number} [arc.needle.bottom.width=15] Set needle bottom width in pixel.
   * @property {number} [arc.needle.bottom.len=0] Set needle bottom length in pixel. Setting this value, will make bottom larger starting from center.
   * @property {object} [arc.rangeText] Set rangeText options.
   * @property {Array} [arc.rangeText.values] Set range text values to be shown around Arc.
   * - When `unit: 'absolute'`: Given values are treated as absolute values.
   * - When `unit: '%'`: Given values are treated as percentages.
   * @property {string} [arc.rangeText.unit="absolute"] Specify the range text unit.
   * - "absolute": Show absolute value
   * - "%": Show percentage value
   * @property {boolean} [arc.rangeText.fiexed=false] Set if range text shown will be fixed w/o data toggle update. Only available for gauge chart.
   * @property {Function} [arc.rangeText.format] Set format function for the range text.
   * @property {number} [arc.rangeText.position] Set position function or object for the range text.
   * @see [Demo: Donut corner radius](https://naver.github.io/billboard.js/demo/#DonutChartOptions.DonutCornerRadius)
   * @see [Demo: Donut corner radius](https://naver.github.io/billboard.js/demo/#PieChartOptions.CornerRadius)
   * @see [Demo: Donut needle](https://naver.github.io/billboard.js/demo/#DonutChartOptions.DonutNeedle)
   * @see [Demo: Donut RangeText](https://naver.github.io/billboard.js/demo/#DonutChartOptions.DonutRangeText)
   * @see [Demo: Gauge corner radius](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeCornerRadius)
   * @see [Demo: Gauge needle](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeNeedle)
   * @see [Demo: Gauge RangeText](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeRangeText)
   * @example
   *  arc: {
   *      cornerRadius: 12,
   *
   *      // can customize corner radius for each data with function callback
   *      //
   *      // The function will receive:
   *      // - id {string}: Data id
   *      // - value {number}: Data value
   *      // - outerRadius {number}: Outer radius value
   *      cornerRadius: function(id, value, outerRadius) {
   *          return (id === "data1" && value > 10) ?
   *          	50 : outerRadius * 1.2;
   *      },
   *
   *      // set ratio relative of outer radius
   *      cornerRadius: {
   *          ratio: 0.5
   *      },
   *
   *      needle: {
   *       	show: true,
   *       	color: "red", // any valid CSS color
   *       	path: function(length) {
   *       	  const len = length - 20;
   *
   *       	  // will return upper arrow shape path
   *       	  // Note: The path should begun from '0,0' coordinate to top center.
   *       	  const path = `M 0 -${len + 20}
   *       		L -12 -${len}
   *       		L -5 -${len}
   *       		L -5 0
   *       		A 1 1 0 0 0 5 0
   *       		L 5 -${len}
   *       		L 12 -${len} Z`;
   *
   *       	  return path;
   *       	},
   *       	value: 40,  // will make needle to point value 40.
   *       	length: 80, // needle length in percentages relative to radius.
   *
   *       	top: {
   *       	  // rx and ry are the two radii of the ellipse;
   *       	  // https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve
   *       	  rx: 1,
   *       	  ry: 1,
   *       	  width: 5
   *       	},
   *       	bottom: {
   *       	  // rx and ry are the two radii of the ellipse;
   *       	  // https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve
   *       	  rx: 1,
   *       	  ry: 1,
   *       	  width: 10
   *       	  len: 10
   *       	}
   *      },
   *
   *      rangeText: {
   *       	values: [15, 30, 50, 75, 95],
   *       	unit: "%",
   *       	fixed: false, // only available for gauge chart
   *       	format: function(v) {
   *       	  return v === 15 ? "Fifteen" : v;
   *       	},
   *
   *       	position: function(v) {
   *       	  return v === 15 ? {x: 20, y: 10} : null; // can return one props value also.
   *       	},
   *       	position: {x: 10, y: 15},
   *       	position: {x: 10}
   *      }
   *  }
   */
  arc_cornerRadius: 0,
  arc_cornerRadius_ratio: 0,
  arc_needle_show: !1,
  arc_needle_color: void 0,
  arc_needle_value: void 0,
  arc_needle_path: void 0,
  arc_needle_length: 100,
  arc_needle_top_rx: 0,
  arc_needle_top_ry: 0,
  arc_needle_top_width: 0,
  arc_needle_bottom_rx: 1,
  arc_needle_bottom_ry: 1,
  arc_needle_bottom_width: 15,
  arc_needle_bottom_len: 0,
  arc_rangeText_values: void 0,
  arc_rangeText_unit: "absolute",
  arc_rangeText_fixed: !1,
  arc_rangeText_format: void 0,
  arc_rangeText_position: void 0
}, xm = {
  /**
   * Set pie options
   * @name pie
   * @memberof Options
   * @type {object}
   * @property {object} pie Pie object
   * @property {boolean} [pie.label.show=true] Show or hide label on each pie piece.
   * @property {Function} [pie.label.format] Set formatter for the label on each pie piece.
   * @property {number|Function} [pie.label.ratio=undefined] Set ratio of labels position.
   * @property {number} [pie.label.threshold=0.05] Set threshold ratio to show/hide labels.
   * @property {boolean|object} [pie.expand=true] Enable or disable expanding pie pieces.
   * @property {number} [pie.expand.rate=0.98] Set expand rate.
   * @property {number} [pie.expand.duration=50] Set expand transition time in ms.
   * @property {number|object} [pie.innerRadius=0] Sets the inner radius of pie arc.
   * @property {number|object|undefined} [pie.outerRadius=undefined] Sets the outer radius of pie arc.
   * @property {number} [pie.padAngle=0] Set padding between data.
   * @property {number} [pie.padding=0] Sets the gap between pie arcs.
   * @property {number} [pie.startingAngle=0] Set starting angle where data draws.
   * @see [Demo: expand.rate](https://naver.github.io/billboard.js/demo/#PieChartOptions.ExpandRate)
   * @see [Demo: innerRadius](https://naver.github.io/billboard.js/demo/#PieChartOptions.InnerRadius)
   * @see [Demo: outerRadius](https://naver.github.io/billboard.js/demo/#PieChartOptions.OuterRadius)
   * @see [Demo: startingAngle](https://naver.github.io/billboard.js/demo/#PieChartOptions.StartingAngle)
   * @example
   *  pie: {
   *      label: {
   *          show: false,
   *          format: function(value, ratio, id) {
   *              return d3.format("$")(value);
   *
   *              // to multiline, return with '\n' character
   *              // return value +"%\nLine1\n2Line2";
   *          },
   *
   *          // 0.1(10%) ratio value means, the minimum ratio to show text label relative to the total value.
   *          // if data value is below than 0.1, text label will be hidden.
   *          threshold: 0.1,
   *
   *          // set ratio callback. Should return ratio value
   *          ratio: function(d, radius, h) {
   *              ...
   *              return ratio;
   *          },
   *          // or set ratio number
   *          ratio: 0.5
   *      },
   *
   *      // disable expand transition for interaction
   *      expand: false,
   *
   *      expand: {
   *      	// set duration of expand transition to 500ms.
   *          duration: 500,
   *
   *      	// set expand area rate
   *          rate: 1
   *      },
   *
   *      innerRadius: 0,
   *
   *      // set different innerRadius for each data
   *      innerRadius: {
   *      	data1: 10,
   *      	data2: 0
   *      },
   *
   *      outerRadius: 100,
   *
   *      // set different outerRadius for each data
   *      outerRadius: {
   *      	data1: 50,
   *      	data2: 100
   *      }
   *
   *      padAngle: 0.1,
   *      padding: 0,
   *      startingAngle: 1
   *  }
   */
  pie_label_show: !0,
  pie_label_format: void 0,
  pie_label_ratio: void 0,
  pie_label_threshold: 0.05,
  pie_expand: {},
  pie_expand_rate: 0.98,
  pie_expand_duration: 50,
  pie_innerRadius: 0,
  pie_outerRadius: void 0,
  pie_padAngle: 0,
  pie_padding: 0,
  pie_startingAngle: 0
};
function Yo(e, t) {
  Dt(qr.prototype, Object.values(nm).concat(e)), Dt(Ho.prototype, tm), Wr.setOptions(Object.values(im).concat(t || []));
}
function pc(e, t) {
  Yo([Zr, gc, lm].concat(e || [])), Wr.setOptions([Kr, gm].concat(t || []));
}
function _m(e, t) {
  Dt(qr.prototype, [rm, Zr].concat(e || [])), Wr.setOptions([Kr].concat(t || []));
}
var vc = function() {
  return pc(am, [dm]), (vc = function() {
    return ve.AREA;
  })();
}, xc = function() {
  return pc(), (xc = function() {
    return ve.LINE;
  })();
}, _c = function() {
  return _m(void 0, [vm, xm]), (_c = function() {
    return ve.PIE;
  })();
}, yc = function() {
  return Yo([om, Zr], [hm, Kr]), (yc = function() {
    return ve.BAR;
  })();
}, mc = function() {
  return Yo([Zr, gc], [Kr, pm]), (mc = function() {
    return ve.SCATTER;
  })();
}, ym = function(e) {
  var t, n = this.internal, i = n.axis, r = n.brush, a = n.config, o = n.scale, s = o.x, l = o.subX, c = n.state, f;
  if (a.subchart_show)
    if (f = e, Array.isArray(f)) {
      i.isTimeSeries() && (f = f.map(function(g) {
        return kt.bind(n)(g);
      }));
      var h = n.withinRange(f, n.getZoomDomain("subX", !0), n.getZoomDomain("subX"));
      h && (c.domain = f, r.move(r.getSelection(), f.map(l)));
    } else
      f = (t = c.domain) !== null && t !== void 0 ? t : s.orgDomain();
  return f;
};
Dt(ym, {
  /**
   * Show subchart
   * - **NOTE:** for ESM imports, needs to import 'subchart' exports and instantiate it by calling `subchart()`.
   * @function subchart․show
   * @instance
   * @memberof Chart
   * @example
   * // for ESM imports, needs to import 'subchart' and must be instantiated first to enable subchart's API.
   * import {subchart} from "billboard.js";
   *
   * const chart = bb.generate({
   *   ...
   *   subchart: {
   *      // need to be instantiated by calling 'subchart()'
   *      enabled: subchart()
   *
   *      // in case don't want subchart to be shown at initialization, instantiate with '!subchart()'
   *      enabled: !subchart()
   *   }
   * });
   *
   * chart.subchart.show();
   */
  show: function() {
    var e, t, n = this.internal, i = n.$el.subchart, r = n.config, a = r.subchart_show;
    if (!a) {
      n.unbindZoomEvent(), r.subchart_show = !a, !i.main && n.initSubchart();
      var o = i.main.selectAll(".".concat(Ee.target));
      n.data.targets.length !== o.size() && (n.updateSizes(), n.updateTargetsForSubchart(n.data.targets), o = (e = i.main) === null || e === void 0 ? void 0 : e.selectAll(".".concat(Ee.target))), o == null || o.style("opacity", null), (t = i.main) === null || t === void 0 || t.style("display", null), this.resize();
    }
  },
  /**
   * Hide generated subchart
   * - **NOTE:** for ESM imports, needs to import 'subchart' exports and instantiate it by calling `subchart()`.
   * @function subchart․hide
   * @instance
   * @memberof Chart
   * @example
   *  chart.subchart.hide();
   */
  hide: function() {
    var e = this.internal, t = e.$el.subchart.main, n = e.config;
    n.subchart_show && (t == null ? void 0 : t.style("display")) !== "none" && (n.subchart_show = !1, t.style("display", "none"), this.resize());
  },
  /**
   * Toggle the visiblity of subchart
   * - **NOTE:** for ESM imports, needs to import 'subchart' exports and instantiate it by calling `subchart()`.
   * @function subchart․toggle
   * @instance
   * @memberof Chart
   * @example
   * // When subchart is hidden, will be shown
   * // When subchart is shown, will be hidden
   * chart.subchart.toggle();
   */
  toggle: function() {
    var e = this.internal, t = e.config;
    this.subchart[t.subchart_show ? "hide" : "show"]();
  },
  /**
   * Reset subchart selection
   * @function subchart․reset
   * @instance
   * @memberof Chart
   * @example
   * // Reset subchart selection
   * chart.subchart.reset();
   */
  reset: function() {
    var e = this.internal, t = e.brush;
    t.clear(t.getSelection());
  }
});
var mm = function(e) {
  var t, n = this.internal, i = n.axis, r = n.config, a = n.org, o = n.scale, s = n.state, l = i.isCategorized(), c;
  if (r.zoom_enabled)
    if (c = e, Array.isArray(c)) {
      i.isTimeSeries() && (c = c.map(function(g) {
        return kt.bind(n)(g);
      }));
      var f = n.withinRange(c, n.getZoomDomain("zoom", !0), n.getZoomDomain("zoom"));
      if (f) {
        if (s.domain = c, c = n.getZoomDomainValue(c), n.api.tooltip.hide(), r.subchart_show) {
          var h = o.zoom || o.x;
          n.brush.getSelection().call(n.brush.move, c.map(h));
        } else {
          var h = l ? o.x.orgScale() : a.xScale || o.x;
          n.updateCurrentZoomTransform(h, c);
        }
        n.setZoomResetButton();
      }
    } else
      c = n.zoom.getDomain();
  return (t = s.domain) !== null && t !== void 0 ? t : c;
};
Dt(mm, {
  /**
   * Enable and disable zooming.
   * @function zoom․enable
   * @instance
   * @memberof Chart
   * @param {string|boolean} enabled Possible string values are "wheel" or "drag". If enabled is true, "wheel" will be used. If false is given, zooming will be disabled.<br>When set to false, the current zooming status will be reset.
   * @example
   *  // Enable zooming using the mouse wheel
   *  chart.zoom.enable(true);
   *  // Or
   *  chart.zoom.enable("wheel");
   *
   *  // Enable zooming by dragging
   *  chart.zoom.enable("drag");
   *
   *  // Disable zooming
   *  chart.zoom.enable(false);
   */
  enable: function(e) {
    var t = this.internal, n = t.config;
    /^(drag|wheel)$/.test(e) && (n.zoom_type = e), n.zoom_enabled = !!e, t.zoom ? e === !1 && t.bindZoomEvent(!1) : (t.initZoom(), t.bindZoomEvent()), t.updateAndRedraw();
  },
  /**
   * Set or get x Axis maximum zoom range value
   * @function zoom․max
   * @instance
   * @memberof Chart
   * @param {number} [max] maximum value to set for zoom
   * @returns {number} zoom max value
   * @example
   *  // Set maximum range value
   *  chart.zoom.max(20);
   */
  max: function(e) {
    var t = this.internal, n = t.config, i = t.org.xDomain;
    return (e === 0 || e) && (n.zoom_x_max = yn("max", [i[1], e])), n.zoom_x_max;
  },
  /**
   * Set or get x Axis minimum zoom range value
   * @function zoom․min
   * @instance
   * @memberof Chart
   * @param {number} [min] minimum value to set for zoom
   * @returns {number} zoom min value
   * @example
   *  // Set minimum range value
   *  chart.zoom.min(-1);
   */
  min: function(e) {
    var t = this.internal, n = t.config, i = t.org.xDomain;
    return (e === 0 || e) && (n.zoom_x_min = yn("min", [i[0], e])), n.zoom_x_min;
  },
  /**
   * Set zoom range
   * @function zoom․range
   * @instance
   * @memberof Chart
   * @param {object} [range] zoom range
   * @returns {object} zoom range value
   * {
   *   min: 0,
   *   max: 100
   * }
   * @example
   *  chart.zoom.range({
   *      min: 10,
   *      max: 100
   *  });
   */
  range: function(e) {
    var t = this.zoom;
    if (Be(e)) {
      var n = e.min, i = e.max;
      Ye(n) && t.min(n), Ye(i) && t.max(i);
    }
    return {
      min: t.min(),
      max: t.max()
    };
  }
});
var bm = {
  /**
   * Called when dragging.
   * Data points can be selected.
   * @private
   * @param {object} mouse Object
   */
  drag: function(e) {
    var t = this, n = t.config, i = t.state, r = t.$el.main, a = n.data_selection_grouped, o = n.interaction_enabled && n.data_selection_isselectable;
    if (!(t.hasArcType() || !n.data_selection_enabled || // do nothing if not selectable
    n.zoom_enabled && !t.zoom.altDomain || // skip if zoomable because of conflict drag behavior
    !n.data_selection_multiple)) {
      var s = i.dragStart || [0, 0], l = s[0], c = s[1], f = e[0], h = e[1], g = Math.min(l, f), x = Math.max(l, f), y = a ? i.margin.top : Math.min(c, h), T = a ? i.height : Math.max(c, h);
      r.select(".".concat(En.dragarea)).attr("x", g).attr("y", y).attr("width", x - g).attr("height", T - y), r.selectAll(".".concat(ct.shapes)).selectAll(".".concat(ct.shape)).filter(function(A) {
        return o == null ? void 0 : o.bind(t.api)(A);
      }).each(function(A, w) {
        var C = le(this), R = C.classed(it.SELECTED), S = C.classed(En.INCLUDED), I = !1, L;
        if (C.classed(pt.circle)) {
          var p = +C.attr("cx") * 1, U = +C.attr("cy") * 1;
          L = t.togglePoint, I = g < p && p < x && y < U && U < T;
        } else if (C.classed(Ot.bar)) {
          var H = __(this), p = H.x, U = H.y, q = H.width, X = H.height;
          L = t.togglePath, I = !(x < p || p + q < g) && !(T < U || U + X < y);
        } else
          return;
        I ^ S && (C.classed(En.INCLUDED, !S), C.classed(it.SELECTED, !R), L.call(t, !R, C, A, w));
      });
    }
  },
  /**
   * Called when the drag starts.
   * Adds and Shows the drag area.
   * @private
   * @param {object} mouse Object
   */
  dragstart: function(e) {
    var t = this, n = t.config, i = t.state, r = t.$el.main;
    t.hasArcType() || !n.data_selection_enabled || (i.dragStart = e, r.select(".".concat(Ee.chart)).append("rect").attr("class", En.dragarea).style("opacity", "0.1"), t.setDragStatus(!0));
  },
  /**
   * Called when the drag finishes.
   * Removes the drag area.
   * @private
   */
  dragend: function() {
    var e = this, t = e.config, n = e.$el.main, i = e.$T;
    e.hasArcType() || !t.data_selection_enabled || (i(n.select(".".concat(En.dragarea))).style("opacity", "0").remove(), n.selectAll(".".concat(ct.shape)).classed(En.INCLUDED, !1), e.setDragStatus(!1));
  }
};
ye(ye({}, bm), {
  /**
   * Select a point
   * @param {object} target Target point
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  selectPoint: function(e, t, n) {
    var i = this, r = i.config, a = i.$el.main, o = i.$T, s = r.axis_rotated, l = (s ? i.circleY : i.circleX).bind(i), c = (s ? i.circleX : i.circleY).bind(i), f = i.pointSelectR.bind(i);
    Ze(r.data_onselected, i.api, t, e.node()), o(a.select(".".concat(it.selectedCircles).concat(i.getTargetSelectorSuffix(t.id))).selectAll(".".concat(it.selectedCircle, "-").concat(n)).data([t]).enter().append("circle").attr("class", function() {
      return i.generateClass(it.selectedCircle, n);
    }).attr("cx", l).attr("cy", c).attr("stroke", i.color).attr("r", function(h) {
      return i.pointSelectR(h) * 1.4;
    })).attr("r", f);
  },
  /**
   * Unelect a point
   * @param {object} target Target point
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  unselectPoint: function(e, t, n) {
    var i = this, r = i.config, a = i.$el.main, o = i.$T;
    Ze(r.data_onunselected, i.api, t, e == null ? void 0 : e.node()), o(a.select(".".concat(it.selectedCircles).concat(i.getTargetSelectorSuffix(t.id))).selectAll(".".concat(it.selectedCircle, "-").concat(n))).attr("r", 0).remove();
  },
  /**
   * Toggles the selection of points
   * @param {boolean} selected whether or not to select.
   * @param {object} target Target object
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  togglePoint: function(e, t, n, i) {
    var r = "".concat(e ? "" : "un", "selectPoint");
    this[r](t, n, i);
  },
  /**
   * Select a path
   * @param {object} target Target path
   * @param {object} d Data object
   * @private
   */
  selectPath: function(e, t) {
    var n = this, i = n.config;
    Ze(i.data_onselected, n.api, t, e.node()), i.interaction_brighten && e.style("filter", "brightness(1.25)");
  },
  /**
   * Unelect a path
   * @private
   * @param {object} target Target path
   * @param {object} d Data object
   */
  unselectPath: function(e, t) {
    var n = this, i = n.config;
    Ze(i.data_onunselected, n.api, t, e.node()), i.interaction_brighten && e.style("filter", null);
  },
  /**
   * Toggles the selection of lines
   * @param {boolean} selected whether or not to select.
   * @param {object} target Target object
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  togglePath: function(e, t, n, i) {
    this["".concat(e ? "" : "un", "selectPath")](t, n, i);
  },
  /**
   * Returns the toggle method of the target
   * @param {object} that shape
   * @param {object} d Data object
   * @returns {Function} toggle method
   * @private
   */
  getToggle: function(e, t) {
    var n = this;
    return e.nodeName === "path" ? n.togglePath : n.isStepType(t) ? function() {
    } : (
      // circle is hidden in step chart, so treat as within the click area
      n.togglePoint
    );
  },
  /**
   * Toggles the selection of shapes
   * @param {object} that shape
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  toggleShape: function(e, t, n) {
    var i, r = this, a = r.config, o = r.$el.main;
    if (a.data_selection_enabled && a.data_selection_isselectable.bind(r.api)(t)) {
      var s = le(e), l = s.classed(it.SELECTED), c = r.getToggle(e, t).bind(r), f;
      if (!a.data_selection_multiple) {
        var h = (i = r.isPointFocusOnly) === null || i === void 0 ? void 0 : i.call(r), g = ".".concat(h ? it.selectedCircles : ct.shapes);
        a.data_selection_grouped && (g += r.getTargetSelectorSuffix(t.id)), o.selectAll(g).selectAll(h ? ".".concat(it.selectedCircle) : ".".concat(ct.shape, ".").concat(it.SELECTED)).classed(it.SELECTED, !1).each(function(x) {
          var y = le(this);
          f = y, c(!1, y, x, x.index);
        });
      }
      (!f || f.node() !== s.node()) && (s.classed(it.SELECTED, !l), c(!l, s, t, n));
    }
  }
});
var Ba = {}, Tm = {
  /**
   * Version information
   * @property {string} version version
   * @example
   *    bb.version;  // "1.0.0"
   * @memberof bb
   */
  version: "3.14.3",
  /**
   * Generate chart
   * - **NOTE:** Bear in mind for the possiblity of ***throwing an error***, during the generation when:
   *   - Unused option value is given.
   *     - ex) For `data.type="pie"` option, setting 'axis' option can cause unexpected generation error.
   *   - Insufficient value is given for certain option used.
   *     - ex) `data: { x: "x", columns: [["x"], ["data1", 30, 200, 100]] }`
   * @param {Options} config chart options
   * @memberof bb
   * @returns {Chart}
   * @see {@link Options} for different generation options
   * @see {@link Chart} for different methods API
   * @example
   *  <!-- chart holder -->
   * <div id="LineChart"></div>
   * @example
   *  // Generate chart with options
   *  var chart = bb.generate({
   *      "bindto": "#LineChart"
   *      "data": {
   *          "columns": [
   *              ["data1", 30, 200, 100, 400, 150, 250],
   *              ["data2", 50, 20, 10, 40, 15, 25]
   *           ],
   *          "type": "line"
   *      }
   *  });
   *
   *  // call some API
   *  // ex) get the data of 'data1'
   *  chart.data("data1");
   * @example
   * // Generate chart by importing ESM
   * // Import types to be used only, where this will make smaller bundle size.
   * import bb, {
   *   area,
   *   areaLineRange,
   *   areaSpline,
   *   areaSplineRange,
   *   areaStep,
   *   bar,
   *   bubble,
   *   donut,
   *   gauge,
   *   line,
   *   pie,
   *   polar,
   *   radar,
   *   scatter,
   *   spline,
   *   step
   * }
   *
   * bb.generate({
   *      "bindto": "#LineChart"
   *      "data": {
   *          "columns": [
   *              ["data1", 30, 200, 100, 400, 150, 250],
   *              ["data2", 50, 20, 10, 40, 15, 25]
   *           ]
   *      },
   *      type: line(),
   *
   *      // or
   *      types: {
   *        data1: bar(),
   *        data2: step()
   *      }
   * });
   */
  generate: function(e) {
    var t = oi({}, Ba, e), n = new Ho(t);
    return n.internal.charts = this.instance, this.instance.push(n), n;
  },
  /**
   * Set or get global default options.
   * - **NOTE:**
   *   - The options values settings are valid within page context only.
   *   - If is called multiple times, will override the last value.
   * @param {Options} options chart options
   * @memberof bb
   * @returns {Options}
   * @see {@link Options}
   * @example
   * // Set same option value as for `.generate()`
   * bb.defaults({
   *   data: {
   *     type: "bar"
   *   }
   * });
   *
   * bb.defaults();  // {data:{type: "bar"}}
   *
   * // data.type defaults to 'bar'
   * var chart = bb.generate({ ... });
   */
  defaults: function(e) {
    return Be(e) && (Ba = e), Ba;
  },
  /**
   * An array containing instance created
   * @property {Array} instance instance array
   * @example
   *  // generate charts
   *  var chart1 = bb.generate(...);
   *  var chart2 = bb.generate(...);
   *
   *  bb.instance;  // [ chart1, chart2, ... ]
   * @memberof bb
   */
  instance: [],
  /**
   * Namespace for plugins
   * @property {object} plugin plugin namespace
   * @example
   *  // Stanford diagram plugin
   *  bb.plugin.stanford;
   * @memberof bb
   */
  plugin: {}
};
const wm = (e) => {
  $m(e.MsgQueue), Bu(Zt(".block")), Bu(Zt("table td"));
};
function $m(e) {
  e.forEach((t) => Am(t == null ? void 0 : t.data));
}
const Xa = (e, t = (n) => {
}) => ({
  axis: ((i) => (t(i), i))({
    x: {
      tick: {
        format(i) {
          return e[0].xlabels[i] || i;
        }
      }
    }
  })
});
function Am(e) {
  if (!e)
    return;
  const { chartID: t, chartType: n, chartData: i } = e, r = {
    bindto: `#${t}`
  };
  switch (n) {
    case "lineChart":
      Object.assign(r, ks(Qi({
        data: {
          columns: i.map((a) => [a.key, ...a.values.map(({ y: o }) => o)]),
          type: xc()
        }
      }, Xa(i)), {
        interaction: { enabled: !0 }
      }));
      break;
    case "barChart":
      Object.assign(r, Qi({
        data: {
          columns: i.map((a) => [a.key, ...a.values.map(({ y: o }) => o)]),
          type: yc()
        }
      }, Xa(i, (a) => a.x.tick.multiline = !0)));
      break;
    case "pieChart":
    case "pie3DChart":
      Object.assign(r, {
        data: {
          columns: Object.values(i[0].xlabels).map((a, o) => [a, i[0].values[o].y]),
          type: _c()
        }
      });
      break;
    case "areaChart":
      Object.assign(r, Qi({
        data: {
          columns: i.map((a) => [a.key, ...a.values.map(({ y: o }) => o)]),
          type: vc()
        },
        interaction: { enabled: !0 }
      }, Xa(i)));
      break;
    case "scatterChart":
      Object.assign(r, {
        data: {
          xs: {
            y: "x"
          },
          columns: i.map((a, o) => [o ? "y" : "x", ...a]),
          type: mc()
        },
        axis: {
          x: {
            label: "X",
            showDist: !0,
            tick: {
              format: Pi(".02f")
            }
          },
          y: {
            label: "Y",
            showDist: !0,
            tick: {
              format: Pi(".02f")
            }
          }
        }
      });
      break;
  }
  r.data && Tm.generate(r);
}
function Bu(e) {
  const t = e;
  for (let n = 0; n < t.length; n++) {
    const i = Zt(t[n]).find(".numeric-bullet-style");
    if (i.length > 0) {
      let r = "", a = "", o = 0;
      const s = [];
      let l = 0;
      const c = [];
      for (let f = 0; f < i.length; f++) {
        const h = Zt(i[f]).data("bulltname"), g = Zt(i[f]).data("bulltlvl");
        o == 0 ? (r = h, a = g, s[l] = o, c[l] = h, o++) : h == r && g == a ? (r = h, a = g, o++, s[l] = o, c[l] = h) : h != r && g == a || h != r && Number(g) > Number(a) ? (r = h, a = g, l++, s[l] = o, c[l] = h, o = 1) : h != r && Number(g) < Number(a) && (r = h, a = g, l--, o = s[l] + 1);
        const x = Cm(c[l], o);
        Zt(i[f]).html(x);
      }
    }
  }
}
function Cm(e, t) {
  let n = "";
  switch (e) {
    case "arabicPeriod":
      n = t + ". ";
      break;
    case "arabicParenR":
      n = t + ") ";
      break;
    case "alphaLcParenR":
      n = sr(t, "lowerCase") + ") ";
      break;
    case "alphaLcPeriod":
      n = sr(t, "lowerCase") + ". ";
      break;
    case "alphaUcParenR":
      n = sr(t, "upperCase") + ") ";
      break;
    case "alphaUcPeriod":
      n = sr(t, "upperCase") + ". ";
      break;
    case "romanUcPeriod":
      n = Xu(t) + ". ";
      break;
    case "romanLcParenR":
      n = Xu(t) + ") ";
      break;
    case "hebrew2Minus":
      n = Sm.format(t) + "-";
      break;
    default:
      n = String(t);
  }
  return n;
}
function Xu(e) {
  var a;
  if (!+e)
    return !1;
  const t = String(+e).split(""), n = [
    "",
    "C",
    "CC",
    "CCC",
    "CD",
    "D",
    "DC",
    "DCC",
    "DCCC",
    "CM",
    "",
    "X",
    "XX",
    "XXX",
    "XL",
    "L",
    "LX",
    "LXX",
    "LXXX",
    "XC",
    "",
    "I",
    "II",
    "III",
    "IV",
    "V",
    "VI",
    "VII",
    "VIII",
    "IX"
  ];
  let i = "", r = 3;
  for (; r--; )
    i = (n[+((a = t.pop()) != null ? a : "") + r * 10] || "") + i;
  return Array(+t.join("") + 1).join("M") + i;
}
const Sm = km([
  [1e3, ""],
  [400, "ת"],
  [300, "ש"],
  [200, "ר"],
  [100, "ק"],
  [90, "צ"],
  [80, "פ"],
  [70, "ע"],
  [60, "ס"],
  [50, "נ"],
  [40, "מ"],
  [30, "ל"],
  [20, "כ"],
  [10, "י"],
  [9, "ט"],
  [8, "ח"],
  [7, "ז"],
  [6, "ו"],
  [5, "ה"],
  [4, "ד"],
  [3, "ג"],
  [2, "ב"],
  [1, "א"],
  [/יה/, "ט״ו"],
  [/יו/, "ט״ז"],
  [/([א-ת])([א-ת])$/, "$1״$2"],
  [/^([א-ת])$/, "$1׳"]
]);
function km(e) {
  return e.slice().sort(function(t, n) {
    return n[1].length - t[1].length;
  }), {
    format: function(t) {
      let n = "";
      return Zt.each(e, function() {
        const i = this[0];
        if (parseInt(i) > 0)
          for (; t >= i; t -= i)
            n += this[1];
        else
          n = n.replace(i, this[1]);
      }), n;
    }
  };
}
function sr(e, t) {
  e = Number(e) - 1;
  let n = "";
  return t == "upperCase" ? n = ((e / 26 >= 1 ? String.fromCharCode(e / 26 + 64) : "") + String.fromCharCode(e % 26 + 65)).toUpperCase() : t == "lowerCase" && (n = ((e / 26 >= 1 ? String.fromCharCode(e / 26 + 64) : "") + String.fromCharCode(e % 26 + 65)).toLowerCase()), n;
}
const Em = {
  create() {
    return Ef("pptx.worker.js").defaults(() => new Worker(new URL("" + new URL("../worker/pptx.worker.js", import.meta.url).href, self.location), { type: "module" }));
  }
}, Rm = /* @__PURE__ */ Tf({
  __name: "PptxRender",
  props: {
    data: {},
    options: { type: Function, default: Lf }
  },
  setup(e) {
    const t = e, n = wf(null);
    return (() => {
      const i = {
        isDone: !1,
        thumbElement: null,
        worker: null,
        timer: null
      }, r = {
        // 启动worker逻辑
        startWorker() {
          i.worker && i.worker.terminate(), i.timer && clearInterval(i.timer);
          const a = i.worker = Em.create();
          a.addEventListener("message", (o) => {
            this.processMessage(o.data);
          }, !1), a.addEventListener("error", (o) => {
            console.error(o);
          }, !1), a.postMessage({
            type: "processPPTX",
            data: t.data,
            IE11: "MSInputMethodContext" in window && "documentMode" in document,
            options: t.options()
          }), i.timer = window.setInterval(this.stopWorker, 500);
        },
        // 停止worker逻辑
        stopWorker() {
          var a;
          i.isDone && ((a = i.worker) == null || a.terminate(), console.log("worker terminated"), i.timer && clearInterval(i.timer));
        },
        // 窗口拖动大小，自动调整位置
        resize() {
          if (n.value) {
            const a = Zt(n.value), o = Math.max(...Array.from(a.children("section")).map((l) => l.offsetWidth)), s = a[0].offsetWidth;
            a.css({
              transform: `scale(${s / o})`,
              "transform-origin": "top left"
            });
          }
        },
        // 核心处理逻辑
        processMessage(a) {
          if (i.isDone || !n.value)
            return;
          const o = Zt(n.value), { thumbElement: s } = i;
          switch (a.type) {
            case "slide":
              console.log("正在处理:", a.slide_num), o.append(a.data);
              break;
            case "pptx-thumb":
              s && Zt(s).attr("src", `data:image/jpeg;base64,${a.data}`);
              break;
            case "slideSize":
              break;
            case "globalCSS":
              o.append(`<style>${a.data}</style>`);
              break;
            case "ExecutionTime":
            case "Done":
              console.log("pptx渲染完成，耗时", a.data), wm(a.charts), i.isDone = !0;
              break;
            case "WARN":
              console.warn("PPTX processing warning: ", a.data);
              break;
            case "ERROR":
              i.isDone = !0, console.error("PPTX processing error: ", a.data);
              break;
            case "DEBUG":
              console.debug("Worker: ", a.data);
              break;
            case "INFO":
            default:
              console.info("Worker: ", a.data);
          }
        }
      };
      $f(() => {
        r.startWorker();
      });
    })(), (i, r) => (Af(), Cf("div", {
      class: "pptx-wrapper",
      ref_key: "wrapper",
      ref: n
    }, null, 512));
  }
});
const Pm = /* @__PURE__ */ Rf(Rm, [["__scopeId", "data-v-09692024"]]);
export {
  Pm as default
};
