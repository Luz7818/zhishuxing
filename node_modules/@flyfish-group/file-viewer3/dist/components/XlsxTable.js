var Q_ = Object.defineProperty, X_ = Object.defineProperties;
var q_ = Object.getOwnPropertyDescriptors;
var Vd = Object.getOwnPropertySymbols;
var JR = Object.prototype.hasOwnProperty, e0 = Object.prototype.propertyIsEnumerable;
var Wd = Math.pow, ZR = (t, e, n) => e in t ? Q_(t, e, { enumerable: !0, configurable: !0, writable: !0, value: n }) : t[e] = n, Xe = (t, e) => {
  for (var n in e || (e = {}))
    JR.call(e, n) && ZR(t, n, e[n]);
  if (Vd)
    for (var n of Vd(e))
      e0.call(e, n) && ZR(t, n, e[n]);
  return t;
}, On = (t, e) => X_(t, q_(e));
var Wn = (t, e) => {
  var n = {};
  for (var i in t)
    JR.call(t, i) && e.indexOf(i) < 0 && (n[i] = t[i]);
  if (t != null && Vd)
    for (var i of Vd(t))
      e.indexOf(i) < 0 && e0.call(t, i) && (n[i] = t[i]);
  return n;
};
var t0 = (t, e, n) => new Promise((i, r) => {
  var o = (l) => {
    try {
      a(n.next(l));
    } catch (u) {
      r(u);
    }
  }, s = (l) => {
    try {
      a(n.throw(l));
    } catch (u) {
      r(u);
    }
  }, a = (l) => l.done ? i(l.value) : Promise.resolve(l.value).then(o, s);
  a((n = n.apply(t, e)).next());
});
import { defineComponent as lS, markRaw as Z_, openBlock as Kc, createElementBlock as Qc, renderSlot as J_, ref as mu, onUnmounted as eN, computed as tN, watch as nN, onMounted as iN, unref as n0, createElementVNode as $d, createTextVNode as rN, createCommentVNode as oN, createVNode as sN, Fragment as aN, renderList as lN, normalizeClass as uN, toDisplayString as cN, nextTick as hN } from "vue";
import { c as Ud, g as dN } from "./_commonjsHelpers.js";
import { r as _I } from "./worker-ref.js";
import { c as Yd } from "./_commonjs-dynamic-modules.js";
import { _ as fN } from "./index.js";
const gN = "data:image/png;base64,", mN = "data:image/gif;base64,";
var ju = function(t) {
  return t && t.Math === Math && t;
}, En = (
  // eslint-disable-next-line es/no-global-this -- safe
  ju(typeof globalThis == "object" && globalThis) || ju(typeof window == "object" && window) || // eslint-disable-next-line no-restricted-globals -- safe
  ju(typeof self == "object" && self) || ju(typeof Ud == "object" && Ud) || ju(typeof Ud == "object" && Ud) || // eslint-disable-next-line no-new-func -- fallback
  function() {
    return this;
  }() || Function("return this")()
), uS = {}, fi = function(t) {
  try {
    return !!t();
  } catch (e) {
    return !0;
  }
}, pN = fi, Er = !pN(function() {
  return Object.defineProperty({}, 1, { get: function() {
    return 7;
  } })[1] !== 7;
}), wN = fi, tp = !wN(function() {
  var t = function() {
  }.bind();
  return typeof t != "function" || t.hasOwnProperty("prototype");
}), AN = tp, jd = Function.prototype.call, Tr = AN ? jd.bind(jd) : function() {
  return jd.apply(jd, arguments);
}, NI = {}, DI = {}.propertyIsEnumerable, kI = Object.getOwnPropertyDescriptor, CN = kI && !DI.call({ 1: 2 }, 1);
NI.f = CN ? function(e) {
  var n = kI(this, e);
  return !!n && n.enumerable;
} : DI;
var np = function(t, e) {
  return {
    enumerable: !(t & 1),
    configurable: !(t & 2),
    writable: !(t & 4),
    value: e
  };
}, LI = tp, BI = Function.prototype, Gv = BI.call, vN = LI && BI.bind.bind(Gv, Gv), an = LI ? vN : function(t) {
  return function() {
    return Gv.apply(t, arguments);
  };
}, FI = an, yN = FI({}.toString), SN = FI("".slice), al = function(t) {
  return SN(yN(t), 8, -1);
}, bN = an, RN = fi, EN = al, wA = Object, TN = bN("".split), MN = RN(function() {
  return !wA("z").propertyIsEnumerable(0);
}) ? function(t) {
  return EN(t) === "String" ? TN(t, "") : wA(t);
} : wA, ip = function(t) {
  return t == null;
}, IN = ip, ON = TypeError, sd = function(t) {
  if (IN(t))
    throw new ON("Can't call method on " + t);
  return t;
}, PN = MN, xN = sd, rp = function(t) {
  return PN(xN(t));
}, AA = typeof document == "object" && document.all, Tn = typeof AA == "undefined" && AA !== void 0 ? function(t) {
  return typeof t == "function" || t === AA;
} : function(t) {
  return typeof t == "function";
}, HN = Tn, co = function(t) {
  return typeof t == "object" ? t !== null : HN(t);
}, CA = En, _N = Tn, NN = function(t) {
  return _N(t) ? t : void 0;
}, xu = function(t, e) {
  return arguments.length < 2 ? NN(CA[t]) : CA[t] && CA[t][e];
}, DN = an, op = DN({}.isPrototypeOf), kN = En, i0 = kN.navigator, r0 = i0 && i0.userAgent, sp = r0 ? String(r0) : "", VI = En, vA = sp, o0 = VI.process, s0 = VI.Deno, a0 = o0 && o0.versions || s0 && s0.version, l0 = a0 && a0.v8, wr, sm;
l0 && (wr = l0.split("."), sm = wr[0] > 0 && wr[0] < 4 ? 1 : +(wr[0] + wr[1]));
!sm && vA && (wr = vA.match(/Edge\/(\d+)/), (!wr || wr[1] >= 74) && (wr = vA.match(/Chrome\/(\d+)/), wr && (sm = +wr[1])));
var LN = sm, u0 = LN, BN = fi, FN = En, VN = FN.String, cS = !!Object.getOwnPropertySymbols && !BN(function() {
  var t = Symbol("symbol detection");
  return !VN(t) || !(Object(t) instanceof Symbol) || // Chrome 38-40 symbols are not inherited from DOM collections prototypes to instances
  !Symbol.sham && u0 && u0 < 41;
}), WN = cS, WI = WN && !Symbol.sham && typeof Symbol.iterator == "symbol", $N = xu, UN = Tn, YN = op, jN = WI, GN = Object, hS = jN ? function(t) {
  return typeof t == "symbol";
} : function(t) {
  var e = $N("Symbol");
  return UN(e) && YN(e.prototype, GN(t));
}, zN = String, ap = function(t) {
  try {
    return zN(t);
  } catch (e) {
    return "Object";
  }
}, KN = Tn, QN = ap, XN = TypeError, ad = function(t) {
  if (KN(t))
    return t;
  throw new XN(QN(t) + " is not a function");
}, qN = ad, ZN = ip, lp = function(t, e) {
  var n = t[e];
  return ZN(n) ? void 0 : qN(n);
}, yA = Tr, SA = Tn, bA = co, JN = TypeError, eD = function(t, e) {
  var n, i;
  if (e === "string" && SA(n = t.toString) && !bA(i = yA(n, t)) || SA(n = t.valueOf) && !bA(i = yA(n, t)) || e !== "string" && SA(n = t.toString) && !bA(i = yA(n, t)))
    return i;
  throw new JN("Can't convert object to primitive value");
}, $I = { exports: {} }, c0 = En, tD = Object.defineProperty, dS = function(t, e) {
  try {
    tD(c0, t, { value: e, configurable: !0, writable: !0 });
  } catch (n) {
    c0[t] = e;
  }
  return e;
}, nD = En, iD = dS, h0 = "__core-js_shared__", d0 = $I.exports = nD[h0] || iD(h0, {});
(d0.versions || (d0.versions = [])).push({
  version: "3.40.0",
  mode: "global",
  copyright: "© 2014-2025 Denis Pushkarev (zloirock.ru)",
  license: "https://github.com/zloirock/core-js/blob/v3.40.0/LICENSE",
  source: "https://github.com/zloirock/core-js"
});
var fS = $I.exports, f0 = fS, UI = function(t, e) {
  return f0[t] || (f0[t] = e || {});
}, rD = sd, oD = Object, ld = function(t) {
  return oD(rD(t));
}, sD = an, aD = ld, lD = sD({}.hasOwnProperty), ho = Object.hasOwn || function(e, n) {
  return lD(aD(e), n);
}, uD = an, cD = 0, hD = Math.random(), dD = uD(1 .toString), YI = function(t) {
  return "Symbol(" + (t === void 0 ? "" : t) + ")_" + dD(++cD + hD, 36);
}, fD = En, gD = UI, g0 = ho, mD = YI, pD = cS, wD = WI, uu = fD.Symbol, RA = gD("wks"), AD = wD ? uu.for || uu : uu && uu.withoutSetter || mD, Xs = function(t) {
  return g0(RA, t) || (RA[t] = pD && g0(uu, t) ? uu[t] : AD("Symbol." + t)), RA[t];
}, CD = Tr, m0 = co, p0 = hS, vD = lp, yD = eD, SD = Xs, bD = TypeError, RD = SD("toPrimitive"), ED = function(t, e) {
  if (!m0(t) || p0(t))
    return t;
  var n = vD(t, RD), i;
  if (n) {
    if (e === void 0 && (e = "default"), i = CD(n, t, e), !m0(i) || p0(i))
      return i;
    throw new bD("Can't convert object to primitive value");
  }
  return e === void 0 && (e = "number"), yD(t, e);
}, TD = ED, MD = hS, jI = function(t) {
  var e = TD(t, "string");
  return MD(e) ? e : e + "";
}, ID = En, w0 = co, zv = ID.document, OD = w0(zv) && w0(zv.createElement), gS = function(t) {
  return OD ? zv.createElement(t) : {};
}, PD = Er, xD = fi, HD = gS, GI = !PD && !xD(function() {
  return Object.defineProperty(HD("div"), "a", {
    get: function() {
      return 7;
    }
  }).a !== 7;
}), _D = Er, ND = Tr, DD = NI, kD = np, LD = rp, BD = jI, FD = ho, VD = GI, A0 = Object.getOwnPropertyDescriptor;
uS.f = _D ? A0 : function(e, n) {
  if (e = LD(e), n = BD(n), VD)
    try {
      return A0(e, n);
    } catch (i) {
    }
  if (FD(e, n))
    return kD(!ND(DD.f, e, n), e[n]);
};
var qs = {}, WD = Er, $D = fi, zI = WD && $D(function() {
  return Object.defineProperty(function() {
  }, "prototype", {
    value: 42,
    writable: !1
  }).prototype !== 42;
}), UD = co, YD = String, jD = TypeError, ts = function(t) {
  if (UD(t))
    return t;
  throw new jD(YD(t) + " is not an object");
}, GD = Er, zD = GI, KD = zI, Gd = ts, C0 = jI, QD = TypeError, EA = Object.defineProperty, XD = Object.getOwnPropertyDescriptor, TA = "enumerable", MA = "configurable", IA = "writable";
qs.f = GD ? KD ? function(e, n, i) {
  if (Gd(e), n = C0(n), Gd(i), typeof e == "function" && n === "prototype" && "value" in i && IA in i && !i[IA]) {
    var r = XD(e, n);
    r && r[IA] && (e[n] = i.value, i = {
      configurable: MA in i ? i[MA] : r[MA],
      enumerable: TA in i ? i[TA] : r[TA],
      writable: !1
    });
  }
  return EA(e, n, i);
} : EA : function(e, n, i) {
  if (Gd(e), n = C0(n), Gd(i), zD)
    try {
      return EA(e, n, i);
    } catch (r) {
    }
  if ("get" in i || "set" in i)
    throw new QD("Accessors not supported");
  return "value" in i && (e[n] = i.value), e;
};
var qD = Er, ZD = qs, JD = np, ud = qD ? function(t, e, n) {
  return ZD.f(t, e, JD(1, n));
} : function(t, e, n) {
  return t[e] = n, t;
}, KI = { exports: {} }, Kv = Er, ek = ho, QI = Function.prototype, tk = Kv && Object.getOwnPropertyDescriptor, mS = ek(QI, "name"), nk = mS && function() {
}.name === "something", ik = mS && (!Kv || Kv && tk(QI, "name").configurable), rk = {
  EXISTS: mS,
  PROPER: nk,
  CONFIGURABLE: ik
}, ok = an, sk = Tn, Qv = fS, ak = ok(Function.toString);
sk(Qv.inspectSource) || (Qv.inspectSource = function(t) {
  return ak(t);
});
var lk = Qv.inspectSource, uk = En, ck = Tn, v0 = uk.WeakMap, hk = ck(v0) && /native code/.test(String(v0)), dk = UI, fk = YI, y0 = dk("keys"), XI = function(t) {
  return y0[t] || (y0[t] = fk(t));
}, pS = {}, gk = hk, qI = En, mk = co, pk = ud, OA = ho, PA = fS, wk = XI, Ak = pS, S0 = "Object already initialized", Xv = qI.TypeError, Ck = qI.WeakMap, am, Oh, lm, vk = function(t) {
  return lm(t) ? Oh(t) : am(t, {});
}, yk = function(t) {
  return function(e) {
    var n;
    if (!mk(e) || (n = Oh(e)).type !== t)
      throw new Xv("Incompatible receiver, " + t + " required");
    return n;
  };
};
if (gk || PA.state) {
  var Pr = PA.state || (PA.state = new Ck());
  Pr.get = Pr.get, Pr.has = Pr.has, Pr.set = Pr.set, am = function(t, e) {
    if (Pr.has(t))
      throw new Xv(S0);
    return e.facade = t, Pr.set(t, e), e;
  }, Oh = function(t) {
    return Pr.get(t) || {};
  }, lm = function(t) {
    return Pr.has(t);
  };
} else {
  var vl = wk("state");
  Ak[vl] = !0, am = function(t, e) {
    if (OA(t, vl))
      throw new Xv(S0);
    return e.facade = t, pk(t, vl, e), e;
  }, Oh = function(t) {
    return OA(t, vl) ? t[vl] : {};
  }, lm = function(t) {
    return OA(t, vl);
  };
}
var Sk = {
  set: am,
  get: Oh,
  has: lm,
  enforce: vk,
  getterFor: yk
}, wS = an, bk = fi, Rk = Tn, zd = ho, qv = Er, Ek = rk.CONFIGURABLE, Tk = lk, ZI = Sk, Mk = ZI.enforce, Ik = ZI.get, b0 = String, _g = Object.defineProperty, Ok = wS("".slice), Pk = wS("".replace), xk = wS([].join), Hk = qv && !bk(function() {
  return _g(function() {
  }, "length", { value: 8 }).length !== 8;
}), _k = String(String).split("String"), Nk = KI.exports = function(t, e, n) {
  Ok(b0(e), 0, 7) === "Symbol(" && (e = "[" + Pk(b0(e), /^Symbol\(([^)]*)\).*$/, "$1") + "]"), n && n.getter && (e = "get " + e), n && n.setter && (e = "set " + e), (!zd(t, "name") || Ek && t.name !== e) && (qv ? _g(t, "name", { value: e, configurable: !0 }) : t.name = e), Hk && n && zd(n, "arity") && t.length !== n.arity && _g(t, "length", { value: n.arity });
  try {
    n && zd(n, "constructor") && n.constructor ? qv && _g(t, "prototype", { writable: !1 }) : t.prototype && (t.prototype = void 0);
  } catch (r) {
  }
  var i = Mk(t);
  return zd(i, "source") || (i.source = xk(_k, typeof e == "string" ? e : "")), t;
};
Function.prototype.toString = Nk(function() {
  return Rk(this) && Ik(this).source || Tk(this);
}, "toString");
var Dk = KI.exports, kk = Tn, Lk = qs, Bk = Dk, Fk = dS, Vk = function(t, e, n, i) {
  i || (i = {});
  var r = i.enumerable, o = i.name !== void 0 ? i.name : e;
  if (kk(n) && Bk(n, o, i), i.global)
    r ? t[e] = n : Fk(e, n);
  else {
    try {
      i.unsafe ? t[e] && (r = !0) : delete t[e];
    } catch (s) {
    }
    r ? t[e] = n : Lk.f(t, e, {
      value: n,
      enumerable: !1,
      configurable: !i.nonConfigurable,
      writable: !i.nonWritable
    });
  }
  return t;
}, JI = {}, Wk = Math.ceil, $k = Math.floor, Uk = Math.trunc || function(e) {
  var n = +e;
  return (n > 0 ? $k : Wk)(n);
}, Yk = Uk, cd = function(t) {
  var e = +t;
  return e !== e || e === 0 ? 0 : Yk(e);
}, jk = cd, Gk = Math.max, zk = Math.min, Kk = function(t, e) {
  var n = jk(t);
  return n < 0 ? Gk(n + e, 0) : zk(n, e);
}, Qk = cd, Xk = Math.min, qk = function(t) {
  var e = Qk(t);
  return e > 0 ? Xk(e, 9007199254740991) : 0;
}, Zk = qk, hd = function(t) {
  return Zk(t.length);
}, Jk = rp, eL = Kk, tL = hd, R0 = function(t) {
  return function(e, n, i) {
    var r = Jk(e), o = tL(r);
    if (o === 0)
      return !t && -1;
    var s = eL(i, o), a;
    if (t && n !== n) {
      for (; o > s; )
        if (a = r[s++], a !== a)
          return !0;
    } else
      for (; o > s; s++)
        if ((t || s in r) && r[s] === n)
          return t || s || 0;
    return !t && -1;
  };
}, nL = {
  // `Array.prototype.includes` method
  // https://tc39.es/ecma262/#sec-array.prototype.includes
  includes: R0(!0),
  // `Array.prototype.indexOf` method
  // https://tc39.es/ecma262/#sec-array.prototype.indexof
  indexOf: R0(!1)
}, iL = an, xA = ho, rL = rp, oL = nL.indexOf, sL = pS, E0 = iL([].push), eO = function(t, e) {
  var n = rL(t), i = 0, r = [], o;
  for (o in n)
    !xA(sL, o) && xA(n, o) && E0(r, o);
  for (; e.length > i; )
    xA(n, o = e[i++]) && (~oL(r, o) || E0(r, o));
  return r;
}, AS = [
  "constructor",
  "hasOwnProperty",
  "isPrototypeOf",
  "propertyIsEnumerable",
  "toLocaleString",
  "toString",
  "valueOf"
], aL = eO, lL = AS, uL = lL.concat("length", "prototype");
JI.f = Object.getOwnPropertyNames || function(e) {
  return aL(e, uL);
};
var tO = {};
tO.f = Object.getOwnPropertySymbols;
var cL = xu, hL = an, dL = JI, fL = tO, gL = ts, mL = hL([].concat), pL = cL("Reflect", "ownKeys") || function(e) {
  var n = dL.f(gL(e)), i = fL.f;
  return i ? mL(n, i(e)) : n;
}, T0 = ho, wL = pL, AL = uS, CL = qs, nO = function(t, e, n) {
  for (var i = wL(e), r = CL.f, o = AL.f, s = 0; s < i.length; s++) {
    var a = i[s];
    !T0(t, a) && !(n && T0(n, a)) && r(t, a, o(e, a));
  }
}, vL = fi, yL = Tn, SL = /#|\.prototype\./, dd = function(t, e) {
  var n = RL[bL(t)];
  return n === TL ? !0 : n === EL ? !1 : yL(e) ? vL(e) : !!e;
}, bL = dd.normalize = function(t) {
  return String(t).replace(SL, ".").toLowerCase();
}, RL = dd.data = {}, EL = dd.NATIVE = "N", TL = dd.POLYFILL = "P", ML = dd, Kd = En, IL = uS.f, OL = ud, PL = Vk, xL = dS, HL = nO, _L = ML, Mn = function(t, e) {
  var n = t.target, i = t.global, r = t.stat, o, s, a, l, u, c;
  if (i ? s = Kd : r ? s = Kd[n] || xL(n, {}) : s = Kd[n] && Kd[n].prototype, s)
    for (a in e) {
      if (u = e[a], t.dontCallGetSet ? (c = IL(s, a), l = c && c.value) : l = s[a], o = _L(i ? a : n + (r ? "." : "#") + a, t.forced), !o && l !== void 0) {
        if (typeof u == typeof l)
          continue;
        HL(u, l);
      }
      (t.sham || l && l.sham) && OL(u, "sham", !0), PL(s, a, u, t);
    }
}, NL = tp, iO = Function.prototype, M0 = iO.apply, I0 = iO.call, up = typeof Reflect == "object" && Reflect.apply || (NL ? I0.bind(M0) : function() {
  return I0.apply(M0, arguments);
}), DL = an, kL = ad, rO = function(t, e, n) {
  try {
    return DL(kL(Object.getOwnPropertyDescriptor(t, e)[n]));
  } catch (i) {
  }
}, LL = co, BL = function(t) {
  return LL(t) || t === null;
}, FL = BL, VL = String, WL = TypeError, $L = function(t) {
  if (FL(t))
    return t;
  throw new WL("Can't set " + VL(t) + " as a prototype");
}, UL = rO, YL = co, jL = sd, GL = $L, oO = Object.setPrototypeOf || ("__proto__" in {} ? function() {
  var t = !1, e = {}, n;
  try {
    n = UL(Object.prototype, "__proto__", "set"), n(e, []), t = e instanceof Array;
  } catch (i) {
  }
  return function(r, o) {
    return jL(r), GL(o), YL(r) && (t ? n(r, o) : r.__proto__ = o), r;
  };
}() : void 0), zL = qs.f, KL = function(t, e, n) {
  n in t || zL(t, n, {
    configurable: !0,
    get: function() {
      return e[n];
    },
    set: function(i) {
      e[n] = i;
    }
  });
}, QL = Tn, XL = co, O0 = oO, qL = function(t, e, n) {
  var i, r;
  return (
    // it can work only with native `setPrototypeOf`
    O0 && // we haven't completely correct pre-ES6 way for getting `new.target`, so use this
    QL(i = e.constructor) && i !== n && XL(r = i.prototype) && r !== n.prototype && O0(t, r), t
  );
}, ZL = Xs, JL = ZL("toStringTag"), sO = {};
sO[JL] = "z";
var eB = String(sO) === "[object z]", tB = eB, nB = Tn, Ng = al, iB = Xs, rB = iB("toStringTag"), oB = Object, sB = Ng(function() {
  return arguments;
}()) === "Arguments", aB = function(t, e) {
  try {
    return t[e];
  } catch (n) {
  }
}, aO = tB ? Ng : function(t) {
  var e, n, i;
  return t === void 0 ? "Undefined" : t === null ? "Null" : typeof (n = aB(e = oB(t), rB)) == "string" ? n : sB ? Ng(e) : (i = Ng(e)) === "Object" && nB(e.callee) ? "Arguments" : i;
}, lB = aO, uB = String, cp = function(t) {
  if (lB(t) === "Symbol")
    throw new TypeError("Cannot convert a Symbol value to a string");
  return uB(t);
}, cB = cp, hB = function(t, e) {
  return t === void 0 ? arguments.length < 2 ? "" : e : cB(t);
}, dB = co, fB = ud, gB = function(t, e) {
  dB(e) && "cause" in e && fB(t, "cause", e.cause);
}, mB = an, lO = Error, pB = mB("".replace), wB = function(t) {
  return String(new lO(t).stack);
}("zxcasd"), uO = /\n\s*at [^:]*:[^\n]*/, AB = uO.test(wB), CB = function(t, e) {
  if (AB && typeof t == "string" && !lO.prepareStackTrace)
    for (; e--; )
      t = pB(t, uO, "");
  return t;
}, vB = fi, yB = np, SB = !vB(function() {
  var t = new Error("a");
  return "stack" in t ? (Object.defineProperty(t, "stack", yB(1, 7)), t.stack !== 7) : !0;
}), bB = ud, RB = CB, EB = SB, P0 = Error.captureStackTrace, TB = function(t, e, n, i) {
  EB && (P0 ? P0(t, e) : bB(t, "stack", RB(n, i)));
}, x0 = xu, MB = ho, H0 = ud, IB = op, _0 = oO, N0 = nO, D0 = KL, OB = qL, PB = hB, xB = gB, HB = TB, _B = Er, NB = function(t, e, n, i) {
  var r = "stackTraceLimit", o = i ? 2 : 1, s = t.split("."), a = s[s.length - 1], l = x0.apply(null, s);
  if (l) {
    var u = l.prototype;
    if (MB(u, "cause") && delete u.cause, !n)
      return l;
    var c = x0("Error"), h = e(function(d, g) {
      var m = PB(i ? g : d, void 0), w = i ? new l(d) : new l();
      return m !== void 0 && H0(w, "message", m), HB(w, h, w.stack, 2), this && IB(u, this) && OB(w, this, h), arguments.length > o && xB(w, arguments[o]), w;
    });
    h.prototype = u, a !== "Error" ? _0 ? _0(h, c) : N0(h, c, { name: !0 }) : _B && r in l && (D0(h, l, r), D0(h, l, "prepareStackTrace")), N0(h, l);
    try {
      u.name !== a && H0(u, "name", a), u.constructor = h;
    } catch (d) {
    }
    return h;
  }
}, cO = Mn, DB = En, fo = up, hO = NB, Zv = "WebAssembly", k0 = DB[Zv], um = new Error("e", { cause: 7 }).cause !== 7, ll = function(t, e) {
  var n = {};
  n[t] = hO(t, e, um), cO({ global: !0, constructor: !0, arity: 1, forced: um }, n);
}, CS = function(t, e) {
  if (k0 && k0[t]) {
    var n = {};
    n[t] = hO(Zv + "." + t, e, um), cO({ target: Zv, stat: !0, constructor: !0, arity: 1, forced: um }, n);
  }
};
ll("Error", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
ll("EvalError", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
ll("RangeError", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
ll("ReferenceError", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
ll("SyntaxError", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
ll("TypeError", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
ll("URIError", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
CS("CompileError", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
CS("LinkError", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
CS("RuntimeError", function(t) {
  return function(n) {
    return fo(t, this, arguments);
  };
});
var kB = al, dO = Array.isArray || function(e) {
  return kB(e) === "Array";
}, LB = Er, BB = dO, FB = TypeError, VB = Object.getOwnPropertyDescriptor, WB = LB && !function() {
  if (this !== void 0)
    return !0;
  try {
    Object.defineProperty([], "length", { writable: !1 }).length = 1;
  } catch (t) {
    return t instanceof TypeError;
  }
}(), fO = WB ? function(t, e) {
  if (BB(t) && !VB(t, "length").writable)
    throw new FB("Cannot set read only .length");
  return t.length = e;
} : function(t, e) {
  return t.length = e;
}, $B = TypeError, UB = 9007199254740991, gO = function(t) {
  if (t > UB)
    throw $B("Maximum allowed index exceeded");
  return t;
}, YB = Mn, jB = ld, GB = hd, zB = fO, KB = gO, QB = fi, XB = QB(function() {
  return [].push.call({ length: 4294967296 }, 1) !== 4294967297;
}), qB = function() {
  try {
    Object.defineProperty([], "length", { writable: !1 }).push();
  } catch (t) {
    return t instanceof TypeError;
  }
}, ZB = XB || !qB();
YB({ target: "Array", proto: !0, arity: 1, forced: ZB }, {
  // eslint-disable-next-line no-unused-vars -- required for `.length`
  push: function(e) {
    var n = jB(this), i = GB(n), r = arguments.length;
    KB(i + r);
    for (var o = 0; o < r; o++)
      n[i] = arguments[o], i++;
    return zB(n, i), i;
  }
});
var HA = an, Qd = Set.prototype, go = {
  // eslint-disable-next-line es/no-set -- safe
  Set,
  add: HA(Qd.add),
  has: HA(Qd.has),
  remove: HA(Qd.delete),
  proto: Qd
}, JB = go.has, ul = function(t) {
  return JB(t), t;
}, eF = Tr, cl = function(t, e, n) {
  for (var i = n ? t : t.iterator, r = t.next, o, s; !(o = eF(r, i)).done; )
    if (s = e(o.value), s !== void 0)
      return s;
}, mO = an, tF = cl, pO = go, nF = pO.Set, wO = pO.proto, iF = mO(wO.forEach), AO = mO(wO.keys), rF = AO(new nF()).next, fd = function(t, e, n) {
  return n ? tF({ iterator: AO(t), next: rF }, e) : iF(t, e);
}, CO = go, oF = fd, sF = CO.Set, aF = CO.add, vS = function(t) {
  var e = new sF();
  return oF(t, function(n) {
    aF(e, n);
  }), e;
}, lF = rO, uF = go, gd = lF(uF.proto, "size", "get") || function(t) {
  return t.size;
}, cF = function(t) {
  return {
    iterator: t,
    next: t.next,
    done: !1
  };
}, L0 = ad, vO = ts, B0 = Tr, hF = cd, dF = cF, F0 = "Invalid size", fF = RangeError, gF = TypeError, mF = Math.max, yO = function(t, e) {
  this.set = t, this.size = mF(e, 0), this.has = L0(t.has), this.keys = L0(t.keys);
};
yO.prototype = {
  getIterator: function() {
    return dF(vO(B0(this.keys, this.set)));
  },
  includes: function(t) {
    return B0(this.has, this.set, t);
  }
};
var hl = function(t) {
  vO(t);
  var e = +t.size;
  if (e !== e)
    throw new gF(F0);
  var n = hF(e);
  if (n < 0)
    throw new fF(F0);
  return new yO(t, n);
}, pF = ul, SO = go, wF = vS, AF = gd, CF = hl, vF = fd, yF = cl, SF = SO.has, V0 = SO.remove, bF = function(e) {
  var n = pF(this), i = CF(e), r = wF(n);
  return AF(n) <= i.size ? vF(n, function(o) {
    i.includes(o) && V0(r, o);
  }) : yF(i.getIterator(), function(o) {
    SF(n, o) && V0(r, o);
  }), r;
}, RF = xu, W0 = function(t) {
  return {
    size: t,
    has: function() {
      return !1;
    },
    keys: function() {
      return {
        next: function() {
          return { done: !0 };
        }
      };
    }
  };
}, $0 = function(t) {
  return {
    size: t,
    has: function() {
      return !0;
    },
    keys: function() {
      throw new Error("e");
    }
  };
}, dl = function(t, e) {
  var n = RF("Set");
  try {
    new n()[t](W0(0));
    try {
      return new n()[t](W0(-1)), !1;
    } catch (r) {
      if (!e)
        return !0;
      try {
        return new n()[t]($0(-1 / 0)), !1;
      } catch (o) {
        var i = new n();
        return i.add(1), i.add(2), e(i[t]($0(1 / 0)));
      }
    }
  } catch (r) {
    return !1;
  }
}, EF = Mn, TF = bF, MF = dl, IF = !MF("difference", function(t) {
  return t.size === 0;
});
EF({ target: "Set", proto: !0, real: !0, forced: IF }, {
  difference: TF
});
var OF = ul, yS = go, PF = gd, xF = hl, HF = fd, _F = cl, NF = yS.Set, U0 = yS.add, DF = yS.has, kF = function(e) {
  var n = OF(this), i = xF(e), r = new NF();
  return PF(n) > i.size ? _F(i.getIterator(), function(o) {
    DF(n, o) && U0(r, o);
  }) : HF(n, function(o) {
    i.includes(o) && U0(r, o);
  }), r;
}, LF = Mn, BF = fi, FF = kF, VF = dl, WF = !VF("intersection", function(t) {
  return t.size === 2 && t.has(1) && t.has(2);
}) || BF(function() {
  return String(Array.from((/* @__PURE__ */ new Set([1, 2, 3])).intersection(/* @__PURE__ */ new Set([3, 2])))) !== "3,2";
});
LF({ target: "Set", proto: !0, real: !0, forced: WF }, {
  intersection: FF
});
var $F = Tr, Y0 = ts, UF = lp, SS = function(t, e, n) {
  var i, r;
  Y0(t);
  try {
    if (i = UF(t, "return"), !i) {
      if (e === "throw")
        throw n;
      return n;
    }
    i = $F(i, t);
  } catch (o) {
    r = !0, i = o;
  }
  if (e === "throw")
    throw n;
  if (r)
    throw i;
  return Y0(i), n;
}, YF = ul, jF = go.has, GF = gd, zF = hl, KF = fd, QF = cl, XF = SS, qF = function(e) {
  var n = YF(this), i = zF(e);
  if (GF(n) <= i.size)
    return KF(n, function(o) {
      if (i.includes(o))
        return !1;
    }, !0) !== !1;
  var r = i.getIterator();
  return QF(r, function(o) {
    if (jF(n, o))
      return XF(r, "normal", !1);
  }) !== !1;
}, ZF = Mn, JF = qF, eV = dl, tV = !eV("isDisjointFrom", function(t) {
  return !t;
});
ZF({ target: "Set", proto: !0, real: !0, forced: tV }, {
  isDisjointFrom: JF
});
var nV = ul, iV = gd, rV = fd, oV = hl, sV = function(e) {
  var n = nV(this), i = oV(e);
  return iV(n) > i.size ? !1 : rV(n, function(r) {
    if (!i.includes(r))
      return !1;
  }, !0) !== !1;
}, aV = Mn, lV = sV, uV = dl, cV = !uV("isSubsetOf", function(t) {
  return t;
});
aV({ target: "Set", proto: !0, real: !0, forced: cV }, {
  isSubsetOf: lV
});
var hV = ul, dV = go.has, fV = gd, gV = hl, mV = cl, pV = SS, wV = function(e) {
  var n = hV(this), i = gV(e);
  if (fV(n) < i.size)
    return !1;
  var r = i.getIterator();
  return mV(r, function(o) {
    if (!dV(n, o))
      return pV(r, "normal", !1);
  }) !== !1;
}, AV = Mn, CV = wV, vV = dl, yV = !vV("isSupersetOf", function(t) {
  return !t;
});
AV({ target: "Set", proto: !0, real: !0, forced: yV }, {
  isSupersetOf: CV
});
var SV = ul, bS = go, bV = vS, RV = hl, EV = cl, TV = bS.add, MV = bS.has, IV = bS.remove, OV = function(e) {
  var n = SV(this), i = RV(e).getIterator(), r = bV(n);
  return EV(i, function(o) {
    MV(n, o) ? IV(r, o) : TV(r, o);
  }), r;
}, PV = Mn, xV = OV, HV = dl;
PV({ target: "Set", proto: !0, real: !0, forced: !HV("symmetricDifference") }, {
  symmetricDifference: xV
});
var _V = ul, NV = go.add, DV = vS, kV = hl, LV = cl, BV = function(e) {
  var n = _V(this), i = kV(e).getIterator(), r = DV(n);
  return LV(i, function(o) {
    NV(r, o);
  }), r;
}, FV = Mn, VV = BV, WV = dl;
FV({ target: "Set", proto: !0, real: !0, forced: !WV("union") }, {
  union: VV
});
var $V = al, UV = an, YV = function(t) {
  if ($V(t) === "Function")
    return UV(t);
}, j0 = YV, jV = ad, GV = tp, zV = j0(j0.bind), bO = function(t, e) {
  return jV(t), e === void 0 ? t : GV ? zV(t, e) : function() {
    return t.apply(e, arguments);
  };
}, KV = xu, RO = KV("document", "documentElement"), QV = an, RS = QV([].slice), XV = TypeError, EO = function(t, e) {
  if (t < e)
    throw new XV("Not enough arguments");
  return t;
}, qV = sp, ZV = /(?:ipad|iphone|ipod).*applewebkit/i.test(qV), Gu = En, JV = sp, eW = al, Xd = function(t) {
  return JV.slice(0, t.length) === t;
}, TO = function() {
  return Xd("Bun/") ? "BUN" : Xd("Cloudflare-Workers") ? "CLOUDFLARE" : Xd("Deno/") ? "DENO" : Xd("Node.js/") ? "NODE" : Gu.Bun && typeof Bun.version == "string" ? "BUN" : Gu.Deno && typeof Deno.version == "object" ? "DENO" : eW(Gu.process) === "process" ? "NODE" : Gu.window && Gu.document ? "BROWSER" : "REST";
}(), tW = TO, nW = tW === "NODE", Ei = En, iW = up, rW = bO, G0 = Tn, oW = ho, MO = fi, z0 = RO, sW = RS, K0 = gS, aW = EO, lW = ZV, uW = nW, Jv = Ei.setImmediate, ey = Ei.clearImmediate, cW = Ei.process, _A = Ei.Dispatch, hW = Ei.Function, Q0 = Ei.MessageChannel, dW = Ei.String, NA = 0, nh = {}, X0 = "onreadystatechange", Ph, ta, DA, kA;
MO(function() {
  Ph = Ei.location;
});
var ES = function(t) {
  if (oW(nh, t)) {
    var e = nh[t];
    delete nh[t], e();
  }
}, LA = function(t) {
  return function() {
    ES(t);
  };
}, q0 = function(t) {
  ES(t.data);
}, Z0 = function(t) {
  Ei.postMessage(dW(t), Ph.protocol + "//" + Ph.host);
};
(!Jv || !ey) && (Jv = function(e) {
  aW(arguments.length, 1);
  var n = G0(e) ? e : hW(e), i = sW(arguments, 1);
  return nh[++NA] = function() {
    iW(n, void 0, i);
  }, ta(NA), NA;
}, ey = function(e) {
  delete nh[e];
}, uW ? ta = function(t) {
  cW.nextTick(LA(t));
} : _A && _A.now ? ta = function(t) {
  _A.now(LA(t));
} : Q0 && !lW ? (DA = new Q0(), kA = DA.port2, DA.port1.onmessage = q0, ta = rW(kA.postMessage, kA)) : Ei.addEventListener && G0(Ei.postMessage) && !Ei.importScripts && Ph && Ph.protocol !== "file:" && !MO(Z0) ? (ta = Z0, Ei.addEventListener("message", q0, !1)) : X0 in K0("script") ? ta = function(t) {
  z0.appendChild(K0("script"))[X0] = function() {
    z0.removeChild(this), ES(t);
  };
} : ta = function(t) {
  setTimeout(LA(t), 0);
});
var IO = {
  set: Jv,
  clear: ey
}, fW = Mn, gW = En, J0 = IO.clear;
fW({ global: !0, bind: !0, enumerable: !0, forced: gW.clearImmediate !== J0 }, {
  clearImmediate: J0
});
var OO = En, mW = up, pW = Tn, wW = TO, AW = sp, CW = RS, vW = EO, yW = OO.Function, SW = /MSIE .\./.test(AW) || wW === "BUN" && function() {
  var t = OO.Bun.version.split(".");
  return t.length < 3 || t[0] === "0" && (t[1] < 3 || t[1] === "3" && t[2] === "0");
}(), bW = function(t, e) {
  var n = e ? 2 : 1;
  return SW ? function(i, r) {
    var o = vW(arguments.length, 1) > n, s = pW(i) ? i : yW(i), a = o ? CW(arguments, n) : [], l = o ? function() {
      mW(s, this, a);
    } : s;
    return e ? t(l, r) : t(l);
  } : t;
}, RW = Mn, PO = En, eE = IO.set, EW = bW, tE = PO.setImmediate ? EW(eE, !1) : eE;
RW({ global: !0, bind: !0, enumerable: !0, forced: PO.setImmediate !== tE }, {
  setImmediate: tE
});
/*! @license DOMPurify 2.5.8 | (c) Cure53 and other contributors | Released under the Apache license 2.0 and Mozilla Public License 2.0 | github.com/cure53/DOMPurify/blob/2.5.8/LICENSE */
function Is(t) {
  "@babel/helpers - typeof";
  return Is = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(e) {
    return typeof e;
  } : function(e) {
    return e && typeof Symbol == "function" && e.constructor === Symbol && e !== Symbol.prototype ? "symbol" : typeof e;
  }, Is(t);
}
function ty(t, e) {
  return ty = Object.setPrototypeOf || function(i, r) {
    return i.__proto__ = r, i;
  }, ty(t, e);
}
function TW() {
  if (typeof Reflect == "undefined" || !Reflect.construct || Reflect.construct.sham)
    return !1;
  if (typeof Proxy == "function")
    return !0;
  try {
    return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    })), !0;
  } catch (t) {
    return !1;
  }
}
function Dg(t, e, n) {
  return TW() ? Dg = Reflect.construct : Dg = function(r, o, s) {
    var a = [null];
    a.push.apply(a, o);
    var l = Function.bind.apply(r, a), u = new l();
    return s && ty(u, s.prototype), u;
  }, Dg.apply(null, arguments);
}
function cr(t) {
  return MW(t) || IW(t) || OW(t) || PW();
}
function MW(t) {
  if (Array.isArray(t))
    return ny(t);
}
function IW(t) {
  if (typeof Symbol != "undefined" && t[Symbol.iterator] != null || t["@@iterator"] != null)
    return Array.from(t);
}
function OW(t, e) {
  if (t) {
    if (typeof t == "string")
      return ny(t, e);
    var n = Object.prototype.toString.call(t).slice(8, -1);
    if (n === "Object" && t.constructor && (n = t.constructor.name), n === "Map" || n === "Set")
      return Array.from(t);
    if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n))
      return ny(t, e);
  }
}
function ny(t, e) {
  (e == null || e > t.length) && (e = t.length);
  for (var n = 0, i = new Array(e); n < e; n++)
    i[n] = t[n];
  return i;
}
function PW() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
var xW = Object.hasOwnProperty, nE = Object.setPrototypeOf, HW = Object.isFrozen, _W = Object.getPrototypeOf, NW = Object.getOwnPropertyDescriptor, Xn = Object.freeze, zi = Object.seal, DW = Object.create, xO = typeof Reflect != "undefined" && Reflect, cm = xO.apply, iy = xO.construct;
cm || (cm = function(e, n, i) {
  return e.apply(n, i);
});
Xn || (Xn = function(e) {
  return e;
});
zi || (zi = function(e) {
  return e;
});
iy || (iy = function(e, n) {
  return Dg(e, cr(n));
});
var kW = Ki(Array.prototype.forEach), iE = Ki(Array.prototype.pop), zu = Ki(Array.prototype.push), kg = Ki(String.prototype.toLowerCase), BA = Ki(String.prototype.toString), rE = Ki(String.prototype.match), er = Ki(String.prototype.replace), LW = Ki(String.prototype.indexOf), BW = Ki(String.prototype.trim), Rn = Ki(RegExp.prototype.test), FA = FW(TypeError);
function Ki(t) {
  return function(e) {
    for (var n = arguments.length, i = new Array(n > 1 ? n - 1 : 0), r = 1; r < n; r++)
      i[r - 1] = arguments[r];
    return cm(t, e, i);
  };
}
function FW(t) {
  return function() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    return iy(t, n);
  };
}
function Ye(t, e, n) {
  var i;
  n = (i = n) !== null && i !== void 0 ? i : kg, nE && nE(t, null);
  for (var r = e.length; r--; ) {
    var o = e[r];
    if (typeof o == "string") {
      var s = n(o);
      s !== o && (HW(e) || (e[r] = s), o = s);
    }
    t[o] = !0;
  }
  return t;
}
function na(t) {
  var e = DW(null), n;
  for (n in t)
    cm(xW, t, [n]) === !0 && (e[n] = t[n]);
  return e;
}
function qd(t, e) {
  for (; t !== null; ) {
    var n = NW(t, e);
    if (n) {
      if (n.get)
        return Ki(n.get);
      if (typeof n.value == "function")
        return Ki(n.value);
    }
    t = _W(t);
  }
  function i(r) {
    return console.warn("fallback value for", r), null;
  }
  return i;
}
var oE = Xn(["a", "abbr", "acronym", "address", "area", "article", "aside", "audio", "b", "bdi", "bdo", "big", "blink", "blockquote", "body", "br", "button", "canvas", "caption", "center", "cite", "code", "col", "colgroup", "content", "data", "datalist", "dd", "decorator", "del", "details", "dfn", "dialog", "dir", "div", "dl", "dt", "element", "em", "fieldset", "figcaption", "figure", "font", "footer", "form", "h1", "h2", "h3", "h4", "h5", "h6", "head", "header", "hgroup", "hr", "html", "i", "img", "input", "ins", "kbd", "label", "legend", "li", "main", "map", "mark", "marquee", "menu", "menuitem", "meter", "nav", "nobr", "ol", "optgroup", "option", "output", "p", "picture", "pre", "progress", "q", "rp", "rt", "ruby", "s", "samp", "section", "select", "shadow", "small", "source", "spacer", "span", "strike", "strong", "style", "sub", "summary", "sup", "table", "tbody", "td", "template", "textarea", "tfoot", "th", "thead", "time", "tr", "track", "tt", "u", "ul", "var", "video", "wbr"]), VA = Xn(["svg", "a", "altglyph", "altglyphdef", "altglyphitem", "animatecolor", "animatemotion", "animatetransform", "circle", "clippath", "defs", "desc", "ellipse", "filter", "font", "g", "glyph", "glyphref", "hkern", "image", "line", "lineargradient", "marker", "mask", "metadata", "mpath", "path", "pattern", "polygon", "polyline", "radialgradient", "rect", "stop", "style", "switch", "symbol", "text", "textpath", "title", "tref", "tspan", "view", "vkern"]), WA = Xn(["feBlend", "feColorMatrix", "feComponentTransfer", "feComposite", "feConvolveMatrix", "feDiffuseLighting", "feDisplacementMap", "feDistantLight", "feFlood", "feFuncA", "feFuncB", "feFuncG", "feFuncR", "feGaussianBlur", "feImage", "feMerge", "feMergeNode", "feMorphology", "feOffset", "fePointLight", "feSpecularLighting", "feSpotLight", "feTile", "feTurbulence"]), VW = Xn(["animate", "color-profile", "cursor", "discard", "fedropshadow", "font-face", "font-face-format", "font-face-name", "font-face-src", "font-face-uri", "foreignobject", "hatch", "hatchpath", "mesh", "meshgradient", "meshpatch", "meshrow", "missing-glyph", "script", "set", "solidcolor", "unknown", "use"]), $A = Xn(["math", "menclose", "merror", "mfenced", "mfrac", "mglyph", "mi", "mlabeledtr", "mmultiscripts", "mn", "mo", "mover", "mpadded", "mphantom", "mroot", "mrow", "ms", "mspace", "msqrt", "mstyle", "msub", "msup", "msubsup", "mtable", "mtd", "mtext", "mtr", "munder", "munderover"]), WW = Xn(["maction", "maligngroup", "malignmark", "mlongdiv", "mscarries", "mscarry", "msgroup", "mstack", "msline", "msrow", "semantics", "annotation", "annotation-xml", "mprescripts", "none"]), sE = Xn(["#text"]), aE = Xn(["accept", "action", "align", "alt", "autocapitalize", "autocomplete", "autopictureinpicture", "autoplay", "background", "bgcolor", "border", "capture", "cellpadding", "cellspacing", "checked", "cite", "class", "clear", "color", "cols", "colspan", "controls", "controlslist", "coords", "crossorigin", "datetime", "decoding", "default", "dir", "disabled", "disablepictureinpicture", "disableremoteplayback", "download", "draggable", "enctype", "enterkeyhint", "face", "for", "headers", "height", "hidden", "high", "href", "hreflang", "id", "inputmode", "integrity", "ismap", "kind", "label", "lang", "list", "loading", "loop", "low", "max", "maxlength", "media", "method", "min", "minlength", "multiple", "muted", "name", "nonce", "noshade", "novalidate", "nowrap", "open", "optimum", "pattern", "placeholder", "playsinline", "poster", "preload", "pubdate", "radiogroup", "readonly", "rel", "required", "rev", "reversed", "role", "rows", "rowspan", "spellcheck", "scope", "selected", "shape", "size", "sizes", "span", "srclang", "start", "src", "srcset", "step", "style", "summary", "tabindex", "title", "translate", "type", "usemap", "valign", "value", "width", "xmlns", "slot"]), UA = Xn(["accent-height", "accumulate", "additive", "alignment-baseline", "ascent", "attributename", "attributetype", "azimuth", "basefrequency", "baseline-shift", "begin", "bias", "by", "class", "clip", "clippathunits", "clip-path", "clip-rule", "color", "color-interpolation", "color-interpolation-filters", "color-profile", "color-rendering", "cx", "cy", "d", "dx", "dy", "diffuseconstant", "direction", "display", "divisor", "dur", "edgemode", "elevation", "end", "fill", "fill-opacity", "fill-rule", "filter", "filterunits", "flood-color", "flood-opacity", "font-family", "font-size", "font-size-adjust", "font-stretch", "font-style", "font-variant", "font-weight", "fx", "fy", "g1", "g2", "glyph-name", "glyphref", "gradientunits", "gradienttransform", "height", "href", "id", "image-rendering", "in", "in2", "k", "k1", "k2", "k3", "k4", "kerning", "keypoints", "keysplines", "keytimes", "lang", "lengthadjust", "letter-spacing", "kernelmatrix", "kernelunitlength", "lighting-color", "local", "marker-end", "marker-mid", "marker-start", "markerheight", "markerunits", "markerwidth", "maskcontentunits", "maskunits", "max", "mask", "media", "method", "mode", "min", "name", "numoctaves", "offset", "operator", "opacity", "order", "orient", "orientation", "origin", "overflow", "paint-order", "path", "pathlength", "patterncontentunits", "patterntransform", "patternunits", "points", "preservealpha", "preserveaspectratio", "primitiveunits", "r", "rx", "ry", "radius", "refx", "refy", "repeatcount", "repeatdur", "restart", "result", "rotate", "scale", "seed", "shape-rendering", "specularconstant", "specularexponent", "spreadmethod", "startoffset", "stddeviation", "stitchtiles", "stop-color", "stop-opacity", "stroke-dasharray", "stroke-dashoffset", "stroke-linecap", "stroke-linejoin", "stroke-miterlimit", "stroke-opacity", "stroke", "stroke-width", "style", "surfacescale", "systemlanguage", "tabindex", "targetx", "targety", "transform", "transform-origin", "text-anchor", "text-decoration", "text-rendering", "textlength", "type", "u1", "u2", "unicode", "values", "viewbox", "visibility", "version", "vert-adv-y", "vert-origin-x", "vert-origin-y", "width", "word-spacing", "wrap", "writing-mode", "xchannelselector", "ychannelselector", "x", "x1", "x2", "xmlns", "y", "y1", "y2", "z", "zoomandpan"]), lE = Xn(["accent", "accentunder", "align", "bevelled", "close", "columnsalign", "columnlines", "columnspan", "denomalign", "depth", "dir", "display", "displaystyle", "encoding", "fence", "frame", "height", "href", "id", "largeop", "length", "linethickness", "lspace", "lquote", "mathbackground", "mathcolor", "mathsize", "mathvariant", "maxsize", "minsize", "movablelimits", "notation", "numalign", "open", "rowalign", "rowlines", "rowspacing", "rowspan", "rspace", "rquote", "scriptlevel", "scriptminsize", "scriptsizemultiplier", "selection", "separator", "separators", "stretchy", "subscriptshift", "supscriptshift", "symmetric", "voffset", "width", "xmlns"]), Zd = Xn(["xlink:href", "xml:id", "xlink:title", "xml:space", "xmlns:xlink"]), $W = zi(/\{\{[\w\W]*|[\w\W]*\}\}/gm), UW = zi(/<%[\w\W]*|[\w\W]*%>/gm), YW = zi(/\${[\w\W]*}/gm), jW = zi(/^data-[\-\w.\u00B7-\uFFFF]+$/), GW = zi(/^aria-[\-\w]+$/), zW = zi(
  /^(?:(?:(?:f|ht)tps?|mailto|tel|callto|cid|xmpp):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i
  // eslint-disable-line no-useless-escape
), KW = zi(/^(?:\w+script|data):/i), QW = zi(
  /[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205F\u3000]/g
  // eslint-disable-line no-control-regex
), XW = zi(/^html$/i), qW = zi(/^[a-z][.\w]*(-[.\w]+)+$/i), ZW = function() {
  return typeof window == "undefined" ? null : window;
}, JW = function(e, n) {
  if (Is(e) !== "object" || typeof e.createPolicy != "function")
    return null;
  var i = null, r = "data-tt-policy-suffix";
  n.currentScript && n.currentScript.hasAttribute(r) && (i = n.currentScript.getAttribute(r));
  var o = "dompurify" + (i ? "#" + i : "");
  try {
    return e.createPolicy(o, {
      createHTML: function(a) {
        return a;
      },
      createScriptURL: function(a) {
        return a;
      }
    });
  } catch (s) {
    return console.warn("TrustedTypes policy " + o + " could not be created."), null;
  }
};
function HO() {
  var t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : ZW(), e = function(H) {
    return HO(H);
  };
  if (e.version = "2.5.8", e.removed = [], !t || !t.document || t.document.nodeType !== 9)
    return e.isSupported = !1, e;
  var n = t.document, i = t.document, r = t.DocumentFragment, o = t.HTMLTemplateElement, s = t.Node, a = t.Element, l = t.NodeFilter, u = t.NamedNodeMap, c = u === void 0 ? t.NamedNodeMap || t.MozNamedAttrMap : u, h = t.HTMLFormElement, d = t.DOMParser, g = t.trustedTypes, m = a.prototype, w = qd(m, "cloneNode"), C = qd(m, "nextSibling"), S = qd(m, "childNodes"), b = qd(m, "parentNode");
  if (typeof o == "function") {
    var R = i.createElement("template");
    R.content && R.content.ownerDocument && (i = R.content.ownerDocument);
  }
  var _ = JW(g, n), O = _ ? _.createHTML("") : "", $ = i, U = $.implementation, G = $.createNodeIterator, z = $.createDocumentFragment, j = $.getElementsByTagName, J = n.importNode, q = {};
  try {
    q = na(i).documentMode ? i.documentMode : {};
  } catch (Se) {
  }
  var re = {};
  e.isSupported = typeof b == "function" && U && U.createHTMLDocument !== void 0 && q !== 9;
  var me = $W, Me = UW, nt = YW, Ne = jW, Ce = GW, pe = KW, wt = QW, f = qW, p = zW, A = null, M = Ye({}, [].concat(cr(oE), cr(VA), cr(WA), cr($A), cr(sE))), P = null, F = Ye({}, [].concat(cr(aE), cr(UA), cr(lE), cr(Zd))), Y = Object.seal(Object.create(null, {
    tagNameCheck: {
      writable: !0,
      configurable: !1,
      enumerable: !0,
      value: null
    },
    attributeNameCheck: {
      writable: !0,
      configurable: !1,
      enumerable: !0,
      value: null
    },
    allowCustomizedBuiltInElements: {
      writable: !0,
      configurable: !1,
      enumerable: !0,
      value: !1
    }
  })), K = null, le = null, ce = !0, Z = !0, He = !1, Pe = !0, W = !1, Qe = !0, Ge = !1, it = !1, Ve = !1, Re = !1, v = !1, y = !1, E = !0, L = !1, I = "user-content-", B = !0, N = !1, x = {}, k = null, D = Ye({}, ["annotation-xml", "audio", "colgroup", "desc", "foreignobject", "head", "iframe", "math", "mi", "mn", "mo", "ms", "mtext", "noembed", "noframes", "noscript", "plaintext", "script", "style", "svg", "template", "thead", "title", "video", "xmp"]), V = null, ne = Ye({}, ["audio", "video", "img", "source", "image", "track"]), ue = null, ye = Ye({}, ["alt", "class", "for", "id", "label", "name", "pattern", "placeholder", "role", "summary", "title", "value", "style", "xmlns"]), Le = "http://www.w3.org/1998/Math/MathML", Ze = "http://www.w3.org/2000/svg", Ue = "http://www.w3.org/1999/xhtml", Oe = Ue, et = !1, Ft = null, Cl = Ye({}, [Le, Ze, Ue], BA), Ji, Uu = ["application/xhtml+xml", "text/html"], Mi = "text/html", Dt, ln = null, Sn = i.createElement("form"), Pt = function(H) {
    return H instanceof RegExp || H instanceof Function;
  }, fA = function(H) {
    ln && ln === H || ((!H || Is(H) !== "object") && (H = {}), H = na(H), Ji = // eslint-disable-next-line unicorn/prefer-includes
    Uu.indexOf(H.PARSER_MEDIA_TYPE) === -1 ? Ji = Mi : Ji = H.PARSER_MEDIA_TYPE, Dt = Ji === "application/xhtml+xml" ? BA : kg, A = "ALLOWED_TAGS" in H ? Ye({}, H.ALLOWED_TAGS, Dt) : M, P = "ALLOWED_ATTR" in H ? Ye({}, H.ALLOWED_ATTR, Dt) : F, Ft = "ALLOWED_NAMESPACES" in H ? Ye({}, H.ALLOWED_NAMESPACES, BA) : Cl, ue = "ADD_URI_SAFE_ATTR" in H ? Ye(
      na(ye),
      // eslint-disable-line indent
      H.ADD_URI_SAFE_ATTR,
      // eslint-disable-line indent
      Dt
      // eslint-disable-line indent
    ) : ye, V = "ADD_DATA_URI_TAGS" in H ? Ye(
      na(ne),
      // eslint-disable-line indent
      H.ADD_DATA_URI_TAGS,
      // eslint-disable-line indent
      Dt
      // eslint-disable-line indent
    ) : ne, k = "FORBID_CONTENTS" in H ? Ye({}, H.FORBID_CONTENTS, Dt) : D, K = "FORBID_TAGS" in H ? Ye({}, H.FORBID_TAGS, Dt) : {}, le = "FORBID_ATTR" in H ? Ye({}, H.FORBID_ATTR, Dt) : {}, x = "USE_PROFILES" in H ? H.USE_PROFILES : !1, ce = H.ALLOW_ARIA_ATTR !== !1, Z = H.ALLOW_DATA_ATTR !== !1, He = H.ALLOW_UNKNOWN_PROTOCOLS || !1, Pe = H.ALLOW_SELF_CLOSE_IN_ATTR !== !1, W = H.SAFE_FOR_TEMPLATES || !1, Qe = H.SAFE_FOR_XML !== !1, Ge = H.WHOLE_DOCUMENT || !1, Re = H.RETURN_DOM || !1, v = H.RETURN_DOM_FRAGMENT || !1, y = H.RETURN_TRUSTED_TYPE || !1, Ve = H.FORCE_BODY || !1, E = H.SANITIZE_DOM !== !1, L = H.SANITIZE_NAMED_PROPS || !1, B = H.KEEP_CONTENT !== !1, N = H.IN_PLACE || !1, p = H.ALLOWED_URI_REGEXP || p, Oe = H.NAMESPACE || Ue, Y = H.CUSTOM_ELEMENT_HANDLING || {}, H.CUSTOM_ELEMENT_HANDLING && Pt(H.CUSTOM_ELEMENT_HANDLING.tagNameCheck) && (Y.tagNameCheck = H.CUSTOM_ELEMENT_HANDLING.tagNameCheck), H.CUSTOM_ELEMENT_HANDLING && Pt(H.CUSTOM_ELEMENT_HANDLING.attributeNameCheck) && (Y.attributeNameCheck = H.CUSTOM_ELEMENT_HANDLING.attributeNameCheck), H.CUSTOM_ELEMENT_HANDLING && typeof H.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements == "boolean" && (Y.allowCustomizedBuiltInElements = H.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements), W && (Z = !1), v && (Re = !0), x && (A = Ye({}, cr(sE)), P = [], x.html === !0 && (Ye(A, oE), Ye(P, aE)), x.svg === !0 && (Ye(A, VA), Ye(P, UA), Ye(P, Zd)), x.svgFilters === !0 && (Ye(A, WA), Ye(P, UA), Ye(P, Zd)), x.mathMl === !0 && (Ye(A, $A), Ye(P, lE), Ye(P, Zd))), H.ADD_TAGS && (A === M && (A = na(A)), Ye(A, H.ADD_TAGS, Dt)), H.ADD_ATTR && (P === F && (P = na(P)), Ye(P, H.ADD_ATTR, Dt)), H.ADD_URI_SAFE_ATTR && Ye(ue, H.ADD_URI_SAFE_ATTR, Dt), H.FORBID_CONTENTS && (k === D && (k = na(k)), Ye(k, H.FORBID_CONTENTS, Dt)), B && (A["#text"] = !0), Ge && Ye(A, ["html", "head", "body"]), A.table && (Ye(A, ["tbody"]), delete K.tbody), Xn && Xn(H), ln = H);
  }, UR = Ye({}, ["mi", "mo", "mn", "ms", "mtext"]), YR = Ye({}, ["annotation-xml"]), G_ = Ye({}, ["title", "style", "font", "a", "script"]), Bd = Ye({}, VA);
  Ye(Bd, WA), Ye(Bd, VW);
  var gA = Ye({}, $A);
  Ye(gA, WW);
  var z_ = function(H) {
    var te = b(H);
    (!te || !te.tagName) && (te = {
      namespaceURI: Oe,
      tagName: "template"
    });
    var ae = kg(H.tagName), ct = kg(te.tagName);
    return Ft[H.namespaceURI] ? H.namespaceURI === Ze ? te.namespaceURI === Ue ? ae === "svg" : te.namespaceURI === Le ? ae === "svg" && (ct === "annotation-xml" || UR[ct]) : !!Bd[ae] : H.namespaceURI === Le ? te.namespaceURI === Ue ? ae === "math" : te.namespaceURI === Ze ? ae === "math" && YR[ct] : !!gA[ae] : H.namespaceURI === Ue ? te.namespaceURI === Ze && !YR[ct] || te.namespaceURI === Le && !UR[ct] ? !1 : !gA[ae] && (G_[ae] || !Bd[ae]) : !!(Ji === "application/xhtml+xml" && Ft[H.namespaceURI]) : !1;
  }, Ii = function(H) {
    zu(e.removed, {
      element: H
    });
    try {
      H.parentNode.removeChild(H);
    } catch (te) {
      try {
        H.outerHTML = O;
      } catch (ae) {
        H.remove();
      }
    }
  }, Fd = function(H, te) {
    try {
      zu(e.removed, {
        attribute: te.getAttributeNode(H),
        from: te
      });
    } catch (ae) {
      zu(e.removed, {
        attribute: null,
        from: te
      });
    }
    if (te.removeAttribute(H), H === "is" && !P[H])
      if (Re || v)
        try {
          Ii(te);
        } catch (ae) {
        }
      else
        try {
          te.setAttribute(H, "");
        } catch (ae) {
        }
  }, jR = function(H) {
    var te, ae;
    if (Ve)
      H = "<remove></remove>" + H;
    else {
      var ct = rE(H, /^[\r\n\t ]+/);
      ae = ct && ct[0];
    }
    Ji === "application/xhtml+xml" && Oe === Ue && (H = '<html xmlns="http://www.w3.org/1999/xhtml"><head></head><body>' + H + "</body></html>");
    var Zn = _ ? _.createHTML(H) : H;
    if (Oe === Ue)
      try {
        te = new d().parseFromString(Zn, Ji);
      } catch (bn) {
      }
    if (!te || !te.documentElement) {
      te = U.createDocument(Oe, "template", null);
      try {
        te.documentElement.innerHTML = et ? O : Zn;
      } catch (bn) {
      }
    }
    var In = te.body || te.documentElement;
    return H && ae && In.insertBefore(i.createTextNode(ae), In.childNodes[0] || null), Oe === Ue ? j.call(te, Ge ? "html" : "body")[0] : Ge ? te.documentElement : In;
  }, GR = function(H) {
    return G.call(
      H.ownerDocument || H,
      H,
      // eslint-disable-next-line no-bitwise
      l.SHOW_ELEMENT | l.SHOW_COMMENT | l.SHOW_TEXT | l.SHOW_PROCESSING_INSTRUCTION | l.SHOW_CDATA_SECTION,
      null,
      !1
    );
  }, mA = function(H) {
    return H instanceof h && (typeof H.nodeName != "string" || typeof H.textContent != "string" || typeof H.removeChild != "function" || !(H.attributes instanceof c) || typeof H.removeAttribute != "function" || typeof H.setAttribute != "function" || typeof H.namespaceURI != "string" || typeof H.insertBefore != "function" || typeof H.hasChildNodes != "function");
  }, Yu = function(H) {
    return Is(s) === "object" ? H instanceof s : H && Is(H) === "object" && typeof H.nodeType == "number" && typeof H.nodeName == "string";
  }, Ao = function(H, te, ae) {
    re[H] && kW(re[H], function(ct) {
      ct.call(e, te, ae, ln);
    });
  }, zR = function(H) {
    var te;
    if (Ao("beforeSanitizeElements", H, null), mA(H) || Rn(/[\u0080-\uFFFF]/, H.nodeName))
      return Ii(H), !0;
    var ae = Dt(H.nodeName);
    if (Ao("uponSanitizeElement", H, {
      tagName: ae,
      allowedTags: A
    }), H.hasChildNodes() && !Yu(H.firstElementChild) && (!Yu(H.content) || !Yu(H.content.firstElementChild)) && Rn(/<[/\w]/g, H.innerHTML) && Rn(/<[/\w]/g, H.textContent) || ae === "select" && Rn(/<template/i, H.innerHTML) || H.nodeType === 7 || Qe && H.nodeType === 8 && Rn(/<[/\w]/g, H.data))
      return Ii(H), !0;
    if (!A[ae] || K[ae]) {
      if (!K[ae] && QR(ae) && (Y.tagNameCheck instanceof RegExp && Rn(Y.tagNameCheck, ae) || Y.tagNameCheck instanceof Function && Y.tagNameCheck(ae)))
        return !1;
      if (B && !k[ae]) {
        var ct = b(H) || H.parentNode, Zn = S(H) || H.childNodes;
        if (Zn && ct)
          for (var In = Zn.length, bn = In - 1; bn >= 0; --bn) {
            var ea = w(Zn[bn], !0);
            ea.__removalCount = (H.__removalCount || 0) + 1, ct.insertBefore(ea, C(H));
          }
      }
      return Ii(H), !0;
    }
    return H instanceof a && !z_(H) || (ae === "noscript" || ae === "noembed" || ae === "noframes") && Rn(/<\/no(script|embed|frames)/i, H.innerHTML) ? (Ii(H), !0) : (W && H.nodeType === 3 && (te = H.textContent, te = er(te, me, " "), te = er(te, Me, " "), te = er(te, nt, " "), H.textContent !== te && (zu(e.removed, {
      element: H.cloneNode()
    }), H.textContent = te)), Ao("afterSanitizeElements", H, null), !1);
  }, KR = function(H, te, ae) {
    if (E && (te === "id" || te === "name") && (ae in i || ae in Sn))
      return !1;
    if (!(Z && !le[te] && Rn(Ne, te))) {
      if (!(ce && Rn(Ce, te))) {
        if (!P[te] || le[te]) {
          if (
            // First condition does a very basic check if a) it's basically a valid custom element tagname AND
            // b) if the tagName passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.tagNameCheck
            // and c) if the attribute name passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.attributeNameCheck
            !(QR(H) && (Y.tagNameCheck instanceof RegExp && Rn(Y.tagNameCheck, H) || Y.tagNameCheck instanceof Function && Y.tagNameCheck(H)) && (Y.attributeNameCheck instanceof RegExp && Rn(Y.attributeNameCheck, te) || Y.attributeNameCheck instanceof Function && Y.attributeNameCheck(te)) || // Alternative, second condition checks if it's an `is`-attribute, AND
            // the value passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.tagNameCheck
            te === "is" && Y.allowCustomizedBuiltInElements && (Y.tagNameCheck instanceof RegExp && Rn(Y.tagNameCheck, ae) || Y.tagNameCheck instanceof Function && Y.tagNameCheck(ae)))
          )
            return !1;
        } else if (!ue[te]) {
          if (!Rn(p, er(ae, wt, ""))) {
            if (!((te === "src" || te === "xlink:href" || te === "href") && H !== "script" && LW(ae, "data:") === 0 && V[H])) {
              if (!(He && !Rn(pe, er(ae, wt, "")))) {
                if (ae)
                  return !1;
              }
            }
          }
        }
      }
    }
    return !0;
  }, QR = function(H) {
    return H !== "annotation-xml" && rE(H, f);
  }, XR = function(H) {
    var te, ae, ct, Zn;
    Ao("beforeSanitizeAttributes", H, null);
    var In = H.attributes;
    if (!(!In || mA(H))) {
      var bn = {
        attrName: "",
        attrValue: "",
        keepAttr: !0,
        allowedAttributes: P
      };
      for (Zn = In.length; Zn--; ) {
        te = In[Zn];
        var ea = te, un = ea.name, pA = ea.namespaceURI;
        if (ae = un === "value" ? te.value : BW(te.value), ct = Dt(un), bn.attrName = ct, bn.attrValue = ae, bn.keepAttr = !0, bn.forceKeepAttr = void 0, Ao("uponSanitizeAttribute", H, bn), ae = bn.attrValue, !bn.forceKeepAttr && (Fd(un, H), !!bn.keepAttr)) {
          if (!Pe && Rn(/\/>/i, ae)) {
            Fd(un, H);
            continue;
          }
          W && (ae = er(ae, me, " "), ae = er(ae, Me, " "), ae = er(ae, nt, " "));
          var qR = Dt(H.nodeName);
          if (KR(qR, ct, ae)) {
            if (L && (ct === "id" || ct === "name") && (Fd(un, H), ae = I + ae), Qe && Rn(/((--!?|])>)|<\/(style|title)/i, ae)) {
              Fd(un, H);
              continue;
            }
            if (_ && Is(g) === "object" && typeof g.getAttributeType == "function" && !pA)
              switch (g.getAttributeType(qR, ct)) {
                case "TrustedHTML": {
                  ae = _.createHTML(ae);
                  break;
                }
                case "TrustedScriptURL": {
                  ae = _.createScriptURL(ae);
                  break;
                }
              }
            try {
              pA ? H.setAttributeNS(pA, un, ae) : H.setAttribute(un, ae), mA(H) ? Ii(H) : iE(e.removed);
            } catch (bce) {
            }
          }
        }
      }
      Ao("afterSanitizeAttributes", H, null);
    }
  }, K_ = function Se(H) {
    var te, ae = GR(H);
    for (Ao("beforeSanitizeShadowDOM", H, null); te = ae.nextNode(); )
      Ao("uponSanitizeShadowNode", te, null), zR(te), XR(te), te.content instanceof r && Se(te.content);
    Ao("afterSanitizeShadowDOM", H, null);
  };
  return e.sanitize = function(Se) {
    var H = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, te, ae, ct, Zn, In;
    if (et = !Se, et && (Se = "<!-->"), typeof Se != "string" && !Yu(Se))
      if (typeof Se.toString == "function") {
        if (Se = Se.toString(), typeof Se != "string")
          throw FA("dirty is not a string, aborting");
      } else
        throw FA("toString is not a function");
    if (!e.isSupported) {
      if (Is(t.toStaticHTML) === "object" || typeof t.toStaticHTML == "function") {
        if (typeof Se == "string")
          return t.toStaticHTML(Se);
        if (Yu(Se))
          return t.toStaticHTML(Se.outerHTML);
      }
      return Se;
    }
    if (it || fA(H), e.removed = [], typeof Se == "string" && (N = !1), N) {
      if (Se.nodeName) {
        var bn = Dt(Se.nodeName);
        if (!A[bn] || K[bn])
          throw FA("root node is forbidden and cannot be sanitized in-place");
      }
    } else if (Se instanceof s)
      te = jR("<!---->"), ae = te.ownerDocument.importNode(Se, !0), ae.nodeType === 1 && ae.nodeName === "BODY" || ae.nodeName === "HTML" ? te = ae : te.appendChild(ae);
    else {
      if (!Re && !W && !Ge && // eslint-disable-next-line unicorn/prefer-includes
      Se.indexOf("<") === -1)
        return _ && y ? _.createHTML(Se) : Se;
      if (te = jR(Se), !te)
        return Re ? null : y ? O : "";
    }
    te && Ve && Ii(te.firstChild);
    for (var ea = GR(N ? Se : te); ct = ea.nextNode(); )
      ct.nodeType === 3 && ct === Zn || (zR(ct), XR(ct), ct.content instanceof r && K_(ct.content), Zn = ct);
    if (Zn = null, N)
      return Se;
    if (Re) {
      if (v)
        for (In = z.call(te.ownerDocument); te.firstChild; )
          In.appendChild(te.firstChild);
      else
        In = te;
      return (P.shadowroot || P.shadowrootmod) && (In = J.call(n, In, !0)), In;
    }
    var un = Ge ? te.outerHTML : te.innerHTML;
    return Ge && A["!doctype"] && te.ownerDocument && te.ownerDocument.doctype && te.ownerDocument.doctype.name && Rn(XW, te.ownerDocument.doctype.name) && (un = "<!DOCTYPE " + te.ownerDocument.doctype.name + `>
` + un), W && (un = er(un, me, " "), un = er(un, Me, " "), un = er(un, nt, " ")), _ && y ? _.createHTML(un) : un;
  }, e.setConfig = function(Se) {
    fA(Se), it = !0;
  }, e.clearConfig = function() {
    ln = null, it = !1;
  }, e.isValidAttribute = function(Se, H, te) {
    ln || fA({});
    var ae = Dt(Se), ct = Dt(H);
    return KR(ae, ct, te);
  }, e.addHook = function(Se, H) {
    typeof H == "function" && (re[Se] = re[Se] || [], zu(re[Se], H));
  }, e.removeHook = function(Se) {
    if (re[Se])
      return iE(re[Se]);
  }, e.removeHooks = function(Se) {
    re[Se] && (re[Se] = []);
  }, e.removeAllHooks = function() {
    re = {};
  }, e;
}
var e$ = HO();
//! moment.js
//! version : 2.30.1
//! authors : Tim Wood, Iskren Chernev, Moment.js contributors
//! license : MIT
//! momentjs.com
var _O;
function X() {
  return _O.apply(null, arguments);
}
function t$(t) {
  _O = t;
}
function yr(t) {
  return t instanceof Array || Object.prototype.toString.call(t) === "[object Array]";
}
function Za(t) {
  return t != null && Object.prototype.toString.call(t) === "[object Object]";
}
function lt(t, e) {
  return Object.prototype.hasOwnProperty.call(t, e);
}
function TS(t) {
  if (Object.getOwnPropertyNames)
    return Object.getOwnPropertyNames(t).length === 0;
  var e;
  for (e in t)
    if (lt(t, e))
      return !1;
  return !0;
}
function si(t) {
  return t === void 0;
}
function qo(t) {
  return typeof t == "number" || Object.prototype.toString.call(t) === "[object Number]";
}
function md(t) {
  return t instanceof Date || Object.prototype.toString.call(t) === "[object Date]";
}
function NO(t, e) {
  var n = [], i, r = t.length;
  for (i = 0; i < r; ++i)
    n.push(e(t[i], i));
  return n;
}
function _s(t, e) {
  for (var n in e)
    lt(e, n) && (t[n] = e[n]);
  return lt(e, "toString") && (t.toString = e.toString), lt(e, "valueOf") && (t.valueOf = e.valueOf), t;
}
function mo(t, e, n, i) {
  return iP(t, e, n, i, !0).utc();
}
function n$() {
  return {
    empty: !1,
    unusedTokens: [],
    unusedInput: [],
    overflow: -2,
    charsLeftOver: 0,
    nullInput: !1,
    invalidEra: null,
    invalidMonth: null,
    invalidFormat: !1,
    userInvalidated: !1,
    iso: !1,
    parsedDateParts: [],
    era: null,
    meridiem: null,
    rfc2822: !1,
    weekdayMismatch: !1
  };
}
function We(t) {
  return t._pf == null && (t._pf = n$()), t._pf;
}
var ry;
Array.prototype.some ? ry = Array.prototype.some : ry = function(t) {
  var e = Object(this), n = e.length >>> 0, i;
  for (i = 0; i < n; i++)
    if (i in e && t.call(this, e[i], i, e))
      return !0;
  return !1;
};
function MS(t) {
  var e = null, n = !1, i = t._d && !isNaN(t._d.getTime());
  if (i && (e = We(t), n = ry.call(e.parsedDateParts, function(r) {
    return r != null;
  }), i = e.overflow < 0 && !e.empty && !e.invalidEra && !e.invalidMonth && !e.invalidWeekday && !e.weekdayMismatch && !e.nullInput && !e.invalidFormat && !e.userInvalidated && (!e.meridiem || e.meridiem && n), t._strict && (i = i && e.charsLeftOver === 0 && e.unusedTokens.length === 0 && e.bigHour === void 0)), Object.isFrozen == null || !Object.isFrozen(t))
    t._isValid = i;
  else
    return i;
  return t._isValid;
}
function hp(t) {
  var e = mo(NaN);
  return t != null ? _s(We(e), t) : We(e).userInvalidated = !0, e;
}
var uE = X.momentProperties = [], YA = !1;
function IS(t, e) {
  var n, i, r, o = uE.length;
  if (si(e._isAMomentObject) || (t._isAMomentObject = e._isAMomentObject), si(e._i) || (t._i = e._i), si(e._f) || (t._f = e._f), si(e._l) || (t._l = e._l), si(e._strict) || (t._strict = e._strict), si(e._tzm) || (t._tzm = e._tzm), si(e._isUTC) || (t._isUTC = e._isUTC), si(e._offset) || (t._offset = e._offset), si(e._pf) || (t._pf = We(e)), si(e._locale) || (t._locale = e._locale), o > 0)
    for (n = 0; n < o; n++)
      i = uE[n], r = e[i], si(r) || (t[i] = r);
  return t;
}
function pd(t) {
  IS(this, t), this._d = new Date(t._d != null ? t._d.getTime() : NaN), this.isValid() || (this._d = /* @__PURE__ */ new Date(NaN)), YA === !1 && (YA = !0, X.updateOffset(this), YA = !1);
}
function Sr(t) {
  return t instanceof pd || t != null && t._isAMomentObject != null;
}
function DO(t) {
  X.suppressDeprecationWarnings === !1 && typeof console != "undefined" && console.warn && console.warn("Deprecation warning: " + t);
}
function Xi(t, e) {
  var n = !0;
  return _s(function() {
    if (X.deprecationHandler != null && X.deprecationHandler(null, t), n) {
      var i = [], r, o, s, a = arguments.length;
      for (o = 0; o < a; o++) {
        if (r = "", typeof arguments[o] == "object") {
          r += `
[` + o + "] ";
          for (s in arguments[0])
            lt(arguments[0], s) && (r += s + ": " + arguments[0][s] + ", ");
          r = r.slice(0, -2);
        } else
          r = arguments[o];
        i.push(r);
      }
      DO(
        t + `
Arguments: ` + Array.prototype.slice.call(i).join("") + `
` + new Error().stack
      ), n = !1;
    }
    return e.apply(this, arguments);
  }, e);
}
var cE = {};
function kO(t, e) {
  X.deprecationHandler != null && X.deprecationHandler(t, e), cE[t] || (DO(e), cE[t] = !0);
}
X.suppressDeprecationWarnings = !1;
X.deprecationHandler = null;
function po(t) {
  return typeof Function != "undefined" && t instanceof Function || Object.prototype.toString.call(t) === "[object Function]";
}
function i$(t) {
  var e, n;
  for (n in t)
    lt(t, n) && (e = t[n], po(e) ? this[n] = e : this["_" + n] = e);
  this._config = t, this._dayOfMonthOrdinalParseLenient = new RegExp(
    (this._dayOfMonthOrdinalParse.source || this._ordinalParse.source) + "|" + /\d{1,2}/.source
  );
}
function oy(t, e) {
  var n = _s({}, t), i;
  for (i in e)
    lt(e, i) && (Za(t[i]) && Za(e[i]) ? (n[i] = {}, _s(n[i], t[i]), _s(n[i], e[i])) : e[i] != null ? n[i] = e[i] : delete n[i]);
  for (i in t)
    lt(t, i) && !lt(e, i) && Za(t[i]) && (n[i] = _s({}, n[i]));
  return n;
}
function OS(t) {
  t != null && this.set(t);
}
var sy;
Object.keys ? sy = Object.keys : sy = function(t) {
  var e, n = [];
  for (e in t)
    lt(t, e) && n.push(e);
  return n;
};
var r$ = {
  sameDay: "[Today at] LT",
  nextDay: "[Tomorrow at] LT",
  nextWeek: "dddd [at] LT",
  lastDay: "[Yesterday at] LT",
  lastWeek: "[Last] dddd [at] LT",
  sameElse: "L"
};
function o$(t, e, n) {
  var i = this._calendar[t] || this._calendar.sameElse;
  return po(i) ? i.call(e, n) : i;
}
function lo(t, e, n) {
  var i = "" + Math.abs(t), r = e - i.length, o = t >= 0;
  return (o ? n ? "+" : "" : "-") + Math.pow(10, Math.max(0, r)).toString().substr(1) + i;
}
var PS = /(\[[^\[]*\])|(\\)?([Hh]mm(ss)?|Mo|MM?M?M?|Do|DDDo|DD?D?D?|ddd?d?|do?|w[o|w]?|W[o|W]?|Qo?|N{1,5}|YYYYYY|YYYYY|YYYY|YY|y{2,4}|yo?|gg(ggg?)?|GG(GGG?)?|e|E|a|A|hh?|HH?|kk?|mm?|ss?|S{1,9}|x|X|zz?|ZZ?|.)/g, Jd = /(\[[^\[]*\])|(\\)?(LTS|LT|LL?L?L?|l{1,4})/g, jA = {}, pu = {};
function be(t, e, n, i) {
  var r = i;
  typeof i == "string" && (r = function() {
    return this[i]();
  }), t && (pu[t] = r), e && (pu[e[0]] = function() {
    return lo(r.apply(this, arguments), e[1], e[2]);
  }), n && (pu[n] = function() {
    return this.localeData().ordinal(
      r.apply(this, arguments),
      t
    );
  });
}
function s$(t) {
  return t.match(/\[[\s\S]/) ? t.replace(/^\[|\]$/g, "") : t.replace(/\\/g, "");
}
function a$(t) {
  var e = t.match(PS), n, i;
  for (n = 0, i = e.length; n < i; n++)
    pu[e[n]] ? e[n] = pu[e[n]] : e[n] = s$(e[n]);
  return function(r) {
    var o = "", s;
    for (s = 0; s < i; s++)
      o += po(e[s]) ? e[s].call(r, t) : e[s];
    return o;
  };
}
function Lg(t, e) {
  return t.isValid() ? (e = LO(e, t.localeData()), jA[e] = jA[e] || a$(e), jA[e](t)) : t.localeData().invalidDate();
}
function LO(t, e) {
  var n = 5;
  function i(r) {
    return e.longDateFormat(r) || r;
  }
  for (Jd.lastIndex = 0; n >= 0 && Jd.test(t); )
    t = t.replace(
      Jd,
      i
    ), Jd.lastIndex = 0, n -= 1;
  return t;
}
var l$ = {
  LTS: "h:mm:ss A",
  LT: "h:mm A",
  L: "MM/DD/YYYY",
  LL: "MMMM D, YYYY",
  LLL: "MMMM D, YYYY h:mm A",
  LLLL: "dddd, MMMM D, YYYY h:mm A"
};
function u$(t) {
  var e = this._longDateFormat[t], n = this._longDateFormat[t.toUpperCase()];
  return e || !n ? e : (this._longDateFormat[t] = n.match(PS).map(function(i) {
    return i === "MMMM" || i === "MM" || i === "DD" || i === "dddd" ? i.slice(1) : i;
  }).join(""), this._longDateFormat[t]);
}
var c$ = "Invalid date";
function h$() {
  return this._invalidDate;
}
var d$ = "%d", f$ = /\d{1,2}/;
function g$(t) {
  return this._ordinal.replace("%d", t);
}
var m$ = {
  future: "in %s",
  past: "%s ago",
  s: "a few seconds",
  ss: "%d seconds",
  m: "a minute",
  mm: "%d minutes",
  h: "an hour",
  hh: "%d hours",
  d: "a day",
  dd: "%d days",
  w: "a week",
  ww: "%d weeks",
  M: "a month",
  MM: "%d months",
  y: "a year",
  yy: "%d years"
};
function p$(t, e, n, i) {
  var r = this._relativeTime[n];
  return po(r) ? r(t, e, n, i) : r.replace(/%d/i, t);
}
function w$(t, e) {
  var n = this._relativeTime[t > 0 ? "future" : "past"];
  return po(n) ? n(e) : n.replace(/%s/i, e);
}
var hE = {
  D: "date",
  dates: "date",
  date: "date",
  d: "day",
  days: "day",
  day: "day",
  e: "weekday",
  weekdays: "weekday",
  weekday: "weekday",
  E: "isoWeekday",
  isoweekdays: "isoWeekday",
  isoweekday: "isoWeekday",
  DDD: "dayOfYear",
  dayofyears: "dayOfYear",
  dayofyear: "dayOfYear",
  h: "hour",
  hours: "hour",
  hour: "hour",
  ms: "millisecond",
  milliseconds: "millisecond",
  millisecond: "millisecond",
  m: "minute",
  minutes: "minute",
  minute: "minute",
  M: "month",
  months: "month",
  month: "month",
  Q: "quarter",
  quarters: "quarter",
  quarter: "quarter",
  s: "second",
  seconds: "second",
  second: "second",
  gg: "weekYear",
  weekyears: "weekYear",
  weekyear: "weekYear",
  GG: "isoWeekYear",
  isoweekyears: "isoWeekYear",
  isoweekyear: "isoWeekYear",
  w: "week",
  weeks: "week",
  week: "week",
  W: "isoWeek",
  isoweeks: "isoWeek",
  isoweek: "isoWeek",
  y: "year",
  years: "year",
  year: "year"
};
function qi(t) {
  return typeof t == "string" ? hE[t] || hE[t.toLowerCase()] : void 0;
}
function xS(t) {
  var e = {}, n, i;
  for (i in t)
    lt(t, i) && (n = qi(i), n && (e[n] = t[i]));
  return e;
}
var A$ = {
  date: 9,
  day: 11,
  weekday: 11,
  isoWeekday: 11,
  dayOfYear: 4,
  hour: 13,
  millisecond: 16,
  minute: 14,
  month: 8,
  quarter: 7,
  second: 15,
  weekYear: 1,
  isoWeekYear: 1,
  week: 5,
  isoWeek: 5,
  year: 1
};
function C$(t) {
  var e = [], n;
  for (n in t)
    lt(t, n) && e.push({ unit: n, priority: A$[n] });
  return e.sort(function(i, r) {
    return i.priority - r.priority;
  }), e;
}
var BO = /\d/, Ti = /\d\d/, FO = /\d{3}/, HS = /\d{4}/, dp = /[+-]?\d{6}/, Nt = /\d\d?/, VO = /\d\d\d\d?/, WO = /\d\d\d\d\d\d?/, fp = /\d{1,3}/, _S = /\d{1,4}/, gp = /[+-]?\d{1,6}/, Hu = /\d+/, mp = /[+-]?\d+/, v$ = /Z|[+-]\d\d:?\d\d/gi, pp = /Z|[+-]\d\d(?::?\d\d)?/gi, y$ = /[+-]?\d+(\.\d{1,3})?/, wd = /[0-9]{0,256}['a-z\u00A0-\u05FF\u0700-\uD7FF\uF900-\uFDCF\uFDF0-\uFF07\uFF10-\uFFEF]{1,256}|[\u0600-\u06FF\/]{1,256}(\s*?[\u0600-\u06FF]{1,256}){1,2}/i, _u = /^[1-9]\d?/, NS = /^([1-9]\d|\d)/, hm;
hm = {};
function fe(t, e, n) {
  hm[t] = po(e) ? e : function(i, r) {
    return i && n ? n : e;
  };
}
function S$(t, e) {
  return lt(hm, t) ? hm[t](e._strict, e._locale) : new RegExp(b$(t));
}
function b$(t) {
  return Qo(
    t.replace("\\", "").replace(
      /\\(\[)|\\(\])|\[([^\]\[]*)\]|\\(.)/g,
      function(e, n, i, r, o) {
        return n || i || r || o;
      }
    )
  );
}
function Qo(t) {
  return t.replace(/[-\/\\^$*+?.()|[\]{}]/g, "\\$&");
}
function $i(t) {
  return t < 0 ? Math.ceil(t) || 0 : Math.floor(t);
}
function Je(t) {
  var e = +t, n = 0;
  return e !== 0 && isFinite(e) && (n = $i(e)), n;
}
var ay = {};
function Rt(t, e) {
  var n, i = e, r;
  for (typeof t == "string" && (t = [t]), qo(e) && (i = function(o, s) {
    s[e] = Je(o);
  }), r = t.length, n = 0; n < r; n++)
    ay[t[n]] = i;
}
function Ad(t, e) {
  Rt(t, function(n, i, r, o) {
    r._w = r._w || {}, e(n, r._w, r, o);
  });
}
function R$(t, e, n) {
  e != null && lt(ay, t) && ay[t](e, n._a, n, t);
}
function wp(t) {
  return t % 4 === 0 && t % 100 !== 0 || t % 400 === 0;
}
var Ln = 0, $o = 1, to = 2, mn = 3, Ar = 4, Uo = 5, Ua = 6, E$ = 7, T$ = 8;
be("Y", 0, 0, function() {
  var t = this.year();
  return t <= 9999 ? lo(t, 4) : "+" + t;
});
be(0, ["YY", 2], 0, function() {
  return this.year() % 100;
});
be(0, ["YYYY", 4], 0, "year");
be(0, ["YYYYY", 5], 0, "year");
be(0, ["YYYYYY", 6, !0], 0, "year");
fe("Y", mp);
fe("YY", Nt, Ti);
fe("YYYY", _S, HS);
fe("YYYYY", gp, dp);
fe("YYYYYY", gp, dp);
Rt(["YYYYY", "YYYYYY"], Ln);
Rt("YYYY", function(t, e) {
  e[Ln] = t.length === 2 ? X.parseTwoDigitYear(t) : Je(t);
});
Rt("YY", function(t, e) {
  e[Ln] = X.parseTwoDigitYear(t);
});
Rt("Y", function(t, e) {
  e[Ln] = parseInt(t, 10);
});
function ih(t) {
  return wp(t) ? 366 : 365;
}
X.parseTwoDigitYear = function(t) {
  return Je(t) + (Je(t) > 68 ? 1900 : 2e3);
};
var $O = Nu("FullYear", !0);
function M$() {
  return wp(this.year());
}
function Nu(t, e) {
  return function(n) {
    return n != null ? (UO(this, t, n), X.updateOffset(this, e), this) : xh(this, t);
  };
}
function xh(t, e) {
  if (!t.isValid())
    return NaN;
  var n = t._d, i = t._isUTC;
  switch (e) {
    case "Milliseconds":
      return i ? n.getUTCMilliseconds() : n.getMilliseconds();
    case "Seconds":
      return i ? n.getUTCSeconds() : n.getSeconds();
    case "Minutes":
      return i ? n.getUTCMinutes() : n.getMinutes();
    case "Hours":
      return i ? n.getUTCHours() : n.getHours();
    case "Date":
      return i ? n.getUTCDate() : n.getDate();
    case "Day":
      return i ? n.getUTCDay() : n.getDay();
    case "Month":
      return i ? n.getUTCMonth() : n.getMonth();
    case "FullYear":
      return i ? n.getUTCFullYear() : n.getFullYear();
    default:
      return NaN;
  }
}
function UO(t, e, n) {
  var i, r, o, s, a;
  if (!(!t.isValid() || isNaN(n))) {
    switch (i = t._d, r = t._isUTC, e) {
      case "Milliseconds":
        return void (r ? i.setUTCMilliseconds(n) : i.setMilliseconds(n));
      case "Seconds":
        return void (r ? i.setUTCSeconds(n) : i.setSeconds(n));
      case "Minutes":
        return void (r ? i.setUTCMinutes(n) : i.setMinutes(n));
      case "Hours":
        return void (r ? i.setUTCHours(n) : i.setHours(n));
      case "Date":
        return void (r ? i.setUTCDate(n) : i.setDate(n));
      case "FullYear":
        break;
      default:
        return;
    }
    o = n, s = t.month(), a = t.date(), a = a === 29 && s === 1 && !wp(o) ? 28 : a, r ? i.setUTCFullYear(o, s, a) : i.setFullYear(o, s, a);
  }
}
function I$(t) {
  return t = qi(t), po(this[t]) ? this[t]() : this;
}
function O$(t, e) {
  if (typeof t == "object") {
    t = xS(t);
    var n = C$(t), i, r = n.length;
    for (i = 0; i < r; i++)
      this[n[i].unit](t[n[i].unit]);
  } else if (t = qi(t), po(this[t]))
    return this[t](e);
  return this;
}
function P$(t, e) {
  return (t % e + e) % e;
}
var tn;
Array.prototype.indexOf ? tn = Array.prototype.indexOf : tn = function(t) {
  var e;
  for (e = 0; e < this.length; ++e)
    if (this[e] === t)
      return e;
  return -1;
};
function DS(t, e) {
  if (isNaN(t) || isNaN(e))
    return NaN;
  var n = P$(e, 12);
  return t += (e - n) / 12, n === 1 ? wp(t) ? 29 : 28 : 31 - n % 7 % 2;
}
be("M", ["MM", 2], "Mo", function() {
  return this.month() + 1;
});
be("MMM", 0, 0, function(t) {
  return this.localeData().monthsShort(this, t);
});
be("MMMM", 0, 0, function(t) {
  return this.localeData().months(this, t);
});
fe("M", Nt, _u);
fe("MM", Nt, Ti);
fe("MMM", function(t, e) {
  return e.monthsShortRegex(t);
});
fe("MMMM", function(t, e) {
  return e.monthsRegex(t);
});
Rt(["M", "MM"], function(t, e) {
  e[$o] = Je(t) - 1;
});
Rt(["MMM", "MMMM"], function(t, e, n, i) {
  var r = n._locale.monthsParse(t, i, n._strict);
  r != null ? e[$o] = r : We(n).invalidMonth = t;
});
var x$ = "January_February_March_April_May_June_July_August_September_October_November_December".split(
  "_"
), YO = "Jan_Feb_Mar_Apr_May_Jun_Jul_Aug_Sep_Oct_Nov_Dec".split("_"), jO = /D[oD]?(\[[^\[\]]*\]|\s)+MMMM?/, H$ = wd, _$ = wd;
function N$(t, e) {
  return t ? yr(this._months) ? this._months[t.month()] : this._months[(this._months.isFormat || jO).test(e) ? "format" : "standalone"][t.month()] : yr(this._months) ? this._months : this._months.standalone;
}
function D$(t, e) {
  return t ? yr(this._monthsShort) ? this._monthsShort[t.month()] : this._monthsShort[jO.test(e) ? "format" : "standalone"][t.month()] : yr(this._monthsShort) ? this._monthsShort : this._monthsShort.standalone;
}
function k$(t, e, n) {
  var i, r, o, s = t.toLocaleLowerCase();
  if (!this._monthsParse)
    for (this._monthsParse = [], this._longMonthsParse = [], this._shortMonthsParse = [], i = 0; i < 12; ++i)
      o = mo([2e3, i]), this._shortMonthsParse[i] = this.monthsShort(
        o,
        ""
      ).toLocaleLowerCase(), this._longMonthsParse[i] = this.months(o, "").toLocaleLowerCase();
  return n ? e === "MMM" ? (r = tn.call(this._shortMonthsParse, s), r !== -1 ? r : null) : (r = tn.call(this._longMonthsParse, s), r !== -1 ? r : null) : e === "MMM" ? (r = tn.call(this._shortMonthsParse, s), r !== -1 ? r : (r = tn.call(this._longMonthsParse, s), r !== -1 ? r : null)) : (r = tn.call(this._longMonthsParse, s), r !== -1 ? r : (r = tn.call(this._shortMonthsParse, s), r !== -1 ? r : null));
}
function L$(t, e, n) {
  var i, r, o;
  if (this._monthsParseExact)
    return k$.call(this, t, e, n);
  for (this._monthsParse || (this._monthsParse = [], this._longMonthsParse = [], this._shortMonthsParse = []), i = 0; i < 12; i++) {
    if (r = mo([2e3, i]), n && !this._longMonthsParse[i] && (this._longMonthsParse[i] = new RegExp(
      "^" + this.months(r, "").replace(".", "") + "$",
      "i"
    ), this._shortMonthsParse[i] = new RegExp(
      "^" + this.monthsShort(r, "").replace(".", "") + "$",
      "i"
    )), !n && !this._monthsParse[i] && (o = "^" + this.months(r, "") + "|^" + this.monthsShort(r, ""), this._monthsParse[i] = new RegExp(o.replace(".", ""), "i")), n && e === "MMMM" && this._longMonthsParse[i].test(t))
      return i;
    if (n && e === "MMM" && this._shortMonthsParse[i].test(t))
      return i;
    if (!n && this._monthsParse[i].test(t))
      return i;
  }
}
function GO(t, e) {
  if (!t.isValid())
    return t;
  if (typeof e == "string") {
    if (/^\d+$/.test(e))
      e = Je(e);
    else if (e = t.localeData().monthsParse(e), !qo(e))
      return t;
  }
  var n = e, i = t.date();
  return i = i < 29 ? i : Math.min(i, DS(t.year(), n)), t._isUTC ? t._d.setUTCMonth(n, i) : t._d.setMonth(n, i), t;
}
function zO(t) {
  return t != null ? (GO(this, t), X.updateOffset(this, !0), this) : xh(this, "Month");
}
function B$() {
  return DS(this.year(), this.month());
}
function F$(t) {
  return this._monthsParseExact ? (lt(this, "_monthsRegex") || KO.call(this), t ? this._monthsShortStrictRegex : this._monthsShortRegex) : (lt(this, "_monthsShortRegex") || (this._monthsShortRegex = H$), this._monthsShortStrictRegex && t ? this._monthsShortStrictRegex : this._monthsShortRegex);
}
function V$(t) {
  return this._monthsParseExact ? (lt(this, "_monthsRegex") || KO.call(this), t ? this._monthsStrictRegex : this._monthsRegex) : (lt(this, "_monthsRegex") || (this._monthsRegex = _$), this._monthsStrictRegex && t ? this._monthsStrictRegex : this._monthsRegex);
}
function KO() {
  function t(l, u) {
    return u.length - l.length;
  }
  var e = [], n = [], i = [], r, o, s, a;
  for (r = 0; r < 12; r++)
    o = mo([2e3, r]), s = Qo(this.monthsShort(o, "")), a = Qo(this.months(o, "")), e.push(s), n.push(a), i.push(a), i.push(s);
  e.sort(t), n.sort(t), i.sort(t), this._monthsRegex = new RegExp("^(" + i.join("|") + ")", "i"), this._monthsShortRegex = this._monthsRegex, this._monthsStrictRegex = new RegExp(
    "^(" + n.join("|") + ")",
    "i"
  ), this._monthsShortStrictRegex = new RegExp(
    "^(" + e.join("|") + ")",
    "i"
  );
}
function W$(t, e, n, i, r, o, s) {
  var a;
  return t < 100 && t >= 0 ? (a = new Date(t + 400, e, n, i, r, o, s), isFinite(a.getFullYear()) && a.setFullYear(t)) : a = new Date(t, e, n, i, r, o, s), a;
}
function Hh(t) {
  var e, n;
  return t < 100 && t >= 0 ? (n = Array.prototype.slice.call(arguments), n[0] = t + 400, e = new Date(Date.UTC.apply(null, n)), isFinite(e.getUTCFullYear()) && e.setUTCFullYear(t)) : e = new Date(Date.UTC.apply(null, arguments)), e;
}
function dm(t, e, n) {
  var i = 7 + e - n, r = (7 + Hh(t, 0, i).getUTCDay() - e) % 7;
  return -r + i - 1;
}
function QO(t, e, n, i, r) {
  var o = (7 + n - i) % 7, s = dm(t, i, r), a = 1 + 7 * (e - 1) + o + s, l, u;
  return a <= 0 ? (l = t - 1, u = ih(l) + a) : a > ih(t) ? (l = t + 1, u = a - ih(t)) : (l = t, u = a), {
    year: l,
    dayOfYear: u
  };
}
function _h(t, e, n) {
  var i = dm(t.year(), e, n), r = Math.floor((t.dayOfYear() - i - 1) / 7) + 1, o, s;
  return r < 1 ? (s = t.year() - 1, o = r + Xo(s, e, n)) : r > Xo(t.year(), e, n) ? (o = r - Xo(t.year(), e, n), s = t.year() + 1) : (s = t.year(), o = r), {
    week: o,
    year: s
  };
}
function Xo(t, e, n) {
  var i = dm(t, e, n), r = dm(t + 1, e, n);
  return (ih(t) - i + r) / 7;
}
be("w", ["ww", 2], "wo", "week");
be("W", ["WW", 2], "Wo", "isoWeek");
fe("w", Nt, _u);
fe("ww", Nt, Ti);
fe("W", Nt, _u);
fe("WW", Nt, Ti);
Ad(
  ["w", "ww", "W", "WW"],
  function(t, e, n, i) {
    e[i.substr(0, 1)] = Je(t);
  }
);
function $$(t) {
  return _h(t, this._week.dow, this._week.doy).week;
}
var U$ = {
  dow: 0,
  // Sunday is the first day of the week.
  doy: 6
  // The week that contains Jan 6th is the first week of the year.
};
function Y$() {
  return this._week.dow;
}
function j$() {
  return this._week.doy;
}
function G$(t) {
  var e = this.localeData().week(this);
  return t == null ? e : this.add((t - e) * 7, "d");
}
function z$(t) {
  var e = _h(this, 1, 4).week;
  return t == null ? e : this.add((t - e) * 7, "d");
}
be("d", 0, "do", "day");
be("dd", 0, 0, function(t) {
  return this.localeData().weekdaysMin(this, t);
});
be("ddd", 0, 0, function(t) {
  return this.localeData().weekdaysShort(this, t);
});
be("dddd", 0, 0, function(t) {
  return this.localeData().weekdays(this, t);
});
be("e", 0, 0, "weekday");
be("E", 0, 0, "isoWeekday");
fe("d", Nt);
fe("e", Nt);
fe("E", Nt);
fe("dd", function(t, e) {
  return e.weekdaysMinRegex(t);
});
fe("ddd", function(t, e) {
  return e.weekdaysShortRegex(t);
});
fe("dddd", function(t, e) {
  return e.weekdaysRegex(t);
});
Ad(["dd", "ddd", "dddd"], function(t, e, n, i) {
  var r = n._locale.weekdaysParse(t, i, n._strict);
  r != null ? e.d = r : We(n).invalidWeekday = t;
});
Ad(["d", "e", "E"], function(t, e, n, i) {
  e[i] = Je(t);
});
function K$(t, e) {
  return typeof t != "string" ? t : isNaN(t) ? (t = e.weekdaysParse(t), typeof t == "number" ? t : null) : parseInt(t, 10);
}
function Q$(t, e) {
  return typeof t == "string" ? e.weekdaysParse(t) % 7 || 7 : isNaN(t) ? null : t;
}
function kS(t, e) {
  return t.slice(e, 7).concat(t.slice(0, e));
}
var X$ = "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_"), XO = "Sun_Mon_Tue_Wed_Thu_Fri_Sat".split("_"), q$ = "Su_Mo_Tu_We_Th_Fr_Sa".split("_"), Z$ = wd, J$ = wd, eU = wd;
function tU(t, e) {
  var n = yr(this._weekdays) ? this._weekdays : this._weekdays[t && t !== !0 && this._weekdays.isFormat.test(e) ? "format" : "standalone"];
  return t === !0 ? kS(n, this._week.dow) : t ? n[t.day()] : n;
}
function nU(t) {
  return t === !0 ? kS(this._weekdaysShort, this._week.dow) : t ? this._weekdaysShort[t.day()] : this._weekdaysShort;
}
function iU(t) {
  return t === !0 ? kS(this._weekdaysMin, this._week.dow) : t ? this._weekdaysMin[t.day()] : this._weekdaysMin;
}
function rU(t, e, n) {
  var i, r, o, s = t.toLocaleLowerCase();
  if (!this._weekdaysParse)
    for (this._weekdaysParse = [], this._shortWeekdaysParse = [], this._minWeekdaysParse = [], i = 0; i < 7; ++i)
      o = mo([2e3, 1]).day(i), this._minWeekdaysParse[i] = this.weekdaysMin(
        o,
        ""
      ).toLocaleLowerCase(), this._shortWeekdaysParse[i] = this.weekdaysShort(
        o,
        ""
      ).toLocaleLowerCase(), this._weekdaysParse[i] = this.weekdays(o, "").toLocaleLowerCase();
  return n ? e === "dddd" ? (r = tn.call(this._weekdaysParse, s), r !== -1 ? r : null) : e === "ddd" ? (r = tn.call(this._shortWeekdaysParse, s), r !== -1 ? r : null) : (r = tn.call(this._minWeekdaysParse, s), r !== -1 ? r : null) : e === "dddd" ? (r = tn.call(this._weekdaysParse, s), r !== -1 || (r = tn.call(this._shortWeekdaysParse, s), r !== -1) ? r : (r = tn.call(this._minWeekdaysParse, s), r !== -1 ? r : null)) : e === "ddd" ? (r = tn.call(this._shortWeekdaysParse, s), r !== -1 || (r = tn.call(this._weekdaysParse, s), r !== -1) ? r : (r = tn.call(this._minWeekdaysParse, s), r !== -1 ? r : null)) : (r = tn.call(this._minWeekdaysParse, s), r !== -1 || (r = tn.call(this._weekdaysParse, s), r !== -1) ? r : (r = tn.call(this._shortWeekdaysParse, s), r !== -1 ? r : null));
}
function oU(t, e, n) {
  var i, r, o;
  if (this._weekdaysParseExact)
    return rU.call(this, t, e, n);
  for (this._weekdaysParse || (this._weekdaysParse = [], this._minWeekdaysParse = [], this._shortWeekdaysParse = [], this._fullWeekdaysParse = []), i = 0; i < 7; i++) {
    if (r = mo([2e3, 1]).day(i), n && !this._fullWeekdaysParse[i] && (this._fullWeekdaysParse[i] = new RegExp(
      "^" + this.weekdays(r, "").replace(".", "\\.?") + "$",
      "i"
    ), this._shortWeekdaysParse[i] = new RegExp(
      "^" + this.weekdaysShort(r, "").replace(".", "\\.?") + "$",
      "i"
    ), this._minWeekdaysParse[i] = new RegExp(
      "^" + this.weekdaysMin(r, "").replace(".", "\\.?") + "$",
      "i"
    )), this._weekdaysParse[i] || (o = "^" + this.weekdays(r, "") + "|^" + this.weekdaysShort(r, "") + "|^" + this.weekdaysMin(r, ""), this._weekdaysParse[i] = new RegExp(o.replace(".", ""), "i")), n && e === "dddd" && this._fullWeekdaysParse[i].test(t))
      return i;
    if (n && e === "ddd" && this._shortWeekdaysParse[i].test(t))
      return i;
    if (n && e === "dd" && this._minWeekdaysParse[i].test(t))
      return i;
    if (!n && this._weekdaysParse[i].test(t))
      return i;
  }
}
function sU(t) {
  if (!this.isValid())
    return t != null ? this : NaN;
  var e = xh(this, "Day");
  return t != null ? (t = K$(t, this.localeData()), this.add(t - e, "d")) : e;
}
function aU(t) {
  if (!this.isValid())
    return t != null ? this : NaN;
  var e = (this.day() + 7 - this.localeData()._week.dow) % 7;
  return t == null ? e : this.add(t - e, "d");
}
function lU(t) {
  if (!this.isValid())
    return t != null ? this : NaN;
  if (t != null) {
    var e = Q$(t, this.localeData());
    return this.day(this.day() % 7 ? e : e - 7);
  } else
    return this.day() || 7;
}
function uU(t) {
  return this._weekdaysParseExact ? (lt(this, "_weekdaysRegex") || LS.call(this), t ? this._weekdaysStrictRegex : this._weekdaysRegex) : (lt(this, "_weekdaysRegex") || (this._weekdaysRegex = Z$), this._weekdaysStrictRegex && t ? this._weekdaysStrictRegex : this._weekdaysRegex);
}
function cU(t) {
  return this._weekdaysParseExact ? (lt(this, "_weekdaysRegex") || LS.call(this), t ? this._weekdaysShortStrictRegex : this._weekdaysShortRegex) : (lt(this, "_weekdaysShortRegex") || (this._weekdaysShortRegex = J$), this._weekdaysShortStrictRegex && t ? this._weekdaysShortStrictRegex : this._weekdaysShortRegex);
}
function hU(t) {
  return this._weekdaysParseExact ? (lt(this, "_weekdaysRegex") || LS.call(this), t ? this._weekdaysMinStrictRegex : this._weekdaysMinRegex) : (lt(this, "_weekdaysMinRegex") || (this._weekdaysMinRegex = eU), this._weekdaysMinStrictRegex && t ? this._weekdaysMinStrictRegex : this._weekdaysMinRegex);
}
function LS() {
  function t(c, h) {
    return h.length - c.length;
  }
  var e = [], n = [], i = [], r = [], o, s, a, l, u;
  for (o = 0; o < 7; o++)
    s = mo([2e3, 1]).day(o), a = Qo(this.weekdaysMin(s, "")), l = Qo(this.weekdaysShort(s, "")), u = Qo(this.weekdays(s, "")), e.push(a), n.push(l), i.push(u), r.push(a), r.push(l), r.push(u);
  e.sort(t), n.sort(t), i.sort(t), r.sort(t), this._weekdaysRegex = new RegExp("^(" + r.join("|") + ")", "i"), this._weekdaysShortRegex = this._weekdaysRegex, this._weekdaysMinRegex = this._weekdaysRegex, this._weekdaysStrictRegex = new RegExp(
    "^(" + i.join("|") + ")",
    "i"
  ), this._weekdaysShortStrictRegex = new RegExp(
    "^(" + n.join("|") + ")",
    "i"
  ), this._weekdaysMinStrictRegex = new RegExp(
    "^(" + e.join("|") + ")",
    "i"
  );
}
function BS() {
  return this.hours() % 12 || 12;
}
function dU() {
  return this.hours() || 24;
}
be("H", ["HH", 2], 0, "hour");
be("h", ["hh", 2], 0, BS);
be("k", ["kk", 2], 0, dU);
be("hmm", 0, 0, function() {
  return "" + BS.apply(this) + lo(this.minutes(), 2);
});
be("hmmss", 0, 0, function() {
  return "" + BS.apply(this) + lo(this.minutes(), 2) + lo(this.seconds(), 2);
});
be("Hmm", 0, 0, function() {
  return "" + this.hours() + lo(this.minutes(), 2);
});
be("Hmmss", 0, 0, function() {
  return "" + this.hours() + lo(this.minutes(), 2) + lo(this.seconds(), 2);
});
function qO(t, e) {
  be(t, 0, 0, function() {
    return this.localeData().meridiem(
      this.hours(),
      this.minutes(),
      e
    );
  });
}
qO("a", !0);
qO("A", !1);
function ZO(t, e) {
  return e._meridiemParse;
}
fe("a", ZO);
fe("A", ZO);
fe("H", Nt, NS);
fe("h", Nt, _u);
fe("k", Nt, _u);
fe("HH", Nt, Ti);
fe("hh", Nt, Ti);
fe("kk", Nt, Ti);
fe("hmm", VO);
fe("hmmss", WO);
fe("Hmm", VO);
fe("Hmmss", WO);
Rt(["H", "HH"], mn);
Rt(["k", "kk"], function(t, e, n) {
  var i = Je(t);
  e[mn] = i === 24 ? 0 : i;
});
Rt(["a", "A"], function(t, e, n) {
  n._isPm = n._locale.isPM(t), n._meridiem = t;
});
Rt(["h", "hh"], function(t, e, n) {
  e[mn] = Je(t), We(n).bigHour = !0;
});
Rt("hmm", function(t, e, n) {
  var i = t.length - 2;
  e[mn] = Je(t.substr(0, i)), e[Ar] = Je(t.substr(i)), We(n).bigHour = !0;
});
Rt("hmmss", function(t, e, n) {
  var i = t.length - 4, r = t.length - 2;
  e[mn] = Je(t.substr(0, i)), e[Ar] = Je(t.substr(i, 2)), e[Uo] = Je(t.substr(r)), We(n).bigHour = !0;
});
Rt("Hmm", function(t, e, n) {
  var i = t.length - 2;
  e[mn] = Je(t.substr(0, i)), e[Ar] = Je(t.substr(i));
});
Rt("Hmmss", function(t, e, n) {
  var i = t.length - 4, r = t.length - 2;
  e[mn] = Je(t.substr(0, i)), e[Ar] = Je(t.substr(i, 2)), e[Uo] = Je(t.substr(r));
});
function fU(t) {
  return (t + "").toLowerCase().charAt(0) === "p";
}
var gU = /[ap]\.?m?\.?/i, mU = Nu("Hours", !0);
function pU(t, e, n) {
  return t > 11 ? n ? "pm" : "PM" : n ? "am" : "AM";
}
var JO = {
  calendar: r$,
  longDateFormat: l$,
  invalidDate: c$,
  ordinal: d$,
  dayOfMonthOrdinalParse: f$,
  relativeTime: m$,
  months: x$,
  monthsShort: YO,
  week: U$,
  weekdays: X$,
  weekdaysMin: q$,
  weekdaysShort: XO,
  meridiemParse: gU
}, Lt = {}, Ku = {}, Nh;
function wU(t, e) {
  var n, i = Math.min(t.length, e.length);
  for (n = 0; n < i; n += 1)
    if (t[n] !== e[n])
      return n;
  return i;
}
function dE(t) {
  return t && t.toLowerCase().replace("_", "-");
}
function AU(t) {
  for (var e = 0, n, i, r, o; e < t.length; ) {
    for (o = dE(t[e]).split("-"), n = o.length, i = dE(t[e + 1]), i = i ? i.split("-") : null; n > 0; ) {
      if (r = Ap(o.slice(0, n).join("-")), r)
        return r;
      if (i && i.length >= n && wU(o, i) >= n - 1)
        break;
      n--;
    }
    e++;
  }
  return Nh;
}
function CU(t) {
  return !!(t && t.match("^[^/\\\\]*$"));
}
function Ap(t) {
  var e = null, n;
  if (Lt[t] === void 0 && typeof module != "undefined" && module && module.exports && CU(t))
    try {
      e = Nh._abbr, n = require, n("./locale/" + t), Fs(e);
    } catch (i) {
      Lt[t] = null;
    }
  return Lt[t];
}
function Fs(t, e) {
  var n;
  return t && (si(e) ? n = ns(t) : n = FS(t, e), n ? Nh = n : typeof console != "undefined" && console.warn && console.warn(
    "Locale " + t + " not found. Did you forget to load it?"
  )), Nh._abbr;
}
function FS(t, e) {
  if (e !== null) {
    var n, i = JO;
    if (e.abbr = t, Lt[t] != null)
      kO(
        "defineLocaleOverride",
        "use moment.updateLocale(localeName, config) to change an existing locale. moment.defineLocale(localeName, config) should only be used for creating a new locale See http://momentjs.com/guides/#/warnings/define-locale/ for more info."
      ), i = Lt[t]._config;
    else if (e.parentLocale != null)
      if (Lt[e.parentLocale] != null)
        i = Lt[e.parentLocale]._config;
      else if (n = Ap(e.parentLocale), n != null)
        i = n._config;
      else
        return Ku[e.parentLocale] || (Ku[e.parentLocale] = []), Ku[e.parentLocale].push({
          name: t,
          config: e
        }), null;
    return Lt[t] = new OS(oy(i, e)), Ku[t] && Ku[t].forEach(function(r) {
      FS(r.name, r.config);
    }), Fs(t), Lt[t];
  } else
    return delete Lt[t], null;
}
function vU(t, e) {
  if (e != null) {
    var n, i, r = JO;
    Lt[t] != null && Lt[t].parentLocale != null ? Lt[t].set(oy(Lt[t]._config, e)) : (i = Ap(t), i != null && (r = i._config), e = oy(r, e), i == null && (e.abbr = t), n = new OS(e), n.parentLocale = Lt[t], Lt[t] = n), Fs(t);
  } else
    Lt[t] != null && (Lt[t].parentLocale != null ? (Lt[t] = Lt[t].parentLocale, t === Fs() && Fs(t)) : Lt[t] != null && delete Lt[t]);
  return Lt[t];
}
function ns(t) {
  var e;
  if (t && t._locale && t._locale._abbr && (t = t._locale._abbr), !t)
    return Nh;
  if (!yr(t)) {
    if (e = Ap(t), e)
      return e;
    t = [t];
  }
  return AU(t);
}
function yU() {
  return sy(Lt);
}
function VS(t) {
  var e, n = t._a;
  return n && We(t).overflow === -2 && (e = n[$o] < 0 || n[$o] > 11 ? $o : n[to] < 1 || n[to] > DS(n[Ln], n[$o]) ? to : n[mn] < 0 || n[mn] > 24 || n[mn] === 24 && (n[Ar] !== 0 || n[Uo] !== 0 || n[Ua] !== 0) ? mn : n[Ar] < 0 || n[Ar] > 59 ? Ar : n[Uo] < 0 || n[Uo] > 59 ? Uo : n[Ua] < 0 || n[Ua] > 999 ? Ua : -1, We(t)._overflowDayOfYear && (e < Ln || e > to) && (e = to), We(t)._overflowWeeks && e === -1 && (e = E$), We(t)._overflowWeekday && e === -1 && (e = T$), We(t).overflow = e), t;
}
var SU = /^\s*((?:[+-]\d{6}|\d{4})-(?:\d\d-\d\d|W\d\d-\d|W\d\d|\d\d\d|\d\d))(?:(T| )(\d\d(?::\d\d(?::\d\d(?:[.,]\d+)?)?)?)([+-]\d\d(?::?\d\d)?|\s*Z)?)?$/, bU = /^\s*((?:[+-]\d{6}|\d{4})(?:\d\d\d\d|W\d\d\d|W\d\d|\d\d\d|\d\d|))(?:(T| )(\d\d(?:\d\d(?:\d\d(?:[.,]\d+)?)?)?)([+-]\d\d(?::?\d\d)?|\s*Z)?)?$/, RU = /Z|[+-]\d\d(?::?\d\d)?/, ef = [
  ["YYYYYY-MM-DD", /[+-]\d{6}-\d\d-\d\d/],
  ["YYYY-MM-DD", /\d{4}-\d\d-\d\d/],
  ["GGGG-[W]WW-E", /\d{4}-W\d\d-\d/],
  ["GGGG-[W]WW", /\d{4}-W\d\d/, !1],
  ["YYYY-DDD", /\d{4}-\d{3}/],
  ["YYYY-MM", /\d{4}-\d\d/, !1],
  ["YYYYYYMMDD", /[+-]\d{10}/],
  ["YYYYMMDD", /\d{8}/],
  ["GGGG[W]WWE", /\d{4}W\d{3}/],
  ["GGGG[W]WW", /\d{4}W\d{2}/, !1],
  ["YYYYDDD", /\d{7}/],
  ["YYYYMM", /\d{6}/, !1],
  ["YYYY", /\d{4}/, !1]
], GA = [
  ["HH:mm:ss.SSSS", /\d\d:\d\d:\d\d\.\d+/],
  ["HH:mm:ss,SSSS", /\d\d:\d\d:\d\d,\d+/],
  ["HH:mm:ss", /\d\d:\d\d:\d\d/],
  ["HH:mm", /\d\d:\d\d/],
  ["HHmmss.SSSS", /\d\d\d\d\d\d\.\d+/],
  ["HHmmss,SSSS", /\d\d\d\d\d\d,\d+/],
  ["HHmmss", /\d\d\d\d\d\d/],
  ["HHmm", /\d\d\d\d/],
  ["HH", /\d\d/]
], EU = /^\/?Date\((-?\d+)/i, TU = /^(?:(Mon|Tue|Wed|Thu|Fri|Sat|Sun),?\s)?(\d{1,2})\s(Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)\s(\d{2,4})\s(\d\d):(\d\d)(?::(\d\d))?\s(?:(UT|GMT|[ECMP][SD]T)|([Zz])|([+-]\d{4}))$/, MU = {
  UT: 0,
  GMT: 0,
  EDT: -4 * 60,
  EST: -5 * 60,
  CDT: -5 * 60,
  CST: -6 * 60,
  MDT: -6 * 60,
  MST: -7 * 60,
  PDT: -7 * 60,
  PST: -8 * 60
};
function eP(t) {
  var e, n, i = t._i, r = SU.exec(i) || bU.exec(i), o, s, a, l, u = ef.length, c = GA.length;
  if (r) {
    for (We(t).iso = !0, e = 0, n = u; e < n; e++)
      if (ef[e][1].exec(r[1])) {
        s = ef[e][0], o = ef[e][2] !== !1;
        break;
      }
    if (s == null) {
      t._isValid = !1;
      return;
    }
    if (r[3]) {
      for (e = 0, n = c; e < n; e++)
        if (GA[e][1].exec(r[3])) {
          a = (r[2] || " ") + GA[e][0];
          break;
        }
      if (a == null) {
        t._isValid = !1;
        return;
      }
    }
    if (!o && a != null) {
      t._isValid = !1;
      return;
    }
    if (r[4])
      if (RU.exec(r[4]))
        l = "Z";
      else {
        t._isValid = !1;
        return;
      }
    t._f = s + (a || "") + (l || ""), $S(t);
  } else
    t._isValid = !1;
}
function IU(t, e, n, i, r, o) {
  var s = [
    OU(t),
    YO.indexOf(e),
    parseInt(n, 10),
    parseInt(i, 10),
    parseInt(r, 10)
  ];
  return o && s.push(parseInt(o, 10)), s;
}
function OU(t) {
  var e = parseInt(t, 10);
  return e <= 49 ? 2e3 + e : e <= 999 ? 1900 + e : e;
}
function PU(t) {
  return t.replace(/\([^()]*\)|[\n\t]/g, " ").replace(/(\s\s+)/g, " ").replace(/^\s\s*/, "").replace(/\s\s*$/, "");
}
function xU(t, e, n) {
  if (t) {
    var i = XO.indexOf(t), r = new Date(
      e[0],
      e[1],
      e[2]
    ).getDay();
    if (i !== r)
      return We(n).weekdayMismatch = !0, n._isValid = !1, !1;
  }
  return !0;
}
function HU(t, e, n) {
  if (t)
    return MU[t];
  if (e)
    return 0;
  var i = parseInt(n, 10), r = i % 100, o = (i - r) / 100;
  return o * 60 + r;
}
function tP(t) {
  var e = TU.exec(PU(t._i)), n;
  if (e) {
    if (n = IU(
      e[4],
      e[3],
      e[2],
      e[5],
      e[6],
      e[7]
    ), !xU(e[1], n, t))
      return;
    t._a = n, t._tzm = HU(e[8], e[9], e[10]), t._d = Hh.apply(null, t._a), t._d.setUTCMinutes(t._d.getUTCMinutes() - t._tzm), We(t).rfc2822 = !0;
  } else
    t._isValid = !1;
}
function _U(t) {
  var e = EU.exec(t._i);
  if (e !== null) {
    t._d = /* @__PURE__ */ new Date(+e[1]);
    return;
  }
  if (eP(t), t._isValid === !1)
    delete t._isValid;
  else
    return;
  if (tP(t), t._isValid === !1)
    delete t._isValid;
  else
    return;
  t._strict ? t._isValid = !1 : X.createFromInputFallback(t);
}
X.createFromInputFallback = Xi(
  "value provided is not in a recognized RFC2822 or ISO format. moment construction falls back to js Date(), which is not reliable across all browsers and versions. Non RFC2822/ISO date formats are discouraged. Please refer to http://momentjs.com/guides/#/warnings/js-date/ for more info.",
  function(t) {
    t._d = /* @__PURE__ */ new Date(t._i + (t._useUTC ? " UTC" : ""));
  }
);
function ru(t, e, n) {
  return t != null ? t : e != null ? e : n;
}
function NU(t) {
  var e = new Date(X.now());
  return t._useUTC ? [
    e.getUTCFullYear(),
    e.getUTCMonth(),
    e.getUTCDate()
  ] : [e.getFullYear(), e.getMonth(), e.getDate()];
}
function WS(t) {
  var e, n, i = [], r, o, s;
  if (!t._d) {
    for (r = NU(t), t._w && t._a[to] == null && t._a[$o] == null && DU(t), t._dayOfYear != null && (s = ru(t._a[Ln], r[Ln]), (t._dayOfYear > ih(s) || t._dayOfYear === 0) && (We(t)._overflowDayOfYear = !0), n = Hh(s, 0, t._dayOfYear), t._a[$o] = n.getUTCMonth(), t._a[to] = n.getUTCDate()), e = 0; e < 3 && t._a[e] == null; ++e)
      t._a[e] = i[e] = r[e];
    for (; e < 7; e++)
      t._a[e] = i[e] = t._a[e] == null ? e === 2 ? 1 : 0 : t._a[e];
    t._a[mn] === 24 && t._a[Ar] === 0 && t._a[Uo] === 0 && t._a[Ua] === 0 && (t._nextDay = !0, t._a[mn] = 0), t._d = (t._useUTC ? Hh : W$).apply(
      null,
      i
    ), o = t._useUTC ? t._d.getUTCDay() : t._d.getDay(), t._tzm != null && t._d.setUTCMinutes(t._d.getUTCMinutes() - t._tzm), t._nextDay && (t._a[mn] = 24), t._w && typeof t._w.d != "undefined" && t._w.d !== o && (We(t).weekdayMismatch = !0);
  }
}
function DU(t) {
  var e, n, i, r, o, s, a, l, u;
  e = t._w, e.GG != null || e.W != null || e.E != null ? (o = 1, s = 4, n = ru(
    e.GG,
    t._a[Ln],
    _h(_t(), 1, 4).year
  ), i = ru(e.W, 1), r = ru(e.E, 1), (r < 1 || r > 7) && (l = !0)) : (o = t._locale._week.dow, s = t._locale._week.doy, u = _h(_t(), o, s), n = ru(e.gg, t._a[Ln], u.year), i = ru(e.w, u.week), e.d != null ? (r = e.d, (r < 0 || r > 6) && (l = !0)) : e.e != null ? (r = e.e + o, (e.e < 0 || e.e > 6) && (l = !0)) : r = o), i < 1 || i > Xo(n, o, s) ? We(t)._overflowWeeks = !0 : l != null ? We(t)._overflowWeekday = !0 : (a = QO(n, i, r, o, s), t._a[Ln] = a.year, t._dayOfYear = a.dayOfYear);
}
X.ISO_8601 = function() {
};
X.RFC_2822 = function() {
};
function $S(t) {
  if (t._f === X.ISO_8601) {
    eP(t);
    return;
  }
  if (t._f === X.RFC_2822) {
    tP(t);
    return;
  }
  t._a = [], We(t).empty = !0;
  var e = "" + t._i, n, i, r, o, s, a = e.length, l = 0, u, c;
  for (r = LO(t._f, t._locale).match(PS) || [], c = r.length, n = 0; n < c; n++)
    o = r[n], i = (e.match(S$(o, t)) || [])[0], i && (s = e.substr(0, e.indexOf(i)), s.length > 0 && We(t).unusedInput.push(s), e = e.slice(
      e.indexOf(i) + i.length
    ), l += i.length), pu[o] ? (i ? We(t).empty = !1 : We(t).unusedTokens.push(o), R$(o, i, t)) : t._strict && !i && We(t).unusedTokens.push(o);
  We(t).charsLeftOver = a - l, e.length > 0 && We(t).unusedInput.push(e), t._a[mn] <= 12 && We(t).bigHour === !0 && t._a[mn] > 0 && (We(t).bigHour = void 0), We(t).parsedDateParts = t._a.slice(0), We(t).meridiem = t._meridiem, t._a[mn] = kU(
    t._locale,
    t._a[mn],
    t._meridiem
  ), u = We(t).era, u !== null && (t._a[Ln] = t._locale.erasConvertYear(u, t._a[Ln])), WS(t), VS(t);
}
function kU(t, e, n) {
  var i;
  return n == null ? e : t.meridiemHour != null ? t.meridiemHour(e, n) : (t.isPM != null && (i = t.isPM(n), i && e < 12 && (e += 12), !i && e === 12 && (e = 0)), e);
}
function LU(t) {
  var e, n, i, r, o, s, a = !1, l = t._f.length;
  if (l === 0) {
    We(t).invalidFormat = !0, t._d = /* @__PURE__ */ new Date(NaN);
    return;
  }
  for (r = 0; r < l; r++)
    o = 0, s = !1, e = IS({}, t), t._useUTC != null && (e._useUTC = t._useUTC), e._f = t._f[r], $S(e), MS(e) && (s = !0), o += We(e).charsLeftOver, o += We(e).unusedTokens.length * 10, We(e).score = o, a ? o < i && (i = o, n = e) : (i == null || o < i || s) && (i = o, n = e, s && (a = !0));
  _s(t, n || e);
}
function BU(t) {
  if (!t._d) {
    var e = xS(t._i), n = e.day === void 0 ? e.date : e.day;
    t._a = NO(
      [e.year, e.month, n, e.hour, e.minute, e.second, e.millisecond],
      function(i) {
        return i && parseInt(i, 10);
      }
    ), WS(t);
  }
}
function FU(t) {
  var e = new pd(VS(nP(t)));
  return e._nextDay && (e.add(1, "d"), e._nextDay = void 0), e;
}
function nP(t) {
  var e = t._i, n = t._f;
  return t._locale = t._locale || ns(t._l), e === null || n === void 0 && e === "" ? hp({ nullInput: !0 }) : (typeof e == "string" && (t._i = e = t._locale.preparse(e)), Sr(e) ? new pd(VS(e)) : (md(e) ? t._d = e : yr(n) ? LU(t) : n ? $S(t) : VU(t), MS(t) || (t._d = null), t));
}
function VU(t) {
  var e = t._i;
  si(e) ? t._d = new Date(X.now()) : md(e) ? t._d = new Date(e.valueOf()) : typeof e == "string" ? _U(t) : yr(e) ? (t._a = NO(e.slice(0), function(n) {
    return parseInt(n, 10);
  }), WS(t)) : Za(e) ? BU(t) : qo(e) ? t._d = new Date(e) : X.createFromInputFallback(t);
}
function iP(t, e, n, i, r) {
  var o = {};
  return (e === !0 || e === !1) && (i = e, e = void 0), (n === !0 || n === !1) && (i = n, n = void 0), (Za(t) && TS(t) || yr(t) && t.length === 0) && (t = void 0), o._isAMomentObject = !0, o._useUTC = o._isUTC = r, o._l = n, o._i = t, o._f = e, o._strict = i, FU(o);
}
function _t(t, e, n, i) {
  return iP(t, e, n, i, !1);
}
var WU = Xi(
  "moment().min is deprecated, use moment.max instead. http://momentjs.com/guides/#/warnings/min-max/",
  function() {
    var t = _t.apply(null, arguments);
    return this.isValid() && t.isValid() ? t < this ? this : t : hp();
  }
), $U = Xi(
  "moment().max is deprecated, use moment.min instead. http://momentjs.com/guides/#/warnings/min-max/",
  function() {
    var t = _t.apply(null, arguments);
    return this.isValid() && t.isValid() ? t > this ? this : t : hp();
  }
);
function rP(t, e) {
  var n, i;
  if (e.length === 1 && yr(e[0]) && (e = e[0]), !e.length)
    return _t();
  for (n = e[0], i = 1; i < e.length; ++i)
    (!e[i].isValid() || e[i][t](n)) && (n = e[i]);
  return n;
}
function UU() {
  var t = [].slice.call(arguments, 0);
  return rP("isBefore", t);
}
function YU() {
  var t = [].slice.call(arguments, 0);
  return rP("isAfter", t);
}
var jU = function() {
  return Date.now ? Date.now() : +/* @__PURE__ */ new Date();
}, Qu = [
  "year",
  "quarter",
  "month",
  "week",
  "day",
  "hour",
  "minute",
  "second",
  "millisecond"
];
function GU(t) {
  var e, n = !1, i, r = Qu.length;
  for (e in t)
    if (lt(t, e) && !(tn.call(Qu, e) !== -1 && (t[e] == null || !isNaN(t[e]))))
      return !1;
  for (i = 0; i < r; ++i)
    if (t[Qu[i]]) {
      if (n)
        return !1;
      parseFloat(t[Qu[i]]) !== Je(t[Qu[i]]) && (n = !0);
    }
  return !0;
}
function zU() {
  return this._isValid;
}
function KU() {
  return Mr(NaN);
}
function Cp(t) {
  var e = xS(t), n = e.year || 0, i = e.quarter || 0, r = e.month || 0, o = e.week || e.isoWeek || 0, s = e.day || 0, a = e.hour || 0, l = e.minute || 0, u = e.second || 0, c = e.millisecond || 0;
  this._isValid = GU(e), this._milliseconds = +c + u * 1e3 + // 1000
  l * 6e4 + // 1000 * 60
  a * 1e3 * 60 * 60, this._days = +s + o * 7, this._months = +r + i * 3 + n * 12, this._data = {}, this._locale = ns(), this._bubble();
}
function Bg(t) {
  return t instanceof Cp;
}
function ly(t) {
  return t < 0 ? Math.round(-1 * t) * -1 : Math.round(t);
}
function QU(t, e, n) {
  var i = Math.min(t.length, e.length), r = Math.abs(t.length - e.length), o = 0, s;
  for (s = 0; s < i; s++)
    (n && t[s] !== e[s] || !n && Je(t[s]) !== Je(e[s])) && o++;
  return o + r;
}
function oP(t, e) {
  be(t, 0, 0, function() {
    var n = this.utcOffset(), i = "+";
    return n < 0 && (n = -n, i = "-"), i + lo(~~(n / 60), 2) + e + lo(~~n % 60, 2);
  });
}
oP("Z", ":");
oP("ZZ", "");
fe("Z", pp);
fe("ZZ", pp);
Rt(["Z", "ZZ"], function(t, e, n) {
  n._useUTC = !0, n._tzm = US(pp, t);
});
var XU = /([\+\-]|\d\d)/gi;
function US(t, e) {
  var n = (e || "").match(t), i, r, o;
  return n === null ? null : (i = n[n.length - 1] || [], r = (i + "").match(XU) || ["-", 0, 0], o = +(r[1] * 60) + Je(r[2]), o === 0 ? 0 : r[0] === "+" ? o : -o);
}
function YS(t, e) {
  var n, i;
  return e._isUTC ? (n = e.clone(), i = (Sr(t) || md(t) ? t.valueOf() : _t(t).valueOf()) - n.valueOf(), n._d.setTime(n._d.valueOf() + i), X.updateOffset(n, !1), n) : _t(t).local();
}
function uy(t) {
  return -Math.round(t._d.getTimezoneOffset());
}
X.updateOffset = function() {
};
function qU(t, e, n) {
  var i = this._offset || 0, r;
  if (!this.isValid())
    return t != null ? this : NaN;
  if (t != null) {
    if (typeof t == "string") {
      if (t = US(pp, t), t === null)
        return this;
    } else
      Math.abs(t) < 16 && !n && (t = t * 60);
    return !this._isUTC && e && (r = uy(this)), this._offset = t, this._isUTC = !0, r != null && this.add(r, "m"), i !== t && (!e || this._changeInProgress ? lP(
      this,
      Mr(t - i, "m"),
      1,
      !1
    ) : this._changeInProgress || (this._changeInProgress = !0, X.updateOffset(this, !0), this._changeInProgress = null)), this;
  } else
    return this._isUTC ? i : uy(this);
}
function ZU(t, e) {
  return t != null ? (typeof t != "string" && (t = -t), this.utcOffset(t, e), this) : -this.utcOffset();
}
function JU(t) {
  return this.utcOffset(0, t);
}
function eY(t) {
  return this._isUTC && (this.utcOffset(0, t), this._isUTC = !1, t && this.subtract(uy(this), "m")), this;
}
function tY() {
  if (this._tzm != null)
    this.utcOffset(this._tzm, !1, !0);
  else if (typeof this._i == "string") {
    var t = US(v$, this._i);
    t != null ? this.utcOffset(t) : this.utcOffset(0, !0);
  }
  return this;
}
function nY(t) {
  return this.isValid() ? (t = t ? _t(t).utcOffset() : 0, (this.utcOffset() - t) % 60 === 0) : !1;
}
function iY() {
  return this.utcOffset() > this.clone().month(0).utcOffset() || this.utcOffset() > this.clone().month(5).utcOffset();
}
function rY() {
  if (!si(this._isDSTShifted))
    return this._isDSTShifted;
  var t = {}, e;
  return IS(t, this), t = nP(t), t._a ? (e = t._isUTC ? mo(t._a) : _t(t._a), this._isDSTShifted = this.isValid() && QU(t._a, e.toArray()) > 0) : this._isDSTShifted = !1, this._isDSTShifted;
}
function oY() {
  return this.isValid() ? !this._isUTC : !1;
}
function sY() {
  return this.isValid() ? this._isUTC : !1;
}
function sP() {
  return this.isValid() ? this._isUTC && this._offset === 0 : !1;
}
var aY = /^(-|\+)?(?:(\d*)[. ])?(\d+):(\d+)(?::(\d+)(\.\d*)?)?$/, lY = /^(-|\+)?P(?:([-+]?[0-9,.]*)Y)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)W)?(?:([-+]?[0-9,.]*)D)?(?:T(?:([-+]?[0-9,.]*)H)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)S)?)?$/;
function Mr(t, e) {
  var n = t, i = null, r, o, s;
  return Bg(t) ? n = {
    ms: t._milliseconds,
    d: t._days,
    M: t._months
  } : qo(t) || !isNaN(+t) ? (n = {}, e ? n[e] = +t : n.milliseconds = +t) : (i = aY.exec(t)) ? (r = i[1] === "-" ? -1 : 1, n = {
    y: 0,
    d: Je(i[to]) * r,
    h: Je(i[mn]) * r,
    m: Je(i[Ar]) * r,
    s: Je(i[Uo]) * r,
    ms: Je(ly(i[Ua] * 1e3)) * r
    // the millisecond decimal point is included in the match
  }) : (i = lY.exec(t)) ? (r = i[1] === "-" ? -1 : 1, n = {
    y: ia(i[2], r),
    M: ia(i[3], r),
    w: ia(i[4], r),
    d: ia(i[5], r),
    h: ia(i[6], r),
    m: ia(i[7], r),
    s: ia(i[8], r)
  }) : n == null ? n = {} : typeof n == "object" && ("from" in n || "to" in n) && (s = uY(
    _t(n.from),
    _t(n.to)
  ), n = {}, n.ms = s.milliseconds, n.M = s.months), o = new Cp(n), Bg(t) && lt(t, "_locale") && (o._locale = t._locale), Bg(t) && lt(t, "_isValid") && (o._isValid = t._isValid), o;
}
Mr.fn = Cp.prototype;
Mr.invalid = KU;
function ia(t, e) {
  var n = t && parseFloat(t.replace(",", "."));
  return (isNaN(n) ? 0 : n) * e;
}
function fE(t, e) {
  var n = {};
  return n.months = e.month() - t.month() + (e.year() - t.year()) * 12, t.clone().add(n.months, "M").isAfter(e) && --n.months, n.milliseconds = +e - +t.clone().add(n.months, "M"), n;
}
function uY(t, e) {
  var n;
  return t.isValid() && e.isValid() ? (e = YS(e, t), t.isBefore(e) ? n = fE(t, e) : (n = fE(e, t), n.milliseconds = -n.milliseconds, n.months = -n.months), n) : { milliseconds: 0, months: 0 };
}
function aP(t, e) {
  return function(n, i) {
    var r, o;
    return i !== null && !isNaN(+i) && (kO(
      e,
      "moment()." + e + "(period, number) is deprecated. Please use moment()." + e + "(number, period). See http://momentjs.com/guides/#/warnings/add-inverted-param/ for more info."
    ), o = n, n = i, i = o), r = Mr(n, i), lP(this, r, t), this;
  };
}
function lP(t, e, n, i) {
  var r = e._milliseconds, o = ly(e._days), s = ly(e._months);
  t.isValid() && (i = i == null ? !0 : i, s && GO(t, xh(t, "Month") + s * n), o && UO(t, "Date", xh(t, "Date") + o * n), r && t._d.setTime(t._d.valueOf() + r * n), i && X.updateOffset(t, o || s));
}
var cY = aP(1, "add"), hY = aP(-1, "subtract");
function uP(t) {
  return typeof t == "string" || t instanceof String;
}
function dY(t) {
  return Sr(t) || md(t) || uP(t) || qo(t) || gY(t) || fY(t) || t === null || t === void 0;
}
function fY(t) {
  var e = Za(t) && !TS(t), n = !1, i = [
    "years",
    "year",
    "y",
    "months",
    "month",
    "M",
    "days",
    "day",
    "d",
    "dates",
    "date",
    "D",
    "hours",
    "hour",
    "h",
    "minutes",
    "minute",
    "m",
    "seconds",
    "second",
    "s",
    "milliseconds",
    "millisecond",
    "ms"
  ], r, o, s = i.length;
  for (r = 0; r < s; r += 1)
    o = i[r], n = n || lt(t, o);
  return e && n;
}
function gY(t) {
  var e = yr(t), n = !1;
  return e && (n = t.filter(function(i) {
    return !qo(i) && uP(t);
  }).length === 0), e && n;
}
function mY(t) {
  var e = Za(t) && !TS(t), n = !1, i = [
    "sameDay",
    "nextDay",
    "lastDay",
    "nextWeek",
    "lastWeek",
    "sameElse"
  ], r, o;
  for (r = 0; r < i.length; r += 1)
    o = i[r], n = n || lt(t, o);
  return e && n;
}
function pY(t, e) {
  var n = t.diff(e, "days", !0);
  return n < -6 ? "sameElse" : n < -1 ? "lastWeek" : n < 0 ? "lastDay" : n < 1 ? "sameDay" : n < 2 ? "nextDay" : n < 7 ? "nextWeek" : "sameElse";
}
function wY(t, e) {
  arguments.length === 1 && (arguments[0] ? dY(arguments[0]) ? (t = arguments[0], e = void 0) : mY(arguments[0]) && (e = arguments[0], t = void 0) : (t = void 0, e = void 0));
  var n = t || _t(), i = YS(n, this).startOf("day"), r = X.calendarFormat(this, i) || "sameElse", o = e && (po(e[r]) ? e[r].call(this, n) : e[r]);
  return this.format(
    o || this.localeData().calendar(r, this, _t(n))
  );
}
function AY() {
  return new pd(this);
}
function CY(t, e) {
  var n = Sr(t) ? t : _t(t);
  return this.isValid() && n.isValid() ? (e = qi(e) || "millisecond", e === "millisecond" ? this.valueOf() > n.valueOf() : n.valueOf() < this.clone().startOf(e).valueOf()) : !1;
}
function vY(t, e) {
  var n = Sr(t) ? t : _t(t);
  return this.isValid() && n.isValid() ? (e = qi(e) || "millisecond", e === "millisecond" ? this.valueOf() < n.valueOf() : this.clone().endOf(e).valueOf() < n.valueOf()) : !1;
}
function yY(t, e, n, i) {
  var r = Sr(t) ? t : _t(t), o = Sr(e) ? e : _t(e);
  return this.isValid() && r.isValid() && o.isValid() ? (i = i || "()", (i[0] === "(" ? this.isAfter(r, n) : !this.isBefore(r, n)) && (i[1] === ")" ? this.isBefore(o, n) : !this.isAfter(o, n))) : !1;
}
function SY(t, e) {
  var n = Sr(t) ? t : _t(t), i;
  return this.isValid() && n.isValid() ? (e = qi(e) || "millisecond", e === "millisecond" ? this.valueOf() === n.valueOf() : (i = n.valueOf(), this.clone().startOf(e).valueOf() <= i && i <= this.clone().endOf(e).valueOf())) : !1;
}
function bY(t, e) {
  return this.isSame(t, e) || this.isAfter(t, e);
}
function RY(t, e) {
  return this.isSame(t, e) || this.isBefore(t, e);
}
function EY(t, e, n) {
  var i, r, o;
  if (!this.isValid())
    return NaN;
  if (i = YS(t, this), !i.isValid())
    return NaN;
  switch (r = (i.utcOffset() - this.utcOffset()) * 6e4, e = qi(e), e) {
    case "year":
      o = Fg(this, i) / 12;
      break;
    case "month":
      o = Fg(this, i);
      break;
    case "quarter":
      o = Fg(this, i) / 3;
      break;
    case "second":
      o = (this - i) / 1e3;
      break;
    case "minute":
      o = (this - i) / 6e4;
      break;
    case "hour":
      o = (this - i) / 36e5;
      break;
    case "day":
      o = (this - i - r) / 864e5;
      break;
    case "week":
      o = (this - i - r) / 6048e5;
      break;
    default:
      o = this - i;
  }
  return n ? o : $i(o);
}
function Fg(t, e) {
  if (t.date() < e.date())
    return -Fg(e, t);
  var n = (e.year() - t.year()) * 12 + (e.month() - t.month()), i = t.clone().add(n, "months"), r, o;
  return e - i < 0 ? (r = t.clone().add(n - 1, "months"), o = (e - i) / (i - r)) : (r = t.clone().add(n + 1, "months"), o = (e - i) / (r - i)), -(n + o) || 0;
}
X.defaultFormat = "YYYY-MM-DDTHH:mm:ssZ";
X.defaultFormatUtc = "YYYY-MM-DDTHH:mm:ss[Z]";
function TY() {
  return this.clone().locale("en").format("ddd MMM DD YYYY HH:mm:ss [GMT]ZZ");
}
function MY(t) {
  if (!this.isValid())
    return null;
  var e = t !== !0, n = e ? this.clone().utc() : this;
  return n.year() < 0 || n.year() > 9999 ? Lg(
    n,
    e ? "YYYYYY-MM-DD[T]HH:mm:ss.SSS[Z]" : "YYYYYY-MM-DD[T]HH:mm:ss.SSSZ"
  ) : po(Date.prototype.toISOString) ? e ? this.toDate().toISOString() : new Date(this.valueOf() + this.utcOffset() * 60 * 1e3).toISOString().replace("Z", Lg(n, "Z")) : Lg(
    n,
    e ? "YYYY-MM-DD[T]HH:mm:ss.SSS[Z]" : "YYYY-MM-DD[T]HH:mm:ss.SSSZ"
  );
}
function IY() {
  if (!this.isValid())
    return "moment.invalid(/* " + this._i + " */)";
  var t = "moment", e = "", n, i, r, o;
  return this.isLocal() || (t = this.utcOffset() === 0 ? "moment.utc" : "moment.parseZone", e = "Z"), n = "[" + t + '("]', i = 0 <= this.year() && this.year() <= 9999 ? "YYYY" : "YYYYYY", r = "-MM-DD[T]HH:mm:ss.SSS", o = e + '[")]', this.format(n + i + r + o);
}
function OY(t) {
  t || (t = this.isUtc() ? X.defaultFormatUtc : X.defaultFormat);
  var e = Lg(this, t);
  return this.localeData().postformat(e);
}
function PY(t, e) {
  return this.isValid() && (Sr(t) && t.isValid() || _t(t).isValid()) ? Mr({ to: this, from: t }).locale(this.locale()).humanize(!e) : this.localeData().invalidDate();
}
function xY(t) {
  return this.from(_t(), t);
}
function HY(t, e) {
  return this.isValid() && (Sr(t) && t.isValid() || _t(t).isValid()) ? Mr({ from: this, to: t }).locale(this.locale()).humanize(!e) : this.localeData().invalidDate();
}
function _Y(t) {
  return this.to(_t(), t);
}
function cP(t) {
  var e;
  return t === void 0 ? this._locale._abbr : (e = ns(t), e != null && (this._locale = e), this);
}
var hP = Xi(
  "moment().lang() is deprecated. Instead, use moment().localeData() to get the language configuration. Use moment().locale() to change languages.",
  function(t) {
    return t === void 0 ? this.localeData() : this.locale(t);
  }
);
function dP() {
  return this._locale;
}
var fm = 1e3, wu = 60 * fm, gm = 60 * wu, fP = (365 * 400 + 97) * 24 * gm;
function Au(t, e) {
  return (t % e + e) % e;
}
function gP(t, e, n) {
  return t < 100 && t >= 0 ? new Date(t + 400, e, n) - fP : new Date(t, e, n).valueOf();
}
function mP(t, e, n) {
  return t < 100 && t >= 0 ? Date.UTC(t + 400, e, n) - fP : Date.UTC(t, e, n);
}
function NY(t) {
  var e, n;
  if (t = qi(t), t === void 0 || t === "millisecond" || !this.isValid())
    return this;
  switch (n = this._isUTC ? mP : gP, t) {
    case "year":
      e = n(this.year(), 0, 1);
      break;
    case "quarter":
      e = n(
        this.year(),
        this.month() - this.month() % 3,
        1
      );
      break;
    case "month":
      e = n(this.year(), this.month(), 1);
      break;
    case "week":
      e = n(
        this.year(),
        this.month(),
        this.date() - this.weekday()
      );
      break;
    case "isoWeek":
      e = n(
        this.year(),
        this.month(),
        this.date() - (this.isoWeekday() - 1)
      );
      break;
    case "day":
    case "date":
      e = n(this.year(), this.month(), this.date());
      break;
    case "hour":
      e = this._d.valueOf(), e -= Au(
        e + (this._isUTC ? 0 : this.utcOffset() * wu),
        gm
      );
      break;
    case "minute":
      e = this._d.valueOf(), e -= Au(e, wu);
      break;
    case "second":
      e = this._d.valueOf(), e -= Au(e, fm);
      break;
  }
  return this._d.setTime(e), X.updateOffset(this, !0), this;
}
function DY(t) {
  var e, n;
  if (t = qi(t), t === void 0 || t === "millisecond" || !this.isValid())
    return this;
  switch (n = this._isUTC ? mP : gP, t) {
    case "year":
      e = n(this.year() + 1, 0, 1) - 1;
      break;
    case "quarter":
      e = n(
        this.year(),
        this.month() - this.month() % 3 + 3,
        1
      ) - 1;
      break;
    case "month":
      e = n(this.year(), this.month() + 1, 1) - 1;
      break;
    case "week":
      e = n(
        this.year(),
        this.month(),
        this.date() - this.weekday() + 7
      ) - 1;
      break;
    case "isoWeek":
      e = n(
        this.year(),
        this.month(),
        this.date() - (this.isoWeekday() - 1) + 7
      ) - 1;
      break;
    case "day":
    case "date":
      e = n(this.year(), this.month(), this.date() + 1) - 1;
      break;
    case "hour":
      e = this._d.valueOf(), e += gm - Au(
        e + (this._isUTC ? 0 : this.utcOffset() * wu),
        gm
      ) - 1;
      break;
    case "minute":
      e = this._d.valueOf(), e += wu - Au(e, wu) - 1;
      break;
    case "second":
      e = this._d.valueOf(), e += fm - Au(e, fm) - 1;
      break;
  }
  return this._d.setTime(e), X.updateOffset(this, !0), this;
}
function kY() {
  return this._d.valueOf() - (this._offset || 0) * 6e4;
}
function LY() {
  return Math.floor(this.valueOf() / 1e3);
}
function BY() {
  return new Date(this.valueOf());
}
function FY() {
  var t = this;
  return [
    t.year(),
    t.month(),
    t.date(),
    t.hour(),
    t.minute(),
    t.second(),
    t.millisecond()
  ];
}
function VY() {
  var t = this;
  return {
    years: t.year(),
    months: t.month(),
    date: t.date(),
    hours: t.hours(),
    minutes: t.minutes(),
    seconds: t.seconds(),
    milliseconds: t.milliseconds()
  };
}
function WY() {
  return this.isValid() ? this.toISOString() : null;
}
function $Y() {
  return MS(this);
}
function UY() {
  return _s({}, We(this));
}
function YY() {
  return We(this).overflow;
}
function jY() {
  return {
    input: this._i,
    format: this._f,
    locale: this._locale,
    isUTC: this._isUTC,
    strict: this._strict
  };
}
be("N", 0, 0, "eraAbbr");
be("NN", 0, 0, "eraAbbr");
be("NNN", 0, 0, "eraAbbr");
be("NNNN", 0, 0, "eraName");
be("NNNNN", 0, 0, "eraNarrow");
be("y", ["y", 1], "yo", "eraYear");
be("y", ["yy", 2], 0, "eraYear");
be("y", ["yyy", 3], 0, "eraYear");
be("y", ["yyyy", 4], 0, "eraYear");
fe("N", jS);
fe("NN", jS);
fe("NNN", jS);
fe("NNNN", nj);
fe("NNNNN", ij);
Rt(
  ["N", "NN", "NNN", "NNNN", "NNNNN"],
  function(t, e, n, i) {
    var r = n._locale.erasParse(t, i, n._strict);
    r ? We(n).era = r : We(n).invalidEra = t;
  }
);
fe("y", Hu);
fe("yy", Hu);
fe("yyy", Hu);
fe("yyyy", Hu);
fe("yo", rj);
Rt(["y", "yy", "yyy", "yyyy"], Ln);
Rt(["yo"], function(t, e, n, i) {
  var r;
  n._locale._eraYearOrdinalRegex && (r = t.match(n._locale._eraYearOrdinalRegex)), n._locale.eraYearOrdinalParse ? e[Ln] = n._locale.eraYearOrdinalParse(t, r) : e[Ln] = parseInt(t, 10);
});
function GY(t, e) {
  var n, i, r, o = this._eras || ns("en")._eras;
  for (n = 0, i = o.length; n < i; ++n) {
    switch (typeof o[n].since) {
      case "string":
        r = X(o[n].since).startOf("day"), o[n].since = r.valueOf();
        break;
    }
    switch (typeof o[n].until) {
      case "undefined":
        o[n].until = 1 / 0;
        break;
      case "string":
        r = X(o[n].until).startOf("day").valueOf(), o[n].until = r.valueOf();
        break;
    }
  }
  return o;
}
function zY(t, e, n) {
  var i, r, o = this.eras(), s, a, l;
  for (t = t.toUpperCase(), i = 0, r = o.length; i < r; ++i)
    if (s = o[i].name.toUpperCase(), a = o[i].abbr.toUpperCase(), l = o[i].narrow.toUpperCase(), n)
      switch (e) {
        case "N":
        case "NN":
        case "NNN":
          if (a === t)
            return o[i];
          break;
        case "NNNN":
          if (s === t)
            return o[i];
          break;
        case "NNNNN":
          if (l === t)
            return o[i];
          break;
      }
    else if ([s, a, l].indexOf(t) >= 0)
      return o[i];
}
function KY(t, e) {
  var n = t.since <= t.until ? 1 : -1;
  return e === void 0 ? X(t.since).year() : X(t.since).year() + (e - t.offset) * n;
}
function QY() {
  var t, e, n, i = this.localeData().eras();
  for (t = 0, e = i.length; t < e; ++t)
    if (n = this.clone().startOf("day").valueOf(), i[t].since <= n && n <= i[t].until || i[t].until <= n && n <= i[t].since)
      return i[t].name;
  return "";
}
function XY() {
  var t, e, n, i = this.localeData().eras();
  for (t = 0, e = i.length; t < e; ++t)
    if (n = this.clone().startOf("day").valueOf(), i[t].since <= n && n <= i[t].until || i[t].until <= n && n <= i[t].since)
      return i[t].narrow;
  return "";
}
function qY() {
  var t, e, n, i = this.localeData().eras();
  for (t = 0, e = i.length; t < e; ++t)
    if (n = this.clone().startOf("day").valueOf(), i[t].since <= n && n <= i[t].until || i[t].until <= n && n <= i[t].since)
      return i[t].abbr;
  return "";
}
function ZY() {
  var t, e, n, i, r = this.localeData().eras();
  for (t = 0, e = r.length; t < e; ++t)
    if (n = r[t].since <= r[t].until ? 1 : -1, i = this.clone().startOf("day").valueOf(), r[t].since <= i && i <= r[t].until || r[t].until <= i && i <= r[t].since)
      return (this.year() - X(r[t].since).year()) * n + r[t].offset;
  return this.year();
}
function JY(t) {
  return lt(this, "_erasNameRegex") || GS.call(this), t ? this._erasNameRegex : this._erasRegex;
}
function ej(t) {
  return lt(this, "_erasAbbrRegex") || GS.call(this), t ? this._erasAbbrRegex : this._erasRegex;
}
function tj(t) {
  return lt(this, "_erasNarrowRegex") || GS.call(this), t ? this._erasNarrowRegex : this._erasRegex;
}
function jS(t, e) {
  return e.erasAbbrRegex(t);
}
function nj(t, e) {
  return e.erasNameRegex(t);
}
function ij(t, e) {
  return e.erasNarrowRegex(t);
}
function rj(t, e) {
  return e._eraYearOrdinalRegex || Hu;
}
function GS() {
  var t = [], e = [], n = [], i = [], r, o, s, a, l, u = this.eras();
  for (r = 0, o = u.length; r < o; ++r)
    s = Qo(u[r].name), a = Qo(u[r].abbr), l = Qo(u[r].narrow), e.push(s), t.push(a), n.push(l), i.push(s), i.push(a), i.push(l);
  this._erasRegex = new RegExp("^(" + i.join("|") + ")", "i"), this._erasNameRegex = new RegExp("^(" + e.join("|") + ")", "i"), this._erasAbbrRegex = new RegExp("^(" + t.join("|") + ")", "i"), this._erasNarrowRegex = new RegExp(
    "^(" + n.join("|") + ")",
    "i"
  );
}
be(0, ["gg", 2], 0, function() {
  return this.weekYear() % 100;
});
be(0, ["GG", 2], 0, function() {
  return this.isoWeekYear() % 100;
});
function vp(t, e) {
  be(0, [t, t.length], 0, e);
}
vp("gggg", "weekYear");
vp("ggggg", "weekYear");
vp("GGGG", "isoWeekYear");
vp("GGGGG", "isoWeekYear");
fe("G", mp);
fe("g", mp);
fe("GG", Nt, Ti);
fe("gg", Nt, Ti);
fe("GGGG", _S, HS);
fe("gggg", _S, HS);
fe("GGGGG", gp, dp);
fe("ggggg", gp, dp);
Ad(
  ["gggg", "ggggg", "GGGG", "GGGGG"],
  function(t, e, n, i) {
    e[i.substr(0, 2)] = Je(t);
  }
);
Ad(["gg", "GG"], function(t, e, n, i) {
  e[i] = X.parseTwoDigitYear(t);
});
function oj(t) {
  return pP.call(
    this,
    t,
    this.week(),
    this.weekday() + this.localeData()._week.dow,
    this.localeData()._week.dow,
    this.localeData()._week.doy
  );
}
function sj(t) {
  return pP.call(
    this,
    t,
    this.isoWeek(),
    this.isoWeekday(),
    1,
    4
  );
}
function aj() {
  return Xo(this.year(), 1, 4);
}
function lj() {
  return Xo(this.isoWeekYear(), 1, 4);
}
function uj() {
  var t = this.localeData()._week;
  return Xo(this.year(), t.dow, t.doy);
}
function cj() {
  var t = this.localeData()._week;
  return Xo(this.weekYear(), t.dow, t.doy);
}
function pP(t, e, n, i, r) {
  var o;
  return t == null ? _h(this, i, r).year : (o = Xo(t, i, r), e > o && (e = o), hj.call(this, t, e, n, i, r));
}
function hj(t, e, n, i, r) {
  var o = QO(t, e, n, i, r), s = Hh(o.year, 0, o.dayOfYear);
  return this.year(s.getUTCFullYear()), this.month(s.getUTCMonth()), this.date(s.getUTCDate()), this;
}
be("Q", 0, "Qo", "quarter");
fe("Q", BO);
Rt("Q", function(t, e) {
  e[$o] = (Je(t) - 1) * 3;
});
function dj(t) {
  return t == null ? Math.ceil((this.month() + 1) / 3) : this.month((t - 1) * 3 + this.month() % 3);
}
be("D", ["DD", 2], "Do", "date");
fe("D", Nt, _u);
fe("DD", Nt, Ti);
fe("Do", function(t, e) {
  return t ? e._dayOfMonthOrdinalParse || e._ordinalParse : e._dayOfMonthOrdinalParseLenient;
});
Rt(["D", "DD"], to);
Rt("Do", function(t, e) {
  e[to] = Je(t.match(Nt)[0]);
});
var wP = Nu("Date", !0);
be("DDD", ["DDDD", 3], "DDDo", "dayOfYear");
fe("DDD", fp);
fe("DDDD", FO);
Rt(["DDD", "DDDD"], function(t, e, n) {
  n._dayOfYear = Je(t);
});
function fj(t) {
  var e = Math.round(
    (this.clone().startOf("day") - this.clone().startOf("year")) / 864e5
  ) + 1;
  return t == null ? e : this.add(t - e, "d");
}
be("m", ["mm", 2], 0, "minute");
fe("m", Nt, NS);
fe("mm", Nt, Ti);
Rt(["m", "mm"], Ar);
var gj = Nu("Minutes", !1);
be("s", ["ss", 2], 0, "second");
fe("s", Nt, NS);
fe("ss", Nt, Ti);
Rt(["s", "ss"], Uo);
var mj = Nu("Seconds", !1);
be("S", 0, 0, function() {
  return ~~(this.millisecond() / 100);
});
be(0, ["SS", 2], 0, function() {
  return ~~(this.millisecond() / 10);
});
be(0, ["SSS", 3], 0, "millisecond");
be(0, ["SSSS", 4], 0, function() {
  return this.millisecond() * 10;
});
be(0, ["SSSSS", 5], 0, function() {
  return this.millisecond() * 100;
});
be(0, ["SSSSSS", 6], 0, function() {
  return this.millisecond() * 1e3;
});
be(0, ["SSSSSSS", 7], 0, function() {
  return this.millisecond() * 1e4;
});
be(0, ["SSSSSSSS", 8], 0, function() {
  return this.millisecond() * 1e5;
});
be(0, ["SSSSSSSSS", 9], 0, function() {
  return this.millisecond() * 1e6;
});
fe("S", fp, BO);
fe("SS", fp, Ti);
fe("SSS", fp, FO);
var Ns, AP;
for (Ns = "SSSS"; Ns.length <= 9; Ns += "S")
  fe(Ns, Hu);
function pj(t, e) {
  e[Ua] = Je(("0." + t) * 1e3);
}
for (Ns = "S"; Ns.length <= 9; Ns += "S")
  Rt(Ns, pj);
AP = Nu("Milliseconds", !1);
be("z", 0, 0, "zoneAbbr");
be("zz", 0, 0, "zoneName");
function wj() {
  return this._isUTC ? "UTC" : "";
}
function Aj() {
  return this._isUTC ? "Coordinated Universal Time" : "";
}
var ee = pd.prototype;
ee.add = cY;
ee.calendar = wY;
ee.clone = AY;
ee.diff = EY;
ee.endOf = DY;
ee.format = OY;
ee.from = PY;
ee.fromNow = xY;
ee.to = HY;
ee.toNow = _Y;
ee.get = I$;
ee.invalidAt = YY;
ee.isAfter = CY;
ee.isBefore = vY;
ee.isBetween = yY;
ee.isSame = SY;
ee.isSameOrAfter = bY;
ee.isSameOrBefore = RY;
ee.isValid = $Y;
ee.lang = hP;
ee.locale = cP;
ee.localeData = dP;
ee.max = $U;
ee.min = WU;
ee.parsingFlags = UY;
ee.set = O$;
ee.startOf = NY;
ee.subtract = hY;
ee.toArray = FY;
ee.toObject = VY;
ee.toDate = BY;
ee.toISOString = MY;
ee.inspect = IY;
typeof Symbol != "undefined" && Symbol.for != null && (ee[Symbol.for("nodejs.util.inspect.custom")] = function() {
  return "Moment<" + this.format() + ">";
});
ee.toJSON = WY;
ee.toString = TY;
ee.unix = LY;
ee.valueOf = kY;
ee.creationData = jY;
ee.eraName = QY;
ee.eraNarrow = XY;
ee.eraAbbr = qY;
ee.eraYear = ZY;
ee.year = $O;
ee.isLeapYear = M$;
ee.weekYear = oj;
ee.isoWeekYear = sj;
ee.quarter = ee.quarters = dj;
ee.month = zO;
ee.daysInMonth = B$;
ee.week = ee.weeks = G$;
ee.isoWeek = ee.isoWeeks = z$;
ee.weeksInYear = uj;
ee.weeksInWeekYear = cj;
ee.isoWeeksInYear = aj;
ee.isoWeeksInISOWeekYear = lj;
ee.date = wP;
ee.day = ee.days = sU;
ee.weekday = aU;
ee.isoWeekday = lU;
ee.dayOfYear = fj;
ee.hour = ee.hours = mU;
ee.minute = ee.minutes = gj;
ee.second = ee.seconds = mj;
ee.millisecond = ee.milliseconds = AP;
ee.utcOffset = qU;
ee.utc = JU;
ee.local = eY;
ee.parseZone = tY;
ee.hasAlignedHourOffset = nY;
ee.isDST = iY;
ee.isLocal = oY;
ee.isUtcOffset = sY;
ee.isUtc = sP;
ee.isUTC = sP;
ee.zoneAbbr = wj;
ee.zoneName = Aj;
ee.dates = Xi(
  "dates accessor is deprecated. Use date instead.",
  wP
);
ee.months = Xi(
  "months accessor is deprecated. Use month instead",
  zO
);
ee.years = Xi(
  "years accessor is deprecated. Use year instead",
  $O
);
ee.zone = Xi(
  "moment().zone is deprecated, use moment().utcOffset instead. http://momentjs.com/guides/#/warnings/zone/",
  ZU
);
ee.isDSTShifted = Xi(
  "isDSTShifted is deprecated. See http://momentjs.com/guides/#/warnings/dst-shifted/ for more information",
  rY
);
function Cj(t) {
  return _t(t * 1e3);
}
function vj() {
  return _t.apply(null, arguments).parseZone();
}
function CP(t) {
  return t;
}
var ut = OS.prototype;
ut.calendar = o$;
ut.longDateFormat = u$;
ut.invalidDate = h$;
ut.ordinal = g$;
ut.preparse = CP;
ut.postformat = CP;
ut.relativeTime = p$;
ut.pastFuture = w$;
ut.set = i$;
ut.eras = GY;
ut.erasParse = zY;
ut.erasConvertYear = KY;
ut.erasAbbrRegex = ej;
ut.erasNameRegex = JY;
ut.erasNarrowRegex = tj;
ut.months = N$;
ut.monthsShort = D$;
ut.monthsParse = L$;
ut.monthsRegex = V$;
ut.monthsShortRegex = F$;
ut.week = $$;
ut.firstDayOfYear = j$;
ut.firstDayOfWeek = Y$;
ut.weekdays = tU;
ut.weekdaysMin = iU;
ut.weekdaysShort = nU;
ut.weekdaysParse = oU;
ut.weekdaysRegex = uU;
ut.weekdaysShortRegex = cU;
ut.weekdaysMinRegex = hU;
ut.isPM = fU;
ut.meridiem = pU;
function mm(t, e, n, i) {
  var r = ns(), o = mo().set(i, e);
  return r[n](o, t);
}
function vP(t, e, n) {
  if (qo(t) && (e = t, t = void 0), t = t || "", e != null)
    return mm(t, e, n, "month");
  var i, r = [];
  for (i = 0; i < 12; i++)
    r[i] = mm(t, i, n, "month");
  return r;
}
function zS(t, e, n, i) {
  typeof t == "boolean" ? (qo(e) && (n = e, e = void 0), e = e || "") : (e = t, n = e, t = !1, qo(e) && (n = e, e = void 0), e = e || "");
  var r = ns(), o = t ? r._week.dow : 0, s, a = [];
  if (n != null)
    return mm(e, (n + o) % 7, i, "day");
  for (s = 0; s < 7; s++)
    a[s] = mm(e, (s + o) % 7, i, "day");
  return a;
}
function yj(t, e) {
  return vP(t, e, "months");
}
function Sj(t, e) {
  return vP(t, e, "monthsShort");
}
function bj(t, e, n) {
  return zS(t, e, n, "weekdays");
}
function Rj(t, e, n) {
  return zS(t, e, n, "weekdaysShort");
}
function Ej(t, e, n) {
  return zS(t, e, n, "weekdaysMin");
}
Fs("en", {
  eras: [
    {
      since: "0001-01-01",
      until: 1 / 0,
      offset: 1,
      name: "Anno Domini",
      narrow: "AD",
      abbr: "AD"
    },
    {
      since: "0000-12-31",
      until: -1 / 0,
      offset: 1,
      name: "Before Christ",
      narrow: "BC",
      abbr: "BC"
    }
  ],
  dayOfMonthOrdinalParse: /\d{1,2}(th|st|nd|rd)/,
  ordinal: function(t) {
    var e = t % 10, n = Je(t % 100 / 10) === 1 ? "th" : e === 1 ? "st" : e === 2 ? "nd" : e === 3 ? "rd" : "th";
    return t + n;
  }
});
X.lang = Xi(
  "moment.lang is deprecated. Use moment.locale instead.",
  Fs
);
X.langData = Xi(
  "moment.langData is deprecated. Use moment.localeData instead.",
  ns
);
var Co = Math.abs;
function Tj() {
  var t = this._data;
  return this._milliseconds = Co(this._milliseconds), this._days = Co(this._days), this._months = Co(this._months), t.milliseconds = Co(t.milliseconds), t.seconds = Co(t.seconds), t.minutes = Co(t.minutes), t.hours = Co(t.hours), t.months = Co(t.months), t.years = Co(t.years), this;
}
function yP(t, e, n, i) {
  var r = Mr(e, n);
  return t._milliseconds += i * r._milliseconds, t._days += i * r._days, t._months += i * r._months, t._bubble();
}
function Mj(t, e) {
  return yP(this, t, e, 1);
}
function Ij(t, e) {
  return yP(this, t, e, -1);
}
function gE(t) {
  return t < 0 ? Math.floor(t) : Math.ceil(t);
}
function Oj() {
  var t = this._milliseconds, e = this._days, n = this._months, i = this._data, r, o, s, a, l;
  return t >= 0 && e >= 0 && n >= 0 || t <= 0 && e <= 0 && n <= 0 || (t += gE(cy(n) + e) * 864e5, e = 0, n = 0), i.milliseconds = t % 1e3, r = $i(t / 1e3), i.seconds = r % 60, o = $i(r / 60), i.minutes = o % 60, s = $i(o / 60), i.hours = s % 24, e += $i(s / 24), l = $i(SP(e)), n += l, e -= gE(cy(l)), a = $i(n / 12), n %= 12, i.days = e, i.months = n, i.years = a, this;
}
function SP(t) {
  return t * 4800 / 146097;
}
function cy(t) {
  return t * 146097 / 4800;
}
function Pj(t) {
  if (!this.isValid())
    return NaN;
  var e, n, i = this._milliseconds;
  if (t = qi(t), t === "month" || t === "quarter" || t === "year")
    switch (e = this._days + i / 864e5, n = this._months + SP(e), t) {
      case "month":
        return n;
      case "quarter":
        return n / 3;
      case "year":
        return n / 12;
    }
  else
    switch (e = this._days + Math.round(cy(this._months)), t) {
      case "week":
        return e / 7 + i / 6048e5;
      case "day":
        return e + i / 864e5;
      case "hour":
        return e * 24 + i / 36e5;
      case "minute":
        return e * 1440 + i / 6e4;
      case "second":
        return e * 86400 + i / 1e3;
      case "millisecond":
        return Math.floor(e * 864e5) + i;
      default:
        throw new Error("Unknown unit " + t);
    }
}
function is(t) {
  return function() {
    return this.as(t);
  };
}
var bP = is("ms"), xj = is("s"), Hj = is("m"), _j = is("h"), Nj = is("d"), Dj = is("w"), kj = is("M"), Lj = is("Q"), Bj = is("y"), Fj = bP;
function Vj() {
  return Mr(this);
}
function Wj(t) {
  return t = qi(t), this.isValid() ? this[t + "s"]() : NaN;
}
function fl(t) {
  return function() {
    return this.isValid() ? this._data[t] : NaN;
  };
}
var $j = fl("milliseconds"), Uj = fl("seconds"), Yj = fl("minutes"), jj = fl("hours"), Gj = fl("days"), zj = fl("months"), Kj = fl("years");
function Qj() {
  return $i(this.days() / 7);
}
var Do = Math.round, cu = {
  ss: 44,
  // a few seconds to seconds
  s: 45,
  // seconds to minute
  m: 45,
  // minutes to hour
  h: 22,
  // hours to day
  d: 26,
  // days to month/week
  w: null,
  // weeks to month
  M: 11
  // months to year
};
function Xj(t, e, n, i, r) {
  return r.relativeTime(e || 1, !!n, t, i);
}
function qj(t, e, n, i) {
  var r = Mr(t).abs(), o = Do(r.as("s")), s = Do(r.as("m")), a = Do(r.as("h")), l = Do(r.as("d")), u = Do(r.as("M")), c = Do(r.as("w")), h = Do(r.as("y")), d = o <= n.ss && ["s", o] || o < n.s && ["ss", o] || s <= 1 && ["m"] || s < n.m && ["mm", s] || a <= 1 && ["h"] || a < n.h && ["hh", a] || l <= 1 && ["d"] || l < n.d && ["dd", l];
  return n.w != null && (d = d || c <= 1 && ["w"] || c < n.w && ["ww", c]), d = d || u <= 1 && ["M"] || u < n.M && ["MM", u] || h <= 1 && ["y"] || ["yy", h], d[2] = e, d[3] = +t > 0, d[4] = i, Xj.apply(null, d);
}
function Zj(t) {
  return t === void 0 ? Do : typeof t == "function" ? (Do = t, !0) : !1;
}
function Jj(t, e) {
  return cu[t] === void 0 ? !1 : e === void 0 ? cu[t] : (cu[t] = e, t === "s" && (cu.ss = e - 1), !0);
}
function eG(t, e) {
  if (!this.isValid())
    return this.localeData().invalidDate();
  var n = !1, i = cu, r, o;
  return typeof t == "object" && (e = t, t = !1), typeof t == "boolean" && (n = t), typeof e == "object" && (i = Object.assign({}, cu, e), e.s != null && e.ss == null && (i.ss = e.s - 1)), r = this.localeData(), o = qj(this, !n, i, r), n && (o = r.pastFuture(+this, o)), r.postformat(o);
}
var zA = Math.abs;
function yl(t) {
  return (t > 0) - (t < 0) || +t;
}
function yp() {
  if (!this.isValid())
    return this.localeData().invalidDate();
  var t = zA(this._milliseconds) / 1e3, e = zA(this._days), n = zA(this._months), i, r, o, s, a = this.asSeconds(), l, u, c, h;
  return a ? (i = $i(t / 60), r = $i(i / 60), t %= 60, i %= 60, o = $i(n / 12), n %= 12, s = t ? t.toFixed(3).replace(/\.?0+$/, "") : "", l = a < 0 ? "-" : "", u = yl(this._months) !== yl(a) ? "-" : "", c = yl(this._days) !== yl(a) ? "-" : "", h = yl(this._milliseconds) !== yl(a) ? "-" : "", l + "P" + (o ? u + o + "Y" : "") + (n ? u + n + "M" : "") + (e ? c + e + "D" : "") + (r || i || t ? "T" : "") + (r ? h + r + "H" : "") + (i ? h + i + "M" : "") + (t ? h + s + "S" : "")) : "P0D";
}
var st = Cp.prototype;
st.isValid = zU;
st.abs = Tj;
st.add = Mj;
st.subtract = Ij;
st.as = Pj;
st.asMilliseconds = bP;
st.asSeconds = xj;
st.asMinutes = Hj;
st.asHours = _j;
st.asDays = Nj;
st.asWeeks = Dj;
st.asMonths = kj;
st.asQuarters = Lj;
st.asYears = Bj;
st.valueOf = Fj;
st._bubble = Oj;
st.clone = Vj;
st.get = Wj;
st.milliseconds = $j;
st.seconds = Uj;
st.minutes = Yj;
st.hours = jj;
st.days = Gj;
st.weeks = Qj;
st.months = zj;
st.years = Kj;
st.humanize = eG;
st.toISOString = yp;
st.toString = yp;
st.toJSON = yp;
st.locale = cP;
st.localeData = dP;
st.toIsoString = Xi(
  "toIsoString() is deprecated. Please use toISOString() instead (notice the capitals)",
  yp
);
st.lang = hP;
be("X", 0, 0, "unix");
be("x", 0, 0, "valueOf");
fe("x", mp);
fe("X", y$);
Rt("X", function(t, e, n) {
  n._d = new Date(parseFloat(t) * 1e3);
});
Rt("x", function(t, e, n) {
  n._d = new Date(Je(t));
});
//! moment.js
X.version = "2.30.1";
t$(_t);
X.fn = ee;
X.min = UU;
X.max = YU;
X.now = jU;
X.utc = mo;
X.unix = Cj;
X.months = yj;
X.isDate = md;
X.locale = Fs;
X.invalid = hp;
X.duration = Mr;
X.isMoment = Sr;
X.weekdays = bj;
X.parseZone = vj;
X.localeData = ns;
X.isDuration = Bg;
X.monthsShort = Sj;
X.weekdaysMin = Ej;
X.defineLocale = FS;
X.updateLocale = vU;
X.locales = yU;
X.weekdaysShort = Rj;
X.normalizeUnits = qi;
X.relativeTimeRounding = Zj;
X.relativeTimeThreshold = Jj;
X.calendarFormat = pY;
X.prototype = ee;
X.HTML5_FMT = {
  DATETIME_LOCAL: "YYYY-MM-DDTHH:mm",
  // <input type="datetime-local" />
  DATETIME_LOCAL_SECONDS: "YYYY-MM-DDTHH:mm:ss",
  // <input type="datetime-local" step="1" />
  DATETIME_LOCAL_MS: "YYYY-MM-DDTHH:mm:ss.SSS",
  // <input type="datetime-local" step="0.001" />
  DATE: "YYYY-MM-DD",
  // <input type="date" />
  TIME: "HH:mm",
  // <input type="time" />
  TIME_SECONDS: "HH:mm:ss",
  // <input type="time" step="1" />
  TIME_MS: "HH:mm:ss.SSS",
  // <input type="time" step="0.001" />
  WEEK: "GGGG-[W]WW",
  // <input type="week" />
  MONTH: "YYYY-MM"
  // <input type="month" />
};
function RP(t) {
  const e = t.length;
  let n = 0;
  for (; n < e; )
    t[n] = [t[n]], n += 1;
}
function hy(t, e) {
  const n = e.length;
  let i = 0;
  for (; i < n; )
    t.push(e[i]), i += 1;
}
function rh(t) {
  const e = [];
  if (!t || t.length === 0 || !t[0] || t[0].length === 0)
    return e;
  const n = t.length, i = t[0].length;
  for (let r = 0; r < n; r++)
    for (let o = 0; o < i; o++)
      e[o] || (e[o] = []), e[o][r] = t[r][o];
  return e;
}
function rn(t, e, n, i) {
  let r = -1, o = t, s = n;
  Array.isArray(t) || (o = Array.from(t));
  const a = o.length;
  for (i && a && (r += 1, s = o[r]), r += 1; r < a; )
    s = e(s, o[r], r, o), r += 1;
  return s;
}
function Zo(t, e) {
  let n = 0, i = t;
  Array.isArray(t) || (i = Array.from(t));
  const r = i.length, o = [];
  let s = -1;
  for (; n < r; ) {
    const a = i[n];
    e(a, n, i) && (s += 1, o[s] = a), n += 1;
  }
  return o;
}
function Ke(t, e) {
  let n = 0, i = t;
  Array.isArray(t) || (i = Array.from(t));
  const r = i.length, o = [];
  let s = -1;
  for (; n < r; ) {
    const a = i[n];
    s += 1, o[s] = e(a, n, i), n += 1;
  }
  return o;
}
function T(t, e) {
  let n = 0, i = t;
  Array.isArray(t) || (i = Array.from(t));
  const r = i.length;
  for (; n < r && e(i[n], n, i) !== !1; )
    n += 1;
  return t;
}
function EP(t) {
  return rn(t, (e, n) => e + n, 0);
}
function tG(t) {
  return rn(t, (e, n) => e > n ? e : n, Array.isArray(t) ? t[0] : void 0);
}
function nG(t) {
  return rn(t, (e, n) => e < n ? e : n, Array.isArray(t) ? t[0] : void 0);
}
function iG(t) {
  return t.length ? EP(t) / t.length : 0;
}
function TP(t) {
  return rn(t, (e, n) => e.concat(Array.isArray(n) ? TP(n) : n), []);
}
function KS(t) {
  const e = [];
  return T(t, (n) => {
    e.indexOf(n) === -1 && e.push(n);
  }), e;
}
function dy() {
  for (var t = arguments.length, e = new Array(t), n = 0; n < t; n++)
    e[n] = arguments[n];
  const [i, ...r] = [...e];
  let o = i;
  return T(r, (s) => {
    o = o.filter((a) => !s.includes(a));
  }), o;
}
function rG() {
  for (var t = arguments.length, e = new Array(t), n = 0; n < t; n++)
    e[n] = arguments[n];
  const [i, ...r] = [...e];
  let o = i;
  return T(r, (s) => {
    o = o.filter((a) => s.includes(a));
  }), o;
}
function oG() {
  for (var t = arguments.length, e = new Array(t), n = 0; n < t; n++)
    e[n] = arguments[n];
  const [i, ...r] = [...e], o = new Set(i);
  return T(r, (s) => {
    T(s, (a) => {
      o.has(a) || o.add(a);
    });
  }), Array.from(o);
}
function fy(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : " ";
  return t.split(e);
}
const sG = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  arrayAvg: iG,
  arrayEach: T,
  arrayFilter: Zo,
  arrayFlatten: TP,
  arrayMap: Ke,
  arrayMax: tG,
  arrayMin: nG,
  arrayReduce: rn,
  arraySum: EP,
  arrayUnique: KS,
  extendArray: hy,
  getDifferenceOfArrays: dy,
  getIntersectionOfArrays: rG,
  getUnionOfArrays: oG,
  pivot: rh,
  stringToArray: fy,
  to2dArray: RP
}, Symbol.toStringTag, { value: "Module" }));
function mt(t) {
  for (var e = arguments.length, n = new Array(e > 1 ? e - 1 : 0), i = 1; i < e; i++)
    n[i - 1] = arguments[i];
  return rn(t, (o, s, a) => {
    const l = s.replace(/\r?\n\s*/g, ""), u = n[a] ? n[a] : "";
    return o + l + u;
  }, "").trim();
}
function Gt(t) {
  let e;
  switch (typeof t) {
    case "string":
    case "number":
      e = `${t}`;
      break;
    case "object":
      e = t === null ? "" : t.toString();
      break;
    case "undefined":
      e = "";
      break;
    default:
      e = t.toString();
      break;
  }
  return e;
}
function de(t) {
  return typeof t != "undefined";
}
function Mt(t) {
  return typeof t == "undefined";
}
function zt(t) {
  return t === null || t === "" || Mt(t);
}
function MP(t) {
  return Object.prototype.toString.call(t) === "[object RegExp]";
}
const Xc = "length", oh = (t) => parseInt(t, 16), mE = (t) => parseInt(t, 10), pm = (t, e, n) => t.substr(e, n), sh = (t) => t.codePointAt(0) - 65, gy = (t) => `${t}`.replace(/\-/g, ""), aG = (t) => oh(pm(gy(t), oh("12"), sh("F"))) / (oh(pm(gy(t), sh("B"), ~~![][Xc])) || 9), lG = () => typeof location != "undefined" && /^([a-z0-9\-]+\.)?\x68\x61\x6E\x64\x73\x6F\x6E\x74\x61\x62\x6C\x65\x2E\x63\x6F\x6D$/i.test(location.host);
let pE = !1;
const wE = {
  invalid: () => mt`
    The license key for Handsontable is invalid.\x20
    If you need any help, contact us at support@handsontable.com.`,
  expired: (t) => {
    let {
      keyValidityDate: e,
      hotVersion: n
    } = t;
    return mt`
    The license key for Handsontable expired on ${e}, and is not valid for the installed\x20
    version ${n}. Renew your license key at handsontable.com or downgrade to a version released prior\x20
    to ${e}. If you need any help, contact us at sales@handsontable.com.`;
  },
  missing: () => mt`
    The license key for Handsontable is missing. Use your purchased key to activate the product.\x20
    Alternatively, you can activate Handsontable to use for non-commercial purposes by\x20
    passing the key: 'non-commercial-and-evaluation'. If you need any help, contact\x20
    us at support@handsontable.com.`,
  non_commercial: () => ""
}, AE = {
  invalid: () => mt`
    The license key for Handsontable is invalid.\x20
    <a href="https://handsontable.com/docs/tutorial-license-key.html" target="_blank">Read more</a> on how to\x20
    install it properly or contact us at <a href="mailto:support@handsontable.com">support@handsontable.com</a>.`,
  expired: (t) => {
    let {
      keyValidityDate: e,
      hotVersion: n
    } = t;
    return mt`
    The license key for Handsontable expired on ${e}, and is not valid for the installed\x20
    version ${n}. <a href="https://handsontable.com/pricing" target="_blank">Renew</a> your\x20
    license key or downgrade to a version released prior to ${e}. If you need any\x20
    help, contact us at <a href="mailto:sales@handsontable.com">sales@handsontable.com</a>.`;
  },
  missing: () => mt`
    The license key for Handsontable is missing. Use your purchased key to activate the product.\x20
    Alternatively, you can activate Handsontable to use for non-commercial purposes by\x20
    passing the key: 'non-commercial-and-evaluation'.\x20
    <a href="https://handsontable.com/docs/tutorial-license-key.html" target="_blank">Read more</a> about it in\x20
    the documentation or contact us at <a href="mailto:support@handsontable.com">support@handsontable.com</a>.`,
  non_commercial: () => ""
};
function IP(t, e) {
  const n = !zt(t), i = typeof t == "string" && t.toLowerCase() === "non-commercial-and-evaluation", r = "14.6.1";
  let o, s = "invalid", a = "invalid";
  t = gy(t || "");
  const l = uG(t);
  if (n || i || l)
    if (l) {
      const u = X("17/10/2024", "DD/MM/YYYY"), c = Math.floor(u.toDate().getTime() / 864e5), h = aG(t);
      o = X((h + 1) * 864e5, "x").format("MMMM DD, YYYY"), c > h ? (s = "expired", a = "expired") : (s = "valid", a = "valid");
    } else
      i ? (s = "non_commercial", a = "valid") : (s = "invalid", a = "invalid");
  else
    s = "missing", a = "missing";
  if (lG() && (s = "valid", a = "valid"), !pE && s !== "valid" && (wE[s]({
    keyValidityDate: o,
    hotVersion: r
  }) && console[s === "non_commercial" ? "info" : "warn"](wE[s]({
    keyValidityDate: o,
    hotVersion: r
  })), pE = !0), a !== "valid" && e.parentNode && AE[a]({
    keyValidityDate: o,
    hotVersion: r
  })) {
    const c = document.createElement("div");
    c.className = "hot-display-license-info", c.innerHTML = AE[a]({
      keyValidityDate: o,
      hotVersion: r
    }), e.parentNode.insertBefore(c, e.nextSibling);
  }
}
function uG(t) {
  let e = [][Xc], n = e;
  if (t[Xc] !== sh("Z"))
    return !1;
  for (let i = "", r = "B<H4P+".split(""), o = sh(r.shift()); o; o = sh(r.shift() || "A"))
    --o < ""[Xc] ? n = n | (mE(`${mE(oh(i) + (oh(pm(t, Math.abs(o), 2)) + []).padStart(2, "0"))}`) % 97 || 2) >> 1 : i = pm(t, o, o ? r[Xc] === 1 ? 9 : 8 : 6);
  return n === e;
}
const cG = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  _injectProductInfo: IP,
  isDefined: de,
  isEmpty: zt,
  isRegExp: MP,
  isUndefined: Mt,
  stringify: Gt
}, Symbol.toStringTag, { value: "Module" }));
function Bn(t) {
  return t[0].toUpperCase() + t.substr(1);
}
function hG() {
  const t = [];
  for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
    n[i] = arguments[i];
  let r = n.length;
  for (; r; ) {
    r -= 1;
    const o = Gt(n[r]).toLowerCase();
    t.indexOf(o) === -1 && t.push(o);
  }
  return t.length === 1;
}
function QS() {
  function t() {
    return Math.floor((1 + Math.random()) * 65536).toString(16).substring(1);
  }
  return t() + t() + t() + t();
}
function XS(t) {
  return /^([0-9][0-9]?%$)|(^100%$)/.test(t);
}
function qS(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
  return `${t}`.replace(/(?:\\)?\[([^[\]]+)]/g, (n, i) => n.charAt(0) === "\\" ? n.substr(1, n.length - 1) : e[i] === void 0 ? "" : e[i]);
}
function my(t) {
  return Sp(`${t}`, {
    ALLOWED_TAGS: []
  });
}
function Sp(t, e) {
  return e$.sanitize(t, e);
}
const dG = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  equalsIgnoreCase: hG,
  isPercentValue: XS,
  randomString: QS,
  sanitize: Sp,
  stripTags: my,
  substitute: qS,
  toUpperCaseFirst: Bn
}, Symbol.toStringTag, { value: "Module" })), Du = (t) => ["tabindex", t], fG = () => ["role", "treegrid"], oo = () => ["role", "presentation"], gG = () => ["role", "gridcell"], mG = () => ["role", "rowheader"], OP = () => ["role", "rowgroup"], pG = () => ["role", "columnheader"], py = () => ["role", "row"], wG = () => ["role", "menu"], AG = () => ["role", "menuitem"], CG = () => ["role", "menuitemcheckbox"], vG = () => ["role", "combobox"], PP = () => ["role", "listbox"], yG = () => ["role", "option"], SG = () => ["role", "checkbox"], bG = () => ["scope", "col"], RG = () => ["scope", "row"], EG = () => ["type", "text"], wm = (t) => ["aria-label", t], gl = () => ["aria-hidden", "true"], TG = () => ["aria-disabled", "true"], MG = () => ["aria-multiselectable", "true"], xP = (t) => ["aria-haspopup", t], HP = (t) => ["aria-rowcount", t], bp = (t) => ["aria-colcount", t], _P = (t) => ["aria-rowindex", t], ZS = (t) => ["aria-colindex", t], Gi = (t) => ["aria-expanded", t], IG = (t) => ["aria-sort", t], CE = () => ["aria-readonly", "true"], vE = () => ["aria-invalid", "true"], NP = (t) => ["aria-checked", t], JS = () => ["aria-selected", "true"], OG = () => ["aria-autocomplete", "list"], PG = (t) => ["aria-controls", t], xG = (t) => ["aria-activedescendant", t], HG = (t) => ["aria-live", t], _G = (t) => ["aria-relevant", t], NG = (t) => ["aria-setsize", t], DG = (t) => ["aria-posinset", t];
function DP(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, n = -1, i = null, r = t;
  for (; r !== null; ) {
    if (n === e) {
      i = r;
      break;
    }
    r.host && r.nodeType === Node.DOCUMENT_FRAGMENT_NODE ? r = r.host : (n += 1, r = r.parentNode);
  }
  return i;
}
function kP(t, e) {
  const n = t.closest(".handsontable");
  return !!n && (n.parentNode === e || n === e);
}
function LP(t) {
  return Object.getPrototypeOf(t.parent) && t.frameElement;
}
function Ja(t) {
  return LP(t) && t.parent;
}
function kG(t) {
  return !!Object.getPrototypeOf(t.parent);
}
function js(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [], n = arguments.length > 2 ? arguments[2] : void 0;
  const {
    ELEMENT_NODE: i,
    DOCUMENT_FRAGMENT_NODE: r
  } = Node;
  let o = t;
  for (; o != null && o !== n; ) {
    const {
      nodeType: s,
      nodeName: a
    } = o;
    if (s === i && (e.includes(a) || e.includes(o)))
      return o;
    const {
      host: l
    } = o;
    l && s === r ? o = l : o = o.parentNode;
  }
  return null;
}
function qc(t, e, n) {
  const i = [];
  let r = t;
  for (; r && (r = js(r, e, n), !(!r || n && !n.contains(r))); )
    i.push(r), r.host && r.nodeType === Node.DOCUMENT_FRAGMENT_NODE ? r = r.host : r = r.parentNode;
  const o = i.length;
  return o ? i[o - 1] : null;
}
function Vs(t, e) {
  let n = t.parentNode, i = [];
  for (typeof e == "string" ? t.defaultView ? i = Array.prototype.slice.call(t.querySelectorAll(e), 0) : i = Array.prototype.slice.call(t.ownerDocument.querySelectorAll(e), 0) : i.push(e); n !== null; ) {
    if (i.indexOf(n) > -1)
      return !0;
    n = n.parentNode;
  }
  return !1;
}
function BP(t) {
  let e = 0, n = t;
  if (n.previousSibling)
    for (; n = n.previousSibling; )
      e += 1;
  return e;
}
function ys(t, e, n) {
  const i = n.parentElement.querySelector(`.ht_clone_${t}`);
  return i ? i.contains(e) : null;
}
function FP(t) {
  return !t || !t.length ? [] : t.filter((e) => !!e);
}
function VP(t, e) {
  if (!t || !t.length)
    return e ? {
      regexFree: [],
      regexes: []
    } : [];
  const n = [], i = [];
  return i.push(...t.filter((r) => {
    const o = r instanceof RegExp;
    return o && e && n.push(r), !o;
  })), e ? {
    regexFree: i,
    regexes: n
  } : i;
}
function Te(t, e) {
  return t.classList === void 0 || typeof e != "string" || e === "" ? !1 : t.classList.contains(e);
}
function Q(t, e) {
  typeof e == "string" && (e = e.split(" ")), e = FP(e), e.length > 0 && t.classList.add(...e);
}
function xe(t, e) {
  typeof e == "string" ? e = e.split(" ") : e instanceof RegExp && (e = [e]);
  let {
    regexFree: n,
    // eslint-disable-next-line prefer-const
    regexes: i
  } = VP(e, !0);
  n = FP(n), n.length > 0 && t.classList.remove(...n), i.forEach((r) => {
    t.classList.forEach((o) => {
      r.test(o) && t.classList.remove(o);
    });
  });
}
function Ee(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [], n = arguments.length > 2 ? arguments[2] : void 0;
  Array.isArray(e) || (e = [[e, n]]), e.forEach((i) => {
    Array.isArray(i) && i[0] !== "" && t.setAttribute(...i);
  });
}
function ml(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
  typeof e == "string" ? e = e.split(" ") : e instanceof RegExp && (e = [e]);
  const {
    regexFree: n,
    regexes: i
  } = VP(e, !0);
  n.forEach((r) => {
    r !== "" && t.removeAttribute(r);
  }), i.forEach((r) => {
    t.getAttributeNames().forEach((o) => {
      r.test(o) && t.removeAttribute(o);
    });
  });
}
function eb(t) {
  if (t.nodeType === 3)
    t.parentNode.removeChild(t);
  else if (["TABLE", "THEAD", "TBODY", "TFOOT", "TR"].indexOf(t.nodeName) > -1) {
    const e = t.childNodes;
    for (let n = e.length - 1; n >= 0; n--)
      eb(e[n]);
  }
}
function br(t) {
  let e;
  for (; e = t.lastChild; )
    t.removeChild(e);
}
const WP = /(<(.*)>|&(.*);)/;
function pl(t, e) {
  let n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
  WP.test(e) ? t.innerHTML = n ? Sp(e) : e : Gs(t, e);
}
function Gs(t, e) {
  const n = t.firstChild;
  n && n.nodeType === 3 && n.nextSibling === null ? n.textContent = e : (br(t), t.appendChild(t.ownerDocument.createTextNode(e)));
}
function el(t) {
  const e = t.ownerDocument.documentElement;
  let n = t;
  for (; n !== e; ) {
    if (n === null)
      return !1;
    if (n.nodeType === Node.DOCUMENT_FRAGMENT_NODE)
      if (n.host) {
        if (n.host.impl)
          return el(n.host.impl);
        if (n.host)
          return el(n.host);
        throw new Error("Lost in Web Components world");
      } else
        return !1;
    else if (uo(n).display === "none")
      return !1;
    n = n.parentNode;
  }
  return !0;
}
function gn(t) {
  const e = t.ownerDocument, n = e.defaultView, i = e.documentElement;
  let r = t, o, s, a;
  for (o = r.offsetLeft, s = r.offsetTop, a = r; (r = r.offsetParent) && !(r === e.body || !("offsetLeft" in r)); )
    o += r.offsetLeft, s += r.offsetTop, a = r;
  return a && a.style.position === "fixed" && (o += n.pageXOffset || i.scrollLeft, s += n.pageYOffset || i.scrollTop), {
    left: o,
    top: s
  };
}
function tb() {
  return (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : window).scrollY;
}
function Rp() {
  return (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : window).scrollX;
}
function Ep(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : window;
  return t === e ? tb(e) : t.scrollTop;
}
function nb(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : window;
  return t === e ? Rp(e) : t.scrollLeft;
}
function Dh(t) {
  let e = t.ownerDocument, n = e ? e.defaultView : void 0;
  e || (e = t.document ? t.document : t, n = e.defaultView);
  const i = ["auto", "scroll"];
  let r = t.parentNode;
  for (; r && r.style && e.body !== r; ) {
    let {
      overflow: o,
      overflowX: s,
      overflowY: a
    } = r.style;
    if ([o, s, a].includes("scroll"))
      return r;
    if ({
      overflow: o,
      overflowX: s,
      overflowY: a
    } = n.getComputedStyle(r), i.includes(o) || i.includes(s) || i.includes(a) || r.clientHeight <= r.scrollHeight + 1 && (i.includes(a) || i.includes(o)) || r.clientWidth <= r.scrollWidth + 1 && (i.includes(s) || i.includes(o)))
      return r;
    r = r.parentNode;
  }
  return n;
}
function $P(t) {
  return t.scrollHeight - t.clientHeight;
}
function UP(t) {
  return t.scrollWidth - t.clientWidth;
}
function tl(t) {
  const e = t.ownerDocument, n = e.defaultView;
  let i = t.parentNode;
  for (; i && i.style && e.body !== i; ) {
    if (i.style.overflow !== "visible" && i.style.overflow !== "")
      return i;
    const r = uo(i, n), o = ["scroll", "hidden", "auto"], s = r.getPropertyValue("overflow"), a = r.getPropertyValue("overflow-y"), l = r.getPropertyValue("overflow-x");
    if (o.includes(s) || o.includes(a) || o.includes(l))
      return i;
    i = i.parentNode;
  }
  return n;
}
function Eu(t, e) {
  let n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : window;
  if (t) {
    if (t === n)
      return e === "width" ? `${n.innerWidth}px` : e === "height" ? `${n.innerHeight}px` : void 0;
  } else
    return;
  const i = t.style[e];
  if (i !== "" && i !== void 0)
    return i;
  const r = uo(t, n);
  if (r[e] !== "" && r[e] !== void 0)
    return r[e];
}
function LG(t, e) {
  const {
    selectorText: n
  } = e;
  let i = !1;
  return e.type === CSSRule.STYLE_RULE && n && (t.msMatchesSelector ? i = t.msMatchesSelector(n) : t.matches && (i = t.matches(n))), i;
}
function uo(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : window;
  return t.currentStyle || e.getComputedStyle(t);
}
function It(t) {
  return t.offsetWidth;
}
function Zt(t) {
  return t.offsetHeight;
}
function Ya(t) {
  return t.clientHeight || t.innerHeight;
}
function ib(t) {
  return t.clientWidth || t.innerWidth;
}
function BG(t, e, n) {
  t.addEventListener(e, n, !1);
}
function FG(t, e, n) {
  t.removeEventListener(e, n, !1);
}
function rb(t) {
  return t.selectionStart ? t.selectionStart : 0;
}
function YP(t) {
  return t.selectionEnd ? t.selectionEnd : 0;
}
function VG() {
  let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : window;
  const e = t.document;
  let n = "";
  return t.getSelection ? n = t.getSelection().toString() : e.selection && e.selection.type !== "Control" && (n = e.selection.createRange().text), n;
}
function wy() {
  let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : window;
  t.getSelection && (t.getSelection().empty ? t.getSelection().empty() : t.getSelection().removeAllRanges && t.getSelection().removeAllRanges());
}
function Cd(t, e, n) {
  if (n === void 0 && (n = e), t.setSelectionRange) {
    t.focus();
    try {
      t.setSelectionRange(e, n);
    } catch (i) {
      const r = t.parentNode, o = r.style.display;
      r.style.display = "block", t.setSelectionRange(e, n), r.style.display = o;
    }
  }
}
let KA;
function WG() {
  let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : document;
  const e = t.createElement("div");
  e.style.height = "200px", e.style.width = "100%";
  const n = t.createElement("div");
  n.style.boxSizing = "content-box", n.style.height = "150px", n.style.left = "0px", n.style.overflow = "hidden", n.style.position = "absolute", n.style.top = "0px", n.style.width = "200px", n.style.visibility = "hidden", n.appendChild(e), (t.body || t.documentElement).appendChild(n);
  const i = e.offsetWidth;
  n.style.overflow = "scroll";
  let r = e.offsetWidth;
  return i === r && (r = n.clientWidth), (t.body || t.documentElement).removeChild(n), i - r;
}
function pn() {
  let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : document;
  return KA === void 0 && (KA = WG(t)), KA;
}
function jP(t) {
  return t.offsetWidth !== t.clientWidth;
}
function GP(t) {
  return t.offsetHeight !== t.clientHeight;
}
function Tp(t, e, n) {
  t.style.transform = `translate3d(${e},${n},0)`;
}
function $G(t) {
  let e;
  return t.style.transform && (e = t.style.transform) !== "" ? ["transform", e] : -1;
}
function vd(t) {
  t.style.transform && t.style.transform !== "" && (t.style.transform = "");
}
function ah(t) {
  return t && (["INPUT", "SELECT", "TEXTAREA"].indexOf(t.nodeName) > -1 || t.contentEditable === "true");
}
function Mp(t) {
  return ah(t) && t.hasAttribute("data-hot-input") === !1;
}
function UG(t) {
  const e = t.ownerDocument.activeElement;
  Mp(e) || t.select();
}
function ob(t) {
  return !t.parentNode;
}
function zP(t, e) {
  new IntersectionObserver((i, r) => {
    i.forEach((o) => {
      o.isIntersecting && t.offsetParent !== null && (e(), r.unobserve(t));
    });
  }, {
    root: t.ownerDocument.body
  }).observe(t);
}
function sb(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
  const i = t.ownerDocument, r = i.createRange(), o = i.defaultView.getSelection();
  Ee(t, "contenteditable", !0), n && Ee(t, ...gl()), e && Q(t, "invisibleSelection"), r.selectNodeContents(t), o.removeAllRanges(), o.addRange(r);
}
function ab(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
  const n = t.ownerDocument.defaultView.getSelection();
  t.hasAttribute("aria-hidden") && t.removeAttribute("aria-hidden"), n.removeAllRanges(), e && xe(t, "invisibleSelection"), t.removeAttribute("contenteditable");
}
function KP(t, e) {
  let n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
  sb(t, n), e(), ab(t, n);
}
const YG = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  HTML_CHARACTERS: WP,
  addClass: Q,
  addEvent: BG,
  clearTextSelection: wy,
  closest: js,
  closestDown: qc,
  empty: br,
  fastInnerHTML: pl,
  fastInnerText: Gs,
  getCaretPosition: rb,
  getComputedStyle: uo,
  getCssTransform: $G,
  getFrameElement: LP,
  getMaximumScrollLeft: UP,
  getMaximumScrollTop: $P,
  getParent: DP,
  getParentWindow: Ja,
  getScrollLeft: nb,
  getScrollTop: Ep,
  getScrollableElement: Dh,
  getScrollbarWidth: pn,
  getSelectionEndPosition: YP,
  getSelectionText: VG,
  getStyle: Eu,
  getTrimmingContainer: tl,
  getWindowScrollLeft: Rp,
  getWindowScrollTop: tb,
  hasAccessToParentWindow: kG,
  hasClass: Te,
  hasHorizontalScrollbar: GP,
  hasVerticalScrollbar: jP,
  index: BP,
  innerHeight: Ya,
  innerWidth: ib,
  isChildOf: Vs,
  isDetached: ob,
  isInput: ah,
  isOutsideInput: Mp,
  isThisHotChild: kP,
  isVisible: el,
  makeElementContentEditableAndSelectItsContent: sb,
  matchesCSSRules: LG,
  observeVisibilityChangeOnce: zP,
  offset: gn,
  outerHeight: Zt,
  outerWidth: It,
  overlayContainsElement: ys,
  removeAttribute: ml,
  removeClass: xe,
  removeContentEditableFromElementAndDeselect: ab,
  removeEvent: FG,
  removeTextNodes: eb,
  resetCssTransform: vd,
  runWithSelectedContendEditableElement: KP,
  selectElementIfAllowed: UG,
  setAttribute: Ee,
  setCaretPosition: Cd,
  setOverlayPosition: Tp
}, Symbol.toStringTag, { value: "Module" }));
function Et(t) {
  return typeof t == "function";
}
function QP(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 200, n = 0;
  const i = {
    lastCallThrottled: !0
  };
  let r = null;
  function o() {
    for (var s = arguments.length, a = new Array(s), l = 0; l < s; l++)
      a[l] = arguments[l];
    const u = Date.now();
    let c = !1;
    i.lastCallThrottled = !0, n || (n = u, c = !0);
    const h = e - (u - n);
    return c ? (i.lastCallThrottled = !1, t.apply(this, a)) : (r && clearTimeout(r), r = setTimeout(() => {
      i.lastCallThrottled = !1, t.apply(this, a), n = 0, r = void 0;
    }, h)), i;
  }
  return o;
}
function jG(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 200, n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 10;
  const i = QP(t, e);
  let r = n;
  function o() {
    r = n;
  }
  function s() {
    for (var a = arguments.length, l = new Array(a), u = 0; u < a; u++)
      l[u] = arguments[u];
    return r ? (r -= 1, t.apply(this, l)) : i.apply(this, l);
  }
  return s.clearHits = o, s;
}
function Ip(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 200, n = null, i;
  function r() {
    for (var o = arguments.length, s = new Array(o), a = 0; a < o; a++)
      s[a] = arguments[a];
    return n && clearTimeout(n), n = setTimeout(() => {
      i = t.apply(this, s);
    }, e), i;
  }
  return r;
}
function GG() {
  for (var t = arguments.length, e = new Array(t), n = 0; n < t; n++)
    e[n] = arguments[n];
  const [i, ...r] = e;
  return function() {
    for (var s = arguments.length, a = new Array(s), l = 0; l < s; l++)
      a[l] = arguments[l];
    return rn(r, (u, c) => c(u), i.apply(this, a));
  };
}
function lb(t) {
  for (var e = arguments.length, n = new Array(e > 1 ? e - 1 : 0), i = 1; i < e; i++)
    n[i - 1] = arguments[i];
  return function() {
    for (var o = arguments.length, s = new Array(o), a = 0; a < o; a++)
      s[a] = arguments[a];
    return t.apply(this, n.concat(s));
  };
}
function XP(t) {
  const e = t.length;
  function n(i) {
    return function() {
      for (var o = arguments.length, s = new Array(o), a = 0; a < o; a++)
        s[a] = arguments[a];
      const l = i.concat(s);
      let u;
      return l.length >= e ? u = t.apply(this, l) : u = n(l), u;
    };
  }
  return n([]);
}
function zG(t) {
  const e = t.length;
  function n(i) {
    return function() {
      for (var o = arguments.length, s = new Array(o), a = 0; a < o; a++)
        s[a] = arguments[a];
      const l = i.concat(s.reverse());
      let u;
      return l.length >= e ? u = t.apply(this, l) : u = n(l), u;
    };
  }
  return n([]);
}
function Am(t, e, n, i, r, o, s, a) {
  return de(a) ? t.call(e, n, i, r, o, s, a) : de(s) ? t.call(e, n, i, r, o, s) : de(o) ? t.call(e, n, i, r, o) : de(r) ? t.call(e, n, i, r) : de(i) ? t.call(e, n, i) : de(n) ? t.call(e, n) : t.call(e);
}
const KG = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  curry: XP,
  curryRight: zG,
  debounce: Ip,
  fastCall: Am,
  isFunction: Et,
  partial: lb,
  pipe: GG,
  throttle: QP,
  throttleAfterHits: jG
}, Symbol.toStringTag, { value: "Module" }));
var QG = an, yE = dO, XG = Tn, SE = al, qG = cp, bE = QG([].push), ZG = function(t) {
  if (XG(t))
    return t;
  if (yE(t)) {
    for (var e = t.length, n = [], i = 0; i < e; i++) {
      var r = t[i];
      typeof r == "string" ? bE(n, r) : (typeof r == "number" || SE(r) === "Number" || SE(r) === "String") && bE(n, qG(r));
    }
    var o = n.length, s = !0;
    return function(a, l) {
      if (s)
        return s = !1, l;
      if (yE(this))
        return l;
      for (var u = 0; u < o; u++)
        if (n[u] === a)
          return l;
    };
  }
}, JG = Mn, qP = xu, ZP = up, ez = Tr, yd = an, JP = fi, RE = Tn, EE = hS, ex = RS, tz = ZG, nz = cS, iz = String, Ws = qP("JSON", "stringify"), tf = yd(/./.exec), TE = yd("".charAt), rz = yd("".charCodeAt), oz = yd("".replace), sz = yd(1 .toString), az = /[\uD800-\uDFFF]/g, ME = /^[\uD800-\uDBFF]$/, IE = /^[\uDC00-\uDFFF]$/, OE = !nz || JP(function() {
  var t = qP("Symbol")("stringify detection");
  return Ws([t]) !== "[null]" || Ws({ a: t }) !== "{}" || Ws(Object(t)) !== "{}";
}), PE = JP(function() {
  return Ws("\uDF06\uD834") !== '"\\udf06\\ud834"' || Ws("\uDEAD") !== '"\\udead"';
}), lz = function(t, e) {
  var n = ex(arguments), i = tz(e);
  if (!(!RE(i) && (t === void 0 || EE(t))))
    return n[1] = function(r, o) {
      if (RE(i) && (o = ez(i, this, iz(r), o)), !EE(o))
        return o;
    }, ZP(Ws, null, n);
}, uz = function(t, e, n) {
  var i = TE(n, e - 1), r = TE(n, e + 1);
  return tf(ME, t) && !tf(IE, r) || tf(IE, t) && !tf(ME, i) ? "\\u" + sz(rz(t, 0), 16) : t;
};
Ws && JG({ target: "JSON", stat: !0, arity: 3, forced: OE || PE }, {
  // eslint-disable-next-line no-unused-vars -- required for `.length`
  stringify: function(e, n, i) {
    var r = ex(arguments), o = ZP(OE ? lz : Ws, null, r);
    return PE && typeof o == "string" ? oz(o, az, uz) : o;
  }
});
function Tu(t) {
  let e;
  return Array.isArray(t) ? e = t.length ? new Array(t.length).fill(null) : [] : (e = {}, Fe(t, (n, i) => {
    i !== "__children" && (n && typeof n == "object" && !Array.isArray(n) ? e[i] = Tu(n) : Array.isArray(n) ? n.length && typeof n[0] == "object" && !Array.isArray(n[0]) ? e[i] = [Tu(n[0])] : e[i] = [] : e[i] = null);
  })), e;
}
function Ir(t, e) {
  return e.prototype.constructor = e, t.prototype = new e(), t.prototype.constructor = t, t;
}
function nn(t, e, n) {
  const i = Array.isArray(n);
  return Fe(e, (r, o) => {
    (i === !1 || n.includes(o)) && (t[o] = r);
  }), t;
}
function Sd(t, e) {
  Fe(e, (n, i) => {
    e[i] && typeof e[i] == "object" ? (t[i] || (Array.isArray(e[i]) ? t[i] = [] : Object.prototype.toString.call(e[i]) === "[object Date]" ? t[i] = e[i] : t[i] = {}), Sd(t[i], e[i])) : t[i] = e[i];
  });
}
function Fn(t) {
  return typeof t == "object" ? JSON.parse(JSON.stringify(t)) : t;
}
function Rr(t) {
  const e = {};
  return Fe(t, (n, i) => {
    e[i] = n;
  }), e;
}
function pt(t) {
  t.MIXINS || (t.MIXINS = []);
  for (var e = arguments.length, n = new Array(e > 1 ? e - 1 : 0), i = 1; i < e; i++)
    n[i - 1] = arguments[i];
  return T(n, (r) => {
    t.MIXINS.push(r.MIXIN_NAME), Fe(r, (o, s) => {
      if (t.prototype[s] !== void 0)
        throw new Error(`Mixin conflict. Property '${s}' already exist and cannot be overwritten.`);
      if (typeof o == "function")
        t.prototype[s] = o;
      else {
        const a = function(c, h) {
          const d = `_${c}`, g = (m) => {
            let w = m;
            return (Array.isArray(w) || Be(w)) && (w = Fn(w)), w;
          };
          return function() {
            return this[d] === void 0 && (this[d] = g(h)), this[d];
          };
        }, l = function(c) {
          const h = `_${c}`;
          return function(d) {
            this[h] = d;
          };
        };
        Object.defineProperty(t.prototype, s, {
          get: a(s, o),
          set: l(s),
          configurable: !0
        });
      }
    });
  }), t;
}
function Op(t, e) {
  return JSON.stringify(t) === JSON.stringify(e);
}
function Be(t) {
  return Object.prototype.toString.call(t) === "[object Object]";
}
function wo(t, e, n, i) {
  i.value = n, i.writable = i.writable !== !1, i.enumerable = i.enumerable !== !1, i.configurable = i.configurable !== !1, Object.defineProperty(t, e, i);
}
function Fe(t, e) {
  for (const n in t)
    if ((!t.hasOwnProperty || t.hasOwnProperty && Object.prototype.hasOwnProperty.call(t, n)) && e(t[n], n, t) === !1)
      break;
  return t;
}
function tx(t, e) {
  const n = e.split(".");
  let i = t;
  return Fe(n, (r) => {
    if (i = i[r], i === void 0)
      return i = void 0, !1;
  }), i;
}
function Vg(t, e, n) {
  const i = e.split(".");
  let r = t;
  i.forEach((o, s) => {
    s !== i.length - 1 ? (Bt(r, o) || (r[o] = {}), r = r[o]) : r[o] = n;
  });
}
function Pp(t) {
  if (!Be(t))
    return 0;
  const e = function(n) {
    let i = 0;
    return Be(n) ? Fe(n, (r, o) => {
      o !== "__children" && (i += e(r));
    }) : i += 1, i;
  };
  return e(t);
}
function ji(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "value";
  const n = `_${e}`, i = {
    _touched: !1,
    [n]: t,
    isTouched() {
      return this._touched;
    }
  };
  return Object.defineProperty(i, e, {
    get() {
      return this[n];
    },
    set(r) {
      this._touched = !0, this[n] = r;
    },
    enumerable: !0,
    configurable: !0
  }), i;
}
function Bt(t, e) {
  return Object.prototype.hasOwnProperty.call(t, e);
}
const cz = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  clone: Rr,
  createObjectPropListener: ji,
  deepClone: Fn,
  deepExtend: Sd,
  deepObjectSize: Pp,
  defineGetter: wo,
  duckSchema: Tu,
  extend: nn,
  getProperty: tx,
  hasOwnProperty: Bt,
  inherit: Ir,
  isObject: Be,
  isObjectEqual: Op,
  mixin: pt,
  objectEach: Fe,
  setProperty: Vg
}, Symbol.toStringTag, { value: "Module" }));
function xp(t) {
  return window.requestAnimationFrame(t);
}
function kh(t) {
  window.cancelAnimationFrame(t);
}
function ub() {
  return "ontouchstart" in window;
}
function nx() {
  return typeof window != "undefined";
}
let Sl;
function ix(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
  return Sl || (typeof Intl == "object" ? Sl = new Intl.Collator(t, e).compare : typeof String.prototype.localeCompare == "function" ? Sl = (n, i) => `${n}`.localeCompare(i) : Sl = (n, i) => n === i ? 0 : n > i ? -1 : 1, Sl);
}
const hz = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  cancelAnimationFrame: kh,
  getComparisonFunction: ix,
  isCSR: nx,
  isTouchSupported: ub,
  requestAnimationFrame: xp
}, Symbol.toStringTag, { value: "Module" })), Li = (t) => {
  const e = {
    value: !1
  };
  return e.test = (n, i) => {
    e.value = t(n, i);
  }, e;
}, rs = {
  chrome: Li((t, e) => /Chrome/.test(t) && /Google/.test(e)),
  chromeWebKit: Li((t) => /CriOS/.test(t)),
  edge: Li((t) => /Edge/.test(t)),
  edgeWebKit: Li((t) => /EdgiOS/.test(t)),
  firefox: Li((t) => /Firefox/.test(t)),
  firefoxWebKit: Li((t) => /FxiOS/.test(t)),
  mobile: Li((t) => /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(t)),
  safari: Li((t, e) => /Safari/.test(t) && /Apple Computer/.test(e))
}, ku = {
  mac: Li((t) => /^Mac/.test(t)),
  win: Li((t) => /^Win/.test(t)),
  linux: Li((t) => /^Linux/.test(t)),
  ios: Li((t) => /iPhone|iPad|iPod/i.test(t))
};
function rx() {
  let {
    userAgent: t = navigator.userAgent,
    vendor: e = navigator.vendor
  } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
  Fe(rs, (n) => {
    let {
      test: i
    } = n;
    return void i(t, e);
  });
}
function ox() {
  let {
    platform: t = navigator.platform
  } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
  Fe(ku, (e) => {
    let {
      test: n
    } = e;
    return void n(t);
  });
}
nx() && (rx(), ox());
function cb() {
  return rs.chrome.value;
}
function sx() {
  return rs.chromeWebKit.value;
}
function Wg() {
  return rs.firefox.value;
}
function ax() {
  return rs.firefoxWebKit.value;
}
function hb() {
  return rs.safari.value;
}
function lx() {
  return rs.edge.value;
}
function dz() {
  return rs.edgeWebKit.value;
}
function Yo() {
  return rs.mobile.value;
}
function db() {
  return ku.ios.value;
}
function fb() {
  let {
    maxTouchPoints: t
  } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : navigator;
  return t > 2 && ku.mac.value;
}
function Ay() {
  return ku.win.value;
}
function Lh() {
  return ku.mac.value;
}
function fz() {
  return ku.linux.value;
}
const gz = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  isChrome: cb,
  isChromeWebKit: sx,
  isEdge: lx,
  isEdgeWebKit: dz,
  isFirefox: Wg,
  isFirefoxWebKit: ax,
  isIOS: db,
  isIpadOS: fb,
  isLinuxOS: fz,
  isMacOS: Lh,
  isMobileBrowser: Yo,
  isSafari: hb,
  isWindowsOS: Ay,
  setBrowserMeta: rx,
  setPlatformMeta: ox
}, Symbol.toStringTag, { value: "Module" })), we = {
  ALT: 18,
  ARROW_DOWN: 40,
  ARROW_LEFT: 37,
  ARROW_RIGHT: 39,
  ARROW_UP: 38,
  AUDIO_DOWN: Wg() ? 182 : 174,
  AUDIO_MUTE: Wg() ? 181 : 173,
  AUDIO_UP: Wg() ? 183 : 175,
  BACKSPACE: 8,
  CAPS_LOCK: 20,
  COMMA: 188,
  COMMAND_LEFT: 91,
  COMMAND_RIGHT: 93,
  COMMAND_FIREFOX: 224,
  CONTROL: 17,
  DELETE: 46,
  END: 35,
  ENTER: 13,
  ESCAPE: 27,
  F1: 112,
  F2: 113,
  F3: 114,
  F4: 115,
  F5: 116,
  F6: 117,
  F7: 118,
  F8: 119,
  F9: 120,
  F10: 121,
  F11: 122,
  F12: 123,
  F13: 124,
  F14: 125,
  F15: 126,
  F16: 127,
  F17: 128,
  F18: 129,
  F19: 130,
  HOME: 36,
  INSERT: 45,
  MEDIA_NEXT: 176,
  MEDIA_PLAY_PAUSE: 179,
  MEDIA_PREV: 177,
  MEDIA_STOP: 178,
  NULL: 0,
  NUM_LOCK: 144,
  PAGE_DOWN: 34,
  PAGE_UP: 33,
  PAUSE: 19,
  PERIOD: 190,
  SCROLL_LOCK: 145,
  SHIFT: 16,
  SPACE: 32,
  TAB: 9,
  A: 65,
  C: 67,
  D: 68,
  F: 70,
  L: 76,
  O: 79,
  P: 80,
  S: 83,
  V: 86,
  X: 88,
  Y: 89,
  Z: 90
}, mz = [we.ALT, we.ARROW_DOWN, we.ARROW_LEFT, we.ARROW_RIGHT, we.ARROW_UP, we.AUDIO_DOWN, we.AUDIO_MUTE, we.AUDIO_UP, we.BACKSPACE, we.CAPS_LOCK, we.DELETE, we.END, we.ENTER, we.ESCAPE, we.F1, we.F2, we.F3, we.F4, we.F5, we.F6, we.F7, we.F8, we.F9, we.F10, we.F11, we.F12, we.F13, we.F14, we.F15, we.F16, we.F17, we.F18, we.F19, we.HOME, we.INSERT, we.MEDIA_NEXT, we.MEDIA_PLAY_PAUSE, we.MEDIA_PREV, we.MEDIA_STOP, we.NULL, we.NUM_LOCK, we.PAGE_DOWN, we.PAGE_UP, we.PAUSE, we.SCROLL_LOCK, we.SHIFT, we.TAB];
function ux(t) {
  return t === 32 || // space
  t >= 48 && t <= 57 || // 0-9
  t >= 96 && t <= 111 || // numpad
  t >= 186 && t <= 192 || // ;=,-./`
  t >= 219 && t <= 222 || // []{}\|"'
  t >= 226 || // special chars (229 for Asian chars)
  t >= 65 && t <= 90;
}
function gb(t) {
  return mz.includes(t);
}
function pz(t) {
  const e = [];
  return Lh() ? e.push(we.COMMAND_LEFT, we.COMMAND_RIGHT, we.COMMAND_FIREFOX) : e.push(we.CONTROL), e.includes(t);
}
function cx(t) {
  return [we.CONTROL, we.COMMAND_LEFT, we.COMMAND_RIGHT, we.COMMAND_FIREFOX].includes(t);
}
function Lu(t, e) {
  const n = e.split("|");
  let i = !1;
  return T(n, (r) => {
    if (t === we[r])
      return i = !0, !1;
  }), i;
}
const wz = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  KEY_CODES: we,
  isCtrlKey: pz,
  isCtrlMetaKey: cx,
  isFunctionKey: gb,
  isKey: Lu,
  isPrintableChar: ux
}, Symbol.toStringTag, { value: "Module" }));
function Zi(t) {
  t.isImmediatePropagationEnabled = !1, t.cancelBubble = !0;
}
function Os(t) {
  return t.isImmediatePropagationEnabled === !1;
}
function zs(t) {
  return t.button === 2;
}
function Hp(t) {
  return t.button === 0;
}
function hx(t) {
  return t instanceof TouchEvent;
}
function dx(t, e) {
  const n = {
    x: t.offsetX,
    y: t.offsetY
  };
  let i = t.target;
  if (!(e instanceof HTMLElement) || i !== e && i.contains(e))
    return n;
  for (; i !== e; )
    n.x += i.offsetLeft, n.y += i.offsetTop, i = i.offsetParent;
  return n;
}
const Az = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  isImmediatePropagationStopped: Os,
  isLeftClick: Hp,
  isRightClick: zs,
  isTouchEvent: hx,
  offsetRelativeTo: dx,
  stopImmediatePropagation: Zi
}, Symbol.toStringTag, { value: "Module" }));
var fx = {}, Cz = eO, vz = AS, yz = Object.keys || function(e) {
  return Cz(e, vz);
}, Sz = Er, bz = zI, Rz = qs, Ez = ts, Tz = rp, Mz = yz;
fx.f = Sz && !bz ? Object.defineProperties : function(e, n) {
  Ez(e);
  for (var i = Tz(n), r = Mz(n), o = r.length, s = 0, a; o > s; )
    Rz.f(e, a = r[s++], i[a]);
  return e;
};
var Iz = ts, Oz = fx, xE = AS, Pz = pS, xz = RO, Hz = gS, _z = XI, HE = ">", _E = "<", Cy = "prototype", vy = "script", gx = _z("IE_PROTO"), QA = function() {
}, mx = function(t) {
  return _E + vy + HE + t + _E + "/" + vy + HE;
}, NE = function(t) {
  t.write(mx("")), t.close();
  var e = t.parentWindow.Object;
  return t = null, e;
}, Nz = function() {
  var t = Hz("iframe"), e = "java" + vy + ":", n;
  return t.style.display = "none", xz.appendChild(t), t.src = String(e), n = t.contentWindow.document, n.open(), n.write(mx("document.F=Object")), n.close(), n.F;
}, nf, $g = function() {
  try {
    nf = new ActiveXObject("htmlfile");
  } catch (e) {
  }
  $g = typeof document != "undefined" ? document.domain && nf ? NE(nf) : Nz() : NE(nf);
  for (var t = xE.length; t--; )
    delete $g[Cy][xE[t]];
  return $g();
};
Pz[gx] = !0;
var Dz = Object.create || function(e, n) {
  var i;
  return e !== null ? (QA[Cy] = Iz(e), i = new QA(), QA[Cy] = null, i[gx] = e) : i = $g(), n === void 0 ? i : Oz.f(i, n);
}, kz = Xs, Lz = Dz, Bz = qs.f, yy = kz("unscopables"), Sy = Array.prototype;
Sy[yy] === void 0 && Bz(Sy, yy, {
  configurable: !0,
  value: Lz(null)
});
var mb = function(t) {
  Sy[yy][t] = !0;
}, Fz = mb;
Fz("flatMap");
function gt() {
  de(console) && console.warn(...arguments);
}
function pb() {
  de(console) && console.error(...arguments);
}
function Vz(t, e, n) {
  return (e = Wz(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Wz(t) {
  var e = $z(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function $z(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const bl = [
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * Fired after resetting a cell's meta. This happens when the {@link Core#updateSettings} method is called.
   *
   * @event Hooks#afterCellMetaReset
   */
  "afterCellMetaReset",
  /**
   * Fired after one or more cells has been changed. The changes are triggered in any situation when the
   * value is entered using an editor or changed using API (e.q [`setDataAtCell`](@/api/core.md#setdataatcell) method).
   *
   * __Note:__ For performance reasons, the `changes` array is null for `"loadData"` source.
   *
   * @event Hooks#afterChange
   * @param {Array[]} changes 2D array containing information about each of the edited cells `[[row, prop, oldVal, newVal], ...]`. `row` is a visual row index.
   * @param {string} [source] String that identifies source of hook call ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   * @example
   * ::: only-for javascript
   * ```js
   * new Handsontable(element, {
   *   afterChange: (changes) => {
   *     changes?.forEach(([row, prop, oldValue, newValue]) => {
   *       // Some logic...
   *     });
   *   }
   * })
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * <HotTable
   *   afterChange={(changes, source) => {
   *     changes?.forEach(([row, prop, oldValue, newValue]) => {
   *       // Some logic...
   *     });
   *   }}
   * />
   * ```
   * :::
   */
  "afterChange",
  /**
   * Fired each time user opens {@link ContextMenu} and after setting up the Context Menu's default options. These options are a collection
   * which user can select by setting an array of keys or an array of objects in {@link Options#contextMenu} option.
   *
   * @event Hooks#afterContextMenuDefaultOptions
   * @param {Array} predefinedItems An array of objects containing information about the pre-defined Context Menu items.
   */
  "afterContextMenuDefaultOptions",
  /**
   * Fired each time user opens {@link ContextMenu} plugin before setting up the Context Menu's items but after filtering these options by
   * user ([`contextMenu`](@/api/options.md#contextmenu) option). This hook can by helpful to determine if user use specified menu item or to set up
   * one of the menu item to by always visible.
   *
   * @event Hooks#beforeContextMenuSetItems
   * @param {object[]} menuItems An array of objects containing information about to generated Context Menu items.
   */
  "beforeContextMenuSetItems",
  /**
   * Fired by {@link DropdownMenu} plugin after setting up the Dropdown Menu's default options. These options are a
   * collection which user can select by setting an array of keys or an array of objects in {@link Options#dropdownMenu}
   * option.
   *
   * @event Hooks#afterDropdownMenuDefaultOptions
   * @param {object[]} predefinedItems An array of objects containing information about the pre-defined Context Menu items.
   */
  "afterDropdownMenuDefaultOptions",
  /**
   * Fired by {@link DropdownMenu} plugin before setting up the Dropdown Menu's items but after filtering these options
   * by user ([`dropdownMenu`](@/api/options.md#dropdownmenu) option). This hook can by helpful to determine if user use specified menu item or to set
   * up one of the menu item to by always visible.
   *
   * @event Hooks#beforeDropdownMenuSetItems
   * @param {object[]} menuItems An array of objects containing information about to generated Dropdown Menu items.
   */
  "beforeDropdownMenuSetItems",
  /**
   * Fired by {@link ContextMenu} plugin after hiding the Context Menu. This hook is fired when {@link Options#contextMenu}
   * option is enabled.
   *
   * @event Hooks#afterContextMenuHide
   * @param {object} context The Context Menu plugin instance.
   */
  "afterContextMenuHide",
  /**
   * Fired by {@link ContextMenu} plugin before opening the Context Menu. This hook is fired when {@link Options#contextMenu}
   * option is enabled.
   *
   * @event Hooks#beforeContextMenuShow
   * @param {object} context The Context Menu instance.
   */
  "beforeContextMenuShow",
  /**
   * Fired by {@link ContextMenu} plugin after opening the Context Menu. This hook is fired when {@link Options#contextMenu}
   * option is enabled.
   *
   * @event Hooks#afterContextMenuShow
   * @param {object} context The Context Menu plugin instance.
   */
  "afterContextMenuShow",
  /**
   * Fired by {@link CopyPaste} plugin after reaching the copy limit while copying data. This hook is fired when
   * {@link Options#copyPaste} option is enabled.
   *
   * @event Hooks#afterCopyLimit
   * @param {number} selectedRows Count of selected copyable rows.
   * @param {number} selectedColumns Count of selected copyable columns.
   * @param {number} copyRowsLimit Current copy rows limit.
   * @param {number} copyColumnsLimit Current copy columns limit.
   */
  "afterCopyLimit",
  /**
   * Fired before created a new column.
   *
   * @event Hooks#beforeCreateCol
   * @param {number} index Represents the visual index of first newly created column in the data source array.
   * @param {number} amount Number of newly created columns in the data source array.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   * @returns {*} If `false` then creating columns is cancelled.
   * @example
   * ::: only-for javascript
   * ```js
   * // Return `false` to cancel column inserting.
   * new Handsontable(element, {
   *   beforeCreateCol: function(data, coords) {
   *     return false;
   *   }
   * });
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * // Return `false` to cancel column inserting.
   * <HotTable
   *   beforeCreateCol={(data, coords) => {
   *     return false;
   *   }}
   * />
   * ```
   * :::
   */
  "beforeCreateCol",
  /**
   * Fired after the order of columns has changed.
   * This hook is fired by changing column indexes of any type supported by the {@link IndexMapper}.
   *
   * @event Hooks#afterColumnSequenceChange
   * @param {'init'|'remove'|'insert'|'move'|'update'} [source] A string that indicates what caused the change to the order of columns.
   */
  "afterColumnSequenceChange",
  /**
   * Fired after created a new column.
   *
   * @event Hooks#afterCreateCol
   * @param {number} index Represents the visual index of first newly created column in the data source.
   * @param {number} amount Number of newly created columns in the data source.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   */
  "afterCreateCol",
  /**
   * Fired before created a new row.
   *
   * @event Hooks#beforeCreateRow
   * @param {number} index Represents the visual index of first newly created row in the data source array.
   * @param {number} amount Number of newly created rows in the data source array.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   * @returns {*|boolean} If false is returned the action is canceled.
   */
  "beforeCreateRow",
  /**
   * Fired after created a new row.
   *
   * @event Hooks#afterCreateRow
   * @param {number} index Represents the visual index of first newly created row in the data source array.
   * @param {number} amount Number of newly created rows in the data source array.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   */
  "afterCreateRow",
  /**
   * Fired after all selected cells are deselected.
   *
   * @event Hooks#afterDeselect
   */
  "afterDeselect",
  /**
   * Fired after destroying the Handsontable instance.
   *
   * @event Hooks#afterDestroy
   */
  "afterDestroy",
  /**
   * Hook fired after `keydown` event is handled.
   *
   * @event Hooks#afterDocumentKeyDown
   * @param {Event} event A native `keydown` event object.
   */
  "afterDocumentKeyDown",
  /**
   * Fired inside the Walkontable's selection `draw` method. Can be used to add additional class names to cells, depending on the current selection.
   *
   * @event Hooks#afterDrawSelection
   * @param {number} currentRow Row index of the currently processed cell.
   * @param {number} currentColumn Column index of the currently cell.
   * @param {number[]} cornersOfSelection Array of the current selection in a form of `[startRow, startColumn, endRow, endColumn]`.
   * @param {number|undefined} layerLevel Number indicating which layer of selection is currently processed.
   * @since 0.38.1
   * @returns {string|undefined} Can return a `String`, which will act as an additional `className` to be added to the currently processed cell.
   */
  "afterDrawSelection",
  /**
   * Fired inside the Walkontable's `refreshSelections` method. Can be used to remove additional class names from all cells in the table.
   *
   * @event Hooks#beforeRemoveCellClassNames
   * @since 0.38.1
   * @returns {string[]|undefined} Can return an `Array` of `String`s. Each of these strings will act like class names to be removed from all the cells in the table.
   */
  "beforeRemoveCellClassNames",
  /**
   * Fired after getting the cell settings.
   *
   * @event Hooks#afterGetCellMeta
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {object} cellProperties Object containing the cell properties.
   */
  "afterGetCellMeta",
  /**
   * Fired after retrieving information about a column header and appending it to the table header.
   *
   * @event Hooks#afterGetColHeader
   * @param {number} column Visual column index.
   * @param {HTMLTableCellElement} TH Header's TH element.
   * @param {number} [headerLevel=0] (Since 12.2.0) Header level index. Accepts positive (0 to n)
   *                                 and negative (-1 to -n) values. For positive values, 0 points to the
   *                                 topmost header. For negative values, -1 points to the bottom-most
   *                                 header (the header closest to the cells).
   */
  "afterGetColHeader",
  /**
   * Fired after retrieving information about a row header and appending it to the table header.
   *
   * @event Hooks#afterGetRowHeader
   * @param {number} row Visual row index.
   * @param {HTMLTableCellElement} TH Header's TH element.
   */
  "afterGetRowHeader",
  /**
   * Fired after the Handsontable instance is initiated.
   *
   * @event Hooks#afterInit
   */
  "afterInit",
  /**
   * Fired after Handsontable's [`data`](@/api/options.md#data)
   * gets modified by the [`loadData()`](@/api/core.md#loaddata) method
   * or the [`updateSettings()`](@/api/core.md#updatesettings) method.
   *
   * Read more:
   * - [Binding to data](@/guides/getting-started/binding-to-data/binding-to-data.md)
   * - [Saving data](@/guides/getting-started/saving-data/saving-data.md)
   *
   * @event Hooks#afterLoadData
   * @param {Array} sourceData An [array of arrays](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-arrays), or an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects), that contains Handsontable's data
   * @param {boolean} initialLoad A flag that indicates whether the data was loaded at Handsontable's initialization (`true`) or later (`false`)
   * @param {string} source The source of the call
   */
  "afterLoadData",
  /**
   * Fired after the [`updateData()`](@/api/core.md#updatedata) method
   * modifies Handsontable's [`data`](@/api/options.md#data).
   *
   * Read more:
   * - [Binding to data](@/guides/getting-started/binding-to-data/binding-to-data.md)
   * - [Saving data](@/guides/getting-started/saving-data/saving-data.md)
   *
   * @event Hooks#afterUpdateData
   * @since 11.1.0
   * @param {Array} sourceData An [array of arrays](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-arrays), or an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects), that contains Handsontable's data
   * @param {boolean} initialLoad A flag that indicates whether the data was loaded at Handsontable's initialization (`true`) or later (`false`)
   * @param {string} source The source of the call
   */
  "afterUpdateData",
  /**
   * Fired after a scroll event, which is identified as a momentum scroll (e.g. on an iPad).
   *
   * @event Hooks#afterMomentumScroll
   */
  "afterMomentumScroll",
  /**
   * Fired after a `mousedown` event is triggered on the cell corner (the drag handle).
   *
   * @event Hooks#afterOnCellCornerMouseDown
   * @param {Event} event `mousedown` event object.
   */
  "afterOnCellCornerMouseDown",
  /**
   * Fired after a `dblclick` event is triggered on the cell corner (the drag handle).
   *
   * @event Hooks#afterOnCellCornerDblClick
   * @param {Event} event `dblclick` event object.
   */
  "afterOnCellCornerDblClick",
  /**
   * Fired after clicking on a cell or row/column header. In case the row/column header was clicked, the coordinate
   * indexes are negative.
   *
   * For example clicking on the row header of cell (0, 0) results with `afterOnCellMouseDown` called
   * with coordinates `{row: 0, col: -1}`.
   *
   * @event Hooks#afterOnCellMouseDown
   * @param {Event} event `mousedown` event object.
   * @param {CellCoords} coords Coordinates object containing the visual row and visual column indexes of the clicked cell.
   * @param {HTMLTableCellElement} TD Cell's TD (or TH) element.
   */
  "afterOnCellMouseDown",
  /**
   * Fired after clicking on a cell or row/column header. In case the row/column header was clicked, the coordinate
   * indexes are negative.
   *
   * For example clicking on the row header of cell (0, 0) results with `afterOnCellMouseUp` called
   * with coordinates `{row: 0, col: -1}`.
   *
   * @event Hooks#afterOnCellMouseUp
   * @param {Event} event `mouseup` event object.
   * @param {CellCoords} coords Coordinates object containing the visual row and visual column indexes of the clicked cell.
   * @param {HTMLTableCellElement} TD Cell's TD (or TH) element.
   */
  "afterOnCellMouseUp",
  /**
   * Fired after clicking right mouse button on a cell or row/column header.
   *
   * For example clicking on the row header of cell (0, 0) results with `afterOnCellContextMenu` called
   * with coordinates `{row: 0, col: -1}`.
   *
   * @event Hooks#afterOnCellContextMenu
   * @since 4.1.0
   * @param {Event} event `contextmenu` event object.
   * @param {CellCoords} coords Coordinates object containing the visual row and visual column indexes of the clicked cell.
   * @param {HTMLTableCellElement} TD Cell's TD (or TH) element.
   */
  "afterOnCellContextMenu",
  /**
   * Fired after hovering a cell or row/column header with the mouse cursor. In case the row/column header was
   * hovered, the index is negative.
   *
   * For example, hovering over the row header of cell (0, 0) results with `afterOnCellMouseOver` called
   * with coords `{row: 0, col: -1}`.
   *
   * @event Hooks#afterOnCellMouseOver
   * @param {Event} event `mouseover` event object.
   * @param {CellCoords} coords Hovered cell's visual coordinate object.
   * @param {HTMLTableCellElement} TD Cell's TD (or TH) element.
   */
  "afterOnCellMouseOver",
  /**
   * Fired after leaving a cell or row/column header with the mouse cursor.
   *
   * @event Hooks#afterOnCellMouseOut
   * @param {Event} event `mouseout` event object.
   * @param {CellCoords} coords Leaved cell's visual coordinate object.
   * @param {HTMLTableCellElement} TD Cell's TD (or TH) element.
   */
  "afterOnCellMouseOut",
  /**
   * Fired after one or more columns are removed.
   *
   * @event Hooks#afterRemoveCol
   * @param {number} index Visual index of starter column.
   * @param {number} amount An amount of removed columns.
   * @param {number[]} physicalColumns An array of physical columns removed from the data source.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   */
  "afterRemoveCol",
  /**
   * Fired after one or more rows are removed.
   *
   * @event Hooks#afterRemoveRow
   * @param {number} index Visual index of starter row.
   * @param {number} amount An amount of removed rows.
   * @param {number[]} physicalRows An array of physical rows removed from the data source.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   */
  "afterRemoveRow",
  /**
   * Fired before starting rendering the cell.
   *
   * @event Hooks#beforeRenderer
   * @param {HTMLTableCellElement} TD Currently rendered cell's TD element.
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {string|number} prop Column property name or a column index, if datasource is an array of arrays.
   * @param {*} value Value of the rendered cell.
   * @param {object} cellProperties Object containing the cell's properties.
   */
  "beforeRenderer",
  /**
   * Fired after finishing rendering the cell (after the renderer finishes).
   *
   * @event Hooks#afterRenderer
   * @param {HTMLTableCellElement} TD Currently rendered cell's TD element.
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {string|number} prop Column property name or a column index, if datasource is an array of arrays.
   * @param {*} value Value of the rendered cell.
   * @param {object} cellProperties Object containing the cell's properties.
   */
  "afterRenderer",
  /**
   * Fired after the order of rows has changed.
   * This hook is fired by changing row indexes of any type supported by the {@link IndexMapper}.
   *
   * @event Hooks#afterRowSequenceChange
   * @param {'init'|'remove'|'insert'|'move'|'update'} [source] A string that indicates what caused the change to the order of rows.
   */
  "afterRowSequenceChange",
  /**
   * Fired before the vertical viewport scroll. Triggered by the [`scrollViewportTo()`](@/api/core.md#scrollviewportto)
   * method or table internals.
   *
   * @since 14.0.0
   * @event Hooks#beforeViewportScrollVertically
   * @param {number} visualRow Visual row index.
   * @returns {number | boolean} Returns modified row index (or the same as passed in the method argument) to which
   * the viewport will be scrolled. If the returned value is `false`, the scrolling will be canceled.
   */
  "beforeViewportScrollVertically",
  /**
   * Fired before the horizontal viewport scroll. Triggered by the [`scrollViewportTo()`](@/api/core.md#scrollviewportto)
   * method or table internals.
   *
   * @since 14.0.0
   * @event Hooks#beforeViewportScrollHorizontally
   * @param {number} visualColumn Visual column index.
   * @returns {number | boolean} Returns modified column index (or the same as passed in the method argument) to which
   * the viewport will be scrolled. If the returned value is `false`, the scrolling will be canceled.
   */
  "beforeViewportScrollHorizontally",
  /**
   * Fired before the vertical or horizontal viewport scroll. Triggered by the [`scrollViewportTo()`](@/api/core.md#scrollviewportto)
   * method or table internals.
   *
   * @since 14.0.0
   * @event Hooks#beforeViewportScroll
   */
  "beforeViewportScroll",
  /**
   * Fired after the horizontal scroll event.
   *
   * @event Hooks#afterScrollHorizontally
   */
  "afterScrollHorizontally",
  /**
   * Fired after the vertical scroll event.
   *
   * @event Hooks#afterScrollVertically
   */
  "afterScrollVertically",
  /**
   * Fired after the vertical or horizontal scroll event.
   *
   * @since 14.0.0
   * @event Hooks#afterScroll
   */
  "afterScroll",
  /**
   * Fired after one or more cells are selected (e.g. during mouse move).
   *
   * @event Hooks#afterSelection
   * @param {number} row Selection start visual row index.
   * @param {number} column Selection start visual column index.
   * @param {number} row2 Selection end visual row index.
   * @param {number} column2 Selection end visual column index.
   * @param {object} preventScrolling A reference to the observable object with the `value` property.
   *                                  Property `preventScrolling.value` expects a boolean value that
   *                                  Handsontable uses to control scroll behavior after selection.
   * @param {number} selectionLayerLevel The number which indicates what selection layer is currently modified.
   * @example
   * ::: only-for javascript
   * ```js
   * new Handsontable(element, {
   *   afterSelection: (row, column, row2, column2, preventScrolling, selectionLayerLevel) => {
   *     // If set to `false` (default): when cell selection is outside the viewport,
   *     // Handsontable scrolls the viewport to cell selection's end corner.
   *     // If set to `true`: when cell selection is outside the viewport,
   *     // Handsontable doesn't scroll to cell selection's end corner.
   *     preventScrolling.value = true;
   *   }
   * })
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * <HotTable
   *   afterSelection={(row, column, row2, column2, preventScrolling, selectionLayerLevel) => {
   *     // If set to `false` (default): when cell selection is outside the viewport,
   *     // Handsontable scrolls the viewport to cell selection's end corner.
   *     // If set to `true`: when cell selection is outside the viewport,
   *     // Handsontable doesn't scroll to cell selection's end corner.
   *     preventScrolling.value = true;
   *   }}
   * />
   * ```
   * :::
   */
  "afterSelection",
  /**
   * Fired after one or more cells are selected.
   *
   * The `prop` and `prop2` arguments represent the source object property name instead of the column number.
   *
   * @event Hooks#afterSelectionByProp
   * @param {number} row Selection start visual row index.
   * @param {string} prop Selection start data source object property name.
   * @param {number} row2 Selection end visual row index.
   * @param {string} prop2 Selection end data source object property name.
   * @param {object} preventScrolling A reference to the observable object with the `value` property.
   *                                  Property `preventScrolling.value` expects a boolean value that
   *                                  Handsontable uses to control scroll behavior after selection.
   * @param {number} selectionLayerLevel The number which indicates what selection layer is currently modified.
   * @example
   * ```js
   * ::: only-for javascript
   * new Handsontable(element, {
   *   afterSelectionByProp: (row, column, row2, column2, preventScrolling, selectionLayerLevel) => {
   *     // setting if prevent scrolling after selection
   *     preventScrolling.value = true;
   *   }
   * })
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * <HotTable
   *   afterSelectionByProp={(row, column, row2, column2, preventScrolling, selectionLayerLevel) => {
   *     // setting if prevent scrolling after selection
   *     preventScrolling.value = true;
   *   }}
   * />
   * ```
   * :::
   */
  "afterSelectionByProp",
  /**
   * Fired after one or more cells are selected (e.g. on mouse up).
   *
   * @event Hooks#afterSelectionEnd
   * @param {number} row Selection start visual row index.
   * @param {number} column Selection start visual column index.
   * @param {number} row2 Selection end visual row index.
   * @param {number} column2 Selection end visual column index.
   * @param {number} selectionLayerLevel The number which indicates what selection layer is currently modified.
   */
  "afterSelectionEnd",
  /**
   * Fired after one or more cells are selected (e.g. on mouse up).
   *
   * The `prop` and `prop2` arguments represent the source object property name instead of the column number.
   *
   * @event Hooks#afterSelectionEndByProp
   * @param {number} row Selection start visual row index.
   * @param {string} prop Selection start data source object property index.
   * @param {number} row2 Selection end visual row index.
   * @param {string} prop2 Selection end data source object property index.
   * @param {number} selectionLayerLevel The number which indicates what selection layer is currently modified.
   */
  "afterSelectionEndByProp",
  /**
   * Fired after the focus position within a selected range is changed.
   *
   * @since 14.3.0
   * @event Hooks#afterSelectionFocusSet
   * @param {number} row The focus visual row index position.
   * @param {number} column The focus visual column index position.
   * @param {object} preventScrolling A reference to the observable object with the `value` property.
   *                                  Property `preventScrolling.value` expects a boolean value that
   *                                  Handsontable uses to control scroll behavior after selection.
   * @example
   * ```js
   * ::: only-for javascript
   * new Handsontable(element, {
   *   afterSelectionFocusSet: (row, column, preventScrolling) => {
   *     // If set to `false` (default): when focused cell selection is outside the viewport,
   *     // Handsontable scrolls the viewport to that cell.
   *     // If set to `true`: when focused cell selection is outside the viewport,
   *     // Handsontable doesn't scroll the viewport.
   *     preventScrolling.value = true;
   *   }
   * })
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * <HotTable
   *   afterSelectionFocusSet={(row, column, preventScrolling) => {
   *     // If set to `false` (default): when focused cell selection is outside the viewport,
   *     // Handsontable scrolls the viewport to that cell.
   *     // If set to `true`: when focused cell selection is outside the viewport,
   *     // Handsontable doesn't scroll the viewport.
   *     preventScrolling.value = true;
   *   }}
   * />
   * ```
   * :::
   */
  "afterSelectionFocusSet",
  /**
   * Fired before one or more columns are selected (e.g. During mouse header click or {@link Core#selectColumns} API call).
   *
   * @since 14.0.0
   * @event Hooks#beforeSelectColumns
   * @param {CellCoords} from Selection start coords object.
   * @param {CellCoords} to Selection end coords object.
   * @param {CellCoords} highlight Selection cell focus coords object.
   * @example
   * ::: only-for javascript
   * ```js
   * new Handsontable(element, {
   *   beforeSelectColumns: (from, to, highlight) => {
   *     // Extend the column selection by one column left and one column right.
   *     from.col = Math.max(from.col - 1, 0);
   *     to.col = Math.min(to.col + 1, this.countCols() - 1);
   *   }
   * })
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * <HotTable
   *   beforeSelectColumns={(from, to, highlight) => {
   *     // Extend the column selection by one column left and one column right.
   *     from.col = Math.max(from.col - 1, 0);
   *     to.col = Math.min(to.col + 1, this.countCols() - 1);
   *   }}
   * />
   * ```
   * :::
   */
  "beforeSelectColumns",
  /**
   * Fired after one or more columns are selected (e.g. during mouse header click or {@link Core#selectColumns} API call).
   *
   * @since 14.0.0
   * @event Hooks#afterSelectColumns
   * @param {CellCoords} from Selection start coords object.
   * @param {CellCoords} to Selection end coords object.
   * @param {CellCoords} highlight Selection cell focus coords object.
   */
  "afterSelectColumns",
  /**
   * Fired before one or more rows are selected (e.g. during mouse header click or {@link Core#selectRows} API call).
   *
   * @since 14.0.0
   * @event Hooks#beforeSelectRows
   * @param {CellCoords} from Selection start coords object.
   * @param {CellCoords} to Selection end coords object.
   * @param {CellCoords} highlight Selection cell focus coords object.
   * @example
   * ::: only-for javascript
   * ```js
   * new Handsontable(element, {
   *   beforeSelectRows: (from, to, highlight) => {
   *     // Extend the row selection by one row up and one row bottom more.
   *     from.row = Math.max(from.row - 1, 0);
   *     to.row = Math.min(to.row + 1, this.countRows() - 1);
   *   }
   * })
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * <HotTable
   *   beforeSelectRows={(from, to, highlight) => {
   *     // Extend the row selection by one row up and one row bottom more.
   *     from.row = Math.max(from.row - 1, 0);
   *     to.row = Math.min(to.row + 1, this.countRows() - 1);
   *   }}
   * />
   * ```
   * :::
   */
  "beforeSelectRows",
  /**
   * Fired after one or more rows are selected (e.g. during mouse header click or {@link Core#selectRows} API call).
   *
   * @since 14.0.0
   * @event Hooks#afterSelectRows
   * @param {CellCoords} from Selection start coords object.
   * @param {CellCoords} to Selection end coords object.
   * @param {CellCoords} highlight Selection cell focus coords object.
   */
  "afterSelectRows",
  /**
   * Fired after cell meta is changed.
   *
   * @event Hooks#afterSetCellMeta
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {string} key The updated meta key.
   * @param {*} value The updated meta value.
   */
  "afterSetCellMeta",
  /**
   * Fired after cell meta is removed.
   *
   * @event Hooks#afterRemoveCellMeta
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {string} key The removed meta key.
   * @param {*} value Value which was under removed key of cell meta.
   */
  "afterRemoveCellMeta",
  /**
   * Fired after cell data was changed.
   *
   * @event Hooks#afterSetDataAtCell
   * @param {Array} changes An array of changes in format `[[row, column, oldValue, value], ...]`.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   */
  "afterSetDataAtCell",
  /**
   * Fired after cell data was changed.
   * Called only when [`setDataAtRowProp`](@/api/core.md#setdataatrowprop) was executed.
   *
   * @event Hooks#afterSetDataAtRowProp
   * @param {Array} changes An array of changes in format `[[row, prop, oldValue, value], ...]`.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   */
  "afterSetDataAtRowProp",
  /**
   * Fired after cell source data was changed.
   *
   * @event Hooks#afterSetSourceDataAtCell
   * @since 8.0.0
   * @param {Array} changes An array of changes in format `[[row, column, oldValue, value], ...]`.
   * @param {string} [source] String that identifies source of hook call.
   */
  "afterSetSourceDataAtCell",
  /**
   * Fired after calling the [`updateSettings`](@/api/core.md#updatesettings) method.
   *
   * @event Hooks#afterUpdateSettings
   * @param {object} newSettings New settings object.
   */
  "afterUpdateSettings",
  /**
   * @description
   * A plugin hook executed after validator function, only if validator function is defined.
   * Validation result is the first parameter. This can be used to determinate if validation passed successfully or not.
   *
   * __Returning false from the callback will mark the cell as invalid__.
   *
   * @event Hooks#afterValidate
   * @param {boolean} isValid `true` if valid, `false` if not.
   * @param {*} value The value in question.
   * @param {number} row Visual row index.
   * @param {string|number} prop Property name / visual column index.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   * @returns {undefined | boolean} If `false` the cell will be marked as invalid, `true` otherwise.
   */
  "afterValidate",
  /**
   * Fired before successful change of language (when proper language code was set).
   *
   * @event Hooks#beforeLanguageChange
   * @since 0.35.0
   * @param {string} languageCode New language code.
   */
  "beforeLanguageChange",
  /**
   * Fired after successful change of language (when proper language code was set).
   *
   * @event Hooks#afterLanguageChange
   * @since 0.35.0
   * @param {string} languageCode New language code.
   */
  "afterLanguageChange",
  /**
   * Fired by {@link Autofill} plugin before populating the data in the autofill feature. This hook is fired when
   * {@link Options#fillHandle} option is enabled.
   *
   * @event Hooks#beforeAutofill
   * @param {Array[]} selectionData Data the autofill operation will start from.
   * @param {CellRange} sourceRange The range values will be filled from.
   * @param {CellRange} targetRange The range new values will be filled into.
   * @param {string} direction Declares the direction of the autofill. Possible values: `up`, `down`, `left`, `right`.
   *
   * @returns {boolean|Array[]} If false, the operation is cancelled. If array of arrays, the returned data
   *                              will be passed into [`populateFromArray`](@/api/core.md#populatefromarray) instead of the default autofill
   *                              algorithm's result.
   */
  "beforeAutofill",
  /**
   * Fired by {@link Autofill} plugin after populating the data in the autofill feature. This hook is fired when
   * {@link Options#fillHandle} option is enabled.
   *
   * @event Hooks#afterAutofill
   * @since 8.0.0
   * @param {Array[]} fillData The data that was used to fill the `targetRange`. If `beforeAutofill` was used
   *                            and returned `[[]]`, this will be the same object that was returned from `beforeAutofill`.
   * @param {CellRange} sourceRange The range values will be filled from.
   * @param {CellRange} targetRange The range new values will be filled into.
   * @param {string} direction Declares the direction of the autofill. Possible values: `up`, `down`, `left`, `right`.
   */
  "afterAutofill",
  /**
   * Fired before aligning the cell contents.
   *
   * @event Hooks#beforeCellAlignment
   * @param {object} stateBefore An object with class names defining the cell alignment.
   * @param {CellRange[]} range An array of `CellRange` coordinates where the alignment will be applied.
   * @param {string} type Type of the alignment - either `horizontal` or `vertical`.
   * @param {string} alignmentClass String defining the alignment class added to the cell.
   * Possible values: `htLeft` , `htCenter`, `htRight`, `htJustify`, `htTop`, `htMiddle`, `htBottom`.
   */
  "beforeCellAlignment",
  /**
   * Fired before one or more cells are changed.
   *
   * Use this hook to silently alter the user's changes before Handsontable re-renders.
   *
   * To ignore the user's changes, use a nullified array or return `false`.
   *
   * @event Hooks#beforeChange
   * @param {Array[]} changes 2D array containing information about each of the edited cells `[[row, prop, oldVal, newVal], ...]`. `row` is a visual row index.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   * @returns {undefined | boolean} If `false` all changes were cancelled, `true` otherwise.
   * @example
   * ::: only-for javascript
   * ```js
   * // to alter a single change, overwrite the value with `changes[i][3]`
   * new Handsontable(element, {
   *   beforeChange: (changes, source) => {
   *     // [[row, prop, oldVal, newVal], ...]
   *     changes[0][3] = 10;
   *   }
   * });
   *
   * // to ignore a single change, set `changes[i]` to `null`
   * // or remove `changes[i]` from the array, by using `changes.splice(i, 1)`
   * new Handsontable(element, {
   *   beforeChange: (changes, source) => {
   *     // [[row, prop, oldVal, newVal], ...]
   *     changes[0] = null;
   *   }
   * });
   *
   * // to ignore all changes, return `false`
   * // or set the array's length to 0, by using `changes.length = 0`
   * new Handsontable(element, {
   *   beforeChange: (changes, source) => {
   *     // [[row, prop, oldVal, newVal], ...]
   *     return false;
   *   }
   * });
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * // to alter a single change, overwrite the desired value with `changes[i][3]`
   * <HotTable
   *   beforeChange={(changes, source) => {
   *     // [[row, prop, oldVal, newVal], ...]
   *     changes[0][3] = 10;
   *   }}
   * />
   *
   * // to ignore a single change, set `changes[i]` to `null`
   * // or remove `changes[i]` from the array, by using changes.splice(i, 1).
   * <HotTable
   *   beforeChange={(changes, source) => {
   *     // [[row, prop, oldVal, newVal], ...]
   *     changes[0] = null;
   *   }}
   * />
   *
   * // to ignore all changes, return `false`
   * // or set the array's length to 0 (`changes.length = 0`)
   * <HotTable
   *   beforeChange={(changes, source) => {
   *     // [[row, prop, oldVal, newVal], ...]
   *     return false;
   *   }}
   * />
   * ```
   * :::
   */
  "beforeChange",
  /**
   * Fired right before rendering the changes.
   *
   * @event Hooks#beforeChangeRender
   * @param {Array[]} changes Array in form of `[row, prop, oldValue, newValue]`.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   */
  "beforeChangeRender",
  /**
   * Fired before drawing the borders.
   *
   * @event Hooks#beforeDrawBorders
   * @param {Array} corners Array specifying the current selection borders.
   * @param {string} borderClassName Specifies the border class name.
   */
  "beforeDrawBorders",
  /**
   * Fired before getting cell settings.
   *
   * @event Hooks#beforeGetCellMeta
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {object} cellProperties Object containing the cell's properties.
   */
  "beforeGetCellMeta",
  /**
   * Fired before cell meta is removed.
   *
   * @event Hooks#beforeRemoveCellMeta
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {string} key The removed meta key.
   * @param {*} value Value which is under removed key of cell meta.
   * @returns {*|boolean} If false is returned the action is canceled.
   */
  "beforeRemoveCellMeta",
  /**
   * Fired before the Handsontable instance is initiated.
   *
   * @event Hooks#beforeInit
   */
  "beforeInit",
  /**
   * Fired before the Walkontable instance is initiated.
   *
   * @event Hooks#beforeInitWalkontable
   * @param {object} walkontableConfig Walkontable configuration object.
   */
  "beforeInitWalkontable",
  /**
   * Fired before Handsontable's [`data`](@/api/options.md#data)
   * gets modified by the [`loadData()`](@/api/core.md#loaddata) method
   * or the [`updateSettings()`](@/api/core.md#updatesettings) method.
   *
   * Read more:
   * - [Binding to data](@/guides/getting-started/binding-to-data/binding-to-data.md)
   * - [Saving data](@/guides/getting-started/saving-data/saving-data.md)
   *
   * @event Hooks#beforeLoadData
   * @since 8.0.0
   * @param {Array} sourceData An [array of arrays](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-arrays), or an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects), that contains Handsontable's data
   * @param {boolean} initialLoad A flag that indicates whether the data was loaded at Handsontable's initialization (`true`) or later (`false`)
   * @param {string} source The source of the call
   * @returns {Array} The returned array will be used as Handsontable's new dataset.
   */
  "beforeLoadData",
  /**
   * Fired before the [`updateData()`](@/api/core.md#updatedata) method
   * modifies Handsontable's [`data`](@/api/options.md#data).
   *
   * Read more:
   * - [Binding to data](@/guides/getting-started/binding-to-data/binding-to-data.md)
   * - [Saving data](@/guides/getting-started/saving-data/saving-data.md)
   *
   * @event Hooks#beforeUpdateData
   * @since 11.1.0
   * @param {Array} sourceData An [array of arrays](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-arrays), or an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects), that contains Handsontable's data
   * @param {boolean} initialLoad A flag that indicates whether the data was loaded at Handsontable's initialization (`true`) or later (`false`)
   * @param {string} source The source of the call
   * @returns {Array} The returned array will be used as Handsontable's new dataset.
   */
  "beforeUpdateData",
  /**
   * Hook fired before `keydown` event is handled. It can be used to stop default key bindings.
   *
   * __Note__: To prevent default behavior you need to call `false` in your `beforeKeyDown` handler.
   *
   * @event Hooks#beforeKeyDown
   * @param {Event} event Original DOM event.
   */
  "beforeKeyDown",
  /**
   * Fired after the user clicked a cell, but before all the calculations related with it.
   *
   * @event Hooks#beforeOnCellMouseDown
   * @param {Event} event The `mousedown` event object.
   * @param {CellCoords} coords Cell coords object containing the visual coordinates of the clicked cell.
   * @param {HTMLTableCellElement} TD TD element.
   * @param {object} controller An object with properties `row`, `column` and `cell`. Each property contains
   *                            a boolean value that allows or disallows changing the selection for that particular area.
   */
  "beforeOnCellMouseDown",
  /**
   * Fired after the user clicked a cell.
   *
   * @event Hooks#beforeOnCellMouseUp
   * @param {Event} event The `mouseup` event object.
   * @param {CellCoords} coords Cell coords object containing the visual coordinates of the clicked cell.
   * @param {HTMLTableCellElement} TD TD element.
   */
  "beforeOnCellMouseUp",
  /**
   * Fired after the user clicked a cell, but before all the calculations related with it.
   *
   * @event Hooks#beforeOnCellContextMenu
   * @since 4.1.0
   * @param {Event} event The `contextmenu` event object.
   * @param {CellCoords} coords Cell coords object containing the visual coordinates of the clicked cell.
   * @param {HTMLTableCellElement} TD TD element.
   */
  "beforeOnCellContextMenu",
  /**
   * Fired after the user moved cursor over a cell, but before all the calculations related with it.
   *
   * @event Hooks#beforeOnCellMouseOver
   * @param {Event} event The `mouseover` event object.
   * @param {CellCoords} coords CellCoords object containing the visual coordinates of the clicked cell.
   * @param {HTMLTableCellElement} TD TD element.
   * @param {object} controller An object with properties `row`, `column` and `cell`. Each property contains
   *                            a boolean value that allows or disallows changing the selection for that particular area.
   */
  "beforeOnCellMouseOver",
  /**
   * Fired after the user moved cursor out from a cell, but before all the calculations related with it.
   *
   * @event Hooks#beforeOnCellMouseOut
   * @param {Event} event The `mouseout` event object.
   * @param {CellCoords} coords CellCoords object containing the visual coordinates of the leaved cell.
   * @param {HTMLTableCellElement} TD TD element.
   */
  "beforeOnCellMouseOut",
  /**
   * Fired before one or more columns are about to be removed.
   *
   * @event Hooks#beforeRemoveCol
   * @param {number} index Visual index of starter column.
   * @param {number} amount Amount of columns to be removed.
   * @param {number[]} physicalColumns An array of physical columns removed from the data source.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   * @returns {*|boolean} If false is returned the action is canceled.
   */
  "beforeRemoveCol",
  /**
   * Fired when one or more rows are about to be removed.
   *
   * @event Hooks#beforeRemoveRow
   * @param {number} index Visual index of starter row.
   * @param {number} amount Amount of rows to be removed.
   * @param {number[]} physicalRows An array of physical rows removed from the data source.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   * @returns {*|boolean} If false is returned the action is canceled.
   */
  "beforeRemoveRow",
  /**
   * Fired before Handsontable's view-rendering engine is rendered.
   *
   * __Note:__ In Handsontable 9.x and earlier, the `beforeViewRender` hook was named `beforeRender`.
   *
   * @event Hooks#beforeViewRender
   * @since 10.0.0
   * @param {boolean} isForced If set to `true`, the rendering gets triggered by a change of settings, a change of
   *                           data, or a logic that needs a full Handsontable render cycle.
   *                           If set to `false`, the rendering gets triggered by scrolling or moving the selection.
   * @param {object} skipRender Object with `skipRender` property, if it is set to `true ` the next rendering cycle will be skipped.
   */
  "beforeViewRender",
  /**
   * Fired after Handsontable's view-rendering engine is rendered,
   * but before redrawing the selection borders and before scroll syncing.
   *
   * __Note:__ In Handsontable 9.x and earlier, the `afterViewRender` hook was named `afterRender`.
   *
   * @event Hooks#afterViewRender
   * @since 10.0.0
   * @param {boolean} isForced If set to `true`, the rendering gets triggered by a change of settings, a change of
   *                           data, or a logic that needs a full Handsontable render cycle.
   *                           If set to `false`, the rendering gets triggered by scrolling or moving the selection.
   */
  "afterViewRender",
  /**
   * Fired before Handsontable's view-rendering engine updates the view.
   *
   * The `beforeRender` event is fired right after the Handsontable
   * business logic is executed and right before the rendering engine starts calling
   * the Core logic, renderers, cell meta objects etc. to update the view.
   *
   * @event Hooks#beforeRender
   * @param {boolean} isForced If set to `true`, the rendering gets triggered by a change of settings, a change of
   *                           data, or a logic that needs a full Handsontable render cycle.
   *                           If set to `false`, the rendering gets triggered by scrolling or moving the selection.
   */
  "beforeRender",
  /**
   * Fired after Handsontable's view-rendering engine updates the view.
   *
   * @event Hooks#afterRender
   * @param {boolean} isForced If set to `true`, the rendering gets triggered by a change of settings, a change of
   *                           data, or a logic that needs a full Handsontable render cycle.
   *                           If set to `false`, the rendering gets triggered by scrolling or moving the selection.
   */
  "afterRender",
  /**
   * When the focus position is moved to the next or previous row caused by the {@link Options#autoWrapRow} option
   * the hook is triggered.
   *
   * @since 14.0.0
   * @event Hooks#beforeRowWrap
   * @param {boolean} isWrapEnabled Tells whether the row wrapping is going to happen.
   * There may be situations where the option does not work even though it is enabled.
   * This is due to the priority of other options that may block the feature.
   * For example, when the {@link Options#minSpareCols} is defined, the {@link Options#autoWrapRow} option is not checked.
   * Thus, row wrapping is off.
   * @param {CellCoords} newCoords The new focus position. It is an object with keys `row` and `col`, where a value of `-1` indicates a header.
   * @param {boolean} isFlipped `true` if the row index was flipped, `false` otherwise.
   * Flipped index means that the user reached the last row and the focus is moved to the first row or vice versa.
   */
  "beforeRowWrap",
  /**
   * When the focus position is moved to the next or previous column caused by the {@link Options#autoWrapCol} option
   * the hook is triggered.
   *
   * @since 14.0.0
   * @event Hooks#beforeColumnWrap
   * @param {boolean} isWrapEnabled Tells whether the column wrapping is going to happen.
   * There may be situations where the option does not work even though it is enabled.
   * This is due to the priority of other options that may block the feature.
   * For example, when the {@link Options#minSpareRows} is defined, the {@link Options#autoWrapCol} option is not checked.
   * Thus, column wrapping is off.
   * @param {CellCoords} newCoords The new focus position. It is an object with keys `row` and `col`, where a value of `-1` indicates a header.
   * @param {boolean} isFlipped `true` if the column index was flipped, `false` otherwise.
   * Flipped index means that the user reached the last column and the focus is moved to the first column or vice versa.
   */
  "beforeColumnWrap",
  /**
   * Fired before cell meta is changed.
   *
   * @event Hooks#beforeSetCellMeta
   * @since 8.0.0
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {string} key The updated meta key.
   * @param {*} value The updated meta value.
   * @returns {boolean|undefined} If false is returned the action is canceled.
   */
  "beforeSetCellMeta",
  /**
   * Fired before setting focus selection.
   *
   * @since 14.3.0
   * @event Hooks#beforeSelectionFocusSet
   * @param {CellCoords} coords CellCoords instance.
   */
  "beforeSelectionFocusSet",
  /**
   * Fired before setting range is started but not finished yet.
   *
   * @event Hooks#beforeSetRangeStartOnly
   * @param {CellCoords} coords `CellCoords` instance.
   */
  "beforeSetRangeStartOnly",
  /**
   * Fired before setting range is started.
   *
   * @event Hooks#beforeSetRangeStart
   * @param {CellCoords} coords `CellCoords` instance.
   */
  "beforeSetRangeStart",
  /**
   * Fired before setting range is ended.
   *
   * @event Hooks#beforeSetRangeEnd
   * @param {CellCoords} coords `CellCoords` instance.
   */
  "beforeSetRangeEnd",
  /**
   * Fired before applying selection coordinates to the renderable coordinates for Walkontable (rendering engine).
   * It occurs even when cell coordinates remain unchanged and activates during cell selection and drag selection.
   * The behavior of Shift+Tab differs from Arrow Left when there's no further movement possible.
   *
   * @since 14.0.0
   * @event Hooks#beforeSelectionHighlightSet
   */
  "beforeSelectionHighlightSet",
  /**
   * Fired before the logic of handling a touch scroll, when user started scrolling on a touch-enabled device.
   *
   * @event Hooks#beforeTouchScroll
   */
  "beforeTouchScroll",
  /**
   * Fired before cell validation, only if validator function is defined. This can be used to manipulate the value
   * of changed cell before it is applied to the validator function.
   *
   * __Note:__ this will not affect values of changes. This will change value *ONLY* for validation.
   *
   * @event Hooks#beforeValidate
   * @param {*} value Value of the cell.
   * @param {number} row Visual row index.
   * @param {string|number} prop Property name / column index.
   * @param {string} [source] String that identifies source of hook call
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   */
  "beforeValidate",
  /**
   * Fired before cell value is rendered into the DOM (through renderer function). This can be used to manipulate the
   * value which is passed to the renderer without modifying the renderer itself.
   *
   * @event Hooks#beforeValueRender
   * @param {*} value Cell value to render.
   * @param {object} cellProperties An object containing the cell properties.
   */
  "beforeValueRender",
  /**
   * Fired after Handsontable instance is constructed (using `new` operator).
   *
   * @event Hooks#construct
   */
  "construct",
  /**
   * Fired after Handsontable instance is initiated but before table is rendered.
   *
   * @event Hooks#init
   */
  "init",
  /**
   * Fired when a column header index is about to be modified by a callback function.
   *
   * @event Hooks#modifyColHeader
   * @param {number} column Visual column header index.
   */
  "modifyColHeader",
  /**
   * Fired when a column width is about to be modified by a callback function.
   *
   * @event Hooks#modifyColWidth
   * @param {number} width Current column width.
   * @param {number} column Visual column index.
   */
  "modifyColWidth",
  /**
   * Fired when rendering the list of values in the multiple-selection component of the Filters dropdown.
   * The hook allows modifying the displayed values in that component.
   *
   * @since 14.2.0
   * @event Hooks#modifyFiltersMultiSelectValue
   * @param {object} item The item in the list of values.
   * @param {object} meta The cell properties object.
   */
  "modifyFiltersMultiSelectValue",
  /**
   * Fired when focusing a cell or a header element. Allows replacing the element to be focused by returning a
   * different HTML element.
   *
   * @since 14.0.0
   * @event Hooks#modifyFocusedElement
   * @param {number} row Row index.
   * @param {number} column Column index.
   * @param {HTMLElement|undefined} focusedElement The element to be focused. `null` for focusedElement is intended when focused cell is hidden.
   */
  "modifyFocusedElement",
  /**
   * Fired when a row header index is about to be modified by a callback function.
   *
   * @event Hooks#modifyRowHeader
   * @param {number} row Visual row header index.
   */
  "modifyRowHeader",
  /**
   * Fired when a row height is about to be modified by a callback function.
   *
   * @event Hooks#modifyRowHeight
   * @param {number} height Row height.
   * @param {number} row Visual row index.
   */
  "modifyRowHeight",
  /**
   * Fired when a row height is about to be modified by a callback function. The hook allows to change the row height
   * for the specified overlay type.
   *
   * @since 14.5.0
   * @event Hooks#modifyRowHeightByOverlayName
   * @param {number} height Row height.
   * @param {number} row Visual row index.
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'|'master'} overlayName Overlay name.
   */
  "modifyRowHeightByOverlayName",
  /**
   * Fired when a data was retrieved or modified.
   *
   * @event Hooks#modifyData
   * @param {number} row Physical row index.
   * @param {number} column Visual column index.
   * @param {object} valueHolder Object which contains original value which can be modified by overwriting `.value` property.
   * @param {string} ioMode String which indicates for what operation hook is fired (`get` or `set`).
   */
  "modifyData",
  /**
   * Fired when a data was retrieved or modified from the source data set.
   *
   * @event Hooks#modifySourceData
   * @since 8.0.0
   * @param {number} row Physical row index.
   * @param {number} column Physical column index or property name.
   * @param {object} valueHolder Object which contains original value which can be modified by overwriting `.value` property.
   * @param {string} ioMode String which indicates for what operation hook is fired (`get` or `set`).
   */
  "modifySourceData",
  /**
   * Fired when a data was retrieved or modified.
   *
   * @event Hooks#modifyRowData
   * @param {number} row Physical row index.
   */
  "modifyRowData",
  /**
   * Used to modify the cell coordinates when using the [`getCell`](@/api/core.md#getcell) method, opening editor, getting value from the editor
   * and saving values from the closed editor.
   *
   * @event Hooks#modifyGetCellCoords
   * @since 0.36.0
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {boolean} topmost If set to `true`, it returns the TD element from the topmost overlay. For example,
   *                          if the wanted cell is in the range of fixed rows, it will return a TD element
   *                          from the `top` overlay.
   * @returns {undefined|number[]}
   */
  "modifyGetCellCoords",
  /**
   * Used to modify the cell coordinates when the table is activated (going into the listen mode).
   *
   * @event Hooks#modifyFocusOnTabNavigation
   * @since 14.0.0
   * @param {'from_above' | 'from_below'} tabActivationDir The browsers Tab navigation direction. Depending on
   * whether the user activated the table from the element above or below, another cell can be selected.
   * @param {CellCoords} visualCoords The coords that will be used to select a cell.
   */
  "modifyFocusOnTabNavigation",
  /**
   * Allows modify the visual row index that is used to retrieve the row header element (TH) before it's
   * highlighted (proper CSS class names are added). Modifying the visual row index allows building a custom
   * implementation of the nested headers feature or other features that require highlighting other DOM
   * elements than that the rendering engine, by default, would have highlighted.
   *
   * @event Hooks#beforeHighlightingRowHeader
   * @since 8.4.0
   * @param {number} row Visual row index.
   * @param {number} headerLevel Column header level (0 = most distant to the table).
   * @param {object} highlightMeta An object that contains additional information about processed selection.
   * @returns {number|undefined}
   */
  "beforeHighlightingRowHeader",
  /**
   * Allows modify the visual column index that is used to retrieve the column header element (TH) before it's
   * highlighted (proper CSS class names are added). Modifying the visual column index allows building a custom
   * implementation of the nested headers feature or other features that require highlighting other DOM
   * elements than that the rendering engine, by default, would have highlighted.
   *
   * @event Hooks#beforeHighlightingColumnHeader
   * @since 8.4.0
   * @param {number} column Visual column index.
   * @param {number} headerLevel Row header level (0 = most distant to the table).
   * @param {object} highlightMeta An object that contains additional information about processed selection.
   * @returns {number|undefined}
   */
  "beforeHighlightingColumnHeader",
  /**
   * Fired by {@link PersistentState} plugin, after loading value, saved under given key, from browser local storage.
   *
   * The `persistentStateLoad` hook is fired even when the {@link Options#persistentState} option is disabled.
   *
   * @event Hooks#persistentStateLoad
   * @param {string} key Key.
   * @param {object} valuePlaceholder Object containing the loaded value under `valuePlaceholder.value` (if no value have been saved, `value` key will be undefined).
   */
  "persistentStateLoad",
  /**
   * Fired by {@link PersistentState} plugin after resetting data from local storage. If no key is given, all values associated with table will be cleared.
   * This hook is fired when {@link Options#persistentState} option is enabled.
   *
   * @event Hooks#persistentStateReset
   * @param {string} [key] Key.
   */
  "persistentStateReset",
  /**
   * Fired by {@link PersistentState} plugin, after saving value under given key in browser local storage.
   *
   * The `persistentStateSave` hook is fired even when the {@link Options#persistentState} option is disabled.
   *
   * @event Hooks#persistentStateSave
   * @param {string} key Key.
   * @param {Mixed} value Value to save.
   */
  "persistentStateSave",
  /**
   * Fired by {@link ColumnSorting} and {@link MultiColumnSorting} plugins before sorting the column. If you return `false` value inside callback for hook, then sorting
   * will be not applied by the Handsontable (useful for server-side sorting).
   *
   * This hook is fired when {@link Options#columnSorting} or {@link Options#multiColumnSorting} option is enabled.
   *
   * @event Hooks#beforeColumnSort
   * @param {Array} currentSortConfig Current sort configuration (for all sorted columns).
   * @param {Array} destinationSortConfigs Destination sort configuration (for all sorted columns).
   * @returns {boolean | undefined} If `false` the column will not be sorted, `true` otherwise.
   */
  "beforeColumnSort",
  /**
   * Fired by {@link ColumnSorting} and {@link MultiColumnSorting} plugins after sorting the column. This hook is fired when {@link Options#columnSorting}
   * or {@link Options#multiColumnSorting} option is enabled.
   *
   * @event Hooks#afterColumnSort
   * @param {Array} currentSortConfig Current sort configuration (for all sorted columns).
   * @param {Array} destinationSortConfigs Destination sort configuration (for all sorted columns).
   */
  "afterColumnSort",
  /**
   * Fired by {@link Autofill} plugin after setting range of autofill. This hook is fired when {@link Options#fillHandle}
   * option is enabled.
   *
   * @event Hooks#modifyAutofillRange
   * @param {Array} startArea Array of visual coordinates of the starting point for the drag-down operation (`[startRow, startColumn, endRow, endColumn]`).
   * @param {Array} entireArea Array of visual coordinates of the entire area of the drag-down operation (`[startRow, startColumn, endRow, endColumn]`).
   */
  "modifyAutofillRange",
  /**
   * Fired to allow modifying the copyable range with a callback function.
   *
   * @event Hooks#modifyCopyableRange
   * @param {Array[]} copyableRanges Array of objects defining copyable cells.
   */
  "modifyCopyableRange",
  /**
   * Fired by {@link CopyPaste} plugin before copying the values to the clipboard and before clearing values of
   * the selected cells. This hook is fired when {@link Options#copyPaste} option is enabled.
   *
   * @event Hooks#beforeCut
   * @param {Array[]} data An array of arrays which contains data to cut.
   * @param {object[]} coords An array of objects with ranges of the visual indexes (`startRow`, `startCol`, `endRow`, `endCol`)
   *                       which will be cut out.
   * @returns {*} If returns `false` then operation of the cutting out is canceled.
   * @example
   * ::: only-for javascript
   * ```js
   * // To disregard a single row, remove it from the array using data.splice(i, 1).
   * new Handsontable(element, {
   *   beforeCut: function(data, coords) {
   *     // data -> [[1, 2, 3], [4, 5, 6]]
   *     data.splice(0, 1);
   *     // data -> [[4, 5, 6]]
   *     // coords -> [{startRow: 0, startCol: 0, endRow: 1, endCol: 2}]
   *   }
   * });
   * // To cancel a cutting action, just return `false`.
   * new Handsontable(element, {
   *   beforeCut: function(data, coords) {
   *     return false;
   *   }
   * });
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * // To disregard a single row, remove it from the array using data.splice(i, 1).
   * <HotTable
   *   beforeCut={(data, coords) => {
   *     // data -> [[1, 2, 3], [4, 5, 6]]
   *     data.splice(0, 1);
   *     // data -> [[4, 5, 6]]
   *     // coords -> [{startRow: 0, startCol: 0, endRow: 1, endCol: 2}]
   *   }}
   * />
   * // To cancel a cutting action, just return `false`.
   * <HotTable
   *   beforeCut={(data, coords) => {
   *     return false;
   *   }}
   * />
   * ```
   * :::
   */
  "beforeCut",
  /**
   * Fired by {@link CopyPaste} plugin after data was cut out from the table. This hook is fired when
   * {@link Options#copyPaste} option is enabled.
   *
   * @event Hooks#afterCut
   * @param {Array[]} data An array of arrays with the cut data.
   * @param {object[]} coords An array of objects with ranges of the visual indexes (`startRow`, `startCol`, `endRow`, `endCol`)
   *                       which was cut out.
   */
  "afterCut",
  /**
   * Fired before values are copied to the clipboard.
   *
   * @event Hooks#beforeCopy
   * @param {Array[]} data An array of arrays which contains data to copied.
   * @param {object[]} coords An array of objects with ranges of the visual indexes (`startRow`, `startCol`, `endRow`, `endCol`)
   *                         which will copied.
   * @param {{ columnHeadersCount: number }} copiedHeadersCount (Since 12.3.0) The number of copied column headers.
   * @returns {*} If returns `false` then copying is canceled.
   *
   * @example
   * ::: only-for javascript
   * ```js
   * // To disregard a single row, remove it from array using data.splice(i, 1).
   * ...
   * new Handsontable(document.getElementById('example'), {
   *   beforeCopy: (data, coords) => {
   *     // data -> [[1, 2, 3], [4, 5, 6]]
   *     data.splice(0, 1);
   *     // data -> [[4, 5, 6]]
   *     // coords -> [{startRow: 0, startCol: 0, endRow: 1, endCol: 2}]
   *   }
   * });
   * ...
   *
   * // To cancel copying, return false from the callback.
   * ...
   * new Handsontable(document.getElementById('example'), {
   *   beforeCopy: (data, coords) => {
   *     return false;
   *   }
   * });
   * ...
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * // To disregard a single row, remove it from array using data.splice(i, 1).
   * ...
   * <HotTable
   *   beforeCopy={(data, coords) => {
   *     // data -> [[1, 2, 3], [4, 5, 6]]
   *     data.splice(0, 1);
   *     // data -> [[4, 5, 6]]
   *     // coords -> [{startRow: 0, startCol: 0, endRow: 1, endCol: 2}]
   *   }}
   * />
   * ...
   *
   * // To cancel copying, return false from the callback.
   * ...
   * <HotTable
   *   beforeCopy={(data, coords) => {
   *     return false;
   *   }}
   * />
   * ...
   * ```
   * :::
   */
  "beforeCopy",
  /**
   * Fired by {@link CopyPaste} plugin after data are pasted into table. This hook is fired when {@link Options#copyPaste}
   * option is enabled.
   *
   * @event Hooks#afterCopy
   * @param {Array[]} data An array of arrays which contains the copied data.
   * @param {object[]} coords An array of objects with ranges of the visual indexes (`startRow`, `startCol`, `endRow`, `endCol`)
   *                         which was copied.
   * @param {{ columnHeadersCount: number }} copiedHeadersCount (Since 12.3.0) The number of copied column headers.
   */
  "afterCopy",
  /**
   * Fired by {@link CopyPaste} plugin before values are pasted into table. This hook is fired when
   * {@link Options#copyPaste} option is enabled.
   *
   * @event Hooks#beforePaste
   * @param {Array[]} data An array of arrays which contains data to paste.
   * @param {object[]} coords An array of objects with ranges of the visual indexes (`startRow`, `startCol`, `endRow`, `endCol`)
   *                       that correspond to the previously selected area.
   * @returns {*} If returns `false` then pasting is canceled.
   * @example
   * ```js
   * ::: only-for javascript
   * // To disregard a single row, remove it from array using data.splice(i, 1).
   * new Handsontable(example, {
   *   beforePaste: (data, coords) => {
   *     // data -> [[1, 2, 3], [4, 5, 6]]
   *     data.splice(0, 1);
   *     // data -> [[4, 5, 6]]
   *     // coords -> [{startRow: 0, startCol: 0, endRow: 1, endCol: 2}]
   *   }
   * });
   * // To cancel pasting, return false from the callback.
   * new Handsontable(example, {
   *   beforePaste: (data, coords) => {
   *     return false;
   *   }
   * });
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * // To disregard a single row, remove it from array using data.splice(i, 1).
   * <HotTable
   *   beforePaste={(data, coords) => {
   *     // data -> [[1, 2, 3], [4, 5, 6]]
   *     data.splice(0, 1);
   *     // data -> [[4, 5, 6]]
   *     // coords -> [{startRow: 0, startCol: 0, endRow: 1, endCol: 2}]
   *   }}
   * />
   * // To cancel pasting, return false from the callback.
   * <HotTable
   *   beforePaste={(data, coords) => {
   *     return false;
   *   }}
   * />
   * ```
   * :::
   */
  "beforePaste",
  /**
   * Fired by {@link CopyPaste} plugin after values are pasted into table. This hook is fired when
   * {@link Options#copyPaste} option is enabled.
   *
   * @event Hooks#afterPaste
   * @param {Array[]} data An array of arrays with the pasted data.
   * @param {object[]} coords An array of objects with ranges of the visual indexes (`startRow`, `startCol`, `endRow`, `endCol`)
   *                       that correspond to the previously selected area.
   */
  "afterPaste",
  /**
   * Fired by the {@link ManualColumnFreeze} plugin, before freezing a column.
   *
   * @event Hooks#beforeColumnFreeze
   * @since 12.1.0
   * @param {number} column The visual index of the column that is going to freeze.
   * @param {boolean} freezePerformed If `true`: the column is going to freeze. If `false`: the column is not going to freeze (which might happen if the column is already frozen).
   * @returns {boolean|undefined} If `false`: the column is not going to freeze, and the `afterColumnFreeze` hook won't fire.
   */
  "beforeColumnFreeze",
  /**
   * Fired by the {@link ManualColumnFreeze} plugin, right after freezing a column.
   *
   * @event Hooks#afterColumnFreeze
   * @since 12.1.0
   * @param {number} column The visual index of the frozen column.
   * @param {boolean} freezePerformed If `true`: the column got successfully frozen. If `false`: the column didn't get frozen.
   */
  "afterColumnFreeze",
  /**
   * Fired by {@link ManualColumnMove} plugin before change order of the visual indexes. This hook is fired when
   * {@link Options#manualColumnMove} option is enabled.
   *
   * @event Hooks#beforeColumnMove
   * @param {Array} movedColumns Array of visual column indexes to be moved.
   * @param {number} finalIndex Visual column index, being a start index for the moved columns.
   *                            Points to where the elements will be placed after the moving action.
   *                            To check visualization of final index please take a look at
   *                            [documentation](@/guides/columns/column-moving/column-moving.md).
   * @param {number|undefined} dropIndex Visual column index, being a drop index for the moved columns.
   *                                     Points to where we are going to drop the moved elements. To check
   *                                     visualization of drop index please take a look at
   *                                     [documentation](@/guides/columns/column-moving/column-moving.md).
   *                                     It's `undefined` when `dragColumns` function wasn't called.
   * @param {boolean} movePossible Indicates if it's possible to move rows to the desired position.
   * @returns {undefined | boolean} If `false` the column will not be moved, `true` otherwise.
   */
  "beforeColumnMove",
  /**
   * Fired by {@link ManualColumnMove} plugin after changing order of the visual indexes.
   * This hook is fired when {@link Options#manualColumnMove} option is enabled.
   *
   * @event Hooks#afterColumnMove
   * @param {Array} movedColumns Array of visual column indexes to be moved.
   * @param {number} finalIndex Visual column index, being a start index for the moved columns.
   *                            Points to where the elements will be placed after the moving action.
   *                            To check visualization of final index please take a look at
   *                            [documentation](@/guides/columns/column-moving/column-moving.md).
   * @param {number|undefined} dropIndex Visual column index, being a drop index for the moved columns.
   *                                     Points to where we are going to drop the moved elements.
   *                                     To check visualization of drop index please take a look at
   *                                     [documentation](@/guides/columns/column-moving/column-moving.md).
   *                                     It's `undefined` when `dragColumns` function wasn't called.
   * @param {boolean} movePossible Indicates if it was possible to move columns to the desired position.
   * @param {boolean} orderChanged Indicates if order of columns was changed by move.
   */
  "afterColumnMove",
  /**
   * Fired by the {@link ManualColumnFreeze} plugin, before unfreezing a column.
   *
   * @event Hooks#beforeColumnUnfreeze
   * @since 12.1.0
   * @param {number} column The visual index of the column that is going to unfreeze.
   * @param {boolean} unfreezePerformed If `true`: the column is going to unfreeze. If `false`: the column is not going to unfreeze (which might happen if the column is already unfrozen).
   * @returns {boolean|undefined} If `false`: the column is not going to unfreeze, and the `afterColumnUnfreeze` hook won't fire.
   */
  "beforeColumnUnfreeze",
  /**
   * Fired by the {@link ManualColumnFreeze} plugin, right after unfreezing a column.
   *
   * @event Hooks#afterColumnUnfreeze
   * @since 12.1.0
   * @param {number} column The visual index of the unfrozen column.
   * @param {boolean} unfreezePerformed If `true`: the column got successfully unfrozen. If `false`: the column didn't get unfrozen.
   */
  "afterColumnUnfreeze",
  /**
   * Fired by {@link ManualRowMove} plugin before changing the order of the visual indexes. This hook is fired when
   * {@link Options#manualRowMove} option is enabled.
   *
   * @event Hooks#beforeRowMove
   * @param {Array} movedRows Array of visual row indexes to be moved.
   * @param {number} finalIndex Visual row index, being a start index for the moved rows.
   *                            Points to where the elements will be placed after the moving action.
   *                            To check visualization of final index please take a look at
   *                            [documentation](@/guides/rows/row-moving/row-moving.md).
   * @param {number|undefined} dropIndex Visual row index, being a drop index for the moved rows.
   *                                     Points to where we are going to drop the moved elements.
   *                                     To check visualization of drop index please take a look at
   *                                     [documentation](@/guides/rows/row-moving/row-moving.md).
   *                                     It's `undefined` when `dragRows` function wasn't called.
   * @param {boolean} movePossible Indicates if it's possible to move rows to the desired position.
   * @returns {*|boolean} If false is returned the action is canceled.
   */
  "beforeRowMove",
  /**
   * Fired by {@link ManualRowMove} plugin after changing the order of the visual indexes.
   * This hook is fired when {@link Options#manualRowMove} option is enabled.
   *
   * @event Hooks#afterRowMove
   * @param {Array} movedRows Array of visual row indexes to be moved.
   * @param {number} finalIndex Visual row index, being a start index for the moved rows.
   *                            Points to where the elements will be placed after the moving action.
   *                            To check visualization of final index please take a look at
   *                            [documentation](@/guides/rows/row-moving/row-moving.md).
   * @param {number|undefined} dropIndex Visual row index, being a drop index for the moved rows.
   *                                     Points to where we are going to drop the moved elements.
   *                                     To check visualization of drop index please take a look at
   *                                     [documentation](@/guides/rows/row-moving/row-moving.md).
   *                                     It's `undefined` when `dragRows` function wasn't called.
   * @param {boolean} movePossible Indicates if it was possible to move rows to the desired position.
   * @param {boolean} orderChanged Indicates if order of rows was changed by move.
   */
  "afterRowMove",
  /**
   * Fired by {@link ManualColumnResize} plugin before rendering the table with modified column sizes. This hook is
   * fired when {@link Options#manualColumnResize} option is enabled.
   *
   * @event Hooks#beforeColumnResize
   * @param {number} newSize Calculated new column width.
   * @param {number} column Visual index of the resized column.
   * @param {boolean} isDoubleClick Flag that determines whether there was a double-click.
   * @returns {number} Returns a new column size or `undefined`, if column size should be calculated automatically.
   */
  "beforeColumnResize",
  /**
   * Fired by {@link ManualColumnResize} plugin after rendering the table with modified column sizes. This hook is
   * fired when {@link Options#manualColumnResize} option is enabled.
   *
   * @event Hooks#afterColumnResize
   * @param {number} newSize Calculated new column width.
   * @param {number} column Visual index of the resized column.
   * @param {boolean} isDoubleClick Flag that determines whether there was a double-click.
   */
  "afterColumnResize",
  /**
   * Fired by {@link ManualRowResize} plugin before rendering the table with modified row sizes. This hook is
   * fired when {@link Options#manualRowResize} option is enabled.
   *
   * @event Hooks#beforeRowResize
   * @param {number} newSize Calculated new row height.
   * @param {number} row Visual index of the resized row.
   * @param {boolean} isDoubleClick Flag that determines whether there was a double-click.
   * @returns {number|undefined} Returns the new row size or `undefined` if row size should be calculated automatically.
   */
  "beforeRowResize",
  /**
   * Fired by {@link ManualRowResize} plugin after rendering the table with modified row sizes. This hook is
   * fired when {@link Options#manualRowResize} option is enabled.
   *
   * @event Hooks#afterRowResize
   * @param {number} newSize Calculated new row height.
   * @param {number} row Visual index of the resized row.
   * @param {boolean} isDoubleClick Flag that determines whether there was a double-click.
   */
  "afterRowResize",
  /**
   * Fired after getting the column header renderers.
   *
   * @event Hooks#afterGetColumnHeaderRenderers
   * @param {Function[]} renderers An array of the column header renderers.
   */
  "afterGetColumnHeaderRenderers",
  /**
   * Fired after getting the row header renderers.
   *
   * @event Hooks#afterGetRowHeaderRenderers
   * @param {Function[]} renderers An array of the row header renderers.
   */
  "afterGetRowHeaderRenderers",
  /**
   * Fired before applying stretched column width to column.
   *
   * @event Hooks#beforeStretchingColumnWidth
   * @param {number} stretchedWidth Calculated width.
   * @param {number} column Visual column index.
   * @returns {number|undefined} Returns new width which will be applied to the column element.
   */
  "beforeStretchingColumnWidth",
  /**
   * Fired by the [`Filters`](@/api/filters.md) plugin,
   * before a [column filter](@/guides/columns/column-filter/column-filter.md) gets applied.
   *
   * [`beforeFilter`](#beforefilter) takes two arguments: `conditionsStack` and `previousConditionsStack`, both are
   * arrays of objects.
   *
   * Each object represents one of your [column filters](@/api/filters.md#addcondition),
   * and consists of the following properties:
   *
   * | Property     | Possible values                                                         | Description                                                                                                              |
   * | ------------ | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
   * | `column`     | Number                                                                  | A visual index of the column to which the filter will be applied.                                                        |
   * | `conditions` | Array of objects                                                        | Each object represents one condition. For details, see [`addCondition()`](@/api/filters.md#addcondition).                |
   * | `operation`  | `'conjunction'` \| `'disjunction'` \| `'disjunctionWithExtraCondition'` | An operation to perform on your set of `conditions`. For details, see [`addCondition()`](@/api/filters.md#addcondition). |
   *
   * An example of the format of the `conditionsStack` argument:
   *
   * ```js
   * [
   *   {
   *     column: 2,
   *     conditions: [
   *       {name: 'begins_with', args: [['S']]}
   *     ],
   *     operation: 'conjunction'
   *   },
   *   {
   *     column: 4,
   *     conditions: [
   *       {name: 'not_empty', args: []}
   *     ],
   *     operation: 'conjunction'
   *   },
   * ]
   * ```
   *
   * To perform server-side filtering (i.e., to not apply filtering to Handsontable's UI),
   * set [`beforeFilter`](#beforefilter) to return `false`:
   *
   * ```js
   * new Handsontable(document.getElementById('example'), {
   *   beforeFilter: (conditionsStack) => {
   *     return false;
   *   }
   * });
   *```
   *
   * Read more:
   * - [Guides: Column filter](@/guides/columns/column-filter/column-filter.md)
   * - [Hooks: `afterFilter`](#afterfilter)
   * - [Options: `filters`](@/api/options.md#filters)
   * - [Plugins: `Filters`](@/api/filters.md)
   * – [Plugin methods: `addCondition()`](@/api/filters.md#addcondition)
   *
   * @event Hooks#beforeFilter
   * @param {object[]} conditionsStack An array of objects with your [column filters](@/api/filters.md#addcondition).
   * @param {object[]|null} previousConditionsStack An array of objects with your previous [column filters](@/api/filters.md#addcondition). It can also be `null` if there was no previous filters applied or the conditions did not change between performing the `filter` action.
   * @returns {boolean} To perform server-side filtering (i.e., to not apply filtering to Handsontable's UI), return `false`.
   */
  "beforeFilter",
  /**
   * Fired by the [`Filters`](@/api/filters.md) plugin,
   * after a [column filter](@/guides/columns/column-filter/column-filter.md) gets applied.
   *
   * [`afterFilter`](#afterfilter) takes one argument (`conditionsStack`), which is an array of objects.
   * Each object represents one of your [column filters](@/api/filters.md#addcondition),
   * and consists of the following properties:
   *
   * | Property     | Possible values                                                         | Description                                                                                                              |
   * | ------------ | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
   * | `column`     | Number                                                                  | A visual index of the column to which the filter was applied.                                                            |
   * | `conditions` | Array of objects                                                        | Each object represents one condition. For details, see [`addCondition()`](@/api/filters.md#addcondition).                |
   * | `operation`  | `'conjunction'` \| `'disjunction'` \| `'disjunctionWithExtraCondition'` | An operation to perform on your set of `conditions`. For details, see [`addCondition()`](@/api/filters.md#addcondition). |
   *
   * An example of the format of the `conditionsStack` argument:
   *
   * ```js
   * [
   *   {
   *     column: 2,
   *     conditions: [
   *       {name: 'begins_with', args: [['S']]}
   *     ],
   *     operation: 'conjunction'
   *   },
   *   {
   *     column: 4,
   *     conditions: [
   *       {name: 'not_empty', args: []}
   *     ],
   *     operation: 'conjunction'
   *   },
   * ]
   * ```
   *
   * Read more:
   * - [Guides: Column filter](@/guides/columns/column-filter/column-filter.md)
   * - [Hooks: `beforeFilter`](#beforefilter)
   * - [Options: `filters`](@/api/options.md#filters)
   * - [Plugins: `Filters`](@/api/filters.md)
   * – [Plugin methods: `addCondition()`](@/api/filters.md#addcondition)
   *
   * @event Hooks#afterFilter
   * @param {object[]} conditionsStack An array of objects with your [column filters](@/api/filters.md#addcondition).
   */
  "afterFilter",
  /**
   * Fired by the {@link Formulas} plugin, when any cell value changes.
   *
   * Returns an array of objects that contains:
   * - The addresses (`sheet`, `row`, `col`) and new values (`newValue`) of the changed cells.
   * - The addresses and new values of any cells that had to be recalculated (because their formulas depend on the cells that changed).
   *
   * This hook gets also fired on Handsontable's initialization, returning the addresses and values of all cells.
   *
   * Read more:
   * - [Guides: Formula calculation](@/guides/formulas/formula-calculation/formula-calculation.md)
   * - [HyperFormula documentation: `valuesUpdated`](https://hyperformula.handsontable.com/api/interfaces/listeners.html#valuesupdated)
   *
   * @since 9.0.0
   * @event Hooks#afterFormulasValuesUpdate
   * @param {Array} changes The addresses and new values of all the changed and recalculated cells.
   */
  "afterFormulasValuesUpdate",
  /**
   * Fired when a named expression is added to the Formulas' engine instance.
   *
   * @since 9.0.0
   * @event Hooks#afterNamedExpressionAdded
   * @param {string} namedExpressionName The name of the added expression.
   * @param {Array} changes The values and location of applied changes.
   */
  "afterNamedExpressionAdded",
  /**
   * Fired when a named expression is removed from the Formulas' engine instance.
   *
   * @since 9.0.0
   * @event Hooks#afterNamedExpressionRemoved
   * @param {string} namedExpressionName The name of the removed expression.
   * @param {Array} changes The values and location of applied changes.
   */
  "afterNamedExpressionRemoved",
  /**
   * Fired when a new sheet is added to the Formulas' engine instance.
   *
   * @since 9.0.0
   * @event Hooks#afterSheetAdded
   * @param {string} addedSheetDisplayName The name of the added sheet.
   */
  "afterSheetAdded",
  /**
   * Fired when a sheet in the Formulas' engine instance is renamed.
   *
   * @since 9.0.0
   * @event Hooks#afterSheetRenamed
   * @param {string} oldDisplayName The old name of the sheet.
   * @param {string} newDisplayName The new name of the sheet.
   */
  "afterSheetRenamed",
  /**
   * Fired when a sheet is removed from the Formulas' engine instance.
   *
   * @since 9.0.0
   * @event Hooks#afterSheetRemoved
   * @param {string} removedSheetDisplayName The removed sheet name.
   * @param {Array} changes The values and location of applied changes.
   */
  "afterSheetRemoved",
  /**
   * Fired while retrieving the column header height.
   *
   * @event Hooks#modifyColumnHeaderHeight
   */
  "modifyColumnHeaderHeight",
  /**
   * Fired while retrieving a column header's value.
   *
   * @since 12.3.0
   * @event Hooks#modifyColumnHeaderValue
   * @param {string} value A column header value.
   * @param {number} visualColumnIndex A visual column index.
   * @param {number} [headerLevel=0] Header level index. Accepts positive (`0` to `n`)
   *                                 and negative (`-1` to `-n`) values. For positive values, `0` points to the
   *                                 topmost header. For negative values, `-1` points to the bottom-most
   *                                 header (the header closest to the cells).
   * @returns {string} The column header value to be updated.
   */
  "modifyColumnHeaderValue",
  /**
   * Fired by {@link UndoRedo} plugin before the undo action. Contains information about the action that is being undone.
   * This hook is fired when {@link Options#undo} option is enabled.
   *
   * @event Hooks#beforeUndo
   * @param {object} action The action object. Contains information about the action being undone. The `actionType`
   *                        property of the object specifies the type of the action in a String format. (e.g. `'remove_row'`).
   * @returns {*|boolean} If false is returned the action is canceled.
   */
  "beforeUndo",
  /**
   * Fired by {@link UndoRedo} plugin before changing undo stack.
   *
   * @event Hooks#beforeUndoStackChange
   * @since 8.4.0
   * @param {Array} doneActions Stack of actions which may be undone.
   * @param {string} [source] String that identifies source of action
   *                          ([list of all available sources](@/guides/getting-started/events-and-hooks/events-and-hooks.md#definition-for-source-argument)).
   * @returns {*|boolean} If false is returned the action of changing undo stack is canceled.
   */
  "beforeUndoStackChange",
  /**
   * Fired by {@link UndoRedo} plugin after the undo action. Contains information about the action that is being undone.
   * This hook is fired when {@link Options#undo} option is enabled.
   *
   * @event Hooks#afterUndo
   * @param {object} action The action object. Contains information about the action being undone. The `actionType`
   *                        property of the object specifies the type of the action in a String format. (e.g. `'remove_row'`).
   */
  "afterUndo",
  /**
   * Fired by {@link UndoRedo} plugin after changing undo stack.
   *
   * @event Hooks#afterUndoStackChange
   * @since 8.4.0
   * @param {Array} doneActionsBefore Stack of actions which could be undone before performing new action.
   * @param {Array} doneActionsAfter Stack of actions which can be undone after performing new action.
   */
  "afterUndoStackChange",
  /**
   * Fired by {@link UndoRedo} plugin before the redo action. Contains information about the action that is being redone.
   * This hook is fired when {@link Options#undo} option is enabled.
   *
   * @event Hooks#beforeRedo
   * @param {object} action The action object. Contains information about the action being redone. The `actionType`
   *                        property of the object specifies the type of the action in a String format (e.g. `'remove_row'`).
   * @returns {*|boolean} If false is returned the action is canceled.
   */
  "beforeRedo",
  /**
   * Fired by {@link UndoRedo} plugin before changing redo stack.
   *
   * @event Hooks#beforeRedoStackChange
   * @since 8.4.0
   * @param {Array} undoneActions Stack of actions which may be redone.
   */
  "beforeRedoStackChange",
  /**
   * Fired by {@link UndoRedo} plugin after the redo action. Contains information about the action that is being redone.
   * This hook is fired when {@link Options#undo} option is enabled.
   *
   * @event Hooks#afterRedo
   * @param {object} action The action object. Contains information about the action being redone. The `actionType`
   *                        property of the object specifies the type of the action in a String format (e.g. `'remove_row'`).
   */
  "afterRedo",
  /**
   * Fired by {@link UndoRedo} plugin after changing redo stack.
   *
   * @event Hooks#afterRedoStackChange
   * @since 8.4.0
   * @param {Array} undoneActionsBefore Stack of actions which could be redone before performing new action.
   * @param {Array} undoneActionsAfter Stack of actions which can be redone after performing new action.
   */
  "afterRedoStackChange",
  /**
   * Fired while retrieving the row header width.
   *
   * @event Hooks#modifyRowHeaderWidth
   * @param {number} rowHeaderWidth Row header width.
   */
  "modifyRowHeaderWidth",
  /**
   * Fired when the focus of the selection is being modified (e.g. Moving the focus with the enter/tab keys).
   *
   * @since 14.3.0
   * @event Hooks#modifyTransformFocus
   * @param {CellCoords} delta Cell coords object declaring the delta of the new selection relative to the previous one.
   */
  "modifyTransformFocus",
  /**
   * Fired when the start of the selection is being modified (e.g. Moving the selection with the arrow keys).
   *
   * @event Hooks#modifyTransformStart
   * @param {CellCoords} delta Cell coords object declaring the delta of the new selection relative to the previous one.
   */
  "modifyTransformStart",
  /**
   * Fired when the end of the selection is being modified (e.g. Moving the selection with the arrow keys).
   *
   * @event Hooks#modifyTransformEnd
   * @param {CellCoords} delta Cell coords object declaring the delta of the new selection relative to the previous one.
   */
  "modifyTransformEnd",
  /**
   * Fired after the focus of the selection is being modified (e.g. Moving the focus with the enter/tab keys).
   *
   * @since 14.3.0
   * @event Hooks#afterModifyTransformFocus
   * @param {CellCoords} coords Coords of the freshly focused cell.
   * @param {number} rowTransformDir `-1` if trying to focus a cell with a negative row index. `0` otherwise.
   * @param {number} colTransformDir `-1` if trying to focus a cell with a negative column index. `0` otherwise.
   */
  "afterModifyTransformFocus",
  /**
   * Fired after the start of the selection is being modified (e.g. Moving the selection with the arrow keys).
   *
   * @event Hooks#afterModifyTransformStart
   * @param {CellCoords} coords Coords of the freshly selected cell.
   * @param {number} rowTransformDir `-1` if trying to select a cell with a negative row index. `0` otherwise.
   * @param {number} colTransformDir `-1` if trying to select a cell with a negative column index. `0` otherwise.
   */
  "afterModifyTransformStart",
  /**
   * Fired after the end of the selection is being modified (e.g. Moving the selection with the arrow keys).
   *
   * @event Hooks#afterModifyTransformEnd
   * @param {CellCoords} coords Visual coords of the freshly selected cell.
   * @param {number} rowTransformDir `-1` if trying to select a cell with a negative row index. `0` otherwise.
   * @param {number} colTransformDir `-1` if trying to select a cell with a negative column index. `0` otherwise.
   */
  "afterModifyTransformEnd",
  /**
   * Fired inside the `viewportRowCalculatorOverride` method. Allows modifying the row calculator parameters.
   *
   * @event Hooks#afterViewportRowCalculatorOverride
   * @param {object} calc The row calculator.
   */
  "afterViewportRowCalculatorOverride",
  /**
   * Fired inside the `viewportColumnCalculatorOverride` method. Allows modifying the row calculator parameters.
   *
   * @event Hooks#afterViewportColumnCalculatorOverride
   * @param {object} calc The row calculator.
   */
  "afterViewportColumnCalculatorOverride",
  /**
   * Fired after initializing all the plugins.
   * This hook should be added before Handsontable is initialized.
   *
   * @event Hooks#afterPluginsInitialized
   *
   * @example
   * ```js
   * Handsontable.hooks.add('afterPluginsInitialized', myCallback);
   * ```
   */
  "afterPluginsInitialized",
  /**
   * Fired by {@link HiddenRows} plugin before marking the rows as hidden. Fired only if the {@link Options#hiddenRows} option is enabled.
   * Returning `false` in the callback will prevent the hiding action from completing.
   *
   * @event Hooks#beforeHideRows
   * @param {Array} currentHideConfig Current hide configuration - a list of hidden physical row indexes.
   * @param {Array} destinationHideConfig Destination hide configuration - a list of hidden physical row indexes.
   * @param {boolean} actionPossible `true`, if provided row indexes are valid, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the hiding action will not be completed.
   */
  "beforeHideRows",
  /**
   * Fired by {@link HiddenRows} plugin after marking the rows as hidden. Fired only if the {@link Options#hiddenRows} option is enabled.
   *
   * @event Hooks#afterHideRows
   * @param {Array} currentHideConfig Current hide configuration - a list of hidden physical row indexes.
   * @param {Array} destinationHideConfig Destination hide configuration - a list of hidden physical row indexes.
   * @param {boolean} actionPossible `true`, if provided row indexes are valid, `false` otherwise.
   * @param {boolean} stateChanged `true`, if the action affected any non-hidden rows, `false` otherwise.
   */
  "afterHideRows",
  /**
   * Fired by {@link HiddenRows} plugin before marking the rows as not hidden. Fired only if the {@link Options#hiddenRows} option is enabled.
   * Returning `false` in the callback will prevent the row revealing action from completing.
   *
   * @event Hooks#beforeUnhideRows
   * @param {Array} currentHideConfig Current hide configuration - a list of hidden physical row indexes.
   * @param {Array} destinationHideConfig Destination hide configuration - a list of hidden physical row indexes.
   * @param {boolean} actionPossible `true`, if provided row indexes are valid, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the revealing action will not be completed.
   */
  "beforeUnhideRows",
  /**
   * Fired by {@link HiddenRows} plugin after marking the rows as not hidden. Fired only if the {@link Options#hiddenRows} option is enabled.
   *
   * @event Hooks#afterUnhideRows
   * @param {Array} currentHideConfig Current hide configuration - a list of hidden physical row indexes.
   * @param {Array} destinationHideConfig Destination hide configuration - a list of hidden physical row indexes.
   * @param {boolean} actionPossible `true`, if provided row indexes are valid, `false` otherwise.
   * @param {boolean} stateChanged `true`, if the action affected any hidden rows, `false` otherwise.
   */
  "afterUnhideRows",
  /**
   * Fired by {@link HiddenColumns} plugin before marking the columns as hidden. Fired only if the {@link Options#hiddenColumns} option is enabled.
   * Returning `false` in the callback will prevent the hiding action from completing.
   *
   * @event Hooks#beforeHideColumns
   * @param {Array} currentHideConfig Current hide configuration - a list of hidden physical column indexes.
   * @param {Array} destinationHideConfig Destination hide configuration - a list of hidden physical column indexes.
   * @param {boolean} actionPossible `true`, if the provided column indexes are valid, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the hiding action will not be completed.
   */
  "beforeHideColumns",
  /**
   * Fired by {@link HiddenColumns} plugin after marking the columns as hidden. Fired only if the {@link Options#hiddenColumns} option is enabled.
   *
   * @event Hooks#afterHideColumns
   * @param {Array} currentHideConfig Current hide configuration - a list of hidden physical column indexes.
   * @param {Array} destinationHideConfig Destination hide configuration - a list of hidden physical column indexes.
   * @param {boolean} actionPossible `true`, if the provided column indexes are valid, `false` otherwise.
   * @param {boolean} stateChanged `true`, if the action affected any non-hidden columns, `false` otherwise.
   */
  "afterHideColumns",
  /**
   * Fired by {@link HiddenColumns} plugin before marking the columns as not hidden. Fired only if the {@link Options#hiddenColumns} option is enabled.
   * Returning `false` in the callback will prevent the column revealing action from completing.
   *
   * @event Hooks#beforeUnhideColumns
   * @param {Array} currentHideConfig Current hide configuration - a list of hidden physical column indexes.
   * @param {Array} destinationHideConfig Destination hide configuration - a list of hidden physical column indexes.
   * @param {boolean} actionPossible `true`, if the provided column indexes are valid, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the hiding action will not be completed.
   */
  "beforeUnhideColumns",
  /**
   * Fired by {@link HiddenColumns} plugin after marking the columns as not hidden. Fired only if the {@link Options#hiddenColumns} option is enabled.
   *
   * @event Hooks#afterUnhideColumns
   * @param {Array} currentHideConfig Current hide configuration - a list of hidden physical column indexes.
   * @param {Array} destinationHideConfig Destination hide configuration - a list of hidden physical column indexes.
   * @param {boolean} actionPossible `true`, if the provided column indexes are valid, `false` otherwise.
   * @param {boolean} stateChanged `true`, if the action affected any hidden columns, `false` otherwise.
   */
  "afterUnhideColumns",
  /**
   * Fired by {@link TrimRows} plugin before trimming rows. This hook is fired when {@link Options#trimRows} option is enabled.
   *
   * @event Hooks#beforeTrimRow
   * @param {Array} currentTrimConfig Current trim configuration - a list of trimmed physical row indexes.
   * @param {Array} destinationTrimConfig Destination trim configuration - a list of trimmed physical row indexes.
   * @param {boolean} actionPossible `true`, if all of the row indexes are withing the bounds of the table, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the trimming action will not be completed.
   */
  "beforeTrimRow",
  /**
   * Fired by {@link TrimRows} plugin after trimming rows. This hook is fired when {@link Options#trimRows} option is enabled.
   *
   * @event Hooks#afterTrimRow
   * @param {Array} currentTrimConfig Current trim configuration - a list of trimmed physical row indexes.
   * @param {Array} destinationTrimConfig Destination trim configuration - a list of trimmed physical row indexes.
   * @param {boolean} actionPossible `true`, if all of the row indexes are withing the bounds of the table, `false` otherwise.
   * @param {boolean} stateChanged `true`, if the action affected any non-trimmed rows, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the trimming action will not be completed.
   */
  "afterTrimRow",
  /**
   * Fired by {@link TrimRows} plugin before untrimming rows. This hook is fired when {@link Options#trimRows} option is enabled.
   *
   * @event Hooks#beforeUntrimRow
   * @param {Array} currentTrimConfig Current trim configuration - a list of trimmed physical row indexes.
   * @param {Array} destinationTrimConfig Destination trim configuration - a list of trimmed physical row indexes.
   * @param {boolean} actionPossible `true`, if all of the row indexes are withing the bounds of the table, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the untrimming action will not be completed.
   */
  "beforeUntrimRow",
  /**
   * Fired by {@link TrimRows} plugin after untrimming rows. This hook is fired when {@link Options#trimRows} option is enabled.
   *
   * @event Hooks#afterUntrimRow
   * @param {Array} currentTrimConfig Current trim configuration - a list of trimmed physical row indexes.
   * @param {Array} destinationTrimConfig Destination trim configuration - a list of trimmed physical row indexes.
   * @param {boolean} actionPossible `true`, if all of the row indexes are withing the bounds of the table, `false` otherwise.
   * @param {boolean} stateChanged `true`, if the action affected any trimmed rows, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the untrimming action will not be completed.
   */
  "afterUntrimRow",
  /**
   * Fired by {@link DropdownMenu} plugin before opening the dropdown menu. This hook is fired when {@link Options#dropdownMenu}
   * option is enabled.
   *
   * @event Hooks#beforeDropdownMenuShow
   * @param {DropdownMenu} dropdownMenu The `DropdownMenu` instance.
   */
  "beforeDropdownMenuShow",
  /**
   * Fired by {@link DropdownMenu} plugin after opening the Dropdown Menu. This hook is fired when {@link Options#dropdownMenu}
   * option is enabled.
   *
   * @event Hooks#afterDropdownMenuShow
   * @param {DropdownMenu} dropdownMenu The `DropdownMenu` instance.
   */
  "afterDropdownMenuShow",
  /**
   * Fired by {@link DropdownMenu} plugin after hiding the Dropdown Menu. This hook is fired when {@link Options#dropdownMenu}
   * option is enabled.
   *
   * @event Hooks#afterDropdownMenuHide
   * @param {DropdownMenu} instance The `DropdownMenu` instance.
   */
  "afterDropdownMenuHide",
  /**
   * Fired by {@link NestedRows} plugin before adding a children to the `NestedRows` structure. This hook is fired when
   * {@link Options#nestedRows} option is enabled.
   *
   * @event Hooks#beforeAddChild
   * @param {object} parent The parent object.
   * @param {object|undefined} element The element added as a child. If `undefined`, a blank child was added.
   * @param {number|undefined} index The index within the parent where the new child was added. If `undefined`, the element was added as the last child.
   */
  "beforeAddChild",
  /**
   * Fired by {@link NestedRows} plugin after adding a children to the `NestedRows` structure. This hook is fired when
   * {@link Options#nestedRows} option is enabled.
   *
   * @event Hooks#afterAddChild
   * @param {object} parent The parent object.
   * @param {object|undefined} element The element added as a child. If `undefined`, a blank child was added.
   * @param {number|undefined} index The index within the parent where the new child was added. If `undefined`, the element was added as the last child.
   */
  "afterAddChild",
  /**
   * Fired by {@link NestedRows} plugin before detaching a child from its parent. This hook is fired when
   * {@link Options#nestedRows} option is enabled.
   *
   * @event Hooks#beforeDetachChild
   * @param {object} parent An object representing the parent from which the element is to be detached.
   * @param {object} element The detached element.
   */
  "beforeDetachChild",
  /**
   * Fired by {@link NestedRows} plugin after detaching a child from its parent. This hook is fired when
   * {@link Options#nestedRows} option is enabled.
   *
   * @event Hooks#afterDetachChild
   * @param {object} parent An object representing the parent from which the element was detached.
   * @param {object} element The detached element.
   * @param {number} finalElementPosition The final row index of the detached element.
   */
  "afterDetachChild",
  /**
   * Fired before the editor is opened and rendered.
   *
   * @since 14.2.0
   * @event Hooks#beforeBeginEditing
   * @param {number} row Visual row index of the edited cell.
   * @param {number} column Visual column index of the edited cell.
   * @param {*} initialValue The initial editor value.
   * @param {MouseEvent | KeyboardEvent} event The event which was responsible for opening the editor.
   * @param {boolean} fullEditMode `true` if the editor is opened in full edit mode, `false` otherwise.
   * Editor opened in full edit mode does not close after pressing Arrow keys.
   * @returns {boolean | undefined} If the callback returns `false,` the editor won't be opened after
   * the mouse double click or after pressing the Enter key. Returning `undefined` (or other value
   * than boolean) will result in default behavior, which disallows opening an editor for non-contiguous
   * selection (while pressing Ctrl/Cmd) and for multiple selected cells (while pressing SHIFT).
   * Returning `true` removes those restrictions.
   */
  "beforeBeginEditing",
  /**
   * Fired after the editor is opened and rendered.
   *
   * @event Hooks#afterBeginEditing
   * @param {number} row Visual row index of the edited cell.
   * @param {number} column Visual column index of the edited cell.
   */
  "afterBeginEditing",
  /**
   * Fired by {@link MergeCells} plugin before cell merging. This hook is fired when {@link Options#mergeCells}
   * option is enabled.
   *
   * @event Hooks#beforeMergeCells
   * @param {CellRange} cellRange Selection cell range.
   * @param {boolean} [auto=false] `true` if called automatically by the plugin.
   */
  "beforeMergeCells",
  /**
   * Fired by {@link MergeCells} plugin after cell merging. This hook is fired when {@link Options#mergeCells}
   * option is enabled.
   *
   * @event Hooks#afterMergeCells
   * @param {CellRange} cellRange Selection cell range.
   * @param {object} mergeParent The parent collection of the provided cell range.
   * @param {boolean} [auto=false] `true` if called automatically by the plugin.
   */
  "afterMergeCells",
  /**
   * Fired by {@link MergeCells} plugin before unmerging the cells. This hook is fired when {@link Options#mergeCells}
   * option is enabled.
   *
   * @event Hooks#beforeUnmergeCells
   * @param {CellRange} cellRange Selection cell range.
   * @param {boolean} [auto=false] `true` if called automatically by the plugin.
   */
  "beforeUnmergeCells",
  /**
   * Fired by {@link MergeCells} plugin after unmerging the cells. This hook is fired when {@link Options#mergeCells}
   * option is enabled.
   *
   * @event Hooks#afterUnmergeCells
   * @param {CellRange} cellRange Selection cell range.
   * @param {boolean} [auto=false] `true` if called automatically by the plugin.
   */
  "afterUnmergeCells",
  /**
   * Fired after the table was switched into listening mode. This allows Handsontable to capture keyboard events and
   * respond in the right way.
   *
   * @event Hooks#afterListen
   */
  "afterListen",
  /**
   * Fired after the table was switched off from the listening mode. This makes the Handsontable inert for any
   * keyboard events.
   *
   * @event Hooks#afterUnlisten
   */
  "afterUnlisten",
  /**
   * Fired after the window was resized or the size of the Handsontable root element was changed.
   *
   * @event Hooks#afterRefreshDimensions
   * @param {{ width: number, height: number }} previousDimensions Previous dimensions of the container.
   * @param {{ width: number, height: number }} currentDimensions Current dimensions of the container.
   * @param {boolean} stateChanged `true`, if the container was re-render, `false` otherwise.
   */
  "afterRefreshDimensions",
  /**
   * Cancellable hook, called after resizing a window or after detecting size change of the
   * Handsontable root element, but before redrawing a table.
   *
   * @event Hooks#beforeRefreshDimensions
   * @param {{ width: number, height: number }} previousDimensions Previous dimensions of the container.
   * @param {{ width: number, height: number }} currentDimensions Current dimensions of the container.
   * @param {boolean} actionPossible `true`, if current and previous dimensions are different, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the refresh action will not be completed.
   */
  "beforeRefreshDimensions",
  /**
   * Fired by {@link CollapsibleColumns} plugin before columns collapse. This hook is fired when {@link Options#collapsibleColumns} option is enabled.
   *
   * @event Hooks#beforeColumnCollapse
   * @since 8.0.0
   * @param {Array} currentCollapsedColumns Current collapsible configuration - a list of collapsible physical column indexes.
   * @param {Array} destinationCollapsedColumns Destination collapsible configuration - a list of collapsible physical column indexes.
   * @param {boolean} collapsePossible `true`, if all of the column indexes are withing the bounds of the collapsed sections, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the collapsing action will not be completed.
   */
  "beforeColumnCollapse",
  /**
   * Fired by {@link CollapsibleColumns} plugin before columns collapse. This hook is fired when {@link Options#collapsibleColumns} option is enabled.
   *
   * @event Hooks#afterColumnCollapse
   * @since 8.0.0
   * @param {Array} currentCollapsedColumns Current collapsible configuration - a list of collapsible physical column indexes.
   * @param {Array} destinationCollapsedColumns Destination collapsible configuration - a list of collapsible physical column indexes.
   * @param {boolean} collapsePossible `true`, if all of the column indexes are withing the bounds of the collapsed sections, `false` otherwise.
   * @param {boolean} successfullyCollapsed `true`, if the action affected any non-collapsible column, `false` otherwise.
   */
  "afterColumnCollapse",
  /**
   * Fired by {@link CollapsibleColumns} plugin before columns expand. This hook is fired when {@link Options#collapsibleColumns} option is enabled.
   *
   * @event Hooks#beforeColumnExpand
   * @since 8.0.0
   * @param {Array} currentCollapsedColumns Current collapsible configuration - a list of collapsible physical column indexes.
   * @param {Array} destinationCollapsedColumns Destination collapsible configuration - a list of collapsible physical column indexes.
   * @param {boolean} expandPossible `true`, if all of the column indexes are withing the bounds of the collapsed sections, `false` otherwise.
   * @returns {undefined|boolean} If the callback returns `false`, the expanding action will not be completed.
   */
  "beforeColumnExpand",
  /**
   * Fired by {@link CollapsibleColumns} plugin before columns expand. This hook is fired when {@link Options#collapsibleColumns} option is enabled.
   *
   * @event Hooks#afterColumnExpand
   * @since 8.0.0
   * @param {Array} currentCollapsedColumns Current collapsible configuration - a list of collapsible physical column indexes.
   * @param {Array} destinationCollapsedColumns Destination collapsible configuration - a list of collapsible physical column indexes.
   * @param {boolean} expandPossible `true`, if all of the column indexes are withing the bounds of the collapsed sections, `false` otherwise.
   * @param {boolean} successfullyExpanded `true`, if the action affected any non-collapsible column, `false` otherwise.
   */
  "afterColumnExpand",
  /**
   * Fired by {@link AutoColumnSize} plugin within SampleGenerator utility.
   *
   * @event Hooks#modifyAutoColumnSizeSeed
   * @since 8.4.0
   * @param {string|undefined} seed Seed ID, unique name to categorize samples.
   * @param {object} cellProperties Object containing the cell properties.
   * @param {*} cellValue Value of the cell.
   */
  "modifyAutoColumnSizeSeed"
], Uz = mt`The plugin hook "[hookName]" was removed in Handsontable [removedInVersion].\x20
  Please consult release notes https://github.com/handsontable/handsontable/releases/tag/[removedInVersion] to\x20
  learn about the migration path.`, XA = /* @__PURE__ */ new Map([["modifyRow", "8.0.0"], ["modifyCol", "8.0.0"], ["unmodifyRow", "8.0.0"], ["unmodifyCol", "8.0.0"], ["skipLengthCache", "8.0.0"], ["hiddenColumn", "8.0.0"], ["hiddenRow", "8.0.0"]]), qA = /* @__PURE__ */ new Map([[]]), rf = /* @__PURE__ */ new WeakMap();
class px {
  static getSingleton() {
    return jz();
  }
  /**
   * @type {object}
   */
  /**
   *
   */
  constructor() {
    Vz(this, "globalBucket", void 0), this.globalBucket = this.createEmptyBucket();
  }
  /**
   * Returns a new object with empty handlers related to every registered hook name.
   *
   * @returns {object} The empty bucket object.
   *
   * @example
   * ```js
   * Handsontable.hooks.createEmptyBucket();
   * // Results:
   * {
   * ...
   * afterCreateCol: [],
   * afterCreateRow: [],
   * beforeInit: [],
   * ...
   * }
   * ```
   */
  createEmptyBucket() {
    const e = /* @__PURE__ */ Object.create(null);
    return T(bl, (n) => {
      e[n] = [], this.initOrderMap(e, n);
    }), e;
  }
  /**
   * Get hook bucket based on the context of the object or if argument is `undefined`, get the global hook bucket.
   *
   * @param {object} [context=null] A Handsontable instance.
   * @returns {object} Returns a global or Handsontable instance bucket.
   */
  getBucket() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
    return e ? (e.pluginHookBucket || (e.pluginHookBucket = this.createEmptyBucket()), e.pluginHookBucket) : this.globalBucket;
  }
  /**
   * Adds a listener (globally or locally) to a specified hook name.
   * If the `context` parameter is provided, the hook will be added only to the instance it references.
   * Otherwise, the callback will be used everytime the hook fires on any Handsontable instance.
   * You can provide an array of callback functions as the `callback` argument, this way they will all be fired
   * once the hook is triggered.
   *
   * @see Core#addHook
   * @param {string} key Hook name.
   * @param {Function|Array} callback Callback function or an array of functions.
   * @param {object} [context=null] The context for the hook callback to be added - a Handsontable instance or leave empty.
   * @param {number} [orderIndex] Order index of the callback.
   *                              If > 0, the callback will be added after the others, for example, with an index of 1, the callback will be added before the ones with an index of 2, 3, etc., but after the ones with an index of 0 and lower.
   *                              If < 0, the callback will be added before the others, for example, with an index of -1, the callback will be added after the ones with an index of -2, -3, etc., but before the ones with an index of 0 and higher.
   *                              If 0 or no order index is provided, the callback will be added between the "negative" and "positive" indexes.
   * @returns {Hooks} Instance of Hooks.
   *
   * @example
   * ```js
   * // single callback, added locally
   * Handsontable.hooks.add('beforeInit', myCallback, hotInstance);
   *
   * // single callback, added globally
   * Handsontable.hooks.add('beforeInit', myCallback);
   *
   * // multiple callbacks, added locally
   * Handsontable.hooks.add('beforeInit', [myCallback, anotherCallback], hotInstance);
   *
   * // multiple callbacks, added globally
   * Handsontable.hooks.add('beforeInit', [myCallback, anotherCallback]);
   * ```
   */
  add(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null, r = arguments.length > 3 ? arguments[3] : void 0;
    if (Array.isArray(n))
      T(n, (o) => this.add(e, o, i));
    else {
      XA.has(e) && gt(qS(Uz, {
        hookName: e,
        removedInVersion: XA.get(e)
      })), qA.has(e) && gt(qA.get(e));
      const o = this.getBucket(i);
      if (typeof o[e] == "undefined" && (this.register(e), o[e] = [], this.initOrderMap(o, e)), n.skip = !1, o[e].indexOf(n) === -1) {
        let s = !1;
        n.initialHook && T(o[e], (a, l) => {
          if (a.initialHook)
            return o[e][l] = n, s = !0, !1;
        }), s || o[e].push(n);
      }
      this.setCallbackOrderIndex(o, e, n, r), this.orderBucketByOrderIndex(o, e);
    }
    return this;
  }
  /**
   * Adds a listener to a specified hook. After the hook runs this listener will be automatically removed from the bucket.
   *
   * @see Core#addHookOnce
   * @param {string} key Hook/Event name.
   * @param {Function|Array} callback Callback function.
   * @param {object} [context=null] A Handsontable instance.
   * @param {number} [orderIndex] Order index of the callback.
   *                              If > 0, the callback will be added after the others, for example, with an index of 1, the callback will be added before the ones with an index of 2, 3, etc., but after the ones with an index of 0 and lower.
   *                              If < 0, the callback will be added before the others, for example, with an index of -1, the callback will be added after the ones with an index of -2, -3, etc., but before the ones with an index of 0 and higher.
   *                              If 0 or no order index is provided, the callback will be added between the "negative" and "positive" indexes.
   *
   * @example
   * ```js
   * Handsontable.hooks.once('beforeInit', myCallback, hotInstance);
   * ```
   */
  once(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null, r = arguments.length > 3 ? arguments[3] : void 0;
    Array.isArray(n) ? T(n, (o) => this.once(e, o, i)) : (n.runOnce = !0, this.add(e, n, i, r));
  }
  /**
   * Removes a listener from a hook with a given name. If the `context` argument is provided, it removes a listener from a local hook assigned to the given Handsontable instance.
   *
   * @see Core#removeHook
   * @param {string} key Hook/Event name.
   * @param {Function} callback Callback function (needs the be the function that was previously added to the hook).
   * @param {object} [context=null] Handsontable instance.
   * @returns {boolean} Returns `true` if hook was removed, `false` otherwise.
   *
   * @example
   * ```js
   * Handsontable.hooks.remove('beforeInit', myCallback);
   * ```
   */
  remove(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
    const r = this.getBucket(i);
    return typeof r[e] != "undefined" && r[e].indexOf(n) >= 0 ? (n.skip = !0, !0) : !1;
  }
  /**
   * Checks whether there are any registered listeners for the provided hook name.
   * If the `context` parameter is provided, it only checks for listeners assigned to the given Handsontable instance.
   *
   * @param {string} key Hook name.
   * @param {object} [context=null] A Handsontable instance.
   * @returns {boolean} `true` for success, `false` otherwise.
   */
  has(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
    const i = this.getBucket(n);
    return !!(i[e] !== void 0 && i[e].length);
  }
  /**
   * Runs all local and global callbacks assigned to the hook identified by the `key` parameter.
   * It returns either a return value from the last called callback or the first parameter (`p1`) passed to the `run` function.
   *
   * @see Core#runHooks
   * @param {object} context Handsontable instance.
   * @param {string} key Hook/Event name.
   * @param {*} [p1] Parameter to be passed as an argument to the callback function.
   * @param {*} [p2] Parameter to be passed as an argument to the callback function.
   * @param {*} [p3] Parameter to be passed as an argument to the callback function.
   * @param {*} [p4] Parameter to be passed as an argument to the callback function.
   * @param {*} [p5] Parameter to be passed as an argument to the callback function.
   * @param {*} [p6] Parameter to be passed as an argument to the callback function.
   * @returns {*} Either a return value from the last called callback or `p1`.
   *
   * @example
   * ```js
   * Handsontable.hooks.run(hot, 'beforeInit');
   * ```
   */
  run(e, n, i, r, o, s, a, l) {
    {
      const u = this.globalBucket[n], c = u ? u.length : 0;
      let h = 0;
      if (c)
        for (; h < c; ) {
          if (!u[h] || u[h].skip) {
            h += 1;
            continue;
          }
          const d = Am(u[h], e, i, r, o, s, a, l);
          d !== void 0 && (i = d), u[h] && u[h].runOnce && this.remove(n, u[h]), h += 1;
        }
    }
    {
      const u = this.getBucket(e)[n], c = u ? u.length : 0;
      let h = 0;
      if (c)
        for (; h < c; ) {
          if (!u[h] || u[h].skip) {
            h += 1;
            continue;
          }
          const d = Am(u[h], e, i, r, o, s, a, l);
          d !== void 0 && (i = d), u[h] && u[h].runOnce && this.remove(n, u[h], e), h += 1;
        }
    }
    return i;
  }
  /**
   * Destroy all listeners connected to the context. If no context is provided, the global listeners will be destroyed.
   *
   * @param {object} [context=null] A Handsontable instance.
   * @example
   * ```js
   * // destroy the global listeners
   * Handsontable.hooks.destroy();
   *
   * // destroy the local listeners
   * Handsontable.hooks.destroy(hotInstance);
   * ```
   */
  destroy() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
    Fe(this.getBucket(e), (n, i, r) => r[i].length = 0);
  }
  /**
   * Registers a hook name (adds it to the list of the known hook names). Used by plugins.
   * It is not necessary to call register, but if you use it, your plugin hook will be used returned by
   * the `getRegistered` method. (which itself is used in the [demo](@/guides/getting-started/events-and-hooks/events-and-hooks.md)).
   *
   * @param {string} key The hook name.
   *
   * @example
   * ```js
   * Handsontable.hooks.register('myHook');
   * ```
   */
  register(e) {
    this.isRegistered(e) || bl.push(e);
  }
  /**
   * Deregisters a hook name (removes it from the list of known hook names).
   *
   * @param {string} key The hook name.
   *
   * @example
   * ```js
   * Handsontable.hooks.deregister('myHook');
   * ```
   */
  deregister(e) {
    this.isRegistered(e) && bl.splice(bl.indexOf(e), 1);
  }
  /**
   * Returns a boolean value depending on if a hook by such name has been removed or deprecated.
   *
   * @param {string} hookName The hook name to check.
   * @returns {boolean} Returns `true` if the provided hook name was marked as deprecated or
   * removed from API, `false` otherwise.
   * @example
   * ```js
   * Handsontable.hooks.isDeprecated('skipLengthCache');
   *
   * // Results:
   * true
   * ```
   */
  isDeprecated(e) {
    return qA.has(e) || XA.has(e);
  }
  /**
   * Returns a boolean depending on if a hook by such name has been registered.
   *
   * @param {string} hookName The hook name to check.
   * @returns {boolean} `true` for success, `false` otherwise.
   * @example
   * ```js
   * Handsontable.hooks.isRegistered('beforeInit');
   *
   * // Results:
   * true
   * ```
   */
  isRegistered(e) {
    return bl.indexOf(e) >= 0;
  }
  /**
   * Returns an array of registered hooks.
   *
   * @returns {Array} An array of registered hooks.
   *
   * @example
   * ```js
   * Handsontable.hooks.getRegistered();
   *
   * // Results:
   * [
   * ...
   *   'beforeInit',
   *   'beforeRender',
   *   'beforeSetRangeEnd',
   *   'beforeDrawBorders',
   *   'beforeChange',
   * ...
   * ]
   * ```
   */
  getRegistered() {
    return bl;
  }
  /**
   * Sets the order index of the callback in the bucket object.
   *
   * @private
   * @param {object} bucket The bucket object.
   * @param {string} key Hook name.
   * @param {Function} callback Callback function.
   * @param {number|undefined} orderIndex Order index of the callback.
   */
  setCallbackOrderIndex(e, n, i, r) {
    const o = Number.isInteger(r) ? r : 0, s = this.getCallbackOrderMap(e, n);
    s.set(o, [...s.get(o) || [], i]);
  }
  /**
   * Reorders the callbacks in the bucket object by their order index.
   *
   * @private
   * @param {objcet} bucket The bucket object.
   * @param {string} key Hook name.
   */
  orderBucketByOrderIndex(e, n) {
    const i = this.getCallbackOrderMap(e, n);
    i === void 0 || i.size === 0 || i.size === 1 && i.has(0) || (e[n] = [...i].sort((r, o) => r[0] - o[0]).flatMap((r) => {
      let [, o] = r;
      return o;
    }));
  }
  /**
   * Extends the bucket object with the order property.
   *
   * @private
   * @param {object} bucket The bucket object.
   * @param {string} hook The hook name.
   */
  initOrderMap(e, n) {
    rf.has(e) || rf.set(e, []), rf.get(e)[n] = /* @__PURE__ */ new Map();
  }
  /**
   * Returns the order map for the provided hook.
   *
   * @private
   * @param {object} bucket The bucket object.
   * @param {string} hook The hook name.
   * @returns {Map<number, Array<Function>>} Returns the order map for the provided hook.
   */
  getCallbackOrderMap(e, n) {
    return rf.get(e)[n];
  }
}
const Yz = new px();
function jz() {
  return Yz;
}
const he = px, ZA = /* @__PURE__ */ new Map();
function Or() {
  let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "common";
  ZA.has(t) || ZA.set(t, /* @__PURE__ */ new Map());
  const e = ZA.get(t);
  function n(a, l) {
    e.set(a, l);
  }
  function i(a) {
    return e.get(a);
  }
  function r(a) {
    return e.has(a);
  }
  function o() {
    return [...e.keys()];
  }
  function s() {
    return [...e.values()];
  }
  return {
    register: n,
    getItem: i,
    hasItem: r,
    getNames: o,
    getValues: s
  };
}
const by = /* @__PURE__ */ new WeakMap(), {
  register: Gz,
  getItem: wx,
  hasItem: Ax,
  getNames: zz,
  getValues: Pce
} = Or("editors");
function Kz(t) {
  const e = {}, n = t;
  this.getConstructor = function() {
    return t;
  }, this.getInstance = function(i) {
    return i.guid in e || (e[i.guid] = new n(i)), e[i.guid];
  }, he.getSingleton().add("afterDestroy", function() {
    e[this.guid] = null;
  });
}
function Cx(t, e) {
  let n;
  if (typeof t == "function")
    by.get(t) || ai(null, t), n = by.get(t);
  else if (typeof t == "string")
    n = wx(t);
  else
    throw Error('Only strings and functions can be passed as "editor" parameter');
  if (!n)
    throw Error(`No editor registered under name "${t}"`);
  return n.getInstance(e);
}
function Cm(t) {
  if (typeof t == "function")
    return t;
  if (!Ax(t))
    throw Error(`No registered editor found under "${t}" name`);
  return wx(t).getConstructor();
}
function ai(t, e) {
  t && typeof t != "string" && (e = t, t = e.EDITOR_TYPE);
  const n = new Kz(e);
  typeof t == "string" && Gz(t, n), by.set(e, n);
}
function Qz(t, e, n) {
  return (e = Xz(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Xz(t) {
  var e = qz(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function qz(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
let Ug = 0;
class Zz {
  /**
   * @param {object} [context=null] An object to which event listeners will be stored.
   * @private
   */
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
    Qz(this, "context", void 0), this.context = e || this, this.context.eventListeners || (this.context.eventListeners = []);
  }
  /**
   * Register specified listener (`eventName`) to the element.
   *
   * @param {Element} element Target element.
   * @param {string} eventName Event name.
   * @param {Function} callback Function which will be called after event occur.
   * @param {AddEventListenerOptions|boolean} [options] Listener options if object or useCapture if boolean.
   * @returns {Function} Returns function which you can easily call to remove that event.
   */
  addEventListener(e, n, i) {
    let r = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1;
    function o(s) {
      i.call(this, Jz(s));
    }
    return this.context.eventListeners.push({
      element: e,
      event: n,
      callback: i,
      callbackProxy: o,
      options: r,
      eventManager: this
    }), e.addEventListener(n, o, r), Ug += 1, () => {
      this.removeEventListener(e, n, i);
    };
  }
  /**
   * Remove the event listener previously registered.
   *
   * @param {Element} element Target element.
   * @param {string} eventName Event name.
   * @param {Function} callback Function to remove from the event target. It must be the same as during registration listener.
   * @param {boolean} [onlyOwnEvents] Whether whould remove only events registered using this instance of EventManager.
   */
  removeEventListener(e, n, i) {
    let r = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1, o = this.context.eventListeners.length, s;
    for (; o; )
      if (o -= 1, s = this.context.eventListeners[o], s.event === n && s.element === e) {
        if (i && i !== s.callback || r && s.eventManager !== this)
          continue;
        this.context.eventListeners.splice(o, 1), s.element.removeEventListener(s.event, s.callbackProxy, s.options), Ug -= 1;
      }
  }
  /**
   * Clear all previously registered events.
   *
   * @private
   * @since 0.15.0-beta3
   * @param {boolean} [onlyOwnEvents] Whether whould remove only events registered using this instance of EventManager.
   */
  clearEvents() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    if (!this.context)
      return;
    let n = this.context.eventListeners.length;
    for (; n; ) {
      n -= 1;
      const i = this.context.eventListeners[n];
      e && i.eventManager !== this || (this.context.eventListeners.splice(n, 1), i.element.removeEventListener(i.event, i.callbackProxy, i.options), Ug -= 1);
    }
  }
  /**
   * Clear all previously registered events.
   */
  clear() {
    this.clearEvents();
  }
  /**
   * Destroy instance of EventManager, clearing all events of the context.
   */
  destroy() {
    this.clearEvents(), this.context = null;
  }
  /**
   * Destroy instance of EventManager, clearing only the own events.
   */
  destroyWithOwnEventsOnly() {
    this.clearEvents(!0), this.context = null;
  }
  /**
   * Trigger event at the specified target element.
   *
   * @param {Element} element Target element.
   * @param {string} eventName Event name.
   */
  fireEvent(e, n) {
    let i = e.document, r = e;
    i || (i = e.ownerDocument ? e.ownerDocument : e, r = i.defaultView);
    const o = {
      bubbles: !0,
      cancelable: n !== "mousemove",
      view: r,
      detail: 0,
      screenX: 0,
      screenY: 0,
      clientX: 1,
      clientY: 1,
      ctrlKey: !1,
      altKey: !1,
      shiftKey: !1,
      metaKey: !1,
      button: 0,
      relatedTarget: void 0
    };
    let s;
    i.createEvent ? (s = i.createEvent("MouseEvents"), s.initMouseEvent(n, o.bubbles, o.cancelable, o.view, o.detail, o.screenX, o.screenY, o.clientX, o.clientY, o.ctrlKey, o.altKey, o.shiftKey, o.metaKey, o.button, i.body.parentNode)) : s = i.createEventObject(), e.dispatchEvent ? e.dispatchEvent(s) : e.fireEvent(`on${n}`, s);
  }
}
function Jz(t) {
  const e = t.stopImmediatePropagation;
  return t.stopImmediatePropagation = function() {
    e.apply(this), Zi(this);
  }, t;
}
const Qi = Zz;
function eK() {
  return Ug;
}
function tK(t, e) {
  nK(t, e), e.add(t);
}
function nK(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ra(t, e, n) {
  return (e = iK(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function iK(t) {
  var e = rK(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function rK(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function DE(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var JA = /* @__PURE__ */ new WeakSet();
class Ry {
  /**
   * @param {Core} hotInstance The Handsontable instance.
   * @param {TableMeta} tableMeta The table meta instance.
   * @param {Selection} selection The selection instance.
   */
  constructor(e, n, i) {
    tK(this, JA), ra(this, "hot", void 0), ra(this, "tableMeta", void 0), ra(this, "selection", void 0), ra(this, "eventManager", void 0), ra(this, "destroyed", !1), ra(this, "activeEditor", void 0), ra(this, "cellProperties", void 0), this.hot = e, this.tableMeta = n, this.selection = i, this.eventManager = new Qi(e), this.hot.addHook("afterDocumentKeyDown", (r) => DE(JA, this, oK).call(this, r)), this.eventManager.addEventListener(this.hot.rootDocument.documentElement, "compositionstart", (r) => {
      !this.destroyed && this.hot.isListening() && this.openEditor("", r);
    }), this.hot.view._wt.update("onCellDblClick", (r, o, s) => DE(JA, this, sK).call(this, r, o, s));
  }
  /**
   * Get active editor.
   *
   * @returns {BaseEditor}
   */
  getActiveEditor() {
    return this.activeEditor;
  }
  /**
   * Prepare text input to be displayed at given grid cell.
   */
  prepareEditor() {
    var e;
    if (this.activeEditor && this.activeEditor.isWaiting()) {
      this.closeEditor(!1, !1, (u) => {
        u && this.prepareEditor();
      });
      return;
    }
    const n = (e = this.hot.getSelectedRangeLast()) === null || e === void 0 ? void 0 : e.highlight;
    if (!n || n.isHeader())
      return;
    const {
      row: i,
      col: r
    } = n, o = this.hot.runHooks("modifyGetCellCoords", i, r);
    let s = i, a = r;
    if (Array.isArray(o) && ([s, a] = o), this.cellProperties = this.hot.getCellMeta(s, a), !this.isCellEditable()) {
      this.clearActiveEditor();
      return;
    }
    const l = this.hot.getCell(i, r, !0);
    if (l) {
      const u = this.hot.getCellEditor(this.cellProperties), c = this.hot.colToProp(a), h = this.hot.getSourceDataAtCell(this.hot.toPhysicalRow(s), a);
      this.activeEditor = Cx(u, this.hot), this.activeEditor.prepare(i, r, c, l, h, this.cellProperties);
    }
  }
  /**
   * Check is editor is opened/showed.
   *
   * @returns {boolean}
   */
  isEditorOpened() {
    return this.activeEditor && this.activeEditor.isOpened();
  }
  /**
   * Open editor with initial value.
   *
   * @param {null|string} newInitialValue New value from which editor will start if handled property it's not the `null`.
   * @param {Event} event The event object.
   * @param {boolean} [enableFullEditMode=false] When true, an editor works in full editing mode. Mode disallows closing an editor
   *                                             when arrow keys are pressed.
   */
  openEditor(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    if (!this.isCellEditable()) {
      this.clearActiveEditor();
      return;
    }
    const r = this.hot.getSelectedRangeLast();
    let o = this.hot.runHooks("beforeBeginEditing", r.highlight.row, r.highlight.col, e, n, i);
    if (n instanceof MouseEvent && typeof o != "boolean" && (o = this.hot.selection.getLayerLevel() === 0 && r.isSingle()), o === !1) {
      this.clearActiveEditor();
      return;
    }
    this.activeEditor || (this.hot.scrollToFocusedCell(), this.prepareEditor()), this.activeEditor && (i && this.activeEditor.enableFullEditMode(), this.activeEditor.beginEditing(e, n));
  }
  /**
   * Close editor, finish editing cell.
   *
   * @param {boolean} restoreOriginalValue If `true`, then closes editor without saving value from the editor into a cell.
   * @param {boolean} isCtrlPressed If `true`, then editor will save value to each cell in the last selected range.
   * @param {Function} callback The callback function, fired after editor closing.
   */
  closeEditor(e, n, i) {
    this.activeEditor ? this.activeEditor.finishEditing(e, n, i) : i && i(!1);
  }
  /**
   * Close editor and save changes.
   *
   * @param {boolean} isCtrlPressed If `true`, then editor will save value to each cell in the last selected range.
   */
  closeEditorAndSaveChanges(e) {
    this.closeEditor(!1, e);
  }
  /**
   * Close editor and restore original value.
   *
   * @param {boolean} isCtrlPressed Indication of whether the CTRL button is pressed.
   */
  closeEditorAndRestoreOriginalValue(e) {
    this.closeEditor(!0, e);
  }
  /**
   * Clears reference to an instance of the active editor.
   *
   * @private
   */
  clearActiveEditor() {
    this.activeEditor = void 0;
  }
  /**
   * Checks if the currently selected cell (pointed by selection highlight coords) is editable.
   * Editable cell is when:
   *   - the cell has defined an editor type;
   *   - the cell is not marked as read-only;
   *   - the cell is not hidden.
   *
   * @private
   * @returns {boolean}
   */
  isCellEditable() {
    const e = this.hot.getSelectedRangeLast();
    if (!e)
      return !1;
    const n = this.hot.getCellEditor(this.cellProperties), {
      row: i,
      col: r
    } = e.highlight, {
      rowIndexMapper: o,
      columnIndexMapper: s
    } = this.hot, a = o.isHidden(this.hot.toPhysicalRow(i)) || s.isHidden(this.hot.toPhysicalColumn(r));
    return !(this.cellProperties.readOnly || !n || a);
  }
  /**
   * Controls selection's behavior after clicking `Enter`.
   *
   * @private
   * @param {KeyboardEvent} event The keyboard event object.
   */
  moveSelectionAfterEnter(e) {
    const n = Xe({}, typeof this.tableMeta.enterMoves == "function" ? this.tableMeta.enterMoves(e) : this.tableMeta.enterMoves);
    e.shiftKey && (n.row = -n.row, n.col = -n.col), this.hot.selection.isMultiple() ? this.selection.transformFocus(n.row, n.col) : this.selection.transformStart(n.row, n.col, !0);
  }
  /**
   * Destroy the instance.
   */
  destroy() {
    this.destroyed = !0, this.eventManager.destroy();
  }
}
function oK(t) {
  const e = this.hot.getSelectedRangeLast();
  if (!this.hot.isListening() || !e || e.highlight.isHeader() || Os(t))
    return;
  const {
    keyCode: n
  } = t, i = (t.ctrlKey || t.metaKey) && !t.altKey;
  (!this.activeEditor || this.activeEditor && !this.activeEditor.isWaiting()) && !gb(n) && !cx(n) && !i && !this.isEditorOpened() && this.openEditor("", t);
}
function sK(t, e) {
  e.isCell() && this.openEditor(null, t, !0);
}
const kE = /* @__PURE__ */ new WeakMap();
Ry.getInstance = function(t, e, n) {
  let i = kE.get(t);
  return i || (i = new Ry(t, e, n), kE.set(t, i)), i;
};
const aK = Ry;
function lK(t, e) {
  vx(t, e), e.add(t);
}
function Xu(t, e, n) {
  vx(t, e), e.set(t, n);
}
function vx(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function kt(t, e) {
  return t.get(Wo(t, e));
}
function qu(t, e, n) {
  return t.set(Wo(t, e), n), n;
}
function Wo(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const Cu = Object.freeze({
  CELL: "cell",
  MIXED: "mixed"
});
var vn = /* @__PURE__ */ new WeakMap(), of = /* @__PURE__ */ new WeakMap(), sf = /* @__PURE__ */ new WeakMap(), af = /* @__PURE__ */ new WeakMap(), lf = /* @__PURE__ */ new WeakMap(), bs = /* @__PURE__ */ new WeakSet();
class uK {
  constructor(e) {
    var n = this;
    lK(this, bs), Xu(this, vn, void 0), Xu(this, of, void 0), Xu(this, sf, 1), Xu(this, af, null), Xu(this, lf, /* @__PURE__ */ new Map());
    const i = e.getSettings();
    qu(vn, this, e), qu(of, this, i.imeFastEdit ? Cu.MIXED : Cu.CELL), kt(vn, this).addHook("afterUpdateSettings", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return Wo(bs, n, hK).call(n, ...o);
    }), kt(vn, this).addHook("afterSelection", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return Wo(bs, n, LE).call(n, ...o);
    }), kt(vn, this).addHook("afterSelectionFocusSet", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return Wo(bs, n, LE).call(n, ...o);
    }), kt(vn, this).addHook("afterSelectionEnd", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return Wo(bs, n, cK).call(n, ...o);
    });
  }
  /**
   * Get the current focus mode.
   *
   * @returns {'cell' | 'mixed'}
   */
  getFocusMode() {
    return kt(of, this);
  }
  /**
   * Set the focus mode.
   *
   * @param {'cell' | 'mixed'} focusMode The new focus mode.
   */
  setFocusMode(e) {
    Object.values(Cu).includes(e) ? qu(of, this, e) : gt(`"${e}" is not a valid focus mode.`);
  }
  /**
   * Get the delay after which the focus will change from the cell elements to the active editor's `TEXTAREA`
   * element if the focus mode is set to 'mixed'.
   *
   * @returns {number} Delay in milliseconds.
   */
  getRefocusDelay() {
    return kt(sf, this);
  }
  /**
   * Set the delay after which the focus will change from the cell elements to the active editor's `TEXTAREA`
   * element if the focus mode is set to 'mixed'.
   *
   * @param {number} delay Delay in milliseconds.
   */
  setRefocusDelay(e) {
    qu(sf, this, e);
  }
  /**
   * Set the function to be used as the "refocus element" getter. It should return a focusable HTML element.
   *
   * @param {Function} getRefocusElementFunction The refocus element getter.
   */
  setRefocusElementGetter(e) {
    qu(af, this, e);
  }
  /**
   * Get the element to be used when refocusing the browser after a delay in case of the focus mode being 'mixed'.
   *
   * @returns {HTMLTextAreaElement|HTMLElement|undefined}
   */
  getRefocusElement() {
    if (typeof kt(af, this) == "function")
      return kt(af, this).call(this);
    var e;
    return (e = kt(vn, this).getActiveEditor()) === null || e === void 0 ? void 0 : e.TEXTAREA;
  }
  /**
   * Set the browser's focus to the highlighted cell of the last selection.
   *
   * @param {HTMLTableCellElement} [selectedCell] The highlighted cell/header element.
   */
  focusOnHighlightedCell(e) {
    const n = (i) => {
      var r, o;
      const s = (r = kt(vn, this).getSelectedRangeLast()) === null || r === void 0 ? void 0 : r.highlight;
      if (!s)
        return;
      let a = kt(vn, this).runHooks("modifyFocusedElement", s.row, s.col, i);
      a instanceof HTMLElement || (a = i), a && !((o = kt(vn, this).getActiveEditor()) !== null && o !== void 0 && o.isOpened()) && a.focus({
        preventScroll: !0
      });
    };
    e ? n(e) : Wo(bs, this, wb).call(this, (i) => n(i));
  }
  /**
   * Set the focus to the active editor's `TEXTAREA` element after the provided delay. If no delay is provided, it
   * will be taken from the manager's configuration.
   *
   * @param {number} [delay] Delay in milliseconds.
   */
  refocusToEditorTextarea() {
    var e;
    let n = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : kt(sf, this);
    const i = this.getRefocusElement();
    kt(vn, this).getSettings().imeFastEdit && !((e = kt(vn, this).getActiveEditor()) !== null && e !== void 0 && e.isOpened()) && i && (kt(lf, this).has(n) || kt(lf, this).set(n, Ip(() => {
      i.select();
    }, n)), kt(lf, this).get(n)());
  }
}
function wb(t) {
  var e;
  const n = (e = kt(vn, this).getSelectedRangeLast()) === null || e === void 0 ? void 0 : e.highlight;
  if (!n || !kt(vn, this).selection.isCellVisible(n)) {
    t(null);
    return;
  }
  const i = kt(vn, this).getCell(n.row, n.col, !0);
  i === null ? kt(vn, this).addHookOnce("afterScroll", () => {
    t(kt(vn, this).getCell(n.row, n.col, !0));
  }) : t(i);
}
function LE() {
  Wo(bs, this, wb).call(this, (t) => {
    const {
      activeElement: e
    } = kt(vn, this).rootDocument;
    e && Mp(e) && e.blur(), this.focusOnHighlightedCell(t);
  });
}
function cK() {
  Wo(bs, this, wb).call(this, (t) => {
    this.getFocusMode() === Cu.MIXED && t.nodeName === "TD" && this.refocusToEditorTextarea();
  });
}
function hK(t) {
  typeof t.imeFastEdit == "boolean" && this.setFocusMode(t.imeFastEdit ? Cu.MIXED : Cu.CELL);
}
var dK = co, fK = al, gK = Xs, mK = gK("match"), pK = function(t) {
  var e;
  return dK(t) && ((e = t[mK]) !== void 0 ? !!e : fK(t) === "RegExp");
}, wK = ts, AK = function() {
  var t = wK(this), e = "";
  return t.hasIndices && (e += "d"), t.global && (e += "g"), t.ignoreCase && (e += "i"), t.multiline && (e += "m"), t.dotAll && (e += "s"), t.unicode && (e += "u"), t.unicodeSets && (e += "v"), t.sticky && (e += "y"), e;
}, CK = Tr, vK = ho, yK = op, SK = AK, BE = RegExp.prototype, bK = function(t) {
  var e = t.flags;
  return e === void 0 && !("flags" in BE) && !vK(t, "flags") && yK(BE, t) ? CK(SK, t) : e;
}, Ab = an, RK = ld, EK = Math.floor, eC = Ab("".charAt), TK = Ab("".replace), tC = Ab("".slice), MK = /\$([$&'`]|\d{1,2}|<[^>]*>)/g, IK = /\$([$&'`]|\d{1,2})/g, OK = function(t, e, n, i, r, o) {
  var s = n + t.length, a = i.length, l = IK;
  return r !== void 0 && (r = RK(r), l = MK), TK(o, l, function(u, c) {
    var h;
    switch (eC(c, 0)) {
      case "$":
        return "$";
      case "&":
        return t;
      case "`":
        return tC(e, 0, n);
      case "'":
        return tC(e, s);
      case "<":
        h = r[tC(c, 1, -1)];
        break;
      default:
        var d = +c;
        if (d === 0)
          return u;
        if (d > a) {
          var g = EK(d / 10);
          return g === 0 ? u : g <= a ? i[g - 1] === void 0 ? eC(c, 1) : i[g - 1] + eC(c, 1) : u;
        }
        h = i[d - 1];
    }
    return h === void 0 ? "" : h;
  });
}, PK = Mn, xK = Tr, Cb = an, FE = sd, HK = Tn, _K = ip, NK = pK, Zu = cp, DK = lp, kK = bK, LK = OK, BK = Xs, FK = BK("replace"), VK = TypeError, nC = Cb("".indexOf);
Cb("".replace);
var VE = Cb("".slice), WK = Math.max;
PK({ target: "String", proto: !0 }, {
  replaceAll: function(e, n) {
    var i = FE(this), r, o, s, a, l, u, c, h, d, g, m = 0, w = "";
    if (!_K(e)) {
      if (r = NK(e), r && (o = Zu(FE(kK(e))), !~nC(o, "g")))
        throw new VK("`.replaceAll` does not allow non-global regexes");
      if (s = DK(e, FK), s)
        return xK(s, e, i, n);
    }
    for (a = Zu(i), l = Zu(e), u = HK(n), u || (n = Zu(n)), c = l.length, h = WK(1, c), d = nC(a, l); d !== -1; )
      g = u ? Zu(n(l, d, a)) : LK(l, a, d, [], void 0, n), w += VE(a, m, d) + g, m = d + c, d = d + h > a.length ? -1 : nC(a, l, d + h);
    return m < a.length && (w += VE(a, m)), w;
  }
});
const yx = {
  "&nbsp;": " ",
  "&amp;": "&",
  "&lt;": "<",
  "&gt;": ">"
}, $K = new RegExp(Object.keys(yx).map((t) => `(${t})`).join("|"), "gi");
function UK(t) {
  return (t && t.nodeName || "") === "TABLE";
}
function Ey(t) {
  const e = t.hasColHeaders(), n = t.hasRowHeaders(), i = [e ? -1 : 0, n ? -1 : 0, t.countRows() - 1, t.countCols() - 1], r = t.getData(...i), o = r.length, s = o > 0 ? r[0].length : 0, a = ["<table>", "</table>"], l = e ? ["<thead>", "</thead>"] : [], u = ["<tbody>", "</tbody>"], c = n ? 1 : 0, h = e ? 1 : 0;
  for (let d = 0; d < o; d += 1) {
    const g = e && d === 0, m = [];
    for (let C = 0; C < s; C += 1) {
      const S = !g && n && C === 0;
      let b = "";
      if (g)
        b = `<th>${t.getColHeader(C - c)}</th>`;
      else if (S)
        b = `<th>${t.getRowHeader(d - h)}</th>`;
      else {
        const R = r[d][C], {
          hidden: _,
          rowspan: O,
          colspan: $
        } = t.getCellMeta(d - h, C - c);
        if (!_) {
          const U = [];
          if (O && U.push(`rowspan="${O}"`), $ && U.push(`colspan="${$}"`), zt(R))
            b = `<td ${U.join(" ")}></td>`;
          else {
            const G = R.toString().replace("<", "&lt;").replace(">", "&gt;").replace(/(<br(\s*|\/)>(\r\n|\n)?|\r\n|\n)/g, `<br>\r
`).replace(/\x20/gi, "&nbsp;").replace(/\t/gi, "&#9;");
            b = `<td ${U.join(" ")}>${G}</td>`;
          }
        }
      }
      m.push(b);
    }
    const w = ["<tr>", ...m, "</tr>"].join("");
    g ? l.splice(1, 0, w) : u.splice(-1, 0, w);
  }
  return a.splice(1, 0, l.join(""), u.join("")), a.join("");
}
function Ty(t) {
  const e = t.length, n = ["<table>"];
  for (let i = 0; i < e; i += 1) {
    const r = t[i], o = r.length, s = [];
    i === 0 && n.push("<tbody>");
    for (let a = 0; a < o; a += 1) {
      const l = r[a], u = zt(l) ? "" : l.toString().replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/(<br(\s*|\/)>(\r\n|\n)?|\r\n|\n)/g, `<br>\r
`).replace(/\x20{2,}/gi, (c) => `<span style="mso-spacerun: yes">${"&nbsp;".repeat(c.length - 1)} </span>`).replace(/\t/gi, "&#9;");
      s.push(`<td>${u}</td>`);
    }
    n.push("<tr>", ...s, "</tr>"), i + 1 === e && n.push("</tbody>");
  }
  return n.push("</table>"), n.join("");
}
function Sx(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : document;
  const n = {}, i = e.createDocumentFragment(), r = e.createElement("div");
  i.appendChild(r);
  let o = t;
  if (typeof o == "string") {
    const R = o.replace(/<td\b[^>]*?>([\s\S]*?)<\/\s*td>/g, (_) => {
      const O = _.match(/<td\b[^>]*?>/g)[0], $ = /<p.*?>/g, U = _.substring(O.length, _.lastIndexOf("<")).trim().replaceAll(/\n\s+/g, " ").replaceAll($, `
`).replace(`
`, "").replaceAll(/<\/(.*)>\s+$/mg, "</$1>").replace(/(<(?!br)([^>]+)>)/gi, "").replaceAll(/^&nbsp;$/mg, "");
      return `${O}${U}</td>`;
    });
    r.insertAdjacentHTML("afterbegin", `${R}`), o = r.querySelector("table");
  }
  if (!o || !UK(o))
    return;
  const s = r.querySelector('meta[name$="enerator"]'), a = o.querySelector("tbody th") !== null, l = o.querySelector("tr"), u = l ? Array.from(l.cells).reduce((R, _) => R + _.colSpan, 0) - (a ? 1 : 0) : 0, c = o.tFoot && Array.from(o.tFoot.rows) || [], h = [];
  let d = !1, g = 0, m = 0;
  if (o.tHead) {
    const R = Array.from(o.tHead.rows).filter((_) => {
      const O = _.querySelector("td") !== null;
      return O && h.push(_), !O;
    });
    g = R.length, d = g > 0, g > 1 ? n.nestedHeaders = Array.from(R).reduce((_, O) => {
      const $ = Array.from(O.cells).reduce((U, G, z) => {
        if (a && z === 0)
          return U;
        const {
          colSpan: j,
          innerHTML: J
        } = G, q = j > 1 ? {
          label: J,
          colspan: j
        } : J;
        return U.push(q), U;
      }, []);
      return _.push($), _;
    }, []) : d && (n.colHeaders = Array.from(R[0].children).reduce((_, O, $) => (a && $ === 0 || _.push(O.innerHTML), _), []));
  }
  h.length && (n.fixedRowsTop = h.length), c.length && (n.fixedRowsBottom = c.length);
  const w = [...h, ...Array.from(o.tBodies).reduce((R, _) => (R.push(...Array.from(_.rows)), R), []), ...c];
  m = w.length;
  const C = new Array(m);
  for (let R = 0; R < m; R++)
    C[R] = new Array(u);
  const S = [], b = [];
  for (let R = 0; R < m; R++) {
    const _ = w[R], O = Array.from(_.cells), $ = O.length;
    for (let U = 0; U < $; U++) {
      const G = O[U], {
        nodeName: z,
        innerHTML: j,
        rowSpan: J,
        colSpan: q
      } = G, re = C[R].findIndex((me) => me === void 0);
      if (z === "TD") {
        if (J > 1 || q > 1) {
          for (let Ne = R; Ne < R + J; Ne++)
            if (Ne < m)
              for (let Ce = re; Ce < re + q; Ce++)
                C[Ne][Ce] = null;
          const Me = G.getAttribute("style");
          Me && Me.includes("mso-ignore:colspan") || S.push({
            col: re,
            row: R,
            rowspan: J,
            colspan: q
          });
        }
        let me = "";
        s && /excel/gi.test(s.content) ? me = j.replace(/[\r\n][\x20]{0,2}/g, " ").replace(/<br(\s*|\/)>[\r\n]?[\x20]{0,3}/gim, `\r
`) : me = j.replace(/<br(\s*|\/)>[\r\n]?/gim, `\r
`), C[R][re] = me.replace($K, (Me) => yx[Me]);
      } else
        b.push(j);
    }
  }
  return S.length && (n.mergeCells = S), b.length && (n.rowHeaders = b), C.length && (n.data = C), n;
}
const YK = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  _dataToHTML: Ty,
  htmlToGridSettings: Sx,
  instanceToHTML: Ey
}, Symbol.toStringTag, { value: "Module" }));
function Dn(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
  const n = typeof t;
  if (n === "number")
    return !isNaN(t) && isFinite(t);
  if (n === "string") {
    if (t.length === 0)
      return !1;
    if (t.length === 1)
      return /\d/.test(t);
    const i = Array.from(/* @__PURE__ */ new Set([".", ...e])).map((r) => `\\${r}`).join("|");
    return new RegExp(`^[+-]?(((${i})?\\d+((${i})\\d+)?(e[+-]?\\d+)?)|(0x[a-f\\d]+))$`, "i").test(t.trim());
  } else if (n === "object")
    return !!t && typeof t.valueOf() == "number" && !(t instanceof Date);
  return !1;
}
function bx(t) {
  return Dn(t, [","]);
}
function Ae(t, e, n) {
  let i = -1;
  for (typeof e == "function" ? (n = e, e = t) : i = t - 1; ++i <= e && n(i) !== !1; )
    ;
}
function My(t, e, n) {
  let i = t + 1;
  for (typeof e == "function" && (n = e, e = 0); --i >= e && n(i) !== !1; )
    ;
}
function vb(t, e) {
  return e = parseInt(e.toString().replace("%", ""), 10), e = isNaN(e) ? 0 : e, parseInt(t * e / 100, 10);
}
function ft(t, e, n) {
  return Math.min(t, e) === t ? e : Math.max(t, n) === t ? n : t;
}
const jK = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  clamp: ft,
  isNumeric: Dn,
  isNumericLike: bx,
  rangeEach: Ae,
  rangeEachReverse: My,
  valueAccordingPercent: vb
}, Symbol.toStringTag, { value: "Module" })), Iy = "asc", GK = "desc", WE = /* @__PURE__ */ new Map([[Iy, [-1, 1]], [GK, [1, -1]]]), zK = (t) => `The priority '${t}' is already declared in a map.`, KK = (t) => `The priority '${t}' is not a number.`;
function QK() {
  let {
    errorPriorityExists: t,
    errorPriorityNaN: e
  } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
  const n = /* @__PURE__ */ new Map();
  t = Et(t) ? t : zK, e = Et(e) ? e : KK;
  function i(o, s) {
    if (!Dn(o))
      throw new Error(e(o));
    if (n.has(o))
      throw new Error(t(o));
    n.set(o, s);
  }
  function r() {
    let o = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : Iy;
    const [s, a] = WE.get(o) || WE.get(Iy);
    return [...n].sort((l, u) => l[0] < u[0] ? s : a).map((l) => l[1]);
  }
  return {
    addItem: i,
    getItems: r
  };
}
const XK = (t) => `The id '${t}' is already declared in a map.`;
function _p() {
  let {
    errorIdExists: t
  } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
  const e = /* @__PURE__ */ new Map();
  t = Et(t) ? t : XK;
  function n(u, c) {
    if (l(u))
      throw new Error(t(u));
    e.set(u, c);
  }
  function i(u) {
    return e.delete(u);
  }
  function r() {
    e.clear();
  }
  function o(u) {
    const [c] = a().find((h) => {
      let [d, g] = h;
      return u === g ? d : !1;
    }) || [null];
    return c;
  }
  function s(u) {
    return e.get(u);
  }
  function a() {
    return [...e];
  }
  function l(u) {
    return e.has(u);
  }
  return {
    addItem: n,
    clear: r,
    getId: o,
    getItem: s,
    getItems: a,
    hasItem: l,
    removeItem: i
  };
}
const qK = (t) => `'${t}' value is already declared in a unique set.`;
function ZK() {
  let {
    errorItemExists: t
  } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
  const e = /* @__PURE__ */ new Set();
  t = Et(t) ? t : qK;
  function n(o) {
    if (e.has(o))
      throw new Error(t(o));
    e.add(o);
  }
  function i() {
    return [...e];
  }
  function r() {
    e.clear();
  }
  return {
    addItem: n,
    clear: r,
    getItems: i
  };
}
const yb = (t) => `There is already registered "${t}" plugin.`, JK = (t) => `There is already registered plugin on priority "${t}".`, e2 = (t) => `The priority "${t}" is not a number.`, Rx = QK({
  errorPriorityExists: JK,
  errorPriorityNaN: e2
}), Ex = ZK({
  errorItemExists: yb
}), Oy = _p({
  errorIdExists: yb
});
function Sb() {
  return [...Rx.getItems(), ...Ex.getItems()];
}
function bd(t) {
  const e = Bn(t);
  return Oy.getItem(e);
}
function t2(t) {
  return !!bd(t);
}
function rt(t, e, n) {
  [t, e, n] = i2(t, e, n), bd(t) === void 0 && n2(t, e, n);
}
function n2(t, e, n) {
  const i = Bn(t);
  if (Oy.hasItem(i))
    throw new Error(yb(i));
  n === void 0 ? Ex.addItem(i) : Rx.addItem(n, i), Oy.addItem(i, e);
}
function i2(t, e, n) {
  return typeof t == "function" && (e = t, t = e.PLUGIN_KEY, n = e.PLUGIN_PRIORITY), [t, e, n];
}
const {
  register: r2,
  getItem: o2,
  hasItem: Tx,
  getNames: s2,
  getValues: xce
} = Or("renderers");
function vm(t) {
  if (typeof t == "function")
    return t;
  if (!Tx(t))
    throw Error(`No registered renderer found under "${t}" name`);
  return o2(t);
}
function wi(t, e) {
  typeof t != "string" && (e = t, t = e.RENDERER_TYPE), r2(t, e);
}
const {
  register: a2,
  getItem: l2,
  hasItem: Mx,
  getNames: u2,
  getValues: Hce
} = Or("validators");
function bb(t) {
  if (typeof t == "function")
    return t;
  if (!Mx(t))
    throw Error(`No registered validator found under "${t}" name`);
  return l2(t);
}
function $a(t, e) {
  typeof t != "string" && (e = t, t = e.VALIDATOR_TYPE), a2(t, e);
}
var c2 = Mn, h2 = ld, d2 = hd, f2 = cd, g2 = mb;
c2({ target: "Array", proto: !0 }, {
  at: function(e) {
    var n = h2(this), i = d2(n), r = f2(e), o = r >= 0 ? r : i + r;
    return o < 0 || o >= i ? void 0 : n[o];
  }
});
g2("at");
var m2 = Mn, p2 = an, w2 = sd, A2 = cd, C2 = cp, v2 = fi, y2 = p2("".charAt), S2 = v2(function() {
  return "𠮷".at(-2) !== "\uD842";
});
m2({ target: "String", proto: !0, forced: S2 }, {
  at: function(e) {
    var n = C2(w2(this)), i = n.length, r = A2(e), o = r >= 0 ? r : i + r;
    return o < 0 || o >= i ? void 0 : y2(n, o);
  }
});
function Ju(t, e, n) {
  return (e = b2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function b2(t) {
  var e = R2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function R2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class E2 {
  constructor() {
    Ju(this, "count", 0), Ju(this, "startColumn", null), Ju(this, "endColumn", null), Ju(this, "startPosition", null), Ju(this, "isVisibleInTrimmingContainer", !1);
  }
  /**
   * Initializes the calculation.
   */
  initialize() {
  }
  /**
   * Processes the column.
   *
   * @param {number} column The column index.
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */
  process(e, n) {
    const {
      totalCalculatedWidth: i,
      zeroBasedScrollOffset: r,
      viewportWidth: o,
      columnWidth: s
    } = n, a = r > 0 ? o + 1 : o;
    i >= r && i + s <= r + a && ((this.startColumn === null || this.startColumn === void 0) && (this.startColumn = e), this.endColumn = e);
  }
  /**
   * Finalizes the calculation.
   *
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */
  finalize(e) {
    var n;
    const {
      scrollOffset: i,
      viewportWidth: r,
      inlineStartOffset: o,
      zeroBasedScrollOffset: s,
      totalColumns: a,
      needReverse: l,
      startPositions: u,
      columnWidth: c
    } = e;
    if (this.endColumn === a - 1 && l)
      for (this.startColumn = this.endColumn; this.startColumn > 0; ) {
        const m = u[this.endColumn] + c - u[this.startColumn - 1];
        if (m <= r && (this.startColumn -= 1), m >= r)
          break;
      }
    this.startPosition = (n = u[this.startColumn]) !== null && n !== void 0 ? n : null;
    const h = s > 0 ? r + 1 : r, d = i + r - h, g = this.startColumn === null ? 0 : e.getColumnWidth(this.startColumn);
    // the table is to the left of the viewport
    d < -1 * o || i > u.at(-1) || // the table is to the right of the viewport
    -1 * i - r > -1 * g ? this.isVisibleInTrimmingContainer = !1 : this.isVisibleInTrimmingContainer = !0, a < this.endColumn && (this.endColumn = a - 1), this.startColumn !== null && (this.count = this.endColumn - this.startColumn + 1);
  }
}
function ec(t, e, n) {
  return (e = T2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function T2(t) {
  var e = M2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function M2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class I2 {
  constructor() {
    ec(this, "count", 0), ec(this, "startRow", null), ec(this, "endRow", null), ec(this, "startPosition", null), ec(this, "isVisibleInTrimmingContainer", !1);
  }
  /**
   * Initializes the calculation.
   */
  initialize() {
  }
  /**
   * Processes the row.
   *
   * @param {number} row The row index.
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */
  process(e, n) {
    const {
      totalCalculatedHeight: i,
      zeroBasedScrollOffset: r,
      innerViewportHeight: o,
      rowHeight: s
    } = n;
    i >= r && i + s <= o && (this.startRow === null && (this.startRow = e), this.endRow = e);
  }
  /**
   * Finalizes the calculation.
   *
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */
  finalize(e) {
    var n;
    const {
      scrollOffset: i,
      viewportHeight: r,
      horizontalScrollbarHeight: o,
      totalRows: s,
      needReverse: a,
      startPositions: l,
      rowHeight: u
    } = e;
    if (this.endRow === s - 1 && a)
      for (this.startRow = this.endRow; this.startRow > 0; ) {
        const d = l[this.endRow] + u - l[this.startRow - 1];
        if (d <= r - o && (this.startRow -= 1), d >= r - o)
          break;
      }
    this.startPosition = (n = l[this.startRow]) !== null && n !== void 0 ? n : null;
    const c = i + r - o, h = this.startRow === null ? 0 : e.getRowHeight(this.startRow);
    c < h || i > l.at(-1) ? this.isVisibleInTrimmingContainer = !1 : this.isVisibleInTrimmingContainer = !0, s < this.endRow && (this.endRow = s - 1), this.startRow !== null && (this.count = this.endRow - this.startRow + 1);
  }
}
function tc(t, e, n) {
  return (e = O2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function O2(t) {
  var e = P2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function P2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Ix {
  constructor() {
    tc(this, "count", 0), tc(this, "startColumn", null), tc(this, "endColumn", null), tc(this, "startPosition", null), tc(this, "isVisibleInTrimmingContainer", !1);
  }
  /**
   * Initializes the calculation.
   */
  initialize() {
  }
  /**
   * Processes the column.
   *
   * @param {number} column The column index.
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */
  process(e, n) {
    const {
      totalCalculatedWidth: i,
      zeroBasedScrollOffset: r,
      viewportWidth: o
    } = n;
    i <= r && (this.startColumn = e);
    const s = r > 0 ? o + 1 : o;
    i >= r && i <= r + s && (this.startColumn === null || this.startColumn === void 0) && (this.startColumn = e), this.endColumn = e;
  }
  /**
   * Finalizes the calculation.
   *
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */
  finalize(e) {
    var n;
    const {
      scrollOffset: i,
      viewportWidth: r,
      inlineStartOffset: o,
      zeroBasedScrollOffset: s,
      totalColumns: a,
      needReverse: l,
      startPositions: u,
      columnWidth: c
    } = e;
    if (this.endColumn === a - 1 && l)
      for (this.startColumn = this.endColumn; this.startColumn > 0; ) {
        const g = u[this.endColumn] + c - u[this.startColumn - 1];
        if (this.startColumn -= 1, g > r)
          break;
      }
    this.startPosition = (n = u[this.startColumn]) !== null && n !== void 0 ? n : null;
    const h = s > 0 ? r + 1 : r;
    // the table is to the left of the viewport
    i + r - h < -1 * o || i > u.at(-1) + c || // the table is to the right of the viewport
    -1 * i - r > 0 ? this.isVisibleInTrimmingContainer = !1 : this.isVisibleInTrimmingContainer = !0, a < this.endColumn && (this.endColumn = a - 1), this.startColumn !== null && (this.count = this.endColumn - this.startColumn + 1);
  }
}
function nc(t, e, n) {
  return (e = x2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function x2(t) {
  var e = H2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function H2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Ox {
  constructor() {
    nc(this, "count", 0), nc(this, "startRow", null), nc(this, "endRow", null), nc(this, "startPosition", null), nc(this, "isVisibleInTrimmingContainer", !1);
  }
  /**
   * Initializes the calculation.
   */
  initialize() {
  }
  /**
   * Processes the row.
   *
   * @param {number} row The row index.
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */
  process(e, n) {
    const {
      totalCalculatedHeight: i,
      zeroBasedScrollOffset: r,
      innerViewportHeight: o
    } = n;
    i <= r && (this.startRow = e), i >= r && i <= o && this.startRow === null && (this.startRow = e), this.endRow = e;
  }
  /**
   * Finalizes the calculation.
   *
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */
  finalize(e) {
    var n;
    const {
      scrollOffset: i,
      viewportHeight: r,
      horizontalScrollbarHeight: o,
      totalRows: s,
      needReverse: a,
      startPositions: l,
      rowHeight: u
    } = e;
    if (this.endRow === s - 1 && a)
      for (this.startRow = this.endRow; this.startRow > 0; ) {
        const h = l[this.endRow] + u - l[this.startRow - 1];
        if (this.startRow -= 1, h >= r - o)
          break;
      }
    this.startPosition = (n = l[this.startRow]) !== null && n !== void 0 ? n : null, i + r - o < 0 || i > l.at(-1) + u ? this.isVisibleInTrimmingContainer = !1 : this.isVisibleInTrimmingContainer = !0, s < this.endRow && (this.endRow = s - 1), this.startRow !== null && (this.count = this.endRow - this.startRow + 1);
  }
}
function ic(t, e, n) {
  return (e = _2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function _2(t) {
  var e = N2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function N2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class D2 {
  constructor() {
    ic(this, "count", 0), ic(this, "startColumn", 0), ic(this, "endColumn", 0), ic(this, "startPosition", 0), ic(this, "isVisibleInTrimmingContainer", !0);
  }
  /**
   * Initializes the calculation.
   *
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */
  initialize(e) {
    let {
      totalColumns: n
    } = e;
    this.count = n, this.endColumn = this.count - 1;
  }
  /**
   * Processes the column.
   */
  process() {
  }
  /**
   * Finalizes the calculation.
   */
  finalize() {
  }
}
function rc(t, e, n) {
  return (e = k2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function k2(t) {
  var e = L2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function L2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class B2 {
  constructor() {
    rc(this, "count", 0), rc(this, "startRow", 0), rc(this, "endRow", 0), rc(this, "startPosition", 0), rc(this, "isVisibleInTrimmingContainer", !0);
  }
  /**
   * Initializes the calculation.
   *
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */
  initialize(e) {
    let {
      totalRows: n
    } = e;
    this.count = n, this.endRow = this.count - 1;
  }
  /**
   * Processes the row.
   */
  process() {
  }
  /**
   * Finalizes the calculation.
   */
  finalize() {
  }
}
class F2 extends Ix {
  /**
   * Finalizes the calculation.
   *
   * @param {ViewportColumnsCalculator} viewportCalculator The viewport calculator object.
   */
  finalize(e) {
    var n;
    super.finalize(e);
    const {
      overrideFn: i,
      totalColumns: r,
      startPositions: o
    } = e;
    this.startColumn !== null && typeof i == "function" && i(this), this.startColumn < 0 && (this.startColumn = 0), this.startPosition = (n = o[this.startColumn]) !== null && n !== void 0 ? n : null, r < this.endColumn && (this.endColumn = r - 1), this.startColumn !== null && (this.count = this.endColumn - this.startColumn + 1);
  }
}
class V2 extends Ox {
  /**
   * Finalizes the calculation.
   *
   * @param {ViewportRowsCalculator} viewportCalculator The viewport calculator object.
   */
  finalize(e) {
    var n;
    super.finalize(e);
    const {
      overrideFn: i,
      totalRows: r,
      startPositions: o
    } = e;
    this.startRow !== null && typeof i == "function" && i(this), this.startRow < 0 && (this.startRow = 0), this.startPosition = (n = o[this.startRow]) !== null && n !== void 0 ? n : null, r < this.endRow && (this.endRow = r - 1), this.startRow !== null && (this.count = this.endRow - this.startRow + 1);
  }
}
function $E(t, e, n) {
  return (e = W2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function W2(t) {
  var e = $2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function $2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Px {
  constructor(e) {
    $E(this, "calculationTypes", []), $E(this, "calculationResults", /* @__PURE__ */ new Map()), this.calculationTypes = e;
  }
  /**
   * Initializes all calculators (triggers all calculators before calculating the rows/columns sizes).
   *
   * @param {*} context The context object (rows or columns viewport calculator).
   */
  _initialize(e) {
    this.calculationTypes.forEach((n) => {
      let [i, r] = n;
      this.calculationResults.set(i, r), r.initialize(e);
    });
  }
  /**
   * Processes the row/column at the given index.
   *
   * @param {number} index The index of the row/column.
   * @param {*} context The context object (rows or columns viewport calculator).
   */
  _process(e, n) {
    this.calculationTypes.forEach((i) => {
      let [, r] = i;
      return r.process(e, n);
    });
  }
  /**
   * Finalizes all calculators (triggers all calculators after calculating the rows/columns sizes).
   *
   * @param {*} context The context object (rows or columns viewport calculator).
   */
  _finalize(e) {
    this.calculationTypes.forEach((n) => {
      let [, i] = n;
      return i.finalize(e);
    });
  }
  /**
   * Gets the results for the given calculator.
   *
   * @param {string} calculatorId The id of the calculator.
   * @returns {ColumnsCalculationType | RowsCalculationType}
   */
  getResultsFor(e) {
    return this.calculationResults.get(e);
  }
}
function tr(t, e, n) {
  return (e = U2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function U2(t) {
  var e = Y2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Y2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const Np = 50;
class j2 extends Px {
  /**
   * @param {ViewportColumnsCalculatorOptions} options Object with all options specified for column viewport calculation.
   */
  constructor(e) {
    let {
      calculationTypes: n,
      viewportWidth: i,
      scrollOffset: r,
      totalColumns: o,
      columnWidthFn: s,
      overrideFn: a,
      inlineStartOffset: l
    } = e;
    super(n), tr(this, "viewportWidth", 0), tr(this, "scrollOffset", 0), tr(this, "zeroBasedScrollOffset", 0), tr(this, "totalColumns", 0), tr(this, "columnWidthFn", null), tr(this, "columnWidth", 0), tr(this, "overrideFn", null), tr(this, "inlineStartOffset", 0), tr(this, "totalCalculatedWidth", 0), tr(this, "startPositions", []), tr(this, "needReverse", !0), this.viewportWidth = i, this.scrollOffset = r, this.zeroBasedScrollOffset = Math.max(r, 0), this.totalColumns = o, this.columnWidthFn = s, this.overrideFn = a, this.inlineStartOffset = l, this.calculate();
  }
  /**
   * Calculates viewport.
   */
  calculate() {
    this._initialize(this);
    for (let e = 0; e < this.totalColumns; e++)
      if (this.columnWidth = this.getColumnWidth(e), this._process(e, this), this.startPositions.push(this.totalCalculatedWidth), this.totalCalculatedWidth += this.columnWidth, this.totalCalculatedWidth >= this.zeroBasedScrollOffset + this.viewportWidth) {
        this.needReverse = !1;
        break;
      }
    this._finalize(this);
  }
  /**
   * Gets the column width at the specified column index.
   *
   * @param {number} column Column index.
   * @returns {number}
   */
  getColumnWidth(e) {
    const n = this.columnWidthFn(e);
    return isNaN(n) ? Np : n;
  }
}
function Oi(t, e, n) {
  return (e = G2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function G2(t) {
  var e = z2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function z2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const xx = 23;
class K2 extends Px {
  /**
   * @param {ViewportRowsCalculatorOptions} options Object with all options specified for row viewport calculation.
   */
  constructor(e) {
    let {
      calculationTypes: n,
      viewportHeight: i,
      scrollOffset: r,
      totalRows: o,
      rowHeightFn: s,
      overrideFn: a,
      horizontalScrollbarHeight: l
    } = e;
    super(n), Oi(this, "viewportHeight", 0), Oi(this, "scrollOffset", 0), Oi(this, "zeroBasedScrollOffset", 0), Oi(this, "totalRows", 0), Oi(this, "rowHeightFn", null), Oi(this, "rowHeight", 0), Oi(this, "overrideFn", null), Oi(this, "horizontalScrollbarHeight", 0), Oi(this, "innerViewportHeight", 0), Oi(this, "totalCalculatedHeight", 0), Oi(this, "startPositions", []), Oi(this, "needReverse", !0), this.viewportHeight = i, this.scrollOffset = r, this.zeroBasedScrollOffset = Math.max(r, 0), this.totalRows = o, this.rowHeightFn = s, this.overrideFn = a, this.horizontalScrollbarHeight = l != null ? l : 0, this.innerViewportHeight = this.zeroBasedScrollOffset + this.viewportHeight - this.horizontalScrollbarHeight, this.calculate();
  }
  /**
   * Calculates viewport.
   */
  calculate() {
    this._initialize(this);
    for (let e = 0; e < this.totalRows; e++)
      if (this.rowHeight = this.getRowHeight(e), this._process(e, this), this.startPositions.push(this.totalCalculatedHeight), this.totalCalculatedHeight += this.rowHeight, this.totalCalculatedHeight >= this.innerViewportHeight) {
        this.needReverse = !1;
        break;
      }
    this._finalize(this);
  }
  /**
   * Gets the row height at the specified row index.
   *
   * @param {number} row Row index.
   * @returns {number}
   */
  getRowHeight(e) {
    const n = this.rowHeightFn(e);
    return isNaN(n) ? xx : n;
  }
}
function Q2(t, e, n) {
  X2(t, e), e.set(t, n);
}
function X2(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function UE(t, e, n) {
  return (e = q2(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function q2(t) {
  var e = Z2(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Z2(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Rl(t, e) {
  return t.get(Hx(t, e));
}
function YE(t, e, n) {
  return t.set(Hx(t, e), n), n;
}
function Hx(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var vo = /* @__PURE__ */ new WeakMap();
class ym {
  constructor(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    UE(this, "row", null), UE(this, "col", null), Q2(this, vo, !1), YE(vo, this, i), typeof e != "undefined" && typeof n != "undefined" && (this.row = e, this.col = n);
  }
  /**
   * Checks if the coordinates in your `CellCoords` instance are valid
   * in the context of given table parameters.
   *
   * The `row` index:
   * - Must be an integer.
   * - Must be higher than the number of column headers in the table.
   * - Must be lower than the total number of rows in the table.
   *
   * The `col` index:
   * - Must be an integer.
   * - Must be higher than the number of row headers in the table.
   * - Must be lower than the total number of columns in the table.
   *
   * @param {object} [tableParams] An object with a defined table size.
   * @param {number} [tableParams.countRows=0] The total number of rows.
   * @param {number} [tableParams.countCols=0] The total number of columns.
   * @param {number} [tableParams.countRowHeaders=0] A number of row headers.
   * @param {number} [tableParams.countColHeaders=0] A number of column headers.
   * @returns {boolean} `true`: The coordinates are valid.
   */
  isValid(e) {
    const {
      countRows: n,
      countCols: i,
      countRowHeaders: r,
      countColHeaders: o
    } = Xe({
      countRows: 0,
      countCols: 0,
      countRowHeaders: 0,
      countColHeaders: 0
    }, e);
    return !(!Number.isInteger(this.row) || !Number.isInteger(this.col) || this.row < -o || this.col < -r || this.row >= n || this.col >= i);
  }
  /**
   * Checks if another set of coordinates (`coords`)
   * is equal to the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} coords Coordinates to check.
   * @returns {boolean}
   */
  isEqual(e) {
    return e === this ? !0 : this.row === e.row && this.col === e.col;
  }
  /**
   * Checks if the coordinates point to the headers range. If one of the axis (row or col) point to
   * the header (negative value) then method returns `true`.
   *
   * @returns {boolean}
   */
  isHeader() {
    return !this.isCell();
  }
  /**
   * Checks if the coordinates point to the cells range. If all axis (row and col) point to
   * the cell (positive value) then method returns `true`.
   *
   * @returns {boolean}
   */
  isCell() {
    return this.row >= 0 && this.col >= 0;
  }
  /**
   * Checks if the coordinates runs in RTL mode.
   *
   * @returns {boolean}
   */
  isRtl() {
    return Rl(vo, this);
  }
  /**
   * Checks if another set of coordinates (`testedCoords`)
   * is south-east of the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} testedCoords Coordinates to check.
   * @returns {boolean}
   */
  isSouthEastOf(e) {
    return this.row >= e.row && (Rl(vo, this) ? this.col <= e.col : this.col >= e.col);
  }
  /**
   * Checks if another set of coordinates (`testedCoords`)
   * is north-west of the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} testedCoords Coordinates to check.
   * @returns {boolean}
   */
  isNorthWestOf(e) {
    return this.row <= e.row && (Rl(vo, this) ? this.col >= e.col : this.col <= e.col);
  }
  /**
   * Checks if another set of coordinates (`testedCoords`)
   * is south-west of the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} testedCoords Coordinates to check.
   * @returns {boolean}
   */
  isSouthWestOf(e) {
    return this.row >= e.row && (Rl(vo, this) ? this.col >= e.col : this.col <= e.col);
  }
  /**
   * Checks if another set of coordinates (`testedCoords`)
   * is north-east of the coordinates in your `CellCoords` instance.
   *
   * @param {CellCoords} testedCoords Coordinates to check.
   * @returns {boolean}
   */
  isNorthEastOf(e) {
    return this.row <= e.row && (Rl(vo, this) ? this.col <= e.col : this.col >= e.col);
  }
  /**
   * Normalizes the coordinates in your `CellCoords` instance to the nearest valid position.
   *
   * Coordinates that point to headers (negative values) are normalized to `0`.
   *
   * @returns {CellCoords}
   */
  normalize() {
    return this.row = this.row === null ? this.row : Math.max(this.row, 0), this.col = this.col === null ? this.col : Math.max(this.col, 0), this;
  }
  /**
   * Assigns the coordinates from another `CellCoords` instance (or compatible literal object)
   * to your `CellCoords` instance.
   *
   * @param {CellCoords | { row: number | undefined, col: number | undefined }} coords The CellCoords
   * instance or compatible literal object.
   * @returns {CellCoords}
   */
  assign(e) {
    return Number.isInteger(e == null ? void 0 : e.row) && (this.row = e.row), Number.isInteger(e == null ? void 0 : e.col) && (this.col = e.col), e instanceof ym && YE(vo, this, e.isRtl()), this;
  }
  /**
   * Clones your `CellCoords` instance.
   *
   * @returns {CellCoords}
   */
  clone() {
    return new ym(this.row, this.col, Rl(vo, this));
  }
  /**
   * Converts your `CellCoords` instance into an object literal with `row` and `col` properties.
   *
   * @returns {{row: number, col: number}} An object literal with `row` and `col` properties.
   */
  toObject() {
    return {
      row: this.row,
      col: this.col
    };
  }
}
const Sm = ym;
function J2(t, e, n) {
  eQ(t, e), e.set(t, n);
}
function eQ(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function iC(t, e, n) {
  return (e = tQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function tQ(t) {
  var e = nQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function nQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Pi(t, e) {
  return t.get(_x(t, e));
}
function iQ(t, e, n) {
  return t.set(_x(t, e), n), n;
}
function _x(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Jn = /* @__PURE__ */ new WeakMap();
class Rb {
  constructor(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : e, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : e, r = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1;
    iC(this, "highlight", null), iC(this, "from", null), iC(this, "to", null), J2(this, Jn, !1), this.highlight = e.clone(), this.from = n.clone(), this.to = i.clone(), iQ(Jn, this, r);
  }
  /**
   * Highlights cell selection at the `coords` coordinates.
   *
   * @param {CellCoords} coords Coordinates to use.
   * @returns {CellRange}
   */
  setHighlight(e) {
    return this.highlight = e.clone(), this;
  }
  /**
   * Sets the `coords` coordinates as the start of your range.
   *
   * @param {CellCoords} coords Coordinates to use.
   * @returns {CellRange}
   */
  setFrom(e) {
    return this.from = e.clone(), this;
  }
  /**
   * Sets the `coords` coordinates as the end of your range.
   *
   * @param {CellCoords} coords Coordinates to use.
   * @returns {CellRange}
   */
  setTo(e) {
    return this.to = e.clone(), this;
  }
  /**
   * Checks if the coordinates in your `CellRange` instance are valid
   * in the context of given table parameters.
   *
   * See the [`isValid()`](@/api/cellCoords.md#isvalid) method of the [`CellCoords`](@/api/cellCoords.md) class.
   *
   * @param {object} tableParams An object with a defined table size.
   * @param {number} tableParams.countRows The total number of rows.
   * @param {number} tableParams.countCols The total number of columns.
   * @param {number} tableParams.countRowHeaders A number of row headers.
   * @param {number} tableParams.countColHeaders A number of column headers.
   * @returns {boolean}
   */
  isValid(e) {
    return this.from.isValid(e) && this.to.isValid(e);
  }
  /**
   * Checks if your range is just a single cell or header.
   *
   * @returns {boolean}
   */
  isSingle() {
    return this.isSingleCell() || this.isSingleHeader();
  }
  /**
   * Checks if your range is just a single cell.
   *
   * @returns {boolean}
   */
  isSingleCell() {
    return this.from.row >= 0 && this.from.row === this.to.row && this.from.col >= 0 && this.from.col === this.to.col;
  }
  /**
   * Checks if your range is just a single header.
   *
   * @returns {boolean}
   */
  isSingleHeader() {
    return (this.from.row < 0 || this.from.col < 0) && this.from.row === this.to.row && this.from.col === this.to.col;
  }
  /**
   * Checks if your range covers only headers range (negative coordinates, without any cells).
   *
   * @returns {boolean}
   */
  isHeader() {
    return this.from.isHeader() && this.to.isHeader() ? !0 : this.from.col < 0 && this.to.col < 0 || this.from.row < 0 && this.to.row < 0;
  }
  /**
   * Checks if your range overlaps headers range (negative coordinates).
   *
   * @returns {boolean}
   */
  containsHeaders() {
    return this.from.isHeader() || this.to.isHeader();
  }
  /**
   * Returns the height of your range (as a number of rows, including row headers).
   *
   * @returns {number}
   */
  getOuterHeight() {
    return Math.max(this.from.row, this.to.row) - Math.min(this.from.row, this.to.row) + 1;
  }
  /**
   * Returns the width of your range (as a number of columns, including column headers).
   *
   * @returns {number}
   */
  getOuterWidth() {
    return Math.max(this.from.col, this.to.col) - Math.min(this.from.col, this.to.col) + 1;
  }
  /**
   * Returns the height of your range (as a number of rows, excluding row headers).
   *
   * @returns {number}
   */
  getHeight() {
    if (this.from.row < 0 && this.to.row < 0)
      return 0;
    const e = Math.max(this.from.row, 0), n = Math.max(this.to.row, 0);
    return Math.max(e, n) - Math.min(e, n) + 1;
  }
  /**
   * Returns the width of your range (as a number of columns, excluding column headers).
   *
   * @returns {number}
   */
  getWidth() {
    if (this.from.col < 0 && this.to.col < 0)
      return 0;
    const e = Math.max(this.from.col, 0), n = Math.max(this.to.col, 0);
    return Math.max(e, n) - Math.min(e, n) + 1;
  }
  /**
   * Returns the number of cells within your range (excluding column and row headers).
   *
   * @returns {number}
   */
  getCellsCount() {
    return this.getWidth() * this.getHeight();
  }
  /**
   * Checks if another set of coordinates (`cellCoords`)
   * is within the `from` and `to` coordinates of your range.
   *
   * @param {CellCoords} cellCoords Coordinates to check.
   * @returns {boolean}
   */
  includes(e) {
    const {
      row: n,
      col: i
    } = e, r = this.getOuterTopStartCorner(), o = this.getOuterBottomEndCorner();
    return r.row <= n && o.row >= n && r.col <= i && o.col >= i;
  }
  /**
   * Checks if another range (`cellRange`) is within your range.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */
  includesRange(e) {
    return this.includes(e.getOuterTopStartCorner()) && this.includes(e.getOuterBottomEndCorner());
  }
  /**
   * Checks if another range (`cellRange`) is equal to your range.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */
  isEqual(e) {
    return Math.min(this.from.row, this.to.row) === Math.min(e.from.row, e.to.row) && Math.max(this.from.row, this.to.row) === Math.max(e.from.row, e.to.row) && Math.min(this.from.col, this.to.col) === Math.min(e.from.col, e.to.col) && Math.max(this.from.col, this.to.col) === Math.max(e.from.col, e.to.col);
  }
  /**
   * Checks if another range (`cellRange`) overlaps your range.
   *
   * Range A overlaps range B if the intersection of A and B (or B and A) is not empty.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */
  overlaps(e) {
    return e.isSouthEastOf(this.getOuterTopLeftCorner()) && e.isNorthWestOf(this.getOuterBottomRightCorner());
  }
  /**
   * Checks if coordinates point is south-east of your range.
   *
   * @param {CellCoords} cellCoords Coordinates to check.
   * @returns {boolean}
   */
  isSouthEastOf(e) {
    return this.getOuterTopLeftCorner().isSouthEastOf(e) || this.getOuterBottomRightCorner().isSouthEastOf(e);
  }
  /**
   * Checks if coordinates point is north-west of your range.
   *
   * @param {CellRange} cellCoords Coordinates to check.
   * @returns {boolean}
   */
  isNorthWestOf(e) {
    return this.getOuterTopLeftCorner().isNorthWestOf(e) || this.getOuterBottomRightCorner().isNorthWestOf(e);
  }
  /**
   * Checks if another range (`cellRange`) overlaps your range horizontally.
   *
   * For example: returns `true` if the last column of your range is `5`
   * and the first column of the `cellRange` range is `3`.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */
  isOverlappingHorizontally(e) {
    return this.getOuterTopEndCorner().col >= e.getOuterTopStartCorner().col && this.getOuterTopEndCorner().col <= e.getOuterTopEndCorner().col || this.getOuterTopStartCorner().col <= e.getOuterTopEndCorner().col && this.getOuterTopStartCorner().col >= e.getOuterTopStartCorner().col;
  }
  /**
   * Checks if another range (`cellRange`) overlaps your range vertically.
   *
   * For example: returns `true` if the last row of your range is `5`
   * and the first row of the `cellRange` range is `3`.
   *
   * @param {CellRange} cellRange A range to check.
   * @returns {boolean}
   */
  isOverlappingVertically(e) {
    return this.getOuterBottomStartCorner().row >= e.getOuterTopRightCorner().row && this.getOuterBottomStartCorner().row <= e.getOuterBottomStartCorner().row || this.getOuterTopEndCorner().row <= e.getOuterBottomStartCorner().row && this.getOuterTopEndCorner().row >= e.getOuterTopRightCorner().row;
  }
  /**
   * Adds a cell to your range, at `cellCoords` coordinates.
   *
   * The `cellCoords` coordinates must exceed a corner of your range.
   *
   * @param {CellCoords} cellCoords A new cell's coordinates.
   * @returns {boolean}
   */
  expand(e) {
    const n = this.getOuterTopStartCorner(), i = this.getOuterBottomEndCorner();
    return e.row < n.row || e.col < n.col || e.row > i.row || e.col > i.col ? (this.from = this._createCellCoords(Math.min(n.row, e.row), Math.min(n.col, e.col)), this.to = this._createCellCoords(Math.max(i.row, e.row), Math.max(i.col, e.col)), !0) : !1;
  }
  /**
   * Expand your range with another range (`expandingRange`).
   *
   * @param {CellRange} expandingRange A new range.
   * @param {boolean} [changeDirection=true] If `true`, the direction of your range is changed to the direction
   * of the `expandingRange` range.
   * @returns {boolean}
   */
  expandByRange(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
    if (this.includesRange(e) || !this.overlaps(e))
      return !1;
    const i = this.getOuterTopStartCorner(), r = this.getOuterBottomEndCorner(), o = this.getDirection(), s = e.getOuterTopStartCorner(), a = e.getOuterBottomEndCorner(), l = Math.min(i.row, s.row), u = Math.min(i.col, s.col), c = Math.max(r.row, a.row), h = Math.max(r.col, a.col), d = this._createCellCoords(l, u), g = this._createCellCoords(c, h);
    return this.from = d, this.to = g, this.setDirection(o), n && (this.highlight.row === this.getOuterBottomRightCorner().row && this.getVerticalDirection() === "N-S" && this.flipDirectionVertically(), this.highlight.col === this.getOuterTopRightCorner().col && this.getHorizontalDirection() === "W-E" && this.flipDirectionHorizontally()), !0;
  }
  /**
   * Gets the direction of the selection.
   *
   * @returns {string} Returns one of the values: `'NW-SE'`, `'NE-SW'`, `'SE-NW'`, `'SW-NE'`.
   */
  getDirection() {
    if (this.from.isNorthWestOf(this.to))
      return "NW-SE";
    if (this.from.isNorthEastOf(this.to))
      return "NE-SW";
    if (this.from.isSouthEastOf(this.to))
      return "SE-NW";
    if (this.from.isSouthWestOf(this.to))
      return "SW-NE";
  }
  /**
   * Sets the direction of the selection.
   *
   * @param {string} direction One of the values: `'NW-SE'`, `'NE-SW'`, `'SE-NW'`, `'SW-NE'`.
   */
  setDirection(e) {
    switch (e) {
      case "NW-SE":
        [this.from, this.to] = [this.getOuterTopLeftCorner(), this.getOuterBottomRightCorner()];
        break;
      case "NE-SW":
        [this.from, this.to] = [this.getOuterTopRightCorner(), this.getOuterBottomLeftCorner()];
        break;
      case "SE-NW":
        [this.from, this.to] = [this.getOuterBottomRightCorner(), this.getOuterTopLeftCorner()];
        break;
      case "SW-NE":
        [this.from, this.to] = [this.getOuterBottomLeftCorner(), this.getOuterTopRightCorner()];
        break;
    }
  }
  /**
   * Gets the vertical direction of the selection.
   *
   * @returns {string} Returns one of the values: `N-S` (north->south), `S-N` (south->north).
   */
  getVerticalDirection() {
    return ["NE-SW", "NW-SE"].indexOf(this.getDirection()) > -1 ? "N-S" : "S-N";
  }
  /**
   * Gets the horizontal direction of the selection.
   *
   * @returns {string} Returns one of the values: `W-E` (west->east), `E-W` (east->west).
   */
  getHorizontalDirection() {
    return ["NW-SE", "SW-NE"].indexOf(this.getDirection()) > -1 ? "W-E" : "E-W";
  }
  /**
   * Flips the direction of your range vertically (e.g., `NW-SE` changes to `SW-NE`).
   */
  flipDirectionVertically() {
    switch (this.getDirection()) {
      case "NW-SE":
        this.setDirection("SW-NE");
        break;
      case "NE-SW":
        this.setDirection("SE-NW");
        break;
      case "SE-NW":
        this.setDirection("NE-SW");
        break;
      case "SW-NE":
        this.setDirection("NW-SE");
        break;
    }
  }
  /**
   * Flips the direction of your range horizontally (e.g., `NW-SE` changes to `NE-SW`).
   */
  flipDirectionHorizontally() {
    switch (this.getDirection()) {
      case "NW-SE":
        this.setDirection("NE-SW");
        break;
      case "NE-SW":
        this.setDirection("NW-SE");
        break;
      case "SE-NW":
        this.setDirection("SW-NE");
        break;
      case "SW-NE":
        this.setDirection("SE-NW");
        break;
    }
  }
  /**
   * Gets the top-left (in LTR) or top-right (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */
  getTopStartCorner() {
    return this._createCellCoords(Math.min(this.from.row, this.to.row), Math.min(this.from.col, this.to.col)).normalize();
  }
  /**
   * Gets the top-left corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */
  getTopLeftCorner() {
    return Pi(Jn, this) ? this.getTopEndCorner() : this.getTopStartCorner();
  }
  /**
   * Gets the bottom right (in LTR) or bottom left (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */
  getBottomEndCorner() {
    return this._createCellCoords(Math.max(this.from.row, this.to.row), Math.max(this.from.col, this.to.col)).normalize();
  }
  /**
   * Gets the bottom right corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */
  getBottomRightCorner() {
    return Pi(Jn, this) ? this.getBottomStartCorner() : this.getBottomEndCorner();
  }
  /**
   * Gets the top right (in LTR) or top left (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */
  getTopEndCorner() {
    return this._createCellCoords(Math.min(this.from.row, this.to.row), Math.max(this.from.col, this.to.col)).normalize();
  }
  /**
   * Gets the top right corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */
  getTopRightCorner() {
    return Pi(Jn, this) ? this.getTopStartCorner() : this.getTopEndCorner();
  }
  /**
   * Gets the bottom left (in LTR) or bottom right (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */
  getBottomStartCorner() {
    return this._createCellCoords(Math.max(this.from.row, this.to.row), Math.min(this.from.col, this.to.col)).normalize();
  }
  /**
   * Gets the bottom left corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the corner coordinates are normalized to `0`.
   *
   * @returns {CellCoords}
   */
  getBottomLeftCorner() {
    return Pi(Jn, this) ? this.getBottomEndCorner() : this.getBottomStartCorner();
  }
  /**
   * Gets the top left (in LTR) or top right (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the top and start coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */
  getOuterTopStartCorner() {
    return this._createCellCoords(Math.min(this.from.row, this.to.row), Math.min(this.from.col, this.to.col));
  }
  /**
   * Gets the top left corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the top and left coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */
  getOuterTopLeftCorner() {
    return Pi(Jn, this) ? this.getOuterTopEndCorner() : this.getOuterTopStartCorner();
  }
  /**
   * Gets the bottom right (in LTR) or bottom left (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the top and start coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */
  getOuterBottomEndCorner() {
    return this._createCellCoords(Math.max(this.from.row, this.to.row), Math.max(this.from.col, this.to.col));
  }
  /**
   * Gets the bottom right corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the top and left coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */
  getOuterBottomRightCorner() {
    return Pi(Jn, this) ? this.getOuterBottomStartCorner() : this.getOuterBottomEndCorner();
  }
  /**
   * Gets the top right (in LTR) or top left (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the top and start coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */
  getOuterTopEndCorner() {
    return this._createCellCoords(Math.min(this.from.row, this.to.row), Math.max(this.from.col, this.to.col));
  }
  /**
   * Gets the top right corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the top and left coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */
  getOuterTopRightCorner() {
    return Pi(Jn, this) ? this.getOuterTopStartCorner() : this.getOuterTopEndCorner();
  }
  /**
   * Gets the bottom left (in LTR) or bottom right (in RTL) corner coordinates of your range.
   *
   * If the corner contains header coordinates (negative values),
   * the top and start coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */
  getOuterBottomStartCorner() {
    return this._createCellCoords(Math.max(this.from.row, this.to.row), Math.min(this.from.col, this.to.col));
  }
  /**
   * Gets the bottom left corner coordinates of your range,
   * both in the LTR and RTL layout direction.
   *
   * If the corner contains header coordinates (negative values),
   * the top and left coordinates are pointed to that header.
   *
   * @returns {CellCoords}
   */
  getOuterBottomLeftCorner() {
    return Pi(Jn, this) ? this.getOuterBottomEndCorner() : this.getOuterBottomStartCorner();
  }
  /**
   * Checks if a set of coordinates (`coords`) matches one of the 4 corners of your range.
   *
   * @param {CellCoords} coords Coordinates to check.
   * @returns {boolean}
   */
  isCorner(e) {
    return e.isEqual(this.getOuterTopLeftCorner()) || e.isEqual(this.getOuterTopRightCorner()) || e.isEqual(this.getOuterBottomLeftCorner()) || e.isEqual(this.getOuterBottomRightCorner());
  }
  /**
   * Gets the coordinates of a range corner opposite to the provided `coords`.
   *
   * For example: if the `coords` coordinates match the bottom-right corner of your range,
   * the coordinates of the top-left corner of your range are returned.
   *
   * @param {CellCoords} coords Coordinates to check.
   * @returns {CellCoords}
   */
  getOppositeCorner(e) {
    if (!(e instanceof Sm))
      return !1;
    if (e.isEqual(this.getOuterBottomEndCorner()))
      return this.getOuterTopStartCorner();
    if (e.isEqual(this.getOuterTopStartCorner()))
      return this.getOuterBottomEndCorner();
    if (e.isEqual(this.getOuterTopEndCorner()))
      return this.getOuterBottomStartCorner();
    if (e.isEqual(this.getOuterBottomStartCorner()))
      return this.getOuterTopEndCorner();
  }
  /**
   * Indicates which borders (top, right, bottom, left) are shared between
   * your `CellRange`instance and another `range` that's within your range.
   *
   * @param {CellRange} range A range to compare with.
   * @returns {Array<'top' | 'right' | 'bottom' | 'left'>}
   */
  getBordersSharedWith(e) {
    if (!this.includesRange(e))
      return [];
    const n = {
      top: Math.min(this.from.row, this.to.row),
      bottom: Math.max(this.from.row, this.to.row),
      left: Math.min(this.from.col, this.to.col),
      right: Math.max(this.from.col, this.to.col)
    }, i = {
      top: Math.min(e.from.row, e.to.row),
      bottom: Math.max(e.from.row, e.to.row),
      left: Math.min(e.from.col, e.to.col),
      right: Math.max(e.from.col, e.to.col)
    }, r = [];
    return n.top === i.top && r.push("top"), n.right === i.right && r.push(Pi(Jn, this) ? "left" : "right"), n.bottom === i.bottom && r.push("bottom"), n.left === i.left && r.push(Pi(Jn, this) ? "right" : "left"), r;
  }
  /**
   * Gets the coordinates of the inner cells of your range.
   *
   * @returns {CellCoords[]}
   */
  getInner() {
    const e = this.getOuterTopStartCorner(), n = this.getOuterBottomEndCorner(), i = [];
    for (let r = e.row; r <= n.row; r++)
      for (let o = e.col; o <= n.col; o++)
        !(this.from.row === r && this.from.col === o) && !(this.to.row === r && this.to.col === o) && i.push(this._createCellCoords(r, o));
    return i;
  }
  /**
   * Gets the coordinates of all cells of your range.
   *
   * @returns {CellCoords[]}
   */
  getAll() {
    const e = this.getOuterTopStartCorner(), n = this.getOuterBottomEndCorner(), i = [];
    for (let r = e.row; r <= n.row; r++)
      for (let o = e.col; o <= n.col; o++)
        e.row === r && e.col === o ? i.push(e) : n.row === r && n.col === o ? i.push(n) : i.push(this._createCellCoords(r, o));
    return i;
  }
  /**
   * Runs a callback function on all cells within your range.
   *
   * You can break the iteration by returning `false` in the callback function.
   *
   * @param {function(number, number): boolean} callback A callback function.
   */
  forAll(e) {
    const n = this.getOuterTopStartCorner(), i = this.getOuterBottomEndCorner();
    for (let r = n.row; r <= i.row; r++)
      for (let o = n.col; o <= i.col; o++)
        if (e(r, o) === !1)
          return;
  }
  /**
   * Clones your `CellRange` instance.
   *
   * @returns {CellRange}
   */
  clone() {
    return new Rb(this.highlight, this.from, this.to, Pi(Jn, this));
  }
  /**
   * Converts your `CellRange` instance into an object literal with the following properties:
   *
   * - `from`
   *    - `row`
   *    - `col`
   * - `to`
   *    - `row`
   *    - `col`
   *
   * @returns {{from: {row: number, col: number}, to: {row: number, col: number}}} An object literal with `from` and `to` properties.
   */
  toObject() {
    return {
      from: this.from.toObject(),
      to: this.to.toObject()
    };
  }
  /**
   * Creates and returns a new instance of the `CellCoords` class.
   *
   * The new `CellCoords` instance automatically inherits the LTR/RTL flag
   * from your `CellRange` instance.
   *
   * @private
   * @param {number} row A row index.
   * @param {number} column A column index.
   * @returns {CellCoords}
   */
  _createCellCoords(e, n) {
    return new Sm(e, n, Pi(Jn, this));
  }
}
const Eb = Rb;
function xr(t, e, n) {
  rQ(t, e), e.set(t, n);
}
function rQ(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ve(t, e) {
  return t.get(Nx(t, e));
}
function as(t, e, n) {
  return t.set(Nx(t, e), n), n;
}
function Nx(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var xi = /* @__PURE__ */ new WeakMap(), rC = /* @__PURE__ */ new WeakMap(), wn = /* @__PURE__ */ new WeakMap(), El = /* @__PURE__ */ new WeakMap(), uf = /* @__PURE__ */ new WeakMap(), nr = /* @__PURE__ */ new WeakMap(), oC = /* @__PURE__ */ new WeakMap(), oc = /* @__PURE__ */ new WeakMap(), oa = /* @__PURE__ */ new WeakMap(), Hr = /* @__PURE__ */ new WeakMap();
let oQ = class {
  /**
   * @param {FacadeGetter} facadeGetter Gets an instance facade.
   * @param {DomBindings} domBindings Bindings into dom.
   * @param {Settings} wtSettings The walkontable settings.
   * @param {EventManager} eventManager The walkontable event manager.
   * @param {Table} wtTable The table.
   * @param {SelectionManager} selectionManager Selections.
   * @param {Event} [parent=null] The main Event instance.
   */
  constructor(e, n, i, r, o, s) {
    let a = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : null;
    xr(this, xi, void 0), xr(this, rC, void 0), xr(this, wn, void 0), xr(this, El, void 0), xr(this, uf, void 0), xr(this, nr, void 0), xr(this, oC, void 0), xr(this, oc, void 0), xr(this, oa, [null, null]), xr(this, Hr, [null, null]), as(xi, this, i), as(rC, this, n), as(wn, this, o), as(El, this, s), as(uf, this, a), as(nr, this, r), as(oC, this, e), this.registerEvents();
  }
  /**
   * Adds listeners for mouse and touch events.
   *
   * @private
   */
  registerEvents() {
    ve(nr, this).addEventListener(ve(wn, this).holder, "contextmenu", (i) => this.onContextMenu(i)), ve(nr, this).addEventListener(ve(wn, this).TABLE, "mouseover", (i) => this.onMouseOver(i)), ve(nr, this).addEventListener(ve(wn, this).TABLE, "mouseout", (i) => this.onMouseOut(i));
    const e = () => {
      ve(nr, this).addEventListener(ve(wn, this).holder, "touchstart", (i) => this.onTouchStart(i)), ve(nr, this).addEventListener(ve(wn, this).holder, "touchend", (i) => this.onTouchEnd(i)), this.momentumScrolling || (this.momentumScrolling = {}), ve(nr, this).addEventListener(ve(wn, this).holder, "scroll", () => {
        clearTimeout(this.momentumScrolling._timeout), this.momentumScrolling.ongoing || ve(xi, this).getSetting("onBeforeTouchScroll"), this.momentumScrolling.ongoing = !0, this.momentumScrolling._timeout = setTimeout(() => {
          this.touchApplied || (this.momentumScrolling.ongoing = !1, ve(xi, this).getSetting("onAfterMomentumScroll"));
        }, 200);
      });
    }, n = () => {
      ve(nr, this).addEventListener(ve(wn, this).holder, "mouseup", (i) => this.onMouseUp(i)), ve(nr, this).addEventListener(ve(wn, this).holder, "mousedown", (i) => this.onMouseDown(i));
    };
    Yo() ? e() : (ub() && e(), n());
  }
  /**
   * Checks if an element is already selected.
   *
   * @private
   * @param {Element} touchTarget An element to check.
   * @returns {boolean}
   */
  selectedCellWasTouched(e) {
    const i = this.parentCell(e).coords;
    if (ve(oc, this) && i) {
      const [r, o] = [i.row, ve(oc, this).from.row], [s, a] = [i.col, ve(oc, this).from.col];
      return r === o && s === a;
    }
    return !1;
  }
  /**
   * Gets closest TD or TH element.
   *
   * @private
   * @param {Element} elem An element from the traversing starts.
   * @returns {object} Contains coordinates and reference to TD or TH if it exists. Otherwise it's empty object.
   */
  parentCell(e) {
    const n = {}, i = ve(wn, this).TABLE, r = qc(e, ["TD", "TH"], i);
    return r ? (n.coords = ve(wn, this).getCoords(r), n.TD = r) : Te(e, "wtBorder") && Te(e, "current") ? (n.coords = ve(El, this).getFocusSelection().cellRange.highlight, n.TD = ve(wn, this).getCell(n.coords)) : Te(e, "wtBorder") && Te(e, "area") && ve(El, this).getAreaSelection().cellRange && (n.coords = ve(El, this).getAreaSelection().cellRange.to, n.TD = ve(wn, this).getCell(n.coords)), n;
  }
  /**
   * OnMouseDown callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */
  onMouseDown(e) {
    const n = ve(rC, this).rootDocument.activeElement, i = lb(DP, e.target), r = e.target;
    if (!["TD", "TH"].includes(n.nodeName) && (r === n || i(0) === n || i(1) === n))
      return;
    const o = this.parentCell(r);
    Te(r, "corner") ? ve(xi, this).getSetting("onCellCornerMouseDown", e, r) : o.TD && ve(xi, this).has("onCellMouseDown") && this.callListener("onCellMouseDown", e, o.coords, o.TD), (e.button === 0 || this.touchApplied) && o.TD && (ve(Hr, this)[0] = o.TD, clearTimeout(ve(oa, this)[0]), ve(oa, this)[0] = setTimeout(() => {
      ve(Hr, this)[0] = null;
    }, 1e3));
  }
  /**
   * OnContextMenu callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */
  onContextMenu(e) {
    if (ve(xi, this).has("onCellContextMenu")) {
      const n = this.parentCell(e.target);
      n.TD && this.callListener("onCellContextMenu", e, n.coords, n.TD);
    }
  }
  /**
   * OnMouseOver callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */
  onMouseOver(e) {
    if (!ve(xi, this).has("onCellMouseOver"))
      return;
    const n = ve(wn, this).TABLE, i = qc(e.target, ["TD", "TH"], n), r = ve(uf, this) || this;
    i && i !== r.lastMouseOver && Vs(i, n) && (r.lastMouseOver = i, this.callListener("onCellMouseOver", e, ve(wn, this).getCoords(i), i));
  }
  /**
   * OnMouseOut callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */
  onMouseOut(e) {
    if (!ve(xi, this).has("onCellMouseOut"))
      return;
    const n = ve(wn, this).TABLE, i = qc(e.target, ["TD", "TH"], n), r = qc(e.relatedTarget, ["TD", "TH"], n), o = ve(uf, this) || this;
    i && i !== r && Vs(i, n) && (this.callListener("onCellMouseOut", e, ve(wn, this).getCoords(i), i), r === null && (o.lastMouseOver = null));
  }
  /**
   * OnMouseUp callback.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */
  onMouseUp(e) {
    const n = this.parentCell(e.target);
    n.TD && ve(xi, this).has("onCellMouseUp") && this.callListener("onCellMouseUp", e, n.coords, n.TD), !(e.button !== 0 && !this.touchApplied) && (n.TD === ve(Hr, this)[0] && n.TD === ve(Hr, this)[1] ? (Te(e.target, "corner") ? this.callListener("onCellCornerDblClick", e, n.coords, n.TD) : this.callListener("onCellDblClick", e, n.coords, n.TD), ve(Hr, this)[0] = null, ve(Hr, this)[1] = null) : n.TD === ve(Hr, this)[0] && (ve(Hr, this)[1] = n.TD, clearTimeout(ve(oa, this)[1]), ve(oa, this)[1] = setTimeout(() => {
      ve(Hr, this)[1] = null;
    }, 500)));
  }
  /**
   * OnTouchStart callback. Simulates mousedown event.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */
  onTouchStart(e) {
    as(oc, this, ve(El, this).getFocusSelection().cellRange), this.touchApplied = !0, this.onMouseDown(e);
  }
  /**
   * OnTouchEnd callback. Simulates mouseup event.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */
  onTouchEnd(e) {
    var n;
    const i = e.target, r = (n = this.parentCell(i)) === null || n === void 0 ? void 0 : n.coords, o = de(r) && r.row >= 0 && r.col >= 0;
    if (e.cancelable && o && ve(xi, this).getSetting("isDataViewInstance")) {
      const a = ["A", "BUTTON", "INPUT"];
      db() && (sx() || ax()) && this.selectedCellWasTouched(i) && !a.includes(i.tagName) ? e.preventDefault() : this.selectedCellWasTouched(i) || e.preventDefault();
    }
    this.onMouseUp(e), this.touchApplied = !1;
  }
  /**
   * Call listener with backward compatibility.
   *
   * @private
   * @param {string} name Name of listener.
   * @param {MouseEvent} event The event object.
   * @param {CellCoords} coords Coordinates.
   * @param {HTMLElement} target Event target.
   */
  callListener(e, n, i, r) {
    const o = ve(xi, this).getSettingPure(e);
    o && o(n, i, r, ve(oC, this).call(this));
  }
  /**
   * Clears double-click timeouts and destroys the internal eventManager instance.
   */
  destroy() {
    clearTimeout(ve(oa, this)[0]), clearTimeout(ve(oa, this)[1]), ve(nr, this).destroy();
  }
};
const Dx = oQ;
function sC(t, e, n) {
  return (e = sQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function sQ(t) {
  var e = aQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function aQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class lQ {
  /**
   * @param {number} offset The scroll horizontal offset.
   * @param {number} total The total width of the table.
   * @param {number} countTH The number of rendered row headers.
   */
  constructor(e, n, i) {
    sC(this, "offset", void 0), sC(this, "total", void 0), sC(this, "countTH", void 0), this.offset = e, this.total = n, this.countTH = i;
  }
  /**
   * @param {number} index The visual column index.
   * @returns {number}
   */
  offsetted(e) {
    return e + this.offset;
  }
  /**
   * @param {number} index The visual column index.
   * @returns {number}
   */
  unOffsetted(e) {
    return e - this.offset;
  }
  /**
   * @param {number} index The visual column index.
   * @returns {number}
   */
  renderedToSource(e) {
    return this.offsetted(e);
  }
  /**
   * @param {number} index The visual column index.
   * @returns {number}
   */
  sourceToRendered(e) {
    return this.unOffsetted(e);
  }
  /**
   * @param {number} index The visual column index.
   * @returns {number}
   */
  offsettedTH(e) {
    return e - this.countTH;
  }
  /**
   * @param {number} index The visual column index.
   * @returns {number}
   */
  unOffsettedTH(e) {
    return e + this.countTH;
  }
  /**
   * @param {number} index The visual column index.
   * @returns {number}
   */
  visibleRowHeadedColumnToSourceColumn(e) {
    return this.renderedToSource(this.offsettedTH(e));
  }
  /**
   * @param {number} index The visual column index.
   * @returns {number}
   */
  sourceColumnToVisibleRowHeadedColumn(e) {
    return this.unOffsettedTH(this.sourceToRendered(e));
  }
}
const uQ = lQ;
function aC(t, e, n) {
  return (e = cQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function cQ(t) {
  var e = hQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function hQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class dQ {
  /**
   * @param {number} offset The scroll vertical offset.
   * @param {number} total The total height of the table.
   * @param {number} countTH The number of rendered column headers.
   */
  constructor(e, n, i) {
    aC(this, "offset", void 0), aC(this, "total", void 0), aC(this, "countTH", void 0), this.offset = e, this.total = n, this.countTH = i;
  }
  /**
   * @param {number} index The visual row index.
   * @returns {number}
   */
  offsetted(e) {
    return e + this.offset;
  }
  /**
   * @param {number} index The visual row index.
   * @returns {number}
   */
  unOffsetted(e) {
    return e - this.offset;
  }
  /**
   * @param {number} index The visual row index.
   * @returns {number}
   */
  renderedToSource(e) {
    return this.offsetted(e);
  }
  /**
   * @param {number} index The visual row index.
   * @returns {number}
   */
  sourceToRendered(e) {
    return this.unOffsetted(e);
  }
  /**
   * @param {number} index The visual row index.
   * @returns {number}
   */
  offsettedTH(e) {
    return e - this.countTH;
  }
  /**
   * @param {number} index The visual row index.
   * @returns {number}
   */
  unOffsettedTH(e) {
    return e + this.countTH;
  }
  /**
   * @param {number} index The visual row index.
   * @returns {number}
   */
  visibleColHeadedRowToSourceRow(e) {
    return this.renderedToSource(this.offsettedTH(e));
  }
  /**
   * @param {number} index The visual row index.
   * @returns {number}
   */
  sourceRowToVisibleColHeadedRow(e) {
    return this.unOffsettedTH(this.sourceToRendered(e));
  }
}
const fQ = dQ, gQ = 0, bm = 1, Rm = 2;
function cf(t, e, n) {
  return (e = mQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function mQ(t) {
  var e = pQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function pQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class jE {
  constructor() {
    cf(this, "currentSize", 0), cf(this, "nextSize", 0), cf(this, "currentOffset", 0), cf(this, "nextOffset", 0);
  }
  /**
   * Sets new size of the rendered DOM elements.
   *
   * @param {number} size The size.
   */
  setSize(e) {
    this.currentSize = this.nextSize, this.nextSize = e;
  }
  /**
   * Sets new offset.
   *
   * @param {number} offset The offset.
   */
  setOffset(e) {
    this.currentOffset = this.nextOffset, this.nextOffset = e;
  }
}
function lC(t, e, n) {
  return (e = wQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function wQ(t) {
  var e = AQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function AQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class CQ {
  constructor() {
    lC(this, "size", new jE()), lC(this, "workingSpace", gQ), lC(this, "sharedSize", null);
  }
  /**
   * Sets the size for rendered elements. It can be a size for rows, cells or size for row
   * headers etc.
   *
   * @param {number} size The size.
   */
  setSize(e) {
    this.size.setSize(e);
  }
  /**
   * Sets the offset for rendered elements. The offset describes the shift between 0 and
   * the first rendered element according to the scroll position.
   *
   * @param {number} offset The offset.
   */
  setOffset(e) {
    this.size.setOffset(e);
  }
  /**
   * Returns ViewSize instance.
   *
   * @returns {ViewSize}
   */
  getViewSize() {
    return this.size;
  }
  /**
   * Checks if this ViewSizeSet is sharing the size with another instance.
   *
   * @returns {boolean}
   */
  isShared() {
    return this.sharedSize instanceof jE;
  }
  /**
   * Checks what working space describes this size instance.
   *
   * @param {number} workingSpace The number which describes the type of the working space (see constants.js).
   * @returns {boolean}
   */
  isPlaceOn(e) {
    return this.workingSpace === e;
  }
  /**
   * Appends the ViewSizeSet instance to this instance that turns it into a shared mode.
   *
   * @param {ViewSizeSet} viewSize The instance of the ViewSizeSet class.
   */
  append(e) {
    this.workingSpace = bm, e.workingSpace = Rm, this.sharedSize = e.getViewSize();
  }
  /**
   * Prepends the ViewSize instance to this instance that turns it into a shared mode.
   *
   * @param {ViewSizeSet} viewSize The instance of the ViewSizeSet class.
   */
  prepend(e) {
    this.workingSpace = Rm, e.workingSpace = bm, this.sharedSize = e.getViewSize();
  }
}
function Tl(t, e, n) {
  return (e = vQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function vQ(t) {
  var e = yQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function yQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class kx {
  constructor(e, n, i) {
    Tl(this, "rootNode", void 0), Tl(this, "nodesPool", void 0), Tl(this, "sizeSet", new CQ()), Tl(this, "childNodeType", void 0), Tl(this, "visualIndex", 0), Tl(this, "collectedNodes", []), this.rootNode = e, this.nodesPool = n, this.childNodeType = i.toUpperCase();
  }
  /**
   * Sets the size for rendered elements. It can be a size for rows, cells or size for row
   * headers etc. It depends for what table renderer this instance was created.
   *
   * @param {number} size The size.
   * @returns {OrderView}
   */
  setSize(e) {
    return this.sizeSet.setSize(e), this;
  }
  /**
   * Sets the offset for rendered elements. The offset describes the shift between 0 and
   * the first rendered element according to the scroll position.
   *
   * @param {number} offset The offset.
   * @returns {OrderView}
   */
  setOffset(e) {
    return this.sizeSet.setOffset(e), this;
  }
  /**
   * Checks if this instance of the view shares the root node with another instance. This happens only once when
   * a row (TR) as a root node is managed by two OrderView instances. If this happens another DOM injection
   * algorithm is performed to achieve consistent order.
   *
   * @returns {boolean}
   */
  isSharedViewSet() {
    return this.sizeSet.isShared();
  }
  /**
   * Returns rendered DOM element based on visual index.
   *
   * @param {number} visualIndex The visual index.
   * @returns {HTMLElement}
   */
  getNode(e) {
    return e < this.collectedNodes.length ? this.collectedNodes[e] : null;
  }
  /**
   * Returns currently processed DOM element.
   *
   * @returns {HTMLElement}
   */
  getCurrentNode() {
    const e = this.collectedNodes.length;
    return e > 0 ? this.collectedNodes[e - 1] : null;
  }
  /**
   * Returns rendered child count for this instance.
   *
   * @returns {number}
   */
  getRenderedChildCount() {
    const {
      rootNode: e,
      sizeSet: n
    } = this;
    let i = 0;
    if (this.isSharedViewSet()) {
      let r = e.firstElementChild;
      for (; r; ) {
        if (r.tagName === this.childNodeType)
          i += 1;
        else if (n.isPlaceOn(bm))
          break;
        r = r.nextElementSibling;
      }
    } else
      i = e.childElementCount;
    return i;
  }
  /**
   * Setups and prepares all necessary properties and start the rendering process.
   * This method has to be called only once (at the start) for the render cycle.
   */
  start() {
    this.collectedNodes.length = 0, this.visualIndex = 0;
    const {
      rootNode: e,
      sizeSet: n
    } = this, i = this.isSharedViewSet(), {
      nextSize: r
    } = n.getViewSize();
    let o = this.getRenderedChildCount();
    for (; o < r; ) {
      const a = this.nodesPool();
      !i || i && n.isPlaceOn(Rm) ? e.appendChild(a) : e.insertBefore(a, e.firstChild), o += 1;
    }
    const s = i && n.isPlaceOn(bm);
    for (; o > r; )
      e.removeChild(s ? e.firstChild : e.lastChild), o -= 1;
  }
  /**
   * Renders the DOM element based on visual index (which is calculated internally).
   * This method has to be called as many times as the size count is met (to cover all previously rendered DOM elements).
   */
  render() {
    const {
      rootNode: e,
      sizeSet: n
    } = this;
    let i = this.visualIndex;
    this.isSharedViewSet() && n.isPlaceOn(Rm) && (i += n.sharedSize.nextSize);
    let r = e.childNodes[i];
    if (r.tagName !== this.childNodeType) {
      const o = this.nodesPool();
      e.replaceChild(o, r), r = o;
    }
    this.collectedNodes.push(r), this.visualIndex += 1;
  }
  /**
   * Ends the render process.
   * This method has to be called only once (at the end) for the render cycle.
   */
  end() {
  }
}
class Lx extends kx {
  /**
   * The method results in merging external order view into the current order. This happens only for order views which
   * operate on the same root node.
   *
   * In the table, there is only one scenario when this happens. TR root element
   * has a common root node with cells order view and row headers order view. Both classes have to share
   * information about their order sizes to make proper diff calculations.
   *
   * @param {OrderView} orderView The order view to merging with. The view will be added at the beginning of the list.
   * @returns {SharedOrderView}
   */
  prependView(e) {
    return this.sizeSet.prepend(e.sizeSet), e.sizeSet.append(this.sizeSet), this;
  }
  /**
   * The method results in merging external order view into the current order. This happens only for order views which
   * operate on the same root node.
   *
   * In the table, there is only one scenario when this happens. TR root element
   * has a common root node with cells order view and row headers order view. Both classes have to share
   * information about their order sizes to make proper diff calculations.
   *
   * @param {OrderView} orderView The order view to merging with. The view will be added at the end of the list.
   * @returns {SharedOrderView}
   */
  appendView(e) {
    return this.sizeSet.append(e.sizeSet), e.sizeSet.prepend(this.sizeSet), this;
  }
}
function SQ(t, e, n) {
  return (e = bQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function bQ(t) {
  var e = RQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function RQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class EQ {
  constructor(e) {
    SQ(this, "nodeType", void 0), this.nodeType = e.toUpperCase();
  }
  /**
   * Set document owner for this instance.
   *
   * @param {HTMLDocument} rootDocument The document window owner.
   */
  setRootDocument(e) {
    this.rootDocument = e;
  }
  /**
   * Obtains an element. The returned elements in the feature can be cached.
   *
   * @returns {HTMLElement}
   */
  obtain() {
    return this.rootDocument.createElement(this.nodeType);
  }
}
function sc(t, e, n) {
  return (e = TQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function TQ(t) {
  var e = MQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function MQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Rd {
  constructor(e, n) {
    sc(this, "nodesPool", null), sc(this, "nodeType", void 0), sc(this, "rootNode", void 0), sc(this, "table", null), sc(this, "renderedNodes", 0), this.nodesPool = typeof e == "string" ? new EQ(e) : null, this.nodeType = e, this.rootNode = n;
  }
  /**
   * Sets the table renderer instance to the current renderer.
   *
   * @param {TableRenderer} table The TableRenderer instance.
   */
  setTable(e) {
    this.nodesPool && this.nodesPool.setRootDocument(e.rootDocument), this.table = e;
  }
  /**
   * Adjusts the number of rendered nodes.
   */
  adjust() {
  }
  /**
   * Renders the contents to the elements.
   */
  render() {
  }
}
function GE(t, e, n) {
  return (e = IQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function IQ(t) {
  var e = OQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function OQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class PQ extends Rd {
  constructor() {
    super("TH"), GE(this, "orderViews", /* @__PURE__ */ new WeakMap()), GE(this, "sourceRowIndex", 0);
  }
  /**
   * Obtains the instance of the SharedOrderView class which is responsible for rendering the nodes to the root node.
   *
   * @param {HTMLTableRowElement} rootNode The TR element, which is root element for row headers (TH).
   * @returns {SharedOrderView}
   */
  obtainOrderView(e) {
    let n;
    return this.orderViews.has(e) ? n = this.orderViews.get(e) : (n = new Lx(e, (i) => this.nodesPool.obtain(this.sourceRowIndex, i), this.nodeType), this.orderViews.set(e, n)), n;
  }
  /**
   * Renders the cells.
   */
  render() {
    const {
      rowsToRender: e,
      rowHeaderFunctions: n,
      rowHeadersCount: i,
      rows: r,
      cells: o
    } = this.table;
    for (let s = 0; s < e; s++) {
      const a = this.table.renderedRowToSource(s), l = r.getRenderedNode(s);
      this.sourceRowIndex = a;
      const u = this.obtainOrderView(l), c = o.obtainOrderView(l);
      u.appendView(c).setSize(i).setOffset(this.table.renderedColumnToSource(0)).start();
      for (let h = 0; h < i; h++) {
        u.render();
        const d = u.getCurrentNode();
        d.className = "", d.removeAttribute("style"), ml(d, [new RegExp("aria-(.*)"), new RegExp("role")]), this.table.isAriaEnabled() && Ee(d, [mG(), RG(), ZS(h + 1), Du(-1)]), n[h](a, d, h);
      }
      u.end();
    }
  }
}
class xQ extends Rd {
  constructor(e) {
    super(null, e);
  }
  /**
   * Adjusts the number of the rendered elements.
   */
  adjust() {
    const {
      columnHeadersCount: e,
      rowHeadersCount: n
    } = this.table;
    let i = this.rootNode.firstChild;
    if (e) {
      const {
        columnsToRender: r
      } = this.table, o = r + n;
      for (let a = 0, l = e; a < l; a++) {
        for (i = this.rootNode.childNodes[a], i || (i = this.table.rootDocument.createElement("tr"), this.rootNode.appendChild(i)), this.renderedNodes = i.childNodes.length; this.renderedNodes < o; )
          i.appendChild(this.table.rootDocument.createElement("th")), this.renderedNodes += 1;
        for (; this.renderedNodes > o; )
          i.removeChild(i.lastChild), this.renderedNodes -= 1;
      }
      const s = this.rootNode.childNodes.length;
      if (s > e)
        for (let a = e; a < s; a++)
          this.rootNode.removeChild(this.rootNode.lastChild);
    } else
      i && br(i);
  }
  /**
   * Renders the TH elements.
   */
  render() {
    const {
      columnHeadersCount: e
    } = this.table;
    this.table.isAriaEnabled() && Ee(this.rootNode, [OP()]);
    for (let n = 0; n < e; n += 1) {
      const {
        columnHeaderFunctions: i,
        columnsToRender: r,
        rowHeadersCount: o
      } = this.table, s = this.rootNode.childNodes[n];
      this.table.isAriaEnabled() && Ee(s, [py(), _P(n + 1)]);
      for (let a = -1 * o; a < r; a += 1) {
        const l = this.table.renderedColumnToSource(a), u = s.childNodes[a + o];
        u.className = "", u.removeAttribute("style"), ml(u, [new RegExp("aria-(.*)"), new RegExp("role")]), this.table.isAriaEnabled() && Ee(u, [ZS(a + 1 + this.table.rowHeadersCount), Du(-1), pG(), ...a >= 0 ? [bG()] : [
          // Adding `role=row` to the corner headers to prevent
          // https://github.com/handsontable/dev-handsontable/issues/1574
          py()
        ]]), i[n](l, u, n);
      }
    }
  }
}
let zE = !1;
class HQ extends Rd {
  constructor(e) {
    super(null, e);
  }
  /**
   * Adjusts the number of the rendered elements.
   */
  adjust() {
    const {
      columnsToRender: e,
      rowHeadersCount: n
    } = this.table, i = e + n;
    for (; this.renderedNodes < i; )
      this.rootNode.appendChild(this.table.rootDocument.createElement("col")), this.renderedNodes += 1;
    for (; this.renderedNodes > i; )
      this.rootNode.removeChild(this.rootNode.lastChild), this.renderedNodes -= 1;
  }
  /**
   * Renders the col group elements.
   */
  render() {
    this.adjust();
    const {
      columnsToRender: e,
      rowHeadersCount: n
    } = this.table;
    !zE && e > 1e3 && (zE = !0, gt(mt`Performance tip: Handsontable rendered more than 1000 visible columns.\x20
        Consider limiting the number of rendered columns by specifying the table width and/or\x20
        turning off the "renderAllColumns" option.`));
    for (let r = 0; r < n; r++) {
      const o = this.table.renderedColumnToSource(r), s = this.table.columnUtils.getHeaderWidth(o);
      this.rootNode.childNodes[r].style.width = `${s}px`;
    }
    for (let r = 0; r < e; r++) {
      const o = this.table.renderedColumnToSource(r), s = this.table.columnUtils.getStretchedColumnWidth(o);
      this.rootNode.childNodes[r + n].style.width = `${s}px`;
    }
    const i = this.rootNode.firstChild;
    i && Q(i, "rowHeader");
  }
}
function _Q(t, e, n) {
  return (e = NQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function NQ(t) {
  var e = DQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function DQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const hf = {
  rowEven: "ht__row_even",
  rowOdd: "ht__row_odd"
};
let KE = !1;
class kQ extends Rd {
  constructor(e) {
    super("TR", e), _Q(this, "orderView", void 0), this.orderView = new kx(e, (n) => this.nodesPool.obtain(n), this.nodeType);
  }
  /**
   * Returns currently rendered node.
   *
   * @param {string} visualIndex Visual index of the rendered node (it always goeas from 0 to N).
   * @returns {HTMLTableRowElement}
   */
  getRenderedNode(e) {
    return this.orderView.getNode(e);
  }
  /**
   * Renders the cells.
   */
  render() {
    const {
      rowsToRender: e
    } = this.table;
    !KE && e > 1e3 && (KE = !0, gt(mt`Performance tip: Handsontable rendered more than 1000 visible rows.\x20
        Consider limiting the number of rendered rows by specifying the table height and/or\x20
        turning off the "renderAllRows" option.`)), this.table.isAriaEnabled() && Ee(this.rootNode, [OP()]), this.orderView.setSize(e).setOffset(this.table.renderedRowToSource(0)).start();
    for (let r = 0; r < e; r++) {
      this.orderView.render();
      const o = this.orderView.getCurrentNode(), s = this.table.renderedRowToSource(r);
      if (this.table.isAriaEnabled()) {
        var n, i;
        Ee(o, [
          py(),
          // `aria-rowindex` is incremented by both tbody and thead rows.
          _P(s + ((n = (i = this.table.rowUtils) === null || i === void 0 || (i = i.dataAccessObject) === null || i === void 0 ? void 0 : i.columnHeaders.length) !== null && n !== void 0 ? n : 0) + 1)
        ]);
      }
      xe(o, [hf.rowEven, hf.rowOdd]), (s + 1) % 2 === 0 ? Q(o, hf.rowEven) : Q(o, hf.rowOdd);
    }
    this.orderView.end();
  }
}
function QE(t, e, n) {
  return (e = LQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function LQ(t) {
  var e = BQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function BQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class FQ extends Rd {
  constructor() {
    super("TD"), QE(this, "orderViews", /* @__PURE__ */ new WeakMap()), QE(this, "sourceRowIndex", 0);
  }
  /**
   * Obtains the instance of the SharedOrderView class which is responsible for rendering the nodes to the root node.
   *
   * @param {HTMLTableRowElement} rootNode The TR element, which is root element for cells (TD).
   * @returns {SharedOrderView}
   */
  obtainOrderView(e) {
    let n;
    return this.orderViews.has(e) ? n = this.orderViews.get(e) : (n = new Lx(e, (i) => this.nodesPool.obtain(this.sourceRowIndex, i), this.nodeType), this.orderViews.set(e, n)), n;
  }
  /**
   * Renders the cells.
   */
  render() {
    const {
      rowsToRender: e,
      columnsToRender: n,
      rows: i,
      rowHeaders: r
    } = this.table;
    for (let a = 0; a < e; a++) {
      const l = this.table.renderedRowToSource(a), u = i.getRenderedNode(a);
      this.sourceRowIndex = l;
      const c = this.obtainOrderView(u), h = r.obtainOrderView(u);
      c.prependView(h).setSize(n).setOffset(this.table.renderedColumnToSource(0)).start();
      for (let d = 0; d < n; d++) {
        c.render();
        const g = c.getCurrentNode(), m = this.table.renderedColumnToSource(d);
        if (Te(g, "hide") || (g.className = ""), g.removeAttribute("style"), g.removeAttribute("dir"), ml(g, [new RegExp("aria-(.*)"), new RegExp("role")]), this.table.cellRenderer(l, m, g), this.table.isAriaEnabled()) {
          var o, s;
          Ee(g, [
            ...g.hasAttribute("role") ? [] : [gG()],
            Du(-1),
            // `aria-colindex` is incremented by both tbody and thead rows.
            ZS(m + ((o = (s = this.table.rowUtils) === null || s === void 0 || (s = s.dataAccessObject) === null || s === void 0 ? void 0 : s.rowHeaders.length) !== null && o !== void 0 ? o : 0) + 1)
          ]);
        }
      }
      c.end();
    }
  }
}
function cn(t, e, n) {
  return (e = VQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function VQ(t) {
  var e = WQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function WQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class $Q {
  constructor(e) {
    let {
      cellRenderer: n
    } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    cn(this, "rootNode", void 0), cn(this, "rootDocument", void 0), cn(this, "rowHeaders", null), cn(this, "columnHeaders", null), cn(this, "colGroup", null), cn(this, "rows", null), cn(this, "cells", null), cn(this, "rowFilter", null), cn(this, "columnFilter", null), cn(this, "rowUtils", null), cn(this, "columnUtils", null), cn(this, "rowsToRender", 0), cn(this, "columnsToRender", 0), cn(this, "rowHeaderFunctions", []), cn(this, "rowHeadersCount", 0), cn(this, "columnHeaderFunctions", []), cn(this, "columnHeadersCount", 0), cn(this, "cellRenderer", void 0), cn(this, "activeOverlayName", void 0), this.rootNode = e, this.rootDocument = this.rootNode.ownerDocument, this.cellRenderer = n;
  }
  /**
   * Sets the overlay that is currently rendered. If `null` is provided, the master overlay is set.
   *
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'|'master'} overlayName The overlay name.
   */
  setActiveOverlayName(e) {
    this.activeOverlayName = e;
  }
  /**
   * Set row and column util classes.
   *
   * @param {RowUtils} rowUtils RowUtils instance which provides useful methods related to row sizes.
   * @param {ColumnUtils} columnUtils ColumnUtils instance which provides useful methods related to row sizes.
   */
  setAxisUtils(e, n) {
    this.rowUtils = e, this.columnUtils = n;
  }
  /**
   * Sets viewport size of the table.
   *
   * @param {number} rowsCount An amount of rows to render.
   * @param {number} columnsCount An amount of columns to render.
   */
  setViewportSize(e, n) {
    this.rowsToRender = e, this.columnsToRender = n;
  }
  /**
   * Sets row and column filter instances.
   *
   * @param {RowFilter} rowFilter Row filter instance which contains all necessary information about row index transformation.
   * @param {ColumnFilter} columnFilter Column filter instance which contains all necessary information about row
   * index transformation.
   */
  setFilters(e, n) {
    this.rowFilter = e, this.columnFilter = n;
  }
  /**
   * Sets row and column header functions.
   *
   * @param {Function[]} rowHeaders Row header functions. Factories for creating content for row headers.
   * @param {Function[]} columnHeaders Column header functions. Factories for creating content for column headers.
   */
  setHeaderContentRenderers(e, n) {
    this.rowHeaderFunctions = e, this.rowHeadersCount = e.length, this.columnHeaderFunctions = n, this.columnHeadersCount = n.length;
  }
  /**
   * Sets table renderers.
   *
   * @param {renderers} renderers The renderer units.
   * @param {RowHeadersRenderer} renderers.rowHeaders Row headers renderer.
   * @param {ColumnHeadersRenderer} renderers.columnHeaders Column headers renderer.
   * @param {ColGroupRenderer} renderers.colGroup Col group renderer.
   * @param {RowsRenderer} renderers.rows Rows renderer.
   * @param {CellsRenderer} renderers.cells Cells renderer.
   */
  setRenderers() {
    let {
      rowHeaders: e,
      columnHeaders: n,
      colGroup: i,
      rows: r,
      cells: o
    } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    e.setTable(this), n.setTable(this), i.setTable(this), r.setTable(this), o.setTable(this), this.rowHeaders = e, this.columnHeaders = n, this.colGroup = i, this.rows = r, this.cells = o;
  }
  /**
   * Transforms visual/rendered row index to source index.
   *
   * @param {number} rowIndex Rendered index.
   * @returns {number}
   */
  renderedRowToSource(e) {
    return this.rowFilter.renderedToSource(e);
  }
  /**
   * Transforms visual/rendered column index to source index.
   *
   * @param {number} columnIndex Rendered index.
   * @returns {number}
   */
  renderedColumnToSource(e) {
    return this.columnFilter.renderedToSource(e);
  }
  /**
   * Returns `true` if the accessibility-related ARIA tags should be added to the table, `false` otherwise.
   *
   * @returns {boolean}
   */
  isAriaEnabled() {
    return this.rowUtils.wtSettings.getSetting("ariaTags");
  }
  /**
   * Renders the table.
   */
  render() {
    this.colGroup.adjust(), this.columnHeaders.adjust(), this.rows.adjust(), this.rowHeaders.adjust(), this.columnHeaders.render(), this.rows.render(), this.rowHeaders.render(), this.cells.render(), this.columnUtils.calculateWidths(), this.colGroup.render();
    const {
      rowsToRender: e,
      rows: n
    } = this;
    for (let i = 0; i < e; i++) {
      const r = n.getRenderedNode(i);
      if (r.firstChild) {
        const o = this.renderedRowToSource(i), s = this.rowUtils.getHeightByOverlayName(o, this.activeOverlayName);
        s ? r.firstChild.style.height = `${s - 1}px` : r.firstChild.style.height = "";
      }
    }
  }
}
class UQ {
  constructor() {
    let {
      TABLE: e,
      THEAD: n,
      COLGROUP: i,
      TBODY: r,
      rowUtils: o,
      columnUtils: s,
      cellRenderer: a
    } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    this.renderer = new $Q(e, {
      cellRenderer: a
    }), this.renderer.setRenderers({
      rowHeaders: new PQ(),
      columnHeaders: new xQ(n),
      colGroup: new HQ(i),
      rows: new kQ(r),
      cells: new FQ()
    }), this.renderer.setAxisUtils(o, s);
  }
  /**
   * Sets the overlay that is currently rendered. If `null` is provided, the master overlay is set.
   *
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'|'master'} overlayName The overlay name.
   * @returns {Renderer}
   */
  setActiveOverlayName(e) {
    return this.renderer.setActiveOverlayName(e), this;
  }
  /**
   * Sets filter calculators for newly calculated row and column position. The filters are used to transform visual
   * indexes (0 to N) to source indexes provided by Handsontable.
   *
   * @param {RowFilter} rowFilter The row filter instance.
   * @param {ColumnFilter} columnFilter The column filter instance.
   * @returns {Renderer}
   */
  setFilters(e, n) {
    return this.renderer.setFilters(e, n), this;
  }
  /**
   * Sets the viewport size of the rendered table.
   *
   * @param {number} rowsCount An amount of rows to render.
   * @param {number} columnsCount An amount of columns to render.
   * @returns {Renderer}
   */
  setViewportSize(e, n) {
    return this.renderer.setViewportSize(e, n), this;
  }
  /**
   * Sets row and column header functions.
   *
   * @param {Function[]} rowHeaders Row header functions. Factories for creating content for row headers.
   * @param {Function[]} columnHeaders Column header functions. Factories for creating content for column headers.
   * @returns {Renderer}
   */
  setHeaderContentRenderers(e, n) {
    return this.renderer.setHeaderContentRenderers(e, n), this;
  }
  /**
   * Adjusts the table (preparing for render).
   */
  adjust() {
    this.renderer.adjust();
  }
  /**
   * Renders the table.
   */
  render() {
    this.renderer.render();
  }
}
function ac(t, e, n) {
  YQ(t, e), e.set(t, n);
}
function YQ(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function df(t, e, n) {
  return (e = jQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function jQ(t) {
  var e = GQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function GQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Pn(t, e) {
  return t.get(Bx(t, e));
}
function lc(t, e, n) {
  return t.set(Bx(t, e), n), n;
}
function Bx(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var ff = /* @__PURE__ */ new WeakMap(), Ml = /* @__PURE__ */ new WeakMap(), uc = /* @__PURE__ */ new WeakMap(), gf = /* @__PURE__ */ new WeakMap(), sa = /* @__PURE__ */ new WeakMap();
class zQ {
  /**
   * @param {ColumnStretchingOptions} options Object with all options specified for column viewport calculation.
   */
  constructor(e) {
    let {
      totalColumns: n,
      stretchMode: i,
      stretchingColumnWidthFn: r,
      columnWidthFn: o
    } = e;
    df(this, "stretchAllRatio", 0), df(this, "stretchLastWidth", 0), df(this, "stretchAllColumnsWidth", []), ac(this, ff, 0), df(this, "needVerifyLastColumnWidth", !0), ac(this, Ml, () => 0), ac(this, uc, (s) => s), ac(this, gf, (s) => s), ac(this, sa, () => "none"), lc(Ml, this, n), lc(sa, this, i), lc(uc, this, r != null ? r : Pn(uc, this)), lc(gf, this, o != null ? o : Pn(gf, this));
  }
  /**
   * Recalculate columns stretching.
   *
   * @param {number} totalWidth The total width of the table.
   */
  refreshStretching(e) {
    if (Pn(sa, this).call(this) === "none")
      return;
    this.stretchAllRatio = 0, this.stretchAllColumnsWidth = [], this.needVerifyLastColumnWidth = !0, this.stretchLastWidth = 0, lc(ff, this, e);
    let n = 0;
    for (let r = 0; r < Pn(Ml, this).call(this); r++) {
      const o = this._getColumnWidth(r), s = Pn(uc, this).call(this, void 0, r);
      typeof s == "number" ? e -= s : n += o;
    }
    const i = e - n;
    if (Pn(sa, this).call(this) === "all" && i > 0)
      this.stretchAllRatio = e / n, this.stretchAllColumnsWidth = [], this.needVerifyLastColumnWidth = !0;
    else if (Pn(sa, this).call(this) === "last" && e !== 1 / 0) {
      const r = this._getColumnWidth(Pn(Ml, this).call(this) - 1), o = i + r;
      this.stretchLastWidth = o >= 0 ? o : r;
    }
  }
  /**
   * Get stretched column width based on stretchH (all or last) setting passed in handsontable instance.
   *
   * @param {number} column The visual column index.
   * @param {number} baseWidth The default column width.
   * @returns {number|null}
   */
  getStretchedColumnWidth(e, n) {
    let i = null;
    return Pn(sa, this).call(this) === "all" && this.stretchAllRatio !== 0 ? i = this._getStretchedAllColumnWidth(e, n) : Pn(sa, this).call(this) === "last" && this.stretchLastWidth !== 0 && (i = this._getStretchedLastColumnWidth(e)), i;
  }
  /**
   * @param {number} column The visual column index.
   * @param {number} baseWidth The default column width.
   * @returns {number}
   * @private
   */
  _getStretchedAllColumnWidth(e, n) {
    let i = 0;
    if (!this.stretchAllColumnsWidth[e]) {
      const r = Math.round(n * this.stretchAllRatio), o = Pn(uc, this).call(this, r, e);
      o === void 0 ? this.stretchAllColumnsWidth[e] = r : this.stretchAllColumnsWidth[e] = isNaN(o) ? this._getColumnWidth(e) : o;
    }
    if (this.stretchAllColumnsWidth.length === Pn(Ml, this).call(this) && this.needVerifyLastColumnWidth) {
      this.needVerifyLastColumnWidth = !1;
      for (let r = 0; r < this.stretchAllColumnsWidth.length; r++)
        i += this.stretchAllColumnsWidth[r];
      i !== Pn(ff, this) && (this.stretchAllColumnsWidth[this.stretchAllColumnsWidth.length - 1] += Pn(ff, this) - i);
    }
    return this.stretchAllColumnsWidth[e];
  }
  /**
   * @param {number} column The visual column index.
   * @returns {number|null}
   * @private
   */
  _getStretchedLastColumnWidth(e) {
    return e === Pn(Ml, this).call(this) - 1 ? this.stretchLastWidth : null;
  }
  /**
   * @param {number} column The visual column index.
   * @returns {number}
   * @private
   */
  _getColumnWidth(e) {
    let n = Pn(gf, this).call(this, e);
    return isNaN(n) && (n = Np), n;
  }
}
function mf(t, e, n) {
  return (e = KQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function KQ(t) {
  var e = QQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function QQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class XQ {
  /**
   * @param {TableDao} dataAccessObject The table Data Access Object.
   * @param {Settings} wtSettings The walkontable settings.
   */
  constructor(e, n) {
    mf(this, "dataAccessObject", void 0), mf(this, "wtSettings", void 0), mf(this, "headerWidths", /* @__PURE__ */ new Map()), mf(this, "stretching", void 0), this.dataAccessObject = e, this.wtSettings = n, this.stretching = new zQ({
      totalColumns: () => this.wtSettings.getSetting("totalColumns"),
      stretchMode: () => this.wtSettings.getSetting("stretchH"),
      stretchingColumnWidthFn: (i, r) => this.wtSettings.getSetting("onBeforeStretchingColumnWidth", i, r),
      columnWidthFn: (i) => this.dataAccessObject.wtTable.getColumnWidth(i)
    });
  }
  /**
   * Returns column width based on passed source index.
   *
   * @param {number} sourceIndex Column source index.
   * @returns {number}
   */
  getWidth(e) {
    return this.wtSettings.getSetting("columnWidth", e) || this.wtSettings.getSetting("defaultColumnWidth");
  }
  /**
   * Returns stretched column width based on passed source index.
   *
   * @param {number} sourceIndex Column source index.
   * @returns {number}
   */
  getStretchedColumnWidth(e) {
    let n = this.getWidth(e);
    const i = this.stretching.getStretchedColumnWidth(e, n);
    return i && (n = i), n;
  }
  /**
   * Returns column header height based on passed header level.
   *
   * @param {number} level Column header level.
   * @returns {number}
   */
  getHeaderHeight(e) {
    let n = this.wtSettings.getSetting("defaultRowHeight");
    const i = this.dataAccessObject.wtViewport.oversizedColumnHeaders[e];
    return i !== void 0 && (n = n ? Math.max(n, i) : i), n;
  }
  /**
   * Returns column header width based on passed source index.
   *
   * @param {number} sourceIndex Column source index.
   * @returns {number}
   */
  getHeaderWidth(e) {
    return this.headerWidths.get(this.dataAccessObject.wtTable.columnFilter.sourceToRendered(e));
  }
  /**
   * Refreshes the stretching column width by recalculating the widths of the columns.
   */
  refreshStretching() {
    const {
      wtTable: e,
      wtViewport: n,
      cloneSource: i
    } = this.dataAccessObject, r = i ? i.wtTable.holder : e.holder, o = r.offsetHeight < r.scrollHeight ? pn() : 0;
    this.stretching.refreshStretching(n.getViewportWidth() - o);
  }
  /**
   * Calculates column header widths that can be retrieved from the cache.
   */
  calculateWidths() {
    const {
      wtSettings: e
    } = this;
    let n = e.getSetting("rowHeaderWidth");
    if (this.refreshStretching(), n = e.getSetting("onModifyRowHeaderWidth", n), n != null) {
      const i = e.getSetting("rowHeaders").length, r = e.getSetting("defaultColumnWidth");
      for (let o = 0; o < i; o++) {
        let s = Array.isArray(n) ? n[o] : n;
        s = s == null ? r : s, this.headerWidths.set(o, s);
      }
    }
  }
}
function XE(t, e, n) {
  return (e = qQ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function qQ(t) {
  var e = ZQ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function ZQ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class JQ {
  /**
   * @param {TableDao} dataAccessObject The table Data Access Object.
   * @param {Settings} wtSettings The walkontable settings.
   */
  constructor(e, n) {
    XE(this, "dataAccessObject", void 0), XE(this, "wtSettings", void 0), this.dataAccessObject = e, this.wtSettings = n;
  }
  /**
   * Returns row height based on passed source index.
   *
   * @param {number} sourceIndex Row source index.
   * @returns {number}
   */
  getHeight(e) {
    let n = this.wtSettings.getSetting("rowHeight", e);
    const i = this.dataAccessObject.wtViewport.oversizedRows[e];
    return i !== void 0 && (n = n === void 0 ? i : Math.max(n, i)), n;
  }
  /**
   * Returns row height based on passed source index for the specified overlay type.
   *
   * @param {number} sourceIndex Row source index.
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'|'master'} overlayName The overlay name.
   * @returns {number}
   */
  getHeightByOverlayName(e, n) {
    let i = this.wtSettings.getSetting("rowHeightByOverlayName", e, n);
    const r = this.dataAccessObject.wtViewport.oversizedRows[e];
    return r !== void 0 && (i = i === void 0 ? r : Math.max(i, r)), i;
  }
}
function _r(t, e, n) {
  return (e = e3(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function e3(t) {
  var e = t3(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function t3(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class n3 {
  /**
   *
   * @abstract
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {'master'|CLONE_TYPES_ENUM} name Overlay name.
   */
  constructor(e, n, i, r, o) {
    _r(this, "wtSettings", null), _r(this, "domBindings", void 0), _r(this, "TBODY", null), _r(this, "THEAD", null), _r(this, "COLGROUP", null), _r(this, "hasTableHeight", !0), _r(this, "hasTableWidth", !0), _r(this, "isTableVisible", !1), _r(this, "tableOffset", 0), _r(this, "holderOffset", 0), this.domBindings = i, this.isMaster = o === "master", this.name = o, this.dataAccessObject = e, this.facadeGetter = n, this.wtSettings = r, this.instance = this.dataAccessObject.wot, this.wot = this.dataAccessObject.wot, this.TABLE = i.rootTable, eb(this.TABLE), this.spreader = this.createSpreader(this.TABLE), this.hider = this.createHider(this.spreader), this.holder = this.createHolder(this.hider), this.wtRootElement = this.holder.parentNode, this.isMaster && this.alignOverlaysWithTrimmingContainer(), this.fixTableDomTree(), this.rowFilter = null, this.columnFilter = null, this.correctHeaderWidth = !1;
    const s = this.wtSettings.getSettingPure("rowHeaderWidth");
    this.wtSettings.update("rowHeaderWidth", () => this._modifyRowHeaderWidth(s)), this.rowUtils = new JQ(this.dataAccessObject, this.wtSettings), this.columnUtils = new XQ(this.dataAccessObject, this.wtSettings), this.tableRenderer = new UQ({
      // TODO refactoring, It can be passed through IOC.
      TABLE: this.TABLE,
      THEAD: this.THEAD,
      COLGROUP: this.COLGROUP,
      TBODY: this.TBODY,
      rowUtils: this.rowUtils,
      columnUtils: this.columnUtils,
      cellRenderer: this.wtSettings.getSettingPure("cellRenderer")
    });
  }
  /**
   * Returns a boolean that is true if this Table represents a specific overlay, identified by the overlay name.
   * For MasterTable, it returns false.
   *
   * @param {string} overlayTypeName The overlay type.
   * @returns {boolean}
   */
  is(e) {
    return this.name === e;
  }
  /**
   *
   */
  fixTableDomTree() {
    const e = this.domBindings.rootDocument;
    this.TBODY = this.TABLE.querySelector("tbody"), this.TBODY || (this.TBODY = e.createElement("tbody"), this.TABLE.appendChild(this.TBODY)), this.THEAD = this.TABLE.querySelector("thead"), this.THEAD || (this.THEAD = e.createElement("thead"), this.TABLE.insertBefore(this.THEAD, this.TBODY)), this.COLGROUP = this.TABLE.querySelector("colgroup"), this.COLGROUP || (this.COLGROUP = e.createElement("colgroup"), this.TABLE.insertBefore(this.COLGROUP, this.THEAD));
  }
  /**
   * @param {HTMLTableElement} table An element to process.
   * @returns {HTMLElement}
   */
  createSpreader(e) {
    const n = e.parentNode;
    let i;
    return (!n || n.nodeType !== Node.ELEMENT_NODE || !Te(n, "wtHolder")) && (i = this.domBindings.rootDocument.createElement("div"), i.className = "wtSpreader", n && n.insertBefore(i, e), i.appendChild(e)), i.style.position = "relative", this.wtSettings.getSetting("ariaTags") && Ee(i, [oo()]), i;
  }
  /**
   * @param {HTMLElement} spreader An element to the hider element is injected.
   * @returns {HTMLElement}
   */
  createHider(e) {
    const n = e.parentNode;
    let i;
    return (!n || n.nodeType !== Node.ELEMENT_NODE || !Te(n, "wtHolder")) && (i = this.domBindings.rootDocument.createElement("div"), i.className = "wtHider", n && n.insertBefore(i, e), i.appendChild(e)), this.wtSettings.getSetting("ariaTags") && Ee(i, [oo()]), i;
  }
  /**
   *
   * @param {HTMLElement} hider An element to the holder element is injected.
   * @returns {HTMLElement}
   */
  createHolder(e) {
    const n = e.parentNode;
    let i;
    return (!n || n.nodeType !== Node.ELEMENT_NODE || !Te(n, "wtHolder")) && (i = this.domBindings.rootDocument.createElement("div"), i.style.position = "relative", i.className = "wtHolder", n && n.insertBefore(i, e), this.isMaster && (i.parentNode.className += "ht_master handsontable", i.parentNode.setAttribute("dir", this.wtSettings.getSettingPure("rtlMode") ? "rtl" : "ltr"), this.wtSettings.getSetting("ariaTags") && Ee(i.parentNode, [oo()])), i.appendChild(e)), this.wtSettings.getSetting("ariaTags") && Ee(i, [oo()]), i;
  }
  /**
   * Redraws the table.
   *
   * @param {boolean} [fastDraw=false] If TRUE, will try to avoid full redraw and only update the border positions.
   *                                   If FALSE or UNDEFINED, will perform a full redraw.
   * @returns {Table}
   */
  draw() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    const {
      wtSettings: n
    } = this, {
      wtOverlays: i,
      wtViewport: r
    } = this.dataAccessObject, o = n.getSetting("totalRows"), s = n.getSetting("totalColumns"), a = n.getSetting("rowHeaders"), l = a.length, u = n.getSetting("columnHeaders"), c = u.length;
    let h = e;
    if (this.isMaster && (i.beforeDraw(), this.holderOffset = gn(this.holder), h = r.createCalculators(h), l && !n.getSetting("fixedColumnsStart"))) {
      const g = i.inlineStartOverlay.getScrollPosition(), m = this.correctHeaderWidth;
      this.correctHeaderWidth = g !== 0, m !== this.correctHeaderWidth && (h = !1);
    }
    if (h)
      this.isMaster && i.refresh(!0);
    else {
      this.isMaster ? this.tableOffset = gn(this.TABLE) : this.tableOffset = this.dataAccessObject.parentTableOffset;
      const g = o > 0 ? this.getFirstRenderedRow() : 0, m = s > 0 ? this.getFirstRenderedColumn() : 0;
      this.rowFilter = new fQ(g, o, c), this.columnFilter = new uQ(m, s, l);
      let w = !0;
      if (this.isMaster) {
        this.alignOverlaysWithTrimmingContainer();
        const C = {};
        this.wtSettings.getSetting("beforeDraw", !0, C), w = C.skipRender !== !0;
      }
      if (w) {
        this.tableRenderer.setHeaderContentRenderers(a, u), (this.is(no) || this.is($s)) && this.tableRenderer.setHeaderContentRenderers(a, []), this.resetOversizedRows(), this.tableRenderer.setActiveOverlayName(this.name).setViewportSize(this.getRenderedRowsCount(), this.getRenderedColumnsCount()).setFilters(this.rowFilter, this.columnFilter).render();
        let C;
        if (this.isMaster && (C = this.dataAccessObject.workspaceWidth, r.containerWidth = null, this.markOversizedColumnHeaders()), this.adjustColumnHeaderHeights(), (this.isMaster || this.is(no)) && this.markOversizedRows(), this.isMaster) {
          this.wtSettings.getSetting("externalRowCalculator") || r.createVisibleCalculators(), i.refresh(!1), i.applyToDOM();
          const S = It(this.hider), b = It(this.TABLE);
          S !== 0 && b !== S && (this.columnUtils.calculateWidths(), this.tableRenderer.renderer.colGroup.render()), C !== r.getWorkspaceWidth() && (r.containerWidth = null, this.columnUtils.calculateWidths(), this.tableRenderer.renderer.colGroup.render()), this.wtSettings.getSetting("onDraw", !0);
        } else
          this.is(no) && this.dataAccessObject.cloneSource.wtOverlays.adjustElementsSize();
      }
    }
    let d = !1;
    return this.isMaster && (d = i.topOverlay.resetFixedPosition(), i.bottomOverlay.clone && (d = i.bottomOverlay.resetFixedPosition() || d), d = i.inlineStartOverlay.resetFixedPosition() || d, i.topInlineStartCornerOverlay && i.topInlineStartCornerOverlay.resetFixedPosition(), i.bottomInlineStartCornerOverlay && i.bottomInlineStartCornerOverlay.clone && i.bottomInlineStartCornerOverlay.resetFixedPosition()), d ? (i.refreshAll(), i.adjustElementsSize()) : this.dataAccessObject.selectionManager.setActiveOverlay(this.facadeGetter()).render(h), this.isMaster && i.afterDraw(), this.dataAccessObject.drawn = !0, this;
  }
  /**
   * @param {number} col The visual column index.
   */
  markIfOversizedColumnHeader(e) {
    const n = this.columnFilter.renderedToSource(e);
    let i = this.wtSettings.getSetting("columnHeaders").length;
    const r = this.wtSettings.getSetting("defaultRowHeight");
    let o, s, a;
    const l = this.wtSettings.getSetting("columnHeaderHeight") || [];
    for (; i; )
      i -= 1, o = this.getColumnHeaderHeight(i), s = this.getColumnHeader(n, i), s && (a = Ya(s), (!o && r < a || o < a) && (this.dataAccessObject.wtViewport.oversizedColumnHeaders[i] = a), Array.isArray(l) ? l[i] !== null && l[i] !== void 0 && (this.dataAccessObject.wtViewport.oversizedColumnHeaders[i] = l[i]) : isNaN(l) || (this.dataAccessObject.wtViewport.oversizedColumnHeaders[i] = l), this.dataAccessObject.wtViewport.oversizedColumnHeaders[i] < (l[i] || l) && (this.dataAccessObject.wtViewport.oversizedColumnHeaders[i] = l[i] || l));
  }
  /**
   *
   */
  adjustColumnHeaderHeights() {
    const {
      wtSettings: e
    } = this, n = this.THEAD.childNodes, i = this.dataAccessObject.wtViewport.oversizedColumnHeaders, r = e.getSetting("columnHeaders");
    for (let o = 0, s = r.length; o < s; o++)
      if (i[o]) {
        if (!n[o] || n[o].childNodes.length === 0)
          return;
        n[o].childNodes[0].style.height = `${i[o]}px`;
      }
  }
  /**
   * Resets cache of row heights. The cache should be cached for each render cycle in a case
   * when new cell values have content which increases/decreases cell height.
   */
  resetOversizedRows() {
    const {
      wtSettings: e
    } = this, {
      wtViewport: n
    } = this.dataAccessObject;
    if (!(!this.isMaster && !this.is(no)) && !e.getSetting("externalRowCalculator")) {
      const i = this.getRenderedRowsCount();
      for (let r = 0; r < i; r++) {
        const o = this.rowFilter.renderedToSource(r);
        n.oversizedRows && n.oversizedRows[o] && (n.oversizedRows[o] = void 0);
      }
    }
  }
  /**
   * Get cell element at coords.
   * Negative coords.row or coords.col are used to retrieve header cells. If there are multiple header levels, the
   * negative value corresponds to the distance from the working area. For example, when there are 3 levels of column
   * headers, coords.col=-1 corresponds to the most inner header element, while coords.col=-3 corresponds to the
   * outmost header element.
   *
   * In case an element for the coords is not rendered, the method returns an error code.
   * To produce the error code, the input parameters are validated in the order in which they
   * are given. Thus, if both the row and the column coords are out of the rendered bounds,
   * the method returns the error code for the row.
   *
   * @param {CellCoords} coords The cell coordinates.
   * @returns {HTMLElement|number} HTMLElement on success or Number one of the exit codes on error:
   *  -1 row before viewport
   *  -2 row after viewport
   *  -3 column before viewport
   *  -4 column after viewport.
   */
  getCell(e) {
    let n = e.row, i = e.col;
    const r = this.wtSettings.getSetting("onModifyGetCellCoords", n, i);
    if (r && Array.isArray(r) && ([n, i] = r), this.isRowBeforeRenderedRows(n))
      return -1;
    if (this.isRowAfterRenderedRows(n))
      return -2;
    if (this.isColumnBeforeRenderedColumns(i))
      return -3;
    if (this.isColumnAfterRenderedColumns(i))
      return -4;
    const o = this.getRow(n);
    if (!o && n >= 0)
      throw new Error("TR was expected to be rendered but is not");
    const s = o.childNodes[this.columnFilter.sourceColumnToVisibleRowHeadedColumn(i)];
    if (!s && i >= 0)
      throw new Error("TD or TH was expected to be rendered but is not");
    return s;
  }
  /**
   * Get the DOM element of the row with the provided index.
   *
   * @param {number} rowIndex Row index.
   * @returns {HTMLTableRowElement|boolean} Return the row's DOM element or `false` if the row with the provided
   * index doesn't exist.
   */
  getRow(e) {
    let n = null, i = null;
    if (e < 0) {
      var r;
      n = (r = this.rowFilter) === null || r === void 0 ? void 0 : r.sourceRowToVisibleColHeadedRow(e), i = this.THEAD;
    } else {
      var o;
      n = (o = this.rowFilter) === null || o === void 0 ? void 0 : o.sourceToRendered(e), i = this.TBODY;
    }
    return n !== void 0 && i !== void 0 ? i.childNodes.length < n + 1 ? !1 : i.childNodes[n] : !1;
  }
  /**
   * GetColumnHeader.
   *
   * @param {number} col Column index.
   * @param {number} [level=0] Header level (0 = most distant to the table).
   * @returns {object} HTMLElement on success or undefined on error.
   */
  getColumnHeader(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
    const i = this.THEAD.childNodes[n];
    return i == null ? void 0 : i.childNodes[this.columnFilter.sourceColumnToVisibleRowHeadedColumn(e)];
  }
  /**
   * Gets all columns headers (TH elements) from the table.
   *
   * @param {number} column A source column index.
   * @returns {HTMLTableCellElement[]}
   */
  getColumnHeaders(e) {
    const n = [], i = this.columnFilter.sourceColumnToVisibleRowHeadedColumn(e);
    return this.THEAD.childNodes.forEach((r) => {
      const o = r.childNodes[i];
      o && n.push(o);
    }), n;
  }
  /**
   * GetRowHeader.
   *
   * @param {number} row Row index.
   * @param {number} [level=0] Header level (0 = most distant to the table).
   * @returns {HTMLElement} HTMLElement on success or Number one of the exit codes on error: `null table doesn't have
   *   row headers`.
   */
  getRowHeader(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
    const i = this.wtSettings.getSetting("rowHeaders").length;
    if (n >= i)
      return;
    const r = this.rowFilter.sourceToRendered(e), o = r < 0 ? this.rowFilter.sourceRowToVisibleColHeadedRow(e) : r, a = (r < 0 ? this.THEAD : this.TBODY).childNodes[o];
    return a == null ? void 0 : a.childNodes[n];
  }
  /**
   * Gets all rows headers (TH elements) from the table.
   *
   * @param {number} row A source row index.
   * @returns {HTMLTableCellElement[]}
   */
  getRowHeaders(e) {
    const n = [], i = this.wtSettings.getSetting("rowHeaders").length;
    for (let r = 0; r < i; r++) {
      const o = this.TBODY.childNodes[this.rowFilter.sourceToRendered(e)], s = o == null ? void 0 : o.childNodes[r];
      s && n.push(s);
    }
    return n;
  }
  /**
   * Returns cell coords object for a given TD (or a child element of a TD element).
   *
   * @param {HTMLTableCellElement} TD A cell DOM element (or a child of one).
   * @returns {CellCoords|null} The coordinates of the provided TD element (or the closest TD element) or null, if the
   *   provided element is not applicable.
   */
  getCoords(e) {
    let n = e;
    if (n.nodeName !== "TD" && n.nodeName !== "TH" && (n = js(n, ["TD", "TH"])), n === null)
      return null;
    const i = n.parentNode, r = i.parentNode;
    let o = BP(i), s = n.cellIndex;
    return ys(rl, n, this.wtRootElement) || ys(nl, n, this.wtRootElement) ? r.nodeName === "THEAD" && (o -= r.childNodes.length) : ys($s, n, this.wtRootElement) || ys(no, n, this.wtRootElement) ? o = this.wtSettings.getSetting("totalRows") - r.childNodes.length + o : r === this.THEAD ? o = this.rowFilter.visibleColHeadedRowToSourceRow(o) : o = this.rowFilter.renderedToSource(o), ys(rl, n, this.wtRootElement) || ys(il, n, this.wtRootElement) || ys($s, n, this.wtRootElement) ? s = this.columnFilter.offsettedTH(s) : s = this.columnFilter.visibleRowHeadedColumnToSourceColumn(s), this.wot.createCellCoords(o, s);
  }
  /**
   * Check if any of the rendered rows is higher than expected, and if so, cache them.
   */
  markOversizedRows() {
    if (this.wtSettings.getSetting("externalRowCalculator"))
      return;
    let e = this.TBODY.childNodes.length;
    const n = e * this.wtSettings.getSetting("defaultRowHeight"), i = Ya(this.TBODY) - 1;
    let r, o, s, a, l;
    if (!(n === i && !this.wtSettings.getSetting("fixedRowsBottom")))
      for (; e; )
        e -= 1, s = this.rowFilter.renderedToSource(e), r = this.getRowHeight(s), a = this.getTrForRow(s), l = a.querySelector("th"), l ? o = Ya(l) : o = Ya(a) - 1, (!r && this.wtSettings.getSetting("defaultRowHeight") < o || r < o) && (o += 1, this.dataAccessObject.wtViewport.oversizedRows[s] = o);
  }
  /**
   * @param {number} row The visual row index.
   * @returns {HTMLTableElement}
   */
  getTrForRow(e) {
    return this.TBODY.childNodes[this.rowFilter.sourceToRendered(e)];
  }
  /**
   * Checks if the column index (negative value from -1 to N) is rendered.
   *
   * @param {number} column The column index (negative value from -1 to N).
   * @returns {boolean}
   */
  isColumnHeaderRendered(e) {
    if (e >= 0)
      return !1;
    const i = this.wtSettings.getSetting("rowHeaders").length;
    return Math.abs(e) <= i;
  }
  /**
   * Checks if the row index (negative value from -1 to N) is rendered.
   *
   * @param {number} row The row index (negative value from -1 to N).
   * @returns {boolean}
   */
  isRowHeaderRendered(e) {
    if (e >= 0)
      return !1;
    const i = this.wtSettings.getSetting("columnHeaders").length;
    return Math.abs(e) <= i;
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * Check if the given row index is lower than the index of the first row that
   * is currently rendered and return TRUE in that case, or FALSE otherwise.
   *
   * Negative row index is used to check the columns' headers.
   *
   *  Headers
   *           +--------------+                                     │
   *       -3  │    │    │    │                                     │
   *           +--------------+                                     │
   *       -2  │    │    │    │                                     │ TRUE
   *           +--------------+                                     │
   *       -1  │    │    │    │                                     │
   *  Cells  +==================+                                   │
   *        0  ┇    ┇    ┇    ┇ <--- For fixedRowsTop: 1            │
   *           +--------------+      the master overlay do       ---+ first rendered row (index 1)
   *        1  │ A2 │ B2 │ C2 │      not render the first row.      │
   *           +--------------+                                     │ FALSE
   *        2  │ A3 │ B3 │ C3 │                                     │
   *           +--------------+                                  ---+ last rendered row
   *                                                                │
   *                                                                │ FALSE
   *
   * @param {number} row The visual row index.
   * @memberof Table#
   * @function isRowBeforeRenderedRows
   * @returns {boolean}
   */
  /* eslint-enable jsdoc/require-description-complete-sentence */
  isRowBeforeRenderedRows(e) {
    const n = this.getFirstRenderedRow();
    return e < 0 && n <= 0 ? !this.isRowHeaderRendered(e) : e < n;
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * Check if the given column index is greater than the index of the last column that
   * is currently rendered and return TRUE in that case, or FALSE otherwise.
   *
   * The negative row index is used to check the columns' headers. However,
   * keep in mind that for negative indexes, the method always returns FALSE as
   * it is not possible to render headers partially. The "after" index can not be
   * lower than -1.
   *
   *  Headers
   *           +--------------+                                     │
   *       -3  │    │    │    │                                     │
   *           +--------------+                                     │
   *       -2  │    │    │    │                                     │ FALSE
   *           +--------------+                                     │
   *       -1  │    │    │    │                                     │
   *  Cells  +==================+                                   │
   *        0  ┇    ┇    ┇    ┇ <--- For fixedRowsTop: 1            │
   *           +--------------+      the master overlay do       ---+ first rendered row (index 1)
   *        1  │ A2 │ B2 │ C2 │      not render the first rows      │
   *           +--------------+                                     │ FALSE
   *        2  │ A3 │ B3 │ C3 │                                     │
   *           +--------------+                                  ---+ last rendered row
   *                                                                │
   *                                                                │ TRUE
   *
   * @param {number} row The visual row index.
   * @memberof Table#
   * @function isRowAfterRenderedRows
   * @returns {boolean}
   */
  /* eslint-enable jsdoc/require-description-complete-sentence */
  isRowAfterRenderedRows(e) {
    return e > this.getLastRenderedRow();
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * Check if the given column index is lower than the index of the first column that
   * is currently rendered and return TRUE in that case, or FALSE otherwise.
   *
   * Negative column index is used to check the rows' headers.
   *
   *                            For fixedColumnsStart: 1 the master overlay
   *                            do not render this first columns.
   *  Headers    -3   -2   -1    |
   *           +----+----+----║┄ ┄ +------+------+
   *           │    │    │    ║    │  B1  │  C1  │
   *           +--------------║┄ ┄ --------------│
   *           │    │    │    ║    │  B2  │  C2  │
   *           +--------------║┄ ┄ --------------│
   *           │    │    │    ║    │  B3  │  C3  │
   *           +----+----+----║┄ ┄ +------+------+
   *                               ╷             ╷
   *      -------------------------+-------------+---------------->
   *          TRUE             first    FALSE   last         FALSE
   *                           rendered         rendered
   *                           column           column
   *
   * @param {number} column The visual column index.
   * @memberof Table#
   * @function isColumnBeforeRenderedColumns
   * @returns {boolean}
   */
  /* eslint-enable jsdoc/require-description-complete-sentence */
  isColumnBeforeRenderedColumns(e) {
    const n = this.getFirstRenderedColumn();
    return e < 0 && n <= 0 ? !this.isColumnHeaderRendered(e) : e < n;
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * Check if the given column index is greater than the index of the last column that
   * is currently rendered and return TRUE in that case, or FALSE otherwise.
   *
   * The negative column index is used to check the rows' headers. However,
   * keep in mind that for negative indexes, the method always returns FALSE as
   * it is not possible to render headers partially. The "after" index can not be
   * lower than -1.
   *
   *                            For fixedColumnsStart: 1 the master overlay
   *                            do not render this first columns.
   *  Headers    -3   -2   -1    |
   *           +----+----+----║┄ ┄ +------+------+
   *           │    │    │    ║    │  B1  │  C1  │
   *           +--------------║┄ ┄ --------------│
   *           │    │    │    ║    │  B2  │  C2  │
   *           +--------------║┄ ┄ --------------│
   *           │    │    │    ║    │  B3  │  C3  │
   *           +----+----+----║┄ ┄ +------+------+
   *                               ╷             ╷
   *      -------------------------+-------------+---------------->
   *          FALSE             first    FALSE   last         TRUE
   *                           rendered         rendered
   *                           column           column
   *
   * @param {number} column The visual column index.
   * @memberof Table#
   * @function isColumnAfterRenderedColumns
   * @returns {boolean}
   */
  /* eslint-enable jsdoc/require-description-complete-sentence */
  isColumnAfterRenderedColumns(e) {
    return this.columnFilter && e > this.getLastRenderedColumn();
  }
  isColumnAfterViewport(e) {
    return this.columnFilter && e > this.getLastVisibleColumn();
  }
  isRowAfterViewport(e) {
    return this.rowFilter && e > this.getLastVisibleRow();
  }
  isColumnBeforeViewport(e) {
    return this.columnFilter && this.columnFilter.sourceToRendered(e) < 0 && e >= 0;
  }
  isLastRowFullyVisible() {
    return this.getLastVisibleRow() === this.getLastRenderedRow();
  }
  isLastColumnFullyVisible() {
    return this.getLastVisibleColumn() === this.getLastRenderedColumn();
  }
  allRowsInViewport() {
    return this.wtSettings.getSetting("totalRows") === this.getVisibleRowsCount();
  }
  allColumnsInViewport() {
    return this.wtSettings.getSetting("totalColumns") === this.getVisibleColumnsCount();
  }
  /**
   * Checks if any of the row's cells content exceeds its initial height, and if so, returns the oversized height.
   *
   * @param {number} sourceRow The physical row index.
   * @returns {number}
   */
  getRowHeight(e) {
    return this.rowUtils.getHeight(e);
  }
  /**
   * @param {number} level The column level.
   * @returns {number}
   */
  getColumnHeaderHeight(e) {
    return this.columnUtils.getHeaderHeight(e);
  }
  /**
   * @param {number} sourceColumn The physical column index.
   * @returns {number}
   */
  getColumnWidth(e) {
    return this.columnUtils.getWidth(e);
  }
  /**
   * @param {number} sourceColumn The physical column index.
   * @returns {number}
   */
  getStretchedColumnWidth(e) {
    return this.columnUtils.getStretchedColumnWidth(e);
  }
  /**
   * Checks if the table has defined size. It returns `true` when the table has width and height
   * set bigger than `0px`.
   *
   * @returns {boolean}
   */
  hasDefinedSize() {
    return this.hasTableHeight && this.hasTableWidth;
  }
  /**
   * Gets table's width. The returned width is the width of the rendered cells that fit in the
   * current viewport. The value may change depends on the viewport position (scroll position).
   *
   * @returns {number}
   */
  getWidth() {
    return It(this.TABLE);
  }
  /**
   * Gets table's height. The returned height is the height of the rendered cells that fit in the
   * current viewport. The value may change depends on the viewport position (scroll position).
   *
   * @returns {number}
   */
  getHeight() {
    return Zt(this.TABLE);
  }
  /**
   * Gets table's total width. The returned width is the width of all rendered cells (including headers)
   * that can be displayed in the table.
   *
   * @returns {number}
   */
  getTotalWidth() {
    const e = It(this.hider);
    return e !== 0 ? e : this.getWidth();
  }
  /**
   * Gets table's total height. The returned height is the height of all rendered cells (including headers)
   * that can be displayed in the table.
   *
   * @returns {number}
   */
  getTotalHeight() {
    const e = Zt(this.hider);
    return e !== 0 ? e : this.getHeight();
  }
  /**
   * Checks if the table is visible. It returns `true` when the holder element (or its parents)
   * has CSS 'display' property different than 'none'.
   *
   * @returns {boolean}
   */
  isVisible() {
    return el(this.TABLE);
  }
  /**
   * Modify row header widths provided by user in class contructor.
   *
   * @private
   * @param {Function} rowHeaderWidthFactory The function which can provide default width values for rows..
   * @returns {number}
   */
  _modifyRowHeaderWidth(e) {
    let n = Et(e) ? e() : null;
    return Array.isArray(n) ? (n = [...n], n[n.length - 1] = this._correctRowHeaderWidth(n[n.length - 1])) : n = this._correctRowHeaderWidth(n), n;
  }
  /**
   * Correct row header width if necessary.
   *
   * @private
   * @param {number} width The width to process.
   * @returns {number}
   */
  _correctRowHeaderWidth(e) {
    let n = e;
    return typeof e != "number" && (n = this.wtSettings.getSetting("defaultColumnWidth")), this.correctHeaderWidth && (n += 1), n;
  }
}
const Bu = n3, i3 = "stickyRowsBottom", Fx = {
  /**
   * Get the source index of the first rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getFirstRenderedRow() {
    const t = this.wtSettings.getSetting("totalRows"), e = this.wtSettings.getSetting("fixedRowsBottom"), n = t - e;
    return t === 0 || e === 0 ? -1 : n < 0 ? 0 : n;
  },
  /**
   * Get the source index of the first row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getFirstVisibleRow() {
    return this.getFirstRenderedRow();
  },
  /**
   * Get the source index of the first row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getFirstPartiallyVisibleRow() {
    return this.getFirstRenderedRow();
  },
  /**
   * Get the source index of the last rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getLastRenderedRow() {
    return this.wtSettings.getSetting("totalRows") - 1;
  },
  /**
   * Get the source index of the last row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getLastVisibleRow() {
    return this.getLastRenderedRow();
  },
  /**
   * Get the source index of the last row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getLastPartiallyVisibleRow() {
    return this.getLastRenderedRow();
  },
  /**
   * Get the number of rendered rows.
   *
   * @returns {number}
   * @this Table
   */
  getRenderedRowsCount() {
    const t = this.wtSettings.getSetting("totalRows");
    return Math.min(this.wtSettings.getSetting("fixedRowsBottom"), t);
  },
  /**
   * Get the number of fully visible rows in the viewport.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getVisibleRowsCount() {
    return this.getRenderedRowsCount();
  },
  /**
   * Get the number of rendered column headers.
   *
   * @returns {number}
   * @this Table
   */
  getColumnHeadersCount() {
    return 0;
  }
};
wo(Fx, "MIXIN_NAME", i3, {
  writable: !1,
  enumerable: !1
});
const Vx = Fx, r3 = "stickyColumnsStart", Wx = {
  /**
   * Get the source index of the first rendered column. If no columns are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getFirstRenderedColumn() {
    return this.wtSettings.getSetting("totalColumns") === 0 ? -1 : 0;
  },
  /**
   * Get the source index of the first column fully visible in the viewport. If no columns are fully visible, returns an error code: -1.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getFirstVisibleColumn() {
    return this.getFirstRenderedColumn();
  },
  /**
   * Get the source index of the first column partially visible in the viewport. If no columns are partially visible, returns an error code: -1.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getFirstPartiallyVisibleColumn() {
    return this.getFirstRenderedColumn();
  },
  /**
   * Get the source index of the last rendered column. If no columns are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getLastRenderedColumn() {
    return this.getRenderedColumnsCount() - 1;
  },
  /**
   * Get the source index of the last column fully visible in the viewport. If no columns are fully visible, returns an error code: -1.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getLastVisibleColumn() {
    return this.getLastRenderedColumn();
  },
  /**
   * Get the source index of the last column partially visible in the viewport. If no columns are partially visible, returns an error code: -1.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getLastPartiallyVisibleColumn() {
    return this.getLastRenderedColumn();
  },
  /**
   * Get the number of rendered columns.
   *
   * @returns {number}
   * @this Table
   */
  getRenderedColumnsCount() {
    const t = this.wtSettings.getSetting("totalColumns");
    return Math.min(this.wtSettings.getSetting("fixedColumnsStart"), t);
  },
  /**
   * Get the number of fully visible columns in the viewport.
   * Assumes that all rendered columns are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getVisibleColumnsCount() {
    return this.getRenderedColumnsCount();
  },
  /**
   * Get the number of rendered row headers.
   *
   * @returns {number}
   * @this Table
   */
  getRowHeadersCount() {
    return this.dataAccessObject.rowHeaders.length;
  }
};
wo(Wx, "MIXIN_NAME", r3, {
  writable: !1,
  enumerable: !1
});
const Tb = Wx;
class Mb extends Bu {
  /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */
  constructor(e, n, i, r) {
    super(e, n, i, r, $s);
  }
}
pt(Mb, Vx);
pt(Mb, Tb);
const o3 = Mb, nl = "top", no = "bottom", il = "inline_start", rl = "top_inline_start_corner", $s = "bottom_inline_start_corner", $x = [nl, no, il, rl, $s], s3 = /* @__PURE__ */ new Map([[nl, `ht_clone_${nl}`], [no, `ht_clone_${no}`], [il, `ht_clone_${il} ht_clone_left`], [rl, `ht_clone_${rl} ht_clone_top_left_corner`], [$s, `ht_clone_${$s} ht_clone_bottom_left_corner`]]);
function a3(t, e) {
  l3(t, e), e.add(t);
}
function l3(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function u3(t, e, n) {
  return (e = c3(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function c3(t) {
  var e = h3(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function h3(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function pf(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var cc = /* @__PURE__ */ new WeakSet();
class d3 {
  /**
   * @param {ScrollDao} dataAccessObject Tha data access object.
   */
  constructor(e) {
    a3(this, cc), u3(this, "dataAccessObject", void 0), this.dataAccessObject = e;
  }
  /**
   * Scrolls viewport to a cell.
   *
   * @param {CellCoords} coords The cell coordinates.
   * @param {boolean} [snapToTop] If `true`, viewport is scrolled to show the cell on the top of the table.
   * @param {boolean} [snapToRight] If `true`, viewport is scrolled to show the cell on the right of the table.
   * @param {boolean} [snapToBottom] If `true`, viewport is scrolled to show the cell on the bottom of the table.
   * @param {boolean} [snapToLeft] If `true`, viewport is scrolled to show the cell on the left of the table.
   * @returns {boolean}
   */
  scrollViewport(e, n, i, r, o) {
    const s = this.scrollViewportHorizontally(e.col, i, o), a = this.scrollViewportVertically(e.row, n, r);
    return s || a;
  }
  /**
   * Scrolls viewport to a column.
   *
   * @param {number} column Visual column index.
   * @param {boolean} [snapToRight] If `true`, viewport is scrolled to show the cell on the right of the table.
   * @param {boolean} [snapToLeft] If `true`, viewport is scrolled to show the cell on the left of the table.
   * @returns {boolean}
   */
  scrollViewportHorizontally(e, n, i) {
    const {
      drawn: r,
      totalColumns: o
    } = this.dataAccessObject;
    if (!r || !Number.isInteger(e) || e < 0 || e > o)
      return !1;
    const s = n === void 0 && i === void 0, {
      fixedColumnsStart: a,
      inlineStartOverlay: l
    } = this.dataAccessObject;
    if (s && e < a || (e = this.dataAccessObject.wtSettings.getSetting("onBeforeViewportScrollHorizontally", e), !Number.isInteger(e) || e < 0 || e > o))
      return !1;
    const u = this.getFirstVisibleColumn(), c = this.getLastVisibleColumn();
    let h = !1;
    return (s && (e < u || e > c) || !s) && (h = l.scrollTo(e, s ? e >= this.getLastPartiallyVisibleColumn() : n)), h;
  }
  /**
   * Scrolls viewport to a row.
   *
   * @param {number} row Visual row index.
   * @param {boolean} [snapToTop] If `true`, viewport is scrolled to show the cell on the top of the table.
   * @param {boolean} [snapToBottom] If `true`, viewport is scrolled to show the cell on the bottom of the table.
   * @returns {boolean}
   */
  scrollViewportVertically(e, n, i) {
    const {
      drawn: r,
      totalRows: o
    } = this.dataAccessObject;
    if (!r || !Number.isInteger(e) || e < 0 || e > o)
      return !1;
    const s = n === void 0 && i === void 0, {
      fixedRowsBottom: a,
      fixedRowsTop: l,
      topOverlay: u
    } = this.dataAccessObject;
    if (s && (e < l || e > o - a - 1) || (e = this.dataAccessObject.wtSettings.getSetting("onBeforeViewportScrollVertically", e), !Number.isInteger(e) || e < 0 || e > o))
      return !1;
    const c = this.getFirstVisibleRow(), h = this.getLastVisibleRow();
    let d = !1;
    return (s && (e < c || e > h) || !s) && (d = u.scrollTo(e, s ? e >= this.getLastPartiallyVisibleRow() : i)), d;
  }
  /**
   * Get first visible row based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */
  getFirstVisibleRow() {
    return this.dataAccessObject.wtTable.getFirstVisibleRow();
  }
  /**
   * Get last visible row based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */
  getLastVisibleRow() {
    return pf(cc, this, ZE).call(this, this.dataAccessObject.wtTable.getLastVisibleRow());
  }
  /**
   * Get first partially visible row based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */
  getFirstPartiallyVisibleRow() {
    return this.dataAccessObject.wtTable.getFirstPartiallyVisibleRow();
  }
  /**
   * Get last visible row based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */
  getLastPartiallyVisibleRow() {
    return pf(cc, this, ZE).call(this, this.dataAccessObject.wtTable.getLastPartiallyVisibleRow());
  }
  /**
   * Get first visible column based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */
  getFirstVisibleColumn() {
    return this.dataAccessObject.wtTable.getFirstVisibleColumn();
  }
  /**
   * Get last visible column based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */
  getLastVisibleColumn() {
    return pf(cc, this, qE).call(this, this.dataAccessObject.wtTable.getLastVisibleColumn());
  }
  /**
   * Get first partially visible column based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */
  getFirstPartiallyVisibleColumn() {
    return this.dataAccessObject.wtTable.getFirstPartiallyVisibleColumn();
  }
  /**
   * Get last partially visible column based on virtual dom and how table is visible in browser window viewport.
   *
   * @returns {number}
   */
  getLastPartiallyVisibleColumn() {
    return pf(cc, this, qE).call(this, this.dataAccessObject.wtTable.getLastPartiallyVisibleColumn());
  }
}
function qE(t) {
  const {
    wtSettings: e,
    inlineStartOverlay: n,
    wtTable: i,
    wtViewport: r,
    totalColumns: o,
    rootWindow: s
  } = this.dataAccessObject;
  if (n.mainTableScrollableElement === s) {
    const a = e.getSetting("rtlMode");
    let l = null;
    if (a) {
      const c = i.TABLE.getBoundingClientRect(), d = this.dataAccessObject.rootWindow.document.documentElement.offsetWidth;
      l = Math.abs(c.right - d);
    } else
      l = gn(i.wtRootElement).left;
    const u = Math.abs(nb(s, s));
    if (l > u) {
      const c = ib(s);
      let h = r.getRowHeaderWidth();
      for (let d = 1; d <= o; d++)
        if (h += n.sumCellSizes(d - 1, d), l + h - u >= c) {
          t = d - 2;
          break;
        }
    }
  }
  return t;
}
function ZE(t) {
  const {
    topOverlay: e,
    wtTable: n,
    wtViewport: i,
    totalRows: r,
    rootWindow: o
  } = this.dataAccessObject;
  if (e.mainTableScrollableElement === o) {
    const s = gn(n.wtRootElement), a = Ep(o, o);
    if (s.top > a) {
      const l = Ya(o);
      let u = i.getColumnHeaderHeight();
      for (let c = 1; c <= r; c++)
        if (u += e.sumCellSizes(c - 1, c), s.top + u - a >= l) {
          t = c - 2;
          break;
        }
    }
  }
  return t;
}
const f3 = d3;
function ir(t, e, n) {
  return (e = g3(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function g3(t) {
  var e = m3(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function m3(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Ib {
  get eventManager() {
    return new Qi(this);
  }
  /**
   * @param {HTMLTableElement} table Main table.
   * @param {Settings} settings The Walkontable settings.
   */
  constructor(e, n) {
    ir(this, "wtTable", void 0), ir(this, "wtScroll", void 0), ir(this, "wtViewport", void 0), ir(this, "wtOverlays", void 0), ir(this, "selectionManager", void 0), ir(this, "wtEvent", void 0), ir(this, "guid", `wt_${QS()}`), ir(this, "drawInterrupted", !1), ir(this, "drawn", !1), ir(this, "domBindings", void 0), ir(this, "wtSettings", void 0), this.domBindings = {
      rootTable: e,
      rootDocument: e.ownerDocument,
      rootWindow: e.ownerDocument.defaultView
    }, this.wtSettings = n, this.wtScroll = new f3(this.createScrollDao());
  }
  findOriginalHeaders() {
    const e = [];
    if (this.wtTable.THEAD.childNodes.length && this.wtTable.THEAD.childNodes[0].childNodes.length) {
      for (let n = 0, i = this.wtTable.THEAD.childNodes[0].childNodes.length; n < i; n++)
        e.push(this.wtTable.THEAD.childNodes[0].childNodes[n].innerHTML);
      this.wtSettings.getSetting("columnHeaders").length || this.wtSettings.update("columnHeaders", [function(n, i) {
        Gs(i, e[n]);
      }]);
    }
  }
  /**
   * Creates and returns the CellCoords object.
   *
   * @param {*} row The row index.
   * @param {*} column The column index.
   * @returns {CellCoords}
   */
  createCellCoords(e, n) {
    return new Sm(e, n, this.wtSettings.getSetting("rtlMode"));
  }
  /**
   * Creates and returns the CellRange object.
   *
   * @param {CellCoords} highlight The highlight coordinates.
   * @param {CellCoords} from The from coordinates.
   * @param {CellCoords} to The to coordinates.
   * @returns {CellRange}
   */
  createCellRange(e, n, i) {
    return new Eb(e, n, i, this.wtSettings.getSetting("rtlMode"));
  }
  /**
   * Force rerender of Walkontable.
   *
   * @param {boolean} [fastDraw=false] When `true`, try to refresh only the positions of borders without rerendering
   *                                   the data. It will only work if Table.draw() does not force
   *                                   rendering anyway.
   * @returns {Walkontable}
   */
  draw() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    return this.drawInterrupted = !1, this.wtTable.isVisible() ? this.wtTable.draw(e) : this.drawInterrupted = !0, this;
  }
  /**
   * Returns the TD at coords. If topmost is set to true, returns TD from the topmost overlay layer,
   * if not set or set to false, returns TD from the master table.
   *
   * @param {CellCoords} coords The cell coordinates.
   * @param {boolean} [topmost=false] If set to `true`, it returns the TD element from the topmost overlay. For example,
   *                                  if the wanted cell is in the range of fixed rows, it will return a TD element
   *                                  from the top overlay.
   * @returns {HTMLElement}
   */
  getCell(e) {
    if (!(arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1))
      return this.wtTable.getCell(e);
    const i = this.wtSettings.getSetting("totalRows"), r = this.wtSettings.getSetting("fixedRowsTop"), o = this.wtSettings.getSetting("fixedRowsBottom"), s = this.wtSettings.getSetting("fixedColumnsStart");
    if (e.row < r && e.col < s)
      return this.wtOverlays.topInlineStartCornerOverlay.clone.wtTable.getCell(e);
    if (e.row < r)
      return this.wtOverlays.topOverlay.clone.wtTable.getCell(e);
    if (e.col < s && e.row >= i - o) {
      if (this.wtOverlays.bottomInlineStartCornerOverlay && this.wtOverlays.bottomInlineStartCornerOverlay.clone)
        return this.wtOverlays.bottomInlineStartCornerOverlay.clone.wtTable.getCell(e);
    } else {
      if (e.col < s)
        return this.wtOverlays.inlineStartOverlay.clone.wtTable.getCell(e);
      if (e.row < i && e.row >= i - o && this.wtOverlays.bottomOverlay && this.wtOverlays.bottomOverlay.clone)
        return this.wtOverlays.bottomOverlay.clone.wtTable.getCell(e);
    }
    return this.wtTable.getCell(e);
  }
  /**
   * Scrolls the viewport to a cell (rerenders if needed).
   *
   * @param {CellCoords} coords The cell coordinates to scroll to.
   * @param {boolean} [snapToTop] If `true`, viewport is scrolled to show the cell on the top of the table.
   * @param {boolean} [snapToRight] If `true`, viewport is scrolled to show the cell on the right of the table.
   * @param {boolean} [snapToBottom] If `true`, viewport is scrolled to show the cell on the bottom of the table.
   * @param {boolean} [snapToLeft] If `true`, viewport is scrolled to show the cell on the left of the table.
   * @returns {boolean}
   */
  scrollViewport(e, n, i, r, o) {
    return e.col < 0 || e.row < 0 ? !1 : this.wtScroll.scrollViewport(e, n, i, r, o);
  }
  /**
   * Scrolls the viewport to a column (rerenders if needed).
   *
   * @param {number} column Visual column index.
   * @param {boolean} [snapToRight] If `true`, viewport is scrolled to show the cell on the right of the table.
   * @param {boolean} [snapToLeft] If `true`, viewport is scrolled to show the cell on the left of the table.
   * @returns {boolean}
   */
  scrollViewportHorizontally(e, n, i) {
    return e < 0 ? !1 : this.wtScroll.scrollViewportHorizontally(e, n, i);
  }
  /**
   * Scrolls the viewport to a row (rerenders if needed).
   *
   * @param {number} row Visual row index.
   * @param {boolean} [snapToTop] If `true`, viewport is scrolled to show the cell on the top of the table.
   * @param {boolean} [snapToBottom] If `true`, viewport is scrolled to show the cell on the bottom of the table.
   * @returns {boolean}
   */
  scrollViewportVertically(e, n, i) {
    return e < 0 ? !1 : this.wtScroll.scrollViewportVertically(e, n, i);
  }
  /**
   * @returns {Array}
   */
  getViewport() {
    return [this.wtTable.getFirstVisibleRow(), this.wtTable.getFirstVisibleColumn(), this.wtTable.getLastVisibleRow(), this.wtTable.getLastVisibleColumn()];
  }
  /**
   * Destroy instance.
   */
  destroy() {
    this.wtOverlays.destroy(), this.wtEvent.destroy();
  }
  /**
   * Create data access object for scroll.
   *
   * @protected
   * @returns {ScrollDao}
   */
  createScrollDao() {
    const e = this;
    return {
      get drawn() {
        return e.drawn;
      },
      get topOverlay() {
        return e.wtOverlays.topOverlay;
      },
      get inlineStartOverlay() {
        return e.wtOverlays.inlineStartOverlay;
      },
      get wtTable() {
        return e.wtTable;
      },
      get wtViewport() {
        return e.wtViewport;
      },
      get wtSettings() {
        return e.wtSettings;
      },
      get rootWindow() {
        return e.domBindings.rootWindow;
      },
      // TODO refactoring, consider about using injecting wtSettings into scroll (it'll enables remove dao layer)
      get totalRows() {
        return e.wtSettings.getSetting("totalRows");
      },
      get totalColumns() {
        return e.wtSettings.getSetting("totalColumns");
      },
      get fixedRowsTop() {
        return e.wtSettings.getSetting("fixedRowsTop");
      },
      get fixedRowsBottom() {
        return e.wtSettings.getSetting("fixedRowsBottom");
      },
      get fixedColumnsStart() {
        return e.wtSettings.getSetting("fixedColumnsStart");
      }
    };
  }
  // TODO refactoring: it will be much better to not use DAO objects. They are needed for now to provide
  // dynamically access to related objects
  /**
   * Create data access object for wtTable.
   *
   * @protected
   * @returns {TableDao}
   */
  getTableDao() {
    const e = this;
    return {
      get wot() {
        return e;
      },
      get parentTableOffset() {
        return e.cloneSource.wtTable.tableOffset;
      },
      get cloneSource() {
        return e.cloneSource;
      },
      get workspaceWidth() {
        return e.wtViewport.getWorkspaceWidth();
      },
      get wtViewport() {
        return e.wtViewport;
      },
      get wtOverlays() {
        return e.wtOverlays;
      },
      get selectionManager() {
        return e.selectionManager;
      },
      get drawn() {
        return e.drawn;
      },
      set drawn(n) {
        e.drawn = n;
      },
      get wtTable() {
        return e.wtTable;
      },
      get startColumnRendered() {
        return e.wtViewport.columnsRenderCalculator.startColumn;
      },
      get startColumnVisible() {
        return e.wtViewport.columnsVisibleCalculator.startColumn;
      },
      get startColumnPartiallyVisible() {
        return e.wtViewport.columnsPartiallyVisibleCalculator.startColumn;
      },
      get endColumnRendered() {
        return e.wtViewport.columnsRenderCalculator.endColumn;
      },
      get endColumnVisible() {
        return e.wtViewport.columnsVisibleCalculator.endColumn;
      },
      get endColumnPartiallyVisible() {
        return e.wtViewport.columnsPartiallyVisibleCalculator.endColumn;
      },
      get countColumnsRendered() {
        return e.wtViewport.columnsRenderCalculator.count;
      },
      get countColumnsVisible() {
        return e.wtViewport.columnsVisibleCalculator.count;
      },
      get startRowRendered() {
        return e.wtViewport.rowsRenderCalculator.startRow;
      },
      get startRowVisible() {
        return e.wtViewport.rowsVisibleCalculator.startRow;
      },
      get startRowPartiallyVisible() {
        return e.wtViewport.rowsPartiallyVisibleCalculator.startRow;
      },
      get endRowRendered() {
        return e.wtViewport.rowsRenderCalculator.endRow;
      },
      get endRowVisible() {
        return e.wtViewport.rowsVisibleCalculator.endRow;
      },
      get endRowPartiallyVisible() {
        return e.wtViewport.rowsPartiallyVisibleCalculator.endRow;
      },
      get countRowsRendered() {
        return e.wtViewport.rowsRenderCalculator.count;
      },
      get countRowsVisible() {
        return e.wtViewport.rowsVisibleCalculator.count;
      },
      get columnHeaders() {
        return e.wtSettings.getSetting("columnHeaders");
      },
      get rowHeaders() {
        return e.wtSettings.getSetting("rowHeaders");
      }
    };
  }
}
function JE(t, e, n) {
  return (e = p3(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function p3(t) {
  var e = w3(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function w3(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class A3 extends Ib {
  /**
   * @param {HTMLTableElement} table Main table.
   * @param {SettingsPure|Settings} settings The Walkontable settings.
   * @param {WalkontableCloneOptions} clone Clone data.
   */
  constructor(e, n, i) {
    super(e, n), JE(this, "cloneSource", void 0), JE(this, "cloneOverlay", void 0);
    const r = this.wtSettings.getSetting("facade", this);
    this.cloneSource = i.source, this.cloneOverlay = i.overlay, this.wtTable = this.cloneOverlay.createTable(this.getTableDao(), r, this.domBindings, this.wtSettings), this.wtViewport = i.viewport, this.selectionManager = i.selectionManager, this.wtEvent = new Dx(r, this.domBindings, this.wtSettings, this.eventManager, this.wtTable, this.selectionManager, i.event), this.findOriginalHeaders();
  }
}
function C3(t, e, n) {
  return (e = v3(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function v3(t) {
  var e = y3(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function y3(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Ed {
  /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {CLONE_TYPES_ENUM} type The overlay type name (clone name).
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   */
  constructor(e, n, i, r, o) {
    C3(this, "wtSettings", null), wo(this, "wot", e, {
      writable: !1
    }), this.domBindings = o, this.facadeGetter = n, this.wtSettings = r;
    const {
      TABLE: s,
      hider: a,
      spreader: l,
      holder: u,
      wtRootElement: c
    } = this.wot.wtTable;
    this.instance = this.wot, this.type = i, this.mainTableScrollableElement = null, this.TABLE = s, this.hider = a, this.spreader = l, this.holder = u, this.wtRootElement = c, this.trimmingContainer = tl(this.hider.parentNode.parentNode), this.needFullRender = this.shouldBeRendered(), this.clone = this.makeClone();
  }
  /**
   * Checks if the overlay rendering state has changed.
   *
   * @returns {boolean}
   */
  hasRenderingStateChanged() {
    return this.needFullRender !== this.shouldBeRendered();
  }
  /**
   * Updates internal state with an information about the need of full rendering of the overlay in the next draw cycles.
   *
   * If the state is changed to render the overlay, the `needFullRender` property is set to `true` which means that
   * the overlay will be fully rendered in the current draw cycle. If the state is changed to not render the overlay,
   * the `needFullRender` property is set to `false` which means that the overlay will be fully rendered in the
   * current draw cycle but it will not be rendered in the next draw cycles.
   *
   * @param {'before' | 'after'} drawPhase The phase of the rendering process.
   */
  updateStateOfRendering(e) {
    e === "before" && this.shouldBeRendered() ? this.needFullRender = !0 : e === "after" && !this.shouldBeRendered() && (this.needFullRender = !1);
  }
  /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */
  shouldBeRendered() {
    return !0;
  }
  /**
   * Update the trimming container.
   */
  updateTrimmingContainer() {
    this.trimmingContainer = tl(this.hider.parentNode.parentNode);
  }
  /**
   * Update the main scrollable element.
   */
  updateMainScrollableElement() {
    const {
      wtTable: e
    } = this.wot, {
      rootWindow: n
    } = this.domBindings;
    n.getComputedStyle(e.wtRootElement.parentNode).getPropertyValue("overflow") === "hidden" ? this.mainTableScrollableElement = this.wot.wtTable.holder : this.mainTableScrollableElement = Dh(e.TABLE);
  }
  /**
   * Calculates coordinates of the provided element, relative to the root Handsontable element.
   * NOTE: The element needs to be a child of the overlay in order for the method to work correctly.
   *
   * @param {HTMLElement} element The cell element to calculate the position for.
   * @param {number} rowIndex Visual row index.
   * @param {number} columnIndex Visual column index.
   * @returns {{top: number, start: number}|undefined}
   */
  getRelativeCellPosition(e, n, i) {
    if (this.clone.wtTable.holder.contains(e) === !1) {
      gt(`The provided element is not a child of the ${this.type} overlay`);
      return;
    }
    const r = this.mainTableScrollableElement === this.domBindings.rootWindow, o = i < this.wtSettings.getSetting("fixedColumnsStart"), s = n < this.wtSettings.getSetting("fixedRowsTop"), a = n >= this.wtSettings.getSetting("totalRows") - this.wtSettings.getSetting("fixedRowsBottom"), l = this.clone.wtTable.spreader, u = {
      start: this.getRelativeStartPosition(l),
      top: l.offsetTop
    }, c = {
      start: this.getRelativeStartPosition(e),
      top: e.offsetTop
    };
    let h = null;
    return r ? h = this.getRelativeCellPositionWithinWindow(s, o, c, u) : h = this.getRelativeCellPositionWithinHolder(s, a, o, c, u), h;
  }
  /**
   * Get inline start value depending of direction.
   *
   * @param {HTMLElement} el Element.
   * @returns {number}
   */
  getRelativeStartPosition(e) {
    return this.isRtl() ? e.offsetParent.offsetWidth - e.offsetLeft - e.offsetWidth : e.offsetLeft;
  }
  /**
   * Calculates coordinates of the provided element, relative to the root Handsontable element within a table with window
   * as a scrollable element.
   *
   * @private
   * @param {boolean} onFixedRowTop `true` if the coordinates point to a place within the top fixed rows.
   * @param {boolean} onFixedColumn `true` if the coordinates point to a place within the fixed columns.
   * @param {number} elementOffset Offset position of the cell element.
   * @param {number} spreaderOffset Offset position of the spreader element.
   * @returns {{top: number, left: number}}
   */
  getRelativeCellPositionWithinWindow(e, n, i, r) {
    const o = this.wot.wtTable.wtRootElement.getBoundingClientRect();
    let s = 0, a = 0;
    if (!n)
      s = r.start;
    else {
      let l = o.left;
      this.isRtl() && (l = this.domBindings.rootWindow.innerWidth - (o.left + o.width + pn())), s = l <= 0 ? -1 * l : 0;
    }
    return e ? a = this.clone.wtTable.TABLE.getBoundingClientRect().top - o.top : a = r.top, {
      start: i.start + s,
      top: i.top + a
    };
  }
  /**
   * Calculates coordinates of the provided element, relative to the root Handsontable element within a table with window
   * as a scrollable element.
   *
   * @private
   * @param {boolean} onFixedRowTop `true` if the coordinates point to a place within the top fixed rows.
   * @param {boolean} onFixedRowBottom `true` if the coordinates point to a place within the bottom fixed rows.
   * @param {boolean} onFixedColumn `true` if the coordinates point to a place within the fixed columns.
   * @param {number} elementOffset Offset position of the cell element.
   * @param {number} spreaderOffset Offset position of the spreader element.
   * @returns {{top: number, left: number}}
   */
  getRelativeCellPositionWithinHolder(e, n, i, r, o) {
    const s = {
      horizontal: this.wot.wtOverlays.inlineStartOverlay.getScrollPosition(),
      vertical: this.wot.wtOverlays.topOverlay.getScrollPosition()
    };
    let a = 0, l = 0;
    if (i || (a = s.horizontal - o.start), n) {
      const u = this.wot.wtTable.wtRootElement.getBoundingClientRect();
      l = this.clone.wtTable.TABLE.getBoundingClientRect().top * -1 + u.top;
    } else
      e || (l = s.vertical - o.top);
    return {
      start: r.start - a,
      top: r.top - l
    };
  }
  /**
   * Make a clone of table for overlay.
   *
   * @returns {Clone}
   */
  makeClone() {
    if ($x.indexOf(this.type) === -1)
      throw new Error(`Clone type "${this.type}" is not supported.`);
    const {
      wtTable: e,
      wtSettings: n
    } = this.wot, {
      rootDocument: i,
      rootWindow: r
    } = this.domBindings, o = i.createElement("div"), s = i.createElement("table"), a = e.wtRootElement.parentNode;
    o.className = `${s3.get(this.type)} handsontable`, o.setAttribute("dir", this.isRtl() ? "rtl" : "ltr"), o.style.position = "absolute", o.style.top = 0, o.style.overflow = "visible", this.isRtl() ? o.style.right = 0 : o.style.left = 0, n.getSetting("ariaTags") && Ee(o, [oo()]), s.className = e.TABLE.className, e.TABLE.getAttribute("role") && s.setAttribute("role", e.TABLE.getAttribute("role")), o.appendChild(s), a.appendChild(o);
    const u = this.wtSettings.getSetting("preventOverflow");
    return u === !0 || u === "horizontal" && this.type === nl || u === "vertical" && this.type === il ? this.mainTableScrollableElement = r : r.getComputedStyle(a).getPropertyValue("overflow") === "hidden" ? this.mainTableScrollableElement = e.holder : this.mainTableScrollableElement = Dh(e.TABLE), new A3(s, this.wtSettings, {
      // todo ioc factory
      source: this.wot,
      overlay: this,
      viewport: this.wot.wtViewport,
      // todo ioc , or factor func if used only here
      event: this.wot.wtEvent,
      // todo ioc , or factory func if used only here
      selectionManager: this.wot.selectionManager
      // todo ioc , or factory func if used only here
    });
  }
  /**
   * Refresh/Redraw overlay.
   *
   * @param {boolean} [fastDraw=false] When `true`, try to refresh only the positions of borders without rerendering
   *                                   the data. It will only work if Table.draw() does not force
   *                                   rendering anyway.
   */
  refresh() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    this.needFullRender && this.clone.draw(e);
  }
  /**
   * Reset overlay styles to initial values.
   */
  reset() {
    const e = this.clone.wtTable.holder, n = this.clone.wtTable.hider, i = e.style, r = n.style, o = e.parentNode.style;
    [i, r, o].forEach((s) => {
      s.width = "", s.height = "";
    });
  }
  /**
   * Determine if Walkontable is running in RTL mode.
   *
   * @returns {boolean}
   */
  isRtl() {
    return this.wtSettings.getSetting("rtlMode");
  }
  /**
   * Destroy overlay instance.
   */
  destroy() {
    this.clone.eventManager.destroy();
  }
}
class S3 extends Ed {
  /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   * @param {BottomOverlay} bottomOverlay The instance of the Top overlay.
   * @param {InlineStartOverlay} inlineStartOverlay The instance of the InlineStart overlay.
   */
  constructor(e, n, i, r, o, s) {
    super(e, n, $s, i, r), this.bottomOverlay = o, this.inlineStartOverlay = s;
  }
  /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {BottomInlineStartCornerOverlayTable}
   */
  createTable() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    return new o3(...n);
  }
  /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */
  shouldBeRendered() {
    return this.wtSettings.getSetting("shouldRenderBottomOverlay") && this.wtSettings.getSetting("shouldRenderInlineStartOverlay");
  }
  /**
   * Updates the corner overlay position.
   *
   * @returns {boolean}
   */
  resetFixedPosition() {
    const {
      wot: e
    } = this;
    if (this.updateTrimmingContainer(), !e.wtTable.holder.parentNode)
      return !1;
    const n = this.clone.wtTable.holder.parentNode;
    if (n.style.top = "", this.trimmingContainer === this.domBindings.rootWindow) {
      const o = this.inlineStartOverlay.getOverlayOffset(), s = this.bottomOverlay.getOverlayOffset();
      n.style[this.isRtl() ? "right" : "left"] = `${o}px`, n.style.bottom = `${s}px`;
    } else
      vd(n), this.repositionOverlay();
    let i = Zt(this.clone.wtTable.TABLE);
    const r = It(this.clone.wtTable.TABLE);
    return this.wot.wtTable.hasDefinedSize() || (i = 0), n.style.height = `${i}px`, n.style.width = `${r}px`, !1;
  }
  /**
   * Reposition the overlay.
   */
  repositionOverlay() {
    const {
      wtTable: e,
      wtViewport: n
    } = this.wot, {
      rootDocument: i
    } = this.domBindings, r = this.clone.wtTable.holder.parentNode;
    let o = 0;
    n.hasVerticalScroll() || (o += n.getWorkspaceHeight() - e.getTotalHeight()), n.hasVerticalScroll() && n.hasHorizontalScroll() && (o += pn(i)), r.style.bottom = `${o}px`;
  }
}
const b3 = "calculatedColumns", Ux = {
  /**
   * Get the source index of the first rendered column. If no columns are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getFirstRenderedColumn() {
    const t = this.dataAccessObject.startColumnRendered;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the first column fully visible in the viewport. If no columns are fully visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getFirstVisibleColumn() {
    const t = this.dataAccessObject.startColumnVisible;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the first column partially visible in the viewport. If no columns are partially visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getFirstPartiallyVisibleColumn() {
    const t = this.dataAccessObject.startColumnPartiallyVisible;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the last rendered column. If no columns are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getLastRenderedColumn() {
    const t = this.dataAccessObject.endColumnRendered;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the last column fully visible in the viewport. If no columns are fully visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getLastVisibleColumn() {
    const t = this.dataAccessObject.endColumnVisible;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the last column partially visible in the viewport. If no columns are partially visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getLastPartiallyVisibleColumn() {
    const t = this.dataAccessObject.endColumnPartiallyVisible;
    return t === null ? -1 : t;
  },
  /**
   * Get the number of rendered columns.
   *
   * @returns {number}
   * @this Table
   */
  getRenderedColumnsCount() {
    return this.dataAccessObject.countColumnsRendered;
  },
  /**
   * Get the number of fully visible columns in the viewport.
   *
   * @returns {number}
   * @this Table
   */
  getVisibleColumnsCount() {
    return this.dataAccessObject.countColumnsVisible;
  },
  /**
   * Get the number of rendered row headers.
   *
   * @returns {number}
   * @this Table
   */
  getRowHeadersCount() {
    return this.dataAccessObject.rowHeaders.length;
  }
};
wo(Ux, "MIXIN_NAME", b3, {
  writable: !1,
  enumerable: !1
});
const Ob = Ux;
class Pb extends Bu {
  /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */
  constructor(e, n, i, r) {
    super(e, n, i, r, no);
  }
}
pt(Pb, Vx);
pt(Pb, Ob);
const R3 = Pb;
function E3(t, e, n) {
  return (e = T3(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function T3(t) {
  var e = M3(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function M3(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class I3 extends Ed {
  /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   */
  constructor(e, n, i, r) {
    super(e, n, no, i, r), E3(this, "cachedFixedRowsBottom", -1), this.cachedFixedRowsBottom = this.wtSettings.getSetting("fixedRowsBottom");
  }
  /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {BottomOverlayTable}
   */
  createTable() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    return new R3(...n);
  }
  /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */
  shouldBeRendered() {
    return this.wtSettings.getSetting("shouldRenderBottomOverlay");
  }
  /**
   * Updates the top overlay position.
   *
   * @returns {boolean}
   */
  resetFixedPosition() {
    if (!this.needFullRender || !this.shouldBeRendered() || !this.wot.wtTable.holder.parentNode)
      return !1;
    const {
      rootWindow: e
    } = this.domBindings, n = this.clone.wtTable.holder.parentNode;
    n.style.top = "";
    let i = 0;
    const r = this.wtSettings.getSetting("preventOverflow");
    this.trimmingContainer === e && (!r || r !== "vertical") ? (i = this.getOverlayOffset(), n.style.bottom = `${i}px`) : (i = this.getScrollPosition(), this.repositionOverlay());
    const o = this.adjustHeaderBordersPosition(i);
    return this.adjustElementsSize(), o;
  }
  /**
   * Updates the bottom overlay position.
   */
  repositionOverlay() {
    const {
      wtTable: e,
      wtViewport: n
    } = this.wot, {
      rootDocument: i
    } = this.domBindings, r = this.clone.wtTable.holder.parentNode;
    let o = 0;
    n.hasVerticalScroll() || (o += n.getWorkspaceHeight() - e.getTotalHeight()), n.hasVerticalScroll() && n.hasHorizontalScroll() && (o += pn(i)), r.style.bottom = `${o}px`;
  }
  /**
   * Sets the main overlay's vertical scroll position.
   *
   * @param {number} pos The scroll position.
   * @returns {boolean}
   */
  setScrollPosition(e) {
    const {
      rootWindow: n
    } = this.domBindings;
    let i = !1;
    return this.mainTableScrollableElement === n ? (n.scrollTo(Rp(n), e), i = !0) : this.mainTableScrollableElement.scrollTop !== e && (this.mainTableScrollableElement.scrollTop = e, i = !0), i;
  }
  /**
   * Triggers onScroll hook callback.
   */
  onScroll() {
    this.wtSettings.getSetting("onScrollHorizontally");
  }
  /**
   * Calculates total sum cells height.
   *
   * @param {number} from Row index which calculates started from.
   * @param {number} to Row index where calculation is finished.
   * @returns {number} Height sum.
   */
  sumCellSizes(e, n) {
    const {
      wtTable: i,
      wtSettings: r
    } = this.wot, o = r.getSetting("defaultRowHeight");
    let s = e, a = 0;
    for (; s < n; ) {
      const l = i.getRowHeight(s);
      a += l === void 0 ? o : l, s += 1;
    }
    return a;
  }
  /**
   * Adjust overlay root element, children and master table element sizes (width, height).
   */
  adjustElementsSize() {
    this.updateTrimmingContainer(), this.needFullRender && (this.adjustRootElementSize(), this.adjustRootChildrenSize());
  }
  /**
   * Adjust overlay root element size (width and height).
   */
  adjustRootElementSize() {
    const {
      wtTable: e,
      wtViewport: n
    } = this.wot, {
      rootDocument: i,
      rootWindow: r
    } = this.domBindings, o = pn(i), a = this.clone.wtTable.holder.parentNode.style, l = this.wtSettings.getSetting("preventOverflow");
    if (this.trimmingContainer !== r || l === "horizontal") {
      let c = n.getWorkspaceWidth();
      this.wot.wtOverlays.hasScrollbarRight && (c -= o), c = Math.min(c, e.wtRootElement.scrollWidth), a.width = `${c}px`;
    } else
      a.width = "";
    this.clone.wtTable.holder.style.width = a.width;
    let u = Zt(this.clone.wtTable.TABLE);
    this.wot.wtTable.hasDefinedSize() || (u = 0), a.height = `${u}px`;
  }
  /**
   * Adjust overlay root childs size.
   */
  adjustRootChildrenSize() {
    const {
      holder: e
    } = this.clone.wtTable;
    this.clone.wtTable.hider.style.width = this.hider.style.width, e.style.width = e.parentNode.style.width, e.style.height = e.parentNode.style.height;
  }
  /**
   * Adjust the overlay dimensions and position.
   */
  applyToDOM() {
    const e = this.wtSettings.getSetting("totalRows");
    if (typeof this.wot.wtViewport.rowsRenderCalculator.startPosition == "number")
      this.spreader.style.top = `${this.wot.wtViewport.rowsRenderCalculator.startPosition}px`;
    else if (e === 0)
      this.spreader.style.top = "0";
    else
      throw new Error("Incorrect value of the rowsRenderCalculator");
    this.spreader.style.bottom = "", this.needFullRender && this.syncOverlayOffset();
  }
  /**
   * Synchronize calculated left position to an element.
   */
  syncOverlayOffset() {
    const e = this.isRtl() ? "right" : "left", {
      spreader: n
    } = this.clone.wtTable;
    typeof this.wot.wtViewport.columnsRenderCalculator.startPosition == "number" ? n.style[e] = `${this.wot.wtViewport.columnsRenderCalculator.startPosition}px` : n.style[e] = "";
  }
  /**
   * Scrolls vertically to a row.
   *
   * @param {number} sourceRow Row index which you want to scroll to.
   * @param {boolean} [bottomEdge=false] If `true`, scrolls according to the bottom edge (top edge is by default).
   */
  scrollTo(e, n) {
    let i = this.getTableParentOffset();
    const o = (this.wot.cloneSource ? this.wot.cloneSource : this.wot).wtTable.holder;
    let s = 0;
    n && o.offsetHeight !== o.clientHeight && (s = pn(this.domBindings.rootDocument)), n ? (i += this.sumCellSizes(0, e + 1), i -= this.wot.wtViewport.getViewportHeight(), i += 1) : i += this.sumCellSizes(this.wtSettings.getSetting("fixedRowsBottom"), e), i += s, this.setScrollPosition(i);
  }
  /**
   * Gets table parent top position.
   *
   * @returns {number}
   */
  getTableParentOffset() {
    return this.mainTableScrollableElement === this.domBindings.rootWindow ? this.wot.wtTable.holderOffset.top : 0;
  }
  /**
   * Gets the main overlay's vertical scroll position.
   *
   * @returns {number} Main table's vertical scroll position.
   */
  getScrollPosition() {
    return Ep(this.mainTableScrollableElement, this.domBindings.rootWindow);
  }
  /**
   * Gets the main overlay's vertical overlay offset.
   *
   * @returns {number} Main table's vertical overlay offset.
   */
  getOverlayOffset() {
    const {
      rootWindow: e
    } = this.domBindings, n = this.wtSettings.getSetting("preventOverflow");
    let i = 0;
    if (this.trimmingContainer === e && (!n || n !== "vertical")) {
      const r = this.wot.wtTable.getTotalHeight(), o = this.clone.wtTable.getTotalHeight(), s = r - o, a = this.domBindings.rootDocument.documentElement.clientHeight;
      i = Math.max(this.getTableParentOffset() - this.getScrollPosition() - a + r, 0), i > s && (i = 0);
    }
    return i;
  }
  /**
   * Adds css classes to hide the header border's header (cell-selection border hiding issue).
   *
   * @param {number} position Header Y position if trimming container is window or scroll top if not.
   * @returns {boolean}
   */
  adjustHeaderBordersPosition(e) {
    const n = this.wtSettings.getSetting("fixedRowsBottom"), i = this.cachedFixedRowsBottom !== n, r = this.wtSettings.getSetting("columnHeaders");
    let o = !1;
    if ((i || n === 0) && r.length > 0) {
      const s = this.wot.wtTable.holder.parentNode, a = Te(s, "innerBorderBottom");
      this.cachedFixedRowsBottom = this.wtSettings.getSetting("fixedRowsBottom"), e || this.wtSettings.getSetting("totalRows") === 0 ? (Q(s, "innerBorderBottom"), o = !a) : (xe(s, "innerBorderBottom"), o = a);
    }
    return o;
  }
}
const O3 = "calculatedRows", Yx = {
  /**
   * Get the source index of the first rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getFirstRenderedRow() {
    const t = this.dataAccessObject.startRowRendered;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the first row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getFirstVisibleRow() {
    const t = this.dataAccessObject.startRowVisible;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the first row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getFirstPartiallyVisibleRow() {
    const t = this.dataAccessObject.startRowPartiallyVisible;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the last rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getLastRenderedRow() {
    const t = this.dataAccessObject.endRowRendered;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the last row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getLastVisibleRow() {
    const t = this.dataAccessObject.endRowVisible;
    return t === null ? -1 : t;
  },
  /**
   * Get the source index of the last row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getLastPartiallyVisibleRow() {
    const t = this.dataAccessObject.endRowPartiallyVisible;
    return t === null ? -1 : t;
  },
  /**
   * Get the number of rendered rows.
   *
   * @returns {number}
   * @this Table
   */
  getRenderedRowsCount() {
    return this.dataAccessObject.countRowsRendered;
  },
  /**
   * Get the number of fully visible rows in the viewport.
   *
   * @returns {number}
   * @this Table
   */
  getVisibleRowsCount() {
    return this.dataAccessObject.countRowsVisible;
  },
  /**
   * Get the number of rendered column headers.
   *
   * @returns {number}
   * @this Table
   */
  getColumnHeadersCount() {
    return this.dataAccessObject.columnHeaders.length;
  }
};
wo(Yx, "MIXIN_NAME", O3, {
  writable: !1,
  enumerable: !1
});
const jx = Yx;
class xb extends Bu {
  /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */
  constructor(e, n, i, r) {
    super(e, n, i, r, il);
  }
}
pt(xb, jx);
pt(xb, Tb);
const P3 = xb, x3 = "localHooks", Gx = {
  /**
   * Internal hooks storage.
   */
  _localHooks: /* @__PURE__ */ Object.create(null),
  /**
   * Add hook to the collection.
   *
   * @param {string} key The hook name.
   * @param {Function} callback The hook callback.
   * @returns {object}
   */
  addLocalHook(t, e) {
    return this._localHooks[t] || (this._localHooks[t] = []), this._localHooks[t].push(e), this;
  },
  /**
   * Run hooks.
   *
   * @param {string} key The name of the hook to run.
   * @param {*} [arg1] An additional parameter passed to the callback function.
   * @param {*} [arg2] An additional parameter passed to the callback function.
   * @param {*} [arg3] An additional parameter passed to the callback function.
   * @param {*} [arg4] An additional parameter passed to the callback function.
   * @param {*} [arg5] An additional parameter passed to the callback function.
   * @param {*} [arg6] An additional parameter passed to the callback function.
   */
  runLocalHooks(t, e, n, i, r, o, s) {
    if (this._localHooks[t]) {
      const a = this._localHooks[t].length;
      for (let l = 0; l < a; l++)
        Am(this._localHooks[t][l], this, e, n, i, r, o, s);
    }
  },
  /**
   * Clear all added hooks.
   *
   * @returns {object}
   */
  clearLocalHooks() {
    return this._localHooks = {}, this;
  }
};
wo(Gx, "MIXIN_NAME", x3, {
  writable: !1,
  enumerable: !1
});
const Vn = Gx;
let zx = class {
  /**
   * @param {object} settings The selection settings object. @todo type.
   * @param {CellRange} cellRange The cell range instance.
   */
  constructor(e, n) {
    this.settings = e, this.cellRange = n || null;
  }
  /**
   * Checks if selection is empty.
   *
   * @returns {boolean}
   */
  isEmpty() {
    return this.cellRange === null;
  }
  /**
   * Adds a cell coords to the selection.
   *
   * @param {CellCoords} coords The cell coordinates to add.
   * @returns {Selection}
   */
  add(e) {
    return this.isEmpty() ? this.cellRange = this.settings.createCellRange(e) : this.cellRange.expand(e), this;
  }
  /**
   * If selection range from or to property equals oldCoords, replace it with newCoords. Return boolean
   * information about success.
   *
   * @param {CellCoords} oldCoords An old cell coordinates to replace.
   * @param {CellCoords} newCoords The new cell coordinates.
   * @returns {boolean}
   */
  replace(e, n) {
    if (!this.isEmpty()) {
      if (this.cellRange.from.isEqual(e))
        return this.cellRange.from = n, !0;
      if (this.cellRange.to.isEqual(e))
        return this.cellRange.to = n, !0;
    }
    return !1;
  }
  /**
   * Clears selection.
   *
   * @returns {Selection}
   */
  clear() {
    return this.cellRange = null, this;
  }
  /**
   * Returns the top left (or top right in RTL) and bottom right (or bottom left in RTL) selection coordinates.
   *
   * @returns {number[]} Returns array of coordinates for example `[1, 1, 5, 5]`.
   */
  getCorners() {
    const e = this.cellRange.getOuterTopStartCorner(), n = this.cellRange.getOuterBottomEndCorner();
    return [e.row, e.col, n.row, n.col];
  }
  /**
   * Destroys the instance.
   */
  destroy() {
    this.runLocalHooks("destroy");
  }
};
pt(zx, Vn);
const H3 = zx, Kx = "active-header", Hb = "header", _b = "area", Nb = "focus", _3 = "fill", N3 = "row", D3 = "column", k3 = "custom-selection", Db = Object.freeze({
  width: "6px",
  height: "6px",
  borderWidth: "1px",
  borderStyle: "solid",
  borderColor: "#FFF"
});
var L3 = mb;
L3("flat");
function B3(t, e) {
  Qx(t, e), e.add(t);
}
function eT(t, e, n) {
  Qx(t, e), e.set(t, n);
}
function Qx(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Jt(t, e) {
  return t.get(lh(t, e));
}
function tT(t, e, n) {
  return t.set(lh(t, e), n), n;
}
function lh(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var rr = /* @__PURE__ */ new WeakMap(), ri = /* @__PURE__ */ new WeakMap(), wf = /* @__PURE__ */ new WeakSet();
class F3 {
  constructor() {
    B3(this, wf), eT(this, rr, void 0), eT(this, ri, void 0);
  }
  /**
   * Sets the Walkontable instance that will be taking into account while scanning the table.
   *
   * @param {Walkontable} activeOverlaysWot The Walkontable instance.
   * @returns {SelectionScanner}
   */
  setActiveOverlay(e) {
    return tT(ri, this, e), this;
  }
  /**
   * Sets the Selection instance to process.
   *
   * @param {Selection} selection The Selection instance.
   * @returns {SelectionScanner}
   */
  setActiveSelection(e) {
    return tT(rr, this, e), this;
  }
  /**
   * Scans the rendered table with selection and returns elements that intersects
   * with selection coordinates.
   *
   * @returns {HTMLTableElement[]}
   */
  scan() {
    const e = Jt(rr, this).settings.selectionType, n = /* @__PURE__ */ new Set();
    return e === "active-header" ? (this.scanColumnsInHeadersRange((i) => n.add(i)), this.scanRowsInHeadersRange((i) => n.add(i))) : e === "area" ? this.scanCellsRange((i) => n.add(i)) : e === "focus" ? (this.scanColumnsInHeadersRange((i) => n.add(i)), this.scanRowsInHeadersRange((i) => n.add(i)), this.scanCellsRange((i) => n.add(i))) : e === "fill" ? this.scanCellsRange((i) => n.add(i)) : e === "header" ? (this.scanColumnsInHeadersRange((i) => n.add(i)), this.scanRowsInHeadersRange((i) => n.add(i))) : e === "row" ? (this.scanRowsInHeadersRange((i) => n.add(i)), this.scanRowsInCellsRange((i) => n.add(i))) : e === "column" && (this.scanColumnsInHeadersRange((i) => n.add(i)), this.scanColumnsInCellsRange((i) => n.add(i))), n;
  }
  /**
   * Scans the table (only rendered headers) and collect all column headers (TH) that match
   * the coordinates passed in the Selection instance.
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */
  scanColumnsInHeadersRange(e) {
    const [n, i, r, o] = Jt(rr, this).getCorners(), {
      wtTable: s
    } = Jt(ri, this), a = s.getRenderedColumnsCount(), l = s.getColumnHeadersCount();
    let u = 0;
    for (let c = -s.getRowHeadersCount(); c < a; c++) {
      const h = s.columnFilter.renderedToSource(c);
      if (!(h < i || h > o)) {
        for (let d = -l; d < 0; d++) {
          if (d < n || d > r)
            continue;
          const g = d + l;
          let m = s.getColumnHeader(h, g);
          const w = Jt(ri, this).getSetting("onBeforeHighlightingColumnHeader", h, g, {
            selectionType: Jt(rr, this).settings.selectionType,
            columnCursor: u,
            selectionWidth: o - i + 1
          });
          w !== null && (w !== h && (m = s.getColumnHeader(w, g)), e(m));
        }
        u += 1;
      }
    }
  }
  /**
   * Scans the table (only rendered headers) and collect all row headers (TH) that match
   * the coordinates passed in the Selection instance.
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */
  scanRowsInHeadersRange(e) {
    const [n, i, r, o] = Jt(rr, this).getCorners(), {
      wtTable: s
    } = Jt(ri, this), a = s.getRenderedRowsCount(), l = s.getRowHeadersCount();
    let u = 0;
    for (let c = -s.getColumnHeadersCount(); c < a; c++) {
      const h = s.rowFilter.renderedToSource(c);
      if (!(h < n || h > r)) {
        for (let d = -l; d < 0; d++) {
          if (d < i || d > o)
            continue;
          const g = d + l;
          let m = s.getRowHeader(h, g);
          const w = Jt(ri, this).getSetting("onBeforeHighlightingRowHeader", h, g, {
            selectionType: Jt(rr, this).settings.selectionType,
            rowCursor: u,
            selectionHeight: r - n + 1
          });
          w !== null && (w !== h && (m = s.getRowHeader(w, g)), e(m));
        }
        u += 1;
      }
    }
  }
  /**
   * Scans the table (only rendered cells) and collect all cells (TR) that match
   * the coordinates passed in the Selection instance.
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */
  scanCellsRange(e) {
    const [n, i, r, o] = Jt(rr, this).getCorners(), {
      wtTable: s
    } = Jt(ri, this);
    lh(wf, this, uC).call(this, (a, l) => {
      if (a >= n && a <= r && l >= i && l <= o) {
        const u = s.getCell(Jt(ri, this).createCellCoords(a, l)), c = Jt(ri, this).getSetting("onAfterDrawSelection", a, l, Jt(rr, this).settings.layerLevel);
        typeof c == "string" && Q(u, c), e(u);
      }
    });
  }
  /**
   * Scans the table (only rendered cells) and collects all cells (TR) that match the coordinates
   * passed in the Selection instance but only for the X axis (rows).
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */
  scanRowsInCellsRange(e) {
    const [n, , i] = Jt(rr, this).getCorners(), {
      wtTable: r
    } = Jt(ri, this);
    lh(wf, this, uC).call(this, (o, s) => {
      if (o >= n && o <= i) {
        const a = r.getCell(Jt(ri, this).createCellCoords(o, s));
        e(a);
      }
    });
  }
  /**
   * Scans the table (only rendered cells) and collects all cells (TR) that match the coordinates
   * passed in the Selection instance but only for the Y axis (columns).
   *
   * @param {function(HTMLTableElement): void} callback The callback function to trigger.
   */
  scanColumnsInCellsRange(e) {
    const [, n, , i] = Jt(rr, this).getCorners(), {
      wtTable: r
    } = Jt(ri, this);
    lh(wf, this, uC).call(this, (o, s) => {
      if (s >= n && s <= i) {
        const a = r.getCell(Jt(ri, this).createCellCoords(o, s));
        e(a);
      }
    });
  }
}
function uC(t) {
  const {
    wtTable: e
  } = Jt(ri, this), n = e.getRenderedRowsCount(), i = e.getRenderedColumnsCount();
  for (let r = 0; r < n; r += 1) {
    const o = e.rowFilter.renderedToSource(r);
    for (let s = 0; s < i; s += 1)
      t(o, e.columnFilter.renderedToSource(s));
  }
}
class V3 {
  // TODO As this is an internal class, should be designed for using {Walkontable}. It uses the facade,
  // TODO Con. Because the class is created on place where the instance reference comes from external origin.
  // TODO Imho, the discrimination for handling both, facade and non-facade should be handled.
  /**
   * @param {WalkontableFacade} wotInstance The Walkontable instance.
   * @param {object} settings The border settings.
   */
  constructor(e, n) {
    n && (this.eventManager = e.eventManager, this.instance = e, this.wot = e, this.settings = n, this.mouseDown = !1, this.main = null, this.top = null, this.bottom = null, this.start = null, this.end = null, this.topStyle = null, this.bottomStyle = null, this.startStyle = null, this.endStyle = null, this.cornerDefaultStyle = Db, this.cornerCenterPointOffset = -(parseInt(this.cornerDefaultStyle.width, 10) / 2), this.corner = null, this.cornerStyle = null, this.createBorders(n), this.registerListeners());
  }
  /**
   * Register all necessary events.
   */
  registerListeners() {
    const e = this.wot.rootDocument.body;
    this.eventManager.addEventListener(e, "mousedown", () => this.onMouseDown()), this.eventManager.addEventListener(e, "mouseup", () => this.onMouseUp());
    for (let n = 0, i = this.main.childNodes.length; n < i; n++) {
      const r = this.main.childNodes[n];
      this.eventManager.addEventListener(r, "mouseenter", (o) => this.onMouseEnter(o, this.main.childNodes[n]));
    }
  }
  /**
   * Mouse down listener.
   *
   * @private
   */
  onMouseDown() {
    this.mouseDown = !0;
  }
  /**
   * Mouse up listener.
   *
   * @private
   */
  onMouseUp() {
    this.mouseDown = !1;
  }
  /**
   * Mouse enter listener for fragment selection functionality.
   *
   * @private
   * @param {Event} event Dom event.
   * @param {HTMLElement} parentElement Part of border element.
   */
  onMouseEnter(e, n) {
    if (!this.mouseDown || !this.wot.getSetting("hideBorderOnMouseDownOver"))
      return;
    e.preventDefault(), Zi(e);
    const i = this, r = this.wot.rootDocument.body, o = n.getBoundingClientRect();
    n.style.display = "none";
    function s(l) {
      if (l.clientY < Math.floor(o.top) || l.clientY > Math.ceil(o.top + o.height) || l.clientX < Math.floor(o.left) || l.clientX > Math.ceil(o.left + o.width))
        return !0;
    }
    function a(l) {
      s(l) && (i.eventManager.removeEventListener(r, "mousemove", a), n.style.display = "block");
    }
    this.eventManager.addEventListener(r, "mousemove", a);
  }
  /**
   * Create border elements.
   *
   * @param {object} settings The border settings.
   */
  createBorders(e) {
    const {
      rootDocument: n
    } = this.wot;
    this.main = n.createElement("div");
    const i = ["top", "start", "bottom", "end", "corner"];
    let r = this.main.style;
    r.position = "absolute", r.top = 0, r.left = 0;
    for (let a = 0; a < 5; a++) {
      const l = i[a], u = n.createElement("div");
      u.className = `wtBorder ${this.settings.className || ""}`, this.settings[l] && this.settings[l].hide && (u.className += " hidden"), r = u.style, r.backgroundColor = this.settings[l] && this.settings[l].color ? this.settings[l].color : e.border.color, r.height = this.settings[l] && this.settings[l].width ? `${this.settings[l].width}px` : `${e.border.width}px`, r.width = this.settings[l] && this.settings[l].width ? `${this.settings[l].width}px` : `${e.border.width}px`, this.main.appendChild(u);
    }
    this.top = this.main.childNodes[0], this.start = this.main.childNodes[1], this.bottom = this.main.childNodes[2], this.end = this.main.childNodes[3], this.topStyle = this.top.style, this.startStyle = this.start.style, this.bottomStyle = this.bottom.style, this.endStyle = this.end.style, this.corner = this.main.childNodes[4], this.corner.className += " corner", this.cornerStyle = this.corner.style, this.cornerStyle.width = this.cornerDefaultStyle.width, this.cornerStyle.height = this.cornerDefaultStyle.height, this.cornerStyle.border = [this.cornerDefaultStyle.borderWidth, this.cornerDefaultStyle.borderStyle, this.cornerDefaultStyle.borderColor].join(" "), Yo() && this.instance.getSetting("isDataViewInstance") && this.createMultipleSelectorHandles(), this.disappear();
    const {
      wtTable: o
    } = this.wot;
    let s = o.bordersHolder;
    s || (s = n.createElement("div"), s.className = "htBorders", o.bordersHolder = s, o.spreader.appendChild(s)), s.appendChild(this.main);
  }
  /**
   * Create multiple selector handler for mobile devices.
   */
  createMultipleSelectorHandles() {
    const {
      rootDocument: e
    } = this.wot;
    this.selectionHandles = {
      top: e.createElement("DIV"),
      topHitArea: e.createElement("DIV"),
      bottom: e.createElement("DIV"),
      bottomHitArea: e.createElement("DIV")
    };
    const n = 10, i = 40;
    this.selectionHandles.top.className = "topSelectionHandle topLeftSelectionHandle", this.selectionHandles.topHitArea.className = "topSelectionHandle-HitArea topLeftSelectionHandle-HitArea", this.selectionHandles.bottom.className = "bottomSelectionHandle bottomRightSelectionHandle", this.selectionHandles.bottomHitArea.className = "bottomSelectionHandle-HitArea bottomRightSelectionHandle-HitArea", this.selectionHandles.styles = {
      top: this.selectionHandles.top.style,
      topHitArea: this.selectionHandles.topHitArea.style,
      bottom: this.selectionHandles.bottom.style,
      bottomHitArea: this.selectionHandles.bottomHitArea.style
    };
    const r = {
      position: "absolute",
      height: `${i}px`,
      width: `${i}px`,
      "border-radius": `${parseInt(i / 1.5, 10)}px`
    };
    Fe(r, (s, a) => {
      this.selectionHandles.styles.bottomHitArea[a] = s, this.selectionHandles.styles.topHitArea[a] = s;
    });
    const o = {
      position: "absolute",
      height: `${n}px`,
      width: `${n}px`,
      "border-radius": `${parseInt(n / 1.5, 10)}px`,
      background: "#F5F5FF",
      border: "1px solid #4285c8"
    };
    Fe(o, (s, a) => {
      this.selectionHandles.styles.bottom[a] = s, this.selectionHandles.styles.top[a] = s;
    }), this.main.appendChild(this.selectionHandles.top), this.main.appendChild(this.selectionHandles.bottom), this.main.appendChild(this.selectionHandles.topHitArea), this.main.appendChild(this.selectionHandles.bottomHitArea);
  }
  /**
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @returns {boolean}
   */
  isPartRange(e, n) {
    const i = this.wot.selectionManager.getAreaSelection();
    return !!(i.cellRange && (e !== i.cellRange.to.row || n !== i.cellRange.to.col));
  }
  /**
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number} top The top position of the handler.
   * @param {number} left The left position of the handler.
   * @param {number} width The width of the handler.
   * @param {number} height The height of the handler.
   */
  updateMultipleSelectionHandlesPosition(e, n, i, r, o, s) {
    const l = this.wot.wtSettings.getSetting("rtlMode") ? "right" : "left", {
      top: u,
      topHitArea: c,
      bottom: h,
      bottomHitArea: d
    } = this.selectionHandles.styles, g = parseInt(u.borderWidth, 10), m = parseInt(u.width, 10), w = parseInt(c.width, 10), C = this.wot.wtTable.getWidth(), S = this.wot.wtTable.getHeight();
    u.top = `${parseInt(i - m - 1, 10)}px`, u[l] = `${parseInt(r - m - 1, 10)}px`, c.top = `${parseInt(i - w / 4 * 3, 10)}px`, c[l] = `${parseInt(r - w / 4 * 3, 10)}px`;
    const b = Math.min(parseInt(r + o, 10), C - m - g * 2), R = Math.min(parseInt(r + o - w / 4, 10), C - w - g * 2);
    h[l] = `${b}px`, d[l] = `${R}px`;
    const _ = Math.min(parseInt(i + s, 10), S - m - g * 2), O = Math.min(parseInt(i + s - w / 4, 10), S - w - g * 2);
    h.top = `${_}px`, d.top = `${O}px`, this.settings.border.cornerVisible && this.settings.border.cornerVisible() ? (u.display = "block", c.display = "block", this.isPartRange(e, n) ? (h.display = "none", d.display = "none") : (h.display = "block", d.display = "block")) : (u.display = "none", h.display = "none", c.display = "none", d.display = "none"), e === this.wot.wtSettings.getSetting("fixedRowsTop") || n === this.wot.wtSettings.getSetting("fixedColumnsStart") ? (u.zIndex = "9999", c.zIndex = "9999") : (u.zIndex = "", c.zIndex = "");
  }
  /**
   * Show border around one or many cells.
   *
   * @param {Array} corners The corner coordinates.
   */
  appear(e) {
    if (this.disabled)
      return;
    const {
      wtTable: n,
      rootDocument: i,
      rootWindow: r
    } = this.wot;
    let o, s, a, l, u, c;
    const h = n.getRenderedRowsCount();
    for (let Ce = 0; Ce < h; Ce += 1) {
      const pe = n.rowFilter.renderedToSource(Ce);
      if (pe >= e[0] && pe <= e[2]) {
        o = pe, u = e[0];
        break;
      }
    }
    for (let Ce = h - 1; Ce >= 0; Ce -= 1) {
      const pe = n.rowFilter.renderedToSource(Ce);
      if (pe >= e[0] && pe <= e[2]) {
        s = pe;
        break;
      }
    }
    const d = n.getRenderedColumnsCount();
    for (let Ce = 0; Ce < d; Ce += 1) {
      const pe = n.columnFilter.renderedToSource(Ce);
      if (pe >= e[1] && pe <= e[3]) {
        a = pe, c = e[1];
        break;
      }
    }
    for (let Ce = d - 1; Ce >= 0; Ce -= 1) {
      const pe = n.columnFilter.renderedToSource(Ce);
      if (pe >= e[1] && pe <= e[3]) {
        l = pe;
        break;
      }
    }
    if (o === void 0 || a === void 0) {
      this.disappear();
      return;
    }
    let g = n.getCell(this.wot.createCellCoords(o, a));
    const m = o !== s || a !== l, w = m ? n.getCell(this.wot.createCellCoords(s, l)) : g, C = gn(g), S = m ? gn(w) : C, b = gn(n.TABLE), R = It(n.TABLE), _ = C.top, O = C.left, $ = this.wot.wtSettings.getSetting("rtlMode");
    let U = 0, G = 0;
    if ($) {
      const Ce = It(g), pe = r.innerWidth - b.left - R;
      G = O + Ce - S.left, U = r.innerWidth - O - Ce - pe - 1;
    } else
      G = S.left + It(w) - O, U = O - b.left - 1;
    if (this.isEntireColumnSelected(o, s)) {
      const Ce = this.getDimensionsFromHeader("columns", a, l, u, b);
      let pe = null;
      Ce && ([pe, U, G] = Ce), pe && (g = pe);
    }
    let z = _ - b.top - 1, j = S.top + Zt(w) - _;
    if (this.isEntireRowSelected(a, l)) {
      const Ce = this.getDimensionsFromHeader("rows", o, s, c, b);
      let pe = null;
      Ce && ([pe, z, j] = Ce), pe && (g = pe);
    }
    const J = uo(g, r);
    parseInt(J.borderTopWidth, 10) > 0 && (z += 1, j = j > 0 ? j - 1 : 0), parseInt(J[$ ? "borderRightWidth" : "borderLeftWidth"], 10) > 0 && (U += 1, G = G > 0 ? G - 1 : 0);
    const q = $ ? "right" : "left";
    this.topStyle.top = `${z}px`, this.topStyle[q] = `${U}px`, this.topStyle.width = `${G}px`, this.topStyle.display = "block", this.startStyle.top = `${z}px`, this.startStyle[q] = `${U}px`, this.startStyle.height = `${j}px`, this.startStyle.display = "block";
    const re = Math.floor(this.settings.border.width / 2);
    this.bottomStyle.top = `${z + j - re}px`, this.bottomStyle[q] = `${U}px`, this.bottomStyle.width = `${G}px`, this.bottomStyle.display = "block", this.endStyle.top = `${z}px`, this.endStyle[q] = `${U + G - re}px`, this.endStyle.height = `${j + 1}px`, this.endStyle.display = "block";
    let me = this.settings.border.cornerVisible;
    me = typeof me == "function" ? me(this.settings.layerLevel) : me;
    const Me = this.wot.getSetting("onModifyGetCellCoords", s, l);
    let [nt, Ne] = [s, l];
    if (Me && Array.isArray(Me) && ([, , nt, Ne] = Me), Yo() || !me || this.isPartRange(nt, Ne))
      this.cornerStyle.display = "none";
    else {
      this.cornerStyle.top = `${z + j + this.cornerCenterPointOffset - 1}px`, this.cornerStyle[q] = `${U + G + this.cornerCenterPointOffset - 1}px`, this.cornerStyle.borderRightWidth = this.cornerDefaultStyle.borderWidth, this.cornerStyle.width = this.cornerDefaultStyle.width, this.cornerStyle.display = "none";
      let Ce = tl(n.TABLE);
      const pe = Ce === r;
      pe && (Ce = i.documentElement);
      const wt = parseInt(this.cornerDefaultStyle.width, 10) / 2, f = parseInt(this.cornerDefaultStyle.height, 10) / 2;
      if (l === this.wot.getSetting("totalColumns") - 1) {
        const p = pe ? w.getBoundingClientRect().left : w.offsetLeft;
        let A = !1, M = 0;
        $ ? (M = p - parseInt(this.cornerDefaultStyle.width, 10) / 2, A = M < 0) : (M = p + It(w) + parseInt(this.cornerDefaultStyle.width, 10) / 2, A = M >= ib(Ce)), A && (this.cornerStyle[q] = `${Math.floor(U + G + this.cornerCenterPointOffset - wt)}px`, this.cornerStyle[$ ? "borderLeftWidth" : "borderRightWidth"] = 0);
      }
      s === this.wot.getSetting("totalRows") - 1 && (pe ? w.getBoundingClientRect().top : w.offsetTop) + Zt(w) + parseInt(this.cornerDefaultStyle.height, 10) / 2 >= Ya(Ce) && (this.cornerStyle.top = `${Math.floor(z + j + this.cornerCenterPointOffset - f)}px`, this.cornerStyle.borderBottomWidth = 0), this.cornerStyle.display = "block";
    }
    Yo() && this.instance.getSetting("isDataViewInstance") && this.updateMultipleSelectionHandlesPosition(s, l, z, U, G, j);
  }
  /**
   * Check whether an entire column of cells is selected.
   *
   * @private
   * @param {number} startRowIndex Start row index.
   * @param {number} endRowIndex End row index.
   * @returns {boolean}
   */
  isEntireColumnSelected(e, n) {
    return e === this.wot.wtTable.getFirstRenderedRow() && n === this.wot.wtTable.getLastRenderedRow();
  }
  /**
   * Check whether an entire row of cells is selected.
   *
   * @private
   * @param {number} startColumnIndex Start column index.
   * @param {number} endColumnIndex End column index.
   * @returns {boolean}
   */
  isEntireRowSelected(e, n) {
    return e === this.wot.wtTable.getFirstRenderedColumn() && n === this.wot.wtTable.getLastRenderedColumn();
  }
  /**
   * Get left/top index and width/height depending on the `direction` provided.
   *
   * @private
   * @param {string} direction `rows` or `columns`, defines if an entire column or row is selected.
   * @param {number} fromIndex Start index of the selection.
   * @param {number} toIndex End index of the selection.
   * @param {number} headerIndex The header index as negative value.
   * @param {number} containerOffset Offset of the container.
   * @returns {Array|boolean} Returns an array of [headerElement, left, width] or [headerElement, top, height], depending on `direction` (`false` in case of an error getting the headers).
   */
  getDimensionsFromHeader(e, n, i, r, o) {
    const {
      wtTable: s
    } = this.wot, a = s.wtRootElement.parentNode;
    let l = null, u = null, c = null, h = null, d = null, g = null, m = null, w = null;
    switch (e) {
      case "rows":
        l = function() {
          return s.getRowHeader(...arguments);
        }, u = function() {
          return Zt(...arguments);
        }, c = "ht__selection--rows", g = "top";
        break;
      case "columns":
        l = function() {
          return s.getColumnHeader(...arguments);
        }, u = function() {
          return It(...arguments);
        }, c = "ht__selection--columns", g = "left";
        break;
    }
    if (a.classList.contains(c)) {
      const C = this.wot.getSetting("columnHeaders").length;
      if (m = l(n, C - r), w = l(i, C - r), !m || !w)
        return !1;
      const S = gn(m), b = gn(w);
      return m && w && (h = S[g] - o[g] - 1, d = b[g] + u(w) - S[g]), [m, h, d];
    }
    return !1;
  }
  /**
   * Change border style.
   *
   * @private
   * @param {string} borderElement Coordinate where add/remove border: top, bottom, start, end.
   * @param {object} border The border object descriptor.
   */
  changeBorderStyle(e, n) {
    const i = this[e].style, r = n[e];
    !r || r.hide ? Q(this[e], "hidden") : (Te(this[e], "hidden") && xe(this[e], "hidden"), i.backgroundColor = r.color, (e === "top" || e === "bottom") && (i.height = `${r.width}px`), (e === "start" || e === "end") && (i.width = `${r.width}px`));
  }
  /**
   * Change border style to default.
   *
   * @private
   * @param {string} position The position type ("top", "bottom", "start", "end") to change.
   */
  changeBorderToDefaultStyle(e) {
    const n = {
      width: 1,
      color: "#000"
    }, i = this[e].style;
    i.backgroundColor = n.color, i.width = `${n.width}px`, i.height = `${n.width}px`;
  }
  /**
   * Toggle class 'hidden' to element.
   *
   * @private
   * @param {string} borderElement Coordinate where add/remove border: top, bottom, start, end.
   * @param {boolean} [remove] Defines type of the action to perform.
   */
  toggleHiddenClass(e, n) {
    this.changeBorderToDefaultStyle(e), n ? Q(this[e], "hidden") : xe(this[e], "hidden");
  }
  /**
   * Hide border.
   */
  disappear() {
    this.topStyle.display = "none", this.bottomStyle.display = "none", this.startStyle.display = "none", this.endStyle.display = "none", this.cornerStyle.display = "none", Yo() && this.instance.getSetting("isDataViewInstance") && (this.selectionHandles.styles.top.display = "none", this.selectionHandles.styles.topHitArea.display = "none", this.selectionHandles.styles.bottom.display = "none", this.selectionHandles.styles.bottomHitArea.display = "none");
  }
  /**
   * Cleans up all the DOM state related to a Border instance. Call this prior to deleting a Border instance.
   */
  destroy() {
    this.eventManager.destroyWithOwnEventsOnly(), this.main.parentNode.removeChild(this.main);
  }
}
const nT = V3;
function W3(t, e) {
  Xx(t, e), e.add(t);
}
function Il(t, e, n) {
  Xx(t, e), e.set(t, n);
}
function Xx(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function je(t, e) {
  return t.get(kb(t, e));
}
function iT(t, e, n) {
  return t.set(kb(t, e), n), n;
}
function kb(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Nn = /* @__PURE__ */ new WeakMap(), li = /* @__PURE__ */ new WeakMap(), cC = /* @__PURE__ */ new WeakMap(), Zc = /* @__PURE__ */ new WeakMap(), hC = /* @__PURE__ */ new WeakMap(), aa = /* @__PURE__ */ new WeakMap(), rT = /* @__PURE__ */ new WeakSet();
class $3 {
  constructor(e) {
    W3(this, rT), Il(this, Nn, void 0), Il(this, li, void 0), Il(this, cC, new F3()), Il(this, Zc, /* @__PURE__ */ new WeakMap()), Il(this, hC, /* @__PURE__ */ new WeakSet()), Il(this, aa, /* @__PURE__ */ new Map()), iT(li, this, e);
  }
  /**
   * Sets the active Walkontable instance.
   *
   * @param {Walkontable} activeWot The overlays or master Walkontable instance.
   * @returns {SelectionManager}
   */
  setActiveOverlay(e) {
    return iT(Nn, this, e), je(cC, this).setActiveOverlay(je(Nn, this)), je(Zc, this).has(je(Nn, this)) || je(Zc, this).set(je(Nn, this), /* @__PURE__ */ new Set()), this;
  }
  /**
   * Gets the Selection instance of the "focus" type.
   *
   * @returns {Selection|null}
   */
  getFocusSelection() {
    return je(li, this) !== null ? je(li, this).getFocus() : null;
  }
  /**
   * Gets the Selection instance of the "area" type.
   *
   * @returns {Selection|null}
   */
  getAreaSelection() {
    return je(li, this) !== null ? je(li, this).createLayeredArea() : null;
  }
  /**
   * Gets the Border instance associated with Selection instance.
   *
   * @param {Selection} selection The selection instance.
   * @returns {Border|null} Returns the Border instance (new for each overlay Walkontable instance).
   */
  getBorderInstance(e) {
    if (!e.settings.border)
      return null;
    if (je(aa, this).has(e)) {
      const i = je(aa, this).get(e);
      if (i.has(je(Nn, this)))
        return i.get(je(Nn, this));
      const r = new nT(je(Nn, this), e.settings);
      return i.set(je(Nn, this), r), r;
    }
    const n = new nT(je(Nn, this), e.settings);
    return je(aa, this).set(e, /* @__PURE__ */ new Map([[je(Nn, this), n]])), n;
  }
  /**
   * Gets all Border instances associated with Selection instance for all overlays.
   *
   * @param {Selection} selection The selection instance.
   * @returns {Border[]}
   */
  getBorderInstances(e) {
    var n, i;
    return Array.from((n = (i = je(aa, this).get(e)) === null || i === void 0 ? void 0 : i.values()) !== null && n !== void 0 ? n : []);
  }
  /**
   * Destroys the Border instance associated with Selection instance.
   *
   * @param {Selection} selection The selection instance.
   */
  destroyBorders(e) {
    je(aa, this).get(e).forEach((n) => n.destroy()), je(aa, this).delete(e);
  }
  /**
   * Renders all the selections (add CSS classes to cells and draw borders).
   *
   * @param {boolean} fastDraw Indicates the render cycle type (fast/slow).
   */
  render(e) {
    if (je(li, this) === null)
      return;
    e && kb(rT, this, U3).call(this);
    const n = Array.from(je(li, this)), i = /* @__PURE__ */ new Map(), r = /* @__PURE__ */ new Map();
    for (let o = 0; o < n.length; o++) {
      const s = n[o], {
        className: a,
        headerAttributes: l,
        createLayers: u,
        selectionType: c
      } = s.settings;
      je(hC, this).has(s) || (je(hC, this).add(s), s.addLocalHook("destroy", () => this.destroyBorders(s)));
      const h = this.getBorderInstance(s);
      if (s.isEmpty()) {
        h == null || h.disappear();
        continue;
      }
      a && je(cC, this).setActiveSelection(s).scan().forEach((m) => {
        if (i.has(m)) {
          const w = i.get(m);
          w.has(a) && u === !0 ? w.set(a, w.get(a) + 1) : w.set(a, 1);
        } else
          i.set(m, /* @__PURE__ */ new Map([[a, 1]]));
        l && (r.has(m) || r.set(m, []), m.nodeName === "TH" && r.get(m).push(...l));
      });
      const d = s.getCorners();
      je(Nn, this).getSetting("onBeforeDrawBorders", d, c), h == null || h.appear(d);
    }
    i.forEach((o, s) => {
      var a;
      const l = Array.from(o).map((u) => {
        let [c, h] = u;
        return h === 1 ? c : [c, ...Array.from({
          length: h - 1
        }, (d, g) => `${c}-${g + 1}`)];
      }).flat();
      l.forEach((u) => je(Zc, this).get(je(Nn, this)).add(u)), Q(s, l), s.nodeName === "TD" && Array.isArray((a = je(li, this).options) === null || a === void 0 ? void 0 : a.cellAttributes) && Ee(s, je(li, this).options.cellAttributes);
    }), Array.from(r.keys()).forEach((o) => {
      Ee(o, [...r.get(o)]);
    });
  }
}
function U3() {
  const t = je(Zc, this).get(je(Nn, this)), e = je(Nn, this).wtSettings.getSetting("onBeforeRemoveCellClassNames");
  if (Array.isArray(e))
    for (let n = 0; n < e.length; n++)
      t.add(e[n]);
  t.forEach((n) => {
    var i, r;
    const o = je(Nn, this).wtTable.TABLE.querySelectorAll(`.${n}`);
    let s = [];
    Array.isArray((i = je(li, this).options) === null || i === void 0 ? void 0 : i.cellAttributes) && (s = je(li, this).options.cellAttributes.map((a) => a[0])), Array.isArray((r = je(li, this).options) === null || r === void 0 ? void 0 : r.headerAttributes) && (s = [...s, ...je(li, this).options.headerAttributes.map((a) => a[0])]);
    for (let a = 0, l = o.length; a < l; a++)
      xe(o[a], n), ml(o[a], s);
  }), t.clear();
}
class Y3 extends Ed {
  /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   */
  constructor(e, n, i, r) {
    super(e, n, il, i, r);
  }
  /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {InlineStartOverlayTable}
   */
  createTable() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    return new P3(...n);
  }
  /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */
  shouldBeRendered() {
    return this.wtSettings.getSetting("shouldRenderInlineStartOverlay");
  }
  /**
   * Updates the left overlay position.
   *
   * @returns {boolean}
   */
  resetFixedPosition() {
    const {
      wtTable: e
    } = this.wot;
    if (!this.needFullRender || !this.shouldBeRendered() || !e.holder.parentNode)
      return !1;
    const {
      rootWindow: n
    } = this.domBindings, i = this.clone.wtTable.holder.parentNode, r = this.wtSettings.getSetting("preventOverflow");
    let o = 0;
    this.trimmingContainer === n && (!r || r !== "horizontal") ? (o = this.getOverlayOffset() * (this.isRtl() ? -1 : 1), Tp(i, `${o}px`, "0px")) : (o = this.getScrollPosition(), vd(i));
    const s = this.adjustHeaderBordersPosition(o);
    return this.adjustElementsSize(), s;
  }
  /**
   * Sets the main overlay's horizontal scroll position.
   *
   * @param {number} pos The scroll position.
   * @returns {boolean}
   */
  setScrollPosition(e) {
    const {
      rootWindow: n
    } = this.domBindings;
    let i = !1;
    return this.isRtl() && (e = -e), this.mainTableScrollableElement === n && n.scrollX !== e ? (n.scrollTo(e, tb(n)), i = !0) : this.mainTableScrollableElement.scrollLeft !== e && (this.mainTableScrollableElement.scrollLeft = e, i = !0), i;
  }
  /**
   * Triggers onScroll hook callback.
   */
  onScroll() {
    this.wtSettings.getSetting("onScrollVertically");
  }
  /**
   * Calculates total sum cells width.
   *
   * @param {number} from Column index which calculates started from.
   * @param {number} to Column index where calculation is finished.
   * @returns {number} Width sum.
   */
  sumCellSizes(e, n) {
    const i = this.wtSettings.getSetting("defaultColumnWidth");
    let r = e, o = 0;
    for (; r < n; )
      o += this.wot.wtTable.getStretchedColumnWidth(r) || i, r += 1;
    return o;
  }
  /**
   * Adjust overlay root element, children and master table element sizes (width, height).
   */
  adjustElementsSize() {
    this.updateTrimmingContainer(), this.needFullRender && (this.adjustRootElementSize(), this.adjustRootChildrenSize());
  }
  /**
   * Adjust overlay root element size (width and height).
   */
  adjustRootElementSize() {
    const {
      wtTable: e
    } = this.wot, {
      rootDocument: n,
      rootWindow: i
    } = this.domBindings, r = pn(n), s = this.clone.wtTable.holder.parentNode.style, a = this.wtSettings.getSetting("preventOverflow");
    if (this.trimmingContainer !== i || a === "vertical") {
      let u = this.wot.wtViewport.getWorkspaceHeight();
      this.wot.wtOverlays.hasScrollbarBottom && (u -= r), u = Math.min(u, e.wtRootElement.scrollHeight), s.height = `${u}px`;
    } else
      s.height = "";
    this.clone.wtTable.holder.style.height = s.height;
    const l = It(this.clone.wtTable.TABLE);
    s.width = `${l}px`;
  }
  /**
   * Adjust overlay root childs size.
   */
  adjustRootChildrenSize() {
    const {
      holder: e
    } = this.clone.wtTable, n = this.wot.selectionManager.getFocusSelection() ? parseInt(Db.width, 10) / 2 : 0;
    this.clone.wtTable.hider.style.height = this.hider.style.height, e.style.height = e.parentNode.style.height, e.style.width = `${parseInt(e.parentNode.style.width, 10) + n}px`;
  }
  /**
   * Adjust the overlay dimensions and position.
   */
  applyToDOM() {
    const e = this.wtSettings.getSetting("totalColumns"), n = this.isRtl() ? "right" : "left";
    if (typeof this.wot.wtViewport.columnsRenderCalculator.startPosition == "number")
      this.spreader.style[n] = `${this.wot.wtViewport.columnsRenderCalculator.startPosition}px`;
    else if (e === 0)
      this.spreader.style[n] = "0";
    else
      throw new Error("Incorrect value of the columnsRenderCalculator");
    this.isRtl() ? this.spreader.style.left = "" : this.spreader.style.right = "", this.needFullRender && this.syncOverlayOffset();
  }
  /**
   * Synchronize calculated top position to an element.
   */
  syncOverlayOffset() {
    typeof this.wot.wtViewport.rowsRenderCalculator.startPosition == "number" ? this.clone.wtTable.spreader.style.top = `${this.wot.wtViewport.rowsRenderCalculator.startPosition}px` : this.clone.wtTable.spreader.style.top = "";
  }
  /**
   * Scrolls horizontally to a column at the left edge of the viewport.
   *
   * @param {number} sourceCol  Column index which you want to scroll to.
   * @param {boolean} [beyondRendered]  If `true`, scrolls according to the right
   *                                    edge (left edge is by default).
   * @returns {boolean}
   */
  scrollTo(e, n) {
    const {
      wtSettings: i
    } = this, r = i.getSetting("rowHeaders"), o = i.getSetting("fixedColumnsStart"), a = (this.wot.cloneSource ? this.wot.cloneSource : this.wot).wtTable.holder, l = o === 0 && r.length > 0 && !Te(a.parentNode, "innerBorderInlineStart") ? 1 : 0;
    let u = this.getTableParentOffset(), c = 0;
    if (n) {
      const h = this.wot.wtTable.getColumnWidth(e), d = this.wot.wtViewport.getViewportWidth();
      h > d && (n = !1);
    }
    return n && a.offsetWidth !== a.clientWidth && (c = pn(this.domBindings.rootDocument)), n ? (u += this.sumCellSizes(0, e + 1), u -= this.wot.wtViewport.getViewportWidth(), u += l) : u += this.sumCellSizes(this.wtSettings.getSetting("fixedColumnsStart"), e), u += c, UP(this.mainTableScrollableElement) === u - l && l > 0 && this.wot.wtOverlays.expandHiderHorizontallyBy(l), this.setScrollPosition(u);
  }
  /**
   * Gets table parent left position.
   *
   * @returns {number}
   */
  getTableParentOffset() {
    const e = this.wtSettings.getSetting("preventOverflow");
    let n = 0;
    return !e && this.trimmingContainer === this.domBindings.rootWindow && (n = this.wot.wtTable.holderOffset.left), n;
  }
  /**
   * Gets the main overlay's horizontal scroll position.
   *
   * @returns {number} Main table's horizontal scroll position.
   */
  getScrollPosition() {
    return Math.abs(nb(this.mainTableScrollableElement, this.domBindings.rootWindow));
  }
  /**
   * Gets the main overlay's horizontal overlay offset.
   *
   * @returns {number} Main table's horizontal overlay offset.
   */
  getOverlayOffset() {
    const {
      rootWindow: e
    } = this.domBindings, n = this.wtSettings.getSetting("preventOverflow");
    let i = 0;
    if (this.trimmingContainer === e && (!n || n !== "horizontal")) {
      this.isRtl() ? i = Math.abs(Math.min(this.getTableParentOffset() - this.getScrollPosition(), 0)) : i = Math.max(this.getScrollPosition() - this.getTableParentOffset(), 0);
      const r = this.wot.wtTable.getTotalWidth(), o = this.clone.wtTable.getTotalWidth(), s = r - o;
      i > s && (i = 0);
    }
    return i;
  }
  /**
   * Adds css classes to hide the header border's header (cell-selection border hiding issue).
   *
   * @param {number} position Header X position if trimming container is window or scroll top if not.
   * @returns {boolean}
   */
  adjustHeaderBordersPosition(e) {
    const {
      wtSettings: n
    } = this, i = this.wot.wtTable.holder.parentNode, r = n.getSetting("rowHeaders"), o = n.getSetting("fixedColumnsStart"), s = n.getSetting("totalRows"), a = n.getSetting("preventOverflow") === "vertical";
    s ? xe(i, "emptyRows") : Q(i, "emptyRows");
    let l = !1;
    if (!a) {
      if (o && !r.length)
        Q(i, "innerBorderLeft innerBorderInlineStart");
      else if (!o && r.length) {
        const u = Te(i, "innerBorderInlineStart");
        e ? (Q(i, "innerBorderLeft innerBorderInlineStart"), l = !u) : (xe(i, "innerBorderLeft innerBorderInlineStart"), l = u);
      }
    }
    return l;
  }
}
const j3 = "stickyRowsTop", qx = {
  /**
   * Get the source index of the first rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getFirstRenderedRow() {
    return this.wtSettings.getSetting("totalRows") === 0 ? -1 : 0;
  },
  /**
   * Get the source index of the first row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getFirstVisibleRow() {
    return this.getFirstRenderedRow();
  },
  /**
   * Get the source index of the first row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getFirstPartiallyVisibleRow() {
    return this.getFirstRenderedRow();
  },
  /**
   * Get the source index of the last rendered row. If no rows are rendered, returns an error code: -1.
   *
   * @returns {number}
   * @this Table
   */
  getLastRenderedRow() {
    return this.getRenderedRowsCount() - 1;
  },
  /**
   * Get the source index of the last row fully visible in the viewport. If no rows are fully visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getLastVisibleRow() {
    return this.getLastRenderedRow();
  },
  /**
   * Get the source index of the last row partially visible in the viewport. If no rows are partially visible, returns an error code: -1.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getLastPartiallyVisibleRow() {
    return this.getLastRenderedRow();
  },
  /**
   * Get the number of rendered rows.
   *
   * @returns {number}
   * @this Table
   */
  getRenderedRowsCount() {
    const t = this.wtSettings.getSetting("totalRows");
    return Math.min(this.wtSettings.getSetting("fixedRowsTop"), t);
  },
  /**
   * Get the number of fully visible rows in the viewport.
   * Assumes that all rendered rows are fully visible.
   *
   * @returns {number}
   * @this Table
   */
  getVisibleRowsCount() {
    return this.getRenderedRowsCount();
  },
  /**
   * Get the number of rendered column headers.
   *
   * @returns {number}
   * @this Table
   */
  getColumnHeadersCount() {
    return this.dataAccessObject.columnHeaders.length;
  }
};
wo(qx, "MIXIN_NAME", j3, {
  writable: !1,
  enumerable: !1
});
const Zx = qx;
class Lb extends Bu {
  /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */
  constructor(e, n, i, r) {
    super(e, n, i, r, rl);
  }
}
pt(Lb, Zx);
pt(Lb, Tb);
const G3 = Lb;
function oT(t, e, n) {
  return (e = z3(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function z3(t) {
  var e = K3(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function K3(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Q3 extends Ed {
  /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   * @param {TopOverlay} topOverlay The instance of the Top overlay.
   * @param {InlineStartOverlay} inlineStartOverlay The instance of the InlineStart overlay.
   */
  constructor(e, n, i, r, o, s) {
    super(e, n, rl, i, r), oT(this, "topOverlay", void 0), oT(this, "inlineStartOverlay", void 0), this.topOverlay = o, this.inlineStartOverlay = s;
  }
  /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {TopInlineStartCornerOverlayTable}
   */
  createTable() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    return new G3(...n);
  }
  /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */
  shouldBeRendered() {
    return this.wtSettings.getSetting("shouldRenderTopOverlay") && this.wtSettings.getSetting("shouldRenderInlineStartOverlay");
  }
  /**
   * Updates the corner overlay position.
   *
   * @returns {boolean}
   */
  resetFixedPosition() {
    if (this.updateTrimmingContainer(), !this.wot.wtTable.holder.parentNode)
      return !1;
    const e = this.clone.wtTable.holder.parentNode;
    if (this.trimmingContainer === this.domBindings.rootWindow) {
      const r = this.inlineStartOverlay.getOverlayOffset() * (this.isRtl() ? -1 : 1), o = this.topOverlay.getOverlayOffset();
      Tp(e, `${r}px`, `${o}px`);
    } else
      vd(e);
    let n = Zt(this.clone.wtTable.TABLE);
    const i = It(this.clone.wtTable.TABLE);
    return this.wot.wtTable.hasDefinedSize() || (n = 0), e.style.height = `${n}px`, e.style.width = `${i}px`, !1;
  }
}
class Bb extends Bu {
  /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */
  constructor(e, n, i, r) {
    super(e, n, i, r, nl);
  }
}
pt(Bb, Zx);
pt(Bb, Ob);
const X3 = Bb;
function q3(t, e, n) {
  return (e = Z3(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Z3(t) {
  var e = J3(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function J3(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class eX extends Ed {
  /**
   * @param {Walkontable} wotInstance The Walkontable instance. @TODO refactoring: check if can be deleted.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {DomBindings} domBindings Dom elements bound to the current instance.
   */
  constructor(e, n, i, r) {
    super(e, n, nl, i, r), q3(this, "cachedFixedRowsTop", -1), this.cachedFixedRowsTop = this.wtSettings.getSetting("fixedRowsTop");
  }
  /**
   * Factory method to create a subclass of `Table` that is relevant to this overlay.
   *
   * @see Table#constructor
   * @param {...*} args Parameters that will be forwarded to the `Table` constructor.
   * @returns {TopOverlayTable}
   */
  createTable() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    return new X3(...n);
  }
  /**
   * Checks if overlay should be fully rendered.
   *
   * @returns {boolean}
   */
  shouldBeRendered() {
    return this.wtSettings.getSetting("shouldRenderTopOverlay");
  }
  /**
   * Updates the top overlay position.
   *
   * @returns {boolean}
   */
  resetFixedPosition() {
    if (!this.needFullRender || !this.shouldBeRendered() || !this.wot.wtTable.holder.parentNode)
      return !1;
    const e = this.clone.wtTable.holder.parentNode, {
      rootWindow: n
    } = this.domBindings, i = this.wtSettings.getSetting("preventOverflow");
    let r = 0, o = !1;
    if (this.trimmingContainer === n && (!i || i !== "vertical")) {
      const {
        wtTable: a
      } = this.wot, l = a.hider.getBoundingClientRect(), u = Math.ceil(l.bottom), c = e.offsetHeight;
      o = u === c, r = this.getOverlayOffset(), Tp(e, "0px", `${r}px`);
    } else
      r = this.getScrollPosition(), vd(e);
    const s = this.adjustHeaderBordersPosition(r, o);
    return this.adjustElementsSize(), s;
  }
  /**
   * Sets the main overlay's vertical scroll position.
   *
   * @param {number} pos The scroll position.
   * @returns {boolean}
   */
  setScrollPosition(e) {
    const n = this.domBindings.rootWindow;
    let i = !1;
    return this.mainTableScrollableElement === n && n.scrollY !== e ? (n.scrollTo(Rp(n), e), i = !0) : this.mainTableScrollableElement.scrollTop !== e && (this.mainTableScrollableElement.scrollTop = e, i = !0), i;
  }
  /**
   * Triggers onScroll hook callback.
   */
  onScroll() {
    this.wtSettings.getSetting("onScrollHorizontally");
  }
  /**
   * Calculates total sum cells height.
   *
   * @param {number} from Row index which calculates started from.
   * @param {number} to Row index where calculation is finished.
   * @returns {number} Height sum.
   */
  sumCellSizes(e, n) {
    const i = this.wtSettings.getSetting("defaultRowHeight");
    let r = e, o = 0;
    for (; r < n; ) {
      const s = this.wot.wtTable.getRowHeight(r);
      o += s === void 0 ? i : s, r += 1;
    }
    return o;
  }
  /**
   * Adjust overlay root element, children and master table element sizes (width, height).
   */
  adjustElementsSize() {
    this.updateTrimmingContainer(), this.needFullRender && (this.adjustRootElementSize(), this.adjustRootChildrenSize());
  }
  /**
   * Adjust overlay root element size (width and height).
   */
  adjustRootElementSize() {
    const {
      wtTable: e
    } = this.wot, {
      rootDocument: n,
      rootWindow: i
    } = this.domBindings, r = pn(n), s = this.clone.wtTable.holder.parentNode.style, a = this.wtSettings.getSetting("preventOverflow");
    if (this.trimmingContainer !== i || a === "horizontal") {
      let u = this.wot.wtViewport.getWorkspaceWidth();
      this.wot.wtOverlays.hasScrollbarRight && (u -= r), u = Math.min(u, e.wtRootElement.scrollWidth), s.width = `${u}px`;
    } else
      s.width = "";
    this.clone.wtTable.holder.style.width = s.width;
    let l = Zt(this.clone.wtTable.TABLE);
    this.wot.wtTable.hasDefinedSize() || (l = 0), s.height = `${l}px`;
  }
  /**
   * Adjust overlay root childs size.
   */
  adjustRootChildrenSize() {
    const {
      holder: e
    } = this.clone.wtTable, n = this.wot.selectionManager.getFocusSelection() ? parseInt(Db.height, 10) / 2 : 0;
    this.clone.wtTable.hider.style.width = this.hider.style.width, e.style.width = e.parentNode.style.width, e.style.height = `${parseInt(e.parentNode.style.height, 10) + n}px`;
  }
  /**
   * Adjust the overlay dimensions and position.
   */
  applyToDOM() {
    const e = this.wtSettings.getSetting("totalRows");
    if (typeof this.wot.wtViewport.rowsRenderCalculator.startPosition == "number")
      this.spreader.style.top = `${this.wot.wtViewport.rowsRenderCalculator.startPosition}px`;
    else if (e === 0)
      this.spreader.style.top = "0";
    else
      throw new Error("Incorrect value of the rowsRenderCalculator");
    this.spreader.style.bottom = "", this.needFullRender && this.syncOverlayOffset();
  }
  /**
   * Synchronize calculated left position to an element.
   */
  syncOverlayOffset() {
    const e = this.isRtl() ? "right" : "left", {
      spreader: n
    } = this.clone.wtTable;
    typeof this.wot.wtViewport.columnsRenderCalculator.startPosition == "number" ? n.style[e] = `${this.wot.wtViewport.columnsRenderCalculator.startPosition}px` : n.style[e] = "";
  }
  /**
   * Scrolls vertically to a row.
   *
   * @param {number} sourceRow Row index which you want to scroll to.
   * @param {boolean} [bottomEdge] If `true`, scrolls according to the bottom edge (top edge is by default).
   * @returns {boolean}
   */
  scrollTo(e, n) {
    const {
      wot: i,
      wtSettings: r
    } = this, s = (i.cloneSource ? i.cloneSource : i).wtTable.holder, a = r.getSetting("columnHeaders"), u = r.getSetting("fixedRowsTop") === 0 && a.length > 0 && !Te(s.parentNode, "innerBorderTop") ? 1 : 0;
    let c = this.getTableParentOffset(), h = 0;
    if (n) {
      const d = this.wot.wtTable.getRowHeight(e), g = this.wot.wtViewport.getViewportHeight();
      d > g && (n = !1);
    }
    if (n && s.offsetHeight !== s.clientHeight && (h = pn(this.domBindings.rootDocument)), n) {
      const d = r.getSetting("fixedRowsBottom"), g = r.getSetting("totalRows");
      c += this.sumCellSizes(0, e + 1), c -= i.wtViewport.getViewportHeight() - this.sumCellSizes(g - d, g), c += 1, c += u;
    } else
      c += this.sumCellSizes(r.getSetting("fixedRowsTop"), e);
    return c += h, $P(this.mainTableScrollableElement) === c - u && u > 0 && this.wot.wtOverlays.expandHiderVerticallyBy(u), this.setScrollPosition(c);
  }
  /**
   * Gets table parent top position.
   *
   * @returns {number}
   */
  getTableParentOffset() {
    return this.mainTableScrollableElement === this.domBindings.rootWindow ? this.wot.wtTable.holderOffset.top : 0;
  }
  /**
   * Gets the main overlay's vertical scroll position.
   *
   * @returns {number} Main table's vertical scroll position.
   */
  getScrollPosition() {
    return Ep(this.mainTableScrollableElement, this.domBindings.rootWindow);
  }
  /**
   * Gets the main overlay's vertical overlay offset.
   *
   * @returns {number} Main table's vertical overlay offset.
   */
  getOverlayOffset() {
    const {
      rootWindow: e
    } = this.domBindings, n = this.wtSettings.getSetting("preventOverflow");
    let i = 0;
    if (this.trimmingContainer === e && (!n || n !== "vertical")) {
      const r = this.wot.wtTable.getTotalHeight(), o = this.clone.wtTable.getTotalHeight(), s = r - o;
      i = Math.max(this.getScrollPosition() - this.getTableParentOffset(), 0), i > s && (i = 0);
    }
    return i;
  }
  /**
   * Adds css classes to hide the header border's header (cell-selection border hiding issue).
   *
   * @param {number} position Header Y position if trimming container is window or scroll top if not.
   * @param {boolean} [skipInnerBorderAdjusting=false] If `true` the inner border adjusting will be skipped.
   * @returns {boolean}
   */
  adjustHeaderBordersPosition(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    const {
      wtSettings: i
    } = this, r = this.wot.wtTable.holder.parentNode, o = i.getSetting("totalColumns"), s = i.getSetting("preventOverflow") === "horizontal";
    o ? xe(r, "emptyColumns") : Q(r, "emptyColumns");
    let a = !1;
    if (!n && !s) {
      const l = i.getSetting("fixedRowsTop"), u = this.cachedFixedRowsTop !== l, c = i.getSetting("columnHeaders");
      if ((u || l === 0) && c.length > 0) {
        const h = Te(r, "innerBorderTop");
        this.cachedFixedRowsTop = i.getSetting("fixedRowsTop"), e || i.getSetting("totalRows") === 0 ? (Q(r, "innerBorderTop"), a = !h) : (xe(r, "innerBorderTop"), a = h);
      }
    }
    return a;
  }
}
function Af(t, e, n) {
  tX(t, e), e.set(t, n);
}
function tX(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function yo(t, e, n) {
  return (e = nX(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function nX(t) {
  var e = iX(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function iX(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function la(t, e, n) {
  return t.set(Jx(t, e), n), n;
}
function So(t, e) {
  return t.get(Jx(t, e));
}
function Jx(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Ol = /* @__PURE__ */ new WeakMap(), Cf = /* @__PURE__ */ new WeakMap(), Pl = /* @__PURE__ */ new WeakMap(), vf = /* @__PURE__ */ new WeakMap();
class rX {
  /**
   * @param {Walkontable} wotInstance The Walkontable instance. @todo refactoring remove.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {EventManager} eventManager The walkontable event manager.
   * @param {MasterTable} wtTable The master table.
   */
  constructor(e, n, i, r, o, s) {
    yo(this, "wot", null), Af(this, Ol, []), yo(this, "topOverlay", null), yo(this, "bottomOverlay", null), yo(this, "inlineStartOverlay", null), yo(this, "topInlineStartCornerOverlay", null), yo(this, "bottomInlineStartCornerOverlay", null), yo(this, "browserLineHeight", void 0), yo(this, "wtSettings", null), Af(this, Cf, !1), Af(this, Pl, 0), Af(this, vf, null), yo(this, "resizeObserver", new ResizeObserver((c) => {
      xp(() => {
        !Array.isArray(c) || !c.length || (la(Pl, this, So(Pl, this) + 1), So(Pl, this) === 100 && (gt("The ResizeObserver callback was fired too many times in direct succession.\nThis may be due to an infinite loop caused by setting a dynamic height/width (for example, with the `dvh` units) to a Handsontable container's parent. \nThe observer will be disconnected."), this.resizeObserver.disconnect()), So(vf, this) !== null && clearTimeout(So(vf, this)), la(vf, this, setTimeout(() => {
          la(Pl, this, 0);
        }, 100)), this.wtSettings.getSetting("onContainerElementResize"));
      });
    })), this.wot = e, this.wtSettings = r, this.domBindings = i, this.facadeGetter = n, this.wtTable = s;
    const {
      rootDocument: a,
      rootWindow: l
    } = this.domBindings;
    this.instance = this.wot, this.eventManager = o, this.scrollbarSize = pn(a);
    const u = l.getComputedStyle(s.wtRootElement.parentNode).getPropertyValue("overflow") === "hidden";
    this.scrollableElement = u ? s.holder : Dh(s.TABLE), this.initOverlays(), this.hasScrollbarBottom = !1, this.hasScrollbarRight = !1, this.destroyed = !1, this.keyPressed = !1, this.spreaderLastSize = {
      width: null,
      height: null
    }, this.verticalScrolling = !1, this.horizontalScrolling = !1, this.initBrowserLineHeight(), this.registerListeners(), this.lastScrollX = l.scrollX, this.lastScrollY = l.scrollY;
  }
  /**
   * Get the list of references to all overlays.
   *
   * @param {boolean} [includeMaster = false] If set to `true`, the list will contain the master table as the last
   * element.
   * @returns {(TopOverlay|TopInlineStartCornerOverlay|InlineStartOverlay|BottomOverlay|BottomInlineStartCornerOverlay)[]}
   */
  getOverlays() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    const n = [...So(Ol, this)];
    return e && n.push(this.wtTable), n;
  }
  /**
   * Retrieve browser line height and apply its value to `browserLineHeight`.
   *
   * @private
   */
  initBrowserLineHeight() {
    const {
      rootWindow: e,
      rootDocument: n
    } = this.domBindings, i = e.getComputedStyle(n.body), r = parseInt(i.lineHeight, 10), o = parseInt(i.fontSize, 10) * 1.2;
    this.browserLineHeight = r || o;
  }
  /**
   * Prepare overlays based on user settings.
   *
   * @private
   */
  initOverlays() {
    const e = [this.wot, this.facadeGetter, this.wtSettings, this.domBindings];
    this.topOverlay = new eX(...e), this.bottomOverlay = new I3(...e), this.inlineStartOverlay = new Y3(...e), this.topInlineStartCornerOverlay = new Q3(...e, this.topOverlay, this.inlineStartOverlay), this.bottomInlineStartCornerOverlay = new S3(...e, this.bottomOverlay, this.inlineStartOverlay), la(Ol, this, [this.topOverlay, this.bottomOverlay, this.inlineStartOverlay, this.topInlineStartCornerOverlay, this.bottomInlineStartCornerOverlay]);
  }
  /**
   * Runs logic for the overlays before the table is drawn.
   */
  beforeDraw() {
    la(Cf, this, So(Ol, this).reduce((e, n) => n.hasRenderingStateChanged() || e, !1)), So(Ol, this).forEach((e) => e.updateStateOfRendering("before"));
  }
  /**
   * Runs logic for the overlays after the table is drawn.
   */
  afterDraw() {
    this.syncScrollWithMaster(), So(Ol, this).forEach((e) => {
      const n = e.hasRenderingStateChanged();
      e.updateStateOfRendering("after"), n && !e.needFullRender && e.reset();
    });
  }
  /**
   * Refresh and redraw table.
   */
  refreshAll() {
    if (this.wot.drawn) {
      if (!this.wtTable.holder.parentNode) {
        this.destroy();
        return;
      }
      this.wot.draw(!0), this.verticalScrolling && this.inlineStartOverlay.onScroll(), this.horizontalScrolling && this.topOverlay.onScroll(), this.verticalScrolling = !1, this.horizontalScrolling = !1;
    }
  }
  /**
   * Register all necessary event listeners.
   */
  registerListeners() {
    const {
      rootDocument: e,
      rootWindow: n
    } = this.domBindings, {
      mainTableScrollableElement: i
    } = this.topOverlay, {
      mainTableScrollableElement: r
    } = this.inlineStartOverlay;
    this.eventManager.addEventListener(e.documentElement, "keydown", (h) => this.onKeyDown(h)), this.eventManager.addEventListener(e.documentElement, "keyup", () => this.onKeyUp()), this.eventManager.addEventListener(e, "visibilitychange", () => this.onKeyUp()), this.eventManager.addEventListener(i, "scroll", (h) => this.onTableScroll(h), {
      passive: !0
    }), i !== r && this.eventManager.addEventListener(r, "scroll", (h) => this.onTableScroll(h), {
      passive: !0
    });
    const o = n.devicePixelRatio && n.devicePixelRatio > 1, s = this.scrollableElement === n, a = this.wtSettings.getSetting("preventWheel"), l = {
      passive: s
    };
    (a || o || !cb()) && this.eventManager.addEventListener(this.wtTable.wtRootElement, "wheel", (h) => this.onCloneWheel(h, a), l), [this.topOverlay, this.bottomOverlay, this.inlineStartOverlay, this.topInlineStartCornerOverlay, this.bottomInlineStartCornerOverlay].forEach((h) => {
      if (h && h.needFullRender) {
        const {
          holder: d
        } = h.clone.wtTable;
        this.eventManager.addEventListener(d, "wheel", (g) => this.onCloneWheel(g, a), l);
      }
    });
    let c;
    this.eventManager.addEventListener(n, "resize", () => {
      clearTimeout(c), c = setTimeout(() => {
        this.wtSettings.getSetting("onWindowResize"), la(Pl, this, 0);
      }, 200);
    }), s || this.resizeObserver.observe(this.wtTable.wtRootElement.parentElement);
  }
  /**
   * Deregister all previously registered listeners.
   */
  deregisterListeners() {
    this.eventManager.clearEvents(!0);
  }
  /**
   * Scroll listener.
   *
   * @param {Event} event The mouse event object.
   */
  onTableScroll(e) {
    const n = this.domBindings.rootWindow, i = this.inlineStartOverlay.mainTableScrollableElement, r = this.topOverlay.mainTableScrollableElement, o = e.target;
    this.keyPressed && (r !== n && o !== n && !e.target.contains(r) || i !== n && o !== n && !e.target.contains(i)) || this.syncScrollPositions(e);
  }
  /**
   * Wheel listener for cloned overlays.
   *
   * @param {Event} event The mouse event object.
   * @param {boolean} preventDefault If `true`, the `preventDefault` will be called on event object.
   */
  onCloneWheel(e, n) {
    const {
      rootWindow: i
    } = this.domBindings, r = this.inlineStartOverlay.mainTableScrollableElement, o = this.topOverlay.mainTableScrollableElement, s = e.target, a = o !== i && s !== i && !s.contains(o), l = r !== i && s !== i && !s.contains(r);
    if (this.keyPressed && (a || l) || this.scrollableElement === i)
      return;
    const u = this.translateMouseWheelToScroll(e);
    (n || this.scrollableElement !== i && u) && e.preventDefault();
  }
  /**
   * Key down listener.
   *
   * @param {Event} event The keyboard event object.
   */
  onKeyDown(e) {
    this.keyPressed = Lu(e.keyCode, "ARROW_UP|ARROW_RIGHT|ARROW_DOWN|ARROW_LEFT");
  }
  /**
   * Key up listener.
   */
  onKeyUp() {
    this.keyPressed = !1;
  }
  /**
   * Translate wheel event into scroll event and sync scroll overlays position.
   *
   * @private
   * @param {Event} event The mouse event object.
   * @returns {boolean}
   */
  translateMouseWheelToScroll(e) {
    let n = isNaN(e.deltaY) ? -1 * e.wheelDeltaY : e.deltaY, i = isNaN(e.deltaX) ? -1 * e.wheelDeltaX : e.deltaX;
    e.deltaMode === 1 && (i += i * this.browserLineHeight, n += n * this.browserLineHeight);
    const r = this.scrollVertically(n), o = this.scrollHorizontally(i);
    return r || o;
  }
  /**
   * Scrolls main scrollable element horizontally.
   *
   * @param {number} delta Relative value to scroll.
   * @returns {boolean}
   */
  scrollVertically(e) {
    const n = this.scrollableElement.scrollTop;
    return this.scrollableElement.scrollTop += e, n !== this.scrollableElement.scrollTop;
  }
  /**
   * Scrolls main scrollable element horizontally.
   *
   * @param {number} delta Relative value to scroll.
   * @returns {boolean}
   */
  scrollHorizontally(e) {
    const n = this.scrollableElement.scrollLeft;
    return this.scrollableElement.scrollLeft += e, n !== this.scrollableElement.scrollLeft;
  }
  /**
   * Synchronize scroll position between master table and overlay table.
   *
   * @private
   */
  syncScrollPositions() {
    if (this.destroyed)
      return;
    const {
      rootWindow: e
    } = this.domBindings, n = this.topOverlay.clone.wtTable.holder, i = this.inlineStartOverlay.clone.wtTable.holder, [r, o] = [this.scrollableElement.scrollLeft, this.scrollableElement.scrollTop];
    if (this.horizontalScrolling = n.scrollLeft !== r || this.lastScrollX !== e.scrollX, this.verticalScrolling = i.scrollTop !== o || this.lastScrollY !== e.scrollY, this.lastScrollX = e.scrollX, this.lastScrollY = e.scrollY, this.horizontalScrolling) {
      n.scrollLeft = r;
      const s = this.bottomOverlay.needFullRender ? this.bottomOverlay.clone.wtTable.holder : null;
      s && (s.scrollLeft = r);
    }
    this.verticalScrolling && (i.scrollTop = o), this.refreshAll();
  }
  /**
   * Synchronize overlay scrollbars with the master scrollbar.
   */
  syncScrollWithMaster() {
    if (!So(Cf, this))
      return;
    const e = this.topOverlay.mainTableScrollableElement, {
      scrollLeft: n,
      scrollTop: i
    } = e;
    this.topOverlay.needFullRender && (this.topOverlay.clone.wtTable.holder.scrollLeft = n), this.bottomOverlay.needFullRender && (this.bottomOverlay.clone.wtTable.holder.scrollLeft = n), this.inlineStartOverlay.needFullRender && (this.inlineStartOverlay.clone.wtTable.holder.scrollTop = i), la(Cf, this, !1);
  }
  /**
   * Update the main scrollable elements for all the overlays.
   */
  updateMainScrollableElements() {
    this.deregisterListeners(), this.inlineStartOverlay.updateMainScrollableElement(), this.topOverlay.updateMainScrollableElement(), this.bottomOverlay.needFullRender && this.bottomOverlay.updateMainScrollableElement();
    const {
      wtTable: e
    } = this, {
      rootWindow: n
    } = this.domBindings;
    n.getComputedStyle(e.wtRootElement.parentNode).getPropertyValue("overflow") === "hidden" ? this.scrollableElement = e.holder : this.scrollableElement = Dh(e.TABLE), this.registerListeners();
  }
  /**
   *
   */
  destroy() {
    this.resizeObserver.disconnect(), this.eventManager.destroy(), this.topOverlay.destroy(), this.bottomOverlay.clone && this.bottomOverlay.destroy(), this.inlineStartOverlay.destroy(), this.topInlineStartCornerOverlay && this.topInlineStartCornerOverlay.destroy(), this.bottomInlineStartCornerOverlay && this.bottomInlineStartCornerOverlay.clone && this.bottomInlineStartCornerOverlay.destroy(), this.destroyed = !0;
  }
  /**
   * @param {boolean} [fastDraw=false] When `true`, try to refresh only the positions of borders without rerendering
   *                                   the data. It will only work if Table.draw() does not force
   *                                   rendering anyway.
   */
  refresh() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    this.updateLastSpreaderSize() && this.adjustElementsSize(), this.bottomOverlay.clone && this.bottomOverlay.refresh(e), this.inlineStartOverlay.refresh(e), this.topOverlay.refresh(e), this.topInlineStartCornerOverlay && this.topInlineStartCornerOverlay.refresh(e), this.bottomInlineStartCornerOverlay && this.bottomInlineStartCornerOverlay.clone && this.bottomInlineStartCornerOverlay.refresh(e);
  }
  /**
   * Update the last cached spreader size with the current size.
   *
   * @returns {boolean} `true` if the lastSpreaderSize cache was updated, `false` otherwise.
   */
  updateLastSpreaderSize() {
    const e = this.wtTable.spreader, n = e.clientWidth, i = e.clientHeight, r = n !== this.spreaderLastSize.width || i !== this.spreaderLastSize.height;
    return r && (this.spreaderLastSize.width = n, this.spreaderLastSize.height = i), r;
  }
  /**
   * Adjust overlays elements size and master table size.
   */
  adjustElementsSize() {
    const {
      wtViewport: e
    } = this.wot, {
      wtTable: n
    } = this, {
      rootWindow: i
    } = this.domBindings, r = this.scrollableElement === i, o = this.wtSettings.getSetting("totalColumns"), s = this.wtSettings.getSetting("totalRows"), a = e.getRowHeaderWidth(), u = e.getColumnHeaderHeight() + this.topOverlay.sumCellSizes(0, s) + 1, c = a + this.inlineStartOverlay.sumCellSizes(0, o), d = n.hider.style, g = () => r ? !1 : this.scrollableElement.scrollTop > Math.max(0, u - n.holder.clientHeight), m = () => r ? !1 : this.scrollableElement.scrollLeft > Math.max(0, c - n.holder.clientWidth), w = g() ? 1 : 0, C = m() ? 1 : 0;
    if (d.width = `${c + C}px`, d.height = `${u + w}px`, this.scrollbarSize > 0) {
      const {
        scrollHeight: S,
        scrollWidth: b
      } = n.wtRootElement, {
        scrollHeight: R,
        scrollWidth: _
      } = n.holder;
      this.hasScrollbarRight = S < R, this.hasScrollbarBottom = b < _, this.hasScrollbarRight && n.hider.scrollWidth + this.scrollbarSize > b ? this.hasScrollbarBottom = !0 : this.hasScrollbarBottom && n.hider.scrollHeight + this.scrollbarSize > S && (this.hasScrollbarRight = !0);
    }
    this.topOverlay.adjustElementsSize(), this.inlineStartOverlay.adjustElementsSize(), this.bottomOverlay.adjustElementsSize();
  }
  /**
   * Expand the hider vertically element by the provided delta value.
   *
   * @param {number} heightDelta The delta value to expand the hider element by.
   */
  expandHiderVerticallyBy(e) {
    const {
      wtTable: n
    } = this;
    n.hider.style.height = `${parseInt(n.hider.style.height, 10) + e}px`;
  }
  /**
   * Expand the hider horizontally element by the provided delta value.
   *
   * @param {number} widthDelta The delta value to expand the hider element by.
   */
  expandHiderHorizontallyBy(e) {
    const {
      wtTable: n
    } = this;
    n.hider.style.width = `${parseInt(n.hider.style.width, 10) + e}px`;
  }
  /**
   *
   */
  applyToDOM() {
    this.wtTable.isVisible() && (this.topOverlay.applyToDOM(), this.bottomOverlay.clone && this.bottomOverlay.applyToDOM(), this.inlineStartOverlay.applyToDOM());
  }
  /**
   * Get the parent overlay of the provided element.
   *
   * @param {HTMLElement} element An element to process.
   * @returns {object|null}
   */
  getParentOverlay(e) {
    if (!e)
      return null;
    const n = [this.topOverlay, this.inlineStartOverlay, this.bottomOverlay, this.topInlineStartCornerOverlay, this.bottomInlineStartCornerOverlay];
    let i = null;
    return T(n, (r) => {
      r && r.clone && r.clone.wtTable.TABLE.contains(e) && (i = r.clone);
    }), i;
  }
  /**
   * Synchronize the class names between the main overlay table and the tables on the other overlays.
   *
   */
  syncOverlayTableClassNames() {
    const e = this.wtTable.TABLE, n = [this.topOverlay, this.inlineStartOverlay, this.bottomOverlay, this.topInlineStartCornerOverlay, this.bottomInlineStartCornerOverlay];
    T(n, (i) => {
      i && (i.clone.wtTable.TABLE.className = e.className);
    });
  }
}
const oX = rX;
function sT(t, e, n) {
  return (e = sX(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function sX(t) {
  var e = aX(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function aX(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class lX {
  /**
   * @param {SettingsPure} settings The user defined settings.
   */
  constructor(e) {
    sT(this, "settings", {}), sT(this, "defaults", Object.freeze(this.getDefaults())), Fe(this.defaults, (n, i) => {
      if (e[i] !== void 0)
        this.settings[i] = e[i];
      else {
        if (n === void 0)
          throw new Error(`A required setting "${i}" was not provided`);
        this.settings[i] = n;
      }
    });
  }
  /**
   * Generate defaults for a settings.
   * Void 0 means it is required, null means it can be empty.
   *
   * @private
   * @returns {SettingsPure}
   */
  getDefaults() {
    return {
      facade: void 0,
      table: void 0,
      // Determines whether the Walkontable instance is used as dataset viewer. When its instance is used as
      // a context menu, autocomplete list, etc, the returned value is `false`.
      isDataViewInstance: !0,
      // presentation mode
      externalRowCalculator: !1,
      stretchH: "none",
      // values: all, last, none
      currentRowClassName: null,
      currentColumnClassName: null,
      preventOverflow() {
        return !1;
      },
      preventWheel: !1,
      // data source
      data: void 0,
      freezeOverlays: !1,
      // Number of renderable columns for the left overlay.
      fixedColumnsStart: 0,
      // Number of renderable rows for the top overlay.
      fixedRowsTop: 0,
      // Number of renderable rows for the bottom overlay.
      fixedRowsBottom: 0,
      // Enable the inline start overlay when conditions are met (left for LTR and right for RTL document mode).
      shouldRenderInlineStartOverlay: () => this.getSetting("fixedColumnsStart") > 0 || this.getSetting("rowHeaders").length > 0,
      // Enable the top overlay when conditions are met.
      shouldRenderTopOverlay: () => this.getSetting("fixedRowsTop") > 0 || this.getSetting("columnHeaders").length > 0,
      // Enable the bottom overlay when conditions are met.
      shouldRenderBottomOverlay: () => this.getSetting("fixedRowsBottom") > 0,
      minSpareRows: 0,
      // this must be array of functions: [function (row, TH) {}]
      rowHeaders() {
        return [];
      },
      // this must be array of functions: [function (column, TH) {}]
      columnHeaders() {
        return [];
      },
      totalRows: void 0,
      totalColumns: void 0,
      cellRenderer: (e, n, i) => {
        const r = this.getSetting("data", e, n);
        Gs(i, r == null ? "" : r);
      },
      // columnWidth: 50,
      columnWidth() {
      },
      rowHeight() {
      },
      rowHeightByOverlayName() {
      },
      defaultRowHeight: 23,
      defaultColumnWidth: 50,
      selections: null,
      hideBorderOnMouseDownOver: !1,
      viewportRowCalculatorOverride: null,
      viewportColumnCalculatorOverride: null,
      // callbacks
      onCellMouseDown: null,
      onCellContextMenu: null,
      onCellMouseOver: null,
      onCellMouseOut: null,
      onCellMouseUp: null,
      // onCellMouseOut: null,
      onCellDblClick: null,
      onCellCornerMouseDown: null,
      onCellCornerDblClick: null,
      beforeDraw: null,
      onDraw: null,
      onBeforeRemoveCellClassNames: null,
      onAfterDrawSelection: null,
      onBeforeDrawBorders: null,
      // viewport scroll hooks
      onBeforeViewportScrollHorizontally: (e) => e,
      onBeforeViewportScrollVertically: (e) => e,
      // native scroll hooks
      onScrollHorizontally: null,
      onScrollVertically: null,
      //
      onBeforeTouchScroll: null,
      onAfterMomentumScroll: null,
      onBeforeStretchingColumnWidth: (e) => e,
      onModifyRowHeaderWidth: null,
      onModifyGetCellCoords: null,
      onBeforeHighlightingRowHeader: (e) => e,
      onBeforeHighlightingColumnHeader: (e) => e,
      onWindowResize: null,
      onContainerElementResize: null,
      renderAllColumns: !1,
      renderAllRows: !1,
      groups: !1,
      rowHeaderWidth: null,
      columnHeaderHeight: null,
      headerClassName: null,
      rtlMode: !1,
      ariaTags: !0
    };
  }
  /**
   * Update settings.
   *
   * @param {object|string} settings The singular settings to update or if passed as object to merge with.
   * @param {*} value The value to set if the first argument is passed as string.
   * @returns {Settings}
   */
  update(e, n) {
    return n === void 0 ? Fe(e, (i, r) => {
      this.settings[r] = i;
    }) : this.settings[e] = n, this;
  }
  /**
   * Get setting by name.
   *
   * @param {$Keys<SettingsPure>} key The settings key to retrieve.
   * @param {*} [param1] Additional parameter passed to the options defined as function.
   * @param {*} [param2] Additional parameter passed to the options defined as function.
   * @param {*} [param3] Additional parameter passed to the options defined as function.
   * @param {*} [param4] Additional parameter passed to the options defined as function.
   * @returns {*}
   */
  getSetting(e, n, i, r, o) {
    return typeof this.settings[e] == "function" ? this.settings[e](n, i, r, o) : n !== void 0 && Array.isArray(this.settings[e]) ? this.settings[e][n] : this.settings[e];
  }
  /**
   * Get a setting value without any evaluation.
   *
   * @param {string} key The settings key to retrieve.
   * @returns {*}
   */
  getSettingPure(e) {
    return this.settings[e];
  }
  /**
   * Checks if setting exists.
   *
   * @param {boolean} key The settings key to check.
   * @returns {boolean}
   */
  has(e) {
    return !!this.settings[e];
  }
}
class Fb extends Bu {
  /**
   * @param {TableDao} dataAccessObject The data access object.
   * @param {FacadeGetter} facadeGetter Function which return proper facade.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   */
  constructor(e, n, i, r) {
    super(e, n, i, r, "master");
  }
  alignOverlaysWithTrimmingContainer() {
    const e = tl(this.wtRootElement), {
      rootWindow: n
    } = this.domBindings;
    if (e === n)
      this.wtSettings.getSetting("preventOverflow") || (this.holder.style.overflow = "visible", this.wtRootElement.style.overflow = "visible");
    else {
      const i = e.parentElement, r = Eu(e, "height", n), o = Eu(e, "overflow", n), s = this.holder.style, {
        scrollWidth: a,
        scrollHeight: l
      } = e;
      let u = e.offsetWidth, c = e.offsetHeight;
      if (i && ["auto", "hidden", "scroll"].includes(o)) {
        const d = e.cloneNode(!1);
        d.style.overflow = "auto", d.style.position = "absolute", e.nextElementSibling ? i.insertBefore(d, e.nextElementSibling) : i.appendChild(d);
        const g = parseInt(uo(d, n).height, 10);
        i.removeChild(d), g === 0 && (c = 0);
      }
      c = Math.min(c, l), s.height = r === "auto" ? "auto" : `${c}px`, u = Math.min(u, a), s.width = `${u}px`, s.overflow = "", this.hasTableHeight = s.height === "auto" ? !0 : c > 0, this.hasTableWidth = u > 0;
    }
    this.isTableVisible = el(this.TABLE);
  }
  markOversizedColumnHeaders() {
    const {
      wtSettings: e
    } = this, {
      wtViewport: n
    } = this.dataAccessObject, i = "master", o = e.getSetting("columnHeaders").length;
    if (o && !n.hasOversizedColumnHeadersMarked[i]) {
      const a = e.getSetting("rowHeaders").length, l = this.getRenderedColumnsCount();
      for (let u = 0; u < o; u++)
        for (let c = -1 * a; c < l; c++)
          this.markIfOversizedColumnHeader(c);
      n.hasOversizedColumnHeadersMarked[i] = !0;
    }
  }
}
pt(Fb, jx);
pt(Fb, Ob);
const uX = Fb;
class cX {
  /**
   * @param {ViewportDao} dataAccessObject The Walkontable instance.
   * @param {DomBindings} domBindings Bindings into DOM.
   * @param {Settings} wtSettings The Walkontable settings.
   * @param {EventManager} eventManager The instance event manager.
   * @param {Table} wtTable The table.
   */
  constructor(e, n, i, r, o) {
    this.dataAccessObject = e, this.wot = e.wot, this.instance = this.wot, this.domBindings = n, this.wtSettings = i, this.wtTable = o, this.oversizedRows = [], this.oversizedColumnHeaders = [], this.hasOversizedColumnHeadersMarked = {}, this.clientHeight = 0, this.containerWidth = NaN, this.rowHeaderWidth = NaN, this.rowsVisibleCalculator = null, this.columnsVisibleCalculator = null, this.rowsCalculatorTypes = /* @__PURE__ */ new Map([["rendered", () => this.wtSettings.getSetting("renderAllRows") ? new B2() : new V2()], ["fullyVisible", () => new I2()], ["partiallyVisible", () => new Ox()]]), this.columnsCalculatorTypes = /* @__PURE__ */ new Map([["rendered", () => this.wtSettings.getSetting("renderAllColumns") ? new D2() : new F2()], ["fullyVisible", () => new E2()], ["partiallyVisible", () => new Ix()]]), this.eventManager = r, this.eventManager.addEventListener(this.domBindings.rootWindow, "resize", () => {
      this.clientHeight = this.getWorkspaceHeight();
    });
  }
  /**
   * @returns {number}
   */
  getWorkspaceHeight() {
    const e = this.domBindings.rootDocument, n = this.dataAccessObject.topOverlayTrimmingContainer;
    let i = 0;
    return n === this.domBindings.rootWindow ? i = e.documentElement.clientHeight : i = Zt(n) > 0 && n.clientHeight > 0 ? n.clientHeight : 1 / 0, i;
  }
  getWorkspaceWidth() {
    const {
      wtSettings: e
    } = this, {
      rootDocument: n,
      rootWindow: i
    } = this.domBindings, r = this.dataAccessObject.inlineStartOverlayTrimmingContainer, o = n.documentElement.offsetWidth, s = e.getSetting("totalColumns"), a = e.getSetting("preventOverflow"), l = e.getSetting("rtlMode"), u = this.wtTable.TABLE.getBoundingClientRect(), c = l ? u.right - o : u.left, h = o - c;
    let d, g;
    if (a)
      return It(this.wtTable.wtRootElement);
    if (e.getSetting("freezeOverlays") ? d = Math.min(h, o) : d = Math.min(this.getContainerFillWidth(), h, o), r === i && s > 0 && this.sumColumnWidths(0, s - 1) > d)
      return n.documentElement.clientWidth;
    if (r !== i && (g = Eu(this.dataAccessObject.inlineStartOverlayTrimmingContainer, "overflow", i), g === "scroll" || g === "hidden" || g === "auto"))
      return Math.max(d, r.clientWidth);
    const m = e.getSetting("stretchH");
    return m === "none" || !m ? Math.max(d, It(this.wtTable.TABLE)) : d;
  }
  /**
   * Checks if viewport has vertical scroll.
   *
   * @returns {boolean}
   */
  hasVerticalScroll() {
    return this.wtTable.hider.offsetHeight > this.getWorkspaceHeight();
  }
  /**
   * Checks if viewport has horizontal scroll.
   *
   * @returns {boolean}
   */
  hasHorizontalScroll() {
    return this.wtTable.hider.offsetWidth > this.getWorkspaceWidth();
  }
  /**
   * @param {number} from The visual column index from the width sum is start calculated.
   * @param {number} length The length of the column to traverse.
   * @returns {number}
   */
  sumColumnWidths(e, n) {
    let i = 0, r = e;
    for (; r < n; )
      i += this.wtTable.getColumnWidth(r), r += 1;
    return i;
  }
  /**
   * @returns {number}
   */
  getContainerFillWidth() {
    if (this.containerWidth)
      return this.containerWidth;
    const e = this.wtTable.holder, n = this.domBindings.rootDocument.createElement("div");
    n.style.width = "100%", n.style.height = "1px", e.appendChild(n);
    const i = n.offsetWidth;
    return this.containerWidth = i, e.removeChild(n), i;
  }
  /**
   * @returns {number}
   */
  getWorkspaceOffset() {
    return gn(this.wtTable.TABLE);
  }
  /**
   * @returns {number}
   */
  getColumnHeaderHeight() {
    return this.wtSettings.getSetting("columnHeaders").length ? isNaN(this.columnHeaderHeight) && (this.columnHeaderHeight = Zt(this.wtTable.THEAD)) : this.columnHeaderHeight = 0, this.columnHeaderHeight;
  }
  /**
   * @returns {number}
   */
  getViewportHeight() {
    let e = this.getWorkspaceHeight();
    if (e === 1 / 0)
      return e;
    const n = this.getColumnHeaderHeight();
    return n > 0 && (e -= n), e;
  }
  /**
   * @returns {number}
   */
  getRowHeaderWidth() {
    const e = this.wtSettings.getSetting("rowHeaderWidth"), n = this.wtSettings.getSetting("rowHeaders");
    if (e) {
      this.rowHeaderWidth = 0;
      for (let i = 0, r = n.length; i < r; i++)
        this.rowHeaderWidth += e[i] || e;
    }
    if (isNaN(this.rowHeaderWidth))
      if (n.length) {
        let i = this.wtTable.TABLE.querySelector("TH");
        this.rowHeaderWidth = 0;
        for (let r = 0, o = n.length; r < o; r++)
          i ? (this.rowHeaderWidth += It(i), i = i.nextSibling) : this.rowHeaderWidth += 50;
      } else
        this.rowHeaderWidth = 0;
    return this.rowHeaderWidth = this.wtSettings.getSetting("onModifyRowHeaderWidth", this.rowHeaderWidth) || this.rowHeaderWidth, this.rowHeaderWidth;
  }
  /**
   * @returns {number}
   */
  getViewportWidth() {
    const e = this.getWorkspaceWidth();
    if (e === 1 / 0)
      return e;
    const n = this.getRowHeaderWidth();
    return n > 0 ? e - n : e;
  }
  /**
   * Creates rows calculators. The type of the calculations can be chosen from the list:
   *  - 'rendered' Calculates rows that should be rendered within the current table's viewport;
   *  - 'fullyVisible' Calculates rows that are fully visible (used mostly for scrolling purposes);
   *  - 'partiallyVisible' Calculates rows that are partially visible (used mostly for scrolling purposes).
   *
   * @param {'rendered' | 'fullyVisible' | 'partiallyVisible'} calculatorTypes The list of the calculation types.
   * @returns {ViewportRowsCalculator}
   */
  createRowsCalculator() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : ["rendered", "fullyVisible", "partiallyVisible"];
    const {
      wtSettings: n,
      wtTable: i
    } = this;
    let r = this.getViewportHeight(), o, s;
    this.rowHeaderWidth = NaN;
    let a = this.dataAccessObject.topScrollPosition - this.dataAccessObject.topParentOffset;
    const l = n.getSetting("fixedRowsTop"), u = n.getSetting("fixedRowsBottom"), c = n.getSetting("totalRows");
    return l && a >= 0 && (s = this.dataAccessObject.topOverlay.sumCellSizes(0, l), a += s, r -= s), u && this.dataAccessObject.bottomOverlay.clone && (s = this.dataAccessObject.bottomOverlay.sumCellSizes(c - u, c), r -= s), i.holder.clientHeight === i.holder.offsetHeight ? o = 0 : o = pn(this.domBindings.rootDocument), new K2({
      calculationTypes: e.map((h) => [h, this.rowsCalculatorTypes.get(h)()]),
      viewportHeight: r,
      scrollOffset: a,
      totalRows: n.getSetting("totalRows"),
      rowHeightFn: (h) => i.getRowHeight(h),
      overrideFn: n.getSettingPure("viewportRowCalculatorOverride"),
      horizontalScrollbarHeight: o
    });
  }
  /**
   * Creates columns calculators. The type of the calculations can be chosen from the list:
   *  - 'rendered' Calculates columns that should be rendered within the current table's viewport;
   *  - 'fullyVisible' Calculates columns that are fully visible (used mostly for scrolling purposes);
   *  - 'partiallyVisible' Calculates columns that are partially visible (used mostly for scrolling purposes).
   *
   * @param {'rendered' | 'fullyVisible' | 'partiallyVisible'} calculatorTypes The list of the calculation types.
   * @returns {ViewportColumnsCalculator}
   */
  createColumnsCalculator() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : ["rendered", "fullyVisible", "partiallyVisible"];
    const {
      wtSettings: n,
      wtTable: i
    } = this;
    let r = this.getViewportWidth(), o = Math.abs(this.dataAccessObject.inlineStartScrollPosition) - this.dataAccessObject.inlineStartParentOffset;
    this.columnHeaderHeight = NaN;
    const s = n.getSetting("fixedColumnsStart");
    if (s && o >= 0) {
      const a = this.dataAccessObject.inlineStartOverlay.sumCellSizes(0, s);
      o += a, r -= a;
    }
    return i.holder.clientWidth !== i.holder.offsetWidth && (r -= pn(this.domBindings.rootDocument)), new j2({
      calculationTypes: e.map((a) => [a, this.columnsCalculatorTypes.get(a)()]),
      viewportWidth: r,
      scrollOffset: o,
      totalColumns: n.getSetting("totalColumns"),
      columnWidthFn: (a) => i.getColumnWidth(a),
      overrideFn: n.getSettingPure("viewportColumnCalculatorOverride"),
      inlineStartOffset: this.dataAccessObject.inlineStartParentOffset
    });
  }
  /**
   * Creates rowsRenderCalculator and columnsRenderCalculator (before draw, to determine what rows and
   * cols should be rendered).
   *
   * @param {boolean} fastDraw If `true`, will try to avoid full redraw and only update the border positions.
   *                           If `false` or `undefined`, will perform a full redraw.
   * @returns {boolean} The fastDraw value, possibly modified.
   */
  createCalculators() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    const {
      wtSettings: n
    } = this, i = this.createRowsCalculator(), r = this.createColumnsCalculator();
    if (e && !n.getSetting("renderAllRows")) {
      const o = i.getResultsFor("fullyVisible");
      e = this.areAllProposedVisibleRowsAlreadyRendered(o);
    }
    if (e && !n.getSetting("renderAllColumns")) {
      const o = r.getResultsFor("fullyVisible");
      e = this.areAllProposedVisibleColumnsAlreadyRendered(o);
    }
    return e || (this.rowsRenderCalculator = i.getResultsFor("rendered"), this.columnsRenderCalculator = r.getResultsFor("rendered")), this.rowsVisibleCalculator = i.getResultsFor("fullyVisible"), this.columnsVisibleCalculator = r.getResultsFor("fullyVisible"), this.rowsPartiallyVisibleCalculator = i.getResultsFor("partiallyVisible"), this.columnsPartiallyVisibleCalculator = r.getResultsFor("partiallyVisible"), e;
  }
  /**
   * Creates rows and columns calculators (after draw, to determine what are
   * the actually fully visible and partially visible rows and columns).
   */
  createVisibleCalculators() {
    const e = this.createRowsCalculator(["fullyVisible", "partiallyVisible"]), n = this.createColumnsCalculator(["fullyVisible", "partiallyVisible"]);
    this.rowsVisibleCalculator = e.getResultsFor("fullyVisible"), this.columnsVisibleCalculator = n.getResultsFor("fullyVisible"), this.rowsPartiallyVisibleCalculator = e.getResultsFor("partiallyVisible"), this.columnsPartiallyVisibleCalculator = n.getResultsFor("partiallyVisible");
  }
  /**
   * Returns information whether proposedRowsVisibleCalculator viewport
   * is contained inside rows rendered in previous draw (cached in rowsRenderCalculator).
   *
   * @param {ViewportRowsCalculator} proposedRowsVisibleCalculator The instance of the viewport calculator to compare with.
   * @returns {boolean} Returns `true` if all proposed visible rows are already rendered (meaning: redraw is not needed).
   *                    Returns `false` if at least one proposed visible row is not already rendered (meaning: redraw is needed).
   */
  areAllProposedVisibleRowsAlreadyRendered(e) {
    if (!this.rowsVisibleCalculator)
      return !1;
    let {
      startRow: n,
      endRow: i
    } = e;
    if (n === null && i === null) {
      if (!e.isVisibleInTrimmingContainer)
        return !0;
      n = this.rowsPartiallyVisibleCalculator.startRow, i = this.rowsPartiallyVisibleCalculator.endRow;
    }
    const {
      startRow: r,
      endRow: o
    } = this.rowsRenderCalculator;
    return n < r || n === r && n > 0 ? !1 : !(i > o || i === o && i < this.wtSettings.getSetting("totalRows") - 1);
  }
  /**
   * Returns information whether proposedColumnsVisibleCalculator viewport
   * is contained inside column rendered in previous draw (cached in columnsRenderCalculator).
   *
   * @param {ViewportRowsCalculator} proposedColumnsVisibleCalculator The instance of the viewport calculator to compare with.
   * @returns {boolean} Returns `true` if all proposed visible columns are already rendered (meaning: redraw is not needed).
   *                    Returns `false` if at least one proposed visible column is not already rendered (meaning: redraw is needed).
   */
  areAllProposedVisibleColumnsAlreadyRendered(e) {
    if (!this.columnsVisibleCalculator)
      return !1;
    let {
      startColumn: n,
      endColumn: i
    } = e;
    if (n === null && i === null) {
      if (!e.isVisibleInTrimmingContainer)
        return !0;
      n = this.columnsPartiallyVisibleCalculator.startColumn, i = this.columnsPartiallyVisibleCalculator.endColumn;
    }
    const {
      startColumn: r,
      endColumn: o
    } = this.columnsRenderCalculator;
    return n < r || n === r && n > 0 ? !1 : !(i > o || i === o && i < this.wtSettings.getSetting("totalColumns") - 1);
  }
  /**
   * Resets values in keys of the hasOversizedColumnHeadersMarked object after updateSettings.
   */
  resetHasOversizedColumnHeadersMarked() {
    Fe(this.hasOversizedColumnHeadersMarked, (e, n, i) => {
      i[n] = void 0;
    });
  }
}
const hX = cX;
class dX extends Ib {
  /**
   * @param {HTMLTableElement} table Main table.
   * @param {SettingsPure} settings The Walkontable settings.
   */
  constructor(e, n) {
    super(e, new lX(n));
    const i = this.wtSettings.getSetting("facade", this);
    this.wtTable = new uX(this.getTableDao(), i, this.domBindings, this.wtSettings), this.wtViewport = new hX(this.getViewportDao(), this.domBindings, this.wtSettings, this.eventManager, this.wtTable), this.selectionManager = new $3(this.wtSettings.getSetting("selections")), this.wtEvent = new Dx(i, this.domBindings, this.wtSettings, this.eventManager, this.wtTable, this.selectionManager), this.wtOverlays = new oX(
      // TODO create DAO and remove reference to the Walkontable instance.
      this,
      i,
      this.domBindings,
      this.wtSettings,
      this.eventManager,
      this.wtTable
    ), this.exportSettingsAsClassNames(), this.findOriginalHeaders();
  }
  /**
   * Export settings as class names added to the parent element of the table.
   */
  exportSettingsAsClassNames() {
    const e = {
      rowHeaders: "htRowHeaders",
      columnHeaders: "htColumnHeaders"
    }, n = [], i = [];
    Fe(e, (r, o) => {
      this.wtSettings.getSetting(o).length && i.push(r), n.push(r);
    }), xe(this.wtTable.wtRootElement.parentNode, n), Q(this.wtTable.wtRootElement.parentNode, i);
  }
  /**
   * Gets the overlay instance by its name.
   *
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'} overlayName The overlay name.
   * @returns {Overlay | null}
   */
  getOverlayByName(e) {
    var n;
    if (!$x.includes(e))
      return null;
    const i = e.replace(/_([a-z])/g, (r) => r[1].toUpperCase());
    return (n = this.wtOverlays[`${i}Overlay`]) !== null && n !== void 0 ? n : null;
  }
  /**
   * @returns {ViewportDao}
   */
  getViewportDao() {
    const e = this;
    return {
      get wot() {
        return e;
      },
      get topOverlayTrimmingContainer() {
        return e.wtOverlays.topOverlay.trimmingContainer;
      },
      get inlineStartOverlayTrimmingContainer() {
        return e.wtOverlays.inlineStartOverlay.trimmingContainer;
      },
      get topScrollPosition() {
        return e.wtOverlays.topOverlay.getScrollPosition();
      },
      get topParentOffset() {
        return e.wtOverlays.topOverlay.getTableParentOffset();
      },
      get inlineStartScrollPosition() {
        return e.wtOverlays.inlineStartOverlay.getScrollPosition();
      },
      get inlineStartParentOffset() {
        return e.wtOverlays.inlineStartOverlay.getTableParentOffset();
      },
      get topOverlay() {
        return e.wtOverlays.topOverlay;
      },
      get inlineStartOverlay() {
        return e.wtOverlays.inlineStartOverlay;
      },
      get bottomOverlay() {
        return e.wtOverlays.bottomOverlay;
      }
    };
  }
}
class Vb {
  /**
   * @param {SettingsPure|Walkontable} settingsOrInstance The Walkontable settings.
   */
  constructor(e) {
    e instanceof Ib ? this._wot = e : this._initFromSettings(e);
  }
  _initFromSettings(e) {
    e.facade = (n) => {
      const i = new Vb(n);
      return () => i;
    }, this._wot = new dX(e.table, e);
  }
  get guid() {
    return this._wot.guid;
  }
  get rootDocument() {
    return this._wot.domBindings.rootDocument;
  }
  get rootWindow() {
    return this._wot.domBindings.rootWindow;
  }
  get wtSettings() {
    return this._wot.wtSettings;
  }
  get cloneSource() {
    return this._wot.cloneSource;
  }
  get cloneOverlay() {
    return this._wot.cloneOverlay;
  }
  get selectionManager() {
    return this._wot.selectionManager;
  }
  get wtViewport() {
    return this._wot.wtViewport;
  }
  get wtOverlays() {
    return this._wot.wtOverlays;
  }
  get wtTable() {
    return this._wot.wtTable;
  }
  get wtEvent() {
    return this._wot.wtEvent;
  }
  get wtScroll() {
    return this._wot.wtScroll;
  }
  get drawn() {
    return this._wot.drawn;
  }
  set drawn(e) {
    this._wot.drawn = e;
  }
  get drawInterrupted() {
    return this._wot.drawInterrupted;
  }
  set drawInterrupted(e) {
    this._wot.drawInterrupted = e;
  }
  get lastMouseOver() {
    return this._wot.lastMouseOver;
  }
  set lastMouseOver(e) {
    this._wot.lastMouseOver = e;
  }
  get momentumScrolling() {
    return this._wot.momentumScrolling;
  }
  set momentumScrolling(e) {
    this._wot.momentumScrolling = e;
  }
  get touchApplied() {
    return this._wot.touchApplied;
  }
  set touchApplied(e) {
    this._wot.touchApplied = e;
  }
  get domBindings() {
    return this._wot.domBindings;
  }
  get eventListeners() {
    return this._wot.eventListeners;
  }
  set eventListeners(e) {
    this._wot.eventListeners = e;
  }
  get eventManager() {
    return this._wot.eventManager;
  }
  createCellCoords(e, n) {
    return this._wot.createCellCoords(e, n);
  }
  createCellRange(e, n, i) {
    return this._wot.createCellRange(e, n, i);
  }
  draw() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    return this._wot.draw(e), this;
  }
  getCell(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    return this._wot.getCell(e, n);
  }
  scrollViewport(e, n, i, r, o) {
    return this._wot.scrollViewport(e, n, i, r, o);
  }
  scrollViewportHorizontally(e, n, i) {
    return this._wot.scrollViewportHorizontally(e, n, i);
  }
  scrollViewportVertically(e, n, i) {
    return this._wot.scrollViewportVertically(e, n, i);
  }
  getViewport() {
    return this._wot.getViewport();
  }
  getOverlayName() {
    return this._wot.cloneOverlay ? this._wot.cloneOverlay.type : "master";
  }
  getOverlayByName(e) {
    return this._wot.getOverlayByName(e);
  }
  exportSettingsAsClassNames() {
    return this._wot.exportSettingsAsClassNames();
  }
  update(e, n) {
    return this._wot.wtSettings.update(e, n), this;
  }
  getSetting(e, n, i, r, o) {
    return this._wot.wtSettings.getSetting(e, n, i, r, o);
  }
  hasSetting(e) {
    return this._wot.wtSettings.hasSetting(e);
  }
  destroy() {
    this._wot.destroy();
  }
}
function aT(t) {
  let {
    isShiftKey: e,
    isLeftClick: n,
    isRightClick: i,
    coords: r,
    selection: o,
    controller: s,
    cellCoordsFactory: a
  } = t;
  const l = o.isSelected() ? o.getSelectedRange().current() : null, u = o.isSelectedByCorner(), c = o.isSelectedByRowHeader();
  if (o.markSource("mouse"), e && l)
    r.row >= 0 && r.col >= 0 && !s.cell ? o.setRangeEnd(r) : (u || c) && r.row >= 0 && r.col >= 0 && !s.cell ? o.setRangeEnd(a(r.row, r.col)) : u && r.row < 0 && !s.column ? o.setRangeEnd(a(l.to.row, r.col)) : c && r.col < 0 && !s.row ? o.setRangeEnd(a(r.row, l.to.col)) : (!u && !c && r.col < 0 || u && r.col < 0) && !s.row ? o.selectRows(Math.max(l.from.row, 0), r.row, r.col) : (!u && !c && r.row < 0 || c && r.row < 0) && !s.column && o.selectColumns(Math.max(l.from.col, 0), r.col, r.row);
  else {
    const h = !o.inInSelection(r), d = n || i && h;
    r.row < 0 && r.col >= 0 && !s.column ? d && o.selectColumns(r.col, r.col, r.row) : r.col < 0 && r.row >= 0 && !s.row ? d && o.selectRows(r.row, r.row, r.col) : r.col >= 0 && r.row >= 0 && !s.cell ? d && o.setRangeStart(r) : r.col < 0 && r.row < 0 && o.selectAll(!0, !0, {
      disableHeadersHighlight: !0,
      focusPosition: {
        row: 0,
        col: 0
      }
    });
  }
  o.markEndSource();
}
function fX(t) {
  let {
    isLeftClick: e,
    coords: n,
    selection: i,
    controller: r,
    cellCoordsFactory: o
  } = t;
  if (!e)
    return;
  const s = i.isSelectedByRowHeader(), a = i.isSelectedByColumnHeader(), l = i.tableProps.countCols(), u = i.tableProps.countRows();
  i.markSource("mouse"), a && !r.column ? i.setRangeEnd(o(u - 1, n.col)) : s && !r.row ? i.setRangeEnd(o(n.row, l - 1)) : r.cell || i.setRangeEnd(n), i.markEndSource();
}
const gX = /* @__PURE__ */ new Map([["mousedown", aT], ["mouseover", fX], ["touchstart", aT]]);
function lT(t, e) {
  let {
    coords: n,
    selection: i,
    controller: r,
    cellCoordsFactory: o
  } = e;
  gX.get(t.type)({
    coords: n,
    selection: i,
    controller: r,
    cellCoordsFactory: o,
    isShiftKey: t.shiftKey,
    isLeftClick: Hp(t) || t.type === "touchstart",
    isRightClick: zs(t)
  });
}
const e1 = /* @__PURE__ */ new WeakMap(), Wb = Symbol("rootInstance");
function mX(t) {
  e1.set(t, !0);
}
function pX(t) {
  return t === Wb;
}
function Yg(t) {
  return e1.has(t);
}
function wX(t, e) {
  t1(t, e), e.add(t);
}
function ua(t, e, n) {
  t1(t, e), e.set(t, n);
}
function t1(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ls(t, e, n) {
  return (e = AX(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function AX(t) {
  var e = CX(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function CX(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function An(t, e) {
  return t.get(Bh(t, e));
}
function Hi(t, e, n) {
  return t.set(Bh(t, e), n), n;
}
function Bh(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var dC = /* @__PURE__ */ new WeakMap(), yf = /* @__PURE__ */ new WeakMap(), Sf = /* @__PURE__ */ new WeakMap(), bo = /* @__PURE__ */ new WeakMap(), or = /* @__PURE__ */ new WeakMap(), fC = /* @__PURE__ */ new WeakMap(), gC = /* @__PURE__ */ new WeakMap(), jg = /* @__PURE__ */ new WeakSet();
class vX {
  /**
   * @param {Hanstontable} hotInstance Instance of {@link Handsontable}.
   */
  constructor(e) {
    wX(this, jg), ls(this, "hot", void 0), ls(this, "eventManager", void 0), ls(this, "settings", void 0), ls(this, "THEAD", void 0), ls(this, "TBODY", void 0), ls(this, "_wt", void 0), ls(this, "activeWt", void 0), ua(this, dC, 0), ua(this, yf, 0), ls(this, "postponedAdjustElementsSize", !1), ua(this, Sf, !1), ua(this, bo, void 0), ua(this, or, void 0), ua(this, fC, 0), ua(this, gC, 0), this.hot = e, this.eventManager = new Qi(this.hot), this.settings = this.hot.getSettings(), this.createElements(), this.registerEvents(), this.initializeWalkontable();
  }
  /**
   * Renders WalkontableUI.
   */
  render() {
    this.hot.isRenderSuspended() || (this.hot.runHooks("beforeRender", this.hot.forceFullRender), this.postponedAdjustElementsSize && (this.postponedAdjustElementsSize = !1, this.adjustElementsSize(!0)), this._wt.draw(!this.hot.forceFullRender), this.hot.runHooks("afterRender", this.hot.forceFullRender), this.hot.forceFullRender = !1, this.hot.renderCall = !1);
  }
  /**
   * Adjust overlays elements size and master table size.
   */
  adjustElementsSize() {
    this.hot.isRenderSuspended() ? this.postponedAdjustElementsSize = !0 : this._wt.wtOverlays.adjustElementsSize();
  }
  /**
   * Returns td object given coordinates.
   *
   * @param {CellCoords} coords Renderable cell coordinates.
   * @param {boolean} topmost Indicates whether the cell should be calculated from the topmost.
   * @returns {HTMLTableCellElement|null}
   */
  getCellAtCoords(e, n) {
    const i = this._wt.getCell(e, n);
    return i < 0 ? null : i;
  }
  /**
   * Scroll viewport to a cell.
   *
   * @param {CellCoords} coords Renderable cell coordinates.
   * @param {boolean} [snapToTop] If `true`, viewport is scrolled to show the cell on the top of the table.
   * @param {boolean} [snapToRight] If `true`, viewport is scrolled to show the cell on the right side of the table.
   * @param {boolean} [snapToBottom] If `true`, viewport is scrolled to show the cell on the bottom side of the table.
   * @param {boolean} [snapToLeft] If `true`, viewport is scrolled to show the cell on the left side of the table.
   * @returns {boolean}
   */
  scrollViewport(e, n, i, r, o) {
    return this._wt.scrollViewport(e, n, i, r, o);
  }
  /**
   * Scroll viewport to a column.
   *
   * @param {number} column Renderable column index.
   * @param {boolean} [snapToRight] If `true`, viewport is scrolled to show the cell on the right side of the table.
   * @param {boolean} [snapToLeft] If `true`, viewport is scrolled to show the cell on the left side of the table.
   * @returns {boolean}
   */
  scrollViewportHorizontally(e, n, i) {
    return this._wt.scrollViewportHorizontally(e, n, i);
  }
  /**
   * Scroll viewport to a row.
   *
   * @param {number} row Renderable row index.
   * @param {boolean} [snapToTop] If `true`, viewport is scrolled to show the cell on the top of the table.
   * @param {boolean} [snapToBottom] If `true`, viewport is scrolled to show the cell on the bottom side of the table.
   * @returns {boolean}
   */
  scrollViewportVertically(e, n, i) {
    return this._wt.scrollViewportVertically(e, n, i);
  }
  /**
   * Prepares DOMElements and adds correct className to the root element.
   *
   * @private
   */
  createElements() {
    const {
      rootElement: e,
      rootDocument: n
    } = this.hot, i = e.getAttribute("style");
    i && e.setAttribute("data-originalstyle", i), Q(e, "handsontable"), Hi(or, this, n.createElement("TABLE")), Q(An(or, this), "htCore"), this.hot.getSettings().tableClassName && Q(An(or, this), this.hot.getSettings().tableClassName), this.settings.ariaTags && (Ee(An(or, this), [oo()]), Ee(e, [fG(), HP(-1), bp(this.hot.countCols()), MG()])), this.THEAD = n.createElement("THEAD"), An(or, this).appendChild(this.THEAD), this.TBODY = n.createElement("TBODY"), An(or, this).appendChild(this.TBODY), this.hot.table = An(or, this), this.hot.container.insertBefore(An(or, this), this.hot.container.firstChild);
  }
  /**
   * Attaches necessary listeners.
   *
   * @private
   */
  registerEvents() {
    const {
      rootElement: e,
      rootDocument: n,
      selection: i,
      rootWindow: r
    } = this.hot, o = n.documentElement;
    this.eventManager.addEventListener(e, "mousedown", (a) => {
      Hi(Sf, this, !0), this.isTextSelectionAllowed(a.target) || (wy(r), a.preventDefault(), r.focus());
    }), this.eventManager.addEventListener(e, "mouseup", () => {
      Hi(Sf, this, !1);
    }), this.eventManager.addEventListener(e, "mousemove", (a) => {
      An(Sf, this) && !this.isTextSelectionAllowed(a.target) && (this.settings.fragmentSelection && wy(r), a.preventDefault());
    }), this.eventManager.addEventListener(o, "keyup", (a) => {
      i.isInProgress() && !a.shiftKey && i.finish();
    }), this.eventManager.addEventListener(o, "mouseup", (a) => {
      i.isInProgress() && Hp(a) && i.finish(), Hi(bo, this, !1);
      const l = Mp(n.activeElement);
      ah(n.activeElement) && !l || (l || !i.isSelected() && !i.isSelectedByAnyHeader() && !e.contains(a.target) && !zs(a)) && this.hot.unlisten();
    }), this.eventManager.addEventListener(o, "contextmenu", (a) => {
      i.isInProgress() && zs(a) && (i.finish(), Hi(bo, this, !1));
    }), this.eventManager.addEventListener(o, "touchend", () => {
      i.isInProgress() && i.finish(), Hi(bo, this, !1);
    }), this.eventManager.addEventListener(o, "mousedown", (a) => {
      const l = a.target, u = a.x || a.clientX, c = a.y || a.clientY;
      let h = a.target;
      if (An(bo, this) || !e || !this.hot.view)
        return;
      const {
        holder: d
      } = this._wt.wtTable;
      if (h === d) {
        const m = pn(n);
        if (n.elementFromPoint(u + m, c) !== d || n.elementFromPoint(u, c + m) !== d)
          return;
      } else
        for (; h !== o; ) {
          if (h === null) {
            if (a.isTargetWebComponent)
              break;
            return;
          }
          if (h === e)
            return;
          h = h.parentNode;
        }
      (typeof this.settings.outsideClickDeselects == "function" ? this.settings.outsideClickDeselects(l) : this.settings.outsideClickDeselects) ? this.hot.deselectCell() : this.hot.destroyEditor(!1, !1);
    });
    let s = Ja(r);
    for (; s !== null; )
      this.eventManager.addEventListener(s.document.documentElement, "click", () => {
        this.hot.unlisten();
      }), s = Ja(s);
    this.eventManager.addEventListener(An(or, this), "selectstart", (a) => {
      this.settings.fragmentSelection || ah(a.target) || a.preventDefault();
    });
  }
  /**
   * Translate renderable cell coordinates to visual coordinates.
   *
   * @param {CellCoords} coords The cell coordinates.
   * @returns {CellCoords}
   */
  translateFromRenderableToVisualCoords(e) {
    let {
      row: n,
      col: i
    } = e;
    return this.hot._createCellCoords(...this.translateFromRenderableToVisualIndex(n, i));
  }
  /**
   * Translate renderable row and column indexes to visual row and column indexes.
   *
   * @param {number} renderableRow Renderable row index.
   * @param {number} renderableColumn Renderable columnIndex.
   * @returns {number[]}
   */
  translateFromRenderableToVisualIndex(e, n) {
    let i = e >= 0 ? this.hot.rowIndexMapper.getVisualFromRenderableIndex(e) : e, r = n >= 0 ? this.hot.columnIndexMapper.getVisualFromRenderableIndex(n) : n;
    return i === null && (i = e), r === null && (r = n), [i, r];
  }
  /**
   * Returns the number of renderable indexes.
   *
   * @private
   * @param {IndexMapper} indexMapper The IndexMapper instance for specific axis.
   * @param {number} maxElements Maximum number of elements (rows or columns).
   *
   * @returns {number|*}
   */
  countRenderableIndexes(e, n) {
    const i = Math.min(e.getNotTrimmedIndexesLength(), n), r = e.getNearestNotHiddenIndex(i - 1, -1);
    return r === null ? 0 : e.getRenderableFromVisualIndex(r) + 1;
  }
  /**
   * Returns the number of renderable columns.
   *
   * @returns {number}
   */
  countRenderableColumns() {
    return this.countRenderableIndexes(this.hot.columnIndexMapper, this.settings.maxCols);
  }
  /**
   * Returns the number of renderable rows.
   *
   * @returns {number}
   */
  countRenderableRows() {
    return this.countRenderableIndexes(this.hot.rowIndexMapper, this.settings.maxRows);
  }
  /**
   * Returns number of not hidden row indexes counting from the passed starting index.
   * The counting direction can be controlled by `incrementBy` argument.
   *
   * @param {number} visualIndex The visual index from which the counting begins.
   * @param {number} incrementBy If `-1` then counting is backwards or forward when `1`.
   * @returns {number}
   */
  countNotHiddenRowIndexes(e, n) {
    return this.countNotHiddenIndexes(e, n, this.hot.rowIndexMapper, this.countRenderableRows());
  }
  /**
   * Returns number of not hidden column indexes counting from the passed starting index.
   * The counting direction can be controlled by `incrementBy` argument.
   *
   * @param {number} visualIndex The visual index from which the counting begins.
   * @param {number} incrementBy If `-1` then counting is backwards or forward when `1`.
   * @returns {number}
   */
  countNotHiddenColumnIndexes(e, n) {
    return this.countNotHiddenIndexes(e, n, this.hot.columnIndexMapper, this.countRenderableColumns());
  }
  /**
   * Returns number of not hidden indexes counting from the passed starting index.
   * The counting direction can be controlled by `incrementBy` argument.
   *
   * @param {number} visualIndex The visual index from which the counting begins.
   * @param {number} incrementBy If `-1` then counting is backwards or forward when `1`.
   * @param {IndexMapper} indexMapper The IndexMapper instance for specific axis.
   * @param {number} renderableIndexesCount Total count of renderable indexes for specific axis.
   * @returns {number}
   */
  countNotHiddenIndexes(e, n, i, r) {
    if (isNaN(e) || e < 0)
      return 0;
    const o = i.getNearestNotHiddenIndex(e, n), s = i.getRenderableFromVisualIndex(o);
    if (!Number.isInteger(s))
      return 0;
    let a = 0;
    return n < 0 ? a = s + 1 : n > 0 && (a = r - s), a;
  }
  /**
   * The function returns the number of not hidden column indexes that fit between the first and
   * last fixed column in the left (or right in RTL mode) overlay.
   *
   * @returns {number}
   */
  countNotHiddenFixedColumnsStart() {
    const e = this.hot.countCols(), n = Math.min(parseInt(this.settings.fixedColumnsStart, 10), e) - 1;
    return this.countNotHiddenColumnIndexes(n, -1);
  }
  /**
   * The function returns the number of not hidden row indexes that fit between the first and
   * last fixed row in the top overlay.
   *
   * @returns {number}
   */
  countNotHiddenFixedRowsTop() {
    const e = this.hot.countRows(), n = Math.min(parseInt(this.settings.fixedRowsTop, 10), e) - 1;
    return this.countNotHiddenRowIndexes(n, -1);
  }
  /**
   * The function returns the number of not hidden row indexes that fit between the first and
   * last fixed row in the bottom overlay.
   *
   * @returns {number}
   */
  countNotHiddenFixedRowsBottom() {
    const e = this.hot.countRows(), n = Math.max(e - parseInt(this.settings.fixedRowsBottom, 10), 0);
    return this.countNotHiddenRowIndexes(n, 1);
  }
  /**
   * The function returns the number of renderable column indexes within the passed range of the visual indexes.
   *
   * @param {number} columnStart The column visual start index.
   * @param {number} columnEnd The column visual end index.
   * @returns {number}
   */
  countRenderableColumnsInRange(e, n) {
    let i = 0;
    for (let r = e; r <= n; r++)
      this.hot.columnIndexMapper.getRenderableFromVisualIndex(r) !== null && (i += 1);
    return i;
  }
  /**
   * The function returns the number of renderable row indexes within the passed range of the visual indexes.
   *
   * @param {number} rowStart The row visual start index.
   * @param {number} rowEnd The row visual end index.
   * @returns {number}
   */
  countRenderableRowsInRange(e, n) {
    let i = 0;
    for (let r = e; r <= n; r++)
      this.hot.rowIndexMapper.getRenderableFromVisualIndex(r) !== null && (i += 1);
    return i;
  }
  /**
   * Checks if at least one cell than belongs to the main table is not covered by the top, left or
   * bottom overlay.
   *
   * @returns {boolean}
   */
  isMainTableNotFullyCoveredByOverlays() {
    const e = this.countNotHiddenFixedRowsTop() + this.countNotHiddenFixedRowsBottom(), n = this.countNotHiddenFixedColumnsStart();
    return this.hot.countRenderedRows() > e && this.hot.countRenderedCols() > n;
  }
  /**
   * Defines default configuration and initializes WalkOnTable instance.
   *
   * @private
   */
  initializeWalkontable() {
    const e = {
      ariaTags: this.settings.ariaTags,
      rtlMode: this.hot.isRtl(),
      externalRowCalculator: this.hot.getPlugin("autoRowSize") && this.hot.getPlugin("autoRowSize").isEnabled(),
      table: An(or, this),
      isDataViewInstance: () => Yg(this.hot),
      preventOverflow: () => this.settings.preventOverflow,
      preventWheel: () => this.settings.preventWheel,
      stretchH: () => this.settings.stretchH,
      data: (o, s) => this.hot.getDataAtCell(...this.translateFromRenderableToVisualIndex(o, s)),
      totalRows: () => this.countRenderableRows(),
      totalColumns: () => this.countRenderableColumns(),
      // Number of renderable columns for the left overlay.
      fixedColumnsStart: () => this.countNotHiddenFixedColumnsStart(),
      // Number of renderable rows for the top overlay.
      fixedRowsTop: () => this.countNotHiddenFixedRowsTop(),
      // Number of renderable rows for the bottom overlay.
      fixedRowsBottom: () => this.countNotHiddenFixedRowsBottom(),
      // Enable the inline start overlay when conditions are met.
      shouldRenderInlineStartOverlay: () => this.settings.fixedColumnsStart > 0 || e.rowHeaders().length > 0,
      // Enable the top overlay when conditions are met.
      shouldRenderTopOverlay: () => this.settings.fixedRowsTop > 0 || e.columnHeaders().length > 0,
      // Enable the bottom overlay when conditions are met.
      shouldRenderBottomOverlay: () => this.settings.fixedRowsBottom > 0,
      minSpareRows: () => this.settings.minSpareRows,
      renderAllRows: this.settings.renderAllRows,
      renderAllColumns: this.settings.renderAllColumns,
      rowHeaders: () => {
        const o = [];
        return this.hot.hasRowHeaders() && o.push((s, a) => {
          const l = s >= 0 ? this.hot.rowIndexMapper.getVisualFromRenderableIndex(s) : s;
          this.appendRowHeader(l, a);
        }), this.hot.runHooks("afterGetRowHeaderRenderers", o), Hi(yf, this, o.length), this.hot.getSettings().ariaTags && Bh(jg, this, n1).call(this) === this.hot.countCols() && Bh(jg, this, yX).call(this, An(yf, this)), o;
      },
      columnHeaders: () => {
        const o = [];
        return this.hot.hasColHeaders() && o.push((s, a) => {
          const l = s >= 0 ? this.hot.columnIndexMapper.getVisualFromRenderableIndex(s) : s;
          this.appendColHeader(l, a);
        }), this.hot.runHooks("afterGetColumnHeaderRenderers", o), Hi(dC, this, o.length), o;
      },
      columnWidth: (o) => {
        const s = this.hot.columnIndexMapper.getVisualFromRenderableIndex(o);
        return this.hot.getColWidth(s === null ? o : s);
      },
      rowHeight: (o) => {
        const s = this.hot.rowIndexMapper.getVisualFromRenderableIndex(o);
        return this.hot.getRowHeight(s === null ? o : s);
      },
      rowHeightByOverlayName: (o, s) => {
        const a = this.hot.rowIndexMapper.getVisualFromRenderableIndex(o), l = a === null ? o : a;
        return this.hot.runHooks("modifyRowHeightByOverlayName", this.hot.getRowHeight(l), l, s);
      },
      cellRenderer: (o, s, a) => {
        const [l, u] = this.translateFromRenderableToVisualIndex(o, s), c = this.hot.runHooks("modifyGetCellCoords", l, u);
        let h = l, d = u;
        Array.isArray(c) && ([h, d] = c);
        const g = this.hot.getCellMeta(h, d), m = this.hot.colToProp(d);
        let w = this.hot.getDataAtRowProp(h, m);
        this.hot.hasHook("beforeValueRender") && (w = this.hot.runHooks("beforeValueRender", w, g)), this.hot.runHooks("beforeRenderer", a, l, u, m, w, g), this.hot.getCellRenderer(g)(this.hot, a, l, u, m, w, g), this.hot.runHooks("afterRenderer", a, l, u, m, w, g);
      },
      selections: this.hot.selection.highlight,
      hideBorderOnMouseDownOver: () => this.settings.fragmentSelection,
      onWindowResize: () => {
        this.hot && !this.hot.isDestroyed && this.hot.refreshDimensions();
      },
      onContainerElementResize: () => {
        this.hot && !this.hot.isDestroyed && el(this.hot.rootElement) && this.hot.refreshDimensions();
      },
      onCellMouseDown: (o, s, a, l) => {
        const u = this.translateFromRenderableToVisualCoords(s), c = {
          row: !1,
          column: !1,
          cell: !1
        };
        this.hot.listen(), this.activeWt = l, Hi(bo, this, !0), this.hot.runHooks("beforeOnCellMouseDown", o, u, a, c), !Os(o) && (lT(o, {
          coords: u,
          selection: this.hot.selection,
          controller: c,
          cellCoordsFactory: (h, d) => this.hot._createCellCoords(h, d)
        }), this.hot.runHooks("afterOnCellMouseDown", o, u, a), this.activeWt = this._wt);
      },
      onCellContextMenu: (o, s, a, l) => {
        const u = this.translateFromRenderableToVisualCoords(s);
        this.activeWt = l, Hi(bo, this, !1), this.hot.selection.isInProgress() && this.hot.selection.finish(), this.hot.runHooks("beforeOnCellContextMenu", o, u, a), !Os(o) && (this.hot.runHooks("afterOnCellContextMenu", o, u, a), this.activeWt = this._wt);
      },
      onCellMouseOut: (o, s, a, l) => {
        const u = this.translateFromRenderableToVisualCoords(s);
        this.activeWt = l, this.hot.runHooks("beforeOnCellMouseOut", o, u, a), !Os(o) && (this.hot.runHooks("afterOnCellMouseOut", o, u, a), this.activeWt = this._wt);
      },
      onCellMouseOver: (o, s, a, l) => {
        const u = this.translateFromRenderableToVisualCoords(s), c = {
          row: !1,
          column: !1,
          cell: !1
        };
        this.activeWt = l, this.hot.runHooks("beforeOnCellMouseOver", o, u, a, c), !Os(o) && (An(bo, this) && lT(o, {
          coords: u,
          selection: this.hot.selection,
          controller: c,
          cellCoordsFactory: (h, d) => this.hot._createCellCoords(h, d)
        }), this.hot.runHooks("afterOnCellMouseOver", o, u, a), this.activeWt = this._wt);
      },
      onCellMouseUp: (o, s, a, l) => {
        const u = this.translateFromRenderableToVisualCoords(s);
        this.activeWt = l, this.hot.runHooks("beforeOnCellMouseUp", o, u, a), !(Os(o) || this.hot.isDestroyed) && (this.hot.runHooks("afterOnCellMouseUp", o, u, a), this.activeWt = this._wt);
      },
      onCellCornerMouseDown: (o) => {
        o.preventDefault(), this.hot.runHooks("afterOnCellCornerMouseDown", o);
      },
      onCellCornerDblClick: (o) => {
        o.preventDefault(), this.hot.runHooks("afterOnCellCornerDblClick", o);
      },
      beforeDraw: (o, s) => this.beforeRender(o, s),
      onDraw: (o) => this.afterRender(o),
      onBeforeViewportScrollVertically: (o) => {
        const s = this.hot.rowIndexMapper, a = o < 0;
        let l = o;
        return !a && (l = s.getVisualFromRenderableIndex(o), l === null) ? o : (l = this.hot.runHooks("beforeViewportScrollVertically", l), this.hot.runHooks("beforeViewportScroll"), a ? l : s.getRenderableFromVisualIndex(l));
      },
      onBeforeViewportScrollHorizontally: (o) => {
        const s = this.hot.columnIndexMapper, a = o < 0;
        let l = o;
        return !a && (l = s.getVisualFromRenderableIndex(o), l === null) ? o : (l = this.hot.runHooks("beforeViewportScrollHorizontally", l), this.hot.runHooks("beforeViewportScroll"), a ? l : s.getRenderableFromVisualIndex(l));
      },
      onScrollVertically: () => {
        this.hot.runHooks("afterScrollVertically"), this.hot.runHooks("afterScroll");
      },
      onScrollHorizontally: () => {
        this.hot.runHooks("afterScrollHorizontally"), this.hot.runHooks("afterScroll");
      },
      onBeforeRemoveCellClassNames: () => this.hot.runHooks("beforeRemoveCellClassNames"),
      onBeforeHighlightingRowHeader: (o, s, a) => {
        const l = this.hot.rowIndexMapper, u = o < 0;
        let c = o;
        u || (c = l.getVisualFromRenderableIndex(o));
        const h = this.hot.runHooks("beforeHighlightingRowHeader", c, s, a);
        return u ? h : l.getRenderableFromVisualIndex(l.getNearestNotHiddenIndex(h, 1));
      },
      onBeforeHighlightingColumnHeader: (o, s, a) => {
        const l = this.hot.columnIndexMapper, u = o < 0;
        let c = o;
        u || (c = l.getVisualFromRenderableIndex(o));
        const h = this.hot.runHooks("beforeHighlightingColumnHeader", c, s, a);
        return u ? h : l.getRenderableFromVisualIndex(l.getNearestNotHiddenIndex(h, 1));
      },
      onAfterDrawSelection: (o, s, a) => {
        let l;
        const [u, c] = this.translateFromRenderableToVisualIndex(o, s), h = this.hot.selection.getSelectedRange();
        if (h.size() > 0) {
          const g = h.peekByIndex(a != null ? a : 0);
          l = [g.from.row, g.from.col, g.to.row, g.to.col];
        }
        return this.hot.runHooks("afterDrawSelection", u, c, l, a);
      },
      onBeforeDrawBorders: (o, s) => {
        const [a, l, u, c] = o, h = [this.hot.rowIndexMapper.getVisualFromRenderableIndex(a), this.hot.columnIndexMapper.getVisualFromRenderableIndex(l), this.hot.rowIndexMapper.getVisualFromRenderableIndex(u), this.hot.columnIndexMapper.getVisualFromRenderableIndex(c)];
        return this.hot.runHooks("beforeDrawBorders", h, s);
      },
      onBeforeTouchScroll: () => this.hot.runHooks("beforeTouchScroll"),
      onAfterMomentumScroll: () => this.hot.runHooks("afterMomentumScroll"),
      onBeforeStretchingColumnWidth: (o, s) => {
        const a = this.hot.columnIndexMapper.getVisualFromRenderableIndex(s);
        return this.hot.runHooks("beforeStretchingColumnWidth", o, a);
      },
      onModifyRowHeaderWidth: (o) => this.hot.runHooks("modifyRowHeaderWidth", o),
      onModifyGetCellCoords: (o, s, a) => {
        const l = this.hot.rowIndexMapper, u = this.hot.columnIndexMapper, c = s >= 0 ? u.getVisualFromRenderableIndex(s) : s, h = o >= 0 ? l.getVisualFromRenderableIndex(o) : o, d = this.hot.runHooks("modifyGetCellCoords", h, c, a);
        if (Array.isArray(d)) {
          const [g, m, w, C] = d;
          return [g >= 0 ? l.getRenderableFromVisualIndex(l.getNearestNotHiddenIndex(g, 1)) : g, m >= 0 ? u.getRenderableFromVisualIndex(u.getNearestNotHiddenIndex(m, 1)) : m, w >= 0 ? l.getRenderableFromVisualIndex(l.getNearestNotHiddenIndex(w, -1)) : w, C >= 0 ? u.getRenderableFromVisualIndex(u.getNearestNotHiddenIndex(C, -1)) : C];
        }
      },
      viewportRowCalculatorOverride: (o) => {
        let s = this.settings.viewportRowRenderingOffset;
        if (s === "auto" && this.settings.fixedRowsTop && (s = 10), s > 0 || s === "auto") {
          const a = this.countRenderableRows(), l = o.startRow, u = o.endRow;
          if (typeof s == "number")
            o.startRow = Math.max(l - s, 0), o.endRow = Math.min(u + s, a - 1);
          else if (s === "auto") {
            const c = Math.ceil(u / a * 12);
            o.startRow = Math.max(l - c, 0), o.endRow = Math.min(u + c, a - 1);
          }
        }
        this.hot.runHooks("afterViewportRowCalculatorOverride", o);
      },
      viewportColumnCalculatorOverride: (o) => {
        let s = this.settings.viewportColumnRenderingOffset;
        if (s === "auto" && this.settings.fixedColumnsStart && (s = 10), s > 0 || s === "auto") {
          const a = this.countRenderableColumns(), l = o.startColumn, u = o.endColumn;
          if (typeof s == "number" && (o.startColumn = Math.max(l - s, 0), o.endColumn = Math.min(u + s, a - 1)), s === "auto") {
            const c = Math.ceil(u / a * 6);
            o.startColumn = Math.max(l - c, 0), o.endColumn = Math.min(u + c, a - 1);
          }
        }
        this.hot.runHooks("afterViewportColumnCalculatorOverride", o);
      },
      rowHeaderWidth: () => this.settings.rowHeaderWidth,
      columnHeaderHeight: () => {
        const o = this.hot.runHooks("modifyColumnHeaderHeight");
        return this.settings.columnHeaderHeight || o;
      }
    };
    this.hot.runHooks("beforeInitWalkontable", e), this._wt = new Vb(e), this.activeWt = this._wt;
    const n = this._wt.wtTable.spreader, {
      width: i,
      height: r
    } = this.hot.rootElement.getBoundingClientRect();
    this.setLastSize(i, r), this.eventManager.addEventListener(n, "mousedown", (o) => {
      o.target === n && o.which === 3 && o.stopPropagation();
    }), this.eventManager.addEventListener(n, "contextmenu", (o) => {
      o.target === n && o.which === 3 && o.stopPropagation();
    }), this.eventManager.addEventListener(this.hot.rootDocument.documentElement, "click", () => {
      this.settings.observeDOMVisibility && this._wt.drawInterrupted && (this.hot.forceFullRender = !0, this.render());
    });
  }
  /**
   * Checks if it's possible to create text selection in element.
   *
   * @private
   * @param {HTMLElement} el The element to check.
   * @returns {boolean}
   */
  isTextSelectionAllowed(e) {
    if (ah(e))
      return !0;
    const n = Vs(e, this._wt.wtTable.spreader);
    return !!(this.settings.fragmentSelection === !0 && n || this.settings.fragmentSelection === "cell" && this.isSelectedOnlyCell() && n || !this.settings.fragmentSelection && this.isCellEdited() && this.isSelectedOnlyCell());
  }
  /**
   * Checks if user's been called mousedown.
   *
   * @private
   * @returns {boolean}
   */
  isMouseDown() {
    return An(bo, this);
  }
  /**
   * Check if selected only one cell.
   *
   * @private
   * @returns {boolean}
   */
  isSelectedOnlyCell() {
    var e, n;
    return (e = (n = this.hot.getSelectedRangeLast()) === null || n === void 0 ? void 0 : n.isSingleCell()) !== null && e !== void 0 ? e : !1;
  }
  /**
   * Checks if active cell is editing.
   *
   * @private
   * @returns {boolean}
   */
  isCellEdited() {
    const e = this.hot.getActiveEditor();
    return e && e.isOpened();
  }
  /**
   * `beforeDraw` callback.
   *
   * @private
   * @param {boolean} force If `true` rendering was triggered by a change of settings or data or `false` if
   *                        rendering was triggered by scrolling or moving selection.
   * @param {object} skipRender Object with `skipRender` property, if it is set to `true ` the next rendering
   *                            cycle will be skipped.
   */
  beforeRender(e, n) {
    e && this.hot.runHooks("beforeViewRender", this.hot.forceFullRender, n);
  }
  /**
   * `afterRender` callback.
   *
   * @private
   * @param {boolean} force If `true` rendering was triggered by a change of settings or data or `false` if
   *                        rendering was triggered by scrolling or moving selection.
   */
  afterRender(e) {
    e && this.hot.runHooks("afterViewRender", this.hot.forceFullRender);
  }
  /**
   * Append row header to a TH element.
   *
   * @private
   * @param {number} visualRowIndex The visual row index.
   * @param {HTMLTableHeaderCellElement} TH The table header element.
   */
  appendRowHeader(e, n) {
    if (n.firstChild) {
      const i = n.firstChild;
      if (!Te(i, "relative")) {
        br(n), this.appendRowHeader(e, n);
        return;
      }
      this.updateCellHeader(i.querySelector(".rowHeader"), e, this.hot.getRowHeader);
    } else {
      const {
        rootDocument: i,
        getRowHeader: r
      } = this.hot, o = i.createElement("div"), s = i.createElement("span");
      o.className = "relative", s.className = "rowHeader", this.updateCellHeader(s, e, r), o.appendChild(s), n.appendChild(o);
    }
    this.hot.runHooks("afterGetRowHeader", e, n);
  }
  /**
   * Append column header to a TH element.
   *
   * @private
   * @param {number} visualColumnIndex Visual column index.
   * @param {HTMLTableCellElement} TH The table header element.
   * @param {Function} [label] The function that returns the header label.
   * @param {number} [headerLevel=0] The index of header level counting from the top (positive
   *                                 values counting from 0 to N).
   */
  appendColHeader(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : this.hot.getColHeader, r = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 0;
    const o = () => {
      const s = e >= 0 ? this.hot.getColumnMeta(e).headerClassName : null;
      return s ? s.split(" ") : [];
    };
    if (n.firstChild) {
      const s = n.firstChild;
      Te(s, "relative") ? (this.updateCellHeader(s.querySelector(".colHeader"), e, i, r), s.className = "", Q(s, ["relative", ...o()])) : (br(n), this.appendColHeader(e, n, i, r));
    } else {
      const {
        rootDocument: s
      } = this.hot, a = s.createElement("div"), l = s.createElement("span"), u = o();
      a.classList.add("relative", ...u), l.className = "colHeader", this.settings.ariaTags && (Ee(a, ...oo()), Ee(l, ...oo())), this.updateCellHeader(l, e, i, r), a.appendChild(l), n.appendChild(a);
    }
    this.hot.runHooks("afterGetColHeader", e, n, r);
  }
  /**
   * Updates header cell content.
   *
   * @private
   * @param {HTMLElement} element Element to update.
   * @param {number} index Row index or column index.
   * @param {Function} content Function which should be returns content for this cell.
   * @param {number} [headerLevel=0] The index of header level counting from the top (positive
   *                                 values counting from 0 to N).
   */
  updateCellHeader(e, n, i) {
    let r = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 0, o = n;
    const s = this._wt.wtOverlays.getParentOverlay(e) || this._wt;
    e.parentNode && (Te(e, "colHeader") ? o = s.wtTable.columnFilter.sourceToRendered(n) : Te(e, "rowHeader") && (o = s.wtTable.rowFilter.sourceToRendered(n))), o > -1 ? pl(e, i(n, r)) : (Gs(e, String.fromCharCode(160)), Q(e, "cornerHeader"));
  }
  /**
   * Given a element's left (or right in RTL mode) position relative to the viewport, returns maximum
   * element width until the right (or left) edge of the viewport (before scrollbar).
   *
   * @private
   * @param {number} inlineOffset The left (or right in RTL mode) offset.
   * @returns {number}
   */
  maximumVisibleElementWidth(e) {
    const i = this._wt.wtViewport.getWorkspaceWidth() - e;
    return i > 0 ? i : 0;
  }
  /**
   * Given a element's top position relative to the viewport, returns maximum element height until the bottom
   * edge of the viewport (before scrollbar).
   *
   * @private
   * @param {number} topOffset The top offset.
   * @returns {number}
   */
  maximumVisibleElementHeight(e) {
    const i = this._wt.wtViewport.getWorkspaceHeight() - e;
    return i > 0 ? i : 0;
  }
  /**
   * Sets new dimensions of the container.
   *
   * @param {number} width The table width.
   * @param {number} height The table height.
   */
  setLastSize(e, n) {
    Hi(fC, this, e), Hi(gC, this, n);
  }
  /**
   * Returns cached dimensions.
   *
   * @returns {object}
   */
  getLastSize() {
    return {
      width: An(fC, this),
      height: An(gC, this)
    };
  }
  /**
   * Returns the first rendered row in the DOM (usually is not visible in the table's viewport).
   *
   * @returns {number | null}
   */
  getFirstRenderedVisibleRow() {
    return this._wt.wtViewport.rowsRenderCalculator ? this.hot.rowIndexMapper.getNearestNotHiddenIndex(this._wt.wtTable.getFirstRenderedRow(), 1) : null;
  }
  /**
   * Returns the last rendered row in the DOM (usually is not visible in the table's viewport).
   *
   * @returns {number | null}
   */
  getLastRenderedVisibleRow() {
    return this._wt.wtViewport.rowsRenderCalculator ? this.hot.rowIndexMapper.getNearestNotHiddenIndex(this._wt.wtTable.getLastRenderedRow(), -1) : null;
  }
  /**
   * Returns the first rendered column in the DOM (usually is not visible in the table's viewport).
   *
   * @returns {number | null}
   */
  getFirstRenderedVisibleColumn() {
    return this._wt.wtViewport.columnsRenderCalculator ? this.hot.columnIndexMapper.getNearestNotHiddenIndex(this._wt.wtTable.getFirstRenderedColumn(), 1) : null;
  }
  /**
   * Returns the last rendered column in the DOM (usually is not visible in the table's viewport).
   *
   * @returns {number | null}
   */
  getLastRenderedVisibleColumn() {
    return this._wt.wtViewport.columnsRenderCalculator ? this.hot.columnIndexMapper.getNearestNotHiddenIndex(this._wt.wtTable.getLastRenderedColumn(), -1) : null;
  }
  /**
   * Returns the first fully visible row in the table viewport. When the table has overlays the method returns
   * the first row of the master table that is not overlapped by overlay.
   *
   * @returns {number}
   */
  getFirstFullyVisibleRow() {
    return this.hot.rowIndexMapper.getVisualFromRenderableIndex(this._wt.wtScroll.getFirstVisibleRow());
  }
  /**
   * Returns the last fully visible row in the table viewport. When the table has overlays the method returns
   * the first row of the master table that is not overlapped by overlay.
   *
   * @returns {number}
   */
  getLastFullyVisibleRow() {
    return this.hot.rowIndexMapper.getVisualFromRenderableIndex(this._wt.wtScroll.getLastVisibleRow());
  }
  /**
   * Returns the first fully visible column in the table viewport. When the table has overlays the method returns
   * the first row of the master table that is not overlapped by overlay.
   *
   * @returns {number}
   */
  getFirstFullyVisibleColumn() {
    return this.hot.columnIndexMapper.getVisualFromRenderableIndex(this._wt.wtScroll.getFirstVisibleColumn());
  }
  /**
   * Returns the last fully visible column in the table viewport. When the table has overlays the method returns
   * the first row of the master table that is not overlapped by overlay.
   *
   * @returns {number}
   */
  getLastFullyVisibleColumn() {
    return this.hot.columnIndexMapper.getVisualFromRenderableIndex(this._wt.wtScroll.getLastVisibleColumn());
  }
  /**
   * Returns the first partially visible row in the table viewport. When the table has overlays the method returns
   * the first row of the master table that is not overlapped by overlay.
   *
   * @returns {number}
   */
  getFirstPartiallyVisibleRow() {
    return this.hot.rowIndexMapper.getVisualFromRenderableIndex(this._wt.wtScroll.getFirstPartiallyVisibleRow());
  }
  /**
   * Returns the last partially visible row in the table viewport. When the table has overlays the method returns
   * the first row of the master table that is not overlapped by overlay.
   *
   * @returns {number}
   */
  getLastPartiallyVisibleRow() {
    return this.hot.rowIndexMapper.getVisualFromRenderableIndex(this._wt.wtScroll.getLastPartiallyVisibleRow());
  }
  /**
   * Returns the first partially visible column in the table viewport. When the table has overlays the method returns
   * the first row of the master table that is not overlapped by overlay.
   *
   * @returns {number}
   */
  getFirstPartiallyVisibleColumn() {
    return this.hot.columnIndexMapper.getVisualFromRenderableIndex(this._wt.wtScroll.getFirstPartiallyVisibleColumn());
  }
  /**
   * Returns the last partially visible column in the table viewport. When the table has overlays the method returns
   * the first row of the master table that is not overlapped by overlay.
   *
   * @returns {number}
   */
  getLastPartiallyVisibleColumn() {
    return this.hot.columnIndexMapper.getVisualFromRenderableIndex(this._wt.wtScroll.getLastPartiallyVisibleColumn());
  }
  /**
   * Returns the total count of the rendered column headers.
   *
   * @returns {number}
   */
  getColumnHeadersCount() {
    return An(dC, this);
  }
  /**
   * Returns the total count of the rendered row headers.
   *
   * @returns {number}
   */
  getRowHeadersCount() {
    return An(yf, this);
  }
  /**
   * Returns the table's viewport width. When the table has defined the size of the container,
   * and the columns do not fill the entire viewport, the viewport width is equal to the sum of
   * the columns' widths.
   *
   * @returns {number}
   */
  getViewportWidth() {
    return this._wt.wtViewport.getViewportWidth();
  }
  /**
   * Returns the table's total width including the scrollbar width.
   *
   * @returns {number}
   */
  getWorkspaceWidth() {
    return this._wt.wtViewport.getWorkspaceWidth();
  }
  /**
   * Returns the table's viewport height. When the table has defined the size of the container,
   * and the rows do not fill the entire viewport, the viewport height is equal to the sum of
   * the rows' heights.
   *
   * @returns {number}
   */
  getViewportHeight() {
    return this._wt.wtViewport.getViewportHeight();
  }
  /**
   * Returns the table's total height including the scrollbar height.
   *
   * @returns {number}
   */
  getWorkspaceHeight() {
    return this._wt.wtViewport.getWorkspaceHeight();
  }
  /**
   * Checks to what overlay the provided element belongs.
   *
   * @param {HTMLElement} element The DOM element to check.
   * @returns {'master'|'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'}
   */
  getElementOverlayName(e) {
    var n;
    return ((n = this._wt.wtOverlays.getParentOverlay(e)) !== null && n !== void 0 ? n : this._wt).wtTable.name;
  }
  /**
   * Gets the overlay instance by its name.
   *
   * @param {'inline_start'|'top'|'top_inline_start_corner'|'bottom'|'bottom_inline_start_corner'} overlayName The overlay name.
   * @returns {Overlay | null}
   */
  getOverlayByName(e) {
    return this._wt.getOverlayByName(e);
  }
  /**
   * Checks if the table is visible or not.
   *
   * @returns {boolean}
   */
  isVisible() {
    return this._wt.wtTable.isVisible();
  }
  /**
   * Destroys internal WalkOnTable's instance. Detaches all of the bonded listeners.
   *
   * @private
   */
  destroy() {
    this._wt.destroy(), this.eventManager.destroy();
  }
}
function n1() {
  return parseInt(this.hot.rootElement.getAttribute(bp()[0]), 10);
}
function yX(t) {
  const e = Bh(jg, this, n1).call(this) + t;
  Ee(this.hot.rootElement, ...bp(e));
}
const SX = vX, i1 = "ABCDEFGHIJKLMNOPQRSTUVWXYZ", Py = i1.length;
function Dp(t) {
  let e = t + 1, n = "", i;
  for (; e > 0; )
    i = (e - 1) % Py, n = String.fromCharCode(65 + i) + n, e = parseInt((e - i) / Py, 10);
  return n;
}
function bX(t) {
  let e = 0;
  if (t)
    for (let n = 0, i = t.length - 1; n < t.length; n += 1, i -= 1)
      e += Wd(Py, i) * (i1.indexOf(t[n]) + 1);
  return e -= 1, e;
}
function RX() {
  let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 100, e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 4;
  const n = [];
  let i, r;
  for (i = 0; i < t; i++) {
    const o = [];
    for (r = 0; r < e; r++)
      o.push(Dp(r) + (i + 1));
    n.push(o);
  }
  return n;
}
function EX() {
  let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 100, e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 4;
  const n = [];
  let i, r;
  for (i = 0; i < t; i++) {
    const o = {};
    for (r = 0; r < e; r++)
      o[`prop${r}`] = Dp(r) + (i + 1);
    n.push(o);
  }
  return n;
}
function TX(t, e) {
  const n = [];
  let i;
  for (let r = 0; r < t; r++) {
    i = [];
    for (let o = 0; o < e; o++)
      i.push("");
    n.push(i);
  }
  return n;
}
function $b(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, n = t;
  const i = [];
  return (!Array.isArray(t) || !Array.isArray(t[0])) && (n = [t]), n.forEach((r, o) => {
    Array.isArray(r) ? r.forEach((s, a) => {
      i.push([o + e, a, s]);
    }) : Object.keys(r).forEach((s) => {
      i.push([o + e, s, r[s]]);
    });
  }), i;
}
function Ub(t) {
  let e = 0;
  return Array.isArray(t) && (t[0] && Array.isArray(t[0]) ? e = t[0].length : t[0] && Be(t[0]) && (e = Pp(t[0]))), e;
}
function r1(t) {
  return !!(Array.isArray(t) && t.length && t.every((e) => Array.isArray(e)));
}
function o1(t) {
  return !!(Array.isArray(t) && t.length && t.every((e) => typeof e == "object" && !Array.isArray(e) && e !== null));
}
const MX = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  countFirstRowKeys: Ub,
  createEmptySpreadsheetData: TX,
  createSpreadsheetData: RX,
  createSpreadsheetObjectData: EX,
  dataRowToChangesArray: $b,
  isArrayOfArrays: r1,
  isArrayOfObjects: o1,
  spreadsheetColumnIndex: bX,
  spreadsheetColumnLabel: Dp
}, Symbol.toStringTag, { value: "Module" }));
function hc(t, e, n) {
  return (e = IX(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function IX(t) {
  var e = OX(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function OX(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class PX {
  constructor(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
    hc(this, "hot", void 0), hc(this, "data", void 0), hc(this, "dataType", "array"), hc(this, "colToProp", () => {
    }), hc(this, "propToCol", () => {
    }), this.hot = e, this.data = n;
  }
  /**
   * Run the `modifyRowData` hook and return either the modified or the source data for the provided row.
   *
   * @private
   * @param {number} rowIndex Row index.
   * @returns {Array|object} Source or modified row of data.
   */
  modifyRowData(e) {
    let n;
    return this.hot.hasHook("modifyRowData") && (n = this.hot.runHooks("modifyRowData", e)), n !== void 0 && !Number.isInteger(n) ? n : this.data[e];
  }
  /**
   * Get all data.
   *
   * @param {boolean} [toArray=false] If `true` return source data as an array of arrays even when source data was provided
   *                                  in another format.
   * @returns {Array}
   */
  getData() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    return !this.data || this.data.length === 0 ? this.data : this.getByRange(null, null, e);
  }
  /**
   * Set new data source.
   *
   * @param {Array} data The new data.
   */
  setData(e) {
    this.data = e;
  }
  /**
   * Returns array of column values from the data source. `column` is the index of the row in the data source.
   *
   * @param {number} column Visual column index.
   * @returns {Array}
   */
  getAtColumn(e) {
    const n = [];
    return T(this.data, (i, r) => {
      const o = this.getAtCell(r, e);
      n.push(o);
    }), n;
  }
  /**
   * Returns a single row of the data or a subset of its columns. If a column range or `toArray` arguments are provided, it
   * operates only on the columns declared by the `columns` setting or the data schema.
   *
   * @param {number} row Physical row index.
   * @param {number} [startColumn] Starting index for the column range (optional).
   * @param {number} [endColumn] Ending index for the column range (optional).
   * @param {boolean} [toArray=false] `true` if the returned value should be forced to be presented as an array.
   * @returns {Array|object}
   */
  getAtRow(e, n, i) {
    let r = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1;
    const o = n === void 0 && i === void 0, {
      dataDotNotation: s
    } = this.hot.getSettings();
    let a = null, l = null;
    if (a = this.modifyRowData(e), Array.isArray(a))
      l = [], o ? a.forEach((u, c) => {
        l[c] = this.getAtPhysicalCell(e, c, a);
      }) : Ae(n, i, (u) => {
        l[u - n] = this.getAtPhysicalCell(e, u, a);
      });
    else if (Be(a) || Et(a))
      if (r ? l = [] : l = {}, !o || r) {
        const c = this.countFirstRowKeys() - 1;
        Ae(0, c, (h) => {
          const d = this.colToProp(h);
          if (h >= (n || 0) && h <= (i || c) && !Number.isInteger(d)) {
            const g = this.getAtPhysicalCell(e, d, a);
            r ? l.push(g) : s ? Vg(l, d, g) : l[d] = g;
          }
        });
      } else
        Fe(a, (u, c) => {
          const h = this.getAtPhysicalCell(e, c, a);
          s ? Vg(l, c, h) : l[c] = h;
        });
    return l;
  }
  /**
   * Set the provided value in the source data set at the provided coordinates.
   *
   * @param {number} row Physical row index.
   * @param {number|string} column Property name / physical column index.
   * @param {*} value The value to be set at the provided coordinates.
   */
  setAtCell(e, n, i) {
    if (!(e >= this.countRows() || n >= this.countFirstRowKeys())) {
      if (this.hot.hasHook("modifySourceData")) {
        const r = ji(i);
        this.hot.runHooks("modifySourceData", e, n, r, "set"), r.isTouched() && (i = r.value);
      }
      Number.isInteger(n) ? this.data[e][n] = i : Vg(this.data[e], n, i);
    }
  }
  /**
   * Get data from the source data set using the physical indexes.
   *
   * @private
   * @param {number} row Physical row index.
   * @param {string|number|Function} column Physical column index / property / function.
   * @param {Array|object} dataRow A representation of a data row.
   * @returns {*} Value at the provided coordinates.
   */
  getAtPhysicalCell(e, n, i) {
    let r = null;
    if (i)
      if (typeof n == "string") {
        const {
          dataDotNotation: o
        } = this.hot.getSettings();
        r = o ? tx(i, n) : i[n];
      } else
        typeof n == "function" ? r = n(i) : r = i[n];
    if (this.hot.hasHook("modifySourceData")) {
      const o = ji(r);
      this.hot.runHooks("modifySourceData", e, n, o, "get"), o.isTouched() && (r = o.value);
    }
    return r;
  }
  /**
   * Returns a single value from the data.
   *
   * @param {number} row Physical row index.
   * @param {number} columnOrProp Visual column index or property.
   * @returns {*}
   */
  getAtCell(e, n) {
    const i = this.modifyRowData(e);
    return this.getAtPhysicalCell(e, this.colToProp(n), i);
  }
  /**
   * Returns source data by passed range.
   *
   * @param {object} [start] Object with physical `row` and `col` keys (or visual column index, if data type is an array of objects).
   * @param {object} [end] Object with physical `row` and `col` keys (or visual column index, if data type is an array of objects).
   * @param {boolean} [toArray=false] If `true` return source data as an array of arrays even when source data was provided
   *                                  in another format.
   * @returns {Array}
   */
  getByRange() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null, n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1, r = !1, o = null, s = null, a = null, l = null;
    e === null || n === null ? (r = !0, o = 0, a = this.countRows() - 1) : (o = Math.min(e.row, n.row), s = Math.min(e.col, n.col), a = Math.max(e.row, n.row), l = Math.max(e.col, n.col));
    const u = [];
    return Ae(o, a, (c) => {
      u.push(r ? this.getAtRow(c, void 0, void 0, i) : this.getAtRow(c, s, l, i));
    }), u;
  }
  /**
   * Count number of rows.
   *
   * @returns {number}
   */
  countRows() {
    if (this.hot.hasHook("modifySourceLength")) {
      const e = this.hot.runHooks("modifySourceLength");
      if (Number.isInteger(e))
        return e;
    }
    return this.data.length;
  }
  /**
   * Count number of columns.
   *
   * @returns {number}
   */
  countFirstRowKeys() {
    return Ub(this.data);
  }
  /**
   * Destroy instance.
   */
  destroy() {
    this.data = null, this.hot = null;
  }
}
const xX = PX;
function uT(t, e, n) {
  return (e = HX(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function HX(t) {
  var e = _X(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function _X(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class wl {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
    uT(this, "indexedValues", []), uT(this, "initValueOrFn", void 0), this.initValueOrFn = e;
  }
  /**
   * Get full list of values for particular indexes.
   *
   * @returns {Array}
   */
  getValues() {
    return this.indexedValues;
  }
  /**
   * Get value for the particular index.
   *
   * @param {number} index Index for which value is got.
   * @returns {*}
   */
  getValueAtIndex(e) {
    const n = this.indexedValues;
    if (e < n.length)
      return n[e];
  }
  /**
   * Set new values for particular indexes.
   *
   * Note: Please keep in mind that `change` hook triggered by the method may not update cache of a collection immediately.
   *
   * @param {Array} values List of set values.
   */
  setValues(e) {
    this.indexedValues = e.slice(), this.runLocalHooks("change");
  }
  /**
   * Set new value for the particular index.
   *
   * @param {number} index The index.
   * @param {*} value The value to save.
   *
   * Note: Please keep in mind that it is not possible to set value beyond the map (not respecting already set
   * map's size). Please use the `setValues` method when you would like to extend the map.
   * Note: Please keep in mind that `change` hook triggered by the method may not update cache of a collection immediately.
   *
   * @returns {boolean}
   */
  setValueAtIndex(e, n) {
    return e < this.indexedValues.length ? (this.indexedValues[e] = n, this.runLocalHooks("change"), !0) : !1;
  }
  /**
   * Clear all values to the defaults.
   */
  clear() {
    this.setDefaultValues();
  }
  /**
   * Get length of the index map.
   *
   * @returns {number}
   */
  getLength() {
    return this.getValues().length;
  }
  /**
   * Set default values for elements from `0` to `n`, where `n` is equal to the handled variable.
   *
   * Note: Please keep in mind that `change` hook triggered by the method may not update cache of a collection immediately.
   *
   * @private
   * @param {number} [length] Length of list.
   */
  setDefaultValues() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.indexedValues.length;
    this.indexedValues.length = 0, Et(this.initValueOrFn) ? Ae(e - 1, (n) => this.indexedValues.push(this.initValueOrFn(n))) : Ae(e - 1, () => this.indexedValues.push(this.initValueOrFn)), this.runLocalHooks("change");
  }
  /**
   * Initialize list with default values for particular indexes.
   *
   * @private
   * @param {number} length New length of indexed list.
   * @returns {IndexMap}
   */
  init(e) {
    return this.setDefaultValues(e), this.runLocalHooks("init"), this;
  }
  /**
   * Add values to the list.
   *
   * Note: Please keep in mind that `change` hook triggered by the method may not update cache of a collection immediately.
   *
   * @private
   */
  insert() {
    this.runLocalHooks("change");
  }
  /**
   * Remove values from the list.
   *
   * Note: Please keep in mind that `change` hook triggered by the method may not update cache of a collection immediately.
   *
   * @private
   */
  remove() {
    this.runLocalHooks("change");
  }
  /**
   * Destroys the Map instance.
   */
  destroy() {
    this.clearLocalHooks(), this.indexedValues = null, this.initValueOrFn = null;
  }
}
pt(wl, Vn);
function Yb(t, e, n, i) {
  const r = n.length ? n[0] : void 0;
  return [...t.slice(0, r), ...n.map((o, s) => Et(i) ? i(o, s) : i), ...r === void 0 ? [] : t.slice(r)];
}
function jb(t, e) {
  return Zo(t, (n, i) => e.includes(i) === !1);
}
class Zs extends wl {
  /**
   * Add values to list and reorganize.
   *
   * @private
   * @param {number} insertionIndex Position inside the list.
   * @param {Array} insertedIndexes List of inserted indexes.
   */
  insert(e, n) {
    this.indexedValues = Yb(this.indexedValues, e, n, this.initValueOrFn), super.insert(e, n);
  }
  /**
   * Remove values from the list and reorganize.
   *
   * @private
   * @param {Array} removedIndexes List of removed indexes.
   */
  remove(e) {
    this.indexedValues = jb(this.indexedValues, e), super.remove(e);
  }
}
class kp extends Zs {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    super(e);
  }
  /**
   * Get physical indexes which are hidden.
   *
   * Note: Indexes marked as hidden are included in a {@link DataMap}, but aren't rendered.
   *
   * @returns {Array}
   */
  getHiddenIndexes() {
    return rn(this.getValues(), (e, n, i) => (n && e.push(i), e), []);
  }
}
function Gb(t, e, n) {
  return [...t.slice(0, e), ...n, ...t.slice(e)];
}
function Fh(t, e) {
  return Zo(t, (n) => e.includes(n) === !1);
}
function zb(t, e) {
  return Ke(t, (n) => n - e.filter((i) => i < n).length);
}
function Kb(t, e) {
  const n = e[0], i = e.length;
  return Ke(t, (r) => r >= n ? r + i : r);
}
function NX(t, e, n) {
  return (e = DX(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function DX(t) {
  var e = kX(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function kX(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Lp extends wl {
  constructor() {
    super(...arguments), NX(this, "orderOfIndexes", []);
  }
  /**
   * Get full list of ordered values for particular indexes.
   *
   * @returns {Array}
   */
  getValues() {
    return this.orderOfIndexes.map((e) => this.indexedValues[e]);
  }
  /**
   * Set new values for particular indexes. Entries are linked and stored in a certain order.
   *
   * Note: Please keep in mind that `change` hook triggered by the method may not update cache of a collection immediately.
   *
   * @param {Array} values List of set values.
   */
  setValues(e) {
    this.orderOfIndexes = [...Array(e.length).keys()], super.setValues(e);
  }
  /**
   * Set value at index and add it to the linked list of entries. Entries are stored in a certain order.
   *
   * Note: Value will be added at the end of the queue.
   *
   * @param {number} index The index.
   * @param {*} value The value to save.
   * @param {number} position Position to which entry will be added.
   *
   * @returns {boolean}
   */
  setValueAtIndex(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : this.orderOfIndexes.length;
    return e < this.indexedValues.length ? (this.indexedValues[e] = n, this.orderOfIndexes.includes(e) === !1 && this.orderOfIndexes.splice(i, 0, e), this.runLocalHooks("change"), !0) : !1;
  }
  /**
   * Clear value for particular index.
   *
   * @param {number} physicalIndex Physical index.
   */
  clearValue(e) {
    this.orderOfIndexes = Fh(this.orderOfIndexes, [e]), Et(this.initValueOrFn) ? super.setValueAtIndex(e, this.initValueOrFn(e)) : super.setValueAtIndex(e, this.initValueOrFn);
  }
  /**
   * Get length of the index map.
   *
   * @returns {number}
   */
  getLength() {
    return this.orderOfIndexes.length;
  }
  /**
   * Set default values for elements from `0` to `n`, where `n` is equal to the handled variable.
   *
   * Note: Please keep in mind that `change` hook triggered by the method may not update cache of a collection immediately.
   *
   * @private
   * @param {number} [length] Length of list.
   */
  setDefaultValues() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.indexedValues.length;
    this.orderOfIndexes.length = 0, super.setDefaultValues(e);
  }
  /**
   * Add values to list and reorganize. It updates list of indexes related to ordered values.
   *
   * @private
   * @param {number} insertionIndex Position inside the list.
   * @param {Array} insertedIndexes List of inserted indexes.
   */
  insert(e, n) {
    this.indexedValues = Yb(this.indexedValues, e, n, this.initValueOrFn), this.orderOfIndexes = Kb(this.orderOfIndexes, n), super.insert(e, n);
  }
  /**
   * Remove values from the list and reorganize. It updates list of indexes related to ordered values.
   *
   * @private
   * @param {Array} removedIndexes List of removed indexes.
   */
  remove(e) {
    this.indexedValues = jb(this.indexedValues, e), this.orderOfIndexes = Fh(this.orderOfIndexes, e), this.orderOfIndexes = zb(this.orderOfIndexes, e), super.remove(e);
  }
  /**
   * Get every entry containing index and value, respecting order of indexes.
   *
   * @returns {Array}
   */
  getEntries() {
    return this.orderOfIndexes.map((e) => [e, this.getValueAtIndex(e)]);
  }
}
class Td extends Zs {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    super(e);
  }
  /**
   * Get physical indexes which are trimmed.
   *
   * Note: Indexes marked as trimmed aren't included in a {@link DataMap} and aren't rendered.
   *
   * @returns {Array}
   */
  getTrimmedIndexes() {
    return rn(this.getValues(), (e, n, i) => (n && e.push(i), e), []);
  }
}
const cT = /* @__PURE__ */ new Map([["indexesSequence", {
  getListWithInsertedItems: Gb,
  getListWithRemovedItems: Fh
}], ["physicallyIndexed", {
  getListWithInsertedItems: Yb,
  getListWithRemovedItems: jb
}]]), s1 = (t) => {
  if (cT.has(t) === !1)
    throw new Error(`Alter strategy with ID '${t}' does not exist.`);
  return cT.get(t);
};
class a1 extends wl {
  constructor() {
    super((e) => e);
  }
  /**
   * Add values to list and reorganize.
   *
   * @private
   * @param {number} insertionIndex Position inside the list.
   * @param {Array} insertedIndexes List of inserted indexes.
   */
  insert(e, n) {
    const i = Kb(this.indexedValues, n);
    this.indexedValues = Gb(i, e, n), super.insert(e, n);
  }
  /**
   * Remove values from the list and reorganize.
   *
   * @private
   * @param {Array} removedIndexes List of removed indexes.
   */
  remove(e) {
    const n = Fh(this.indexedValues, e);
    this.indexedValues = zb(n, e), super.remove(e);
  }
}
const hT = /* @__PURE__ */ new Map([["hiding", kp], ["index", wl], ["linkedPhysicalIndexToValue", Lp], ["physicalIndexToValue", Zs], ["trimming", Td]]);
function LX(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
  if (!hT.has(t))
    throw new Error(`The provided map type ("${t}") does not exist.`);
  return new (hT.get(t))(e);
}
function BX(t, e, n) {
  return (e = FX(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function FX(t) {
  var e = VX(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function VX(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
let xy = 0;
class Qb {
  constructor() {
    BX(this, "collection", /* @__PURE__ */ new Map());
  }
  /**
   * Register custom index map.
   *
   * @param {string} uniqueName Unique name of the index map.
   * @param {IndexMap} indexMap Index map containing miscellaneous (i.e. Meta data, indexes sequence), updated after remove and insert data actions.
   */
  register(e, n) {
    this.collection.has(e) === !1 && (this.collection.set(e, n), n.addLocalHook("change", () => this.runLocalHooks("change", n)), xy += 1);
  }
  /**
   * Unregister custom index map.
   *
   * @param {string} name Name of the index map.
   */
  unregister(e) {
    const n = this.collection.get(e);
    de(n) && (n.destroy(), this.collection.delete(e), this.runLocalHooks("change", n), xy -= 1);
  }
  /**
   * Unregisters and destroys all collected index map instances.
   */
  unregisterAll() {
    this.collection.forEach((e, n) => this.unregister(n)), this.collection.clear();
  }
  /**
   * Get index map for the provided name.
   *
   * @param {string} [name] Name of the index map.
   * @returns {Array|IndexMap}
   */
  get(e) {
    return Mt(e) ? Array.from(this.collection.values()) : this.collection.get(e);
  }
  /**
   * Get collection size.
   *
   * @returns {number}
   */
  getLength() {
    return this.collection.size;
  }
  /**
   * Remove some indexes and corresponding mappings and update values of the others within all collection's index maps.
   *
   * @private
   * @param {Array} removedIndexes List of removed indexes.
   */
  removeFromEvery(e) {
    this.collection.forEach((n) => {
      n.remove(e);
    });
  }
  /**
   * Insert new indexes and corresponding mapping and update values of the others all collection's index maps.
   *
   * @private
   * @param {number} insertionIndex Position inside the actual list.
   * @param {Array} insertedIndexes List of inserted indexes.
   */
  insertToEvery(e, n) {
    this.collection.forEach((i) => {
      i.insert(e, n);
    });
  }
  /**
   * Set default values to index maps within collection.
   *
   * @param {number} length Destination length for all stored maps.
   */
  initEvery(e) {
    this.collection.forEach((n) => {
      n.init(e);
    });
  }
}
pt(Qb, Vn);
function WX() {
  return xy;
}
function mC(t, e, n) {
  return (e = $X(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function $X(t) {
  var e = UX(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function UX(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class dT extends Qb {
  constructor(e, n) {
    super(), mC(this, "mergedValuesCache", []), mC(this, "aggregationFunction", void 0), mC(this, "fallbackValue", void 0), this.aggregationFunction = e, this.fallbackValue = n;
  }
  /**
   * Get merged values for all indexes.
   *
   * @param {boolean} [readFromCache=true] Determine if read results from the cache.
   * @returns {Array}
   */
  getMergedValues() {
    if ((arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0) === !0)
      return this.mergedValuesCache;
    if (this.getLength() === 0)
      return [];
    const n = Ke(this.get(), (o) => o.getValues()), i = [], r = de(n[0]) && n[0].length || 0;
    for (let o = 0; o < r; o += 1) {
      const s = [];
      for (let a = 0; a < this.getLength(); a += 1)
        s.push(n[a][o]);
      i.push(s);
    }
    return Ke(i, this.aggregationFunction);
  }
  /**
   * Get merged value for particular index.
   *
   * @param {number} index Index for which we calculate single result.
   * @param {boolean} [readFromCache=true] Determine if read results from the cache.
   * @returns {*}
   */
  getMergedValueAtIndex(e, n) {
    const i = this.getMergedValues(n)[e];
    return de(i) ? i : this.fallbackValue;
  }
  /**
   * Rebuild cache for the collection.
   */
  updateCache() {
    this.mergedValuesCache = this.getMergedValues(!1);
  }
}
function YX(t, e, n) {
  jX(t, e), e.set(t, n);
}
function jX(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function GX(t, e, n) {
  return t.set(l1(t, e), n), n;
}
function zX(t, e) {
  return t.get(l1(t, e));
}
function l1(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var pC = /* @__PURE__ */ new WeakMap();
class u1 {
  constructor() {
    YX(this, pC, []);
  }
  /**
   * Subscribes to the observer.
   *
   * @param {Function} callback A function that will be called when the new changes will appear.
   * @returns {ChangesObserver}
   */
  subscribe(e) {
    return this.addLocalHook("change", e), this._write(zX(pC, this)), this;
  }
  /**
   * Unsubscribes all subscriptions. After the method call, the observer would not produce
   * any new events.
   *
   * @returns {ChangesObserver}
   */
  unsubscribe() {
    return this.runLocalHooks("unsubscribe"), this.clearLocalHooks(), this;
  }
  /**
   * The write method is executed by the ChangesObservable module. The module produces all
   * changes events that are distributed further by the observer.
   *
   * @private
   * @param {object} changes The chunk of changes produced by the ChangesObservable module.
   * @returns {ChangesObserver}
   */
  _write(e) {
    return e.length > 0 && this.runLocalHooks("change", e), this;
  }
  /**
   * The write method is executed by the ChangesObservable module. The module produces initial
   * changes that will be used to notify new subscribers.
   *
   * @private
   * @param {object} initialChanges The chunk of changes produced by the ChangesObservable module.
   */
  _writeInitialChanges(e) {
    GX(pC, this, e);
  }
}
pt(u1, Vn);
function fT(t, e) {
  const n = [];
  let i = 0, r = 0;
  for (; i < t.length && r < e.length; i++, r++)
    t[i] !== e[r] && n.push({
      op: "replace",
      index: r,
      oldValue: t[i],
      newValue: e[r]
    });
  for (; i < e.length; i++)
    n.push({
      op: "insert",
      index: i,
      oldValue: void 0,
      newValue: e[i]
    });
  for (; r < t.length; r++)
    n.push({
      op: "remove",
      index: r,
      oldValue: t[r],
      newValue: void 0
    });
  return n;
}
function dc(t, e, n) {
  KX(t, e), e.set(t, n);
}
function KX(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function _i(t, e) {
  return t.get(c1(t, e));
}
function bf(t, e, n) {
  return t.set(c1(t, e), n), n;
}
function c1(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Rf = /* @__PURE__ */ new WeakMap(), fc = /* @__PURE__ */ new WeakMap(), Ef = /* @__PURE__ */ new WeakMap(), Tf = /* @__PURE__ */ new WeakMap(), Mf = /* @__PURE__ */ new WeakMap();
class QX {
  constructor() {
    let {
      initialIndexValue: e
    } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    dc(this, Rf, /* @__PURE__ */ new Set()), dc(this, fc, []), dc(this, Ef, []), dc(this, Tf, !1), dc(this, Mf, !1), bf(Mf, this, e != null ? e : !1);
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * Creates and returns a new instance of the ChangesObserver object. The resource
   * allows subscribing to the index changes that during the code running may change.
   * Changes are emitted as an array of the index change. Each change is represented
   * separately as an object with `op`, `index`, `oldValue`, and `newValue` props.
   *
   * For example:
   * ```
   * [
   *   { op: 'replace', index: 1, oldValue: false, newValue: true },
   *   { op: 'replace', index: 3, oldValue: false, newValue: true },
   *   { op: 'insert', index: 4, oldValue: false, newValue: true },
   * ]
   * // or when the new index map changes have less indexes
   * [
   *   { op: 'replace', index: 1, oldValue: false, newValue: true },
   *   { op: 'remove', index: 4, oldValue: false, newValue: true },
   * ]
   * ```
   *
   * @returns {ChangesObserver}
   */
  /* eslint-enable jsdoc/require-description-complete-sentence */
  createObserver() {
    const e = new u1();
    return _i(Rf, this).add(e), e.addLocalHook("unsubscribe", () => {
      _i(Rf, this).delete(e);
    }), e._writeInitialChanges(fT(_i(fc, this), _i(Ef, this))), e;
  }
  /**
   * The method is an entry point for triggering new index map changes. Emitting the
   * changes triggers comparing algorithm which compares last saved state with a new
   * state. When there are some differences, the changes are sent to all subscribers.
   *
   * @param {Array} indexesState An array with index map state.
   */
  emit(e) {
    let n = _i(Ef, this);
    (!_i(Tf, this) || _i(fc, this).length !== e.length) && (e.length === 0 ? e = new Array(n.length).fill(_i(Mf, this)) : bf(fc, this, new Array(e.length).fill(_i(Mf, this))), _i(Tf, this) || (bf(Tf, this, !0), n = _i(fc, this)));
    const i = fT(n, e);
    _i(Rf, this).forEach((r) => r._write(i)), bf(Ef, this, e);
  }
}
function $n(t, e, n) {
  return (e = XX(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function XX(t) {
  var e = qX(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function qX(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Hy {
  constructor() {
    $n(this, "indexesSequence", new a1()), $n(this, "trimmingMapsCollection", new dT((e) => e.some((n) => n === !0), !1)), $n(this, "hidingMapsCollection", new dT((e) => e.some((n) => n === !0), !1)), $n(this, "variousMapsCollection", new Qb()), $n(this, "hidingChangesObservable", new QX({
      initialIndexValue: !1
    })), $n(this, "notTrimmedIndexesCache", []), $n(this, "notHiddenIndexesCache", []), $n(this, "isBatched", !1), $n(this, "indexesSequenceChanged", !1), $n(this, "indexesChangeSource", void 0), $n(this, "trimmedIndexesChanged", !1), $n(this, "hiddenIndexesChanged", !1), $n(this, "renderablePhysicalIndexesCache", []), $n(this, "fromPhysicalToVisualIndexesCache", /* @__PURE__ */ new Map()), $n(this, "fromVisualToRenderableIndexesCache", /* @__PURE__ */ new Map()), this.indexesSequence.addLocalHook("change", () => {
      this.indexesSequenceChanged = !0, this.updateCache(), this.runLocalHooks("indexesSequenceChange", this.indexesChangeSource), this.runLocalHooks("change", this.indexesSequence, null);
    }), this.trimmingMapsCollection.addLocalHook("change", (e) => {
      this.trimmedIndexesChanged = !0, this.updateCache(), this.runLocalHooks("change", e, this.trimmingMapsCollection);
    }), this.hidingMapsCollection.addLocalHook("change", (e) => {
      this.hiddenIndexesChanged = !0, this.updateCache(), this.runLocalHooks("change", e, this.hidingMapsCollection);
    }), this.variousMapsCollection.addLocalHook("change", (e) => {
      this.runLocalHooks("change", e, this.variousMapsCollection);
    });
  }
  /**
   * Suspends the cache update for this map. The method is helpful to group multiple
   * operations, which affects the cache. In this case, the cache will be updated once after
   * calling the `resumeOperations` method.
   */
  suspendOperations() {
    this.isBatched = !0;
  }
  /**
   * Resumes the cache update for this map. It recalculates the cache and restores the
   * default behavior where each map modification updates the cache.
   */
  resumeOperations() {
    this.isBatched = !1, this.updateCache();
  }
  /**
   * It creates and returns the new instance of the ChangesObserver object. The object
   * allows listening to the index changes that happen while the Handsontable is running.
   *
   * @param {string} indexMapType The index map type which we want to observe.
   *                              Currently, only the 'hiding' index map types are observable.
   * @returns {ChangesObserver}
   */
  createChangesObserver(e) {
    if (e !== "hiding")
      throw new Error(`Unsupported index map type "${e}".`);
    return this.hidingChangesObservable.createObserver();
  }
  /**
   * Creates and registers a new `IndexMap` for a specified `IndexMapper` instance.
   *
   * @param {string} indexName A unique index name.
   * @param {string} mapType The index map type (e.g., "hiding", "trimming", "physicalIndexToValue").
   * @param {*} [initValueOrFn] The initial value for the index map.
   * @returns {IndexMap}
   */
  createAndRegisterIndexMap(e, n, i) {
    return this.registerMap(e, LX(n, i));
  }
  /**
   * Register map which provide some index mappings. Type of map determining to which collection it will be added.
   *
   * @param {string} uniqueName Name of the index map. It should be unique.
   * @param {IndexMap} indexMap Registered index map updated on items removal and insertion.
   * @returns {IndexMap}
   */
  registerMap(e, n) {
    if (this.trimmingMapsCollection.get(e) || this.hidingMapsCollection.get(e) || this.variousMapsCollection.get(e))
      throw Error(`Map with name "${e}" has been already registered.`);
    n instanceof Td ? this.trimmingMapsCollection.register(e, n) : n instanceof kp ? this.hidingMapsCollection.register(e, n) : this.variousMapsCollection.register(e, n);
    const i = this.getNumberOfIndexes();
    return i > 0 && n.init(i), n;
  }
  /**
   * Unregister a map with given name.
   *
   * @param {string} name Name of the index map.
   */
  unregisterMap(e) {
    this.trimmingMapsCollection.unregister(e), this.hidingMapsCollection.unregister(e), this.variousMapsCollection.unregister(e);
  }
  /**
   * Unregisters all collected index map instances from all map collection types.
   */
  unregisterAll() {
    this.trimmingMapsCollection.unregisterAll(), this.hidingMapsCollection.unregisterAll(), this.variousMapsCollection.unregisterAll();
  }
  /**
   * Get a physical index corresponding to the given visual index.
   *
   * @param {number} visualIndex Visual index.
   * @returns {number|null} Returns translated index mapped by passed visual index.
   */
  getPhysicalFromVisualIndex(e) {
    const n = this.notTrimmedIndexesCache[e];
    return de(n) ? n : null;
  }
  /**
   * Get a physical index corresponding to the given renderable index.
   *
   * @param {number} renderableIndex Renderable index.
   * @returns {null|number}
   */
  getPhysicalFromRenderableIndex(e) {
    const n = this.renderablePhysicalIndexesCache[e];
    return de(n) ? n : null;
  }
  /**
   * Get a visual index corresponding to the given physical index.
   *
   * @param {number} physicalIndex Physical index to search.
   * @returns {number|null} Returns a visual index of the index mapper.
   */
  getVisualFromPhysicalIndex(e) {
    const n = this.fromPhysicalToVisualIndexesCache.get(e);
    return de(n) ? n : null;
  }
  /**
   * Get a visual index corresponding to the given renderable index.
   *
   * @param {number} renderableIndex Renderable index.
   * @returns {null|number}
   */
  getVisualFromRenderableIndex(e) {
    return this.getVisualFromPhysicalIndex(this.getPhysicalFromRenderableIndex(e));
  }
  /**
   * Get a renderable index corresponding to the given visual index.
   *
   * @param {number} visualIndex Visual index.
   * @returns {null|number}
   */
  getRenderableFromVisualIndex(e) {
    const n = this.fromVisualToRenderableIndexesCache.get(e);
    return de(n) ? n : null;
  }
  /**
   * Search for the nearest not-hidden row or column.
   *
   * @param {number} fromVisualIndex The visual index of the row or column from which the search starts.<br><br>
   * If the row or column from which the search starts is not hidden, the method simply returns the `fromVisualIndex` number.
   * @param {number} searchDirection The search direction.<br><br>`1`: search from `fromVisualIndex` to the end of the dataset.<br><br>
   * `-1`: search from `fromVisualIndex` to the beginning of the dataset (i.e., to the row or column at visual index `0`).
   * @param {boolean} searchAlsoOtherWayAround `true`: if a search in a first direction failed, try the opposite direction.<br><br>
   * `false`: search in one direction only.
   *
   * @returns {number|null} A visual index of a row or column, or `null`.
   */
  getNearestNotHiddenIndex(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    if (this.getPhysicalFromVisualIndex(e) === null)
      return null;
    if (this.fromVisualToRenderableIndexesCache.has(e))
      return e;
    const o = Array.from(this.fromVisualToRenderableIndexesCache.keys());
    let s = -1;
    return n > 0 ? s = o.findIndex((a) => a > e) : s = o.reverse().findIndex((a) => a < e), s === -1 ? i ? this.getNearestNotHiddenIndex(e, -n, !1) : null : o[s];
  }
  /**
   * Set default values for all indexes in registered index maps.
   *
   * @param {number} [length] Destination length for all stored index maps.
   */
  initToLength() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.getNumberOfIndexes();
    this.notTrimmedIndexesCache = [...new Array(e).keys()], this.notHiddenIndexesCache = [...new Array(e).keys()], this.suspendOperations(), this.indexesChangeSource = "init", this.indexesSequence.init(e), this.indexesChangeSource = void 0, this.trimmingMapsCollection.initEvery(e), this.resumeOperations(), this.suspendOperations(), this.hidingMapsCollection.initEvery(e), this.variousMapsCollection.initEvery(e), this.resumeOperations(), this.runLocalHooks("init");
  }
  /**
   * Trim/extend the mappers to fit the desired length.
   *
   * @param {number} length New mapper length.
   */
  fitToLength(e) {
    const n = this.getNumberOfIndexes();
    if (e < n) {
      const i = [...Array(this.getNumberOfIndexes() - e).keys()].map((r) => r + e);
      this.removeIndexes(i);
    } else
      this.insertIndexes(n, e - n);
  }
  /**
   * Get sequence of indexes.
   *
   * @returns {Array} Physical indexes.
   */
  getIndexesSequence() {
    return this.indexesSequence.getValues();
  }
  /**
   * Set completely new indexes sequence.
   *
   * @param {Array} indexes Physical indexes.
   */
  setIndexesSequence(e) {
    this.indexesChangeSource === void 0 && (this.indexesChangeSource = "update"), this.indexesSequence.setValues(e), this.indexesChangeSource === "update" && (this.indexesChangeSource = void 0);
  }
  /**
   * Get all NOT trimmed indexes.
   *
   * Note: Indexes marked as trimmed aren't included in a {@link DataMap} and aren't rendered.
   *
   * @param {boolean} [readFromCache=true] Determine if read indexes from cache.
   * @returns {Array} List of physical indexes. Index of this native array is a "visual index",
   * value of this native array is a "physical index".
   */
  getNotTrimmedIndexes() {
    return (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0) === !0 ? this.notTrimmedIndexesCache : this.getIndexesSequence().filter((i) => this.isTrimmed(i) === !1);
  }
  /**
   * Get length of all NOT trimmed indexes.
   *
   * Note: Indexes marked as trimmed aren't included in a {@link DataMap} and aren't rendered.
   *
   * @returns {number}
   */
  getNotTrimmedIndexesLength() {
    return this.getNotTrimmedIndexes().length;
  }
  /**
   * Get all NOT hidden indexes.
   *
   * Note: Indexes marked as hidden are included in a {@link DataMap}, but aren't rendered.
   *
   * @param {boolean} [readFromCache=true] Determine if read indexes from cache.
   * @returns {Array} List of physical indexes. Please keep in mind that index of this native array IS NOT a "visual index".
   */
  getNotHiddenIndexes() {
    return (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0) === !0 ? this.notHiddenIndexesCache : this.getIndexesSequence().filter((i) => this.isHidden(i) === !1);
  }
  /**
   * Get length of all NOT hidden indexes.
   *
   * Note: Indexes marked as hidden are included in a {@link DataMap}, but aren't rendered.
   *
   * @returns {number}
   */
  getNotHiddenIndexesLength() {
    return this.getNotHiddenIndexes().length;
  }
  /**
   * Get list of physical indexes (respecting the sequence of indexes) which may be rendered (when they are in a viewport).
   *
   * @param {boolean} [readFromCache=true] Determine if read indexes from cache.
   * @returns {Array} List of physical indexes. Index of this native array is a "renderable index",
   * value of this native array is a "physical index".
   */
  getRenderableIndexes() {
    return (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0) === !0 ? this.renderablePhysicalIndexesCache : this.getNotTrimmedIndexes().filter((i) => this.isHidden(i) === !1);
  }
  /**
   * Get length of all NOT trimmed and NOT hidden indexes.
   *
   * @returns {number}
   */
  getRenderableIndexesLength() {
    return this.getRenderableIndexes().length;
  }
  /**
   * Get number of all indexes.
   *
   * @returns {number}
   */
  getNumberOfIndexes() {
    return this.getIndexesSequence().length;
  }
  /**
   * Move indexes in the index mapper.
   *
   * @param {number|Array} movedIndexes Visual index(es) to move.
   * @param {number} finalIndex Visual index being a start index for the moved elements.
   */
  moveIndexes(e, n) {
    typeof e == "number" && (e = [e]);
    const i = Ke(e, (u) => this.getPhysicalFromVisualIndex(u)), r = this.getNotTrimmedIndexesLength(), o = e.length, s = Fh(this.getIndexesSequence(), i), a = s.filter((u) => this.isTrimmed(u) === !1);
    let l = s.indexOf(a[a.length - 1]) + 1;
    if (n + o < r) {
      const u = a[n];
      l = s.indexOf(u);
    }
    this.indexesChangeSource = "move", this.setIndexesSequence(Gb(s, l, i)), this.indexesChangeSource = void 0;
  }
  /**
   * Get whether index is trimmed. Index marked as trimmed isn't included in a {@link DataMap} and isn't rendered.
   *
   * @param {number} physicalIndex Physical index.
   * @returns {boolean}
   */
  isTrimmed(e) {
    return this.trimmingMapsCollection.getMergedValueAtIndex(e);
  }
  /**
   * Get whether index is hidden. Index marked as hidden is included in a {@link DataMap}, but isn't rendered.
   *
   * @param {number} physicalIndex Physical index.
   * @returns {boolean}
   */
  isHidden(e) {
    return this.hidingMapsCollection.getMergedValueAtIndex(e);
  }
  /**
   * Insert new indexes and corresponding mapping and update values of the others, for all stored index maps.
   *
   * @private
   * @param {number} firstInsertedVisualIndex First inserted visual index.
   * @param {number} amountOfIndexes Amount of inserted indexes.
   */
  insertIndexes(e, n) {
    const i = this.getNotTrimmedIndexes()[e], r = de(i) ? i : this.getNumberOfIndexes(), o = this.getIndexesSequence().includes(i) ? this.getIndexesSequence().indexOf(i) : this.getNumberOfIndexes(), s = Ke(new Array(n).fill(r), (a, l) => a + l);
    this.suspendOperations(), this.indexesChangeSource = "insert", this.indexesSequence.insert(o, s), this.indexesChangeSource = void 0, this.trimmingMapsCollection.insertToEvery(o, s), this.hidingMapsCollection.insertToEvery(o, s), this.variousMapsCollection.insertToEvery(o, s), this.resumeOperations();
  }
  /**
   * Remove some indexes and corresponding mappings and update values of the others, for all stored index maps.
   *
   * @private
   * @param {Array} removedIndexes List of removed indexes.
   */
  removeIndexes(e) {
    this.suspendOperations(), this.indexesChangeSource = "remove", this.indexesSequence.remove(e), this.indexesChangeSource = void 0, this.trimmingMapsCollection.removeFromEvery(e), this.hidingMapsCollection.removeFromEvery(e), this.variousMapsCollection.removeFromEvery(e), this.resumeOperations();
  }
  /**
   * Rebuild cache for some indexes. Every action on indexes sequence or indexes skipped in the process of rendering
   * by default reset cache, thus batching some index maps actions is recommended.
   *
   * @private
   * @param {boolean} [force=false] Determine if force cache update.
   */
  updateCache() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    const n = this.indexesSequenceChanged || this.trimmedIndexesChanged || this.hiddenIndexesChanged;
    (e === !0 || this.isBatched === !1 && n === !0) && (this.trimmingMapsCollection.updateCache(), this.hidingMapsCollection.updateCache(), this.notTrimmedIndexesCache = this.getNotTrimmedIndexes(!1), this.notHiddenIndexesCache = this.getNotHiddenIndexes(!1), this.renderablePhysicalIndexesCache = this.getRenderableIndexes(!1), this.cacheFromPhysicalToVisualIndexes(), this.cacheFromVisualToRenderableIndexes(), this.hiddenIndexesChanged && this.hidingChangesObservable.emit(this.hidingMapsCollection.getMergedValues()), this.runLocalHooks("cacheUpdated", {
      indexesSequenceChanged: this.indexesSequenceChanged,
      trimmedIndexesChanged: this.trimmedIndexesChanged,
      hiddenIndexesChanged: this.hiddenIndexesChanged
    }), this.indexesSequenceChanged = !1, this.trimmedIndexesChanged = !1, this.hiddenIndexesChanged = !1);
  }
  /**
   * Update cache for translations from physical to visual indexes.
   *
   * @private
   */
  cacheFromPhysicalToVisualIndexes() {
    const e = this.getNotTrimmedIndexesLength();
    this.fromPhysicalToVisualIndexesCache.clear();
    for (let n = 0; n < e; n += 1) {
      const i = this.getPhysicalFromVisualIndex(n);
      this.fromPhysicalToVisualIndexesCache.set(i, n);
    }
  }
  /**
   * Update cache for translations from visual to renderable indexes.
   *
   * @private
   */
  cacheFromVisualToRenderableIndexes() {
    const e = this.getRenderableIndexesLength();
    this.fromVisualToRenderableIndexesCache.clear();
    for (let n = 0; n < e; n += 1) {
      const i = this.getPhysicalFromRenderableIndex(n), r = this.getVisualFromPhysicalIndex(i);
      this.fromVisualToRenderableIndexesCache.set(r, n);
    }
  }
}
pt(Hy, Vn);
function ZX(t, e) {
  return Fe(e, (n, i) => {
    Mt(t[i]) && (t[i] = n);
  }), t;
}
function h1(t) {
  const n = /^([a-zA-Z]{2})-([a-zA-Z]{2})$/.exec(t);
  return n ? `${n[1].toLowerCase()}-${n[2].toUpperCase()}` : t;
}
function d1(t) {
  de(t) && pb(mt`Language with code "${t}" was not found. You should register particular language\x20
    before using it. Read more about this issue at: https://docs.handsontable.com/i18n/missing-language-code.`);
}
function JX(t, e) {
  return Array.isArray(t) && Number.isInteger(e) ? t[e] : t;
}
const {
  register: e4,
  getValues: t4
} = Or("phraseFormatters");
function n4(t, e) {
  e4(t, e);
}
function i4() {
  return t4();
}
n4("pluralize", JX);
const f1 = "ContextMenu:items", De = f1, Xb = `${De}.noItems`, Bp = `${De}.insertRowAbove`, Fp = `${De}.insertRowBelow`, Vp = `${De}.insertColumnOnTheLeft`, Wp = `${De}.insertColumnOnTheRight`, $p = `${De}.removeRow`, Up = `${De}.removeColumn`, Yp = `${De}.undo`, jp = `${De}.redo`, Vh = `${De}.readOnly`, Gp = `${De}.clearColumn`, zp = `${De}.copy`, Kp = `${De}.copyWithHeaders`, Qp = `${De}.copyWithGroupHeaders`, Xp = `${De}.copyHeadersOnly`, qp = `${De}.cut`, Zp = `${De}.freezeColumn`, Jp = `${De}.unfreezeColumn`, ew = `${De}.mergeCells`, tw = `${De}.unmergeCells`, nw = `${De}.addComment`, iw = `${De}.editComment`, rw = `${De}.removeComment`, ow = `${De}.readOnlyComment`, sw = `${De}.align`, Wh = `${De}.align.left`, $h = `${De}.align.center`, Uh = `${De}.align.right`, Yh = `${De}.align.justify`, jh = `${De}.align.top`, Gh = `${De}.align.middle`, zh = `${De}.align.bottom`, aw = `${De}.borders`, lw = `${De}.borders.top`, uw = `${De}.borders.right`, cw = `${De}.borders.bottom`, hw = `${De}.borders.left`, dw = `${De}.borders.remove`, fw = `${De}.nestedHeaders.insertChildRow`, gw = `${De}.nestedHeaders.detachFromParent`, mw = `${De}.hideColumn`, pw = `${De}.showColumn`, ww = `${De}.hideRow`, Aw = `${De}.showRow`, qn = "Filters:", Kt = `${qn}conditions`, Md = `${Kt}.none`, Cw = `${Kt}.isEmpty`, vw = `${Kt}.isNotEmpty`, yw = `${Kt}.isEqualTo`, Sw = `${Kt}.isNotEqualTo`, bw = `${Kt}.beginsWith`, Rw = `${Kt}.endsWith`, Ew = `${Kt}.contains`, Tw = `${Kt}.doesNotContain`, r4 = `${Kt}.byValue`, Mw = `${Kt}.greaterThan`, Iw = `${Kt}.greaterThanOrEqualTo`, Ow = `${Kt}.lessThan`, Pw = `${Kt}.lessThanOrEqualTo`, xw = `${Kt}.isBetween`, Hw = `${Kt}.isNotBetween`, _w = `${Kt}.after`, Nw = `${Kt}.before`, Dw = `${Kt}.today`, kw = `${Kt}.tomorrow`, Lw = `${Kt}.yesterday`, Bw = `${qn}labels.filterByCondition`, Fw = `${qn}labels.filterByValue`, Vw = `${qn}labels.conjunction`, Id = `${qn}labels.disjunction`, Kh = `${qn}values.blankCells`, Ww = `${qn}buttons.selectAll`, $w = `${qn}buttons.clear`, Uw = `${qn}buttons.ok`, Yw = `${qn}buttons.cancel`, jw = `${qn}buttons.placeholder.search`, Gw = `${qn}buttons.placeholder.value`, zw = `${qn}buttons.placeholder.secondValue`, qb = "CheckboxRenderer:", Zb = `${qb}checked`, Jb = `${qb}unchecked`, o4 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  CHECKBOX_CHECKED: Zb,
  CHECKBOX_RENDERER_NAMESPACE: qb,
  CHECKBOX_UNCHECKED: Jb,
  CONTEXTMENU_ITEMS_ADD_COMMENT: nw,
  CONTEXTMENU_ITEMS_ALIGNMENT: sw,
  CONTEXTMENU_ITEMS_ALIGNMENT_BOTTOM: zh,
  CONTEXTMENU_ITEMS_ALIGNMENT_CENTER: $h,
  CONTEXTMENU_ITEMS_ALIGNMENT_JUSTIFY: Yh,
  CONTEXTMENU_ITEMS_ALIGNMENT_LEFT: Wh,
  CONTEXTMENU_ITEMS_ALIGNMENT_MIDDLE: Gh,
  CONTEXTMENU_ITEMS_ALIGNMENT_RIGHT: Uh,
  CONTEXTMENU_ITEMS_ALIGNMENT_TOP: jh,
  CONTEXTMENU_ITEMS_BORDERS: aw,
  CONTEXTMENU_ITEMS_BORDERS_BOTTOM: cw,
  CONTEXTMENU_ITEMS_BORDERS_LEFT: hw,
  CONTEXTMENU_ITEMS_BORDERS_RIGHT: uw,
  CONTEXTMENU_ITEMS_BORDERS_TOP: lw,
  CONTEXTMENU_ITEMS_CLEAR_COLUMN: Gp,
  CONTEXTMENU_ITEMS_COPY: zp,
  CONTEXTMENU_ITEMS_COPY_COLUMN_HEADERS_ONLY: Xp,
  CONTEXTMENU_ITEMS_COPY_WITH_COLUMN_GROUP_HEADERS: Qp,
  CONTEXTMENU_ITEMS_COPY_WITH_COLUMN_HEADERS: Kp,
  CONTEXTMENU_ITEMS_CUT: qp,
  CONTEXTMENU_ITEMS_EDIT_COMMENT: iw,
  CONTEXTMENU_ITEMS_FREEZE_COLUMN: Zp,
  CONTEXTMENU_ITEMS_HIDE_COLUMN: mw,
  CONTEXTMENU_ITEMS_HIDE_ROW: ww,
  CONTEXTMENU_ITEMS_INSERT_LEFT: Vp,
  CONTEXTMENU_ITEMS_INSERT_RIGHT: Wp,
  CONTEXTMENU_ITEMS_MERGE_CELLS: ew,
  CONTEXTMENU_ITEMS_NESTED_ROWS_DETACH_CHILD: gw,
  CONTEXTMENU_ITEMS_NESTED_ROWS_INSERT_CHILD: fw,
  CONTEXTMENU_ITEMS_NO_ITEMS: Xb,
  CONTEXTMENU_ITEMS_READ_ONLY: Vh,
  CONTEXTMENU_ITEMS_READ_ONLY_COMMENT: ow,
  CONTEXTMENU_ITEMS_REDO: jp,
  CONTEXTMENU_ITEMS_REMOVE_BORDERS: dw,
  CONTEXTMENU_ITEMS_REMOVE_COLUMN: Up,
  CONTEXTMENU_ITEMS_REMOVE_COMMENT: rw,
  CONTEXTMENU_ITEMS_REMOVE_ROW: $p,
  CONTEXTMENU_ITEMS_ROW_ABOVE: Bp,
  CONTEXTMENU_ITEMS_ROW_BELOW: Fp,
  CONTEXTMENU_ITEMS_SHOW_COLUMN: pw,
  CONTEXTMENU_ITEMS_SHOW_ROW: Aw,
  CONTEXTMENU_ITEMS_UNDO: Yp,
  CONTEXTMENU_ITEMS_UNFREEZE_COLUMN: Jp,
  CONTEXTMENU_ITEMS_UNMERGE_CELLS: tw,
  CONTEXT_MENU_ITEMS_NAMESPACE: f1,
  FILTERS_BUTTONS_CANCEL: Yw,
  FILTERS_BUTTONS_CLEAR: $w,
  FILTERS_BUTTONS_OK: Uw,
  FILTERS_BUTTONS_PLACEHOLDER_SEARCH: jw,
  FILTERS_BUTTONS_PLACEHOLDER_SECOND_VALUE: zw,
  FILTERS_BUTTONS_PLACEHOLDER_VALUE: Gw,
  FILTERS_BUTTONS_SELECT_ALL: Ww,
  FILTERS_CONDITIONS_AFTER: _w,
  FILTERS_CONDITIONS_BEFORE: Nw,
  FILTERS_CONDITIONS_BEGINS_WITH: bw,
  FILTERS_CONDITIONS_BETWEEN: xw,
  FILTERS_CONDITIONS_BY_VALUE: r4,
  FILTERS_CONDITIONS_CONTAINS: Ew,
  FILTERS_CONDITIONS_EMPTY: Cw,
  FILTERS_CONDITIONS_ENDS_WITH: Rw,
  FILTERS_CONDITIONS_EQUAL: yw,
  FILTERS_CONDITIONS_GREATER_THAN: Mw,
  FILTERS_CONDITIONS_GREATER_THAN_OR_EQUAL: Iw,
  FILTERS_CONDITIONS_LESS_THAN: Ow,
  FILTERS_CONDITIONS_LESS_THAN_OR_EQUAL: Pw,
  FILTERS_CONDITIONS_NAMESPACE: Kt,
  FILTERS_CONDITIONS_NONE: Md,
  FILTERS_CONDITIONS_NOT_BETWEEN: Hw,
  FILTERS_CONDITIONS_NOT_CONTAIN: Tw,
  FILTERS_CONDITIONS_NOT_EMPTY: vw,
  FILTERS_CONDITIONS_NOT_EQUAL: Sw,
  FILTERS_CONDITIONS_TODAY: Dw,
  FILTERS_CONDITIONS_TOMORROW: kw,
  FILTERS_CONDITIONS_YESTERDAY: Lw,
  FILTERS_DIVS_FILTER_BY_CONDITION: Bw,
  FILTERS_DIVS_FILTER_BY_VALUE: Fw,
  FILTERS_LABELS_CONJUNCTION: Vw,
  FILTERS_LABELS_DISJUNCTION: Id,
  FILTERS_NAMESPACE: qn,
  FILTERS_VALUES_BLANK_CELLS: Kh
}, Symbol.toStringTag, { value: "Module" }));
/**
 * @preserve
 * Authors: Handsoncode
 * Last updated: Nov 15, 2017
 *
 * Description: Definition file for English - United States language-country.
 */
const s4 = {
  languageCode: "en-US",
  [Xb]: "No available options",
  [Bp]: "Insert row above",
  [Fp]: "Insert row below",
  [Vp]: "Insert column left",
  [Wp]: "Insert column right",
  [$p]: ["Remove row", "Remove rows"],
  [Up]: ["Remove column", "Remove columns"],
  [Yp]: "Undo",
  [jp]: "Redo",
  [Vh]: "Read only",
  [Gp]: "Clear column",
  [sw]: "Alignment",
  [Wh]: "Left",
  [$h]: "Center",
  [Uh]: "Right",
  [Yh]: "Justify",
  [jh]: "Top",
  [Gh]: "Middle",
  [zh]: "Bottom",
  [Zp]: "Freeze column",
  [Jp]: "Unfreeze column",
  [aw]: "Borders",
  [lw]: "Top",
  [uw]: "Right",
  [cw]: "Bottom",
  [hw]: "Left",
  [dw]: "Remove border(s)",
  [nw]: "Add comment",
  [iw]: "Edit comment",
  [rw]: "Delete comment",
  [ow]: "Read-only comment",
  [ew]: "Merge cells",
  [tw]: "Unmerge cells",
  [zp]: "Copy",
  [Kp]: ["Copy with header", "Copy with headers"],
  [Qp]: ["Copy with group header", "Copy with group headers"],
  [Xp]: ["Copy header only", "Copy headers only"],
  [qp]: "Cut",
  [fw]: "Insert child row",
  [gw]: "Detach from parent",
  [mw]: ["Hide column", "Hide columns"],
  [pw]: ["Show column", "Show columns"],
  [ww]: ["Hide row", "Hide rows"],
  [Aw]: ["Show row", "Show rows"],
  [Md]: "None",
  [Cw]: "Is empty",
  [vw]: "Is not empty",
  [yw]: "Is equal to",
  [Sw]: "Is not equal to",
  [bw]: "Begins with",
  [Rw]: "Ends with",
  [Ew]: "Contains",
  [Tw]: "Does not contain",
  [Mw]: "Greater than",
  [Iw]: "Greater than or equal to",
  [Ow]: "Less than",
  [Pw]: "Less than or equal to",
  [xw]: "Is between",
  [Hw]: "Is not between",
  [_w]: "After",
  [Nw]: "Before",
  [Dw]: "Today",
  [kw]: "Tomorrow",
  [Lw]: "Yesterday",
  [Kh]: "Blank cells",
  [Bw]: "Filter by condition",
  [Fw]: "Filter by value",
  [Vw]: "And",
  [Id]: "Or",
  [Ww]: "Select all",
  [$w]: "Clear",
  [Uw]: "OK",
  [Yw]: "Cancel",
  [jw]: "Search",
  [Gw]: "Value",
  [zw]: "Second value",
  [Zb]: "Checked",
  [Jb]: "Unchecked"
}, g1 = s4, a4 = o4, _y = g1.languageCode, {
  register: l4,
  getItem: m1,
  hasItem: u4,
  getValues: c4
} = Or("languagesDictionaries");
eR(g1);
function eR(t, e) {
  let n = t, i = e;
  return Be(t) && (i = t, n = i.languageCode), h4(n, i), l4(n, Fn(i)), Fn(i);
}
function h4(t, e) {
  t !== _y && ZX(e, m1(_y));
}
function p1(t) {
  return tR(t) ? Fn(m1(t)) : null;
}
function tR(t) {
  return u4(t);
}
function d4() {
  return c4();
}
function w1(t, e, n) {
  const i = p1(t);
  if (i === null)
    return null;
  const r = i[e];
  if (Mt(r))
    return null;
  const o = f4(r, n);
  return Array.isArray(o) ? o[0] : o;
}
function f4(t, e) {
  let n = t;
  return T(i4(), (i) => {
    n = i(t, e);
  }), n;
}
function g4(t) {
  let e = h1(t);
  return tR(e) || (e = _y, d1(t)), e;
}
function m4(t, e, n) {
  return (e = p4(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function p4(t) {
  var e = w4(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function w4(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class A4 extends H3 {
  constructor(e, n) {
    super(e, null), m4(this, "visualCellRange", null), this.visualCellRange = n || null, this.commit();
  }
  /**
   * Adds a cell coords to the selection.
   *
   * @param {CellCoords} coords Visual coordinates of a cell.
   * @returns {VisualSelection}
   */
  add(e) {
    return this.visualCellRange === null ? this.visualCellRange = this.settings.createCellRange(e) : this.visualCellRange.expand(e), this;
  }
  /**
   * Clears visual and renderable selection.
   *
   * @returns {VisualSelection}
   */
  clear() {
    return this.visualCellRange = null, super.clear();
  }
  /**
   * Trims the passed cell range object by removing all coordinates that points to the hidden rows
   * or columns. The result is a new cell range object that points only to the visible indexes or `null`.
   *
   * @private
   * @param {CellRange} cellRange Cells range object to be trimmed.
   * @returns {CellRange} Visual non-hidden cells range coordinates.
   */
  trimToVisibleCellsRangeOnly(e) {
    let {
      from: n,
      to: i
    } = e, r = this.getNearestNotHiddenCoords(n, 1), o = this.getNearestNotHiddenCoords(i, -1);
    return r === null || o === null ? null : ((r.row > o.row || r.col > o.col) && (r = n, o = i), this.settings.createCellRange(r, r, o));
  }
  /**
   * Gets nearest coordinates that points to the visible row and column indexes. If there are no visible
   * rows and/or columns the `null` value is returned.
   *
   * @private
   * @param {CellCoords} coords The coords object as starting point for finding the nearest visible coordinates.
   * @param {1|-1} rowSearchDirection The search direction. For value 1, it means searching from top to bottom for
   *                                  rows and from left to right for columns. For -1, it is the other way around.
   * @param {1|-1} columnSearchDirection The same as above but for rows.
   * @returns {CellCoords|null} Visual cell coordinates.
   */
  getNearestNotHiddenCoords(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : n;
    const r = this.getNearestNotHiddenIndex(this.settings.rowIndexMapper, e.row, n);
    if (r === null)
      return null;
    const o = this.getNearestNotHiddenIndex(this.settings.columnIndexMapper, e.col, i);
    return o === null ? null : this.settings.createCellCoords(r, o);
  }
  /**
   * Gets nearest visual index. If there are no visible rows or columns the `null` value is returned.
   *
   * @private
   * @param {IndexMapper} indexMapper The IndexMapper instance for specific axis.
   * @param {number} visualIndex The index as starting point for finding the nearest visible index.
   * @param {1|-1} searchDirection The search direction. For value 1, it means searching from top to bottom for
   *                               rows and from left to right for columns. For -1, it is the other way around.
   * @returns {number|null} Visual row/column index.
   */
  getNearestNotHiddenIndex(e, n, i) {
    return n < 0 ? n : e.getNearestNotHiddenIndex(n, i);
  }
  /**
   * Override internally stored visual indexes added by the Selection's `add` function. It should be executed
   * at the end of process of adding visual selection coordinates.
   *
   * @returns {VisualSelection}
   */
  commit() {
    if (this.visualCellRange === null)
      return this;
    const e = this.trimToVisibleCellsRangeOnly(this.visualCellRange);
    return e === null ? this.cellRange = null : this.cellRange = this.createRenderableCellRange(e.from, e.to), this;
  }
  /**
   * Some selection may be a part of broader cell range. This function sync coordinates of current selection
   * and the broader cell range when needed (current selection can't be presented visually).
   *
   * @param {CellRange} broaderCellRange Visual range. Actual cell range may be contained in the broader cell range.
   * When there is no way to represent some cell range visually we try to find range containing just the first visible cell.
   *
   * Warn: Please keep in mind that this function may change coordinates of the handled broader range.
   *
   * @returns {VisualSelection}
   */
  syncWith(e) {
    const n = e.from.clone().normalize(), i = e.getVerticalDirection() === "N-S" ? 1 : -1, r = e.getHorizontalDirection() === "W-E" ? 1 : -1, o = this.settings.visualToRenderableCoords(this.visualCellRange.highlight);
    let s = null;
    if ((o === null || o.col === null || o.row === null) && (s = this.getNearestNotHiddenCoords(n, i, r)), s !== null && e.overlaps(s)) {
      const a = e.highlight.clone();
      if (a.row >= 0 && (a.row = s.row), a.col >= 0 && (a.col = s.col), this.cellRange === null) {
        const l = this.settings.visualToRenderableCoords(a);
        this.cellRange = this.settings.createCellRange(l);
      }
      e.setHighlight(a);
    }
    return this.settings.selectionType === "focus" && o !== null && s === null && e.setHighlight(this.visualCellRange.highlight), this;
  }
  /**
   * Returns the top left (TL) and bottom right (BR) selection coordinates (renderable indexes).
   * The method overwrites the original method to support header selection for hidden cells.
   * To make the header selection working, the CellCoords and CellRange have to support not
   * complete coordinates (`null` values for example, `row: null`, `col: 2`).
   *
   * @returns {Array} Returns array of coordinates for example `[1, 1, 5, 5]`.
   */
  getCorners() {
    const {
      from: e,
      to: n
    } = this.cellRange;
    return [Math.min(e.row, n.row), Math.min(e.col, n.col), Math.max(e.row, n.row), Math.max(e.col, n.col)];
  }
  /**
   * Returns the top left (or top right in RTL) and bottom right (or bottom left in RTL) selection
   * coordinates (visual indexes).
   *
   * @returns {Array} Returns array of coordinates for example `[1, 1, 5, 5]`.
   */
  getVisualCorners() {
    const e = this.settings.renderableToVisualCoords(this.cellRange.getTopStartCorner()), n = this.settings.renderableToVisualCoords(this.cellRange.getBottomEndCorner());
    return [e.row, e.col, n.row, n.col];
  }
  /**
   * Creates a new CellRange object based on visual coordinates which before object creation are
   * translated to renderable indexes.
   *
   * @param {CellCoords} visualFromCoords The CellCoords object which contains coordinates that
   *                                      points to the beginning of the selection.
   * @param {CellCoords} visualToCoords The CellCoords object which contains coordinates that
   *                                    points to the end of the selection.
   * @returns {CellRange|null}
   */
  createRenderableCellRange(e, n) {
    const i = this.settings.visualToRenderableCoords(e), r = this.settings.visualToRenderableCoords(n);
    return i.row === null || i.col === null || r.row === null || r.col === null ? null : this.settings.createCellRange(i, i, r);
  }
}
const os = A4;
function wC(t) {
  let i = t, {
    activeHeaderClassName: e
  } = i, n = Wn(i, [
    "activeHeaderClassName"
  ]);
  return new os(On(Xe({
    className: e
  }, n), {
    selectionType: Kx
  }));
}
function C4(t) {
  let i = t, {
    areaCornerVisible: e
  } = i, n = Wn(i, [
    "areaCornerVisible"
  ]);
  return new os(On(Xe({
    className: "area",
    createLayers: !0,
    border: {
      width: 1,
      color: "#4b89ff",
      cornerVisible: e
    }
  }, n), {
    selectionType: _b
  }));
}
function v4(t) {
  let e = Wn(t, []);
  return new os(On(Xe({
    className: "highlight"
  }, e), {
    selectionType: _b
  }));
}
function y4(t) {
  let i = t, {
    columnClassName: e
  } = i, n = Wn(i, [
    "columnClassName"
  ]);
  return new os(On(Xe({
    className: e
  }, n), {
    selectionType: D3
  }));
}
function S4(t) {
  let i = t, {
    cellCornerVisible: e
  } = i, n = Wn(i, [
    "cellCornerVisible"
  ]);
  return new os(On(Xe({
    className: "current",
    headerAttributes: [JS()],
    border: {
      width: 2,
      color: "#4b89ff",
      cornerVisible: e
    }
  }, n), {
    selectionType: Nb
  }));
}
function b4(t) {
  let r = t, {
    border: e,
    visualCellRange: n
  } = r, i = Wn(r, [
    "border",
    "visualCellRange"
  ]);
  return new os(On(Xe(Xe({}, e), i), {
    selectionType: k3
  }), n);
}
function R4(t) {
  let e = Wn(t, []);
  return new os(On(Xe({
    className: "fill",
    border: {
      width: 1,
      color: "#ff0000"
    }
  }, e), {
    selectionType: _3
  }));
}
function gT(t) {
  let i = t, {
    headerClassName: e
  } = i, n = Wn(i, [
    "headerClassName"
  ]);
  return new os(On(Xe({
    className: e
  }, n), {
    selectionType: Hb
  }));
}
function E4(t) {
  let i = t, {
    rowClassName: e
  } = i, n = Wn(i, [
    "rowClassName"
  ]);
  return new os(On(Xe({
    className: e
  }, n), {
    selectionType: N3
  }));
}
function T4(t, e) {
  M4(t, e), e.add(t);
}
function M4(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ei(t, e, n) {
  return (e = I4(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function I4(t) {
  var e = O4(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function O4(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Ro(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Nr = /* @__PURE__ */ new WeakSet();
class P4 {
  constructor(e) {
    T4(this, Nr), ei(this, "options", void 0), ei(this, "layerLevel", 0), ei(this, "focus", void 0), ei(this, "fill", void 0), ei(this, "layeredAreas", /* @__PURE__ */ new Map()), ei(this, "areas", /* @__PURE__ */ new Map()), ei(this, "rowHeaders", /* @__PURE__ */ new Map()), ei(this, "columnHeaders", /* @__PURE__ */ new Map()), ei(this, "activeRowHeaders", /* @__PURE__ */ new Map()), ei(this, "activeColumnHeaders", /* @__PURE__ */ new Map()), ei(this, "activeCornerHeaders", /* @__PURE__ */ new Map()), ei(this, "rowHighlights", /* @__PURE__ */ new Map()), ei(this, "columnHighlights", /* @__PURE__ */ new Map()), ei(this, "customSelections", []), this.options = e, this.focus = S4(e), this.fill = R4(e);
  }
  /**
   * Check if highlight cell rendering is disabled for specified highlight type.
   *
   * @param {string} highlightType Highlight type. Possible values are: `cell`, `area`, `fill` or `header`.
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @returns {boolean}
   */
  isEnabledFor(e, n) {
    let i = e;
    e === Nb && (i = "current");
    let r = this.options.disabledCellSelection(n.row, n.col);
    return typeof r == "string" && (r = [r]), r === !1 || Array.isArray(r) && !r.includes(i);
  }
  /**
   * Set a new layer level to make access to the desire `area` and `header` highlights.
   *
   * @param {number} [level=0] Layer level to use.
   * @returns {Highlight}
   */
  useLayerLevel() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0;
    return this.layerLevel = e, this;
  }
  /**
   * Get Walkontable Selection instance created for controlling highlight of the currently
   * focused cell (or header).
   *
   * @returns {Selection}
   */
  getFocus() {
    return this.focus;
  }
  /**
   * Get Walkontable Selection instance created for controlling highlight of the autofill functionality.
   *
   * @returns {Selection}
   */
  getFill() {
    return this.fill;
  }
  /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * `area` highlights.
   *
   * @returns {Selection}
   */
  createLayeredArea() {
    return Ro(Nr, this, Eo).call(this, this.layeredAreas, C4);
  }
  /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the cells.
   *
   * @returns {Selection[]}
   */
  getLayeredAreas() {
    return [...this.layeredAreas.values()];
  }
  /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * `highlight` highlights.
   *
   * @returns {Selection}
   */
  createArea() {
    return Ro(Nr, this, Eo).call(this, this.areas, v4);
  }
  /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the cells.
   *
   * @returns {Selection[]}
   */
  getAreas() {
    return [...this.areas.values()];
  }
  /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * header highlight for rows.
   *
   * @returns {Selection}
   */
  createRowHeader() {
    return Ro(Nr, this, Eo).call(this, this.rowHeaders, gT);
  }
  /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the headers.
   *
   * @returns {Selection[]}
   */
  getRowHeaders() {
    return [...this.rowHeaders.values()];
  }
  /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * header highlight for columns.
   *
   * @returns {Selection}
   */
  createColumnHeader() {
    return Ro(Nr, this, Eo).call(this, this.columnHeaders, gT);
  }
  /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the headers.
   *
   * @returns {Selection[]}
   */
  getColumnHeaders() {
    return [...this.columnHeaders.values()];
  }
  /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight for active row headers.
   *
   * @returns {Selection}
   */
  createActiveRowHeader() {
    return Ro(Nr, this, Eo).call(this, this.activeRowHeaders, wC);
  }
  /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the active headers.
   *
   * @returns {Selection[]}
   */
  getActiveRowHeaders() {
    return [...this.activeRowHeaders.values()];
  }
  /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight for active column headers.
   *
   * @returns {Selection}
   */
  createActiveColumnHeader() {
    return Ro(Nr, this, Eo).call(this, this.activeColumnHeaders, wC);
  }
  /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the active headers.
   *
   * @returns {Selection[]}
   */
  getActiveColumnHeaders() {
    return [...this.activeColumnHeaders.values()];
  }
  /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight for the headers corner.
   *
   * @returns {Selection}
   */
  createActiveCornerHeader() {
    return Ro(Nr, this, Eo).call(this, this.activeCornerHeaders, wC);
  }
  /**
   * Get all Walkontable Selection instances which describes the state of the visual highlight of the headers corner.
   *
   * @returns {Selection[]}
   */
  getActiveCornerHeaders() {
    return [...this.activeCornerHeaders.values()];
  }
  /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight cells in a row.
   *
   * @returns {Selection}
   */
  createRowHighlight() {
    return Ro(Nr, this, Eo).call(this, this.rowHighlights, E4);
  }
  /**
   * Get all Walkontable Selection instances which describes the state of the rows highlighting.
   *
   * @returns {Selection[]}
   */
  getRowHighlights() {
    return [...this.rowHighlights.values()];
  }
  /**
   * Creates (if not exist in the cache) Walkontable Selection instance created for controlling
   * highlight cells in a column.
   *
   * @returns {Selection}
   */
  createColumnHighlight() {
    return Ro(Nr, this, Eo).call(this, this.columnHighlights, y4);
  }
  /**
   * Get all Walkontable Selection instances which describes the state of the columns highlighting.
   *
   * @returns {Selection[]}
   */
  getColumnHighlights() {
    return [...this.columnHighlights.values()];
  }
  /**
   * Get Walkontable Selection instance created for controlling highlight of the custom selection functionality.
   *
   * @returns {Selection}
   */
  getCustomSelections() {
    return [...this.customSelections.values()];
  }
  /**
   * Add selection to the custom selection instance. The new selection are added to the end of the selection collection.
   *
   * @param {object} selectionInstance The selection instance.
   */
  addCustomSelection(e) {
    this.customSelections.push(b4(Xe(Xe({}, this.options), e)));
  }
  /**
   * Perform cleaning visual highlights for the whole table.
   */
  clear() {
    this.focus.clear(), this.fill.clear(), T(this.areas.values(), (e) => void e.clear()), T(this.layeredAreas.values(), (e) => void e.clear()), T(this.rowHeaders.values(), (e) => void e.clear()), T(this.columnHeaders.values(), (e) => void e.clear()), T(this.activeRowHeaders.values(), (e) => void e.clear()), T(this.activeColumnHeaders.values(), (e) => void e.clear()), T(this.activeCornerHeaders.values(), (e) => void e.clear()), T(this.rowHighlights.values(), (e) => void e.clear()), T(this.columnHighlights.values(), (e) => void e.clear());
  }
  /**
   * This object can be iterate over using `for of` syntax or using internal `arrayEach` helper.
   *
   * @returns {Selection[]}
   */
  [Symbol.iterator]() {
    return [this.focus, this.fill, ...this.areas.values(), ...this.layeredAreas.values(), ...this.rowHeaders.values(), ...this.columnHeaders.values(), ...this.activeRowHeaders.values(), ...this.activeColumnHeaders.values(), ...this.activeCornerHeaders.values(), ...this.rowHighlights.values(), ...this.columnHighlights.values(), ...this.customSelections][Symbol.iterator]();
  }
}
function Eo(t, e) {
  const n = this.layerLevel;
  if (t.has(n))
    return t.get(n);
  const i = e(Xe({
    layerLevel: n
  }, this.options));
  return t.set(n, i), i;
}
const x4 = P4;
function mT(t, e, n) {
  return (e = H4(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function H4(t) {
  var e = _4(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function _4(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class N4 {
  constructor(e) {
    mT(this, "ranges", []), mT(this, "createCellRange", void 0), this.createCellRange = e;
  }
  /**
   * Check if selected range is empty.
   *
   * @returns {boolean}
   */
  isEmpty() {
    return this.size() === 0;
  }
  /**
   * Set coordinates to the class instance. It clears all previously added coordinates and push `coords`
   * to the collection.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @returns {SelectionRange}
   */
  set(e) {
    return this.clear(), this.ranges.push(this.createCellRange(e)), this;
  }
  /**
   * Add coordinates to the class instance. The new coordinates are added to the end of the range collection.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @returns {SelectionRange}
   */
  add(e) {
    return this.ranges.push(this.createCellRange(e)), this;
  }
  /**
   * Removes from the stack the last added coordinates.
   *
   * @returns {SelectionRange}
   */
  pop() {
    return this.ranges.pop(), this;
  }
  /**
   * Get last added coordinates from ranges, it returns a CellRange instance.
   *
   * @returns {CellRange|undefined}
   */
  current() {
    return this.peekByIndex(this.size() - 1);
  }
  /**
   * Get previously added coordinates from ranges, it returns a CellRange instance.
   *
   * @returns {CellRange|undefined}
   */
  previous() {
    return this.peekByIndex(this.size() - 2);
  }
  /**
   * Returns `true` if coords is within selection coords. This method iterates through all selection layers to check if
   * the coords object is within selection range.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @returns {boolean}
   */
  includes(e) {
    return this.ranges.some((n) => n.includes(e));
  }
  /**
   * Clear collection.
   *
   * @returns {SelectionRange}
   */
  clear() {
    return this.ranges.length = 0, this;
  }
  /**
   * Get count of added all coordinates added to the selection.
   *
   * @returns {number}
   */
  size() {
    return this.ranges.length;
  }
  /**
   * Peek the coordinates based on the index where that coordinate resides in the collection.
   *
   * @param {number} [index=0] An index where the coordinate will be retrieved from. The index '0' gets the
   * latest range.
   * @returns {CellRange|undefined}
   */
  peekByIndex() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0, n;
    return e >= 0 && e < this.size() && (n = this.ranges[e]), n;
  }
  [Symbol.iterator]() {
    return this.ranges[Symbol.iterator]();
  }
}
const D4 = N4;
function k4(t, e) {
  A1(t, e), e.add(t);
}
function AC(t, e, n) {
  A1(t, e), e.set(t, n);
}
function A1(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ot(t, e) {
  return t.get(sn(t, e));
}
function If(t, e, n) {
  return t.set(sn(t, e), n), n;
}
function sn(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Of = /* @__PURE__ */ new WeakMap(), Ot = /* @__PURE__ */ new WeakMap(), Cr = /* @__PURE__ */ new WeakMap(), dn = /* @__PURE__ */ new WeakSet();
class C1 {
  constructor(e, n) {
    k4(this, dn), AC(this, Of, void 0), AC(this, Ot, void 0), AC(this, Cr, {
      x: 0,
      y: 0
    }), If(Of, this, e), If(Ot, this, n);
  }
  /**
   * Selects cell relative to the current cell (if possible).
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   * @param {boolean} [createMissingRecords=false] If `true` the new rows/columns will be created if necessary. Otherwise, row/column will
   *                        be created according to `minSpareRows/minSpareCols` settings of Handsontable.
   * @returns {CellCoords} Visual coordinates after transformation.
   */
  transformStart(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    const r = ot(Ot, this).createCellCoords(e, n);
    let o = ot(Of, this).current().highlight;
    const s = ot(Ot, this).visualToRenderableCoords(o);
    let a = 0, l = 0;
    if (this.runLocalHooks("beforeTransformStart", r), s.row !== null && s.col !== null) {
      const {
        width: u,
        height: c
      } = sn(dn, this, v1).call(this), {
        row: h,
        col: d
      } = sn(dn, this, wT).call(this, o), g = ot(Ot, this).fixedRowsBottom(), m = ot(Ot, this).minSpareRows(), w = ot(Ot, this).minSpareCols(), C = ot(Ot, this).autoWrapRow(), S = ot(Ot, this).autoWrapCol(), b = ot(Ot, this).createCellCoords(h + r.row, d + r.col);
      if (b.row >= c) {
        const O = ji(i && m > 0 && g === 0), $ = b.col + 1, U = ot(Ot, this).createCellCoords(b.row - c, $ >= u ? $ - u : $);
        this.runLocalHooks("beforeColumnWrap", O, sn(dn, this, xl).call(this, U), $ >= u), O.value ? this.runLocalHooks("insertRowRequire", ot(Ot, this).countRenderableRows()) : S && b.assign(U);
      } else if (b.row < 0) {
        const O = ji(S), $ = b.col - 1, U = ot(Ot, this).createCellCoords(c + b.row, $ < 0 ? u + $ : $);
        this.runLocalHooks("beforeColumnWrap", O, sn(dn, this, xl).call(this, U), $ < 0), S && b.assign(U);
      }
      if (b.col >= u) {
        const O = ji(i && w > 0), $ = b.row + 1, U = ot(Ot, this).createCellCoords($ >= c ? $ - c : $, b.col - u);
        this.runLocalHooks("beforeRowWrap", O, sn(dn, this, xl).call(this, U), $ >= c), O.value ? this.runLocalHooks("insertColRequire", ot(Ot, this).countRenderableColumns()) : C && b.assign(U);
      } else if (b.col < 0) {
        const O = ji(C), $ = b.row - 1, U = ot(Ot, this).createCellCoords($ < 0 ? c + $ : $, u + b.col);
        this.runLocalHooks("beforeRowWrap", O, sn(dn, this, xl).call(this, U), $ < 0), C && b.assign(U);
      }
      const {
        rowDir: R,
        colDir: _
      } = sn(dn, this, pT).call(this, b);
      a = R, l = _, o = sn(dn, this, xl).call(this, b);
    }
    return this.runLocalHooks("afterTransformStart", o, a, l), o;
  }
  /**
   * Sets selection end cell relative to the current selection end cell (if possible).
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   * @returns {CellCoords} Visual coordinates after transformation.
   */
  transformEnd(e, n) {
    const i = ot(Ot, this).createCellCoords(e, n), r = ot(Of, this).current(), o = ot(Ot, this).visualToRenderableCoords(r.highlight), s = sn(dn, this, CC).call(this, r.to.row, r.from.row), a = sn(dn, this, vC).call(this, r.to.col, r.from.col), l = r.to.clone();
    let u = 0, c = 0;
    if (this.runLocalHooks("beforeTransformEnd", i), o.row !== null && o.col !== null && s !== null && a !== null) {
      const {
        row: h,
        col: d
      } = sn(dn, this, wT).call(this, r.highlight), g = ot(Ot, this).createCellCoords(s + i.row, a + i.col), m = r.getTopStartCorner(), w = r.getTopEndCorner(), C = r.getBottomEndCorner();
      if (i.col < 0 && a >= d && g.col < d) {
        const _ = g.col - d;
        g.col = sn(dn, this, vC).call(this, m.col, w.col) + _;
      } else if (i.col > 0 && a <= d && g.col > d) {
        const _ = sn(dn, this, vC).call(this, w.col, m.col), O = Math.max(g.col - _, 1);
        g.col = _ + O;
      }
      if (i.row < 0 && s >= h && g.row < h) {
        const _ = g.row - h;
        g.row = sn(dn, this, CC).call(this, m.row, C.row) + _;
      } else if (i.row > 0 && s <= h && g.row > h) {
        const _ = sn(dn, this, CC).call(this, C.row, m.row), O = Math.max(g.row - _, 1);
        g.row = _ + O;
      }
      const {
        rowDir: S,
        colDir: b
      } = sn(dn, this, pT).call(this, g);
      u = S, c = b;
      const R = sn(dn, this, xl).call(this, g);
      i.row === 0 && i.col !== 0 ? l.col = R.col : i.row !== 0 && i.col === 0 ? l.row = R.row : (l.row = R.row, l.col = R.col);
    }
    return this.runLocalHooks("afterTransformEnd", l, u, c), l;
  }
  /**
   * Sets the additional offset in table size that may occur when the `navigableHeaders` option
   * is enabled.
   *
   * @param {{x: number, y: number}} offset Offset as x and y properties.
   */
  setOffsetSize(e) {
    let {
      x: n,
      y: i
    } = e;
    If(Cr, this, {
      x: n,
      y: i
    });
  }
  /**
   * Resets the offset size to the default values.
   */
  resetOffsetSize() {
    If(Cr, this, {
      x: 0,
      y: 0
    });
  }
}
function pT(t) {
  const {
    width: e,
    height: n
  } = sn(dn, this, v1).call(this);
  let i = 0, r = 0;
  return t.row < 0 ? (i = -1, t.row = 0) : t.row > 0 && t.row >= n && (i = 1, t.row = n - 1), t.col < 0 ? (r = -1, t.col = 0) : t.col > 0 && t.col >= e && (r = 1, t.col = e - 1), {
    rowDir: i,
    colDir: r
  };
}
function v1() {
  return {
    width: ot(Cr, this).x + ot(Ot, this).countRenderableColumns(),
    height: ot(Cr, this).y + ot(Ot, this).countRenderableRows()
  };
}
function CC(t, e) {
  const n = ot(Ot, this).findFirstNonHiddenRenderableRow(t, e);
  return n === null ? null : ot(Cr, this).y + n;
}
function vC(t, e) {
  const n = ot(Ot, this).findFirstNonHiddenRenderableColumn(t, e);
  return n === null ? null : ot(Cr, this).x + n;
}
function wT(t) {
  const {
    row: e,
    col: n
  } = ot(Ot, this).visualToRenderableCoords(t);
  if (e === null || n === null)
    throw new Error("Renderable coords are not visible.");
  return ot(Ot, this).createCellCoords(ot(Cr, this).y + e, ot(Cr, this).x + n);
}
function xl(t) {
  const e = t.clone();
  return e.col = t.col - ot(Cr, this).x, e.row = t.row - ot(Cr, this).y, ot(Ot, this).renderableToVisualCoords(e);
}
pt(C1, Vn);
const AT = C1, Kw = 0, Qw = 1, y1 = 2, nR = 3, L4 = [nR, y1], B4 = [["number"], ["number", "string"], ["number", "undefined"], ["number", "string", "undefined"]], yC = Symbol("root"), CT = Symbol("child");
function Mu(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : yC;
  if (e !== yC && e !== CT)
    throw new Error("The second argument is used internally only and cannot be overwritten.");
  const n = Array.isArray(t), i = e === yC;
  let r = Kw;
  if (n) {
    const o = t[0];
    t.length === 0 ? r = Qw : i && o instanceof Eb ? r = nR : i && Array.isArray(o) ? r = Mu(o, CT) : t.length >= 2 && t.length <= 4 && !t.some((a, l) => !B4[l].includes(typeof a)) && (r = y1);
  }
  return r;
}
function Qh(t) {
  let {
    createCellCoords: e,
    createCellRange: n,
    keepDirection: i = !1,
    propToCol: r
  } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
  if (!L4.includes(t))
    throw new Error("Unsupported selection ranges schema type was provided.");
  return function(o) {
    const s = t === nR;
    let a = s ? o.from.row : o[0], l = s ? o.from.col : o[1], u = s ? o.to.row : o[2], c = s ? o.to.col : o[3];
    if (typeof r == "function" && (typeof l == "string" && (l = r(l)), typeof c == "string" && (c = r(c))), Mt(u) && (u = a), Mt(c) && (c = l), !i) {
      const g = a, m = l, w = u, C = c;
      a = Math.min(g, w), l = Math.min(m, C), u = Math.max(g, w), c = Math.max(m, C);
    }
    const h = e(a, l), d = e(u, c);
    return n(h, h, d);
  };
}
function F4(t) {
  const e = Mu(t.getSelected());
  if (e === Kw || e === Qw)
    return [];
  const n = Qh(e, {
    createCellCoords: t._createCellCoords.bind(t),
    createCellRange: t._createCellRange.bind(t)
  }), i = /* @__PURE__ */ new Set();
  T(t.getSelected(), (s) => {
    const {
      from: a,
      to: l
    } = n(s), u = Math.max(a.col, 0), c = l.col - u + 1;
    T(Array.from(new Array(c), (h, d) => u + d), (h) => {
      i.has(h) || i.add(h);
    });
  });
  const r = Array.from(i).sort((s, a) => s - a);
  return rn(r, (s, a, l, u) => (l !== 0 && a === u[l - 1] + 1 ? s[s.length - 1][1] += 1 : s.push([a, 1]), s), []);
}
function V4(t) {
  const e = Mu(t.getSelected());
  if (e === Kw || e === Qw)
    return [];
  const n = Qh(e, {
    createCellCoords: t._createCellCoords.bind(t),
    createCellRange: t._createCellRange.bind(t)
  }), i = /* @__PURE__ */ new Set();
  T(t.getSelected(), (s) => {
    const {
      from: a,
      to: l
    } = n(s), u = Math.max(a.row, 0), c = l.row - u + 1;
    T(Array.from(new Array(c), (h, d) => u + d), (h) => {
      i.has(h) || i.add(h);
    });
  });
  const r = Array.from(i).sort((s, a) => s - a);
  return rn(r, (s, a, l, u) => (l !== 0 && a === u[l - 1] + 1 ? s[s.length - 1][1] += 1 : s.push([a, 1]), s), []);
}
function Hl(t, e, n) {
  W4(t, e), e.set(t, n);
}
function W4(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ca(t, e, n) {
  return (e = $4(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function $4(t) {
  var e = U4(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function U4(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function xt(t, e) {
  return t.get(S1(t, e));
}
function Dr(t, e, n) {
  return t.set(S1(t, e), n), n;
}
function S1(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var xn = /* @__PURE__ */ new WeakMap(), ha = /* @__PURE__ */ new WeakMap(), Pf = /* @__PURE__ */ new WeakMap(), gc = /* @__PURE__ */ new WeakMap(), xf = /* @__PURE__ */ new WeakMap(), mc = /* @__PURE__ */ new WeakMap();
class b1 {
  constructor(e, n) {
    var i = this;
    ca(this, "settings", void 0), ca(this, "tableProps", void 0), ca(this, "inProgress", !1), ca(this, "selectedRange", new D4((r, o, s) => this.tableProps.createCellRange(r, o, s))), ca(this, "highlight", void 0), Hl(this, xn, void 0), Hl(this, ha, void 0), ca(this, "selectedByRowHeader", /* @__PURE__ */ new Set()), ca(this, "selectedByColumnHeader", /* @__PURE__ */ new Set()), Hl(this, Pf, !1), Hl(this, gc, !1), Hl(this, xf, "unknown"), Hl(this, mc, -1), this.settings = e, this.tableProps = n, this.highlight = new x4({
      headerClassName: e.currentHeaderClassName,
      activeHeaderClassName: e.activeHeaderClassName,
      rowClassName: e.currentRowClassName,
      columnClassName: e.currentColClassName,
      cellAttributes: [JS()],
      rowIndexMapper: this.tableProps.rowIndexMapper,
      columnIndexMapper: this.tableProps.columnIndexMapper,
      disabledCellSelection: (r, o) => this.tableProps.isDisabledCellSelection(r, o),
      cellCornerVisible: function() {
        return i.isCellCornerVisible(...arguments);
      },
      areaCornerVisible: function() {
        return i.isAreaCornerVisible(...arguments);
      },
      visualToRenderableCoords: (r) => this.tableProps.visualToRenderableCoords(r),
      renderableToVisualCoords: (r) => this.tableProps.renderableToVisualCoords(r),
      createCellCoords: (r, o) => this.tableProps.createCellCoords(r, o),
      createCellRange: (r, o, s) => this.tableProps.createCellRange(r, o, s)
    }), Dr(xn, this, new AT(this.selectedRange, {
      rowIndexMapper: this.tableProps.rowIndexMapper,
      columnIndexMapper: this.tableProps.columnIndexMapper,
      countRenderableRows: () => this.tableProps.countRenderableRows(),
      countRenderableColumns: () => this.tableProps.countRenderableColumns(),
      visualToRenderableCoords: (r) => this.tableProps.visualToRenderableCoords(r),
      renderableToVisualCoords: (r) => this.tableProps.renderableToVisualCoords(r),
      findFirstNonHiddenRenderableRow: function() {
        return i.tableProps.findFirstNonHiddenRenderableRow(...arguments);
      },
      findFirstNonHiddenRenderableColumn: function() {
        return i.tableProps.findFirstNonHiddenRenderableColumn(...arguments);
      },
      createCellCoords: (r, o) => this.tableProps.createCellCoords(r, o),
      fixedRowsBottom: () => e.fixedRowsBottom,
      minSpareRows: () => e.minSpareRows,
      minSpareCols: () => e.minSpareCols,
      autoWrapRow: () => e.autoWrapRow,
      autoWrapCol: () => e.autoWrapCol
    })), Dr(ha, this, new AT(this.selectedRange, {
      rowIndexMapper: this.tableProps.rowIndexMapper,
      columnIndexMapper: this.tableProps.columnIndexMapper,
      countRenderableRows: () => {
        const r = this.selectedRange.current();
        return this.tableProps.countRenderableRowsInRange(0, r.getOuterBottomEndCorner().row);
      },
      countRenderableColumns: () => {
        const r = this.selectedRange.current();
        return this.tableProps.countRenderableColumnsInRange(0, r.getOuterBottomEndCorner().col);
      },
      visualToRenderableCoords: (r) => this.tableProps.visualToRenderableCoords(r),
      renderableToVisualCoords: (r) => this.tableProps.renderableToVisualCoords(r),
      findFirstNonHiddenRenderableRow: function() {
        return i.tableProps.findFirstNonHiddenRenderableRow(...arguments);
      },
      findFirstNonHiddenRenderableColumn: function() {
        return i.tableProps.findFirstNonHiddenRenderableColumn(...arguments);
      },
      createCellCoords: (r, o) => this.tableProps.createCellCoords(r, o),
      fixedRowsBottom: () => 0,
      minSpareRows: () => 0,
      minSpareCols: () => 0,
      autoWrapRow: () => !0,
      autoWrapCol: () => !0
    })), xt(xn, this).addLocalHook("beforeTransformStart", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("beforeModifyTransformStart", ...o);
    }), xt(xn, this).addLocalHook("afterTransformStart", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("afterModifyTransformStart", ...o);
    }), xt(xn, this).addLocalHook("beforeTransformEnd", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("beforeModifyTransformEnd", ...o);
    }), xt(xn, this).addLocalHook("afterTransformEnd", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("afterModifyTransformEnd", ...o);
    }), xt(xn, this).addLocalHook("insertRowRequire", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("insertRowRequire", ...o);
    }), xt(xn, this).addLocalHook("insertColRequire", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("insertColRequire", ...o);
    }), xt(xn, this).addLocalHook("beforeRowWrap", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("beforeRowWrap", ...o);
    }), xt(xn, this).addLocalHook("beforeColumnWrap", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("beforeColumnWrap", ...o);
    }), xt(ha, this).addLocalHook("beforeTransformStart", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("beforeModifyTransformFocus", ...o);
    }), xt(ha, this).addLocalHook("afterTransformStart", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("afterModifyTransformFocus", ...o);
    });
  }
  /**
   * Get data layer for current selection.
   *
   * @returns {SelectionRange}
   */
  getSelectedRange() {
    return this.selectedRange;
  }
  /**
   * Marks the source of the selection. It can be one of the following values: `mouse`, or any other string.
   *
   * @param {'mouse' | 'unknown' | string} sourceName The source name.
   */
  markSource(e) {
    Dr(xf, this, e);
  }
  /**
   * Marks end of the selection source. It restores the selection source to default value which is 'unknown'.
   */
  markEndSource() {
    Dr(xf, this, "unknown");
  }
  /**
   * Returns the source of the selection.
   *
   * @returns {'mouse' | 'unknown' | string}
   */
  getSelectionSource() {
    return xt(xf, this);
  }
  /**
   * Set the number of expected layers. The method is not obligatory to call. It is used mostly internally
   * to determine when the last selection layer of non-contiguous is applied, thus the viewport scroll is triggered.
   *
   * @param {number} layersCount The number of expected layers.
   */
  setExpectedLayers(e) {
    Dr(mc, this, e);
  }
  /**
   * Indicate that selection process began. It sets internally `.inProgress` property to `true`.
   */
  begin() {
    this.inProgress = !0;
  }
  /**
   * Indicate that selection process finished. It sets internally `.inProgress` property to `false`.
   */
  finish() {
    this.runLocalHooks("afterSelectionFinished", Array.from(this.selectedRange)), this.inProgress = !1, Dr(mc, this, -1);
  }
  /**
   * Check if the process of selecting the cell/cells is in progress.
   *
   * @returns {boolean}
   */
  isInProgress() {
    return this.inProgress;
  }
  /**
   * Starts selection range on given coordinate object.
   *
   * @param {CellCoords} coords Visual coords.
   * @param {boolean} [multipleSelection] If `true`, selection will be worked in 'multiple' mode. This option works
   *                                      only when 'selectionMode' is set as 'multiple'. If the argument is not defined
   *                                      the default trigger will be used.
   * @param {boolean} [fragment=false] If `true`, the selection will be treated as a partial selection where the
   *                                   `setRangeEnd` method won't be called on every `setRangeStart` call.
   * @param {CellCoords} [highlightCoords] If set, allows changing the coordinates of the highlight/focus cell.
   */
  setRangeStart(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1, r = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : e;
    const o = this.settings.selectionMode === "multiple", s = Mt(n) ? this.tableProps.getShortcutManager().isCtrlPressed() : n, a = e.clone();
    Dr(Pf, this, !1), this.runLocalHooks(`beforeSetRangeStart${i ? "Only" : ""}`, a), (!o || o && !s && Mt(n)) && this.selectedRange.clear(), this.selectedRange.add(a).current().setHighlight(r.clone()), this.getLayerLevel() === 0 && (this.selectedByRowHeader.clear(), this.selectedByColumnHeader.clear()), i || this.setRangeEnd(e);
  }
  /**
   * Starts selection range on given coordinate object.
   *
   * @param {CellCoords} coords Visual coords.
   * @param {boolean} [multipleSelection] If `true`, selection will be worked in 'multiple' mode. This option works
   *                                      only when 'selectionMode' is set as 'multiple'. If the argument is not defined
   *                                      the default trigger will be used.
   * @param {CellCoords} [highlightCoords] If set, allows changing the coordinates of the highlight/focus cell.
   */
  setRangeStartOnly(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : e;
    this.setRangeStart(e, n, !0, i);
  }
  /**
   * Ends selection range on given coordinate object.
   *
   * @param {CellCoords} coords Visual coords.
   */
  setRangeEnd(e) {
    if (this.selectedRange.isEmpty())
      return;
    const n = e.clone(), i = this.tableProps.countRows(), r = this.tableProps.countCols(), o = this.selectedRange.current().clone().setTo(e).isSingleHeader();
    if ((i > 0 || r > 0) && (i === 0 && n.col < 0 && !o || r === 0 && n.row < 0 && !o))
      return;
    this.runLocalHooks("beforeSetRangeEnd", n), this.begin();
    const s = this.selectedRange.current();
    if (this.settings.navigableHeaders || s.highlight.normalize(), this.settings.selectionMode === "single")
      s.setFrom(s.highlight), s.setTo(s.highlight);
    else {
      const l = s.getHorizontalDirection(), u = s.getVerticalDirection(), c = this.isMultiple();
      s.setTo(n), c && (l !== s.getHorizontalDirection() || s.getWidth() === 1 && !s.includes(s.highlight)) && s.from.assign({
        col: s.highlight.col
      }), c && (u !== s.getVerticalDirection() || s.getHeight() === 1 && !s.includes(s.highlight)) && s.from.assign({
        row: s.highlight.row
      });
    }
    i > 0 && r > 0 && (!this.settings.navigableHeaders || this.settings.navigableHeaders && !s.isSingleHeader()) && s.to.normalize(), this.runLocalHooks("beforeHighlightSet"), this.setRangeFocus(this.selectedRange.current().highlight), this.applyAndCommit();
    const a = xt(mc, this) === -1 || this.selectedRange.size() === xt(mc, this);
    this.runLocalHooks("afterSetRangeEnd", e, a);
  }
  /**
   * Applies and commits the selection to all layers (using the Walkontable Selection API) based on the selection (CellRanges)
   * collected in the `selectedRange` module.
   *
   * @param {CellRange} [cellRange] The cell range to apply. If not provided, the current selection is used.
   * @param {number} [layerLevel] The layer level to apply. If not provided, the current layer level is used.
   */
  applyAndCommit() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.selectedRange.current(), n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.getLayerLevel();
    const i = this.tableProps.countRows(), r = this.tableProps.countCols();
    n < this.highlight.layerLevel && (T(this.highlight.getAreas(), (m) => void m.clear()), T(this.highlight.getLayeredAreas(), (m) => void m.clear()), T(this.highlight.getRowHeaders(), (m) => void m.clear()), T(this.highlight.getColumnHeaders(), (m) => void m.clear()), T(this.highlight.getActiveRowHeaders(), (m) => void m.clear()), T(this.highlight.getActiveColumnHeaders(), (m) => void m.clear()), T(this.highlight.getActiveCornerHeaders(), (m) => void m.clear()), T(this.highlight.getRowHighlights(), (m) => void m.clear()), T(this.highlight.getColumnHighlights(), (m) => void m.clear())), this.highlight.useLayerLevel(n);
    const o = this.highlight.createArea(), s = this.highlight.createLayeredArea(), a = this.highlight.createRowHeader(), l = this.highlight.createColumnHeader(), u = this.highlight.createActiveRowHeader(), c = this.highlight.createActiveColumnHeader(), h = this.highlight.createActiveCornerHeader(), d = this.highlight.createRowHighlight(), g = this.highlight.createColumnHighlight();
    if (o.clear(), s.clear(), a.clear(), l.clear(), u.clear(), c.clear(), h.clear(), d.clear(), g.clear(), this.highlight.isEnabledFor(_b, e.highlight) && (this.isMultiple() || n >= 1) && (o.add(e.from).add(e.to).commit(), s.add(e.from).add(e.to).commit(), n === 1)) {
      const m = this.selectedRange.previous();
      this.highlight.useLayerLevel(n - 1), this.highlight.createArea().add(m.from).commit().syncWith(m), this.highlight.createLayeredArea().add(m.from).commit().syncWith(m), this.highlight.useLayerLevel(n);
    }
    if (this.highlight.isEnabledFor(Hb, e.highlight)) {
      if (!e.isSingleHeader()) {
        const C = this.tableProps.createCellCoords(Math.max(e.from.row, 0), -1), S = this.tableProps.createCellCoords(e.to.row, -1), b = this.tableProps.createCellCoords(-1, Math.max(e.from.col, 0)), R = this.tableProps.createCellCoords(-1, e.to.col);
        this.settings.selectionMode === "single" ? (a.add(C).commit(), l.add(b).commit(), d.add(C).commit(), g.add(b).commit()) : (a.add(C).add(S).commit(), l.add(b).add(R).commit(), d.add(C).add(S).commit(), g.add(b).add(R).commit());
      }
      const m = !xt(gc, this) && this.isEntireRowSelected() && (r > 0 && r === e.getWidth() || r === 0 && this.isSelectedByRowHeader()), w = !xt(gc, this) && this.isEntireColumnSelected() && (i > 0 && i === e.getHeight() || i === 0 && this.isSelectedByColumnHeader());
      m && u.add(this.tableProps.createCellCoords(Math.max(e.from.row, 0), Math.min(-this.tableProps.countRowHeaders(), -1))).add(this.tableProps.createCellCoords(Math.max(e.to.row, 0), -1)).commit(), w && c.add(this.tableProps.createCellCoords(Math.min(-this.tableProps.countColHeaders(), -1), Math.max(e.from.col, 0))).add(this.tableProps.createCellCoords(-1, Math.max(e.to.col, 0))).commit(), m && w && h.add(this.tableProps.createCellCoords(-this.tableProps.countColHeaders(), -this.tableProps.countRowHeaders())).add(this.tableProps.createCellCoords(-1, -1)).commit();
    }
  }
  /**
   * Sets the selection focus position at the specified coordinates.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   */
  setRangeFocus(e) {
    if (this.selectedRange.isEmpty())
      return;
    const n = this.selectedRange.current();
    this.inProgress || this.runLocalHooks("beforeSetFocus", e);
    const i = this.highlight.getFocus();
    i.clear(), n.setHighlight(e), this.inProgress || this.runLocalHooks("beforeHighlightSet"), this.highlight.isEnabledFor(Nb, n.highlight) && i.add(n.highlight).commit().syncWith(n), this.inProgress || (Dr(Pf, this, !0), this.runLocalHooks("afterSetFocus", n.highlight));
  }
  /**
   * Selects cell relative to the current cell (if possible).
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   * @param {boolean} [createMissingRecords=false] If `true` the new rows/columns will be created if necessary.
   * Otherwise, row/column will be created according to `minSpareRows/minSpareCols` settings of Handsontable.
   */
  transformStart(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    this.settings.navigableHeaders ? xt(xn, this).setOffsetSize({
      x: this.tableProps.countRowHeaders(),
      y: this.tableProps.countColHeaders()
    }) : xt(xn, this).resetOffsetSize(), this.setRangeStart(xt(xn, this).transformStart(e, n, i));
  }
  /**
   * Sets selection end cell relative to the current selection end cell (if possible).
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   */
  transformEnd(e, n) {
    this.settings.navigableHeaders ? xt(xn, this).setOffsetSize({
      x: this.tableProps.countRowHeaders(),
      y: this.tableProps.countColHeaders()
    }) : xt(xn, this).resetOffsetSize(), this.setRangeEnd(xt(xn, this).transformEnd(e, n));
  }
  /**
   * Transforms the focus cell selection relative to the current focus position.
   *
   * @param {number} rowDelta Rows number to move, value can be passed as negative number.
   * @param {number} colDelta Columns number to move, value can be passed as negative number.
   */
  transformFocus(e, n) {
    const i = this.selectedRange.current(), {
      row: r,
      col: o
    } = i.getOuterTopStartCorner(), s = this.tableProps.countRenderableColumnsInRange(0, o - 1), a = this.tableProps.countRenderableRowsInRange(0, r - 1);
    i.highlight.isHeader() ? xt(ha, this).setOffsetSize({
      x: o < 0 ? Math.abs(o) : -s,
      y: r < 0 ? Math.abs(r) : -a
    }) : xt(ha, this).setOffsetSize({
      x: o < 0 ? 0 : -s,
      y: r < 0 ? 0 : -a
    });
    const l = xt(ha, this).transformStart(e, n);
    this.setRangeFocus(l.normalize());
  }
  /**
   * Transforms the last selection layer down or up by the index count.
   *
   * @param {number} visualRowIndex Visual row index from which the selection will be shifted.
   * @param {number} amount The number of rows to shift the selection.
   */
  shiftRows(e, n) {
    if (!this.isSelected())
      return;
    const i = this.selectedRange.current();
    if (this.isSelectedByCorner())
      this.selectAll(!0, !0, {
        disableHeadersHighlight: !0
      });
    else if (this.isSelectedByColumnHeader() || i.getOuterTopStartCorner().row >= e) {
      const {
        from: r,
        to: o,
        highlight: s
      } = i, a = this.tableProps.countRows(), l = this.isSelectedByRowHeader(), u = this.isSelectedByColumnHeader(), c = u ? -1 : 0, h = u ? 0 : n;
      this.getSelectedRange().pop();
      const d = this.tableProps.createCellCoords(ft(r.row + h, c, a - 1), r.col), g = this.tableProps.createCellCoords(ft(o.row + n, c, a - 1), o.col);
      this.markSource("shift"), s.row >= e ? this.setRangeStartOnly(d, !0, this.tableProps.createCellCoords(ft(s.row + n, 0, a - 1), s.col)) : this.setRangeStartOnly(d, !0), l && this.selectedByRowHeader.add(this.getLayerLevel()), u && this.selectedByColumnHeader.add(this.getLayerLevel()), this.setRangeEnd(g), this.markEndSource();
    }
  }
  /**
   * Transforms the last selection layer left or right by the index count.
   *
   * @param {number} visualColumnIndex Visual column index from which the selection will be shifted.
   * @param {number} amount The number of columns to shift the selection.
   */
  shiftColumns(e, n) {
    if (!this.isSelected())
      return;
    const i = this.selectedRange.current();
    if (this.isSelectedByCorner())
      this.selectAll(!0, !0, {
        disableHeadersHighlight: !0
      });
    else if (this.isSelectedByRowHeader() || i.getOuterTopStartCorner().col >= e) {
      const {
        from: r,
        to: o,
        highlight: s
      } = i, a = this.tableProps.countCols(), l = this.isSelectedByRowHeader(), u = this.isSelectedByColumnHeader(), c = l ? -1 : 0, h = l ? 0 : n;
      this.getSelectedRange().pop();
      const d = this.tableProps.createCellCoords(r.row, ft(r.col + h, c, a - 1)), g = this.tableProps.createCellCoords(o.row, ft(o.col + n, c, a - 1));
      this.markSource("shift"), s.col >= e ? this.setRangeStartOnly(d, !0, this.tableProps.createCellCoords(s.row, ft(s.col + n, 0, a - 1))) : this.setRangeStartOnly(d, !0), l && this.selectedByRowHeader.add(this.getLayerLevel()), u && this.selectedByColumnHeader.add(this.getLayerLevel()), this.setRangeEnd(g), this.markEndSource();
    }
  }
  /**
   * Returns currently used layer level.
   *
   * @returns {number} Returns layer level starting from 0. If no selection was added to the table -1 is returned.
   */
  getLayerLevel() {
    return this.selectedRange.size() - 1;
  }
  /**
   * Returns `true` if currently there is a selection on the screen, `false` otherwise.
   *
   * @returns {boolean}
   */
  isSelected() {
    return !this.selectedRange.isEmpty();
  }
  /**
   * Returns information if we have a multi-selection. This method check multi-selection only on the latest layer of
   * the selection.
   *
   * @returns {boolean}
   */
  isMultiple() {
    if (!this.isSelected())
      return !1;
    const e = ji(!this.selectedRange.current().isSingle());
    return this.runLocalHooks("afterIsMultipleSelection", e), e.value;
  }
  /**
   * Checks if the last selection involves changing the focus cell position only.
   *
   * @returns {boolean}
   */
  isFocusSelectionChanged() {
    return this.isSelected() && xt(Pf, this);
  }
  /**
   * Returns `true` if the selection was applied by clicking to the row header. If the `layerLevel`
   * argument is passed then only that layer will be checked. Otherwise, it checks if any row header
   * was clicked on any selection layer level.
   *
   * @param {number} [layerLevel=this.getLayerLevel()] Selection layer level to check.
   * @returns {boolean}
   */
  isSelectedByRowHeader() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.getLayerLevel();
    return !this.isSelectedByCorner(e) && (e === -1 ? this.selectedByRowHeader.size > 0 : this.selectedByRowHeader.has(e));
  }
  /**
   * Returns `true` if the selection consists of entire rows (including their headers). If the `layerLevel`
   * argument is passed then only that layer will be checked. Otherwise, it checks the selection for all layers.
   *
   * @param {number} [layerLevel=this.getLayerLevel()] Selection layer level to check.
   * @returns {boolean}
   */
  isEntireRowSelected() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.getLayerLevel();
    const n = (r) => {
      const {
        col: o
      } = r.getOuterTopStartCorner(), s = this.tableProps.countRowHeaders(), a = this.tableProps.countCols();
      return (s > 0 && o < 0 || s === 0) && r.getWidth() === a;
    };
    if (e === -1)
      return Array.from(this.selectedRange).some((r) => n(r));
    const i = this.selectedRange.peekByIndex(e);
    return i ? n(i) : !1;
  }
  /**
   * Returns `true` if the selection was applied by clicking to the column header. If the `layerLevel`
   * argument is passed then only that layer will be checked. Otherwise, it checks if any column header
   * was clicked on any selection layer level.
   *
   * @param {number} [layerLevel=this.getLayerLevel()] Selection layer level to check.
   * @returns {boolean}
   */
  isSelectedByColumnHeader() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.getLayerLevel();
    return !this.isSelectedByCorner() && (e === -1 ? this.selectedByColumnHeader.size > 0 : this.selectedByColumnHeader.has(e));
  }
  /**
   * Returns `true` if the selection consists of entire columns (including their headers). If the `layerLevel`
   * argument is passed then only that layer will be checked. Otherwise, it checks the selection for all layers.
   *
   * @param {number} [layerLevel=this.getLayerLevel()] Selection layer level to check.
   * @returns {boolean}
   */
  isEntireColumnSelected() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.getLayerLevel();
    const n = (r) => {
      const {
        row: o
      } = r.getOuterTopStartCorner(), s = this.tableProps.countColHeaders(), a = this.tableProps.countRows();
      return (s > 0 && o < 0 || s === 0) && r.getHeight() === a;
    };
    if (e === -1)
      return Array.from(this.selectedRange).some((r) => n(r));
    const i = this.selectedRange.peekByIndex(e);
    return i ? n(i) : !1;
  }
  /**
   * Returns `true` if the selection was applied by clicking on the row or column header on any layer level.
   *
   * @returns {boolean}
   */
  isSelectedByAnyHeader() {
    return this.isSelectedByRowHeader(-1) || this.isSelectedByColumnHeader(-1) || this.isSelectedByCorner();
  }
  /**
   * Returns `true` if the selection was applied by clicking on the left-top corner overlay.
   *
   * @returns {boolean}
   */
  isSelectedByCorner() {
    return this.selectedByColumnHeader.has(this.getLayerLevel()) && this.selectedByRowHeader.has(this.getLayerLevel());
  }
  /**
   * Returns `true` if coords is within selection coords. This method iterates through all selection layers to check if
   * the coords object is within selection range.
   *
   * @param {CellCoords} coords The CellCoords instance with defined visual coordinates.
   * @returns {boolean}
   */
  inInSelection(e) {
    return this.selectedRange.includes(e);
  }
  /**
   * Returns `true` if the cell corner should be visible.
   *
   * @private
   * @returns {boolean} `true` if the corner element has to be visible, `false` otherwise.
   */
  isCellCornerVisible() {
    return this.settings.fillHandle && !this.tableProps.isEditorOpened() && !this.isMultiple();
  }
  /**
   * Returns `true` if the cell coordinates are visible (renderable).
   *
   * @private
   * @param {CellCoords} coords The cell coordinates to check.
   * @returns {boolean}
   */
  isCellVisible(e) {
    const n = this.tableProps.visualToRenderableCoords(e);
    return n.row !== null && n.col !== null;
  }
  /**
   * Returns `true` if the area corner should be visible.
   *
   * @param {number} layerLevel The layer level.
   * @returns {boolean} `true` if the corner element has to be visible, `false` otherwise.
   */
  isAreaCornerVisible(e) {
    return Number.isInteger(e) && e !== this.getLayerLevel() ? !1 : this.settings.fillHandle && !this.tableProps.isEditorOpened() && this.isMultiple();
  }
  /**
   * Clear the selection by resetting the collected ranges and highlights.
   */
  clear() {
    this.selectedRange.clear(), this.highlight.clear();
  }
  /**
   * Deselects all selected cells.
   */
  deselect() {
    this.isSelected() && (this.inProgress = !1, this.clear(), this.runLocalHooks("afterDeselect"));
  }
  /**
   * Selects all cells and headers.
   *
   * @param {boolean} [includeRowHeaders=false] `true` If the selection should include the row headers,
   * `false` otherwise.
   * @param {boolean} [includeColumnHeaders=false] `true` If the selection should include the column
   * headers, `false` otherwise.
   * @param {object} [options] Additional object with options.
   * @param {{row: number, col: number} | boolean} [options.focusPosition] The argument allows changing the cell/header
   * focus position. The value takes an object with a `row` and `col` properties from -N to N, where
   * negative values point to the headers and positive values point to the cell range. If `false`, the focus
   * position won't be changed.
   * @param {boolean} [options.disableHeadersHighlight] If `true`, disables highlighting the headers even when
   * the logical coordinates points on them.
   */
  selectAll() {
    var e;
    let n = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1, i = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1, r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {
      focusPosition: !1,
      disableHeadersHighlight: !1
    };
    const o = this.tableProps.countRows(), s = this.tableProps.countCols(), a = this.tableProps.countRowHeaders(), l = this.tableProps.countColHeaders(), u = i ? -l : 0, c = n ? -a : 0;
    if (u === 0 && c === 0 && (o === 0 || s === 0))
      return;
    let h = (e = this.getSelectedRange().current()) === null || e === void 0 ? void 0 : e.highlight;
    const {
      focusPosition: d,
      disableHeadersHighlight: g
    } = r;
    Dr(gc, this, g), d && Number.isInteger(d == null ? void 0 : d.row) && Number.isInteger(d == null ? void 0 : d.col) && (h = this.tableProps.createCellCoords(ft(d.row, u, o - 1), ft(d.col, c, s - 1)));
    const m = this.tableProps.createCellCoords(u, c), w = this.tableProps.createCellCoords(o - 1, s - 1);
    this.clear(), this.setRangeStartOnly(m, void 0, h), c < 0 && this.selectedByRowHeader.add(this.getLayerLevel()), u < 0 && this.selectedByColumnHeader.add(this.getLayerLevel()), this.setRangeEnd(w), this.finish(), Dr(gc, this, !1);
  }
  /**
   * Make multiple, non-contiguous selection specified by `row` and `column` values or a range of cells
   * finishing at `endRow`, `endColumn`. The method supports two input formats, first as an array of arrays such
   * as `[[rowStart, columnStart, rowEnd, columnEnd]]` and second format as an array of CellRange objects.
   * If the passed ranges have another format the exception will be thrown.
   *
   * @param {Array[]|CellRange[]} selectionRanges The coordinates which define what the cells should be selected.
   * @returns {boolean} Returns `true` if selection was successful, `false` otherwise.
   */
  selectCells(e) {
    var n = this;
    const i = Mu(e);
    if (i === Qw)
      return !1;
    if (i === Kw)
      throw new Error(mt`Unsupported format of the selection ranges was passed. To select cells pass\x20
        the coordinates as an array of arrays ([[rowStart, columnStart/columnPropStart, rowEnd,\x20
        columnEnd/columnPropEnd]]) or as an array of CellRange objects.`);
    const r = Qh(i, {
      createCellCoords: function() {
        return n.tableProps.createCellCoords(...arguments);
      },
      createCellRange: function() {
        return n.tableProps.createCellRange(...arguments);
      },
      propToCol: (l) => this.tableProps.propToCol(l),
      keepDirection: !0
    }), o = this.settings.navigableHeaders, s = {
      countRows: this.tableProps.countRows(),
      countCols: this.tableProps.countCols(),
      countRowHeaders: o ? this.tableProps.countRowHeaders() : 0,
      countColHeaders: o ? this.tableProps.countColHeaders() : 0
    }, a = !e.some((l) => {
      const u = r(l), c = u.isValid(s);
      return !(c && !u.containsHeaders() || c && u.containsHeaders() && u.isSingleHeader());
    });
    return a && (this.clear(), this.setExpectedLayers(e.length), T(e, (l) => {
      const {
        from: u,
        to: c
      } = r(l);
      this.setRangeStartOnly(u.clone(), !1), this.setRangeEnd(c.clone());
    }), this.finish()), a;
  }
  /**
   * Select column specified by `startColumn` visual index or column property or a range of columns finishing at
   * `endColumn`.
   *
   * @param {number|string} startColumn Visual column index or column property from which the selection starts.
   * @param {number|string} [endColumn] Visual column index or column property from to the selection finishes.
   * @param {number | { row: number, col: number }} [focusPosition=0] The argument allows changing the cell/header focus
   * position. The value can take visual row index from -N to N, where negative values point to the headers and positive
   * values point to the cell range. An object with `row` and `col` properties also can be passed to change the focus
   * position horizontally.
   * @returns {boolean} Returns `true` if selection was successful, `false` otherwise.
   */
  selectColumns(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : e, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
    const r = typeof e == "string" ? this.tableProps.propToCol(e) : e, o = typeof n == "string" ? this.tableProps.propToCol(n) : n, s = this.tableProps.countRows(), a = this.tableProps.countCols(), l = this.tableProps.countColHeaders(), u = l === 0 ? 0 : -l, c = this.tableProps.createCellCoords(u, r), h = this.tableProps.createCellCoords(s - 1, o), d = this.tableProps.createCellRange(c, c, h).isValid({
      countRows: s,
      countCols: a,
      countRowHeaders: 0,
      countColHeaders: l
    });
    if (d) {
      let g = 0, m = 0;
      Number.isInteger(i == null ? void 0 : i.row) && Number.isInteger(i == null ? void 0 : i.col) ? (g = ft(i.row, u, s - 1), m = ft(i.col, Math.min(r, o), Math.max(r, o))) : (g = ft(i, u, s - 1), m = r);
      const w = this.tableProps.createCellCoords(g, m), C = l === 0 ? 0 : ft(w.row, u, -1), S = s - 1, b = this.tableProps.createCellCoords(C, r), R = this.tableProps.createCellCoords(S, o);
      this.runLocalHooks("beforeSelectColumns", b, R, w), b.row = C, R.row = S, this.setRangeStartOnly(b, void 0, w), this.selectedByColumnHeader.add(this.getLayerLevel()), this.setRangeEnd(R), this.runLocalHooks("afterSelectColumns", b, R, w), this.finish();
    }
    return d;
  }
  /**
   * Select row specified by `startRow` visual index or a range of rows finishing at `endRow`.
   *
   * @param {number} startRow Visual row index from which the selection starts.
   * @param {number} [endRow] Visual row index from to the selection finishes.
   * @param {number | { row: number, col: number }} [focusPosition=0] The argument allows changing the cell/header focus
   * position. The value can take visual row index from -N to N, where negative values point to the headers and positive
   * values point to the cell range. An object with `row` and `col` properties also can be passed to change the focus
   * position horizontally.
   * @returns {boolean} Returns `true` if selection was successful, `false` otherwise.
   */
  selectRows(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : e, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
    const r = this.tableProps.countRows(), o = this.tableProps.countCols(), s = this.tableProps.countRowHeaders(), a = s === 0 ? 0 : -s, l = this.tableProps.createCellCoords(e, a), u = this.tableProps.createCellCoords(n, o - 1), c = this.tableProps.createCellRange(l, l, u).isValid({
      countRows: r,
      countCols: o,
      countRowHeaders: s,
      countColHeaders: 0
    });
    if (c) {
      let h = 0, d = 0;
      Number.isInteger(i == null ? void 0 : i.row) && Number.isInteger(i == null ? void 0 : i.col) ? (h = ft(i.row, Math.min(e, n), Math.max(e, n)), d = ft(i.col, a, o - 1)) : (h = e, d = ft(i, a, o - 1));
      const g = this.tableProps.createCellCoords(h, d), m = s === 0 ? 0 : ft(g.col, a, -1), w = o - 1, C = this.tableProps.createCellCoords(e, m), S = this.tableProps.createCellCoords(n, w);
      this.runLocalHooks("beforeSelectRows", C, S, g), C.col = m, S.col = w, this.setRangeStartOnly(C, void 0, g), this.selectedByRowHeader.add(this.getLayerLevel()), this.setRangeEnd(S), this.runLocalHooks("afterSelectRows", C, S, g), this.finish();
    }
    return c;
  }
  /**
   * Refreshes the whole selection by clearing, reapplying and committing the renderable selection (Walkontable Selection API)
   * by using already added visual ranges.
   */
  refresh() {
    if (!this.isSelected())
      return;
    const e = this.tableProps.countRows(), n = this.tableProps.countCols();
    if (e === 0 || n === 0) {
      this.deselect();
      return;
    }
    const i = this.selectedRange.peekByIndex(this.selectedRange.size() - 1), {
      from: r,
      to: o,
      highlight: s
    } = i;
    this.clear(), s.assign({
      row: ft(s.row, -1 / 0, e - 1),
      col: ft(s.col, -1 / 0, n - 1)
    }), r.assign({
      row: ft(r.row, -1 / 0, e - 1),
      col: ft(r.col, -1 / 0, n - 1)
    }), o.assign({
      row: ft(o.row, 0, e - 1),
      col: ft(o.col, 0, n - 1)
    }), this.selectedRange.ranges.push(i), this.highlight.getFocus().add(s).commit().syncWith(i), this.applyAndCommit(i);
  }
  /**
   * Refreshes the whole selection by recommitting (recalculating visual indexes to renderable ones) the renderable selection
   * that was already added.
   */
  commit() {
    if (this.highlight.getCustomSelections().forEach((r) => {
      r.commit();
    }), !this.isSelected())
      return;
    const n = this.highlight.getFocus(), i = this.getLayerLevel();
    n.commit().syncWith(this.selectedRange.current());
    for (let r = 0; r < this.selectedRange.size(); r += 1) {
      this.highlight.useLayerLevel(r);
      const o = this.highlight.createArea(), s = this.highlight.createLayeredArea(), a = this.highlight.createRowHeader(), l = this.highlight.createColumnHeader(), u = this.highlight.createActiveRowHeader(), c = this.highlight.createActiveColumnHeader(), h = this.highlight.createActiveCornerHeader(), d = this.highlight.createRowHighlight(), g = this.highlight.createColumnHighlight();
      o.commit(), s.commit(), a.commit(), l.commit(), u.commit(), c.commit(), h.commit(), d.commit(), g.commit();
    }
    this.highlight.useLayerLevel(i);
  }
}
pt(b1, Vn);
const Y4 = b1, vT = /^(\r\n|\n\r|\r|\n)/, j4 = /^[^\t\r\n]+/, yT = /^\t/;
function G4(t) {
  const e = [[""]];
  if (t.length === 0)
    return e;
  let n = 0, i = 0, r;
  for (; t.length > 0 && r !== t.length; )
    if (r = t.length, t.match(yT))
      t = t.replace(yT, ""), n += 1, e[i][n] = "";
    else if (t.match(vT))
      t = t.replace(vT, ""), n = 0, i += 1, e[i] = [""];
    else {
      let o = "";
      if (t.startsWith('"')) {
        let s = 0, a = !0;
        for (; a; ) {
          const l = t.slice(0, 1);
          l === '"' && (s += 1), o += l, t = t.slice(1), (t.length === 0 || t.match(/^[\t\r\n]/) && s % 2 === 0) && (a = !1);
        }
        o = o.replace(/^"/, "").replace(/"$/, "").replace(/["]*/g, (l) => new Array(Math.floor(l.length / 2)).fill('"').join(""));
      } else {
        const s = t.match(j4);
        o = s ? s[0] : "", t = t.slice(o.length);
      }
      e[i][n] = o;
    }
  return e;
}
function uh(t) {
  let e, n, i, r, o = "", s;
  for (e = 0, n = t.length; e < n; e += 1) {
    for (r = t[e].length, i = 0; i < r; i += 1)
      i > 0 && (o += "	"), s = t[e][i], typeof s == "string" ? s.indexOf(`
`) > -1 ? o += `"${s.replace(/"/g, '""')}"` : o += s : s == null ? o += "" : o += s;
    e !== n - 1 && (o += `
`);
  }
  return o;
}
function da(t, e, n) {
  return (e = z4(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function z4(t) {
  var e = K4(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function K4(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class hu {
  /**
   * @type {number}
   */
  static get DESTINATION_RENDERER() {
    return 1;
  }
  /**
   * @type {number}
   */
  static get DESTINATION_CLIPBOARD_GENERATOR() {
    return 2;
  }
  /**
   * Instance of {@link Handsontable}.
   *
   * @private
   * @type {Handsontable}
   */
  /**
   * @param {object} hotInstance Instance of Handsontable.
   * @param {Array} data Array of arrays or array of objects containing data.
   * @param {MetaManager} metaManager The meta manager instance.
   */
  constructor(e, n, i) {
    da(this, "hot", void 0), da(this, "metaManager", void 0), da(this, "tableMeta", void 0), da(this, "dataSource", void 0), da(this, "duckSchema", void 0), da(this, "colToPropCache", void 0), da(this, "propToColCache", void 0), this.hot = e, this.metaManager = i, this.tableMeta = i.getTableMeta(), this.dataSource = n, this.duckSchema = this.createDuckSchema(), this.createMap();
  }
  /**
   * Generates cache for property to and from column addressation.
   */
  createMap() {
    const e = this.getSchema();
    if (typeof e == "undefined")
      throw new Error("trying to create `columns` definition but you didn't provide `schema` nor `data`");
    const n = this.tableMeta.columns;
    let i;
    if (this.colToPropCache = [], this.propToColCache = /* @__PURE__ */ new Map(), n) {
      let r = 0, o = 0, s = !1;
      if (typeof n == "function") {
        const a = Pp(e);
        r = a > 0 ? a : this.countFirstRowKeys(), s = !0;
      } else {
        const a = this.tableMeta.maxCols;
        r = Math.min(a, n.length);
      }
      for (i = 0; i < r; i++) {
        const a = s ? n(i) : n[i];
        if (Be(a)) {
          if (typeof a.data != "undefined") {
            const l = s ? o : i;
            this.colToPropCache[l] = a.data, this.propToColCache.set(a.data, l);
          }
          o += 1;
        }
      }
    } else
      this.recursiveDuckColumns(e);
  }
  /**
   * Get the amount of physical columns in the first data row.
   *
   * @returns {number} Amount of physical columns in the first data row.
   */
  countFirstRowKeys() {
    return Ub(this.dataSource);
  }
  /**
   * Generates columns' translation cache.
   *
   * @param {object} schema An object to generate schema from.
   * @param {number} lastCol The column index.
   * @param {number} parent The property cache for recursive calls.
   * @returns {number}
   */
  recursiveDuckColumns(e, n, i) {
    let r = n, o = i, s;
    return typeof r == "undefined" && (r = 0, o = ""), typeof e == "object" && !Array.isArray(e) && Fe(e, (a, l) => {
      a === null ? (s = o + l, this.colToPropCache.push(s), this.propToColCache.set(s, r), r += 1) : r = this.recursiveDuckColumns(a, r, `${l}.`);
    }), r;
  }
  /**
   * Returns property name that corresponds with the given column index.
   *
   * @param {string|number} column Visual column index or another passed argument.
   * @returns {string|number} Column property, physical column index or passed argument.
   */
  colToProp(e) {
    if (Number.isInteger(e) === !1)
      return e;
    const n = this.hot.toPhysicalColumn(e);
    return n === null ? e : this.colToPropCache && de(this.colToPropCache[n]) ? this.colToPropCache[n] : n;
  }
  /**
   * Translates property into visual column index.
   *
   * @param {string|number} prop Column property which may be also a physical column index.
   * @returns {string|number} Visual column index or passed argument.
   */
  propToCol(e) {
    const n = this.propToColCache.get(e);
    if (de(n))
      return this.hot.toVisualColumn(n);
    const i = this.hot.toVisualColumn(e);
    return i === null ? e : i;
  }
  /**
   * Returns data's schema.
   *
   * @returns {object}
   */
  getSchema() {
    const e = this.tableMeta.dataSchema;
    return e ? typeof e == "function" ? e() : e : this.duckSchema;
  }
  /**
   * Creates the duck schema based on the current dataset.
   *
   * @returns {Array|object}
   */
  createDuckSchema() {
    return this.dataSource && this.dataSource[0] ? Tu(this.dataSource[0]) : {};
  }
  /**
   * Refresh the data schema.
   */
  refreshDuckSchema() {
    this.duckSchema = this.createDuckSchema();
  }
  /**
   * Creates row at the bottom of the data array.
   *
   * @param {number} [index] Physical index of the row before which the new row will be inserted.
   * @param {number} [amount=1] An amount of rows to add.
   * @param {object} [options] Additional options for created rows.
   * @param {string} [options.source] Source of method call.
   * @param {'above'|'below'} [options.mode] Sets where the row is inserted: above or below the passed index.
   * @fires Hooks#afterCreateRow
   * @returns {number} Returns number of created rows.
   */
  createRow(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1, {
      source: i,
      mode: r = "above"
    } = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    const o = this.hot.countSourceRows();
    let s = o, a = 0, l = e;
    if ((typeof l != "number" || l >= o) && (l = o), l < this.hot.countRows() && (s = this.hot.toPhysicalRow(l)), this.hot.runHooks("beforeCreateRow", l, n, i) === !1 || s === null)
      return {
        delta: 0
      };
    const c = this.tableMeta.maxRows, h = this.getSchema().length, d = [];
    for (; a < n && o + a < c; ) {
      let m = null;
      this.hot.dataType === "array" ? this.tableMeta.dataSchema ? m = Fn(this.getSchema()) : (m = [], Ae(h - 1, () => m.push(null))) : this.hot.dataType === "function" ? m = this.tableMeta.dataSchema(l + a) : (m = {}, Sd(m, this.getSchema())), d.push(m), a += 1;
    }
    this.hot.rowIndexMapper.insertIndexes(l, a), r === "below" && (s = Math.min(s + 1, o)), this.spliceData(s, 0, d);
    const g = this.hot.toVisualRow(s);
    return this.hot.countSourceRows() === d.length && this.hot.columnIndexMapper.initToLength(this.hot.getInitialColumnCount()), a > 0 && (e == null ? this.metaManager.createRow(null, a) : i !== "auto" && this.metaManager.createRow(s, n)), this.hot.runHooks("afterCreateRow", g, a, i), this.hot.forceFullRender = !0, {
      delta: a,
      startPhysicalIndex: s
    };
  }
  /**
   * Creates column at the right of the data array.
   *
   * @param {number} [index] Visual index of the column before which the new column will be inserted.
   * @param {number} [amount=1] An amount of columns to add.
   * @param {object} [options] Additional options for created columns.
   * @param {string} [options.source] Source of method call.
   * @param {'start'|'end'} [options.mode] Sets where the column is inserted: at the start (left in [LTR](@/api/options.md#layoutdirection), right in [RTL](@/api/options.md#layoutdirection)) or at the end (right in LTR, left in LTR)
   * the passed index.
   * @fires Hooks#afterCreateCol
   * @returns {number} Returns number of created columns.
   */
  createCol(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1, {
      source: i,
      mode: r = "start"
    } = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    if (!this.hot.isColumnModificationAllowed())
      throw new Error("Cannot create new column. When data source in an object, you can only have as much columns as defined in first data row, data schema or in the 'columns' setting.If you want to be able to add new columns, you have to use array datasource.");
    const o = this.dataSource, s = this.tableMeta.maxCols, a = this.hot.countSourceCols();
    let l = e;
    if ((typeof l != "number" || l >= a) && (l = a), this.hot.runHooks("beforeCreateCol", l, n, i) === !1)
      return {
        delta: 0
      };
    let c = a;
    l < this.hot.countCols() && (c = this.hot.toPhysicalColumn(l));
    const h = this.hot.countSourceRows();
    let d = this.hot.countCols(), g = 0, m = c;
    r === "end" && (m = Math.min(m + 1, a));
    const w = m;
    for (; g < n && d < s; ) {
      if (typeof l != "number" || l >= d)
        if (h > 0)
          for (let S = 0; S < h; S += 1)
            typeof o[S] == "undefined" && (o[S] = []), o[S].push(null);
        else
          o.push([null]);
      else
        for (let S = 0; S < h; S++)
          o[S].splice(m, 0, null);
      g += 1, m += 1, d += 1;
    }
    this.hot.columnIndexMapper.insertIndexes(l, g), g > 0 && (e == null ? this.metaManager.createColumn(null, g) : i !== "auto" && this.metaManager.createColumn(w, n));
    const C = this.hot.toVisualColumn(w);
    return this.hot.runHooks("afterCreateCol", C, g, i), this.hot.forceFullRender = !0, this.refreshDuckSchema(), {
      delta: g,
      startPhysicalIndex: w
    };
  }
  /**
   * Removes row from the data array.
   *
   * @fires Hooks#beforeRemoveRow
   * @fires Hooks#afterRemoveRow
   * @param {number} [index] Visual index of the row to be removed. If not provided, the last row will be removed.
   * @param {number} [amount=1] Amount of the rows to be removed. If not provided, one row will be removed.
   * @param {string} [source] Source of method call.
   * @returns {boolean} Returns `false` when action was cancelled, otherwise `true`.
   */
  removeRow(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1, i = arguments.length > 2 ? arguments[2] : void 0, r = Number.isInteger(e) ? e : -n;
    const o = this.visualRowsToPhysical(r, n), s = this.hot.countSourceRows();
    if (r = (s + r) % s, this.hot.runHooks("beforeRemoveRow", r, o.length, o, i) === !1)
      return !1;
    const l = o.length;
    if (this.filterData(r, l, o), r < this.hot.countRows()) {
      this.hot.rowIndexMapper.removeIndexes(o);
      const c = de(this.tableMeta.columns) || de(this.tableMeta.dataSchema);
      this.hot.rowIndexMapper.getNotTrimmedIndexesLength() === 0 && c === !1 && this.hot.columnIndexMapper.setIndexesSequence([]);
    }
    return o.slice(0).sort((c, h) => h - c).forEach((c) => {
      this.metaManager.removeRow(c, 1);
    }), this.hot.runHooks("afterRemoveRow", r, l, o, i), this.hot.forceFullRender = !0, !0;
  }
  /**
   * Removes column from the data array.
   *
   * @fires Hooks#beforeRemoveCol
   * @fires Hooks#afterRemoveCol
   * @param {number} [index] Visual index of the column to be removed. If not provided, the last column will be removed.
   * @param {number} [amount=1] Amount of the columns to be removed. If not provided, one column will be removed.
   * @param {string} [source] Source of method call.
   * @returns {boolean} Returns `false` when action was cancelled, otherwise `true`.
   */
  removeCol(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1, i = arguments.length > 2 ? arguments[2] : void 0;
    if (this.hot.dataType === "object" || this.tableMeta.columns)
      throw new Error("cannot remove column with object data source or columns option specified");
    let r = typeof e != "number" ? -n : e;
    r = (this.hot.countCols() + r) % this.hot.countCols();
    const o = this.visualColumnsToPhysical(r, n), s = o.slice(0).sort((h, d) => d - h);
    if (this.hot.runHooks("beforeRemoveCol", r, n, o, i) === !1)
      return !1;
    let l = !0;
    const u = s.length, c = this.dataSource;
    for (let h = 0; h < u; h++)
      l && o[0] !== o[h] - h && (l = !1);
    if (l)
      for (let h = 0, d = this.hot.countSourceRows(); h < d; h++)
        c[h].splice(o[0], n), h === 0 && this.metaManager.removeColumn(o[0], n);
    else
      for (let h = 0, d = this.hot.countSourceRows(); h < d; h++)
        for (let g = 0; g < u; g++)
          c[h].splice(s[g], 1), h === 0 && this.metaManager.removeColumn(s[g], 1);
    return r < this.hot.countCols() && (this.hot.columnIndexMapper.removeIndexes(o), this.hot.columnIndexMapper.getNotTrimmedIndexesLength() === 0 && this.hot.rowIndexMapper.setIndexesSequence([])), this.hot.runHooks("afterRemoveCol", r, n, o, i), this.hot.forceFullRender = !0, this.refreshDuckSchema(), !0;
  }
  /**
   * Add/Removes data from the column.
   *
   * @param {number} col Physical index of column in which do you want to do splice.
   * @param {number} index Index at which to start changing the array. If negative, will begin that many elements from the end.
   * @param {number} amount An integer indicating the number of old array elements to remove. If amount is 0, no elements are removed.
   * @param {Array} [elements] The new columns to add.
   * @returns {Array} Returns removed portion of columns.
   */
  spliceCol(e, n, i) {
    const r = this.hot.getDataAtCol(e), o = r.slice(n, n + i), s = r.slice(n + i);
    for (var a = arguments.length, l = new Array(a > 3 ? a - 3 : 0), u = 3; u < a; u++)
      l[u - 3] = arguments[u];
    hy(l, s);
    let c = 0;
    for (; c < i; )
      l.push(null), c += 1;
    return RP(l), this.hot.populateFromArray(n, e, l, null, null, "spliceCol"), o;
  }
  /**
   * Add/Removes data from the row.
   *
   * @param {number} row Physical index of row in which do you want to do splice.
   * @param {number} index Index at which to start changing the array. If negative, will begin that many elements from the end.
   * @param {number} amount An integer indicating the number of old array elements to remove. If amount is 0, no elements are removed.
   * @param {Array} [elements] The new rows to add.
   * @returns {Array} Returns removed portion of rows.
   */
  spliceRow(e, n, i) {
    const r = this.hot.getSourceDataAtRow(e), o = r.slice(n, n + i), s = r.slice(n + i);
    for (var a = arguments.length, l = new Array(a > 3 ? a - 3 : 0), u = 3; u < a; u++)
      l[u - 3] = arguments[u];
    hy(l, s);
    let c = 0;
    for (; c < i; )
      l.push(null), c += 1;
    return this.hot.populateFromArray(e, n, [l], null, null, "spliceRow"), o;
  }
  /**
   * Add/remove row(s) to/from the data source.
   *
   * @param {number} index Physical index of the element to add/remove.
   * @param {number} deleteCount Number of rows to remove.
   * @param {Array<object>} elements Row elements to be added.
   */
  spliceData(e, n, i) {
    if (this.hot.runHooks("beforeDataSplice", e, n, i) !== !1) {
      const o = [...this.dataSource.slice(0, e), ...i, ...this.dataSource.slice(e)];
      this.dataSource.length = 0, o.forEach((s) => this.dataSource.push(s));
    }
  }
  /**
   * Filter unwanted data elements from the data source.
   *
   * @param {number} index Visual index of the element to remove.
   * @param {number} amount Number of rows to add/remove.
   * @param {number} physicalRows Physical row indexes.
   */
  filterData(e, n, i) {
    let r = this.hot.runHooks("filterData", e, n, i);
    Array.isArray(r) === !1 && (r = this.dataSource.filter((o, s) => i.indexOf(s) === -1)), this.dataSource.length = 0, Array.prototype.push.apply(this.dataSource, r);
  }
  /**
   * Returns single value from the data array.
   *
   * @param {number} row Visual row index.
   * @param {number} prop The column property.
   * @returns {*}
   */
  get(e, n) {
    const i = this.hot.toPhysicalRow(e);
    let r = this.dataSource[i];
    const o = this.hot.runHooks("modifyRowData", i);
    r = isNaN(o) ? o : r;
    const {
      dataDotNotation: s
    } = this.hot.getSettings();
    let a = null;
    if (r && r.hasOwnProperty && Bt(r, n))
      a = r[n];
    else if (s && typeof n == "string" && n.indexOf(".") > -1) {
      let l = r;
      if (!l)
        return null;
      const u = n.split(".");
      for (let c = 0, h = u.length; c < h; c++)
        if (l = l[u[c]], typeof l == "undefined")
          return null;
      a = l;
    } else
      typeof n == "function" && (a = n(this.dataSource.slice(i, i + 1)[0]));
    if (this.hot.hasHook("modifyData")) {
      const l = ji(a);
      this.hot.runHooks("modifyData", i, this.propToCol(n), l, "get"), l.isTouched() && (a = l.value);
    }
    return a;
  }
  /**
   * Returns single value from the data array (intended for clipboard copy to an external application).
   *
   * @param {number} row Physical row index.
   * @param {number} prop The column property.
   * @returns {string}
   */
  getCopyable(e, n) {
    return this.hot.getCellMeta(e, this.propToCol(n)).copyable ? this.get(e, n) : "";
  }
  /**
   * Saves single value to the data array.
   *
   * @param {number} row Visual row index.
   * @param {number} prop The column property.
   * @param {string} value The value to set.
   */
  set(e, n, i) {
    const r = this.hot.toPhysicalRow(e);
    let o = i, s = this.dataSource[r];
    const a = this.hot.runHooks("modifyRowData", r);
    if (s = isNaN(a) ? a : s, this.hot.hasHook("modifyData")) {
      const u = ji(o);
      this.hot.runHooks("modifyData", r, this.propToCol(n), u, "set"), u.isTouched() && (o = u.value);
    }
    const {
      dataDotNotation: l
    } = this.hot.getSettings();
    if (s && s.hasOwnProperty && Bt(s, n))
      s[n] = o;
    else if (l && typeof n == "string" && n.indexOf(".") > -1) {
      let u = s, c = 0, h;
      const d = n.split(".");
      for (c = 0, h = d.length - 1; c < h; c++)
        typeof u[d[c]] == "undefined" && (u[d[c]] = {}), u = u[d[c]];
      u[d[c]] = o;
    } else
      typeof n == "function" ? n(this.dataSource.slice(r, r + 1)[0], o) : s[n] = o;
  }
  /**
   * This ridiculous piece of code maps rows Id that are present in table data to those displayed for user.
   * The trick is, the physical row id (stored in settings.data) is not necessary the same
   * as the visual (displayed) row id (e.g. When sorting is applied).
   *
   * @param {number} index Visual row index.
   * @param {number} amount An amount of rows to translate.
   * @returns {number}
   */
  visualRowsToPhysical(e, n) {
    const i = this.hot.countSourceRows(), r = [];
    let o = (i + e) % i, s = n, a;
    for (; o < i && s; )
      a = this.hot.toPhysicalRow(o), r.push(a), s -= 1, o += 1;
    return r;
  }
  /**
   *
   * @param {number} index Visual column index.
   * @param {number} amount An amount of rows to translate.
   * @returns {Array}
   */
  visualColumnsToPhysical(e, n) {
    const i = this.hot.countCols(), r = [];
    let o = (i + e) % i, s = n;
    for (; o < i && s; ) {
      const a = this.hot.toPhysicalColumn(o);
      r.push(a), s -= 1, o += 1;
    }
    return r;
  }
  /**
   * Clears the data array.
   */
  clear() {
    for (let e = 0; e < this.hot.countSourceRows(); e++)
      for (let n = 0; n < this.hot.countCols(); n++)
        this.set(e, this.colToProp(n), "");
  }
  /**
   * Get data length.
   *
   * @returns {number}
   */
  getLength() {
    const e = this.tableMeta.maxRows;
    let n;
    e < 0 || e === 0 ? n = 0 : n = e || 1 / 0;
    const i = this.hot.rowIndexMapper.getNotTrimmedIndexesLength();
    return Math.min(i, n);
  }
  /**
   * Returns the data array.
   *
   * @returns {Array}
   */
  getAll() {
    const e = {
      row: 0,
      col: 0
    }, n = {
      row: Math.max(this.hot.countRows() - 1, 0),
      col: Math.max(this.hot.countCols() - 1, 0)
    };
    return e.row - n.row === 0 && !this.hot.countSourceRows() ? [] : this.getRange(e, n, hu.DESTINATION_RENDERER);
  }
  /**
   * Count the number of columns cached in the `colToProp` cache.
   *
   * @returns {number} Amount of cached columns.
   */
  countCachedColumns() {
    return this.colToPropCache.length;
  }
  /**
   * Returns data range as array.
   *
   * @param {object} [start] Start selection position. Visual indexes.
   * @param {object} [end] End selection position. Visual indexes.
   * @param {number} destination Destination of datamap.get.
   * @returns {Array}
   */
  getRange(e, n, i) {
    const r = [];
    let o, s, a;
    const l = this.tableMeta.maxRows, u = this.tableMeta.maxCols;
    if (l === 0 || u === 0)
      return [];
    const c = i === hu.DESTINATION_CLIPBOARD_GENERATOR ? this.getCopyable : this.get, h = Math.min(Math.max(l - 1, 0), Math.max(e.row, n.row)), d = Math.min(Math.max(u - 1, 0), Math.max(e.col, n.col));
    for (o = Math.min(e.row, n.row); o <= h; o++) {
      a = [];
      const g = o >= 0 ? this.hot.toPhysicalRow(o) : o;
      for (s = Math.min(e.col, n.col); s <= d && g !== null; s++)
        a.push(c.call(this, o, this.colToProp(s)));
      g !== null && r.push(a);
    }
    return r;
  }
  /**
   * Return data as text (tab separated columns).
   *
   * @param {object} [start] Start selection position. Visual indexes.
   * @param {object} [end] End selection position. Visual indexes.
   * @returns {string}
   */
  getText(e, n) {
    return uh(this.getRange(e, n, hu.DESTINATION_RENDERER));
  }
  /**
   * Return data as copyable text (tab separated columns intended for clipboard copy to an external application).
   *
   * @param {object} [start] Start selection position. Visual indexes.
   * @param {object} [end] End selection position. Visual indexes.
   * @returns {string}
   */
  getCopyableText(e, n) {
    return uh(this.getRange(e, n, hu.DESTINATION_CLIPBOARD_GENERATOR));
  }
  /**
   * Destroy instance.
   */
  destroy() {
    this.hot = null, this.metaManager = null, this.dataSource = null, this.duckSchema = null, this.colToPropCache.length = 0, this.propToColCache.clear(), this.propToColCache = void 0;
  }
}
const Q4 = hu, {
  register: X4,
  getItem: q4,
  hasItem: R1,
  getNames: Z4,
  getValues: Dce
} = Or("cellTypes");
function iR(t) {
  if (!R1(t))
    throw Error(`You declared cell type "${t}" as a string that is not mapped to a known object.
                 Cell type must be an object or a string mapped to an object registered by
                 "Handsontable.cellTypes.registerCellType" method`);
  return q4(t);
}
function Di(t, e) {
  typeof t != "string" && (e = t, t = e.CELL_TYPE);
  const {
    editor: n,
    renderer: i,
    validator: r
  } = e;
  n && ai(t, n), i && wi(t, i), r && $a(t, r), X4(t, e);
}
function J4(t, e) {
  var n;
  return t === "CELL_TYPE" ? !1 : ((n = e._automaticallyAssignedMetaProps) === null || n === void 0 ? void 0 : n.has(t)) || !Bt(e, t);
}
function Xw(t, e) {
  let n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : t;
  const i = typeof e.type == "string" ? iR(e.type) : e.type;
  if (t._automaticallyAssignedMetaProps && Fe(e, (o, s) => void t._automaticallyAssignedMetaProps.delete(s)), !Be(i))
    return;
  n === t && !t._automaticallyAssignedMetaProps && (t._automaticallyAssignedMetaProps = /* @__PURE__ */ new Set());
  const r = {};
  Fe(i, (o, s) => {
    if (J4(s, n)) {
      var a;
      r[s] = o, (a = t._automaticallyAssignedMetaProps) === null || a === void 0 || a.add(s);
    }
  }), nn(t, r);
}
function eq(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
  function n() {
  }
  Ir(n, t);
  for (let i = 0; i < e.length; i++)
    n.prototype[e[i]] = void 0;
  return n;
}
function Gg(t) {
  return Number.isInteger(t) && t >= 0;
}
function zg(t, e) {
  if (!t())
    throw new Error(`Assertion failed: ${e}`);
}
function Hf(t) {
  return t == null;
}
const E1 = () => ({
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * Information on which of the meta properties were added automatically.
   * For example: setting the `renderer` property directly won't extend the `_automaticallyAssignedMetaProps`
   * entry, but setting a `type` will modify it to `Set(3) {'renderer', 'editor', 'validator', ...}`.
   *
   * @private
   * @type {Set}
   * @default undefined
   */
  _automaticallyAssignedMetaProps: void 0,
  /**
   * The `activeHeaderClassName` option lets you add a CSS class name
   * to every currently-active, currently-selected header (when a whole column or row is selected).
   *
   * Read more:
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentColClassName`](#currentColClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`invalidCellClassName`](#invalidCellClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`TableClassName`](#TableClassName)
   * - [`className`](#className)
   *
   * @memberof Options#
   * @type {string}
   * @since 0.38.2
   * @default 'ht__active_highlight'
   * @category Core
   *
   * @example
   * ```js
   * // add an `ht__active_highlight` CSS class name
   * // to every currently-active, currently-selected header
   * activeHeaderClassName: 'ht__active_highlight',
   * ```
   */
  activeHeaderClassName: "ht__active_highlight",
  /**
   * The `allowEmpty` option determines whether Handsontable accepts the following values:
   * - `null`
   * - `undefined`
   * - `''`
   *
   * You can set the `allowEmpty` option to one of the following:
   *
   * | Setting          | Description                                                                                                                          |
   * | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
   * | `true` (default) | - Accept `null`, `undefined` and `''` values<br>- Mark cells that contain `null`, `undefined` or `''` values as `valid`              |
   * | `false`          | - Don't accept `null`, `undefined` and `''` values<br>- Mark cells that contain `null`, `undefined` or `''` values with as `invalid` |
   *
   * ::: tip
   * To use the [`allowEmpty`](#allowempty) option, you need to set the [`validator`](#validator) option (or the [`type`](#type) option).
   * :::
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // allow empty values in each cell of the entire grid
   * allowEmpty: true,
   *
   * // or
   * columns: [
   *   {
   *     type: 'date',
   *     dateFormat: 'DD/MM/YYYY',
   *     // allow empty values in each cell of the 'date' column
   *     allowEmpty: true
   *   }
   * ],
   * ```
   */
  allowEmpty: !0,
  /**
   * The `allowHtml` option configures whether [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * and [`dropdown`](@/guides/cell-types/dropdown-cell-type/dropdown-cell-type.md) cells' [`source`](#source) data
   * is treated as HTML.
   *
   * You can set the `allowHtml` option to one of the following:
   *
   * | Setting           | Description                                         |
   * | ----------------- | --------------------------------------------------- |
   * | `false` (default) | The [`source`](#source) data is not treated as HTML |
   * | `true`            | The [`source`](#source) data is treated as HTML     |
   *
   * __Warning:__ Setting the `allowHtml` option to `true` can cause serious XSS vulnerabilities.
   *
   * Read more:
   * - [Autocomplete cell type](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * - [Dropdown cell type](@/guides/cell-types/dropdown-cell-type/dropdown-cell-type.md)
   * - [`source`](#source)
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *   // set the `type` of each cell in this column to `autocomplete`
   *   type: 'autocomplete',
   *   // set options available in every `autocomplete` cell of this column
   *   source: ['<strong>foo</strong>', '<strong>bar</strong>']
   *   // use HTML in the `source` list
   *   allowHtml: true,
   *   },
   * ],
   * ```
   */
  allowHtml: !1,
  /**
   * If set to `true`, the `allowInsertColumn` option adds the following menu items to the [context menu](@/guides/accessories-and-menus/context-menu/context-menu.md):
   * - **Insert column left**
   * - **Insert column right**
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // hide the 'Insert column left' and 'Insert column right' menu items from the context menu
   * allowInsertColumn: false,
   * ```
   */
  allowInsertColumn: !0,
  /**
   * If set to `true`, the `allowInsertRow` option adds the following menu items to the [context menu](@/guides/accessories-and-menus/context-menu/context-menu.md):
   * - **Insert row above**
   * - **Insert row below**
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // hide the 'Insert row above' and 'Insert row below' menu items from the context menu
   * allowInsertRow: false,
   * ```
   */
  allowInsertRow: !0,
  /**
   * The `allowInvalid` option determines whether Handsontable accepts values
   * that were marked as `invalid` by the [cell validator](@/guides/cell-functions/cell-validator/cell-validator.md).
   *
   * You can set the `allowInvalid` option to one of the following:
   *
   * | Setting          | Description                                                                                                                                                                        |
   * | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `true` (default) | - Accept `invalid` values<br>- Allow the user to close the [cell editor](@/guides/cell-functions/cell-editor/cell-editor.md) with `invalid` values<br>- Save `invalid` values into the data source                   |
   * | `false`          | - Don't accept `invalid` values<br>- Don't allow the user to close the [cell editor](@/guides/cell-functions/cell-editor/cell-editor.md) with `invalid` values<br>- Don't save `invalid` values into the data source |
   *
   * Setting the `allowInvalid` option to `false` can be useful when used with the [Autocomplete strict mode](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md#autocomplete-strict-mode).
   *
   * Read more:
   * - [Cell validator](@/guides/cell-functions/cell-validator/cell-validator.md)
   * - [Cell editor](@/guides/cell-functions/cell-editor/cell-editor.md)
   * - [Autocomplete strict mode](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md#autocomplete-strict-mode)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // don't accept `invalid` values
   * // don't allow the user to close the cell editor
   * // don't save `invalid` values into the data source
   * allowInvalid: false,
   * ```
   */
  allowInvalid: !0,
  /**
   * If set to `true`, the `allowRemoveColumn` option adds the following menu items to the [context menu](@/guides/accessories-and-menus/context-menu/context-menu.md):
   * - **Remove column**
   *
   * Read more:
   * - [Context menu](@/guides/accessories-and-menus/context-menu/context-menu.md)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // hide the 'Remove column' menu item from the context menu
   * allowRemoveColumn: false,
   * ```
   */
  allowRemoveColumn: !0,
  /**
   * If set to `true`, the `allowRemoveRow` option adds the following menu items to the [context menu](@/guides/accessories-and-menus/context-menu/context-menu.md):
   * - **Remove row**
   *
   * Read more:
   * - [Context menu](@/guides/accessories-and-menus/context-menu/context-menu.md)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // hide the 'Remove row' menu item from the context menu
   * allowRemoveRow: false,
   * ```
   */
  allowRemoveRow: !0,
  /**
   * If set to `true`, the accessibility-related ARIA tags will be added to the table. If set to `false`, they
   * will be omitted.
   * Defaults to `true`.
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   * @since 14.0.0
   */
  ariaTags: !0,
  /**
   * The `autoColumnSize` option configures the [`AutoColumnSize`](@/api/autoColumnSize.md) plugin.
   *
   * You can set the `autoColumnSize` option to one of the following:
   *
   * | Setting   | Description                                                                                  |
   * | --------- | -------------------------------------------------------------------------------------------- |
   * | `false`   | Disable the [`AutoColumnSize`](@/api/autoColumnSize.md) plugin                               |
   * | `true`    | Enable the [`AutoColumnSize`](@/api/autoColumnSize.md) plugin with the default configuration |
   * | An object | Enable the [`AutoColumnSize`](@/api/autoColumnSize.md) plugin and modify the plugin options  |
   *
   * If you set the `autoColumnSize` option to an object, you can set the following [`AutoColumnSize`](@/api/autoColumnSize.md) plugin options:
   *
   * | Property                | Possible values                 | Description                                                                                                    |
   * | ----------------------- | ------------------------------- | -------------------------------------------------------------------------------------------------------------- |
   * | `syncLimit`             | A number \| A percentage string | The number/percentage of columns to keep in sync<br>(default: `50`)                                            |
   * | `useHeaders`            | `true` \| `false`               | When calculating column widths:<br>`true`: use column headers<br>`false`: don't use column headers          |
   * | `samplingRatio`         | A number                        | The number of samples of the same length to be used in column width calculations                               |
   * | `allowSampleDuplicates` | `true` \| `false`               | When calculating column widths:<br>`true`: Allow duplicate samples<br>`false`: Don't allow duplicate samples |
   *
   * By default, the `autoColumnSize` option is set to `undefined`,
   * but the [`AutoColumnSize`](@/api/autoColumnSize.md) plugin acts as enabled.
   * To disable the [`AutoColumnSize`](@/api/autoColumnSize.md) plugin completely,
   * set the `autoColumnSize` option to `false`.
   *
   * Using the [`colWidths`](#colWidths) option forcibly disables the [`AutoColumnSize`](@/api/autoColumnSize.md) plugin.
   *
   * Read more:
   * - [Plugins: `AutoColumnSize`](@/api/autoColumnSize.md)
   *
   * @memberof Options#
   * @type {object|boolean}
   * @default undefined
   * @category AutoColumnSize
   *
   * @example
   * ```js
   * autoColumnSize: {
   *   // keep 40% of columns in sync (the rest of columns: async)
   *   syncLimit: '40%',
   *   // when calculating column widths, use column headers
   *   useHeaders: true,
   *   // when calculating column widths, use 10 samples of the same length
   *   samplingRatio: 10,
   *   // when calculating column widths, allow duplicate samples
   *   allowSampleDuplicates: true
   * },
   * ```
   */
  autoColumnSize: void 0,
  /**
   * The `autoRowSize` option configures the [`AutoRowSize`](@/api/autoRowSize.md) plugin.
   *
   * You can set the `autoRowSize` option to one of the following:
   *
   * | Setting   | Description                                                                            |
   * | --------- | -------------------------------------------------------------------------------------- |
   * | `false`   | Disable the [`AutoRowSize`](@/api/autoRowSize.md) plugin                               |
   * | `true`    | Enable the [`AutoRowSize`](@/api/autoRowSize.md) plugin with the default configuration |
   * | An object | Enable the [`AutoRowSize`](@/api/autoRowSize.md) plugin and modify the plugin options  |
   *
   * To give Handsontable's scrollbar a proper size, set the `autoRowSize` option to `true`.
   *
   * If you set the `autoRowSize` option to an object, you can set the following [`AutoRowSize`](@/api/autoRowSize.md) plugin options:
   *
   * | Property    | Possible values                 | Description                                                       |
   * | ----------- | ------------------------------- | ----------------------------------------------------------------- |
   * | `syncLimit` | A number \| A percentage string | The number/percentage of rows to keep in sync<br>(default: `500`) |
   *
   * Using the [`rowHeights`](#rowHeights) option forcibly disables the [`AutoRowSize`](@/api/autoRowSize.md) plugin.
   *
   * Read more:
   * - [Plugins: `AutoRowSize`](@/api/autoRowSize.md)
   *
   * @memberof Options#
   * @type {object|boolean}
   * @default undefined
   * @category AutoRowSize
   *
   * @example
   * ```js
   * autoRowSize: {
   *   // keep 40% of rows in sync (the rest of rows: async)
   *   syncLimit: '40%'
   * },
   * ```
   */
  autoRowSize: void 0,
  /**
   * | Setting           | Description                                                                                                                                                                                                                                  |
   * | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `false` (default) | When you select a bottom-most cell, pressing <kbd>**↓**</kbd> doesn't do anything.<br><br>When you select a top-most cell, pressing <kbd>**↑**</kbd> doesn't do anything.                                                                    |
   * | `true`            | When you select a bottom-most cell, pressing <kbd>**↓**</kbd> takes you to the top-most cell of the next column.<br><br>When you select a top-most cell, pressing <kbd>**↑**</kbd> takes you to the bottom-most cell of the previous column. |
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // when you select a bottom-most cell, pressing ⬇ doesn't do anything
   * // when you select a top-most cell, pressing ⬆ doesn't do anything
   * autoWrapCol: false, // default setting
   *
   * // when you select a bottom-most cell, pressing ⬇ takes you to the top-most cell of the next column
   * // when you select a top-most cell, pressing ⬆ takes you to the bottom-most cell of the previous column
   * autoWrapCol: true,
   * ```
   */
  autoWrapCol: !1,
  /**
   * | Setting           | Description                                                                                                                                                                                                                                                                                                        |
   * | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
   * | `false` (default) | When you select the first cell of a row, pressing <kbd>**←**</kbd>* (or <kbd>**Shift**</kbd>+<kbd>**Tab**</kbd>\*\*) doesn't do anything.<br><br>When you select the last cell of a row, pressing <kbd>**→**</kbd>* (or <kbd>**Tab**</kbd>**) doesn't do anything.                                                  |
   * | `true`            | When you select the first cell of a row, pressing <kbd>**←**</kbd>* (or <kbd>**Shift**</kbd>+<kbd>**Tab**</kbd>\*\*) takes you to the last cell of the row above.<br><br>When you select the last cell of a row, pressing <kbd>**→**</kbd>* (or <kbd>**Tab**</kbd>**) takes you to the first cell of the row below. |
   *
   * \* The exact key depends on your [`layoutDirection`](#layoutdirection) configuration.<br>
   * \*\* Unless [`tabNavigation`](#tabnavigation) is set to `false`.
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // when you select the first cell of a row, pressing ⬅ (or Shift+Tab) doesn't do anything
   * // when you select the last cell of a row, pressing ➡ (or Tab) doesn't do anything
   * autoWrapRow: false, // default setting
   *
   * // when you select the first cell of a row, pressing ⬅ (or Shift+Tab) takes you to the last cell of the row above
   * // when you select the last cell of a row, pressing ➡ (or Tab) takes you to the first cell of the row below
   * autoWrapRow: true,
   * ```
   */
  autoWrapRow: !1,
  /**
   * @description
   * The `bindRowsWithHeaders` option configures the [`BindRowsWithHeaders`](@/api/bindRowsWithHeaders.md) plugin.
   *
   * You can set the `bindRowsWithHeaders` option to one of the following:
   *
   * | Setting | Description                                                                  |
   * | ------- | ---------------------------------------------------------------------------- |
   * | `false` | Disable the the [`BindRowsWithHeaders`](@/api/bindRowsWithHeaders.md) plugin |
   * | `true`  | Enable the the [`BindRowsWithHeaders`](@/api/bindRowsWithHeaders.md) plugin  |
   *
   * Read more:
   * - [Plugins: `BindRowsWithHeaders`](@/api/bindRowsWithHeaders.md)
   *
   * @memberof Options#
   * @type {boolean|string}
   * @default undefined
   * @category BindRowsWithHeaders
   *
   * @example
   * ```js
   * // enable the `BindRowsWithHeaders` plugin
   * bindRowsWithHeaders: true
   * ```
   */
  bindRowsWithHeaders: void 0,
  /**
   * The `cell` option lets you apply [configuration options](@/guides/getting-started/configuration-options/configuration-options.md) to individual cells.
   *
   * The `cell` option overwrites the [top-level grid options](@/guides/getting-started/configuration-options/configuration-options.md#set-grid-options),
   * and the [`columns`](#columns) options.
   *
   * Read more:
   * - [Configuration options: Setting cell options](@/guides/getting-started/configuration-options/configuration-options.md#set-cell-options)
   * - [`columns`](#columns)
   *
   * @memberof Options#
   * @type {Array[]}
   * @default []
   * @category Core
   *
   * @example
   * ```js
   * // set the `cell` option to an array of objects
   * cell: [
   *   // make the cell with coordinates (0, 0) read-only
   *   {
   *     row: 0,
   *     col: 0,
   *     readOnly: true
   *   }
   * ],
   * ```
   */
  cell: [],
  /**
   * @description
   * The `cells` option lets you apply any other [configuration options](@/guides/getting-started/configuration-options/configuration-options.md) to
   * individual grid elements (columns, rows, cells), based on any logic you implement.
   *
   * The `cells` option overwrites all other options (including options set by [`columns`](#columns) and [`cell`](#cell)).
   * It takes the following parameters:
   *
   * | Parameter | Required | Type             | Description                                                                                                                                                                                                                                                                                                                             |
   * | --------- | -------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `row`     | Yes      | Number           | A physical row index                                                                                                                                                                                                                                                                                                                    |
   * | `column`  | Yes      | Number           | A physical column index                                                                                                                                                                                                                                                                                                                 |
   * | `prop`    | No       | String \| Number | If [`data`](#data) is set to an [array of arrays](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-arrays), `prop` is the same number as `column`.<br><br>If [`data`](#data) is set to an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects), `prop` is a property name for the column's data object. |
   *
   * Read more:
   * - [Configuration options: Implementing custom logic](@/guides/getting-started/configuration-options/configuration-options.md#implement-custom-logic)
   * - [Configuration options: Setting row options](@/guides/getting-started/configuration-options/configuration-options.md#set-row-options)
   * - [`columns`](#columns)
   * - [`cell`](#cell)
   *
   * @memberof Options#
   * @type {Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // set the `cells` option to your custom function
   * cells(row, column, prop) {
   *   const cellProperties = { readOnly: false };
   *   const visualRowIndex = this.instance.toVisualRow(row);
   *   const visualColIndex = this.instance.toVisualColumn(column);
   *
   *   if (visualRowIndex === 0 && visualColIndex === 0) {
   *     cellProperties.readOnly = true;
   *   }
   *
   *   return cellProperties;
   * },
   * ```
   */
  cells: void 0,
  /**
   * The `checkedTemplate` option lets you configure what value
   * a checked [`checkbox`](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md) cell has.
   *
   * You can set the `checkedTemplate` option to one of the following:
   *
   * | Setting          | Description                                                                                                                                                                              |
   * | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `true` (default) | If a [`checkbox`](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md) cell is checked,<br>the [`getDataAtCell`](@/api/core.md#getDataAtCell) method for this cell returns `true`                  |
   * | A string         | If a [`checkbox`](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md) cell is checked,<br>the [`getDataAtCell`](@/api/core.md#getDataAtCell) method for this cell returns a string of your choice |
   *
   * Read more:
   * - [Checkbox cell type: Checkbox template](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md#checkbox-template)
   * - [`getDataAtCell()`](@/api/core.md#getDataAtCell)
   * - [`uncheckedTemplate`](#uncheckedTemplate)
   *
   * @memberof Options#
   * @type {boolean|string|number}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     // set the `type` of each cell in this column to `checkbox`
   *     // when checked, the cell's value is `true`
   *     // when unchecked, the cell's value is `false`
   *     type: 'checkbox',
   *   },
   *   {
   *     // set the `type` of each cell in this column to `checkbox`
   *     type: 'checkbox',
   *     // when checked, the cell's value is `'Yes'`
   *     checkedTemplate: 'Yes',
   *     // when unchecked, the cell's value is `'No'`
   *     uncheckedTemplate: 'No'
   *  }
   * ],
   * ```
   */
  checkedTemplate: void 0,
  /**
   * The `className` option lets you add CSS class names to every currently-selected element.
   *
   * You can set the `className` option to one of the following:
   *
   * | Setting             | Description                                                      |
   * | ------------------- | ---------------------------------------------------------------- |
   * | A string            | Add a single CSS class name to every currently-selected element  |
   * | An array of strings | Add multiple CSS class names to every currently-selected element |
   *
   * ::: tip
   * Don't change the `className` metadata of the [column summary](@/guides/columns/column-summary/column-summary.md) row.
   * To style the summary row, use the class name assigned automatically by the [`ColumnSummary`](@/api/columnSummary.md) plugin: `columnSummaryResult`.
   * :::
   *
   * To apply different CSS class names on different levels, use Handsontable's [cascading configuration](@/guides/getting-started/configuration-options/configuration-options.md#cascading-configuration).
   *
   * Read more:
   * - [Configuration options: Cascading configuration](@/guides/getting-started/configuration-options/configuration-options.md#cascading-configuration)
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentColClassName`](#currentColClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`activeHeaderClassName`](#activeHeaderClassName)
   * - [`invalidCellClassName`](#invalidCellClassName)
   * - [`placeholderCellClassName`](#placeholderCellClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`TableClassName`](#TableClassName)
   *
   * @memberof Options#
   * @type {string|string[]}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // add a `your-class-name` CSS class name
   * // to every currently-selected element
   * className: 'your-class-name',
   *
   * // add `first-class-name` and `second-class-name` CSS class names
   * // to every currently-selected element
   * className: ['first-class-name', 'second-class-name'],
   * ```
   */
  className: void 0,
  /**
   * The `colHeaders` option configures your grid's column headers.
   *
   * You can set the `colHeaders` option to one of the following:
   *
   * | Setting  | Description                                                          |
   * | -------- | -------------------------------------------------------------------- |
   * | `true`   | Enable the default column headers ('A', 'B', 'C', ...)               |
   * | `false`  | Disable column headers                                               |
   * | An array | Define your own column headers (e.g. `['One', 'Two', 'Three', ...]`) |
   * | A function | Define your own column headers, using a function                     |
   *
   * Read more:
   * - [Column header](@/guides/columns/column-header/column-header.md)
   *
   * @memberof Options#
   * @type {boolean|string[]|Function}
   * @default null
   * @category Core
   *
   * @example
   * ```js
   * // enable the default column headers
   * colHeaders: true,
   *
   * // set your own column headers
   * colHeaders: ['One', 'Two', 'Three'],
   *
   * // set your own column headers, using a function
   * colHeaders: function(visualColumnIndex) {
   *   return `${visualColumnIndex} + : AB`;
   * },
   * ```
   */
  colHeaders: null,
  /**
   * @description
   * The `collapsibleColumns` option configures the [`CollapsibleColumns`](@/api/collapsibleColumns.md) plugin.
   *
   * You can set the `collapsibleColumns` option to one of the following:
   *
   * | Setting              | Description                                                                                       |
   * | -------------------- | ------------------------------------------------------------------------------------------------- |
   * | `false`              | Disable the [`CollapsibleColumns`](@/api/collapsibleColumns.md) plugin                            |
   * | `true`               | Enable the [`CollapsibleColumns`](@/api/collapsibleColumns.md) plugin                             |
   * | An array of objects  | Enable the [`CollapsibleColumns`](@/api/collapsibleColumns.md) plugin for selected column headers |
   *
   * Read more:
   * - [Plugins: `CollapsibleColumns`](@/api/collapsibleColumns.md)
   *
   * @memberof Options#
   * @type {boolean|object[]}
   * @default undefined
   * @category CollapsibleColumns
   *
   * @example
   * ```js
   * // enable column collapsing for all headers
   * collapsibleColumns: true,
   *
   * // enable column collapsing for selected headers
   * collapsibleColumns: [
   *   {row: -4, col: 1, collapsible: true},
   *   {row: -3, col: 5, collapsible: true}
   * ],
   * ```
   */
  collapsibleColumns: void 0,
  /**
   * @description
   * The `columnHeaderHeight` option configures the height of column headers.
   *
   * You can set the `columnHeaderHeight` option to one of the following:
   *
   * | Setting  | Description                                         |
   * | -------- | --------------------------------------------------- |
   * | A number | Set the same height for every column header         |
   * | An array | Set different heights for individual column headers |
   *
   * @memberof Options#
   * @type {number|number[]}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // set the same height for every column header
   * columnHeaderHeight: 25,
   *
   * // set different heights for individual column headers
   * columnHeaderHeight: [25, 30, 55],
   * ```
   */
  columnHeaderHeight: void 0,
  /**
   * @description
   * The `columns` option lets you apply any other [configuration options](@/guides/getting-started/configuration-options/configuration-options.md) to individual columns (or ranges of columns).
   *
   * You can set the `columns` option to one of the following:
   * - An array of objects (each object represents one column)
   * - A function that returns an array of objects
   *
   * The `columns` option overwrites the [top-level grid options](@/guides/getting-started/configuration-options/configuration-options.md#set-grid-options).
   *
   * When you use `columns`, the [`startCols`](#startCols), [`minCols`](#minCols), and [`maxCols`](#maxCols) options are ignored.
   *
   * Read more:
   * - [Configuration options: Setting column options](@/guides/getting-started/configuration-options/configuration-options.md#set-column-options)
   * - [`startCols`](#startCols)
   * - [`minCols`](#minCols)
   * - [`maxCols`](#maxCols)
   * - [`data`](#data)
   *
   * @memberof Options#
   * @type {object[]|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // set the `columns` option to an array of objects
   * // each object represents one column
   * columns: [
   *   {
   *     // column options for the first (by physical index) column
   *     type: 'numeric',
   *     numericFormat: {
   *       pattern: '0,0.00 $'
   *     }
   *   },
   *   {
   *     // column options for the second (by physical index) column
   *     type: 'text',
   *     readOnly: true
   *   }
   * ],
   *
   * // or set the `columns` option to a function, based on physical indexes
   * columns(index) {
   *   return {
   *     type: index > 0 ? 'numeric' : 'text',
   *     readOnly: index < 1
   *   }
   * }
   * ```
   */
  columns: void 0,
  /**
   * @description
   * The `columnSorting` option configures the [`ColumnSorting`](@/api/columnSorting.md) plugin.
   *
   * You can set the `columnSorting` option to one of the following:
   *
   * | Setting    | Description                                                                                                                            |
   * | ---------- | -------------------------------------------------------------------------------------------------------------------------------------- |
   * | `true`     | Enable the [`ColumnSorting`](@/api/columnSorting.md) plugin with the default configuration                                             |
   * | `false`    | Disable the [`ColumnSorting`](@/api/columnSorting.md) plugin                                                                           |
   * | An object  | - Enable the [`ColumnSorting`](@/api/columnSorting.md) plugin<br>- Modify the [`ColumnSorting`](@/api/columnSorting.md) plugin options |
   *
   * If you set the `columnSorting` option to an object,
   * you can set the following [`ColumnSorting`](@/api/columnSorting.md) plugin options:
   *
   * | Option                   | Possible settings                                                                                                                                |
   * | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
   * | `indicator`              | `true`: Display the arrow icon in the column header, to indicate a sortable column<br>`false`: Don't display the arrow icon in the column header  |
   * | `headerAction`           | `true`: Enable clicking on the column header to sort the column<br>`false`: Disable clicking on the column header to sort the column             |
   * | `sortEmptyCells`         | `true`: Sort empty cells as well<br>`false`: Place empty cells at the end                                                                        |
   * | `compareFunctionFactory` | A [custom compare function](@/guides/rows/rows-sorting/rows-sorting.md#add-a-custom-comparator)                                                                |
   *
   * If you set the `columnSorting` option to an object,
   * you can also sort individual columns at Handsontable's initialization.
   * In the `columnSorting` object, add an object named `initialConfig`,
   * with the following properties:
   *
   * | Option      | Possible settings   | Description                                                      |
   * | ----------- | ------------------- | ---------------------------------------------------------------- |
   * | `column`    | A number            | The index of the column that you want to sort at initialization  |
   * | `sortOrder` | `'asc'` \| `'desc'` | The sorting order:<br>`'asc'`: ascending<br>`'desc'`: descending |
   *
   * Read more:
   * - [Rows sorting](@/guides/rows/rows-sorting/rows-sorting.md)
   * - [Rows sorting: Custom compare functions](@/guides/rows/rows-sorting/rows-sorting.md#add-a-custom-comparator)
   * - [`multiColumnSorting`](#multiColumnSorting)
   *
   * @memberof Options#
   * @type {boolean|object}
   * @default undefined
   * @category ColumnSorting
   *
   * @example
   * ```js
   * // enable the `ColumnSorting` plugin
   * columnSorting: true
   *
   * // enable the `ColumnSorting` plugin with custom configuration
   * columnSorting: {
   *   // sort empty cells as well
   *   sortEmptyCells: true,
   *   // display the arrow icon in the column header
   *   indicator: true,
   *   // disable clicking on the column header to sort the column
   *   headerAction: false,
   *   // add a custom compare function
   *   compareFunctionFactory(sortOrder, columnMeta) {
   *     return function(value, nextValue) {
   *       // some value comparisons which will return -1, 0 or 1...
   *     }
   *   }
   * }
   *
   * // enable the `ColumnSorting` plugin
   * columnSorting: {
   *   // at initialization, sort column 1 in ascending order
   *   initialConfig: {
   *     column: 1,
   *     sortOrder: 'asc'
   *   },
   *   // at initialization, sort column 2 in descending order
   *   initialConfig: {
   *     column: 2,
   *     sortOrder: 'desc'
   *   }
   * }
   * ```
   */
  columnSorting: void 0,
  /**
   * @description
   * The `columnSummary` option configures the [`ColumnSummary`](@/api/columnSummary.md) plugin.
   *
   * You can set the `columnSummary` option to an array of objects.
   * Each object configures a single column summary, using the following properties:
   *
   * | Property                 | Possible values                                                         | Description                                                                                                                  |
   * | ------------------------ | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
   * | `sourceColumn`           | A number                                                                | [Column to summarize](@/guides/columns/column-summary/column-summary.md#step-2-select-cells-that-you-want-to-summarize)                     |
   * | `ranges`                 | An array                                                                | [Ranges of rows to summarize](@/guides/columns/column-summary/column-summary.md#step-2-select-cells-that-you-want-to-summarize)             |
   * | `type`                   | `'sum'` \| `'min'` \| `'max'` \| `'count'` \| `'average'` \| `'custom'` | [Summary function](@/guides/columns/column-summary/column-summary.md#step-3-calculate-your-summary)                                         |
   * | `destinationRow`         | A number                                                                | [Destination cell's row coordinate](@/guides/columns/column-summary/column-summary.md#step-4-provide-the-destination-cell-s-coordinates)    |
   * | `destinationColumn`      | A number                                                                | [Destination cell's column coordinate](@/guides/columns/column-summary/column-summary.md#step-4-provide-the-destination-cell-s-coordinates) |
   * | `forceNumeric`           | `true`  \| `false`                                                      | [Treat non-numerics as numerics](@/guides/columns/column-summary/column-summary.md#force-numeric-values)                                  |
   * | `reversedRowCoords`      | `true`  \| `false`                                                      | [Reverse row coordinates](@/guides/columns/column-summary/column-summary.md#step-5-make-room-for-the-destination-cell)                      |
   * | `suppressDataTypeErrors` | `true`  \| `false`                                                      | [Suppress data type errors](@/guides/columns/column-summary/column-summary.md#throw-data-type-errors)                                    |
   * | `readOnly`               | `true`  \| `false`                                                      | Make summary cell read-only                                                                                                  |
   * | `roundFloat`             | `true`  \| `false`  \| A number                                         | [Round summary result](@/guides/columns/column-summary/column-summary.md#round-a-column-summary-result)                                  |
   * | `customFunction`         | A function                                                              | [Custom summary function](@/guides/columns/column-summary/column-summary.md#implement-a-custom-summary-function)                         |
   *
   * Read more:
   * - [Column summary](@/guides/columns/column-summary/column-summary.md)
   * - [Plugins: `ColumnSummary`](@/api/columnSummary.md)
   *
   * @memberof Options#
   * @type {object[]|Function}
   * @default undefined
   * @category ColumnSummary
   *
   * @example
   * ```js
   * columnSummary: [
   *   {
   *     sourceColumn: 0,
   *     ranges: [
   *       [0, 2], [4], [6, 8]
   *     ],
   *     type: 'custom',
   *     destinationRow: 4,
   *     destinationColumn: 1,
   *     forceNumeric: true,
   *     reversedRowCoords: true,
   *     suppressDataTypeErrors: false,
   *     readOnly: true,
   *     roundFloat: false,
   *     customFunction(endpoint) {
   *        return 100;
   *     }
   *   }
   * ],
   * ```
   */
  columnSummary: void 0,
  /**
   * The `colWidths` option sets columns' widths, in pixels.
   *
   * The default column width is 50px. To change it, set the `colWidths` option to one of the following:
   *
   * | Setting     | Description                                                                                          | Example                                                           |
   * | ----------- | ---------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
   * | A number    | Set the same width for every column                                                                  | `colWidths: 100`                                                  |
   * | A string    | Set the same width for every column                                                                  | `colWidths: '100px'`                                              |
   * | An array    | Set widths separately for each column                                                                | `colWidths: [100, 120, undefined]`                                |
   * | A function  | Set column widths dynamically,<br>on each render                                                     | `colWidths(visualColumnIndex) { return visualColumnIndex * 10; }` |
   * | `undefined` | Used by the [modifyColWidth](@/api/hooks.md#modifyColWidth) hook,<br>to detect column width changes. | `colWidths: undefined`                                            |
   *
   * Setting `colWidths` even for a single column disables the {@link AutoColumnSize} plugin
   * for all columns. For this reason, if you use `colWidths`, we recommend you set a width for each one
   * of your columns. Otherwise, every column with an undefined width defaults back to 50px,
   * which may cut longer columns names.
   *
   * Read more:
   * - [Column width](@/guides/columns/column-width/column-width.md)
   * - [Hooks: `modifyColWidth`](@/api/hooks.md#modifyColWidth)
   * - [`autoColumnSize`](#autoColumnSize)
   *
   * @memberof Options#
   * @type {number|number[]|string|string[]|Array<undefined>|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // set every column's width to 100px
   * colWidths: 100,
   *
   * // set every column's width to 100px
   * colWidths: '100px',
   *
   * // set the first (by visual index) column's width to 100
   * // set the second (by visual index) column's width to 120
   * // set the third (by visual index) column's width to `undefined`, so that it defaults to 50px
   * // set any other column's width to the default 50px (note that longer cell values and column names can get cut)
   * colWidths: [100, 120, undefined],
   *
   * // set each column's width individually, using a function
   * colWidths(visualColumnIndex) {
   *   return visualColumnIndex * 10;
   * },
   * ```
   */
  colWidths: void 0,
  /**
   * The `commentedCellClassName` option lets you add a CSS class name to cells
   * that have comments.
   *
   * Read more:
   * - [Comments](@/guides/cell-features/comments/comments.md)
   * - [`comments`](#comments)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`activeHeaderClassName`](#activeHeaderClassName)
   * - [`invalidCellClassName`](#invalidCellClassName)
   * - [`placeholderCellClassName`](#placeholderCellClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`TableClassName`](#TableClassName)
   * - [`className`](#className)
   *
   * @memberof Options#
   * @type {string}
   * @default 'htCommentCell'
   * @category Core
   *
   * @example
   * ```js
   * // add a `has-comment` CSS class name
   * // to each cell that has a comment
   * commentedCellClassName: 'has-comment',
   * ```
   */
  commentedCellClassName: "htCommentCell",
  /**
   * @description
   * The `comments` option configures the [`Comments`](@/api/comments.md) plugin.
   *
   * You can set the `comments` option to one of the following:
   *
   * | Setting   | Description                                                                                                                                                                           |
   * | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `true`    | - Enable the [`Comments`](@/api/comments.md) plugin<br>- Add comment menu items to the [context menu](@/guides/accessories-and-menus/context-menu/context-menu.md)                                 |
   * | `false`   | Disable the [`Comments`](@/api/comments.md) plugin                                                                                                                                    |
   * | An object | - Enable the [`Comments`](@/api/comments.md) plugin<br>- Add comment menu items to the [context menu](@/guides/accessories-and-menus/context-menu/context-menu.md)<br>- Configure comment settings |
   *
   * If you set the `comments` option to an object, you can configure the following comment options:
   *
   * | Option         | Possible settings           | Description                                         |
   * | -------------- | --------------------------- | --------------------------------------------------- |
   * | `displayDelay` | A number (default: `250`)   | Display comments after a delay (in milliseconds)    |
   * | `readOnly`     | `true` \| `false` (default) | `true`: Make comments read-only                     |
   * | `style`        | An object                   | Set comment boxes' `width` and `height` (in pixels) |
   *
   * Read more:
   * - [Comments](@/guides/cell-features/comments/comments.md)
   * - [Context menu](@/guides/accessories-and-menus/context-menu/context-menu.md)
   * - [`width`](#width)
   * - [`height`](#height)
   * - [`readOnly`](#readOnly)
   * - [`commentedCellClassName`](#commentedCellClassName)
   *
   * @memberof Options#
   * @type {boolean|object[]}
   * @default false
   * @category Comments
   *
   * @example
   * ```js
   * // enable the `Comments` plugin
   * comments: true,
   *
   * // enable the `Comments` plugin
   * // and configure its settings
   * comments: {
   *   // display all comments with a 1-second delay
   *   displayDelay: 1000,
   *   // make all comments read-only
   *   readOnly: true,
   *   // set the default size of all comment boxes
   *   style: {
   *     width: 300,
   *     height: 100
   *   }
   * }
   * ```
   */
  comments: !1,
  /**
   * @description
   * The `contextMenu` option configures the [`ContextMenu`](@/api/contextMenu.md) plugin.
   *
   * You can set the `contextMenu` option to one of the following:
   *
   * | Setting   | Description                                                                                                                                                                                             |
   * | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `false`   | Disable the [`ContextMenu`](@/api/contextMenu.md) plugin                                                                                                                                                |
   * | `true`    | - Enable the [`ContextMenu`](@/api/contextMenu.md) plugin<br>- Use the [default context menu options](@/guides/accessories-and-menus/context-menu/context-menu.md#context-menu-with-default-options)                 |
   * | An array  | - Enable the [`ContextMenu`](@/api/contextMenu.md) plugin<br>- Modify [individual context menu options](@/guides/accessories-and-menus/context-menu/context-menu.md#context-menu-with-specific-options)              |
   * | An object | - Enable the [`ContextMenu`](@/api/contextMenu.md) plugin<br>- Apply a [custom context menu configuration](@/guides/accessories-and-menus/context-menu/context-menu.md#context-menu-with-a-fully-custom-configuration) |
   *
   * Read more:
   * - [Context menu](@/guides/accessories-and-menus/context-menu/context-menu.md)
   * - [Context menu: Context menu with default options](@/guides/accessories-and-menus/context-menu/context-menu.md#context-menu-with-default-options)
   * - [Context menu: Context menu with specific options](@/guides/accessories-and-menus/context-menu/context-menu.md#context-menu-with-specific-options)
   * - [Context menu: Context menu with fully custom configuration options](@/guides/accessories-and-menus/context-menu/context-menu.md#context-menu-with-a-fully-custom-configuration)
   * - [Plugins: `ContextMenu`](@/api/contextMenu.md)
   *
   * @memberof Options#
   * @type {boolean|string[]|object}
   * @default undefined
   * @category ContextMenu
   *
   * @example
   * ```js
   * // enable the `ContextMenu` plugin
   * // use the default context menu options
   * contextMenu: true,
   *
   * // enable the `ContextMenu` plugin
   * // and modify individual context menu options
   * contextMenu: ['row_above', 'row_below', '---------', 'undo', 'redo'],
   *
   * // enable the `ContextMenu` plugin
   * // and apply a custom context menu configuration
   * contextMenu: {
   *   items: {
   *     'option1': {
   *       name: 'Option 1'
   *     },
   *     'option2': {
   *       name: 'Option 2',
   *       submenu: {
   *         items: [
   *           {
   *             key: 'option2:suboption1',
   *             name: 'Suboption 1',
   *             callback: function(key, options) {
   *               ...
   *             }
   *           },
   *           ...
   *         ]
   *       }
   *     }
   *   }
   * },
   * ```
   */
  contextMenu: void 0,
  /**
   * @description
   * The `copyable` option determines whether a cell's value can be copied to the clipboard or not.
   *
   * You can set the `copyable` option to one of the following:
   *
   * | Setting                                                                                                        | Description                                                                                                            |
   * | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
   * | `true` (default)                                                                                               | - On pressing <kbd>**Ctrl**</kbd>/<kbd>**Cmd**</kbd>+<kbd>**C**</kbd>, add the cell's value to the clipboard         |
   * | `false`<br>(default for the [`password`](@/guides/cell-types/password-cell-type/password-cell-type.md) [cell type](#type))        | - On pressing <kbd>**Ctrl**</kbd>/<kbd>**Cmd**</kbd>+<kbd>**C**</kbd>, add an empty string (`""`) to the clipboard   |
   *
   * Read more:
   * - [Clipboard](@/guides/cell-features/clipboard/clipboard.md)
   * - [Configuration options: Cascading configuration](@/guides/getting-started/configuration-options/configuration-options.md#cascading-configuration)
   * - [Password cell type](@/guides/cell-types/password-cell-type/password-cell-type.md)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // enable copying for each cell of the entire grid
   * copyable: true,
   *
   * // enable copying for individual columns
   * columns: [
   *   {
   *     // enable copying for each cell of this column
   *     copyable: true
   *   },
   *   {
   *     // disable copying for each cell of this column
   *     copyable: false
   *   }
   * ]
   *
   * // enable copying for specific cells
   * cell: [
   *   {
   *     col: 0,
   *     row: 0,
   *     // disable copying for cell (0, 0)
   *     copyable: false,
   *   }
   * ],
   * ```
   */
  copyable: !0,
  /**
   * The `copyPaste` option configures the [`CopyPaste`](@/api/copyPaste.md) plugin.
   *
   * You can set the `copyPaste` option to one of the following:
   *
   * | Setting           | Description                                                                                                            |
   * | ----------------- | ---------------------------------------------------------------------------------------------------------------------- |
   * | `true` (default)  | Enable the [`CopyPaste`](@/api/copyPaste.md) plugin with the default configuration                                     |
   * | `false`           | Disable the [`CopyPaste`](@/api/copyPaste.md) plugin                                                                   |
   * | An object         | - Enable the [`CopyPaste`](@/api/copyPaste.md) plugin<br>- Modify the [`CopyPaste`](@/api/copyPaste.md) plugin options |
   *
   * ##### copyPaste: Additional options
   *
   * If you set the `copyPaste` option to an object, you can set the following `CopyPaste` plugin options:
   *
   * | Option                   | Possible settings                                  | Description                                                                                                                                                                                         |
   * | ------------------------ | -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `columnsLimit`           | A number (default: `Infinity`)                     | The maximum number of columns that can be copied                                                                                                                                                    |
   * | `rowsLimit`              | A number (default: `Infinity`)                     | The maximum number of columns that can be copied                                                                                                                                                    |
   * | `pasteMode`              | `'overwrite'` \| `'shift_down'` \| `'shift_right'` | When pasting:<br>`'overwrite'`: overwrite the currently-selected cells<br>`'shift_down'`: move the currently-selected cells down<br>`'shift_right'`: move the currently-selected cells to the right |
   * | `copyColumnHeaders`      | Boolean (default: `false`)                         | `true`: add a context menu option for copying cells along with their nearest column headers                                                                                                         |
   * | `copyColumnGroupHeaders` | Boolean (default: `false`)                         | `true`: add a context menu option for copying cells along with all their related columns headers                                                                                                    |
   * | `copyColumnHeadersOnly`  | Boolean (default: `false`)                         | `true`: add a context menu option for copying column headers nearest to the selected cells (without copying cells)                                                                    |
   * | `uiContainer`            | An HTML element                                    | The UI container for the secondary focusable element                                                                                                                                                |
   *
   * Read more:
   * - [Plugins: `CopyPaste`](@/api/copyPaste.md)
   * - [Guides: Clipboard](@/guides/cell-features/clipboard/clipboard.md)
   *
   * @memberof Options#
   * @type {object|boolean}
   * @default true
   * @category CopyPaste
   *
   * @example
   * ```js
   * // enable the plugin with the default configuration
   * copyPaste: true // set by default
   *
   * // disable the plugin
   * copyPaste: false,
   *
   * // enable the plugin with a custom configuration
   * copyPaste: {
   *   // set a maximum number of columns that can be copied
   *   columnsLimit: 25,
   *
   *   // set a maximum number of rows that can be copied
   *   rowsLimit: 50,
   *
   *   // set the paste behavior
   *   pasteMode: 'shift_down',
   *
   *   // add the option to copy cells along with their nearest column headers
   *   copyColumnHeaders: true,
   *
   *   // add the option to copy cells along with all their related columns headers
   *   copyColumnGroupHeaders: true,
   *
   *   // add the option to copy just column headers (without copying cells)
   *   copyColumnHeadersOnly: true,
   *
   *   // set a UI container
   *   uiContainer: document.body,
   * },
   * ```
   */
  copyPaste: !0,
  /**
   * The `correctFormat` option configures whether incorrectly-formatted times and dates are amended or not.
   *
   * When the user enters dates and times, Handsontable can automatically adjust their format
   * to match the [`dateFormat`](#dateFormat) and [`timeFormat`](@/guides/cell-types/time-cell-type/time-cell-type.md) settings.
   *
   * You can set the `correctFormat` option to one of the following:
   *
   * | Setting           | Description                                                                                                                                               |
   * | ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `false` (default) | Don't correct the format of the entered date or time (treat the entered date or time as invalid)                                                          |
   * | `true`            | Correct the format of the entered date or time to match the [`dateFormat`](#dateFormat) or [`timeFormat`](@/guides/cell-types/time-cell-type/time-cell-type.md) settings |
   *
   * Read more:
   * - [Date cell type](@/guides/cell-types/date-cell-type/date-cell-type.md)
   * - [Time cell type](@/guides/cell-types/time-cell-type/time-cell-type.md)
   * - [`dateFormat`](#dateFormat)
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *   // set the `type` of each cell in this column to `date`
   *   type: 'date',
   *   // for every `date` cell of this column, set the date format to `YYYY-MM-DD`
   *   dateFormat: 'YYYY-MM-DD',
   *   // enforce the `YYYY-MM-DD` date format
   *   correctFormat: true
   *   },
   *
   *   {
   *   // set the `type` of each cell in this column to `time`
   *   type: 'time',
   *   // for every `time` cell of this column, set the time format to `h:mm:ss a`
   *   timeFormat: 'h:mm:ss a',
   *   // enforce the `h:mm:ss a` time format
   *   correctFormat: true
   *   },
   * ],
   * ```
   */
  correctFormat: !1,
  /**
   * The `currentColClassName` option lets you add a CSS class name
   * to each cell of the currently-visible, currently-selected columns.
   *
   * Read more:
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`activeHeaderClassName`](#activeHeaderClassName)
   * - [`invalidCellClassName`](#invalidCellClassName)
   * - [`placeholderCellClassName`](#placeholderCellClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`TableClassName`](#TableClassName)
   * - [`className`](#className)
   *
   * @memberof Options#
   * @type {string}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // add a `your-class-name` CSS class name
   * // to each cell of the currently-visible, currently-selected columns
   * currentColClassName: 'your-class-name',
   * ```
   */
  currentColClassName: void 0,
  /**
   * The `currentHeaderClassName` option lets you add a CSS class name
   * to every currently-visible, currently-selected header.
   *
   * Read more:
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentColClassName`](#currentColClassName)
   * - [`activeHeaderClassName`](#activeHeaderClassName)
   * - [`invalidCellClassName`](#invalidCellClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`TableClassName`](#TableClassName)
   * - [`className`](#className)
   *
   * @memberof Options#
   * @type {string}
   * @default 'ht__highlight'
   * @category Core
   *
   * @example
   * ```js
   * // add an `ht__highlight` CSS class name
   * // to every currently-visible, currently-selected header
   * currentHeaderClassName: 'ht__highlight',
   * ```
   */
  currentHeaderClassName: "ht__highlight",
  /**
   * The `currentRowClassName` option lets you add a CSS class name
   * to each cell of the currently-visible, currently-selected rows.
   *
   * Read more:
   * - [`currentColClassName`](#currentColClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`activeHeaderClassName`](#activeHeaderClassName)
   * - [`invalidCellClassName`](#invalidCellClassName)
   * - [`placeholderCellClassName`](#placeholderCellClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`TableClassName`](#TableClassName)
   * - [`className`](#className)
   *
   * @memberof Options#
   * @type {string}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // add a `your-class-name` CSS class name
   * // to each cell of the currently-visible, currently-selected rows
   * currentRowClassName: 'your-class-name',
   * ```
   */
  currentRowClassName: void 0,
  /**
   * @description
   * The `customBorders` option configures the [`CustomBorders`](@/api/customBorders.md) plugin.
   *
   * To enable the [`CustomBorders`](@/api/customBorders.md) plugin
   * (and add its menu items to the [context menu](@/guides/accessories-and-menus/context-menu/context-menu.md)),
   * set the `customBorders` option to `true`.
   *
   * To enable the [`CustomBorders`](@/api/customBorders.md) plugin
   * and add a predefined border around a particular cell,
   * set the `customBorders` option to an array of objects.
   * Each object represents a border configuration for one cell, and has the following properties:
   *
   * | Property | Sub-properties     | Types                              | Description                                                       |
   * | -------- | ------------------ | ---------------------------------- | ----------------------------------------------------------------- |
   * | `row`    | -                  | `row`: Number                      | The cell's row coordinate.                                        |
   * | `col`    | -                  | `col`: Number                      | The cell's column coordinate.                                     |
   * | `start`  | `width`<br>`color` | `width`: Number<br>`color`: String | If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is LTR (default): `start` sets the width (`width`) and color (`color`) of the left-hand border.<br><br>If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is RTL: `start` sets the width (`width`) and color (`color`) of the right-hand border. |
   * | `end`    | `width`<br>`color` | `width`: Number<br>`color`: String | If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is LTR (default): `end` sets the width (`width`) and color (`color`) of the right-hand border.<br><br>If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is RTL: `end` sets the width (`width`) and color (`color`) of the left-hand border. |
   * | `top`    | `width`<br>`color` | `width`: Number<br>`color`: String | Sets the width (`width`) and color (`color`) of the top border. |
   * | `bottom` | `width`<br>`color` | `width`: Number<br>`color`: String | Sets the width (`width`) and color (`color`) of the bottom border. |
   *
   * To enable the [`CustomBorders`](@/api/customBorders.md) plugin
   * and add a predefined border around a range of cells,
   * set the `customBorders` option to an array of objects.
   * Each object represents a border configuration for a single range of cells, and has the following properties:
   *
   * | Property | Sub-properties                               | Types                                                            | Description                                                                                  |
   * | -------- | -------------------------------------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
   * | `range`  | `from` {`row`, `col`}<br>`to` {`row`, `col`} | `from`: Object<br>`to`: Object<br>`row`: Number<br>`col`: Number | If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is LTR (default):<br>- `from` selects the range's top-left corner.<br>- `to` selects the range's bottom-right corner.<br><br>If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is RTL: <br>- `from` selects the range's top-right corner.<br>- `to` selects the range's bottom-left corner. |
   * | `start`  | `width`<br>`color` | `width`: Number<br>`color`: String | If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is LTR (default): `start` sets the width (`width`) and color (`color`) of the left-hand border.<br><br>If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is RTL: `start` sets the width (`width`) and color (`color`) of the right-hand border. |
   * | `end`    | `width`<br>`color` | `width`: Number<br>`color`: String | If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is LTR (default): `end` sets the width (`width`) and color (`color`) of the right-hand border.<br><br>If the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is RTL: `end` sets the width (`width`) and color (`color`) of the left-hand border. |
   * | `top`    | `width`<br>`color`                           | `width`: Number<br>`color`: String                               | Sets the width (`width`) and color (`color`) of the top border. |
   * | `bottom` | `width`<br>`color`                           | `width`: Number<br>`color`: String                               | Sets the width (`width`) and color (`color`) of the bottom border. |
   *
   * Read more:
   * - [Formatting cells: Custom cell borders](@/guides/cell-features/formatting-cells/formatting-cells.md#custom-cell-borders)
   * - [Context menu](@/guides/accessories-and-menus/context-menu/context-menu.md)
   * - [Plugins: `CustomBorders`](@/api/customBorders.md)
   * - [Layout direction](@/guides/internationalization/layout-direction/layout-direction.md)
   * - [`layoutDirection`](#layoutDirection)
   *
   * @memberof Options#
   * @type {boolean|object[]}
   * @default false
   * @category CustomBorders
   *
   * @example
   * ```js
   * // enable the `CustomBorders` plugin
   * customBorders: true,
   *
   * // enable the `CustomBorders` plugin
   * // and add a predefined border for a particular cell
   * customBorders: [
   *   // add an object with a border configuration for one cell
   *   {
   *     // set the cell's row coordinate
   *     row: 2,
   *     // set the cell's column coordinate
   *     col: 2,
   *     // set the left/right border's width and color
   *     start: {
   *       width: 2,
   *       color: 'red'
   *     },
   *     // set the right/left border's width and color
   *     end: {
   *       width: 1,
   *       color: 'green'
   *     },
   *     // set the top border's width and color
   *     top: '',
   *     // set the bottom border's width and color
   *     bottom: ''
   *   }
   * ],
   *
   * // enable the `CustomBorders` plugin
   * // and add a predefined border for a range of cells
   * customBorders: [
   *   // add an object with a border configuration for one range of cells
   *   {
   *     // select a range of cells
   *     range: {
   *       // set the range's top-left corner
   *       from: {
   *         row: 1,
   *         col: 1
   *       },
   *       // set the range's bottom-right corner
   *       to: {
   *         row: 3,
   *         col: 4
   *       }
   *     },
   *     // set the left/right border's width and color
   *     start: {
   *       width: 2,
   *       color: 'red'
   *     },
   *     // set the right/left border's width and color
   *     end: {},
   *     // set the top border's width and color
   *     top: {},
   *     // set the bottom border's width and color
   *     bottom: {}
   *   }
   * ],
   * ```
   */
  customBorders: !1,
  /**
   * @description
   * The `data` option sets the initial [data](@/guides/getting-started/binding-to-data/binding-to-data.md) of your Handsontable instance.
   *
   * Handsontable's data is bound to your source data by reference (i.e. when you edit Handsontable's data, your source data alters as well).
   *
   * You can set the `data` option:
   * - Either to an [array of arrays](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-arrays).
   * - Or to an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects).
   *
   * If you don't set the `data` option (or set it to `null`), Handsontable renders as an empty 5x5 grid by default.
   *
   * Read more:
   * - [Binding to data](@/guides/getting-started/binding-to-data/binding-to-data.md)
   * - [`dataSchema`](#dataSchema)
   * - [`startRows`](#startRows)
   * - [`startCols`](#startCols)
   *
   * @memberof Options#
   * @type {Array[]|object[]}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // as an array of arrays
   * data: [
   *   ['A', 'B', 'C'],
   *   ['D', 'E', 'F'],
   *   ['G', 'H', 'J']
   * ]
   *
   * // as an array of objects
   * data: [
   *   {id: 1, name: 'Ted Right'},
   *   {id: 2, name: 'Frank Honest'},
   *   {id: 3, name: 'Joan Well'},
   *   {id: 4, name: 'Gail Polite'},
   *   {id: 5, name: 'Michael Fair'},
   * ]
   * ```
   */
  data: void 0,
  /**
   * @description
   * If `true`, Handsontable will interpret the dots in the columns mapping as a nested object path. If your dataset contains
   * the dots in the object keys and you don't want Handsontable to interpret them as a nested object path, set this option to `false`.
   *
   * The option only works when defined in the global table settings.
   *
   * @since 14.4.0
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // All dots are interpreted as nested object paths
   * dataDotNotation: true,
   * data: [
   *   { id: 1, name: { first: 'Ted', last: 'Right' }, user: { address: '1234 Any Street' } },
   * ],
   * columns={[
   *   { data: 'name.first' },
   *   { data: 'user.address' },
   * ]},
   * ```
   * ```js
   * // All dots are interpreted as simple object keys
   * dataDotNotation: false,
   * data: [
   *   { id: 1, 'name.first': 'Ted', 'user.address': '1234 Any Street' },
   * ],
   * columns={[
   *   { data: 'name.first' },
   *   { data: 'user.address' },
   * ]},
   * ```
   */
  dataDotNotation: !0,
  /**
   * @description
   * When the [`data`](#data) option is set to an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects)
   * (or is empty), the `dataSchema` option defines the structure of new rows.
   *
   * Using the `dataSchema` option, you can start out with an empty grid.
   *
   * You can set the `dataSchema` option to one of the following:
   * - An object
   * - A function
   *
   * Read more:
   * - [Binding to data: Array of objects with custom data schema](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects-with-custom-data-schema)
   * - [Binding to data: Function data source and schema](@/guides/getting-started/binding-to-data/binding-to-data.md#function-data-source-and-schema)
   * - [`data`](#data)
   *
   * @memberof Options#
   * @type {object|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // with `dataSchema`, you can start with an empty grid
   * data: null,
   * dataSchema: {id: null, name: {first: null, last: null}, address: null},
   * colHeaders: ['ID', 'First Name', 'Last Name', 'Address'],
   * columns: [
   *   {data: 'id'},
   *   {data: 'name.first'},
   *   {data: 'name.last'},
   *   {data: 'address'}
   * ],
   * startRows: 5,
   * minSpareRows: 1
   * ```
   */
  dataSchema: void 0,
  /**
   * The `dateFormat` option configures the date format accepted by [`date`](@/guides/cell-types/date-cell-type/date-cell-type.md) cells.
   *
   * You can set the `dateFormat` option to a string with a proper date format. The default value is: `'DD/MM/YYYY'`.
   *
   * To automatically correct dates whose format doesn't match the `dateFormat` setting, use the [`correctFormat`](#correctFormat) option.
   *
   * Read more:
   * - [Date cell type](@/guides/cell-types/date-cell-type/date-cell-type.md)
   * - [`correctFormat`](#correctFormat)
   * - [`defaultDate`](#defaultDate)
   *
   * @memberof Options#
   * @type {string}
   * @default 'DD/MM/YYYY'
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *   // set the `type` of each cell in this column to `date`
   *   type: 'date',
   *   // for every `date` cell of this column, set the date format to `YYYY-MM-DD`
   *   dateFormat: 'YYYY-MM-DD',
   *   },
   * ],
   * ```
   */
  dateFormat: "DD/MM/YYYY",
  /**
   * The `timeFormat` option configures the time format accepted by [`time`](@/guides/cell-types/time-cell-type/time-cell-type.md) cells.
   *
   * You can set the `timeFormat` option to a string with a proper time format. The default value is: `'h:mm:ss a'`.
   *
   * To automatically correct times whose format doesn't match the `timeFormat` setting, use the [`correctFormat`](#correctFormat) option.
   *
   * Read more:
   * - [Time cell type](@/guides/cell-types/time-cell-type/time-cell-type.md)
   * - [`correctFormat`](#correctFormat)
   *
   * @memberof Options#
   * @type {string}
   * @default 'h:mm:ss a'
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *   // set the `type` of each cell in this column to `time`
   *   type: 'time',
   *   // for every `time` cell of this column, set the time format to `h:mm:ss a`
   *   timeFormat: 'h:mm:ss a',
   *   },
   * ],
   * ```
   */
  timeFormat: "h:mm:ss a",
  /**
   * The `datePickerConfig` option configures the `date` [cell editor](@/guides/cell-functions/cell-editor/cell-editor.md)'s date picker, which uses an external dependency: [Pikaday](https://github.com/Pikaday/Pikaday/tree/1.8.2).
   *
   * You can set the `datePickerConfig` option to an object with any of the available [Pikaday options](https://github.com/Pikaday/Pikaday/tree/1.8.2#configuration),
   * except for the following, which are always overwritten by the `date` [cell editor](@/guides/cell-functions/cell-editor/cell-editor.md):
   * - `bound`
   * - `container`
   * - `field`
   * - `trigger`
   *
   * If the `datePickerConfig` option is not defined, the `date` [cell editor](@/guides/cell-functions/cell-editor/cell-editor.md) overwrites the following [Pikaday options](https://github.com/Pikaday/Pikaday/tree/1.8.2#configuration) as well:
   *
   * | Pikaday option       | Handsontable's setting |
   * | -------------------- | ---------------------- |
   * | `format`             | `'DD/MM/YYYY'`         |
   * | `reposition`         | `false`                |
   *
   * Read more:
   * - [`editor`](#editor)
   * - [`dateFormat`](#dateFormat)
   * - [Cell editor](@/guides/cell-functions/cell-editor/cell-editor.md)
   * - [All Pikaday options &#8594;](https://github.com/Pikaday/Pikaday/tree/1.8.2#configuration)
   *
   * @memberof Options#
   * @type {object}
   * @default undefined
   * @category Core
   */
  datePickerConfig: void 0,
  /**
   * The `defaultDate` option configures the date displayed
   * in empty [`date`](@/guides/cell-types/date-cell-type/date-cell-type.md) cells.
   *
   * You can set the `defaultDate` option to a string.
   *
   * Read more:
   * - [Date cell type](@/guides/cell-types/date-cell-type/date-cell-type.md)
   * - [`dateFormat`](#dateFormat)
   *
   * @memberof Options#
   * @type {string}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     // set the `type` of each cell in this column to `date`
   *     type: 'date',
   *     // in every empty `date` cell of this column, display `2015-02-02`
   *     defaultDate: '2015-02-02'
   *   }
   * ],
   * ```
   */
  defaultDate: void 0,
  /**
   * @description
   * The `disableVisualSelection` option configures how
   * [selection](@/guides/cell-features/selection/selection.md) is shown.
   *
   * You can set the `disableVisualSelection` option to one of the following:
   *
   * | Setting           | Description                                                                                         |
   * | ----------------- | --------------------------------------------------------------------------------------------------- |
   * | `false` (default) | - Show single-cell selection<br>- Show range selection<br>- Show header selection                   |
   * | `true`            | - Don't show single-cell selection<br>- Don't show range selection<br>- Don't show header selection |
   * | `'current'`       | - Don't show single-cell selection<br>- Show range selection<br>- Show header selection             |
   * | `'area'`          | - Show single-cell selection<br>- Don't show range selection<br>- Show header selection             |
   * | `'header'`        | - Show single-cell selection<br>- Show range selection<br>- Don't show header selection             |
   * | An array          | A combination of `'current'`, `'area'`, and/or `'header'`                                           |
   *
   * Read more:
   * - [Selection](@/guides/cell-features/selection/selection.md)
   *
   * @memberof Options#
   * @type {boolean|string|string[]}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // don't show single-cell selection
   * // don't show range selection
   * // don't show header selection
   * disableVisualSelection: true,
   *
   * // don't show single-cell selection
   * // show range selection
   * // show header selection
   * disableVisualSelection: 'current',
   *
   * // don't show single-cell selection
   * // don't show range selection
   * // show header selection
   * disableVisualSelection: ['current', 'area'],
   * ```
   */
  disableVisualSelection: !1,
  /**
   * @description
   * The `dragToScroll` option configures the [`DragToScroll`](@/api/dragToScroll.md) plugin.
   *
   * You can set the `dragToScroll` option to one of the following:
   *
   * | Setting          | Description                                                                 |
   * | ---------------- | --------------------------------------------------------------------------- |
   * | `true` (default) | When selection reaches the edge of the grid's viewport, scroll the viewport |
   * | `false`          | Don't scroll the viewport                                                   |
   *
   * Read more:
   * - [Plugins: `DragToScroll`](@/api/dragToScroll.md)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category DragToScroll
   *
   * @example
   * ```js
   * // when selection reaches the edge of the grid's viewport, scroll the viewport
   * dragToScroll: true,
   * ```
   */
  dragToScroll: !0,
  /**
   * The `dropdownMenu` option configures the [`DropdownMenu`](@/api/dropdownMenu.md) plugin.
   *
   * You can set the `dropdownMenu` option to one of the following:
   *
   * | Setting   | Description                                                                                                                                                                                  |
   * | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `false`   | Disable the [`DropdownMenu`](@/api/dropdownMenu.md) plugin                                                                                                                                   |
   * | `true`    | - Enable the [`DropdownMenu`](@/api/dropdownMenu.md) plugin<br>- Use the [default context menu options](@/guides/accessories-and-menus/context-menu/context-menu.md#context-menu-with-default-options)    |
   * | An array  | - Enable the [`DropdownMenu`](@/api/dropdownMenu.md) plugin<br>- Modify [individual context menu options](@/guides/accessories-and-menus/context-menu/context-menu.md#context-menu-with-specific-options) |
   * | An object | - Enable the [`DropdownMenu`](@/api/dropdownMenu.md) plugin<br>- Apply a custom dropdown menu configuration                                                                                  |
   *
   * Read more:
   * - [Context menu](@/guides/accessories-and-menus/context-menu/context-menu.md)
   * - [Plugins: `DropdownMenu`](@/api/dropdownMenu.md)
   *
   * @memberof Options#
   * @type {boolean|object|string[]}
   * @default undefined
   * @category DropdownMenu
   *
   * @example
   * ```js
   * // enable the `DropdownMenu` plugin
   * // use the default context menu options
   * dropdownMenu: true,
   *
   * // enable the `DropdownMenu` plugin
   * // and modify individual context menu options
   * dropdownMenu: ['---------', 'undo', 'redo'],
   *
   * // enable the `DropdownMenu` plugin
   * // and apply a custom dropdown menu configuration
   * dropdownMenu: {
   *   items: {
   *     'option1': {
   *       name: 'Option 1'
   *     },
   *     'option2': {
   *       name: 'Option 2',
   *       submenu: {
   *         items: [
   *           {
   *             key: 'option2:suboption1',
   *             name: 'Suboption 1',
   *             callback(key, options) {
   *               ...
   *             }
   *           },
   *           ...
   *         ]
   *       }
   *     }
   *   }
   * },
   * ```
   */
  dropdownMenu: void 0,
  /**
   * The `editor` option sets a [cell editor](@/guides/cell-functions/cell-editor/cell-editor.md) for a cell.
   *
   * You can set the `editor` option to one of the following [cell editor aliases](@/guides/cell-functions/cell-editor/cell-editor.md):
   *
   * | Alias               | Cell editor function                                                       |
   * | ------------------- | -------------------------------------------------------------------------- |
   * | A custom alias      | Your [custom cell editor](@/guides/cell-functions/cell-editor/cell-editor.md) function |
   * | `'autocomplete'`    | `AutocompleteEditor`                                                       |
   * | `'base'`            | `BaseEditor`                                                               |
   * | `'checkbox'`        | `CheckboxEditor`                                                           |
   * | `'date'`            | `DateEditor`                                                               |
   * | `'dropdown'`        | `DropdownEditor`                                                           |
   * | `'handsontable'`    | `HandsontableEditor`                                                       |
   * | `'numeric'`         | `NumericEditor`                                                            |
   * | `'password'`        | `PasswordEditor`                                                           |
   * | `'select'`          | `SelectEditor`                                                             |
   * | `'text'`            | `TextEditor`                                                               |
   * | `'time'`            | `TimeEditor`                                                               |
   *
   * To disable editing cells through cell editors,
   * set the `editor` option to `false`.
   * You'll still be able to change cells' content through Handsontable's API
   * or through plugins (e.g. [`CopyPaste`](@/api/copyPaste.md)), though.
   *
   * To set the [`editor`](#editor), [`renderer`](#renderer), and [`validator`](#validator)
   * options all at once, use the [`type`](#type) option.
   *
   * Read more:
   * - [Cell editor](@/guides/cell-functions/cell-editor/cell-editor.md)
   * - [Cell type](@/guides/cell-types/cell-type/cell-type.md)
   * - [Configuration options: Cascading configuration](@/guides/getting-started/configuration-options/configuration-options.md#cascading-configuration)
   * - [`type`](#type)
   *
   * @memberof Options#
   * @type {string|Function|boolean}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // use the `numeric` editor for each cell of the entire grid
   * editor: 'numeric',
   *
   * // apply the `editor` option to individual columns
   * columns: [
   *   {
   *     // use the `autocomplete` editor for each cell of this column
   *     editor: 'autocomplete'
   *   },
   *   {
   *     // disable editing cells through cell editors for each cell of this column
   *     editor: false
   *   }
   * ]
   * ```
   */
  editor: void 0,
  /**
   * The `enterBeginsEditing` option configures the action of the <kbd>**Enter**</kbd> key.
   *
   * You can set the `enterBeginsEditing` option to one of the following:
   *
   * | Setting          | Description                                                                                                                                                                                               |
   * | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `true` (default) | - On pressing <kbd>**Enter**</kbd> once, enter the editing mode of the active cell<br>- On pressing <kbd>**Enter**</kbd> twice, move to another cell,<br>as configured by the [`enterMoves`](#enterMoves) setting |
   * | `false`          | - On pressing <kbd>**Enter**</kbd> once, move to another cell,<br>as configured by the [`enterMoves`](#enterMoves) setting                                                                                    |
   *
   * Read more:
   * - [`enterMoves`](#enterMoves)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // press Enter once to start editing
   * // press Enter twice to move to another cell
   * enterBeginsEditing: true,
   *
   * // press Enter once to move to another cell
   * enterBeginsEditing: false,
   * ```
   */
  enterBeginsEditing: !0,
  /**
   * The `enterMoves` option configures the action of the <kbd>**Enter**</kbd> key.
   *
   * If the [`enterBeginsEditing`](#enterBeginsEditing) option is set to `true`,
   * the `enterMoves` setting applies to the **second** pressing of the <kbd>**Enter**</kbd> key.
   *
   * If the [`enterBeginsEditing`](#enterBeginsEditing) option is set to `false`,
   * the `enterMoves` setting applies to the **first** pressing of the <kbd>**Enter**</kbd> key.
   *
   * You can set the `enterMoves` option to an object with the following properties
   * (or to a function that returns such an object):
   *
   * | Property | Type   | Description                                                                                                                                              |
   * | -------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `col`    | Number | - On pressing <kbd>**Enter**</kbd>, move selection `col` columns right<br>- On pressing <kbd>**Shift**</kbd>+<kbd>**Enter**</kbd>, move selection `col` columns left |
   * | `row`    | Number | - On pressing <kbd>**Enter**</kbd>, move selection `row` rows down<br>- On pressing <kbd>**Shift**</kbd>+<kbd>**Enter**</kbd>, move selection `row` rows up          |
   *
   * Read more:
   * - [`enterBeginsEditing`](#enterBeginsEditing)
   *
   * @memberof Options#
   * @type {object|Function}
   * @default {col: 0, row: 1}
   * @category Core
   *
   * @example
   * ```js
   * // on pressing Enter, move selection 1 column right and 1 row down
   * // on pressing Shift+Enter, move selection 1 column left and 1 row up
   * enterMoves: {col: 1, row: 1},
   *
   * // the same setting, as a function
   * // `event` is a DOM Event object received on pressing Enter
   * // you can use it to check whether the user pressed Enter or Shift+Enter
   * enterMoves(event) {
   *   return {col: 1, row: 1};
   * },
   * ```
   */
  enterMoves: {
    col: 0,
    row: 1
  },
  /**
   * The `fillHandle` option configures the [Autofill](@/api/autofill.md) plugin.
   *
   * You can set the `fillHandle` option to one the following:
   *
   * | Setting        | Description                                                                |
   * | -------------- | -------------------------------------------------------------------------- |
   * | `true`         | - Enable autofill in all directions<br>- Add the fill handle               |
   * | `false`        | Disable autofill                                                           |
   * | `'vertical'`   | - Enable vertical autofill<br>- Add the fill handle                        |
   * | `'horizontal'` | - Enable horizontal autofill<br>- Add the fill handle                      |
   * | An object      | - Enable autofill<br>- Add the fill handle<br>- Configure autofill options |
   *
   * If you set the `fillHandle` option to an object, you can configure the following autofill options:
   *
   * | Option          | Possible settings              | Description                                                                                               |
   * | --------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------- |
   * | `autoInsertRow` | `true` (default) \| `false`    | `true`: When you reach the grid's bottom, add new rows<br>`false`: When you reach the grid's bottom, stop |
   * | `direction`     | `'vertical'` \| `'horizontal'` | `'vertical'`: Enable vertical autofill<br>`'horizontal'`: Enable horizontal autofill                      |
   *
   * Read more:
   * - [AutoFill values](@/guides/cell-features/autofill-values/autofill-values.md)
   *
   * @memberof Options#
   * @type {boolean|string|object}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // enable autofill in all directions
   * // with `autoInsertRow` enabled
   * fillHandle: true,
   *
   * // enable vertical autofill
   * // with `autoInsertRow` enabled
   * fillHandle: 'vertical',
   *
   * // enable horizontal autofill
   * // with `autoInsertRow` enabled
   * fillHandle: 'horizontal',
   *
   * // enable autofill in all directions
   * // with `autoInsertRow` disabled
   * fillHandle: {
   *   autoInsertRow: false,
   * },
   *
   * // enable vertical autofill
   * // with `autoInsertRow` disabled
   * fillHandle: {
   *   autoInsertRow: false,
   *   direction: 'vertical'
   * },
   * ```
   */
  fillHandle: {
    autoInsertRow: !1
  },
  /**
   * The `filter` option configures whether [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md) cells'
   * lists are updated by the end user's input.
   *
   * You can set the `filter` option to one of the following:
   *
   * | Setting          | Description                                                                                                           |
   * | ---------------- | --------------------------------------------------------------------------------------------------------------------- |
   * | `true` (default) | When the end user types into the input area, only options matching the input are displayed                            |
   * | `false`          | When the end user types into the input area, all options are displayed<br>(options matching the input are put in bold |
   *
   * Read more:
   * - [Autocomplete cell type](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * - [`source`](#source)
   * - [`filteringCaseSensitive`](#filteringCaseSensitive)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * columns: [{
   *   // set the `type` of each cell in this column to `autocomplete`
   *   type: 'autocomplete',
   *   // set options available in every `autocomplete` cell of this column
   *   source: ['A', 'B', 'C'],
   *   // when the end user types in `A`, display only the A option
   *   // when the end user types in `B`, display only the B option
   *   // when the end user types in `C`, display only the C option
   *   filter: true
   * }],
   * ```
   */
  filter: !0,
  /**
   * The `filteringCaseSensitive` option configures whether [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md) cells'
   * input is case-sensitive.
   *
   * You can set the `filteringCaseSensitive` option to one of the following:
   *
   * | Setting           | Description                                                                                        |
   * | ----------------- | -------------------------------------------------------------------------------------------------- |
   * | `false` (default) | [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md) cells' input is not case-sensitive |
   * | `true`            | [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md) cells' input is case-sensitive     |
   *
   * Read more:
   * - [Autocomplete cell type](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * - [`source`](#source)
   * - [`filter`](#filter)
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     type: 'autocomplete',
   *     source: [ ... ],
   *     // match case while searching autocomplete options
   *     filteringCaseSensitive: true
   *   }
   * ],
   * ```
   */
  filteringCaseSensitive: !1,
  /**
   * The `filters` option configures the [`Filters`](@/api/filters.md) plugin.
   *
   * You can set the `filters` option to one of the following:
   *
   * | Setting | Description                                      |
   * | ------- | ------------------------------------------------ |
   * | `false` | Disable the [`Filters`](@/api/filters.md) plugin |
   * | `true`  | Enable the [`Filters`](@/api/filters.md) plugin  |
   *
   * Read more:
   * - [Column filter](@/guides/columns/column-filter/column-filter.md)
   * - [Plugins: `Filters`](@/api/filters.md)
   * - [`dropdownMenu`](#dropdownMenu)
   *
   * @memberof Options#
   * @type {boolean}
   * @default undefined
   * @category Filters
   *
   * @example
   * ```js
   * // enable the `Filters` plugin
   * filters: true,
   * ```
   */
  filters: void 0,
  /**
   * `fixedColumnsLeft` is a legacy option.
   *
   * If your grid's [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is LTR (default), `fixedColumnsLeft` acts like the [`fixedColumnsStart`](#fixedColumnsStart) option.
   *
   * If your grid's [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is RTL, using `fixedColumnsLeft` throws an error.
   *
   * Use [`fixedColumnsStart`](#fixedColumnsStart), which works in any layout direction.
   *
   * Read more:
   * - [`fixedColumnsStart`](#fixedcolumnsstart)
   *
   * @memberof Options#
   * @type {number}
   * @default 0
   * @category Core
   *
   * @example
   * ```js
   * // freeze the first 3 columns from the left
   * fixedColumnsLeft: 3,
   * ```
   */
  fixedColumnsLeft: 0,
  /**
   * If your grid's [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is LTR (default), the `fixedColumnsStart` option sets the number of [frozen columns](@/guides/columns/column-freezing/column-freezing.md) at the left-hand edge of the grid.
   *
   * If your grid's [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) is RTL, the `fixedColumnsStart` option sets the number of [frozen columns](@/guides/columns/column-freezing/column-freezing.md) at the right-hand edge of the grid.
   *
   * Read more:
   * - [Column freezing](@/guides/columns/column-freezing/column-freezing.md)
   * - [Layout direction](@/guides/internationalization/layout-direction/layout-direction.md)
   * - [`fixedColumnsLeft`](#fixedcolumnsleft)
   * - [`layoutDirection`](#layoutDirection)
   *
   * @memberof Options#
   * @type {number}
   * @default 0
   * @category Core
   *
   * @example
   * ```js
   * // when `layoutDirection` is set to `inherit` (default)
   * // freeze the first 3 columns from the left or from the right
   * // depending on your HTML document's `dir` attribute
   * layoutDirection: 'inherit',
   * fixedColumnsStart: 3,
   *
   * // when `layoutDirection` is set to `rtl`
   * // freeze the first 3 columns from the right
   * // regardless of your HTML document's `dir` attribute
   * layoutDirection: 'rtl',
   * fixedColumnsStart: 3,
   *
   * // when `layoutDirection` is set to `ltr`
   * // freeze the first 3 columns from the left
   * // regardless of your HTML document's `dir` attribute
   * layoutDirection: 'ltr',
   * fixedColumnsStart: 3,
   * ```
   */
  fixedColumnsStart: 0,
  /**
   * The `fixedRowsBottom` option sets the number of [frozen rows](@/guides/rows/row-freezing/row-freezing.md)
   * at the bottom of the grid.
   *
   * Read more:
   * - [Row freezing](@/guides/rows/row-freezing/row-freezing.md)
   *
   * @memberof Options#
   * @type {number}
   * @default 0
   * @category Core
   *
   * @example
   * ```js
   * // freeze the bottom 3 rows
   * fixedRowsBottom: 3,
   * ```
   */
  fixedRowsBottom: 0,
  /**
   * The `fixedRowsTop` option sets the number of [frozen rows](@/guides/rows/row-freezing/row-freezing.md) at the top of the grid.
   *
   * Read more:
   * - [Row freezing](@/guides/rows/row-freezing/row-freezing.md)
   *
   * @memberof Options#
   * @type {number}
   * @default 0
   * @category Core
   *
   * @example
   * ```js
   * // freeze the top 3 rows
   * fixedRowsTop: 3,
   * ```
   */
  fixedRowsTop: 0,
  /**
   * The `formulas` option configures the [`Formulas`](@/api/formulas.md) plugin.
   *
   * The [`Formulas`](@/api/formulas.md) plugin uses the [HyperFormula](https://handsontable.github.io/hyperformula/) calculation engine.
   * To install [HyperFormula](https://handsontable.github.io/hyperformula/), read the following:
   * - [Formula calculation: Initialization methods](@/guides/formulas/formula-calculation/formula-calculation.md#initialization-methods)
   *
   * You can set the `formulas` option to an object with the following properties:
   *
   * | Property    | Possible values                                                                                                                                                                                                        |
   * | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `engine`    | `HyperFormula` \|<br>A [HyperFormula](https://handsontable.github.io/hyperformula/) instance \|<br>A [HyperFormula configuration](https://handsontable.github.io/hyperformula/api/interfaces/configparams.html) object |
   * | `sheetId`   | A number                                                                                                                                                                                                               |
   * | `sheetName` | A string                                                                                                                                                                                                               |
   *
   * Read more:
   * - [Plugins: `Formulas`](@/api/formulas.md)
   * - [Formula calculation](@/guides/formulas/formula-calculation/formula-calculation.md)
   * - [HyperFormula documentation: Client-side installation](https://handsontable.github.io/hyperformula/guide/client-side-installation)
   * - [HyperFormula documentation: Configuration options](https://handsontable.github.io/hyperformula/api/interfaces/configparams.html)
   *
   * @memberof Options#
   * @type {object}
   * @default undefined
   * @category Formulas
   *
   * @example
   * ```js
   * // either add the `HyperFormula` class
   * formulas: {
   *   // set `engine` to `HyperFormula`
   *   engine: HyperFormula,
   *   sheetId: 1,
   *   sheetName: 'Sheet 1'
   * }
   *
   * // or, add a HyperFormula instance
   * // initialized with the `'internal-use-in-handsontable'` license key
   * const hyperformulaInstance = HyperFormula.buildEmpty({
   *   licenseKey: 'internal-use-in-handsontable',
   * });
   *
   * formulas: {
   *   // set `engine` to a HyperFormula instance
   *   engine: hyperformulaInstance,
   *   sheetId: 1,
   *   sheetName: 'Sheet 1'
   * }
   *
   * // or, add a HyperFormula configuration object
   * formulas: {
   *   // set `engine` to a HyperFormula configuration object
   *   engine: {
   *     hyperformula: HyperFormula // or `engine: hyperformulaInstance`
   *     leapYear1900: false,       // this option comes from HyperFormula
   *     // add more HyperFormula configuration options
   *   },
   *   sheetId: 1,
   *   sheetName: 'Sheet 1'
   * }
   *
   * // use the same HyperFormula instance in multiple Handsontable instances
   *
   * // a Handsontable instance `hot1`
   * formulas: {
   *   engine: HyperFormula,
   *   sheetId: 1,
   *   sheetName: 'Sheet 1'
   * }
   *
   * // a Handsontable instance `hot2`
   * formulas: {
   *   engine: hot1.getPlugin('formulas').engine,
   *   sheetId: 1,
   *   sheetName: 'Sheet 1'
   * }
   * ```
   */
  formulas: void 0,
  /**
   * The `fragmentSelection` option configures text selection settings.
   *
   * You can set the `fragmentSelection` option to one of the following:
   *
   * | Setting           | Description                                        |
   * | ----------------- | ------------------------------------------------- |
   * | `false` (default) | Disable text selection                            |
   * | `true`            | Enable text selection in multiple cells at a time |
   * | `'cell'`          | Enable text selection in one cell at a time       |
   *
   * @memberof Options#
   * @type {boolean|string}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // enable text selection in multiple cells at a time
   * fragmentSelection: true,
   *
   * // enable text selection in one cell a time
   * fragmentSelection: 'cell',
   * ```
   */
  fragmentSelection: !1,
  /**
   * The `headerClassName` option allows adding one or more class names to the column headers' inner `div` element.
   * It can be used to align the labels in the column headers to left, center or right by setting this option to
   * `htLeft`, `htCenter`, or `htRight` respectively.
   *
   * @since 14.5.0
   * @memberof Options#
   * @type {string}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // Adding class names to all column headers
   * headerClassName: 'htRight my-class',
   *
   * columns: [
   *  {
   *    // Adding class names to the column header of a single column
   *    headerClassName: 'htRight my-class',
   *  }
   * ]
   * ```
   */
  headerClassName: void 0,
  /**
   * The `height` option configures the height of your grid.
   *
   * You can set `height` option to one of the following:
   *
   * | Setting                                                                    | Example                    |
   * | -------------------------------------------------------------------------- | -------------------------- |
   * | A number of pixels                                                         | `height: 500`              |
   * | A string with a [CSS unit](https://www.w3schools.com/cssref/css_units.asp) | `height: '75vw'`           |
   * | A function that returns a valid number or string                           | `height() { return 500; }` |
   *
   * Read more:
   * - [Grid size](@/guides/getting-started/grid-size/grid-size.md)
   *
   * @memberof Options#
   * @type {number|string|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // set the grid's height to 500px
   * height: 500,
   *
   * // set the grid's height to 75vh
   * height: '75vh',
   *
   * // set the grid's height to 500px, using a function
   * height() {
   *   return 500;
   * },
   * ```
   */
  height: void 0,
  /**
   * The `hiddenColumns` option configures the [`HiddenColumns`](@/api/hiddenColumns.md) plugin.
   *
   * You can set the `hiddenColumns` option to one of the following:
   *
   * | Setting   | Description                                                                                  |
   * | --------- | -------------------------------------------------------------------------------------------- |
   * | `false`   | Disable the [`HiddenColumns`](@/api/hiddenColumns.md) plugin                                 |
   * | `true`    | Enable the [`HiddenColumns`](@/api/hiddenColumns.md) plugin with the default plugin options  |
   * | An object | - Enable the [`HiddenColumns`](@/api/hiddenColumns.md) plugin<br>- Modify the plugin options |
   *
   * If you set the `hiddenColumns` to an object, you can set the following [`HiddenColumns`](@/api/hiddenColumns.md) plugin options:
   *
   * | Property           | Possible values     | Description                                                                                                                                             |
   * | ------------------ | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `columns`          | An array of indexes | An array of indexes of columns that are hidden at initialization                                                                                        |
   * | `copyPasteEnabled` | `true` \| `false`   | `true`: when copying or pasting data, take hidden columns into account<br>`false`: when copying or pasting data, don't take hidden columns into account |
   * | `indicators`       | `true` \| `false`   | `true`: display UI markers to indicate the presence of hidden columns<br>`false`: display UI markers                                                    |
   *
   * Read more:
   * - [Plugins: `HiddenColumns`](@/api/hiddenColumns.md)
   * - [Column hiding](@/guides/columns/column-hiding/column-hiding.md)
   *
   * @memberof Options#
   * @type {boolean|object}
   * @default undefined
   * @category HiddenColumns
   *
   * @example
   * ```js
   * // enable the `HiddenColumns` plugin
   * hiddenColumns: true,
   *
   * // enable `HiddenColumns` plugin, and modify the plugin options
   * hiddenColumns: {
   *   // set columns that are hidden by default
   *   columns: [5, 10, 15],
   *   // when copying or pasting data, take hidden columns into account
   *   copyPasteEnabled: true,
   *   // show where hidden columns are
   *   indicators: true
   * }
   * ```
   */
  hiddenColumns: void 0,
  /**
   * The `hiddenRows` option configures the [`HiddenRows`](@/api/hiddenRows.md) plugin.
   *
   * You can set the `hiddenRows` option to one of the following:
   *
   * | Setting   | Description                                                                            |
   * | --------- | -------------------------------------------------------------------------------------- |
   * | `false`   | Disable the [`HiddenRows`](@/api/hiddenRows.md) plugin                                 |
   * | `true`    | Enable the [`HiddenRows`](@/api/hiddenRows.md) plugin with the default plugin options  |
   * | An object | - Enable the [`HiddenRows`](@/api/hiddenRows.md) plugin<br>- Modify the plugin options |
   *
   * If you set the `hiddenRows` to an object, you can set the following [`HiddenRows`](@/api/hiddenRows.md) plugin options:
   *
   * | Property           | Possible values     | Description                                                                                                                                       |
   * | ------------------ | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `rows   `          | An array of indexes | An array of indexes of rows that are hidden at initialization                                                                                     |
   * | `copyPasteEnabled` | `true` \| `false`   | `true`: when copying or pasting data, take hidden rows into account<br>`false`: when copying or pasting data, don't take hidden rows into account |
   * | `indicators`       | `true` \| `false`   | `true`: display UI markers to indicate the presence of hidden rows<br>`false`: display UI markers                                                 |
   *
   * Read more:
   * - [Plugins: `HiddenRows`](@/api/hiddenRows.md)
   * - [Row hiding](@/guides/rows/row-hiding/row-hiding.md)
   *
   * @memberof Options#
   * @type {boolean|object}
   * @default undefined
   * @category HiddenRows
   *
   * @example
   * ```js
   * // enable the `HiddenRows` plugin
   * hiddenRows: true,
   *
   * // enable `HiddenRows` plugin, and modify the plugin options
   * hiddenRows: {
   *   // set rows that are hidden by default
   *   rows: [5, 10, 15],
   *   // when copying or pasting data, take hidden rows into account
   *   copyPasteEnabled: true,
   *   // show where hidden rows are
   *   indicators: true
   * }
   * ```
   */
  hiddenRows: void 0,
  /**
   * The `invalidCellClassName` option lets you add a CSS class name to cells
   * that were marked as `invalid` by the [cell validator](@/guides/cell-functions/cell-validator/cell-validator.md).
   *
   * Read more:
   * - [Cell validator](@/guides/cell-functions/cell-validator/cell-validator.md)
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`activeHeaderClassName`](#activeHeaderClassName)
   * - [`currentColClassName`](#currentColClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`TableClassName`](#TableClassName)
   * - [`className`](#className)
   *
   * @memberof Options#
   * @type {string}
   * @default 'htInvalid'
   * @category Core
   *
   * @example
   * ```js
   * // add a `highlight-error` CSS class name
   * // to every `invalid` cell`
   * invalidCellClassName: 'highlight-error',
   * ```
   */
  invalidCellClassName: "htInvalid",
  /**
   * The `imeFastEdit` option allows using the "fast edit" feature for the IME users. It's disabled by default
   * because of its incompatibility with some of the accessibility features.
   *
   * Enabling this option can make a negative impact on how some screen readers handle reading the table cells.
   *
   * @since 14.0.0
   * @memberof Options#
   * @type {boolean}
   * @category Core
   */
  imeFastEdit: !1,
  /**
   * The `isEmptyCol` option lets you define your own custom method
   * for checking if a column at a given visual index is empty.
   *
   * The `isEmptyCol` setting overwrites the built-in [`isEmptyCol`](@/api/core.md#isEmptyCol) method.
   *
   * @memberof Options#
   * @type {Function}
   * @param {number} col Visual column index.
   * @returns {boolean}
   * @category Core
   *
   * @example
   * ```js
   * // overwrite the built-in `isEmptyCol` method
   * isEmptyCol(visualColumnIndex) {
   *    // your custom method
   *    ...
   * },
   * ```
   */
  isEmptyCol(t) {
    let e, n, i;
    for (e = 0, n = this.countRows(); e < n; e++)
      if (i = this.getDataAtCell(e, t), zt(i) === !1)
        return !1;
    return !0;
  },
  /**
   * The `isEmptyRow` option lets you define your own custom method
   * for checking if a row at a given visual index is empty.
   *
   * The `isEmptyRow` setting overwrites the built-in [`isEmptyRow`](@/api/core.md#isEmptyRow) method.
   *
   * @memberof Options#
   * @type {Function}
   * @param {number} row Visual row index.
   * @returns {boolean}
   * @category Core
   *
   * @example
   * ```js
   * // overwrite the built-in `isEmptyRow` method
   * isEmptyRow(visualRowIndex) {
   *    // your custom method
   *    ...
   * },
   * ```
   */
  isEmptyRow(t) {
    let e, n, i, r;
    for (e = 0, n = this.countCols(); e < n; e++)
      if (i = this.getDataAtCell(t, e), zt(i) === !1)
        return typeof i == "object" ? (r = this.getCellMeta(t, e), Op(this.getSchema()[r.prop], i)) : !1;
    return !0;
  },
  /**
   * @description
   * The `label` option configures [`checkbox`](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md) cells` labels.
   *
   * You can set the `label` option to an object with the following properties:
   *
   * | Property    | Possible values                   | Description                                                                                                                                                                                                             |
   * | ----------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `position`  | `'after'` (default) \| `'before'` | `'after'`: place the label to the right of the checkbox<br>`'before'`: place the label to the left of the checkbox                                                                                                      |
   * | `value`     | A string \| A function            | The label's text                                                                                                                                                                                                        |
   * | `separated` | `false` (default) \| `true`       | `false`: don't separate the label from the checkbox<br>`true`: separate the label from the checkbox                                                                                                                     |
   * | `property`  | A string                          | - A [`data`](#data) object property name that's used as the label's text <br>- Works only when the [`data`](#data) option is set to an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects) |
   *
   * Read more:
   * - [Checkbox cell type: Checkbox labels](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md#checkbox-labels)
   *
   * @memberof Options#
   * @type {object}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * columns: [{
   *   type: 'checkbox',
   *   // add 'My label:' after the checkbox
   *   label: { position: 'after', value: 'My label: ', separated: true }
   * }],
   * ```
   */
  label: void 0,
  /**
   * The `language` option configures Handsontable's [language](@/guides/internationalization/language/language.md) settings.
   *
   * You can set the `language` option to one of the following:
   *
   * | Setting             | Description                 |
   * | ------------------- | --------------------------- |
   * | `'en-US'` (default) | English - United States     |
   * | `'ar-AR'`           | Arabic - Global<br><br>To properly render this language, set the [layout direction](@/guides/internationalization/layout-direction/layout-direction.md) to RTL. |
   * | `'cs-CZ'`           | Czech - Czech Republic      |
   * | `'de-CH'`           | German - Switzerland        |
   * | `'de-DE'`           | German - Germany            |
   * | `'es-MX'`           | Spanish - Mexico            |
   * | `'fr-FR'`           | French - France             |
   * | `'hr-HR'`           | Croatian - Croatia          |
   * | `'it-IT'`           | Italian - Italy             |
   * | `'ja-JP'`           | Japanese - Japan            |
   * | `'ko-KR'`           | Korean - Korea              |
   * | `'lv-LV'`           | Latvian - Latvia            |
   * | `'nb-NO'`           | Norwegian (Bokmål) - Norway |
   * | `'nl-NL'`           | Dutch - Netherlands         |
   * | `'pl-PL'`           | Polish - Poland             |
   * | `'pt-BR'`           | Portuguese - Brazil         |
   * | `'ru-RU'`           | Russian - Russia            |
   * | `'sr-SP'`           | Serbian (Latin) - Serbia    |
   * | `'zh-CN'`           | Chinese - China             |
   * | `'zh-TW'`           | Chinese - Taiwan            |
   *
   * Read more:
   * - [Language](@/guides/internationalization/language/language.md)
   * - [`locale`](#locale)
   * - [`layoutDirection`](#layoutdirection)
   *
   * @memberof Options#
   * @type {string}
   * @default 'en-US'
   * @category Core
   *
   * @example
   * ```js
   * // set Handsontable's language to Polish
   * language: 'pl-PL',
   * ```
   */
  language: "en-US",
  /**
   * The `layoutDirection` option configures whether Handsontable renders from the left to the right, or from the right to the left.
   *
   * You can set the layout direction only at Handsontable's [initialization](@/guides/getting-started/installation/installation.md#initialize-handsontable). Any change of the `layoutDirection` option after the initialization (e.g. using the [`updateSettings()`](@/api/core.md#updatesettings) method) is ignored.
   *
   * You can set the `layoutDirection` option only [for the entire grid](@/guides/getting-started/configuration-options/configuration-options.md#set-grid-options).
   * You can't set it for individual columns, rows, or cells.
   *
   * You can set the `layoutDirection` option to one of the following strings:
   *
   * | Setting             | Description                                                                                                                                                                                  |
   * | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `inherit` (default) | Set Handsontable's layout direction automatically,<br>based on the value of your HTML document's [`dir`](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/dir) attribute  |
   * | `rtl`               | Render Handsontable from the right to the left,<br>even when your HTML document's [`dir`](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/dir) attribute is set to `ltr` |
   * | `ltr`               | Render Handsontable from the left to the right,<br>even when your HTML document's [`dir`](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/dir) attribute is set to `rtl` |
   *
   * Read more:
   * - [Layout direction](@/guides/internationalization/layout-direction/layout-direction.md)
   * - [Language](@/guides/internationalization/language/language.md)
   * - [`language`](#language)
   * - [`locale`](#locale)
   * - [`fixedColumnsStart`](#fixedcolumnsstart)
   * - [`customBorders`](#customBorders)
   *
   * @memberof Options#
   * @type {string}
   * @default 'inherit'
   * @category Core
   *
   * @example
   * ```js
   * // inherit Handsontable's layout direction
   * // from the value of your HTML document's `dir` attribute
   * layoutDirection: 'inherit',
   *
   * // render Handsontable from the right to the left
   * // regardless of your HTML document's `dir`
   * layoutDirection: 'rtl',
   *
   * // render Handsontable from the left to the right
   * // regardless of your HTML document's `dir`
   * layoutDirection: 'ltr',
   * ```
   */
  layoutDirection: "inherit",
  /**
   * The `licenseKey` option sets your Handsontable license key.
   *
   * You can set the `licenseKey` option to one of the following:
   *
   * | Setting                                                                                                 | Description                                                                                       |
   * | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
   * | A string with your [commercial license key](@/guides/getting-started/license-key/license-key.md#commercial-license) | For [commercial use](@/guides/technical-specification/software-license/software-license.md#commercial-use)         |
   * | `'non-commercial-and-evaluation'`                                                                       | For [non-commercial use](@/guides/technical-specification/software-license/software-license.md#non-commercial-use) |
   *
   * Read more:
   * - [License key](@/guides/getting-started/license-key/license-key.md)
   *
   * @memberof Options#
   * @type {string}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // for commercial use
   * licenseKey: 'xxxxx-xxxxx-xxxxx-xxxxx-xxxxx', // your commercial license key
   *
   * // for non-commercial use
   * licenseKey: 'non-commercial-and-evaluation',
   * ```
   */
  licenseKey: void 0,
  /**
   * The `locale` option configures Handsontable's [locale](@/guides/internationalization/locale/locale.md) settings.
   *
   * You can set the `locale` option to any valid and canonicalized Unicode BCP 47 locale tag,
   * both for the [entire grid](@/guides/internationalization/locale/locale.md#set-the-grid-s-locale),
   * and for [individual columns](@/guides/internationalization/locale/locale.md#set-a-column-s-locale).
   *
   * Read more:
   * - [Locale](@/guides/internationalization/locale/locale.md)
   * - [`language`](#language)
   * - [`layoutDirection`](#layoutdirection)
   *
   * @memberof Options#
   * @type {string}
   * @default 'en-US'
   * @category Core
   *
   * @example
   * ```js
   * // set the entire grid's locale to Polish
   * locale: 'pl-PL',
   *
   * // set individual columns' locales
   * columns: [
   *   {
   *     // set the first column's locale to Polish
   *     locale: 'pl-PL',
   *   },
   *   {
   *     // set the second column's locale to German
   *     locale: 'de-DE',
   *   },
   * ],
   * ```
   */
  locale: "en-US",
  /**
   * The `manualColumnFreeze` option configures the [`ManualColumnFreeze`](@/api/manualColumnFreeze.md) plugin.
   *
   * You can set the `manualColumnFreeze` option to one of the following:
   *
   * | Setting  | Description                                                            |
   * | -------- | ---------------------------------------------------------------------- |
   * | `true`   | Enable the [`ManualColumnFreeze`](@/api/manualColumnFreeze.md) plugin  |
   * | `false`  | Disable the [`ManualColumnFreeze`](@/api/manualColumnFreeze.md) plugin |
   *
   * Read more:
   * - [Column freezing](@/guides/columns/column-freezing/column-freezing.md#user-triggered-freeze)
   *
   * @memberof Options#
   * @type {boolean}
   * @default undefined
   * @category ManualColumnFreeze
   *
   * @example
   * ```js
   * // enable the `ManualColumnFreeze` plugin
   * manualColumnFreeze: true,
   * ```
   */
  manualColumnFreeze: void 0,
  /**
   * The `manualColumnMove` option configures the [`ManualColumnMove`](@/api/manualColumnMove.md) plugin.
   *
   * You can set the `manualColumnMove` option to one of the following:
   *
   * | Setting  | Description                                                                                                        |
   * | -------- | ------------------------------------------------------------------------------------------------------------------ |
   * | `true`   | Enable the [`ManualColumnMove`](@/api/manualColumnMove.md) plugin                                                  |
   * | `false`  | Disable the [`ManualColumnMove`](@/api/manualColumnMove.md) plugin                                                 |
   * | An array | - Enable the [`ManualColumnMove`](@/api/manualColumnMove.md) plugin<br>- Move individual columns at initialization |
   *
   * Read more:
   * - [Column moving](@/guides/columns/column-moving/column-moving.md)
   *
   * @memberof Options#
   * @type {boolean|number[]}
   * @default undefined
   * @category ManualColumnMove
   *
   * @example
   * ```js
   * // enable the `ManualColumnMove` plugin
   * manualColumnMove: true,
   *
   * // enable the `ManualColumnMove` plugin
   * // at initialization, move column 0 to 1
   * // at initialization, move column 1 to 4
   * // at initialization, move column 2 to 6
   * manualColumnMove: [1, 4, 6],
   * ```
   */
  manualColumnMove: void 0,
  /**
   * @description
   * The `manualColumnResize` option configures the [`ManualColumnResize`](@/api/manualColumnResize.md) plugin.
   *
   * You can set the `manualColumnResize` option to one of the following:
   *
   * | Setting  | Description                                                                                                           |
   * | -------- | --------------------------------------------------------------------------------------------------------------------- |
   * | `true`   | Enable the [`ManualColumnResize`](@/api/manualColumnResize.md) plugin                                                 |
   * | `false`  | Disable the [`ManualColumnResize`](@/api/manualColumnResize.md) plugin                                                |
   * | An array | - Enable the [`ManualColumnResize`](@/api/manualColumnResize.md) plugin<br>- Set initial widths of individual columns |
   *
   * Read more:
   * - [Column width: Column stretching](@/guides/columns/column-width/column-width.md#column-stretching)
   *
   * @memberof Options#
   * @type {boolean|number[]}
   * @default undefined
   * @category ManualColumnResize
   *
   * @example
   * ```js
   * // enable the `manualColumnResize` plugin
   * manualColumnResize: true,
   *
   * // enable the `manualColumnResize` plugin
   * // set the initial width of column 0 to 40 pixels
   * // set the initial width of column 1 to 50 pixels
   * // set the initial width of column 2 to 60 pixels
   * manualColumnResize: [40, 50, 60],
   * ```
   */
  manualColumnResize: void 0,
  /**
   * @description
   * The `manualRowMove` option configures the [`ManualRowMove`](@/api/manualRowMove.md) plugin.
   *
   * You can set the `manualRowMove` option to one of the following:
   *
   * | Setting  | Description                                                                                               |
   * | -------- | --------------------------------------------------------------------------------------------------------- |
   * | `true`   | Enable the [`ManualRowMove`](@/api/manualRowMove.md) plugin                                               |
   * | `false`  | Disable the [`ManualRowMove`](@/api/manualRowMove.md) plugin                                              |
   * | An array | - Enable the [`ManualRowMove`](@/api/manualRowMove.md) plugin<br>- Move individual rows at initialization |
   *
   * Read more:
   * - [Row moving](@/guides/rows/row-moving/row-moving.md)
   *
   * @memberof Options#
   * @type {boolean|number[]}
   * @default undefined
   * @category ManualRowMove
   *
   * @example
   * ```js
   * // enable the `ManualRowMove` plugin
   * manualRowMove: true,
   *
   * // enable the `ManualRowMove` plugin
   * // at initialization, move row 1 to 0
   * // at initialization, move row 4 to 1
   * // at initialization, move row 6 to 2
   * manualRowMove: [1, 4, 6],
   * ```
   */
  manualRowMove: void 0,
  /**
   * @description
   * The `manualRowResize` option configures the [`ManualRowResize`](@/api/manualRowResize.md) plugin.
   *
   * You can set the `manualRowResize` option to one of the following:
   *
   * | Setting  | Description                                                                                                   |
   * | -------- | ------------------------------------------------------------------------------------------------------------- |
   * | `true`   | Enable the [`ManualRowResize`](@/api/manualRowResize.md) plugin                                               |
   * | `false`  | Disable the [`ManualRowResize`](@/api/manualRowResize.md) plugin                                              |
   * | An array | - Enable the [`ManualRowResize`](@/api/manualRowResize.md) plugin<br>- Set initial heights of individual rows |
   *
   * Read more:
   * - [Row height: Adjust the row height manually](@/guides/rows/row-height/row-height.md#adjust-the-row-height-manually)
   *
   * @memberof Options#
   * @type {boolean|number[]}
   * @default undefined
   * @category ManualRowResize
   *
   * @example
   * ```js
   * // enable the `ManualRowResize` plugin
   * manualRowResize: true,
   *
   * // enable the `ManualRowResize` plugin
   * // set the initial height of row 0 to 40 pixels
   * // set the initial height of row 1 to 50 pixels
   * // set the initial height of row 2 to 60 pixels
   * manualRowResize: [40, 50, 60],
   * ```
   */
  manualRowResize: void 0,
  /**
   * The `maxCols` option sets a maximum number of columns.
   *
   * The `maxCols` option is used:
   * - At initialization: if the `maxCols` value is lower than the initial number of columns,
   * Handsontable trims columns from the right.
   * - At runtime: for example, when inserting columns.
   *
   * @memberof Options#
   * @type {number}
   * @default Infinity
   * @category Core
   *
   * @example
   * ```js
   * // set the maximum number of columns to 300
   * maxCols: 300,
   * ```
   */
  maxCols: 1 / 0,
  /**
   * The `maxRows` option sets a maximum number of rows.
   *
   * The `maxRows` option is used:
   * - At initialization: if the `maxRows` value is lower than the initial number of rows,
   * Handsontable trims rows from the bottom.
   * - At runtime: for example, when inserting rows.
   *
   * @memberof Options#
   * @type {number}
   * @default Infinity
   * @category Core
   *
   * @example
   * ```js
   * // set the maximum number of rows to 300
   * maxRows: 300,
   * ```
   */
  maxRows: 1 / 0,
  /**
   * @description
   * The `mergeCells` option configures the [`MergeCells`](@/api/mergeCells.md) plugin.
   *
   * You can set the `mergeCells` option to one of the following:
   *
   * | Setting             | Description                                                                                         |
   * | ------------------- | --------------------------------------------------------------------------------------------------- |
   * | `true`              | Enable the [`MergeCells`](@/api/mergeCells.md) plugin                                               |
   * | `false`             | Disable the [`MergeCells`](@/api/mergeCells.md) plugin                                              |
   * | An array of objects | - Enable the [`MergeCells`](@/api/mergeCells.md) plugin<br>- Merge specific cells at initialization |
   *
   * To merge specific cells at Handsontable's initialization,
   * set the `mergeCells` option to an array of objects, with the following properties:
   *
   * | Property  | Description                                                |
   * | --------- | ---------------------------------------------------------- |
   * | `row`     | The row index of the merged section's beginning            |
   * | `col`     | The column index of the merged section's beginning         |
   * | `rowspan` | The width (as a number of rows) of the merged section      |
   * | `colspan` | The height (as a number of columns ) of the merged section |
   *
   * Read more:
   * - [Merge cells](@/guides/cell-features/merge-cells/merge-cells.md)
   *
   * @memberof Options#
   * @type {boolean|object[]}
   * @default false
   * @category MergeCells
   *
   * @example
   * ```js
   * // enable the `MergeCells` plugin
   * mergeCells: true,
   *
   * // enable the `MergeCells` plugin
   * // and merge specific cells at initialization
   * mergeCells: [
   *   // merge cells from cell (1,1) to cell (3,3)
   *   {row: 1, col: 1, rowspan: 3, colspan: 3},
   *   // merge cells from cell (3,4) to cell (2,2)
   *   {row: 3, col: 4, rowspan: 2, colspan: 2},
   *   // merge cells from cell (5,6) to cell (3,3)
   *   {row: 5, col: 6, rowspan: 3, colspan: 3}
   * ],
   * ```
   */
  mergeCells: !1,
  /**
   * The `minCols` option sets a minimum number of columns.
   *
   * The `minCols` option is used:
   * - At initialization: if the `minCols` value is higher than the initial number of columns,
   * Handsontable adds empty columns to the right.
   * - At runtime: for example, when removing columns.
   *
   * The `minCols` option works only when your [`data`](#data) is an [array of arrays](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-arrays).
   * When your [`data`](#data) is an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects),
   * you can only have as many columns as defined in:
   * - The first data row
   * - The [`dataSchema`](#dataSchema) option
   * - The [`columns`](#columns) option
   *
   * @memberof Options#
   * @type {number}
   * @default 0
   * @category Core
   *
   * @example
   * ```js
   * // set the minimum number of columns to 10
   * minCols: 10,
   * ```
   */
  minCols: 0,
  /**
   * The `minRows` option sets a minimum number of rows.
   *
   * The `minRows` option is used:
   * - At initialization: if the `minRows` value is higher than the initial number of rows,
   * Handsontable adds empty rows at the bottom.
   * - At runtime: for example, when removing rows.
   *
   * @memberof Options#
   * @type {number}
   * @default 0
   * @category Core
   *
   * @example
   * ```js
   * // set the minimum number of rows to 10
   * minRows: 10,
   * ```
   */
  minRows: 0,
  /**
   * The `minSpareCols` option sets a minimum number of empty columns
   * at the grid's right-hand end.
   *
   * If there already are other empty columns at the grid's right-hand end,
   * they are counted into the `minSpareCols` value.
   *
   * The total number of columns can't exceed the [`maxCols`](#maxCols) value.
   *
   * The `minSpareCols` option works only when your [`data`](#data) is an [array of arrays](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-arrays).
   * When your [`data`](#data) is an [array of objects](@/guides/getting-started/binding-to-data/binding-to-data.md#array-of-objects),
   * you can only have as many columns as defined in:
   * - The first data row
   * - The [`dataSchema`](#dataSchema) option
   * - The [`columns`](#columns) option
   *
   * @memberof Options#
   * @type {number}
   * @default 0
   * @category Core
   *
   * @example
   * ```js
   * // at Handsontable's initialization, add at least 3 empty columns on the right
   * minSpareCols: 3,
   * ```
   */
  minSpareCols: 0,
  /**
   * The `minSpareRows` option sets a minimum number of empty rows
   * at the bottom of the grid.
   *
   * If there already are other empty rows at the bottom,
   * they are counted into the `minSpareRows` value.
   *
   * The total number of rows can't exceed the [`maxRows`](#maxRows) value.
   *
   * @memberof Options#
   * @type {number}
   * @default 0
   * @category Core
   *
   * @example
   * ```js
   * // at Handsontable's initialization, add at least 3 empty rows at the bottom
   * minSpareRows: 3,
   * ```
   */
  minSpareRows: 0,
  /**
   * @description
   * The `multiColumnSorting` option configures the [`MultiColumnSorting`](@/api/columnSorting.md) plugin.
   *
   * You can set the `multiColumnSorting` option to one of the following:
   *
   * | Setting    | Description                                                                                                                                                |
   * | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `true`     | Enable the [`MultiColumnSorting`](@/api/multiColumnSorting.md) plugin with the default configuration                                                       |
   * | `false`    | Disable the [`MultiColumnSorting`](@/api/multiColumnSorting.md) plugin                                                                                     |
   * | An object  | - Enable the [`MultiColumnSorting`](@/api/multiColumnSorting.md) plugin<br>- Modify the [`MultiColumnSorting`](@/api/multiColumnSorting.md) plugin options |
   *
   * If you set the `multiColumnSorting` option to an object,
   * you can set the following [`MultiColumnSorting`](@/api/multiColumnSorting.md) plugin options:
   *
   * | Option                   | Possible settings                                                                                                                                |
   * | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
   * | `indicator`              | `true`: Display the arrow icon in the column header, to indicate a sortable column<br>`false`: Don't display the arrow icon in the column header |
   * | `headerAction`           | `true`: Enable clicking on the column header to sort the column<br>`false`: Disable clicking on the column header to sort the column             |
   * | `sortEmptyCells`         | `true`: Sort empty cells as well<br>`false`: Place empty cells at the end                                                                        |
   * | `compareFunctionFactory` | A [custom compare function](@/guides/rows/rows-sorting/rows-sorting.md#add-a-custom-comparator)                                                               |
   *
   * If you set the `multiColumnSorting` option to an object,
   * you can also sort individual columns at Handsontable's initialization.
   * In the `multiColumnSorting` object, add an object named `initialConfig`,
   * with the following properties:
   *
   * | Option      | Possible settings   | Description                                                      |
   * | ----------- | ------------------- | ---------------------------------------------------------------- |
   * | `column`    | A number            | The index of the column that you want to sort at initialization  |
   * | `sortOrder` | `'asc'` \| `'desc'` | The sorting order:<br>`'asc'`: ascending<br>`'desc'`: descending |
   *
   * Read more:
   * - [Rows sorting](@/guides/rows/rows-sorting/rows-sorting.md)
   * - [`columnSorting`](#columnSorting)
   *
   * @memberof Options#
   * @type {boolean|object}
   * @default undefined
   * @category MultiColumnSorting
   *
   * @example
   * ```js
   * // enable the `MultiColumnSorting` plugin
   * multiColumnSorting: true
   *
   * // enable the `MultiColumnSorting` plugin with custom configuration
   * multiColumnSorting: {
   *   // sort empty cells as well
   *   sortEmptyCells: true,
   *   // display the arrow icon in the column header
   *   indicator: true,
   *   // disable clicking on the column header to sort the column
   *   headerAction: false,
   *   // add a custom compare function
   *   compareFunctionFactory(sortOrder, columnMeta) {
   *     return function(value, nextValue) {
   *       // some value comparisons which will return -1, 0 or 1...
   *     }
   *   }
   * }
   *
   * // enable the `MultiColumnSorting` plugin
   * multiColumnSorting: {
   *   // at initialization, sort column 1 in ascending order
   *   initialConfig: {
   *     column: 1,
   *     sortOrder: 'asc'
   *   },
   *   // at initialization, sort column 2 in descending order
   *   initialConfig: {
   *     column: 2,
   *     sortOrder: 'desc'
   *   }
   * }
   * ```
   */
  multiColumnSorting: void 0,
  /**
   * When set to `true`, the `navigableHeaders` option lets you navigate [row headers](@/guides/rows/row-header/row-header.md) and [column headers](@/guides/columns/column-header/column-header.md), using the arrow keys or the <kbd>**Tab**</kbd> key (if the [`tabNavigation`](#tabNavigation) option is set to `true`).
   *
   * @since 14.0.0
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // you can navigate row and column headers with the keyboard
   * navigableHeaders: true,
   *
   * // default behavior: you can't navigate row and column headers with the keyboard
   * navigableHeaders: false,
   * ```
   */
  navigableHeaders: !1,
  /**
   * When set to `false`, the `tabNavigation` option changes the behavior of the
   * <kbd>Tab</kbd> and <kbd>Shift</kbd>+<kbd>Tab</kbd> keyboard shortcuts. The Handsontable
   * no more captures that shortcuts to make the grid navigation available (`tabNavigation: true`)
   * but returns control to the browser so the native page navigation is possible.
   *
   * @since 14.0.0
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // you can't navigate row and column headers using <kbd>Tab</kbd> or <kbd>Shift</kbd>+<kbd>Tab</kbd> keyboard shortcuts
   * tabNavigation: false,
   *
   * // default behavior: you can navigate row and column headers using <kbd>Tab</kbd> or <kbd>Shift</kbd>+<kbd>Tab</kbd> keyboard shortcuts
   * tabNavigation: true,
   * ```
   */
  tabNavigation: !0,
  /**
   * @description
   * The `nestedHeaders` option configures the [`NestedHeaders`](@/api/nestedHeaders.md) plugin.
   *
   * You can set the `nestedHeaders` option to one of the following:
   *
   * | Setting           | Description                                                                                                                           |
   * | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
   * | `false` (default) | Disable the [`NestedHeaders`](@/api/nestedHeaders.md) plugin                                                                          |
   * | `true`            | - Enable the [`NestedHeaders`](@/api/nestedHeaders.md) plugin<br>- Don't configure any nested headers                                 |
   * | Array of arrays   | - Enable the [`NestedHeaders`](@/api/nestedHeaders.md) plugin<br>- Configure headers that are nested on Handsontable's initialization |
   *
   * If you set the `nestedHeaders` option to an array of arrays, each array configures one set of nested headers.
   *
   * Each array element configures one header, and can be one of the following:
   *
   * | Array element | Description                                                                                  |
   * | ------------- | -------------------------------------------------------------------------------------------- |
   * | A string      | The header's label                                                                           |
   * | An object     | Properties:<br>`label` (string): the header's label<br>`colspan` (integer): the column width |
   *
   * Read more:
   * - [Plugins: `NestedHeaders`](@/api/nestedHeaders.md)
   * - [Column groups: Nested headers](@/guides/columns/column-groups/column-groups.md#nested-headers)
   *
   * @memberof Options#
   * @type {boolean|Array[]}
   * @default undefined
   * @category NestedHeaders
   *
   * @example
   * ```js
   * nestedHeaders: [
   *   ['A', {label: 'B', colspan: 8}, 'C'],
   *   ['D', {label: 'E', colspan: 4}, {label: 'F', colspan: 4}, 'G'],
   *   ['H', 'I', 'J', 'K', 'L', 'M', 'N', 'R', 'S', 'T']
   * ],
   * ```
   */
  nestedHeaders: void 0,
  /**
   * @description
   * The `nestedRows` option configures the [`NestedRows`](@/api/nestedRows.md) plugin.
   *
   * You can set the `nestedRows` option to one of the following:
   *
   * | Setting           | Description                                            |
   * | ----------------- | ------------------------------------------------------ |
   * | `false` (default) | Disable the [`NestedRows`](@/api/nestedRows.md) plugin |
   * | `true`            | Enable the [`NestedRows`](@/api/nestedRows.md) plugin  |
   *
   * Read more:
   * - [Plugins: `NestedRows`](@/guides/rows/row-parent-child/row-parent-child.md)
   *
   * @example
   * ```js
   * // enable the `NestedRows` plugin
   * nestedRows: true,
   * ```
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category NestedRows
   */
  nestedRows: void 0,
  /**
   * The `noWordWrapClassName` option lets you add a CSS class name
   * to each cell that has the [`wordWrap`](#wordWrap) option set to `false`.
   *
   * Read more:
   * - [`wordWrap`](#wordWrap)
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentColClassName`](#currentColClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`invalidCellClassName`](#invalidCellClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`TableClassName`](#TableClassName)
   * - [`className`](#className)
   *
   * @memberof Options#
   * @type {string}
   * @default 'htNoWrap'
   * @category Core
   *
   * @example
   * ```js
   * // add an `is-noWrapCell` CSS class name
   * // to each cell that doesn't wrap content
   * noWordWrapClassName: 'is-noWrapCell',
   * ```
   */
  noWordWrapClassName: "htNoWrap",
  /**
   * The `numericFormat` option configures the number format and the currency format
   * of [`numeric`](@/guides/cell-types/numeric-cell-type/numeric-cell-type.md) cells` displayed output
   * in the numeric cell renderer.
   *
   * You can set the `numericFormat` option to an object with the following properties:
   *
   * | Property    | Possible values                                                               | Description     |
   * | ----------- | ----------------------------------------------------------------------------- | --------------- |
   * | `pattern`   | All [`numbro.js` number formats](https://numbrojs.com/format.html#numbers)    | Number format   |
   * | `culture`   | All [`numbro.js` currency formats](https://numbrojs.com/format.html#currency) | Currency format |
   *
   * The `numericFormat` option as no effect on the numeric cell editor.
   *
   * In the source data, numeric data is stored as JavaScript numbers.
   *
   * Read more:
   * - [Numeric cell type](@/guides/cell-types/numeric-cell-type/numeric-cell-type.md)
   * - [Third-party licenses](@/guides/technical-specification/third-party-licenses/third-party-licenses.md)
   *
   * @memberof Options#
   * @since 0.35.0
   * @type {object}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     // set the `type` of each cell in this column to `numeric`
   *     type: 'numeric',
   *     // set the `numericFormat` option for every `numeric` cell of this column
   *     numericFormat: {
   *       // set the number format
   *       pattern: '0,00',
   *       // set the currency format
   *       culture: 'en-US'
   *     }
   *   }
   * ],
   * ```
   */
  numericFormat: void 0,
  /**
   * If the `observeDOMVisibility` option is set to `true`,
   * Handsontable rerenders every time it detects that the grid was made visible in the DOM.
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // don't rerender the grid on visibility changes
   * observeDOMVisibility: false,
   * ```
   */
  observeDOMVisibility: !0,
  /**
   * The `outsideClickDeselects` option determines what happens to the current [selection](@/guides/cell-features/selection/selection.md)
   * when you click outside of the grid.
   *
   * You can set the `outsideClickDeselects` option to one of the following:
   *
   * | Setting          | Description                                                                                              |
   * | ---------------- | -------------------------------------------------------------------------------------------------------- |
   * | `true` (default) | On a mouse click outside of the grid, clear the current [selection](@/guides/cell-features/selection/selection.md) |
   * | `false`          | On a mouse click outside of the grid, keep the current [selection](@/guides/cell-features/selection/selection.md)  |
   * | A function       | A function that takes the click event target and returns a boolean                                       |
   *
   * @memberof Options#
   * @type {boolean|Function}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // on a mouse click outside of the grid, clear the current selection
   * outsideClickDeselects: true,
   *
   * // on a mouse click outside of the grid, keep the current selection
   * outsideClickDeselects: false,
   *
   * // take the click event target and return `false`
   * outsideClickDeselects(event) {
   *   return false;
   * }
   *
   * // take the click event target and return `true`
   * outsideClickDeselects(event) {
   *   return false;
   * }
   * ```
   */
  outsideClickDeselects: !0,
  /**
   * @description
   * The `persistentState` option configures the [`PersistentState`](@/api/persistentState.md) plugin.
   *
   * You can set the `persistentState` to one of the following:
   *
   * | Setting           | Description                                                      |
   * | ----------------- | ---------------------------------------------------------------- |
   * | `false` (default) | Disable the [`PersistentState`](@/api/persistentState.md) plugin |
   * | `true`            | Enable the [`PersistentState`](@/api/persistentState.md) plugin  |
   *
   * Read more:
   * - [Saving data: Saving data locally](@/guides/getting-started/saving-data/saving-data.md#save-data-locally)
   * - [Plugins: `PersistentState`](@/api/persistentState.md)
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category PersistentState
   *
   * @example
   * ```js
   * // enable the `PersistentState` plugin
   * persistentState: true,
   * ```
   */
  persistentState: void 0,
  /**
   * The `placeholder` option lets you display placeholder text in every empty cell.
   *
   * You can set the `placeholder` option to one of the following:
   *
   * | Setting            | Example        | Description                                                           |
   * | ------------------ | -------------- | --------------------------------------------------------------------- |
   * | A non-empty string | `'Empty cell'` | Display `Empty cell` text in empty cells                              |
   * | A non-string value | `000`          | Display `000` text in empty cells (non-string values get stringified) |
   *
   * Read more:
   * - [`placeholderCellClassName`](#placeholderCellClassName)
   *
   * @memberof Options#
   * @type {string}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // display 'Empty cell' text
   * // in every empty cell of the entire grid
   * placeholder: 'Empty cell',
   *
   * // or
   * columns: [
   *   {
   *     data: 'date',
   *     dateFormat: 'DD/MM/YYYY',
   *     // display 'Empty date cell' text
   *     // in every empty cell of the `date` column
   *     placeholder: 'Empty date cell'
   *   }
   * ],
   * ```
   */
  placeholder: void 0,
  /**
   * The `placeholderCellClassName` option lets you add a CSS class name to cells
   * that contain [`placeholder`](#placeholder) text.
   *
   * Read more:
   * - [Cell validator](@/guides/cell-functions/cell-validator/cell-validator.md)
   * - [`placeholder`](#placeholder)
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`activeHeaderClassName`](#activeHeaderClassName)
   * - [`currentColClassName`](#currentColClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`TableClassName`](#TableClassName)
   * - [`className`](#className)
   *
   * @memberof Options#
   * @type {string}
   * @default 'htPlaceholder'
   * @category Core
   *
   * @example
   * ```js
   * // add a `has-placeholder` CSS class name
   * // to each cell that contains `placeholder` text
   * placeholderCellClassName: 'has-placeholder',
   * ```
   */
  placeholderCellClassName: "htPlaceholder",
  /**
   * The `preventOverflow` option configures preventing Handsontable
   * from overflowing outside of its parent element.
   *
   * You can set the `preventOverflow` option to one of the following:
   *
   * | Setting           | Description                      |
   * | ----------------- | -------------------------------- |
   * | `false` (default) | Don't prevent overflowing        |
   * | `'horizontal'`      | Prevent horizontal overflowing |
   * | `'vertical'`        | Prevent vertical overflowing   |
   *
   * @memberof Options#
   * @type {string|boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // prevent horizontal overflowing
   * preventOverflow: 'horizontal',
   * ```
   */
  preventOverflow: !1,
  /**
   * The `preventWheel` option configures preventing the `wheel` event's default action
   * on overlays.
   *
   * You can set the `preventWheel` option to one of the following:
   *
   * | Setting           | Description                                      |
   * | ----------------- | ------------------------------------------------ |
   * | `false` (default) | Don't prevent the `wheel` event's default action |
   * | `true`            | Prevent the `wheel` event's default action       |
   *
   * @memberof Options#
   * @private
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // don't prevent the `wheel` event's default action
   * preventWheel: false,
   * ```
   */
  preventWheel: !1,
  /**
   * @description
   * The `readOnly` option determines whether a [cell](@/guides/cell-features/disabled-cells/disabled-cells.md#read-only-specific-cells),
   * [comment](@/guides/cell-features/comments/comments.md#make-a-comment-read-only), [column](@/guides/cell-features/disabled-cells/disabled-cells.md#read-only-columns)
   * or the [entire grid](@/guides/cell-features/disabled-cells/disabled-cells.md#read-only-grid) is editable or not. You can configure it as follows:
   *
   * | Setting           | Description                                                                                                                |
   * | ----------------- | ------------------------------------------------------------------------------------------------------------------------- |
   * | `false` (default) | Set as editable                                                                                                           |
   * | `true`            | - Set as read-only<br>- Add the [`readOnlyCellClassName`](#readOnlyCellClassName) CSS class name (by default: `htDimmed`) |
   *
   * `readOnly` cells can't be changed by the [`populateFromArray()`](@/api/core.md#populatefromarray) method.
   *
   * Read more:
   * - [Disabled cells](@/guides/cell-features/disabled-cells/disabled-cells.md)
   * - [Configuration options: Cascading configuration](@/guides/getting-started/configuration-options/configuration-options.md#cascading-configuration)
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // make the entire grid read-only
   * const configurationOptions = {
   *   columnSorting: true,
   * };
   *
   * // make the third column read-only
   * const configurationOptions = {
   *   columns: [
   *     {},
   *     {},
   *     {
   *       readOnly: true,
   *     },
   *   ],
   * };
   *
   * // make a specific cell read-only
   * const configurationOptions = {
   *   cell: [
   *     {
   *       row: 0,
   *       col: 0,
   *       readOnly: true,
   *     },
   * };
   * ```
   */
  readOnly: !1,
  /**
   * The `readOnlyCellClassName` option lets you add a CSS class name to [read-only](#readOnly) cells.
   *
   * Read more:
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentColClassName`](#currentColClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`activeHeaderClassName`](#activeHeaderClassName)
   * - [`invalidCellClassName`](#invalidCellClassName)
   * - [`placeholderCellClassName`](#placeholderCellClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`TableClassName`](#TableClassName)
   *
   * @memberof Options#
   * @type {string}
   * @default 'htDimmed'
   * @category Core
   *
   * @example
   * ```js
   * // add a `is-readOnly` CSS class name
   * // to every read-only cell
   * readOnlyCellClassName: 'is-readOnly',
   * ```
   */
  readOnlyCellClassName: "htDimmed",
  /**
   * The `renderAllRows` option controls Handsontable's [row virtualization](@/guides/rows/row-virtualization/row-virtualization.md).
   * You can configure it as follows:
   *
   * | Setting           | Description                                                                                                                                                                                     |
   * | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `false` (default) | Enable [row virtualization](@/guides/rows/row-virtualization/row-virtualization.md), rendering only the visible rows for optimal performance with large datasets.                                                  |
   * | `true`            | Disable [row virtualization](@/guides/rows/row-virtualization/row-virtualization.md)<br>(render all rows of the grid), rendering all rows in the dataset for consistent rendering and screen reader accessibility. |
   *
   * Setting `renderAllRows` to `true` overwrites the [`viewportRowRenderingOffset`](#viewportRowRenderingOffset) setting.
   *
   * Read more:
   * - [Row virtualization](@/guides/rows/row-virtualization/row-virtualization.md)
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // disable row virtualization
   * renderAllRows: true,
   * ```
   */
  renderAllRows: !1,
  /**
   * The `renderAllColumns` option configures Handsontable's [column virtualization](@/guides/columns/column-virtualization/column-virtualization.md).
   *
   * You can set the `renderAllColumns` option to one of the following:
   *
   * | Setting           | Description                                                                                                                                                                                                                      |
   * | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `false` (default) | Enable [column virtualization](@/guides/columns/column-virtualization/column-virtualization.md), rendering only visible columns for better performance with many columns.                                                                              |
   * | `true`            | Disable [column virtualization](@/guides/columns/column-virtualization/column-virtualization.md)<br>(render all columns of the grid), rendering all columns in the dataset, and ensuring all columns are available regardless of horizontal scrolling. |
   *
   * Setting `renderAllColumns` to `true` overwrites the [`viewportColumnRenderingOffset`](#viewportColumnRenderingOffset) setting.
   *
   * Read more:
   * - [Column virtualization](@/guides/columns/column-virtualization/column-virtualization.md)
   *
   * @since 14.1.0
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * // disable column virtualization
   * renderAllColumns: true,
   * ```
   */
  renderAllColumns: !1,
  /**
   * @description
   * The `renderer` option sets a [cell renderer](@/guides/cell-functions/cell-renderer/cell-renderer.md) for a cell.
   *
   * You can set the `renderer` option to one of the following:
   * - A custom renderer function
   * - One of the following [cell renderer aliases](@/guides/cell-functions/cell-renderer/cell-renderer.md):
   *
   * | Alias               | Cell renderer function                                                         |
   * | ------------------- | ------------------------------------------------------------------------------ |
   * | A custom alias      | Your [custom cell renderer](@/guides/cell-functions/cell-renderer/cell-renderer.md) function |
   * | `'autocomplete'`    | `AutocompleteRenderer`                                                         |
   * | `'base'`            | `BaseRenderer`                                                                 |
   * | `'checkbox'`        | `CheckboxRenderer`                                                             |
   * | `'date'`            | `DateRenderer`                                                                 |
   * | `'dropdown'`        | `DropdownRenderer`                                                             |
   * | `'html'`            | `HtmlRenderer`                                                                 |
   * | `'numeric'`         | `NumericRenderer`                                                              |
   * | `'password'`        | `PasswordRenderer`                                                             |
   * | `'text'`            | `TextRenderer`                                                                 |
   * | `'time'`            | `TimeRenderer`                                                                 |
   *
   * To set the [`renderer`](#renderer), [`editor`](#editor), and [`validator`](#validator)
   * options all at once, use the [`type`](#type) option.
   *
   * Read more:
   * - [Cell renderer](@/guides/cell-functions/cell-renderer/cell-renderer.md)
   * - [Cell type](@/guides/cell-types/cell-type/cell-type.md)
   * - [Configuration options: Cascading configuration](@/guides/getting-started/configuration-options/configuration-options.md#cascading-configuration)
   * - [`type`](#type)
   *
   * @memberof Options#
   * @type {string|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // use the `numeric` renderer for each cell of the entire grid
   * renderer: `'numeric'`,
   *
   * // add a custom renderer function
   * renderer(hotInstance, td, row, column, prop, value, cellProperties) {
   *   // your custom renderer's logic
   *   ...
   * }
   *
   * // apply the `renderer` option to individual columns
   * columns: [
   *   {
   *     // use the `autocomplete` renderer for each cell of this column
   *     renderer: 'autocomplete'
   *   },
   *   {
   *     // use the `myCustomRenderer` renderer for each cell of this column
   *     renderer: 'myCustomRenderer'
   *   }
   * ]
   * ```
   */
  renderer: void 0,
  /**
   * The `rowHeaders` option configures your grid's row headers.
   *
   * You can set the `rowHeaders` option to one of the following:
   *
   * | Setting    | Description                                                       |
   * | ---------- | ----------------------------------------------------------------- |
   * | `true`     | Enable the default row headers ('1', '2', '3', ...)               |
   * | `false`    | Disable row headers                                               |
   * | An array   | Define your own row headers (e.g. `['One', 'Two', 'Three', ...]`) |
   * | A function | Define your own row headers, using a function                     |
   *
   * Read more:
   * - [Row header](@/guides/rows/row-header/row-header.md)
   *
   * @memberof Options#
   * @type {boolean|string[]|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // enable the default row headers
   * rowHeaders: true,
   *
   * // set your own row headers
   * rowHeaders: ['One', 'Two', 'Three'],
   *
   * // set your own row headers, using a function
   * rowHeaders: function(visualRowIndex) {
   *   return `${visualRowIndex}: AB`;
   * },
   * ```
   */
  rowHeaders: void 0,
  /**
   * @description
   * The `rowHeaderWidth` option configures the width of row headers.
   *
   * You can set the `rowHeaderWidth` option to one of the following:
   *
   * | Setting  | Description                                     |
   * | -------- | ----------------------------------------------- |
   * | A number | Set the same width for every row header         |
   * | An array | Set different widths for individual row headers |
   *
   * @memberof Options#
   * @type {number|number[]}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // set the same width for every row header
   * rowHeaderWidth: 25,
   *
   * // set different widths for individual row headers
   * rowHeaderWidth: [25, 30, 55],
   * ```
   */
  rowHeaderWidth: void 0,
  /**
   * The `rowHeights` option sets rows' heights, in pixels.
   *
   * In the rendering process, the default row height is 23 px (22 px + 1 px of the row's bottom border).
   * You can change it to equal or greater than 23px, by setting the `rowHeights` option to one of the following:
   *
   * | Setting     | Description                                                                                         | Example                                                      |
   * | ----------- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
   * | A number    | Set the same height for every row                                                                   | `rowHeights: 100`                                            |
   * | A string    | Set the same height for every row                                                                   | `rowHeights: '100px'`                                        |
   * | An array    | Set heights separately for each row                                                                 | `rowHeights: [100, 120, undefined]`                          |
   * | A function  | Set row heights dynamically,<br>on each render                                                      | `rowHeights(visualRowIndex) { return visualRowIndex * 10; }` |
   * | `undefined` | Used by the [modifyRowHeight](@/api/hooks.md#modifyRowHeight) hook,<br>to detect row height changes | `rowHeights: undefined`                                      |
   *
   * The `rowHeights` option also sets the minimum row height that can be set
   * via the {@link ManualRowResize} and {@link AutoRowSize} plugins (if they are enabled).
   *
   * Read more:
   * - [Row height](@/guides/rows/row-height/row-height.md)
   *
   * @memberof Options#
   * @type {number|number[]|string|string[]|Array<undefined>|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // set every row's height to 100px
   * rowHeights: 100,
   *
   * // set every row's height to 100px
   * rowHeights: '100px',
   *
   * // set the first (by visual index) row's height to 100
   * // set the second (by visual index) row's height to 120
   * // set the third (by visual index) row's height to `undefined`
   * // set any other row's height to the default 23px
   * rowHeights: [100, 120, undefined],
   *
   * // set each row's height individually, using a function
   * rowHeights(visualRowIndex) {
   *   return visualRowIndex * 10;
   * },
   * ```
   */
  rowHeights: void 0,
  /**
   * @description
   * The `search` option configures the [`Search`](@/api/search.md) plugin.
   *
   * You can set the `search` option to one of the following:
   *
   * | Setting           | Description                                                                          |
   * | ----------------- | ------------------------------------------------------------------------------------ |
   * | `false` (default) | Disable the [`Search`](@/api/search.md) plugin                                       |
   * | `true`            | Enable the [`Search`](@/api/search.md) plugin with the default configuration         |
   * | An object         | - Enable the [`Search`](@/api/search.md) plugin<br>- Apply your custom configuration |
   *
   * If you set the `search` option to an object, you can configure the following search options:
   *
   * | Option              | Possible settings | Description                                                                                          |
   * | ------------------- | ----------------- | ---------------------------------------------------------------------------------------------------- |
   * | `searchResultClass` | A string          | Add a custom CSS class name to search results                                                        |
   * | `queryMethod`       | A function        | Add a [custom query method](@/guides/navigation/searching-values/searching-values.md#custom-query-method)  |
   * | `callback`          | A function        | Add a [custom callback function](@/guides/navigation/searching-values/searching-values.md#custom-callback) |
   *
   * Read more:
   * - [Searching values](@/guides/navigation/searching-values/searching-values.md)
   * - [Searching values: Custom query method](@/guides/navigation/searching-values/searching-values.md#custom-query-method)
   * - [Searching values: Custom callback](@/guides/navigation/searching-values/searching-values.md#custom-callback)
   *
   * @memberof Options#
   * @type {boolean|object}
   * @default false
   * @category Search
   *
   * @example
   * ```js
   * // enable the `Search` plugin with the default configuration
   * search: true,
   *
   * // enable the `Search` plugin with a custom configuration
   * search: {
   *   // add a `customClass` CSS class name to search results
   *   searchResultClass: 'customClass',
   *   // add a custom query method
   *   queryMethod(queryStr, value) {
   *     ...
   *   },
   *   // add a custom callback function
   *   callback(instance, row, column, value, result) {
   *     ...
   *   }
   * }
   * ```
   */
  search: !1,
  /**
   * @description
   * The `selectionMode` option configures how [selection](@/guides/cell-features/selection/selection.md) works.
   *
   * You can set the `selectionMode` option to one of the following:
   *
   * | Setting      | Description                                                  |
   * | ------------ | ------------------------------------------------------------ |
   * | `'single'`   | Allow the user to select only one cell at a time.            |
   * | `'range'`    | Allow the user to select one range of cells at a time.       |
   * | `'multiple'` | Allow the user to select multiple ranges of cells at a time. |
   *
   * Read more:
   * - [Selection: Selecting ranges](@/guides/cell-features/selection/selection.md#select-ranges)
   *
   * @memberof Options#
   * @type {string}
   * @default 'multiple'
   * @category Core
   *
   * @example
   * ```js
   * // you can only select one cell at at a time
   * selectionMode: 'single',
   *
   * // you can select one range of cells at a time
   * selectionMode: 'range',
   *
   * // you can select multiple ranges of cells at a time
   * selectionMode: 'multiple',
   * ```
   */
  selectionMode: "multiple",
  /**
   * The `selectOptions` option configures options that the end user can choose from in [`select`](@/guides/cell-types/select-cell-type/select-cell-type.md) cells.
   *
   * You can set the `selectOptions` option to one of the following:
   *
   * | Setting                         | Description                                                                   |
   * | ------------------------------- | ----------------------------------------------------------------------------- |
   * | An array of strings             | Each string is one option's value and label                                   |
   * | An object with key-string pairs | - Each key is one option's value<br>- The key's string is that option's label |
   * | A function                      | A function that returns an object with key-string pairs                       |
   *
   * Read more:
   * - [Select cell type](@/guides/cell-types/select-cell-type/select-cell-type.md)
   *
   * @memberof Options#
   * @type {string[]|object|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     // set the `type` of each cell in this column to `select`
   *     type: 'select',
   *     // set the first option's value and label to `A`
   *     // set the second option's value and label to `B`
   *     // set the third option's value and label to `C`
   *     selectOptions: ['A', 'B', 'C'],
   *   },
   *   {
   *     // set the `type` of each cell in this column to `select`
   *     type: 'select',
   *     selectOptions: {
   *       // set the first option's value to `value1` and label to `Label 1`
   *       value1: 'Label 1',
   *       // set the second option's value to `value2` and label to `Label 2`
   *       value2: 'Label 2',
   *       // set the third option's value to `value3` and label to `Label 3`
   *       value3: 'Label 3',
   *     },
   *   },
   *   {
   *     // set the `type` of each cell in this column to `select`
   *     type: 'select',
   *     // set `selectOption` to a function that returns available options as an object
   *     selectOptions(visualRow, visualColumn, prop) {
   *       return {
   *         value1: 'Label 1',
   *         value2: 'Label 2',
   *         value3: 'Label 3',
   *       };
   *   },
   * ],
   * ```
   */
  selectOptions: void 0,
  /**
   * @description
   * The `skipColumnOnPaste` option determines whether you can paste data into a given column.
   *
   * You can only apply the `skipColumnOnPaste` option to an entire column, using the [`columns`](#columns) option.
   *
   * You can set the `skipColumnOnPaste` option to one of the following:
   *
   * | Setting           | Description                                                                                           |
   * | ----------------- | ----------------------------------------------------------------------------------------------------- |
   * | `false` (default) | Enable pasting data into this column                                                                  |
   * | `true`            | - Disable pasting data into this column<br>- On pasting, paste data into the next column to the right |
   *
   * Read more:
   * - [Configuration options: Setting column options](@/guides/getting-started/configuration-options/configuration-options.md#set-column-options)
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     // disable pasting data into this column
   *     skipColumnOnPaste: true
   *   }
   * ],
   * ```
   */
  skipColumnOnPaste: !1,
  /**
   * @description
   *
   * The `skipRowOnPaste` option determines whether you can paste data into a given row.
   *
   * You can only apply the `skipRowOnPaste` option to an entire row, using the [`cells`](#cells) option.
   *
   * You can set the `skipRowOnPaste` option to one of the following:
   *
   * | Setting           | Description                                                                         |
   * | ----------------- | ----------------------------------------------------------------------------------- |
   * | `false` (default) | Enable pasting data into this row                                                   |
   * | `true`            | - Disable pasting data into this row<br>- On pasting, paste data into the row below |
   *
   * Read more:
   * - [Configuration options: Setting row options](@/guides/getting-started/configuration-options/configuration-options.md#set-row-options)
   *
   * @memberof Options#
   * @type {boolean}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * cells(row, column) {
   *  const cellProperties = {};
   *
   *  // disable pasting data into row 1
   *  if (row === 1) {
   *    cellProperties.skipRowOnPaste = true;
   *  }
   *
   *  return cellProperties;
   * }
   * ```
   */
  skipRowOnPaste: !1,
  /**
   * The `sortByRelevance` option configures whether [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md) cells'
   * lists are sorted in the same order as provided in the [`source`](#source) option.
   *
   * You can set the `sortByRelevance` option to one of the following:
   *
   * | Setting          | Description                                                                  |
   * | ---------------- | ---------------------------------------------------------------------------- |
   * | `true` (default) | Sort options in the same order as provided in the [`source`](#source) option |
   * | `false`          | Sort options alphabetically                                                  |
   *
   * Read more:
   * - [`source`](#source)
   * - [Autocomplete cell type](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * columns: [{
   *   // set the `type` of each cell in this column to `autocomplete`
   *   type: 'autocomplete',
   *   // set options available in every `autocomplete` cell of this column
   *   source: ['D', 'C', 'B', 'A'],
   *   // sort the `autocomplete` option in this order: D, C, B, A
   *   sortByRelevance: true
   * }],
   * ```
   */
  sortByRelevance: !0,
  /**
   * The `source` option sets options available in [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * and [`dropdown`](@/guides/cell-types/dropdown-cell-type/dropdown-cell-type.md) cells.
   *
   * You can set the `source` option to one of the following:
   *
   * - An array
   * - A function
   *
   * Read more:
   * - [Autocomplete cell type](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * - [Dropdown cell type](@/guides/cell-types/dropdown-cell-type/dropdown-cell-type.md)
   * - [`strict`](#strict)
   * - [`allowHtml`](#allowHtml)
   * - [`filter`](#filter)
   * - [`sortByRelevance`](#sortByRelevance)
   *
   * @memberof Options#
   * @type {Array|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // set `source` to an array
   * columns: [{
   *   // set the `type` of each cell in this column to `autocomplete`
   *   type: 'autocomplete',
   *   // set options available in every `autocomplete` cell of this column
   *   source: ['A', 'B', 'C', 'D']
   * }],
   *
   * // set `source` to a function
   * columns: [{
   *   // set the `type` of each cell in this column to `autocomplete`
   *   type: 'autocomplete',
   *   // for every `autocomplete` cell in this column, fetch data from an external source
   *   source(query, callback) {
   *     fetch('https://example.com/query?q=' + query, function(response) {
   *       callback(response.items);
   *     })
   *   }
   * }],
   * ```
   */
  source: void 0,
  /**
   * @description
   * If the [`data`](#data) option is not set, the `startCols` option sets the initial number of empty columns.
   *
   * The `startCols` option works only in Handsontable's constructor.
   *
   * @memberof Options#
   * @type {number}
   * @default 5
   * @category Core
   *
   * @example
   * ```js
   * // start with 15 empty columns
   * startCols: 15,
   * ```
   */
  startCols: 5,
  /**
   * @description
   * If the [`data`](#data) option is not set, the `startRows` option sets the initial number of empty rows.
   *
   * The `startRows` option works only in Handsontable's constructor.
   *
   * @memberof Options#
   * @type {number}
   * @default 5
   * @category Core
   *
   * @example
   * ```js
   * // start with 15 empty rows
   * startRows: 15,
   * ```
   */
  startRows: 5,
  /**
   * @description
   * The `stretchH` option determines what happens when the declared grid width
   * is different from the calculated sum of all column widths.
   *
   * You can set the `stretchH` option to one of the following:
   *
   * | Setting            | Description                                                       |
   * | ------------------ | ----------------------------------------------------------------- |
   * | `'none'` (default) | Don't fit the grid to the container (disable column stretching)   |
   * | `'last'`           | Fit the grid to the container, by stretching only the last column |
   * | `'all'`            | Fit the grid to the container, by stretching all columns evenly   |
   *
   * Read more:
   * - [Column width: Column stretching](@/guides/columns/column-width/column-width.md#column-stretching)
   *
   * @memberof Options#
   * @type {string}
   * @default 'none'
   * @category Core
   *
   * @example
   * ```js
   * // fit the grid to the container
   * // by stretching all columns evenly
   * stretchH: 'all',
   * ```
   */
  stretchH: "none",
  /**
   * The `strict` option configures the behavior of [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md) cells.
   *
   * You can set the `strict` option to one of the following:
   *
   * | Setting | Mode                                                                                          | Description                                                                                |
   * | ------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
   * | `true`  | [Strict mode](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md#autocomplete-strict-mode)         | The end user:<br>- Can only choose one of suggested values<br>- Can't enter a custom value |
   * | `false` | [Flexible mode](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md#autocomplete-flexible-mode)     | The end user:<br>- Can choose one of suggested values<br>- Can enter a custom value        |
   *
   * Read more:
   * - [Autocomplete cell type](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * - [`source`](#source)
   *
   * @memberof Options#
   * @type {boolean}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *   // set the `type` of each cell in this column to `autocomplete`
   *   type: 'autocomplete',
   *   // set options available in every `autocomplete` cell of this column
   *   source: ['A', 'B', 'C'],
   *   // values entered must match `A`, `B`, or `C`
   *   strict: true
   *   },
   * ],
   * ```
   */
  strict: void 0,
  /**
   * The `tableClassName` option lets you add CSS class names
   * to every Handsontable instance inside the `container` element.
   *
   * You can set the `tableClassName` option to one of the following:
   *
   * | Setting             | Description                                                                                |
   * | ------------------- | ------------------------------------------------------------------------------------------ |
   * | A string            | Add a single CSS class name to every Handsontable instance inside the `container` element  |
   * | An array of strings | Add multiple CSS class names to every Handsontable instance inside the `container` element |
   *
   * Read more:
   * - [`currentRowClassName`](#currentRowClassName)
   * - [`currentColClassName`](#currentColClassName)
   * - [`currentHeaderClassName`](#currentHeaderClassName)
   * - [`activeHeaderClassName`](#activeHeaderClassName)
   * - [`invalidCellClassName`](#invalidCellClassName)
   * - [`placeholderCellClassName`](#placeholderCellClassName)
   * - [`readOnlyCellClassName`](#readOnlyCellClassName)
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   * - [`commentedCellClassName`](#commentedCellClassName)
   * - [`className`](#className)
   *
   * @memberof Options#
   * @type {string|string[]}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // add a `your-class-name` CSS class name
   * // to every Handsontable instance inside the `container` element
   * tableClassName: 'your-class-name',
   *
   * // add `first-class-name` and `second-class-name` CSS class names
   * // to every Handsontable instance inside the `container` element
   * tableClassName: ['first-class-name', 'second-class-name'],
   * ```
   */
  tableClassName: void 0,
  /**
   * The `tabMoves` option configures the action of the <kbd>**Tab**</kbd> key.
   *
   * You can set the `tabMoves` option to an object with the following properties
   * (or to a function that returns such an object):
   *
   * | Property | Type   | Description                                                                                                                                              |
   * | -------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | `row`    | Number | - On pressing <kbd>**Tab**</kbd>, move selection `row` rows down<br>- On pressing <kbd>**Shift**</kbd>+<kbd>**Tab**</kbd>, move selection `row` rows up              |
   * | `col`    | Number | - On pressing <kbd>**Tab**</kbd>, move selection `col` columns right<br>- On pressing <kbd>**Shift**</kbd>+<kbd>**Tab**</kbd>, move selection `col` columns left     |
   *
   * @memberof Options#
   * @type {object|Function}
   * @default {row: 0, col: 1}
   * @category Core
   *
   * @example
   * ```js
   * // on pressing Tab, move selection 2 rows down and 2 columns right
   * // on pressing Shift+Tab, move selection 2 rows up and 2 columns left
   * tabMoves: {row: 2, col: 2},
   *
   * // the same setting, as a function
   * // `event` is a DOM Event object received on pressing Tab
   * // you can use it to check whether the user pressed Tab or Shift+Tab
   * tabMoves(event) {
   *   return {row: 2, col: 2};
   * },
   * ```
   */
  tabMoves: {
    row: 0,
    col: 1
  },
  /**
   * @description
   * The `title` option configures [column header](@/guides/columns/column-header/column-header.md) names.
   *
   * You can set the `title` option to a string.
   *
   * Read more:
   * - [Column header](@/guides/columns/column-header/column-header.md)
   * - [`columns`](#columns)
   *
   * @memberof Options#
   * @type {string}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     // set the first column header name to `First name`
   *     title: 'First name',
   *     type: 'text',
   *   },
   *   {
   *     // set the second column header name to `Last name`
   *     title: 'Last name',
   *     type: 'text',
   *   }
   * ],
   * ```
   */
  title: void 0,
  /**
   * The `trimDropdown` option configures the width of the [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * and [`dropdown`](@/guides/cell-types/dropdown-cell-type/dropdown-cell-type.md) lists.
   *
   * You can set the `trimDropdown` option to one of the following:
   *
   * | Setting          | Description                                                                     |
   * | ---------------- | ------------------------------------------------------------------------------- |
   * | `true` (default) | Make the dropdown/autocomplete list's width the same as the edited cell's width |
   * | `false`          | Scale the dropdown/autocomplete list's width to the list's content              |
   *
   * Read more:
   * - [Autocomplete cell type](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * - [Dropdown cell type](@/guides/cell-types/dropdown-cell-type/dropdown-cell-type.md)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     type: 'autocomplete',
   *     // for each cell of this column
   *     // make the `autocomplete` list's width the same as the edited cell's width
   *     trimDropdown: true,
   *   },
   *   {
   *     type: 'dropdown',
   *     // for each cell of this column
   *     // scale the `dropdown` list's width to the list's content
   *     trimDropdown: false,
   *   }
   * ],
   * ```
   */
  trimDropdown: !0,
  /**
   * @description
   * The `trimRows` option configures the [`TrimRows`](@/api/trimRows.md) plugin.
   *
   * You can set the `trimRows` option to one of the following:
   *
   * | Setting                          | Description                                                                                   |
   * | -------------------------------- | --------------------------------------------------------------------------------------------- |
   * | `false`                          | Disable the [`TrimRows`](@/api/trimRows.md) plugin                                            |
   * | `true`                           | Enable the [`TrimRows`](@/api/trimRows.md) plugin                                             |
   * | An array of physical row indexes | - Enable the [`TrimRows`](@/api/trimRows.md) plugin<br>- Trim selected rows at initialization |
   *
   * Read more:
   * - [Plugins: `TrimRows`](@/api/trimRows.md)
   * - [Row trimming](@/guides/rows/row-trimming/row-trimming.md)
   *
   * @memberof Options#
   * @type {boolean|number[]}
   * @default undefined
   * @category TrimRows
   *
   * @example
   * ```js
   * // enable the `TrimRows` plugin
   * trimRows: true,
   *
   * // enable the `TrimRows` plugin
   * // at Handsontable's initialization, trim rows 5, 10, and 15
   * trimRows: [5, 10, 15],
   * ```
   */
  trimRows: void 0,
  /**
   * The `trimWhitespace` option configures automatic whitespace removal. This option
   * affects the cell renderer and the cell editor.
   *
   * You can set the `trimWhitespace` option to one of the following:
   *
   * | Setting          | Description                                                     |
   * | ---------------- | --------------------------------------------------------------- |
   * | `true` (default) | Remove whitespace at the beginning and at the end of each cell |
   * | `false`          | Don't remove whitespace                                         |
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     // don't remove whitespace
   *     // from any cell of this column
   *     trimWhitespace: false
   *   }
   * ]
   * ```
   */
  trimWhitespace: !0,
  /**
   * @description
   * The `type` option lets you set the [`renderer`](#renderer), [`editor`](#editor), and [`validator`](#validator)
   * options all at once, by selecting a [cell type](@/guides/cell-types/cell-type/cell-type.md).
   *
   * You can set the `type` option to one of the following:
   *
   * | Cell type                                                         | Renderer, editor & validator                                                                                                                                                                                                                       |
   * | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   * | A [custom cell type](@/guides/cell-types/cell-type/cell-type.md)            | Renderer: your [custom cell renderer](@/guides/cell-functions/cell-renderer/cell-renderer.md)<br>Editor: your [custom cell editor](@/guides/cell-functions/cell-editor/cell-editor.md)<br>Validator: your [custom cell validator](@/guides/cell-functions/cell-validator/cell-validator.md) |
   * | [`'autocomplete'`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md) | Renderer: `AutocompleteRenderer`<br>Editor: `AutocompleteEditor`<br>Validator: `AutocompleteValidator`                                                                         |
   * | [`'checkbox'`](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md)         | Renderer: `CheckboxRenderer`<br>Editor: `CheckboxEditor`<br>Validator: -                                                                                                                               |
   * | [`'date'`](@/guides/cell-types/date-cell-type/date-cell-type.md)                 | Renderer: `DateRenderer`<br>Editor: `DateEditor`<br>Validator: `DateValidator`                                                                                                 |
   * | [`'dropdown'`](@/guides/cell-types/dropdown-cell-type/dropdown-cell-type.md)         | Renderer: `DropdownRenderer`<br>Editor: `DropdownEditor`<br>Validator: `DropdownValidator`                                                                                     |
   * | [`'handsontable'`](@/guides/cell-types/handsontable-cell-type/handsontable-cell-type.md) | Renderer: `AutocompleteRenderer`<br>Editor: `HandsontableEditor`<br>Validator: -                                                                                                                       |
   * | [`'numeric'`](@/guides/cell-types/numeric-cell-type/numeric-cell-type.md)           | Renderer: `NumericRenderer`<br>Editor: `NumericEditor`<br>Validator: `NumericValidator`                                                                                        |
   * | [`'password'`](@/guides/cell-types/password-cell-type/password-cell-type.md)         | Renderer: `PasswordRenderer`<br>Editor: `PasswordEditor`<br>Validator: -                                                                                                                               |
   * | `'text'`                                                          | Renderer: `TextRenderer`<br>Editor: `TextEditor`<br>Validator: -                                                                                                                                       |
   * | [`'time`'](@/guides/cell-types/time-cell-type/time-cell-type.md)                 | Renderer: `TimeRenderer`<br>Editor: `TimeEditor`<br>Validator: `TimeValidator`                                                                                                 |
   *
   * Read more:
   * - [Cell type](@/guides/cell-types/cell-type/cell-type.md)
   * - [Cell renderer](@/guides/cell-functions/cell-renderer/cell-renderer.md)
   * - [Cell editor](@/guides/cell-functions/cell-editor/cell-editor.md)
   * - [Cell validator](@/guides/cell-functions/cell-validator/cell-validator.md)
   * - [Configuration options: Cascading configuration](@/guides/getting-started/configuration-options/configuration-options.md#cascading-configuration)
   * - [`renderer`](#renderer)
   * - [`editor`](#editor)
   * - [`validator`](#validator)
   *
   * @memberof Options#
   * @type {string}
   * @default 'text'
   * @category Core
   *
   * @example
   * ```js
   * // set the `numeric` cell type for each cell of the entire grid
   * type: `'numeric'`,
   *
   * // apply the `type` option to individual columns
   * columns: [
   *   {
   *     // set the `autocomplete` cell type for each cell of this column
   *     type: 'autocomplete'
   *   },
   *   {
   *     // set the `myCustomCellType` cell type for each cell of this column
   *     type: 'myCustomCellType'
   *   }
   * ]
   * ```
   */
  type: "text",
  /**
   * The `uncheckedTemplate` option lets you configure what value
   * an unchecked [`checkbox`](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md) cell has.
   *
   * You can set the `uncheckedTemplate` option to one of the following:
   *
   * | Setting           | Description                                                                                                                                                                                |
   * | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
   * | `false` (default) | If a [`checkbox`](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md) cell is unchecked,<br>the [`getDataAtCell`](@/api/core.md#getDataAtCell) method for this cell returns `false`                 |
   * | A string          | If a [`checkbox`](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md) cell is unchecked,<br>the [`getDataAtCell`](@/api/core.md#getDataAtCell) method for this cell returns a string of your choice |
   *
   * Read more:
   * - [Checkbox cell type: Checkbox template](@/guides/cell-types/checkbox-cell-type/checkbox-cell-type.md#checkbox-template)
   * - [`getDataAtCell()`](@/api/core.md#getDataAtCell)
   * - [`checkedTemplate`](#checkedTemplate)
   *
   * @memberof Options#
   * @type {boolean|string|number}
   * @default false
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     // set the `type` of each cell in this column to `checkbox`
   *     // when unchecked, the cell's value is `false`
   *     // when checked, the cell's value is `true`
   *     type: 'checkbox',
   *   },
   *   {
   *     // set the `type` of each cell in this column to `checkbox`
   *     // when unchecked, the cell's value is `'No'`
   *     // when checked, the cell's value is `'Yes'`
   *     type: 'checkbox',
   *     uncheckedTemplate: 'No'
   *     checkedTemplate: 'Yes',
   *  }
   * ],
   * ```
   */
  uncheckedTemplate: void 0,
  /**
   * The `undo` option configures the [`UndoRedo`](@/api/undoRedo.md) plugin.
   *
   * You can set the `undo` option to one of the following:
   *
   * | Setting | Description                                        |
   * | ------- | -------------------------------------------------- |
   * | `true`  | Enable the [`UndoRedo`](@/api/undoRedo.md) plugin  |
   * | `false` | Disable the [`UndoRedo`](@/api/undoRedo.md) plugin |
   *
   * By default, the `undo` option is set to `undefined`,
   * but the [`UndoRedo`](@/api/undoRedo.md) plugin acts as enabled.
   * To disable the [`UndoRedo`](@/api/undoRedo.md) plugin completely,
   * set the `undo` option to `false`.
   *
   * Read more:
   * - [Undo and redo](@/guides/accessories-and-menus/undo-redo/undo-redo.md)
   *
   * @memberof Options#
   * @type {boolean}
   * @default undefined
   * @category UndoRedo
   *
   * @example
   * ```js
   * // enable the `UndoRedo` plugin
   * undo: true,
   * ```
   */
  undo: void 0,
  /**
   * @description
   * The `validator` option sets a [cell validator](@/guides/cell-functions/cell-validator/cell-validator.md) for a cell.
   *
   * You can set the `validator` option to one of the following:
   *
   * | Setting              | Description                                                                      |
   * | -------------------- | -------------------------------------------------------------------------------- |
   * | A string             | A [cell validator alias](@/guides/cell-functions/cell-validator/cell-validator.md)              |
   * | A function           | Your [custom cell validator function](@/guides/cell-functions/cell-validator/cell-validator.md) |
   * | A regular expression | A regular expression used for cell validation                                    |
   *
   * By setting the `validator` option to a string,
   * you can use one of the following [cell validator aliases](@/guides/cell-functions/cell-validator/cell-validator.md):
   *
   * | Alias               | Cell validator function                                                 |
   * | ------------------- | ----------------------------------------------------------------------- |
   * | A custom alias      | Your [custom cell validator](@/guides/cell-functions/cell-validator/cell-validator.md) |
   * | `'autocomplete'`    | `AutocompleteValidator`                                                 |
   * | `'date'`            | `DateValidator`                                                         |
   * | `'dropdown'`        | `DropdownValidator`                                                     |
   * | `'numeric'`         | `NumericValidator`                                                      |
   * | `'time'`            | `TimeValidator`                                                         |
   *
   * To set the [`editor`](#editor), [`renderer`](#renderer), and [`validator`](#validator)
   * options all at once, use the [`type`](#type) option.
   *
   * Read more:
   * - [Cell validator](@/guides/cell-functions/cell-validator/cell-validator.md)
   * - [Cell type](@/guides/cell-types/cell-type/cell-type.md)
   * - [Configuration options: Cascading configuration](@/guides/getting-started/configuration-options/configuration-options.md#cascading-configuration)
   * - [`type`](#type)
   *
   * @memberof Options#
   * @type {Function|RegExp|string}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *    {
   *      // use a built-in `numeric` cell validator
   *      validator: 'numeric'
   *    },
   *    {
   *      // validate against a regular expression
   *      validator: /^[0-9]$/
   *    },
   *    {
   *      // add a custom cell validator function
   *      validator(value, callback) {
   *          ...
   *      }
   *    },
   * ],
   * ```
   */
  validator: void 0,
  /**
   * @description
   * The `viewportColumnRenderingOffset` option configures the number of columns
   * to be rendered outside of the grid's viewport.
   *
   * You can set the `viewportColumnRenderingOffset` option to one of the following:
   *
   * | Setting            | Description                                             |
   * | ------------------ | ------------------------------------------------------- |
   * | `auto` (default)   | Use the offset calculated automatically by Handsontable |
   * | A number           | Set the offset manually                                 |
   *
   * The `viewportColumnRenderingOffset` setting is ignored when [`renderAllColumns`](#renderAllColumns) is set to `true`.
   *
   * Read more:
   * - [Performance: Define the number of pre-rendered rows and columns](@/guides/optimization/performance/performance.md#define-the-number-of-pre-rendered-rows-and-columns)
   *
   * @memberof Options#
   * @type {number|string}
   * @default 'auto'
   * @category Core
   *
   * @example
   * ```js
   * // render 70 columns outside of the grid's viewport
   * viewportColumnRenderingOffset: 70,
   * ```
   */
  viewportColumnRenderingOffset: "auto",
  /**
   * @description
   * The `viewportRowRenderingOffset` option configures the number of rows
   * to be rendered outside of the grid's viewport.
   *
   * You can set the `viewportRowRenderingOffset` option to one of the following:
   *
   * | Setting            | Description                                             |
   * | ------------------ | ------------------------------------------------------- |
   * | `auto` (default)   | Use the offset calculated automatically by Handsontable |
   * | A number           | Set the offset manually                                 |
   *
   * The `viewportRowRenderingOffset` setting is ignored when [`renderAllRows`](#renderAllRows) is set to `true`.
   *
   * Read more:
   * - [Performance: Define the number of pre-rendered rows and columns](@/guides/optimization/performance/performance.md#define-the-number-of-pre-rendered-rows-and-columns)
   * - [Column virtualization](@/guides/columns/column-virtualization/column-virtualization.md)
   *
   * @memberof Options#
   * @type {number|string}
   * @default 'auto'
   * @category Core
   *
   * @example
   * ```js
   * // render 70 rows outside of the grid's viewport
   * viewportRowRenderingOffset: 70,
   * ```
   */
  viewportRowRenderingOffset: "auto",
  /**
   * The `visibleRows` option sets the height of the [`autocomplete`](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * and [`dropdown`](@/guides/cell-types/dropdown-cell-type/dropdown-cell-type.md) lists.
   *
   * When the number of list options exceeds the `visibleRows` number, a scrollbar appears.
   *
   * Read more:
   * - [Autocomplete cell type](@/guides/cell-types/autocomplete-cell-type/autocomplete-cell-type.md)
   * - [Dropdown cell type](@/guides/cell-types/dropdown-cell-type/dropdown-cell-type.md)
   *
   * @memberof Options#
   * @type {number}
   * @default 10
   * @category Core
   *
   * @example
   * ```js
   * columns: [
   *   {
   *     type: 'autocomplete',
   *     // set the `autocomplete` list's height to 15 options
   *     // for each cell of this column
   *     visibleRows: 15,
   *   },
   *   {
   *     type: 'dropdown',
   *     // set the `dropdown` list's height to 5 options
   *     // for each cell of this column
   *     visibleRows: 5,
   *   }
   * ],
   * ```
   */
  visibleRows: 10,
  /**
   * The `width` option configures the width of your grid.
   *
   * You can set the `width` option to one of the following:
   *
   * | Setting                                                                    | Example                   |
   * | -------------------------------------------------------------------------- | ------------------------- |
   * | A number of pixels                                                         | `width: 500`              |
   * | A string with a [CSS unit](https://www.w3schools.com/cssref/css_units.asp) | `width: '75vw'`           |
   * | A function that returns a valid number or string                           | `width() { return 500; }` |
   *
   * Read more:
   * - [Grid size](@/guides/getting-started/grid-size/grid-size.md)
   *
   * @memberof Options#
   * @type {number|string|Function}
   * @default undefined
   * @category Core
   *
   * @example
   * ```js
   * // set the grid's width to 500px
   * width: 500,
   *
   * // set the grid's width to 75vw
   * width: '75vw',
   *
   * // set the grid's width to 500px, using a function
   * width() {
   *   return 500;
   * },
   * ```
   */
  width: void 0,
  /**
   * The `wordWrap` option configures whether content that exceeds a column's width is wrapped or not.
   *
   * You can set the `wordWrap` option to one of the following:
   *
   * | Setting          | Description                                             |
   * | ---------------- | ------------------------------------------------------- |
   * | `true` (default) | If content exceeds the column's width, wrap the content |
   * | `false`          | Don't wrap content                                      |
   *
   * To style cells that don't wrap content, use the [`noWordWrapClassName`](#noWordWrapClassName) option.
   *
   * Read more:
   * - [`noWordWrapClassName`](#noWordWrapClassName)
   *
   * @memberof Options#
   * @type {boolean}
   * @default true
   * @category Core
   *
   * @example
   * ```js
   * // set column width for every column of the entire grid
   * colWidths: 100,
   *
   * columns: [
   *   {
   *     // don't wrap content in this column
   *     wordWrap: false,
   *   },
   *   {
   *     // if content exceeds this column's width, wrap the content
   *     wordWrap: true,
   *   }
   * ],
   * ```
   */
  wordWrap: !0
  /* eslint-enable jsdoc/require-description-complete-sentence */
});
function ST(t, e, n) {
  return (e = tq(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function tq(t) {
  var e = nq(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function nq(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function iq() {
  return class {
  };
}
class rq {
  constructor(e) {
    ST(this, "metaCtor", iq()), ST(this, "meta", void 0), this.meta = this.metaCtor.prototype, nn(this.meta, E1()), this.meta.instance = e;
  }
  /**
   * Gets constructor of the global meta object. Necessary for inheritance for creating the next meta layers.
   *
   * @returns {Function}
   */
  getMetaConstructor() {
    return this.metaCtor;
  }
  /**
   * Gets settings object for this layer.
   *
   * @returns {object}
   */
  getMeta() {
    return this.meta;
  }
  /**
   * Updates global settings object by merging settings with the current state.
   *
   * @param {object} settings An object to merge with.
   */
  updateMeta(e) {
    var n;
    nn(this.meta, e), Xw(this.meta, On(Xe({}, e), {
      type: (n = e.type) !== null && n !== void 0 ? n : this.meta.type
    }), e);
  }
}
function oq(t, e, n) {
  return (e = sq(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function sq(t) {
  var e = aq(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function aq(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class lq {
  constructor(e) {
    oq(this, "meta", void 0);
    const n = e.getMetaConstructor();
    this.meta = new n();
  }
  /**
   * Gets settings object for this layer.
   *
   * @returns {TableMeta}
   */
  getMeta() {
    return this.meta;
  }
  /**
   * Updates table settings object by merging settings with the current state.
   *
   * @param {object} settings An object to merge with.
   */
  updateMeta(e) {
    nn(this.meta, e), Xw(this.meta, e, e);
  }
}
function _f(t, e, n) {
  return (e = uq(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function uq(t) {
  var e = cq(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function cq(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Em {
  constructor(e) {
    _f(this, "valueFactory", void 0), _f(this, "data", []), _f(this, "index", []), _f(this, "holes", /* @__PURE__ */ new Set()), this.valueFactory = e;
  }
  /**
   * Gets or if data not exist creates and returns new data.
   *
   * @param {number} key The item key as zero-based index.
   * @returns {*}
   */
  obtain(e) {
    zg(() => Gg(e), "Expecting an unsigned number.");
    const n = this._getStorageIndexByKey(e);
    let i;
    if (n >= 0)
      i = this.data[n], i === void 0 && (i = this.valueFactory(e), this.data[n] = i);
    else if (i = this.valueFactory(e), this.holes.size > 0) {
      const r = this.holes.values().next().value;
      this.holes.delete(r), this.data[r] = i, this.index[e] = r;
    } else
      this.data.push(i), this.index[e] = this.data.length - 1;
    return i;
  }
  /**
   * Inserts an empty data to the map. This method creates an empty space for obtaining
   * new data.
   *
   * @param {number} key The key as volatile zero-based index at which to begin inserting space for new data.
   * @param {number} [amount=1] Ammount of data to insert.
   */
  insert(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1;
    zg(() => Gg(e) || Hf(e), "Expecting an unsigned number or null/undefined argument.");
    const i = [], r = this.data.length;
    for (let s = 0; s < n; s++)
      i.push(r + s), this.data.push(void 0);
    const o = Hf(e) ? this.index.length : e;
    this.index = [...this.index.slice(0, o), ...i, ...this.index.slice(o)];
  }
  /**
   * Removes (soft remove) data from "index" and according to the amount of data.
   *
   * @param {number} key The key as volatile zero-based index at which to begin removing the data.
   * @param {number} [amount=1] Ammount data to remove.
   */
  remove(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1;
    zg(() => Gg(e) || Hf(e), "Expecting an unsigned number or null/undefined argument.");
    const i = this.index.splice(Hf(e) ? this.index.length - n : e, n);
    for (let r = 0; r < i.length; r++) {
      const o = i[r];
      typeof o == "number" && this.holes.add(o);
    }
  }
  /**
   * Returns the size of the data which this map holds.
   *
   * @returns {number}
   */
  size() {
    return this.data.length - this.holes.size;
  }
  /**
   * Returns a new Iterator object that contains the values for each item in the LazyMap object.
   *
   * @returns {Iterator}
   */
  values() {
    return Zo(this.data, (e, n) => !this.holes.has(n))[Symbol.iterator]();
  }
  /**
   * Returns a new Iterator object that contains an array of `[index, value]` for each item in the LazyMap object.
   *
   * @returns {Iterator}
   */
  entries() {
    const e = [];
    for (let i = 0; i < this.data.length; i++) {
      const r = this._getKeyByStorageIndex(i);
      r !== -1 && e.push([r, this.data[i]]);
    }
    let n = 0;
    return {
      next: () => {
        if (n < e.length) {
          const i = e[n];
          return n += 1, {
            value: i,
            done: !1
          };
        }
        return {
          done: !0
        };
      }
    };
  }
  /**
   * Clears the map.
   */
  clear() {
    this.data = [], this.index = [], this.holes.clear();
  }
  /**
   * Gets storage index calculated from the key associated with the specified value.
   *
   * @param {number} key Volatile zero-based index.
   * @returns {number} Returns index 0-N or -1 if no storage index found.
   */
  _getStorageIndexByKey(e) {
    return this.index.length > e ? this.index[e] : -1;
  }
  /**
   * Gets the key associated with the specified value calculated from storage index.
   *
   * @param {number} dataIndex Zero-based storage index.
   * @returns {number} Returns index 0-N or -1 if no key found.
   */
  _getKeyByStorageIndex(e) {
    return this.index.indexOf(e);
  }
  /**
   * Makes this object iterable.
   *
   * @returns {Iterator}
   */
  [Symbol.iterator]() {
    return this.entries();
  }
}
function bT(t, e, n) {
  return (e = hq(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function hq(t) {
  var e = dq(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function dq(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const fq = ["data", "width"];
class gq {
  constructor(e) {
    bT(this, "globalMeta", void 0), bT(this, "metas", new Em(() => this._createMeta())), this.globalMeta = e, this.metas = new Em(() => this._createMeta());
  }
  /**
   * Updates column meta object by merging settings with the current state.
   *
   * @param {number} physicalColumn The physical column index which points what column meta object is updated.
   * @param {object} settings An object to merge with.
   */
  updateMeta(e, n) {
    const i = this.getMeta(e);
    nn(i, n), Xw(i, n);
  }
  /**
   * Creates one or more columns at specific position.
   *
   * @param {number} physicalColumn The physical column index which points from what position the column is added.
   * @param {number} amount An amount of columns to add.
   */
  createColumn(e, n) {
    this.metas.insert(e, n);
  }
  /**
   * Removes one or more columns from the collection.
   *
   * @param {number} physicalColumn The physical column index which points from what position the column is removed.
   * @param {number} amount An amount columns to remove.
   */
  removeColumn(e, n) {
    this.metas.remove(e, n);
  }
  /**
   * Gets settings object for this layer.
   *
   * @param {number} physicalColumn The physical column index.
   * @returns {object}
   */
  getMeta(e) {
    return this.metas.obtain(e);
  }
  /**
   * Gets constructor of the column meta object. Necessary for inheritance - creating the next meta layers.
   *
   * @param {number} physicalColumn The physical column index.
   * @returns {Function}
   */
  getMetaConstructor(e) {
    return this.metas.obtain(e).constructor;
  }
  /**
   * Clears all saved column meta objects.
   */
  clearCache() {
    this.metas.clear();
  }
  /**
   * Creates and returns new column meta object with properties inherited from the global meta layer.
   *
   * @private
   * @returns {object}
   */
  _createMeta() {
    return eq(this.globalMeta.getMetaConstructor(), fq).prototype;
  }
}
function RT(t, e, n) {
  return (e = mq(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function mq(t) {
  var e = pq(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function pq(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class wq {
  constructor(e) {
    RT(this, "columnMeta", void 0), RT(this, "metas", new Em(() => this._createRow())), this.columnMeta = e;
  }
  /**
   * Updates cell meta object by merging settings with the current state.
   *
   * @param {number} physicalRow The physical row index which points what cell meta object is updated.
   * @param {number} physicalColumn The physical column index which points what cell meta object is updated.
   * @param {object} settings An object to merge with.
   */
  updateMeta(e, n, i) {
    const r = this.getMeta(e, n);
    nn(r, i), Xw(r, i);
  }
  /**
   * Creates one or more rows at specific position.
   *
   * @param {number} physicalRow The physical row index which points from what position the row is added.
   * @param {number} amount An amount of rows to add.
   */
  createRow(e, n) {
    this.metas.insert(e, n);
  }
  /**
   * Creates one or more columns at specific position.
   *
   * @param {number} physicalColumn The physical column index which points from what position the column is added.
   * @param {number} amount An amount of columns to add.
   */
  createColumn(e, n) {
    for (let i = 0; i < this.metas.size(); i++)
      this.metas.obtain(i).insert(e, n);
  }
  /**
   * Removes one or more rows from the collection.
   *
   * @param {number} physicalRow The physical row index which points from what position the row is removed.
   * @param {number} amount An amount of rows to remove.
   */
  removeRow(e, n) {
    this.metas.remove(e, n);
  }
  /**
   * Removes one or more columns from the collection.
   *
   * @param {number} physicalColumn The physical column index which points from what position the column is removed.
   * @param {number} amount An amount of columns to remove.
   */
  removeColumn(e, n) {
    for (let i = 0; i < this.metas.size(); i++)
      this.metas.obtain(i).remove(e, n);
  }
  /**
   * Gets settings object for this layer.
   *
   * @param {number} physicalRow The physical row index.
   * @param {number} physicalColumn The physical column index.
   * @param {string} [key] If the key exists its value will be returned, otherwise the whole cell meta object.
   * @returns {object}
   */
  getMeta(e, n, i) {
    const r = this.metas.obtain(e).obtain(n);
    return i === void 0 ? r : r[i];
  }
  /**
   * Sets settings object for this layer defined by "key" property.
   *
   * @param {number} physicalRow The physical row index.
   * @param {number} physicalColumn The physical column index.
   * @param {string} key The property name to set.
   * @param {*} value Value to save.
   */
  setMeta(e, n, i, r) {
    var o;
    const s = this.metas.obtain(e).obtain(n);
    (o = s._automaticallyAssignedMetaProps) === null || o === void 0 || o.delete(i), s[i] = r;
  }
  /**
   * Removes a property defined by the "key" argument from the cell meta object.
   *
   * @param {number} physicalRow The physical row index.
   * @param {number} physicalColumn The physical column index.
   * @param {string} key The property name to remove.
   */
  removeMeta(e, n, i) {
    const r = this.metas.obtain(e).obtain(n);
    delete r[i];
  }
  /**
   * Returns all cell meta objects that were created during the Handsontable operation. As cell meta
   * objects are created lazy, the length of the returned collection depends on how and when the
   * table has asked for access to that meta objects.
   *
   * @returns {object[]}
   */
  getMetas() {
    const e = [], n = Array.from(this.metas.values());
    for (let i = 0; i < n.length; i++)
      de(n[i]) && e.push(...n[i].values());
    return e;
  }
  /**
   * Returns all cell meta objects that were created during the Handsontable operation but for
   * specific row index.
   *
   * @param {number} physicalRow The physical row index.
   * @returns {object[]}
   */
  getMetasAtRow(e) {
    zg(() => Gg(e), "Expecting an unsigned number.");
    const n = new Map(this.metas);
    return n.has(e) ? Array.from(n.get(e).values()) : [];
  }
  /**
   * Clears all saved cell meta objects.
   */
  clearCache() {
    this.metas.clear();
  }
  /**
   * Creates and returns new structure for cell meta objects stored in columnar axis.
   *
   * @private
   * @returns {object}
   */
  _createRow() {
    return new Em((e) => this._createMeta(e));
  }
  /**
   * Creates and returns new cell meta object with properties inherited from the column meta layer.
   *
   * @private
   * @param {number} physicalColumn The physical column index.
   * @returns {object}
   */
  _createMeta(e) {
    const n = this.columnMeta.getMetaConstructor(e);
    return new n();
  }
}
class T1 {
  constructor(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : [];
    this.hot = e, this.globalMeta = new rq(e), this.tableMeta = new lq(this.globalMeta), this.columnMeta = new gq(this.globalMeta), this.cellMeta = new wq(this.columnMeta), i.forEach((r) => new r(this)), this.globalMeta.updateMeta(n);
  }
  /**
   * Gets the global meta object that is a root of all default settings, which are recognizable by Handsontable.
   * Other layers inherites all properties from this. Adding, removing, or changing property in that
   * object has a direct reflection to all layers.
   *
   * @returns {object}
   */
  getGlobalMeta() {
    return this.globalMeta.getMeta();
  }
  /**
   * Updates global settings object by merging settings with the current state.
   *
   * @param {object} settings An object to merge with.
   */
  updateGlobalMeta(e) {
    this.globalMeta.updateMeta(e);
  }
  /**
   * Gets settings object that was passed in the Handsontable constructor. That layer contains all
   * default settings inherited from the GlobalMeta layer merged with settings passed by the developer.
   * Adding, removing, or changing property in that object has no direct reflection on any other layers.
   *
   * @returns {TableMeta}
   */
  getTableMeta() {
    return this.tableMeta.getMeta();
  }
  /**
   * Updates table settings object by merging settings with the current state.
   *
   * @param {object} settings An object to merge with.
   */
  updateTableMeta(e) {
    this.tableMeta.updateMeta(e);
  }
  /**
   * Gets column meta object that is a root of all settings defined in the column property of the Handsontable
   * settings. Each column in the Handsontable is associated with a unique meta object which identified by
   * the physical column index. Adding, removing, or changing property in that object has a direct reflection
   * only for the CellMeta layer. The reflection will be visible only if the property doesn't exist in the lower
   * layers (prototype lookup).
   *
   * @param {number} physicalColumn The physical column index.
   * @returns {object}
   */
  getColumnMeta(e) {
    return this.columnMeta.getMeta(e);
  }
  /**
   * Updates column meta object by merging settings with the current state.
   *
   * @param {number} physicalColumn The physical column index which points what column meta object is updated.
   * @param {object} settings An object to merge with.
   */
  updateColumnMeta(e, n) {
    this.columnMeta.updateMeta(e, n);
  }
  /**
   * Gets the cell meta object that is a root of all settings defined for the specific cell rendered by
   * the Handsontable. Each cell meta inherits settings from higher layers. When a property doesn't
   * exist in that layer, it is looked up through a prototype to the highest layer. Starting
   * from CellMeta -> ColumnMeta and ending to GlobalMeta, which stores default settings. Adding,
   * removing, or changing property in that object has no direct reflection on any other layers.
   *
   * @param {number} physicalRow The physical row index.
   * @param {number} physicalColumn The physical column index.
   * @param {object} options Additional options that are used to extend the cell meta object.
   * @param {number} options.visualRow The visual row index of the currently requested cell meta object.
   * @param {number} options.visualColumn The visual column index of the currently requested cell meta object.
   * @returns {object}
   */
  getCellMeta(e, n, i) {
    let {
      visualRow: r,
      visualColumn: o
    } = i;
    const s = this.cellMeta.getMeta(e, n);
    return s.visualRow = r, s.visualCol = o, s.row = e, s.col = n, this.runLocalHooks("afterGetCellMeta", s), s;
  }
  /**
   * Gets a value (defined by the `key` property) from the cell meta object.
   *
   * @param {number} physicalRow The physical row index.
   * @param {number} physicalColumn The physical column index.
   * @param {string} key Defines the value that will be returned from the cell meta object.
   * @returns {*}
   */
  getCellMetaKeyValue(e, n, i) {
    if (typeof i != "string")
      throw new Error("The passed cell meta object key is not a string");
    return this.cellMeta.getMeta(e, n, i);
  }
  /**
   * Sets settings object for cell meta object defined by "key" property.
   *
   * @param {number} physicalRow The physical row index.
   * @param {number} physicalColumn The physical column index.
   * @param {string} key The property name to set.
   * @param {*} value Value to save.
   */
  setCellMeta(e, n, i, r) {
    this.cellMeta.setMeta(e, n, i, r);
  }
  /**
   * Updates cell meta object by merging settings with the current state.
   *
   * @param {number} physicalRow The physical row index which points what cell meta object is updated.
   * @param {number} physicalColumn The physical column index which points what cell meta object is updated.
   * @param {object} settings An object to merge with.
   */
  updateCellMeta(e, n, i) {
    this.cellMeta.updateMeta(e, n, i);
  }
  /**
   * Removes a property defined by the "key" argument from the cell meta object.
   *
   * @param {number} physicalRow The physical row index.
   * @param {number} physicalColumn The physical column index.
   * @param {string} key The property name to remove.
   */
  removeCellMeta(e, n, i) {
    this.cellMeta.removeMeta(e, n, i);
  }
  /**
   * Returns all cell meta objects that were created during the Handsontable operation. As cell meta
   * objects are created lazy, the length of the returned collection depends on how and when the
   * table has asked for access to that meta objects.
   *
   * @returns {object[]}
   */
  getCellsMeta() {
    return this.cellMeta.getMetas();
  }
  /**
   * Returns all cell meta objects that were created during the Handsontable operation but for
   * specific row index.
   *
   * @param {number} physicalRow The physical row index.
   * @returns {object[]}
   */
  getCellsMetaAtRow(e) {
    return this.cellMeta.getMetasAtRow(e);
  }
  /**
   * Creates one or more rows at specific position.
   *
   * @param {number} physicalRow The physical row index which points from what position the row is added.
   * @param {number} [amount=1] An amount of rows to add.
   */
  createRow(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1;
    this.cellMeta.createRow(e, n);
  }
  /**
   * Removes one or more rows from the collection.
   *
   * @param {number} physicalRow The physical row index which points from what position the row is removed.
   * @param {number} [amount=1] An amount rows to remove.
   */
  removeRow(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1;
    this.cellMeta.removeRow(e, n);
  }
  /**
   * Creates one or more columns at specific position.
   *
   * @param {number} physicalColumn The physical column index which points from what position the column is added.
   * @param {number} [amount=1] An amount of columns to add.
   */
  createColumn(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1;
    this.cellMeta.createColumn(e, n), this.columnMeta.createColumn(e, n);
  }
  /**
   * Removes one or more columns from the collection.
   *
   * @param {number} physicalColumn The physical column index which points from what position the column is removed.
   * @param {number} [amount=1] An amount of columns to remove.
   */
  removeColumn(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1;
    this.cellMeta.removeColumn(e, n), this.columnMeta.removeColumn(e, n);
  }
  /**
   * Clears all saved cell meta objects. It keeps column meta, table meta, and global meta intact.
   */
  clearCellsCache() {
    this.cellMeta.clearCache();
  }
  /**
   * Clears all saved cell and columns meta objects.
   */
  clearCache() {
    this.cellMeta.clearCache(), this.columnMeta.clearCache();
  }
}
pt(T1, Vn);
function ET(t, e, n, i) {
  const {
    hotInstance: r,
    dataMap: o,
    dataSource: s,
    internalSource: a,
    source: l,
    metaManager: u,
    firstRun: c
  } = i, h = Bn(a), d = r.getSettings();
  Array.isArray(d.dataSchema) ? r.dataType = "array" : Et(d.dataSchema) ? r.dataType = "function" : r.dataType = "object", o && o.destroy(), t = r.runHooks(`before${h}`, t, c, l);
  const g = new Q4(r, t, u);
  if (e(g), typeof t == "object" && t !== null)
    t.push && t.splice || (t = [t]);
  else if (t === null) {
    const m = g.getSchema();
    t = [];
    let w, C = 0, S = 0;
    for (C = 0, S = d.startRows; C < S; C++)
      if ((r.dataType === "object" || r.dataType === "function") && d.dataSchema)
        w = Fn(m), t.push(w);
      else if (r.dataType === "array")
        w = Fn(m[0]), t.push(w);
      else {
        w = [];
        for (let b = 0, R = d.startCols; b < R; b++)
          w.push(null);
        t.push(w);
      }
  } else
    throw new Error(`${a} only accepts array of objects or array of arrays (${typeof t} given)`);
  Array.isArray(t[0]) && (r.dataType = "array"), d.data = t, g.dataSource = t, s.data = t, s.dataType = r.dataType, s.colToProp = g.colToProp.bind(g), s.propToCol = g.propToCol.bind(g), s.countCachedColumns = g.countCachedColumns.bind(g), n(g), r.runHooks(`after${h}`, t, c, l), c || (r.runHooks("afterChange", null, a), r.render()), r.getSettings().ariaTags && Ee(r.rootElement, [
    HP(-1),
    // If run after initialization, add the number of row headers.
    bp(r.countCols() + (r.view ? r.countRowHeaders() : 0))
  ]);
}
function TT(t, e, n) {
  return (e = Aq(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Aq(t) {
  var e = Cq(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Cq(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class vq {
  constructor(e) {
    TT(this, "metaManager", void 0), TT(this, "metaSyncMemo", /* @__PURE__ */ new Map()), this.metaManager = e, e.addLocalHook("afterGetCellMeta", (n) => this.extendCellMeta(n)), he.getSingleton().add("beforeRender", (n) => {
      n && this.metaSyncMemo.clear();
    }, this.metaManager.hot);
  }
  /**
   * Extends the cell meta object by user-specific properties.
   *
   * The cell meta object can be extended dynamically,
   * either by Handsontable's hooks (`beforeGetCellMeta` and`afterGetCellMeta`),
   * or by Handsontable's `cells` option.
   *
   * To boost performance, the extending process is triggered only once per one slow Handsontable render cycle.
   *
   * @param {object} cellMeta The cell meta object.
   */
  extendCellMeta(e) {
    var n;
    const {
      row: i,
      col: r
    } = e;
    if ((n = this.metaSyncMemo.get(i)) !== null && n !== void 0 && n.has(r))
      return;
    const {
      visualRow: o,
      visualCol: s
    } = e, a = this.metaManager.hot, l = a.colToProp(s);
    e.prop = l, a.runHooks("beforeGetCellMeta", o, s, e);
    const u = Bt(e, "type") ? e.type : null;
    let c = Et(e.cells) ? e.cells(i, r, l) : null;
    if (u)
      if (c) {
        var h;
        c.type = (h = c.type) !== null && h !== void 0 ? h : u;
      } else
        c = {
          type: u
        };
    c && this.metaManager.updateCellMeta(i, r, c), a.runHooks("afterGetCellMeta", o, s, e), this.metaSyncMemo.has(i) || this.metaSyncMemo.set(i, /* @__PURE__ */ new Set()), this.metaSyncMemo.get(i).add(r);
  }
}
function yq(t, e, n) {
  Sq(t, e), e.set(t, n);
}
function Sq(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function SC(t, e, n) {
  return (e = bq(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function bq(t) {
  var e = Rq(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Rq(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Eq(t, e) {
  return t.get(Tq(t, e));
}
function Tq(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var MT = /* @__PURE__ */ new WeakMap();
class Mq {
  constructor(e) {
    SC(this, "metaManager", void 0), SC(this, "usageTracker", /* @__PURE__ */ new Set()), SC(this, "propDescriptors", /* @__PURE__ */ new Map([["ariaTags", {
      initOnly: !0
    }], ["fixedColumnsLeft", {
      target: "fixedColumnsStart",
      onChange(n) {
        if (this.metaManager.hot.isRtl() && n === "fixedColumnsLeft")
          throw new Error("The `fixedColumnsLeft` is not supported for RTL. Please use option `fixedColumnsStart`.");
        if (this.usageTracker.has("fixedColumnsLeft") && this.usageTracker.has("fixedColumnsStart"))
          throw new Error("The `fixedColumnsLeft` and `fixedColumnsStart` should not be used together. Please use only the option `fixedColumnsStart`.");
      }
    }], ["layoutDirection", {
      initOnly: !0
    }], ["renderAllColumns", {
      initOnly: !0
    }], ["renderAllRows", {
      initOnly: !0
    }]])), yq(this, MT, (n, i, r) => {
      if (!r)
        throw new Error(`The \`${n}\` option can not be updated after the Handsontable is initialized.`);
    }), this.metaManager = e, this.extendMetaProps();
  }
  /**
   * Extends the meta options based on the object descriptors from the `propDescriptors` list.
   */
  extendMetaProps() {
    this.propDescriptors.forEach((e, n) => {
      const {
        initOnly: i,
        target: r,
        onChange: o
      } = e, s = typeof r == "string", a = s ? r : n, l = `_${a}`;
      this.metaManager.globalMeta.meta[l] = this.metaManager.globalMeta.meta[a], o ? (this.installPropWatcher(n, l, o), s && this.installPropWatcher(r, l, o)) : i && (this.installPropWatcher(n, l, Eq(MT, this)), this.metaManager.globalMeta.meta._initOnlySettings || (this.metaManager.globalMeta.meta._initOnlySettings = []), this.metaManager.globalMeta.meta._initOnlySettings.push(n));
    });
  }
  /**
   * Installs the property watcher to the `propName` option and forwards getter and setter to
   * the new one.
   *
   * @param {string} propName The property to watch.
   * @param {string} origProp The property from/to the value is forwarded.
   * @param {Function} onChange The callback.
   */
  installPropWatcher(e, n, i) {
    const r = this;
    Object.defineProperty(this.metaManager.globalMeta.meta, e, {
      get() {
        return this[n];
      },
      set(o) {
        const s = !r.usageTracker.has(e);
        r.usageTracker.add(e), i.call(r, e, o, s), this[n] = o;
      },
      enumerable: !0,
      configurable: !0
    });
  }
}
const M1 = "gridDefault", Js = "editorManager.handlingEditor", Iq = {
  name: "editorCloseAndSave",
  callback(t) {
    t._getEditorManager().closeEditorAndSaveChanges();
  }
}, Oq = "hooksRefRegisterer", I1 = {
  /**
   * Internal hooks storage.
   */
  _hooksStorage: /* @__PURE__ */ Object.create(null),
  /**
   * Add hook to the collection.
   *
   * @param {string} key The hook name.
   * @param {Function} callback The hook callback.
   * @returns {object}
   */
  addHook(t, e) {
    return this._hooksStorage[t] || (this._hooksStorage[t] = []), this.hot.addHook(t, e), this._hooksStorage[t].push(e), this;
  },
  /**
   * Remove all hooks listeners by hook name.
   *
   * @param {string} key The hook name.
   */
  removeHooksByKey(t) {
    T(this._hooksStorage[t] || [], (e) => {
      this.hot.removeHook(t, e);
    });
  },
  /**
   * Clear all added hooks.
   */
  clearHooks() {
    Fe(this._hooksStorage, (t, e) => this.removeHooksByKey(e)), this._hooksStorage = {};
  }
};
wo(I1, "MIXIN_NAME", Oq, {
  writable: !1,
  enumerable: !1
});
const Pq = I1;
function sr(t, e, n) {
  return (e = xq(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function xq(t) {
  var e = Hq(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Hq(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const _q = "base", Xt = Object.freeze({
  VIRGIN: "STATE_VIRGIN",
  // before editing
  EDITING: "STATE_EDITING",
  WAITING: "STATE_WAITING",
  // waiting for async validation
  FINISHED: "STATE_FINISHED"
});
class Fu {
  static get EDITOR_TYPE() {
    return _q;
  }
  /**
   * A reference to the source instance of the Handsontable.
   *
   * @type {Handsontable}
   */
  /**
   * @param {Handsontable} hotInstance A reference to the source instance of the Handsontable.
   */
  constructor(e) {
    sr(this, "hot", void 0), sr(this, "state", Xt.VIRGIN), sr(this, "_opened", !1), sr(this, "_fullEditMode", !1), sr(this, "_closeCallback", null), sr(this, "TD", null), sr(this, "row", null), sr(this, "col", null), sr(this, "prop", null), sr(this, "originalValue", null), sr(this, "cellProperties", null), this.hot = e, this.init();
  }
  /**
   * Fires callback after closing editor.
   *
   * @private
   * @param {boolean} result The editor value.
   */
  _fireCallbacks(e) {
    this._closeCallback && (this._closeCallback(e), this._closeCallback = null);
  }
  /**
   * Initializes an editor's intance.
   */
  init() {
  }
  /**
   * Required method to get current value from editable element.
   */
  getValue() {
    throw Error("Editor getValue() method unimplemented");
  }
  /**
   * Required method to set new value into editable element.
   */
  setValue() {
    throw Error("Editor setValue() method unimplemented");
  }
  /**
   * Required method to open editor.
   */
  open() {
    throw Error("Editor open() method unimplemented");
  }
  /**
   * Required method to close editor.
   */
  close() {
    throw Error("Editor close() method unimplemented");
  }
  /**
   * Prepares editor's meta data.
   *
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number|string} prop The column property (passed when datasource is an array of objects).
   * @param {HTMLTableCellElement} td The rendered cell element.
   * @param {*} value The rendered value.
   * @param {object} cellProperties The cell meta object (see {@link Core#getCellMeta}).
   */
  prepare(e, n, i, r, o, s) {
    this.TD = r, this.row = e, this.col = n, this.prop = i, this.originalValue = o, this.cellProperties = s, this.state = this.isOpened() ? this.state : Xt.VIRGIN;
  }
  /**
   * Fallback method to provide extendable editors in ES5.
   *
   * @returns {Function}
   */
  extend() {
    return class extends this.constructor {
    };
  }
  /**
   * Saves value from editor into data storage.
   *
   * @param {*} value The editor value.
   * @param {boolean} ctrlDown If `true`, applies value to each cell in the last selected range.
   */
  saveValue(e, n) {
    let i, r, o, s;
    if (n) {
      const l = this.hot.getSelectedLast();
      i = Math.max(Math.min(l[0], l[2]), 0), r = Math.max(Math.min(l[1], l[3]), 0), o = Math.max(l[0], l[2]), s = Math.max(l[1], l[3]);
    } else
      [i, r, o, s] = [this.row, this.col, null, null];
    const a = this.hot.runHooks("modifyGetCellCoords", i, r);
    Array.isArray(a) && ([i, r] = a), this.hot.populateFromArray(i, r, e, o, s, "edit");
  }
  /**
   * Begins editing on a highlighted cell and hides fillHandle corner if was present.
   *
   * @param {*} newInitialValue The initial editor value.
   * @param {Event} event The keyboard event object.
   */
  beginEditing(e, n) {
    if (this.state !== Xt.VIRGIN)
      return;
    const i = this.hot, r = i.rowIndexMapper.getRenderableFromVisualIndex(this.row), o = i.columnIndexMapper.getRenderableFromVisualIndex(this.col);
    if (i.view.scrollViewport(i._createCellCoords(r, o)), this.state = Xt.EDITING, this.isInFullEditMode()) {
      const s = typeof e == "string" ? e : Gt(this.originalValue);
      this.setValue(s);
    }
    this.open(n), this._opened = !0, this.focus(), i.view.render(), i.runHooks("afterBeginEditing", this.row, this.col);
  }
  /**
   * Finishes editing and start saving or restoring process for editing cell or last selected range.
   *
   * @param {boolean} restoreOriginalValue If true, then closes editor without saving value from the editor into a cell.
   * @param {boolean} ctrlDown If true, then saveValue will save editor's value to each cell in the last selected range.
   * @param {Function} callback The callback function, fired after editor closing.
   */
  finishEditing(e, n, i) {
    let r;
    if (i) {
      const o = this._closeCallback;
      this._closeCallback = (s) => {
        o && o(s), i(s), this.hot.view.render();
      };
    }
    if (!this.isWaiting()) {
      if (this.state === Xt.VIRGIN) {
        this.hot._registerTimeout(() => {
          this._fireCallbacks(!0);
        });
        return;
      }
      if (this.state === Xt.EDITING) {
        if (e) {
          this.cancelChanges(), this.hot.view.render();
          return;
        }
        const o = this.getValue();
        this.cellProperties.trimWhitespace ? r = [[typeof o == "string" ? String.prototype.trim.call(o || "") : o]] : r = [[o]], this.state = Xt.WAITING, this.saveValue(r, n), this.hot.getCellValidator(this.cellProperties) ? this.hot.addHookOnce("postAfterValidate", (s) => {
          this.state = Xt.FINISHED, this.discardEditor(s);
        }) : (this.state = Xt.FINISHED, this.discardEditor(!0));
      }
    }
  }
  /**
   * Finishes editing without singout saving value.
   */
  cancelChanges() {
    this.state = Xt.FINISHED, this.discardEditor();
  }
  /**
   * Verifies result of validation or closes editor if user's cancelled changes.
   *
   * @param {boolean|undefined} result If `false` and the cell using allowInvalid option,
   *                                   then an editor won't be closed until validation is passed.
   */
  discardEditor(e) {
    this.state === Xt.FINISHED && (e === !1 && this.cellProperties.allowInvalid !== !0 ? (this.hot.selectCell(this.row, this.col), this.focus(), this.state = Xt.EDITING, this._fireCallbacks(!1)) : (this.close(), this._opened = !1, this._fullEditMode = !1, this.state = Xt.VIRGIN, this._fireCallbacks(!0), this.hot.getShortcutManager().setActiveContextName("grid")));
  }
  /**
   * Switch editor into full edit mode. In this state navigation keys don't close editor. This mode is activated
   * automatically after hit ENTER or F2 key on the cell or while editing cell press F2 key.
   */
  enableFullEditMode() {
    this._fullEditMode = !0;
  }
  /**
   * Checks if editor is in full edit mode.
   *
   * @returns {boolean}
   */
  isInFullEditMode() {
    return this._fullEditMode;
  }
  /**
   * Returns information whether the editor is open.
   *
   * @returns {boolean}
   */
  isOpened() {
    return this._opened;
  }
  /**
   * Returns information whether the editor is waiting, eg.: for async validation.
   *
   * @returns {boolean}
   */
  isWaiting() {
    return this.state === Xt.WAITING;
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * Gets the object that provides information about the edited cell size and its position
   * relative to the table viewport.
   *
   * The rectangle has six integer properties:
   *  - `top` The top position relative to the table viewport
   *  - `start` The left (or right in RTL) position relative to the table viewport
   *  - `width` The cell's current width;
   *  - `maxWidth` The maximum cell's width after which the editor goes out of the table viewport
   *  - `height` The cell's current height;
   *  - `maxHeight` The maximum cell's height after which the editor goes out of the table viewport
   *
   * @returns {{top: number, start: number, width: number, maxWidth: number, height: number, maxHeight: number} | undefined}
   */
  getEditedCellRect() {
    var e;
    const n = this.getEditedCell();
    if (!n)
      return;
    const {
      wtOverlays: i,
      wtViewport: r
    } = this.hot.view._wt, o = this.hot.rootWindow, s = gn(n), a = It(n), l = gn(this.hot.rootElement), u = It(this.hot.rootElement), c = i.topOverlay.holder, h = i.inlineStartOverlay.holder, d = c !== o ? c.scrollTop : 0, g = h !== o ? h.scrollLeft : 0, m = o.innerWidth - l.left - u, {
      wtTable: w
    } = (e = i.getParentOverlay(n)) !== null && e !== void 0 ? e : this.hot.view._wt, C = w.name, S = ["master", "inline_start"].includes(C) ? d : 0, b = ["master", "top", "bottom"].includes(C) ? g : 0, R = s.top === l.top ? 0 : 1;
    let _ = s.top - l.top - R - S, O = 0;
    this.hot.isRtl() ? O = o.innerWidth - s.left - a - m - 1 + b : O = s.left - l.left - 1 - b, ["top", "top_inline_start_corner"].includes(C) && (_ += i.topOverlay.getOverlayOffset()), ["inline_start", "top_inline_start_corner"].includes(C) && (O += Math.abs(i.inlineStartOverlay.getOverlayOffset()));
    const $ = this.hot.hasColHeaders(), U = this.hot.rowIndexMapper.getRenderableFromVisualIndex(this.row), G = this.hot.columnIndexMapper.getRenderableFromVisualIndex(this.col), j = this.hot.rowIndexMapper.getRenderableIndexesLength() - this.hot.view._wt.getSetting("fixedRowsBottom");
    ($ && U <= 0 || U === j) && (_ += 1), G <= 0 && (O += 1);
    const J = r.rowsRenderCalculator.startPosition, q = r.columnsRenderCalculator.startPosition, re = Math.abs(i.inlineStartOverlay.getScrollPosition()), me = i.topOverlay.getScrollPosition(), Me = pn(this.hot.rootDocument);
    let nt = n.offsetTop;
    if (["inline_start", "master"].includes(C) && (nt += J - me), ["bottom", "bottom_inline_start_corner"].includes(C)) {
      const {
        wtViewport: K,
        wtTable: le
      } = i.bottomOverlay.clone;
      nt += K.getWorkspaceHeight() - le.getHeight() - Me;
    }
    let Ne = n.offsetLeft;
    this.hot.isRtl() ? (Ne >= 0 ? Ne = w.getWidth() - n.offsetLeft : Ne = Math.abs(Ne), Ne += q - re - a) : ["top", "master", "bottom"].includes(C) && (Ne += q - re);
    const Ce = uo(this.TD, this.hot.rootWindow), pe = this.hot.isRtl() ? "borderRightWidth" : "borderLeftWidth", wt = parseInt(Ce[pe], 10) > 0 ? 0 : 1, f = parseInt(Ce.borderTopWidth, 10) > 0 ? 0 : 1, p = It(n) + wt, A = Zt(n) + f, M = jP(c) ? Me : 0, P = GP(h) ? Me : 0, F = this.hot.view.maximumVisibleElementWidth(Ne) - M + wt, Y = Math.max(this.hot.view.maximumVisibleElementHeight(nt) - P + f, 23);
    return {
      top: _,
      start: O,
      height: A,
      maxHeight: Y,
      width: p,
      maxWidth: F
    };
  }
  /* eslint-enable jsdoc/require-description-complete-sentence */
  /**
   * Gets className of the edited cell if exist.
   *
   * @returns {string}
   */
  getEditedCellsLayerClass() {
    switch (this.checkEditorSection()) {
      case "inline-start":
        return "ht_clone_left ht_clone_inline_start";
      case "bottom":
        return "ht_clone_bottom";
      case "bottom-inline-start-corner":
        return "ht_clone_bottom_left_corner ht_clone_bottom_inline_start_corner";
      case "top":
        return "ht_clone_top";
      case "top-inline-start-corner":
        return "ht_clone_top_left_corner ht_clone_top_inline_start_corner";
      default:
        return "ht_clone_master";
    }
  }
  /**
   * Gets HTMLTableCellElement of the edited cell if exist.
   *
   * @returns {HTMLTableCellElement|null}
   */
  getEditedCell() {
    return this.hot.getCell(this.row, this.col, !0);
  }
  /**
   * Returns name of the overlay, where editor is placed.
   *
   * @private
   * @returns {string}
   */
  checkEditorSection() {
    const e = this.hot.countRows();
    let n = "";
    return this.row < this.hot.getSettings().fixedRowsTop ? this.col < this.hot.getSettings().fixedColumnsStart ? n = "top-inline-start-corner" : n = "top" : this.hot.getSettings().fixedRowsBottom && this.row >= e - this.hot.getSettings().fixedRowsBottom ? this.col < this.hot.getSettings().fixedColumnsStart ? n = "bottom-inline-start-corner" : n = "bottom" : this.col < this.hot.getSettings().fixedColumnsStart && (n = "inline-start"), n;
  }
}
pt(Fu, Pq);
const Nq = {
  name: "editorCloseAndSaveByArrowKeys",
  callback(t, e, n) {
    const i = t._getEditorManager(), r = i.getActiveEditor();
    r.isInFullEditMode() && r.state === Xt.EDITING || (i.closeEditorAndSaveChanges(), t.getSelected() && (n.includes("arrowdown") ? t.selection.transformStart(1, 0) : n.includes("arrowup") ? t.selection.transformStart(-1, 0) : n.includes("arrowleft") ? t.selection.transformStart(0, -1 * t.getDirectionFactor()) : n.includes("arrowright") && t.selection.transformStart(0, t.getDirectionFactor())), e.preventDefault());
  }
}, Dq = {
  name: "editorCloseAndSaveByEnter",
  callback(t, e) {
    const n = t._getEditorManager();
    n.closeEditorAndSaveChanges(e.ctrlKey || e.metaKey), n.moveSelectionAfterEnter(e);
  }
}, kq = {
  name: "editorCloseWithoutSaving",
  callback(t) {
    const e = t._getEditorManager();
    e.closeEditorAndRestoreOriginalValue(t.getShortcutManager().isCtrlPressed()), e.activeEditor.focus();
  }
}, Lq = {
  name: "editorFastOpen",
  callback(t, e) {
    const {
      highlight: n
    } = t.getSelectedRangeLast();
    n.isHeader() || t._getEditorManager().openEditor(null, e, !0);
  }
}, Bq = {
  name: "editorOpen",
  callback(t, e, n) {
    const i = t._getEditorManager(), r = t.getSelectedRangeLast(), {
      highlight: o
    } = r;
    if (t.selection.isMultiple() && !r.isHeader() && t.countRenderedCols() > 0 && t.countRenderedRows() > 0) {
      const s = t.getSettings(), a = typeof s.enterMoves == "function" ? s.enterMoves(e) : s.enterMoves;
      n.includes("shift") ? t.selection.transformFocus(-a.row, -a.col) : t.selection.transformFocus(a.row, a.col);
      return;
    }
    o.isHeader() || (t.getSettings().enterBeginsEditing ? i.cellProperties.readOnly ? i.moveSelectionAfterEnter(e) : i.openEditor(null, e, !0) : i.moveSelectionAfterEnter(e), Zi(e));
  }
};
function Fq() {
  return [Iq, Nq, Dq, kq, Lq, Bq];
}
const Vq = {
  name: "extendCellsSelectionDown",
  callback(t) {
    const {
      highlight: e
    } = t.getSelectedRangeLast();
    !t.selection.isSelectedByColumnHeader() && !t.selection.isSelectedByCorner() && (e.isCell() || e.isHeader() && t.selection.isSelectedByRowHeader()) && t.selection.transformEnd(1, 0);
  }
}, Wq = {
  name: "extendCellsSelectionDownByViewportHeight",
  callback(t) {
    const {
      to: e
    } = t.getSelectedRangeLast(), n = Math.min(e.row + t.countVisibleRows(), t.countRows() - 1), i = t.rowIndexMapper.getNearestNotHiddenIndex(n, -1);
    if (i !== null) {
      const r = t._createCellCoords(i, e.col), o = e.row - t.view.getFirstFullyVisibleRow(), s = Math.min(r.row - o, t.countRows() - 1);
      t.selection.setRangeEnd(r), t.scrollViewportTo({
        row: s,
        verticalSnap: "top",
        horizontalSnap: "start"
      });
    }
  }
}, $q = {
  name: "extendCellsSelectionLeft",
  callback(t) {
    const {
      highlight: e
    } = t.getSelectedRangeLast();
    !t.selection.isSelectedByRowHeader() && !t.selection.isSelectedByCorner() && (e.isCell() || e.isHeader() && t.selection.isSelectedByColumnHeader()) && t.selection.transformEnd(0, -1 * t.getDirectionFactor());
  }
}, Uq = {
  name: "extendCellsSelectionRight",
  callback(t) {
    const {
      highlight: e
    } = t.getSelectedRangeLast();
    !t.selection.isSelectedByRowHeader() && !t.selection.isSelectedByCorner() && (e.isCell() || e.isHeader() && t.selection.isSelectedByColumnHeader()) && t.selection.transformEnd(0, t.getDirectionFactor());
  }
}, Yq = {
  name: "extendCellsSelectionToColumns",
  callback(t) {
    const {
      highlight: e,
      from: n,
      to: i
    } = t.getSelectedRangeLast();
    t.selection.isSelectedByRowHeader() ? t.selection.selectAll(!0, !0) : t.selectColumns(n.col, i.col, e);
  }
}, jq = {
  name: "extendCellsSelectionToMostBottom",
  callback(t) {
    const {
      selection: e,
      rowIndexMapper: n
    } = t, {
      highlight: i,
      from: r,
      to: o
    } = t.getSelectedRangeLast(), s = i.isHeader() && t.selection.isSelectedByRowHeader();
    if (i.isCell() || s) {
      const a = n.getNearestNotHiddenIndex(t.countRows() - 1, -1), l = r.clone();
      l.row = i.row, e.setRangeStart(l, void 0, !1, i.clone()), s && e.selectedByRowHeader.add(e.getLayerLevel()), e.setRangeEnd(t._createCellCoords(a, o.col));
    }
  }
}, Gq = {
  name: "extendCellsSelectionToMostInlineEnd",
  callback(t) {
    const {
      selection: e,
      columnIndexMapper: n
    } = t, {
      highlight: i,
      from: r,
      to: o
    } = t.getSelectedRangeLast();
    if (!t.selection.isSelectedByRowHeader() && !t.selection.isSelectedByCorner() && i.isCell()) {
      const s = n.getNearestNotHiddenIndex(t.countCols() - 1, -1), a = r.clone();
      a.col = i.col, e.setRangeStart(a, void 0, !1, i.clone()), e.setRangeEnd(t._createCellCoords(o.row, s));
    }
  }
}, zq = {
  name: "extendCellsSelectionToMostInlineStart",
  callback(t) {
    const {
      selection: e,
      columnIndexMapper: n
    } = t, {
      highlight: i,
      from: r,
      to: o
    } = t.getSelectedRangeLast();
    if (!t.selection.isSelectedByRowHeader() && !t.selection.isSelectedByCorner() && i.isCell()) {
      const s = parseInt(t.getSettings().fixedColumnsStart, 10), a = n.getNearestNotHiddenIndex(s, 1), l = r.clone();
      l.col = i.col, e.setRangeStart(l, void 0, !1, i.clone()), e.setRangeEnd(t._createCellCoords(o.row, a));
    }
  }
}, Kq = {
  name: "extendCellsSelectionToMostLeft",
  callback(t) {
    const {
      selection: e,
      columnIndexMapper: n
    } = t, {
      highlight: i,
      from: r,
      to: o
    } = t.getSelectedRangeLast(), s = i.isHeader() && t.selection.isSelectedByColumnHeader();
    if (i.isCell() || s) {
      const a = n.getNearestNotHiddenIndex(...t.isRtl() ? [t.countCols() - 1, -1] : [0, 1]), l = r.clone();
      l.col = i.col, e.setRangeStart(l, void 0, !1, i.clone()), s && e.selectedByColumnHeader.add(e.getLayerLevel()), e.setRangeEnd(t._createCellCoords(o.row, a));
    }
  }
}, Qq = {
  name: "extendCellsSelectionToMostRight",
  callback(t) {
    const {
      selection: e,
      columnIndexMapper: n
    } = t, {
      highlight: i,
      from: r,
      to: o
    } = t.getSelectedRangeLast(), s = i.isHeader() && t.selection.isSelectedByColumnHeader();
    if (i.isCell() || s) {
      const a = n.getNearestNotHiddenIndex(...t.isRtl() ? [0, 1] : [t.countCols() - 1, -1]), l = r.clone();
      l.col = i.col, e.setRangeStart(l, void 0, !1, i.clone()), s && e.selectedByColumnHeader.add(e.getLayerLevel()), e.setRangeEnd(t._createCellCoords(o.row, a));
    }
  }
}, Xq = {
  name: "extendCellsSelectionToMostTop",
  callback(t) {
    const {
      selection: e,
      rowIndexMapper: n
    } = t, {
      highlight: i,
      from: r,
      to: o
    } = t.getSelectedRangeLast(), s = i.isHeader() && t.selection.isSelectedByRowHeader();
    if (i.isCell() || s) {
      const a = n.getNearestNotHiddenIndex(0, 1), l = r.clone();
      l.row = i.row, e.setRangeStart(l, void 0, !1, i.clone()), s && e.selectedByRowHeader.add(e.getLayerLevel()), e.setRangeEnd(t._createCellCoords(a, o.col));
    }
  }
}, qq = {
  name: "extendCellsSelectionToRows",
  callback(t) {
    const {
      highlight: e,
      from: n,
      to: i
    } = t.getSelectedRangeLast();
    t.selection.isSelectedByColumnHeader() ? t.selection.selectAll(!0, !0) : t.selectRows(n.row, i.row, e);
  }
}, Zq = {
  name: "extendCellsSelectionUp",
  callback(t) {
    const {
      highlight: e
    } = t.getSelectedRangeLast();
    !t.selection.isSelectedByColumnHeader() && !t.selection.isSelectedByCorner() && (e.isCell() || e.isHeader() && t.selection.isSelectedByRowHeader()) && t.selection.transformEnd(-1, 0);
  }
}, Jq = {
  name: "extendCellsSelectionUpByViewportHeight",
  callback(t) {
    const {
      to: e
    } = t.getSelectedRangeLast(), n = Math.max(e.row - t.countVisibleRows(), 0), i = t.rowIndexMapper.getNearestNotHiddenIndex(n, 1);
    if (i !== null) {
      const r = t._createCellCoords(i, e.col), o = e.row - t.view.getFirstFullyVisibleRow(), s = Math.max(r.row - o, 0);
      t.selection.setRangeEnd(r), t.scrollViewportTo({
        row: s,
        verticalSnap: "top",
        horizontalSnap: "start"
      });
    }
  }
};
function eZ() {
  return [Vq, Wq, $q, Uq, Yq, jq, Gq, zq, Kq, Qq, Xq, qq, Zq, Jq];
}
const tZ = {
  name: "moveCellSelectionDown",
  callback(t) {
    let {
      selection: e
    } = t;
    e.transformStart(1, 0);
  }
}, nZ = {
  name: "moveCellSelectionDownByViewportHeight",
  callback(t) {
    const {
      navigableHeaders: e
    } = t.getSettings(), n = e ? t.countColHeaders() : 0, {
      row: i
    } = t.getSelectedRangeLast().highlight;
    let r = t.countVisibleRows() + n;
    i === t.countRows() - 1 ? r = 1 : i + r > t.countRows() && (r = t.countRows() - i - 1), t.selection.transformStart(r, 0), t.getSelectedRangeLast().highlight.row < 0 && t.scrollViewportTo({
      row: 0
    });
  }
}, iZ = {
  name: "moveCellSelectionInlineEnd",
  callback(t, e) {
    const n = t.getSettings(), i = t.getSelectedRangeLast(), r = typeof n.tabMoves == "function" ? n.tabMoves(e) : n.tabMoves;
    t.selection.isMultiple() && !i.isHeader() && t.countRenderedCols() > 0 && t.countRenderedRows() > 0 ? t.selection.transformFocus(-r.row, -r.col) : t.selection.transformStart(-r.row, -r.col);
  }
}, rZ = {
  name: "moveCellSelectionInlineStart",
  callback(t, e) {
    const n = t.getSettings(), i = t.getSelectedRangeLast(), r = typeof n.tabMoves == "function" ? n.tabMoves(e) : n.tabMoves;
    t.selection.isMultiple() && !i.isHeader() && t.countRenderedCols() > 0 && t.countRenderedRows() > 0 ? t.selection.transformFocus(r.row, r.col) : t.selection.transformStart(r.row, r.col);
  }
}, oZ = {
  name: "moveCellSelectionLeft",
  callback(t) {
    t.selection.transformStart(0, -1 * t.getDirectionFactor());
  }
}, sZ = {
  name: "moveCellSelectionRight",
  callback(t) {
    t.selection.transformStart(0, t.getDirectionFactor());
  }
}, aZ = {
  name: "moveCellSelectionToMostBottom",
  callback(t) {
    const {
      col: e
    } = t.getSelectedRangeLast().highlight;
    let n = t.rowIndexMapper.getNearestNotHiddenIndex(t.countRows() - 1, -1);
    n === null && (n = -1), t.selection.setRangeStart(t._createCellCoords(n, e));
  }
}, lZ = {
  name: "moveCellSelectionToMostBottomInlineEnd",
  callback(t) {
    const {
      selection: e,
      rowIndexMapper: n,
      columnIndexMapper: i
    } = t, r = parseInt(t.getSettings().fixedRowsBottom, 10), o = n.getNearestNotHiddenIndex(t.countRows() - r - 1, -1), s = i.getNearestNotHiddenIndex(t.countCols() - 1, -1);
    e.setRangeStart(t._createCellCoords(o, s));
  }
}, uZ = {
  name: "moveCellSelectionToMostInlineEnd",
  callback(t) {
    const {
      selection: e,
      columnIndexMapper: n
    } = t;
    e.setRangeStart(t._createCellCoords(t.getSelectedRangeLast().highlight.row, n.getNearestNotHiddenIndex(t.countCols() - 1, -1)));
  }
}, cZ = {
  name: "moveCellSelectionToMostInlineStart",
  callback(t) {
    const {
      selection: e,
      columnIndexMapper: n
    } = t, i = parseInt(t.getSettings().fixedColumnsStart, 10), r = t.getSelectedRangeLast().highlight.row, o = n.getNearestNotHiddenIndex(i, 1);
    e.setRangeStart(t._createCellCoords(r, o));
  }
}, hZ = {
  name: "moveCellSelectionToMostLeft",
  callback(t) {
    const {
      selection: e,
      columnIndexMapper: n
    } = t, i = t.getSelectedRangeLast().highlight.row;
    let r = n.getNearestNotHiddenIndex(...t.isRtl() ? [t.countCols() - 1, -1] : [0, 1]);
    r === null && (r = t.isRtl() ? -1 : -t.countRowHeaders()), e.setRangeStart(t._createCellCoords(i, r));
  }
}, dZ = {
  name: "moveCellSelectionToMostRight",
  callback(t) {
    const {
      selection: e,
      columnIndexMapper: n
    } = t, {
      row: i
    } = t.getSelectedRangeLast().highlight;
    let r = n.getNearestNotHiddenIndex(...t.isRtl() ? [0, 1] : [t.countCols() - 1, -1]);
    r === null && (r = t.isRtl() ? -t.countRowHeaders() : -1), e.setRangeStart(t._createCellCoords(i, r));
  }
}, fZ = {
  name: "moveCellSelectionToMostTop",
  callback(t) {
    const {
      col: e
    } = t.getSelectedRangeLast().highlight;
    let n = t.rowIndexMapper.getNearestNotHiddenIndex(0, 1);
    n === null && (n = -t.countColHeaders()), t.selection.setRangeStart(t._createCellCoords(n, e));
  }
}, gZ = {
  name: "moveCellSelectionToMostTopInlineStart",
  callback(t) {
    const {
      selection: e,
      rowIndexMapper: n,
      columnIndexMapper: i
    } = t, r = parseInt(t.getSettings().fixedRowsTop, 10), o = parseInt(t.getSettings().fixedColumnsStart, 10), s = n.getNearestNotHiddenIndex(r, 1), a = i.getNearestNotHiddenIndex(o, 1);
    e.setRangeStart(t._createCellCoords(s, a));
  }
}, mZ = {
  name: "moveCellSelectionUp",
  callback(t) {
    let {
      selection: e
    } = t;
    e.transformStart(-1, 0);
  }
}, pZ = {
  name: "moveCellSelectionUpByViewportHight",
  callback(t) {
    const {
      navigableHeaders: e
    } = t.getSettings(), n = e ? t.countColHeaders() : 0, {
      row: i
    } = t.getSelectedRangeLast().highlight;
    let r = -(t.countVisibleRows() + n);
    i === -n ? r = -1 : i + r < n && (r = -(i + n)), t.selection.transformStart(r, 0), t.getSelectedRangeLast().highlight.row < 0 && t.scrollViewportTo({
      row: 0
    });
  }
};
function wZ() {
  return [tZ, nZ, iZ, rZ, oZ, sZ, aZ, lZ, uZ, cZ, hZ, dZ, fZ, gZ, mZ, pZ];
}
const AZ = {
  name: "emptySelectedCells",
  callback(t) {
    t.emptySelectedCells(), t._getEditorManager().prepareEditor();
  }
}, CZ = {
  name: "scrollToFocusedCell",
  callback(t) {
    const {
      highlight: e
    } = t.getSelectedRangeLast(), n = t.view.getFirstFullyVisibleRow() - 1, i = t.view.getFirstFullyVisibleColumn() - 1, r = t.view.getLastFullyVisibleRow() + 1, o = t.view.getLastFullyVisibleColumn() + 1, s = t._createCellCoords(n, i), a = t._createCellCoords(r, o);
    if (!t._createCellRange(s, s, a).includes(e) && (e.row >= 0 || e.col >= 0)) {
      const u = {};
      if (e.col >= 0) {
        const c = Math.floor(t.countVisibleCols() / 2);
        u.col = Math.max(e.col - c, 0);
      }
      if (e.row >= 0) {
        const c = Math.floor(t.countVisibleRows() / 2);
        u.row = Math.max(e.row - c, 0);
      }
      t.scrollViewportTo(On(Xe({}, u), {
        verticalSnap: "top",
        horizontalSnap: "start"
      }));
    }
  }
}, vZ = {
  name: "selectAllCells",
  callback(t) {
    t.selection.selectAll(!0, !0, {
      disableHeadersHighlight: !0
    });
  }
}, yZ = {
  name: "selectAllCellsAndHeaders",
  callback(t) {
    t.selection.selectAll(!0, !0, {
      disableHeadersHighlight: !1
    });
  }
}, SZ = {
  name: "populateSelectedCellsData",
  callback(t) {
    const e = t.getSelectedRange(), {
      row: n,
      col: i
    } = e[e.length - 1].highlight.normalize(), r = t.getDataAtCell(n, i), o = /* @__PURE__ */ new Map();
    for (let s = 0; s < e.length; s++)
      e[s].forAll((a, l) => {
        if (a >= 0 && l >= 0 && (a !== n || l !== i)) {
          const {
            readOnly: u
          } = t.getCellMeta(a, l);
          u || o.set(`${a}x${l}`, [a, l, r]);
        }
      });
    t.setDataAtCell(Array.from(o.values()));
  }
}, bZ = [...Fq(), ...eZ(), ...wZ(), AZ, CZ, vZ, yZ, SZ];
function O1(t) {
  const e = {};
  return bZ.forEach((n) => {
    let {
      name: i,
      callback: r
    } = n;
    e[i] = function() {
      for (var o = arguments.length, s = new Array(o), a = 0; a < o; a++)
        s[a] = arguments[a];
      return r(t, ...s);
    };
  }), e;
}
function RZ(t) {
  const e = t.getShortcutManager().addContext("editor"), n = O1(t), i = {
    group: Js
  };
  e.addShortcuts([{
    keys: [["Enter"], ["Enter", "Shift"]],
    callback: (r, o) => n.editorCloseAndSaveByEnter(r, o)
  }, {
    keys: [["Enter", "Control/Meta"], ["Enter", "Control/Meta", "Shift"]],
    captureCtrl: !0,
    callback: (r, o) => n.editorCloseAndSaveByEnter(r, o)
  }, {
    keys: [["Tab"], ["Tab", "Shift"], ["PageDown"], ["PageUp"]],
    forwardToContext: t.getShortcutManager().getContext("grid"),
    callback: (r, o) => n.editorCloseAndSave(r, o)
  }, {
    keys: [["ArrowDown"], ["ArrowUp"], ["ArrowLeft"], ["ArrowRight"]],
    preventDefault: !1,
    callback: (r, o) => n.editorCloseAndSaveByArrowKeys(r, o)
  }, {
    keys: [["Escape"], ["Escape", "Control/Meta"]],
    callback: () => n.editorCloseWithoutSaving()
  }], i);
}
function EZ(t) {
  const e = t.getShortcutManager().addContext("grid"), n = O1(t), i = {
    runOnlyIf: () => {
      const {
        navigableHeaders: r
      } = t.getSettings();
      return de(t.getSelected()) && (r || !r && t.countRenderedRows() > 0 && t.countRenderedCols() > 0);
    },
    group: M1
  };
  e.addShortcuts([{
    keys: [["F2"]],
    callback: (r) => n.editorFastOpen(r)
  }, {
    keys: [["Enter"], ["Enter", "Shift"]],
    callback: (r, o) => n.editorOpen(r, o)
  }, {
    keys: [["Backspace"], ["Delete"]],
    callback: () => n.emptySelectedCells()
  }], {
    group: Js,
    runOnlyIf: () => de(t.getSelected())
  }), e.addShortcuts([{
    keys: [["Control/Meta", "A"]],
    callback: () => n.selectAllCells(),
    runOnlyIf: () => {
      var r;
      return !((r = t.getSelectedRangeLast()) !== null && r !== void 0 && r.highlight.isHeader());
    }
  }, {
    keys: [["Control/Meta", "A"]],
    callback: () => {
    },
    runOnlyIf: () => {
      var r;
      return (r = t.getSelectedRangeLast()) === null || r === void 0 ? void 0 : r.highlight.isHeader();
    },
    preventDefault: !0
  }, {
    keys: [["Control/Meta", "Shift", "Space"]],
    callback: () => n.selectAllCellsAndHeaders()
  }, {
    keys: [["Control/Meta", "Enter"]],
    callback: () => n.populateSelectedCellsData(),
    runOnlyIf: () => {
      var r, o;
      return !((r = t.getSelectedRangeLast()) !== null && r !== void 0 && r.highlight.isHeader()) && ((o = t.getSelectedRangeLast()) === null || o === void 0 ? void 0 : o.getCellsCount()) > 1;
    }
  }, {
    keys: [["Control", "Space"]],
    captureCtrl: !0,
    callback: () => n.extendCellsSelectionToColumns()
  }, {
    keys: [["Shift", "Space"]],
    stopPropagation: !0,
    callback: () => n.extendCellsSelectionToRows()
  }, {
    keys: [["ArrowUp"]],
    callback: () => n.moveCellSelectionUp()
  }, {
    keys: [["ArrowUp", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.moveCellSelectionToMostTop()
  }, {
    keys: [["ArrowUp", "Shift"]],
    callback: () => n.extendCellsSelectionUp()
  }, {
    keys: [["ArrowUp", "Shift", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.extendCellsSelectionToMostTop(),
    runOnlyIf: () => !(t.selection.isSelectedByCorner() || t.selection.isSelectedByColumnHeader())
  }, {
    keys: [["ArrowDown"]],
    callback: () => n.moveCellSelectionDown()
  }, {
    keys: [["ArrowDown", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.moveCellSelectionToMostBottom()
  }, {
    keys: [["ArrowDown", "Shift"]],
    callback: () => n.extendCellsSelectionDown()
  }, {
    keys: [["ArrowDown", "Shift", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.extendCellsSelectionToMostBottom(),
    runOnlyIf: () => !(t.selection.isSelectedByCorner() || t.selection.isSelectedByColumnHeader())
  }, {
    keys: [["ArrowLeft"]],
    callback: () => n.moveCellSelectionLeft()
  }, {
    keys: [["ArrowLeft", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.moveCellSelectionToMostLeft()
  }, {
    keys: [["ArrowLeft", "Shift"]],
    callback: () => n.extendCellsSelectionLeft()
  }, {
    keys: [["ArrowLeft", "Shift", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.extendCellsSelectionToMostLeft(),
    runOnlyIf: () => !(t.selection.isSelectedByCorner() || t.selection.isSelectedByRowHeader())
  }, {
    keys: [["ArrowRight"]],
    callback: () => n.moveCellSelectionRight()
  }, {
    keys: [["ArrowRight", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.moveCellSelectionToMostRight()
  }, {
    keys: [["ArrowRight", "Shift"]],
    callback: () => n.extendCellsSelectionRight()
  }, {
    keys: [["ArrowRight", "Shift", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.extendCellsSelectionToMostRight(),
    runOnlyIf: () => !(t.selection.isSelectedByCorner() || t.selection.isSelectedByRowHeader())
  }, {
    keys: [["Home"]],
    captureCtrl: !0,
    callback: () => n.moveCellSelectionToMostInlineStart(),
    runOnlyIf: () => t.view.isMainTableNotFullyCoveredByOverlays()
  }, {
    keys: [["Home", "Shift"]],
    callback: () => n.extendCellsSelectionToMostInlineStart()
  }, {
    keys: [["Home", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.moveCellSelectionToMostTopInlineStart(),
    runOnlyIf: () => t.view.isMainTableNotFullyCoveredByOverlays()
  }, {
    keys: [["End"]],
    captureCtrl: !0,
    callback: () => n.moveCellSelectionToMostInlineEnd(),
    runOnlyIf: () => t.view.isMainTableNotFullyCoveredByOverlays()
  }, {
    keys: [["End", "Shift"]],
    callback: () => n.extendCellsSelectionToMostInlineEnd()
  }, {
    keys: [["End", "Control/Meta"]],
    captureCtrl: !0,
    callback: () => n.moveCellSelectionToMostBottomInlineEnd(),
    runOnlyIf: () => t.view.isMainTableNotFullyCoveredByOverlays()
  }, {
    keys: [["PageUp"]],
    callback: () => n.moveCellSelectionUpByViewportHight()
  }, {
    keys: [["PageUp", "Shift"]],
    callback: () => n.extendCellsSelectionUpByViewportHeight()
  }, {
    keys: [["PageDown"]],
    callback: () => n.moveCellSelectionDownByViewportHeight()
  }, {
    keys: [["PageDown", "Shift"]],
    callback: () => n.extendCellsSelectionDownByViewportHeight()
  }, {
    keys: [["Tab"]],
    // The property value is controlled by focusCatcher module (https://github.com/handsontable/handsontable/blob/master/handsontable/src/core/focusCatcher/index.js)
    preventDefault: !1,
    callback: (r) => n.moveCellSelectionInlineStart(r)
  }, {
    keys: [["Shift", "Tab"]],
    // The property value is controlled by focusCatcher module (https://github.com/handsontable/handsontable/blob/master/handsontable/src/core/focusCatcher/index.js)
    preventDefault: !1,
    callback: (r) => n.moveCellSelectionInlineEnd(r)
  }, {
    keys: [["Control/Meta", "Backspace"]],
    callback: () => n.scrollToFocusedCell()
  }], i);
}
function TZ(t) {
  [EZ, RZ].forEach((e) => e(t));
}
function MZ(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
  const n = t.rootElement, i = IT(t), r = IT(t);
  return i.addEventListener("focus", () => e == null ? void 0 : e.onFocusFromTop()), r.addEventListener("focus", () => e == null ? void 0 : e.onFocusFromBottom()), n.firstChild.before(i), n.lastChild.after(r), {
    /**
     * Activates the detector by resetting the tabIndex of the input elements.
     */
    activate() {
      t._registerTimeout(() => {
        i.tabIndex = 0, r.tabIndex = 0;
      }, 10);
    },
    /**
     * Deactivates the detector by setting tabIndex to -1.
     */
    deactivate() {
      t._registerTimeout(() => {
        i.tabIndex = -1, r.tabIndex = -1;
      }, 10);
    }
  };
}
function IT(t) {
  const n = t.rootDocument.createElement("input");
  return n.type = "text", n.classList.add("htFocusCatcher"), t.getSettings().ariaTags && Ee(n, [oo(), gl()]), n;
}
function IZ(t) {
  const e = OZ(t);
  let n;
  const {
    activate: i,
    deactivate: r
  } = MZ(t, {
    onFocusFromTop() {
      var h;
      const d = (h = e(n)) !== null && h !== void 0 ? h : Ny(t);
      d && (t.runHooks("modifyFocusOnTabNavigation", "from_above", d), t.selectCell(d.row, d.col)), t.listen();
    },
    onFocusFromBottom() {
      var h;
      const d = (h = e(n)) !== null && h !== void 0 ? h : Dy(t);
      d && (t.runHooks("modifyFocusOnTabNavigation", "from_below", d), t.selectCell(d.row, d.col)), t.listen();
    }
  }), o = {
    wrapped: !1,
    flipped: !1
  };
  let s = !0, a = !1, l = !1;
  t.addHook("afterListen", () => r()), t.addHook("afterUnlisten", () => i()), t.addHook("afterSelection", (h, d, g, m, w) => {
    if (a && (o.wrapped && o.flipped || l) && (l = !1, w.value = !0), s) {
      var C;
      n = (C = t.getSelectedRangeLast()) === null || C === void 0 ? void 0 : C.highlight;
    }
  }), t.addHook("beforeRowWrap", (h, d, g) => {
    o.wrapped = !0, o.flipped = g;
  });
  function u() {
    o.wrapped = !1, o.flipped = !1, t.deselectCell(), t.unlisten();
  }
  const c = {
    keys: [["Tab"], ["Shift", "Tab"]],
    preventDefault: !1,
    stopPropagation: !1,
    relativeToGroup: M1,
    group: "focusCatcher"
  };
  t.getShortcutManager().getContext("grid").addShortcuts([On(Xe({}, c), {
    callback: () => {
      const {
        tabNavigation: h
      } = t.getSettings();
      a = !0, t.getSelectedRangeLast() && !h && (s = !1), h || (l = !0);
    },
    position: "before"
  }), On(Xe({}, c), {
    callback: (h) => {
      const {
        tabNavigation: d,
        autoWrapRow: g
      } = t.getSettings();
      if (a = !1, s = !0, !d || !t.selection.isSelected() || g && o.wrapped && o.flipped || !g && o.wrapped)
        return g && o.wrapped && o.flipped && (n = h.shiftKey ? Ny(t) : Dy(t)), u(), !1;
      h.preventDefault();
    },
    position: "after"
  })]);
}
function Ny(t) {
  const {
    rowIndexMapper: e,
    columnIndexMapper: n
  } = t, {
    navigableHeaders: i
  } = t.getSettings();
  let r = i && t.countColHeaders() > 0 ? -t.countColHeaders() : 0, o = i && t.countRowHeaders() > 0 ? -t.countRowHeaders() : 0;
  return r === 0 && (r = e.getVisualFromRenderableIndex(r)), o === 0 && (o = n.getVisualFromRenderableIndex(o)), r === null || o === null ? null : t._createCellCoords(r, o);
}
function Dy(t) {
  var e, n;
  const {
    rowIndexMapper: i,
    columnIndexMapper: r
  } = t, {
    navigableHeaders: o
  } = t.getSettings();
  let s = i.getRenderableIndexesLength() - 1, a = r.getRenderableIndexesLength() - 1;
  if (s < 0) {
    if (!o || t.countColHeaders() === 0)
      return null;
    s = -1;
  }
  if (a < 0) {
    if (!o || t.countColHeaders() === 0)
      return null;
    a = -1;
  }
  return t._createCellCoords((e = i.getVisualFromRenderableIndex(s)) !== null && e !== void 0 ? e : s, (n = r.getVisualFromRenderableIndex(a)) !== null && n !== void 0 ? n : a);
}
function OZ(t) {
  return (e) => {
    if (!e)
      return null;
    const n = Ny(t), i = Dy(t);
    return e.col < n.col && (e.col = n.col), e.col > i.col && (e.col = i.col), e.row < n.row && (e.row = n.row), e.row > i.row && (e.row = i.row), e;
  };
}
function PZ(t) {
  return (e) => {
    let {
      col: n
    } = e;
    t.scrollViewportTo({
      col: n
    });
  };
}
function xZ() {
  return () => {
  };
}
function HZ(t) {
  return (e) => {
    t.scrollViewportTo(e.toObject());
  };
}
function _Z(t) {
  return (e) => {
    t.scrollViewportTo(e.toObject());
  };
}
function NZ(t) {
  return (e) => {
    t.scrollViewportTo(e.toObject());
  };
}
function DZ(t) {
  return (e) => {
    let {
      row: n
    } = e;
    t.scrollViewportTo({
      row: n
    });
  };
}
function kZ(t) {
  return (e) => {
    const n = t.selection.getSelectionSource(), {
      row: i,
      col: r
    } = e;
    if (i < 0 && r >= 0)
      t.scrollViewportTo({
        col: r
      });
    else if (r < 0 && i >= 0)
      t.scrollViewportTo({
        row: i
      });
    else {
      if (n === "mouse" && (r === t.view.getLastPartiallyVisibleColumn() || i === t.view.getLastPartiallyVisibleRow()))
        return;
      t.scrollViewportTo({
        row: i,
        col: r
      });
    }
  };
}
function LZ(t) {
  const {
    selection: e
  } = t;
  let n = !1, i = !1;
  return {
    resume() {
      i = !1;
    },
    suspend() {
      i = !0;
    },
    skipNextScrollCycle() {
      n = !0;
    },
    scrollTo(r) {
      var o;
      if (n || i) {
        n = !1;
        return;
      }
      let s;
      e.isFocusSelectionChanged() ? s = HZ(t) : e.isSelectedByCorner() ? s = xZ() : e.isSelectedByRowHeader() ? s = DZ(t) : e.isSelectedByColumnHeader() ? s = PZ(t) : e.getSelectedRange().size() === 1 && e.isMultiple() ? s = _Z(t) : e.getSelectedRange().size() === 1 && !e.isMultiple() ? s = kZ(t) : e.getSelectedRange().size() > 1 && (s = NZ(t)), (o = s) === null || o === void 0 || o(r);
    }
  };
}
const OT = /* @__PURE__ */ new Map([
  [" ", "space"],
  // custom mapping
  ["spacebar", "space"],
  ["scroll", "scrolllock"],
  ["del", "delete"],
  ["esc", "escape"],
  ["medianexttrack", "mediatracknext"],
  ["mediaprevioustrack", "mediatrackprevious"],
  ["volumeup", "audiovolumeup"],
  ["volumedown", "audiovolumedown"],
  ["volumemute", "audiovolumemute"],
  ["multiply", "*"],
  ["add", "+"],
  ["divide", "/"],
  ["subtract", "-"],
  ["left", "arrowleft"],
  ["right", "arrowright"],
  ["up", "arrowup"],
  ["down", "arrowdown"]
]), Nf = (t) => t.map((e) => {
  const n = e.toLowerCase();
  return OT.has(n) ? OT.get(n) : n;
}).sort().join("+"), BZ = (t) => t.split("+"), PT = /* @__PURE__ */ new Map([
  [96, "numpad0"],
  [97, "numpad1"],
  [98, "numpad2"],
  [99, "numpad3"],
  [100, "numpad4"],
  [101, "numpad5"],
  [102, "numpad6"],
  [103, "numpad7"],
  [104, "numpad8"],
  [105, "numpad9"],
  [106, "multiply"],
  [107, "add"],
  [108, "decimal"],
  // firefox
  [109, "subtract"],
  [110, "decimal"],
  [111, "divide"],
  [112, "f1"],
  [113, "f2"],
  [114, "f3"],
  [115, "f4"],
  [116, "f5"],
  [117, "f6"],
  [118, "f7"],
  [119, "f8"],
  [120, "f9"],
  [121, "f10"],
  [122, "f11"],
  [123, "f12"],
  [186, "semicolon"],
  [187, "equal"],
  [188, "comma"],
  [189, "minus"],
  [190, "period"],
  [191, "slash"],
  [192, "backquote"],
  [219, "bracketleft"],
  [220, "backslash"],
  [221, "bracketright"],
  [222, "quote"]
]), bC = (t) => {
  let {
    which: e,
    key: n
  } = t;
  if (PT.has(e))
    return PT.get(e);
  const i = String.fromCharCode(e).toLowerCase();
  return /^[a-z0-9]$/.test(i) ? i : n.toLowerCase();
}, P1 = Symbol("shortcut-context");
function x1(t) {
  return Be(t) && t.__kindOf === P1;
}
const FZ = (t) => {
  const e = _p({
    errorIdExists: (l) => `The "${l}" shortcut is already registered in the "${t}" context.`
  }), n = function() {
    let {
      keys: l,
      callback: u,
      group: c,
      runOnlyIf: h = () => !0,
      captureCtrl: d = !1,
      preventDefault: g = !0,
      stopPropagation: m = !1,
      relativeToGroup: w,
      position: C,
      forwardToContext: S
    } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    if (Mt(c))
      throw new Error("You need to define the shortcut's group.");
    if (Et(u) === !1)
      throw new Error("The shortcut's callback needs to be a function.");
    if (Array.isArray(l) === !1)
      throw new Error(mt`Pass the shortcut\'s keys as an array of arrays,\x20
      using the KeyboardEvent.key properties:\x20
      https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key/Key_Values.`);
    const b = {
      callback: u,
      group: c,
      runOnlyIf: h,
      captureCtrl: d,
      preventDefault: g,
      stopPropagation: m
    };
    de(w) && (b.relativeToGroup = w, b.position = C), x1(S) && (b.forwardToContext = S), l.forEach((R) => {
      const _ = Nf(R);
      if (e.hasItem(_)) {
        const $ = e.getItem(_);
        let U = $.findIndex((G) => G.group === w);
        U !== -1 ? C === "before" ? U -= 1 : U += 1 : U = $.length, $.splice(U, 0, b);
      } else
        e.addItem(_, [b]);
    });
  }, i = function(l) {
    let u = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    l.forEach((c) => {
      Fe(u, (h, d) => {
        Object.prototype.hasOwnProperty.call(c, d) === !1 && (c[d] = u[d]);
      }), n(c);
    });
  }, r = (l) => {
    const u = Nf(l);
    e.removeItem(u);
  };
  return {
    __kindOf: P1,
    addShortcut: n,
    addShortcuts: i,
    getShortcuts: (l) => {
      const u = Nf(l), c = e.getItem(u);
      return de(c) ? c.slice() : [];
    },
    hasShortcut: (l) => {
      const u = Nf(l);
      return e.hasItem(u);
    },
    removeShortcutsByKeys: r,
    removeShortcutsByGroup: (l) => {
      e.getItems().forEach((c) => {
        let [h, d] = c;
        const g = d.filter((m) => m.group !== l);
        g.length === 0 ? r(BZ(h)) : (d.length = 0, d.push(...g));
      });
    }
  };
};
function VZ() {
  const t = /* @__PURE__ */ new Set();
  return {
    /**
     * Press a key.
     *
     * @param {string} key Names of the shortcut's keys,
     * (coming from [`KeyboardEvent.key`](https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key/Key_Values)),
     * in lowercase or uppercase, unified across browsers
     */
    press(e) {
      t.add(e);
    },
    /**
     * Release a pressed key.
     *
     * @param {string} key Names of the shortcut's keys,
     * (coming from [`KeyboardEvent.key`](https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key/Key_Values)),
     * in lowercase or uppercase, unified across browsers
     */
    release(e) {
      t.delete(e);
    },
    /**
     * Release all pressed keys.
     */
    releaseAll() {
      t.clear();
    },
    /**
     * Check if a key is pressed.
     *
     * @param {string} key Names of the shortcut's keys,
     * (coming from [`KeyboardEvent.key`](https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key/Key_Values)),
     * in lowercase or uppercase, unified across browsers
     * @returns {boolean}
     */
    isPressed(e) {
      return t.has(e);
    }
  };
}
const WZ = ["meta", "alt", "shift", "control"], pc = VZ(), wc = [];
let Df = 0;
function $Z(t, e, n, i, r) {
  const o = (g) => WZ.includes(g), s = function(g) {
    let m = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    const w = [];
    return g.altKey && w.push("alt"), m && (g.ctrlKey || g.metaKey) ? w.push("control/meta") : (g.ctrlKey && w.push("control"), g.metaKey && w.push("meta")), g.shiftKey && w.push("shift"), w;
  }, a = (g) => {
    if (e(g) === !1)
      return;
    const m = n(g);
    if (g.keyCode === 229 || m === !1 || Os(g))
      return;
    const w = bC(g);
    let C = [];
    o(w) || (C = s(g));
    const S = [w].concat(C);
    !r(g, S) && (Lh() && C.includes("meta") || !Lh() && C.includes("control")) && r(g, [w].concat(s(g, !0))), i(g);
  }, l = (g) => {
    if (g.key) {
      const m = bC(g);
      o(m) && pc.press(m);
    }
  }, u = (g) => {
    if (g.key) {
      const m = bC(g);
      o(m) && pc.release(m);
    }
  }, c = () => {
    pc.releaseAll();
  };
  return {
    mount: () => {
      let g = t;
      for (Df += 1; g; )
        Df === 1 && (g.document.documentElement.addEventListener("keydown", l), wc.push({
          event: "keydown",
          listener: l
        }), g.document.documentElement.addEventListener("keyup", u), wc.push({
          event: "keyup",
          listener: u
        })), g.document.documentElement.addEventListener("keydown", a), g.document.documentElement.addEventListener("blur", c), g = Ja(g);
    },
    unmount: () => {
      let g = t;
      for (Df -= 1; g; ) {
        if (Df === 0) {
          for (let m = 0; m < wc.length; m++) {
            const {
              event: w,
              listener: C
            } = wc[m];
            g.document.documentElement.removeEventListener(w, C);
          }
          wc.length = 0;
        }
        g.document.documentElement.removeEventListener("keydown", a), g.document.documentElement.removeEventListener("blur", c), g = Ja(g);
      }
    },
    isPressed: (g) => pc.isPressed(g),
    releasePressedKeys: () => pc.releaseAll()
  };
}
const UZ = (t) => {
  let {
    ownerWindow: e,
    handleEvent: n,
    beforeKeyDown: i,
    afterKeyDown: r
  } = t;
  const o = _p({
    errorIdExists: (m) => `The "${m}" context name is already registered.`
  });
  let s = "grid";
  const a = (m) => {
    const w = FZ(m);
    return o.addItem(m, w), w;
  }, l = () => s, u = (m) => o.getItem(m), c = (m) => {
    if (!o.hasItem(m))
      throw new Error(mt`You've tried to activate the "${m}" shortcut context\x20
        that does not exist. Before activation, register the context using the "addContext" method.`);
    s = m;
  };
  let h = !1;
  const d = function(m, w) {
    let C = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : l();
    const S = x1(C) ? C : u(C);
    let b = !1;
    if (!S.hasShortcut(w))
      return b;
    const R = S.getShortcuts(w);
    for (let _ = 0; _ < R.length; _++) {
      const {
        callback: O,
        runOnlyIf: $,
        preventDefault: U,
        stopPropagation: G,
        captureCtrl: z,
        forwardToContext: j
      } = R[_];
      if ($(m) === !0) {
        if (h = z, b = O(m, w) === !1, h = !1, U && m.preventDefault(), G && (Zi(m), m.stopPropagation()), b)
          break;
        j && d(m, w, j);
      }
    }
    return b;
  }, g = $Z(e, n, i, r, d);
  return g.mount(), {
    addContext: a,
    getActiveContextName: l,
    getContext: u,
    setActiveContextName: c,
    /**
     * Returns whether `control` or `meta` keys are pressed.
     *
     * @memberof ShortcutManager#
     * @type {Function}
     * @returns {boolean}
     */
    isCtrlPressed: () => !h && (g.isPressed("control") || g.isPressed("meta")),
    /**
     * Release every previously pressed key.
     *
     * @type {Function}
     * @memberof ShortcutManager#
     */
    releasePressedKeys: () => g.releasePressedKeys(),
    /**
     * Destroy a context manager instance.
     *
     * @type {Function}
     * @memberof ShortcutManager#
     */
    destroy: () => g.unmount()
  };
};
let RC = null;
const EC = /* @__PURE__ */ new Map();
function H1(t, e) {
  var n, i = this;
  let r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1, o = this;
  const s = new Qi(o);
  let a, l, u, c, h, d, g = !0;
  pX(r) && mX(this), this.rootElement = t, this.rootDocument = t.ownerDocument, this.rootWindow = this.rootDocument.defaultView, this.isDestroyed = !1, this.renderSuspendedCounter = 0, this.executionSuspendedCounter = 0;
  const m = (n = e == null ? void 0 : e.layoutDirection) !== null && n !== void 0 ? n : "inherit", w = ["rtl", "ltr"].includes(m) ? m : this.rootWindow.getComputedStyle(this.rootElement).direction;
  this.rootElement.setAttribute("dir", w), this.isRtl = function() {
    return w === "rtl";
  }, this.isLtr = function() {
    return !o.isRtl();
  }, this.getDirectionFactor = function() {
    return o.isLtr() ? 1 : -1;
  }, e.language = g4(e.language);
  const C = new T1(o, e, [vq, Mq]), S = C.getTableMeta(), b = C.getGlobalMeta(), R = _p();
  this.container = this.rootDocument.createElement("div"), this.renderCall = !1, t.insertBefore(this.container, t.firstChild), Yg(this) && IP(e.licenseKey, t), this.guid = `ht_${QS()}`, EC.set(this.guid, this), this.columnIndexMapper = new Hy(), this.rowIndexMapper = new Hy(), this.columnIndexMapper.addLocalHook("indexesSequenceChange", (f) => {
    o.runHooks("afterColumnSequenceChange", f);
  }), this.rowIndexMapper.addLocalHook("indexesSequenceChange", (f) => {
    o.runHooks("afterRowSequenceChange", f);
  }), l = new xX(o), (!this.rootElement.id || this.rootElement.id.substring(0, 3) === "ht_") && (this.rootElement.id = this.guid);
  const _ = (f) => {
    const {
      row: p,
      col: A
    } = f;
    return o._createCellCoords(
      // We just store indexes for rows and columns without headers.
      p >= 0 ? o.rowIndexMapper.getRenderableFromVisualIndex(p) : p,
      A >= 0 ? o.columnIndexMapper.getRenderableFromVisualIndex(A) : A
    );
  }, O = (f) => {
    const {
      row: p,
      col: A
    } = f;
    return o._createCellCoords(
      // We just store indexes for rows and columns without headers.
      p >= 0 ? o.rowIndexMapper.getVisualFromRenderableIndex(p) : p,
      A >= 0 ? o.columnIndexMapper.getVisualFromRenderableIndex(A) : A
      // eslint-disable-line max-len
    );
  }, $ = (f, p) => {
    const A = p > f ? 1 : -1, M = Math.min(f, p), P = Math.max(f, p), F = o.rowIndexMapper.getNearestNotHiddenIndex(f, A);
    return F === null || A === 1 && F > P || A === -1 && F < M ? null : F >= 0 ? o.rowIndexMapper.getRenderableFromVisualIndex(F) : F;
  }, U = (f, p) => {
    const A = p > f ? 1 : -1, M = Math.min(f, p), P = Math.max(f, p), F = o.columnIndexMapper.getNearestNotHiddenIndex(f, A);
    return F === null || A === 1 && F > P || A === -1 && F < M ? null : F >= 0 ? o.columnIndexMapper.getRenderableFromVisualIndex(F) : F;
  };
  let G = new Y4(S, {
    rowIndexMapper: o.rowIndexMapper,
    columnIndexMapper: o.columnIndexMapper,
    countCols: () => o.countCols(),
    countRows: () => o.countRows(),
    propToCol: (f) => a.propToCol(f),
    isEditorOpened: () => o.getActiveEditor() ? o.getActiveEditor().isOpened() : !1,
    countRenderableColumns: () => this.view.countRenderableColumns(),
    countRenderableRows: () => this.view.countRenderableRows(),
    countRowHeaders: () => this.countRowHeaders(),
    countColHeaders: () => this.countColHeaders(),
    countRenderableRowsInRange: function() {
      return i.view.countRenderableRowsInRange(...arguments);
    },
    countRenderableColumnsInRange: function() {
      return i.view.countRenderableColumnsInRange(...arguments);
    },
    getShortcutManager: () => o.getShortcutManager(),
    createCellCoords: (f, p) => o._createCellCoords(f, p),
    createCellRange: (f, p, A) => o._createCellRange(f, p, A),
    visualToRenderableCoords: _,
    renderableToVisualCoords: O,
    findFirstNonHiddenRenderableRow: $,
    findFirstNonHiddenRenderableColumn: U,
    isDisabledCellSelection: (f, p) => f < 0 || p < 0 ? o.getSettings().disableVisualSelection : o.getCellMeta(f, p).disableVisualSelection
  });
  this.selection = G;
  const z = (f) => {
    let {
      hiddenIndexesChanged: p
    } = f;
    p && this.selection.commit();
  };
  this.columnIndexMapper.addLocalHook("cacheUpdated", z), this.rowIndexMapper.addLocalHook("cacheUpdated", z), this.selection.addLocalHook("afterSetRangeEnd", (f, p) => {
    const A = ji(!1), M = this.selection.getSelectedRange(), {
      from: P,
      to: F
    } = M.current(), Y = M.size() - 1;
    this.runHooks("afterSelection", P.row, P.col, F.row, F.col, A, Y), this.runHooks("afterSelectionByProp", P.row, o.colToProp(P.col), F.row, o.colToProp(F.col), A, Y), p && (!A.isTouched() || A.isTouched() && !A.value) && d.scrollTo(f);
    const K = G.isSelectedByRowHeader(), le = G.isSelectedByColumnHeader();
    K && le ? Q(this.rootElement, ["ht__selection--rows", "ht__selection--columns"]) : K ? (xe(this.rootElement, "ht__selection--columns"), Q(this.rootElement, "ht__selection--rows")) : le ? (xe(this.rootElement, "ht__selection--rows"), Q(this.rootElement, "ht__selection--columns")) : xe(this.rootElement, ["ht__selection--rows", "ht__selection--columns"]), G.getSelectionSource() !== "shift" && c.closeEditor(null), o.view.render(), c.prepareEditor();
  }), this.selection.addLocalHook("beforeSetFocus", (f) => {
    this.runHooks("beforeSelectionFocusSet", f.row, f.col);
  }), this.selection.addLocalHook("afterSetFocus", (f) => {
    const p = ji(!1);
    this.runHooks("afterSelectionFocusSet", f.row, f.col, p), (!p.isTouched() || p.isTouched() && !p.value) && d.scrollTo(f), c.closeEditor(), o.view.render(), c.prepareEditor();
  }), this.selection.addLocalHook("afterSelectionFinished", (f) => {
    const p = f.length - 1, {
      from: A,
      to: M
    } = f[p];
    this.runHooks("afterSelectionEnd", A.row, A.col, M.row, M.col, p), this.runHooks("afterSelectionEndByProp", A.row, o.colToProp(A.col), M.row, o.colToProp(M.col), p);
  }), this.selection.addLocalHook("afterIsMultipleSelection", (f) => {
    const p = this.runHooks("afterIsMultipleSelection", f.value);
    f.value && (f.value = p);
  }), this.selection.addLocalHook("afterDeselect", () => {
    c.closeEditor(), o.view.render(), xe(this.rootElement, ["ht__selection--rows", "ht__selection--columns"]), this.runHooks("afterDeselect");
  }), this.selection.addLocalHook("beforeHighlightSet", () => this.runHooks("beforeSelectionHighlightSet")).addLocalHook("beforeSetRangeStart", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("beforeSetRangeStart", ...p);
  }).addLocalHook("beforeSetRangeStartOnly", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("beforeSetRangeStartOnly", ...p);
  }).addLocalHook("beforeSetRangeEnd", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("beforeSetRangeEnd", ...p);
  }).addLocalHook("beforeSelectColumns", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("beforeSelectColumns", ...p);
  }).addLocalHook("afterSelectColumns", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("afterSelectColumns", ...p);
  }).addLocalHook("beforeSelectRows", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("beforeSelectRows", ...p);
  }).addLocalHook("afterSelectRows", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("afterSelectRows", ...p);
  }).addLocalHook("beforeModifyTransformStart", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("modifyTransformStart", ...p);
  }).addLocalHook("afterModifyTransformStart", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("afterModifyTransformStart", ...p);
  }).addLocalHook("beforeModifyTransformFocus", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("modifyTransformFocus", ...p);
  }).addLocalHook("afterModifyTransformFocus", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("afterModifyTransformFocus", ...p);
  }).addLocalHook("beforeModifyTransformEnd", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("modifyTransformEnd", ...p);
  }).addLocalHook("afterModifyTransformEnd", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("afterModifyTransformEnd", ...p);
  }).addLocalHook("beforeRowWrap", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("beforeRowWrap", ...p);
  }).addLocalHook("beforeColumnWrap", function() {
    for (var f = arguments.length, p = new Array(f), A = 0; A < f; A++)
      p[A] = arguments[A];
    return i.runHooks("beforeColumnWrap", ...p);
  }).addLocalHook("insertRowRequire", (f) => this.alter("insert_row_above", f, 1, "auto")).addLocalHook("insertColRequire", (f) => this.alter("insert_col_start", f, 1, "auto")), u = {
    /**
     * Inserts or removes rows and columns.
     *
     * @private
     * @param {string} action Possible values: "insert_row_above", "insert_row_below", "insert_col_start", "insert_col_end",
     *                        "remove_row", "remove_col".
     * @param {number|Array} index Row or column visual index which from the alter action will be triggered.
     *                             Alter actions such as "remove_row" and "remove_col" support array indexes in the
     *                             format `[[index, amount], [index, amount]...]` this can be used to remove
     *                             non-consecutive columns or rows in one call.
     * @param {number} [amount=1] Amount of rows or columns to remove.
     * @param {string} [source] Optional. Source of hook runner.
     * @param {boolean} [keepEmptyRows] Optional. Flag for preventing deletion of empty rows.
     */
    alter(f, p) {
      var A, M;
      let P = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1, F = arguments.length > 3 ? arguments[3] : void 0, Y = arguments.length > 4 ? arguments[4] : void 0;
      const K = (le) => {
        if (le.length === 0)
          return [];
        const ce = [...le];
        return ce.sort((He, Pe) => {
          let [W] = He, [Qe] = Pe;
          return W === Qe ? 0 : W > Qe ? 1 : -1;
        }), rn(ce, (He, Pe) => {
          let [W, Qe] = Pe;
          const Ge = He[He.length - 1], [it, Ve] = Ge, Re = it + Ve;
          if (W <= Re) {
            const v = Math.max(Qe - (Re - W), 0);
            Ge[1] += v;
          } else
            He.push([W, Qe]);
          return He;
        }, [ce[0]]);
      };
      switch (f) {
        case "insert_row_below":
        case "insert_row_above":
          const le = o.countSourceRows();
          if (S.maxRows === le)
            return;
          const ce = f === "insert_row_below" ? "below" : "above";
          p = (A = p) !== null && A !== void 0 ? A : ce === "below" ? le : 0;
          const {
            delta: Z,
            startPhysicalIndex: He
          } = a.createRow(p, P, {
            source: F,
            mode: ce
          });
          G.shiftRows(o.toVisualRow(He), Z);
          break;
        case "insert_col_start":
        case "insert_col_end":
          const Pe = f === "insert_col_end" ? "end" : "start";
          p = (M = p) !== null && M !== void 0 ? M : Pe === "end" ? o.countSourceCols() : 0;
          const {
            delta: W,
            startPhysicalIndex: Qe
          } = a.createCol(p, P, {
            source: F,
            mode: Pe
          });
          if (W) {
            if (Array.isArray(S.colHeaders)) {
              const Ve = [o.toVisualColumn(Qe), 0];
              Ve.length += W, Array.prototype.splice.apply(S.colHeaders, Ve);
            }
            G.shiftColumns(o.toVisualColumn(Qe), W);
          }
          break;
        case "remove_row":
          const Ge = (Ve) => {
            let Re = 0;
            T(Ve, (v) => {
              let [y, E] = v;
              const L = zt(y) ? o.countRows() - 1 : Math.max(y - Re, 0);
              if (Number.isInteger(y) && (y = Math.max(y - Re, 0)), !a.removeRow(y, E, F))
                return;
              if (G.isSelected()) {
                const {
                  row: k
                } = o.getSelectedRangeLast().highlight;
                k >= y && k <= y + E - 1 && c.closeEditor(!0);
              }
              const B = o.countRows();
              B === 0 ? G.deselect() : F === "ContextMenu.removeRow" ? G.refresh() : G.shiftRows(y, -E);
              const N = S.fixedRowsTop;
              N >= L + 1 && (S.fixedRowsTop -= Math.min(E, N - L));
              const x = S.fixedRowsBottom;
              x && L >= B - x && (S.fixedRowsBottom -= Math.min(E, x)), Re += E;
            });
          };
          Array.isArray(p) ? Ge(K(p)) : Ge([[p, P]]);
          break;
        case "remove_col":
          const it = (Ve) => {
            let Re = 0;
            T(Ve, (v) => {
              let [y, E] = v;
              const L = zt(y) ? o.countCols() - 1 : Math.max(y - Re, 0);
              let I = o.toPhysicalColumn(L);
              if (Number.isInteger(y) && (y = Math.max(y - Re, 0)), !a.removeCol(y, E, F))
                return;
              if (G.isSelected()) {
                const {
                  col: k
                } = o.getSelectedRangeLast().highlight;
                k >= y && k <= y + E - 1 && c.closeEditor(!0);
              }
              o.countCols() === 0 ? G.deselect() : F === "ContextMenu.removeColumn" ? G.refresh() : G.shiftColumns(y, -E);
              const x = S.fixedColumnsStart;
              x >= L + 1 && (S.fixedColumnsStart -= Math.min(E, x - L)), Array.isArray(S.colHeaders) && (typeof I == "undefined" && (I = -1), S.colHeaders.splice(I, E)), Re += E;
            });
          };
          Array.isArray(p) ? it(K(p)) : it([[p, P]]);
          break;
        default:
          throw new Error(`There is no such action "${f}"`);
      }
      o.view.render(), Y || u.adjustRowsAndCols();
    },
    /**
     * Makes sure there are empty rows at the bottom of the table.
     *
     * @private
     */
    adjustRowsAndCols() {
      const f = S.minRows, p = S.minSpareRows, A = S.minCols, M = S.minSpareCols;
      if (f) {
        const P = o.countRows();
        P < f && a.createRow(P, f - P, {
          source: "auto"
        });
      }
      if (p) {
        const P = o.countEmptyRows(!0);
        if (P < p) {
          const F = p - P, Y = Math.min(F, S.maxRows - o.countSourceRows());
          a.createRow(o.countRows(), Y, {
            source: "auto"
          });
        }
      }
      {
        let P;
        (A || M) && (P = o.countEmptyCols(!0));
        let F = o.countCols();
        if (A && !S.columns && F < A) {
          const Y = A - F;
          P += Y, a.createCol(F, Y, {
            source: "auto"
          });
        }
        if (M && !S.columns && o.dataType === "array" && P < M) {
          F = o.countCols();
          const Y = M - P, K = Math.min(Y, S.maxCols - F);
          a.createCol(F, K, {
            source: "auto"
          });
        }
      }
      o.view && o.view.adjustElementsSize();
    },
    /**
     * Populate the data from the provided 2d array from the given cell coordinates.
     *
     * @private
     * @param {object} start Start selection position. Visual indexes.
     * @param {Array} input 2d data array.
     * @param {object} [end] End selection position (only for drag-down mode). Visual indexes.
     * @param {string} [source="populateFromArray"] Source information string.
     * @param {string} [method="overwrite"] Populate method. Possible options: `shift_down`, `shift_right`, `overwrite`.
     * @returns {object|undefined} Ending td in pasted area (only if any cell was changed).
     */
    populateFromArray(f, p, A, M, P) {
      let F, Y, K, le;
      const ce = [], Z = {}, He = [], Pe = f.row, W = f.col;
      if (Y = p.length, Y === 0)
        return !1;
      let Qe = 0, Ge = 0;
      switch (Be(A) && (Qe = A.col - W + 1, Ge = A.row - Pe + 1), P) {
        case "shift_down":
          const it = rh(p), Ve = it.length, Re = Math.max(Ve, Qe), v = o.getData().slice(Pe), y = rh(v).slice(W, W + Re);
          for (K = 0; K < Re; K += 1)
            if (K < Ve) {
              for (F = 0, Y = it[K].length; F < Ge - Y; F += 1)
                it[K].push(it[K][F % Y]);
              K < y.length ? He.push(it[K].concat(y[K])) : He.push(it[K].concat(new Array(v.length).fill(null)));
            } else
              He.push(it[K % Ve].concat(y[K]));
          o.populateFromArray(Pe, W, rh(He));
          break;
        case "shift_right":
          const E = p.length, L = Math.max(E, Ge), I = o.getData().slice(Pe).map((ue) => ue.slice(W));
          for (F = 0; F < L; F += 1)
            if (F < E) {
              for (K = 0, le = p[F].length; K < Qe - le; K += 1)
                p[F].push(p[F][K % le]);
              if (F < I.length)
                for (let ue = 0; ue < I[F].length; ue += 1)
                  p[F].push(I[F][ue]);
              else
                p[F].push(...new Array(I[0].length).fill(null));
            } else
              p.push(p[F % Y].slice(0, L).concat(I[F]));
          o.populateFromArray(Pe, W, p);
          break;
        case "overwrite":
        default:
          Z.row = f.row, Z.col = f.col;
          let B = 0, N = 0, x = !0, k;
          const D = function(ye) {
            let Le = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
            const Ze = p[ye % p.length];
            return Le !== null ? Ze[Le % Ze.length] : Ze;
          }, V = p.length, ne = A ? A.row - f.row + 1 : 0;
          for (A ? Y = ne : Y = Math.max(V, ne), F = 0; F < Y && !(A && Z.row > A.row && ne > V || !S.allowInsertRow && Z.row > o.countRows() - 1 || Z.row >= S.maxRows); F++) {
            const ue = F - B, ye = D(ue).length, Le = A ? A.col - f.col + 1 : 0;
            if (A ? le = Le : le = Math.max(ye, Le), Z.col = f.col, k = o.getCellMeta(Z.row, Z.col), (M === "CopyPaste.paste" || M === "Autofill.fill") && k.skipRowOnPaste) {
              B += 1, Z.row += 1, Y += 1;
              continue;
            }
            for (N = 0, K = 0; K < le && !(A && Z.col > A.col && Le > ye || !S.allowInsertColumn && Z.col > o.countCols() - 1 || Z.col >= S.maxCols); K++) {
              if (k = o.getCellMeta(Z.row, Z.col), (M === "CopyPaste.paste" || M === "Autofill.fill") && k.skipColumnOnPaste) {
                N += 1, Z.col += 1, le += 1;
                continue;
              }
              if (k.readOnly && M !== "UndoRedo.undo") {
                Z.col += 1;
                continue;
              }
              const Ze = K - N;
              let Ue = D(ue, Ze), Oe = o.getDataAtCell(Z.row, Z.col);
              if (Ue !== null && typeof Ue == "object")
                if (Array.isArray(Ue) && Oe === null && (Oe = []), Oe === null || typeof Oe != "object")
                  x = !1;
                else {
                  const et = Tu(Array.isArray(Oe) ? Oe : Oe[0] || Oe), Ft = Tu(Array.isArray(Ue) ? Ue : Ue[0] || Ue);
                  Op(et, Ft) || Array.isArray(et) && Array.isArray(Ft) ? Ue = Fn(Ue) : x = !1;
                }
              else
                Oe !== null && typeof Oe == "object" && (x = !1);
              x && ce.push([Z.row, Z.col, Ue]), x = !0, Z.col += 1;
            }
            Z.row += 1;
          }
          o.setDataAtCell(ce, null, null, M || "populateFromArray");
          break;
      }
    }
  };
  function j(f) {
    const p = h1(f);
    tR(p) ? (o.runHooks("beforeLanguageChange", p), b.language = p, o.runHooks("afterLanguageChange", p)) : d1(f);
  }
  function J(f, p) {
    const A = f === "className" ? o.rootElement : o.table;
    if (g)
      Q(A, p);
    else {
      let M = [], P = [];
      b[f] && (M = Array.isArray(b[f]) ? b[f] : fy(b[f])), p && (P = Array.isArray(p) ? p : fy(p));
      const F = dy(M, P), Y = dy(P, M);
      F.length && xe(A, F), Y.length && Q(A, Y);
    }
    b[f] = p;
  }
  this.init = function() {
    l.setData(S.data), o.runHooks("beforeInit"), (Yo() || fb()) && Q(o.rootElement, "mobile"), this.updateSettings(S, !0), this.view = new SX(this), c = aK.getInstance(o, S, G), d = LZ(o), h = new uK(o), Yg(this) && IZ(o), o.runHooks("init"), this.forceFullRender = !0, this.view.render(), g && o.rootElement.offsetParent === null && zP(o.rootElement, () => {
      o.view._wt.wtOverlays.updateLastSpreaderSize(), o.render(), o.view.adjustElementsSize();
    }), typeof g == "object" && (o.runHooks("afterChange", g[0], g[1]), g = !1), o.runHooks("afterInit");
  };
  function q() {
    let f = !1;
    return {
      validatorsInQueue: 0,
      valid: !0,
      addValidatorToQueue() {
        this.validatorsInQueue += 1, f = !1;
      },
      removeValidatorFormQueue() {
        this.validatorsInQueue = this.validatorsInQueue - 1 < 0 ? 0 : this.validatorsInQueue - 1, this.checkIfQueueIsEmpty();
      },
      onQueueEmpty() {
      },
      checkIfQueueIsEmpty() {
        this.validatorsInQueue === 0 && f === !1 && (f = !0, this.onQueueEmpty(this.valid));
      }
    };
  }
  function re(f) {
    const p = f.replace(",", ".");
    return isNaN(parseFloat(p)) === !1 ? parseFloat(p) : f;
  }
  function me(f, p, A) {
    if (!f.length) {
      A();
      return;
    }
    const M = o.getActiveEditor(), P = new q();
    let F = !0;
    P.onQueueEmpty = () => {
      M && F && M.cancelChanges(), A();
    };
    for (let Y = f.length - 1; Y >= 0; Y--) {
      const [K, le, , ce] = f[Y], Z = a.propToCol(le);
      let He;
      Number.isInteger(Z) ? He = o.getCellMeta(K, Z) : He = Xe(Xe({}, Object.getPrototypeOf(S)), S), He.type === "numeric" && typeof ce == "string" && bx(ce) && (f[Y][3] = re(ce)), o.getCellValidator(He) && (P.addValidatorToQueue(), o.validateCell(f[Y][3], He, function(Pe, W) {
        return function(Qe) {
          if (typeof Qe != "boolean")
            throw new Error("Validation error: result is not boolean");
          Qe === !1 && W.allowInvalid === !1 && (F = !1, f.splice(Pe, 1), W.valid = !0), P.removeValidatorFormQueue();
        };
      }(Y, He), p));
    }
    P.checkIfQueueIsEmpty();
  }
  function Me(f, p) {
    for (let M = f.length - 1; M >= 0; M--) {
      let P = !1;
      if (f[M] === null) {
        f.splice(M, 1);
        continue;
      }
      if (!((f[M][2] === null || f[M][2] === void 0) && (f[M][3] === null || f[M][3] === void 0))) {
        if (S.allowInsertRow)
          for (; f[M][0] > o.countRows() - 1; ) {
            const {
              delta: F
            } = a.createRow(void 0, void 0, {
              source: p
            });
            if (F === 0) {
              P = !0;
              break;
            }
          }
        if (o.dataType === "array" && (!S.columns || S.columns.length === 0) && S.allowInsertColumn)
          for (; a.propToCol(f[M][1]) > o.countCols() - 1; ) {
            const {
              delta: F
            } = a.createCol(void 0, void 0, {
              source: p
            });
            if (F === 0) {
              P = !0;
              break;
            }
          }
        P || a.set(f[M][0], f[M][1], f[M][3]);
      }
    }
    const A = f.length > 0;
    if (o.forceFullRender = !0, A) {
      u.adjustRowsAndCols(), o.runHooks("beforeChangeRender", f, p), c.closeEditor(), o.view.render(), c.prepareEditor(), o.view.adjustElementsSize(), o.runHooks("afterChange", f, p || "edit");
      const M = o.getActiveEditor();
      M && de(M.refreshValue) && M.refreshValue();
    } else
      o.view.render();
  }
  this._createCellCoords = function(f, p) {
    return o.view._wt.createCellCoords(f, p);
  }, this._createCellRange = function(f, p, A) {
    return o.view._wt.createCellRange(f, p, A);
  }, this.validateCell = function(f, p, A, M) {
    let P = o.getCellValidator(p);
    function F(Y) {
      if (!(arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0) || p.hidden === !0) {
        A(Y);
        return;
      }
      const le = p.visualCol, ce = p.visualRow, Z = o.getCell(ce, le, !0);
      if (Z && Z.nodeName !== "TH") {
        const He = o.rowIndexMapper.getRenderableFromVisualIndex(ce), Pe = o.columnIndexMapper.getRenderableFromVisualIndex(le);
        o.view._wt.getSetting("cellRenderer", He, Pe, Z);
      }
      A(Y);
    }
    MP(P) && (P = function(Y) {
      return function(K, le) {
        le(Y.test(K));
      };
    }(P)), Et(P) ? (f = o.runHooks("beforeValidate", f, p.visualRow, p.prop, M), o._registerImmediate(() => {
      P.call(p, f, (Y) => {
        o && (Y = o.runHooks("afterValidate", Y, f, p.visualRow, p.prop, M), p.valid = Y, F(Y), o.runHooks("postAfterValidate", Y, f, p.visualRow, p.prop, M));
      });
    })) : o._registerImmediate(() => {
      p.valid = !0, F(p.valid, !1);
    });
  };
  function nt(f, p, A) {
    return Array.isArray(f) ? f : [[f, p, A]];
  }
  function Ne(f, p) {
    const A = o.getActiveEditor(), M = o.runHooks("beforeChange", f, p || "edit"), P = f.filter((F) => F !== null);
    return M === !1 || P.length === 0 ? (A && A.cancelChanges(), []) : P;
  }
  this.setDataAtCell = function(f, p, A, M) {
    const P = nt(f, p, A), F = [];
    let Y = M, K, le, ce;
    for (K = 0, le = P.length; K < le; K++) {
      if (typeof P[K] != "object")
        throw new Error("Method `setDataAtCell` accepts row number or changes array of arrays as its first parameter");
      if (typeof P[K][1] != "number")
        throw new Error("Method `setDataAtCell` accepts row and column number as its parameters. If you want to use object property name, use method `setDataAtRowProp`");
      P[K][1] >= this.countCols() ? ce = P[K][1] : ce = a.colToProp(P[K][1]), F.push([P[K][0], ce, l.getAtCell(this.toPhysicalRow(P[K][0]), P[K][1]), P[K][2]]);
    }
    !Y && typeof f == "object" && (Y = p);
    const Z = Ne(F, M);
    o.runHooks("afterSetDataAtCell", Z, Y), me(Z, Y, () => {
      Me(Z, Y);
    });
  }, this.setDataAtRowProp = function(f, p, A, M) {
    const P = nt(f, p, A), F = [];
    let Y = M, K, le;
    for (K = 0, le = P.length; K < le; K++)
      F.push([P[K][0], P[K][1], l.getAtCell(this.toPhysicalRow(P[K][0]), P[K][1]), P[K][2]]);
    !Y && typeof f == "object" && (Y = p);
    const ce = Ne(F, M);
    o.runHooks("afterSetDataAtRowProp", ce, Y), me(ce, Y, () => {
      Me(ce, Y);
    });
  }, this.listen = function() {
    o && !o.isListening() && (EC.forEach((f) => {
      o !== f && f.unlisten();
    }), RC = o.guid, o.runHooks("afterListen"));
  }, this.unlisten = function() {
    this.isListening() && (RC = null, o.runHooks("afterUnlisten"));
  }, this.isListening = function() {
    return RC === o.guid;
  }, this.destroyEditor = function() {
    let f = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1, p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
    c.closeEditor(f), o.view.render(), p && G.isSelected() && c.prepareEditor();
  }, this.populateFromArray = function(f, p, A, M, P, F, Y) {
    if (!(typeof A == "object" && typeof A[0] == "object"))
      throw new Error("populateFromArray parameter `input` must be an array of arrays");
    const K = typeof M == "number" ? o._createCellCoords(M, P) : null;
    return u.populateFromArray(o._createCellCoords(f, p), A, K, F, Y);
  }, this.spliceCol = function(f, p, A) {
    for (var M = arguments.length, P = new Array(M > 3 ? M - 3 : 0), F = 3; F < M; F++)
      P[F - 3] = arguments[F];
    return a.spliceCol(f, p, A, ...P);
  }, this.spliceRow = function(f, p, A) {
    for (var M = arguments.length, P = new Array(M > 3 ? M - 3 : 0), F = 3; F < M; F++)
      P[F - 3] = arguments[F];
    return a.spliceRow(f, p, A, ...P);
  }, this.getSelected = function() {
    if (G.isSelected())
      return Ke(G.getSelectedRange(), (f) => {
        let {
          from: p,
          to: A
        } = f;
        return [p.row, p.col, A.row, A.col];
      });
  }, this.getSelectedLast = function() {
    const f = this.getSelected();
    let p;
    return f && f.length > 0 && (p = f[f.length - 1]), p;
  }, this.getSelectedRange = function() {
    if (G.isSelected())
      return Array.from(G.getSelectedRange());
  }, this.getSelectedRangeLast = function() {
    const f = this.getSelectedRange();
    let p;
    return f && f.length > 0 && (p = f[f.length - 1]), p;
  }, this.emptySelectedCells = function(f) {
    if (!G.isSelected() || this.countRows() === 0 || this.countCols() === 0)
      return;
    const p = [];
    T(G.getSelectedRange(), (A) => {
      if (A.isSingleHeader())
        return;
      const M = A.getTopStartCorner(), P = A.getBottomEndCorner();
      Ae(M.row, P.row, (F) => {
        Ae(M.col, P.col, (Y) => {
          this.getCellMeta(F, Y).readOnly || p.push([F, Y, null]);
        });
      });
    }), p.length > 0 && this.setDataAtCell(p, f);
  }, this.isRenderSuspended = function() {
    return this.renderSuspendedCounter > 0;
  }, this.suspendRender = function() {
    this.renderSuspendedCounter += 1;
  }, this.resumeRender = function() {
    const f = this.renderSuspendedCounter - 1;
    this.renderSuspendedCounter = Math.max(f, 0), !this.isRenderSuspended() && f === this.renderSuspendedCounter && (this.renderCall ? this.render() : o.view.render());
  }, this.render = function() {
    this.view && (this.renderCall = !0, this.forceFullRender = !0, this.isRenderSuspended() || o.view.render());
  }, this.batchRender = function(f) {
    this.suspendRender();
    const p = f();
    return this.resumeRender(), p;
  }, this.isExecutionSuspended = function() {
    return this.executionSuspendedCounter > 0;
  }, this.suspendExecution = function() {
    this.executionSuspendedCounter += 1, this.columnIndexMapper.suspendOperations(), this.rowIndexMapper.suspendOperations();
  }, this.resumeExecution = function() {
    let f = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    const p = this.executionSuspendedCounter - 1;
    this.executionSuspendedCounter = Math.max(p, 0), (!this.isExecutionSuspended() && p === this.executionSuspendedCounter || f) && (this.columnIndexMapper.resumeOperations(), this.rowIndexMapper.resumeOperations());
  }, this.batchExecution = function(f) {
    let p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    this.suspendExecution();
    const A = f();
    return this.resumeExecution(p), A;
  }, this.batch = function(f) {
    this.suspendRender(), this.suspendExecution();
    const p = f();
    return this.resumeExecution(), this.resumeRender(), p;
  }, this.refreshDimensions = function() {
    if (!o.view)
      return;
    const {
      width: f,
      height: p
    } = o.view.getLastSize(), {
      width: A,
      height: M
    } = o.rootElement.getBoundingClientRect(), P = A !== f || M !== p;
    o.runHooks("beforeRefreshDimensions", {
      width: f,
      height: p
    }, {
      width: A,
      height: M
    }, P) !== !1 && ((P || o.view._wt.wtOverlays.scrollableElement === o.rootWindow) && (o.view.setLastSize(A, M), o.render()), o.runHooks("afterRefreshDimensions", {
      width: f,
      height: p
    }, {
      width: A,
      height: M
    }, P));
  }, this.updateData = function(f, p) {
    ET(f, (A) => {
      a = A;
    }, (A) => {
      a = A, o.columnIndexMapper.fitToLength(this.getInitialColumnCount()), o.rowIndexMapper.fitToLength(this.countSourceRows()), u.adjustRowsAndCols(), G.refresh();
    }, {
      hotInstance: o,
      dataMap: a,
      dataSource: l,
      internalSource: "updateData",
      source: p,
      metaManager: C,
      firstRun: g
    });
  }, this.loadData = function(f, p) {
    ET(f, (A) => {
      a = A;
    }, () => {
      C.clearCellsCache(), o.initIndexMappers(), u.adjustRowsAndCols(), G.refresh(), g && (g = [null, "loadData"]);
    }, {
      hotInstance: o,
      dataMap: a,
      dataSource: l,
      internalSource: "loadData",
      source: p,
      metaManager: C,
      firstRun: g
    });
  }, this.getInitialColumnCount = function() {
    const f = S.columns;
    let p = 0;
    if (Array.isArray(f))
      p = f.length;
    else if (Et(f))
      if (o.dataType === "array") {
        const A = this.countSourceCols();
        for (let M = 0; M < A; M += 1)
          f(M) && (p += 1);
      } else
        (o.dataType === "object" || o.dataType === "function") && (p = a.colToPropCache.length);
    else if (de(S.dataSchema)) {
      const A = a.getSchema();
      p = Array.isArray(A) ? A.length : Pp(A);
    } else
      p = this.countSourceCols();
    return p;
  }, this.initIndexMappers = function() {
    this.columnIndexMapper.initToLength(this.getInitialColumnCount()), this.rowIndexMapper.initToLength(this.countSourceRows());
  }, this.getData = function(f, p, A, M) {
    return Mt(f) ? a.getAll() : a.getRange(o._createCellCoords(f, p), o._createCellCoords(A, M), a.DESTINATION_RENDERER);
  }, this.getCopyableText = function(f, p, A, M) {
    return a.getCopyableText(o._createCellCoords(f, p), o._createCellCoords(A, M));
  }, this.getCopyableData = function(f, p) {
    return a.getCopyable(f, a.colToProp(p));
  }, this.getSchema = function() {
    return a.getSchema();
  }, this.updateSettings = function(f) {
    let p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    const A = (g ? o.loadData : o.updateData).bind(this);
    let M = !1, P, F;
    if (de(f.rows))
      throw new Error('The "rows" setting is no longer supported. Do you mean startRows, minRows or maxRows?');
    if (de(f.cols))
      throw new Error('The "cols" setting is no longer supported. Do you mean startCols, minCols or maxCols?');
    if (de(f.ganttChart))
      throw new Error('Since 8.0.0 the "ganttChart" setting is no longer supported.');
    for (P in f)
      P === "data" || (P === "language" ? j(f.language) : P === "className" ? J("className", f.className) : P === "tableClassName" && o.table ? (J("tableClassName", f.tableClassName), o.view._wt.wtOverlays.syncOverlayTableClassNames()) : he.getSingleton().isRegistered(P) || he.getSingleton().isDeprecated(P) ? (Et(f[P]) || Array.isArray(f[P])) && (f[P].initialHook = !0, o.addHook(P, f[P])) : !p && Bt(f, P) && (b[P] = f[P]));
    f.data === void 0 && S.data === void 0 ? A(null, "updateSettings") : f.data !== void 0 ? A(f.data, "updateSettings") : f.columns !== void 0 && (a.createMap(), o.initIndexMappers());
    const Y = o.countCols(), K = S.columns;
    if (K && Et(K) && (M = !0), (f.cell !== void 0 || f.cells !== void 0 || f.columns !== void 0) && C.clearCache(), Y > 0)
      for (P = 0, F = 0; P < Y; P++) {
        if (K) {
          const Z = M ? K(P) : K[F];
          Z && C.updateColumnMeta(F, Z);
        }
        F += 1;
      }
    de(f.cell) && Fe(f.cell, (Z) => {
      o.setCellMetaObject(Z.row, Z.col, Z);
    }), o.runHooks("afterCellMetaReset");
    let le = o.rootElement.style.height;
    le !== "" && (le = parseInt(o.rootElement.style.height, 10));
    let ce = f.height;
    if (Et(ce) && (ce = ce()), p && o.rootElement.getAttribute("style") && o.rootElement.setAttribute("data-initialstyle", o.rootElement.getAttribute("style")), ce === null) {
      const Z = o.rootElement.getAttribute("data-initialstyle");
      Z && (Z.indexOf("height") > -1 || Z.indexOf("overflow") > -1) ? o.rootElement.setAttribute("style", Z) : (o.rootElement.style.height = "", o.rootElement.style.overflow = "");
    } else
      ce !== void 0 && (o.rootElement.style.height = isNaN(ce) ? `${ce}` : `${ce}px`, o.rootElement.style.overflow = "hidden");
    if (typeof f.width != "undefined") {
      let Z = f.width;
      Et(Z) && (Z = Z()), o.rootElement.style.width = isNaN(Z) ? `${Z}` : `${Z}px`;
    }
    p || (o.view && (o.view._wt.wtViewport.resetHasOversizedColumnHeadersMarked(), o.view._wt.exportSettingsAsClassNames()), o.runHooks("afterUpdateSettings", f)), u.adjustRowsAndCols(), o.view && !g && (o.forceFullRender = !0, o.view.render(), o.view._wt.wtOverlays.adjustElementsSize()), !p && o.view && (le === "" || ce === "" || ce === void 0) && le !== ce && o.view._wt.wtOverlays.updateMainScrollableElements();
  }, this.getValue = function() {
    const f = o.getSelectedLast();
    if (S.getValue) {
      if (Et(S.getValue))
        return S.getValue.call(o);
      if (f)
        return o.getData()[f[0][0]][S.getValue];
    } else if (f)
      return o.getDataAtCell(f[0], f[1]);
  }, this.getSettings = function() {
    return S;
  }, this.clear = function() {
    this.selectAll(), this.emptySelectedCells();
  }, this.alter = function(f, p, A, M, P) {
    u.alter(f, p, A, M, P);
  }, this.getCell = function(f, p) {
    let A = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1, M = p, P = f;
    if (p >= 0) {
      if (this.columnIndexMapper.isHidden(this.toPhysicalColumn(p)))
        return null;
      M = this.columnIndexMapper.getRenderableFromVisualIndex(p);
    }
    if (f >= 0) {
      if (this.rowIndexMapper.isHidden(this.toPhysicalRow(f)))
        return null;
      P = this.rowIndexMapper.getRenderableFromVisualIndex(f);
    }
    return P === null || M === null || P === void 0 || M === void 0 ? null : o.view.getCellAtCoords(o._createCellCoords(P, M), A);
  }, this.getCoords = function(f) {
    const p = this.view._wt.wtTable.getCoords(f);
    if (p === null)
      return null;
    const {
      row: A,
      col: M
    } = p;
    let P = A, F = M;
    return A >= 0 && (P = this.rowIndexMapper.getVisualFromRenderableIndex(A)), M >= 0 && (F = this.columnIndexMapper.getVisualFromRenderableIndex(M)), o._createCellCoords(P, F);
  }, this.colToProp = function(f) {
    return a.colToProp(f);
  }, this.propToCol = function(f) {
    return a.propToCol(f);
  }, this.toVisualRow = (f) => this.rowIndexMapper.getVisualFromPhysicalIndex(f), this.toVisualColumn = (f) => this.columnIndexMapper.getVisualFromPhysicalIndex(f), this.toPhysicalRow = (f) => this.rowIndexMapper.getPhysicalFromVisualIndex(f), this.toPhysicalColumn = (f) => this.columnIndexMapper.getPhysicalFromVisualIndex(f), this.getDataAtCell = function(f, p) {
    return a.get(f, a.colToProp(p));
  }, this.getDataAtRowProp = function(f, p) {
    return a.get(f, p);
  }, this.getDataAtCol = function(f) {
    const p = [], A = a.getRange(o._createCellCoords(0, f), o._createCellCoords(S.data.length - 1, f), a.DESTINATION_RENDERER);
    for (let M = 0; M < A.length; M += 1)
      for (let P = 0; P < A[M].length; P += 1)
        p.push(A[M][P]);
    return p;
  }, this.getDataAtProp = function(f) {
    const p = [], A = a.getRange(o._createCellCoords(0, a.propToCol(f)), o._createCellCoords(S.data.length - 1, a.propToCol(f)), a.DESTINATION_RENDERER);
    for (let M = 0; M < A.length; M += 1)
      for (let P = 0; P < A[M].length; P += 1)
        p.push(A[M][P]);
    return p;
  }, this.getSourceData = function(f, p, A, M) {
    let P;
    return f === void 0 ? P = l.getData() : P = l.getByRange(o._createCellCoords(f, p), o._createCellCoords(A, M)), P;
  }, this.getSourceDataArray = function(f, p, A, M) {
    let P;
    return f === void 0 ? P = l.getData(!0) : P = l.getByRange(o._createCellCoords(f, p), o._createCellCoords(A, M), !0), P;
  }, this.getSourceDataAtCol = function(f) {
    return l.getAtColumn(f);
  }, this.setSourceDataAtCell = function(f, p, A, M) {
    const P = nt(f, p, A), F = this.hasHook("afterSetSourceDataAtCell"), Y = [];
    F && T(P, (le) => {
      let [ce, Z, He] = le;
      Y.push([
        ce,
        Z,
        l.getAtCell(ce, Z),
        // The previous value.
        He
      ]);
    }), T(P, (le) => {
      let [ce, Z, He] = le;
      l.setAtCell(ce, Z, He);
    }), F && this.runHooks("afterSetSourceDataAtCell", Y, M), this.render();
    const K = o.getActiveEditor();
    K && de(K.refreshValue) && K.refreshValue();
  }, this.getSourceDataAtRow = function(f) {
    return l.getAtRow(f);
  }, this.getSourceDataAtCell = function(f, p) {
    return l.getAtCell(f, p);
  }, this.getDataAtRow = function(f) {
    return a.getRange(o._createCellCoords(f, 0), o._createCellCoords(f, this.countCols() - 1), a.DESTINATION_RENDERER)[0] || [];
  }, this.getDataType = function(f, p, A, M) {
    const P = f === void 0 ? [0, 0, this.countRows(), this.countCols()] : [f, p, A, M], [F, Y] = P;
    let [, , K, le] = P, ce = null, Z = null;
    K === void 0 && (K = F), le === void 0 && (le = Y);
    let He = "mixed";
    return Ae(Math.max(Math.min(F, K), 0), Math.max(F, K), (Pe) => {
      let W = !0;
      return Ae(Math.max(Math.min(Y, le), 0), Math.max(Y, le), (Qe) => (Z = this.getCellMeta(Pe, Qe).type, ce ? W = ce === Z : ce = Z, W)), He = W ? Z : "mixed", W;
    }), He;
  }, this.removeCellMeta = function(f, p, A) {
    const [M, P] = [this.toPhysicalRow(f), this.toPhysicalColumn(p)];
    let F = C.getCellMetaKeyValue(M, P, A);
    o.runHooks("beforeRemoveCellMeta", f, p, A, F) !== !1 && (C.removeCellMeta(M, P, A), o.runHooks("afterRemoveCellMeta", f, p, A, F)), F = null;
  }, this.spliceCellsMeta = function(f) {
    let p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
    for (var A = arguments.length, M = new Array(A > 2 ? A - 2 : 0), P = 2; P < A; P++)
      M[P - 2] = arguments[P];
    if (M.length > 0 && !Array.isArray(M[0]))
      throw new Error("The 3rd argument (cellMetaRows) has to be passed as an array of cell meta objects array.");
    p > 0 && C.removeRow(this.toPhysicalRow(f), p), M.length > 0 && T(M.reverse(), (F) => {
      C.createRow(this.toPhysicalRow(f)), T(F, (Y, K) => this.setCellMetaObject(f, K, Y));
    }), o.render();
  }, this.setCellMetaObject = function(f, p, A) {
    typeof A == "object" && Fe(A, (M, P) => {
      this.setCellMeta(f, p, P, M);
    });
  }, this.setCellMeta = function(f, p, A, M) {
    if (o.runHooks("beforeSetCellMeta", f, p, A, M) === !1)
      return;
    let F = f, Y = p;
    f < this.countRows() && (F = this.toPhysicalRow(f)), p < this.countCols() && (Y = this.toPhysicalColumn(p)), C.setCellMeta(F, Y, A, M), o.runHooks("afterSetCellMeta", f, p, A, M);
  }, this.getCellsMeta = function() {
    return C.getCellsMeta();
  }, this.getCellMeta = function(f, p) {
    let A = this.toPhysicalRow(f), M = this.toPhysicalColumn(p);
    return A === null && (A = f), M === null && (M = p), C.getCellMeta(A, M, {
      visualRow: f,
      visualColumn: p
    });
  }, this.getColumnMeta = function(f) {
    return C.getColumnMeta(this.toPhysicalColumn(f));
  }, this.getCellMetaAtRow = function(f) {
    return C.getCellsMetaAtRow(f);
  }, this.isColumnModificationAllowed = function() {
    return !(o.dataType === "object" || S.columns);
  }, this.getCellRenderer = function(f, p) {
    const A = typeof f == "number" ? o.getCellMeta(f, p).renderer : f.renderer;
    return typeof A == "string" ? vm(A) : Mt(A) ? vm("text") : A;
  }, this.getCellEditor = function(f, p) {
    const A = typeof f == "number" ? o.getCellMeta(f, p).editor : f.editor;
    return typeof A == "string" ? Cm(A) : Mt(A) ? Cm("text") : A;
  }, this.getCellValidator = function(f, p) {
    const A = typeof f == "number" ? o.getCellMeta(f, p).validator : f.validator;
    return typeof A == "string" ? bb(A) : A;
  }, this.validateCells = function(f) {
    this._validateCells(f);
  }, this.validateRows = function(f, p) {
    if (!Array.isArray(f))
      throw new Error("validateRows parameter `rows` must be an array");
    this._validateCells(p, f);
  }, this.validateColumns = function(f, p) {
    if (!Array.isArray(f))
      throw new Error("validateColumns parameter `columns` must be an array");
    this._validateCells(p, void 0, f);
  }, this._validateCells = function(f, p, A) {
    const M = new q();
    f && (M.onQueueEmpty = f);
    let P = o.countRows() - 1;
    for (; P >= 0; ) {
      if (p !== void 0 && p.indexOf(P) === -1) {
        P -= 1;
        continue;
      }
      let F = o.countCols() - 1;
      for (; F >= 0; ) {
        if (A !== void 0 && A.indexOf(F) === -1) {
          F -= 1;
          continue;
        }
        M.addValidatorToQueue(), o.validateCell(o.getDataAtCell(P, F), o.getCellMeta(P, F), (Y) => {
          if (typeof Y != "boolean")
            throw new Error("Validation error: result is not boolean");
          Y === !1 && (M.valid = !1), M.removeValidatorFormQueue();
        }, "validateCells"), F -= 1;
      }
      P -= 1;
    }
    M.checkIfQueueIsEmpty();
  }, this.getRowHeader = function(f) {
    let p = S.rowHeaders, A = f;
    return A !== void 0 && (A = o.runHooks("modifyRowHeader", A)), A === void 0 ? (p = [], Ae(o.countRows() - 1, (M) => {
      p.push(o.getRowHeader(M));
    })) : Array.isArray(p) && p[A] !== void 0 ? p = p[A] : Et(p) ? p = p(A) : p && typeof p != "string" && typeof p != "number" && (p = A + 1), p;
  }, this.hasRowHeaders = function() {
    return !!S.rowHeaders;
  }, this.hasColHeaders = function() {
    if (S.colHeaders !== void 0 && S.colHeaders !== null)
      return !!S.colHeaders;
    for (let f = 0, p = o.countCols(); f < p; f++)
      if (o.getColHeader(f))
        return !0;
    return !1;
  }, this.getColHeader = function(f) {
    let p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : -1;
    const A = o.runHooks("modifyColHeader", f);
    if (A === void 0) {
      const K = [], le = o.countCols();
      for (let ce = 0; ce < le; ce++)
        K.push(o.getColHeader(ce));
      return K;
    }
    let M = S.colHeaders;
    const P = function(K) {
      const le = [], ce = o.countCols();
      let Z = 0;
      for (; Z < ce; Z++)
        Et(S.columns) && S.columns(Z) && le.push(Z);
      return le[K];
    }, F = o.toPhysicalColumn(A), Y = P(F);
    return S.colHeaders === !1 ? M = null : S.columns && Et(S.columns) && S.columns(Y) && S.columns(Y).title ? M = S.columns(Y).title : S.columns && S.columns[F] && S.columns[F].title ? M = S.columns[F].title : Array.isArray(S.colHeaders) && S.colHeaders[F] !== void 0 ? M = S.colHeaders[F] : Et(S.colHeaders) ? M = S.colHeaders(F) : S.colHeaders && typeof S.colHeaders != "string" && typeof S.colHeaders != "number" && (M = Dp(A)), M = o.runHooks("modifyColumnHeaderValue", M, f, p), M;
  }, this._getColWidthFromSettings = function(f) {
    let p;
    if (f >= 0 && (p = o.getCellMeta(0, f).width), (p === void 0 || p === S.width) && (p = S.colWidths), p != null) {
      switch (typeof p) {
        case "object":
          p = p[f];
          break;
        case "function":
          p = p(f);
          break;
      }
      typeof p == "string" && (p = parseInt(p, 10));
    }
    return p;
  }, this.getColWidth = function(f) {
    let p = o._getColWidthFromSettings(f);
    return p = o.runHooks("modifyColWidth", p, f), p === void 0 && (p = Np), p;
  }, this._getRowHeightFromSettings = function(f) {
    let p = S.rowHeights;
    if (p != null) {
      switch (typeof p) {
        case "object":
          p = p[f];
          break;
        case "function":
          p = p(f);
          break;
      }
      typeof p == "string" && (p = parseInt(p, 10));
    }
    return p;
  }, this.getRowHeight = function(f) {
    let p = o._getRowHeightFromSettings(f);
    return p = o.runHooks("modifyRowHeight", p, f), p;
  }, this.countSourceRows = function() {
    return l.countRows();
  }, this.countSourceCols = function() {
    return l.countFirstRowKeys();
  }, this.countRows = function() {
    return a.getLength();
  }, this.countCols = function() {
    const f = S.maxCols, p = this.columnIndexMapper.getNotTrimmedIndexesLength();
    return Math.min(f, p);
  }, this.countRenderedRows = function() {
    return o.view._wt.drawn ? o.view._wt.wtTable.getRenderedRowsCount() : -1;
  }, this.countVisibleRows = function() {
    return o.view._wt.drawn ? o.view._wt.wtTable.getVisibleRowsCount() : -1;
  }, this.countRenderedCols = function() {
    return o.view._wt.drawn ? o.view._wt.wtTable.getRenderedColumnsCount() : -1;
  }, this.countVisibleCols = function() {
    return o.view._wt.drawn ? o.view._wt.wtTable.getVisibleColumnsCount() : -1;
  }, this.countRowHeaders = function() {
    return this.view.getRowHeadersCount();
  }, this.countColHeaders = function() {
    return this.view.getColumnHeadersCount();
  }, this.countEmptyRows = function() {
    let f = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1, p = 0;
    return My(o.countRows() - 1, (A) => {
      if (o.isEmptyRow(A))
        p += 1;
      else if (f === !0)
        return !1;
    }), p;
  }, this.countEmptyCols = function() {
    let f = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1, p = 0;
    return My(o.countCols() - 1, (A) => {
      if (o.isEmptyCol(A))
        p += 1;
      else if (f === !0)
        return !1;
    }), p;
  }, this.isEmptyRow = function(f) {
    return S.isEmptyRow.call(o, f);
  }, this.isEmptyCol = function(f) {
    return S.isEmptyCol.call(o, f);
  }, this.selectCell = function(f, p, A, M) {
    let P = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : !0, F = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : !0;
    return Mt(f) || Mt(p) ? !1 : this.selectCells([[f, p, A, M]], P, F);
  }, this.selectCells = function() {
    let f = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : [[]], p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, A = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
    p === !1 && d.suspend();
    const M = G.selectCells(f);
    return M && A && o.listen(), d.resume(), M;
  }, this.selectColumns = function(f) {
    let p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : f, A = arguments.length > 2 ? arguments[2] : void 0;
    return G.selectColumns(f, p, A);
  }, this.selectRows = function(f) {
    let p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : f, A = arguments.length > 2 ? arguments[2] : void 0;
    return G.selectRows(f, p, A);
  }, this.deselectCell = function() {
    G.deselect();
  }, this.selectAll = function() {
    let f = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0, p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : f, A = arguments.length > 2 ? arguments[2] : void 0;
    d.skipNextScrollCycle(), G.selectAll(f, p, A);
  };
  const Ce = (f, p) => f.getNearestNotHiddenIndex(p, 1, !0);
  this.scrollViewportTo = function(f) {
    var p;
    if (typeof f == "number") {
      var A;
      f = {
        row: arguments[0],
        col: arguments[1],
        verticalSnap: arguments[2] ? "bottom" : "top",
        horizontalSnap: arguments[3] ? "end" : "start",
        considerHiddenIndexes: (A = arguments[4]) !== null && A !== void 0 ? A : !0
      };
    }
    const {
      row: M,
      col: P,
      verticalSnap: F,
      horizontalSnap: Y,
      considerHiddenIndexes: K
    } = (p = f) !== null && p !== void 0 ? p : {};
    let le, ce, Z, He;
    F !== void 0 && (le = F === "top", ce = !le), Y !== void 0 && (Z = Y === "start", He = !Z);
    let Pe = M, W = P;
    if (K === void 0 || K) {
      const it = Number.isInteger(M) && M >= 0, Ve = Number.isInteger(P) && P >= 0, Re = it ? Ce(this.rowIndexMapper, M) : void 0, v = Ve ? Ce(this.columnIndexMapper, P) : void 0;
      if (Re === null || v === null)
        return !1;
      Pe = it ? o.rowIndexMapper.getRenderableFromVisualIndex(Re) : M, W = Ve ? o.columnIndexMapper.getRenderableFromVisualIndex(v) : P;
    }
    const Qe = Number.isInteger(Pe), Ge = Number.isInteger(W);
    return Qe && Pe >= 0 && Ge && W >= 0 ? o.view.scrollViewport(o._createCellCoords(Pe, W), le, He, ce, Z) : Qe && Pe >= 0 && (Ge && W < 0 || !Ge) ? o.view.scrollViewportVertically(Pe, le, ce) : Ge && W >= 0 && (Qe && Pe < 0 || !Qe) ? o.view.scrollViewportHorizontally(W, He, Z) : !1;
  }, this.scrollToFocusedCell = function() {
    let f = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : () => {
    };
    if (!this.selection.isSelected())
      return;
    this.addHookOnce("afterScroll", f);
    const {
      highlight: p
    } = this.getSelectedRangeLast();
    this.scrollViewportTo(p.toObject()) ? this.view.render() : (this.removeHook("afterScroll", f), this._registerImmediate(() => f()));
  }, this.destroy = function() {
    if (o._clearTimeouts(), o._clearImmediates(), o.view && o.view.destroy(), l && l.destroy(), l = null, this.getShortcutManager().destroy(), C.clearCache(), EC.delete(this.guid), Yg(o)) {
      const f = this.rootDocument.querySelector(".hot-display-license-info");
      f && f.parentNode.removeChild(f);
    }
    br(o.rootElement), s.destroy(), c && c.destroy(), o.batchExecution(() => {
      o.rowIndexMapper.unregisterAll(), o.columnIndexMapper.unregisterAll(), R.getItems().forEach((f) => {
        let [, p] = f;
        p.destroy();
      }), R.clear(), o.runHooks("afterDestroy");
    }, !0), he.getSingleton().destroy(o), Fe(o, (f, p, A) => {
      Et(f) ? A[p] = pe(p) : p !== "guid" && (A[p] = null);
    }), o.isDestroyed = !0, a && a.destroy(), a = null, u = null, G = null, c = null, o = null;
  };
  function pe(f) {
    return () => {
      throw new Error(`The "${f}" method cannot be called because this Handsontable instance has been destroyed`);
    };
  }
  this.getActiveEditor = function() {
    return c.getActiveEditor();
  }, this.getFirstRenderedVisibleRow = function() {
    return o.view.getFirstRenderedVisibleRow();
  }, this.getLastRenderedVisibleRow = function() {
    return o.view.getLastRenderedVisibleRow();
  }, this.getFirstRenderedVisibleColumn = function() {
    return o.view.getFirstRenderedVisibleColumn();
  }, this.getLastRenderedVisibleColumn = function() {
    return o.view.getLastRenderedVisibleColumn();
  }, this.getFirstFullyVisibleRow = function() {
    return o.view.getFirstFullyVisibleRow();
  }, this.getLastFullyVisibleRow = function() {
    return o.view.getLastFullyVisibleRow();
  }, this.getFirstFullyVisibleColumn = function() {
    return o.view.getFirstFullyVisibleColumn();
  }, this.getLastFullyVisibleColumn = function() {
    return o.view.getLastFullyVisibleColumn();
  }, this.getFirstPartiallyVisibleRow = function() {
    return o.view.getFirstPartiallyVisibleRow();
  }, this.getLastPartiallyVisibleRow = function() {
    return o.view.getLastPartiallyVisibleRow();
  }, this.getFirstPartiallyVisibleColumn = function() {
    return o.view.getFirstPartiallyVisibleColumn();
  }, this.getLastPartiallyVisibleColumn = function() {
    return o.view.getLastPartiallyVisibleColumn();
  }, this.getPlugin = function(f) {
    const p = Bn(f);
    return p === "UndoRedo" ? this.undoRedo : R.getItem(p);
  }, this.getPluginName = function(f) {
    return f === this.undoRedo ? this.undoRedo.constructor.PLUGIN_KEY : R.getId(f);
  }, this.getInstance = function() {
    return o;
  }, this.addHook = function(f, p, A) {
    he.getSingleton().add(f, p, o, A);
  }, this.hasHook = function(f) {
    return he.getSingleton().has(f, o) || he.getSingleton().has(f);
  }, this.addHookOnce = function(f, p, A) {
    he.getSingleton().once(f, p, o, A);
  }, this.removeHook = function(f, p) {
    he.getSingleton().remove(f, p, o);
  }, this.runHooks = function(f, p, A, M, P, F, Y) {
    return he.getSingleton().run(o, f, p, A, M, P, F, Y);
  }, this.getTranslatedPhrase = function(f, p) {
    return w1(S.language, f, p);
  }, this.toHTML = () => Ey(this), this.toTableElement = () => {
    const f = this.rootDocument.createElement("div");
    return f.insertAdjacentHTML("afterbegin", Ey(this)), f.firstElementChild;
  }, this.timeouts = [], this._registerTimeout = function(f) {
    let p = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, A = f;
    typeof A == "function" && (A = setTimeout(A, p)), this.timeouts.push(A);
  }, this._clearTimeouts = function() {
    T(this.timeouts, (f) => {
      clearTimeout(f);
    });
  }, this.immediates = [], this._registerImmediate = function(f) {
    this.immediates.push(setImmediate(f));
  }, this._clearImmediates = function() {
    T(this.immediates, (f) => {
      clearImmediate(f);
    });
  }, this._getEditorManager = function() {
    return c;
  }, this.isRtl = function() {
    return o.rootWindow.getComputedStyle(o.rootElement).direction === "rtl";
  }, this.isLtr = function() {
    return !o.isRtl();
  }, this.getDirectionFactor = function() {
    return o.isLtr() ? 1 : -1;
  };
  const wt = UZ({
    handleEvent() {
      return o.isListening();
    },
    beforeKeyDown: (f) => this.runHooks("beforeKeyDown", f),
    afterKeyDown: (f) => {
      this.isDestroyed || o.runHooks("afterDocumentKeyDown", f);
    },
    ownerWindow: this.rootWindow
  });
  this.addHook("beforeOnCellMouseDown", (f) => {
    f.ctrlKey === !1 && f.metaKey === !1 && wt.releasePressedKeys();
  }), this.getShortcutManager = function() {
    return wt;
  }, this.getFocusManager = function() {
    return h;
  }, Sb().forEach((f) => {
    const p = bd(f);
    R.addItem(f, new p(this));
  }), TZ(o), wt.setActiveContextName("grid"), he.getSingleton().run(o, "construct");
}
function xT(t, e, n) {
  t.addEventListener(e, n, !1);
}
function HT(t, e, n) {
  t.removeEventListener(e, n, !1);
}
function _T(t) {
  return t.ownerDocument.defaultView.getComputedStyle(t);
}
function YZ(t) {
  const e = {
    minHeight: 200,
    maxHeight: 300,
    minWidth: 100,
    maxWidth: 300
  }, n = t.body, i = t.createTextNode(""), r = t.createElement("span");
  let o;
  function s() {
    i.textContent = o.value, r.style.position = "absolute", r.style.fontSize = _T(o).fontSize, r.style.fontFamily = _T(o).fontFamily, r.style.whiteSpace = "pre", n.appendChild(r);
    const c = r.clientWidth + 2;
    n.removeChild(r);
    const h = o.style;
    h.height = `${e.minHeight}px`, e.minWidth > c ? h.width = `${e.minWidth}px` : c > e.maxWidth ? h.width = `${e.maxWidth}px` : h.width = `${c}px`;
    const d = o.scrollHeight ? o.scrollHeight - 1 : 0;
    e.minHeight > d ? h.height = `${e.minHeight}px` : e.maxHeight < d ? (h.height = `${e.maxHeight}px`, h.overflowY = "visible") : h.height = `${d}px`;
  }
  function a() {
    t.defaultView.setTimeout(s, 0);
  }
  function l(c) {
    if (c && c.minHeight)
      if (c.minHeight === "inherit")
        e.minHeight = o.clientHeight;
      else {
        const h = parseInt(c.minHeight, 10);
        isNaN(h) || (e.minHeight = h);
      }
    if (c && c.maxHeight)
      if (c.maxHeight === "inherit")
        e.maxHeight = o.clientHeight;
      else {
        const h = parseInt(c.maxHeight, 10);
        isNaN(h) || (e.maxHeight = h);
      }
    if (c && c.minWidth)
      if (c.minWidth === "inherit")
        e.minWidth = o.clientWidth;
      else {
        const h = parseInt(c.minWidth, 10);
        isNaN(h) || (e.minWidth = h);
      }
    if (c && c.maxWidth)
      if (c.maxWidth === "inherit")
        e.maxWidth = o.clientWidth;
      else {
        const h = parseInt(c.maxWidth, 10);
        isNaN(h) || (e.maxWidth = h);
      }
    r.firstChild || (r.className = "autoResize", r.style.display = "inline-block", r.appendChild(i));
  }
  function u(c, h) {
    let d = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    o = c, l(h), o.nodeName === "TEXTAREA" && (o.style.resize = "none", o.style.height = `${e.minHeight}px`, o.style.minWidth = `${e.minWidth}px`, o.style.maxWidth = `${e.maxWidth}px`, o.style.overflowY = "hidden"), d && (xT(o, "input", s), xT(o, "keydown", a)), s();
  }
  return {
    init: u,
    resize: s,
    unObserve() {
      HT(o, "input", s), HT(o, "keydown", a);
    }
  };
}
function NT(t, e) {
  const n = rb(e), i = e.value.split(`
`);
  let r = n, o = 0;
  for (let s = 0; s < i.length; s++) {
    const a = i[s];
    s !== 0 && (o += i[s - 1].length + 1);
    const l = o + a.length;
    if (t === "home" ? r = o : t === "end" && (r = l), n <= l)
      break;
  }
  Cd(e, r);
}
function fa(t, e, n) {
  return (e = jZ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function jZ(t) {
  var e = GZ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function GZ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const zZ = "ht_editor_visible", kf = "ht_editor_hidden", DT = "textEditor", KZ = "text";
class Al extends Fu {
  static get EDITOR_TYPE() {
    return KZ;
  }
  /**
   * Instance of {@link EventManager}.
   *
   * @private
   * @type {EventManager}
   */
  /**
   * @param {Core} hotInstance The Handsontable instance.
   */
  constructor(e) {
    super(e), fa(this, "eventManager", new Qi(this)), fa(this, "autoResize", YZ(this.hot.rootDocument)), fa(this, "TEXTAREA", void 0), fa(this, "textareaStyle", void 0), fa(this, "TEXTAREA_PARENT", void 0), fa(this, "textareaParentStyle", void 0), fa(this, "layerClass", void 0), this.eventManager = new Qi(this), this.createElements(), this.bindEvents(), this.hot.addHookOnce("afterDestroy", () => this.destroy());
  }
  /**
   * Gets current value from editable element.
   *
   * @returns {number}
   */
  getValue() {
    return this.TEXTAREA.value;
  }
  /**
   * Sets new value into editable element.
   *
   * @param {*} newValue The editor value.
   */
  setValue(e) {
    this.TEXTAREA.value = e;
  }
  /**
   * Opens the editor and adjust its size.
   */
  open() {
    this.refreshDimensions(), this.showEditableElement(), this.hot.getShortcutManager().setActiveContextName("editor"), this.registerShortcuts();
  }
  /**
   * Closes the editor.
   */
  close() {
    this.autoResize.unObserve(), kP(this.hot.rootDocument.activeElement, this.hot.rootElement) && this.hot.listen(), this.hideEditableElement(), this.unregisterShortcuts();
  }
  /**
   * Prepares editor's meta data.
   *
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number|string} prop The column property (passed when datasource is an array of objects).
   * @param {HTMLTableCellElement} td The rendered cell element.
   * @param {*} value The rendered value.
   * @param {object} cellProperties The cell meta object (see {@link Core#getCellMeta}).
   */
  prepare(e, n, i, r, o, s) {
    const a = this.state;
    if (super.prepare(e, n, i, r, o, s), !s.readOnly) {
      this.refreshDimensions(!0);
      const {
        allowInvalid: l
      } = s;
      l && !this.isOpened() && (this.TEXTAREA.value = ""), a !== Xt.FINISHED && !this.isOpened() && this.hideEditableElement();
    }
  }
  /**
   * Begins editing on a highlighted cell and hides fillHandle corner if was present.
   *
   * @param {*} newInitialValue The editor initial value.
   * @param {Event} event The keyboard event object.
   */
  beginEditing(e, n) {
    this.state === Xt.VIRGIN && (this.TEXTAREA.value = "", super.beginEditing(e, n));
  }
  /**
   * Sets focus state on the select element.
   */
  focus() {
    this.TEXTAREA.select(), Cd(this.TEXTAREA, this.TEXTAREA.value.length);
  }
  /**
   * Creates an editor's elements and adds necessary CSS classnames.
   */
  createElements() {
    const {
      rootDocument: e
    } = this.hot;
    this.TEXTAREA = e.createElement("TEXTAREA"), Ee(this.TEXTAREA, [["data-hot-input", ""], Du(-1)]), Q(this.TEXTAREA, "handsontableInput"), this.textareaStyle = this.TEXTAREA.style, this.textareaStyle.width = 0, this.textareaStyle.height = 0, this.textareaStyle.overflowY = "visible", this.TEXTAREA_PARENT = e.createElement("DIV"), Q(this.TEXTAREA_PARENT, "handsontableInputHolder"), Te(this.TEXTAREA_PARENT, this.layerClass) && xe(this.TEXTAREA_PARENT, this.layerClass), Q(this.TEXTAREA_PARENT, kf), this.textareaParentStyle = this.TEXTAREA_PARENT.style, this.TEXTAREA_PARENT.appendChild(this.TEXTAREA), this.hot.rootElement.appendChild(this.TEXTAREA_PARENT);
  }
  /**
   * Moves an editable element out of the viewport, but element must be able to hold focus for IME support.
   *
   * @private
   */
  hideEditableElement() {
    lx() && (this.textareaStyle.textIndent = "-99999px"), this.textareaStyle.overflowY = "visible", this.textareaParentStyle.opacity = "0", this.textareaParentStyle.height = "1px", xe(this.TEXTAREA_PARENT, this.layerClass), Q(this.TEXTAREA_PARENT, kf);
  }
  /**
   * Resets an editable element position.
   *
   * @private
   */
  showEditableElement() {
    this.textareaParentStyle.height = "", this.textareaParentStyle.overflow = "", this.textareaParentStyle.position = "", this.textareaParentStyle[this.hot.isRtl() ? "left" : "right"] = "auto", this.textareaParentStyle.opacity = "1", this.textareaStyle.textIndent = "";
    const e = this.TEXTAREA_PARENT.childNodes;
    let n = !1;
    Ae(e.length - 1, (i) => {
      const r = e[i];
      if (Te(r, "handsontableEditor"))
        return n = !0, !1;
    }), Te(this.TEXTAREA_PARENT, kf) && xe(this.TEXTAREA_PARENT, kf), n ? (this.layerClass = zZ, Q(this.TEXTAREA_PARENT, this.layerClass)) : (this.layerClass = this.getEditedCellsLayerClass(), Q(this.TEXTAREA_PARENT, this.layerClass));
  }
  /**
   * Refreshes editor's value using source data.
   *
   * @private
   */
  refreshValue() {
    const e = this.hot.toPhysicalRow(this.row), n = this.hot.getSourceDataAtCell(e, this.col);
    this.originalValue = n, this.setValue(n), this.refreshDimensions();
  }
  /**
   * Refreshes editor's size and position.
   *
   * @private
   * @param {boolean} force Indicates if the refreshing editor dimensions should be triggered.
   */
  refreshDimensions() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    if (this.state !== Xt.EDITING && !e)
      return;
    if (this.TD = this.getEditedCell(), !this.TD) {
      e || this.close();
      return;
    }
    const {
      top: n,
      start: i,
      width: r,
      maxWidth: o,
      height: s,
      maxHeight: a
    } = this.getEditedCellRect();
    this.textareaParentStyle.top = `${n}px`, this.textareaParentStyle[this.hot.isRtl() ? "right" : "left"] = `${i}px`, this.showEditableElement();
    const l = uo(this.TD, this.hot.rootWindow);
    this.TEXTAREA.style.fontSize = l.fontSize, this.TEXTAREA.style.fontFamily = l.fontFamily, this.TEXTAREA.style.backgroundColor = this.TD.style.backgroundColor;
    const u = uo(this.TEXTAREA), c = parseInt(u.paddingLeft, 10) + parseInt(u.paddingRight, 10), h = parseInt(u.paddingTop, 10) + parseInt(u.paddingBottom, 10), d = r - c, g = s - h, m = o - c, w = a - h;
    this.autoResize.init(this.TEXTAREA, {
      minWidth: Math.min(d, m),
      minHeight: Math.min(g, w),
      // TEXTAREA should never be wider than visible part of the viewport (should not cover the scrollbar)
      maxWidth: m,
      maxHeight: w
    }, !0);
  }
  /**
   * Binds events and hooks.
   *
   * @private
   */
  bindEvents() {
    db() && this.eventManager.addEventListener(this.TEXTAREA, "focusout", () => this.finishEditing(!1)), this.addHook("afterScrollHorizontally", () => this.refreshDimensions()), this.addHook("afterScrollVertically", () => this.refreshDimensions()), this.addHook("afterColumnResize", () => {
      this.refreshDimensions(), this.state === Xt.EDITING && this.focus();
    }), this.addHook("afterRowResize", () => {
      this.refreshDimensions(), this.state === Xt.EDITING && this.focus();
    });
  }
  /**
   * Ugly hack for autocompleteEditor.
   *
   * @private
   */
  allowKeyEventPropagation() {
  }
  /**
   * Destroys the internal event manager and clears attached hooks.
   *
   * @private
   */
  destroy() {
    this.eventManager.destroy(), this.clearHooks();
  }
  /**
   * Register shortcuts responsible for handling editor.
   *
   * @private
   */
  registerShortcuts() {
    const n = this.hot.getShortcutManager().getContext("editor"), i = {
      runOnlyIf: () => de(this.hot.getSelected()),
      group: DT
    }, r = () => {
      this.hot.rootDocument.execCommand("insertText", !1, `
`);
    };
    n.addShortcuts([{
      keys: [["Control", "Enter"]],
      callback: () => (r(), !1),
      runOnlyIf: (o) => !this.hot.selection.isMultiple() && // We trigger a data population for multiple selection.
      // catch CTRL but not right ALT (which in some systems triggers ALT+CTRL)
      !o.altKey
    }, {
      keys: [["Meta", "Enter"]],
      callback: () => (r(), !1),
      runOnlyIf: () => !this.hot.selection.isMultiple()
      // We trigger a data population for multiple selection.
    }, {
      keys: [["Alt", "Enter"]],
      callback: () => (r(), !1)
    }, {
      keys: [["Home"]],
      callback: (o, s) => {
        let [a] = s;
        NT(a, this.TEXTAREA);
      }
    }, {
      keys: [["End"]],
      callback: (o, s) => {
        let [a] = s;
        NT(a, this.TEXTAREA);
      }
    }], i);
  }
  /**
   * Unregister shortcuts responsible for handling editor.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("editor").removeShortcutsByGroup(DT);
  }
}
const QZ = "base";
function Od(t, e, n, i, r, o, s) {
  const a = s.ariaTags, l = [], u = [], c = [], h = [];
  s.className && Q(e, s.className), s.readOnly ? (l.push(s.readOnlyCellClassName), a && h.push(CE())) : a && c.push(CE()[0]), s.valid === !1 && s.invalidCellClassName ? (l.push(s.invalidCellClassName), a && h.push(vE())) : (u.push(s.invalidCellClassName), a && c.push(vE()[0])), s.wordWrap === !1 && s.noWordWrapClassName && l.push(s.noWordWrapClassName), !o && s.placeholder && l.push(s.placeholderCellClassName), xe(e, u), Q(e, l), ml(e, c), Ee(e, h);
}
Od.RENDERER_TYPE = QZ;
const XZ = "text";
function ss(t, e, n, i, r, o, s) {
  Od.apply(this, [t, e, n, i, r, o, s]);
  let a = o;
  if (!a && s.placeholder && (a = s.placeholder), a = Gt(a), s.trimWhitespace && (a = a.trim()), s.rendererTemplate) {
    br(e);
    const l = t.rootDocument.createElement("TEMPLATE");
    l.setAttribute("bind", "{{}}"), l.innerHTML = s.rendererTemplate, HTMLTemplateElement.decorate(l), l.model = t.getSourceDataAtRow(n), e.appendChild(l);
  } else
    Gs(e, a);
}
ss.RENDERER_TYPE = XZ;
const qZ = "text", _1 = {
  CELL_TYPE: qZ,
  editor: Al,
  renderer: ss
};
Di(_1);
Ie.editors = {
  BaseEditor: Fu
};
function Ie(t, e) {
  const n = new H1(t, e || {}, Wb);
  return n.init(), n;
}
Ie.Core = function(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
  return new H1(t, e, Wb);
};
Ie.DefaultSettings = E1();
Ie.hooks = he.getSingleton();
Ie.CellCoords = Sm;
Ie.CellRange = Eb;
Ie.packageName = "handsontable";
Ie.buildDate = "17/10/2024 09:16:16";
Ie.version = "14.6.1";
Ie.languages = {
  dictionaryKeys: a4,
  getLanguageDictionary: p1,
  getLanguagesDictionaries: d4,
  registerLanguageDictionary: eR,
  getTranslatedPhrase: w1
};
function ZZ(t, e, n) {
  return (e = tJ(e)) in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : t[e] = n, t;
}
function kT(t, e) {
  var n = Object.keys(t);
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(t);
    e && (i = i.filter(function(r) {
      return Object.getOwnPropertyDescriptor(t, r).enumerable;
    })), n.push.apply(n, i);
  }
  return n;
}
function JZ(t) {
  for (var e = 1; e < arguments.length; e++) {
    var n = arguments[e] != null ? arguments[e] : {};
    e % 2 ? kT(Object(n), !0).forEach(function(i) {
      ZZ(t, i, n[i]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(n)) : kT(Object(n)).forEach(function(i) {
      Object.defineProperty(t, i, Object.getOwnPropertyDescriptor(n, i));
    });
  }
  return t;
}
function eJ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function tJ(t) {
  var e = eJ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function ch(t) {
  "@babel/helpers - typeof";
  return ch = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(e) {
    return typeof e;
  } : function(e) {
    return e && typeof Symbol == "function" && e.constructor === Symbol && e !== Symbol.prototype ? "symbol" : typeof e;
  }, ch(t);
}
var vu = Symbol("unassigned"), nJ = "The Handsontable instance bound to this component was destroyed and cannot be used properly.";
function Tm(t, e) {
  return Object.prototype.hasOwnProperty.call(t, e);
}
function N1(t) {
  var e = Ie.hooks.getRegistered(), n = {};
  Object.assign(n, Ie.DefaultSettings);
  for (var i in n)
    n[i] = {
      default: vu
    };
  for (var r = 0; r < e.length; r++)
    n[e[r]] = {
      default: vu
    };
  return n.settings = {
    default: vu
  }, t === "HotTable" && (n.id = {
    type: String,
    default: "hot-".concat(Math.random().toString(36).substring(5))
  }), n;
}
function D1(t) {
  var e = {}, n = t.settings;
  if (n !== vu)
    for (var i in n)
      Tm(n, i) && n[i] !== vu && (e[i] = n[i]);
  for (var r in t)
    Tm(t, r) && r !== "settings" && t[r] !== vu && (e[r] = t[r]);
  return e;
}
function LT(t, e) {
  var n = D1(t), i = t.settings ? t.settings : n, r = t.settings ? n : null, o = {};
  for (var s in i)
    Tm(i, s) && i[s] !== void 0 && (!(e && s !== "data") || !BT(e[s], i[s])) && (o[s] = i[s]);
  for (var a in r)
    Tm(r, a) && a !== "id" && a !== "settings" && r[a] !== void 0 && (!(e && a !== "data") || !BT(e[a], r[a])) && (o[a] = r[a]);
  return o;
}
function BT(t, e) {
  var n = function(r) {
    var o = function() {
      var s = /* @__PURE__ */ new WeakSet();
      return function(a, l) {
        if (ch(l) === "object" && l !== null) {
          if (s.has(l))
            return;
          s.add(l);
        }
        return l;
      };
    }();
    return JSON.stringify(r, o);
  };
  return typeof t == "function" && typeof e == "function" ? t.toString() === e.toString() : ch(t) !== ch(e) ? !1 : n(t) === n(e);
}
var iJ = "14.6.1", rR = lS({
  name: "HotTable",
  props: N1("HotTable"),
  provide: function() {
    return {
      columnsCache: this.columnsCache
    };
  },
  watch: {
    $props: {
      handler: function(e) {
        var n = LT(e, this.hotInstance ? this.hotInstance.getSettings() : void 0);
        !this.hotInstance || n === void 0 || (n.data && (this.hotInstance.isColumnModificationAllowed() || !this.hotInstance.isColumnModificationAllowed() && this.hotInstance.countSourceCols() === this.miscCache.currentSourceColumns) && (this.matchHotMappersSize(), delete n.data), Object.keys(n).length ? this.hotInstance.updateSettings(n) : this.hotInstance.render(), this.miscCache.currentSourceColumns = this.hotInstance.countSourceCols());
      },
      deep: !0,
      immediate: !0
    }
  },
  data: function() {
    return {
      /* eslint-disable vue/no-reserved-keys */
      __hotInstance: null,
      /* eslint-enable vue/no-reserved-keys */
      miscCache: {
        currentSourceColumns: null
      },
      columnSettings: null,
      columnsCache: /* @__PURE__ */ new Map(),
      get hotInstance() {
        return !this.__hotInstance || this.__hotInstance && !this.__hotInstance.isDestroyed ? this.__hotInstance : (console.warn(nJ), null);
      },
      set hotInstance(e) {
        this.__hotInstance = e;
      }
    };
  },
  methods: {
    /**
     * Initialize Handsontable.
     */
    hotInit: function() {
      var e = LT(this.$props);
      e.columns = this.columnSettings ? this.columnSettings : e.columns, this.hotInstance = Z_(new Ie.Core(this.$el, e)), this.hotInstance.init(), this.miscCache.currentSourceColumns = this.hotInstance.countSourceCols();
    },
    matchHotMappersSize: function() {
      var e = this;
      if (this.hotInstance) {
        var n = this.hotInstance.getSourceData(), i = [], r = [], o = this.hotInstance.rowIndexMapper.getNumberOfIndexes(), s = this.hotInstance.isColumnModificationAllowed(), a = 0;
        if (n && n.length !== o && n.length < o)
          for (var l = n.length; l < o; l++)
            i.push(l);
        if (s) {
          var u;
          if (a = this.hotInstance.columnIndexMapper.getNumberOfIndexes(), n && n[0] && ((u = n[0]) === null || u === void 0 ? void 0 : u.length) !== a && n[0].length < a)
            for (var c = n[0].length; c < a; c++)
              r.push(c);
        }
        this.hotInstance.batch(function() {
          i.length > 0 ? e.hotInstance.rowIndexMapper.removeIndexes(i) : e.hotInstance.rowIndexMapper.insertIndexes(o - 1, n.length - o), s && n.length !== 0 && (r.length > 0 ? e.hotInstance.columnIndexMapper.removeIndexes(r) : e.hotInstance.columnIndexMapper.insertIndexes(a - 1, n[0].length - a));
        });
      }
    },
    /**
     * Get settings for the columns provided in the `hot-column` components.
     *
     * @returns {HotTableProps[] | undefined}
     */
    getColumnSettings: function() {
      var e = Array.from(this.columnsCache.values());
      return e.length ? e : void 0;
    }
  },
  mounted: function() {
    this.columnSettings = this.getColumnSettings(), this.hotInit();
  },
  beforeUnmount: function() {
    this.hotInstance && this.hotInstance.destroy();
  },
  version: iJ
}), rJ = ["id"];
function oJ(t, e, n, i, r, o) {
  return Kc(), Qc("div", {
    id: t.id
  }, [J_(t.$slots, "default")], 8, rJ);
}
rR.render = oJ;
rR.__file = "src/HotTable.vue";
var sJ = lS({
  name: "HotColumn",
  props: N1("HotColumn"),
  inject: ["columnsCache"],
  methods: {
    /**
     * Create the column settings based on the data provided to the `hot-column`
     * component and it's child components.
     */
    createColumnSettings: function() {
      var e = D1(this.$props), n = JZ({}, e);
      e.renderer && (n.renderer = e.renderer), e.editor && (n.editor = e.editor), this.columnsCache.set(this, n);
    }
  },
  mounted: function() {
    this.createColumnSettings();
  },
  unmounted: function() {
    this.columnsCache.delete(this);
  },
  render: function() {
    return null;
  }
});
sJ.__file = "src/HotColumn.vue";
function aJ(t, e) {
  return `${t}-${e}`;
}
(() => {
  if (!window.matchMedia)
    return {
      value: mu(null)
    };
  const t = mu(window.matchMedia("(prefers-color-scheme: dark)").matches);
  return window.matchMedia("(prefers-color-scheme: dark)").addEventListener("change", (e) => t.value = e.matches), {
    value: t
  };
})();
const lJ = {
  create() {
    return _I("xlsx.worker.js").defaults(() => new Worker(new URL("" + new URL("../worker/xlsx.worker.js", import.meta.url).href, self.location), { type: "module" }));
  }
}, uJ = {
  create() {
    return _I("sheet.worker.js").defaults(() => new Worker(new URL("" + new URL("../worker/sheet.worker.js", import.meta.url).href, self.location), { type: "module" }));
  }
}, FT = "handsontableEditor", cJ = "handsontable";
class oR extends Al {
  static get EDITOR_TYPE() {
    return cJ;
  }
  /**
   * Opens the editor and adjust its size.
   */
  open() {
    super.open(), this.htEditor && this.htEditor.destroy(), this.htContainer.style.display === "none" && (this.htContainer.style.display = ""), this.htEditor = new this.hot.constructor(this.htContainer, this.htOptions), this.htEditor.init(), this.htEditor.rootElement.style.display = "", this.cellProperties.strict ? this.htEditor.selectCell(0, 0) : this.htEditor.deselectCell(), Cd(this.TEXTAREA, 0, this.TEXTAREA.value.length), this.refreshDimensions();
  }
  /**
   * Closes the editor.
   */
  close() {
    this.htEditor && (this.htEditor.rootElement.style.display = "none"), this.removeHooksByKey("beforeKeyDown"), super.close();
  }
  /**
   * Prepares editor's meta data and configuration of the internal Handsontable's instance.
   *
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number|string} prop The column property (passed when datasource is an array of objects).
   * @param {HTMLTableCellElement} td The rendered cell element.
   * @param {*} value The rendered value.
   * @param {object} cellProperties The cell meta object (see {@link Core#getCellMeta}).
   */
  prepare(e, n, i, r, o, s) {
    super.prepare(e, n, i, r, o, s);
    const a = this, l = {
      startRows: 0,
      startCols: 0,
      minRows: 0,
      minCols: 0,
      className: "listbox",
      copyPaste: !1,
      autoColumnSize: !1,
      autoRowSize: !1,
      readOnly: !0,
      fillHandle: !1,
      autoWrapCol: !1,
      autoWrapRow: !1,
      ariaTags: !1,
      afterOnCellMouseDown(u, c) {
        const h = this.getSourceData(c.row, c.col);
        h !== void 0 && a.setValue(h), a.hot.destroyEditor();
      },
      preventWheel: !0,
      layoutDirection: this.hot.isRtl() ? "rtl" : "ltr"
    };
    this.cellProperties.handsontable && nn(l, s.handsontable), this.htOptions = l;
  }
  /**
   * Begins editing on a highlighted cell and hides fillHandle corner if was present.
   *
   * @param {*} newInitialValue The editor initial value.
   * @param {*} event The keyboard event object.
   */
  beginEditing(e, n) {
    const i = this.hot.getSettings().onBeginEditing;
    i && i() === !1 || super.beginEditing(e, n);
  }
  /**
   * Creates an editor's elements and adds necessary CSS classnames.
   */
  createElements() {
    super.createElements();
    const e = this.hot.rootDocument.createElement("DIV");
    e.className = "handsontableEditor", this.TEXTAREA_PARENT.appendChild(e), this.htContainer = e, this.assignHooks();
  }
  /**
   * Finishes editing and start saving or restoring process for editing cell or last selected range.
   *
   * @param {boolean} restoreOriginalValue If true, then closes editor without saving value from the editor into a cell.
   * @param {boolean} ctrlDown If true, then saveValue will save editor's value to each cell in the last selected range.
   * @param {Function} callback The callback function, fired after editor closing.
   */
  finishEditing(e, n, i) {
    if (this.htEditor && this.htEditor.isListening() && this.hot.listen(), this.htEditor && this.htEditor.getSelectedLast()) {
      const r = this.htEditor.getValue();
      r !== void 0 && this.setValue(r);
    }
    super.finishEditing(e, n, i);
  }
  /**
   * Assigns afterDestroy callback to prevent memory leaks.
   *
   * @private
   */
  assignHooks() {
    this.hot.addHook("afterDestroy", () => {
      this.htEditor && this.htEditor.destroy();
    });
  }
  /**
   * Register shortcuts responsible for handling editor.
   *
   * @private
   */
  registerShortcuts() {
    const n = this.hot.getShortcutManager().getContext("editor");
    super.registerShortcuts();
    const i = {
      group: FT,
      relativeToGroup: Js,
      position: "before"
    }, r = (o, s) => {
      const a = this.htEditor;
      if (o !== void 0 && (o < 0 || a.flipped && o > a.countRows() - 1 ? a.deselectCell() : a.selectCell(o, 0), a.getData().length))
        return s.preventDefault(), Zi(s), this.hot.listen(), this.TEXTAREA.focus(), !1;
    };
    n.addShortcuts([{
      keys: [["ArrowUp"]],
      callback: (o) => {
        const s = this.htEditor;
        let a, l;
        return !s.getSelectedLast() && s.flipped ? a = s.countRows() - 1 : s.getSelectedLast() && (s.flipped ? (l = s.getSelectedLast()[0], a = Math.max(0, l - 1)) : (l = s.getSelectedLast()[0], a = l - 1)), r(a, o);
      },
      preventDefault: !1
      // Doesn't block default behaviour (navigation) for a `textArea` HTMLElement.
    }, {
      keys: [["ArrowDown"]],
      callback: (o) => {
        const s = this.htEditor;
        let a, l;
        if (!s.getSelectedLast() && !s.flipped)
          a = 0;
        else if (s.getSelectedLast()) {
          if (s.flipped)
            a = s.getSelectedLast()[0] + 1;
          else if (!s.flipped) {
            const u = s.countRows() - 1;
            l = s.getSelectedLast()[0], a = Math.min(u, l + 1);
          }
        }
        return r(a, o);
      },
      preventDefault: !1
      // Doesn't block default behaviour (navigation) for a `textArea` HTMLElement.
    }], i);
  }
  /**
   * Unregister shortcuts responsible for handling editor.
   *
   * @private
   */
  unregisterShortcuts() {
    super.unregisterShortcuts(), this.hot.getShortcutManager().getContext("editor").removeShortcutsByGroup(FT);
  }
}
function hJ(t, e, n) {
  dJ(t, e), e.set(t, n);
}
function dJ(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Lf(t, e, n) {
  return (e = fJ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function fJ(t) {
  var e = gJ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function gJ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function VT(t, e) {
  return t.get(mJ(t, e));
}
function mJ(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const pJ = "autocomplete";
var TC = /* @__PURE__ */ new WeakMap();
class sR extends oR {
  constructor() {
    super(...arguments), Lf(this, "query", null), Lf(this, "strippedChoices", []), Lf(this, "rawChoices", []), hJ(this, TC, this.hot.guid.slice(0, 9)), Lf(this, "sortByRelevance", function(e, n, i) {
      const r = [], o = [], s = e.length;
      let a = n.length, l, u, c, h;
      if (s === 0) {
        for (c = 0; c < a; c++)
          o.push(c);
        return o;
      }
      for (c = 0; c < a; c++) {
        if (u = my(Gt(n[c])), i)
          h = u.indexOf(e);
        else {
          const d = this.cellProperties.locale;
          h = u.toLocaleLowerCase(d).indexOf(e.toLocaleLowerCase(d));
        }
        h !== -1 && (l = u.length - h - s, r.push({
          baseIndex: c,
          index: h,
          charsLeft: l,
          value: u
        }));
      }
      for (r.sort((d, g) => {
        if (g.index === -1)
          return -1;
        if (d.index === -1)
          return 1;
        if (d.index < g.index)
          return -1;
        if (g.index < d.index)
          return 1;
        if (d.index === g.index) {
          if (d.charsLeft < g.charsLeft)
            return -1;
          if (d.charsLeft > g.charsLeft)
            return 1;
        }
        return 0;
      }), c = 0, a = r.length; c < a; c++)
        o.push(r[c].baseIndex);
      return o;
    });
  }
  static get EDITOR_TYPE() {
    return pJ;
  }
  /**
   * Gets current value from editable element.
   *
   * @returns {string}
   */
  getValue() {
    const e = this.rawChoices.find((n) => this.stripValueIfNeeded(n) === this.TEXTAREA.value);
    return de(e) ? e : this.TEXTAREA.value;
  }
  /**
   * Creates an editor's elements and adds necessary CSS classnames.
   */
  createElements() {
    super.createElements(), Q(this.htContainer, "autocompleteEditor"), Q(this.htContainer, this.hot.rootWindow.navigator.platform.indexOf("Mac") === -1 ? "" : "htMacScroll"), this.hot.getSettings().ariaTags && Ee(this.TEXTAREA, [EG(), vG(), xP("listbox"), OG()]);
  }
  /**
   * Prepares editor's metadata and configuration of the internal Handsontable's instance.
   *
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number|string} prop The column property (passed when datasource is an array of objects).
   * @param {HTMLTableCellElement} td The rendered cell element.
   * @param {*} value The rendered value.
   * @param {object} cellProperties The cell meta object (see {@link Core#getCellMeta}).
   */
  prepare(e, n, i, r, o, s) {
    super.prepare(e, n, i, r, o, s), this.hot.getSettings().ariaTags && Ee(this.TEXTAREA, [Gi("false"), PG(`${VT(TC, this)}-listbox-${e}-${n}`)]);
  }
  /**
   * Opens the editor and adjust its size and internal Handsontable's instance.
   */
  open() {
    super.open();
    const e = this.cellProperties.trimDropdown === void 0 ? !0 : this.cellProperties.trimDropdown, n = this.hot.getSettings().ariaTags, i = Array.isArray(this.cellProperties.source) ? this.cellProperties.source : null, r = i == null ? void 0 : i.length, {
      row: o,
      col: s
    } = this;
    this.showEditableElement(), this.focus();
    let a = pn();
    a === 0 && Lh() && (a += 15), this.addHook("beforeKeyDown", (l) => this.onBeforeKeyDown(l)), this.htEditor.updateSettings({
      colWidths: e ? [It(this.TEXTAREA) - 2] : void 0,
      width: e ? It(this.TEXTAREA) + a : void 0,
      autoColumnSize: !0,
      renderer: (l, u, c, h, d, g, m) => {
        ss(l, u, c, h, d, g, m);
        const {
          filteringCaseSensitive: w,
          allowHtml: C,
          locale: S
        } = this.cellProperties, b = this.query;
        let R = Gt(g), _, O;
        R && !C && (_ = w === !0 ? R.indexOf(b) : R.toLocaleLowerCase(S).indexOf(b.toLocaleLowerCase(S)), _ !== -1 && (O = R.substr(_, b.length), R = R.replace(O, `<strong>${O}</strong>`))), n && Ee(u, [
          yG(),
          // Add `setsize` and `posinset` only if the source is an array.
          ...i ? [NG(r)] : [],
          ...i ? [DG(i.indexOf(g) + 1)] : [],
          ["id", `${this.htEditor.rootElement.id}_${c}-${h}`]
        ]), u.innerHTML = R;
      },
      afterSelectionEnd: (l, u) => {
        if (n) {
          const c = this.htEditor.getCell(l, u, !0);
          Ee(c, [JS()]), Ee(this.TEXTAREA, ...xG(c.id));
        }
      }
    }), n && (Ee(this.htEditor.view._wt.wtOverlays.wtTable.TABLE, ...oo()), Ee(this.htEditor.rootElement, [PP(), HG("polite"), _G("text"), ["id", `${VT(TC, this)}-listbox-${o}-${s}`]]), Ee(this.TEXTAREA, ...Gi("true"))), this.hot._registerTimeout(() => {
      this.queryChoices(this.TEXTAREA.value);
    });
  }
  /**
   * Closes the editor.
   */
  close() {
    this.removeHooksByKey("beforeKeyDown"), super.close(), this.hot.getSettings().ariaTags && Ee(this.TEXTAREA, [Gi("false")]);
  }
  /**
   * Verifies result of validation or closes editor if user's cancelled changes.
   *
   * @param {boolean|undefined} result If `false` and the cell using allowInvalid option,
   *                                   then an editor won't be closed until validation is passed.
   */
  discardEditor(e) {
    super.discardEditor(e), this.hot.view.render();
  }
  /**
   * Prepares choices list based on applied argument.
   *
   * @private
   * @param {string} query The query.
   */
  queryChoices(e) {
    const n = this.cellProperties.source;
    this.query = e, typeof n == "function" ? n.call(this.cellProperties, e, (i) => {
      this.rawChoices = i, this.updateChoicesList(this.stripValuesIfNeeded(i));
    }) : Array.isArray(n) ? (this.rawChoices = n, this.updateChoicesList(this.stripValuesIfNeeded(n))) : this.updateChoicesList([]);
  }
  /**
   * Updates list of the possible completions to choose.
   *
   * @private
   * @param {Array} choicesList The choices list to process.
   */
  updateChoicesList(e) {
    const n = rb(this.TEXTAREA), i = YP(this.TEXTAREA), r = this.cellProperties.sortByRelevance, o = this.cellProperties.filter;
    let s = null, a = null, l = e;
    r && (s = this.sortByRelevance(this.stripValueIfNeeded(this.getValue()), l, this.cellProperties.filteringCaseSensitive));
    const u = Array.isArray(s) ? s.length : 0;
    if (o === !1)
      u && (a = s[0]);
    else {
      const c = [];
      for (let h = 0, d = l.length; h < d && !(r && u <= h); h++)
        u ? c.push(l[s[h]]) : c.push(l[h]);
      a = 0, l = c;
    }
    this.strippedChoices = l, l.length === 0 ? this.htEditor.rootElement.style.display = "none" : this.htEditor.rootElement.style.display = "", this.htEditor.loadData(rh([l])), l.length > 0 && (this.updateDropdownDimensions(), this.flipDropdownIfNeeded(), this.cellProperties.strict === !0 && this.highlightBestMatchingChoice(a)), this.hot.listen(), Cd(this.TEXTAREA, n, n === i ? void 0 : i);
  }
  /**
   * Checks where is enough place to open editor.
   *
   * @private
   * @returns {boolean}
   */
  flipDropdownIfNeeded() {
    const e = tl(this.hot.view._wt.wtTable.TABLE), n = e === this.hot.rootWindow, i = this.cellProperties.preventOverflow;
    if (n || !n && (i || i === "horizontal"))
      return !1;
    const r = gn(this.TEXTAREA), o = Zt(this.TEXTAREA), s = this.getDropdownHeight(), a = e.scrollTop, l = Zt(this.hot.view._wt.wtTable.THEAD), u = gn(e), c = r.top - u.top - l + a, h = e.scrollHeight - c - l - o, d = s > h && c > h;
    return d ? this.flipDropdown(s) : this.unflipDropdown(), this.limitDropdownIfNeeded(d ? c : h, s), d;
  }
  /**
   * Checks if the internal table should generate scrollbar or could be rendered without it.
   *
   * @private
   * @param {number} spaceAvailable The free space as height defined in px available for dropdown list.
   * @param {number} dropdownHeight The dropdown height.
   */
  limitDropdownIfNeeded(e, n) {
    if (n > e) {
      let i = 0, r = 0, o = 0, s = null;
      do
        o = this.htEditor.getRowHeight(r) || this.htEditor.view._wt.getSetting("defaultRowHeight"), i += o, r += 1;
      while (i < e);
      s = i - o, this.htEditor.flipped && (this.htEditor.rootElement.style.top = `${parseInt(this.htEditor.rootElement.style.top, 10) + n - s}px`), this.setDropdownHeight(i - o);
    }
  }
  /**
   * Configures editor to open it at the top.
   *
   * @private
   * @param {number} dropdownHeight The dropdown height.
   */
  flipDropdown(e) {
    const n = this.htEditor.rootElement.style;
    n.position = "absolute", n.top = `${-e}px`, this.htEditor.flipped = !0;
  }
  /**
   * Configures editor to open it at the bottom.
   *
   * @private
   */
  unflipDropdown() {
    const e = this.htEditor.rootElement.style;
    e.position = "absolute", e.top = "", this.htEditor.flipped = void 0;
  }
  /**
   * Updates width and height of the internal Handsontable's instance.
   *
   * @private
   */
  updateDropdownDimensions() {
    const e = this.htEditor.getColWidth(0) + pn(this.hot.rootDocument) + 2, n = this.cellProperties.trimDropdown;
    this.htEditor.updateSettings({
      height: this.getDropdownHeight(),
      width: n ? void 0 : e
    }), this.htEditor.view._wt.wtTable.alignOverlaysWithTrimmingContainer();
  }
  /**
   * Sets new height of the internal Handsontable's instance.
   *
   * @private
   * @param {number} height The new dropdown height.
   */
  setDropdownHeight(e) {
    this.htEditor.updateSettings({
      height: e
    });
  }
  /**
   * Creates new selection on specified row index, or deselects selected cells.
   *
   * @private
   * @param {number|undefined} index The visual row index.
   */
  highlightBestMatchingChoice(e) {
    typeof e == "number" ? this.htEditor.selectCell(e, 0, void 0, void 0, void 0, !1) : this.htEditor.deselectCell();
  }
  /**
   * Calculates and return the internal Handsontable's height.
   *
   * @private
   * @returns {number}
   */
  getDropdownHeight() {
    const e = this.htEditor.getRowHeight(0) || 23, n = this.cellProperties.visibleRows;
    return this.strippedChoices.length >= n ? n * e : this.strippedChoices.length * e + 8;
  }
  /**
   * Sanitizes value from potential dangerous tags.
   *
   * @private
   * @param {string} value The value to sanitize.
   * @returns {string}
   */
  stripValueIfNeeded(e) {
    return this.stripValuesIfNeeded([e])[0];
  }
  /**
   * Sanitizes an array of the values from potential dangerous tags.
   *
   * @private
   * @param {string[]} values The value to sanitize.
   * @returns {string[]}
   */
  stripValuesIfNeeded(e) {
    const {
      allowHtml: n
    } = this.cellProperties, i = Ke(e, (o) => Gt(o));
    return Ke(i, (o) => n ? o : my(o));
  }
  /**
   * Captures use of arrow down and up to control their behaviour.
   *
   * @private
   * @param {number} keyCode The keyboard keycode.
   * @returns {boolean}
   */
  allowKeyEventPropagation(e) {
    const n = this.htEditor.getSelectedRangeLast(), i = {
      row: n ? n.from.row : -1
    };
    let r = !1;
    return e === we.ARROW_DOWN && i.row > 0 && i.row < this.htEditor.countRows() - 1 && (r = !0), e === we.ARROW_UP && i.row > -1 && (r = !0), r;
  }
  /**
   * OnBeforeKeyDown callback.
   *
   * @private
   * @param {KeyboardEvent} event The keyboard event object.
   */
  onBeforeKeyDown(e) {
    if (ux(e.keyCode) || e.keyCode === we.BACKSPACE || e.keyCode === we.DELETE || e.keyCode === we.INSERT) {
      let n = 10;
      if (e.keyCode === we.C && (e.ctrlKey || e.metaKey))
        return;
      this.isOpened() || (n += 10), this.htEditor && this.hot._registerTimeout(() => {
        this.queryChoices(this.TEXTAREA.value);
      }, n);
    }
  }
}
const wJ = "checkbox";
class k1 extends Fu {
  static get EDITOR_TYPE() {
    return wJ;
  }
  beginEditing(e, n) {
    if (n && n.type === "mouseup" && n.target.nodeName === "TD") {
      const i = this.TD.querySelector('input[type="checkbox"]');
      Te(i, "htBadValue") || i.click();
    }
  }
  finishEditing() {
  }
  init() {
  }
  open() {
  }
  close() {
  }
  getValue() {
  }
  setValue() {
  }
  focus() {
  }
}
/*!
 * Pikaday
 *
 * Copyright © 2014 David Bushell | BSD & MIT license | https://github.com/Pikaday/Pikaday
 */
const Ds = () => window, Vi = () => window.document, ky = (t, e) => window.setTimeout(t, e), Kr = function(t, e, n, i) {
  t.addEventListener(e, n, !!i);
}, To = function(t, e, n, i) {
  t.removeEventListener(e, n, !!i);
}, AJ = function(t) {
  return t.trim ? t.trim() : t.replace(/^\s+|\s+$/g, "");
}, pi = function(t, e) {
  return (" " + t.className + " ").indexOf(" " + e + " ") !== -1;
}, MC = function(t, e) {
  pi(t, e) || (t.className = t.className === "" ? e : t.className + " " + e);
}, IC = function(t, e) {
  t.className = AJ((" " + t.className + " ").replace(" " + e + " ", " "));
}, aR = function(t) {
  return /Array/.test(Object.prototype.toString.call(t));
}, vi = function(t) {
  return /Date/.test(Object.prototype.toString.call(t)) && !isNaN(t.getTime());
}, CJ = function(t) {
  let e = t.getDay();
  return e === 0 || e === 6;
}, vJ = function(t) {
  return t % 4 === 0 && t % 100 !== 0 || t % 400 === 0;
}, WT = function(t, e) {
  return [31, vJ(t) ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31][e];
}, Bf = function(t) {
  vi(t) && t.setHours(0, 0, 0, 0);
}, Ff = function(t, e) {
  return t.getTime() === e.getTime();
}, Mm = function(t, e, n) {
  let i, r;
  for (i in e)
    r = t[i] !== void 0, r && typeof e[i] == "object" && e[i] !== null && e[i].nodeName === void 0 ? vi(e[i]) ? n && (t[i] = new Date(e[i].getTime())) : aR(e[i]) ? n && (t[i] = e[i].slice(0)) : t[i] = Mm({}, e[i], n) : (n || !r) && (t[i] = e[i]);
  return t;
}, $T = function(t, e, n) {
  let i = Vi().createEvent("HTMLEvents");
  i.initEvent(e, !0, !1), i = Mm(i, n), t.dispatchEvent(i);
}, UT = function(t) {
  return t.month < 0 && (t.year -= Math.ceil(Math.abs(t.month) / 12), t.month += 12), t.month > 11 && (t.year += Math.floor(Math.abs(t.month) / 12), t.month -= 12), t;
}, kr = {
  // bind the picker to a form field
  field: null,
  // automatically show/hide the picker on `field` focus (default `true` if `field` is set)
  bound: void 0,
  // data-attribute on the input field with an aria assistance text (only applied when `bound` is set)
  ariaLabel: "Use the arrow keys to pick a date",
  // position of the datepicker, relative to the field (default to bottom & left)
  // ('bottom' & 'left' keywords are not used, 'top' & 'right' are modifier on the bottom/left position)
  position: "bottom left",
  // automatically fit in the viewport even if it means repositioning from the position option
  reposition: !0,
  // the default output format for `.toString()` and `field` value
  format: "YYYY-MM-DD",
  // the toString function which gets passed a current date object and format
  // and returns a string
  toString: null,
  // used to create date object from current input string
  parse: null,
  // the initial date to view when first opened
  defaultDate: null,
  // make the `defaultDate` the initial selected value
  setDefaultDate: !1,
  // first day of week (0: Sunday, 1: Monday etc)
  firstDay: 0,
  // minimum number of days in the week that gets week number one
  // default ISO 8601, week 01 is the week with the first Thursday (4)
  firstWeekOfYearMinDays: 4,
  // the default flag for moment's strict date parsing
  formatStrict: !1,
  // the minimum/earliest date that can be selected
  minDate: null,
  // the maximum/latest date that can be selected
  maxDate: null,
  // number of years either side, or array of upper/lower range
  yearRange: 10,
  // show week numbers at head of row
  showWeekNumber: !1,
  // Week picker mode
  pickWholeWeek: !1,
  // used internally (don't config outside)
  minYear: 0,
  maxYear: 9999,
  minMonth: void 0,
  maxMonth: void 0,
  startRange: null,
  endRange: null,
  isRTL: !1,
  // Additional text to append to the year in the calendar title
  yearSuffix: "",
  // Render the month after year in the calendar title
  showMonthAfterYear: !1,
  // Render days of the calendar grid that fall in the next or previous month
  showDaysInNextAndPreviousMonths: !1,
  // Allows user to select days that fall in the next or previous month
  enableSelectionDaysInNextAndPreviousMonths: !1,
  // how many months are visible
  numberOfMonths: 1,
  // when numberOfMonths is used, this will help you to choose where the main calendar will be (default `left`, can be set to `right`)
  // only used for the first display or when a selected date is not visible
  mainCalendar: "left",
  // Specify a DOM element to render the calendar in
  container: void 0,
  // Blur field when date is selected
  blurFieldOnSelect: !0,
  // internationalization
  i18n: {
    previousMonth: "Previous Month",
    nextMonth: "Next Month",
    months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
    weekdays: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
    weekdaysShort: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"]
  },
  // Theme Classname
  theme: null,
  // events array
  events: [],
  // callback function
  onSelect: null,
  onOpen: null,
  onClose: null,
  onDraw: null,
  // Enable keyboard input
  keyboardInput: !0
}, YT = function(t, e, n) {
  for (e += t.firstDay; e >= 7; )
    e -= 7;
  return n ? t.i18n.weekdaysShort[e] : t.i18n.weekdays[e];
}, yJ = function(t) {
  let e = [], n = "false";
  if (t.isEmpty)
    if (t.showDaysInNextAndPreviousMonths)
      e.push("is-outside-current-month"), t.enableSelectionDaysInNextAndPreviousMonths || e.push("is-selection-disabled");
    else
      return '<td class="is-empty"></td>';
  return t.isDisabled && e.push("is-disabled"), t.isToday && e.push("is-today"), t.isSelected && (e.push("is-selected"), n = "true"), t.hasEvent && e.push("has-event"), t.isInRange && e.push("is-inrange"), t.isStartRange && e.push("is-startrange"), t.isEndRange && e.push("is-endrange"), '<td data-day="' + t.day + '" class="' + e.join(" ") + '" aria-selected="' + n + '"><button class="pika-button pika-day" type="button" data-pika-year="' + t.year + '" data-pika-month="' + t.month + '" data-pika-day="' + t.day + '">' + t.day + "</button></td>";
}, SJ = function(t, e) {
  t.setHours(0, 0, 0, 0);
  let n = t.getDate(), i = t.getDay(), r = e, o = r - 1, s = 7, a = function(d) {
    return (d + s - 1) % s;
  };
  t.setDate(n + o - a(i));
  let l = new Date(t.getFullYear(), 0, r), u = 24 * 60 * 60 * 1e3, c = (t.getTime() - l.getTime()) / u;
  return 1 + Math.round((c - o + a(l.getDay())) / s);
}, bJ = function(t, e, n, i, r) {
  let o = new Date(i, n, e);
  return '<td class="pika-week">' + (t.hasMoment ? t.moment(o).isoWeek() : SJ(o, r)) + "</td>";
}, RJ = function(t, e, n, i) {
  return '<tr class="pika-row' + (n ? " pick-whole-week" : "") + (i ? " is-selected" : "") + '">' + (e ? t.reverse() : t).join("") + "</tr>";
}, EJ = function(t) {
  return "<tbody>" + t.join("") + "</tbody>";
}, TJ = function(t) {
  let e, n = [];
  for (t.showWeekNumber && n.push("<th></th>"), e = 0; e < 7; e++)
    n.push('<th scope="col"><abbr title="' + YT(t, e) + '">' + YT(t, e, !0) + "</abbr></th>");
  return "<thead><tr>" + (t.isRTL ? n.reverse() : n).join("") + "</tr></thead>";
}, MJ = function(t, e, n, i, r, o) {
  let s, a, l, u = t._o, c = n === u.minYear, h = n === u.maxYear, d = '<div id="' + o + '" class="pika-title" role="heading" aria-live="polite">', g, m, w = !0, C = !0;
  for (l = [], s = 0; s < 12; s++)
    l.push('<option value="' + (n === r ? s - e : 12 + s - e) + '"' + (s === i ? ' selected="selected"' : "") + (c && s < u.minMonth || h && s > u.maxMonth ? ' disabled="disabled"' : "") + ">" + u.i18n.months[s] + "</option>");
  for (g = '<div class="pika-label">' + u.i18n.months[i] + '<select class="pika-select pika-select-month" tabindex="-1">' + l.join("") + "</select></div>", aR(u.yearRange) ? (s = u.yearRange[0], a = u.yearRange[1] + 1) : (s = n - u.yearRange, a = 1 + n + u.yearRange), l = []; s < a && s <= u.maxYear; s++)
    s >= u.minYear && l.push('<option value="' + s + '"' + (s === n ? ' selected="selected"' : "") + ">" + s + "</option>");
  return m = '<div class="pika-label">' + n + u.yearSuffix + '<select class="pika-select pika-select-year" tabindex="-1">' + l.join("") + "</select></div>", u.showMonthAfterYear ? d += m + g : d += g + m, c && (i === 0 || u.minMonth >= i) && (w = !1), h && (i === 11 || u.maxMonth <= i) && (C = !1), e === 0 && (d += '<button class="pika-prev' + (w ? "" : " is-disabled") + '" type="button">' + u.i18n.previousMonth + "</button>"), e === t._o.numberOfMonths - 1 && (d += '<button class="pika-next' + (C ? "" : " is-disabled") + '" type="button">' + u.i18n.nextMonth + "</button>"), d += "</div>";
}, IJ = function(t, e, n) {
  return '<table cellpadding="0" cellspacing="0" class="pika-table" role="grid" aria-labelledby="' + n + '">' + TJ(t) + EJ(e) + "</table>";
};
function Ly(t) {
  let e = this, n = e.config(t);
  e._onMouseDown = function(r) {
    if (!e._v)
      return;
    r = r || Ds().event;
    let o = r.target || r.srcElement;
    if (o)
      if (pi(o, "is-disabled") || (pi(o, "pika-button") && !pi(o, "is-empty") && !pi(o.parentNode, "is-disabled") ? (e.setDate(
        new Date(
          o.getAttribute("data-pika-year"),
          o.getAttribute("data-pika-month"),
          o.getAttribute("data-pika-day")
        )
      ), n.bound && ky(function() {
        e.hide(), n.blurFieldOnSelect && n.field && n.field.blur();
      }, 100)) : pi(o, "pika-prev") ? e.prevMonth() : pi(o, "pika-next") && e.nextMonth()), pi(o, "pika-select"))
        e._c = !0;
      else if (r.preventDefault)
        r.preventDefault();
      else
        return r.returnValue = !1, !1;
  }, e._onChange = function(r) {
    r = r || Ds().event;
    let o = r.target || r.srcElement;
    o && (pi(o, "pika-select-month") ? e.gotoMonth(o.value) : pi(o, "pika-select-year") && e.gotoYear(o.value));
  }, e._onKeyChange = function(r) {
    if (r = r || Ds().event, e.isVisible())
      switch (r.keyCode) {
        case 13:
        case 27:
          n.field && n.field.blur();
          break;
        case 37:
          e.adjustDate("subtract", 1);
          break;
        case 38:
          e.adjustDate("subtract", 7);
          break;
        case 39:
          e.adjustDate("add", 1);
          break;
        case 40:
          e.adjustDate("add", 7);
          break;
        case 8:
        case 46:
          e.setDate(null);
          break;
      }
  }, e._parseFieldValue = function() {
    if (n.parse)
      return n.parse(n.field.value, n.format);
    if (this.hasMoment) {
      let r = this.moment(
        n.field.value,
        n.format,
        n.formatStrict
      );
      return r && r.isValid() ? r.toDate() : null;
    } else
      return new Date(Date.parse(n.field.value));
  }, e._onInputChange = function(r) {
    let o;
    r.firedBy !== e && (o = e._parseFieldValue(), vi(o) && e.setDate(o), e._v || e.show());
  }, e._onInputFocus = function() {
    e.show();
  }, e._onInputClick = function() {
    e.show();
  }, e._onInputBlur = function() {
    let r = Vi().activeElement;
    do
      if (pi(r, "pika-single"))
        return;
    while (r = r.parentNode);
    e._c || (e._b = ky(function() {
      e.hide();
    }, 50)), e._c = !1;
  }, e._onClick = function(r) {
    r = r || Ds().event;
    let o = r.target || r.srcElement, s = o;
    if (o) {
      !hasEventListeners && pi(o, "pika-select") && (o.onchange || (o.setAttribute("onchange", "return;"), Kr(o, "change", e._onChange)));
      do
        if (pi(s, "pika-single") || s === n.trigger)
          return;
      while (s = s.parentNode);
      e._v && o !== n.trigger && s !== n.trigger && e.hide();
    }
  }, e.el = Vi().createElement("div"), e.el.className = "pika-single" + (n.isRTL ? " is-rtl" : "") + (n.theme ? " " + n.theme : ""), Kr(e.el, "mousedown", e._onMouseDown, !0), Kr(e.el, "touchend", e._onMouseDown, !0), Kr(e.el, "change", e._onChange), n.keyboardInput && Kr(Vi(), "keydown", e._onKeyChange), n.field && (n.container ? n.container.appendChild(e.el) : n.bound ? Vi().body.appendChild(e.el) : n.field.parentNode.insertBefore(
    e.el,
    n.field.nextSibling
  ), Kr(n.field, "change", e._onInputChange), n.defaultDate || (n.defaultDate = e._parseFieldValue(), n.setDefaultDate = !0));
  let i = n.defaultDate;
  vi(i) ? n.setDefaultDate ? e.setDate(i, !0) : e.gotoDate(i) : e.gotoDate(/* @__PURE__ */ new Date()), n.bound ? (this.hide(), e.el.className += " is-bound", Kr(n.trigger, "click", e._onInputClick), Kr(n.trigger, "focus", e._onInputFocus), Kr(n.trigger, "blur", e._onInputBlur)) : this.show();
}
Ly.prototype = {
  /**
   * configure functionality
   */
  config: function(t) {
    this._o || (this._o = Mm({}, kr, !0));
    let e = Mm(this._o, t, !0);
    e.isRTL = !!e.isRTL, e.field = e.field && e.field.nodeName ? e.field : null, e.theme = typeof e.theme == "string" && e.theme ? e.theme : null, e.bound = !!(e.bound !== void 0 ? e.field && e.bound : e.field), e.trigger = e.trigger && e.trigger.nodeName ? e.trigger : e.field, e.disableWeekends = !!e.disableWeekends, e.disableDayFn = typeof e.disableDayFn == "function" ? e.disableDayFn : null;
    let n = parseInt(e.numberOfMonths, 10) || 1;
    if (e.numberOfMonths = n > 4 ? 4 : n, vi(e.minDate) || (e.minDate = !1), vi(e.maxDate) || (e.maxDate = !1), e.minDate && e.maxDate && e.maxDate < e.minDate && (e.maxDate = e.minDate = !1), e.minDate && this.setMinDate(e.minDate), e.maxDate && this.setMaxDate(e.maxDate), aR(e.yearRange)) {
      let i = (/* @__PURE__ */ new Date()).getFullYear() - 10;
      e.yearRange[0] = parseInt(e.yearRange[0], 10) || i, e.yearRange[1] = parseInt(e.yearRange[1], 10) || i;
    } else
      e.yearRange = Math.abs(parseInt(e.yearRange, 10)) || kr.yearRange, e.yearRange > 100 && (e.yearRange = 100);
    return e;
  },
  /**
   * return a formatted string of the current selection (using Moment.js if available)
   */
  toString: function(t) {
    return t = t || this._o.format, vi(this._d) ? this._o.toString ? this._o.toString(this._d, t) : this.hasMoment ? this.moment(this._d).format(t) : this._d.toDateString() : "";
  },
  /**
   * return a Moment.js object of the current selection (if available)
   */
  getMoment: function() {
    return this.hasMoment ? this.moment(this._d) : null;
  },
  /**
   * set the current selection from a Moment.js object (if available)
   */
  setMoment: function(t, e) {
    this.hasMoment && this.moment.isMoment(t) && this.setDate(t.toDate(), e);
  },
  /**
   * Sets the momentjs function as dependency to parse and format the dates
   */
  useMoment: function(t) {
    this.hasMoment = !0, this.moment = t;
  },
  /**
   * return a Date object of the current selection
   */
  getDate: function() {
    return vi(this._d) ? new Date(this._d.getTime()) : null;
  },
  /**
   * set the current selection
   */
  setDate: function(t, e) {
    if (!t)
      return this._d = null, this._o.field && (this._o.field.value = "", $T(this._o.field, "change", { firedBy: this })), this.draw();
    if (typeof t == "string" && (t = new Date(Date.parse(t))), !vi(t))
      return;
    let n = this._o.minDate, i = this._o.maxDate;
    vi(n) && t < n ? t = n : vi(i) && t > i && (t = i), this._d = new Date(t.getTime()), Bf(this._d), this.gotoDate(this._d), this._o.field && (this._o.field.value = this.toString(), $T(this._o.field, "change", { firedBy: this })), !e && typeof this._o.onSelect == "function" && this._o.onSelect.call(this, this.getDate());
  },
  /**
   * clear and reset the date
   */
  clear: function() {
    this.setDate(null);
  },
  /**
   * change view to a specific date
   */
  gotoDate: function(t) {
    let e = !0;
    if (vi(t)) {
      if (this.calendars) {
        let n = new Date(this.calendars[0].year, this.calendars[0].month, 1), i = new Date(this.calendars[this.calendars.length - 1].year, this.calendars[this.calendars.length - 1].month, 1), r = t.getTime();
        i.setMonth(i.getMonth() + 1), i.setDate(i.getDate() - 1), e = r < n.getTime() || i.getTime() < r;
      }
      e && (this.calendars = [{
        month: t.getMonth(),
        year: t.getFullYear()
      }], this._o.mainCalendar === "right" && (this.calendars[0].month += 1 - this._o.numberOfMonths)), this.adjustCalendars();
    }
  },
  adjustDate: function(t, e) {
    let n = this.getDate() || /* @__PURE__ */ new Date(), i = parseInt(e) * 24 * 60 * 60 * 1e3, r;
    t === "add" ? r = new Date(n.valueOf() + i) : t === "subtract" && (r = new Date(n.valueOf() - i)), this.setDate(r);
  },
  adjustCalendars: function() {
    this.calendars[0] = UT(this.calendars[0]);
    for (let t = 1; t < this._o.numberOfMonths; t++)
      this.calendars[t] = UT({
        month: this.calendars[0].month + t,
        year: this.calendars[0].year
      });
    this.draw();
  },
  gotoToday: function() {
    this.gotoDate(/* @__PURE__ */ new Date());
  },
  /**
   * change view to a specific month (zero-index, e.g. 0: January)
   */
  gotoMonth: function(t) {
    isNaN(t) || (this.calendars[0].month = parseInt(t, 10), this.adjustCalendars());
  },
  nextMonth: function() {
    this.calendars[0].month++, this.adjustCalendars();
  },
  prevMonth: function() {
    this.calendars[0].month--, this.adjustCalendars();
  },
  /**
   * change view to a specific full year (e.g. "2012")
   */
  gotoYear: function(t) {
    isNaN(t) || (this.calendars[0].year = parseInt(t, 10), this.adjustCalendars());
  },
  /**
   * change the minDate
   */
  setMinDate: function(t) {
    t instanceof Date ? (Bf(t), this._o.minDate = t, this._o.minYear = t.getFullYear(), this._o.minMonth = t.getMonth()) : (this._o.minDate = kr.minDate, this._o.minYear = kr.minYear, this._o.minMonth = kr.minMonth, this._o.startRange = kr.startRange), this.draw();
  },
  /**
   * change the maxDate
   */
  setMaxDate: function(t) {
    t instanceof Date ? (Bf(t), this._o.maxDate = t, this._o.maxYear = t.getFullYear(), this._o.maxMonth = t.getMonth()) : (this._o.maxDate = kr.maxDate, this._o.maxYear = kr.maxYear, this._o.maxMonth = kr.maxMonth, this._o.endRange = kr.endRange), this.draw();
  },
  setStartRange: function(t) {
    this._o.startRange = t;
  },
  setEndRange: function(t) {
    this._o.endRange = t;
  },
  /**
   * refresh the HTML
   */
  draw: function(t) {
    if (!this._v && !t)
      return;
    let e = this._o, n = e.minYear, i = e.maxYear, r = e.minMonth, o = e.maxMonth, s = "", a;
    this._y <= n && (this._y = n, !isNaN(r) && this._m < r && (this._m = r)), this._y >= i && (this._y = i, !isNaN(o) && this._m > o && (this._m = o));
    for (let l = 0; l < e.numberOfMonths; l++)
      a = "pika-title-" + Math.random().toString(36).replace(/[^a-z]+/g, "").substr(0, 2), s += '<div class="pika-lendar">' + MJ(this, l, this.calendars[l].year, this.calendars[l].month, this.calendars[0].year, a) + this.render(this.calendars[l].year, this.calendars[l].month, a) + "</div>";
    this.el.innerHTML = s, e.bound && e.field.type !== "hidden" && ky(function() {
      e.trigger.focus();
    }, 1), typeof this._o.onDraw == "function" && this._o.onDraw(this), e.bound && e.field.setAttribute("aria-label", e.ariaLabel);
  },
  adjustPosition: function() {
    let t, e, n, i, r, o, s, a, l, u, c;
    this._o.container || (this.el.style.position = "absolute", t = this._o.trigger, e = this.el.offsetWidth, n = this.el.offsetHeight, i = Ds().innerWidth || Vi().documentElement.clientWidth, r = Ds().innerHeight || Vi().documentElement.clientHeight, o = Vi().body.scrollTop || Vi().documentElement.scrollTop, u = !0, c = !0, l = t.getBoundingClientRect(), s = l.left + Ds().pageXOffset, a = l.bottom + Ds().pageYOffset, (this._o.reposition && s + e > i || this._o.position.indexOf("right") > -1 && s - e + t.offsetWidth > 0) && (s = s - e + t.offsetWidth, u = !1), (this._o.reposition && a + n > r + o || this._o.position.indexOf("top") > -1 && a - n - t.offsetHeight > 0) && (a = a - n - t.offsetHeight, c = !1), s < 0 && (s = 0), a < 0 && (a = 0), this.el.style.left = s + "px", this.el.style.top = a + "px", MC(this.el, u ? "left-aligned" : "right-aligned"), MC(this.el, c ? "bottom-aligned" : "top-aligned"), IC(this.el, u ? "right-aligned" : "left-aligned"), IC(this.el, c ? "top-aligned" : "bottom-aligned"));
  },
  /**
   * render HTML for a particular month
   */
  render: function(t, e, n) {
    let i = this._o, r = /* @__PURE__ */ new Date(), o = WT(t, e), s = new Date(t, e, 1).getDay(), a = [], l = [];
    Bf(r), i.firstDay > 0 && (s -= i.firstDay, s < 0 && (s += 7));
    let u = e === 0 ? 11 : e - 1, c = e === 11 ? 0 : e + 1, h = e === 0 ? t - 1 : t, d = e === 11 ? t + 1 : t, g = WT(h, u), m = o + s, w = m;
    for (; w > 7; )
      w -= 7;
    m += 7 - w;
    let C = !1;
    for (let S = 0, b = 0; S < m; S++) {
      let R = new Date(t, e, 1 + (S - s)), _ = vi(this._d) ? Ff(R, this._d) : !1, O = Ff(R, r), $ = i.events.indexOf(R.toDateString()) !== -1, U = S < s || S >= o + s, G = 1 + (S - s), z = e, j = t, J = i.startRange && Ff(i.startRange, R), q = i.endRange && Ff(i.endRange, R), re = i.startRange && i.endRange && i.startRange < R && R < i.endRange, me = i.minDate && R < i.minDate || i.maxDate && R > i.maxDate || i.disableWeekends && CJ(R) || i.disableDayFn && i.disableDayFn(R);
      U && (S < s ? (G = g + G, z = u, j = h) : (G = G - o, z = c, j = d));
      let Me = {
        day: G,
        month: z,
        year: j,
        hasEvent: $,
        isSelected: _,
        isToday: O,
        isDisabled: me,
        isEmpty: U,
        isStartRange: J,
        isEndRange: q,
        isInRange: re,
        showDaysInNextAndPreviousMonths: i.showDaysInNextAndPreviousMonths,
        enableSelectionDaysInNextAndPreviousMonths: i.enableSelectionDaysInNextAndPreviousMonths
      };
      i.pickWholeWeek && _ && (C = !0), l.push(yJ(Me)), ++b === 7 && (i.showWeekNumber && l.unshift(bJ(this, S - s, e, t, i.firstWeekOfYearMinDays)), a.push(RJ(l, i.isRTL, i.pickWholeWeek, C)), l = [], b = 0, C = !1);
    }
    return IJ(i, a, n);
  },
  isVisible: function() {
    return this._v;
  },
  show: function() {
    this.isVisible() || (this._v = !0, this.draw(), IC(this.el, "is-hidden"), this._o.bound && (Kr(Vi(), "click", this._onClick), this.adjustPosition()), typeof this._o.onOpen == "function" && this._o.onOpen.call(this));
  },
  hide: function() {
    let t = this._v;
    t !== !1 && (this._o.bound && To(Vi(), "click", this._onClick), this._o.container || (this.el.style.position = "static", this.el.style.left = "auto", this.el.style.top = "auto"), MC(this.el, "is-hidden"), this._v = !1, t !== void 0 && typeof this._o.onClose == "function" && this._o.onClose.call(this));
  },
  /**
   * GAME OVER
   */
  destroy: function() {
    let t = this._o;
    this.hide(), To(this.el, "mousedown", this._onMouseDown, !0), To(this.el, "touchend", this._onMouseDown, !0), To(this.el, "change", this._onChange), t.keyboardInput && To(Vi(), "keydown", this._onKeyChange), t.field && (To(t.field, "change", this._onInputChange), t.bound && (To(t.trigger, "click", this._onInputClick), To(t.trigger, "focus", this._onInputFocus), To(t.trigger, "blur", this._onInputBlur))), this.el.parentNode && this.el.parentNode.removeChild(this.el);
  }
};
function OC(t, e, n) {
  return (e = OJ(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function OJ(t) {
  var e = PJ(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function PJ(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const L1 = "date", jT = "dateEditor";
class B1 extends Al {
  constructor() {
    super(...arguments), OC(this, "defaultDateFormat", "DD/MM/YYYY"), OC(this, "parentDestroyed", !1), OC(this, "$datePicker", null);
  }
  static get EDITOR_TYPE() {
    return L1;
  }
  init() {
    if (typeof X != "function")
      throw new Error("You need to include moment.js to your project.");
    if (typeof Ly != "function")
      throw new Error("You need to include Pikaday to your project.");
    super.init(), this.hot.addHook("afterDestroy", () => {
      this.parentDestroyed = !0, this.destroyElements();
    });
  }
  /**
   * Create data picker instance.
   */
  createElements() {
    super.createElements(), this.datePicker = this.hot.rootDocument.createElement("DIV"), this.datePickerStyle = this.datePicker.style, this.datePickerStyle.position = "absolute", this.datePickerStyle.top = 0, this.datePickerStyle.left = 0, this.datePickerStyle.zIndex = 9999, this.datePicker.setAttribute("dir", this.hot.isRtl() ? "rtl" : "ltr"), Q(this.datePicker, "htDatepickerHolder"), this.hot.rootDocument.body.appendChild(this.datePicker), this.eventManager.addEventListener(this.datePicker, "mousedown", (e) => {
      Te(e.target, "pika-day") && this.hideDatepicker(), e.stopPropagation();
    });
  }
  /**
   * Destroy data picker instance.
   */
  destroyElements() {
    const e = this.datePicker.parentNode;
    this.$datePicker && this.$datePicker.destroy(), e && e.removeChild(this.datePicker);
  }
  /**
   * Prepare editor to appear.
   *
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number|string} prop The column property (passed when datasource is an array of objects).
   * @param {HTMLTableCellElement} td The rendered cell element.
   * @param {*} value The rendered value.
   * @param {object} cellProperties The cell meta object (see {@link Core#getCellMeta}).
   */
  prepare(e, n, i, r, o, s) {
    super.prepare(e, n, i, r, o, s);
  }
  /**
   * Open editor.
   *
   * @param {Event} [event=null] The event object.
   */
  open() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
    const i = this.hot.getShortcutManager().getContext("editor");
    this.showDatepicker(e), super.open(), i.addShortcuts([{
      keys: [["ArrowLeft"]],
      callback: () => {
        this.$datePicker.adjustDate("subtract", 1);
      }
    }, {
      keys: [["ArrowRight"]],
      callback: () => {
        this.$datePicker.adjustDate("add", 1);
      }
    }, {
      keys: [["ArrowUp"]],
      callback: () => {
        this.$datePicker.adjustDate("subtract", 7);
      }
    }, {
      keys: [["ArrowDown"]],
      callback: () => {
        this.$datePicker.adjustDate("add", 7);
      }
    }], {
      group: jT
    });
  }
  /**
   * Close editor.
   */
  close() {
    var e;
    this._opened = !1, (e = this.$datePicker) !== null && e !== void 0 && e.destroy && this.$datePicker.destroy(), this.hot._registerTimeout(() => {
      const r = this.hot._getEditorManager();
      r.closeEditor(), this.hot.view.render(), r.prepareEditor();
    }), this.hot.getShortcutManager().getContext("editor").removeShortcutsByGroup(jT), super.close();
  }
  /**
   * Finishes editing and start saving or restoring process for editing cell or last selected range.
   *
   * @param {boolean} restoreOriginalValue If true, then closes editor without saving value from the editor into a cell.
   * @param {boolean} ctrlDown If true, then saveValue will save editor's value to each cell in the last selected range.
   */
  finishEditing() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1, n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    super.finishEditing(e, n);
  }
  /**
   * Show data picker.
   *
   * @param {Event} event The event object.
   */
  showDatepicker(e) {
    const n = this.cellProperties.dateFormat || this.defaultDateFormat, i = this.hot.view.isMouseDown(), r = e ? gb(e.keyCode) : !1;
    let o;
    this.datePicker.style.display = "block", this.$datePicker = new Ly(this.getDatePickerConfig()), typeof this.$datePicker.useMoment == "function" && this.$datePicker.useMoment(X), this.$datePicker._onInputFocus = function() {
    }, this.originalValue ? (o = this.originalValue, X(o, n, !0).isValid() && this.$datePicker.setMoment(X(o, n), !0), this.getValue() !== this.originalValue && this.setValue(this.originalValue), !r && !i && this.setValue("")) : this.cellProperties.defaultDate ? (o = this.cellProperties.defaultDate, X(o, n, !0).isValid() && this.$datePicker.setMoment(X(o, n), !0), !r && !i && this.setValue("")) : this.$datePicker.gotoToday();
  }
  /**
   * Hide data picker.
   */
  hideDatepicker() {
    this.datePickerStyle.display = "none", this.$datePicker.hide();
  }
  /**
   * Get date picker options.
   *
   * @returns {object}
   */
  getDatePickerConfig() {
    const e = this.TEXTAREA, n = {};
    this.cellProperties && this.cellProperties.datePickerConfig && Sd(n, this.cellProperties.datePickerConfig);
    const i = n.onSelect, r = n.onClose;
    return n.field = e, n.trigger = e, n.container = this.datePicker, n.bound = !1, n.keyboardInput = !1, n.format = n.format || this.defaultDateFormat, n.reposition = n.reposition || !1, n.isRTL = !1, n.onSelect = (o) => {
      let s = o;
      isNaN(s.getTime()) || (s = X(s).format(this.cellProperties.dateFormat || this.defaultDateFormat)), this.setValue(s), i && i();
    }, n.onClose = () => {
      this.parentDestroyed || this.finishEditing(!1), r && r();
    }, n;
  }
  /**
   * Refreshes datepicker's size and position. The method is called internally by Handsontable.
   *
   * @private
   * @param {boolean} force Indicates if the refreshing editor dimensions should be triggered.
   */
  refreshDimensions(e) {
    var n;
    if (super.refreshDimensions(e), this.state !== Xt.EDITING)
      return;
    if (this.TD = this.getEditedCell(), !this.TD) {
      this.hideDatepicker();
      return;
    }
    const {
      rowIndexMapper: i,
      columnIndexMapper: r
    } = this.hot, {
      wtOverlays: o
    } = this.hot.view._wt, {
      wtTable: s
    } = (n = o.getParentOverlay(this.TD)) !== null && n !== void 0 ? n : this.hot.view._wt, a = i.getVisualFromRenderableIndex(s.getFirstPartiallyVisibleRow()), l = i.getVisualFromRenderableIndex(s.getLastPartiallyVisibleRow()), u = r.getVisualFromRenderableIndex(s.getFirstPartiallyVisibleColumn()), c = r.getVisualFromRenderableIndex(s.getLastPartiallyVisibleColumn());
    if (this.row >= a && this.row <= l && this.col >= u && this.col <= c) {
      const h = this.TD.getBoundingClientRect();
      this.datePickerStyle.top = `${this.hot.rootWindow.pageYOffset + h.top + Zt(this.TD)}px`;
      let d = this.hot.rootWindow.pageXOffset;
      this.hot.isRtl() ? d += h.right - It(this.datePicker) : d += h.left, this.datePickerStyle.left = `${d}px`;
    } else
      this.hideDatepicker();
  }
}
const xJ = "dropdown";
class lR extends sR {
  static get EDITOR_TYPE() {
    return xJ;
  }
  /**
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number|string} prop The column property (passed when datasource is an array of objects).
   * @param {HTMLTableCellElement} td The rendered cell element.
   * @param {*} value The rendered value.
   * @param {object} cellProperties The cell meta object (see {@link Core#getCellMeta}).
   */
  prepare(e, n, i, r, o, s) {
    super.prepare(e, n, i, r, o, s), this.cellProperties.filter = !1, this.cellProperties.strict = !0;
  }
}
he.getSingleton().add("beforeValidate", function(t, e, n) {
  const i = this.propToCol(n);
  if (Number.isInteger(i)) {
    const r = this.getCellMeta(e, i);
    r.editor === lR && r.strict === void 0 && (r.filter = !1, r.strict = !0);
  }
});
const HJ = "numeric";
class F1 extends Al {
  static get EDITOR_TYPE() {
    return HJ;
  }
}
const _J = "password";
class V1 extends Al {
  static get EDITOR_TYPE() {
    return _J;
  }
  createElements() {
    super.createElements(), this.TEXTAREA = this.hot.rootDocument.createElement("input"), this.TEXTAREA.setAttribute("type", "password"), this.TEXTAREA.setAttribute("data-hot-input", ""), this.TEXTAREA.className = "handsontableInput", this.textareaStyle = this.TEXTAREA.style, this.textareaStyle.width = 0, this.textareaStyle.height = 0, br(this.TEXTAREA_PARENT), this.TEXTAREA_PARENT.appendChild(this.TEXTAREA);
  }
}
const PC = "ht_editor_visible", GT = "selectEditor", NJ = "select";
class W1 extends Fu {
  static get EDITOR_TYPE() {
    return NJ;
  }
  /**
   * Initializes editor instance, DOM Element and mount hooks.
   */
  init() {
    this.select = this.hot.rootDocument.createElement("select"), this.select.setAttribute("data-hot-input", "true"), this.select.style.display = "none", Q(this.select, "htSelectEditor"), this.hot.rootElement.appendChild(this.select), this.registerHooks();
  }
  /**
   * Returns select's value.
   *
   * @returns {*}
   */
  getValue() {
    return this.select.value;
  }
  /**
   * Sets value in the select element.
   *
   * @param {*} value A new select's value.
   */
  setValue(e) {
    this.select.value = e;
  }
  /**
   * Opens the editor and adjust its size.
   */
  open() {
    this._opened = !0, this.refreshDimensions(), this.select.style.display = "", this.hot.getShortcutManager().setActiveContextName("editor"), this.registerShortcuts();
  }
  /**
   * Closes the editor.
   */
  close() {
    this._opened = !1, this.select.style.display = "none", Te(this.select, PC) && xe(this.select, PC), this.unregisterShortcuts(), this.clearHooks();
  }
  /**
   * Sets focus state on the select element.
   */
  focus() {
    this.select.focus();
  }
  /**
   * Binds hooks to refresh editor's size after scrolling of the viewport or resizing of columns/rows.
   *
   * @private
   */
  registerHooks() {
    this.addHook("afterScrollHorizontally", () => this.refreshDimensions()), this.addHook("afterScrollVertically", () => this.refreshDimensions()), this.addHook("afterColumnResize", () => this.refreshDimensions()), this.addHook("afterRowResize", () => this.refreshDimensions());
  }
  /**
   * Prepares editor's meta data and a list of available options.
   *
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number|string} prop The column property (passed when datasource is an array of objects).
   * @param {HTMLTableCellElement} td The rendered cell element.
   * @param {*} value The rendered value.
   * @param {object} cellProperties The cell meta object (see {@link Core#getCellMeta}).
   */
  prepare(e, n, i, r, o, s) {
    super.prepare(e, n, i, r, o, s);
    const a = this.cellProperties.selectOptions;
    let l;
    typeof a == "function" ? l = this.prepareOptions(a(this.row, this.col, this.prop)) : l = this.prepareOptions(a), br(this.select), Fe(l, (u, c) => {
      const h = this.hot.rootDocument.createElement("OPTION");
      h.value = c, pl(h, u), this.select.appendChild(h);
    });
  }
  /**
   * Creates consistent list of available options.
   *
   * @private
   * @param {Array|object} optionsToPrepare The list of the values to render in the select eleemnt.
   * @returns {object}
   */
  prepareOptions(e) {
    let n = {};
    if (Array.isArray(e))
      for (let i = 0, r = e.length; i < r; i++)
        n[e[i]] = e[i];
    else
      typeof e == "object" && (n = e);
    return n;
  }
  /**
   * Refreshes editor's value using source data.
   *
   * @private
   */
  refreshValue() {
    const e = this.hot.getSourceDataAtCell(this.row, this.prop);
    this.originalValue = e, this.setValue(e), this.refreshDimensions();
  }
  /**
   * Refreshes editor's size and position.
   *
   * @private
   */
  refreshDimensions() {
    if (this.state !== Xt.EDITING)
      return;
    if (this.TD = this.getEditedCell(), !this.TD) {
      this.close();
      return;
    }
    const {
      top: e,
      start: n,
      width: i,
      height: r
    } = this.getEditedCellRect(), o = this.select.style;
    o.height = `${r}px`, o.width = `${i}px`, o.top = `${e}px`, o[this.hot.isRtl() ? "right" : "left"] = `${n}px`, o.margin = "0px", Q(this.select, PC);
  }
  /**
   * Register shortcuts responsible for handling editor.
   *
   * @private
   */
  registerShortcuts() {
    const n = this.hot.getShortcutManager().getContext("editor"), i = {
      group: GT
    };
    this.isInFullEditMode() && n.addShortcuts([{
      keys: [["ArrowUp"]],
      callback: () => {
        const r = this.select.selectedIndex - 1;
        r >= 0 && (this.select[r].selected = !0);
      }
    }, {
      keys: [["ArrowDown"]],
      callback: () => {
        const r = this.select.selectedIndex + 1;
        r <= this.select.length - 1 && (this.select[r].selected = !0);
      }
    }], i);
  }
  /**
   * Unregister shortcuts responsible for handling editor.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("editor").removeShortcutsByGroup(GT);
  }
}
const DJ = "time";
class $1 extends Al {
  static get EDITOR_TYPE() {
    return DJ;
  }
  /**
   * Prepares editor's meta data.
   *
   * @param {number} row The visual row index.
   * @param {number} col The visual column index.
   * @param {number|string} prop The column property (passed when datasource is an array of objects).
   * @param {HTMLTableCellElement} td The rendered cell element.
   * @param {*} value The rendered value.
   * @param {object} cellProperties The cell meta object (see {@link Core#getCellMeta}).
   */
  prepare(e, n, i, r, o, s) {
    super.prepare(e, n, i, r, o, s), this.TEXTAREA.dir = "ltr";
  }
}
function kJ() {
  ai(Fu), ai(sR), ai(k1), ai(B1), ai(lR), ai(oR), ai(F1), ai(V1), ai(W1), ai(Al), ai($1);
}
const LJ = "html";
function uR(t, e, n, i, r, o, s) {
  Od.apply(this, [t, e, n, i, r, o, s]), pl(e, o == null ? "" : o, !1);
}
uR.RENDERER_TYPE = LJ;
const BJ = "autocomplete";
function Vu(t, e, n, i, r, o, s) {
  const {
    rootDocument: a
  } = t, l = s.allowHtml ? uR : ss, u = a.createElement("DIV"), c = t.getSettings().ariaTags;
  if (u.className = "htAutocompleteArrow", c && u.setAttribute(...gl()), u.appendChild(a.createTextNode(String.fromCharCode(9660))), l.apply(this, [t, e, n, i, r, o, s]), e.firstChild || e.appendChild(a.createTextNode(String.fromCharCode(160))), e.insertBefore(u, e.firstChild), Q(e, "htAutocomplete"), !t.acArrowListener) {
    const h = new Qi(t);
    t.acArrowListener = function(d) {
      Te(d.target, "htAutocompleteArrow") && t.view._wt.getSetting("onCellDblClick", null, t._createCellCoords(n, i), e);
    }, h.addEventListener(t.rootElement, "mousedown", t.acArrowListener), t.addHookOnce("afterDestroy", () => {
      h.destroy();
    });
  }
}
Vu.RENDERER_TYPE = BJ;
const FJ = "dropdown";
function cR(t, e, n, i, r, o, s) {
  Vu.apply(this, [t, e, n, i, r, o, s]);
}
cR.RENDERER_TYPE = FJ;
const zT = /* @__PURE__ */ new WeakMap(), KT = /* @__PURE__ */ new WeakMap(), VJ = "htBadValue", Iu = "data-row", Ou = "data-col", WJ = "checkboxRenderer", U1 = "checkbox";
he.getSingleton().add("modifyAutoColumnSizeSeed", function(t, e, n) {
  const {
    label: i,
    type: r,
    row: o,
    column: s,
    prop: a
  } = e;
  if (r === U1) {
    if (i) {
      const {
        value: l,
        property: u
      } = i;
      let c = n;
      if (l)
        c = typeof l == "function" ? l(o, s, a, n) : l;
      else if (u) {
        const h = this.getDataAtRowProp(o, u);
        c = h !== null ? h : n;
      }
      t = c;
    }
    return t;
  }
});
function hR(t, e, n, i, r, o, s) {
  const {
    rootDocument: a
  } = t, l = t.getSettings().ariaTags;
  Od.apply(this, [t, e, n, i, r, o, s]), $J(t);
  let u = UJ(a);
  const c = s.label;
  let h = !1;
  if (typeof s.checkedTemplate == "undefined" && (s.checkedTemplate = !0), typeof s.uncheckedTemplate == "undefined" && (s.uncheckedTemplate = !1), br(e), o === s.checkedTemplate || Gt(o).toLocaleLowerCase(s.locale) === Gt(s.checkedTemplate).toLocaleLowerCase(s.locale) ? u.checked = !0 : o === s.uncheckedTemplate || Gt(o).toLocaleLowerCase(s.locale) === Gt(s.uncheckedTemplate).toLocaleLowerCase(s.locale) ? u.checked = !1 : zt(o) ? Q(u, "noValue") : (u.style.display = "none", Q(u, VJ), h = !0), Ee(u, [[Iu, n], [Ou, i]]), l && Ee(u, [wm(u.checked ? t.getTranslatedPhrase(Zb) : t.getTranslatedPhrase(Jb)), NP(u.checked), SG()]), !h && c) {
    let w = "";
    if (c.value)
      w = typeof c.value == "function" ? c.value.call(this, n, i, r, o) : c.value;
    else if (c.property) {
      const S = t.getDataAtRowProp(n, c.property);
      w = S !== null ? S : "";
    }
    const C = YJ(a, w, c.separated !== !0);
    c.position === "before" ? c.separated ? (e.appendChild(C), e.appendChild(u)) : (C.appendChild(u), u = C) : (!c.position || c.position === "after") && (c.separated ? (e.appendChild(u), e.appendChild(C)) : (C.insertBefore(u, C.firstChild), u = C));
  }
  (!c || c && !c.separated) && e.appendChild(u), h && e.appendChild(a.createTextNode("#bad-value#")), zT.has(t) || (zT.set(t, !0), d());
  function d() {
    const C = t.getShortcutManager().getContext("grid"), S = {
      group: WJ,
      relativeToGroup: Js,
      position: "before"
    };
    C.addShortcuts([{
      keys: [["space"]],
      callback: () => (g(), !m()),
      runOnlyIf: () => {
        var b;
        return (b = t.getSelectedRangeLast()) === null || b === void 0 ? void 0 : b.highlight.isCell();
      }
    }, {
      keys: [["enter"]],
      callback: () => (g(), !m()),
      runOnlyIf: () => {
        const b = t.getSelectedRangeLast();
        return t.getSettings().enterBeginsEditing && (b == null ? void 0 : b.isSingle()) && b.highlight.isCell();
      }
    }, {
      keys: [["delete"], ["backspace"]],
      callback: () => (g(!0), !m()),
      runOnlyIf: () => {
        var b;
        return (b = t.getSelectedRangeLast()) === null || b === void 0 ? void 0 : b.highlight.isCell();
      }
    }], S);
  }
  function g() {
    let w = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    const C = t.getSelectedRange(), S = [], b = /* @__PURE__ */ new Map();
    let R = [], _ = 0;
    if (C) {
      for (let O = 0; O < C.length; O++) {
        const {
          row: $,
          col: U
        } = C[O].getTopStartCorner(), {
          row: G,
          col: z
        } = C[O].getBottomEndCorner();
        for (let j = $; j <= G; j += 1)
          for (let J = U; J <= z; J += 1) {
            const q = t.getCellMeta(j, J), re = {
              checkedTemplate: q.checkedTemplate,
              uncheckedTemplate: q.uncheckedTemplate
            };
            if (q.type !== "checkbox") {
              w === !0 && !q.readOnly && (b.has(S.length) ? b.set(S.length, [...b.get(S.length), [j, J, null]]) : b.set(S.length, [[j, J, null]]));
              continue;
            }
            if (q.readOnly === !0)
              continue;
            typeof q.checkedTemplate == "undefined" && (q.checkedTemplate = !0), typeof q.uncheckedTemplate == "undefined" && (q.uncheckedTemplate = !1);
            const me = t.getDataAtCell(j, J);
            w === !1 ? [q.checkedTemplate, q.checkedTemplate.toString()].includes(me) ? R.push([j, J, q.uncheckedTemplate, re]) : [q.uncheckedTemplate, q.uncheckedTemplate.toString(), null, void 0].includes(me) && R.push([j, J, q.checkedTemplate, re]) : R.push([j, J, q.uncheckedTemplate, re]), _ += 1;
          }
        S.push(_), _ = 0;
      }
      R.every((O) => {
        let [, , $] = O;
        return $ === R[0][2];
      }) ? R = R.map((O) => {
        let [$, U, G] = O;
        return [$, U, G];
      }) : R = R.map((O) => {
        let [$, U, , G] = O;
        return [$, U, G.checkedTemplate];
      }), R.length > 0 && S.forEach((O, $) => {
        let U = R.splice(0, O);
        b.size && b.has($) && (U = [...U, ...b.get($)]), t.setDataAtCell(U);
      });
    }
  }
  function m() {
    const w = t.getSelectedRange();
    if (w) {
      for (let C = 0; C < w.length; C++) {
        const S = w[C].getTopStartCorner(), b = w[C].getBottomEndCorner();
        for (let R = S.row; R <= b.row; R++)
          for (let _ = S.col; _ <= b.col; _++) {
            const O = t.getCellMeta(R, _), $ = t.getCell(R, _);
            if ($ == null)
              return !0;
            if ($.querySelectorAll("input[type=checkbox]").length > 0 && !O.readOnly)
              return !0;
          }
      }
      return !1;
    }
  }
}
hR.RENDERER_TYPE = U1;
function $J(t) {
  let e = KT.get(t);
  if (!e) {
    const {
      rootElement: n
    } = t;
    e = new Qi(t), e.addEventListener(n, "click", (i) => GJ(i, t)), e.addEventListener(n, "mouseup", (i) => jJ(i, t)), e.addEventListener(n, "change", (i) => zJ(i, t)), KT.set(t, e);
  }
  return e;
}
function UJ(t) {
  const e = t.createElement("input");
  return e.className = "htCheckboxRendererInput", e.type = "checkbox", e.setAttribute("tabindex", "-1"), e.cloneNode(!1);
}
function YJ(t, e, n) {
  const i = t.createElement("label");
  return i.className = `htCheckboxRendererLabel ${n ? "fullWidth" : ""}`, i.appendChild(t.createTextNode(e)), i.cloneNode(!0);
}
function jJ(t, e) {
  const {
    target: n
  } = t;
  dR(n) && (!n.hasAttribute(Iu) || !n.hasAttribute(Ou) || setTimeout(e.listen, 10));
}
function GJ(t, e) {
  const {
    target: n
  } = t;
  if (!dR(n) || !n.hasAttribute(Iu) || !n.hasAttribute(Ou))
    return;
  const i = parseInt(n.getAttribute(Iu), 10), r = parseInt(n.getAttribute(Ou), 10);
  e.getCellMeta(i, r).readOnly && t.preventDefault();
}
function zJ(t, e) {
  const {
    target: n
  } = t;
  if (!dR(n) || !n.hasAttribute(Iu) || !n.hasAttribute(Ou))
    return;
  const i = parseInt(n.getAttribute(Iu), 10), r = parseInt(n.getAttribute(Ou), 10), o = e.getCellMeta(i, r);
  if (!o.readOnly) {
    let s = null;
    t.target.checked ? s = o.uncheckedTemplate === void 0 ? !0 : o.checkedTemplate : s = o.uncheckedTemplate === void 0 ? !1 : o.uncheckedTemplate, e.setDataAtCell(i, r, s);
  }
}
function dR(t) {
  return t.tagName === "INPUT" && t.getAttribute("type") === "checkbox";
}
const KJ = "handsontable";
function fR(t, e, n, i, r, o, s) {
  Vu.apply(this, [t, e, n, i, r, o, s]);
}
fR.RENDERER_TYPE = KJ;
var Y1 = { exports: {} };
(function(t, e) {
  (function(n) {
    t.exports = n();
  })(function() {
    return function n(i, r, o) {
      function s(u, c) {
        if (!r[u]) {
          if (!i[u]) {
            var h = typeof Yd == "function" && Yd;
            if (!c && h)
              return h(u, !0);
            if (a)
              return a(u, !0);
            var d = new Error("Cannot find module '" + u + "'");
            throw d.code = "MODULE_NOT_FOUND", d;
          }
          var g = r[u] = { exports: {} };
          i[u][0].call(g.exports, function(m) {
            return s(i[u][1][m] || m);
          }, g, g.exports, n, i, r, o);
        }
        return r[u].exports;
      }
      for (var a = typeof Yd == "function" && Yd, l = 0; l < o.length; l++)
        s(o[l]);
      return s;
    }({ 1: [function(n, i, r) {
      (function(o) {
        var s, a = /^-?(?:\d+(?:\.\d*)?|\.\d+)(?:e[+-]?\d+)?$/i, l = Math.ceil, u = Math.floor, c = "[BigNumber Error] ", h = c + "Number primitive has more than 15 significant digits: ", d = 1e14, g = 14, m = 9007199254740991, w = [1, 10, 100, 1e3, 1e4, 1e5, 1e6, 1e7, 1e8, 1e9, 1e10, 1e11, 1e12, 1e13], C = 1e7, S = 1e9;
        function b(z) {
          var j = 0 | z;
          return 0 < z || z === j ? j : j - 1;
        }
        function R(z) {
          for (var j, J, q = 1, re = z.length, me = z[0] + ""; q < re; ) {
            for (j = z[q++] + "", J = g - j.length; J--; j = "0" + j)
              ;
            me += j;
          }
          for (re = me.length; me.charCodeAt(--re) === 48; )
            ;
          return me.slice(0, re + 1 || 1);
        }
        function _(z, j) {
          var J, q, re = z.c, me = j.c, Me = z.s, nt = j.s, Ne = z.e, Ce = j.e;
          if (!Me || !nt)
            return null;
          if (J = re && !re[0], q = me && !me[0], J || q)
            return J ? q ? 0 : -nt : Me;
          if (Me != nt)
            return Me;
          if (J = Me < 0, q = Ne == Ce, !re || !me)
            return q ? 0 : !re ^ J ? 1 : -1;
          if (!q)
            return Ce < Ne ^ J ? 1 : -1;
          for (nt = (Ne = re.length) < (Ce = me.length) ? Ne : Ce, Me = 0; Me < nt; Me++)
            if (re[Me] != me[Me])
              return re[Me] > me[Me] ^ J ? 1 : -1;
          return Ne == Ce ? 0 : Ce < Ne ^ J ? 1 : -1;
        }
        function O(z, j, J, q) {
          if (z < j || J < z || z !== (z < 0 ? l(z) : u(z)))
            throw Error(c + (q || "Argument") + (typeof z == "number" ? z < j || J < z ? " out of range: " : " not an integer: " : " not a primitive number: ") + String(z));
        }
        function $(z) {
          var j = z.c.length - 1;
          return b(z.e / g) == j && z.c[j] % 2 != 0;
        }
        function U(z, j) {
          return (1 < z.length ? z.charAt(0) + "." + z.slice(1) : z) + (j < 0 ? "e" : "e+") + j;
        }
        function G(z, j, J) {
          var q, re;
          if (j < 0) {
            for (re = J + "."; ++j; re += J)
              ;
            z = re + z;
          } else if (++j > (q = z.length)) {
            for (re = J, j -= q; --j; re += J)
              ;
            z += re;
          } else
            j < q && (z = z.slice(0, j) + "." + z.slice(j));
          return z;
        }
        (s = function z(j) {
          var J, q, re, me, Me, nt, Ne, Ce, pe, wt, f = W.prototype = { constructor: W, toString: null, valueOf: null }, p = new W(1), A = 20, M = 4, P = -7, F = 21, Y = -1e7, K = 1e7, le = !1, ce = 1, Z = 0, He = { prefix: "", groupSize: 3, secondaryGroupSize: 0, groupSeparator: ",", decimalSeparator: ".", fractionGroupSize: 0, fractionGroupSeparator: " ", suffix: "" }, Pe = "0123456789abcdefghijklmnopqrstuvwxyz";
          function W(v, y) {
            var E, L, I, B, N, x, k, D, V = this;
            if (!(V instanceof W))
              return new W(v, y);
            if (y == null) {
              if (v instanceof W)
                return V.s = v.s, V.e = v.e, void (V.c = (v = v.c) ? v.slice() : v);
              if ((x = typeof v == "number") && 0 * v == 0) {
                if (V.s = 1 / v < 0 ? (v = -v, -1) : 1, v === ~~v) {
                  for (B = 0, N = v; 10 <= N; N /= 10, B++)
                    ;
                  return V.e = B, void (V.c = [v]);
                }
                D = String(v);
              } else {
                if (D = String(v), !a.test(D))
                  return re(V, D, x);
                V.s = D.charCodeAt(0) == 45 ? (D = D.slice(1), -1) : 1;
              }
              -1 < (B = D.indexOf(".")) && (D = D.replace(".", "")), 0 < (N = D.search(/e/i)) ? (B < 0 && (B = N), B += +D.slice(N + 1), D = D.substring(0, N)) : B < 0 && (B = D.length);
            } else {
              if (O(y, 2, Pe.length, "Base"), D = String(v), y == 10)
                return Ve(V = new W(v instanceof W ? v : D), A + V.e + 1, M);
              if (x = typeof v == "number") {
                if (0 * v != 0)
                  return re(V, D, x, y);
                if (V.s = 1 / v < 0 ? (D = D.slice(1), -1) : 1, W.DEBUG && 15 < D.replace(/^0\.0*|\./, "").length)
                  throw Error(h + v);
                x = !1;
              } else
                V.s = D.charCodeAt(0) === 45 ? (D = D.slice(1), -1) : 1;
              for (E = Pe.slice(0, y), B = N = 0, k = D.length; N < k; N++)
                if (E.indexOf(L = D.charAt(N)) < 0) {
                  if (L == ".") {
                    if (B < N) {
                      B = k;
                      continue;
                    }
                  } else if (!I && (D == D.toUpperCase() && (D = D.toLowerCase()) || D == D.toLowerCase() && (D = D.toUpperCase()))) {
                    I = !0, N = -1, B = 0;
                    continue;
                  }
                  return re(V, String(v), x, y);
                }
              -1 < (B = (D = q(D, y, 10, V.s)).indexOf(".")) ? D = D.replace(".", "") : B = D.length;
            }
            for (N = 0; D.charCodeAt(N) === 48; N++)
              ;
            for (k = D.length; D.charCodeAt(--k) === 48; )
              ;
            if (D = D.slice(N, ++k)) {
              if (k -= N, x && W.DEBUG && 15 < k && (m < v || v !== u(v)))
                throw Error(h + V.s * v);
              if (K < (B = B - N - 1))
                V.c = V.e = null;
              else if (B < Y)
                V.c = [V.e = 0];
              else {
                if (V.e = B, V.c = [], N = (B + 1) % g, B < 0 && (N += g), N < k) {
                  for (N && V.c.push(+D.slice(0, N)), k -= g; N < k; )
                    V.c.push(+D.slice(N, N += g));
                  D = D.slice(N), N = g - D.length;
                } else
                  N -= k;
                for (; N--; D += "0")
                  ;
                V.c.push(+D);
              }
            } else
              V.c = [V.e = 0];
          }
          function Qe(v, y, E, L) {
            var I, B, N, x, k;
            if (E == null ? E = M : O(E, 0, 8), !v.c)
              return v.toString();
            if (I = v.c[0], N = v.e, y == null)
              k = R(v.c), k = L == 1 || L == 2 && (N <= P || F <= N) ? U(k, N) : G(k, N, "0");
            else if (B = (v = Ve(new W(v), y, E)).e, x = (k = R(v.c)).length, L == 1 || L == 2 && (y <= B || B <= P)) {
              for (; x < y; k += "0", x++)
                ;
              k = U(k, B);
            } else if (y -= N, k = G(k, B, "0"), x < B + 1) {
              if (0 < --y)
                for (k += "."; y--; k += "0")
                  ;
            } else if (0 < (y += B - x))
              for (B + 1 == x && (k += "."); y--; k += "0")
                ;
            return v.s < 0 && I ? "-" + k : k;
          }
          function Ge(v, y) {
            for (var E, L = 1, I = new W(v[0]); L < v.length; L++) {
              if (!(E = new W(v[L])).s) {
                I = E;
                break;
              }
              y.call(I, E) && (I = E);
            }
            return I;
          }
          function it(v, y, E) {
            for (var L = 1, I = y.length; !y[--I]; y.pop())
              ;
            for (I = y[0]; 10 <= I; I /= 10, L++)
              ;
            return (E = L + E * g - 1) > K ? v.c = v.e = null : v.c = E < Y ? [v.e = 0] : (v.e = E, y), v;
          }
          function Ve(v, y, E, L) {
            var I, B, N, x, k, D, V, ne = v.c, ue = w;
            if (ne) {
              e: {
                for (I = 1, x = ne[0]; 10 <= x; x /= 10, I++)
                  ;
                if ((B = y - I) < 0)
                  B += g, N = y, V = (k = ne[D = 0]) / ue[I - N - 1] % 10 | 0;
                else if ((D = l((B + 1) / g)) >= ne.length) {
                  if (!L)
                    break e;
                  for (; ne.length <= D; ne.push(0))
                    ;
                  k = V = 0, N = (B %= g) - g + (I = 1);
                } else {
                  for (k = x = ne[D], I = 1; 10 <= x; x /= 10, I++)
                    ;
                  V = (N = (B %= g) - g + I) < 0 ? 0 : k / ue[I - N - 1] % 10 | 0;
                }
                if (L = L || y < 0 || ne[D + 1] != null || (N < 0 ? k : k % ue[I - N - 1]), L = E < 4 ? (V || L) && (E == 0 || E == (v.s < 0 ? 3 : 2)) : 5 < V || V == 5 && (E == 4 || L || E == 6 && (0 < B ? 0 < N ? k / ue[I - N] : 0 : ne[D - 1]) % 10 & 1 || E == (v.s < 0 ? 8 : 7)), y < 1 || !ne[0])
                  return ne.length = 0, L ? (y -= v.e + 1, ne[0] = ue[(g - y % g) % g], v.e = -y || 0) : ne[0] = v.e = 0, v;
                if (B == 0 ? (ne.length = D, x = 1, D--) : (ne.length = D + 1, x = ue[g - B], ne[D] = 0 < N ? u(k / ue[I - N] % ue[N]) * x : 0), L)
                  for (; ; ) {
                    if (D == 0) {
                      for (B = 1, N = ne[0]; 10 <= N; N /= 10, B++)
                        ;
                      for (N = ne[0] += x, x = 1; 10 <= N; N /= 10, x++)
                        ;
                      B != x && (v.e++, ne[0] == d && (ne[0] = 1));
                      break;
                    }
                    if (ne[D] += x, ne[D] != d)
                      break;
                    ne[D--] = 0, x = 1;
                  }
                for (B = ne.length; ne[--B] === 0; ne.pop())
                  ;
              }
              v.e > K ? v.c = v.e = null : v.e < Y && (v.c = [v.e = 0]);
            }
            return v;
          }
          function Re(v) {
            var y, E = v.e;
            return E === null ? v.toString() : (y = R(v.c), y = E <= P || F <= E ? U(y, E) : G(y, E, "0"), v.s < 0 ? "-" + y : y);
          }
          return W.clone = z, W.ROUND_UP = 0, W.ROUND_DOWN = 1, W.ROUND_CEIL = 2, W.ROUND_FLOOR = 3, W.ROUND_HALF_UP = 4, W.ROUND_HALF_DOWN = 5, W.ROUND_HALF_EVEN = 6, W.ROUND_HALF_CEIL = 7, W.ROUND_HALF_FLOOR = 8, W.EUCLID = 9, W.config = W.set = function(v) {
            var y, E;
            if (v != null) {
              if (typeof v != "object")
                throw Error(c + "Object expected: " + v);
              if (v.hasOwnProperty(y = "DECIMAL_PLACES") && (O(E = v[y], 0, S, y), A = E), v.hasOwnProperty(y = "ROUNDING_MODE") && (O(E = v[y], 0, 8, y), M = E), v.hasOwnProperty(y = "EXPONENTIAL_AT") && ((E = v[y]) && E.pop ? (O(E[0], -S, 0, y), O(E[1], 0, S, y), P = E[0], F = E[1]) : (O(E, -S, S, y), P = -(F = E < 0 ? -E : E))), v.hasOwnProperty(y = "RANGE"))
                if ((E = v[y]) && E.pop)
                  O(E[0], -S, -1, y), O(E[1], 1, S, y), Y = E[0], K = E[1];
                else {
                  if (O(E, -S, S, y), !E)
                    throw Error(c + y + " cannot be zero: " + E);
                  Y = -(K = E < 0 ? -E : E);
                }
              if (v.hasOwnProperty(y = "CRYPTO")) {
                if ((E = v[y]) !== !!E)
                  throw Error(c + y + " not true or false: " + E);
                if (E) {
                  if (typeof crypto == "undefined" || !crypto || !crypto.getRandomValues && !crypto.randomBytes)
                    throw le = !E, Error(c + "crypto unavailable");
                  le = E;
                } else
                  le = E;
              }
              if (v.hasOwnProperty(y = "MODULO_MODE") && (O(E = v[y], 0, 9, y), ce = E), v.hasOwnProperty(y = "POW_PRECISION") && (O(E = v[y], 0, S, y), Z = E), v.hasOwnProperty(y = "FORMAT")) {
                if (typeof (E = v[y]) != "object")
                  throw Error(c + y + " not an object: " + E);
                He = E;
              }
              if (v.hasOwnProperty(y = "ALPHABET")) {
                if (typeof (E = v[y]) != "string" || /^.$|[+-.\s]|(.).*\1/.test(E))
                  throw Error(c + y + " invalid: " + E);
                Pe = E;
              }
            }
            return { DECIMAL_PLACES: A, ROUNDING_MODE: M, EXPONENTIAL_AT: [P, F], RANGE: [Y, K], CRYPTO: le, MODULO_MODE: ce, POW_PRECISION: Z, FORMAT: He, ALPHABET: Pe };
          }, W.isBigNumber = function(v) {
            return v instanceof W || v && v._isBigNumber === !0 || !1;
          }, W.maximum = W.max = function() {
            return Ge(arguments, f.lt);
          }, W.minimum = W.min = function() {
            return Ge(arguments, f.gt);
          }, W.random = (me = 9007199254740992, Me = Math.random() * me & 2097151 ? function() {
            return u(Math.random() * me);
          } : function() {
            return 8388608 * (1073741824 * Math.random() | 0) + (8388608 * Math.random() | 0);
          }, function(v) {
            var y, E, L, I, B, N = 0, x = [], k = new W(p);
            if (v == null ? v = A : O(v, 0, S), I = l(v / g), le)
              if (crypto.getRandomValues) {
                for (y = crypto.getRandomValues(new Uint32Array(I *= 2)); N < I; )
                  9e15 <= (B = 131072 * y[N] + (y[N + 1] >>> 11)) ? (E = crypto.getRandomValues(new Uint32Array(2)), y[N] = E[0], y[N + 1] = E[1]) : (x.push(B % 1e14), N += 2);
                N = I / 2;
              } else {
                if (!crypto.randomBytes)
                  throw le = !1, Error(c + "crypto unavailable");
                for (y = crypto.randomBytes(I *= 7); N < I; )
                  9e15 <= (B = 281474976710656 * (31 & y[N]) + 1099511627776 * y[N + 1] + 4294967296 * y[N + 2] + 16777216 * y[N + 3] + (y[N + 4] << 16) + (y[N + 5] << 8) + y[N + 6]) ? crypto.randomBytes(7).copy(y, N) : (x.push(B % 1e14), N += 7);
                N = I / 7;
              }
            if (!le)
              for (; N < I; )
                (B = Me()) < 9e15 && (x[N++] = B % 1e14);
            for (I = x[--N], v %= g, I && v && (B = w[g - v], x[N] = u(I / B) * B); x[N] === 0; x.pop(), N--)
              ;
            if (N < 0)
              x = [L = 0];
            else {
              for (L = -1; x[0] === 0; x.splice(0, 1), L -= g)
                ;
              for (N = 1, B = x[0]; 10 <= B; B /= 10, N++)
                ;
              N < g && (L -= g - N);
            }
            return k.e = L, k.c = x, k;
          }), W.sum = function() {
            for (var v = 1, y = arguments, E = new W(y[0]); v < y.length; )
              E = E.plus(y[v++]);
            return E;
          }, q = function() {
            var v = "0123456789";
            function y(E, L, I, B) {
              for (var N, x, k = [0], D = 0, V = E.length; D < V; ) {
                for (x = k.length; x--; k[x] *= L)
                  ;
                for (k[0] += B.indexOf(E.charAt(D++)), N = 0; N < k.length; N++)
                  k[N] > I - 1 && (k[N + 1] == null && (k[N + 1] = 0), k[N + 1] += k[N] / I | 0, k[N] %= I);
              }
              return k.reverse();
            }
            return function(E, L, I, B, N) {
              var x, k, D, V, ne, ue, ye, Le, Ze = E.indexOf("."), Ue = A, Oe = M;
              for (0 <= Ze && (V = Z, Z = 0, E = E.replace(".", ""), ue = (Le = new W(L)).pow(E.length - Ze), Z = V, Le.c = y(G(R(ue.c), ue.e, "0"), 10, I, v), Le.e = Le.c.length), D = V = (ye = y(E, L, I, N ? (x = Pe, v) : (x = v, Pe))).length; ye[--V] == 0; ye.pop())
                ;
              if (!ye[0])
                return x.charAt(0);
              if (Ze < 0 ? --D : (ue.c = ye, ue.e = D, ue.s = B, ye = (ue = J(ue, Le, Ue, Oe, I)).c, ne = ue.r, D = ue.e), Ze = ye[k = D + Ue + 1], V = I / 2, ne = ne || k < 0 || ye[k + 1] != null, ne = Oe < 4 ? (Ze != null || ne) && (Oe == 0 || Oe == (ue.s < 0 ? 3 : 2)) : V < Ze || Ze == V && (Oe == 4 || ne || Oe == 6 && 1 & ye[k - 1] || Oe == (ue.s < 0 ? 8 : 7)), k < 1 || !ye[0])
                E = ne ? G(x.charAt(1), -Ue, x.charAt(0)) : x.charAt(0);
              else {
                if (ye.length = k, ne)
                  for (--I; ++ye[--k] > I; )
                    ye[k] = 0, k || (++D, ye = [1].concat(ye));
                for (V = ye.length; !ye[--V]; )
                  ;
                for (Ze = 0, E = ""; Ze <= V; E += x.charAt(ye[Ze++]))
                  ;
                E = G(E, D, x.charAt(0));
              }
              return E;
            };
          }(), J = function() {
            function v(L, I, B) {
              var N, x, k, D, V = 0, ne = L.length, ue = I % C, ye = I / C | 0;
              for (L = L.slice(); ne--; )
                V = ((x = ue * (k = L[ne] % C) + (N = ye * k + (D = L[ne] / C | 0) * ue) % C * C + V) / B | 0) + (N / C | 0) + ye * D, L[ne] = x % B;
              return V && (L = [V].concat(L)), L;
            }
            function y(L, I, B, N) {
              var x, k;
              if (B != N)
                k = N < B ? 1 : -1;
              else
                for (x = k = 0; x < B; x++)
                  if (L[x] != I[x]) {
                    k = L[x] > I[x] ? 1 : -1;
                    break;
                  }
              return k;
            }
            function E(L, I, B, N) {
              for (var x = 0; B--; )
                L[B] -= x, x = L[B] < I[B] ? 1 : 0, L[B] = x * N + L[B] - I[B];
              for (; !L[0] && 1 < L.length; L.splice(0, 1))
                ;
            }
            return function(L, I, B, N, x) {
              var k, D, V, ne, ue, ye, Le, Ze, Ue, Oe, et, Ft, Cl, Ji, Uu, Mi, Dt, ln = L.s == I.s ? 1 : -1, Sn = L.c, Pt = I.c;
              if (!(Sn && Sn[0] && Pt && Pt[0]))
                return new W(L.s && I.s && (Sn ? !Pt || Sn[0] != Pt[0] : Pt) ? Sn && Sn[0] == 0 || !Pt ? 0 * ln : ln / 0 : NaN);
              for (Ue = (Ze = new W(ln)).c = [], ln = B + (D = L.e - I.e) + 1, x || (x = d, D = b(L.e / g) - b(I.e / g), ln = ln / g | 0), V = 0; Pt[V] == (Sn[V] || 0); V++)
                ;
              if (Pt[V] > (Sn[V] || 0) && D--, ln < 0)
                Ue.push(1), ne = !0;
              else {
                for (Ji = Sn.length, Mi = Pt.length, ln += 2, 1 < (ue = u(x / (Pt[V = 0] + 1))) && (Pt = v(Pt, ue, x), Sn = v(Sn, ue, x), Mi = Pt.length, Ji = Sn.length), Cl = Mi, et = (Oe = Sn.slice(0, Mi)).length; et < Mi; Oe[et++] = 0)
                  ;
                Dt = Pt.slice(), Dt = [0].concat(Dt), Uu = Pt[0], Pt[1] >= x / 2 && Uu++;
                do {
                  if (ue = 0, (k = y(Pt, Oe, Mi, et)) < 0) {
                    if (Ft = Oe[0], Mi != et && (Ft = Ft * x + (Oe[1] || 0)), 1 < (ue = u(Ft / Uu)))
                      for (x <= ue && (ue = x - 1), Le = (ye = v(Pt, ue, x)).length, et = Oe.length; y(ye, Oe, Le, et) == 1; )
                        ue--, E(ye, Mi < Le ? Dt : Pt, Le, x), Le = ye.length, k = 1;
                    else
                      ue == 0 && (k = ue = 1), Le = (ye = Pt.slice()).length;
                    if (Le < et && (ye = [0].concat(ye)), E(Oe, ye, et, x), et = Oe.length, k == -1)
                      for (; y(Pt, Oe, Mi, et) < 1; )
                        ue++, E(Oe, Mi < et ? Dt : Pt, et, x), et = Oe.length;
                  } else
                    k === 0 && (ue++, Oe = [0]);
                  Ue[V++] = ue, Oe[0] ? Oe[et++] = Sn[Cl] || 0 : (Oe = [Sn[Cl]], et = 1);
                } while ((Cl++ < Ji || Oe[0] != null) && ln--);
                ne = Oe[0] != null, Ue[0] || Ue.splice(0, 1);
              }
              if (x == d) {
                for (V = 1, ln = Ue[0]; 10 <= ln; ln /= 10, V++)
                  ;
                Ve(Ze, B + (Ze.e = V + D * g - 1) + 1, N, ne);
              } else
                Ze.e = D, Ze.r = +ne;
              return Ze;
            };
          }(), nt = /^(-?)0([xbo])(?=\w[\w.]*$)/i, Ne = /^([^.]+)\.$/, Ce = /^\.([^.]+)$/, pe = /^-?(Infinity|NaN)$/, wt = /^\s*\+(?=[\w.])|^\s+|\s+$/g, re = function(v, y, E, L) {
            var I, B = E ? y : y.replace(wt, "");
            if (pe.test(B))
              v.s = isNaN(B) ? null : B < 0 ? -1 : 1, v.c = v.e = null;
            else {
              if (!E && (B = B.replace(nt, function(N, x, k) {
                return I = (k = k.toLowerCase()) == "x" ? 16 : k == "b" ? 2 : 8, L && L != I ? N : x;
              }), L && (I = L, B = B.replace(Ne, "$1").replace(Ce, "0.$1")), y != B))
                return new W(B, I);
              if (W.DEBUG)
                throw Error(c + "Not a" + (L ? " base " + L : "") + " number: " + y);
              v.c = v.e = v.s = null;
            }
          }, f.absoluteValue = f.abs = function() {
            var v = new W(this);
            return v.s < 0 && (v.s = 1), v;
          }, f.comparedTo = function(v, y) {
            return _(this, new W(v, y));
          }, f.decimalPlaces = f.dp = function(v, y) {
            var E, L, I;
            if (v != null)
              return O(v, 0, S), y == null ? y = M : O(y, 0, 8), Ve(new W(this), v + this.e + 1, y);
            if (!(E = this.c))
              return null;
            if (L = ((I = E.length - 1) - b(this.e / g)) * g, I = E[I])
              for (; I % 10 == 0; I /= 10, L--)
                ;
            return L < 0 && (L = 0), L;
          }, f.dividedBy = f.div = function(v, y) {
            return J(this, new W(v, y), A, M);
          }, f.dividedToIntegerBy = f.idiv = function(v, y) {
            return J(this, new W(v, y), 0, 1);
          }, f.exponentiatedBy = f.pow = function(v, y) {
            var E, L, I, B, N, x, k, D, V = this;
            if ((v = new W(v)).c && !v.isInteger())
              throw Error(c + "Exponent not an integer: " + Re(v));
            if (y != null && (y = new W(y)), N = 14 < v.e, !V.c || !V.c[0] || V.c[0] == 1 && !V.e && V.c.length == 1 || !v.c || !v.c[0])
              return D = new W(Math.pow(+Re(V), N ? 2 - $(v) : +Re(v))), y ? D.mod(y) : D;
            if (x = v.s < 0, y) {
              if (y.c ? !y.c[0] : !y.s)
                return new W(NaN);
              (L = !x && V.isInteger() && y.isInteger()) && (V = V.mod(y));
            } else {
              if (9 < v.e && (0 < V.e || V.e < -1 || (V.e == 0 ? 1 < V.c[0] || N && 24e7 <= V.c[1] : V.c[0] < 8e13 || N && V.c[0] <= 9999975e7)))
                return B = V.s < 0 && $(v) ? -0 : 0, -1 < V.e && (B = 1 / B), new W(x ? 1 / B : B);
              Z && (B = l(Z / g + 2));
            }
            for (k = N ? (E = new W(0.5), x && (v.s = 1), $(v)) : (I = Math.abs(+Re(v))) % 2, D = new W(p); ; ) {
              if (k) {
                if (!(D = D.times(V)).c)
                  break;
                B ? D.c.length > B && (D.c.length = B) : L && (D = D.mod(y));
              }
              if (I) {
                if ((I = u(I / 2)) === 0)
                  break;
                k = I % 2;
              } else if (Ve(v = v.times(E), v.e + 1, 1), 14 < v.e)
                k = $(v);
              else {
                if ((I = +Re(v)) == 0)
                  break;
                k = I % 2;
              }
              V = V.times(V), B ? V.c && V.c.length > B && (V.c.length = B) : L && (V = V.mod(y));
            }
            return L ? D : (x && (D = p.div(D)), y ? D.mod(y) : B ? Ve(D, Z, M, void 0) : D);
          }, f.integerValue = function(v) {
            var y = new W(this);
            return v == null ? v = M : O(v, 0, 8), Ve(y, y.e + 1, v);
          }, f.isEqualTo = f.eq = function(v, y) {
            return _(this, new W(v, y)) === 0;
          }, f.isFinite = function() {
            return !!this.c;
          }, f.isGreaterThan = f.gt = function(v, y) {
            return 0 < _(this, new W(v, y));
          }, f.isGreaterThanOrEqualTo = f.gte = function(v, y) {
            return (y = _(this, new W(v, y))) === 1 || y === 0;
          }, f.isInteger = function() {
            return !!this.c && b(this.e / g) > this.c.length - 2;
          }, f.isLessThan = f.lt = function(v, y) {
            return _(this, new W(v, y)) < 0;
          }, f.isLessThanOrEqualTo = f.lte = function(v, y) {
            return (y = _(this, new W(v, y))) === -1 || y === 0;
          }, f.isNaN = function() {
            return !this.s;
          }, f.isNegative = function() {
            return this.s < 0;
          }, f.isPositive = function() {
            return 0 < this.s;
          }, f.isZero = function() {
            return !!this.c && this.c[0] == 0;
          }, f.minus = function(v, y) {
            var E, L, I, B, N = this, x = N.s;
            if (y = (v = new W(v, y)).s, !x || !y)
              return new W(NaN);
            if (x != y)
              return v.s = -y, N.plus(v);
            var k = N.e / g, D = v.e / g, V = N.c, ne = v.c;
            if (!k || !D) {
              if (!V || !ne)
                return V ? (v.s = -y, v) : new W(ne ? N : NaN);
              if (!V[0] || !ne[0])
                return ne[0] ? (v.s = -y, v) : new W(V[0] ? N : M == 3 ? -0 : 0);
            }
            if (k = b(k), D = b(D), V = V.slice(), x = k - D) {
              for ((I = (B = x < 0) ? (x = -x, V) : (D = k, ne)).reverse(), y = x; y--; I.push(0))
                ;
              I.reverse();
            } else
              for (L = (B = (x = V.length) < (y = ne.length)) ? x : y, x = y = 0; y < L; y++)
                if (V[y] != ne[y]) {
                  B = V[y] < ne[y];
                  break;
                }
            if (B && (I = V, V = ne, ne = I, v.s = -v.s), 0 < (y = (L = ne.length) - (E = V.length)))
              for (; y--; V[E++] = 0)
                ;
            for (y = d - 1; x < L; ) {
              if (V[--L] < ne[L]) {
                for (E = L; E && !V[--E]; V[E] = y)
                  ;
                --V[E], V[L] += d;
              }
              V[L] -= ne[L];
            }
            for (; V[0] == 0; V.splice(0, 1), --D)
              ;
            return V[0] ? it(v, V, D) : (v.s = M == 3 ? -1 : 1, v.c = [v.e = 0], v);
          }, f.modulo = f.mod = function(v, y) {
            var E, L, I = this;
            return v = new W(v, y), !I.c || !v.s || v.c && !v.c[0] ? new W(NaN) : !v.c || I.c && !I.c[0] ? new W(I) : (ce == 9 ? (L = v.s, v.s = 1, E = J(I, v, 0, 3), v.s = L, E.s *= L) : E = J(I, v, 0, ce), (v = I.minus(E.times(v))).c[0] || ce != 1 || (v.s = I.s), v);
          }, f.multipliedBy = f.times = function(v, y) {
            var E, L, I, B, N, x, k, D, V, ne, ue, ye, Le, Ze, Ue, Oe = this, et = Oe.c, Ft = (v = new W(v, y)).c;
            if (!(et && Ft && et[0] && Ft[0]))
              return !Oe.s || !v.s || et && !et[0] && !Ft || Ft && !Ft[0] && !et ? v.c = v.e = v.s = null : (v.s *= Oe.s, et && Ft ? (v.c = [0], v.e = 0) : v.c = v.e = null), v;
            for (L = b(Oe.e / g) + b(v.e / g), v.s *= Oe.s, (k = et.length) < (ne = Ft.length) && (Le = et, et = Ft, Ft = Le, I = k, k = ne, ne = I), I = k + ne, Le = []; I--; Le.push(0))
              ;
            for (Ze = d, Ue = C, I = ne; 0 <= --I; ) {
              for (E = 0, ue = Ft[I] % Ue, ye = Ft[I] / Ue | 0, B = I + (N = k); I < B; )
                E = ((D = ue * (D = et[--N] % Ue) + (x = ye * D + (V = et[N] / Ue | 0) * ue) % Ue * Ue + Le[B] + E) / Ze | 0) + (x / Ue | 0) + ye * V, Le[B--] = D % Ze;
              Le[B] = E;
            }
            return E ? ++L : Le.splice(0, 1), it(v, Le, L);
          }, f.negated = function() {
            var v = new W(this);
            return v.s = -v.s || null, v;
          }, f.plus = function(v, y) {
            var E, L = this, I = L.s;
            if (y = (v = new W(v, y)).s, !I || !y)
              return new W(NaN);
            if (I != y)
              return v.s = -y, L.minus(v);
            var B = L.e / g, N = v.e / g, x = L.c, k = v.c;
            if (!B || !N) {
              if (!x || !k)
                return new W(I / 0);
              if (!x[0] || !k[0])
                return k[0] ? v : new W(x[0] ? L : 0 * I);
            }
            if (B = b(B), N = b(N), x = x.slice(), I = B - N) {
              for ((E = 0 < I ? (N = B, k) : (I = -I, x)).reverse(); I--; E.push(0))
                ;
              E.reverse();
            }
            for ((I = x.length) - (y = k.length) < 0 && (E = k, k = x, x = E, y = I), I = 0; y; )
              I = (x[--y] = x[y] + k[y] + I) / d | 0, x[y] = d === x[y] ? 0 : x[y] % d;
            return I && (x = [I].concat(x), ++N), it(v, x, N);
          }, f.precision = f.sd = function(v, y) {
            var E, L, I;
            if (v != null && v !== !!v)
              return O(v, 1, S), y == null ? y = M : O(y, 0, 8), Ve(new W(this), v, y);
            if (!(E = this.c))
              return null;
            if (L = (I = E.length - 1) * g + 1, I = E[I]) {
              for (; I % 10 == 0; I /= 10, L--)
                ;
              for (I = E[0]; 10 <= I; I /= 10, L++)
                ;
            }
            return v && this.e + 1 > L && (L = this.e + 1), L;
          }, f.shiftedBy = function(v) {
            return O(v, -m, m), this.times("1e" + v);
          }, f.squareRoot = f.sqrt = function() {
            var v, y, E, L, I, B = this, N = B.c, x = B.s, k = B.e, D = A + 4, V = new W("0.5");
            if (x !== 1 || !N || !N[0])
              return new W(!x || x < 0 && (!N || N[0]) ? NaN : N ? B : 1 / 0);
            if ((E = (x = Math.sqrt(+Re(B))) == 0 || x == 1 / 0 ? (((y = R(N)).length + k) % 2 == 0 && (y += "0"), x = Math.sqrt(+y), k = b((k + 1) / 2) - (k < 0 || k % 2), new W(y = x == 1 / 0 ? "1e" + k : (y = x.toExponential()).slice(0, y.indexOf("e") + 1) + k)) : new W(x + "")).c[0]) {
              for ((x = (k = E.e) + D) < 3 && (x = 0); ; )
                if (I = E, E = V.times(I.plus(J(B, I, D, 1))), R(I.c).slice(0, x) === (y = R(E.c)).slice(0, x)) {
                  if (E.e < k && --x, (y = y.slice(x - 3, x + 1)) != "9999" && (L || y != "4999")) {
                    +y && (+y.slice(1) || y.charAt(0) != "5") || (Ve(E, E.e + A + 2, 1), v = !E.times(E).eq(B));
                    break;
                  }
                  if (!L && (Ve(I, I.e + A + 2, 0), I.times(I).eq(B))) {
                    E = I;
                    break;
                  }
                  D += 4, x += 4, L = 1;
                }
            }
            return Ve(E, E.e + A + 1, M, v);
          }, f.toExponential = function(v, y) {
            return v != null && (O(v, 0, S), v++), Qe(this, v, y, 1);
          }, f.toFixed = function(v, y) {
            return v != null && (O(v, 0, S), v = v + this.e + 1), Qe(this, v, y);
          }, f.toFormat = function(v, y, E) {
            var L;
            if (E == null)
              v != null && y && typeof y == "object" ? (E = y, y = null) : v && typeof v == "object" ? (E = v, v = y = null) : E = He;
            else if (typeof E != "object")
              throw Error(c + "Argument not an object: " + E);
            if (L = this.toFixed(v, y), this.c) {
              var I, B = L.split("."), N = +E.groupSize, x = +E.secondaryGroupSize, k = E.groupSeparator || "", D = B[0], V = B[1], ne = this.s < 0, ue = ne ? D.slice(1) : D, ye = ue.length;
              if (x && (I = N, N = x, ye -= x = I), 0 < N && 0 < ye) {
                for (I = ye % N || N, D = ue.substr(0, I); I < ye; I += N)
                  D += k + ue.substr(I, N);
                0 < x && (D += k + ue.slice(I)), ne && (D = "-" + D);
              }
              L = V ? D + (E.decimalSeparator || "") + ((x = +E.fractionGroupSize) ? V.replace(new RegExp("\\d{" + x + "}\\B", "g"), "$&" + (E.fractionGroupSeparator || "")) : V) : D;
            }
            return (E.prefix || "") + L + (E.suffix || "");
          }, f.toFraction = function(v) {
            var y, E, L, I, B, N, x, k, D, V, ne, ue, ye = this, Le = ye.c;
            if (v != null && (!(x = new W(v)).isInteger() && (x.c || x.s !== 1) || x.lt(p)))
              throw Error(c + "Argument " + (x.isInteger() ? "out of range: " : "not an integer: ") + Re(x));
            if (!Le)
              return new W(ye);
            for (y = new W(p), D = E = new W(p), L = k = new W(p), ue = R(Le), B = y.e = ue.length - ye.e - 1, y.c[0] = w[(N = B % g) < 0 ? g + N : N], v = !v || 0 < x.comparedTo(y) ? 0 < B ? y : D : x, N = K, K = 1 / 0, x = new W(ue), k.c[0] = 0; V = J(x, y, 0, 1), (I = E.plus(V.times(L))).comparedTo(v) != 1; )
              E = L, L = I, D = k.plus(V.times(I = D)), k = I, y = x.minus(V.times(I = y)), x = I;
            return I = J(v.minus(E), L, 0, 1), k = k.plus(I.times(D)), E = E.plus(I.times(L)), k.s = D.s = ye.s, ne = J(D, L, B *= 2, M).minus(ye).abs().comparedTo(J(k, E, B, M).minus(ye).abs()) < 1 ? [D, L] : [k, E], K = N, ne;
          }, f.toNumber = function() {
            return +Re(this);
          }, f.toPrecision = function(v, y) {
            return v != null && O(v, 1, S), Qe(this, v, y, 2);
          }, f.toString = function(v) {
            var y, E = this, L = E.s, I = E.e;
            return I === null ? L ? (y = "Infinity", L < 0 && (y = "-" + y)) : y = "NaN" : (y = v == null ? I <= P || F <= I ? U(R(E.c), I) : G(R(E.c), I, "0") : v === 10 ? G(R((E = Ve(new W(E), A + I + 1, M)).c), E.e, "0") : (O(v, 2, Pe.length, "Base"), q(G(R(E.c), I, "0"), 10, v, L, !0)), L < 0 && E.c[0] && (y = "-" + y)), y;
          }, f.valueOf = f.toJSON = function() {
            return Re(this);
          }, f._isBigNumber = !0, typeof Symbol == "function" && typeof Symbol.iterator == "symbol" && (f[Symbol.toStringTag] = "BigNumber", f[Symbol.for("nodejs.util.inspect.custom")] = f.valueOf), j != null && W.set(j), W;
        }()).default = s.BigNumber = s, i !== void 0 && i.exports ? i.exports = s : (o || (o = typeof self != "undefined" && self ? self : window), o.BigNumber = s);
      })(this);
    }, {}], 2: [function(n, i, r) {
      i.exports = { languageTag: "en-US", delimiters: { thousands: ",", decimal: "." }, abbreviations: { thousand: "k", million: "m", billion: "b", trillion: "t" }, spaceSeparated: !1, ordinal: function(o) {
        var s = o % 10;
        return ~~(o % 100 / 10) == 1 ? "th" : s === 1 ? "st" : s === 2 ? "nd" : s === 3 ? "rd" : "th";
      }, currency: { symbol: "$", position: "prefix", code: "USD" }, currencyFormat: { thousandSeparated: !0, totalLength: 4, spaceSeparated: !0 }, formats: { fourDigits: { totalLength: 4, spaceSeparated: !0 }, fullWithTwoDecimals: { output: "currency", thousandSeparated: !0, mantissa: 2 }, fullWithTwoDecimalsNoCurrency: { thousandSeparated: !0, mantissa: 2 }, fullWithNoDecimals: { output: "currency", thousandSeparated: !0, mantissa: 0 } } };
    }, {}], 3: [function(n, i, r) {
      function o(b, R) {
        return function(_) {
          if (Array.isArray(_))
            return _;
        }(b) || function(_, O) {
          var $ = [], U = !0, G = !1, z = void 0;
          try {
            for (var j, J = _[Symbol.iterator](); !(U = (j = J.next()).done) && ($.push(j.value), !O || $.length !== O); U = !0)
              ;
          } catch (q) {
            G = !0, z = q;
          } finally {
            try {
              U || J.return == null || J.return();
            } finally {
              if (G)
                throw z;
            }
          }
          return $;
        }(b, R) || function() {
          throw new TypeError("Invalid attempt to destructure non-iterable instance");
        }();
      }
      var s = n("./globalState"), a = n("./validating"), l = n("./parsing"), u = ["B", "KB", "MB", "GB", "TB", "PB", "EB", "ZB", "YB"], c = { general: { scale: 1024, suffixes: u, marker: "bd" }, binary: { scale: 1024, suffixes: ["B", "KiB", "MiB", "GiB", "TiB", "PiB", "EiB", "ZiB", "YiB"], marker: "b" }, decimal: { scale: 1e3, suffixes: u, marker: "d" } }, h = { totalLength: 0, characteristic: 0, forceAverage: !1, average: !1, mantissa: -1, optionalMantissa: !0, thousandSeparated: !1, spaceSeparated: !1, negative: "sign", forceSign: !1 };
      function d(b) {
        var R = 1 < arguments.length && arguments[1] !== void 0 ? arguments[1] : {}, _ = 2 < arguments.length ? arguments[2] : void 0;
        if (typeof R == "string" && (R = l.parseFormat(R)), !a.validateFormat(R))
          return "ERROR: invalid format";
        var O = R.prefix || "", $ = R.postfix || "", U = function(G, z, j) {
          switch (z.output) {
            case "currency":
              return z = S(z, s.currentCurrencyDefaultFormat()), function(Pe, W, Qe) {
                var Ge = Qe.currentCurrency(), it = Object.assign({}, h, W), Ve = void 0, Re = "", v = !!it.totalLength || !!it.forceAverage || it.average, y = W.currencyPosition || Ge.position, E = W.currencySymbol || Ge.symbol;
                it.spaceSeparated && (Re = " "), y === "infix" && (Ve = Re + E + Re);
                var L = C({ instance: Pe, providedFormat: W, state: Qe, decimalSeparator: Ve });
                return y === "prefix" && (L = Pe._value < 0 && it.negative === "sign" ? "-".concat(Re).concat(E).concat(L.slice(1)) : E + Re + L), y && y !== "postfix" || (L = L + (Re = v ? "" : Re) + E), L;
              }(G, z, s);
            case "percent":
              return z = S(z, s.currentPercentageDefaultFormat()), function(Pe, W, Qe, Ge) {
                var it = W.prefixSymbol, Ve = C({ instance: Ge(100 * Pe._value), providedFormat: W, state: Qe }), Re = Object.assign({}, h, W);
                return it ? "%".concat(Re.spaceSeparated ? " " : "").concat(Ve) : "".concat(Ve).concat(Re.spaceSeparated ? " " : "", "%");
              }(G, z, s, j);
            case "byte":
              return z = S(z, s.currentByteDefaultFormat()), p = G, M = s, P = j, F = (A = z).base || "binary", Y = c[F], K = g(p._value, Y.suffixes, Y.scale), le = K.value, ce = K.suffix, Z = C({ instance: P(le), providedFormat: A, state: M, defaults: M.currentByteDefaultFormat() }), He = M.currentAbbreviations(), "".concat(Z).concat(He.spaced ? " " : "").concat(ce);
            case "time":
              return z = S(z, s.currentTimeDefaultFormat()), Ce = G, pe = Math.floor(Ce._value / 60 / 60), wt = Math.floor((Ce._value - 60 * pe * 60) / 60), f = Math.round(Ce._value - 60 * pe * 60 - 60 * wt), "".concat(pe, ":").concat(wt < 10 ? "0" : "").concat(wt, ":").concat(f < 10 ? "0" : "").concat(f);
            case "ordinal":
              return z = S(z, s.currentOrdinalDefaultFormat()), J = G, q = z, me = (re = s).currentOrdinal(), Me = Object.assign({}, h, q), nt = C({ instance: J, providedFormat: q, state: re }), Ne = me(J._value), "".concat(nt).concat(Me.spaceSeparated ? " " : "").concat(Ne);
            case "number":
            default:
              return C({ instance: G, providedFormat: z, numbro: j });
          }
          var J, q, re, me, Me, nt, Ne, Ce, pe, wt, f, p, A, M, P, F, Y, K, le, ce, Z, He;
        }(b, R, _);
        return U = (U = O + U) + $;
      }
      function g(b, R, _) {
        var O = R[0], $ = Math.abs(b);
        if (_ <= $) {
          for (var U = 1; U < R.length; ++U) {
            var G = Math.pow(_, U), z = Math.pow(_, U + 1);
            if (G <= $ && $ < z) {
              O = R[U], b /= G;
              break;
            }
          }
          O === R[0] && (b /= Math.pow(_, R.length - 1), O = R[R.length - 1]);
        }
        return { value: b, suffix: O };
      }
      function m(b) {
        for (var R = "", _ = 0; _ < b; _++)
          R += "0";
        return R;
      }
      function w(b, R) {
        return b.toString().indexOf("e") !== -1 ? function(_, O) {
          var $ = _.toString(), U = o($.split("e"), 2), G = U[0], z = U[1], j = o(G.split("."), 2), J = j[0], q = j[1], re = q === void 0 ? "" : q;
          if (0 < +z)
            $ = J + re + m(z - re.length);
          else {
            var me = ".";
            me = +J < 0 ? "-0".concat(me) : "0".concat(me);
            var Me = (m(-z - 1) + Math.abs(J) + re).substr(0, O);
            Me.length < O && (Me += m(O - Me.length)), $ = me + Me;
          }
          return 0 < +z && 0 < O && ($ += ".".concat(m(O))), $;
        }(b, R) : (Math.round(+"".concat(b, "e+").concat(R)) / Math.pow(10, R)).toFixed(R);
      }
      function C(b) {
        var R = b.instance, _ = b.providedFormat, O = b.state, $ = O === void 0 ? s : O, U = b.decimalSeparator, G = b.defaults, z = G === void 0 ? $.currentDefaults() : G, j = R._value;
        if (j === 0 && $.hasZeroFormat())
          return $.getZeroFormat();
        if (!isFinite(j))
          return j.toString();
        var J, q, re, me, Me, nt, Ne, Ce, pe = Object.assign({}, h, z, _), wt = pe.totalLength, f = wt ? 0 : pe.characteristic, p = pe.optionalCharacteristic, A = pe.forceAverage, M = !!wt || !!A || pe.average, P = wt ? -1 : M && _.mantissa === void 0 ? 0 : pe.mantissa, F = !wt && (_.optionalMantissa === void 0 ? P === -1 : pe.optionalMantissa), Y = pe.trimMantissa, K = pe.thousandSeparated, le = pe.spaceSeparated, ce = pe.negative, Z = pe.forceSign, He = pe.exponential, Pe = "";
        if (M) {
          var W = function(v) {
            var y = v.value, E = v.forceAverage, L = v.abbreviations, I = v.spaceSeparated, B = I !== void 0 && I, N = v.totalLength, x = N === void 0 ? 0 : N, k = "", D = Math.abs(y), V = -1;
            if (D >= Math.pow(10, 12) && !E || E === "trillion" ? (k = L.trillion, y /= Math.pow(10, 12)) : D < Math.pow(10, 12) && D >= Math.pow(10, 9) && !E || E === "billion" ? (k = L.billion, y /= Math.pow(10, 9)) : D < Math.pow(10, 9) && D >= Math.pow(10, 6) && !E || E === "million" ? (k = L.million, y /= Math.pow(10, 6)) : (D < Math.pow(10, 6) && D >= Math.pow(10, 3) && !E || E === "thousand") && (k = L.thousand, y /= Math.pow(10, 3)), k && (k = (B ? " " : "") + k), x) {
              var ne = y.toString().split(".")[0];
              V = Math.max(x - ne.length, 0);
            }
            return { value: y, abbreviation: k, mantissaPrecision: V };
          }({ value: j, forceAverage: A, abbreviations: $.currentAbbreviations(), spaceSeparated: le, totalLength: wt });
          j = W.value, Pe += W.abbreviation, wt && (P = W.mantissaPrecision);
        }
        if (He) {
          var Qe = (q = (J = { value: j, characteristicPrecision: f }).value, re = J.characteristicPrecision, me = re === void 0 ? 0 : re, Me = o(q.toExponential().split("e"), 2), nt = Me[0], Ne = Me[1], Ce = +nt, me && 1 < me && (Ce *= Math.pow(10, me - 1), Ne = 0 <= (Ne = +Ne - (me - 1)) ? "+".concat(Ne) : Ne), { value: Ce, abbreviation: "e".concat(Ne) });
          j = Qe.value, Pe = Qe.abbreviation + Pe;
        }
        var Ge, it, Ve, Re = function(v, y, E, L, I) {
          if (L === -1)
            return v;
          var B = w(y, L), N = o(B.toString().split("."), 2), x = N[0], k = N[1], D = k === void 0 ? "" : k;
          if (D.match(/^0+$/) && (E || I))
            return x;
          var V = D.match(/0+$/);
          return I && V ? "".concat(x, ".").concat(D.toString().slice(0, V.index)) : B.toString();
        }(j.toString(), j, F, P, Y);
        return Re = function(v, y, E, L, I) {
          var B = L.currentDelimiters(), N = B.thousands;
          I = I || B.decimal;
          var x = B.thousandsSize || 3, k = v.toString(), D = k.split(".")[0], V = k.split(".")[1];
          return E && (y < 0 && (D = D.slice(1)), function(ne, ue) {
            for (var ye = [], Le = 0, Ze = ne; 0 < Ze; Ze--)
              Le === ue && (ye.unshift(Ze), Le = 0), Le++;
            return ye;
          }(D.length, x).forEach(function(ne, ue) {
            D = D.slice(0, ne + ue) + N + D.slice(ne + ue);
          }), y < 0 && (D = "-".concat(D))), k = V ? D + I + V : D;
        }(Re = function(v, y, E, L) {
          var I = v, B = o(I.toString().split("."), 2), N = B[0], x = B[1];
          if (N.match(/^-?0$/) && E)
            return x ? "".concat(N.replace("0", ""), ".").concat(x) : N.replace("0", "");
          if (N.length < L)
            for (var k = L - N.length, D = 0; D < k; D++)
              I = "0".concat(I);
          return I.toString();
        }(Re, 0, p, f), j, K, $, U), (M || He) && (Re = Re + Pe), (Z || j < 0) && (Ge = Re, Ve = ce, Re = (it = j) === 0 ? Ge : +Ge == 0 ? Ge.replace("-", "") : 0 < it ? "+".concat(Ge) : Ve === "sign" ? Ge : "(".concat(Ge.replace("-", ""), ")")), Re;
      }
      function S(b, R) {
        if (!b)
          return R;
        var _ = Object.keys(b);
        return _.length === 1 && _[0] === "output" ? R : b;
      }
      i.exports = function(b) {
        return { format: function() {
          for (var R = arguments.length, _ = new Array(R), O = 0; O < R; O++)
            _[O] = arguments[O];
          return d.apply(void 0, _.concat([b]));
        }, getByteUnit: function() {
          for (var R = arguments.length, _ = new Array(R), O = 0; O < R; O++)
            _[O] = arguments[O];
          return function($) {
            var U = c.general;
            return g($._value, U.suffixes, U.scale).suffix;
          }.apply(void 0, _.concat([b]));
        }, getBinaryByteUnit: function() {
          for (var R = arguments.length, _ = new Array(R), O = 0; O < R; O++)
            _[O] = arguments[O];
          return function($) {
            var U = c.binary;
            return g($._value, U.suffixes, U.scale).suffix;
          }.apply(void 0, _.concat([b]));
        }, getDecimalByteUnit: function() {
          for (var R = arguments.length, _ = new Array(R), O = 0; O < R; O++)
            _[O] = arguments[O];
          return function($) {
            var U = c.decimal;
            return g($._value, U.suffixes, U.scale).suffix;
          }.apply(void 0, _.concat([b]));
        }, formatOrDefault: S };
      };
    }, { "./globalState": 4, "./parsing": 8, "./validating": 10 }], 4: [function(n, i, r) {
      var o = n("./en-US"), s = n("./validating"), a = n("./parsing"), l = {}, u = void 0, c = {}, h = null, d = {};
      function g(w) {
        u = w;
      }
      function m() {
        return c[u];
      }
      l.languages = function() {
        return Object.assign({}, c);
      }, l.currentLanguage = function() {
        return u;
      }, l.currentCurrency = function() {
        return m().currency;
      }, l.currentAbbreviations = function() {
        return m().abbreviations;
      }, l.currentDelimiters = function() {
        return m().delimiters;
      }, l.currentOrdinal = function() {
        return m().ordinal;
      }, l.currentDefaults = function() {
        return Object.assign({}, m().defaults, d);
      }, l.currentOrdinalDefaultFormat = function() {
        return Object.assign({}, l.currentDefaults(), m().ordinalFormat);
      }, l.currentByteDefaultFormat = function() {
        return Object.assign({}, l.currentDefaults(), m().byteFormat);
      }, l.currentPercentageDefaultFormat = function() {
        return Object.assign({}, l.currentDefaults(), m().percentageFormat);
      }, l.currentCurrencyDefaultFormat = function() {
        return Object.assign({}, l.currentDefaults(), m().currencyFormat);
      }, l.currentTimeDefaultFormat = function() {
        return Object.assign({}, l.currentDefaults(), m().timeFormat);
      }, l.setDefaults = function(w) {
        w = a.parseFormat(w), s.validateFormat(w) && (d = w);
      }, l.getZeroFormat = function() {
        return h;
      }, l.setZeroFormat = function(w) {
        return h = typeof w == "string" ? w : null;
      }, l.hasZeroFormat = function() {
        return h !== null;
      }, l.languageData = function(w) {
        if (w) {
          if (c[w])
            return c[w];
          throw new Error('Unknown tag "'.concat(w, '"'));
        }
        return m();
      }, l.registerLanguage = function(w) {
        var C = 1 < arguments.length && arguments[1] !== void 0 && arguments[1];
        if (!s.validateLanguage(w))
          throw new Error("Invalid language data");
        c[w.languageTag] = w, C && g(w.languageTag);
      }, l.setLanguage = function(w) {
        var C = 1 < arguments.length && arguments[1] !== void 0 ? arguments[1] : o.languageTag;
        if (!c[w]) {
          var S = w.split("-")[0], b = Object.keys(c).find(function(R) {
            return R.split("-")[0] === S;
          });
          return c[b] ? void g(b) : void g(C);
        }
        g(w);
      }, l.registerLanguage(o), u = o.languageTag, i.exports = l;
    }, { "./en-US": 2, "./parsing": 8, "./validating": 10 }], 5: [function(n, i, r) {
      i.exports = function(o) {
        return { loadLanguagesInNode: function(s) {
          return a = o, void s.forEach(function(l) {
            var u = void 0;
            try {
              u = n("../languages/".concat(l));
            } catch (c) {
              console.error('Unable to load "'.concat(l, '". No matching language file found.'));
            }
            u && a.registerLanguage(u);
          });
          var a;
        } };
      };
    }, {}], 6: [function(n, i, r) {
      var o = n("bignumber.js");
      function s(a, l, u) {
        var c = new o(a._value), h = l;
        return u.isNumbro(l) && (h = l._value), h = new o(h), a._value = c.minus(h).toNumber(), a;
      }
      i.exports = function(a) {
        return { add: function(l, u) {
          return h = u, d = a, g = new o((c = l)._value), m = h, d.isNumbro(h) && (m = h._value), m = new o(m), c._value = g.plus(m).toNumber(), c;
          var c, h, d, g, m;
        }, subtract: function(l, u) {
          return s(l, u, a);
        }, multiply: function(l, u) {
          return h = u, d = a, g = new o((c = l)._value), m = h, d.isNumbro(h) && (m = h._value), m = new o(m), c._value = g.times(m).toNumber(), c;
          var c, h, d, g, m;
        }, divide: function(l, u) {
          return h = u, d = a, g = new o((c = l)._value), m = h, d.isNumbro(h) && (m = h._value), m = new o(m), c._value = g.dividedBy(m).toNumber(), c;
          var c, h, d, g, m;
        }, set: function(l, u) {
          return c = l, d = h = u, a.isNumbro(h) && (d = h._value), c._value = d, c;
          var c, h, d;
        }, difference: function(l, u) {
          return c = u, s(d = (h = a)(l._value), c, h), Math.abs(d._value);
          var c, h, d;
        } };
      };
    }, { "bignumber.js": 1 }], 7: [function(n, i, r) {
      function o(C, S) {
        for (var b = 0; b < S.length; b++) {
          var R = S[b];
          R.enumerable = R.enumerable || !1, R.configurable = !0, "value" in R && (R.writable = !0), Object.defineProperty(C, R.key, R);
        }
      }
      var s = n("./globalState"), a = n("./validating"), l = n("./loading")(w), u = n("./unformatting"), c = n("./formatting")(w), h = n("./manipulating")(w), d = n("./parsing"), g = function() {
        function C(R) {
          (function(_, O) {
            if (!(_ instanceof O))
              throw new TypeError("Cannot call a class as a function");
          })(this, C), this._value = R;
        }
        var S, b;
        return S = C, (b = [{ key: "clone", value: function() {
          return w(this._value);
        } }, { key: "format", value: function() {
          var R = 0 < arguments.length && arguments[0] !== void 0 ? arguments[0] : {};
          return c.format(this, R);
        } }, { key: "formatCurrency", value: function(R) {
          return typeof R == "string" && (R = d.parseFormat(R)), (R = c.formatOrDefault(R, s.currentCurrencyDefaultFormat())).output = "currency", c.format(this, R);
        } }, { key: "formatTime", value: function() {
          var R = 0 < arguments.length && arguments[0] !== void 0 ? arguments[0] : {};
          return R.output = "time", c.format(this, R);
        } }, { key: "binaryByteUnits", value: function() {
          return c.getBinaryByteUnit(this);
        } }, { key: "decimalByteUnits", value: function() {
          return c.getDecimalByteUnit(this);
        } }, { key: "byteUnits", value: function() {
          return c.getByteUnit(this);
        } }, { key: "difference", value: function(R) {
          return h.difference(this, R);
        } }, { key: "add", value: function(R) {
          return h.add(this, R);
        } }, { key: "subtract", value: function(R) {
          return h.subtract(this, R);
        } }, { key: "multiply", value: function(R) {
          return h.multiply(this, R);
        } }, { key: "divide", value: function(R) {
          return h.divide(this, R);
        } }, { key: "set", value: function(R) {
          return h.set(this, m(R));
        } }, { key: "value", value: function() {
          return this._value;
        } }, { key: "valueOf", value: function() {
          return this._value;
        } }]) && o(S.prototype, b), C;
      }();
      function m(C) {
        var S = C;
        return w.isNumbro(C) ? S = C._value : typeof C == "string" ? S = w.unformat(C) : isNaN(C) && (S = NaN), S;
      }
      function w(C) {
        return new g(m(C));
      }
      w.version = "2.1.2", w.isNumbro = function(C) {
        return C instanceof g;
      }, w.language = s.currentLanguage, w.registerLanguage = s.registerLanguage, w.setLanguage = s.setLanguage, w.languages = s.languages, w.languageData = s.languageData, w.zeroFormat = s.setZeroFormat, w.defaultFormat = s.currentDefaults, w.setDefaults = s.setDefaults, w.defaultCurrencyFormat = s.currentCurrencyDefaultFormat, w.validate = a.validate, w.loadLanguagesInNode = l.loadLanguagesInNode, w.unformat = u.unformat, i.exports = w;
    }, { "./formatting": 3, "./globalState": 4, "./loading": 5, "./manipulating": 6, "./parsing": 8, "./unformatting": 9, "./validating": 10 }], 8: [function(n, i, r) {
      i.exports = { parseFormat: function(o) {
        var s, a, l, u, c, h, d, g, m, w, C, S, b, R, _, O, $, U, G, z, j = 1 < arguments.length && arguments[1] !== void 0 ? arguments[1] : {};
        return typeof o != "string" ? o : (a = j, o = (l = (s = o).match(/^{([^}]*)}/)) ? (a.prefix = l[1], s.slice(l[0].length)) : s, c = j, function(J, q) {
          if (J.indexOf("$") === -1) {
            if (J.indexOf("%") === -1)
              return J.indexOf("bd") !== -1 ? (q.output = "byte", q.base = "general") : J.indexOf("b") !== -1 ? (q.output = "byte", q.base = "binary") : J.indexOf("d") !== -1 ? (q.output = "byte", q.base = "decimal") : J.indexOf(":") === -1 ? J.indexOf("o") !== -1 && (q.output = "ordinal") : q.output = "time";
            q.output = "percent";
          } else
            q.output = "currency";
        }(o = (h = (u = o).match(/{([^}]*)}$/)) ? (c.postfix = h[1], u.slice(0, -h[0].length)) : u, j), d = j, (g = o.match(/[1-9]+[0-9]*/)) && (d.totalLength = +g[0]), m = j, (w = o.split(".")[0].match(/0+/)) && (m.characteristic = w[0].length), function(J, q) {
          if (J.indexOf(".") !== -1) {
            var re = J.split(".")[0];
            q.optionalCharacteristic = re.indexOf("0") === -1;
          }
        }(o, j), C = j, o.indexOf("a") !== -1 && (C.average = !0), b = j, (S = o).indexOf("K") !== -1 ? b.forceAverage = "thousand" : S.indexOf("M") !== -1 ? b.forceAverage = "million" : S.indexOf("B") !== -1 ? b.forceAverage = "billion" : S.indexOf("T") !== -1 && (b.forceAverage = "trillion"), function(J, q) {
          var re = J.split(".")[1];
          if (re) {
            var me = re.match(/0+/);
            me && (q.mantissa = me[0].length);
          }
        }(o, j), _ = j, (R = o).match(/\[\.]/) ? _.optionalMantissa = !0 : R.match(/\./) && (_.optionalMantissa = !1), O = j, o.indexOf(",") !== -1 && (O.thousandSeparated = !0), $ = j, o.indexOf(" ") !== -1 && ($.spaceSeparated = !0), G = j, (U = o).match(/^\+?\([^)]*\)$/) && (G.negative = "parenthesis"), U.match(/^\+?-/) && (G.negative = "sign"), z = j, o.match(/^\+/) && (z.forceSign = !0), j);
      } };
    }, {}], 9: [function(n, i, r) {
      var o = [{ key: "ZiB", factor: Math.pow(1024, 7) }, { key: "ZB", factor: Math.pow(1e3, 7) }, { key: "YiB", factor: Math.pow(1024, 8) }, { key: "YB", factor: Math.pow(1e3, 8) }, { key: "TiB", factor: Math.pow(1024, 4) }, { key: "TB", factor: Math.pow(1e3, 4) }, { key: "PiB", factor: Math.pow(1024, 5) }, { key: "PB", factor: Math.pow(1e3, 5) }, { key: "MiB", factor: Math.pow(1024, 2) }, { key: "MB", factor: Math.pow(1e3, 2) }, { key: "KiB", factor: Math.pow(1024, 1) }, { key: "KB", factor: Math.pow(1e3, 1) }, { key: "GiB", factor: Math.pow(1024, 3) }, { key: "GB", factor: Math.pow(1e3, 3) }, { key: "EiB", factor: Math.pow(1024, 6) }, { key: "EB", factor: Math.pow(1e3, 6) }, { key: "B", factor: 1 }];
      function s(l) {
        return l.replace(/[-/\\^$*+?.()|[\]{}]/g, "\\$&");
      }
      function a(l, u) {
        var c = 2 < arguments.length && arguments[2] !== void 0 ? arguments[2] : "", h = 3 < arguments.length ? arguments[3] : void 0, d = 4 < arguments.length ? arguments[4] : void 0, g = 5 < arguments.length ? arguments[5] : void 0, m = 6 < arguments.length ? arguments[6] : void 0;
        if (l !== "")
          return l === d ? 0 : function w(C, S) {
            var b = 2 < arguments.length && arguments[2] !== void 0 ? arguments[2] : "", R = 3 < arguments.length ? arguments[3] : void 0, _ = 4 < arguments.length ? arguments[4] : void 0, O = 5 < arguments.length ? arguments[5] : void 0, $ = 6 < arguments.length ? arguments[6] : void 0;
            if (!isNaN(+C))
              return +C;
            var U = "", G = C.replace(/(^[^(]*)\((.*)\)([^)]*$)/, "$1$2$3");
            if (G !== C)
              return -1 * w(G, S, b, R, _, O, $);
            for (var z = 0; z < o.length; z++) {
              var j = o[z];
              if ((U = C.replace(j.key, "")) !== C)
                return w(U, S, b, R, _, O, $) * j.factor;
            }
            if ((U = C.replace("%", "")) !== C)
              return w(U, S, b, R, _, O, $) / 100;
            var J = parseFloat(C);
            if (!isNaN(J)) {
              var q = R(J);
              if (q && q !== "." && (U = C.replace(new RegExp("".concat(s(q), "$")), "")) !== C)
                return w(U, S, b, R, _, O, $);
              var re = {};
              Object.keys(O).forEach(function(wt) {
                re[O[wt]] = wt;
              });
              for (var me = Object.keys(re).sort().reverse(), Me = me.length, nt = 0; nt < Me; nt++) {
                var Ne = me[nt], Ce = re[Ne];
                if ((U = C.replace(Ne, "")) !== C) {
                  var pe = void 0;
                  switch (Ce) {
                    case "thousand":
                      pe = Math.pow(10, 3);
                      break;
                    case "million":
                      pe = Math.pow(10, 6);
                      break;
                    case "billion":
                      pe = Math.pow(10, 9);
                      break;
                    case "trillion":
                      pe = Math.pow(10, 12);
                  }
                  return w(U, S, b, R, _, O, $) * pe;
                }
              }
            }
          }(function(w, C) {
            var S = 2 < arguments.length && arguments[2] !== void 0 ? arguments[2] : "", b = w.replace(S, "");
            return b = (b = b.replace(new RegExp("([0-9])".concat(s(C.thousands), "([0-9])"), "g"), "$1$2")).replace(C.decimal, ".");
          }(l, u, c), u, c, h, d, g, m);
      }
      i.exports = { unformat: function(l, u) {
        var c, h, d, g = n("./globalState"), m = g.currentDelimiters(), w = g.currentCurrency().symbol, C = g.currentOrdinal(), S = g.getZeroFormat(), b = g.currentAbbreviations(), R = void 0;
        if (typeof l == "string")
          R = function(_, O) {
            if (!_.indexOf(":") || O.thousands === ":")
              return !1;
            var $ = _.split(":");
            if ($.length !== 3)
              return !1;
            var U = +$[0], G = +$[1], z = +$[2];
            return !isNaN(U) && !isNaN(G) && !isNaN(z);
          }(l, m) ? (c = l.split(":"), h = +c[0], d = +c[1], +c[2] + 60 * d + 3600 * h) : a(l, m, w, C, S, b, u);
        else {
          if (typeof l != "number")
            return;
          R = l;
        }
        if (R !== void 0)
          return R;
      } };
    }, { "./globalState": 4 }], 10: [function(n, i, r) {
      function o(m) {
        return function(w) {
          if (Array.isArray(w)) {
            for (var C = 0, S = new Array(w.length); C < w.length; C++)
              S[C] = w[C];
            return S;
          }
        }(m) || function(w) {
          if (Symbol.iterator in Object(w) || Object.prototype.toString.call(w) === "[object Arguments]")
            return Array.from(w);
        }(m) || function() {
          throw new TypeError("Invalid attempt to spread non-iterable instance");
        }();
      }
      function s(m) {
        return (s = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(w) {
          return typeof w;
        } : function(w) {
          return w && typeof Symbol == "function" && w.constructor === Symbol && w !== Symbol.prototype ? "symbol" : typeof w;
        })(m);
      }
      var a = n("./unformatting"), l = /^[a-z]{2,3}(-[a-zA-Z]{4})?(-([A-Z]{2}|[0-9]{3}))?$/, u = { output: { type: "string", validValues: ["currency", "percent", "byte", "time", "ordinal", "number"] }, base: { type: "string", validValues: ["decimal", "binary", "general"], restriction: function(m, w) {
        return w.output === "byte";
      }, message: "`base` must be provided only when the output is `byte`", mandatory: function(m) {
        return m.output === "byte";
      } }, characteristic: { type: "number", restriction: function(m) {
        return 0 <= m;
      }, message: "value must be positive" }, prefix: "string", postfix: "string", forceAverage: { type: "string", validValues: ["trillion", "billion", "million", "thousand"] }, average: "boolean", currencyPosition: { type: "string", validValues: ["prefix", "infix", "postfix"] }, currencySymbol: "string", totalLength: { type: "number", restrictions: [{ restriction: function(m) {
        return 0 <= m;
      }, message: "value must be positive" }, { restriction: function(m, w) {
        return !w.exponential;
      }, message: "`totalLength` is incompatible with `exponential`" }] }, mantissa: { type: "number", restriction: function(m) {
        return 0 <= m;
      }, message: "value must be positive" }, optionalMantissa: "boolean", trimMantissa: "boolean", optionalCharacteristic: "boolean", thousandSeparated: "boolean", spaceSeparated: "boolean", abbreviations: { type: "object", children: { thousand: "string", million: "string", billion: "string", trillion: "string" } }, negative: { type: "string", validValues: ["sign", "parenthesis"] }, forceSign: "boolean", exponential: { type: "boolean" }, prefixSymbol: { type: "boolean", restriction: function(m, w) {
        return w.output === "percent";
      }, message: "`prefixSymbol` can be provided only when the output is `percent`" } }, c = { languageTag: { type: "string", mandatory: !0, restriction: function(m) {
        return m.match(l);
      }, message: "the language tag must follow the BCP 47 specification (see https://tools.ieft.org/html/bcp47)" }, delimiters: { type: "object", children: { thousands: "string", decimal: "string", thousandsSize: "number" }, mandatory: !0 }, abbreviations: { type: "object", children: { thousand: { type: "string", mandatory: !0 }, million: { type: "string", mandatory: !0 }, billion: { type: "string", mandatory: !0 }, trillion: { type: "string", mandatory: !0 } }, mandatory: !0 }, spaceSeparated: "boolean", ordinal: { type: "function", mandatory: !0 }, currency: { type: "object", children: { symbol: "string", position: "string", code: "string" }, mandatory: !0 }, defaults: "format", ordinalFormat: "format", byteFormat: "format", percentageFormat: "format", currencyFormat: "format", timeDefaults: "format", formats: { type: "object", children: { fourDigits: { type: "format", mandatory: !0 }, fullWithTwoDecimals: { type: "format", mandatory: !0 }, fullWithTwoDecimalsNoCurrency: { type: "format", mandatory: !0 }, fullWithNoDecimals: { type: "format", mandatory: !0 } } } };
      function h(m) {
        return !!a.unformat(m);
      }
      function d(m, w, C) {
        var S = 3 < arguments.length && arguments[3] !== void 0 && arguments[3], b = Object.keys(m).map(function(R) {
          if (!w[R])
            return console.error("".concat(C, " Invalid key: ").concat(R)), !1;
          var _ = m[R], O = w[R];
          if (typeof O == "string" && (O = { type: O }), O.type === "format") {
            if (!d(_, u, "[Validate ".concat(R, "]"), !0))
              return !1;
          } else if (s(_) !== O.type)
            return console.error("".concat(C, " ").concat(R, ' type mismatched: "').concat(O.type, '" expected, "').concat(s(_), '" provided')), !1;
          if (O.restrictions && O.restrictions.length)
            for (var $ = O.restrictions.length, U = 0; U < $; U++) {
              var G = O.restrictions[U], z = G.restriction, j = G.message;
              if (!z(_, m))
                return console.error("".concat(C, " ").concat(R, " invalid value: ").concat(j)), !1;
            }
          return O.restriction && !O.restriction(_, m) ? (console.error("".concat(C, " ").concat(R, " invalid value: ").concat(O.message)), !1) : O.validValues && O.validValues.indexOf(_) === -1 ? (console.error("".concat(C, " ").concat(R, " invalid value: must be among ").concat(JSON.stringify(O.validValues), ', "').concat(_, '" provided')), !1) : !(O.children && !d(_, O.children, "[Validate ".concat(R, "]")));
        });
        return S || b.push.apply(b, o(Object.keys(w).map(function(R) {
          var _ = w[R];
          if (typeof _ == "string" && (_ = { type: _ }), _.mandatory) {
            var O = _.mandatory;
            if (typeof O == "function" && (O = O(m)), O && m[R] === void 0)
              return console.error("".concat(C, ' Missing mandatory key "').concat(R, '"')), !1;
          }
          return !0;
        }))), b.reduce(function(R, _) {
          return R && _;
        }, !0);
      }
      function g(m) {
        return d(m, u, "[Validate format]");
      }
      i.exports = { validate: function(m, w) {
        var C = h(m), S = g(w);
        return C && S;
      }, validateFormat: g, validateInput: h, validateLanguage: function(m) {
        return d(m, c, "[Validate language]");
      } };
    }, { "./unformatting": 9 }] }, {}, [7])(7);
  });
})(Y1);
var QJ = Y1.exports;
const ga = /* @__PURE__ */ dN(QJ), XJ = "numeric";
function j1(t, e) {
  if (Dn(t)) {
    const n = e.numericFormat, i = n && n.culture || "-", r = n && n.pattern;
    if (typeof i != "undefined" && !ga.languages()[i]) {
      const o = i.replace("-", ""), s = ga.allLanguages ? ga.allLanguages[i] : ga[o];
      s && ga.registerLanguage(s);
    }
    ga.setLanguage(i), t = ga(t).format(r || "0");
  }
  return t;
}
function gR(t, e, n, i, r, o, s) {
  let a = o;
  if (Dn(a)) {
    const l = s.className || "", u = l.length ? l.split(" ") : [];
    a = j1(a, s), u.indexOf("htLeft") < 0 && u.indexOf("htCenter") < 0 && u.indexOf("htRight") < 0 && u.indexOf("htJustify") < 0 && u.push("htRight"), u.indexOf("htNumeric") < 0 && u.push("htNumeric"), s.className = u.join(" "), e.dir = "ltr";
  }
  ss(t, e, n, i, r, a, s);
}
gR.RENDERER_TYPE = XJ;
const qJ = "password";
function mR(t, e, n, i, r, o, s) {
  ss.apply(this, [t, e, n, i, r, o, s]);
  const a = s.hashLength || e.innerHTML.length, l = s.hashSymbol || "*";
  let u = "";
  Ae(a - 1, () => {
    u += l;
  }), pl(e, u);
}
mR.RENDERER_TYPE = qJ;
const ZJ = "select";
function pR(t, e, n, i, r, o, s) {
  ss.apply(this, [t, e, n, i, r, o, s]);
}
pR.RENDERER_TYPE = ZJ;
const JJ = "time";
function wR(t, e, n, i, r, o, s) {
  ss.apply(this, [t, e, n, i, r, o, s]), e.dir = "ltr";
}
wR.RENDERER_TYPE = JJ;
function e6() {
  wi(Vu), wi(Od), wi(hR), wi(cR), wi(fR), wi(uR), wi(gR), wi(mR), wi(pR), wi(ss), wi(wR);
}
const t6 = "autocomplete";
function qw(t, e) {
  let n = t;
  if (n == null && (n = ""), this.allowEmpty && n === "") {
    e(!0);
    return;
  }
  this.strict && this.source ? typeof this.source == "function" ? this.source(n, QT(n, e)) : QT(n, e)(this.source) : e(!0);
}
qw.VALIDATOR_TYPE = t6;
function QT(t, e) {
  const n = t;
  return function(i) {
    let r = !1;
    for (let o = 0, s = i.length; o < s; o++)
      if (n === i[o]) {
        r = !0;
        break;
      }
    e(r);
  };
}
function G1(t) {
  const e = new Date(t);
  return isNaN((/* @__PURE__ */ new Date(`${t}T00:00`)).getDate()) ? e : new Date(e.getTime() + e.getTimezoneOffset() * 6e4);
}
const n6 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  getNormalizedDate: G1
}, Symbol.toStringTag, { value: "Module" })), i6 = "date";
function AR(t, e) {
  const n = Cx(L1, this.instance);
  let i = t, r = !0;
  i == null && (i = "");
  let o = X(i, this.dateFormat || n.defaultDateFormat, !0).isValid(), s = X(new Date(i)).isValid() || o;
  if (this.allowEmpty && i === "" && (s = !0, o = !0), s || (r = !1), !s && o && (r = !0), s && !o)
    if (this.correctFormat === !0) {
      const a = r6(i, this.dateFormat);
      this.instance.setDataAtCell(this.visualRow, this.visualCol, a, "dateValidator"), r = !0;
    } else
      r = !1;
  e(r);
}
AR.VALIDATOR_TYPE = i6;
function r6(t, e) {
  const n = X(G1(t)), i = X(t, e), r = t.search(/[A-z]/g) > -1;
  let o;
  return n.isValid() && n.format("x") === i.format("x") || !i.isValid() || r ? o = n : o = i, o.format(e);
}
const o6 = "dropdown";
function CR(t, e) {
  qw.apply(this, [t, e]);
}
CR.VALIDATOR_TYPE = o6;
const s6 = "numeric";
function vR(t, e) {
  let n = t;
  n == null && (n = ""), this.allowEmpty && n === "" ? e(!0) : e(n === "" ? !1 : Dn(t));
}
vR.VALIDATOR_TYPE = s6;
const a6 = [
  "YYYY-MM-DDTHH:mm:ss.SSSZ",
  "X",
  // Unix timestamp
  "x"
  // Unix ms timestamp
], l6 = "time";
function yR(t, e) {
  const n = this.timeFormat || "h:mm:ss a";
  let i = !0, r = t;
  r === null && (r = ""), r = /^\d{3,}$/.test(r) ? parseInt(r, 10) : r;
  const o = /^\d{1,2}$/.test(r);
  o && (r += ":00");
  const s = X(r, a6, !0).isValid() ? X(r) : X(r, n);
  let a = s.isValid(), l = X(r, n, !0).isValid() && !o;
  if (this.allowEmpty && r === "" && (a = !0, l = !0), a || (i = !1), !a && l && (i = !0), a && !l)
    if (this.correctFormat === !0) {
      const u = s.format(n);
      this.instance.setDataAtCell(this.visualRow, this.visualCol, u, "timeValidator"), i = !0;
    } else
      i = !1;
  e(i);
}
yR.VALIDATOR_TYPE = l6;
function u6() {
  $a(qw), $a(CR), $a(AR), $a(vR), $a(yR);
}
const c6 = "autocomplete", h6 = {
  CELL_TYPE: c6,
  editor: sR,
  renderer: Vu,
  validator: qw
}, d6 = "checkbox", f6 = {
  CELL_TYPE: d6,
  editor: k1,
  renderer: hR
}, g6 = "date";
function z1(t, e, n, i, r, o, s) {
  Vu.apply(this, [t, e, n, i, r, o, s]);
}
z1.RENDERER_TYPE = g6;
const m6 = "date", p6 = {
  CELL_TYPE: m6,
  editor: B1,
  // displays small gray arrow on right side of the cell
  renderer: z1,
  validator: AR
}, w6 = "dropdown", A6 = {
  CELL_TYPE: w6,
  editor: lR,
  // displays small gray arrow on right side of the cell
  renderer: cR,
  validator: CR
}, C6 = "handsontable", v6 = {
  CELL_TYPE: C6,
  editor: oR,
  // displays small gray arrow on right side of the cell
  renderer: fR
}, y6 = "numeric", S6 = {
  CELL_TYPE: y6,
  editor: F1,
  renderer: gR,
  validator: vR,
  dataType: "number"
}, b6 = "password", R6 = {
  CELL_TYPE: b6,
  editor: V1,
  renderer: mR,
  copyable: !1
}, E6 = "select", T6 = {
  CELL_TYPE: E6,
  editor: W1,
  renderer: pR
}, M6 = "time", I6 = {
  CELL_TYPE: M6,
  editor: $1,
  renderer: wR,
  validator: yR
};
function O6() {
  Di(h6), Di(f6), Di(p6), Di(A6), Di(v6), Di(S6), Di(R6), Di(T6), Di(_1), Di(I6);
}
function P6(t, e) {
  K1(t, e), e.add(t);
}
function x6(t, e, n) {
  K1(t, e), e.set(t, n);
}
function K1(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function _l(t, e, n) {
  return (e = H6(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function H6(t) {
  var e = _6(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function _6(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Nl(t, e) {
  return t.get(Q1(t, e));
}
function Q1(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const XT = /* @__PURE__ */ new Map([["plugin", t2], ["cell-type", R1], ["editor", Ax], ["renderer", Tx], ["validator", Mx]]), N6 = "base", xC = [];
let Lr = null;
var ma = /* @__PURE__ */ new WeakMap(), qT = /* @__PURE__ */ new WeakSet();
class at {
  static get PLUGIN_KEY() {
    return N6;
  }
  /**
   * The `SETTING_KEYS` getter defines the keys that, when present in the config object, trigger the plugin update
   * after the `updateSettings` calls.
   * - When it returns `true`, the plugin updates after all `updateSettings` calls, regardless of the contents of the
   * config object.
   * - When it returns `false`, the plugin never updates on `updateSettings` calls.
   *
   * @returns {string[] | boolean}
   */
  static get SETTING_KEYS() {
    return [this.PLUGIN_KEY];
  }
  /**
   * The instance of the {@link EventManager} class.
   *
   * @type {EventManager}
   */
  /**
   * @param {object} hotInstance Handsontable instance.
   */
  constructor(e) {
    P6(this, qT), _l(this, "eventManager", new Qi(this)), _l(this, "pluginName", null), _l(this, "pluginsInitializedCallbacks", []), _l(this, "isPluginsReady", !1), _l(this, "enabled", !1), _l(this, "initialized", !1), x6(this, ma, {}), wo(this, "hot", e, {
      writable: !1
    }), Lr = null, this.hot.addHook("afterPluginsInitialized", () => this.onAfterPluginsInitialized()), this.hot.addHook("afterUpdateSettings", (n) => this.onUpdateSettings(n)), this.hot.addHook("beforeInit", () => this.init());
  }
  init() {
    this.pluginName = this.hot.getPluginName(this);
    const e = this.constructor.PLUGIN_DEPS, n = Array.isArray(e) ? e : [];
    if (n.length > 0) {
      const r = [];
      if (n.forEach((o) => {
        const [s, a] = o.split(":");
        if (!XT.has(s))
          throw new Error(`Unknown plugin dependency type "${s}" was found.`);
        XT.get(s)(a) || r.push(` - ${a} (${s})`);
      }), r.length > 0) {
        const o = [`The ${this.pluginName} plugin requires the following modules:
`, `${r.join(`
`)}
`].join("");
        xC.push(o);
      }
    }
    if (Lr || (Lr = Sb()), Lr.indexOf("UndoRedo") >= 0 && Lr.splice(Lr.indexOf("UndoRedo"), 1), Lr.indexOf(this.pluginName) >= 0 && Lr.splice(Lr.indexOf(this.pluginName), 1), this.hot.addHookOnce("afterPluginsInitialized", () => {
      this.isEnabled && this.isEnabled() && this.enablePlugin();
    }), Lr.length === 0) {
      if (xC.length > 0) {
        const r = [`${xC.join(`
`)}
`, "You have to import and register them manually."].join("");
        throw new Error(r);
      }
      this.hot.runHooks("afterPluginsInitialized");
    }
    this.initialized = !0;
  }
  /**
   * Enable plugin for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled = !0;
  }
  /**
   * Disable plugin for this Handsontable instance.
   */
  disablePlugin() {
    var e;
    (e = this.eventManager) === null || e === void 0 || e.clear(), this.clearHooks(), this.enabled = !1;
  }
  /**
   * Add listener to plugin hooks system.
   *
   * @param {string} name The hook name.
   * @param {Function} callback The listener function to add.
   * @param {number} [orderIndex] Order index of the callback.
   *                              If > 0, the callback will be added after the others, for example, with an index of 1, the callback will be added before the ones with an index of 2, 3, etc., but after the ones with an index of 0 and lower.
   *                              If < 0, the callback will be added before the others, for example, with an index of -1, the callback will be added after the ones with an index of -2, -3, etc., but before the ones with an index of 0 and higher.
   *                              If 0 or no order index is provided, the callback will be added between the "negative" and "positive" indexes.
   */
  addHook(e, n, i) {
    Nl(ma, this)[e] = Nl(ma, this)[e] || [];
    const r = Nl(ma, this)[e];
    this.hot.addHook(e, n, i), r.push(n), Nl(ma, this)[e] = r;
  }
  /**
   * Remove all hooks listeners by hook name.
   *
   * @param {string} name The hook name.
   */
  removeHooks(e) {
    T(Nl(ma, this)[e] || [], (n) => {
      this.hot.removeHook(e, n);
    });
  }
  /**
   * Clear all hooks.
   */
  clearHooks() {
    const e = Nl(ma, this);
    Fe(e, (n, i) => this.removeHooks(i)), e.length = 0;
  }
  /**
   * Register function which will be immediately called after all plugins initialized.
   *
   * @param {Function} callback The listener function to call.
   */
  callOnPluginsReady(e) {
    this.isPluginsReady ? e() : this.pluginsInitializedCallbacks.push(e);
  }
  /**
   * On after plugins initialized listener.
   *
   * @private
   */
  onAfterPluginsInitialized() {
    T(this.pluginsInitializedCallbacks, (e) => e()), this.pluginsInitializedCallbacks.length = 0, this.isPluginsReady = !0;
  }
  /**
   * On update settings listener.
   *
   * @private
   * @param {object} newSettings New set of settings passed to the `updateSettings` method.
   */
  onUpdateSettings(e) {
    const n = Q1(qT, this, D6).call(this, e);
    this.isEnabled && (this.enabled && !this.isEnabled() && this.disablePlugin(), !this.enabled && this.isEnabled() && this.enablePlugin(), this.enabled && this.isEnabled() && n && this.updatePlugin(e));
  }
  /**
   * Updates the plugin to use the latest options you have specified.
   *
   * @private
   */
  updatePlugin() {
  }
  /**
   * Destroy plugin.
   */
  destroy() {
    var e;
    (e = this.eventManager) === null || e === void 0 || e.destroy(), this.clearHooks(), Fe(this, (n, i) => {
      i !== "hot" && (this[i] = null);
    }), delete this.t, delete this.hot;
  }
}
function D6(t) {
  if (!t)
    return !1;
  const e = this.constructor.SETTING_KEYS;
  if (typeof e == "boolean")
    return e;
  for (let n = 0; n < e.length; n++)
    if (t[e[n]] !== void 0)
      return !0;
  return !1;
}
function pa(t, e, n) {
  return (e = k6(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function k6(t) {
  var e = L6(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function L6(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
let B6 = class {
  constructor(e) {
    pa(this, "hot", null), pa(this, "container", null), pa(this, "injected", !1), pa(this, "rows", []), pa(this, "columns", []), pa(this, "samples", null), pa(this, "settings", {
      useHeaders: !0
    }), this.hot = e;
  }
  /**
   * Add row.
   *
   * @param {number} row Visual row index.
   * @param {Map} samples Samples Map object.
   */
  addRow(e, n) {
    if (this.columns.length)
      throw new Error("Doesn't support multi-dimensional table");
    this.rows.length || (this.container = this.createContainer(this.hot.rootElement.className));
    const i = {
      row: e
    };
    this.rows.push(i), this.samples = n, this.table = this.createTable(this.hot.table.className), this.table.colGroup.appendChild(this.createColGroupsCol(e)), this.table.tr.appendChild(this.createRow(e)), this.container.container.appendChild(this.table.fragment), i.table = this.table.table;
  }
  /**
   * Add a row consisting of the column headers.
   *
   * @param {Map} samples A map with sampled table values.
   */
  addColumnHeadersRow(e) {
    const n = this.hot.getColHeader(0);
    if (n != null) {
      const i = {
        row: -1
      };
      this.rows.push(i), this.container = this.createContainer(this.hot.rootElement.className), this.samples = e, this.table = this.createTable(this.hot.table.className), this.table.colGroup.appendChild(this.createColGroupsCol()), this.appendColumnHeadersRow(), this.container.container.appendChild(this.table.fragment), i.table = this.table.table;
    }
  }
  /**
   * Add column.
   *
   * @param {number} column Visual column index.
   * @param {Map} samples A map with sampled table values.
   */
  addColumn(e, n) {
    if (this.rows.length)
      throw new Error("Doesn't support multi-dimensional table");
    this.columns.length || (this.container = this.createContainer(this.hot.rootElement.className));
    const i = {
      col: e
    };
    this.columns.push(i), this.samples = n, this.table = this.createTable(this.hot.table.className), this.getSetting("useHeaders") && this.hot.getColHeader(e) !== null && this.hot.view.appendColHeader(e, this.table.th), this.table.tBody.appendChild(this.createCol(e)), this.container.container.appendChild(this.table.fragment), i.table = this.table.table;
  }
  /**
   * Get calculated heights.
   *
   * @param {Function} callback Callback which will be fired for each calculated row.
   */
  getHeights(e) {
    this.injected || this.injectTable(), T(this.rows, (n) => {
      const {
        height: i
      } = n.table.getBoundingClientRect();
      e(n.row, Math.ceil(i) - 1);
    });
  }
  /**
   * Get calculated widths.
   *
   * @param {Function} callback Callback which will be fired for each calculated column.
   */
  getWidths(e) {
    this.injected || this.injectTable(), T(this.columns, (n) => {
      const {
        width: i
      } = n.table.getBoundingClientRect();
      e(n.col, Math.ceil(i));
    });
  }
  /**
   * Set the Ghost Table settings to the provided object.
   *
   * @param {object} settings New Ghost Table Settings.
   */
  setSettings(e) {
    this.settings = e;
  }
  /**
   * Set a single setting of the Ghost Table.
   *
   * @param {string} name Setting name.
   * @param {*} value Setting value.
   */
  setSetting(e, n) {
    this.settings || (this.settings = {}), this.settings[e] = n;
  }
  /**
   * Get the Ghost Table settings.
   *
   * @returns {object|null}
   */
  getSettings() {
    return this.settings;
  }
  /**
   * Get a single Ghost Table setting.
   *
   * @param {string} name The setting name to get.
   * @returns {boolean|null}
   */
  getSetting(e) {
    return this.settings ? this.settings[e] : null;
  }
  /**
   * Create colgroup col elements.
   *
   * @param {number} row Visual row index.
   * @returns {DocumentFragment}
   */
  createColGroupsCol(e) {
    const n = this.hot.rootDocument.createDocumentFragment();
    return this.hot.hasRowHeaders() && n.appendChild(this.createColElement(-1, -1)), this.samples.forEach((i) => {
      T(i.strings, (r) => {
        n.appendChild(this.createColElement(r.col, e));
      });
    }), n;
  }
  /**
   * Create table row element.
   *
   * @param {number} row Visual row index.
   * @returns {DocumentFragment} Returns created table row elements.
   */
  createRow(e) {
    const {
      rootDocument: n
    } = this.hot, i = n.createDocumentFragment(), r = n.createElement("th");
    return this.hot.hasRowHeaders() && (this.hot.view.appendRowHeader(e, r), i.appendChild(r)), this.samples.forEach((o) => {
      T(o.strings, (s) => {
        const a = s.col, l = this.hot.getCellMeta(e, a), u = this.hot.getCellRenderer(l), c = n.createElement("td");
        c.setAttribute("ghost-table", 1), u(this.hot, c, e, a, this.hot.colToProp(a), s.value, l), i.appendChild(c);
      });
    }), i;
  }
  /**
   * Creates DOM elements for headers and appends them to the THEAD element of the table.
   */
  appendColumnHeadersRow() {
    const {
      rootDocument: e
    } = this.hot, n = e.createDocumentFragment(), i = [];
    if (this.hot.hasRowHeaders()) {
      const r = e.createElement("th");
      i.push([-1, r]), n.appendChild(r);
    }
    this.samples.forEach((r) => {
      T(r.strings, (o) => {
        const s = o.col, a = e.createElement("th");
        i.push([s, a]), n.appendChild(a);
      });
    }), this.table.tHead.appendChild(n), T(i, (r) => {
      const [o, s] = r;
      this.hot.view.appendColHeader(o, s);
    });
  }
  /**
   * Create table column elements.
   *
   * @param {number} column Visual column index.
   * @returns {DocumentFragment} Returns created column table column elements.
   */
  createCol(e) {
    const {
      rootDocument: n
    } = this.hot, i = n.createDocumentFragment();
    return this.samples.forEach((r) => {
      T(r.strings, (o) => {
        const s = o.row, a = this.hot.getCellMeta(s, e), l = this.hot.getCellRenderer(a), u = n.createElement("td"), c = n.createElement("tr");
        u.setAttribute("ghost-table", 1), l(this.hot, u, s, e, this.hot.colToProp(e), o.value, a), c.appendChild(u), i.appendChild(c);
      });
    }), i;
  }
  /**
   * Remove table from document and reset internal state.
   */
  clean() {
    this.rows.length = 0, this.rows[-1] = void 0, this.columns.length = 0, this.samples && this.samples.clear(), this.samples = null, this.removeTable();
  }
  /**
   * Inject generated table into document.
   *
   * @param {HTMLElement} [parent=null] The element to which the ghost table is injected.
   */
  injectTable() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
    this.injected || ((e || this.hot.rootElement).appendChild(this.container.fragment), this.injected = !0);
  }
  /**
   * Remove table from document.
   */
  removeTable() {
    this.injected && this.container.container.parentNode && (this.container.container.parentNode.removeChild(this.container.container), this.container = null, this.injected = !1);
  }
  /**
   * Create col element.
   *
   * @param {number} column Visual column index.
   * @param {number} row Visual row index.
   * @returns {HTMLElement}
   */
  createColElement(e, n) {
    const i = this.hot.rootDocument.createElement("col");
    let r = 0;
    n >= 0 && e >= 0 && (r = this.hot.getCellMeta(n, e).colspan);
    let o = this.hot.getColWidth(e);
    if (r > 1)
      for (let s = e + 1; s < e + r; s++)
        o += this.hot.getColWidth(s);
    return i.style.width = `${o}px`, i;
  }
  /**
   * Create table element.
   *
   * @param {string} className The CSS classes to add.
   * @returns {object}
   */
  createTable() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "";
    const {
      rootDocument: n
    } = this.hot, i = n.createDocumentFragment(), r = n.createElement("table"), o = n.createElement("thead"), s = n.createElement("tbody"), a = n.createElement("colgroup"), l = n.createElement("tr"), u = n.createElement("th");
    return this.isVertical() && r.appendChild(a), this.isHorizontal() && (l.appendChild(u), o.appendChild(l), r.style.tableLayout = "auto", r.style.width = "auto"), r.appendChild(o), this.isVertical() && s.appendChild(l), r.appendChild(s), Q(r, e), i.appendChild(r), {
      fragment: i,
      table: r,
      tHead: o,
      tBody: s,
      colGroup: a,
      tr: l,
      th: u
    };
  }
  /**
   * Create container for tables.
   *
   * @param {string} className The CSS classes to add.
   * @returns {object}
   */
  createContainer() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "";
    const {
      rootDocument: n
    } = this.hot, i = n.createDocumentFragment(), r = n.createElement("div"), o = `htGhostTable htAutoSize ${e.trim()}`;
    return Q(r, o), i.appendChild(r), {
      fragment: i,
      container: r
    };
  }
  /**
   * Checks if table is raised vertically (checking rows).
   *
   * @returns {boolean}
   */
  isVertical() {
    return !!(this.rows.length && !this.columns.length);
  }
  /**
   * Checks if table is raised horizontally (checking columns).
   *
   * @returns {boolean}
   */
  isHorizontal() {
    return !!(this.columns.length && !this.rows.length);
  }
};
const SR = B6;
function Vf(t, e, n) {
  return (e = F6(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function F6(t) {
  var e = V6(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function V6(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class bR {
  /**
   * Number of samples to take of each value length.
   *
   * @type {number}
   */
  static get SAMPLE_COUNT() {
    return 3;
  }
  /**
   * Samples prepared for calculations.
   *
   * @type {Map}
   * @default {null}
   */
  constructor(e) {
    Vf(this, "samples", null), Vf(this, "dataFactory", null), Vf(this, "customSampleCount", null), Vf(this, "allowDuplicates", !1), this.dataFactory = e;
  }
  /**
   * Get the sample count for this instance.
   *
   * @returns {number}
   */
  getSampleCount() {
    return this.customSampleCount ? this.customSampleCount : bR.SAMPLE_COUNT;
  }
  /**
   * Set the sample count.
   *
   * @param {number} sampleCount Number of samples to be collected.
   */
  setSampleCount(e) {
    this.customSampleCount = e;
  }
  /**
   * Set if the generator should accept duplicate values.
   *
   * @param {boolean} allowDuplicates `true` to allow duplicate values.
   */
  setAllowDuplicates(e) {
    this.allowDuplicates = e;
  }
  /**
   * Generate samples for row. You can control which area should be sampled by passing `rowRange` object and `colRange` object.
   *
   * @param {object|number} rowRange The rows range to generate the samples.
   * @param {object} colRange The column range to generate the samples.
   * @returns {object}
   */
  generateRowSamples(e, n) {
    return this.generateSamples("row", n, e);
  }
  /**
   * Generate samples for column. You can control which area should be sampled by passing `colRange` object and `rowRange` object.
   *
   * @param {object} colRange Column index.
   * @param {object} rowRange Column index.
   * @returns {object}
   */
  generateColumnSamples(e, n) {
    return this.generateSamples("col", n, e);
  }
  /**
   * Generate collection of samples.
   *
   * @param {string} type Type to generate. Can be `col` or `row`.
   * @param {object} range The range to generate the samples.
   * @param {object|number} specifierRange The range to generate the samples.
   * @returns {Map}
   */
  generateSamples(e, n, i) {
    const r = /* @__PURE__ */ new Map(), {
      from: o,
      to: s
    } = typeof i == "number" ? {
      from: i,
      to: i
    } : i;
    return Ae(o, s, (a) => {
      const l = this.generateSample(e, n, a);
      r.set(a, l);
    }), r;
  }
  /**
   * Generate sample for specified type (`row` or `col`).
   *
   * @param {string} type Samples type `row` or `col`.
   * @param {object} range The range to generate the samples.
   * @param {number} specifierValue The range to generate the samples.
   * @returns {Map}
   */
  generateSample(e, n, i) {
    if (e !== "row" && e !== "col")
      throw new Error("Unsupported sample type");
    const r = /* @__PURE__ */ new Map(), o = e === "row" ? "col" : "row", s = [];
    return Ae(n.from, n.to, (a) => {
      const l = e === "row" ? this.dataFactory(i, a) : this.dataFactory(a, i);
      if (l === !1)
        return;
      const {
        value: u,
        bundleSeed: c
      } = l, h = typeof c == "string" && c.length > 0;
      let d;
      h ? d = c : Be(u) ? d = `${Object.keys(u).length}` : Array.isArray(u) ? d = `${u.length}` : d = `${Gt(u).length}`, r.has(d) || r.set(d, {
        needed: this.getSampleCount(),
        strings: []
      });
      const g = r.get(d);
      g.needed && (!(s.indexOf(u) > -1) || this.allowDuplicates || h) && (g.strings.push({
        value: u,
        [o]: a
      }), s.push(u), g.needed -= 1);
    }), r;
  }
}
const X1 = bR;
function W6(t, e) {
  q1(t, e), e.add(t);
}
function ZT(t, e, n) {
  q1(t, e), e.set(t, n);
}
function q1(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Ac(t, e, n) {
  return (e = $6(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function $6(t) {
  var e = U6(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function U6(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function ja(t, e) {
  return t.get(Jr(t, e));
}
function RR(t, e, n) {
  return t.set(Jr(t, e), n), n;
}
function Jr(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("modifyAutoColumnSizeSeed");
const Cc = "autoColumnSize", Y6 = 10, j6 = "autoColumnSize";
var ou = /* @__PURE__ */ new WeakMap(), Pu = /* @__PURE__ */ new WeakMap(), Ho = /* @__PURE__ */ new WeakSet();
class hh extends at {
  static get PLUGIN_KEY() {
    return Cc;
  }
  static get PLUGIN_PRIORITY() {
    return Y6;
  }
  static get SETTING_KEYS() {
    return !0;
  }
  static get CALCULATION_STEP() {
    return 50;
  }
  static get SYNC_CALCULATION_LIMIT() {
    return 50;
  }
  /**
   * Instance of {@link GhostTable} for rows and columns size calculations.
   *
   * @private
   * @type {GhostTable}
   */
  constructor(e) {
    super(e), W6(this, Ho), Ac(this, "ghostTable", new SR(this.hot)), Ac(this, "samplesGenerator", new X1((n, i) => {
      const r = this.hot.toPhysicalRow(n), o = this.hot.toPhysicalColumn(i);
      if (this.hot.rowIndexMapper.isHidden(r) || this.hot.columnIndexMapper.isHidden(o))
        return !1;
      const s = this.hot.getCellMeta(n, i);
      let a = "";
      s.spanned || (a = this.hot.getDataAtCell(n, i));
      let l = "";
      return this.hot.hasHook("modifyAutoColumnSizeSeed") && (l = this.hot.runHooks("modifyAutoColumnSizeSeed", l, s, a)), {
        value: a,
        bundleSeed: l
      };
    })), Ac(this, "inProgress", !1), Ac(this, "measuredColumns", 0), Ac(this, "columnWidthsMap", new Zs()), ZT(this, ou, []), ZT(this, Pu, []), this.hot.columnIndexMapper.registerMap(j6, this.columnWidthsMap), this.addHook("beforeColumnResize", (n, i, r) => Jr(Ho, this, JT).call(this, n, i, r));
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link #enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return this.hot.getSettings()[Cc] !== !1 && !this.hot.getSettings().colWidths;
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    if (this.enabled)
      return;
    const n = this.hot.getSettings()[Cc];
    n && n.useHeaders !== null && n.useHeaders !== void 0 && this.ghostTable.setSetting("useHeaders", n.useHeaders), this.setSamplingOptions(), this.addHook("afterLoadData", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return Jr(Ho, e, K6).call(e, ...r);
    }), this.addHook("beforeChangeRender", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return Jr(Ho, e, Q6).call(e, ...r);
    }), this.addHook("afterFormulasValuesUpdate", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return Jr(Ho, e, q6).call(e, ...r);
    }), this.addHook("beforeRender", () => Jr(Ho, this, z6).call(this)), this.addHook("modifyColWidth", (i, r) => this.getColumnWidth(r, i)), this.addHook("init", () => Jr(Ho, this, X6).call(this)), super.enablePlugin();
  }
  /**
   * Updates the plugin's state. This method is executed when {@link Core#updateSettings} is invoked.
   */
  updatePlugin() {
    RR(Pu, this, this.findColumnsWhereHeaderWasChanged()), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    super.disablePlugin(), this.addHook("beforeColumnResize", (e, n, i) => Jr(Ho, this, JT).call(this, e, n, i));
  }
  /**
   * Calculates widths for visible columns in the viewport only.
   */
  calculateVisibleColumnsWidth() {
    if (!this.hot.countRows())
      return;
    const e = this.getFirstVisibleColumn(), n = this.getLastVisibleColumn();
    if (e === -1 || n === -1)
      return;
    const i = this.hot.renderCall;
    this.calculateColumnsWidth({
      from: e,
      to: n
    }, void 0, i);
  }
  /**
   * Calculates a columns width.
   *
   * @param {number|object} colRange Visual column index or an object with `from` and `to` visual indexes as a range.
   * @param {number|object} rowRange Visual row index or an object with `from` and `to` visual indexes as a range.
   * @param {boolean} [overwriteCache=false] If `true` the calculation will be processed regardless of whether the width exists in the cache.
   */
  calculateColumnsWidth() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {
      from: 0,
      to: this.hot.countCols() - 1
    }, n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
      from: 0,
      to: this.hot.countRows() - 1
    }, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    const r = typeof e == "number" ? {
      from: e,
      to: e
    } : e, o = typeof n == "number" ? {
      from: n,
      to: n
    } : n;
    Ae(r.from, r.to, (s) => {
      let a = this.hot.toPhysicalColumn(s);
      a === null && (a = s), (i || this.columnWidthsMap.getValueAtIndex(a) === null && !this.hot._getColWidthFromSettings(a)) && this.samplesGenerator.generateColumnSamples(s, o).forEach((u, c) => this.ghostTable.addColumn(c, u));
    }), this.ghostTable.columns.length && (this.hot.batchExecution(() => {
      this.ghostTable.getWidths((s, a) => {
        const l = this.hot.toPhysicalColumn(s);
        this.columnWidthsMap.setValueAtIndex(l, a);
      });
    }, !0), this.measuredColumns = r.to + 1, this.ghostTable.clean());
  }
  /**
   * Calculates all columns width. The calculated column will be cached in the {@link AutoColumnSize#widths} property.
   * To retrieve width for specified column use {@link AutoColumnSize#getColumnWidth} method.
   *
   * @param {object|number} rowRange Row index or an object with `from` and `to` properties which define row range.
   * @param {boolean} [overwriteCache] If `true` the calculation will be processed regardless of whether the width exists in the cache.
   */
  calculateAllColumnsWidth() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {
      from: 0,
      to: this.hot.countRows() - 1
    }, n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1, i = 0;
    const r = this.hot.countCols() - 1;
    let o = null;
    this.inProgress = !0;
    const s = () => {
      if (!this.hot) {
        kh(o), this.inProgress = !1;
        return;
      }
      this.calculateColumnsWidth({
        from: i,
        to: Math.min(i + hh.CALCULATION_STEP, r)
      }, e, n), i = i + hh.CALCULATION_STEP + 1, i < r ? o = xp(s) : (kh(o), this.inProgress = !1, this.hot.view.adjustElementsSize());
    }, a = this.getSyncCalculationLimit();
    a >= 0 && (this.calculateColumnsWidth({
      from: 0,
      to: a
    }, e, n), i = a + 1), i < r ? s() : this.inProgress = !1;
  }
  /**
   * Sets the sampling options.
   *
   * @private
   */
  setSamplingOptions() {
    const e = this.hot.getSettings()[Cc], n = e && Bt(e, "samplingRatio") ? e.samplingRatio : void 0, i = e && Bt(e, "allowSampleDuplicates") ? e.allowSampleDuplicates : void 0;
    n && !isNaN(n) && this.samplesGenerator.setSampleCount(parseInt(n, 10)), i && this.samplesGenerator.setAllowDuplicates(i);
  }
  /**
   * Recalculates all columns width (overwrite cache values).
   */
  recalculateAllColumnsWidth() {
    this.hot.view.isVisible() && this.calculateAllColumnsWidth({
      from: 0,
      to: this.hot.countRows() - 1
    }, !0);
  }
  /**
   * Gets value which tells how many columns should be calculated synchronously (rest of the columns will be calculated
   * asynchronously). The limit is calculated based on `syncLimit` set to `autoColumnSize` option (see {@link Options#autoColumnSize}).
   *
   * @returns {number}
   */
  getSyncCalculationLimit() {
    const e = this.hot.getSettings()[Cc];
    let n = hh.SYNC_CALCULATION_LIMIT;
    const i = this.hot.countCols() - 1;
    return Be(e) && (n = e.syncLimit, XS(n) ? n = vb(i, n) : n >>= 0), Math.min(n, i);
  }
  /**
   * Gets the calculated column width.
   *
   * @param {number} column Visual column index.
   * @param {number} [defaultWidth] Default column width. It will be picked up if no calculated width found.
   * @param {boolean} [keepMinimum=true] If `true` then returned value won't be smaller then 50 (default column width).
   * @returns {number}
   */
  getColumnWidth(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0, r = n;
    return r === void 0 && (r = this.columnWidthsMap.getValueAtIndex(this.hot.toPhysicalColumn(e)), i && typeof r == "number" && (r = Math.max(r, Np))), r;
  }
  /**
   * Gets the first visible column.
   *
   * @returns {number} Returns visual column index, -1 if table is not rendered or if there are no columns to base the the calculations on.
   */
  getFirstVisibleColumn() {
    var e;
    return (e = this.hot.view.getFirstRenderedVisibleColumn()) !== null && e !== void 0 ? e : -1;
  }
  /**
   * Gets the last visible column.
   *
   * @returns {number} Returns visual column index or -1 if table is not rendered.
   */
  getLastVisibleColumn() {
    var e;
    return (e = this.hot.view.getLastRenderedVisibleColumn()) !== null && e !== void 0 ? e : -1;
  }
  /**
   * Collects all columns which titles has been changed in comparison to the previous state.
   *
   * @private
   * @returns {Array} It returns an array of visual column indexes.
   */
  findColumnsWhereHeaderWasChanged() {
    return this.hot.getColHeader().reduce((i, r, o) => {
      const s = ja(ou, this).length;
      return (s - 1 < o || ja(ou, this)[o] !== r) && i.push(this.hot.toVisualColumn(o)), s - 1 < o ? ja(ou, this).push(r) : ja(ou, this)[o] = r, i;
    }, []);
  }
  /**
   * Clears cache of calculated column widths. If you want to clear only selected columns pass an array with their indexes.
   * Otherwise whole cache will be cleared.
   *
   * @param {number[]} [physicalColumns] List of physical column indexes to clear.
   */
  clearCache(e) {
    Array.isArray(e) ? this.hot.batchExecution(() => {
      e.forEach((n) => {
        this.columnWidthsMap.setValueAtIndex(n, null);
      });
    }, !0) : this.columnWidthsMap.clear();
  }
  /**
   * Checks if all widths were calculated. If not then return `true` (need recalculate).
   *
   * @returns {boolean}
   */
  isNeedRecalculate() {
    return !!this.columnWidthsMap.getValues().slice(0, this.measuredColumns).filter((e) => e === null).length;
  }
  /**
   * On before view render listener.
   */
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    this.ghostTable.clean(), super.destroy();
  }
}
function G6(t) {
  const e = {
    from: 0,
    to: this.hot.countRows() - 1
  };
  t.forEach((n) => {
    const i = this.hot.toPhysicalColumn(n);
    i !== null && (this.hot._getColWidthFromSettings(i) || this.samplesGenerator.generateColumnSamples(n, e).forEach((o, s) => this.ghostTable.addColumn(s, o)));
  }), this.ghostTable.columns.length && (this.hot.batchExecution(() => {
    this.ghostTable.getWidths((n, i) => {
      const r = this.hot.toPhysicalColumn(n);
      this.columnWidthsMap.setValueAtIndex(r, i);
    });
  }, !0), this.ghostTable.clean());
}
function z6() {
  this.calculateVisibleColumnsWidth(), this.inProgress || (Jr(Ho, this, G6).call(this, ja(Pu, this)), RR(Pu, this, []));
}
function K6(t, e) {
  e || this.recalculateAllColumnsWidth();
}
function Q6(t) {
  const e = t.reduce((n, i) => {
    let [, r] = i;
    const o = this.hot.propToCol(r);
    return Number.isInteger(o) && n.indexOf(o) === -1 && n.push(o), n;
  }, []);
  ja(Pu, this).push(...e);
}
function JT(t, e, n) {
  let i = t;
  return n && (this.calculateColumnsWidth(e, void 0, !0), i = this.getColumnWidth(e, void 0, !1)), i;
}
function X6() {
  RR(ou, this, this.hot.getColHeader()), this.recalculateAllColumnsWidth();
}
function q6(t) {
  const e = t.reduce((n, i) => {
    var r;
    const o = (r = i.address) === null || r === void 0 ? void 0 : r.col;
    if (Number.isInteger(o)) {
      const s = this.hot.toVisualColumn(o);
      n.indexOf(s) === -1 && n.push(s);
    }
    return n;
  }, []);
  ja(Pu, this).push(...e);
}
const Ga = {
  horizontal: "horizontal",
  vertical: "vertical"
};
function Z6(t, e, n) {
  let i, r, o;
  return e[0] === t[0] && e[1] < t[1] ? (o = "left", i = n(e[0], e[1]), r = n(e[2], t[1] - 1)) : e[2] === t[2] && e[0] === t[0] && e[3] > t[3] ? (o = "right", i = n(e[0], t[3] + 1), r = n(e[2], e[3])) : e[0] < t[0] && e[1] === t[1] ? (o = "up", i = n(e[0], e[1]), r = n(t[0] - 1, e[3])) : e[2] > t[2] && e[1] === t[1] && (o = "down", i = n(t[2] + 1, e[1]), r = n(e[2], e[3])), i && i.normalize(), r && r.normalize(), {
    directionOfDrag: o,
    startOfDragCoords: i,
    endOfDragCoords: r
  };
}
function J6(t) {
  const e = {};
  return t === !0 ? (e.directions = Object.keys(Ga), e.autoInsertRow = !0) : Be(t) ? (de(t.autoInsertRow) ? t.direction === Ga.horizontal ? e.autoInsertRow = !1 : e.autoInsertRow = t.autoInsertRow : e.autoInsertRow = !1, de(t.direction) ? e.directions = [t.direction] : e.directions = Object.keys(Ga)) : typeof t == "string" ? (e.directions = [t], e.autoInsertRow = !0) : (e.directions = [], e.autoInsertRow = !1), e;
}
function e8(t, e) {
  t8(t, e), e.add(t);
}
function t8(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Dl(t, e, n) {
  return (e = n8(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function n8(t) {
  var e = i8(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function i8(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function vc(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("modifyAutofillRange");
he.getSingleton().register("beforeAutofill");
he.getSingleton().register("afterAutofill");
const eM = "autofill", r8 = 20, o8 = ["fillHandle"], s8 = "insert_row_below", a8 = 200;
var kl = /* @__PURE__ */ new WeakSet();
class l8 extends at {
  constructor() {
    super(...arguments), e8(this, kl), Dl(this, "addingStarted", !1), Dl(this, "mouseDownOnCellCorner", !1), Dl(this, "mouseDragOutside", !1), Dl(this, "handleDraggedCells", 0), Dl(this, "directions", []), Dl(this, "autoInsertRow", !1);
  }
  static get PLUGIN_KEY() {
    return eM;
  }
  static get PLUGIN_PRIORITY() {
    return r8;
  }
  static get SETTING_KEYS() {
    return [eM, ...o8];
  }
  /**
   * Checks if the plugin is enabled in the Handsontable settings.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return this.hot.getSettings().fillHandle;
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled || (this.mapSettings(), this.registerEvents(), this.addHook("afterOnCellCornerMouseDown", (e) => vc(kl, this, c8).call(this, e)), this.addHook("afterOnCellCornerDblClick", (e) => vc(kl, this, u8).call(this, e)), this.addHook("beforeOnCellMouseOver", (e, n) => vc(kl, this, h8).call(this, n)), super.enablePlugin());
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - `autofill`
   *  - [`fillHandle`](@/api/options.md#fillhandle)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.clearMappedSettings(), super.disablePlugin();
  }
  /**
   * Gets selection data.
   *
   * @private
   * @returns {object[]} Ranges Array of objects with properties `startRow`, `startCol`, `endRow` and `endCol`.
   */
  getSelectionData() {
    const e = this.hot.getSelectedRangeLast(), {
      row: n,
      col: i
    } = e.getTopStartCorner(), {
      row: r,
      col: o
    } = e.getBottomEndCorner(), s = this.hot.runHooks("modifyCopyableRange", [{
      startRow: n,
      startCol: i,
      endRow: r,
      endCol: o
    }]), a = [], l = [], u = [];
    return T(s, (c) => {
      for (let h = c.startRow; h <= c.endRow; h += 1)
        a.indexOf(h) === -1 && a.push(h);
      for (let h = c.startCol; h <= c.endCol; h += 1)
        l.indexOf(h) === -1 && l.push(h);
    }), T(a, (c) => {
      const h = [];
      T(l, (d) => {
        h.push(this.hot.getCopyableData(c, d));
      }), u.push(h);
    }), u;
  }
  /**
   * Try to apply fill values to the area in fill border, omitting the selection border.
   *
   * @private
   * @returns {boolean} Reports if fill was applied.
   *
   * @fires Hooks#modifyAutofillRange
   * @fires Hooks#beforeAutofill
   * @fires Hooks#afterAutofill
   */
  fillIn() {
    if (this.hot.selection.highlight.getFill().isEmpty())
      return !1;
    const [e, n, i, r] = this.hot.selection.highlight.getFill().getVisualCorners(), o = this.hot.getSelectedRangeLast(), s = o.getTopStartCorner(), a = o.getBottomEndCorner();
    this.resetSelectionOfDraggedArea();
    const l = [s.row, s.col, a.row, a.col], u = this.hot.runHooks("modifyAutofillRange", [Math.min(s.row, e), Math.min(s.col, n), Math.max(a.row, i), Math.max(a.col, r)], l), {
      directionOfDrag: c,
      startOfDragCoords: h,
      endOfDragCoords: d
    } = Z6(l, u, (g, m) => this.hot._createCellCoords(g, m));
    if (h && h.row > -1 && h.col > -1) {
      const g = this.getSelectionData(), m = o.clone(), w = this.hot._createCellRange(h, h, d), C = this.hot.runHooks("beforeAutofill", g, m, w, c);
      if (C === !1)
        return this.hot.selection.highlight.getFill().clear(), this.hot.render(), !1;
      let S = C;
      const b = C;
      if (["up", "left"].indexOf(c) > -1 && !(b.length === 1 && b[0].length === 0))
        if (S = [], c === "up") {
          const R = d.row - h.row + 1, _ = R % b.length;
          for (let O = 0; O < R; O++)
            S.push(b[(O + (b.length - _)) % b.length]);
        } else {
          const R = d.col - h.col + 1, _ = R % b[0].length;
          for (let O = 0; O < b.length; O++) {
            S.push([]);
            for (let $ = 0; $ < R; $++)
              S[O].push(b[O][($ + (b[O].length - _)) % b[O].length]);
          }
        }
      this.hot.populateFromArray(h.row, h.col, S, d.row, d.col, `${this.pluginName}.fill`, null), this.setSelection(u), this.hot.runHooks("afterAutofill", S, m, w, c), this.hot.render();
    } else
      this.hot.view.render();
    return !0;
  }
  /**
   * Reduces the selection area if the handle was dragged outside of the table or on headers.
   *
   * @private
   * @param {CellCoords} coords Indexes of selection corners.
   * @returns {CellCoords}
   */
  reduceSelectionAreaIfNeeded(e) {
    return e.row < 0 && (e.row = 0), e.col < 0 && (e.col = 0), e;
  }
  /**
   * Gets the coordinates of the drag & drop borders.
   *
   * @private
   * @param {CellCoords} coordsOfSelection `CellCoords` coord object.
   * @returns {CellCoords}
   */
  getCoordsOfDragAndDropBorders(e) {
    const n = this.hot.getSelectedRangeLast(), i = n.getBottomEndCorner();
    let r = e;
    if (this.directions.includes(Ga.vertical) && this.directions.includes(Ga.horizontal)) {
      const o = n.getTopStartCorner();
      (i.col <= e.col || o.col >= e.col) && (r = this.hot._createCellCoords(i.row, e.col)), (i.row < e.row || o.row > e.row) && (r = this.hot._createCellCoords(e.row, i.col));
    } else if (this.directions.includes(Ga.vertical))
      r = this.hot._createCellCoords(e.row, i.col);
    else if (this.directions.includes(Ga.horizontal))
      r = this.hot._createCellCoords(i.row, e.col);
    else
      return;
    return this.reduceSelectionAreaIfNeeded(r);
  }
  /**
   * Show the fill border.
   *
   * @private
   * @param {CellCoords} coordsOfSelection `CellCoords` coord object.
   */
  showBorder(e) {
    const n = this.getCoordsOfDragAndDropBorders(e);
    n && this.redrawBorders(n);
  }
  /**
   * Add new row.
   *
   * @private
   */
  addRow() {
    this.hot._registerTimeout(() => {
      this.hot.alter(s8, void 0, 1, `${this.pluginName}.fill`), this.addingStarted = !1;
    }, a8);
  }
  /**
   * Add new rows if they are needed to continue auto-filling values.
   *
   * @private
   */
  addNewRowIfNeeded() {
    if (!this.hot.selection.highlight.getFill().isEmpty() && this.addingStarted === !1 && this.autoInsertRow) {
      const e = this.hot.getSelectedLast(), n = this.hot.selection.highlight.getFill().getVisualCorners(), i = this.hot.countRows();
      e[2] < i - 1 && n[2] === i - 1 && (this.addingStarted = !0, this.addRow());
    }
  }
  /**
   * Get index of last adjacent filled in row.
   *
   * @private
   * @param {Array} cornersOfSelectedCells Indexes of selection corners.
   * @returns {number} Gives number greater than or equal to zero when selection adjacent can be applied.
   *                   Or -1 when selection adjacent can't be applied.
   */
  getIndexOfLastAdjacentFilledInRow(e) {
    const n = this.hot.getData(), i = this.hot.countRows();
    let r;
    for (let o = e[2] + 1; o < i; o++) {
      for (let l = e[1]; l <= e[3]; l++) {
        const u = n[o][l];
        if (!zt(u))
          return -1;
      }
      const s = n[o][e[1] - 1], a = n[o][e[3] + 1];
      (!zt(s) || !zt(a)) && (r = o);
    }
    return r;
  }
  /**
   * Adds a selection from the start area to the specific row index.
   *
   * @private
   * @param {Array} selectStartArea Selection area from which we start to create more comprehensive selection.
   * @param {number} rowIndex The row index into the selection will be added.
   */
  addSelectionFromStartAreaToSpecificRowIndex(e, n) {
    this.hot.selection.highlight.getFill().clear().add(this.hot._createCellCoords(e[0], e[1])).add(this.hot._createCellCoords(n, e[3])).commit();
  }
  /**
   * Sets selection based on passed corners.
   *
   * @private
   * @param {Array} cornersOfArea An array witch defines selection.
   */
  setSelection(e) {
    this.hot.selectCell(...Ke(e, (n) => Math.max(n, 0)), !1, !1);
  }
  /**
   * Try to select cells down to the last row in the left column and then returns if selection was applied.
   *
   * @private
   * @returns {boolean}
   */
  selectAdjacent() {
    const e = this.hot.getSelectedLast(), n = this.getIndexOfLastAdjacentFilledInRow(e);
    return n === -1 || n === void 0 ? !1 : (this.addSelectionFromStartAreaToSpecificRowIndex(e, n), !0);
  }
  /**
   * Resets selection of dragged area.
   *
   * @private
   */
  resetSelectionOfDraggedArea() {
    this.handleDraggedCells = 0, this.hot.selection.highlight.getFill().clear();
  }
  /**
   * Redraws borders.
   *
   * @private
   * @param {CellCoords} coords `CellCoords` coord object.
   */
  redrawBorders(e) {
    this.hot.selection.highlight.getFill().clear().add(this.hot.getSelectedRangeLast().from).add(this.hot.getSelectedRangeLast().to).add(e).commit(), this.hot.view.render();
  }
  /**
   * Get if mouse was dragged outside.
   *
   * @private
   * @param {MouseEvent} event `mousemove` event properties.
   * @returns {boolean}
   */
  getIfMouseWasDraggedOutside(e) {
    const {
      documentElement: n
    } = this.hot.rootDocument, i = gn(this.hot.table).top - (this.hot.rootWindow.pageYOffset || n.scrollTop) + Zt(this.hot.table), r = gn(this.hot.table).left - (this.hot.rootWindow.pageXOffset || n.scrollLeft) + It(this.hot.table);
    return e.clientY > i && e.clientX <= r;
  }
  /**
   * Bind the events used by the plugin.
   *
   * @private
   */
  registerEvents() {
    const {
      documentElement: e
    } = this.hot.rootDocument;
    this.eventManager.addEventListener(e, "mouseup", () => vc(kl, this, d8).call(this)), this.eventManager.addEventListener(e, "mousemove", (n) => vc(kl, this, f8).call(this, n));
  }
  /**
   * Clears mapped settings.
   *
   * @private
   */
  clearMappedSettings() {
    this.directions.length = 0, this.autoInsertRow = !1;
  }
  /**
   * Map settings.
   *
   * @private
   */
  mapSettings() {
    const e = J6(this.hot.getSettings().fillHandle);
    this.directions = e.directions, this.autoInsertRow = e.autoInsertRow;
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function u8() {
  this.selectAdjacent() && this.fillIn();
}
function c8() {
  this.handleDraggedCells = 1, this.mouseDownOnCellCorner = !0;
}
function h8(t) {
  this.mouseDownOnCellCorner && !this.hot.view.isMouseDown() && this.handleDraggedCells && (this.handleDraggedCells += 1, this.showBorder(t), this.addNewRowIfNeeded());
}
function d8() {
  this.handleDraggedCells && (this.handleDraggedCells > 1 && this.fillIn(), this.handleDraggedCells = 0, this.mouseDownOnCellCorner = !1);
}
function f8(t) {
  const e = this.getIfMouseWasDraggedOutside(t);
  this.addingStarted === !1 && this.handleDraggedCells > 0 && e ? (this.mouseDragOutside = !0, this.addingStarted = !0) : this.mouseDragOutside = !1, this.mouseDragOutside && this.autoInsertRow && this.addRow();
}
function g8(t, e) {
  Z1(t, e), e.add(t);
}
function m8(t, e, n) {
  Z1(t, e), e.set(t, n);
}
function Z1(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Ll(t, e, n) {
  return (e = p8(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function p8(t) {
  var e = w8(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function w8(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function A8(t, e, n) {
  return t.set(eo(t, e), n), n;
}
function ER(t, e) {
  return t.get(eo(t, e));
}
function eo(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const Wf = "autoRowSize", C8 = 40, v8 = "autoRowSize";
var Xh = /* @__PURE__ */ new WeakMap(), _o = /* @__PURE__ */ new WeakSet();
class dh extends at {
  static get PLUGIN_KEY() {
    return Wf;
  }
  static get PLUGIN_PRIORITY() {
    return C8;
  }
  static get SETTING_KEYS() {
    return !0;
  }
  static get CALCULATION_STEP() {
    return 50;
  }
  static get SYNC_CALCULATION_LIMIT() {
    return 500;
  }
  /**
   * Columns header's height cache.
   *
   * @private
   * @type {number}
   */
  constructor(e) {
    super(e), g8(this, _o), Ll(this, "headerHeight", null), Ll(this, "ghostTable", new SR(this.hot)), Ll(this, "samplesGenerator", new X1((n, i) => {
      const r = this.hot.toPhysicalRow(n), o = this.hot.toPhysicalColumn(i);
      if (this.hot.rowIndexMapper.isHidden(r) || this.hot.columnIndexMapper.isHidden(o) || n >= 0 && i >= 0 && this.hot.getCellMeta(n, i).hidden)
        return !1;
      let s;
      return n >= 0 ? s = this.hot.getDataAtCell(n, i) : n === -1 && (s = this.hot.getColHeader(i)), {
        value: s
      };
    })), Ll(this, "inProgress", !1), Ll(this, "measuredRows", 0), Ll(this, "rowHeightsMap", new Zs()), m8(this, Xh, []), this.hot.rowIndexMapper.registerMap(v8, this.rowHeightsMap), this.addHook("beforeRowResize", (n, i, r) => eo(_o, this, tM).call(this, n, i, r));
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link AutoRowSize#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    const e = this.hot.getSettings()[Wf];
    return e === !0 || Be(e);
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    this.enabled || (this.setSamplingOptions(), this.addHook("afterLoadData", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return eo(_o, e, b8).call(e, ...i);
    }), this.addHook("beforeChangeRender", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return eo(_o, e, R8).call(e, ...i);
    }), this.addHook("beforeColumnResize", () => this.recalculateAllRowsHeight()), this.addHook("afterFormulasValuesUpdate", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return eo(_o, e, T8).call(e, ...i);
    }), this.addHook("beforeRender", () => eo(_o, this, S8).call(this)), this.addHook("modifyRowHeight", (n, i) => this.getRowHeight(i, n)), this.addHook("init", () => eo(_o, this, E8).call(this)), this.addHook("modifyColumnHeaderHeight", () => this.getColumnHeaderHeight()), super.enablePlugin());
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.headerHeight = null, super.disablePlugin(), this.addHook("beforeRowResize", (e, n, i) => eo(_o, this, tM).call(this, e, n, i));
  }
  /**
   * Calculates heights for visible rows in the viewport only.
   */
  calculateVisibleRowsHeight() {
    if (!this.hot.countCols())
      return;
    const e = this.getFirstVisibleRow(), n = this.getLastVisibleRow();
    if (e === -1 || n === -1)
      return;
    const i = this.hot.renderCall;
    this.calculateRowsHeight({
      from: e,
      to: n
    }, void 0, i);
  }
  /**
   * Calculate a given rows height.
   *
   * @param {number|object} rowRange Row index or an object with `from` and `to` indexes as a range.
   * @param {number|object} colRange Column index or an object with `from` and `to` indexes as a range.
   * @param {boolean} [overwriteCache=false] If `true` the calculation will be processed regardless of whether the width exists in the cache.
   */
  calculateRowsHeight() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {
      from: 0,
      to: this.hot.countRows() - 1
    }, n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
      from: 0,
      to: this.hot.countCols() - 1
    }, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    const r = typeof e == "number" ? {
      from: e,
      to: e
    } : e, o = typeof n == "number" ? {
      from: n,
      to: n
    } : n;
    if (this.hot.getColHeader(0) !== null) {
      const s = this.samplesGenerator.generateRowSamples(-1, o);
      this.ghostTable.addColumnHeadersRow(s.get(-1));
    }
    Ae(r.from, r.to, (s) => {
      let a = this.hot.toPhysicalRow(s);
      a === null && (a = s), (i || this.rowHeightsMap.getValueAtIndex(a) === null) && this.samplesGenerator.generateRowSamples(s, o).forEach((u, c) => this.ghostTable.addRow(c, u));
    }), this.ghostTable.rows.length && (this.hot.batchExecution(() => {
      this.ghostTable.getHeights((s, a) => {
        s < 0 ? this.headerHeight = a : this.rowHeightsMap.setValueAtIndex(this.hot.toPhysicalRow(s), a);
      });
    }, !0), this.measuredRows = r.to + 1, this.ghostTable.clean());
  }
  /**
   * Calculate all rows heights. The calculated row will be cached in the {@link AutoRowSize#heights} property.
   * To retrieve height for specified row use {@link AutoRowSize#getRowHeight} method.
   *
   * @param {object|number} colRange Row index or an object with `from` and `to` properties which define row range.
   * @param {boolean} [overwriteCache] If `true` the calculation will be processed regardless of whether the width exists in the cache.
   */
  calculateAllRowsHeight() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {
      from: 0,
      to: this.hot.countCols() - 1
    }, n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1, i = 0;
    const r = this.hot.countRows() - 1;
    let o = null;
    this.inProgress = !0;
    const s = () => {
      if (!this.hot) {
        kh(o), this.inProgress = !1;
        return;
      }
      this.calculateRowsHeight({
        from: i,
        to: Math.min(i + dh.CALCULATION_STEP, r)
      }, e, n), i = i + dh.CALCULATION_STEP + 1, i < r ? o = xp(s) : (kh(o), this.inProgress = !1, this.hot.view.adjustElementsSize(), this.hot.view._wt.wtOverlays.inlineStartOverlay.needFullRender && this.hot.view._wt.wtOverlays.inlineStartOverlay.clone.draw());
    }, a = this.getSyncCalculationLimit();
    a >= 0 && (this.calculateRowsHeight({
      from: 0,
      to: a
    }, e, n), i = a + 1), i < r ? s() : (this.inProgress = !1, this.hot.view.adjustElementsSize());
  }
  /**
   * Sets the sampling options.
   *
   * @private
   */
  setSamplingOptions() {
    const e = this.hot.getSettings()[Wf], n = e && Bt(e, "samplingRatio") ? e.samplingRatio : void 0, i = e && Bt(e, "allowSampleDuplicates") ? e.allowSampleDuplicates : void 0;
    n && !isNaN(n) && this.samplesGenerator.setSampleCount(parseInt(n, 10)), i && this.samplesGenerator.setAllowDuplicates(i);
  }
  /**
   * Recalculates all rows height (overwrite cache values).
   */
  recalculateAllRowsHeight() {
    this.hot.view.isVisible() && this.calculateAllRowsHeight({
      from: 0,
      to: this.hot.countCols() - 1
    }, !0);
  }
  /**
   * Gets value which tells how many rows should be calculated synchronously (rest of the rows will be calculated
   * asynchronously). The limit is calculated based on `syncLimit` set to autoRowSize option (see {@link Options#autoRowSize}).
   *
   * @returns {number}
   */
  getSyncCalculationLimit() {
    const e = this.hot.getSettings()[Wf];
    let n = dh.SYNC_CALCULATION_LIMIT;
    const i = this.hot.countRows() - 1;
    return Be(e) && (n = e.syncLimit, XS(n) ? n = vb(i, n) : n >>= 0), Math.min(n, i);
  }
  /**
   * Get a row's height, as measured in the DOM.
   *
   * The height returned includes 1 px of the row's bottom border.
   *
   * Mind that this method is different from the
   * [`getRowHeight()`](@/api/core.md#getrowheight) method
   * of Handsontable's [Core](@/api/core.md).
   *
   * @param {number} row A visual row index.
   * @param {number} [defaultHeight] If no height is found, `defaultHeight` is returned instead.
   * @returns {number} The height of the specified row, in pixels.
   */
  getRowHeight(e, n) {
    const i = e < 0 ? this.headerHeight : this.rowHeightsMap.getValueAtIndex(this.hot.toPhysicalRow(e));
    let r = n;
    return i !== null && i > (n || 0) && (r = i), r;
  }
  /**
   * Get the calculated column header height.
   *
   * @returns {number|undefined}
   */
  getColumnHeaderHeight() {
    return this.headerHeight;
  }
  /**
   * Get the first visible row.
   *
   * @returns {number} Returns row index, -1 if table is not rendered or if there are no rows to base the the calculations on.
   */
  getFirstVisibleRow() {
    var e;
    return (e = this.hot.view.getFirstRenderedVisibleRow()) !== null && e !== void 0 ? e : -1;
  }
  /**
   * Gets the last visible row.
   *
   * @returns {number} Returns row index or -1 if table is not rendered.
   */
  getLastVisibleRow() {
    var e;
    return (e = this.hot.view.getLastRenderedVisibleRow()) !== null && e !== void 0 ? e : -1;
  }
  /**
   * Clears cache of calculated row heights. If you want to clear only selected rows pass an array with their indexes.
   * Otherwise whole cache will be cleared.
   *
   * @param {number[]} [physicalRows] List of physical row indexes to clear.
   */
  clearCache(e) {
    this.headerHeight = null, Array.isArray(e) ? this.hot.batchExecution(() => {
      e.forEach((n) => {
        this.rowHeightsMap.setValueAtIndex(n, null);
      });
    }, !0) : this.rowHeightsMap.clear();
  }
  /**
   * Clears cache by range.
   *
   * @param {object|number} range Row index or an object with `from` and `to` properties which define row range.
   */
  clearCacheByRange(e) {
    const {
      from: n,
      to: i
    } = typeof e == "number" ? {
      from: e,
      to: e
    } : e;
    this.hot.batchExecution(() => {
      Ae(Math.min(n, i), Math.max(n, i), (r) => {
        this.rowHeightsMap.setValueAtIndex(r, null);
      });
    }, !0);
  }
  /**
   * Checks if all heights were calculated. If not then return `true` (need recalculate).
   *
   * @returns {boolean}
   */
  isNeedRecalculate() {
    return !!this.rowHeightsMap.getValues().slice(0, this.measuredRows).filter((e) => e === null).length;
  }
  /**
   * On before view render listener.
   */
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    this.ghostTable.clean(), super.destroy();
  }
}
function y8(t) {
  const e = {
    from: 0,
    to: this.hot.countCols() - 1
  };
  t.forEach((n) => {
    this.samplesGenerator.generateRowSamples(n, e).forEach((r, o) => this.ghostTable.addRow(o, r));
  }), this.ghostTable.rows.length && (this.hot.batchExecution(() => {
    this.ghostTable.getHeights((n, i) => {
      const r = this.hot.toPhysicalRow(n);
      this.rowHeightsMap.setValueAtIndex(r, i);
    });
  }, !0), this.ghostTable.clean());
}
function S8() {
  this.calculateVisibleRowsHeight(), this.inProgress || (eo(_o, this, y8).call(this, ER(Xh, this)), A8(Xh, this, []));
}
function tM(t, e, n) {
  let i = t;
  return n && (this.calculateRowsHeight(e, void 0, !0), i = this.getRowHeight(e)), i;
}
function b8(t, e) {
  e || this.recalculateAllRowsHeight();
}
function R8(t) {
  const e = t.reduce((n, i) => {
    let [r] = i;
    return n.indexOf(r) === -1 && n.push(r), n;
  }, []);
  ER(Xh, this).push(...e);
}
function E8() {
  this.recalculateAllRowsHeight();
}
function T8(t) {
  const e = t.reduce((n, i) => {
    var r;
    const o = (r = i.address) === null || r === void 0 ? void 0 : r.row;
    if (Number.isInteger(o)) {
      const s = this.hot.toVisualRow(o);
      n.indexOf(s) === -1 && n.push(s);
    }
    return n;
  }, []);
  ER(Xh, this).push(...e);
}
const {
  getListWithInsertedItems: M8,
  getListWithRemovedItems: I8
} = s1("physicallyIndexed");
class O8 extends wl {
  constructor() {
    super((e) => e);
  }
  /**
   * Add values to list and reorganize.
   *
   * @private
   * @param {number} insertionIndex Position inside the list.
   * @param {Array} insertedIndexes List of inserted indexes.
   */
  insert(e, n) {
    const i = Kb(this.indexedValues, n);
    this.indexedValues = M8(i, e, n, this.initValueOrFn), super.insert(e, n);
  }
  /**
   * Remove values from the list and reorganize.
   *
   * @private
   * @param {Array} removedIndexes List of removed indexes.
   */
  remove(e) {
    const n = I8(this.indexedValues, e);
    this.indexedValues = zb(n, e), super.remove(e);
  }
}
const P8 = O8, {
  getListWithInsertedItems: x8,
  getListWithRemovedItems: H8
} = s1("physicallyIndexed");
class _8 extends wl {
  constructor() {
    super((e) => e);
  }
  /**
   * Add values to list and reorganize.
   *
   * @private
   * @param {number} insertionIndex Position inside the list.
   * @param {Array} insertedIndexes List of inserted indexes.
   */
  insert(e, n) {
    this.indexedValues = x8(this.indexedValues, e, n, (i, r) => this.getNextValue(r)), super.insert(e, n);
  }
  /**
   * Remove values from the list and reorganize.
   *
   * @private
   * @param {Array} removedIndexes List of removed indexes.
   */
  remove(e) {
    this.indexedValues = H8(this.indexedValues, e), super.remove(e);
  }
  /**
   * Get next values, which should be greater than actual maximum value in the list.
   *
   * @param {number} ordinalNumber Position in the list.
   * @returns {number}
   */
  getNextValue(e) {
    return Math.max(...this.getValues()) + 1 + e;
  }
}
const N8 = _8;
function D8(t, e) {
  k8(t, e), e.add(t);
}
function k8(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function L8(t, e, n) {
  return (e = B8(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function B8(t) {
  var e = F8(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function F8(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function V8(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const HC = "bindRowsWithHeaders", W8 = 210, $8 = "loose", U8 = /* @__PURE__ */ new Map([["loose", P8], ["strict", N8]]);
var nM = /* @__PURE__ */ new WeakSet();
class Y8 extends at {
  constructor() {
    super(...arguments), D8(this, nM), L8(this, "headerIndexes", null);
  }
  static get PLUGIN_KEY() {
    return HC;
  }
  static get PLUGIN_PRIORITY() {
    return W8;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link BindRowsWithHeaders#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[HC];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    if (this.enabled)
      return;
    let e = this.hot.getSettings()[HC];
    typeof e != "string" && (e = $8);
    const n = U8.get(e);
    this.headerIndexes = this.hot.rowIndexMapper.registerMap("bindRowsWithHeaders", new n()), this.addHook("modifyRowHeader", (i) => V8(nM, this, j8).call(this, i)), super.enablePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.hot.rowIndexMapper.unregisterMap("bindRowsWithHeaders"), super.disablePlugin();
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function j8(t) {
  return this.headerIndexes.getValueAtIndex(this.hot.toPhysicalRow(t));
}
function G8(t, e) {
  J1(t, e), e.add(t);
}
function z8(t, e, n) {
  J1(t, e), e.set(t, n);
}
function J1(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function iM(t, e, n) {
  return (e = K8(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function K8(t) {
  var e = Q8(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Q8(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function rM(t, e) {
  return t.get(du(t, e));
}
function _C(t, e, n) {
  return t.set(du(t, e), n), n;
}
function du(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const Kg = "collapsibleColumns", X8 = 290, q8 = ["nestedHeaders"], yu = "collapsibleIndicator", oM = Kg, sM = /* @__PURE__ */ new Map([["collapse", {
  hideColumn: !0,
  beforeHook: "beforeColumnCollapse",
  afterHook: "afterColumnCollapse"
}], ["expand", {
  hideColumn: !1,
  beforeHook: "beforeColumnExpand",
  afterHook: "afterColumnExpand"
}]]);
var Bl = /* @__PURE__ */ new WeakMap(), yc = /* @__PURE__ */ new WeakSet();
class Z8 extends at {
  constructor() {
    super(...arguments), G8(this, yc), iM(this, "nestedHeadersPlugin", null), iM(this, "headerStateManager", null), z8(this, Bl, null);
  }
  static get PLUGIN_KEY() {
    return Kg;
  }
  static get PLUGIN_PRIORITY() {
    return X8;
  }
  static get PLUGIN_DEPS() {
    return ["plugin:NestedHeaders"];
  }
  static get SETTING_KEYS() {
    return [Kg, ...q8];
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link CollapsibleColumns#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[Kg];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    if (this.enabled)
      return;
    const {
      nestedHeaders: n
    } = this.hot.getSettings();
    n || gt("You need to configure the Nested Headers plugin in order to use collapsible headers."), _C(Bl, this, this.hot.columnIndexMapper.createAndRegisterIndexMap(this.pluginName, "hiding")), this.nestedHeadersPlugin = this.hot.getPlugin("nestedHeaders"), this.headerStateManager = this.nestedHeadersPlugin.getStateManager(), this.addHook("init", () => du(yc, this, t9).call(this)), this.addHook("afterLoadData", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return du(yc, e, n9).call(e, ...r);
    }), this.addHook("afterGetColHeader", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return du(yc, e, J8).call(e, ...r);
    }), this.addHook("beforeOnCellMouseDown", (i, r, o) => du(yc, this, e9).call(this, i, r, o)), this.registerShortcuts(), super.enablePlugin(), this.updatePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *   - [`collapsibleColumns`](@/api/options.md#collapsiblecolumns)
   *   - [`nestedHeaders`](@/api/options.md#nestedheaders)
   */
  updatePlugin() {
    if (this.hot.view) {
      if (!this.nestedHeadersPlugin.detectedOverlappedHeaders) {
        const {
          collapsibleColumns: e
        } = this.hot.getSettings();
        typeof e == "boolean" ? this.headerStateManager.mapState((n) => ({
          collapsible: n.origColspan > 1
        })) : Array.isArray(e) && (this.headerStateManager.mapState(() => ({
          collapsible: !1
        })), this.headerStateManager.mergeStateWith(e));
      }
      super.updatePlugin();
    }
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.hot.columnIndexMapper.unregisterMap(this.pluginName), _C(Bl, this, null), this.nestedHeadersPlugin = null, this.unregisterShortcuts(), this.clearButtons(), super.disablePlugin();
  }
  /**
   * Register shortcuts responsible for toggling collapsible columns.
   *
   * @private
   */
  registerShortcuts() {
    this.hot.getShortcutManager().getContext("grid").addShortcut({
      keys: [["Enter"]],
      callback: () => {
        var e;
        const {
          row: n,
          col: i
        } = this.hot.getSelectedRangeLast().highlight, {
          collapsible: r,
          isCollapsed: o,
          columnIndex: s
        } = (e = this.headerStateManager.getHeaderTreeNodeData(n, i)) !== null && e !== void 0 ? e : {};
        if (r)
          return o ? this.expandSection({
            row: n,
            col: s
          }) : this.collapseSection({
            row: n,
            col: s
          }), !1;
      },
      runOnlyIf: () => {
        var e, n;
        return ((e = this.hot.getSelectedRangeLast()) === null || e === void 0 ? void 0 : e.isSingle()) && ((n = this.hot.getSelectedRangeLast()) === null || n === void 0 ? void 0 : n.highlight.isHeader());
      },
      group: oM,
      relativeToGroup: Js,
      position: "before"
    });
  }
  /**
   * Unregister shortcuts responsible for toggling collapsible columns.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("grid").removeShortcutsByGroup(oM);
  }
  /**
   * Clears the expand/collapse buttons.
   *
   * @private
   */
  clearButtons() {
    if (!this.hot.view)
      return;
    const e = this.hot.view._wt.getSetting("columnHeaders").length, n = this.hot.view._wt.wtTable.THEAD, i = this.hot.view._wt.wtOverlays.topOverlay.clone.wtTable.THEAD, r = this.hot.view._wt.wtOverlays.topInlineStartCornerOverlay ? this.hot.view._wt.wtOverlays.topInlineStartCornerOverlay.clone.wtTable.THEAD : null, o = function(s) {
      s && s.parentNode.removeChild(s);
    };
    Ae(0, e - 1, (s) => {
      const a = n.childNodes[s], l = i.childNodes[s], u = r ? r.childNodes[s] : null;
      Ae(0, a.childNodes.length - 1, (c) => {
        let h = a.childNodes[c].querySelector(`.${yu}`);
        o(h), l && l.childNodes[c] && (h = l.childNodes[c].querySelector(`.${yu}`), o(h)), r && u && u.childNodes[c] && (h = u.childNodes[c].querySelector(`.${yu}`), o(h));
      });
    });
  }
  /**
   * Expands section at the provided coords.
   *
   * @param {object} coords Contains coordinates information. (`coords.row`, `coords.col`).
   */
  expandSection(e) {
    this.toggleCollapsibleSection([e], "expand");
  }
  /**
   * Collapses section at the provided coords.
   *
   * @param {object} coords Contains coordinates information. (`coords.row`, `coords.col`).
   */
  collapseSection(e) {
    this.toggleCollapsibleSection([e], "collapse");
  }
  /**
   * Collapses or expand all collapsible sections, depending on the action parameter.
   *
   * @param {string} action 'collapse' or 'expand'.
   */
  toggleAllCollapsibleSections(e) {
    const n = this.headerStateManager.mapNodes((i) => {
      const {
        collapsible: r,
        origColspan: o,
        headerLevel: s,
        columnIndex: a,
        isCollapsed: l
      } = i;
      if (r === !0 && o > 1 && (l && e === "expand" || !l && e === "collapse"))
        return {
          row: this.headerStateManager.levelToRowCoords(s),
          col: a
        };
    });
    this.toggleCollapsibleSection(n, e);
  }
  /**
   * Collapses all collapsible sections.
   */
  collapseAll() {
    this.toggleAllCollapsibleSections("collapse");
  }
  /**
   * Expands all collapsible sections.
   */
  expandAll() {
    this.toggleAllCollapsibleSections("expand");
  }
  /**
   * Collapses/Expands a section.
   *
   * @param {Array} coords Array of coords - section coordinates.
   * @param {string} [action] Action definition ('collapse' or 'expand').
   * @fires Hooks#beforeColumnCollapse
   * @fires Hooks#beforeColumnExpand
   * @fires Hooks#afterColumnCollapse
   * @fires Hooks#afterColumnExpand
   */
  toggleCollapsibleSection(e, n) {
    if (!sM.has(n))
      throw new Error(`Unsupported action is passed (${n}).`);
    if (!Array.isArray(e))
      return;
    const i = Zo(e, (g) => {
      let {
        row: m
      } = g;
      return m < 0;
    });
    let r = i.length > 0;
    T(i, (g) => {
      var m;
      let {
        row: w,
        col: C
      } = g;
      const {
        collapsible: S,
        isCollapsed: b
      } = (m = this.headerStateManager.getHeaderSettings(w, C)) !== null && m !== void 0 ? m : {};
      if (!S || b && n === "collapse" || !b && n === "expand")
        return r = !1, !1;
    });
    const o = [], s = [];
    r && T(i, (g) => {
      let {
        row: m,
        col: w
      } = g;
      const {
        colspanCompensation: C,
        affectedColumns: S,
        rollbackModification: b
      } = this.headerStateManager.triggerNodeModification(n, m, w);
      C > 0 && (s.push(...S), o.push(b));
    });
    const a = this.getCollapsedColumns();
    let l = [];
    n === "collapse" ? l = KS([...a, ...s]) : n === "expand" && (l = Zo(a, (g) => !s.includes(g)));
    const u = sM.get(n);
    if (this.hot.runHooks(u.beforeHook, a, l, r) === !1) {
      T(o, (g) => {
        g();
      });
      return;
    }
    this.hot.batchExecution(() => {
      T(s, (g) => {
        rM(Bl, this).setValueAtIndex(this.hot.toPhysicalColumn(g), u.hideColumn);
      });
    }, !0);
    const h = this.getCollapsedColumns().length !== a.length, d = this.hot.getSelectedRangeLast();
    if (n === "collapse" && h && d) {
      const {
        row: g,
        col: m
      } = d.highlight;
      if ((this.hot.rowIndexMapper.isHidden(g) || this.hot.columnIndexMapper.isHidden(m)) && s.includes(m)) {
        const C = g >= 0 ? this.hot.rowIndexMapper.getNearestNotHiddenIndex(g, 1, !0) : g, S = m >= 0 ? this.hot.columnIndexMapper.getNearestNotHiddenIndex(m, 1, !0) : m;
        C !== null && S !== null && this.hot.selectCell(C, S);
      }
    }
    this.hot.runHooks(u.afterHook, a, l, r, h), this.hot.render(), this.hot.view.adjustElementsSize();
  }
  /**
   * Gets an array of physical indexes of collapsed columns.
   *
   * @private
   * @returns {number[]}
   */
  getCollapsedColumns() {
    return rM(Bl, this).getHiddenIndexes();
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    _C(Bl, this, null), super.destroy();
  }
}
function J8(t, e, n) {
  var i;
  const {
    collapsible: r,
    origColspan: o,
    isCollapsed: s
  } = (i = this.headerStateManager.getHeaderSettings(n, t)) !== null && i !== void 0 ? i : {}, a = r && o > 1 && t >= this.hot.getSettings().fixedColumnsStart, l = this.hot.getSettings().ariaTags;
  let u = e.querySelector(`.${yu}`);
  if (ml(e, [Gi("")[0]]), a)
    u || (u = this.hot.rootDocument.createElement("div"), Q(u, yu), e.querySelector("div:first-child").appendChild(u)), xe(u, ["collapsed", "expanded"]), s ? (Q(u, "collapsed"), Gs(u, "+"), l && Ee(e, ...Gi(!1))) : (Q(u, "expanded"), Gs(u, "-"), l && Ee(e, ...Gi(!0))), l && Ee(u, ...gl());
  else {
    var c;
    (c = u) === null || c === void 0 || c.remove();
  }
}
function e9(t, e) {
  Te(t.target, yu) && (Te(t.target, "expanded") ? (this.eventManager.fireEvent(t.target, "mouseup"), this.toggleCollapsibleSection([e], "collapse")) : Te(t.target, "collapsed") && (this.eventManager.fireEvent(t.target, "mouseup"), this.toggleCollapsibleSection([e], "expand")), Zi(t));
}
function t9() {
  this.updatePlugin();
}
function n9(t, e) {
  e || this.updatePlugin();
}
function wa(t, e, n) {
  return (e = i9(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function i9(t) {
  var e = r9(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function r9(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const o9 = ["sortEmptyCells", "indicator", "headerAction", "compareFunctionFactory"], s9 = !1, a9 = !0, l9 = !0;
class u9 {
  constructor(e, n) {
    wa(this, "hot", void 0), wa(this, "sortingStates", new Lp()), wa(this, "sortEmptyCells", s9), wa(this, "indicator", a9), wa(this, "headerAction", l9), wa(this, "compareFunctionFactory", void 0), wa(this, "mapName", void 0), this.hot = e, this.mapName = n, this.hot.columnIndexMapper.registerMap(n, this.sortingStates);
  }
  /**
   * Update column properties which affect the sorting result.
   *
   * **Note**: All column properties can be overwritten by {@link Options#columns} option.
   *
   * @param {object} allSortSettings Column sorting plugin's configuration object.
   */
  updateAllColumnsProperties(e) {
    Be(e) && Fe(e, (n, i) => {
      o9.includes(i) && (this[i] = n);
    });
  }
  /**
   * Get all column properties which affect the sorting result.
   *
   * @returns {object}
   */
  getAllColumnsProperties() {
    const e = {
      sortEmptyCells: this.sortEmptyCells,
      indicator: this.indicator,
      headerAction: this.headerAction
    };
    return typeof this.compareFunctionFactory == "function" && (e.compareFunctionFactory = this.compareFunctionFactory), e;
  }
  /**
   * Get sort order of column.
   *
   * @param {number} searchedColumn Visual column index.
   * @returns {string|undefined} Sort order (`asc` for ascending, `desc` for descending and undefined for not sorted).
   */
  getSortOrderOfColumn(e) {
    var n;
    return (n = this.sortingStates.getValueAtIndex(this.hot.toPhysicalColumn(e))) === null || n === void 0 ? void 0 : n.sortOrder;
  }
  /**
   * Get order of particular column in the states queue.
   *
   * @param {number} column Visual column index.
   * @returns {number}
   */
  getIndexOfColumnInSortQueue(e) {
    return e = this.hot.toPhysicalColumn(e), this.sortingStates.getEntries().findIndex((n) => {
      let [i] = n;
      return i === e;
    });
  }
  /**
   * Get number of sorted columns.
   *
   * @returns {number}
   */
  getNumberOfSortedColumns() {
    return this.sortingStates.getLength();
  }
  /**
   * Get if list of sorted columns is empty.
   *
   * @returns {boolean}
   */
  isListOfSortedColumnsEmpty() {
    return this.getNumberOfSortedColumns() === 0;
  }
  /**
   * Get if particular column is sorted.
   *
   * @param {number} column Visual column index.
   * @returns {boolean}
   */
  isColumnSorted(e) {
    return Be(this.sortingStates.getValueAtIndex(this.hot.toPhysicalColumn(e)));
  }
  /**
   * Queue of sort states containing sorted columns and their orders (Array of objects containing `column` and `sortOrder` properties).
   *
   * **Note**: Please keep in mind that returned objects expose **visual** column index under the `column` key.
   *
   * @returns {Array<object>}
   */
  getSortStates() {
    return this.sortingStates === null ? [] : this.sortingStates.getEntries().map((n) => {
      let [i, r] = n;
      return Xe({
        column: this.hot.toVisualColumn(i)
      }, r);
    });
  }
  /**
   * Get sort state for particular column. Object contains `column` and `sortOrder` properties.
   *
   * **Note**: Please keep in mind that returned objects expose **visual** column index under the `column` key.
   *
   * @param {number} column Visual column index.
   * @returns {object|undefined}
   */
  getColumnSortState(e) {
    const n = this.getSortOrderOfColumn(e);
    if (de(n))
      return {
        column: e,
        sortOrder: n
      };
  }
  /**
   * Set all column states.
   *
   * @param {Array} sortStates Sort states.
   */
  setSortStates(e) {
    this.sortingStates.clear();
    for (let n = 0; n < e.length; n += 1)
      this.sortingStates.setValueAtIndex(this.hot.toPhysicalColumn(e[n].column), {
        sortOrder: e[n].sortOrder
      });
  }
  /**
   * Destroy the state manager.
   */
  destroy() {
    this.hot.columnIndexMapper.unregisterMap(this.mapName), this.sortingStates = null;
  }
}
function eH(t, e, n) {
  const i = e.locale;
  return function(r, o) {
    const {
      sortEmptyCells: s
    } = n;
    return typeof r == "string" && (r = r.toLocaleLowerCase(i)), typeof o == "string" && (o = o.toLocaleLowerCase(i)), r === o ? ao : zt(r) ? zt(o) ? ao : s && t === "asc" ? Yt : jt : zt(o) ? s && t === "asc" ? jt : Yt : isNaN(r) && !isNaN(o) ? t === "asc" ? jt : Yt : !isNaN(r) && isNaN(o) || (isNaN(r) || isNaN(o) || (r = parseFloat(r), o = parseFloat(o)), r < o) ? t === "asc" ? Yt : jt : r > o ? t === "asc" ? jt : Yt : ao;
  };
}
const tH = "default";
function c9(t, e, n) {
  return function(i, r) {
    const o = parseFloat(i), s = parseFloat(r), {
      sortEmptyCells: a
    } = n;
    if (o === s || isNaN(o) && isNaN(s))
      return ao;
    if (a) {
      if (zt(i))
        return t === "asc" ? Yt : jt;
      if (zt(r))
        return t === "asc" ? jt : Yt;
    }
    return isNaN(o) ? jt : isNaN(s) ? Yt : o < s ? t === "asc" ? Yt : jt : o > s ? t === "asc" ? jt : Yt : ao;
  };
}
const h9 = "numeric";
function d9(t, e, n) {
  const i = e.checkedTemplate, r = e.uncheckedTemplate, {
    sortEmptyCells: o
  } = n;
  return function(s, a) {
    const l = zt(s), u = zt(a), c = l ? r : s, h = u ? r : a, d = c === r || c === i, g = h === r || h === i;
    if (o === !1) {
      if (l && u === !1)
        return jt;
      if (l === !1 && u)
        return Yt;
    }
    return d === !1 && g ? t === "asc" ? Yt : jt : d && g === !1 ? t === "asc" ? jt : Yt : d === !1 && g === !1 ? eH(t, e, n)(s, a) : c === r && h === i ? t === "asc" ? Yt : jt : c === i && h === r ? t === "asc" ? jt : Yt : ao;
  };
}
const f9 = "checkbox";
function g9(t, e, n) {
  return aH(t, e.dateFormat, n);
}
const m9 = "date";
function p9(t, e, n) {
  return aH(t, e.timeFormat, n);
}
const w9 = "time", {
  register: Pd,
  getItem: aM,
  hasItem: A9
} = Or("sorting.compareFunctionFactory"), {
  register: nH,
  getItem: C9
} = Or("sorting.mainSortComparator");
function iH(t) {
  return A9(t) ? aM(t) : aM(tH);
}
Pd(f9, d9);
Pd(m9, g9);
Pd(tH, eH);
Pd(h9, c9);
Pd(w9, p9);
const ao = 0, Yt = -1, jt = 1;
function v9(t, e) {
  const n = C9(e);
  for (var i = arguments.length, r = new Array(i > 2 ? i - 2 : 0), o = 2; o < i; o++)
    r[o - 2] = arguments[o];
  t.sort(n(...r));
}
const Im = "asc", Om = "desc", rH = "colHeader";
function y9(t) {
  if (Be(t) === !1)
    return !1;
  const {
    column: e,
    sortOrder: n
  } = t;
  return Number.isInteger(e) && [Im, Om].includes(n);
}
function S9(t) {
  if (t.some((n) => y9(n) === !1))
    return !1;
  const e = t.map((n) => {
    let {
      column: i
    } = n;
    return i;
  });
  return new Set(e).size === e.length;
}
function lM(t) {
  if (t !== Om)
    return t === Im ? Om : Im;
}
function oH(t) {
  return t.querySelector(`.${rH}`);
}
function sH(t, e) {
  if (t < 0 || !e.parentNode)
    return !1;
  const n = e.parentNode.parentNode.childNodes;
  return Array.from(n).indexOf(e.parentNode) - n.length === -1;
}
function TR(t, e, n) {
  return t === -1 && e >= 0 && zs(n) === !1;
}
function aH(t, e, n) {
  return function(i, r) {
    const {
      sortEmptyCells: o
    } = n;
    if (i === r)
      return ao;
    if (zt(i))
      return zt(r) ? ao : o && t === "asc" ? Yt : jt;
    if (zt(r))
      return o && t === "asc" ? jt : Yt;
    const s = X(i, e), a = X(r, e);
    return s.isValid() ? a.isValid() ? a.isAfter(s) ? t === "asc" ? Yt : jt : a.isBefore(s) ? t === "asc" ? jt : Yt : ao : Yt : jt;
  };
}
const b9 = "ascending", R9 = "descending", lH = "indicatorDisabled", uH = "columnSorting", cH = "sortAction", hH = /* @__PURE__ */ new Map([[Im, b9], [Om, R9]]);
function E9(t, e, n, i) {
  const r = [uH];
  if (i && r.push(cH), n === !1)
    return r.push(lH), r;
  const o = t.getSortOrderOfColumn(e);
  return de(o) && r.push(hH.get(o)), r;
}
function T9() {
  return Array.from(hH.values()).concat(cH, lH, uH);
}
function M9(t, e) {
  return function(n, i) {
    const [, ...r] = n, [, ...o] = i;
    return function(a) {
      const l = t[a], u = e[a], c = r[a], h = o[a], d = u.columnSorting;
      return (d.compareFunctionFactory ? d.compareFunctionFactory : iH(u.type))(l, u, d)(c, h);
    }(0);
  };
}
function I9(t, e) {
  O9(t, e), e.add(t);
}
function O9(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function $f(t, e, n) {
  return (e = P9(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function P9(t) {
  var e = x9(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function x9(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function su(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const Pm = "columnSorting", H9 = 50, xm = "append", _9 = "replace", uM = Pm;
nH(Pm, M9);
he.getSingleton().register("beforeColumnSort");
he.getSingleton().register("afterColumnSort");
var Va = /* @__PURE__ */ new WeakSet();
class dH extends at {
  constructor() {
    super(...arguments), I9(this, Va), $f(this, "columnStatesManager", null), $f(this, "columnMetaCache", null), $f(this, "pluginKey", Pm), $f(this, "indexesSequenceCache", null);
  }
  static get PLUGIN_KEY() {
    return Pm;
  }
  static get PLUGIN_PRIORITY() {
    return H9;
  }
  /**
   * Checks if the plugin is enabled in the Handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ColumnSorting#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[this.pluginKey];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    this.enabled || (this.columnStatesManager = new u9(this.hot, `${this.pluginKey}.sortingStates`), this.columnMetaCache = new Zs((n) => {
      let i = this.hot.toVisualColumn(n);
      return i === null && (i = n), this.getMergedPluginSettings(i);
    }), this.hot.columnIndexMapper.registerMap(`${this.pluginKey}.columnMeta`, this.columnMetaCache), this.addHook("afterGetColHeader", (n, i) => su(Va, this, N9).call(this, n, i)), this.addHook("beforeOnCellMouseDown", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return su(Va, e, k9).call(e, ...i);
    }), this.addHook("afterOnCellMouseDown", (n, i) => this.onAfterOnCellMouseDown(n, i)), this.addHook("afterInit", () => su(Va, this, By).call(this)), this.addHook("afterLoadData", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return su(Va, e, D9).call(e, ...i);
    }), this.hot.view && su(Va, this, By).call(this), this.registerShortcuts(), super.enablePlugin());
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    const e = (n, i) => {
      const r = oH(i);
      sH(n, i) === !1 || r === null || this.updateHeaderClasses(r);
    };
    this.hot.addHook("afterGetColHeader", e), this.hot.addHookOnce("afterViewRender", () => {
      this.hot.removeHook("afterGetColHeader", e);
    }), this.hot.batchExecution(() => {
      this.indexesSequenceCache !== null && (this.hot.rowIndexMapper.setIndexesSequence(this.indexesSequenceCache.getValues()), this.hot.rowIndexMapper.unregisterMap(this.pluginKey), this.indexesSequenceCache = null);
    }, !0), this.hot.columnIndexMapper.unregisterMap(`${this.pluginKey}.columnMeta`), this.columnStatesManager.destroy(), this.columnMetaCache = null, this.columnStatesManager = null, this.unregisterShortcuts(), super.disablePlugin();
  }
  /**
   * Register shortcuts responsible for toggling column sorting functionality.
   *
   * @private
   */
  registerShortcuts() {
    this.hot.getShortcutManager().getContext("grid").addShortcut({
      keys: [["Enter"]],
      callback: () => {
        const {
          highlight: e
        } = this.hot.getSelectedRangeLast();
        return this.sort(this.getColumnNextConfig(e.col)), !1;
      },
      runOnlyIf: () => {
        var e, n;
        const i = (e = this.hot.getSelectedRangeLast()) === null || e === void 0 ? void 0 : e.highlight;
        return i && ((n = this.hot.getSelectedRangeLast()) === null || n === void 0 ? void 0 : n.isSingle()) && this.hot.selection.isCellVisible(i) && i.row === -1 && i.col >= 0;
      },
      relativeToGroup: Js,
      position: "before",
      group: uM
    });
  }
  /**
   * Unregister shortcuts responsible for toggling column sorting functionality.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("grid").removeShortcutsByGroup(uM);
  }
  // DIFF - MultiColumnSorting & ColumnSorting: changed function documentation.
  /**
   * Sorts the table by chosen columns and orders.
   *
   * @param {undefined|object} sortConfig Single column sort configuration. The configuration object contains `column` and `sortOrder` properties.
   * First of them contains visual column index, the second one contains sort order (`asc` for ascending, `desc` for descending).
   *
   * **Note**: Please keep in mind that every call of `sort` function set an entirely new sort order. Previous sort configs aren't preserved.
   *
   * @example
   * ```js
   * // sort ascending first visual column
   * hot.getPlugin('columnSorting').sort({ column: 0, sortOrder: 'asc' });
   * ```
   *
   * @fires Hooks#beforeColumnSort
   * @fires Hooks#afterColumnSort
   */
  sort(e) {
    const n = this.getSortConfig(), i = this.getNormalizedSortConfigs(e), r = this.areValidSortConfigs(i);
    this.hot.runHooks("beforeColumnSort", n, i, r) !== !1 && (n.length === 0 && this.indexesSequenceCache === null && (this.indexesSequenceCache = this.hot.rowIndexMapper.registerMap(this.pluginKey, new a1()), this.indexesSequenceCache.setValues(this.hot.rowIndexMapper.getIndexesSequence())), r && (this.columnStatesManager.setSortStates(i), this.sortByPresetSortStates(i), this.saveAllSortSettings(i)), this.hot.runHooks("afterColumnSort", n, r ? i : n, r), r && (this.hot.render(), this.hot.forceFullRender = !1, this.hot.view.render()));
  }
  /**
   * Clear the sort performed on the table.
   */
  clearSort() {
    this.sort([]);
  }
  /**
   * Checks if the table is sorted (any column have to be sorted).
   *
   * @returns {boolean}
   */
  isSorted() {
    return this.enabled && !this.columnStatesManager.isListOfSortedColumnsEmpty();
  }
  /**
   * Get sort configuration for particular column or for all sorted columns. Objects contain `column` and `sortOrder` properties.
   *
   * **Note**: Please keep in mind that returned objects expose **visual** column index under the `column` key. They are handled by the `sort` function.
   *
   * @param {number} [column] Visual column index.
   * @returns {undefined|object|Array}
   */
  getSortConfig(e) {
    return de(e) ? this.columnStatesManager.getColumnSortState(e) : this.columnStatesManager.getSortStates();
  }
  /**
   * @description
   * Warn: Useful mainly for providing server side sort implementation (see in the example below). It doesn't sort the data set. It just sets sort configuration for all sorted columns.
   * Note: Please keep in mind that this method doesn't re-render the table.
   *
   * @example
   * ```js
   * beforeColumnSort: function(currentSortConfig, destinationSortConfigs) {
   *   const columnSortPlugin = this.getPlugin('columnSorting');
   *
   *   columnSortPlugin.setSortConfig(destinationSortConfigs);
   *
   *   // const newData = ... // Calculated data set, ie. from an AJAX call.
   *
   *   this.loadData(newData); // Load new data set and re-render the table.
   *
   *   return false; // The blockade for the default sort action.
   * }
   * ```
   *
   * @param {undefined|object|Array} sortConfig Single column sort configuration or full sort configuration (for all sorted columns).
   * The configuration object contains `column` and `sortOrder` properties. First of them contains visual column index, the second one contains
   * sort order (`asc` for ascending, `desc` for descending).
   */
  setSortConfig(e) {
    const n = this.getNormalizedSortConfigs(e);
    this.areValidSortConfigs(n) && this.columnStatesManager.setSortStates(n);
  }
  /**
   * Get normalized sort configs.
   *
   * @private
   * @param {object|Array} [sortConfig=[]] Single column sort configuration or full sort configuration (for all sorted columns).
   * The configuration object contains `column` and `sortOrder` properties. First of them contains visual column index, the second one contains
   * sort order (`asc` for ascending, `desc` for descending).
   * @returns {Array}
   */
  getNormalizedSortConfigs() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : [];
    return Array.isArray(e) ? e.slice(0, 1) : [e];
  }
  /**
   * Get if sort configs are valid.
   *
   * @private
   * @param {Array} sortConfigs Sort configuration for all sorted columns. Objects contain `column` and `sortOrder` properties.
   * @returns {boolean}
   */
  areValidSortConfigs(e) {
    const n = this.hot.countCols();
    return S9(e) && e.every((i) => {
      let {
        column: r
      } = i;
      return r <= n && r >= 0;
    });
  }
  /**
   * Saves all sorting settings. Saving works only when {@link Options#persistentState} option is enabled.
   *
   * @param {Array} sortConfigs Sort configuration for all sorted columns. Objects contain `column` and `sortOrder` properties.
   *
   * @private
   * @fires Hooks#persistentStateSave
   */
  saveAllSortSettings(e) {
    const n = this.columnStatesManager.getAllColumnsProperties(), i = (r) => {
      let a = r, {
        column: o
      } = a, s = Wn(a, [
        "column"
      ]);
      return Xe({
        column: this.hot.toPhysicalColumn(o)
      }, s);
    };
    n.initialConfig = Ke(e, i), this.hot.runHooks("persistentStateSave", "columnSorting", n);
  }
  /**
   * Get all saved sorting settings. Loading works only when {@link Options#persistentState} option is enabled.
   *
   * @private
   * @returns {object} Previously saved sort settings.
   *
   * @fires Hooks#persistentStateLoad
   */
  getAllSavedSortSettings() {
    const e = {};
    this.hot.runHooks("persistentStateLoad", "columnSorting", e);
    const n = e.value, i = (r) => {
      let a = r, {
        column: o
      } = a, s = Wn(a, [
        "column"
      ]);
      return Xe({
        column: this.hot.toVisualColumn(o)
      }, s);
    };
    return de(n) && Array.isArray(n.initialConfig) && (n.initialConfig = Ke(n.initialConfig, i)), n;
  }
  /**
   * Get next sort configuration for particular column. Object contain `column` and `sortOrder` properties.
   *
   * **Note**: Please keep in mind that returned object expose **visual** column index under the `column` key.
   *
   * @private
   * @param {number} column Visual column index.
   * @returns {undefined|object}
   */
  getColumnNextConfig(e) {
    const n = this.columnStatesManager.getSortOrderOfColumn(e);
    if (de(n)) {
      const r = lM(n);
      return de(r) ? {
        column: e,
        sortOrder: r
      } : void 0;
    }
    const i = this.hot.countCols();
    if (Number.isInteger(e) && e >= 0 && e < i)
      return {
        column: e,
        sortOrder: lM()
      };
  }
  /**
   * Get sort configuration with "next order" for particular column.
   *
   * @private
   * @param {number} columnToChange Visual column index of column which order will be changed.
   * @param {string} strategyId ID of strategy. Possible values: 'append' and 'replace'. The first one
   * change order of particular column and change it's position in the sort queue to the last one. The second one
   * just change order of particular column.
   *
   * **Note**: Please keep in mind that returned objects expose **visual** column index under the `column` key.
   *
   * @returns {Array}
   */
  getNextSortConfig(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : xm;
    const i = this.columnStatesManager.getIndexOfColumnInSortQueue(e), r = i !== -1, o = this.getSortConfig(), s = this.getColumnNextConfig(e);
    if (r) {
      if (Mt(s))
        return [...o.slice(0, i), ...o.slice(i + 1)];
      if (n === xm)
        return [...o.slice(0, i), ...o.slice(i + 1), s];
      if (n === _9)
        return [...o.slice(0, i), s, ...o.slice(i + 1)];
    }
    return de(s) ? o.concat(s) : o;
  }
  /**
   * Get plugin's column config for the specified column index.
   *
   * @private
   * @param {object} columnConfig Configuration inside `columns` property for the specified column index.
   * @returns {object}
   */
  getPluginColumnConfig(e) {
    if (Be(e)) {
      const n = e[this.pluginKey];
      if (Be(n))
        return n;
    }
    return {};
  }
  /**
   * Get plugin settings related properties, properly merged from cascade settings.
   *
   * @private
   * @param {number} column Visual column index.
   * @returns {object}
   */
  getMergedPluginSettings(e) {
    const n = this.hot.getSettings()[this.pluginKey], i = this.columnStatesManager.getAllColumnsProperties(), r = this.hot.getCellMeta(0, e), o = Object.getPrototypeOf(r);
    return Array.isArray(o.columns) ? Object.assign(i, n, this.getPluginColumnConfig(o.columns[e])) : Et(o.columns) ? Object.assign(i, n, this.getPluginColumnConfig(o.columns(e))) : Object.assign(i, n);
  }
  /**
   * Get copy of settings for first cell in the column.
   *
   * @private
   * @param {number} column Visual column index.
   * @returns {object}
   */
  // TODO: Workaround. Inheriting of non-primitive cell meta values doesn't work. Instead of getting properties from column meta we call this function.
  // TODO: Remove test named: "should not break the dataset when inserted new row" (#5431).
  getFirstCellSettings(e) {
    const n = this.hot.getCellMeta(0, e), i = Object.create(n);
    return i[this.pluginKey] = this.columnMetaCache.getValueAtIndex(this.hot.toPhysicalColumn(e)), i;
  }
  /**
   * Get number of rows which should be sorted.
   *
   * @private
   * @param {number} numberOfRows Total number of displayed rows.
   * @returns {number}
   */
  getNumberOfRowsToSort(e) {
    const n = this.hot.getSettings();
    return n.maxRows <= e ? n.maxRows : e - n.minSpareRows;
  }
  /**
   * Performs the sorting using a stable sort function basing on internal state of sorting.
   *
   * @param {Array} sortConfigs Sort configuration for all sorted columns. Objects contain `column` and `sortOrder` properties.
   * @private
   */
  sortByPresetSortStates(e) {
    if (this.hot.rowIndexMapper.setIndexesSequence(this.indexesSequenceCache.getValues()), e.length === 0)
      return;
    const n = [], i = this.hot.countRows(), r = (u) => Ke(e, (c) => this.hot.getDataAtCell(u, c.column));
    for (let u = 0; u < this.getNumberOfRowsToSort(i); u += 1)
      n.push([this.hot.toPhysicalRow(u)].concat(r(u)));
    const o = Ke(n, (u) => u[0]);
    v9(n, this.pluginKey, Ke(e, (u) => u.sortOrder), Ke(e, (u) => this.getFirstCellSettings(u.column)));
    for (let u = n.length; u < i; u += 1)
      n.push([u].concat(r(u)));
    const s = Ke(n, (u) => u[0]), a = new Map(Ke(o, (u, c) => [u, s[c]])), l = Ke(this.hot.rowIndexMapper.getIndexesSequence(), (u) => a.has(u) ? a.get(u) : u);
    this.hot.rowIndexMapper.setIndexesSequence(l);
  }
  /**
   * Sort the table by provided configuration.
   *
   * @private
   * @param {object} allSortSettings All sort config settings. Object may contain `initialConfig`, `indicator`,
   * `sortEmptyCells`, `headerAction` and `compareFunctionFactory` properties.
   */
  sortBySettings(e) {
    if (Be(e)) {
      this.columnStatesManager.updateAllColumnsProperties(e);
      const n = e.initialConfig;
      (Array.isArray(n) || Be(n)) && this.sort(n);
    } else
      this.hot.render();
  }
  /**
   * Callback for the `onAfterGetColHeader` hook. Adds column sorting CSS classes.
   *
   * @param {number} column Visual column index.
   * @param {Element} TH TH HTML element.
   */
  /**
   * Update header classes.
   *
   * @private
   * @param {HTMLElement} headerSpanElement Header span element.
   * @param {...*} args Extra arguments for helpers.
   */
  updateHeaderClasses(e) {
    if (xe(e, T9()), this.enabled !== !1) {
      for (var n = arguments.length, i = new Array(n > 1 ? n - 1 : 0), r = 1; r < n; r++)
        i[r - 1] = arguments[r];
      Q(e, E9(...i));
    }
  }
  /**
   * Overwriting base plugin's `onUpdateSettings` method. Please keep in mind that `onAfterUpdateSettings` isn't called
   * for `updateSettings` in specific situations.
   *
   * @private
   * @param {object} newSettings New settings object.
   */
  onUpdateSettings(e) {
    super.onUpdateSettings(), this.columnMetaCache !== null && this.columnMetaCache.init(this.hot.columnIndexMapper.getNumberOfIndexes()), de(e[this.pluginKey]) && this.sortBySettings(e[this.pluginKey]);
  }
  /**
   * Callback for the `afterLoadData` hook.
   *
   * @param {boolean} initialLoad Flag that determines whether the data has been loaded during the initialization.
   */
  /**
   * Indicates if clickable header was clicked.
   *
   * @private
   * @param {MouseEvent} event The `mousedown` event.
   * @param {number} column Visual column index.
   * @returns {boolean}
   */
  wasClickableHeaderClicked(e, n) {
    return this.getFirstCellSettings(n)[this.pluginKey].headerAction && Te(e.target, rH);
  }
  /**
   * Changes the behavior of selection / dragging.
   *
   * @param {MouseEvent} event The `mousedown` event.
   * @param {CellCoords} coords Visual coordinates.
   * @param {HTMLElement} TD The cell element.
   * @param {object} controller An object with properties `row`, `column` and `cell`. Each property contains
   *                            a boolean value that allows or disallows changing the selection for that particular area.
   */
  /**
   * Callback for the `onAfterOnCellMouseDown` hook.
   *
   * @private
   * @param {Event} event Event which are provided by hook.
   * @param {CellCoords} coords Visual coords of the selected cell.
   */
  onAfterOnCellMouseDown(e, n) {
    if (TR(n.row, n.col, e) !== !1 && this.wasClickableHeaderClicked(e, n.col)) {
      this.hot.getShortcutManager().isCtrlPressed() && (this.hot.deselectCell(), this.hot.selectColumns(n.col));
      const i = this.hot.getActiveEditor(), r = this.getColumnNextConfig(n.col);
      i != null && i.isOpened() && this.hot.getCellValidator(i.row, i.col) ? this.hot.addHookOnce("postAfterValidate", () => {
        this.sort(r);
      }) : this.sort(r);
    }
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    var e;
    (e = this.columnStatesManager) === null || e === void 0 || e.destroy(), super.destroy();
  }
}
function By() {
  const t = this.getAllSavedSortSettings();
  if (Be(t))
    this.sortBySettings(t);
  else {
    const e = this.hot.getSettings()[this.pluginKey];
    this.sortBySettings(e);
  }
}
function N9(t, e) {
  const n = oH(e);
  if (sH(t, e) === !1 || n === null)
    return;
  const i = this.getFirstCellSettings(t)[this.pluginKey], r = i.indicator, o = i.headerAction;
  if (this.updateHeaderClasses(n, this.columnStatesManager, t, r, o), this.hot.getSettings().ariaTags) {
    const s = this.columnStatesManager.getSortOrderOfColumn(t);
    Ee(e, ...IG(s ? `${s}ending` : "none"));
  }
}
function D9(t) {
  t === !0 && this.hot.view && su(Va, this, By).call(this);
}
function k9(t, e, n, i) {
  TR(e.row, e.col, t) !== !1 && this.wasClickableHeaderClicked(t, e.col) && this.hot.getShortcutManager().isCtrlPressed() && (i.column = !0);
}
function Aa(t, e, n) {
  return (e = L9(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function L9(t) {
  var e = B9(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function B9(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class F9 {
  constructor(e, n) {
    Aa(this, "plugin", void 0), Aa(this, "hot", void 0), Aa(this, "endpoints", []), Aa(this, "settings", void 0), Aa(this, "settingsType", "array"), Aa(this, "currentEndpoint", null), Aa(this, "cellsToSetCache", []), this.plugin = e, this.hot = this.plugin.hot, this.settings = n;
  }
  /**
   * Initialize the endpoints provided in the settings.
   */
  initEndpoints() {
    this.endpoints = this.parseSettings(), this.refreshAllEndpoints();
  }
  /**
   * Get a single endpoint object.
   *
   * @param {number} index Index of the endpoint.
   * @returns {object}
   */
  getEndpoint(e) {
    return this.settingsType === "function" ? this.fillMissingEndpointData(this.settings)[e] : this.endpoints[e];
  }
  /**
   * Get an array with all the endpoints.
   *
   * @returns {Array}
   */
  getAllEndpoints() {
    return this.settingsType === "function" ? this.fillMissingEndpointData(this.settings) : this.endpoints;
  }
  /**
   * Used to fill the blanks in the endpoint data provided by a settings function.
   *
   * @private
   * @param {Function} func Function provided in the HOT settings.
   * @returns {Array} An array of endpoints.
   */
  fillMissingEndpointData(e) {
    return this.parseSettings(e.call(this));
  }
  /**
   * Parse plugin's settings.
   *
   * @param {Array} settings The settings array.
   * @returns {object[]}
   */
  parseSettings(e) {
    const n = [];
    let i = e;
    if (!i && typeof this.settings == "function") {
      this.settingsType = "function";
      return;
    }
    return i || (i = this.settings), T(i, (r) => {
      const o = {};
      this.assignSetting(r, o, "ranges", [[0, this.hot.countRows() - 1]]), this.assignSetting(r, o, "reversedRowCoords", !1), this.assignSetting(r, o, "destinationRow", new Error(`
        You must provide a destination row for the Column Summary plugin in order to work properly!
      `)), this.assignSetting(r, o, "destinationColumn", new Error(`
        You must provide a destination column for the Column Summary plugin in order to work properly!
      `)), this.assignSetting(r, o, "sourceColumn", r.destinationColumn), this.assignSetting(r, o, "type", "sum"), this.assignSetting(r, o, "forceNumeric", !1), this.assignSetting(r, o, "suppressDataTypeErrors", !0), this.assignSetting(r, o, "customFunction", null), this.assignSetting(r, o, "readOnly", !0), this.assignSetting(r, o, "roundFloat", !1), n.push(o);
    }), n;
  }
  /**
   * Setter for the internal setting objects.
   *
   * @param {object} settings Object with the settings.
   * @param {object} endpoint Contains information about the endpoint for the the calculation.
   * @param {string} name Settings name.
   * @param {object} defaultValue Default value for the settings.
   */
  assignSetting(e, n, i, r) {
    if (i === "ranges" && e[i] === void 0) {
      n[i] = r;
      return;
    } else if (i === "ranges" && e[i].length === 0)
      return;
    if (e[i] === void 0) {
      if (r instanceof Error)
        throw r;
      n[i] = r;
    } else
      i === "destinationRow" && n.reversedRowCoords ? n[i] = this.hot.countRows() - e[i] - 1 : n[i] = e[i];
  }
  /**
   * Resets the endpoint setup before the structure alteration (like inserting or removing rows/columns). Used for settings provided as a function.
   *
   * @private
   * @param {string} action Type of the action performed.
   * @param {number} index Row/column index.
   * @param {number} number Number of rows/columns added/removed.
   */
  resetSetupBeforeStructureAlteration(e, n, i) {
    if (this.settingsType !== "function")
      return;
    const r = e.indexOf("row") > -1 ? "row" : "col", o = this.getAllEndpoints();
    T(o, (s) => {
      r === "row" && s.destinationRow >= n && (e === "insert_row" ? s.alterRowOffset = i : e === "remove_row" && (s.alterRowOffset = -1 * i)), r === "col" && s.destinationColumn >= n && (e === "insert_col" ? s.alterColumnOffset = i : e === "remove_col" && (s.alterColumnOffset = -1 * i));
    }), this.resetAllEndpoints(o, !1);
  }
  /**
   * AfterCreateRow/afterCreateRow/afterRemoveRow/afterRemoveCol hook callback. Reset and reenables the summary functionality
   * after changing the table structure.
   *
   * @private
   * @param {string} action Type of the action performed.
   * @param {number} index Row/column index.
   * @param {number} number Number of rows/columns added/removed.
   * @param {Array} [logicRows] Array of the logical indexes.
   * @param {string} [source] Source of change.
   * @param {boolean} [forceRefresh] `true` of the endpoints should refresh after completing the function.
   */
  resetSetupAfterStructureAlteration(e, n, i, r, o) {
    let s = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : !0;
    if (this.settingsType === "function") {
      const d = () => (this.hot.removeHook("beforeViewRender", d), this.refreshAllEndpoints());
      this.hot.addHookOnce("beforeViewRender", d);
      return;
    }
    const a = e.indexOf("row") > -1 ? "row" : "col", l = e.indexOf("remove") > -1 ? -1 : 1, u = this.getAllEndpoints(), c = e.indexOf("move_row") === 0, h = n;
    T(u, (d) => {
      a === "row" && d.destinationRow >= h && (d.alterRowOffset = l * i), a === "col" && d.destinationColumn >= h && (d.alterColumnOffset = l * i);
    }), this.resetAllEndpoints(u, !c), c ? T(u, (d) => {
      this.extendEndpointRanges(d, h, r[0], r.length), this.recreatePhysicalRanges(d), this.clearOffsetInformation(d);
    }) : T(u, (d) => {
      this.shiftEndpointCoordinates(d, h);
    }), s && this.refreshAllEndpoints();
  }
  /**
   * Clear the offset information from the endpoint object.
   *
   * @private
   * @param {object} endpoint And endpoint object.
   */
  clearOffsetInformation(e) {
    e.alterRowOffset = void 0, e.alterColumnOffset = void 0;
  }
  /**
   * Extend the row ranges for the provided endpoint.
   *
   * @private
   * @param {object} endpoint The endpoint object.
   * @param {number} placeOfAlteration Index of the row where the alteration takes place.
   * @param {number} previousPosition Previous endpoint result position.
   * @param {number} offset Offset generated by the alteration.
   */
  extendEndpointRanges(e, n, i, r) {
    T(e.ranges, (o) => {
      o[1] && (n >= o[0] && n <= o[1] ? i > o[1] ? o[1] += r : i < o[0] && (o[0] -= r) : i >= o[0] && i <= o[1] && (o[1] -= r, n <= o[0] && (o[0] += 1, o[1] += 1)));
    });
  }
  /**
   * Recreate the physical ranges for the provided endpoint. Used (for example) when a row gets moved and extends an existing range.
   *
   * @private
   * @param {object} endpoint An endpoint object.
   */
  recreatePhysicalRanges(e) {
    const n = e.ranges, i = [], r = [];
    T(n, (o) => {
      const s = [];
      if (o[1])
        for (let a = o[0]; a <= o[1]; a++)
          s.push(this.hot.toPhysicalRow(a));
      else
        s.push(this.hot.toPhysicalRow(o[0]));
      r.push(s);
    }), T(r, (o) => {
      let s = [];
      T(o, (a, l) => {
        l === 0 ? s.push(a) : o[l] !== o[l - 1] + 1 && (s.push(o[l - 1]), i.push(s), s = [], s.push(a)), l === o.length - 1 && (s.push(a), i.push(s));
      });
    }), e.ranges = i;
  }
  /**
   * Shifts the endpoint coordinates by the defined offset.
   *
   * @private
   * @param {object} endpoint Endpoint object.
   * @param {number} offsetStartIndex Index of the performed change (if the change is located after the endpoint, nothing about the endpoint has to be changed.
   */
  shiftEndpointCoordinates(e, n) {
    e.alterRowOffset && e.alterRowOffset !== 0 ? (e.destinationRow += e.alterRowOffset || 0, T(e.ranges, (i) => {
      T(i, (r, o) => {
        r >= n && (i[o] += e.alterRowOffset || 0);
      });
    })) : e.alterColumnOffset && e.alterColumnOffset !== 0 && (e.destinationColumn += e.alterColumnOffset || 0, e.sourceColumn += e.alterColumnOffset || 0);
  }
  /**
   * Resets (removes) the endpoints from the table.
   *
   * @param {Array} [endpoints] Array containing the endpoints.
   * @param {boolean} [useOffset=true] Use the cell offset value.
   */
  resetAllEndpoints() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.getAllEndpoints(), n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
    e.some((r) => {
      const o = r.alterRowOffset || 0, s = r.alterColumnOffset || 0;
      return r.destinationRow + o >= this.hot.countRows() || r.destinationColumn + s >= this.hot.countCols();
    }) || (this.cellsToSetCache = [], T(e, (r) => {
      this.resetEndpointValue(r, n);
    }), this.hot.setDataAtCell(this.cellsToSetCache, "ColumnSummary.reset"), this.cellsToSetCache = []);
  }
  /**
   * Calculate and refresh all defined endpoints.
   */
  refreshAllEndpoints() {
    this.cellsToSetCache = [], T(this.getAllEndpoints(), (e) => {
      this.currentEndpoint = e, this.plugin.calculate(e), this.setEndpointValue(e, "init");
    }), this.currentEndpoint = null, this.hot.setDataAtCell(this.cellsToSetCache, "ColumnSummary.reset"), this.cellsToSetCache = [];
  }
  /**
   * Calculate and refresh endpoints only in the changed columns.
   *
   * @param {Array} changes Array of changes from the `afterChange` hook.
   */
  refreshChangedEndpoints(e) {
    const n = [];
    this.cellsToSetCache = [], T(e, (i, r, o) => {
      `${i[2] || ""}` != `${i[3]}` && T(this.getAllEndpoints(), (s, a) => {
        this.hot.propToCol(o[r][1]) === s.sourceColumn && n.indexOf(a) === -1 && n.push(a);
      });
    }), T(n, (i) => {
      this.refreshEndpoint(this.getEndpoint(i));
    }), this.hot.setDataAtCell(this.cellsToSetCache, "ColumnSummary.reset"), this.cellsToSetCache = [];
  }
  /**
   * Refreshes the cell meta information for the all endpoints after the `updateSettings` method call which in some
   * cases (call with `columns` option) can reset the cell metas to the initial state.
   */
  refreshCellMetas() {
    this.endpoints.forEach((e) => {
      const n = this.hot.toVisualRow(e.destinationRow);
      if (n !== null) {
        const i = this.hot.getCellMeta(n, e.destinationColumn);
        i.readOnly = e.readOnly, i.className = "columnSummaryResult";
      }
    });
  }
  /**
   * Calculate and refresh a single endpoint.
   *
   * @param {object} endpoint Contains the endpoint information.
   */
  refreshEndpoint(e) {
    this.currentEndpoint = e, this.plugin.calculate(e), this.setEndpointValue(e), this.currentEndpoint = null;
  }
  /**
   * Reset the endpoint value.
   *
   * @param {object} endpoint Contains the endpoint information.
   * @param {boolean} [useOffset=true] Use the cell offset value.
   */
  resetEndpointValue(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
    const i = e.alterRowOffset || 0, r = e.alterColumnOffset || 0;
    this.cellsToSetCache.push([this.hot.toVisualRow(e.destinationRow + (n ? i : 0)), this.hot.toVisualColumn(e.destinationColumn + (n ? r : 0)), ""]);
  }
  /**
   * Set the endpoint value.
   *
   * @param {object} endpoint Contains the endpoint information.
   * @param {string} [source] Source of the call information.
   * @param {boolean} [render=false] `true` if it needs to render the table afterwards.
   */
  setEndpointValue(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    const r = this.hot.toVisualRow(e.destinationRow);
    if (e.destinationRow >= this.hot.countRows() || e.destinationColumn >= this.hot.countCols()) {
      this.throwOutOfBoundsWarning();
      return;
    }
    const o = this.hot.toVisualRow(e.destinationRow);
    if (o !== null) {
      const s = this.hot.getCellMeta(o, e.destinationColumn);
      (n === "init" || s.readOnly !== e.readOnly) && (s.readOnly = e.readOnly, s.className = "columnSummaryResult");
    }
    if ((e.roundFloat === !0 || Number.isInteger(e.roundFloat)) && !isNaN(e.result)) {
      const s = e.roundFloat;
      let a = 0;
      Number.isInteger(s) && (a = Math.min(Math.max(0, s), 100)), e.result = e.result.toFixed(a);
    }
    i ? this.hot.setDataAtCell(r, e.destinationColumn, e.result, "ColumnSummary.set") : this.cellsToSetCache.push([r, e.destinationColumn, e.result]), e.alterRowOffset = void 0, e.alterColumnOffset = void 0;
  }
  /**
   * Throw an error for the calculation range being out of boundaries.
   *
   * @private
   */
  throwOutOfBoundsWarning() {
    gt("One of the Column Summary plugins' destination points you provided is beyond the table boundaries!");
  }
}
const V9 = F9;
function NC(t) {
  return t == null || isNaN(t);
}
function W9(t, e) {
  $9(t, e), e.add(t);
}
function $9(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function U9(t, e, n) {
  return (e = Y9(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Y9(t) {
  var e = j9(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function j9(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Uf(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const DC = "columnSummary", G9 = 220;
var Sc = /* @__PURE__ */ new WeakSet();
class z9 extends at {
  constructor() {
    super(...arguments), W9(this, Sc), U9(this, "endpoints", null);
  }
  static get PLUGIN_KEY() {
    return DC;
  }
  static get PLUGIN_PRIORITY() {
    return G9;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ColumnSummary#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[DC];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    this.enabled || (this.settings = this.hot.getSettings()[DC], this.endpoints = new V9(this, this.settings), this.addHook("afterInit", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Uf(Sc, e, K9).call(e, ...i);
    }), this.addHook("afterChange", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Uf(Sc, e, X9).call(e, ...i);
    }), this.addHook("afterUpdateSettings", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Uf(Sc, e, Q9).call(e, ...i);
    }), this.addHook("beforeCreateRow", (n, i, r) => this.endpoints.resetSetupBeforeStructureAlteration("insert_row", n, i, null, r)), this.addHook("beforeCreateCol", (n, i, r) => this.endpoints.resetSetupBeforeStructureAlteration("insert_col", n, i, null, r)), this.addHook("beforeRemoveRow", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return e.endpoints.resetSetupBeforeStructureAlteration("remove_row", ...i);
    }), this.addHook("beforeRemoveCol", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return e.endpoints.resetSetupBeforeStructureAlteration("remove_col", ...i);
    }), this.addHook("afterCreateRow", (n, i, r) => this.endpoints.resetSetupAfterStructureAlteration("insert_row", n, i, null, r)), this.addHook("afterCreateCol", (n, i, r) => this.endpoints.resetSetupAfterStructureAlteration("insert_col", n, i, null, r)), this.addHook("afterRemoveRow", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return e.endpoints.resetSetupAfterStructureAlteration("remove_row", ...i);
    }), this.addHook("afterRemoveCol", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return e.endpoints.resetSetupAfterStructureAlteration("remove_col", ...i);
    }), this.addHook("afterRowMove", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Uf(Sc, e, q9).call(e, ...i);
    }), super.enablePlugin());
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.endpoints = null, this.settings = null, this.currentEndpoint = null, super.disablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`columnSummary`](@/api/options.md#columnsummary)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), this.endpoints.initEndpoints(), super.updatePlugin();
  }
  /**
   * Calculates math for a single endpoint.
   *
   * @private
   * @param {object} endpoint Contains information about the endpoint.
   */
  calculate(e) {
    switch (e.type.toLowerCase()) {
      case "sum":
        e.result = this.calculateSum(e);
        break;
      case "min":
        e.result = this.calculateMinMax(e, e.type);
        break;
      case "max":
        e.result = this.calculateMinMax(e, e.type);
        break;
      case "count":
        e.result = this.countEntries(e);
        break;
      case "average":
        e.result = this.calculateAverage(e);
        break;
      case "custom":
        e.result = e.customFunction.call(this, e);
        break;
    }
  }
  /**
   * Calculates sum of the values contained in ranges provided in the plugin config.
   *
   * @private
   * @param {object} endpoint Contains the endpoint information.
   * @returns {number} Sum for the selected range.
   */
  calculateSum(e) {
    let n = 0;
    return Fe(e.ranges, (i) => {
      n += this.getPartialSum(i, e.sourceColumn);
    }), n;
  }
  /**
   * Returns partial sum of values from a single row range.
   *
   * @private
   * @param {Array} rowRange Range for the sum.
   * @param {number} col Column index.
   * @returns {number} The partial sum.
   */
  getPartialSum(e, n) {
    let i = 0, r = e[1] || e[0], o = null, s = 0;
    do {
      if (o = this.getCellValue(r, n), o = NC(o) ? null : o, o !== null) {
        const a = (`${o}`.split(".")[1] || []).length || 1;
        a > s && (s = a);
      }
      i += o || 0, r -= 1;
    } while (r >= e[0]);
    return Math.round(i * Wd(10, s)) / Wd(10, s);
  }
  /**
   * Calculates the minimal value for the selected ranges.
   *
   * @private
   * @param {object} endpoint Contains the endpoint information.
   * @param {string} type `'min'` or `'max'`.
   * @returns {number} Min or Max value.
   */
  calculateMinMax(e, n) {
    let i = null;
    return Fe(e.ranges, (r) => {
      const o = this.getPartialMinMax(r, e.sourceColumn, n);
      if (i === null && o !== null && (i = o), o !== null)
        switch (n) {
          case "min":
            i = Math.min(i, o);
            break;
          case "max":
            i = Math.max(i, o);
            break;
        }
    }), i === null ? "Not enough data" : i;
  }
  /**
   * Returns a local minimum of the provided sub-range.
   *
   * @private
   * @param {Array} rowRange Range for the calculation.
   * @param {number} col Column index.
   * @param {string} type `'min'` or `'max'`.
   * @returns {number|null} Min or max value.
   */
  getPartialMinMax(e, n, i) {
    let r = null, o = e[1] || e[0], s;
    do {
      if (s = this.getCellValue(o, n), s = NC(s) ? null : s, r === null)
        r = s;
      else if (s !== null)
        switch (i) {
          case "min":
            r = Math.min(r, s);
            break;
          case "max":
            r = Math.max(r, s);
            break;
        }
      o -= 1;
    } while (o >= e[0]);
    return r;
  }
  /**
   * Counts empty cells in the provided row range.
   *
   * @private
   * @param {Array} rowRange Row range for the calculation.
   * @param {number} col Column index.
   * @returns {number} Empty cells count.
   */
  countEmpty(e, n) {
    let i, r = 0, o = e[1] || e[0];
    do
      i = this.getCellValue(o, n), i = NC(i) ? null : i, i === null && (r += 1), o -= 1;
    while (o >= e[0]);
    return r;
  }
  /**
   * Counts non-empty cells in the provided row range.
   *
   * @private
   * @param {object} endpoint Contains the endpoint information.
   * @returns {number} Entry count.
   */
  countEntries(e) {
    let n = 0;
    const i = e.ranges;
    return Fe(i, (r) => {
      const o = r[1] === void 0 ? 1 : r[1] - r[0] + 1, s = this.countEmpty(r, e.sourceColumn);
      n += o, n -= s;
    }), n;
  }
  /**
   * Calculates the average value from the cells in the range.
   *
   * @private
   * @param {object} endpoint Contains the endpoint information.
   * @returns {number} Avarage value.
   */
  calculateAverage(e) {
    const n = this.calculateSum(e), i = this.countEntries(e);
    return n / i;
  }
  /**
   * Returns a cell value, taking into consideration a basic validation.
   *
   * @private
   * @param {number} row Row index.
   * @param {number} col Column index.
   * @returns {string} The cell value.
   */
  getCellValue(e, n) {
    const i = this.hot.toVisualRow(e), r = this.hot.toVisualColumn(n);
    let o = this.hot.getSourceDataAtCell(e, n), s = "";
    if (i !== null && r !== null && (s = this.hot.getCellMeta(i, r).className || ""), s.indexOf("columnSummaryResult") > -1)
      return null;
    if (this.endpoints.currentEndpoint.forceNumeric && (typeof o == "string" && (o = o.replace(/,/, ".")), o = parseFloat(o)), isNaN(o) && !this.endpoints.currentEndpoint.suppressDataTypeErrors)
      throw new Error(mt`ColumnSummary plugin: cell at (${e}, ${n}) is not in a\x20
          numeric format. Cannot do the calculation.`);
    return o;
  }
}
function K9() {
  this.endpoints.initEndpoints();
}
function Q9(t) {
  t.columns !== void 0 && this.endpoints.refreshCellMetas();
}
function X9(t, e) {
  t && e !== "ColumnSummary.reset" && e !== "ColumnSummary.set" && e !== "loadData" && this.endpoints.refreshChangedEndpoints(t);
}
function q9(t, e) {
  this.endpoints.resetSetupBeforeStructureAlteration("move_row", t[0], t.length, t, this.pluginName), this.endpoints.resetSetupAfterStructureAlteration("move_row", e, t.length, t, this.pluginName);
}
function Z9(t, e) {
  fH(t, e), e.add(t);
}
function kC(t, e, n) {
  fH(t, e), e.set(t, n);
}
function fH(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function au(t, e) {
  return t.get(MR(t, e));
}
function Fy(t, e, n) {
  return t.set(MR(t, e), n), n;
}
function MR(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Hm = /* @__PURE__ */ new WeakMap(), Yf = /* @__PURE__ */ new WeakMap(), jf = /* @__PURE__ */ new WeakMap(), cM = /* @__PURE__ */ new WeakSet();
class gH {
  constructor() {
    Z9(this, cM), kC(this, Hm, !0), kC(this, Yf, null), kC(this, jf, new ResizeObserver((e) => MR(cM, this, J9).call(this, e)));
  }
  /**
   * Sets the observed element.
   *
   * @param {HTMLElement} element The element to observe.
   */
  setObservedElement(e) {
    Fy(Yf, this, e);
  }
  /**
   * Stops observing the element.
   */
  unobserve() {
    au(jf, this).unobserve(au(Yf, this));
  }
  /**
   * Starts observing the element.
   */
  observe() {
    Fy(Hm, this, !0), au(jf, this).observe(au(Yf, this));
  }
  /**
   * Destroys the observer.
   */
  destroy() {
    au(jf, this).disconnect();
  }
}
function J9(t) {
  if (au(Hm, this) || !Array.isArray(t) || !t.length) {
    Fy(Hm, this, !1);
    return;
  }
  t.forEach((e) => {
    let {
      borderBoxSize: n
    } = e;
    const {
      inlineSize: i,
      blockSize: r
    } = n[0];
    this.runLocalHooks("resize", i, r);
  });
}
pt(gH, Vn);
function Ca(t, e, n) {
  e5(t, e), e.set(t, n);
}
function e5(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function yt(t, e) {
  return t.get(mH(t, e));
}
function Mo(t, e, n) {
  return t.set(mH(t, e), n), n;
}
function mH(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var va = /* @__PURE__ */ new WeakMap(), LC = /* @__PURE__ */ new WeakMap(), Io = /* @__PURE__ */ new WeakMap(), ya = /* @__PURE__ */ new WeakMap(), us = /* @__PURE__ */ new WeakMap(), Gf = /* @__PURE__ */ new WeakMap(), Fl = /* @__PURE__ */ new WeakMap();
class za {
  static get CLASS_EDITOR_CONTAINER() {
    return "htCommentsContainer";
  }
  static get CLASS_EDITOR() {
    return "htComments";
  }
  static get CLASS_INPUT() {
    return "htCommentTextArea";
  }
  static get CLASS_CELL() {
    return "htCommentCell";
  }
  /**
   * @type {Document}
   */
  constructor(e, n) {
    var i = this;
    Ca(this, va, void 0), Ca(this, LC, !1), Ca(this, Io, null), Ca(this, ya, void 0), Ca(this, us, void 0), Ca(this, Gf, !0), Ca(this, Fl, new gH()), Mo(va, this, e), Mo(LC, this, n), Mo(ya, this, this.createEditor()), Mo(us, this, yt(ya, this).style), yt(Fl, this).setObservedElement(this.getInputElement()), yt(Fl, this).addLocalHook("resize", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.runLocalHooks("resize", ...o);
    }), this.hide();
  }
  /**
   * Set position of the comments editor according to the  provided x and y coordinates.
   *
   * @param {number} x X position (in pixels).
   * @param {number} y Y position (in pixels).
   */
  setPosition(e, n) {
    yt(us, this).left = `${e}px`, yt(us, this).top = `${n}px`;
  }
  /**
   * Set the editor size according to the provided arguments.
   *
   * @param {number} width Width in pixels.
   * @param {number} height Height in pixels.
   */
  setSize(e, n) {
    if (e && n) {
      const i = this.getInputElement();
      i.style.width = `${e}px`, i.style.height = `${n}px`;
    }
  }
  /**
   * Returns the size of the comments editor.
   *
   * @returns {{ width: number, height: number }}
   */
  getSize() {
    return {
      width: It(this.getInputElement()),
      height: Zt(this.getInputElement())
    };
  }
  /**
   * Starts observing the editor size.
   */
  observeSize() {
    yt(Fl, this).observe();
  }
  /**
   * Reset the editor size to its initial state.
   */
  resetSize() {
    const e = this.getInputElement();
    e.style.width = "", e.style.height = "";
  }
  /**
   * Set the read-only state for the comments editor.
   *
   * @param {boolean} state The new read only state.
   */
  setReadOnlyState(e) {
    const n = this.getInputElement();
    n.readOnly = e;
  }
  /**
   * Show the comments editor.
   */
  show() {
    yt(us, this).display = "block", Mo(Gf, this, !1);
  }
  /**
   * Hide the comments editor.
   */
  hide() {
    yt(Fl, this).unobserve(), yt(Gf, this) || (yt(us, this).display = "none"), Mo(Gf, this, !0);
  }
  /**
   * Checks if the editor is visible.
   *
   * @returns {boolean}
   */
  isVisible() {
    return yt(us, this).display === "block";
  }
  /**
   * Set the comment value.
   *
   * @param {string} [value] The value to use.
   */
  setValue() {
    const n = (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "") || "";
    this.getInputElement().value = n;
  }
  /**
   * Get the comment value.
   *
   * @returns {string}
   */
  getValue() {
    return this.getInputElement().value;
  }
  /**
   * Checks if the comment input element is focused.
   *
   * @returns {boolean}
   */
  isFocused() {
    return yt(va, this).activeElement === this.getInputElement();
  }
  /**
   * Focus the comments input element.
   */
  focus() {
    this.getInputElement().focus();
  }
  /**
   * Create the `textarea` to be used as a comments editor.
   *
   * @returns {HTMLElement}
   */
  createEditor() {
    const e = yt(va, this).createElement("div"), n = yt(va, this).createElement("textarea");
    return e.style.display = "none", Mo(Io, this, yt(va, this).createElement("div")), yt(Io, this).setAttribute("dir", yt(LC, this) ? "rtl" : "ltr"), Q(yt(Io, this), za.CLASS_EDITOR_CONTAINER), yt(va, this).body.appendChild(yt(Io, this)), Q(e, za.CLASS_EDITOR), Q(n, za.CLASS_INPUT), n.setAttribute("data-hot-input", !0), e.appendChild(n), yt(Io, this).appendChild(e), e;
  }
  /**
   * Get the input element.
   *
   * @returns {HTMLElement}
   */
  getInputElement() {
    return yt(ya, this).querySelector(`.${za.CLASS_INPUT}`);
  }
  /**
   * Destroy the comments editor.
   */
  destroy() {
    const e = yt(Io, this) ? yt(Io, this).parentNode : null;
    yt(ya, this).parentNode.removeChild(yt(ya, this)), Mo(ya, this, null), Mo(us, this, null), yt(Fl, this).destroy(), e && e.removeChild(yt(Io, this));
  }
}
pt(za, Vn);
const t5 = za;
function BC(t, e, n) {
  return (e = n5(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function n5(t) {
  var e = i5(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function i5(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const r5 = 250, o5 = 250;
class pH {
  constructor(e) {
    BC(this, "wasLastActionShow", !0), BC(this, "showDebounced", null), BC(this, "hidingTimer", null), this.updateDelay(e);
  }
  /**
   * Responsible for hiding comment after proper delay.
   */
  hide() {
    this.wasLastActionShow = !1, this.hidingTimer = setTimeout(() => {
      this.wasLastActionShow === !1 && this.runLocalHooks("hide");
    }, o5);
  }
  /**
   * Responsible for showing comment after proper delay.
   *
   * @param {object} range Coordinates of selected cell.
   */
  show(e) {
    this.wasLastActionShow = !0, this.showDebounced(e);
  }
  /**
   * Cancel hiding comment.
   */
  cancelHiding() {
    this.wasLastActionShow = !0, clearTimeout(this.hidingTimer), this.hidingTimer = null;
  }
  /**
   * Update the switch settings.
   *
   * @param {number} displayDelay Delay of showing the comments (in milliseconds).
   */
  updateDelay() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : r5;
    this.showDebounced = Ip((n) => {
      this.wasLastActionShow && this.runLocalHooks("show", n.from.row, n.from.col);
    }, e);
  }
  /**
   * Destroy the switcher.
   */
  destroy() {
    this.clearLocalHooks();
  }
}
pt(pH, Vn);
const s5 = pH;
function a5(t, e) {
  return t.indexOf(e) !== -1 ? t : `${t.replace("htTop", "").replace("htMiddle", "").replace("htBottom", "").replace("  ", "")} ${e}`;
}
function l5(t, e) {
  return t.indexOf(e) !== -1 ? t : `${t.replace("htLeft", "").replace("htCenter", "").replace("htRight", "").replace("htJustify", "").replace("  ", "")} ${e}`;
}
function Sa(t, e) {
  const n = {};
  return T(t, (i) => {
    i.forAll((r, o) => {
      r >= 0 && o >= 0 && (n[r] || (n[r] = []), n[r][o] = e(r, o));
    });
  }), n;
}
function Ss(t, e, n, i, r) {
  T(t, (o) => {
    o.forAll((s, a) => {
      s >= 0 && a >= 0 && u5(s, a, e, n, i, r);
    });
  });
}
function u5(t, e, n, i, r, o) {
  const s = r(t, e);
  let a = i;
  s.className && (n === "vertical" ? a = a5(s.className, i) : a = l5(s.className, i)), o(t, e, "className", a);
}
function ko(t) {
  return `<span class="selected">${String.fromCharCode(10003)}</span>${t}`;
}
function fh(t, e) {
  let n = !1;
  return Array.isArray(t) && T(t, (i) => (i.forAll((r, o) => {
    if (r >= 0 && o >= 0 && e(r, o))
      return n = !0, !1;
  }), n)), n;
}
function Zw(t, e) {
  const n = {
    top: 0,
    left: 0
  };
  if (e !== t.ownerDocument) {
    const {
      frameElement: i
    } = e.defaultView, {
      top: r,
      left: o
    } = i.getBoundingClientRect();
    n.top = r, n.left = o;
  }
  return n;
}
function c5(t) {
  return function(e, n) {
    const i = this.getCellMeta(e, n).className;
    return i && i.indexOf(t) !== -1;
  };
}
function ti(t, e) {
  return fh(t.getSelectedRange(), c5(e).bind(t));
}
const $e = "---------";
function h5() {
  return {
    name: $e
  };
}
const Bi = "alignment";
function d5() {
  return {
    key: Bi,
    name() {
      return this.getTranslatedPhrase(sw);
    },
    disabled() {
      if (this.countRows() === 0 || this.countCols() === 0)
        return !0;
      const t = this.getSelectedRangeLast();
      return !t || t.isSingleHeader() ? !0 : !(this.getSelectedRange() && !this.selection.isSelectedByCorner());
    },
    submenu: {
      items: [{
        key: `${Bi}:left`,
        checkable: !0,
        ariaLabel() {
          return this.getTranslatedPhrase(Wh);
        },
        ariaChecked() {
          return ti(this, "htLeft");
        },
        name() {
          let t = this.getTranslatedPhrase(Wh);
          return ti(this, "htLeft") && (t = ko(t)), t;
        },
        callback() {
          const t = this.getSelectedRange(), e = Sa(t, (r, o) => this.getCellMeta(r, o).className), n = "horizontal", i = "htLeft";
          this.runHooks("beforeCellAlignment", e, t, n, i), Ss(t, n, i, (r, o) => this.getCellMeta(r, o), (r, o, s, a) => this.setCellMeta(r, o, s, a)), this.render();
        },
        disabled: !1
      }, {
        key: `${Bi}:center`,
        checkable: !0,
        ariaLabel() {
          return this.getTranslatedPhrase($h);
        },
        ariaChecked() {
          return ti(this, "htCenter");
        },
        name() {
          let t = this.getTranslatedPhrase($h);
          return ti(this, "htCenter") && (t = ko(t)), t;
        },
        callback() {
          const t = this.getSelectedRange(), e = Sa(t, (r, o) => this.getCellMeta(r, o).className), n = "horizontal", i = "htCenter";
          this.runHooks("beforeCellAlignment", e, t, n, i), Ss(t, n, i, (r, o) => this.getCellMeta(r, o), (r, o, s, a) => this.setCellMeta(r, o, s, a)), this.render();
        },
        disabled: !1
      }, {
        key: `${Bi}:right`,
        checkable: !0,
        ariaLabel() {
          return this.getTranslatedPhrase(Uh);
        },
        ariaChecked() {
          return ti(this, "htRight");
        },
        name() {
          let t = this.getTranslatedPhrase(Uh);
          return ti(this, "htRight") && (t = ko(t)), t;
        },
        callback() {
          const t = this.getSelectedRange(), e = Sa(t, (r, o) => this.getCellMeta(r, o).className), n = "horizontal", i = "htRight";
          this.runHooks("beforeCellAlignment", e, t, n, i), Ss(t, n, i, (r, o) => this.getCellMeta(r, o), (r, o, s, a) => this.setCellMeta(r, o, s, a)), this.render();
        },
        disabled: !1
      }, {
        key: `${Bi}:justify`,
        checkable: !0,
        ariaLabel() {
          return this.getTranslatedPhrase(Yh);
        },
        ariaChecked() {
          return ti(this, "htJustify");
        },
        name() {
          let t = this.getTranslatedPhrase(Yh);
          return ti(this, "htJustify") && (t = ko(t)), t;
        },
        callback() {
          const t = this.getSelectedRange(), e = Sa(t, (r, o) => this.getCellMeta(r, o).className), n = "horizontal", i = "htJustify";
          this.runHooks("beforeCellAlignment", e, t, n, i), Ss(t, n, i, (r, o) => this.getCellMeta(r, o), (r, o, s, a) => this.setCellMeta(r, o, s, a)), this.render();
        },
        disabled: !1
      }, {
        name: $e
      }, {
        key: `${Bi}:top`,
        checkable: !0,
        ariaLabel() {
          return this.getTranslatedPhrase(jh);
        },
        ariaChecked() {
          return ti(this, "htTop");
        },
        name() {
          let t = this.getTranslatedPhrase(jh);
          return ti(this, "htTop") && (t = ko(t)), t;
        },
        callback() {
          const t = this.getSelectedRange(), e = Sa(t, (r, o) => this.getCellMeta(r, o).className), n = "vertical", i = "htTop";
          this.runHooks("beforeCellAlignment", e, t, n, i), Ss(t, n, i, (r, o) => this.getCellMeta(r, o), (r, o, s, a) => this.setCellMeta(r, o, s, a)), this.render();
        },
        disabled: !1
      }, {
        key: `${Bi}:middle`,
        checkable: !0,
        ariaLabel() {
          return this.getTranslatedPhrase(Gh);
        },
        ariaChecked() {
          return ti(this, "htMiddle");
        },
        name() {
          let t = this.getTranslatedPhrase(Gh);
          return ti(this, "htMiddle") && (t = ko(t)), t;
        },
        callback() {
          const t = this.getSelectedRange(), e = Sa(t, (r, o) => this.getCellMeta(r, o).className), n = "vertical", i = "htMiddle";
          this.runHooks("beforeCellAlignment", e, t, n, i), Ss(t, n, i, (r, o) => this.getCellMeta(r, o), (r, o, s, a) => this.setCellMeta(r, o, s, a)), this.render();
        },
        disabled: !1
      }, {
        key: `${Bi}:bottom`,
        checkable: !0,
        ariaLabel() {
          return this.getTranslatedPhrase(zh);
        },
        ariaChecked() {
          return ti(this, "htBottom");
        },
        name() {
          let t = this.getTranslatedPhrase(zh);
          return ti(this, "htBottom") && (t = ko(t)), t;
        },
        callback() {
          const t = this.getSelectedRange(), e = Sa(t, (r, o) => this.getCellMeta(r, o).className), n = "vertical", i = "htBottom";
          this.runHooks("beforeCellAlignment", e, t, n, i), Ss(t, n, i, (r, o) => this.getCellMeta(r, o), (r, o, s, a) => this.setCellMeta(r, o, s, a)), this.render();
        },
        disabled: !1
      }]
    }
  };
}
const Jw = "clear_column";
function f5() {
  return {
    key: Jw,
    name() {
      return this.getTranslatedPhrase(Gp);
    },
    callback(t, e) {
      const n = e[0].start.col, i = e[0].end.col;
      this.countRows() && this.populateFromArray(0, n, [[null]], Math.max(e[0].start.row, e[0].end.row), i, "ContextMenu.clearColumn");
    },
    disabled() {
      const t = this.getSelectedRangeLast();
      return !t || t.isSingleHeader() && t.highlight.col < 0 ? !0 : !this.selection.isSelectedByColumnHeader();
    }
  };
}
const xd = "col_left";
function g5() {
  return {
    key: xd,
    name() {
      return this.getTranslatedPhrase(Vp);
    },
    callback() {
      const t = this.getSelectedRangeLast().getTopLeftCorner(), e = this.isRtl() ? "insert_col_end" : "insert_col_start";
      this.alter(e, t.col, 1, "ContextMenu.columnLeft");
    },
    disabled() {
      if (!this.isColumnModificationAllowed())
        return !0;
      const t = this.getSelectedRangeLast();
      return !t || t.isSingleHeader() && t.highlight.col < 0 ? !0 : this.selection.isSelectedByCorner() ? this.countCols() === 0 : this.selection.isSelectedByRowHeader() || this.countCols() >= this.getSettings().maxCols;
    },
    hidden() {
      return !this.getSettings().allowInsertColumn;
    }
  };
}
const Hd = "col_right";
function m5() {
  return {
    key: Hd,
    name() {
      return this.getTranslatedPhrase(Wp);
    },
    callback() {
      const t = this.getSelectedRangeLast().getTopRightCorner(), e = this.isRtl() ? "insert_col_start" : "insert_col_end";
      this.alter(e, t.col, 1, "ContextMenu.columnRight");
    },
    disabled() {
      if (!this.isColumnModificationAllowed())
        return !0;
      const t = this.getSelectedRangeLast();
      return !t || t.isSingleHeader() && t.highlight.col < 0 ? !0 : this.selection.isSelectedByCorner() ? !1 : this.selection.isSelectedByRowHeader() || this.countCols() >= this.getSettings().maxCols;
    },
    hidden() {
      return !this.getSettings().allowInsertColumn;
    }
  };
}
const _d = "make_read_only";
function p5() {
  return {
    key: _d,
    checkable: !0,
    ariaChecked() {
      return fh(this.getSelectedRange(), (e, n) => this.getCellMeta(e, n).readOnly);
    },
    ariaLabel() {
      return this.getTranslatedPhrase(Vh);
    },
    name() {
      let t = this.getTranslatedPhrase(Vh);
      return fh(this.getSelectedRange(), (n, i) => this.getCellMeta(n, i).readOnly) && (t = ko(t)), t;
    },
    callback() {
      const t = this.getSelectedRange(), e = fh(t, (n, i) => this.getCellMeta(n, i).readOnly);
      T(t, (n) => {
        n.forAll((i, r) => {
          i >= 0 && r >= 0 && this.setCellMeta(i, r, "readOnly", !e);
        });
      }), this.render();
    },
    disabled() {
      const t = this.getSelectedRangeLast();
      return !!(!t || t.isSingleHeader() || this.selection.isSelectedByCorner() || this.countRows() === 0 || this.countCols() === 0 || !this.getSelectedRange() || this.getSelectedRange().length === 0);
    }
  };
}
const eA = "redo";
function w5() {
  return {
    key: eA,
    name() {
      return this.getTranslatedPhrase(jp);
    },
    callback() {
      this.redo();
    },
    hidden() {
      const t = this.getPlugin("undoRedo");
      return !t || !t.isEnabled();
    },
    disabled() {
      return !this.getPlugin("undoRedo").isRedoAvailable();
    }
  };
}
const Nd = "remove_col";
function A5() {
  return {
    key: Nd,
    name() {
      const t = this.getSelected();
      let e = 0;
      if (t)
        if (t.length > 1)
          e = 1;
        else {
          const [, n, , i] = t[0];
          n - i !== 0 && (e = 1);
        }
      return this.getTranslatedPhrase(Up, e);
    },
    callback() {
      this.alter("remove_col", F4(this), null, "ContextMenu.removeColumn");
    },
    disabled() {
      if (!this.isColumnModificationAllowed())
        return !0;
      const t = this.getSelectedRangeLast();
      if (!t || t.isSingleHeader() && t.highlight.col < 0)
        return !0;
      const e = this.countCols();
      return this.selection.isSelectedByCorner() ? e === 0 : this.selection.isSelectedByRowHeader() || e === 0;
    },
    hidden() {
      return !this.getSettings().allowRemoveColumn;
    }
  };
}
const tA = "remove_row";
function C5() {
  return {
    key: tA,
    name() {
      const t = this.getSelected();
      let e = 0;
      if (t)
        if (t.length > 1)
          e = 1;
        else {
          const [n, , i] = t[0];
          n - i !== 0 && (e = 1);
        }
      return this.getTranslatedPhrase($p, e);
    },
    callback() {
      this.alter("remove_row", V4(this), 1, "ContextMenu.removeRow");
    },
    disabled() {
      const t = this.getSelectedRangeLast();
      if (!t || t.isSingleHeader() && t.highlight.row < 0)
        return !0;
      const e = this.countRows();
      return this.selection.isSelectedByCorner() ? e === 0 : this.selection.isSelectedByColumnHeader() || e === 0;
    },
    hidden() {
      return !this.getSettings().allowRemoveRow;
    }
  };
}
const nA = "row_above";
function v5() {
  return {
    key: nA,
    name() {
      return this.getTranslatedPhrase(Bp);
    },
    callback() {
      const t = this.getSelectedRangeLast().getTopLeftCorner();
      this.alter("insert_row_above", t.row, 1, "ContextMenu.rowAbove");
    },
    disabled() {
      const t = this.getSelectedRangeLast();
      return !t || t.isSingleHeader() && t.highlight.row < 0 ? !0 : this.selection.isSelectedByCorner() ? this.countRows() === 0 : this.selection.isSelectedByColumnHeader() || this.countRows() >= this.getSettings().maxRows;
    },
    hidden() {
      return !this.getSettings().allowInsertRow;
    }
  };
}
const iA = "row_below";
function y5() {
  return {
    key: iA,
    name() {
      return this.getTranslatedPhrase(Fp);
    },
    callback() {
      const t = this.getSelectedRangeLast().getBottomRightCorner();
      this.alter("insert_row_below", t.row, 1, "ContextMenu.rowBelow");
    },
    disabled() {
      const t = this.getSelectedRangeLast();
      return !t || t.isSingleHeader() && t.highlight.row < 0 ? !0 : this.selection.isSelectedByCorner() ? !1 : this.selection.isSelectedByColumnHeader() || this.countRows() >= this.getSettings().maxRows;
    },
    hidden() {
      return !this.getSettings().allowInsertRow;
    }
  };
}
const qh = "no_items";
function S5() {
  return {
    key: qh,
    name() {
      return this.getTranslatedPhrase(Xb);
    },
    disabled: !0,
    isCommand: !1
  };
}
const rA = "undo";
function b5() {
  return {
    key: rA,
    name() {
      return this.getTranslatedPhrase(Yp);
    },
    callback() {
      this.undo();
    },
    hidden() {
      const t = this.getPlugin("undoRedo");
      return !t || !t.isEnabled();
    },
    disabled() {
      return !this.getPlugin("undoRedo").isUndoAvailable();
    }
  };
}
const R5 = [nA, iA, xd, Hd, Jw, tA, Nd, rA, eA, _d, Bi, $e, qh], E5 = {
  [$e]: h5,
  [qh]: S5,
  [nA]: v5,
  [iA]: y5,
  [xd]: g5,
  [Hd]: m5,
  [Jw]: f5,
  [tA]: C5,
  [Nd]: A5,
  [rA]: b5,
  [eA]: w5,
  [_d]: p5,
  [Bi]: d5
};
function wH() {
  const t = {};
  return Fe(E5, (e, n) => {
    t[n] = e();
  }), t;
}
function T5(t) {
  return {
    key: "commentsAddEdit",
    name() {
      var e;
      const n = (e = this.getSelectedRangeLast()) === null || e === void 0 ? void 0 : e.highlight;
      return n != null && n.isCell() && t.getCommentAtCell(n.row, n.col) ? this.getTranslatedPhrase(iw) : this.getTranslatedPhrase(nw);
    },
    callback() {
      const e = this.getSelectedRangeLast();
      t.setRange(e), t.show(), t.focusEditor();
    },
    disabled() {
      const e = this.getSelectedRangeLast();
      return !!(!e || e.highlight.isHeader() || this.selection.isEntireRowSelected() && this.selection.isEntireColumnSelected() || this.countRenderedRows() === 0 || this.countRenderedCols() === 0);
    }
  };
}
function M5(t) {
  return {
    key: "commentsRemove",
    name() {
      return this.getTranslatedPhrase(rw);
    },
    callback() {
      this.getSelectedRangeLast().forAll((n, i) => {
        n >= 0 && i >= 0 && t.removeCommentAtCell(n, i, !1);
      }), this.render();
    },
    disabled() {
      const e = this.getSelectedRangeLast();
      return !!(!e || e.highlight.isHeader() || this.selection.isEntireRowSelected() && this.selection.isEntireColumnSelected() || this.countRenderedRows() === 0 || this.countRenderedCols() === 0);
    }
  };
}
function I5(t) {
  return {
    key: "commentsReadOnly",
    name() {
      const e = this.getTranslatedPhrase(ow);
      return fh(this.getSelectedRange(), (i, r) => t.getCommentMeta(i, r, Qg)) ? ko(e) : e;
    },
    callback() {
      this.getSelectedRangeLast().forAll((n, i) => {
        if (n >= 0 && i >= 0) {
          const r = !!t.getCommentMeta(n, i, Qg);
          t.updateCommentMeta(n, i, {
            [Qg]: !r
          });
        }
      });
    },
    disabled() {
      const e = this.getSelectedRangeLast();
      return !!(!e || e.highlight.isHeader() || !t.getCommentAtCell(e.highlight.row, e.highlight.col) || this.selection.isEntireRowSelected() && this.selection.isEntireColumnSelected() || this.countRenderedRows() === 0 || this.countRenderedCols() === 0);
    }
  };
}
function O5(t, e) {
  AH(t, e), e.add(t);
}
function Vl(t, e, n) {
  AH(t, e), e.set(t, n);
}
function AH(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function P5(t, e, n) {
  return (e = x5(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function x5(t) {
  var e = H5(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function H5(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function ks(t, e, n) {
  return t.set(dr(t, e), n), n;
}
function ze(t, e) {
  return t.get(dr(t, e));
}
function dr(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const gh = "comments", _5 = 60, Lo = "comment", Jc = "value", CH = "style", Qg = "readOnly", zf = gh, Vy = `plugin:${gh}`;
var bt = /* @__PURE__ */ new WeakMap(), Xr = /* @__PURE__ */ new WeakMap(), oA = /* @__PURE__ */ new WeakMap(), Xg = /* @__PURE__ */ new WeakMap(), Wy = /* @__PURE__ */ new WeakMap(), _m = /* @__PURE__ */ new WeakMap(), Br = /* @__PURE__ */ new WeakSet();
class N5 extends at {
  constructor() {
    super(...arguments), O5(this, Br), P5(this, "range", {}), Vl(this, bt, null), Vl(this, Xr, null), Vl(this, oA, !1), Vl(this, Xg, !1), Vl(this, Wy, null), Vl(this, _m, "");
  }
  static get PLUGIN_KEY() {
    return gh;
  }
  static get PLUGIN_PRIORITY() {
    return _5;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link Comments#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[gh];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    this.enabled || (ze(bt, this) || (ks(bt, this, new t5(this.hot.rootDocument, this.hot.isRtl())), ze(bt, this).addLocalHook("resize", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dr(Br, e, W5).call(e, ...i);
    })), ze(Xr, this) || ks(Xr, this, new s5(this.getDisplayDelaySetting())), this.addHook("afterContextMenuDefaultOptions", (n) => this.addToContextMenu(n)), this.addHook("afterRenderer", (n, i, r, o, s, a) => dr(Br, this, B5).call(this, n, a)), this.addHook("afterScroll", () => dr(Br, this, U5).call(this)), this.addHook("afterBeginEditing", () => this.hide()), this.addHook("afterDocumentKeyDown", (n) => dr(Br, this, $5).call(this, n)), ze(Xr, this).addLocalHook("hide", () => this.hide()), ze(Xr, this).addLocalHook("show", (n, i) => this.showAtCell(n, i)), this.registerShortcuts(), this.registerListeners(), super.enablePlugin());
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *   - [`comments`](@/api/options.md#comments)
   */
  updatePlugin() {
    ze(Xr, this).updateDelay(this.getDisplayDelaySetting()), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.unregisterShortcuts(), super.disablePlugin();
  }
  /**
   * Register shortcuts responsible for toggling context menu.
   *
   * @private
   */
  registerShortcuts() {
    const e = this.hot.getShortcutManager(), n = e.getContext("grid"), i = e.addContext(Vy);
    n.addShortcut({
      keys: [["Control", "Alt", "M"]],
      callback: () => {
        const r = this.hot.getSelectedRangeLast();
        ks(Xg, this, !0), this.hot.scrollToFocusedCell(() => {
          this.setRange(r), this.show(), this.focusEditor(), e.setActiveContextName(Vy), this.hot._registerTimeout(() => {
            ks(Xg, this, !1);
          });
        });
      },
      stopPropagation: !0,
      runOnlyIf: () => {
        var r;
        return ((r = this.hot.getSelectedRangeLast()) === null || r === void 0 ? void 0 : r.highlight.isCell()) && !ze(bt, this).isVisible();
      },
      group: zf
    }), i.addShortcut({
      keys: [["Escape"]],
      callback: () => {
        ze(bt, this).setValue(ze(_m, this)), this.hide(), e.setActiveContextName("grid");
      },
      runOnlyIf: () => ze(bt, this).isVisible() && ze(bt, this).isFocused(),
      group: zf
    }), i.addShortcut({
      keys: [["Control/Meta", "Enter"]],
      callback: () => {
        this.hide(), e.setActiveContextName("grid");
      },
      runOnlyIf: () => ze(bt, this).isVisible() && ze(bt, this).isFocused(),
      group: zf
    });
  }
  /**
   * Unregister shortcuts responsible for toggling context menu.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("grid").removeShortcutsByGroup(zf);
  }
  /**
   * Registers all necessary DOM listeners.
   *
   * @private
   */
  registerListeners() {
    const {
      rootDocument: e
    } = this.hot, n = this.getEditorInputElement();
    this.eventManager.addEventListener(e, "mouseover", (i) => dr(Br, this, k5).call(this, i)), this.eventManager.addEventListener(e, "mousedown", (i) => dr(Br, this, D5).call(this, i)), this.eventManager.addEventListener(e, "mouseup", () => dr(Br, this, L5).call(this)), this.eventManager.addEventListener(n, "focus", () => dr(Br, this, V5).call(this)), this.eventManager.addEventListener(n, "blur", () => dr(Br, this, F5).call(this));
  }
  /**
   * Sets the current cell range to be able to use general methods like {@link Comments#setComment}, {@link Comments#removeComment}, {@link Comments#show}.
   *
   * @param {object} range Object with `from` property, each with `row` and `col` properties.
   */
  setRange(e) {
    this.range = e;
  }
  /**
   * Clears the currently selected cell.
   */
  clearRange() {
    this.range = {};
  }
  /**
   * Checks if the event target is a cell containing a comment.
   *
   * @private
   * @param {Event} event DOM event.
   * @returns {boolean}
   */
  targetIsCellWithComment(e) {
    const n = js(e.target, "TD", "TBODY");
    return !!(n && Te(n, "htCommentCell") && js(n, [this.hot.rootElement]));
  }
  /**
   * Checks if the event target is a comment textarea.
   *
   * @private
   * @param {Event} event DOM event.
   * @returns {boolean}
   */
  targetIsCommentTextArea(e) {
    return this.getEditorInputElement() === e.target;
  }
  /**
   * Sets a comment for a cell according to the previously set range (see {@link Comments#setRange}).
   *
   * @param {string} value Comment contents.
   */
  setComment(e) {
    if (!this.range.from)
      throw new Error('Before using this method, first set cell range (hot.getPlugin("comment").setRange())');
    const n = ze(bt, this).getValue();
    let i = "";
    e != null ? i = e : n != null && (i = n);
    const r = this.range.from.row, o = this.range.from.col;
    this.updateCommentMeta(r, o, {
      [Jc]: i
    }), this.hot.render();
  }
  /**
   * Sets a comment for a specified cell.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {string} value Comment contents.
   */
  setCommentAtCell(e, n, i) {
    this.setRange({
      from: this.hot._createCellCoords(e, n)
    }), this.setComment(i);
  }
  /**
   * Removes a comment from a cell according to previously set range (see {@link Comments#setRange}).
   *
   * @param {boolean} [forceRender=true] If set to `true`, the table will be re-rendered at the end of the operation.
   */
  removeComment() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
    if (!this.range.from)
      throw new Error('Before using this method, first set cell range (hot.getPlugin("comment").setRange())');
    this.hot.setCellMeta(this.range.from.row, this.range.from.col, Lo), e && this.hot.render(), this.hide();
  }
  /**
   * Removes a comment from a specified cell.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {boolean} [forceRender=true] If `true`, the table will be re-rendered at the end of the operation.
   */
  removeCommentAtCell(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
    this.setRange({
      from: this.hot._createCellCoords(e, n)
    }), this.removeComment(i);
  }
  /**
   * Gets comment from a cell according to previously set range (see {@link Comments#setRange}).
   *
   * @returns {string|undefined} Returns a content of the comment.
   */
  getComment() {
    const e = this.range.from.row, n = this.range.from.col;
    return this.getCommentMeta(e, n, Jc);
  }
  /**
   * Gets comment from a cell at the provided coordinates.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @returns {string|undefined} Returns a content of the comment.
   */
  getCommentAtCell(e, n) {
    return this.getCommentMeta(e, n, Jc);
  }
  /**
   * Shows the comment editor accordingly to the previously set range (see {@link Comments#setRange}).
   *
   * @returns {boolean} Returns `true` if comment editor was shown.
   */
  show() {
    var e;
    if (!this.range.from)
      throw new Error('Before using this method, first set cell range (hot.getPlugin("comment").setRange())');
    const {
      from: {
        row: n,
        col: i
      }
    } = this.range;
    if (n < 0 || n > this.hot.countSourceRows() - 1 || i < 0 || i > this.hot.countSourceCols() - 1)
      return !1;
    const r = this.hot.getCellMeta(this.range.from.row, this.range.from.col);
    return ze(Xr, this).cancelHiding(), ze(bt, this).setValue((e = r[Lo] ? r[Lo][Jc] : null) !== null && e !== void 0 ? e : ""), ze(bt, this).show(), this.refreshEditor(!0), !0;
  }
  /**
   * Shows comment editor according to cell coordinates.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @returns {boolean} Returns `true` if comment editor was shown.
   */
  showAtCell(e, n) {
    return this.setRange({
      from: this.hot._createCellCoords(e, n)
    }), this.show();
  }
  /**
   * Hides the comment editor.
   */
  hide() {
    ze(bt, this).hide();
  }
  /**
   * Refreshes comment editor position and styling.
   *
   * @param {boolean} [force=false] If `true` then recalculation will be forced.
   */
  refreshEditor() {
    var e, n;
    if (!(arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1) && (!this.range.from || !ze(bt, this).isVisible()))
      return;
    const {
      rowIndexMapper: r,
      columnIndexMapper: o
    } = this.hot, {
      row: s,
      col: a
    } = this.range.from;
    let l = r.getRenderableFromVisualIndex(s), u = o.getRenderableFromVisualIndex(a);
    const c = l === null;
    ze(bt, this).setPosition(0, 0), l === null && (l = r.getRenderableFromVisualIndex(r.getNearestNotHiddenIndex(s, -1))), u === null && (u = o.getRenderableFromVisualIndex(o.getNearestNotHiddenIndex(a, -1)));
    const h = l === null, d = u === null;
    l = (e = l) !== null && e !== void 0 ? e : 0, u = (n = u) !== null && n !== void 0 ? n : 0;
    const {
      rootWindow: g,
      view: {
        _wt: m
      }
    } = this.hot, {
      wtTable: w
    } = m, C = m.getCell({
      row: l,
      col: u
    }, !0), S = this.getCommentMeta(s, a, CH);
    S ? ze(bt, this).setSize(S.width, S.height) : ze(bt, this).resetSize();
    const b = d ? 0 : w.getStretchedColumnWidth(u), R = c && !h ? Zt(C) : 0, {
      left: _,
      top: O,
      width: $,
      height: U
    } = C.getBoundingClientRect(), {
      width: G,
      height: z
    } = ze(bt, this).getSize(), {
      innerWidth: j,
      innerHeight: J
    } = this.hot.rootWindow, q = this.hot.rootDocument.documentElement;
    let re = _ + g.scrollX + b, me = O + g.scrollY + R;
    this.hot.isRtl() && (re -= G + b), this.hot.isLtr() && _ + $ + G > j ? re = _ + g.scrollX - G - 1 : this.hot.isRtl() && re < -(q.scrollWidth - q.clientWidth) && (re = _ + g.scrollX + b + 1), O + z > J && (me -= z - U + 1), ze(bt, this).setPosition(re, me), ze(bt, this).setReadOnlyState(this.getCommentMeta(s, a, Qg)), ze(bt, this).observeSize();
  }
  /**
   * Focuses the comments editor element.
   */
  focusEditor() {
    ze(bt, this).focus();
  }
  /**
   * Sets or update the comment-related cell meta.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {object} metaObject Object defining all the comment-related meta information.
   */
  updateCommentMeta(e, n, i) {
    const r = this.hot.getCellMeta(e, n)[Lo];
    let o;
    r ? (o = Fn(r), Sd(o, i)) : o = i, this.hot.setCellMeta(e, n, Lo, o);
  }
  /**
   * Gets the comment related meta information.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {string} property Cell meta property.
   * @returns {Mixed}
   */
  getCommentMeta(e, n, i) {
    const r = this.hot.getCellMeta(e, n);
    if (r[Lo])
      return r[Lo][i];
  }
  /**
   * Add Comments plugin options to the Context Menu.
   *
   * @private
   * @param {object} options The menu options.
   */
  addToContextMenu(e) {
    e.items.push({
      name: $e
    }, T5(this), M5(this), I5(this));
  }
  /**
   * Get `displayDelay` setting of comment plugin.
   *
   * @private
   * @returns {number|undefined}
   */
  getDisplayDelaySetting() {
    const e = this.hot.getSettings()[gh];
    if (Be(e))
      return e.displayDelay;
  }
  /**
   * Gets the editors input element.
   *
   * @private
   * @returns {HTMLTextAreaElement}
   */
  getEditorInputElement() {
    return ze(bt, this).getInputElement();
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    var e, n;
    (e = ze(bt, this)) === null || e === void 0 || e.destroy(), (n = ze(Xr, this)) === null || n === void 0 || n.destroy(), super.destroy();
  }
}
function D5(t) {
  if (!(!this.hot.view || !this.hot.view._wt) && !ze(oA, this) && !this.targetIsCommentTextArea(t)) {
    const e = js(t.target, "TD", "TBODY");
    let n = null;
    e && (n = this.hot.getCoords(e)), (!e || this.range.from && n && (this.range.from.row !== n.row || this.range.from.col !== n.col)) && this.hide();
  }
}
function k5(t) {
  const {
    rootDocument: e
  } = this.hot;
  if (!(ze(oA, this) || ze(bt, this).isFocused() || Te(t.target, "wtBorder") || ze(Wy, this) === t.target || !ze(bt, this)))
    if (ks(Wy, this, e.elementFromPoint(t.clientX, t.clientY)), this.targetIsCellWithComment(t)) {
      const n = this.hot._createCellRange(this.hot.getCoords(t.target));
      ze(Xr, this).show(n);
    } else
      Vs(t.target, e) && !this.targetIsCommentTextArea(t) && ze(Xr, this).hide();
}
function L5() {
  ks(oA, this, !1);
}
function B5(t, e) {
  e[Lo] && e[Lo][Jc] && Q(t, e.commentedCellClassName);
}
function F5() {
  ks(_m, this, ""), this.hot.getShortcutManager().setActiveContextName("grid"), this.setComment();
}
function V5() {
  ks(_m, this, this.getComment()), this.hot.listen(), this.hot.getShortcutManager().setActiveContextName(Vy);
}
function W5(t, e) {
  this.updateCommentMeta(this.range.from.row, this.range.from.col, {
    [CH]: {
      width: t,
      height: e
    }
  });
}
function $5(t) {
  ze(bt, this).isVisible() && Zi(t);
}
function U5() {
  ze(Xg, this) || this.hide();
}
var hM = ap, Y5 = TypeError, j5 = function(t, e) {
  if (!delete t[e])
    throw new Y5("Cannot delete property " + hM(e) + " of " + hM(t));
}, G5 = Mn, z5 = ld, K5 = hd, Q5 = fO, X5 = j5, q5 = gO, Z5 = [].unshift(0) !== 1, J5 = function() {
  try {
    Object.defineProperty([], "length", { writable: !1 }).unshift();
  } catch (t) {
    return t instanceof TypeError;
  }
}, e7 = Z5 || !J5();
G5({ target: "Array", proto: !0, arity: 1, forced: e7 }, {
  // eslint-disable-next-line no-unused-vars -- required for `.length`
  unshift: function(e) {
    var n = z5(this), i = K5(n), r = arguments.length;
    if (r) {
      q5(i + r);
      for (var o = i; o--; ) {
        var s = o + r;
        o in n ? n[s] = n[o] : X5(n, s);
      }
      for (var a = 0; a < r; a++)
        n[a] = arguments[a];
    }
    return Q5(n, i + r);
  }
});
function FC(t, e, n) {
  return (e = t7(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function t7(t) {
  var e = n7(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function n7(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class vH {
  constructor(e) {
    FC(this, "hot", void 0), FC(this, "commands", {}), FC(this, "commonCallback", null), this.hot = e;
  }
  /**
   * Register command.
   *
   * @param {string} name Command name.
   * @param {object} commandDescriptor Command descriptor object with properties like `key` (command id),
   *                                   `callback` (task to execute), `name` (command name), `disabled` (command availability).
   */
  registerCommand(e, n) {
    this.commands[e] = n;
  }
  /**
   * Set common callback which will be trigger on every executed command.
   *
   * @param {Function} callback Function which will be fired on every command execute.
   */
  setCommonCallback(e) {
    this.commonCallback = e;
  }
  /**
   * Execute command by its name.
   *
   * @param {string} commandName Command id.
   * @param {*} params Arguments passed to command task.
   */
  execute(e) {
    for (var n = arguments.length, i = new Array(n > 1 ? n - 1 : 0), r = 1; r < n; r++)
      i[r - 1] = arguments[r];
    const o = e.split(":"), s = o[0], a = o.length === 2 ? o[1] : null;
    let l = this.commands[s];
    if (!l)
      throw new Error(`Menu command '${s}' not exists.`);
    if (a && l.submenu && (l = i7(a, l.submenu.items)), l.disabled === !0 || typeof l.disabled == "function" && l.disabled.call(this.hot) === !0 || Bt(l, "submenu"))
      return;
    const u = [];
    typeof l.callback == "function" && u.push(l.callback), typeof this.commonCallback == "function" && u.push(this.commonCallback), i.unshift(o.join(":")), T(u, (c) => c.apply(this.hot, i));
  }
}
function i7(t, e) {
  let n;
  return T(e, (i) => {
    const r = i.key ? i.key.split(":") : null;
    if (Array.isArray(r) && r[1] === t)
      return n = i, !1;
  }), n;
}
function VC(t, e, n) {
  return (e = r7(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function r7(t) {
  var e = o7(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function o7(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class yH {
  constructor(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
    VC(this, "hot", void 0), VC(this, "predefinedItems", wH()), VC(this, "defaultOrderPattern", void 0), this.hot = e, this.defaultOrderPattern = n;
  }
  /**
   * Set predefined items.
   *
   * @param {Array} predefinedItemsCollection Array of predefined items.
   */
  setPredefinedItems(e) {
    const n = {};
    this.defaultOrderPattern.length = 0, Fe(e, (i, r) => {
      let o = "";
      i.name === $e ? (n[$e] = i, o = $e) : isNaN(parseInt(r, 10)) ? (i.key = i.key === void 0 ? r : i.key, n[r] = i, o = i.key) : (n[i.key] = i, o = i.key), this.defaultOrderPattern.push(o);
    }), this.predefinedItems = n;
  }
  /**
   * Get all menu items based on pattern.
   *
   * @param {Array|object|boolean} pattern Pattern which you can define by displaying menu items order. If `true` default
   *                                       pattern will be used.
   * @returns {Array}
   */
  getItems() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
    return s7(e, this.defaultOrderPattern, this.predefinedItems);
  }
}
function s7() {
  let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null, e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [], n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
  const i = [];
  let r = t;
  return r && r.items ? r = r.items : Array.isArray(r) || (r = e), Be(r) ? Fe(r, (o, s) => {
    let a = n[typeof o == "string" ? o : s];
    a || (a = o), Be(o) ? nn(a, o) : typeof a == "string" && (a = {
      name: a
    }), a.key === void 0 && (a.key = s), i.push(a);
  }) : T(r, (o, s) => {
    let a = n[o];
    !a && R5.indexOf(o) >= 0 || (a || (a = {
      name: o,
      key: `${s}`
    }), Be(o) && nn(a, o), a.key === void 0 && (a.key = s), i.push(a));
  }), i;
}
function cs(t, e, n) {
  return (e = a7(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function a7(t) {
  var e = l7(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function l7(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class u7 {
  constructor(e, n) {
    cs(this, "top", void 0), cs(this, "topRelative", void 0), cs(this, "left", void 0), cs(this, "leftRelative", void 0), cs(this, "scrollTop", void 0), cs(this, "scrollLeft", void 0), cs(this, "cellHeight", void 0), cs(this, "cellWidth", void 0);
    const i = n.scrollY, r = n.scrollX;
    let o, s, a, l, u, c;
    this.rootWindow = n, this.type = this.getSourceType(e), this.type === "literal" ? (o = parseInt(e.top, 10), a = parseInt(e.left, 10), u = e.height || 0, c = e.width || 0, s = o, l = a, o += i, a += r) : this.type === "event" && (o = parseInt(e.pageY, 10), a = parseInt(e.pageX, 10), u = e.target.clientHeight, c = e.target.clientWidth, s = o - i, l = a - r), this.top = o, this.topRelative = s, this.left = a, this.leftRelative = l, this.scrollTop = i, this.scrollLeft = r, this.cellHeight = u, this.cellWidth = c;
  }
  /**
   * Get source type name.
   *
   * @param {*} object Event or Object with coordinates.
   * @returns {string} Returns one of this values: `'literal'`, `'event'`.
   */
  getSourceType(e) {
    let n = "literal";
    return e instanceof Event && (n = "event"), n;
  }
  /**
   * Checks if element can be placed above the cursor.
   *
   * @param {HTMLElement} element Element to check if it's size will fit above the cursor.
   * @returns {boolean}
   */
  fitsAbove(e) {
    return this.topRelative >= e.offsetHeight;
  }
  /**
   * Checks if element can be placed below the cursor.
   *
   * @param {HTMLElement} element Element to check if it's size will fit below the cursor.
   * @param {number} [viewportHeight] The viewport height.
   * @returns {boolean}
   */
  fitsBelow(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.rootWindow.innerHeight;
    return this.topRelative + e.offsetHeight <= n;
  }
  /**
   * Checks if element can be placed on the right of the cursor.
   *
   * @param {HTMLElement} element Element to check if it's size will fit on the right of the cursor.
   * @param {number} [viewportWidth] The viewport width.
   * @returns {boolean}
   */
  fitsOnRight(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.rootWindow.innerWidth;
    return this.leftRelative + this.cellWidth + e.offsetWidth <= n;
  }
  /**
   * Checks if element can be placed on the left on the cursor.
   *
   * @param {HTMLElement} element Element to check if it's size will fit on the left of the cursor.
   * @returns {boolean}
   */
  fitsOnLeft(e) {
    return this.leftRelative >= e.offsetWidth;
  }
}
function bc(t, e, n) {
  c7(t, e), e.set(t, n);
}
function c7(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function qe(t, e) {
  return t.get(SH(t, e));
}
function Kf(t, e, n) {
  return t.set(SH(t, e), n), n;
}
function SH(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Un = /* @__PURE__ */ new WeakMap(), hs = /* @__PURE__ */ new WeakMap(), ni = /* @__PURE__ */ new WeakMap(), WC = /* @__PURE__ */ new WeakMap(), Wl = /* @__PURE__ */ new WeakMap();
class h7 {
  constructor(e) {
    bc(this, Un, void 0), bc(this, hs, void 0), bc(this, ni, void 0), bc(this, WC, !1), bc(this, Wl, {
      above: 0,
      below: 0,
      left: 0,
      right: 0
    }), Kf(WC, this, e);
  }
  /**
   * Sets offset position for specified directions (`above`, `below`, `left` or `right`).
   *
   * @param {'above' | 'below' | 'left' | 'right'} direction A direction name.
   * @param {number} [offset=0] Offset value.
   * @returns {Positioner}
   */
  setOffset(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
    return qe(Wl, this)[e] = n, this;
  }
  /**
   * Sets the menu element to work with. The element can be owned by the main menu or the submenu.
   *
   * @param {HTMLElement} container The menu container element.
   * @returns {Positioner}
   */
  setElement(e) {
    return Kf(Un, this, e), this;
  }
  /**
   * Sets the parent menu element to work with.
   *
   * @param {HTMLElement} container The parent menu container element.
   * @returns {Positioner}
   */
  setParentElement(e) {
    return Kf(hs, this, e), this;
  }
  /**
   * Updates the menu position.
   *
   * @param {object|MouseEvent} coords The literal object with `top`, `left`, `width` and `height` props or a
   * mouse event object.
   */
  updatePosition(e) {
    Kf(ni, this, new u7(e, qe(Un, this).ownerDocument.defaultView)), qe(WC, this) ? (qe(ni, this).fitsBelow(qe(Un, this)) ? this.setPositionBelowCursor() : qe(ni, this).fitsAbove(qe(Un, this)) ? this.setPositionAboveCursor() : this.setPositionBelowCursor(), this.updateHorizontalPosition()) : (this.setPositionBelowCursor(), this.setPositionOnRightOfCursor());
  }
  /**
   * Updates the menu horizontal position.
   */
  updateHorizontalPosition() {
    qe(Un, this).dir === "rtl" ? qe(ni, this).fitsOnLeft(qe(Un, this)) ? this.setPositionOnLeftOfCursor() : this.setPositionOnRightOfCursor() : qe(ni, this).fitsOnRight(qe(Un, this)) ? this.setPositionOnRightOfCursor() : this.setPositionOnLeftOfCursor();
  }
  /**
   * Sets the menu position above the cursor object.
   */
  setPositionAboveCursor() {
    let e = qe(Wl, this).above + qe(ni, this).top - qe(Un, this).offsetHeight;
    qe(hs, this) && (e = qe(ni, this).top + qe(ni, this).cellHeight - qe(Un, this).offsetHeight + 3), qe(Un, this).style.top = `${e}px`;
  }
  /**
   * Sets the menu position below the cursor object.
   */
  setPositionBelowCursor() {
    let e = qe(Wl, this).below + qe(ni, this).top + 1;
    qe(hs, this) && (e = qe(ni, this).top - 1), qe(Un, this).style.top = `${e}px`;
  }
  /**
   * Sets the menu position on the right of the cursor object.
   */
  setPositionOnRightOfCursor() {
    let e = qe(ni, this).left;
    if (qe(hs, this)) {
      const n = Number.parseInt(getComputedStyle(qe(hs, this).querySelector(".htCore")).borderRightWidth, 10);
      e += qe(ni, this).cellWidth + n;
    } else
      e += qe(Wl, this).right;
    qe(Un, this).style.left = `${e}px`;
  }
  /**
   * Sets the menu position on the left of the cursor object.
   */
  setPositionOnLeftOfCursor() {
    let e = qe(Wl, this).left + qe(ni, this).left - qe(Un, this).offsetWidth;
    if (qe(hs, this)) {
      const n = Number.parseInt(getComputedStyle(qe(hs, this).querySelector(".htCore")).borderLeftWidth, 10);
      e -= n;
    }
    qe(Un, this).style.left = `${e}px`;
  }
}
function bH(t) {
  let {
    initialPage: e = -1,
    size: n = () => 0,
    onItemSelect: i = () => {
    },
    onClear: r = () => {
    }
  } = t;
  const o = /* @__PURE__ */ new Set();
  let s = ft(e, -1, m() - 1);
  function a(C, S) {
    const b = m() - 1;
    return C < 0 && (C = b), C > b && (C = 0), o.has(C) ? -1 : (o.add(C), i(C, !1) === !1 && (C = a(
      S === 1 ? ++C : --C,
      // eslint-disable-line no-plusplus
      S
    )), C);
  }
  function l(C) {
    C > -1 && C < m() && i(C, !0) !== !1 && (s = C);
  }
  function u() {
    return s;
  }
  function c() {
    m() > 0 && (o.clear(), s = a(0, 1));
  }
  function h() {
    m() > 0 && (o.clear(), s = a(m() - 1, -1));
  }
  function d() {
    m() > 0 && (o.clear(), s = a(++s, 1));
  }
  function g() {
    m() > 0 && (o.clear(), s = a(--s, -1));
  }
  function m() {
    return Math.max(n(), 0);
  }
  function w() {
    o.clear(), s = e, r();
  }
  return {
    setCurrentPage: l,
    getCurrentPage: u,
    toFirstItem: c,
    toLastItem: h,
    toNextItem: d,
    toPreviousItem: g,
    getSize: m,
    clear: w
  };
}
function d7(t) {
  return Ke(t, (e) => ({
    start: e.getTopStartCorner(),
    end: e.getBottomEndCorner()
  }));
}
function $y(t) {
  return Bt(t, "submenu");
}
function Uy(t) {
  return new RegExp($e, "i").test(t.name);
}
function qg(t, e) {
  return t.disabled === !0 || typeof t.disabled == "function" && t.disabled.call(e) === !0;
}
function dM(t) {
  return Bt(t, "disableSelection");
}
function f7(t) {
  return Te(t, "htSeparator");
}
function g7(t) {
  return Te(t, "htSubmenu");
}
function m7(t) {
  return Te(t, "htDisabled");
}
function p7(t) {
  return Te(t, "htSelectionDisabled");
}
function w7(t, e) {
  return !t.hidden || !(typeof t.hidden == "function" && t.hidden.call(e));
}
function RH(t, e) {
  const n = t.slice(0);
  for (let i = 0; i < n.length && n[i].name === e; )
    n.shift();
  return n;
}
function A7(t, e) {
  let n = t.slice(0);
  return n.reverse(), n = RH(n, e), n.reverse(), n;
}
function C7(t) {
  const e = [];
  return T(t, (n, i) => {
    i > 0 ? e[e.length - 1].name !== n.name && e.push(n) : e.push(n);
  }), e;
}
function v7(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : $e, n = t.slice(0);
  return n = RH(n, e), n = A7(n, e), n = C7(n), n;
}
function y7(t) {
  return t.checkable === !0;
}
function S7(t) {
  return bH({
    size: () => t.countRows(),
    onItemSelect(e, n) {
      const i = t.getCell(e, 0);
      if (!i || f7(i) || m7(i) || p7(i))
        return !1;
      t.selectCell(e, 0, ...n ? [e, 0, !1, !1] : []);
    },
    onClear() {
      t.deselectCell();
    }
  });
}
function b7(t) {
  const {
    hot: e,
    hotMenu: n
  } = t;
  return [{
    keys: [["Control/Meta", "A"]],
    forwardToContext: e.getShortcutManager().getContext("grid"),
    callback: () => t.close(!0)
  }, {
    keys: [["Tab"], ["Shift", "Tab"]],
    callback: (i, r) => {
      const o = e.getSettings(), s = typeof o.tabMoves == "function" ? o.tabMoves(i) : o.tabMoves;
      r.includes("shift") ? e.selection.transformStart(-s.row, -s.col) : e.selection.transformStart(s.row, s.col), t.close(!0);
    }
  }, {
    keys: [["Escape"]],
    callback: () => t.close()
  }, {
    keys: [["ArrowDown"]],
    callback: () => t.getNavigator().toNextItem()
  }, {
    keys: [["ArrowUp"]],
    callback: () => t.getNavigator().toPreviousItem()
  }, {
    keys: [["ArrowRight"]],
    callback: () => {
      const i = n.getSelectedLast();
      if (i) {
        const r = t.openSubMenu(i[0]);
        r && r.getNavigator().toFirstItem();
      }
    }
  }, {
    keys: [["ArrowLeft"]],
    callback: () => {
      n.getSelectedLast() && t.isSubMenu() && (t.close(), t.isSubMenu() && t.parentMenu.hotMenu.listen());
    }
  }, {
    keys: [["Control/Meta", "ArrowUp"], ["Home"]],
    callback: () => t.getNavigator().toFirstItem()
  }, {
    keys: [["Control/Meta", "ArrowDown"], ["End"]],
    callback: () => t.getNavigator().toLastItem()
  }, {
    keys: [["Enter"], ["Space"]],
    callback: (i) => {
      const r = n.getSelectedLast();
      r && (n.getSourceDataAtRow(r[0]).submenu ? t.openSubMenu(r[0]).getNavigator().toFirstItem() : (t.executeCommand(i), t.close(!0)));
    }
  }, {
    keys: [["PageUp"]],
    callback: () => {
      n.getSelectedLast() ? n.selection.transformStart(-n.countVisibleRows(), 0) : t.getNavigator().toFirstItem();
    }
  }, {
    keys: [["PageDown"]],
    callback: () => {
      n.getSelectedLast() ? n.selection.transformStart(n.countVisibleRows(), 0) : t.getNavigator().toLastItem();
    }
  }];
}
const EH = "menu", fM = EH;
function R7(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
  const n = [];
  i(b7(t)), e.forEach((u) => {
    let {
      shortcuts: c,
      contextName: h
    } = u;
    r(c, h);
  });
  function i(u, c) {
    a(c).addShortcuts(u, {
      group: EH
    });
  }
  function r(u, c) {
    const h = a(c);
    u.forEach((d) => {
      let {
        keys: g
      } = d;
      g.forEach((m) => h.removeShortcutsByKeys(m));
    }), n.push({
      shortcuts: u,
      contextName: c
    }), i(u, c);
  }
  function o() {
    return [...n];
  }
  function s(u) {
    return u ? `${fM}:${u}` : fM;
  }
  function a(u) {
    var c;
    const h = t.hotMenu.getShortcutManager(), d = s(u);
    return (c = h.getContext(d)) !== null && c !== void 0 ? c : h.addContext(d);
  }
  function l(u) {
    t.hotMenu.getShortcutManager().setActiveContextName(s(u));
  }
  return {
    addCustomShortcuts: r,
    getCustomShortcuts: o,
    getContext: a,
    listen: l
  };
}
function E7(t) {
  return (e, n, i, r, o, s) => {
    const a = e.getSourceDataAtRow(i), l = t.rootDocument.createElement("div"), u = typeof s == "function" ? s.call(t) : s, c = typeof a.ariaLabel == "function" ? a.ariaLabel.call(t) : a.ariaLabel, h = typeof a.ariaChecked == "function" ? a.ariaChecked.call(t) : a.ariaChecked;
    if (br(n), Q(l, "htItemWrapper"), t.getSettings().ariaTags) {
      const d = !qg(a, t) && !dM(a) && !Uy(a);
      Ee(n, [...y7(a) ? [CG(), wm(c), NP(h)] : [AG(), wm(u)], ...d ? [Du(-1)] : [], ...qg(a, t) ? [TG()] : [], ...$y(a) ? [Gi(!1)] : []]);
    }
    n.className = "", n.appendChild(l), Uy(a) ? Q(n, "htSeparator") : typeof a.renderer == "function" ? (Q(n, "htCustomMenuRenderer"), n.appendChild(a.renderer(e, l, i, r, o, u))) : pl(l, u), qg(a, t) ? Q(n, "htDisabled") : dM(a) ? Q(n, "htSelectionDisabled") : $y(a) && Q(n, "htSubmenu");
  };
}
function $C(t, e, n) {
  T7(t, e), e.set(t, n);
}
function T7(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Fr(t, e, n) {
  return (e = M7(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function M7(t) {
  var e = I7(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function I7(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function UC(t, e, n) {
  return t.set(TH(t, e), n), n;
}
function ba(t, e) {
  return t.get(TH(t, e));
}
function TH(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const gM = 215;
var Rc = /* @__PURE__ */ new WeakMap(), Qf = /* @__PURE__ */ new WeakMap(), Xf = /* @__PURE__ */ new WeakMap();
class Wu {
  /**
   * Getter for the table border width.
   * This getter retrieves the border width of the table used in the menu.
   *
   * @returns {number} The border width of the table in pixels.
   */
  get tableBorderWidth() {
    return ba(Xf, this) === void 0 && this.hotMenu && UC(Xf, this, parseInt(uo(this.hotMenu.view._wt.wtTable.TABLE).borderWidth, 10)), ba(Xf, this);
  }
  /**
   * @param {Core} hotInstance Handsontable instance.
   * @param {MenuOptions} [options] Menu options.
   */
  constructor(e, n) {
    var i = this;
    Fr(this, "hot", void 0), Fr(this, "options", void 0), Fr(this, "eventManager", new Qi(this)), Fr(this, "container", void 0), Fr(this, "positioner", void 0), Fr(this, "hotMenu", null), Fr(this, "hotSubMenus", {}), Fr(this, "parentMenu", void 0), Fr(this, "menuItems", null), Fr(this, "origOutsideClickDeselects", null), $C(this, Rc, void 0), $C(this, Qf, void 0), $C(this, Xf, void 0), this.hot = e, this.options = n || {
      parent: null,
      name: null,
      className: "",
      keepInViewport: !0,
      standalone: !1,
      minWidth: gM,
      container: this.hot.rootDocument.documentElement
    }, this.container = this.createContainer(this.options.name), this.positioner = new h7(this.options.keepInViewport), this.parentMenu = this.options.parent || null, this.registerEvents(), this.isSubMenu() && this.addLocalHook("afterSelectionChange", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return i.parentMenu.runLocalHooks("afterSelectionChange", ...o);
    });
  }
  /**
   * Register event listeners.
   *
   * @private
   */
  registerEvents() {
    let e = this.hot.rootWindow;
    for (; e; )
      this.eventManager.addEventListener(e.document, "mousedown", (n) => this.onDocumentMouseDown(n)), this.eventManager.addEventListener(e.document, "touchstart", (n) => this.onDocumentMouseDown(n)), this.eventManager.addEventListener(e.document, "contextmenu", (n) => this.onDocumentContextMenu(n)), e = Ja(e);
  }
  /**
   * Set array of objects which defines menu items.
   *
   * @param {Array} menuItems Menu items to display.
   */
  setMenuItems(e) {
    this.menuItems = e;
  }
  /**
   * Gets the controller object that allows modifying the the menu item selection.
   *
   * @returns {Paginator | undefined}
   */
  getNavigator() {
    return ba(Rc, this);
  }
  /**
   * Gets the controller object that allows extending the keyboard shortcuts of the menu.
   *
   * @returns {KeyboardShortcutsMenuController | undefined}
   */
  getKeyboardShortcutsCtrl() {
    return ba(Qf, this);
  }
  /**
   * Returns currently selected menu item. Returns `null` if no item was selected.
   *
   * @returns {object|null}
   */
  getSelectedItem() {
    return this.hasSelectedItem() ? this.hotMenu.getSourceDataAtRow(this.hotMenu.getSelectedLast()[0]) : null;
  }
  /**
   * Checks if the menu has selected (highlighted) any item from the menu list.
   *
   * @returns {boolean}
   */
  hasSelectedItem() {
    return Array.isArray(this.hotMenu.getSelectedLast());
  }
  /**
   * Check if menu is using as sub-menu.
   *
   * @returns {boolean}
   */
  isSubMenu() {
    return this.parentMenu !== null;
  }
  /**
   * Open menu.
   *
   * @fires Hooks#beforeContextMenuShow
   * @fires Hooks#afterContextMenuShow
   */
  open() {
    this.runLocalHooks("beforeOpen"), this.container.removeAttribute("style"), this.container.style.display = "block";
    const e = Ip((a) => this.openSubMenu(a), 300), n = this.options.minWidth || gM;
    let i = !1, r = Zo(this.menuItems, (a) => (a.key === qh && (i = !0), w7(a, this.hot)));
    if (r.length < 1 && !i)
      r.push(wH()[qh]);
    else if (r.length === 0)
      return;
    r = v7(r, $e);
    let o = !1;
    const s = {
      data: r,
      colHeaders: !1,
      autoColumnSize: !0,
      autoWrapRow: !1,
      modifyColWidth(a) {
        return de(a) && a < n ? n : a;
      },
      autoRowSize: !1,
      readOnly: !0,
      editor: !1,
      copyPaste: !1,
      hiddenRows: !0,
      maxCols: 1,
      columns: [{
        data: "name",
        renderer: E7(this.hot)
      }],
      renderAllRows: !0,
      fragmentSelection: !1,
      outsideClickDeselects: !1,
      disableVisualSelection: "area",
      layoutDirection: this.hot.isRtl() ? "rtl" : "ltr",
      ariaTags: !1,
      beforeOnCellMouseOver: (a, l) => {
        ba(Rc, this).setCurrentPage(l.row);
      },
      afterOnCellMouseOver: (a, l) => {
        this.isAllSubMenusClosed() ? e(l.row) : this.openSubMenu(l.row);
      },
      rowHeights: (a) => r[a].name === $e ? 1 : void 0,
      afterOnCellContextMenu: (a) => {
        a.preventDefault(), Ay() && o && this.hasSelectedItem() && this.close(!0);
      },
      afterSelection: (a, l, u, c, h) => {
        this.hotMenu.view.isMouseDown() && (h.value = !0), this.runLocalHooks("afterSelectionChange", this.getSelectedItem());
      },
      beforeOnCellMouseUp: (a) => {
        this.hasSelectedItem() && (o = !this.isCommandPassive(this.getSelectedItem()), this.executeCommand(a));
      },
      afterOnCellMouseUp: (a) => {
        (!Ay() || !zs(a)) && o && this.hasSelectedItem() && (Yo() || fb() ? this.hot._registerTimeout(() => this.close(!0), 325) : this.close(!0));
      },
      afterUnlisten: () => {
        !this.hasSelectedItem() && this.isOpened() && this.hotMenu.listen();
      }
    };
    this.origOutsideClickDeselects = this.hot.getSettings().outsideClickDeselects, this.hot.getSettings().outsideClickDeselects = !1, this.hotMenu = new this.hot.constructor(this.container, s), this.hotMenu.addHook("afterInit", () => this.onAfterInit()), this.hotMenu.init(), UC(Rc, this, S7(this.hotMenu)), UC(Qf, this, R7(this)), ba(Qf, this).listen(), this.focus(), this.isSubMenu() && this.addLocalHook("afterOpen", () => this.parentMenu.runLocalHooks("afterSubmenuOpen", this)), this.runLocalHooks("afterOpen", this);
  }
  /**
   * Close menu.
   *
   * @param {boolean} [closeParent=false] If `true` try to close parent menu if exists.
   */
  close() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    if (this.isOpened()) {
      if (e && this.isSubMenu())
        this.parentMenu.close();
      else if (ba(Rc, this).clear(), this.closeAllSubMenus(), this.container.style.display = "none", this.hotMenu.destroy(), this.hotMenu = null, this.hot.getSettings().outsideClickDeselects = this.origOutsideClickDeselects, this.runLocalHooks("afterClose"), this.isSubMenu()) {
        if (this.hot.getSettings().ariaTags) {
          const n = this.parentMenu.hotMenu.getSelectedLast();
          if (n) {
            const i = this.parentMenu.hotMenu.getCell(n[0], 0);
            Ee(i, [Gi(!1)]);
          }
        }
        this.parentMenu.hotMenu.listen();
      }
    }
  }
  /**
   * Open sub menu at the provided row index.
   *
   * @param {number} row Row index.
   * @returns {Menu|boolean} Returns created menu or `false` if no one menu was created.
   */
  openSubMenu(e) {
    if (!this.hotMenu)
      return !1;
    const n = this.hotMenu.getCell(e, 0);
    if (this.closeAllSubMenus(), !n || !g7(n))
      return !1;
    const i = this.hotMenu.getSourceDataAtRow(e), r = new Wu(this.hot, {
      parent: this,
      name: i.name,
      className: this.options.className,
      keepInViewport: !0,
      container: this.options.container
    });
    return r.setMenuItems(i.submenu.items), r.open(), r.setPosition(n.getBoundingClientRect()), this.hotSubMenus[i.key] = r, this.hot.getSettings().ariaTags && Ee(n, [Gi(!0)]), r;
  }
  /**
   * Close sub menu at row index.
   *
   * @param {number} row Row index.
   */
  closeSubMenu(e) {
    const n = this.hotMenu.getSourceDataAtRow(e), i = this.hotSubMenus[n.key];
    if (i) {
      i.destroy(), delete this.hotSubMenus[n.key];
      const r = this.hotMenu.getCell(e, 0);
      this.hot.getSettings().ariaTags && Ee(r, [Gi(!1)]);
    }
  }
  /**
   * Close all opened sub menus.
   */
  closeAllSubMenus() {
    T(this.hotMenu.getData(), (e, n) => this.closeSubMenu(n));
  }
  /**
   * Checks if all created and opened sub menus are closed.
   *
   * @returns {boolean}
   */
  isAllSubMenusClosed() {
    return Object.keys(this.hotSubMenus).length === 0;
  }
  /**
   * Focus the menu so all keyboard shortcuts become active.
   */
  focus() {
    this.isOpened() && (this.hotMenu.rootElement.focus({
      preventScroll: !0
    }), this.getKeyboardShortcutsCtrl().listen(), this.hotMenu.listen());
  }
  /**
   * Destroy instance.
   */
  destroy() {
    const e = this.container.parentNode;
    this.clearLocalHooks(), this.close(), this.parentMenu = null, this.eventManager.destroy(), e && e.removeChild(this.container);
  }
  /**
   * Checks if menu was opened.
   *
   * @returns {boolean} Returns `true` if menu was opened.
   */
  isOpened() {
    return this.hotMenu !== null;
  }
  /**
   * Execute menu command.
   *
   * The `executeCommand()` method works only for selected cells.
   *
   * When no cells are selected, `executeCommand()` doesn't do anything.
   *
   * @param {Event} [event] The mouse event object.
   */
  executeCommand(e) {
    if (!this.isOpened() || !this.hasSelectedItem())
      return;
    const n = this.getSelectedItem();
    if (this.runLocalHooks("select", n, e), this.isCommandPassive(n))
      return;
    const i = this.hot.getSelectedRange(), r = i ? d7(i) : [];
    this.runLocalHooks("executeCommand", n.key, r, e), this.isSubMenu() && this.parentMenu.runLocalHooks("executeCommand", n.key, r, e);
  }
  /**
   * Checks if the passed command is passive or not. The command is passive when it's marked as
   * disabled, the descriptor object contains `isCommand` property set to `false`, command
   * is a separator, or the item is recognized as submenu. For passive items the menu is not
   * closed automatically after the user trigger the command through the UI.
   *
   * @param {object} commandDescriptor Selected menu item from the menu data source.
   * @returns {boolean}
   */
  isCommandPassive(e) {
    return e.isCommand === !1 || Uy(e) || qg(e, this.hot) || $y(e);
  }
  /**
   * Set offset menu position for specified area (`above`, `below`, `left` or `right`).
   *
   * @param {string} area Specified area name (`above`, `below`, `left` or `right`).
   * @param {number} offset Offset value.
   */
  setOffset(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
    this.positioner.setOffset(e, n);
  }
  /**
   * Set menu position based on dom event or based on literal object.
   *
   * @param {Event|object} coords Event or literal Object with coordinates.
   */
  setPosition(e) {
    this.isSubMenu() && this.positioner.setParentElement(this.parentMenu.container), this.positioner.setElement(this.container).updatePosition(e);
  }
  /**
   * Updates the dimensions of the menu based on its content.
   * This method calculates the real height of the menu by summing up the heights of its items,
   * and adjusts the width and height of the menu's holder and hider elements accordingly.
   */
  updateMenuDimensions() {
    const {
      wtTable: e
    } = this.hotMenu.view._wt, n = this.hotMenu.getSettings().data, i = e.hider.style, r = e.holder.style, o = parseInt(i.width, 10), s = rn(n, (a, l, u) => {
      const h = this.hotMenu.getCell(u, 0) ? Zt(this.hotMenu.getCell(u, 0)) : 0;
      return a + (l.name === $e ? 1 : h);
    }, 0);
    r.width = `${o + 3}px`, r.height = `${s + 3}px`, i.height = r.height;
  }
  /**
   * Create container/wrapper for handsontable.
   *
   * @private
   * @param {string} [name] Class name.
   * @returns {HTMLElement}
   */
  createContainer() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
    const n = this.options.container.ownerDocument;
    let i = e, r;
    return i && (Et(i) && (i = i.call(this.hot), i === null || Mt(i) ? i = "" : i = i.toString()), i = i.replace(/[^A-z0-9]/g, "_"), i = `${this.options.className}Sub_${i}`, r = n.querySelector(`.${this.options.className}.${i}`)), r || (r = n.createElement("div"), Q(r, `htMenu ${this.options.className}`), i && Q(r, i), this.options.container.appendChild(r)), r;
  }
  /**
   * On after init listener.
   *
   * @private
   */
  onAfterInit() {
    this.updateMenuDimensions(), this.hot.getSettings().ariaTags && Ee(this.hotMenu.rootElement, [wG(), Du(-1)]);
  }
  /**
   * Document mouse down listener.
   *
   * @private
   * @param {Event} event The mouse event object.
   */
  onDocumentMouseDown(e) {
    this.isOpened() && (this.options.standalone && this.hotMenu && !Vs(e.target, this.hotMenu.rootElement) ? this.close(!0) : (this.isAllSubMenusClosed() || this.isSubMenu()) && !Vs(e.target, ".htMenu") && this.close(!0));
  }
  /**
   * Document's contextmenu listener.
   *
   * @private
   * @param {MouseEvent} event The mouse event object.
   */
  onDocumentContextMenu(e) {
    this.isOpened() && Te(e.target, "htCore") && Vs(e.target, this.hotMenu.rootElement) && e.preventDefault();
  }
}
pt(Wu, Vn);
function O7(t, e) {
  P7(t, e), e.add(t);
}
function P7(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function YC(t, e, n) {
  return (e = x7(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function x7(t) {
  var e = H7(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function H7(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function qf(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const eh = "contextMenu", _7 = 70, mM = eh;
he.getSingleton().register("afterContextMenuDefaultOptions");
he.getSingleton().register("beforeContextMenuShow");
he.getSingleton().register("afterContextMenuShow");
he.getSingleton().register("afterContextMenuHide");
he.getSingleton().register("afterContextMenuExecute");
var Ec = /* @__PURE__ */ new WeakSet();
class sA extends at {
  constructor() {
    super(...arguments), O7(this, Ec), YC(this, "commandExecutor", new vH(this.hot)), YC(this, "itemsFactory", null), YC(this, "menu", null);
  }
  static get PLUGIN_KEY() {
    return eh;
  }
  static get PLUGIN_PRIORITY() {
    return _7;
  }
  static get PLUGIN_DEPS() {
    return ["plugin:AutoColumnSize"];
  }
  /**
   * Context menu default items order when `contextMenu` options is set as `true`.
   *
   * @returns {string[]}
   */
  static get DEFAULT_ITEMS() {
    return [nA, iA, $e, xd, Hd, $e, tA, Nd, $e, rA, eA, $e, _d, $e, Bi];
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ContextMenu#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[eh];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    if (this.enabled)
      return;
    const n = this.hot.getSettings()[eh];
    typeof n.callback == "function" && this.commandExecutor.setCommonCallback(n.callback), this.menu = new Wu(this.hot, {
      className: "htContextMenu",
      keepInViewport: !0,
      container: n.uiContainer || this.hot.rootDocument.body
    }), this.menu.addLocalHook("beforeOpen", () => qf(Ec, this, D7).call(this)), this.menu.addLocalHook("afterOpen", () => qf(Ec, this, k7).call(this)), this.menu.addLocalHook("afterClose", () => qf(Ec, this, L7).call(this)), this.menu.addLocalHook("executeCommand", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return e.executeCommand.call(e, ...r);
    }), this.addHook("afterOnCellContextMenu", (i) => qf(Ec, this, N7).call(this, i)), this.registerShortcuts(), super.enablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`contextMenu`](@/api/options.md#contextmenu)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.close(), this.menu && (this.menu.destroy(), this.menu = null), this.unregisterShortcuts(), super.disablePlugin();
  }
  /**
   * Register shortcuts responsible for toggling context menu.
   *
   * @private
   */
  registerShortcuts() {
    this.hot.getShortcutManager().getContext("grid").addShortcut({
      keys: [["Control/Meta", "Shift", "Backslash"], ["Shift", "F10"]],
      callback: () => {
        const {
          highlight: e
        } = this.hot.getSelectedRangeLast();
        this.hot.scrollToFocusedCell();
        const n = this.hot.getCell(e.row, e.col, !0).getBoundingClientRect(), i = Zw(this.menu.container, this.hot.rootDocument);
        this.open({
          left: n.left + i.left,
          top: n.top + i.top - 1 + n.height
        }, {
          left: n.width,
          above: -n.height
        }), this.menu.getNavigator().toFirstItem();
      },
      runOnlyIf: () => {
        var e;
        const n = (e = this.hot.getSelectedRangeLast()) === null || e === void 0 ? void 0 : e.highlight;
        return n && this.hot.selection.isCellVisible(n) && !this.menu.isOpened();
      },
      group: mM
    });
  }
  /**
   * Unregister shortcuts responsible for toggling context menu.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("grid").removeShortcutsByGroup(mM);
  }
  /**
   * Opens menu and re-position it based on the passed coordinates.
   *
   * @param {{ top: number, left: number }|Event} position An object with `top` and `left` properties
   * which contains coordinates relative to the browsers viewport (without included scroll offsets).
   * Or if the native event is passed the menu will be positioned based on the `pageX` and `pageY`
   * coordinates.
   * @param {{ above: number, below: number, left: number, right: number }} offset An object allows applying
   * the offset to the menu position.
   * @fires Hooks#beforeContextMenuShow
   * @fires Hooks#afterContextMenuShow
   */
  open(e) {
    var n;
    let i = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
      above: 0,
      below: 0,
      left: 0,
      right: 0
    };
    if ((n = this.menu) !== null && n !== void 0 && n.isOpened())
      return;
    this.prepareMenuItems(), this.menu.open(), this.menu.tableBorderWidth > 0 || (i.below += 1, i.right += 1, i.above -= 1, i.left -= 1), Fe(i, (o, s) => {
      this.menu.setOffset(s, o);
    }), this.menu.setPosition(e);
  }
  /**
   * Closes the menu.
   */
  close() {
    var e;
    (e = this.menu) === null || e === void 0 || e.close(), this.itemsFactory = null;
  }
  /**
   * Execute context menu command.
   *
   * The `executeCommand()` method works only for selected cells.
   *
   * When no cells are selected, `executeCommand()` doesn't do anything.
   *
   * You can execute all predefined commands:
   *  * `'row_above'` - Insert row above
   *  * `'row_below'` - Insert row below
   *  * `'col_left'` - Insert column left
   *  * `'col_right'` - Insert column right
   *  * `'clear_column'` - Clear selected column
   *  * `'remove_row'` - Remove row
   *  * `'remove_col'` - Remove column
   *  * `'undo'` - Undo last action
   *  * `'redo'` - Redo last action
   *  * `'make_read_only'` - Make cell read only
   *  * `'alignment:left'` - Alignment to the left
   *  * `'alignment:top'` - Alignment to the top
   *  * `'alignment:right'` - Alignment to the right
   *  * `'alignment:bottom'` - Alignment to the bottom
   *  * `'alignment:middle'` - Alignment to the middle
   *  * `'alignment:center'` - Alignment to the center (justify).
   *
   * Or you can execute command registered in settings where `key` is your command name.
   *
   * @param {string} commandName The command name to be executed.
   * @param {*} params Additional parameters passed to command executor module.
   */
  executeCommand(e) {
    this.itemsFactory === null && this.prepareMenuItems();
    for (var n = arguments.length, i = new Array(n > 1 ? n - 1 : 0), r = 1; r < n; r++)
      i[r - 1] = arguments[r];
    this.commandExecutor.execute(e, ...i);
  }
  /**
   * Prepares available contextMenu's items list and registers them in commandExecutor.
   *
   * @private
   * @fires Hooks#afterContextMenuDefaultOptions
   * @fires Hooks#beforeContextMenuSetItems
   */
  prepareMenuItems() {
    this.itemsFactory = new yH(this.hot, sA.DEFAULT_ITEMS);
    const e = this.hot.getSettings()[eh], n = {
      items: this.itemsFactory.getItems(e)
    };
    this.hot.runHooks("afterContextMenuDefaultOptions", n), this.itemsFactory.setPredefinedItems(n.items);
    const i = this.itemsFactory.getItems(e);
    this.hot.runHooks("beforeContextMenuSetItems", i), this.menu.setMenuItems(i), T(i, (r) => this.commandExecutor.registerCommand(r.key, r));
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    this.close(), this.menu && this.menu.destroy(), super.destroy();
  }
}
function N7(t) {
  const e = this.hot.getSettings(), n = e.rowHeaders, i = e.colHeaders;
  function r(a) {
    return a.nodeName === "TD" || a.parentNode.nodeName === "TD";
  }
  const o = t.target;
  if (this.close(), Te(o, "handsontableInput") || (t.preventDefault(), t.stopPropagation(), !(n || i) && !r(o) && !(Te(o, "current") && Te(o, "wtBorder"))))
    return;
  const s = Zw(this.menu.container, this.hot.rootDocument);
  this.open({
    top: t.clientY + s.top,
    left: t.clientX + s.left
  });
}
function D7() {
  this.hot.runHooks("beforeContextMenuShow", this);
}
function k7() {
  this.hot.runHooks("afterContextMenuShow", this);
}
function L7() {
  this.hot.listen(), this.hot.runHooks("afterContextMenuHide", this);
}
sA.SEPARATOR = {
  name: $e
};
function B7(t) {
  return {
    key: "copy",
    name() {
      return this.getTranslatedPhrase(zp);
    },
    callback() {
      t.copyCellsOnly();
    },
    disabled() {
      if (this.countRows() === 0 || this.countCols() === 0)
        return !0;
      const e = this.getSelectedRangeLast();
      if (!e || e.isSingleHeader())
        return !0;
      const n = this.getSelected();
      return !n || n.length > 1;
    },
    hidden: !1
  };
}
function F7(t) {
  return {
    key: "copy_column_headers_only",
    name() {
      const e = this.getSelectedRangeLast(), n = e ? ft(e.getWidth() - 1, 0, 1) : 0;
      return this.getTranslatedPhrase(Xp, n);
    },
    callback() {
      t.copyColumnHeadersOnly();
    },
    disabled() {
      if (!this.hasColHeaders())
        return !0;
      const e = this.getSelectedRangeLast();
      if (!e || e.isSingleHeader())
        return !0;
      const n = this.getSelected();
      return !n || n.length > 1;
    },
    hidden: !1
  };
}
function V7(t) {
  return {
    key: "copy_with_column_group_headers",
    name() {
      const e = this.getSelectedRangeLast(), n = e ? ft(e.getWidth() - 1, 0, 1) : 0;
      return this.getTranslatedPhrase(Qp, n);
    },
    callback() {
      t.copyWithAllColumnHeaders();
    },
    disabled() {
      if (!this.hasColHeaders() || !this.getSettings().nestedHeaders)
        return !0;
      const e = this.getSelectedRangeLast();
      if (!e || e.isSingleHeader())
        return !0;
      const n = this.getSelected();
      return !n || n.length > 1;
    },
    hidden: !1
  };
}
function W7(t) {
  return {
    key: "copy_with_column_headers",
    name() {
      const e = this.getSelectedRangeLast(), n = e ? ft(e.getWidth() - 1, 0, 1) : 0;
      return this.getTranslatedPhrase(Kp, n);
    },
    callback() {
      t.copyWithColumnHeaders();
    },
    disabled() {
      if (!this.hasColHeaders())
        return !0;
      const e = this.getSelectedRangeLast();
      if (!e || e.isSingleHeader())
        return !0;
      const n = this.getSelected();
      return !n || n.length > 1;
    },
    hidden: !1
  };
}
function $7(t) {
  return {
    key: "cut",
    name() {
      return this.getTranslatedPhrase(qp);
    },
    callback() {
      t.cut();
    },
    disabled() {
      if (this.countRows() === 0 || this.countCols() === 0)
        return !0;
      const e = this.getSelectedRangeLast();
      if (!e || e.isSingleHeader())
        return !0;
      const n = this.getSelected();
      return !n || n.length > 1;
    },
    hidden: !1
  };
}
class U7 {
  constructor() {
    this.data = {};
  }
  setData(e, n) {
    this.data[e] = n;
  }
  getData(e) {
    return this.data[e] || void 0;
  }
}
class Y7 {
  constructor() {
    this.clipboardData = new U7();
  }
  preventDefault() {
  }
}
function j7(t, e) {
  MH(t, e), e.add(t);
}
function $l(t, e, n) {
  MH(t, e), e.set(t, n);
}
function MH(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Gn(t, e) {
  return t.get(fu(t, e));
}
function Ul(t, e, n) {
  return t.set(fu(t, e), n), n;
}
function fu(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var ds = /* @__PURE__ */ new WeakMap(), jC = /* @__PURE__ */ new WeakMap(), Tc = /* @__PURE__ */ new WeakMap(), Yy = /* @__PURE__ */ new WeakMap(), jy = /* @__PURE__ */ new WeakMap(), Mc = /* @__PURE__ */ new WeakMap(), Ic = /* @__PURE__ */ new WeakSet();
class G7 {
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * @param {{
   *   countRows: function(): number,
   *   countColumns: function(): number,
   *   rowsLimit: function(): number,
   *   columnsLimit: function(): number,
   *   countColumnHeaders: function(): number
   * }} dependencies The utils class dependencies.
   */
  constructor(e) {
    let {
      countRows: n,
      countColumns: i,
      rowsLimit: r,
      columnsLimit: o,
      countColumnHeaders: s
    } = e;
    j7(this, Ic), $l(this, ds, void 0), $l(this, jC, void 0), $l(this, Tc, void 0), $l(this, Yy, void 0), $l(this, jy, void 0), $l(this, Mc, void 0), Ul(jC, this, n), Ul(Tc, this, i), Ul(Yy, this, r), Ul(jy, this, o), Ul(Mc, this, s);
  }
  /* eslint-enable jsdoc/require-description-complete-sentence */
  /**
   * Sets the selection range to be processed.
   *
   * @param {CellRange} selectedRange The selection range represented by the CellRange class.
   */
  setSelectedRange(e) {
    Ul(ds, this, e);
  }
  /**
   * Returns a new coords object within the dataset range (cells) with `startRow`, `startCol`, `endRow`
   * and `endCol` keys.
   *
   * @returns {{startRow: number, startCol: number, endRow: number, endCol: number} | null}
   */
  getCellsRange() {
    if (Gn(jC, this).call(this) === 0 || Gn(Tc, this).call(this) === 0)
      return null;
    const {
      row: e,
      col: n
    } = Gn(ds, this).getTopStartCorner(), {
      row: i,
      col: r
    } = Gn(ds, this).getBottomEndCorner(), o = fu(Ic, this, z7).call(this, e, i), s = fu(Ic, this, GC).call(this, n, r);
    return {
      isRangeTrimmed: i !== o || r !== s,
      startRow: e,
      startCol: n,
      endRow: o,
      endCol: s
    };
  }
  /**
   * Returns a new coords object within the most-bottom column headers range with `startRow`,
   * `startCol`, `endRow` and `endCol` keys.
   *
   * @returns {{startRow: number, startCol: number, endRow: number, endCol: number} | null}
   */
  getMostBottomColumnHeadersRange() {
    if (Gn(Tc, this).call(this) === 0 || Gn(Mc, this).call(this) === 0)
      return null;
    const {
      col: e
    } = Gn(ds, this).getTopStartCorner(), {
      col: n
    } = Gn(ds, this).getBottomEndCorner(), i = fu(Ic, this, GC).call(this, e, n);
    return {
      isRangeTrimmed: n !== i,
      startRow: -1,
      startCol: e,
      endRow: -1,
      endCol: i
    };
  }
  /**
   * Returns a new coords object within all column headers layers (including nested headers) range with
   * `startRow`, `startCol`, `endRow` and `endCol` keys.
   *
   * @returns {{startRow: number, startCol: number, endRow: number, endCol: number} | null}
   */
  getAllColumnHeadersRange() {
    if (Gn(Tc, this).call(this) === 0 || Gn(Mc, this).call(this) === 0)
      return null;
    const {
      col: e
    } = Gn(ds, this).getTopStartCorner(), {
      col: n
    } = Gn(ds, this).getBottomEndCorner(), i = fu(Ic, this, GC).call(this, e, n);
    return {
      isRangeTrimmed: n !== i,
      startRow: -Gn(Mc, this).call(this),
      startCol: e,
      endRow: -1,
      endCol: i
    };
  }
}
function GC(t, e) {
  return Math.min(e, Math.max(t + Gn(jy, this).call(this) - 1, t));
}
function z7(t, e) {
  return Math.min(e, Math.max(t + Gn(Yy, this).call(this) - 1, t));
}
function IH(t) {
  const e = [], n = [];
  return T(t, (i) => {
    const r = Math.min(i.startRow, i.endRow), o = Math.max(i.startRow, i.endRow);
    Ae(r, o, (l) => {
      e.indexOf(l) === -1 && e.push(l);
    });
    const s = Math.min(i.startCol, i.endCol), a = Math.max(i.startCol, i.endCol);
    Ae(s, a, (l) => {
      n.indexOf(l) === -1 && n.push(l);
    });
  }), {
    rows: e,
    columns: n
  };
}
function K7(t, e) {
  OH(t, e), e.add(t);
}
function fs(t, e, n) {
  OH(t, e), e.set(t, n);
}
function OH(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Oc(t, e, n) {
  return (e = Q7(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Q7(t) {
  var e = X7(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function X7(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function ui(t, e) {
  return t.get(ci(t, e));
}
function hr(t, e, n) {
  return t.set(ci(t, e), n), n;
}
function ci(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("afterCopyLimit");
he.getSingleton().register("modifyCopyableRange");
he.getSingleton().register("beforeCut");
he.getSingleton().register("afterCut");
he.getSingleton().register("beforePaste");
he.getSingleton().register("afterPaste");
he.getSingleton().register("beforeCopy");
he.getSingleton().register("afterCopy");
const Zf = "copyPaste", q7 = 80, Z7 = ["fragmentSelection"], pM = ['<meta name="generator" content="Handsontable"/>', '<style type="text/css">td{white-space:normal}br{mso-data-placement:same-cell}</style>'].join("");
var Gy = /* @__PURE__ */ new WeakMap(), zy = /* @__PURE__ */ new WeakMap(), Ky = /* @__PURE__ */ new WeakMap(), Yl = /* @__PURE__ */ new WeakMap(), Jf = /* @__PURE__ */ new WeakMap(), eg = /* @__PURE__ */ new WeakMap(), jl = /* @__PURE__ */ new WeakMap(), Nm = /* @__PURE__ */ new WeakMap(), Ci = /* @__PURE__ */ new WeakSet();
class J7 extends at {
  constructor() {
    super(...arguments), K7(this, Ci), Oc(this, "columnsLimit", 1 / 0), Oc(this, "rowsLimit", 1 / 0), Oc(this, "pasteMode", "overwrite"), Oc(this, "uiContainer", this.hot.rootDocument.body), fs(this, Gy, !1), fs(this, zy, !1), fs(this, Ky, !1), fs(this, Yl, "cells-only"), fs(this, Jf, !1), fs(this, eg, !1), fs(this, jl, new G7({
      countRows: () => this.hot.countRows(),
      countColumns: () => this.hot.countCols(),
      rowsLimit: () => this.rowsLimit,
      columnsLimit: () => this.columnsLimit,
      countColumnHeaders: () => this.hot.view.getColumnHeadersCount()
    })), fs(this, Nm, !1), Oc(this, "copyableRanges", []);
  }
  static get PLUGIN_KEY() {
    return Zf;
  }
  static get SETTING_KEYS() {
    return [Zf, ...Z7];
  }
  static get PLUGIN_PRIORITY() {
    return q7;
  }
  /**
   * Checks if the [`CopyPaste`](#copypaste) plugin is enabled.
   *
   * This method gets called by Handsontable's [`beforeInit`](@/api/hooks.md#beforeinit) hook.
   * If it returns `true`, the [`enablePlugin()`](#enableplugin) method gets called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[Zf];
  }
  /**
   * Enables the [`CopyPaste`](#copypaste) plugin for your Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    if (this.enabled)
      return;
    const {
      [Zf]: n
    } = this.hot.getSettings();
    if (typeof n == "object") {
      var i, r;
      this.pasteMode = (i = n.pasteMode) !== null && i !== void 0 ? i : this.pasteMode, this.rowsLimit = isNaN(n.rowsLimit) ? this.rowsLimit : n.rowsLimit, this.columnsLimit = isNaN(n.columnsLimit) ? this.columnsLimit : n.columnsLimit, hr(Gy, this, !!n.copyColumnHeaders), hr(zy, this, !!n.copyColumnGroupHeaders), hr(Ky, this, !!n.copyColumnHeadersOnly), this.uiContainer = (r = n.uiContainer) !== null && r !== void 0 ? r : this.uiContainer;
    }
    this.addHook("afterContextMenuDefaultOptions", (o) => ci(Ci, this, nee).call(this, o)), this.addHook("afterSelection", function() {
      for (var o = arguments.length, s = new Array(o), a = 0; a < o; a++)
        s[a] = arguments[a];
      return ci(Ci, e, iee).call(e, ...s);
    }), this.addHook("afterSelectionEnd", () => ci(Ci, this, ree).call(this)), this.eventManager.addEventListener(this.hot.rootDocument, "copy", function() {
      return e.onCopy(...arguments);
    }), this.eventManager.addEventListener(this.hot.rootDocument, "cut", function() {
      return e.onCut(...arguments);
    }), this.eventManager.addEventListener(this.hot.rootDocument, "paste", function() {
      return e.onPaste(...arguments);
    }), hb() && (this.eventManager.addEventListener(this.hot.rootDocument.body, "mouseenter", function() {
      for (var o = arguments.length, s = new Array(o), a = 0; a < o; a++)
        s[a] = arguments[a];
      return ci(Ci, e, oee).call(e, ...s);
    }), this.eventManager.addEventListener(this.hot.rootDocument.body, "mouseleave", function() {
      for (var o = arguments.length, s = new Array(o), a = 0; a < o; a++)
        s[a] = arguments[a];
      return ci(Ci, e, see).call(e, ...s);
    }), this.addHook("afterSelection", () => ci(Ci, this, aee).call(this))), super.enablePlugin();
  }
  /**
   * Updates the state of the [`CopyPaste`](#copypaste) plugin.
   *
   * Gets called when [`updateSettings()`](@/api/core.md#updatesettings)
   * is invoked with any of the following configuration options:
   *  - [`copyPaste`](@/api/options.md#copypaste)
   *  - [`fragmentSelection`](@/api/options.md#fragmentselection)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Disables the [`CopyPaste`](#copypaste) plugin for your Handsontable instance.
   */
  disablePlugin() {
    super.disablePlugin();
  }
  /**
   * Copies the contents of the selected cells (and/or their related column headers) to the system clipboard.
   *
   * Takes an optional parameter (`copyMode`) that defines the scope of copying:
   *
   * | `copyMode` value              | Description                                                     |
   * | ----------------------------- | --------------------------------------------------------------- |
   * | `'cells-only'` (default)      | Copy the selected cells                                         |
   * | `'with-column-headers'`       | - Copy the selected cells<br>- Copy the nearest column headers  |
   * | `'with-column-group-headers'` | - Copy the selected cells<br>- Copy all related columns headers |
   * | `'column-headers-only'`       | Copy the nearest column headers (without copying cells)         |
   *
   * @param {string} [copyMode='cells-only'] Copy mode.
   */
  copy() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "cells-only";
    hr(Yl, this, e), hr(Jf, this, !0), ci(Ci, this, wM).call(this, "copy");
  }
  /**
   * Copies the contents of the selected cells.
   */
  copyCellsOnly() {
    this.copy("cells-only");
  }
  /**
   * Copies the contents of column headers that are nearest to the selected cells.
   */
  copyColumnHeadersOnly() {
    this.copy("column-headers-only");
  }
  /**
   * Copies the contents of the selected cells and all their related column headers.
   */
  copyWithAllColumnHeaders() {
    this.copy("with-column-group-headers");
  }
  /**
   * Copies the contents of the selected cells and their nearest column headers.
   */
  copyWithColumnHeaders() {
    this.copy("with-column-headers");
  }
  /**
   * Cuts the contents of the selected cells to the system clipboard.
   */
  cut() {
    hr(eg, this, !0), ci(Ci, this, wM).call(this, "cut");
  }
  /**
   * Converts the contents of multiple ranges (`ranges`) into a single string.
   *
   * @param {Array<{startRow: number, startCol: number, endRow: number, endCol: number}>} ranges Array of objects with properties `startRow`, `endRow`, `startCol` and `endCol`.
   * @returns {string} A string that will be copied to the clipboard.
   */
  getRangedCopyableData(e) {
    return uh(this.getRangedData(e));
  }
  /**
   * Converts the contents of multiple ranges (`ranges`) into an array of arrays.
   *
   * @param {Array<{startRow: number, startCol: number, endRow: number, endCol: number}>} ranges Array of objects with properties `startRow`, `startCol`, `endRow` and `endCol`.
   * @returns {Array[]} An array of arrays that will be copied to the clipboard.
   */
  getRangedData(e) {
    const n = [], {
      rows: i,
      columns: r
    } = IH(e);
    return T(i, (o) => {
      const s = [];
      T(r, (a) => {
        o < 0 ? s.push(this.hot.getColHeader(a, o)) : s.push(this.hot.getCopyableData(o, a));
      }), n.push(s);
    }), n;
  }
  /**
   * Simulates the paste action.
   *
   * For security reasons, modern browsers don't allow reading from the system clipboard.
   *
   * @param {string} pastableText The value to paste, as a raw string.
   * @param {string} [pastableHtml=''] The value to paste, as HTML.
   */
  paste() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "", n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : e;
    if (!e && !n)
      return;
    const i = new Y7();
    e && i.clipboardData.setData("text/plain", e), n && i.clipboardData.setData("text/html", n), this.onPaste(i);
  }
  /**
   * Prepares copyable text from the cells selection in the invisible textarea.
   */
  setCopyableText() {
    const e = this.hot.getSelectedRangeLast();
    if (!e)
      return;
    if (e.isSingleHeader()) {
      this.copyableRanges = [];
      return;
    }
    ui(jl, this).setSelectedRange(e);
    const n = /* @__PURE__ */ new Map([["headers", null], ["cells", null]]);
    ui(Yl, this) === "column-headers-only" ? n.set("headers", ui(jl, this).getMostBottomColumnHeadersRange()) : (ui(Yl, this) === "with-column-headers" ? n.set("headers", ui(jl, this).getMostBottomColumnHeadersRange()) : ui(Yl, this) === "with-column-group-headers" && n.set("headers", ui(jl, this).getAllColumnHeadersRange()), n.set("cells", ui(jl, this).getCellsRange())), this.copyableRanges = Array.from(n.values()).filter((r) => r !== null).map((r) => {
      let {
        startRow: o,
        startCol: s,
        endRow: a,
        endCol: l
      } = r;
      return {
        startRow: o,
        startCol: s,
        endRow: a,
        endCol: l
      };
    }), this.copyableRanges = this.hot.runHooks("modifyCopyableRange", this.copyableRanges);
    const i = n.get("cells");
    if (i !== null && i.isRangeTrimmed) {
      const {
        startRow: r,
        startCol: o,
        endRow: s,
        endCol: a
      } = i;
      this.hot.runHooks("afterCopyLimit", s - r + 1, a - o + 1, this.rowsLimit, this.columnsLimit);
    }
  }
  /**
   * Verifies if editor exists and is open.
   *
   * @private
   * @returns {boolean}
   */
  isEditorOpened() {
    var e;
    return (e = this.hot.getActiveEditor()) === null || e === void 0 ? void 0 : e.isOpened();
  }
  /**
   * Prepares new values to populate them into datasource.
   *
   * @private
   * @param {Array} inputArray An array of the data to populate.
   * @param {Array} [selection] The selection which indicates from what position the data will be populated.
   * @returns {Array} Range coordinates after populate data.
   */
  populateValues(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.hot.getSelectedRangeLast();
    if (!e.length)
      return;
    const i = e.length, r = e[0].length, o = [], {
      row: s,
      col: a
    } = n.getTopStartCorner(), {
      row: l,
      col: u
    } = n.getBottomEndCorner();
    let c = s, h = a, d = s, g = a;
    for (; o.length < i || c <= l; ) {
      const {
        skipRowOnPaste: m,
        visualRow: w
      } = this.hot.getCellMeta(c, a);
      if (c = w + 1, m === !0)
        continue;
      d = w, h = a;
      const C = [], S = o.length % i;
      for (; C.length < r || h <= u; ) {
        const {
          skipColumnOnPaste: b,
          visualCol: R
        } = this.hot.getCellMeta(s, h);
        if (h = R + 1, b === !0)
          continue;
        g = R;
        const _ = C.length % r;
        C.push(e[S][_]);
      }
      o.push(C);
    }
    return hr(Nm, this, !0), this.hot.populateFromArray(s, a, o, void 0, void 0, "CopyPaste.paste", this.pasteMode), [s, a, d, g];
  }
  /**
   * Add the `contenteditable` attribute to the highlighted cell and select its content.
   */
  /**
   * `copy` event callback on textarea element.
   *
   * @param {Event} event ClipboardEvent.
   * @private
   */
  onCopy(e) {
    var n;
    const i = this.hot.getFocusManager().getRefocusElement(), r = (n = e.target) === null || n === void 0 ? void 0 : n.hasAttribute("data-hot-input");
    if (!this.hot.isListening() && !ui(Jf, this) || this.isEditorOpened() || e.target instanceof HTMLElement && (r && e.target !== i || !r && e.target !== this.hot.rootDocument.body))
      return;
    e.preventDefault(), this.setCopyableText(), hr(Jf, this, !1);
    const o = this.getRangedData(this.copyableRanges), s = ci(Ci, this, eee).call(this, this.copyableRanges);
    if (!!this.hot.runHooks("beforeCopy", o, this.copyableRanges, s)) {
      const l = uh(o);
      if (e && e.clipboardData) {
        const u = Ty(o, this.hot.rootDocument);
        e.clipboardData.setData("text/plain", l), e.clipboardData.setData("text/html", [pM, u].join(""));
      } else
        typeof ClipboardEvent == "undefined" && this.hot.rootWindow.clipboardData.setData("Text", l);
      this.hot.runHooks("afterCopy", o, this.copyableRanges, s);
    }
    hr(Yl, this, "cells-only");
  }
  /**
   * `cut` event callback on textarea element.
   *
   * @param {Event} event ClipboardEvent.
   * @private
   */
  onCut(e) {
    var n;
    const i = this.hot.getFocusManager().getRefocusElement(), r = (n = e.target) === null || n === void 0 ? void 0 : n.hasAttribute("data-hot-input");
    if (!this.hot.isListening() && !ui(eg, this) || this.isEditorOpened() || e.target instanceof HTMLElement && (r && e.target !== i || !r && e.target !== this.hot.rootDocument.body))
      return;
    e.preventDefault(), this.setCopyableText(), hr(eg, this, !1);
    const o = this.getRangedData(this.copyableRanges);
    if (!!this.hot.runHooks("beforeCut", o, this.copyableRanges)) {
      const a = uh(o);
      if (e && e.clipboardData) {
        const l = Ty(o, this.hot.rootDocument);
        e.clipboardData.setData("text/plain", a), e.clipboardData.setData("text/html", [pM, l].join(""));
      } else
        typeof ClipboardEvent == "undefined" && this.hot.rootWindow.clipboardData.setData("Text", a);
      this.hot.emptySelectedCells("CopyPaste.cut"), this.hot.runHooks("afterCut", o, this.copyableRanges);
    }
  }
  /**
   * `paste` event callback on textarea element.
   *
   * @param {Event} event ClipboardEvent or pseudo ClipboardEvent, if paste was called manually.
   * @private
   */
  onPaste(e) {
    var n;
    const i = this.hot.getFocusManager().getRefocusElement(), r = (n = e.target) === null || n === void 0 ? void 0 : n.hasAttribute("data-hot-input");
    if (!this.hot.isListening() || this.isEditorOpened() || !this.hot.getSelected() || e.target instanceof HTMLElement && (r && e.target !== i || !r && e.target !== this.hot.rootDocument.body))
      return;
    e.preventDefault();
    let o;
    if (e && typeof e.clipboardData != "undefined") {
      const c = Sp(e.clipboardData.getData("text/html"), {
        ADD_TAGS: ["meta"],
        ADD_ATTR: ["content"],
        FORCE_BODY: !0
      });
      c && /(<table)|(<TABLE)/g.test(c) ? o = Sx(c, this.hot.rootDocument).data : o = e.clipboardData.getData("text/plain");
    } else
      typeof ClipboardEvent == "undefined" && typeof this.hot.rootWindow.clipboardData != "undefined" && (o = this.hot.rootWindow.clipboardData.getData("Text"));
    if (typeof o == "string" && (o = G4(o)), o === void 0 || o && o.length === 0 || this.hot.runHooks("beforePaste", o, this.copyableRanges) === !1)
      return;
    const [s, a, l, u] = this.populateValues(o);
    this.hot.selectCell(s, a, Math.min(this.hot.countRows() - 1, l), Math.min(this.hot.countCols() - 1, u)), this.hot.runHooks("afterPaste", o, this.copyableRanges);
  }
  /**
   * Add copy and cut options to the Context Menu.
   *
   * @param {object} options Contains default added options of the Context Menu.
   */
  /**
   * Destroys the `CopyPaste` plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function wM(t) {
  if (hb()) {
    const e = this.hot.getSelectedRangeLast();
    if (e) {
      const {
        row: n,
        col: i
      } = e.highlight, r = this.hot.getCell(n, i, !0);
      r && KP(r, () => {
        this.hot.rootDocument.execCommand(t);
      });
    }
  } else
    this.hot.rootDocument.execCommand(t);
}
function eee(t) {
  const {
    rows: e
  } = IH(t);
  let n = 0;
  for (let i = 0; i < e.length && !(e[i] >= 0); i++)
    n += 1;
  return {
    columnHeadersCount: n
  };
}
function tee() {
  if (this.hot.isListening()) {
    const t = this.hot.getSelectedRangeLast();
    if (t) {
      const {
        row: e,
        col: n
      } = t.highlight, i = this.hot.getCell(e, n, !0);
      i && sb(i);
    }
  }
}
function PH() {
  if (this.hot.isListening()) {
    const t = this.hot.getSelectedRangeLast();
    if (t) {
      const {
        row: e,
        col: n
      } = t.highlight, i = this.hot.getCell(e, n, !0);
      i != null && i.hasAttribute("contenteditable") && ab(i);
    }
  }
}
function nee(t) {
  t.items.push({
    name: "---------"
  }, B7(this)), ui(Gy, this) && t.items.push(W7(this)), ui(zy, this) && t.items.push(V7(this)), ui(Ky, this) && t.items.push(F7(this)), t.items.push($7(this));
}
function iee(t, e, n, i, r) {
  ui(Nm, this) && (r.value = !0), hr(Nm, this, !1);
}
function ree() {
  this.isEditorOpened() || this.hot.getSettings().fragmentSelection || this.setCopyableText();
}
function oee() {
  ci(Ci, this, PH).call(this);
}
function see() {
  ci(Ci, this, tee).call(this);
}
function aee() {
  ci(Ci, this, PH).call(this);
}
function xH(t, e) {
  return `border_row${t}col${e}`;
}
function th() {
  return {
    width: 1,
    color: "#000"
  };
}
function jo() {
  return {
    hide: !0
  };
}
function lee() {
  return {
    width: 1,
    color: "#000",
    cornerVisible: !1
  };
}
function zC(t) {
  if (de(t.start) || de(t.left)) {
    var e;
    t.start = (e = t.start) !== null && e !== void 0 ? e : t.left;
  }
  if (de(t.end) || de(t.right)) {
    var n;
    t.end = (n = t.end) !== null && n !== void 0 ? n : t.right;
  }
  return delete t.left, delete t.right, t;
}
function Pc(t) {
  return de(t.start) && (t.left = t.start), de(t.end) && (t.right = t.end), t;
}
function KC(t, e) {
  return {
    id: xH(t, e),
    border: lee(),
    row: t,
    col: e,
    top: jo(),
    bottom: jo(),
    start: jo(),
    end: jo()
  };
}
function uee(t, e) {
  return Bt(e, "border") && e.border && (t.border = e.border), Bt(e, "top") && de(e.top) && (e.top ? (Be(e.top) || (e.top = th()), t.top = e.top) : (e.top = jo(), t.top = e.top)), Bt(e, "bottom") && de(e.bottom) && (e.bottom ? (Be(e.bottom) || (e.bottom = th()), t.bottom = e.bottom) : (e.bottom = jo(), t.bottom = e.bottom)), Bt(e, "start") && de(e.start) && (e.start ? (Be(e.start) || (e.start = th()), t.start = e.start) : (e.start = jo(), t.start = e.start)), Bt(e, "end") && de(e.end) && (e.end ? (Be(e.end) || (e.end = th()), t.end = e.end) : (e.end = jo(), t.end = e.end)), t;
}
function Jo(t, e) {
  let n = !1;
  return T(t.getSelectedRange(), (i) => {
    i.forAll((r, o) => {
      if (r < 0 || o < 0)
        return;
      const s = t.getCellMeta(r, o).borders;
      if (s)
        if (e) {
          if (!Bt(s[e], "hide") || s[e].hide === !1)
            return n = !0, !1;
        } else
          return n = !0, !1;
    });
  }), n;
}
function aA(t) {
  return `<span class="selected">${String.fromCharCode(10003)}</span>${t}`;
}
function cee(t) {
  return t.some((e) => de(e.left) || de(e.right));
}
function hee(t) {
  return t.some((e) => de(e.start) || de(e.end));
}
const dee = /* @__PURE__ */ new Map([["left", "start"], ["right", "end"]]);
function fee(t) {
  var e;
  return (e = dee.get(t)) !== null && e !== void 0 ? e : t;
}
function gee(t) {
  return {
    key: "borders:bottom",
    name() {
      let e = this.getTranslatedPhrase(cw);
      return Jo(this, "bottom") && (e = aA(e)), e;
    },
    callback(e, n) {
      const i = Jo(this, "bottom");
      t.prepareBorder(n, "bottom", i);
    }
  };
}
function mee(t) {
  const e = t.hot.isRtl() ? "end" : "start";
  return {
    key: "borders:left",
    name() {
      let n = this.getTranslatedPhrase(hw);
      return Jo(this, e) && (n = aA(n)), n;
    },
    callback(n, i) {
      const r = Jo(this, e);
      t.prepareBorder(i, e, r);
    }
  };
}
function pee(t) {
  return {
    key: "borders:no_borders",
    name() {
      return this.getTranslatedPhrase(dw);
    },
    callback(e, n) {
      t.prepareBorder(n, "noBorders");
    },
    disabled() {
      return !Jo(this);
    }
  };
}
function wee(t) {
  const e = t.hot.isRtl() ? "start" : "end";
  return {
    key: "borders:right",
    name() {
      let n = this.getTranslatedPhrase(uw);
      return Jo(this, e) && (n = aA(n)), n;
    },
    callback(n, i) {
      const r = Jo(this, e);
      t.prepareBorder(i, e, r);
    }
  };
}
function Aee(t) {
  return {
    key: "borders:top",
    name() {
      let e = this.getTranslatedPhrase(lw);
      return Jo(this, "top") && (e = aA(e)), e;
    },
    callback(e, n) {
      const i = Jo(this, "top");
      t.prepareBorder(n, "top", i);
    }
  };
}
function Cee(t, e) {
  vee(t, e), e.add(t);
}
function vee(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function yee(t, e, n) {
  return (e = See(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function See(t) {
  var e = bee(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function bee(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function AM(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const Zg = "customBorders", Ree = 90;
var QC = /* @__PURE__ */ new WeakSet();
class Eee extends at {
  constructor() {
    super(...arguments), Cee(this, QC), yee(this, "savedBorders", []);
  }
  static get PLUGIN_KEY() {
    return Zg;
  }
  static get PLUGIN_PRIORITY() {
    return Ree;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link CustomBorders#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[Zg];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled || (this.addHook("afterContextMenuDefaultOptions", (e) => AM(QC, this, Tee).call(this, e)), this.addHook("init", () => AM(QC, this, Mee).call(this)), super.enablePlugin());
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.hideBorders(), super.disablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`customBorders`](@/api/options.md#customborders)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), this.changeBorderSettings(), super.updatePlugin();
  }
  /**
   * Set custom borders.
   *
   * @example
   * ```js
   * const customBordersPlugin = hot.getPlugin('customBorders');
   *
   * // Using an array of arrays (produced by `.getSelected()` method).
   * customBordersPlugin.setBorders([[1, 1, 2, 2], [6, 2, 0, 2]], {start: {width: 2, color: 'blue'}});
   *
   * // Using an array of CellRange objects (produced by `.getSelectedRange()` method).
   * //  Selecting a cell range.
   * hot.selectCell(0, 0, 2, 2);
   * // Returning selected cells' range with the getSelectedRange method.
   * customBordersPlugin.setBorders(hot.getSelectedRange(), {start: {hide: false, width: 2, color: 'blue'}});
   * ```
   *
   * @param {Array[]|CellRange[]} selectionRanges Array of selection ranges.
   * @param {object} borderObject Object with `top`, `right`, `bottom` and `start` properties.
   */
  setBorders(e, n) {
    let i = ["top", "bottom", "start", "end"], r = null;
    n && (this.checkSettingsCohesion([n]), i = Object.keys(n), r = zC(n));
    const o = Mu(e), s = Qh(o, {
      createCellCoords: this.hot._createCellCoords.bind(this.hot),
      createCellRange: this.hot._createCellRange.bind(this.hot)
    });
    T(e, (a) => {
      s(a).forAll((l, u) => {
        T(i, (c) => {
          this.prepareBorderFromCustomAdded(l, u, r, fee(c));
        });
      });
    }), this.hot.view.render();
  }
  /**
   * Get custom borders.
   *
   * @example
   * ```js
   * const customBordersPlugin = hot.getPlugin('customBorders');
   *
   * // Using an array of arrays (produced by `.getSelected()` method).
   * customBordersPlugin.getBorders([[1, 1, 2, 2], [6, 2, 0, 2]]);
   * // Using an array of CellRange objects (produced by `.getSelectedRange()` method).
   * customBordersPlugin.getBorders(hot.getSelectedRange());
   * // Using without param - return all customBorders.
   * customBordersPlugin.getBorders();
   * ```
   *
   * @param {Array[]|CellRange[]} selectionRanges Array of selection ranges.
   * @returns {object[]} Returns array of border objects.
   */
  getBorders(e) {
    if (!Array.isArray(e))
      return this.savedBorders;
    const n = Mu(e), i = Qh(n, {
      createCellCoords: this.hot._createCellCoords.bind(this.hot),
      createCellRange: this.hot._createCellRange.bind(this.hot)
    }), r = [];
    return T(e, (o) => {
      i(o).forAll((s, a) => {
        T(this.savedBorders, (l) => {
          l.row === s && l.col === a && r.push(Pc(l));
        });
      });
    }), r;
  }
  /**
   * Clear custom borders.
   *
   * @example
   * ```js
   * const customBordersPlugin = hot.getPlugin('customBorders');
   *
   * // Using an array of arrays (produced by `.getSelected()` method).
   * customBordersPlugin.clearBorders([[1, 1, 2, 2], [6, 2, 0, 2]]);
   * // Using an array of CellRange objects (produced by `.getSelectedRange()` method).
   * customBordersPlugin.clearBorders(hot.getSelectedRange());
   * // Using without param - clear all customBorders.
   * customBordersPlugin.clearBorders();
   * ```
   *
   * @param {Array[]|CellRange[]} selectionRanges Array of selection ranges.
   */
  clearBorders(e) {
    e ? this.setBorders(e) : (T(this.savedBorders, (n) => {
      this.clearBordersFromSelectionSettings(n.id), this.clearNullCellRange(), this.hot.removeCellMeta(n.row, n.col, "borders");
    }), this.savedBorders.length = 0);
  }
  /**
   * Insert WalkontableSelection instance into Walkontable settings.
   *
   * @private
   * @param {object} border Object with `row` and `col`, `start`, `end`, `top` and `bottom`, `id` and `border` ({Object} with `color`, `width` and `cornerVisible` property) properties.
   * @param {string} [place] Coordinate where add/remove border - `top`, `bottom`, `start`, `end`.
   */
  insertBorderIntoSettings(e, n) {
    this.checkSavedBorders(e) || this.savedBorders.push(e);
    const r = this.hot._createCellRange(this.hot._createCellCoords(e.row, e.col));
    this.checkCustomSelections(e, r, n) || this.hot.selection.highlight.addCustomSelection({
      border: e,
      visualCellRange: r
    });
  }
  /**
   * Prepare borders from setting (single cell).
   *
   * @private
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {object} borderDescriptor Object with `row` and `col`, `start`, `end`, `top` and `bottom` properties.
   * @param {string} [place] Coordinate where add/remove border - `top`, `bottom`, `start`, `end`.
   */
  prepareBorderFromCustomAdded(e, n, i, r) {
    const o = this.hot.countRows(), s = this.hot.countCols();
    if (e >= o || n >= s)
      return;
    let a = KC(e, n);
    i && (a = uee(a, i), T(this.hot.selection.highlight.customSelections, (l) => {
      if (a.id === l.settings.id)
        return Object.assign(l.settings, i), a.id = l.settings.id, a.top = l.settings.top, a.bottom = l.settings.bottom, a.start = l.settings.start, a.end = l.settings.end, !1;
    })), this.hot.setCellMeta(e, n, "borders", Pc(a)), this.insertBorderIntoSettings(a, r);
  }
  /**
   * Prepare borders from setting (object).
   *
   * @private
   * @param {object} range {CellRange} The CellRange object.
   * @param {object} customBorder Object with `start`, `end`, `top` and `bottom` properties.
   */
  prepareBorderFromCustomAddedRange(e, n) {
    const i = Math.min(e.to.row, this.hot.countRows() - 1), r = Math.min(e.to.col, this.hot.countCols() - 1);
    Ae(e.from.row, i, (o) => {
      Ae(e.from.col, r, (s) => {
        const a = KC(o, s);
        let l = 0;
        o === e.from.row && Bt(n, "top") && (l += 1, a.top = n.top), o === e.to.row && Bt(n, "bottom") && (l += 1, a.bottom = n.bottom), s === e.from.col && Bt(n, "start") && (l += 1, a.start = n.start), s === e.to.col && Bt(n, "end") && (l += 1, a.end = n.end), l > 0 && (this.hot.setCellMeta(o, s, "borders", Pc(a)), this.insertBorderIntoSettings(a));
      });
    });
  }
  /**
   * Remove border (triggered from context menu).
   *
   * @private
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   */
  removeAllBorders(e, n) {
    const i = xH(e, n);
    this.spliceBorder(i), this.clearBordersFromSelectionSettings(i), this.clearNullCellRange(), this.hot.removeCellMeta(e, n, "borders");
  }
  /**
   * Set borders for each cell re. To border position.
   *
   * @private
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {string} place Coordinate where add/remove border - `top`, `bottom`, `start`, `end` and `noBorders`.
   * @param {boolean} remove True when remove borders, and false when add borders.
   */
  setBorder(e, n, i, r) {
    let o = this.hot.getCellMeta(e, n).borders;
    !o || o.border === void 0 ? o = KC(e, n) : o = zC(o), r ? (o[i] = jo(), this.countHide(o) === 4 ? this.removeAllBorders(e, n) : (this.checkCustomSelectionsFromContextMenu(o, i, r) || this.insertBorderIntoSettings(o), this.hot.setCellMeta(e, n, "borders", Pc(o)))) : (o[i] = th(), this.checkCustomSelectionsFromContextMenu(o, i, r) || this.insertBorderIntoSettings(o), this.hot.setCellMeta(e, n, "borders", Pc(o)));
  }
  /**
   * Prepare borders based on cell and border position.
   *
   * @private
   * @param {CellRange[]} selected An array of CellRange objects.
   * @param {string} place Coordinate where add/remove border - `top`, `bottom`, `left`, `right` and `noBorders`.
   * @param {boolean} remove True when remove borders, and false when add borders.
   */
  prepareBorder(e, n, i) {
    T(e, (r) => {
      let {
        start: o,
        end: s
      } = r;
      if (o.row === s.row && o.col === s.col)
        n === "noBorders" ? this.removeAllBorders(o.row, o.col) : this.setBorder(o.row, o.col, n, i);
      else
        switch (n) {
          case "noBorders":
            Ae(o.col, s.col, (a) => {
              Ae(o.row, s.row, (l) => {
                this.removeAllBorders(l, a);
              });
            });
            break;
          case "top":
            Ae(o.col, s.col, (a) => {
              this.setBorder(o.row, a, n, i);
            });
            break;
          case "bottom":
            Ae(o.col, s.col, (a) => {
              this.setBorder(s.row, a, n, i);
            });
            break;
          case "start":
            Ae(o.row, s.row, (a) => {
              this.setBorder(a, o.col, n, i);
            });
            break;
          case "end":
            Ae(o.row, s.row, (a) => {
              this.setBorder(a, s.col, n, i);
            });
            break;
        }
    });
  }
  /**
   * Create borders from settings.
   *
   * @private
   * @param {Array} customBorders Object with `row` and `col`, `start`, `end`, `top` and `bottom` properties.
   */
  createCustomBorders(e) {
    T(e, (n) => {
      const i = zC(n);
      n.range ? this.prepareBorderFromCustomAddedRange(n.range, i) : this.prepareBorderFromCustomAdded(n.row, n.col, i);
    });
  }
  /**
   * Count hide property in border object.
   *
   * @private
   * @param {object} border Object with `row` and `col`, `start`, `end`, `top` and `bottom`, `id` and
   *                        `border` ({Object} with `color`, `width` and `cornerVisible` property) properties.
   * @returns {number}
   */
  countHide(e) {
    const {
      top: n,
      bottom: i,
      start: r,
      end: o
    } = e;
    return rn([n, i, r, o], (a, l) => {
      let u = a;
      return l && l.hide && (u += 1), u;
    }, 0);
  }
  /**
   * Clear borders settings from custom selections.
   *
   * @private
   * @param {string} borderId Border id name as string.
   */
  clearBordersFromSelectionSettings(e) {
    const n = Ke(this.hot.selection.highlight.customSelections, (i) => i.settings.id).indexOf(e);
    n > -1 && this.hot.selection.highlight.customSelections[n].clear();
  }
  /**
   * Clear cellRange with null value.
   *
   * @private
   */
  clearNullCellRange() {
    T(this.hot.selection.highlight.customSelections, (e, n) => {
      if (e.cellRange === null)
        return this.hot.selection.highlight.customSelections[n].destroy(), this.hot.selection.highlight.customSelections.splice(n, 1), !1;
    });
  }
  /**
   * Hide custom borders.
   *
   * @private
   */
  hideBorders() {
    T(this.savedBorders, (e) => {
      this.clearBordersFromSelectionSettings(e.id), this.clearNullCellRange();
    });
  }
  /**
   * Splice border from savedBorders.
   *
   * @private
   * @param {string} borderId Border id name as string.
   */
  spliceBorder(e) {
    const n = Ke(this.savedBorders, (i) => i.id).indexOf(e);
    n > -1 && this.savedBorders.splice(n, 1);
  }
  /**
   * Check if an border already exists in the savedBorders array, and if true update border in savedBorders.
   *
   * @private
   * @param {object} border Object with `row` and `col`, `start`, `end`, `top` and `bottom`, `id` and
   *                        `border` ({Object} with `color`, `width` and `cornerVisible` property) properties.
   *
   * @returns {boolean}
   */
  checkSavedBorders(e) {
    let n = !1;
    return this.countHide(e) === 4 ? (this.spliceBorder(e.id), n = !0) : T(this.savedBorders, (r, o) => {
      if (e.id === r.id)
        return this.savedBorders[o] = e, n = !0, !1;
    }), n;
  }
  /**
   * Check if an border already exists in the customSelections, and if true call toggleHiddenClass method.
   *
   * @private
   * @param {object} border Object with `row` and `col`, `start`, `end`, `top` and `bottom`, `id` and
   *                        `border` ({Object} with `color`, `width` and `cornerVisible` property) properties.
   * @param {string} place Coordinate where add/remove border - `top`, `bottom`, `start`, `end` and `noBorders`.
   * @param {boolean} remove True when remove borders, and false when add borders.
   *
   * @returns {boolean}
   */
  checkCustomSelectionsFromContextMenu(e, n, i) {
    let r = !1;
    return T(this.hot.selection.highlight.customSelections, (o) => {
      if (e.id === o.settings.id) {
        const s = this.hot.view._wt.selectionManager.getBorderInstances(o);
        return T(s, (a) => {
          a.toggleHiddenClass(n, i);
        }), r = !0, !1;
      }
    }), r;
  }
  /**
   * Check if an border already exists in the customSelections, and if true reset cellRange.
   *
   * @private
   * @param {object} border Object with `row` and `col`, `start`, `end`, `top` and `bottom`, `id` and
   *                        `border` ({Object} with `color`, `width` and `cornerVisible` property) properties.
   * @param {CellRange} cellRange The selection range to check.
   * @param {string} [place] Coordinate where add/remove border - `top`, `bottom`, `start`, `end`.
   * @returns {boolean}
   */
  checkCustomSelections(e, n, i) {
    const r = this.countHide(e);
    let o = !1;
    return r === 4 ? (this.removeAllBorders(e.row, e.col), o = !0) : T(this.hot.selection.highlight.customSelections, (s) => {
      if (e.id === s.settings.id) {
        if (s.visualCellRange = n, s.commit(), i) {
          const a = this.hot.view._wt.selectionManager.getBorderInstances(s);
          T(a, (l) => {
            l.changeBorderStyle(i, e);
          });
        }
        return o = !0, !1;
      }
    }), o;
  }
  /**
   * Change borders from settings.
   *
   * @private
   */
  changeBorderSettings() {
    const e = this.hot.getSettings()[Zg];
    if (Array.isArray(e)) {
      const n = Fn(e);
      this.checkSettingsCohesion(n), n.length || (this.savedBorders = n), this.createCustomBorders(n);
    } else
      e !== void 0 && this.createCustomBorders(this.savedBorders);
  }
  /**
   * Checks the settings cohesion. The properties such like "left"/"right" are supported only
   * in the LTR mode and the "left"/"right" options can not be used together with "start"/"end" properties.
   *
   * @private
   * @param {object[]} customBorders The user defined custom border objects array.
   */
  checkSettingsCohesion(e) {
    const n = cee(e), i = hee(e);
    if (n && i)
      throw new Error('The "left"/"right" and "start"/"end" options should not be used together. Please use only the option "start"/"end".');
    if (this.hot.isRtl() && n)
      throw new Error('The "left"/"right" properties are not supported for RTL. Please use option "start"/"end".');
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function Tee(t) {
  this.hot.getSettings()[Zg] && t.items.push({
    name: "---------"
  }, {
    key: "borders",
    name() {
      return this.getTranslatedPhrase(aw);
    },
    disabled() {
      const e = this.getSelectedRangeLast();
      return !e || e.isSingleHeader() ? !0 : this.selection.isSelectedByCorner();
    },
    submenu: {
      items: [Aee(this), wee(this), gee(this), mee(this), pee(this)]
    }
  });
}
function Mee() {
  this.changeBorderSettings();
}
function Iee(t, e) {
  Oee(t, e), e.add(t);
}
function Oee(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function XC(t, e, n) {
  return (e = Pee(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Pee(t) {
  var e = xee(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function xee(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function CM(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const vM = "dragToScroll", Hee = 100;
var qC = /* @__PURE__ */ new WeakSet();
class _ee extends at {
  constructor() {
    super(...arguments), Iee(this, qC), XC(this, "boundaries", null), XC(this, "callback", null), XC(this, "listening", !1);
  }
  static get PLUGIN_KEY() {
    return vM;
  }
  static get PLUGIN_PRIORITY() {
    return Hee;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link DragToScroll#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[vM];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled || (this.addHook("afterOnCellMouseDown", (e) => CM(qC, this, yM).call(this, e)), this.addHook("afterOnCellCornerMouseDown", (e) => CM(qC, this, yM).call(this, e)), this.registerEvents(), super.enablePlugin());
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`dragToScroll`](@/api/options.md#dragtoscroll)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.unregisterEvents(), super.disablePlugin();
  }
  /**
   * Sets the boundaries/dimensions of the scrollable viewport.
   *
   * @param {DOMRect|{left: number, right: number, top: number, bottom: number}} [boundaries] An object with
   * coordinates. Contains the window boundaries by default. The object is compatible with DOMRect.
   */
  setBoundaries() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {
      left: 0,
      right: this.hot.rootWindow.innerWidth,
      top: 0,
      bottom: this.hot.rootWindow.innerHeight
    };
    this.boundaries = e;
  }
  /**
   * Changes callback function.
   *
   * @param {Function} callback The callback function.
   */
  setCallback(e) {
    this.callback = e;
  }
  /**
   * Checks if the mouse position (X, Y) is outside the viewport and fires a callback with calculated X an Y diffs
   * between passed boundaries.
   *
   * @param {number} x Mouse X coordinate to check.
   * @param {number} y Mouse Y coordinate to check.
   */
  check(e, n) {
    let i = 0, r = 0;
    n < this.boundaries.top ? r = n - this.boundaries.top : n > this.boundaries.bottom && (r = n - this.boundaries.bottom), e < this.boundaries.left ? i = e - this.boundaries.left : e > this.boundaries.right && (i = e - this.boundaries.right), this.callback(i, r);
  }
  /**
   * Enables listening on `mousemove` event.
   *
   * @private
   */
  listen() {
    this.listening = !0;
  }
  /**
   * Disables listening on `mousemove` event.
   *
   * @private
   */
  unlisten() {
    this.listening = !1;
  }
  /**
   * Returns current state of listening.
   *
   * @private
   * @returns {boolean}
   */
  isListening() {
    return this.listening;
  }
  /**
   * Registers dom listeners.
   *
   * @private
   */
  registerEvents() {
    const {
      rootWindow: e
    } = this.hot;
    let n = e;
    for (; n; )
      this.eventManager.addEventListener(n.document, "contextmenu", () => this.unlisten()), this.eventManager.addEventListener(n.document, "mouseup", () => this.unlisten()), this.eventManager.addEventListener(n.document, "mousemove", (i) => this.onMouseMove(i)), n = Ja(n);
  }
  /**
   * Unbinds the events used by the plugin.
   *
   * @private
   */
  unregisterEvents() {
    this.eventManager.clear();
  }
  /**
   * 'mouseMove' event callback.
   *
   * @private
   * @param {MouseEvent} event `mousemove` event properties.
   */
  onMouseMove(e) {
    this.isListening() && this.check(e.clientX, e.clientY);
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function yM(t) {
  if (zs(t))
    return;
  const e = this.hot.view._wt.wtOverlays.topOverlay.mainTableScrollableElement;
  this.setBoundaries(e !== this.hot.rootWindow ? e.getBoundingClientRect() : void 0), this.setCallback((n, i) => {
    var r, o;
    const s = (r = e.scrollLeft) !== null && r !== void 0 ? r : e.scrollX, a = (o = e.scrollTop) !== null && o !== void 0 ? o : e.scrollY;
    e.scroll(s + Math.sign(n) * 50, a + Math.sign(i) * 20);
  }), this.listen();
}
function Nee(t, e) {
  HH(t, e), e.add(t);
}
function Dee(t, e, n) {
  HH(t, e), e.set(t, n);
}
function HH(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ZC(t, e, n) {
  return (e = kee(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function kee(t) {
  var e = Lee(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Lee(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Bee(t, e) {
  return t.get(Fi(t, e));
}
function _H(t, e, n) {
  return t.set(Fi(t, e), n), n;
}
function Fi(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("afterDropdownMenuDefaultOptions");
he.getSingleton().register("beforeDropdownMenuShow");
he.getSingleton().register("afterDropdownMenuShow");
he.getSingleton().register("afterDropdownMenuHide");
he.getSingleton().register("afterDropdownMenuExecute");
const Jg = "dropdownMenu", Fee = 230, Zh = "changeType", JC = Jg;
var lA = /* @__PURE__ */ new WeakMap(), fr = /* @__PURE__ */ new WeakSet();
class uA extends at {
  static get PLUGIN_KEY() {
    return Jg;
  }
  static get PLUGIN_PRIORITY() {
    return Fee;
  }
  static get PLUGIN_DEPS() {
    return ["plugin:AutoColumnSize"];
  }
  /**
   * Default menu items order when `dropdownMenu` is enabled by setting the config item to `true`.
   *
   * @returns {Array}
   */
  static get DEFAULT_ITEMS() {
    return [xd, Hd, $e, Nd, $e, Jw, $e, _d, $e, Bi];
  }
  /**
   * Instance of {@link CommandExecutor}.
   *
   * @private
   * @type {CommandExecutor}
   */
  constructor(e) {
    super(e), Nee(this, fr), ZC(this, "commandExecutor", new vH(this.hot)), ZC(this, "itemsFactory", null), ZC(this, "menu", null), Dee(this, lA, !1), this.hot.addHook("afterGetColHeader", (n, i) => Fi(fr, this, Wee).call(this, n, i));
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link DropdownMenu#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return this.hot.getSettings()[Jg];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   *
   * @fires Hooks#afterDropdownMenuDefaultOptions
   * @fires Hooks#beforeDropdownMenuSetItems
   */
  enablePlugin() {
    var e = this;
    if (this.enabled)
      return;
    this.itemsFactory = new yH(this.hot, uA.DEFAULT_ITEMS), this.addHook("beforeOnCellMouseDown", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return Fi(fr, e, zee).call(e, ...o);
    }), this.addHook("beforeViewportScrollHorizontally", function() {
      for (var r = arguments.length, o = new Array(r), s = 0; s < r; s++)
        o[s] = arguments[s];
      return Fi(fr, e, Gee).call(e, ...o);
    });
    const n = this.hot.getSettings()[Jg], i = {
      items: this.itemsFactory.getItems(n)
    };
    this.registerEvents(), typeof n.callback == "function" && this.commandExecutor.setCommonCallback(n.callback), this.registerShortcuts(), super.enablePlugin(), this.callOnPluginsReady(() => {
      this.hot.runHooks("afterDropdownMenuDefaultOptions", i), this.itemsFactory.setPredefinedItems(i.items);
      const r = this.itemsFactory.getItems(n);
      this.menu && this.menu.destroy(), this.menu = new Wu(this.hot, {
        className: "htDropdownMenu",
        keepInViewport: !0,
        container: n.uiContainer || this.hot.rootDocument.body
      }), this.hot.runHooks("beforeDropdownMenuSetItems", r), this.menu.setMenuItems(r), this.menu.addLocalHook("beforeOpen", () => Fi(fr, this, $ee).call(this)), this.menu.addLocalHook("afterOpen", () => Fi(fr, this, Uee).call(this)), this.menu.addLocalHook("afterSubmenuOpen", (o) => Fi(fr, this, Yee).call(this, o)), this.menu.addLocalHook("afterClose", () => Fi(fr, this, jee).call(this)), this.menu.addLocalHook("executeCommand", function() {
        for (var o = arguments.length, s = new Array(o), a = 0; a < o; a++)
          s[a] = arguments[a];
        return e.executeCommand.call(e, ...s);
      }), T(r, (o) => this.commandExecutor.registerCommand(o.key, o));
    });
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`dropdownMenu`](@/api/options.md#dropdownmenu)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.close(), this.menu && this.menu.destroy(), this.unregisterShortcuts(), super.disablePlugin();
  }
  /**
   * Register shortcuts responsible for toggling dropdown menu.
   *
   * @private
   */
  registerShortcuts() {
    const e = this.hot.getShortcutManager().getContext("grid"), n = () => {
      const {
        highlight: i
      } = this.hot.getSelectedRangeLast();
      if ((i.isHeader() && i.row === -1 || i.isCell()) && i.col >= 0) {
        this.hot.selectColumns(i.col, i.col, -1);
        const {
          from: r
        } = this.hot.getSelectedRangeLast(), o = Zw(this.menu.container, this.hot.rootDocument), s = this.hot.getCell(-1, r.col, !0).querySelector(`.${Zh}`), a = s.getBoundingClientRect();
        this.open({
          left: a.left + o.left,
          top: a.top + s.offsetHeight + o.top
        }, {
          left: a.width,
          right: 0,
          above: 0,
          below: 3
        }), this.menu.getNavigator().toFirstItem();
      }
    };
    e.addShortcuts([{
      keys: [["Shift", "Alt", "ArrowDown"], ["Control/Meta", "Enter"]],
      callback: n,
      runOnlyIf: () => {
        var i;
        const r = (i = this.hot.getSelectedRangeLast()) === null || i === void 0 ? void 0 : i.highlight;
        return r && this.hot.selection.isCellVisible(r) && r.isHeader() && !this.menu.isOpened();
      },
      captureCtrl: !0,
      group: JC
    }, {
      keys: [["Shift", "Alt", "ArrowDown"]],
      callback: n,
      runOnlyIf: () => {
        var i;
        const r = (i = this.hot.getSelectedRangeLast()) === null || i === void 0 ? void 0 : i.highlight;
        return r && this.hot.selection.isCellVisible(r) && r.isCell() && !this.menu.isOpened();
      },
      group: JC
    }]);
  }
  /**
   * Unregister shortcuts responsible for toggling dropdown menu.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("grid").removeShortcutsByGroup(JC);
  }
  /**
   * Registers the DOM listeners.
   *
   * @private
   */
  registerEvents() {
    this.eventManager.addEventListener(this.hot.rootElement, "click", (e) => Fi(fr, this, Vee).call(this, e));
  }
  /**
   * Opens menu and re-position it based on the passed coordinates.
   *
   * @param {{ top: number, left: number }|Event} position An object with `top` and `left` properties
   * which contains coordinates relative to the browsers viewport (without included scroll offsets).
   * Or if the native event is passed the menu will be positioned based on the `pageX` and `pageY`
   * coordinates.
   * @param {{ above: number, below: number, left: number, right: number }} offset An object allows applying
   * the offset to the menu position.
   * @fires Hooks#beforeDropdownMenuShow
   * @fires Hooks#afterDropdownMenuShow
   */
  open(e) {
    var n;
    let i = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
      above: 0,
      below: 0,
      left: 0,
      right: 0
    };
    (n = this.menu) !== null && n !== void 0 && n.isOpened() || (this.menu.open(), Fe(i, (r, o) => {
      this.menu.setOffset(o, r);
    }), this.menu.setPosition(e));
  }
  /**
   * Closes dropdown menu.
   */
  close() {
    var e;
    (e = this.menu) === null || e === void 0 || e.close();
  }
  /**
   * Executes context menu command.
   *
   * The `executeCommand()` method works only for selected cells.
   *
   * When no cells are selected, `executeCommand()` doesn't do anything.
   *
   * You can execute all predefined commands:
   *  * `'col_left'` - Insert column left
   *  * `'col_right'` - Insert column right
   *  * `'clear_column'` - Clear selected column
   *  * `'remove_col'` - Remove column
   *  * `'undo'` - Undo last action
   *  * `'redo'` - Redo last action
   *  * `'make_read_only'` - Make cell read only
   *  * `'alignment:left'` - Alignment to the left
   *  * `'alignment:top'` - Alignment to the top
   *  * `'alignment:right'` - Alignment to the right
   *  * `'alignment:bottom'` - Alignment to the bottom
   *  * `'alignment:middle'` - Alignment to the middle
   *  * `'alignment:center'` - Alignment to the center (justify).
   *
   * Or you can execute command registered in settings where `key` is your command name.
   *
   * @param {string} commandName Command name to execute.
   * @param {*} params Additional parameters passed to the command executor.
   */
  executeCommand(e) {
    for (var n = arguments.length, i = new Array(n > 1 ? n - 1 : 0), r = 1; r < n; r++)
      i[r - 1] = arguments[r];
    this.commandExecutor.execute(e, ...i);
  }
  /**
   * Turns on / off listening on dropdown menu.
   *
   * @private
   * @param {boolean} listen Turn on listening when value is set to true, otherwise turn it off.
   */
  setListening() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
    this.menu.isOpened() && (e ? this.menu.hotMenu.listen() : this.menu.hotMenu.unlisten());
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    this.close(), this.menu && this.menu.destroy(), super.destroy();
  }
}
function NH(t) {
  t.getKeyboardShortcutsCtrl().addCustomShortcuts([{
    keys: [["Control/Meta", "A"]],
    callback: () => !1
  }]);
}
function Vee(t) {
  if (Te(t.target, Zh)) {
    const e = Zw(this.menu.container, this.hot.rootDocument), n = t.target.getBoundingClientRect();
    t.stopPropagation(), _H(lA, this, !1), this.open({
      left: n.left + e.left,
      top: n.top + t.target.offsetHeight + e.top
    }, {
      left: n.width,
      right: 0,
      above: 0,
      below: 3
    });
  }
}
function Wee(t, e) {
  const n = e.parentNode;
  if (!n)
    return;
  const i = n.parentNode.childNodes, r = Array.prototype.indexOf.call(i, n);
  if (t < 0 || r !== i.length - 1)
    return;
  const o = e.querySelector(`.${Zh}`);
  if (this.enabled && o)
    return;
  if (!this.enabled) {
    o && o.parentNode.removeChild(o);
    return;
  }
  const s = this.hot.rootDocument.createElement("button");
  s.className = Zh, s.type = "button", s.tabIndex = -1, this.hot.getSettings().ariaTags && (Ee(s, [gl(), wm(" ")]), Ee(e, [xP("menu")])), s.onclick = function() {
    return !1;
  }, e.firstChild.insertBefore(s, e.firstChild.firstChild);
}
function $ee() {
  this.hot.runHooks("beforeDropdownMenuShow", this);
}
function Uee() {
  this.hot.runHooks("afterDropdownMenuShow", this), Fi(fr, this, NH).call(this, this.menu);
}
function Yee(t) {
  Fi(fr, this, NH).call(this, t);
}
function jee() {
  this.hot.listen(), this.hot.runHooks("afterDropdownMenuHide", this);
}
function Gee(t) {
  return Bee(lA, this) ? null : t;
}
function zee(t) {
  Te(t.target, Zh) && _H(lA, this, !0);
}
uA.SEPARATOR = {
  name: $e
};
function SM(t, e, n) {
  return (e = Kee(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Kee(t) {
  var e = Qee(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Qee(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Xee {
  constructor(e) {
    SM(this, "hot", void 0), SM(this, "options", {}), this.hot = e;
  }
  /**
   * Set options for data provider.
   *
   * @param {object} options Object with specified options.
   */
  setOptions(e) {
    this.options = e;
  }
  /**
   * Get table data based on provided settings to the class constructor.
   *
   * @returns {Array}
   */
  getData() {
    const {
      startRow: e,
      startCol: n,
      endRow: i,
      endCol: r
    } = this._getDataRange(), o = this.options, s = [];
    return Ae(e, i, (a) => {
      const l = [];
      !o.exportHiddenRows && this._isHiddenRow(a) || (Ae(n, r, (u) => {
        !o.exportHiddenColumns && this._isHiddenColumn(u) || l.push(this.hot.getDataAtCell(a, u));
      }), s.push(l));
    }), s;
  }
  /**
   * Gets list of row headers.
   *
   * @returns {Array}
   */
  getRowHeaders() {
    const e = [];
    if (this.options.rowHeaders) {
      const {
        startRow: n,
        endRow: i
      } = this._getDataRange(), r = this.hot.getRowHeader();
      Ae(n, i, (o) => {
        !this.options.exportHiddenRows && this._isHiddenRow(o) || e.push(r[o]);
      });
    }
    return e;
  }
  /**
   * Gets list of columns headers.
   *
   * @returns {Array}
   */
  getColumnHeaders() {
    const e = [];
    if (this.options.columnHeaders) {
      const {
        startCol: n,
        endCol: i
      } = this._getDataRange(), r = this.hot.getColHeader();
      Ae(n, i, (o) => {
        !this.options.exportHiddenColumns && this._isHiddenColumn(o) || e.push(r[o]);
      });
    }
    return e;
  }
  /**
   * Get data range object based on settings provided in the class constructor.
   *
   * @private
   * @returns {object} Returns object with keys `startRow`, `startCol`, `endRow` and `endCol`.
   */
  _getDataRange() {
    const e = this.hot.countCols() - 1, n = this.hot.countRows() - 1;
    let [i = 0, r = 0, o = n, s = e] = this.options.range;
    return i = Math.max(i, 0), r = Math.max(r, 0), o = Math.min(o, n), s = Math.min(s, e), {
      startRow: i,
      startCol: r,
      endRow: o,
      endCol: s
    };
  }
  /**
   * Check if row at specified row index is hidden.
   *
   * @private
   * @param {number} row Row index.
   * @returns {boolean}
   */
  _isHiddenRow(e) {
    return this.hot.rowIndexMapper.isHidden(this.hot.toPhysicalRow(e));
  }
  /**
   * Check if column at specified column index is hidden.
   *
   * @private
   * @param {number} column Visual column index.
   * @returns {boolean}
   */
  _isHiddenColumn(e) {
    return this.hot.columnIndexMapper.isHidden(this.hot.toPhysicalColumn(e));
  }
}
const qee = Xee;
function bM(t, e, n) {
  return (e = Zee(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Zee(t) {
  var e = Jee(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Jee(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class IR {
  /**
   * Default options.
   *
   * @returns {object}
   */
  static get DEFAULT_OPTIONS() {
    return {
      mimeType: "text/plain",
      fileExtension: "txt",
      filename: "Handsontable [YYYY]-[MM]-[DD]",
      encoding: "utf-8",
      bom: !1,
      columnHeaders: !1,
      rowHeaders: !1,
      exportHiddenColumns: !1,
      exportHiddenRows: !1,
      range: []
    };
  }
  /**
   * Data provider.
   *
   * @type {DataProvider}
   */
  constructor(e, n) {
    bM(this, "dataProvider", void 0), bM(this, "options", void 0), this.dataProvider = e, this.options = this._mergeOptions(n), this.dataProvider.setOptions(this.options);
  }
  /**
   * Merge options provided by users with defaults.
   *
   * @param {object} options An object with options to merge with.
   * @returns {object} Returns new options object.
   */
  _mergeOptions(e) {
    let n = Rr(this.constructor.DEFAULT_OPTIONS);
    const i = /* @__PURE__ */ new Date();
    return n = nn(Rr(IR.DEFAULT_OPTIONS), n), n = nn(n, e), n.filename = qS(n.filename, {
      YYYY: i.getFullYear(),
      MM: `${i.getMonth() + 1}`.padStart(2, "0"),
      DD: `${i.getDate()}`.padStart(2, "0")
    }), n;
  }
}
const ete = IR, tte = String.fromCharCode(13), nte = String.fromCharCode(34), ite = String.fromCharCode(10);
class rte extends ete {
  /**
   * Default options for exporting CSV format.
   *
   * @returns {object}
   */
  static get DEFAULT_OPTIONS() {
    return {
      mimeType: "text/csv",
      fileExtension: "csv",
      bom: !0,
      columnDelimiter: ",",
      rowDelimiter: `\r
`
    };
  }
  /**
   * Create string body in desired format.
   *
   * @returns {string}
   */
  export() {
    const e = this.options, n = this.dataProvider.getData();
    let i = this.dataProvider.getColumnHeaders();
    const r = i.length > 0, o = this.dataProvider.getRowHeaders(), s = o.length > 0;
    let a = e.bom ? String.fromCharCode(65279) : "";
    return r && (i = Ke(i, (l) => this._escapeCell(l, !0)), s && (a += e.columnDelimiter), a += i.join(e.columnDelimiter), a += e.rowDelimiter), T(n, (l, u) => {
      u > 0 && (a += e.rowDelimiter), s && (a += this._escapeCell(o[u]) + e.columnDelimiter), a += l.map((c) => this._escapeCell(c)).join(e.columnDelimiter);
    }), a;
  }
  /**
   * Escape cell value.
   *
   * @param {*} value Cell value.
   * @param {boolean} [force=false] Indicates if cell value will be escaped forcefully.
   * @returns {string}
   */
  _escapeCell(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1, i = Gt(e);
    return i !== "" && (n || i.indexOf(tte) >= 0 || i.indexOf(nte) >= 0 || i.indexOf(ite) >= 0 || i.indexOf(this.options.columnDelimiter) >= 0) && (i = i.replace(new RegExp('"', "g"), '""'), i = `"${i}"`), i;
  }
}
const ote = rte, ste = "csv", Qy = {
  [ste]: ote
};
function ate(t, e, n) {
  return typeof Qy[t] == "function" ? new Qy[t](e, n) : null;
}
const lte = "exportFile", ute = 240;
class cte extends at {
  static get PLUGIN_KEY() {
    return lte;
  }
  static get PLUGIN_PRIORITY() {
    return ute;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ExportFile#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !0;
  }
  /**
   * @typedef ExportOptions
   * @memberof ExportFile
   * @type {object}
   * @property {boolean} [exportHiddenRows=false] Include hidden rows in the exported file.
   * @property {boolean} [exportHiddenColumns=false] Include hidden columns in the exported file.
   * @property {boolean} [columnHeaders=false] Include column headers in the exported file.
   * @property {boolean} [rowHeaders=false] Include row headers in the exported file.
   * @property {string} [columnDelimiter=','] Column delimiter.
   * @property {string} [range=[]] Cell range that will be exported to file.
   */
  /**
   * Exports table data as a string.
   *
   * @param {string} format Export format type eq. `'csv'`.
   * @param {ExportOptions} options Export options.
   * @returns {string}
   */
  exportAsString(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    return this._createTypeFormatter(e, n).export();
  }
  /**
   * Exports table data as a blob object.
   *
   * @param {string} format Export format type eq. `'csv'`.
   * @param {ExportOptions} options Export options.
   * @returns {Blob}
   */
  exportAsBlob(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    return this._createBlob(this._createTypeFormatter(e, n));
  }
  /**
   * Exports table data as a downloadable file.
   *
   * @param {string} format Export format type eq. `'csv'`.
   * @param {ExportOptions} options Export options.
   */
  downloadFile(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    const {
      rootDocument: i,
      rootWindow: r
    } = this.hot, o = this._createTypeFormatter(e, n), s = this._createBlob(o), a = r.URL || r.webkitURL, l = i.createElement("a"), u = `${o.options.filename}.${o.options.fileExtension}`;
    if (l.download !== void 0) {
      const c = a.createObjectURL(s);
      l.style.display = "none", l.setAttribute("href", c), l.setAttribute("download", u), i.body.appendChild(l), l.dispatchEvent(new MouseEvent("click")), i.body.removeChild(l), setTimeout(() => {
        a.revokeObjectURL(c);
      }, 100);
    } else
      navigator.msSaveOrOpenBlob && navigator.msSaveOrOpenBlob(s, u);
  }
  /**
   * Creates and returns class formatter for specified export type.
   *
   * @private
   * @param {string} format Export format type eq. `'csv'`.
   * @param {ExportOptions} options Export options.
   * @returns {BaseType}
   */
  _createTypeFormatter(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    if (!Qy[e])
      throw new Error(`Export format type "${e}" is not supported.`);
    return ate(e, new qee(this.hot), n);
  }
  /**
   * Creates blob object based on provided type formatter class.
   *
   * @private
   * @param {BaseType} typeFormatter The instance of the specyfic formatter/exporter.
   * @returns {Blob}
   */
  _createBlob(e) {
    let n = null;
    return typeof Blob != "undefined" && (n = new Blob([e.export()], {
      type: `${e.options.mimeType};charset=${e.options.encoding}`
    })), n;
  }
}
function Gl(t, e, n) {
  return (e = hte(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function hte(t) {
  var e = dte(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function dte(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Dd {
  constructor(e, n) {
    let {
      id: i,
      stateless: r = !0
    } = n;
    Gl(this, "hot", void 0), Gl(this, "id", void 0), Gl(this, "elements", []), Gl(this, "hidden", !1), Gl(this, "stateId", ""), Gl(this, "state", void 0), this.hot = e, this.id = i, this.stateId = `Filters.component.${this.id}`, this.state = r ? null : this.hot.columnIndexMapper.registerMap(this.stateId, new Lp());
  }
  /**
   * Gets the list of elements from which the component is built.
   *
   * @returns {BaseUI[]}
   */
  getElements() {
    return this.elements;
  }
  /**
   * Reset elements to its initial state.
   */
  reset() {
    T(this.elements, (e) => e.reset());
  }
  /**
   * Hide component.
   */
  hide() {
    this.hidden = !0;
  }
  /**
   * Show component.
   */
  show() {
    this.hidden = !1;
  }
  /**
   * Check if component is hidden.
   *
   * @returns {boolean}
   */
  isHidden() {
    return this.hot === null || this.hidden;
  }
  /**
   * Restores the component state from the given physical column index. The method
   * internally calls the `setState` method. The state then is individually processed
   * by each component.
   *
   * @param {number} physicalColumn The physical column index.
   */
  restoreState(e) {
    this.state && this.setState(this.state.getValueAtIndex(e));
  }
  /**
   * The custom logic for component state restoring.
   */
  setState() {
    throw new Error("The state setting logic is not implemented");
  }
  /**
   * Saves the component state to the given physical column index. The method
   * internally calls the `getState` method, which returns the current state of
   * the component.
   *
   * @param {number} physicalColumn The physical column index.
   */
  saveState(e) {
    this.state && this.state.setValueAtIndex(e, this.getState());
  }
  /**
   * The custom logic for component state gathering (for stateful components).
   */
  getState() {
    throw new Error("The state gathering logic is not implemented");
  }
  /**
   * Destroy element.
   */
  destroy() {
    this.hot.columnIndexMapper.unregisterMap(this.stateId), this.clearLocalHooks(), T(this.elements, (e) => e.destroy()), this.state = null, this.elements = null, this.hot = null;
  }
}
pt(Dd, Vn);
const Jh = {};
function Ks(t, e) {
  if (!Jh[t])
    throw Error(`Filter condition "${t}" does not exist.`);
  const {
    condition: n,
    descriptor: i
  } = Jh[t];
  let r = e;
  return i.inputValuesDecorator && (r = i.inputValuesDecorator(r)), function(o) {
    return n.apply(o.meta.instance, [].concat([o], [r]));
  };
}
function Ka(t) {
  if (!Jh[t])
    throw Error(`Filter condition "${t}" does not exist.`);
  return Jh[t].descriptor;
}
function on(t, e, n) {
  n.key = t, Jh[t] = {
    condition: e,
    descriptor: n
  };
}
const yn = "none";
function fte() {
  return !0;
}
on(yn, fte, {
  name: Md,
  inputsCount: 0,
  showOperators: !1
});
const mh = "empty";
function gte(t) {
  return zt(t.value);
}
on(mh, gte, {
  name: Cw,
  inputsCount: 0,
  showOperators: !0
});
const em = "not_empty";
function mte(t, e) {
  return !Ks(mh, e)(t);
}
on(em, mte, {
  name: vw,
  inputsCount: 0,
  showOperators: !0
});
const ph = "eq";
function pte(t, e) {
  let [n] = e;
  return Gt(t.value).toLocaleLowerCase(t.meta.locale) === Gt(n);
}
on(ph, pte, {
  name: yw,
  inputsCount: 1,
  showOperators: !0
});
const tm = "neq";
function wte(t, e) {
  return !Ks(ph, e)(t);
}
on(tm, wte, {
  name: Sw,
  inputsCount: 1,
  showOperators: !0
});
const DH = "gt";
function Ate(t, e) {
  let [n] = e, i = n;
  return t.meta.type === "numeric" && (i = parseFloat(i, 10)), t.value > i;
}
on(DH, Ate, {
  name: Mw,
  inputsCount: 1,
  showOperators: !0
});
const kH = "gte";
function Cte(t, e) {
  let [n] = e, i = n;
  return t.meta.type === "numeric" && (i = parseFloat(i, 10)), t.value >= i;
}
on(kH, Cte, {
  name: Iw,
  inputsCount: 1,
  showOperators: !0
});
const LH = "lt";
function vte(t, e) {
  let [n] = e, i = n;
  return t.meta.type === "numeric" && (i = parseFloat(i, 10)), t.value < i;
}
on(LH, vte, {
  name: Ow,
  inputsCount: 1,
  showOperators: !0
});
const BH = "lte";
function yte(t, e) {
  let [n] = e, i = n;
  return t.meta.type === "numeric" && (i = parseFloat(i, 10)), t.value <= i;
}
on(BH, yte, {
  name: Pw,
  inputsCount: 1,
  showOperators: !0
});
const OR = "date_after";
function Ste(t, e) {
  let [n] = e;
  const i = X(t.value, t.meta.dateFormat), r = X(n, t.meta.dateFormat);
  return !i.isValid() || !r.isValid() ? !1 : i.diff(r) >= 0;
}
on(OR, Ste, {
  name: _w,
  inputsCount: 1,
  showOperators: !0
});
const PR = "date_before";
function bte(t, e) {
  let [n] = e;
  const i = X(t.value, t.meta.dateFormat), r = X(n, t.meta.dateFormat);
  return !i.isValid() || !r.isValid() ? !1 : i.diff(r) <= 0;
}
on(PR, bte, {
  name: Nw,
  inputsCount: 1,
  showOperators: !0
});
const Dm = "between";
function Rte(t, e) {
  let [n, i] = e, r = n, o = i;
  if (t.meta.type === "numeric") {
    const s = parseFloat(r, 10), a = parseFloat(o, 10);
    r = Math.min(s, a), o = Math.max(s, a);
  } else if (t.meta.type === "date") {
    const s = Ks(PR, [o]), a = Ks(OR, [r]);
    return s(t) && a(t);
  }
  return t.value >= r && t.value <= o;
}
on(Dm, Rte, {
  name: xw,
  inputsCount: 2,
  showOperators: !0
});
const FH = "not_between";
function Ete(t, e) {
  return !Ks(Dm, e)(t);
}
on(FH, Ete, {
  name: Hw,
  inputsCount: 2,
  showOperators: !0
});
const VH = "begins_with";
function Tte(t, e) {
  let [n] = e;
  return Gt(t.value).toLocaleLowerCase(t.meta.locale).startsWith(Gt(n));
}
on(VH, Tte, {
  name: bw,
  inputsCount: 1,
  showOperators: !0
});
const WH = "ends_with";
function Mte(t, e) {
  let [n] = e;
  return Gt(t.value).toLocaleLowerCase(t.meta.locale).endsWith(Gt(n));
}
on(WH, Mte, {
  name: Rw,
  inputsCount: 1,
  showOperators: !0
});
const xR = "contains";
function Ite(t, e) {
  let [n] = e;
  return Gt(t.value).toLocaleLowerCase(t.meta.locale).indexOf(Gt(n)) >= 0;
}
on(xR, Ite, {
  name: Ew,
  inputsCount: 1,
  showOperators: !0
});
const $H = "not_contains";
function Ote(t, e) {
  return !Ks(xR, e)(t);
}
on($H, Ote, {
  name: Tw,
  inputsCount: 1,
  showOperators: !0
});
const UH = "date_tomorrow";
function Pte(t) {
  const e = X(t.value, t.meta.dateFormat);
  return e.isValid() ? e.isSame(X().subtract(-1, "days").startOf("day"), "d") : !1;
}
on(UH, Pte, {
  name: kw,
  inputsCount: 0
});
const YH = "date_today";
function xte(t) {
  const e = X(t.value, t.meta.dateFormat);
  return e.isValid() ? e.isSame(X().startOf("day"), "d") : !1;
}
on(YH, xte, {
  name: Dw,
  inputsCount: 0
});
const jH = "date_yesterday";
function Hte(t) {
  const e = X(t.value, t.meta.dateFormat);
  return e.isValid() ? e.isSame(X().subtract(1, "days").startOf("day"), "d") : !1;
}
on(jH, Hte, {
  name: Lw,
  inputsCount: 0
});
ix();
function _te(t, e) {
  let n = t;
  return n === "" && (n = `(${e})`), n;
}
const Xy = (/* @__PURE__ */ new Set([1])).has(1), Nte = Xy && typeof Array.from == "function";
function kd(t) {
  let e = t;
  return Xy && (e = new Set(e)), function(n) {
    let i;
    return Xy ? i = e.has(n) : i = !!~e.indexOf(n), i;
  };
}
function GH(t) {
  return t == null ? "" : t;
}
function qy(t) {
  let e = t;
  return Nte ? e = Array.from(new Set(e)) : e = KS(e), e = e.sort((n, i) => typeof n == "number" && typeof i == "number" ? n - i : n === i ? 0 : n > i ? 1 : -1), e;
}
function RM(t, e, n, i) {
  const r = [], o = t === e;
  let s;
  return o || (s = kd(e)), T(t, (a) => {
    let l = !1;
    (o || s(a)) && (l = !0);
    const u = {
      checked: l,
      value: a,
      visualValue: _te(a, n)
    };
    i && i(u), r.push(u);
  }), r;
}
const Qa = "by_value";
function Dte(t, e) {
  let [n] = e;
  return n(t.value);
}
on(Qa, Dte, {
  name: "By value",
  inputsCount: 0,
  inputValuesDecorator(t) {
    let [e] = t;
    return [kd(e)];
  },
  showOperators: !1
});
const ed = {};
function kte(t) {
  if (!ed[t])
    throw Error(`Operation with id "${t}" does not exist.`);
  const e = ed[t].func;
  return function(n, i) {
    return e(n, i);
  };
}
function Lte(t) {
  return ed[t].name;
}
function HR(t, e, n) {
  ed[t] = {
    name: e,
    func: n
  };
}
const Us = "conjunction", Bte = Vw;
function Fte(t, e) {
  return t.every((n) => n.func(e));
}
HR(Us, Bte, Fte);
const km = "disjunction", Vte = Id;
function Wte(t, e) {
  return t.some((n) => n.func(e));
}
HR(km, Vte, Wte);
const _R = "disjunctionWithExtraCondition", $te = Id;
function Ute(t, e) {
  if (t.length < 3)
    throw Error("Operation doesn't work on less then three conditions.");
  return t.slice(0, t.length - 1).some((n) => n.func(e)) && t[t.length - 1].func(e);
}
HR(_R, $te, Ute);
const Yte = "numeric", zH = "text", jte = "date", EM = {
  [Yte]: [yn, $e, mh, em, $e, ph, tm, $e, DH, kH, LH, BH, Dm, FH],
  [zH]: [yn, $e, mh, em, $e, ph, tm, $e, VH, WH, $e, xR, $H],
  [jte]: [yn, $e, mh, em, $e, ph, tm, $e, PR, OR, Dm, $e, UH, YH, jH]
};
function Gte(t) {
  const e = [];
  let n = t;
  return EM[n] || (n = zH), T(EM[n], (i) => {
    let r;
    i === $e ? r = {
      name: $e
    } : r = Rr(Ka(i)), e.push(r);
  }), e;
}
function xc(t, e, n) {
  return (e = zte(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function zte(t) {
  var e = Kte(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Kte(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const ev = "built", tv = "building", TM = ["click", "input", "keydown", "keypress", "keyup", "focus", "blur", "change"];
let vr = class KH {
  static get DEFAULTS() {
    return Rr({
      className: "",
      value: "",
      tagName: "div",
      children: [],
      wrapIt: !0
    });
  }
  /**
   * Instance of Handsontable.
   *
   * @type {Core}
   */
  constructor(e, n) {
    xc(this, "hot", void 0), xc(this, "eventManager", new Qi(this)), xc(this, "options", void 0), xc(this, "_element", void 0), xc(this, "buildState", void 0), this.hot = e, this.options = nn(KH.DEFAULTS, n), this._element = this.hot.rootDocument.createElement(this.options.wrapIt ? "div" : this.options.tagName);
  }
  /**
   * Set the element value.
   *
   * @param {*} value Set the component value.
   */
  setValue(e) {
    this.options.value = e, this.update();
  }
  /**
   * Get the element value.
   *
   * @returns {*}
   */
  getValue() {
    return this.options.value;
  }
  /**
   * Get element as a DOM object.
   *
   * @returns {Element}
   */
  get element() {
    return this.buildState === tv ? this._element : this.buildState === ev ? (this.update(), this._element) : (this.buildState = tv, this.build(), this.buildState = ev, this._element);
  }
  /**
   * Check if element was built (built whole DOM structure).
   *
   * @returns {boolean}
   */
  isBuilt() {
    return this.buildState === ev;
  }
  /**
   * Translate value if it is possible. It's checked if value belongs to namespace of translated phrases.
   *
   * @param {*} value Value which will may be translated.
   * @returns {*} Translated value if translation was possible, original value otherwise.
   */
  translateIfPossible(e) {
    return typeof e == "string" && e.startsWith(qn) ? this.hot.getTranslatedPhrase(e) : e;
  }
  /**
   * Build DOM structure.
   */
  build() {
    const e = (n, i) => {
      this.eventManager.addEventListener(n, i, (r) => this.runLocalHooks(i, r, this));
    };
    if (this.buildState || (this.buildState = tv), this._element.setAttribute("data-hot-input", !0), this.options.tabIndex !== void 0 && this._element.setAttribute("tabindex", this.options.tabIndex), this.options.role !== void 0 && this._element.setAttribute("role", this.options.role), this.options.className && Q(this._element, this.options.className), this.options.children.length)
      T(this.options.children, (n) => this._element.appendChild(n.element));
    else if (this.options.wrapIt) {
      const n = this.hot.rootDocument.createElement(this.options.tagName);
      n.setAttribute("data-hot-input", !0), Fe(this.options, (i, r) => {
        n[r] !== void 0 && r !== "className" && r !== "tagName" && r !== "children" && (n[r] = this.translateIfPossible(i));
      }), this._element.appendChild(n), T(TM, (i) => e(n, i));
    } else
      T(TM, (n) => e(this._element, n));
  }
  /**
   * Update DOM structure.
   */
  update() {
  }
  /**
   * Reset to initial state.
   */
  reset() {
    this.options.value = "", this.update();
  }
  /**
   * Show element.
   */
  show() {
    this.element.style.display = "";
  }
  /**
   * Hide element.
   */
  hide() {
    this.element.style.display = "none";
  }
  /**
   * Focus element.
   */
  focus() {
  }
  destroy() {
    this.eventManager.destroy(), this.eventManager = null, this.hot = null, this._element.parentNode && this._element.parentNode.removeChild(this._element), this._element = null;
  }
};
pt(vr, Vn);
function Qte(t, e) {
  QH(t, e), e.add(t);
}
function Xte(t, e, n) {
  QH(t, e), e.set(t, n);
}
function QH(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function tg(t, e) {
  return t.get(NR(t, e));
}
function qte(t, e, n) {
  return t.set(NR(t, e), n), n;
}
function NR(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var zl = /* @__PURE__ */ new WeakMap(), MM = /* @__PURE__ */ new WeakSet();
class Ys extends vr {
  static get DEFAULTS() {
    return Rr({
      placeholder: "",
      type: "text",
      tagName: "input",
      tabIndex: -1
    });
  }
  /**
   * The reference to the input element.
   *
   * @type {HTMLInputElement}
   */
  constructor(e, n) {
    super(e, nn(Ys.DEFAULTS, n)), Qte(this, MM), Xte(this, zl, void 0), this.registerHooks();
  }
  /**
   * Register all necessary hooks.
   */
  registerHooks() {
    this.addLocalHook("keyup", (e) => NR(MM, this, Zte).call(this, e));
  }
  /**
   * Build DOM structure.
   */
  build() {
    super.build();
    const e = this.hot.rootDocument.createElement("div");
    qte(zl, this, this._element.firstChild), Q(this._element, "htUIInput"), Q(e, "htUIInputIcon"), this._element.appendChild(e), this.update();
  }
  /**
   * Update element.
   */
  update() {
    this.isBuilt() && (tg(zl, this).type = this.options.type, tg(zl, this).placeholder = this.translateIfPossible(this.options.placeholder), tg(zl, this).value = this.translateIfPossible(this.options.value));
  }
  /**
   * Focus element.
   */
  focus() {
    this.isBuilt() && tg(zl, this).focus();
  }
}
function Zte(t) {
  this.options.value = t.target.value;
}
function Jte(t, e) {
  XH(t, e), e.add(t);
}
function Hc(t, e, n) {
  XH(t, e), e.set(t, n);
}
function XH(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Kl(t, e, n) {
  return t.set(wh(t, e), n), n;
}
function Vt(t, e) {
  return t.get(wh(t, e));
}
function wh(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var hn = /* @__PURE__ */ new WeakMap(), ng = /* @__PURE__ */ new WeakMap(), ig = /* @__PURE__ */ new WeakMap(), nv = /* @__PURE__ */ new WeakMap(), rg = /* @__PURE__ */ new WeakMap(), og = /* @__PURE__ */ new WeakSet();
class td extends vr {
  static get DEFAULTS() {
    return Rr({
      className: "htUISelect",
      wrapIt: !1,
      tabIndex: -1
    });
  }
  /**
   * Instance of {@link Menu}.
   *
   * @type {Menu}
   */
  constructor(e, n) {
    super(e, nn(td.DEFAULTS, n)), Jte(this, og), Hc(this, hn, null), Hc(this, ng, []), Hc(this, ig, void 0), Hc(this, nv, void 0), Hc(this, rg, void 0), this.registerHooks();
  }
  /**
   * Gets the instance of the Menu.
   *
   * @returns {Menu}
   */
  getMenu() {
    return Vt(hn, this);
  }
  /**
   * Register all necessary hooks.
   */
  registerHooks() {
    this.addLocalHook("click", () => wh(og, this, nne).call(this));
  }
  /**
   * Set options which can be selected in the list.
   *
   * @param {Array} items Array of objects with required keys `key` and `name`.
   */
  setItems(e) {
    Kl(ng, this, this.translateNames(e)), Vt(hn, this) && Vt(hn, this).setMenuItems(Vt(ng, this));
  }
  /**
   * Translate names of menu items.
   *
   * @param {Array} items Array of objects with required keys `key` and `name`.
   * @returns {Array} Items with translated `name` keys.
   */
  translateNames(e) {
    return T(e, (n) => {
      n.name = this.translateIfPossible(n.name);
    }), e;
  }
  /**
   * Build DOM structure.
   */
  build() {
    super.build(), Kl(hn, this, new Wu(this.hot, {
      className: "htSelectUI htFiltersConditionsMenu",
      keepInViewport: !1,
      standalone: !0,
      container: this.options.menuContainer
    })), Vt(hn, this).setMenuItems(Vt(ng, this));
    const e = new vr(this.hot, {
      className: "htUISelectCaption"
    }), n = new vr(this.hot, {
      className: "htUISelectDropdown"
    });
    Kl(ig, this, e), Kl(nv, this, e.element), Kl(rg, this, n), this.hot.getSettings().ariaTags && (Ee(n.element, [gl()]), Ee(this._element, [PP()])), T([e, n], (i) => this._element.appendChild(i.element)), Vt(hn, this).addLocalHook("select", (i) => wh(og, this, ene).call(this, i)), Vt(hn, this).addLocalHook("afterClose", () => wh(og, this, tne).call(this)), this.update();
  }
  /**
   * Update DOM structure.
   */
  update() {
    if (!this.isBuilt())
      return;
    let e;
    this.options.value ? e = this.options.value.name : e = Vt(hn, this).hot.getTranslatedPhrase(Md), Vt(nv, this).textContent = e, super.update();
  }
  /**
   * Open select dropdown menu with available options.
   */
  openOptions() {
    const e = this.element.getBoundingClientRect();
    Vt(hn, this) && (Vt(hn, this).open(), Vt(hn, this).setPosition({
      left: this.hot.isLtr() ? e.left - 5 : e.left - 31,
      top: e.top - 1,
      width: e.width,
      height: e.height
    }), Vt(hn, this).getNavigator().toFirstItem(), Vt(hn, this).getKeyboardShortcutsCtrl().addCustomShortcuts([{
      keys: [["Tab"], ["Shift", "Tab"]],
      callback: (n) => {
        this.closeOptions(), this.runLocalHooks("tabKeydown", n);
      }
    }, {
      keys: [["Control/Meta", "A"]],
      callback: () => !1
    }]));
  }
  /**
   * Close select dropdown menu.
   */
  closeOptions() {
    Vt(hn, this) && Vt(hn, this).close();
  }
  /**
   * Focus element.
   */
  focus() {
    this.isBuilt() && this.element.focus();
  }
  /**
   * Destroy instance.
   */
  destroy() {
    Vt(hn, this) && (Vt(hn, this).destroy(), Kl(hn, this, null)), Vt(ig, this) && Vt(ig, this).destroy(), Vt(rg, this) && Vt(rg, this).destroy(), super.destroy();
  }
}
function ene(t) {
  t.name !== $e && (this.options.value = t, this.update(), this.runLocalHooks("select", this.options.value));
}
function tne() {
  this.runLocalHooks("afterClose");
}
function nne() {
  this.openOptions();
}
function ine(t, e) {
  rne(t, e), e.add(t);
}
function rne(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function IM(t, e, n) {
  return (e = one(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function one(t) {
  var e = sne(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function sne(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function OM(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var iv = /* @__PURE__ */ new WeakSet();
class Zy extends Dd {
  constructor(e, n) {
    super(e, {
      id: n.id,
      stateless: !1
    }), ine(this, iv), IM(this, "name", ""), IM(this, "addSeparator", !1), this.name = n.name, this.addSeparator = n.addSeparator, this.elements.push(new td(this.hot, {
      menuContainer: n.menuContainer
    })), this.elements.push(new Ys(this.hot, {
      placeholder: Gw
    })), this.elements.push(new Ys(this.hot, {
      placeholder: zw
    })), this.registerHooks();
  }
  /**
   * Register all necessary hooks.
   *
   * @private
   */
  registerHooks() {
    this.getSelectElement().addLocalHook("select", (e) => OM(iv, this, ane).call(this, e)).addLocalHook("afterClose", () => this.runLocalHooks("afterClose")).addLocalHook("tabKeydown", (e) => this.runLocalHooks("selectTabKeydown", e)), T(this.getInputElements(), (e) => {
      e.addLocalHook("keydown", (n) => OM(iv, this, lne).call(this, n));
    });
  }
  /**
   * Set state of the component.
   *
   * @param {object} value State to restore.
   */
  setState(e) {
    if (this.reset(), !e)
      return;
    const n = Rr(e.command);
    n.name.startsWith(Kt) && (n.name = this.hot.getTranslatedPhrase(n.name)), this.getSelectElement().setValue(n), T(e.args, (i, r) => {
      if (r > n.inputsCount - 1)
        return !1;
      const o = this.getInputElement(r);
      o.setValue(i), o[n.inputsCount > r ? "show" : "hide"](), r || this.hot._registerTimeout(() => o.focus(), 10);
    });
  }
  /**
   * Export state of the component (get selected filter and filter arguments).
   *
   * @returns {object} Returns object where `command` key keeps used condition filter and `args` key its arguments.
   */
  getState() {
    const e = this.getSelectElement().getValue() || Ka(yn), n = [];
    return T(this.getInputElements(), (i, r) => {
      e.inputsCount > r && n.push(i.getValue());
    }), {
      command: e,
      args: n
    };
  }
  /**
   * Update state of component.
   *
   * @param {object} condition The condition object.
   * @param {object} condition.command The command object with condition name as `key` property.
   * @param {Array} condition.args An array of values to compare.
   * @param {number} column Physical column index.
   */
  updateState(e, n) {
    const i = Ka(e ? e.name : yn);
    this.state.setValueAtIndex(n, {
      command: i,
      args: e ? e.args : []
    }), e || T(this.getInputElements(), (r) => r.setValue(null));
  }
  /**
   * Get select element.
   *
   * @returns {SelectUI}
   */
  getSelectElement() {
    return this.elements.filter((e) => e instanceof td)[0];
  }
  /**
   * Get input element.
   *
   * @param {number} index Index an array of elements.
   * @returns {InputUI}
   */
  getInputElement() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0;
    return this.getInputElements()[e];
  }
  /**
   * Get input elements.
   *
   * @returns {Array}
   */
  getInputElements() {
    return this.elements.filter((e) => e instanceof Ys);
  }
  /**
   * Get menu object descriptor.
   *
   * @returns {object}
   */
  getMenuItemDescriptor() {
    return {
      key: this.id,
      name: this.name,
      isCommand: !1,
      disableSelection: !0,
      hidden: () => this.isHidden(),
      renderer: (e, n, i, r, o, s) => {
        Q(n.parentNode, "htFiltersMenuCondition"), this.addSeparator && Q(n.parentNode, "border");
        const a = this.hot.rootDocument.createElement("div");
        return Q(a, "htFiltersMenuLabel"), a.textContent = s, n.appendChild(a), n.parentElement.hasAttribute("ghost-table") || T(this.elements, (l) => n.appendChild(l.element)), n;
      }
    };
  }
  /**
   * Reset elements to their initial state.
   */
  reset() {
    const e = this.hot.getPlugin("filters").getSelectedColumn();
    let n = [Ka(yn)];
    if (e !== null) {
      const {
        visualIndex: i
      } = e;
      n = Gte(this.hot.getDataType(0, i, this.hot.countRows(), i));
    }
    T(this.getInputElements(), (i) => i.hide()), this.getSelectElement().setItems(n), super.reset(), this.getSelectElement().setValue(n[0]);
  }
}
function ane(t) {
  T(this.getInputElements(), (e, n) => {
    e[t.inputsCount > n ? "show" : "hide"](), n === 0 && this.hot._registerTimeout(() => e.focus(), 10);
  }), this.runLocalHooks("change", t);
}
function lne(t) {
  Lu(t.keyCode, "ESCAPE") && (this.runLocalHooks("cancel"), Zi(t));
}
function PM(t, e, n) {
  une(t, e), e.set(t, n);
}
function une(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function _c(t, e) {
  return t.get(qH(t, e));
}
function xM(t, e, n) {
  return t.set(qH(t, e), n), n;
}
function qH(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Ql = /* @__PURE__ */ new WeakMap(), rv = /* @__PURE__ */ new WeakMap();
class Lm extends vr {
  static get DEFAULTS() {
    return Rr({
      type: "radio",
      tagName: "input",
      className: "htUIRadio",
      label: {}
    });
  }
  /**
   * The reference to the input element.
   *
   * @type {HTMLInputElement}
   */
  constructor(e, n) {
    super(e, nn(Lm.DEFAULTS, n)), PM(this, Ql, void 0), PM(this, rv, void 0);
  }
  /**
   * Build DOM structure.
   */
  build() {
    super.build();
    const e = this.hot.rootDocument.createElement("label");
    e.textContent = this.translateIfPossible(this.options.label.textContent), e.htmlFor = this.translateIfPossible(this.options.label.htmlFor), xM(rv, this, e), xM(Ql, this, this._element.firstChild), _c(Ql, this).checked = this.options.checked, this._element.appendChild(e), this.update();
  }
  /**
   * Update element.
   */
  update() {
    this.isBuilt() && (_c(rv, this).textContent = this.translateIfPossible(this.options.label.textContent));
  }
  /**
   * Check if radio button is checked.
   *
   * @returns {boolean}
   */
  isChecked() {
    return this.isBuilt() ? _c(Ql, this).checked : !1;
  }
  /**
   * Set input checked attribute.
   *
   * @param {boolean} value Set the component state.
   */
  setChecked() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
    this.isBuilt() && (_c(Ql, this).checked = e);
  }
  /**
   * Focus element.
   */
  focus() {
    this.isBuilt() && _c(Ql, this).focus();
  }
}
function cne(t, e) {
  hne(t, e), e.add(t);
}
function hne(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function dne(t, e, n) {
  return (e = fne(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function fne(t) {
  var e = gne(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function gne(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function mne(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const HM = 0;
var _M = /* @__PURE__ */ new WeakSet();
class pne extends Dd {
  constructor(e, n) {
    super(e, {
      id: n.id,
      stateless: !1
    }), cne(this, _M), dne(this, "name", ""), this.name = n.name, this.buildOperatorsElement();
  }
  /**
   * Get menu object descriptor.
   *
   * @returns {object}
   */
  getMenuItemDescriptor() {
    return {
      key: this.id,
      name: this.name,
      isCommand: !1,
      disableSelection: !0,
      hidden: () => this.isHidden(),
      renderer: (e, n) => (Q(n.parentNode, "htFiltersMenuOperators"), T(this.elements, (i) => n.appendChild(i.element)), n)
    };
  }
  /**
   * Add RadioInputUI elements to component.
   *
   * @private
   */
  buildOperatorsElement() {
    const e = [Us, km];
    T(e, (n) => {
      const i = new Lm(this.hot, {
        name: "operator",
        label: {
          htmlFor: n,
          textContent: Lte(n)
        },
        value: n,
        checked: n === e[HM],
        id: n
      });
      i.addLocalHook("change", (r) => mne(_M, this, wne).call(this, r)), this.elements.push(i);
    });
  }
  /**
   * Set state of operators component to check radio input at specific `index`.
   *
   * @param {number} searchedIndex Index of radio input to check.
   */
  setChecked(e) {
    if (this.elements.length < e)
      throw Error(mt`Radio button with index ${e} doesn't exist.`);
    T(this.elements, (n, i) => {
      n.setChecked(i === e);
    });
  }
  /**
   * Get `id` of active operator.
   *
   * @returns {string}
   */
  getActiveOperationId() {
    const e = this.elements.find((n) => n instanceof Lm && n.isChecked());
    return e ? e.getValue() : Us;
  }
  /**
   * Export state of the component (get selected operator).
   *
   * @returns {string} Returns `id` of selected operator.
   */
  getState() {
    return this.getActiveOperationId();
  }
  /**
   * Set state of the component.
   *
   * @param {object} value State to restore.
   */
  setState(e) {
    this.reset(), e && this.getActiveOperationId() !== e && T(this.elements, (n) => {
      n.setChecked(n.getValue() === e);
    });
  }
  /**
   * Update state of component.
   *
   * @param {string} [operationId='conjunction'] Id of selected operation.
   * @param {number} column Physical column index.
   */
  updateState() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : Us, n = arguments.length > 1 ? arguments[1] : void 0, i = e;
    i === _R && (i = km), this.state.setValueAtIndex(n, i);
  }
  /**
   * Reset elements to their initial state.
   */
  reset() {
    this.setChecked(HM);
  }
}
function wne(t) {
  this.setState(t.target.value);
}
function Ane(t, e, n) {
  Cne(t, e), e.set(t, n);
}
function Cne(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ov(t, e) {
  return t.get(ZH(t, e));
}
function vne(t, e, n) {
  return t.set(ZH(t, e), n), n;
}
function ZH(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Nc = /* @__PURE__ */ new WeakMap();
class nd extends vr {
  static get DEFAULTS() {
    return Rr({
      href: "#",
      tagName: "a",
      tabIndex: -1,
      role: "button"
    });
  }
  /**
   * The reference to the link element.
   *
   * @type {HTMLLinkElement}
   */
  constructor(e, n) {
    super(e, nn(nd.DEFAULTS, n)), Ane(this, Nc, void 0);
  }
  /**
   * Build DOM structure.
   */
  build() {
    super.build(), vne(Nc, this, this._element.firstChild);
  }
  /**
   * Update element.
   */
  update() {
    this.isBuilt() && (ov(Nc, this).textContent = this.translateIfPossible(this.options.textContent));
  }
  /**
   * Focus element.
   */
  focus() {
    this.isBuilt() && ov(Nc, this).focus();
  }
  /**
   * Activate the element.
   */
  activate() {
    ov(Nc, this).click();
  }
}
function yne(t, e) {
  JH(t, e), e.add(t);
}
function Xl(t, e, n) {
  JH(t, e), e.set(t, n);
}
function JH(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ke(t, e) {
  return t.get(gu(t, e));
}
function ar(t, e, n) {
  return t.set(gu(t, e), n), n;
}
function gu(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const NM = "multipleSelect.itemBox";
var gr = /* @__PURE__ */ new WeakMap(), en = /* @__PURE__ */ new WeakMap(), sv = /* @__PURE__ */ new WeakMap(), Oo = /* @__PURE__ */ new WeakMap(), gs = /* @__PURE__ */ new WeakMap(), ms = /* @__PURE__ */ new WeakMap(), Dc = /* @__PURE__ */ new WeakSet();
class id extends vr {
  static get DEFAULTS() {
    return Rr({
      className: "htUIMultipleSelect",
      value: []
    });
  }
  /**
   * List of available select options.
   *
   * @type {Array}
   */
  constructor(e, n) {
    super(e, nn(id.DEFAULTS, n)), yne(this, Dc), Xl(this, gr, []), Xl(this, en, void 0), Xl(this, sv, void 0), Xl(this, Oo, void 0), Xl(this, gs, void 0), Xl(this, ms, void 0), ar(Oo, this, new Ys(this.hot, {
      placeholder: jw,
      className: "htUIMultipleSelectSearch"
    })), ar(gs, this, new nd(this.hot, {
      textContent: Ww,
      className: "htUISelectAll"
    })), ar(ms, this, new nd(this.hot, {
      textContent: $w,
      className: "htUIClearAll"
    })), this.registerHooks();
  }
  /**
   * Gets the instance of the internal Handsontable that acts here as a listbox component.
   *
   * @returns {Handsontable}
   */
  getItemsBox() {
    return ke(en, this);
  }
  /**
   * Register all necessary hooks.
   */
  registerHooks() {
    ke(Oo, this).addLocalHook("keydown", (e) => gu(Dc, this, bne).call(this, e)), ke(Oo, this).addLocalHook("input", (e) => gu(Dc, this, Sne).call(this, e)), ke(gs, this).addLocalHook("click", (e) => gu(Dc, this, Rne).call(this, e)), ke(ms, this).addLocalHook("click", (e) => gu(Dc, this, Ene).call(this, e));
  }
  /**
   * Set available options.
   *
   * @param {Array} items Array of objects with `checked` and `label` property.
   */
  setItems(e) {
    var n;
    ar(gr, this, e), (n = ke(en, this)) === null || n === void 0 || n.loadData(ke(gr, this));
  }
  /**
   * Set a locale for the component.
   *
   * @param {string} locale Locale used for filter actions performed on data, ie. `en-US`.
   */
  setLocale(e) {
    ar(sv, this, e);
  }
  /**
   * Get a locale for the component.
   *
   * @returns {string}
   */
  getLocale() {
    return ke(sv, this);
  }
  /**
   * Get all available options.
   *
   * @returns {Array}
   */
  getItems() {
    return [...ke(gr, this)];
  }
  /**
   * Get element value.
   *
   * @returns {Array} Array of selected values.
   */
  getValue() {
    return Mne(ke(gr, this));
  }
  /**
   * Gets the instance of the search input element.
   *
   * @returns {InputUI}
   */
  getSearchInputElement() {
    return ke(Oo, this);
  }
  /**
   * Gets the instance of the "select all" link element.
   *
   * @returns {LinkUI}
   */
  getSelectAllElement() {
    return ke(gs, this);
  }
  /**
   * Gets the instance of the "clear" link element.
   *
   * @returns {LinkUI}
   */
  getClearAllElement() {
    return ke(ms, this);
  }
  /**
   * Check if all values listed in element are selected.
   *
   * @returns {boolean}
   */
  isSelectedAllValues() {
    return ke(gr, this).length === this.getValue().length;
  }
  /**
   * Build DOM structure.
   */
  build() {
    super.build();
    const {
      rootDocument: e
    } = this.hot, n = e.createElement("div"), i = new vr(this.hot, {
      className: "htUISelectionControls",
      children: [ke(gs, this), ke(ms, this)]
    });
    this._element.appendChild(ke(Oo, this).element), this._element.appendChild(i.element), this._element.appendChild(n);
    const r = (o) => {
      var s;
      if (!this._element)
        return;
      (s = ke(en, this)) === null || s === void 0 || s.destroy(), Q(o, "htUIMultipleSelectHot"), ar(en, this, new this.hot.constructor(o, {
        data: ke(gr, this),
        columns: [{
          data: "checked",
          type: "checkbox",
          label: {
            property: "visualValue",
            position: "after"
          }
        }],
        beforeRenderer: (u, c, h, d, g, m) => {
          u.title = m.instance.getDataAtRowProp(c, m.label.property);
        },
        afterListen: () => {
          this.runLocalHooks("focus", this);
        },
        beforeOnCellMouseUp: () => {
          ke(en, this).listen();
        },
        colWidths: () => ke(en, this).container.scrollWidth - pn(e),
        maxCols: 1,
        autoWrapCol: !0,
        height: 110,
        copyPaste: !1,
        disableVisualSelection: "area",
        fillHandle: !1,
        fragmentSelection: "cell",
        tabMoves: {
          row: 1,
          col: 0
        },
        layoutDirection: this.hot.isRtl() ? "rtl" : "ltr"
      })), ke(en, this).init();
      const l = ke(en, this).getShortcutManager().getContext("grid");
      l.removeShortcutsByKeys(["Tab"]), l.removeShortcutsByKeys(["Shift", "Tab"]), l.addShortcut({
        keys: [["Escape"]],
        callback: (u) => {
          this.runLocalHooks("keydown", u, this);
        },
        group: NM
      }), l.addShortcut({
        keys: [["Tab"], ["Shift", "Tab"]],
        callback: (u) => {
          ke(en, this).deselectCell(), this.runLocalHooks("keydown", u, this), this.runLocalHooks("listTabKeydown", u, this);
        },
        group: NM
      });
    };
    r(n), this.hot._registerTimeout(() => r(n), 100);
  }
  /**
   * Focus element.
   */
  focus() {
    this.isBuilt() && ke(en, this).listen();
  }
  /**
   * Reset DOM structure.
   */
  reset() {
    ke(Oo, this).reset(), ke(gs, this).reset(), ke(ms, this).reset();
  }
  /**
   * Update DOM structure.
   */
  update() {
    this.isBuilt() && (ke(en, this).loadData(Tne(ke(gr, this), this.options.value)), super.update());
  }
  /**
   * Destroy instance.
   */
  destroy() {
    var e;
    (e = ke(en, this)) === null || e === void 0 || e.destroy(), ke(Oo, this).destroy(), ke(ms, this).destroy(), ke(gs, this).destroy(), ar(Oo, this, null), ar(ms, this, null), ar(gs, this, null), ar(en, this, null), ar(gr, this, null), super.destroy();
  }
}
function Sne(t) {
  const e = t.target.value.toLocaleLowerCase(this.getLocale());
  let n;
  e === "" ? n = [...ke(gr, this)] : n = ke(gr, this).filter((i) => `${i.value}`.toLocaleLowerCase(this.getLocale()).indexOf(e) >= 0), ke(en, this).loadData(n);
}
function bne(t) {
  this.runLocalHooks("keydown", t, this), lb(Lu, t.keyCode)("ARROW_DOWN") && (t.preventDefault(), Zi(t), ke(en, this).listen(), ke(en, this).selectCell(0, 0));
}
function Rne(t) {
  const e = [];
  t.preventDefault(), ke(en, this).getSourceData().forEach((n, i) => {
    n.checked = !0, e.push($b(n, i)[0]);
  }), ke(en, this).setSourceDataAtCell(e);
}
function Ene(t) {
  const e = [];
  t.preventDefault(), ke(en, this).getSourceData().forEach((n, i) => {
    n.checked = !1, e.push($b(n, i)[0]);
  }), ke(en, this).setSourceDataAtCell(e);
}
function Tne(t, e) {
  const n = kd(e);
  return t.map((i) => (i.checked = n(i.value), i));
}
function Mne(t) {
  const e = [];
  return t.forEach((n) => {
    n.checked && e.push(n.value);
  }), e;
}
function Ine(t, e) {
  One(t, e), e.add(t);
}
function One(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Pne(t, e, n) {
  return (e = xne(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function xne(t) {
  var e = Hne(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Hne(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function sg(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var kc = /* @__PURE__ */ new WeakSet();
class _ne extends Dd {
  constructor(e, n) {
    super(e, {
      id: n.id,
      stateless: !1
    }), Ine(this, kc), Pne(this, "name", ""), this.name = n.name, this.elements.push(new id(this.hot)), this.registerHooks();
  }
  /**
   * Register all necessary hooks.
   *
   * @private
   */
  registerHooks() {
    this.getMultipleSelectElement().addLocalHook("keydown", (e) => sg(kc, this, Nne).call(this, e)).addLocalHook("listTabKeydown", (e) => this.runLocalHooks("listTabKeydown", e)), this.hot.addHook("modifyFiltersMultiSelectValue", (e, n) => sg(kc, this, Dne).call(this, e, n));
  }
  /**
   * Gets the list of elements from which the component is built.
   *
   * @returns {BaseUI[]}
   */
  getElements() {
    const e = this.getMultipleSelectElement();
    return [e.getSearchInputElement(), e.getSelectAllElement(), e.getClearAllElement(), this.getMultipleSelectElement()];
  }
  /**
   * Set state of the component.
   *
   * @param {object} value The component value.
   */
  setState(e) {
    if (this.reset(), e && e.command.key === Qa) {
      const n = this.getMultipleSelectElement();
      n.setItems(e.itemsSnapshot), n.setValue(e.args[0]), n.setLocale(e.locale);
    }
  }
  /**
   * Export state of the component (get selected filter and filter arguments).
   *
   * @returns {object} Returns object where `command` key keeps used condition filter and `args` key its arguments.
   */
  getState() {
    const e = this.getMultipleSelectElement(), n = e.getItems();
    return {
      command: {
        key: e.isSelectedAllValues() || !n.length ? yn : Qa
      },
      args: [e.getValue()],
      itemsSnapshot: n
    };
  }
  /**
   * Update state of component.
   *
   * @param {object} stateInfo Information about state containing stack of edited column,
   * stack of dependent conditions, data factory and optional condition arguments change. It's described by object containing keys:
   * `editedConditionStack`, `dependentConditionStacks`, `visibleDataFactory` and `conditionArgsChange`.
   */
  updateState(e) {
    const n = (i, r, o, s, a) => {
      const [l] = Zo(r, (h) => h.name === Qa), u = {}, c = this.hot.getTranslatedPhrase(Kh);
      if (l) {
        const h = s(i, a), d = Ke(h, (b) => b.value), g = new Map(h.map((b) => [b.value, this.hot.getCellMeta(b.meta.visualRow, b.meta.visualCol)])), m = qy(d);
        o && (l.args[0] = o);
        const w = [], C = RM(m, l.args[0], c, (b) => {
          b.checked && w.push(b.value), sg(kc, this, DM).call(this, b, g);
        }), S = e.editedConditionStack.column;
        u.locale = this.hot.getCellMeta(0, S).locale, u.args = [w], u.command = Ka(Qa), u.itemsSnapshot = C;
      } else
        u.args = [], u.command = Ka(yn);
      this.state.setValueAtIndex(i, u);
    };
    n(e.editedConditionStack.column, e.editedConditionStack.conditions, e.conditionArgsChange, e.filteredRowsFactory), e.dependentConditionStacks.length && n(e.dependentConditionStacks[0].column, e.dependentConditionStacks[0].conditions, e.conditionArgsChange, e.filteredRowsFactory, e.editedConditionStack);
  }
  /**
   * Get multiple select element.
   *
   * @returns {MultipleSelectUI}
   */
  getMultipleSelectElement() {
    return this.elements.filter((e) => e instanceof id)[0];
  }
  /**
   * Get object descriptor for menu item entry.
   *
   * @returns {object}
   */
  getMenuItemDescriptor() {
    return {
      key: this.id,
      name: this.name,
      isCommand: !1,
      disableSelection: !0,
      hidden: () => this.isHidden(),
      renderer: (e, n, i, r, o, s) => {
        Q(n.parentNode, "htFiltersMenuValue");
        const a = this.hot.rootDocument.createElement("div");
        return Q(a, "htFiltersMenuLabel"), a.textContent = s, n.appendChild(a), n.parentElement.hasAttribute("ghost-table") || T(this.elements, (l) => n.appendChild(l.element)), n;
      }
    };
  }
  /**
   * Reset elements to their initial state.
   */
  reset() {
    const e = this.hot.getTranslatedPhrase(Kh), n = this._getColumnVisibleValues(), i = n.map((l) => l.value), r = new Map(n.map((l) => [l.value, l.meta])), o = qy(i), s = RM(o, o, e, (l) => {
      sg(kc, this, DM).call(this, l, r);
    });
    this.getMultipleSelectElement().setItems(s), super.reset(), this.getMultipleSelectElement().setValue(o);
    const a = this.hot.getPlugin("filters").getSelectedColumn();
    a !== null && this.getMultipleSelectElement().setLocale(this.hot.getCellMeta(0, a.visualIndex).locale);
  }
  /**
   * Get data for currently selected column.
   *
   * @returns {Array}
   * @private
   */
  _getColumnVisibleValues() {
    const e = this.hot.getPlugin("filters").getSelectedColumn();
    return e === null ? [] : Ke(this.hot.getDataAtCol(e.visualIndex), (n, i) => ({
      value: GH(n),
      meta: this.hot.getCellMeta(i, e.visualIndex)
    }));
  }
}
function Nne(t) {
  Lu(t.keyCode, "ESCAPE") && (this.runLocalHooks("cancel"), Zi(t));
}
function DM(t, e) {
  this.hot.hasHook("modifyFiltersMultiSelectValue") && (t.visualValue = this.hot.runHooks("modifyFiltersMultiSelectValue", t.visualValue, e.get(t.value)));
}
function Dne(t, e) {
  switch (e.type) {
    case "numeric":
      return j1(t, e);
    default:
      return t;
  }
}
var e_;
function kne(t, e) {
  Lne(t, e), e.add(t);
}
function Lne(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Bne(t, e, n) {
  return (e = Fne(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Fne(t) {
  var e = Vne(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Vne(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Wne(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var kM = /* @__PURE__ */ new WeakSet();
class rd extends Dd {
  static get BUTTON_OK() {
    return "ok";
  }
  static get BUTTON_CANCEL() {
    return "cancel";
  }
  constructor(e, n) {
    super(e, {
      id: n.id,
      stateless: !0
    }), kne(this, kM), Bne(this, "name", ""), this.name = n.name, this.elements.push(new Ys(this.hot, {
      type: "button",
      value: Uw,
      className: "htUIButton htUIButtonOK",
      identifier: rd.BUTTON_OK
    })), this.elements.push(new Ys(this.hot, {
      type: "button",
      value: Yw,
      className: "htUIButton htUIButtonCancel",
      identifier: rd.BUTTON_CANCEL
    })), this.registerHooks();
  }
  /**
   * Register all necessary hooks.
   *
   * @private
   */
  registerHooks() {
    T(this.elements, (e) => {
      e.addLocalHook("click", (n, i) => Wne(kM, this, $ne).call(this, n, i));
    });
  }
  /**
   * Get menu object descriptor.
   *
   * @returns {object}
   */
  getMenuItemDescriptor() {
    return {
      key: this.id,
      name: this.name,
      isCommand: !1,
      disableSelection: !0,
      hidden: () => this.isHidden(),
      renderer: (e, n) => (Q(n.parentNode, "htFiltersMenuActionBar"), T(this.elements, (i) => n.appendChild(i.element)), n)
    };
  }
  /**
   * Fire accept event.
   */
  accept() {
    this.runLocalHooks("accept");
  }
  /**
   * Fire cancel event.
   */
  cancel() {
    this.runLocalHooks("cancel");
  }
}
e_ = rd;
function $ne(t, e) {
  e.options.identifier === e_.BUTTON_OK ? this.accept() : this.cancel();
}
function ag(t, e, n) {
  return (e = Une(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Une(t) {
  var e = Yne(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Yne(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const LM = "ConditionCollection.filteringStates";
class t_ {
  constructor(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
    ag(this, "hot", void 0), ag(this, "isMapRegistrable", void 0), ag(this, "filteringStates", new Lp()), ag(this, "previousConditionStack", null), this.hot = e, this.isMapRegistrable = n, this.isMapRegistrable === !0 ? this.hot.columnIndexMapper.registerMap(LM, this.filteringStates) : this.filteringStates.init(this.hot.columnIndexMapper.getNumberOfIndexes());
  }
  /**
   * Check if condition collection is empty (so no needed to filter data).
   *
   * @returns {boolean}
   */
  isEmpty() {
    return this.getFilteredColumns().length === 0;
  }
  /**
   * Check if value is matched to the criteria of conditions chain.
   *
   * @param {object} value Object with `value` and `meta` keys.
   * @param {number} column The physical column index.
   * @returns {boolean}
   */
  isMatch(e, n) {
    var i;
    const r = this.filteringStates.getValueAtIndex(n), o = (i = r == null ? void 0 : r.conditions) !== null && i !== void 0 ? i : [], s = r == null ? void 0 : r.operation;
    return this.isMatchInConditions(o, e, s);
  }
  /**
   * Check if the value is matches the conditions.
   *
   * @param {Array} conditions List of conditions.
   * @param {object} value Object with `value` and `meta` keys.
   * @param {string} [operationType='conjunction'] Type of conditions operation.
   * @returns {boolean}
   */
  isMatchInConditions(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : Us;
    return e.length ? kte(i)(e, n) : !0;
  }
  /**
   * Add condition to the collection.
   *
   * @param {number} column The physical column index.
   * @param {object} conditionDefinition Object with keys:
   *  * `command` Object, Command object with condition name as `key` property.
   *  * `args` Array, Condition arguments.
   * @param {string} [operation='conjunction'] Type of conditions operation.
   * @param {number} [position] Position to which condition will be added. When argument is undefined
   * the condition will be processed as the last condition.
   * @fires ConditionCollection#beforeAdd
   * @fires ConditionCollection#afterAdd
   */
  addCondition(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : Us, r = arguments.length > 3 ? arguments[3] : void 0;
    const o = this.hot.getCellMeta(0, e).locale, s = Ke(n.args, (c) => typeof c == "string" ? c.toLocaleLowerCase(o) : c), a = n.name || n.command.key;
    this.previousConditionStack === null && this.setPreviousConditionStack(this.exportAllConditions()), this.runLocalHooks("beforeAdd", e);
    const l = this.getOperation(e);
    if (l) {
      if (l !== i)
        throw Error(mt`The column of index ${e} has been already applied with a \`${l}\`\x20
        filter operation. Use \`removeConditions\` to clear the current conditions and then add new ones.\x20
        Mind that you cannot mix different types of operations (for instance, if you use \`conjunction\`,\x20
        use it consequently for a particular column).`);
    } else if (Mt(ed[i]))
      throw new Error(mt`Unexpected operation named \`${i}\`. Possible ones are\x20
        \`disjunction\` and \`conjunction\`.`);
    const u = this.getConditions(e);
    u.length === 0 ? this.filteringStates.setValueAtIndex(e, {
      operation: i,
      conditions: [{
        name: a,
        args: s,
        func: Ks(a, s)
      }]
    }, r) : u.push({
      name: a,
      args: s,
      func: Ks(a, s)
    }), this.runLocalHooks("afterAdd", e);
  }
  /**
   * Get all added conditions from the collection at specified column index.
   *
   * @param {number} column The physical column index.
   * @returns {Array} Returns conditions collection as an array.
   */
  getConditions(e) {
    var n, i;
    return (n = (i = this.filteringStates.getValueAtIndex(e)) === null || i === void 0 ? void 0 : i.conditions) !== null && n !== void 0 ? n : [];
  }
  /**
   * Get operation for particular column.
   *
   * @param {number} column The physical column index.
   * @returns {string|undefined}
   */
  getOperation(e) {
    var n;
    return (n = this.filteringStates.getValueAtIndex(e)) === null || n === void 0 ? void 0 : n.operation;
  }
  /**
   * Get all filtered physical columns in the order in which actions are performed.
   *
   * @returns {Array}
   */
  getFilteredColumns() {
    return this.filteringStates.getEntries().map((e) => {
      let [n] = e;
      return n;
    });
  }
  /**
   * Gets position in the filtering states stack for the specific column.
   *
   * @param {number} column The physical column index.
   * @returns {number} Returns -1 when the column doesn't exist in the stack.
   */
  getColumnStackPosition(e) {
    return this.getFilteredColumns().indexOf(e);
  }
  /**
   * Export all previously added conditions.
   *
   * @returns {Array}
   */
  exportAllConditions() {
    return rn(this.filteringStates.getEntries(), (e, n) => {
      let [i, {
        operation: r,
        conditions: o
      }] = n;
      return e.push({
        column: i,
        operation: r,
        conditions: Ke(o, (s) => {
          let {
            name: a,
            args: l
          } = s;
          return {
            name: a,
            args: l
          };
        })
      }), e;
    }, []);
  }
  /**
   * Import conditions to the collection.
   *
   * @param {Array} conditions The collection of the conditions.
   */
  importAllConditions(e) {
    this.clean(), T(e, (n) => {
      T(n.conditions, (i) => this.addCondition(n.column, i));
    });
  }
  /**
   * Remove conditions at given column index.
   *
   * @param {number} column The physical column index.
   * @fires ConditionCollection#beforeRemove
   * @fires ConditionCollection#afterRemove
   */
  removeConditions(e) {
    this.setPreviousConditionStack(this.exportAllConditions()), this.runLocalHooks("beforeRemove", e), this.filteringStates.clearValue(e), this.runLocalHooks("afterRemove", e);
  }
  /**
   * Clean all conditions collection and reset order stack.
   *
   * @fires ConditionCollection#beforeClean
   * @fires ConditionCollection#afterClean
   */
  clean() {
    this.runLocalHooks("beforeClean"), this.filteringStates.clear(), this.runLocalHooks("afterClean");
  }
  /**
   * Check if at least one condition was added at specified column index. And if second parameter is passed then additionally
   * check if condition exists under its name.
   *
   * @param {number} column The physical column index.
   * @param {string} [name] Condition name.
   * @returns {boolean}
   */
  hasConditions(e, n) {
    const i = this.getConditions(e);
    return n ? i.some((r) => r.name === n) : i.length > 0;
  }
  /**
   * Updates the `previousConditionStack` property with the provided stack.
   * It is used to store the current conditions before they are modified, allowing for undo operations.
   *
   * @param {Array|null} previousConditionStack The stack of previous conditions.
   */
  setPreviousConditionStack(e) {
    this.previousConditionStack = e;
  }
  /**
   * Destroy object.
   */
  destroy() {
    this.isMapRegistrable && this.hot.columnIndexMapper.unregisterMap(LM), this.filteringStates = null, this.clearLocalHooks();
  }
}
pt(t_, Vn);
const n_ = t_;
function BM(t, e, n) {
  return (e = jne(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function jne(t) {
  var e = Gne(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Gne(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class zne {
  constructor(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : () => [];
    BM(this, "conditionCollection", void 0), BM(this, "columnDataFactory", void 0), this.conditionCollection = e, this.columnDataFactory = n;
  }
  /**
   * Filter data based on the conditions collection.
   *
   * @returns {Array}
   */
  filter() {
    let e = [];
    return T(this.conditionCollection.getFilteredColumns(), (n, i) => {
      let r = this.columnDataFactory(n);
      i && (r = this._getIntersectData(r, e)), e = this.filterByColumn(n, r);
    }), e;
  }
  /**
   * Filter data based on specified physical column index.
   *
   * @param {number} column The physical column index.
   * @param {Array} [dataSource] Data source as array of objects with `value` and `meta` keys (e.g. `{value: 'foo', meta: {}}`).
   * @returns {Array} Returns filtered data.
   */
  filterByColumn(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
    const i = [];
    return T(n, (r) => {
      r !== void 0 && this.conditionCollection.isMatch(r, e) && i.push(r);
    }), i;
  }
  /**
   * Intersect data.
   *
   * @private
   * @param {Array} data The data to intersect.
   * @param {Array} needles The collection intersected rows with the data.
   * @returns {Array}
   */
  _getIntersectData(e, n) {
    const i = [];
    return T(n, (r) => {
      const o = r.meta.visualRow;
      e[o] !== void 0 && (i[o] = e[o]);
    }), i;
  }
}
const i_ = zne;
function Kne(t, e) {
  Qne(t, e), e.add(t);
}
function Qne(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Ra(t, e, n) {
  return (e = Xne(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Xne(t) {
  var e = qne(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function qne(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function av(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var lg = /* @__PURE__ */ new WeakSet();
class r_ {
  constructor(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : () => [];
    Kne(this, lg), Ra(this, "hot", void 0), Ra(this, "conditionCollection", void 0), Ra(this, "columnDataFactory", void 0), Ra(this, "changes", []), Ra(this, "grouping", !1), Ra(this, "latestEditedColumnPosition", -1), Ra(this, "latestOrderStack", []), this.hot = e, this.conditionCollection = n, this.columnDataFactory = i, this.conditionCollection.addLocalHook("beforeRemove", (r) => av(lg, this, Zne).call(this, r)), this.conditionCollection.addLocalHook("afterRemove", (r) => this.updateStatesAtColumn(r)), this.conditionCollection.addLocalHook("afterAdd", (r) => this.updateStatesAtColumn(r)), this.conditionCollection.addLocalHook("beforeClean", () => av(lg, this, Jne).call(this)), this.conditionCollection.addLocalHook("afterClean", () => av(lg, this, eie).call(this));
  }
  /**
   * Enable grouping changes. Grouping is helpful in situations when a lot of conditions is added in one moment. Instead of
   * trigger `update` hook for every condition by adding/removing you can group this changes and call `flush` method to trigger
   * it once.
   */
  groupChanges() {
    this.grouping = !0;
  }
  /**
   * Flush all collected changes. This trigger `update` hook for every previously collected change from condition collection.
   */
  flush() {
    this.grouping = !1, T(this.changes, (e) => {
      this.updateStatesAtColumn(e);
    }), this.changes.length = 0;
  }
  /**
   * Update all related states which should be changed after invoking changes applied to current column.
   *
   * @param {number} column The column index.
   * @param {object} conditionArgsChange Object describing condition changes which can be handled by filters on `update` hook.
   * It contains keys `conditionKey` and `conditionValue` which refers to change specified key of condition to specified value
   * based on referred keys.
   */
  updateStatesAtColumn(e, n) {
    var i = this;
    if (this.grouping) {
      this.changes.indexOf(e) === -1 && this.changes.push(e);
      return;
    }
    const r = this.conditionCollection.exportAllConditions();
    let o = this.conditionCollection.getColumnStackPosition(e);
    o === -1 && (o = this.latestEditedColumnPosition);
    const s = r.slice(0, o), a = r.slice(o);
    a.length && a[0].column === e && a.shift();
    const l = XP(function(c, h) {
      let d = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : [];
      const g = new n_(i.hot, !1), m = [].concat(c, d);
      g.importAllConditions(m);
      const w = i.columnDataFactory(h);
      let C;
      g.isEmpty() ? C = w : C = new i_(g, (b) => i.columnDataFactory(b)).filter(), C = Ke(C, (b) => b.meta.visualRow);
      const S = kd(C);
      return g.destroy(), Zo(w, (b) => S(b.meta.visualRow));
    })(s), u = [].concat(this.conditionCollection.getConditions(e));
    this.runLocalHooks("update", {
      editedConditionStack: {
        column: e,
        conditions: u
      },
      dependentConditionStacks: a,
      filteredRowsFactory: l,
      conditionArgsChange: n
    });
  }
  /**
   * On before conditions clean listener.
   *
   * @private
   */
  /**
   * Destroy instance.
   */
  destroy() {
    this.clearLocalHooks(), Fe(this, (e, n) => {
      this[n] = null;
    });
  }
}
function Zne(t) {
  this.latestEditedColumnPosition = this.conditionCollection.getColumnStackPosition(t);
}
function Jne() {
  this.latestOrderStack = this.conditionCollection.getFilteredColumns();
}
function eie() {
  T(this.latestOrderStack, (t) => {
    this.updateStatesAtColumn(t);
  });
}
pt(r_, Vn);
const tie = r_;
function nie(t) {
  return bH({
    initialPage: 0,
    size: () => t.length,
    onItemSelect: (n, i) => {
      const r = t[n];
      if (r instanceof id)
        return i;
      if (r.element && !el(r.element))
        return !1;
      r.focus();
    }
  });
}
const Lc = "filters";
function iie(t, e) {
  let n = -1;
  const i = nie(e), r = (l) => () => {
    t.isOpened() && t.getKeyboardShortcutsCtrl().listen(Lc), i.setCurrentPage(e.indexOf(l));
  };
  e.forEach((l) => {
    l instanceof vr && (l.addLocalHook("click", r(l)), l.addLocalHook("focus", r(l)), l.addLocalHook("afterClose", r(l)));
  }), t.addLocalHook("afterSelectionChange", (l) => {
    l.key.startsWith("filter_") || i.clear();
  });
  function o(l) {
    const u = t.getKeyboardShortcutsCtrl(), c = l.getKeyboardShortcutsCtrl();
    i.clear(), c.addCustomShortcuts([{
      keys: [["Tab"], ["Shift", "Tab"]],
      forwardToContext: u.getContext(Lc),
      callback: () => {
        l.isSubMenu() && l.close(), u.listen(Lc);
      }
    }]), !l.isSubMenu() && u.addCustomShortcuts([{
      keys: [["Tab"], ["Shift", "Tab"]],
      callback: (h) => {
        const d = t.getNavigator();
        d.getCurrentPage() > -1 && (n = d.getCurrentPage()), d.clear(), h.shiftKey ? i.toPreviousItem() : i.toNextItem();
      }
    }, {
      keys: [["Escape"]],
      callback: () => {
        t.close();
      }
    }, {
      keys: [["Enter"], ["Space"]],
      preventDefault: !1,
      callback: (h) => {
        const d = e[i.getCurrentPage()];
        d instanceof td && (d.openOptions(), h.preventDefault()), d instanceof nd && (d.activate(), h.preventDefault()), d instanceof vr || h.preventDefault();
      }
    }], Lc);
  }
  t.addLocalHook("afterSubmenuOpen", o), t.addLocalHook("afterOpen", o);
  function s() {
    t.focus(), t.getKeyboardShortcutsCtrl().listen(Lc);
  }
  function a() {
    return n;
  }
  return On(Xe({}, i), {
    listen: s,
    getLastMenuPage: a
  });
}
function rie(t, e) {
  o_(t, e), e.add(t);
}
function oie(t, e, n) {
  o_(t, e), e.set(t, n);
}
function o_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Bc(t, e, n) {
  return (e = sie(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function sie(t) {
  var e = aie(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function aie(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function lie(t, e, n) {
  return t.set(Ai(t, e), n), n;
}
function Fc(t, e) {
  return t.get(Ai(t, e));
}
function Ai(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const Jy = "filters", uie = 250, FM = Jy;
var Ea = /* @__PURE__ */ new WeakMap(), Ni = /* @__PURE__ */ new WeakSet();
class cie extends at {
  static get PLUGIN_KEY() {
    return Jy;
  }
  static get PLUGIN_PRIORITY() {
    return uie;
  }
  static get PLUGIN_DEPS() {
    return ["plugin:DropdownMenu", "plugin:HiddenRows", "cell-type:checkbox"];
  }
  /**
   * Instance of {@link DropdownMenu}.
   *
   * @private
   * @type {DropdownMenu}
   */
  constructor(e) {
    super(e), rie(this, Ni), Bc(this, "dropdownMenuPlugin", null), Bc(this, "conditionCollection", null), Bc(this, "conditionUpdateObserver", null), Bc(this, "components", /* @__PURE__ */ new Map([["filter_by_condition", null], ["filter_operators", null], ["filter_by_condition2", null], ["filter_by_value", null], ["filter_action_bar", null]])), Bc(this, "filtersRowsMap", null), oie(this, Ea, void 0), this.hot.addHook("afterGetColHeader", (n, i) => Ai(Ni, this, pie).call(this, n, i));
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link Filters#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[Jy];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    if (this.enabled)
      return;
    this.filtersRowsMap = this.hot.rowIndexMapper.registerMap(this.pluginName, new Td()), this.dropdownMenuPlugin = this.hot.getPlugin("dropdownMenu");
    const e = this.hot.getSettings().dropdownMenu, n = e && e.uiContainer || this.hot.rootDocument.body, i = (s) => (s.addLocalHook("accept", () => Ai(Ni, this, VM).call(this, "accept")), s.addLocalHook("cancel", () => Ai(Ni, this, VM).call(this, "cancel")), s.addLocalHook("change", (a) => Ai(Ni, this, mie).call(this, s, a)), s), r = () => `${this.hot.getTranslatedPhrase(Bw)}:`, o = () => `${this.hot.getTranslatedPhrase(Fw)}:`;
    if (!this.components.get("filter_by_condition")) {
      const s = new Zy(this.hot, {
        id: "filter_by_condition",
        name: r,
        addSeparator: !1,
        menuContainer: n
      });
      s.addLocalHook("afterClose", () => Ai(Ni, this, WM).call(this)), this.components.set("filter_by_condition", i(s));
    }
    if (this.components.get("filter_operators") || this.components.set("filter_operators", new pne(this.hot, {
      id: "filter_operators",
      name: "Operators"
    })), !this.components.get("filter_by_condition2")) {
      const s = new Zy(this.hot, {
        id: "filter_by_condition2",
        name: "",
        addSeparator: !0,
        menuContainer: n
      });
      s.addLocalHook("afterClose", () => Ai(Ni, this, WM).call(this)), this.components.set("filter_by_condition2", i(s));
    }
    if (this.components.get("filter_by_value") || this.components.set("filter_by_value", i(new _ne(this.hot, {
      id: "filter_by_value",
      name: o
    }))), this.components.get("filter_action_bar") || this.components.set("filter_action_bar", i(new rd(this.hot, {
      id: "filter_action_bar",
      name: "Action bar"
    }))), this.conditionCollection || (this.conditionCollection = new n_(this.hot)), this.conditionUpdateObserver || (this.conditionUpdateObserver = new tie(this.hot, this.conditionCollection, (s) => this.getDataMapAtColumn(s)), this.conditionUpdateObserver.addLocalHook("update", (s) => Ai(Ni, this, wie).call(this, s))), this.components.forEach((s) => s.show()), this.addHook("afterDropdownMenuDefaultOptions", (s) => Ai(Ni, this, gie).call(this, s)), this.addHook("afterDropdownMenuShow", () => Ai(Ni, this, die).call(this)), this.addHook("afterDropdownMenuHide", () => Ai(Ni, this, fie).call(this)), this.addHook("afterChange", (s) => Ai(Ni, this, hie).call(this, s)), this.hot.getSettings().dropdownMenu && this.dropdownMenuPlugin && (this.dropdownMenuPlugin.disablePlugin(), this.dropdownMenuPlugin.enablePlugin()), !Fc(Ea, this) && this.dropdownMenuPlugin.enabled) {
      const s = this.dropdownMenuPlugin.menu, a = [
        // A fake menu item that once focused allows escaping from the focus navigation (using Tab keys)
        // to the menu navigation using arrow keys.
        {
          focus: () => {
            const u = s.getNavigator(), c = Fc(Ea, this).getLastMenuPage();
            s.focus(), c > 0 ? u.setCurrentPage(c) : u.toFirstItem();
          }
        },
        ...Array.from(this.components).map((u) => {
          let [, c] = u;
          return c.getElements();
        }).flat()
      ];
      lie(Ea, this, iie(s, a));
      const l = (u) => {
        Fc(Ea, this).listen(), u.preventDefault(), Lu(u.keyCode, "TAB") && (u.shiftKey ? Fc(Ea, this).toPreviousItem() : Fc(Ea, this).toNextItem());
      };
      this.components.get("filter_by_value").addLocalHook("listTabKeydown", l), this.components.get("filter_by_condition").addLocalHook("selectTabKeydown", l);
    }
    this.registerShortcuts(), super.enablePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    if (this.enabled) {
      var e;
      (e = this.dropdownMenuPlugin) !== null && e !== void 0 && e.enabled && this.dropdownMenuPlugin.menu.clearLocalHooks(), this.components.forEach((n, i) => {
        n.destroy(), this.components.set(i, null);
      }), this.conditionCollection.destroy(), this.conditionCollection = null, this.hot.rowIndexMapper.unregisterMap(this.pluginName);
    }
    this.unregisterShortcuts(), super.disablePlugin();
  }
  /**
   * Register shortcuts responsible for clearing the filters.
   *
   * @private
   */
  registerShortcuts() {
    this.hot.getShortcutManager().getContext("grid").addShortcut({
      keys: [["Alt", "A"]],
      stopPropagation: !0,
      callback: () => {
        const e = this.hot.getSelected();
        this.clearConditions(), this.filter(), e && this.hot.selectCells(e);
      },
      group: FM
    });
  }
  /**
   * Unregister shortcuts responsible for clearing the filters.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("grid").removeShortcutsByGroup(FM);
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * @memberof Filters#
   * @function addCondition
   * @description
   * Adds condition to the conditions collection at specified column index.
   *
   * Possible predefined conditions:
   *  * `begins_with` - Begins with
   *  * `between` - Between
   *  * `by_value` - By value
   *  * `contains` - Contains
   *  * `date_after` - After a date
   *  * `date_before` - Before a date
   *  * `date_today` - Today
   *  * `date_tomorrow` - Tomorrow
   *  * `date_yesterday` - Yesterday
   *  * `empty` - Empty
   *  * `ends_with` - Ends with
   *  * `eq` - Equal
   *  * `gt` - Greater than
   *  * `gte` - Greater than or equal
   *  * `lt` - Less than
   *  * `lte` - Less than or equal
   *  * `none` - None (no filter)
   *  * `not_between` - Not between
   *  * `not_contains` - Not contains
   *  * `not_empty` - Not empty
   *  * `neq` - Not equal.
   *
   * Possible operations on collection of conditions:
   *  * `conjunction` - [**Conjunction**](https://en.wikipedia.org/wiki/Logical_conjunction) on conditions collection (by default), i.e. for such operation: <br/> c1 AND c2 AND c3 AND c4 ... AND cn === TRUE, where c1 ... cn are conditions.
   *  * `disjunction` - [**Disjunction**](https://en.wikipedia.org/wiki/Logical_disjunction) on conditions collection, i.e. for such operation: <br/> c1 OR c2 OR c3 OR c4 ... OR cn === TRUE, where c1, c2, c3, c4 ... cn are conditions.
   *  * `disjunctionWithExtraCondition` - **Disjunction** on first `n - 1`\* conditions from collection with an extra requirement computed from the last condition, i.e. for such operation: <br/> c1 OR c2 OR c3 OR c4 ... OR cn-1 AND cn === TRUE, where c1, c2, c3, c4 ... cn are conditions.
   *
   * \* when `n` is collection size; it's used i.e. for one operation introduced from UI (when choosing from filter's drop-down menu two conditions with OR operator between them, mixed with choosing values from the multiple choice select)
   *
   * **Note**: Mind that you cannot mix different types of operations (for instance, if you use `conjunction`, use it consequently for a particular column).
   *
   * @example
   * ::: only-for javascript
   * ```js
   * const container = document.getElementById('example');
   * const hot = new Handsontable(container, {
   *   data: getData(),
   *   filters: true
   * });
   *
   * // access to filters plugin instance
   * const filtersPlugin = hot.getPlugin('filters');
   *
   * // add filter "Greater than" 95 to column at index 1
   * filtersPlugin.addCondition(1, 'gt', [95]);
   * filtersPlugin.filter();
   *
   * // add filter "By value" to column at index 1
   * // in this case all value's that don't match will be filtered.
   * filtersPlugin.addCondition(1, 'by_value', [['ing', 'ed', 'as', 'on']]);
   * filtersPlugin.filter();
   *
   * // add filter "Begins with" with value "de" AND "Not contains" with value "ing"
   * filtersPlugin.addCondition(1, 'begins_with', ['de'], 'conjunction');
   * filtersPlugin.addCondition(1, 'not_contains', ['ing'], 'conjunction');
   * filtersPlugin.filter();
   *
   * // add filter "Begins with" with value "de" OR "Not contains" with value "ing"
   * filtersPlugin.addCondition(1, 'begins_with', ['de'], 'disjunction');
   * filtersPlugin.addCondition(1, 'not_contains', ['ing'], 'disjunction');
   * filtersPlugin.filter();
   * ```
   * :::
   *
   * ::: only-for react
   * ```jsx
   * const hotRef = useRef(null);
   *
   * ...
   *
   * <HotTable
   *   ref={hotRef}
   *   data={getData()}
   *   filters={true}
   * />
   *
   * // access to filters plugin instance
   * const hot = hotRef.current.hotInstance;
   * const filtersPlugin = hot.getPlugin('filters');
   *
   * // add filter "Greater than" 95 to column at index 1
   * filtersPlugin.addCondition(1, 'gt', [95]);
   * filtersPlugin.filter();
   *
   * // add filter "By value" to column at index 1
   * // in this case all value's that don't match will be filtered.
   * filtersPlugin.addCondition(1, 'by_value', [['ing', 'ed', 'as', 'on']]);
   * filtersPlugin.filter();
   *
   * // add filter "Begins with" with value "de" AND "Not contains" with value "ing"
   * filtersPlugin.addCondition(1, 'begins_with', ['de'], 'conjunction');
   * filtersPlugin.addCondition(1, 'not_contains', ['ing'], 'conjunction');
   * filtersPlugin.filter();
   *
   * // add filter "Begins with" with value "de" OR "Not contains" with value "ing"
   * filtersPlugin.addCondition(1, 'begins_with', ['de'], 'disjunction');
   * filtersPlugin.addCondition(1, 'not_contains', ['ing'], 'disjunction');
   * filtersPlugin.filter();
   * ```
   * :::
   *
   * @param {number} column Visual column index.
   * @param {string} name Condition short name.
   * @param {Array} args Condition arguments.
   * @param {string} [operationId=conjunction] `id` of operation which is performed on the column.
   */
  /* eslint-enable jsdoc/require-description-complete-sentence */
  addCondition(e, n, i) {
    let r = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : Us;
    const o = this.hot.toPhysicalColumn(e);
    this.conditionCollection.addCondition(o, {
      command: {
        key: n
      },
      args: i
    }, r);
  }
  /**
   * Removes conditions at specified column index.
   *
   * @param {number} column Visual column index.
   */
  removeConditions(e) {
    const n = this.hot.toPhysicalColumn(e);
    this.conditionCollection.removeConditions(n);
  }
  /**
   * Clears all conditions previously added to the collection for the specified column index or, if the column index
   * was not passed, clear the conditions for all columns.
   *
   * @param {number} [column] Visual column index.
   */
  clearConditions(e) {
    if (e === void 0)
      this.conditionCollection.clean();
    else {
      const n = this.hot.toPhysicalColumn(e);
      this.conditionCollection.removeConditions(n);
    }
  }
  /**
   * Filters data based on added filter conditions.
   *
   * @fires Hooks#beforeFilter
   * @fires Hooks#afterFilter
   */
  filter() {
    const {
      navigableHeaders: e
    } = this.hot.getSettings(), n = this._createDataFilter(), i = !this.conditionCollection.isEmpty();
    let r = [];
    const o = this.conditionCollection.exportAllConditions();
    if (this.hot.runHooks("beforeFilter", o, this.conditionCollection.previousConditionStack) !== !1)
      if (i) {
        const a = [];
        this.hot.batchExecution(() => {
          this.filtersRowsMap.clear(), r = Ke(n.filter(), (u) => u.meta.visualRow);
          const l = kd(r);
          Ae(this.hot.countSourceRows() - 1, (u) => {
            l(u) || a.push(u);
          }), T(a, (u) => {
            this.filtersRowsMap.setValueAtIndex(u, !0);
          });
        }, !0), !e && !r.length && this.hot.deselectCell();
      } else
        this.filtersRowsMap.clear();
    this.hot.runHooks("afterFilter", o), this.conditionCollection.setPreviousConditionStack(null), this.hot.view.adjustElementsSize(), this.hot.render(), this.hot.selection.isSelected() && this.hot.selectCell(e ? -1 : 0, this.hot.getSelectedRangeLast().highlight.col);
  }
  /**
   * Gets last selected column index.
   *
   * @returns {{visualIndex: number, physicalIndex: number} | null} Returns `null` when a column is
   * not selected. Otherwise, returns an object with `visualIndex` and `physicalIndex` properties containing
   * the index of the column.
   */
  getSelectedColumn() {
    var e;
    const n = (e = this.hot.getSelectedRangeLast()) === null || e === void 0 ? void 0 : e.highlight;
    return n ? {
      visualIndex: n.col,
      physicalIndex: this.hot.toPhysicalColumn(n.col)
    } : null;
  }
  /**
   * Returns handsontable source data with cell meta based on current selection.
   *
   * @param {number} [column] The physical column index. By default column index accept the value of the selected column.
   * @returns {Array} Returns array of objects where keys as row index.
   */
  getDataMapAtColumn(e) {
    const n = this.hot.toVisualColumn(e), i = [];
    return T(this.hot.getSourceDataAtCol(n), (r, o) => {
      var s;
      const {
        row: a,
        col: l,
        visualCol: u,
        visualRow: c,
        type: h,
        instance: d,
        dateFormat: g,
        locale: m
      } = this.hot.getCellMeta(o, n), w = (s = this.hot.getDataAtCell(this.hot.toVisualRow(o), n)) !== null && s !== void 0 ? s : r;
      i.push({
        meta: {
          row: a,
          col: l,
          visualCol: u,
          visualRow: c,
          type: h,
          instance: d,
          dateFormat: g,
          locale: m
        },
        value: GH(w)
      });
    }), i;
  }
  /**
   * Update the condition of ValueComponent, based on the handled changes.
   *
   * @private
   * @param {number} columnIndex Column index of handled ValueComponent condition.
   */
  updateValueComponentCondition(e) {
    const n = this.hot.getDataAtCol(e), i = qy(n);
    this.conditionUpdateObserver.updateStatesAtColumn(e, i);
  }
  /**
   * Restores components to its saved state.
   *
   * @private
   * @param {Array} components List of components.
   */
  restoreComponents(e) {
    var n;
    const i = (n = this.getSelectedColumn()) === null || n === void 0 ? void 0 : n.physicalIndex;
    e.forEach((r) => {
      r.isHidden() || r.restoreState(i);
    }), this.updateDependentComponentsVisibility();
  }
  /**
   * After dropdown menu show listener.
   */
  /**
   * Get an operation, based on the number and types of arguments (where arguments are states of components).
   *
   * @param {string} suggestedOperation Operation which was chosen by user from UI.
   * @param {object} byConditionState1 State of first condition component.
   * @param {object} byConditionState2 State of second condition component.
   * @param {object} byValueState State of value component.
   * @private
   * @returns {string}
   */
  getOperationBasedOnArguments(e, n, i, r) {
    let o = e;
    return o === km && n.command.key !== yn && i.command.key !== yn && r.command.key !== yn ? o = _R : r.command.key !== yn && (n.command.key === yn || i.command.key === yn) && (o = Us), o;
  }
  /**
   * On action bar submit listener.
   *
   * @private
   * @param {string} submitType The submit type.
   */
  /**
   * Listen to the keyboard input on document body and forward events to instance of Handsontable
   * created by DropdownMenu plugin.
   *
   * @private
   */
  setListeningDropdownMenu() {
    this.dropdownMenuPlugin && this.dropdownMenuPlugin.setListening();
  }
  /**
   * Updates visibility of some of the components, based on the state of the parent component.
   *
   * @private
   */
  updateDependentComponentsVisibility() {
    const e = this.components.get("filter_by_condition"), {
      command: n
    } = e.getState(), i = [this.components.get("filter_by_condition2"), this.components.get("filter_operators")];
    n.showOperators ? this.showComponents(...i) : this.hideComponents(...i);
  }
  /**
   * On after get column header listener.
   *
   * @param {number} col Visual column index.
   * @param {HTMLTableCellElement} TH Header's TH element.
   */
  /**
   * Creates DataFilter instance based on condition collection.
   *
   * @private
   * @param {ConditionCollection} conditionCollection Condition collection object.
   * @returns {DataFilter}
   */
  _createDataFilter() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.conditionCollection;
    return new i_(e, (n) => this.getDataMapAtColumn(n));
  }
  /**
   * It updates the components state. The state is triggered by ConditionUpdateObserver, which
   * reacts to any condition added to the condition collection. It may be added through the UI
   * components or by API call.
   *
   * @param {object} conditionsState An object with the state generated by UI components.
   */
  /**
   * Returns indexes of passed components inside list of `dropdownMenu` items.
   *
   * @private
   * @param {...BaseComponent} components List of components.
   * @returns {Array}
   */
  getIndexesOfComponents() {
    const e = [];
    if (!this.dropdownMenuPlugin)
      return e;
    const n = this.dropdownMenuPlugin.menu;
    for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
      r[o] = arguments[o];
    return T(r, (s) => {
      T(n.menuItems, (a, l) => {
        a.key === s.getMenuItemDescriptor().key && e.push(l);
      });
    }), e;
  }
  /**
   * Changes visibility of component.
   *
   * @private
   * @param {boolean} visible Determine if components should be visible.
   * @param {...BaseComponent} components List of components.
   */
  changeComponentsVisibility() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
    if (!this.dropdownMenuPlugin)
      return;
    const i = this.dropdownMenuPlugin.menu.hotMenu, r = i.getPlugin("hiddenRows");
    for (var o = arguments.length, s = new Array(o > 1 ? o - 1 : 0), a = 1; a < o; a++)
      s[a - 1] = arguments[a];
    const l = this.getIndexesOfComponents(...s);
    e ? r.showRows(l) : r.hideRows(l), i.render();
  }
  /**
   * Hides components of filters `dropdownMenu`.
   *
   * @private
   * @param {...BaseComponent} components List of components.
   */
  hideComponents() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    this.changeComponentsVisibility(!1, ...n);
  }
  /**
   * Shows components of filters `dropdownMenu`.
   *
   * @private
   * @param {...BaseComponent} components List of components.
   */
  showComponents() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    this.changeComponentsVisibility(!0, ...n);
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    this.enabled && (this.components.forEach((e, n) => {
      e !== null && (e.destroy(), this.components.set(n, null));
    }), this.conditionCollection.destroy(), this.conditionUpdateObserver.destroy(), this.hot.rowIndexMapper.unregisterMap(this.pluginName)), super.destroy();
  }
}
function hie(t) {
  t && T(t, (e) => {
    const [, n] = e, i = this.hot.propToCol(n);
    this.conditionCollection.hasConditions(i) && this.updateValueComponentCondition(i);
  });
}
function die() {
  const t = this.dropdownMenuPlugin.menu;
  this.restoreComponents(Array.from(this.components.values())), t.updateMenuDimensions();
}
function fie() {
  this.components.get("filter_by_condition").getSelectElement().closeOptions(), this.components.get("filter_by_condition2").getSelectElement().closeOptions();
}
function gie(t) {
  t.items.push({
    name: $e
  }), this.components.forEach((e) => {
    t.items.push(e.getMenuItemDescriptor());
  });
}
function VM(t) {
  var e;
  if (t === "accept") {
    const i = this.getSelectedColumn();
    if (i === null) {
      var n;
      (n = this.dropdownMenuPlugin) === null || n === void 0 || n.close();
      return;
    }
    const {
      physicalIndex: r
    } = i, o = this.components.get("filter_by_condition").getState(), s = this.components.get("filter_by_condition2").getState(), a = this.components.get("filter_by_value").getState(), l = this.getOperationBasedOnArguments(this.components.get("filter_operators").getActiveOperationId(), o, s, a);
    this.conditionUpdateObserver.groupChanges();
    let u = this.conditionCollection.getColumnStackPosition(r);
    u === -1 && (u = void 0), this.conditionCollection.removeConditions(r), o.command.key !== yn && (this.conditionCollection.addCondition(r, o, l, u), s.command.key !== yn && this.conditionCollection.addCondition(r, s, l, u)), a.command.key !== yn && this.conditionCollection.addCondition(r, a, l, u), this.conditionUpdateObserver.flush(), this.components.forEach((c) => c.saveState(r)), this.filtersRowsMap.clear(), this.filter();
  }
  (e = this.dropdownMenuPlugin) === null || e === void 0 || e.close();
}
function mie(t, e) {
  this.updateDependentComponentsVisibility(), t.constructor === Zy && !e.inputsCount && this.setListeningDropdownMenu();
}
function WM() {
  this.setListeningDropdownMenu();
}
function pie(t, e) {
  const n = this.hot.toPhysicalColumn(t);
  this.enabled && this.conditionCollection.hasConditions(n) ? Q(e, "htFiltersActive") : xe(e, "htFiltersActive");
}
function wie(t) {
  var e;
  if (!((e = this.dropdownMenuPlugin) !== null && e !== void 0 && e.enabled))
    return;
  const {
    editedConditionStack: {
      conditions: n,
      column: i
    }
  } = t, r = n.filter((s) => s.name === Qa), o = n.filter((s) => s.name !== Qa);
  if (r.length >= 2 || o.length >= 3)
    gt(mt`The filter conditions have been applied properly, but couldn’t be displayed visually.\x20
        The overall amount of conditions exceed the capability of the dropdown menu.\x20
        For more details see the documentation.`);
  else {
    const s = this.conditionCollection.getOperation(i);
    this.components.get("filter_by_condition").updateState(o[0], i), this.components.get("filter_by_condition2").updateState(o[1], i), this.components.get("filter_operators").updateState(s, i), this.components.get("filter_by_value").updateState(t);
  }
}
const s_ = "internal-use-in-handsontable", eS = {
  licenseKey: s_,
  useArrayArithmetic: !0,
  useColumnIndex: !1,
  useStats: !1,
  evaluateNullToZero: !0,
  precisionEpsilon: 1e-13,
  precisionRounding: 14,
  smartRounding: !0,
  leapYear1900: !1,
  nullDate: {
    year: 1899,
    month: 12,
    day: 30
  },
  nullYear: 30,
  dateFormats: ["DD/MM/YYYY", "DD/MM/YY"],
  timeFormats: ["hh:mm", "hh:mm:ss.sss"],
  matchWholeCell: !0,
  useRegularExpressions: !1,
  useWildcards: !0,
  functionArgSeparator: ",",
  thousandSeparator: "",
  decimalSeparator: ".",
  language: "enGB"
};
function a_(t) {
  var e;
  return {
    maxColumns: t.maxColumns,
    maxRows: t.maxRows,
    language: (e = t[kn]) === null || e === void 0 || (e = e.language) === null || e === void 0 ? void 0 : e.langCode
  };
}
function l_(t) {
  return Object.keys(t).reduce((e, n) => (n !== "hyperformula" && (e[n] = t[n]), e), {});
}
function Aie(t) {
  var e;
  const n = t[kn], i = l_(n != null && (e = n.engine) !== null && e !== void 0 && e.hyperformula ? n.engine : {}), r = a_(t);
  return Xe(Xe(Xe({}, eS), i), r);
}
function Cie(t) {
  var e;
  const n = t[kn], i = l_(n != null && (e = n.engine) !== null && e !== void 0 && e.hyperformula ? n.engine : {}), r = a_(t);
  return Xe(Xe({}, i), r);
}
function vie(t, e) {
  return Object.keys(e).some((n) => e[n] !== void 0 && e[n] !== t[n]);
}
function cA() {
  const t = "engine_relationship", e = Or(kn);
  return e.hasItem(t) || e.register(t, /* @__PURE__ */ new Map()), e.getItem(t);
}
function DR() {
  const t = "shared_engine_usage", e = Or(kn);
  return e.hasItem(t) || e.register(t, /* @__PURE__ */ new Map()), e.getItem(t);
}
function yie(t) {
  const e = t.getSettings(), n = e[kn], i = n == null ? void 0 : n.engine;
  if (n === !0 || Mt(i))
    return null;
  if (typeof i.hyperformula == "function" || typeof i == "function") {
    var r;
    return Sie((r = i.hyperformula) !== null && r !== void 0 ? r : i, e, t);
  } else if (typeof i == "object" && Mt(i.hyperformula)) {
    const o = cA(), s = DR().get(i);
    return o.has(i) || o.set(i, []), o.get(i).push(t), s && s.push(t.guid), i.getConfig().licenseKey || i.updateConfig({
      licenseKey: s_
    }), (i.getConfig().leapYear1900 !== eS.leapYear1900 || Op(i.getConfig().nullDate, eS.nullDate) === !1) && gt(mt`If you use HyperFormula with Handsontable, keep the default \`leapYear1900\` and \`nullDate\` 
      settings. Otherwise, HyperFormula's dates may not sync correctly with Handsontable's dates.`), i;
  }
  return null;
}
function Sie(t, e, n) {
  const i = e[kn], r = Aie(e), o = cA(), s = DR();
  bie(t, i.functions), Rie(t, i.language);
  const a = t.buildEmpty(r);
  return o.set(a, [n]), s.set(a, [n.guid]), Eie(a, i.namedExpressions), a.on("sheetAdded", () => {
    a.rebuildAndRecalculate();
  }), a.on("sheetRemoved", () => {
    a.rebuildAndRecalculate();
  }), a;
}
function $M(t) {
  var e;
  const n = cA(), i = n.size === 0 ? [] : Array.from((e = n.get(t)) !== null && e !== void 0 ? e : []);
  return new Map(i.map((r) => [r.getPlugin("formulas").sheetId, r]));
}
function UM(t, e) {
  if (t) {
    const n = cA(), i = n.get(t), r = DR(), o = r.get(t);
    i && i.includes(e) && (i.splice(i.indexOf(e), 1), i.length === 0 && n.delete(t)), o && o.includes(e.guid) && (o.splice(o.indexOf(e.guid), 1), o.length === 0 && (r.delete(t), t.destroy()));
  }
}
function bie(t, e) {
  e && e.forEach((n) => {
    const {
      name: i,
      plugin: r,
      translations: o
    } = n;
    try {
      t.registerFunction(i, r, o);
    } catch (s) {
      gt(s.message);
    }
  });
}
function Rie(t, e) {
  if (e) {
    const {
      langCode: n
    } = e;
    try {
      t.registerLanguage(n, e);
    } catch (i) {
      gt(i.message);
    }
  }
}
function Eie(t, e) {
  e && (t.suspendEvaluation(), e.forEach((n) => {
    const {
      name: i,
      expression: r,
      scope: o,
      options: s
    } = n;
    try {
      t.addNamedExpression(i, r, o, s);
    } catch (a) {
      gt(a.message);
    }
  }), t.resumeEvaluation());
}
function Tie(t, e) {
  return (Mt(e) || !t.doesSheetExist(e)) && (e = t.addSheet(e)), e;
}
const u_ = "DD/MM/YYYY";
function c_(t) {
  return typeof t == "string" && t.charAt(0) === "'" && t.charAt(1) === "=";
}
function Mie(t) {
  return c_(t) ? t.substr(1) : t;
}
function Iie(t) {
  return typeof t == "string" && t.startsWith("=");
}
function kR(t, e) {
  return typeof t == "string" && e === "date";
}
function h_(t, e) {
  return X(t, e, !0).isValid();
}
function d_(t, e) {
  return X(t, e, !0).format(u_);
}
function Oie(t, e) {
  return X(t, u_, !0).format(e);
}
function f_(t, e) {
  const i = new Date(Date.UTC(0, 0, t + -1));
  return X(i).format(e);
}
function Pie(t, e, n) {
  const i = Array.from(Array(n).keys()).filter((o) => t.includes(o) === !1);
  if (e === 0) {
    var r;
    return (r = i[e]) !== null && r !== void 0 ? r : 0;
  }
  return i[e - 1] + 1;
}
function xie(t, e) {
  const n = [];
  return t.forEach((i) => {
    const r = {
      from: i,
      to: e
    };
    n.forEach((o) => {
      const s = o.from > o.to, a = o.to <= r.from;
      o.from > r.from && a && s && (r.from += 1);
    }), r.from >= e && (e += 1), n.push(r);
  }), n;
}
function Hie(t) {
  return t.forEach((e, n) => {
    t.slice(n + 1).forEach((r) => {
      const o = r.from < r.to;
      r.from > e.from && o && (r.from -= 1);
    });
  }), t;
}
function LR(t, e, n) {
  const i = xie(t, Pie(t, e, n));
  return Hie(i);
}
function Ta(t, e, n) {
  _ie(t, e), e.set(t, n);
}
function _ie(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Ht(t, e) {
  return t.get(g_(t, e));
}
function ps(t, e, n) {
  return t.set(g_(t, e), n), n;
}
function g_(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Vc = /* @__PURE__ */ new WeakMap(), ws = /* @__PURE__ */ new WeakMap(), gi = /* @__PURE__ */ new WeakMap(), ug = /* @__PURE__ */ new WeakMap(), lv = /* @__PURE__ */ new WeakMap(), uv = /* @__PURE__ */ new WeakMap(), cg = /* @__PURE__ */ new WeakMap();
class Nie {
  constructor(e, n, i) {
    Ta(this, Vc, void 0), Ta(this, ws, void 0), Ta(this, gi, void 0), Ta(this, ug, []), Ta(this, lv, []), Ta(this, uv, void 0), Ta(this, cg, []), ps(Vc, this, e), ps(ws, this, n), ps(gi, this, i);
  }
  /**
   * Sets removed HF indexes (it should be done right before performing move on HOT).
   *
   * @param {Array<number>} removedIndexes List of removed physical indexes.
   * @returns {Array<number>} List of removed visual indexes.
   */
  setRemovedHfIndexes(e) {
    return ps(cg, this, e.map((n) => {
      const i = Ht(ws, this).getVisualFromPhysicalIndex(n);
      return this.getHfIndexFromVisualIndex(i);
    })), Ht(cg, this);
  }
  /**
   * Gets removed HF indexes (right before performing removal on HOT).
   *
   * @returns {Array<number>} List of removed HF indexes.
   */
  getRemovedHfIndexes() {
    return Ht(cg, this);
  }
  /**
   * Gets corresponding HyperFormula index for particular visual index. It's respecting the idea that HF's engine
   * is fed also with trimmed indexes (business requirements for formula result calculation also for trimmed elements).
   *
   * @param {number} visualIndex Visual index.
   * @returns {number}
   */
  getHfIndexFromVisualIndex(e) {
    const n = Ht(ws, this).getIndexesSequence(), i = Ht(ws, this).getNotTrimmedIndexes();
    return n.indexOf(i[e]);
  }
  /**
   * Synchronizes moves done on HOT to HF engine (based on previously calculated positions).
   *
   * @private
   * @param {Array<{from: number, to: number}>} moves Calculated HF's move positions.
   */
  syncMoves(e) {
    const i = `move${Bn(Ht(Vc, this))}s`;
    Ht(gi, this).getEngine().batch(() => {
      e.forEach((r) => {
        const o = r.from !== r.to, s = r.from + 1 !== r.to;
        o && s && Ht(gi, this).getEngine()[i](Ht(gi, this).getSheetId(), r.from, 1, r.to);
      });
    });
  }
  /**
   * Stores information about performed HOT moves for purpose of calculating where to move HF elements.
   *
   * @param {Array<number>} movedVisualIndexes Sequence of moved visual indexes for certain axis.
   * @param {number} visualFinalIndex Final visual place where to move HOT indexes.
   * @param {boolean} movePossible Indicates if it's possible to move HOT indexes to the desired position.
   */
  storeMovesInformation(e, n, i) {
    i !== !1 && (ps(lv, this, e.map((r) => this.getHfIndexFromVisualIndex(r))), ps(uv, this, this.getHfIndexFromVisualIndex(n)));
  }
  /**
   * Calculating where to move HF elements and performing already calculated moves.
   *
   * @param {boolean} movePossible Indicates if it was possible to move HOT indexes to the desired position.
   * @param {boolean} orderChanged Indicates if order of HOT indexes was changed by move.
   */
  calculateAndSyncMoves(e, n) {
    if (Ht(gi, this).isPerformingUndoRedo() || e === !1 || n === !1)
      return;
    const i = LR(Ht(lv, this), Ht(uv, this), Ht(ws, this).getNumberOfIndexes());
    Ht(gi, this).getSheetId() === null ? Ht(gi, this).getPostponeAction(() => this.syncMoves(i)) : this.syncMoves(i);
  }
  /**
   * Gets callback for hook triggered after performing change of indexes order.
   *
   * @returns {Function}
   */
  getIndexesChangeSyncMethod() {
    const e = `set${Bn(Ht(Vc, this))}Order`;
    return (n) => {
      if (Ht(gi, this).isPerformingUndoRedo())
        return;
      const i = Ht(ws, this).getIndexesSequence();
      if (n === "update" && i.length > 0) {
        const r = Ht(ug, this).map((l) => i.indexOf(l)), o = Ht(gi, this).getEngine().getSheetDimensions(Ht(gi, this).getSheetId());
        let s;
        Ht(Vc, this) === "row" ? s = o.height : s = o.width;
        const a = r.length;
        for (let l = a; l < s; l += 1)
          r.push(l);
        Ht(gi, this).getEngine()[e](Ht(gi, this).getSheetId(), r);
      }
      ps(ug, this, i);
    };
  }
  /**
   * Initialize the AxisSyncer.
   */
  init() {
    ps(ug, this, Ht(ws, this).getIndexesSequence());
  }
}
const YM = Nie;
function Ma(t, e, n) {
  Die(t, e), e.set(t, n);
}
function Die(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Po(t, e) {
  return t.get(m_(t, e));
}
function Ia(t, e, n) {
  return t.set(m_(t, e), n), n;
}
function m_(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var hg = /* @__PURE__ */ new WeakMap(), dg = /* @__PURE__ */ new WeakMap(), cv = /* @__PURE__ */ new WeakMap(), hv = /* @__PURE__ */ new WeakMap(), dv = /* @__PURE__ */ new WeakMap(), fv = /* @__PURE__ */ new WeakMap(), gv = /* @__PURE__ */ new WeakMap();
class kie {
  constructor(e, n, i) {
    Ma(this, hg, void 0), Ma(this, dg, void 0), Ma(this, cv, void 0), Ma(this, hv, !1), Ma(this, dv, !1), Ma(this, fv, null), Ma(this, gv, null), Ia(hg, this, new YM("row", e, this)), Ia(dg, this, new YM("column", n, this)), Ia(cv, this, i);
  }
  /**
   * Gets index synchronizer for a particular axis.
   *
   * @param {'row'|'column'} indexType Type of indexes.
   * @returns {AxisSyncer}
   */
  getForAxis(e) {
    return e === "row" ? Po(hg, this) : Po(dg, this);
  }
  /**
   * Sets flag informing whether an undo action is already performed (we don't execute synchronization in such case).
   *
   * @param {boolean} flagValue Boolean value for the flag.
   */
  setPerformUndo(e) {
    Ia(hv, this, e);
  }
  /**
   * Sets flag informing whether a redo action is already performed (we don't execute synchronization in such case).
   *
   * @param {boolean} flagValue Boolean value for the flag.
   */
  setPerformRedo(e) {
    Ia(dv, this, e);
  }
  /**
   * Gets information whether redo or undo action is already performed (we don't execute synchronization in such case).
   *
   * @private
   * @returns {boolean}
   */
  isPerformingUndoRedo() {
    return Po(hv, this) || Po(dv, this);
  }
  /**
   * Gets HyperFormula's sheet id.
   *
   * @returns {string|null}
   */
  getSheetId() {
    return Po(gv, this);
  }
  /**
   * Gets engine instance that will be used for handled instance of Handsontable.
   *
   * @type {HyperFormula|null}
   */
  getEngine() {
    return Po(fv, this);
  }
  /**
   * Gets method which will postpone execution of some action (needed when synchronization endpoint isn't setup yet).
   *
   * @returns {Function}
   */
  getPostponeAction() {
    return Po(cv, this);
  }
  /**
   * Setups a synchronization endpoint.
   *
   * @param {HyperFormula|null} engine The HF's engine instance which will be synced.
   * @param {string|null} sheetId HyperFormula's sheet name.
   */
  setupSyncEndpoint(e, n) {
    Ia(fv, this, e), Ia(gv, this, n), Po(hg, this).init(), Po(dg, this).init();
  }
}
const Lie = kie;
function Bie(t, e) {
  p_(t, e), e.add(t);
}
function ql(t, e, n) {
  return (e = Fie(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Fie(t) {
  var e = Vie(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Vie(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function mv(t, e, n) {
  p_(t, e), e.set(t, n);
}
function p_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Qs(t, e, n) {
  return t.set(Ct(t, e), n), n;
}
function Su(t, e) {
  return t.get(Ct(t, e));
}
function Ct(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const kn = "formulas", Wie = ["maxRows", "maxColumns", "language"], $ie = 260;
he.getSingleton().register("afterNamedExpressionAdded");
he.getSingleton().register("afterNamedExpressionRemoved");
he.getSingleton().register("afterSheetAdded");
he.getSingleton().register("afterSheetRemoved");
he.getSingleton().register("afterSheetRenamed");
he.getSingleton().register("afterFormulasValuesUpdate");
const $u = (t) => t === "UndoRedo.undo" || t === "UndoRedo.redo" || t === "auto";
var es = /* @__PURE__ */ new WeakMap(), BR = /* @__PURE__ */ new WeakMap(), Wc = /* @__PURE__ */ new WeakMap(), St = /* @__PURE__ */ new WeakSet();
class Uie extends at {
  constructor() {
    var e;
    super(...arguments), e = this, Bie(this, St), mv(this, es, !1), mv(this, BR, !1), mv(this, Wc, [["valuesUpdated", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ct(St, e, ore).call(e, ...i);
    }], ["namedExpressionAdded", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ct(St, e, sre).call(e, ...i);
    }], ["namedExpressionRemoved", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ct(St, e, are).call(e, ...i);
    }], ["sheetAdded", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ct(St, e, lre).call(e, ...i);
    }], ["sheetRenamed", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ct(St, e, ure).call(e, ...i);
    }], ["sheetRemoved", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ct(St, e, cre).call(e, ...i);
    }]]), ql(this, "staticRegister", Or("formulas")), ql(this, "engine", null), ql(this, "sheetName", null), ql(this, "indexSyncer", null), ql(this, "rowAxisSyncer", null), ql(this, "columnAxisSyncer", null);
  }
  static get PLUGIN_KEY() {
    return kn;
  }
  static get PLUGIN_PRIORITY() {
    return $ie;
  }
  static get SETTING_KEYS() {
    return [kn, ...Wie];
  }
  /**
   * HyperFormula's sheet id.
   *
   * @type {number|null}
   */
  get sheetId() {
    return this.sheetName === null ? null : this.engine.getSheetId(this.sheetName);
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link Formulas#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[kn];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e, n = this;
    if (!this.enabled) {
      if (this.engine = (e = yie(this.hot)) !== null && e !== void 0 ? e : this.engine, !this.engine) {
        gt("Missing the required `engine` key in the Formulas settings. Please fill it with either an engine class or an engine instance.");
        return;
      }
      if (this.sheetName !== null && !this.engine.doesSheetExist(this.sheetName)) {
        const i = this.addSheet(this.sheetName, this.hot.getSourceDataArray());
        i !== !1 && (this.sheetName = i);
      }
      this.addHook("beforeLoadData", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, jM).call(n, ...r);
      }), this.addHook("afterLoadData", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, GM).call(n, ...r);
      }), this.addHook("beforeUpdateData", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, jM).call(n, ...r);
      }), this.addHook("afterUpdateData", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, GM).call(n, ...r);
      }), this.addHook("modifyData", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, zie).call(n, ...r);
      }), this.addHook("modifySourceData", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, Kie).call(n, ...r);
      }), this.addHook("beforeValidate", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, Yie).call(n, ...r);
      }), this.addHook("afterSetSourceDataAtCell", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, Qie).call(n, ...r);
      }), this.addHook("afterSetDataAtCell", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, zM).call(n, ...r);
      }), this.addHook("afterSetDataAtRowProp", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, zM).call(n, ...r);
      }), this.addHook("beforeCreateRow", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, Xie).call(n, ...r);
      }), this.addHook("beforeCreateCol", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, qie).call(n, ...r);
      }), this.addHook("afterCreateRow", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, ere).call(n, ...r);
      }), this.addHook("afterCreateCol", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, tre).call(n, ...r);
      }), this.addHook("beforeRemoveRow", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, Zie).call(n, ...r);
      }), this.addHook("beforeRemoveCol", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, Jie).call(n, ...r);
      }), this.addHook("afterRemoveRow", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, nre).call(n, ...r);
      }), this.addHook("afterRemoveCol", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, ire).call(n, ...r);
      }), this.indexSyncer = new Lie(this.hot.rowIndexMapper, this.hot.columnIndexMapper, (i) => {
        this.hot.addHookOnce("init", () => {
          i();
        });
      }), this.rowAxisSyncer = this.indexSyncer.getForAxis("row"), this.columnAxisSyncer = this.indexSyncer.getForAxis("column"), this.hot.addHook("afterRowSequenceChange", this.rowAxisSyncer.getIndexesChangeSyncMethod()), this.hot.addHook("afterColumnSequenceChange", this.columnAxisSyncer.getIndexesChangeSyncMethod()), this.hot.addHook("beforeRowMove", (i, r, o, s) => {
        this.rowAxisSyncer.storeMovesInformation(i, r, s);
      }), this.hot.addHook("beforeColumnMove", (i, r, o, s) => {
        this.columnAxisSyncer.storeMovesInformation(i, r, s);
      }), this.hot.addHook("afterRowMove", (i, r, o, s, a) => {
        this.rowAxisSyncer.calculateAndSyncMoves(s, a);
      }), this.hot.addHook("afterColumnMove", (i, r, o, s, a) => {
        this.columnAxisSyncer.calculateAndSyncMoves(s, a);
      }), this.hot.addHook("beforeColumnFreeze", (i, r) => {
        this.columnAxisSyncer.storeMovesInformation([i], this.hot.getSettings().fixedColumnsStart, r);
      }), this.hot.addHook("afterColumnFreeze", (i, r) => {
        this.columnAxisSyncer.calculateAndSyncMoves(r, r);
      }), this.hot.addHook("beforeColumnUnfreeze", (i, r) => {
        this.columnAxisSyncer.storeMovesInformation([i], this.hot.getSettings().fixedColumnsStart - 1, r);
      }), this.hot.addHook("afterColumnUnfreeze", (i, r) => {
        this.columnAxisSyncer.calculateAndSyncMoves(r, r);
      }), this.addHook("afterCellMetaReset", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, Gie).call(n, ...r);
      }), this.addHook("beforeUndo", () => {
        this.indexSyncer.setPerformUndo(!0), this.engine.undo();
      }), this.addHook("beforeRedo", () => {
        this.indexSyncer.setPerformRedo(!0), this.engine.redo();
      }), this.addHook("afterUndo", () => {
        this.indexSyncer.setPerformUndo(!1);
      }), this.addHook("afterUndo", () => {
        this.indexSyncer.setPerformRedo(!1);
      }), this.addHook("afterDetachChild", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, rre).call(n, ...r);
      }), this.addHook("beforeAutofill", function() {
        for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
          r[o] = arguments[o];
        return Ct(St, n, jie).call(n, ...r);
      }), Su(Wc, this).forEach((i) => {
        let [r, o] = i;
        return this.engine.on(r, o);
      }), super.enablePlugin();
    }
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    Su(Wc, this).forEach((e) => {
      let [n, i] = e;
      return this.engine.off(n, i);
    }), UM(this.engine, this.hot), this.engine = null, super.disablePlugin();
  }
  /**
   * Triggered on `updateSettings`.
   *
   * @private
   * @param {object} newSettings New set of settings passed to the `updateSettings` method.
   */
  updatePlugin(e) {
    const n = Cie(this.hot.getSettings());
    vie(this.engine.getConfig(), n) && this.engine.updateConfig(n);
    const i = this.hot.getSettings()[kn];
    if (de(i) && de(i.sheetName) && i.sheetName !== this.sheetName && this.switchSheet(i.sheetName), !e.data && this.sheetName === null) {
      const r = this.hot.getSettings()[kn].sheetName;
      r && this.engine.doesSheetExist(r) ? this.switchSheet(this.sheetName) : this.sheetName = this.addSheet(r != null ? r : void 0, this.hot.getSourceDataArray());
    }
    super.updatePlugin(e);
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    Su(Wc, this).forEach((e) => {
      var n;
      let [i, r] = e;
      return (n = this.engine) === null || n === void 0 ? void 0 : n.off(i, r);
    }), Qs(Wc, this, null), UM(this.engine, this.hot), this.engine = null, super.destroy();
  }
  /**
   * Add a sheet to the shared HyperFormula instance.
   *
   * @param {string|null} [sheetName] The new sheet name. If not provided (or a null is passed), will be
   * auto-generated by HyperFormula.
   * @param {Array} [sheetData] Data passed to the shared HyperFormula instance. Has to be declared as an array of
   * arrays - array of objects is not supported in this scenario.
   * @returns {boolean|string} `false` if the data format is unusable or it is impossible to add a new sheet to the
   * engine, the created sheet name otherwise.
   */
  addSheet(e, n) {
    if (de(n) && !r1(n))
      return gt("The provided data should be an array of arrays."), !1;
    if (e != null && this.engine.doesSheetExist(e))
      return gt("Sheet with the provided name already exists."), !1;
    try {
      const i = this.engine.addSheet(e != null ? e : void 0);
      return n && this.engine.setSheetContent(this.engine.getSheetId(i), n), i;
    } catch (i) {
      return gt(i.message), !1;
    }
  }
  /**
   * Switch the sheet used as data in the Handsontable instance (it loads the data from the shared HyperFormula
   * instance).
   *
   * @param {string} sheetName Sheet name used in the shared HyperFormula instance.
   */
  switchSheet(e) {
    if (!this.engine.doesSheetExist(e)) {
      pb(`The sheet named \`${e}\` does not exist, switch aborted.`);
      return;
    }
    this.sheetName = e;
    const n = this.engine.getSheetSerialized(this.sheetId);
    n.length > 0 && this.hot.loadData(n, `${Bn(kn)}.switchSheet`);
  }
  /**
   * Get the cell type under specified visual coordinates.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {number} [sheet] The target sheet id, defaults to the current sheet.
   * @returns {string} Possible values: 'FORMULA' | 'VALUE' | 'ARRAYFORMULA' | 'EMPTY'.
   */
  getCellType(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : this.sheetId;
    const r = this.hot.toPhysicalRow(e), o = this.hot.toPhysicalColumn(n);
    return r !== null && o !== null ? this.engine.getCellType({
      sheet: i,
      row: this.rowAxisSyncer.getHfIndexFromVisualIndex(e),
      col: this.columnAxisSyncer.getHfIndexFromVisualIndex(n)
    }) : "EMPTY";
  }
  /**
   * Returns `true` if under specified visual coordinates is formula.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {number} [sheet] The target sheet id, defaults to the current sheet.
   * @returns {boolean}
   */
  isFormulaCellType(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : this.sheetId;
    return this.engine.doesCellHaveFormula({
      sheet: i,
      row: this.rowAxisSyncer.getHfIndexFromVisualIndex(e),
      col: this.columnAxisSyncer.getHfIndexFromVisualIndex(n)
    });
  }
  /**
   * Renders dependent sheets (handsontable instances) based on the changes - list of the
   * recalculated dependent cells.
   *
   * @private
   * @param {object[]} dependentCells The values and location of applied changes within HF engine.
   * @param {boolean} [renderSelf] `true` if it's supposed to render itself, `false` otherwise.
   */
  renderDependentSheets(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    const i = /* @__PURE__ */ new Set();
    e.forEach((r) => {
      var o;
      const s = r == null || (o = r.address) === null || o === void 0 ? void 0 : o.sheet;
      s !== void 0 && (i.has(s) || i.add(s));
    }), $M(this.engine).forEach((r, o) => {
      if ((n || o !== this.sheetId) && i.has(o)) {
        var s;
        r.render(), (s = r.view) === null || s === void 0 || s.adjustElementsSize();
      }
    });
  }
  /**
   * Validates dependent cells based on the cells that are modified by the change.
   *
   * @private
   * @param {object[]} dependentCells The values and location of applied changes within HF engine.
   * @param {object[]} [changedCells] The values and location of applied changes by developer (through API or UI).
   */
  validateDependentCells(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
    const i = (o) => {
      var s;
      const {
        row: a,
        col: l,
        sheet: u
      } = (s = o == null ? void 0 : o.address) !== null && s !== void 0 ? s : {};
      return de(u) ? `${u}:${a}x${l}` : "";
    }, r = new Set(n.map((o) => i(o)));
    e.forEach((o) => {
      var s, a;
      const {
        row: l,
        col: u
      } = (s = o.address) !== null && s !== void 0 ? s : {};
      if (de(l) === !1 || de(u) === !1 || l >= this.hot.countRows() || u >= this.hot.countCols())
        return;
      const c = o == null || (a = o.address) === null || a === void 0 ? void 0 : a.sheet, h = i(o);
      if (c !== void 0 && !r.has(h)) {
        const d = $M(this.engine).get(c);
        if (!d)
          return;
        d.validateCell(d.getDataAtCell(l, u), d.getCellMeta(l, u), () => {
        });
      }
    });
  }
  /**
   * Sync a change from the change-related hooks with the engine.
   *
   * @private
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @param {Handsontable.CellValue} newValue New value.
   * @returns {Array} Array of changes exported from the engine.
   */
  syncChangeWithEngine(e, n, i) {
    const r = {
      row: this.rowAxisSyncer.getHfIndexFromVisualIndex(e),
      col: this.columnAxisSyncer.getHfIndexFromVisualIndex(n),
      sheet: this.sheetId
    };
    if (!this.engine.isItPossibleToSetCellContents(r)) {
      gt(`Not possible to set cell data at ${JSON.stringify(r)}`);
      return;
    }
    const o = this.hot.getCellMeta(e, n);
    return kR(i, o.type) && (h_(i, o.dateFormat) ? i = d_(i, o.dateFormat) : Iie(i) === !1 && (i = `'${i}`)), this.engine.setCellContents(r, i);
  }
}
function Yie(t, e, n) {
  const i = this.hot.propToCol(n);
  if (this.isFormulaCellType(e, i)) {
    const r = {
      row: this.rowAxisSyncer.getHfIndexFromVisualIndex(e),
      col: this.columnAxisSyncer.getHfIndexFromVisualIndex(i),
      sheet: this.sheetId
    }, o = this.hot.getCellMeta(e, i);
    let s = this.engine.getCellValue(r);
    return o.type === "date" && Dn(s) && (s = f_(s, o.dateFormat)), typeof s == "object" && s !== null ? s.value : s;
  }
  return t;
}
function jie(t, e, n) {
  const {
    row: i,
    col: r
  } = e.getTopStartCorner(), {
    row: o,
    col: s
  } = e.getBottomEndCorner(), {
    row: a,
    col: l
  } = n.getTopStartCorner(), {
    row: u,
    col: c
  } = n.getBottomEndCorner(), h = {
    start: {
      row: this.rowAxisSyncer.getHfIndexFromVisualIndex(i),
      col: this.columnAxisSyncer.getHfIndexFromVisualIndex(r),
      sheet: this.sheetId
    },
    end: {
      row: this.rowAxisSyncer.getHfIndexFromVisualIndex(o),
      col: this.columnAxisSyncer.getHfIndexFromVisualIndex(s),
      sheet: this.sheetId
    }
  }, d = {
    start: {
      row: this.rowAxisSyncer.getHfIndexFromVisualIndex(a),
      col: this.columnAxisSyncer.getHfIndexFromVisualIndex(l),
      sheet: this.sheetId
    },
    end: {
      row: this.rowAxisSyncer.getHfIndexFromVisualIndex(u),
      col: this.columnAxisSyncer.getHfIndexFromVisualIndex(c),
      sheet: this.sheetId
    }
  };
  if (this.engine.isItPossibleToSetCellContents(d) === !1)
    return !1;
  const g = this.engine.getFillRangeData(h, d), {
    row: m,
    col: w
  } = h.start, {
    row: C,
    col: S
  } = h.end, b = C - m + 1, R = S - w + 1;
  for (let _ = 0; _ < g.length; _ += 1)
    for (let O = 0; O < g[_].length; O += 1) {
      const $ = g[_][O], U = m + _ % b, G = w + O % R, z = this.hot.getCellMeta(U, G);
      kR($, z.type) && ($.startsWith("'") ? g[_][O] = $.slice(1) : this.isFormulaCellType(U, G, this.sheetId) === !1 && (g[_][O] = Oie($, z.dateFormat)));
    }
  return g;
}
function jM(t, e) {
  (arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "").includes(Bn(kn)) || Qs(BR, this, Mt(this.hot.getSettings().data));
}
function Gie() {
  const t = this.hot.getSourceDataArray();
  let e = !1;
  t.forEach((n, i) => {
    n.forEach((r, o) => {
      const s = this.hot.getCellMeta(i, o), a = s.dateFormat;
      kR(r, s.type) && (e = !0, h_(r, a) ? t[i][o] = d_(r, a) : this.isFormulaCellType(i, o) === !1 && (t[i][o] = `'${r}`));
    });
  }), e === !0 && (Qs(es, this, !0), this.engine.setSheetContent(this.sheetId, t), Qs(es, this, !1));
}
function GM(t, e) {
  if (!(arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "").includes(Bn(kn)))
    if (this.sheetName = Tie(this.engine, this.hot.getSettings()[kn].sheetName), Su(BR, this))
      this.switchSheet(this.sheetName);
    else {
      const i = this.hot.getSourceDataArray();
      if (this.engine.isItPossibleToReplaceSheetContent(this.sheetId, i)) {
        Qs(es, this, !0);
        const r = this.engine.setSheetContent(this.sheetId, i);
        this.indexSyncer.setupSyncEndpoint(this.engine, this.sheetId), this.renderDependentSheets(r), Qs(es, this, !1);
      }
    }
}
function zie(t, e, n, i) {
  if (i !== "get" || Su(es, this) || this.sheetName === null || !this.engine.doesSheetExist(this.sheetName))
    return;
  const r = this.hot.toVisualRow(t);
  if (r === null || e === null)
    return;
  if (!this.isFormulaCellType(r, e) && this.getCellType(r, e) !== "ARRAY") {
    c_(n.value) && (n.value = Mie(n.value));
    return;
  }
  const s = {
    row: this.rowAxisSyncer.getHfIndexFromVisualIndex(r),
    col: this.columnAxisSyncer.getHfIndexFromVisualIndex(e),
    sheet: this.sheetId
  };
  let a = this.engine.getCellValue(s), l = this.hot.getCellsMeta().find((c) => c.visualRow === r && c.visualCol === e);
  l === void 0 && (l = {}), l.type === "date" && Dn(a) && (a = f_(a, l.dateFormat));
  const u = typeof a == "object" && a !== null ? a.value : a;
  n.value = u;
}
function Kie(t, e, n, i) {
  if (i !== "get" || Su(es, this) || this.sheetName === null || !this.engine.doesSheetExist(this.sheetName))
    return;
  const r = this.hot.toVisualRow(t), o = this.hot.propToCol(e);
  if (r === null || o === null || !this.isFormulaCellType(r, o) && this.getCellType(r, o) !== "ARRAY")
    return;
  const a = this.engine.getSheetDimensions(this.engine.getSheetId(this.sheetName));
  if (a.width === 0 && a.height === 0)
    return;
  const l = {
    row: this.rowAxisSyncer.getHfIndexFromVisualIndex(r),
    col: this.columnAxisSyncer.getHfIndexFromVisualIndex(o),
    sheet: this.sheetId
  };
  n.value = this.engine.getCellSerialized(l);
}
function zM(t, e) {
  if ($u(e))
    return;
  const n = [], i = [], r = this.engine.batch(() => {
    t.forEach((o) => {
      let [s, a, , l] = o;
      const u = this.hot.propToCol(a), c = this.hot.toPhysicalRow(s), h = this.hot.toPhysicalColumn(u), d = {
        row: this.rowAxisSyncer.getHfIndexFromVisualIndex(s),
        col: this.columnAxisSyncer.getHfIndexFromVisualIndex(u),
        sheet: this.sheetId
      };
      c !== null && h !== null ? this.syncChangeWithEngine(s, u, l) : n.push([s, u, l]), i.push({
        address: d
      });
    });
  });
  n.length && this.hot.addHookOnce("afterChange", () => {
    const o = this.engine.batch(() => {
      n.forEach((s) => {
        let [a, l, u] = s;
        this.syncChangeWithEngine(a, l, u);
      });
    });
    this.renderDependentSheets(o, !0);
  }), this.renderDependentSheets(r), this.validateDependentCells(r, i);
}
function Qie(t, e) {
  if ($u(e))
    return;
  const n = [], i = [];
  t.forEach((r) => {
    let [o, s, , a] = r;
    const l = this.hot.propToCol(s);
    if (!Dn(l))
      return;
    const u = {
      row: this.rowAxisSyncer.getHfIndexFromVisualIndex(o),
      col: this.columnAxisSyncer.getHfIndexFromVisualIndex(l),
      sheet: this.sheetId
    };
    if (!this.engine.isItPossibleToSetCellContents(u)) {
      gt(`Not possible to set source cell data at ${JSON.stringify(u)}`);
      return;
    }
    i.push({
      address: u
    }), n.push(...this.engine.setCellContents(u, a));
  }), this.renderDependentSheets(n), this.validateDependentCells(n, i);
}
function Xie(t, e) {
  let n = this.rowAxisSyncer.getHfIndexFromVisualIndex(t);
  if (t >= this.hot.countRows() && (n = t), this.sheetId === null || !this.engine.doesSheetExist(this.sheetName) || !this.engine.isItPossibleToAddRows(this.sheetId, [n, e]))
    return !1;
}
function qie(t, e) {
  let n = this.columnAxisSyncer.getHfIndexFromVisualIndex(t);
  if (t >= this.hot.countCols() && (n = t), this.sheetId === null || !this.engine.doesSheetExist(this.sheetName) || !this.engine.isItPossibleToAddColumns(this.sheetId, [n, e]))
    return !1;
}
function Zie(t, e, n) {
  return this.rowAxisSyncer.setRemovedHfIndexes(n).every((o) => this.engine.isItPossibleToRemoveRows(this.sheetId, [o, 1])) === !1 ? !1 : void 0;
}
function Jie(t, e, n) {
  return this.columnAxisSyncer.setRemovedHfIndexes(n).every((o) => this.engine.isItPossibleToRemoveColumns(this.sheetId, [o, 1])) === !1 ? !1 : void 0;
}
function ere(t, e, n) {
  if ($u(n))
    return;
  const i = this.engine.addRows(this.sheetId, [this.rowAxisSyncer.getHfIndexFromVisualIndex(t), e]);
  this.renderDependentSheets(i);
}
function tre(t, e, n) {
  if ($u(n))
    return;
  const i = this.engine.addColumns(this.sheetId, [this.columnAxisSyncer.getHfIndexFromVisualIndex(t), e]);
  this.renderDependentSheets(i);
}
function nre(t, e, n, i) {
  if ($u(i))
    return;
  const r = this.rowAxisSyncer.getRemovedHfIndexes().sort((s, a) => a - s), o = this.engine.batch(() => {
    r.forEach((s) => {
      this.engine.removeRows(this.sheetId, [s, 1]);
    });
  });
  this.renderDependentSheets(o);
}
function ire(t, e, n, i) {
  if ($u(i))
    return;
  const r = this.columnAxisSyncer.getRemovedHfIndexes().sort((s, a) => a - s), o = this.engine.batch(() => {
    r.forEach((s) => {
      this.engine.removeColumns(this.sheetId, [s, 1]);
    });
  });
  this.renderDependentSheets(o);
}
function rre(t, e, n) {
  var i;
  Qs(es, this, !0);
  const r = this.hot.getSourceDataArray(n, 0, n + (((i = e.__children) === null || i === void 0 ? void 0 : i.length) || 0), this.hot.countSourceCols());
  Qs(es, this, !1), r.forEach((o, s) => {
    o.forEach((a, l) => {
      this.engine.setCellContents({
        col: l,
        row: n + s,
        sheet: this.sheetId
      }, [[a]]);
    });
  });
}
function ore(t) {
  this.hot.runHooks("afterFormulasValuesUpdate", t);
}
function sre(t, e) {
  this.hot.runHooks("afterNamedExpressionAdded", t, e);
}
function are(t, e) {
  this.hot.runHooks("afterNamedExpressionRemoved", t, e);
}
function lre(t) {
  this.hot.runHooks("afterSheetAdded", t);
}
function ure(t, e) {
  this.sheetName = e, this.hot.runHooks("afterSheetRenamed", t, e);
}
function cre(t, e) {
  this.hot.runHooks("afterSheetRemoved", t, e);
}
function hre(t) {
  return {
    key: "hidden_columns_hide",
    name() {
      const e = this.getSelectedLast();
      let n = 0;
      if (Array.isArray(e)) {
        const [, i, , r] = e;
        i - r !== 0 && (n = 1);
      }
      return this.getTranslatedPhrase(mw, n);
    },
    callback() {
      const {
        from: e,
        to: n
      } = this.getSelectedRangeLast(), i = Math.max(Math.min(e.col, n.col), 0), r = Math.max(e.col, n.col), o = [];
      for (let l = i; l <= r; l += 1)
        o.push(l);
      t.hideColumns(o);
      const s = o[o.length - 1], a = this.columnIndexMapper.getNearestNotHiddenIndex(s, 1, !0);
      Number.isInteger(a) && a >= 0 ? this.selectColumns(a) : this.deselectCell(), this.render(), this.view.adjustElementsSize();
    },
    disabled: !1,
    hidden() {
      return !(this.selection.isSelectedByColumnHeader() || this.selection.isSelectedByCorner());
    }
  };
}
function dre(t) {
  const e = [];
  return {
    key: "hidden_columns_show",
    name() {
      const n = e.length > 1 ? 1 : 0;
      return this.getTranslatedPhrase(pw, n);
    },
    callback() {
      var n, i;
      if (e.length === 0)
        return;
      let r = e[0], o = e[e.length - 1];
      r = (n = this.columnIndexMapper.getNearestNotHiddenIndex(r - 1, -1)) !== null && n !== void 0 ? n : 0, o = (i = this.columnIndexMapper.getNearestNotHiddenIndex(o + 1, 1)) !== null && i !== void 0 ? i : this.countCols() - 1, t.showColumns(e), this.render(), this.view.adjustElementsSize(), o - r + 1 === this.countCols() || this.selectColumns(r, o);
    },
    disabled: !1,
    hidden() {
      const n = Ke(t.getHiddenColumns(), (h) => this.toPhysicalColumn(h));
      if (!(this.selection.isSelectedByColumnHeader() || this.selection.isSelectedByCorner()) || n.length < 1)
        return !0;
      e.length = 0;
      const i = this.getSelectedRangeLast(), r = i.getTopStartCorner().col, o = i.getBottomEndCorner().col, s = this.columnIndexMapper, a = s.getRenderableFromVisualIndex(r), l = s.getRenderableFromVisualIndex(o), u = s.getNotTrimmedIndexes(), c = [];
      if (r !== o) {
        const h = o - r + 1, d = l - a + 1;
        if (h > d) {
          const g = u.slice(r, o + 1);
          c.push(...g.filter((m) => n.includes(m)));
        }
      } else if (a === 0 && a < r)
        c.push(...u.slice(0, r));
      else if (a === null)
        c.push(...u.slice(0, this.countCols()));
      else {
        const h = this.countCols() - 1, d = s.getRenderableFromVisualIndex(s.getNearestNotHiddenIndex(h, -1));
        l === d && h > o && c.push(...u.slice(o + 1));
      }
      return T(c, (h) => {
        e.push(this.toVisualColumn(h));
      }), e.length === 0;
    }
  };
}
function fre(t, e) {
  w_(t, e), e.add(t);
}
function KM(t, e, n) {
  w_(t, e), e.set(t, n);
}
function w_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Ri(t, e) {
  return t.get(Rs(t, e));
}
function $c(t, e, n) {
  return t.set(Rs(t, e), n), n;
}
function Rs(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("beforeHideColumns");
he.getSingleton().register("afterHideColumns");
he.getSingleton().register("beforeUnhideColumns");
he.getSingleton().register("afterUnhideColumns");
const pv = "hiddenColumns", gre = 310;
var so = /* @__PURE__ */ new WeakMap(), Vr = /* @__PURE__ */ new WeakMap(), Oa = /* @__PURE__ */ new WeakSet();
class mre extends at {
  constructor() {
    super(...arguments), fre(this, Oa), KM(this, so, {}), KM(this, Vr, null);
  }
  static get PLUGIN_KEY() {
    return pv;
  }
  static get PLUGIN_PRIORITY() {
    return gre;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link HiddenColumns#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[pv];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    if (this.enabled)
      return;
    const n = this.hot.getSettings()[pv];
    Be(n) && ($c(so, this, n), Mt(n.copyPasteEnabled) && (n.copyPasteEnabled = !0)), $c(Vr, this, new kp()), Ri(Vr, this).addLocalHook("init", () => Rs(Oa, this, yre).call(this)), this.hot.columnIndexMapper.registerMap(this.pluginName, Ri(Vr, this)), this.addHook("afterContextMenuDefaultOptions", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return Rs(Oa, e, vre).call(e, ...r);
    }), this.addHook("afterGetCellMeta", (i, r, o) => Rs(Oa, this, wre).call(this, i, r, o)), this.addHook("modifyColWidth", (i, r) => Rs(Oa, this, pre).call(this, i, r), 2), this.addHook("afterGetColHeader", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return Rs(Oa, e, Cre).call(e, ...r);
    }), this.addHook("modifyCopyableRange", (i) => Rs(Oa, this, Are).call(this, i)), super.enablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`hiddenColumns`](@/api/options.md#hiddencolumns)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.hot.columnIndexMapper.unregisterMap(this.pluginName), $c(so, this, {}), super.disablePlugin(), this.resetCellsMeta();
  }
  /**
   * Shows the provided columns.
   *
   * @param {number[]} columns Array of visual column indexes.
   */
  showColumns(e) {
    const n = this.getHiddenColumns(), i = this.isValidConfig(e);
    let r = n;
    const o = Ri(Vr, this).getValues().slice(), s = e.length > 0;
    if (i && s) {
      const l = e.map((u) => this.hot.toPhysicalColumn(u));
      T(l, (u) => {
        o[u] = !1;
      }), r = rn(o, (u, c, h) => (c && u.push(this.hot.toVisualColumn(h)), u), []);
    }
    this.hot.runHooks("beforeUnhideColumns", n, r, i && s) !== !1 && (i && s && Ri(Vr, this).setValues(o), this.hot.view.adjustElementsSize(), this.hot.runHooks("afterUnhideColumns", n, r, i && s, i && r.length < n.length));
  }
  /**
   * Shows a single column.
   *
   * @param {...number} column Visual column index.
   */
  showColumn() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    this.showColumns(n);
  }
  /**
   * Hides the columns provided in the array.
   *
   * @param {number[]} columns Array of visual column indexes.
   */
  hideColumns(e) {
    const n = this.getHiddenColumns(), i = this.isValidConfig(e);
    let r = n;
    i && (r = Array.from(new Set(n.concat(e)))), this.hot.runHooks("beforeHideColumns", n, r, i) !== !1 && (i && this.hot.batchExecution(() => {
      T(e, (s) => {
        Ri(Vr, this).setValueAtIndex(this.hot.toPhysicalColumn(s), !0);
      });
    }, !0), this.hot.runHooks("afterHideColumns", n, r, i, i && r.length > n.length));
  }
  /**
   * Hides a single column.
   *
   * @param {...number} column Visual column index.
   */
  hideColumn() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    this.hideColumns(n);
  }
  /**
   * Returns an array of visual indexes of hidden columns.
   *
   * @returns {number[]}
   */
  getHiddenColumns() {
    return Ke(Ri(Vr, this).getHiddenIndexes(), (e) => this.hot.toVisualColumn(e));
  }
  /**
   * Checks if the provided column is hidden.
   *
   * @param {number} column Visual column index.
   * @returns {boolean}
   */
  isHidden(e) {
    return Ri(Vr, this).getValueAtIndex(this.hot.toPhysicalColumn(e)) || !1;
  }
  /**
   * Get if trim config is valid. Check whether all of the provided column indexes are within the bounds of the table.
   *
   * @param {Array} hiddenColumns List of hidden column indexes.
   * @returns {boolean}
   */
  isValidConfig(e) {
    const n = this.hot.countCols();
    return Array.isArray(e) && e.length > 0 ? e.every((i) => Number.isInteger(i) && i >= 0 && i < n) : !1;
  }
  /**
   * Reset all rendered cells meta.
   *
   * @private
   */
  resetCellsMeta() {
    T(this.hot.getCellsMeta(), (e) => {
      e && (e.skipColumnOnPaste = !1);
    });
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    $c(so, this, null), $c(Vr, this, null), super.destroy();
  }
}
function pre(t, e) {
  if (this.isHidden(e))
    return 0;
  if (Ri(so, this).indicators && (this.isHidden(e + 1) || this.isHidden(e - 1)) && typeof t == "number" && this.hot.hasColHeaders())
    return t + 15;
}
function wre(t, e, n) {
  if (Ri(so, this).copyPasteEnabled === !1 && this.isHidden(e) && (n.skipColumnOnPaste = !0), this.isHidden(e - 1))
    n.className = n.className || "", n.className.indexOf("afterHiddenColumn") === -1 && (n.className += " afterHiddenColumn");
  else if (n.className) {
    const i = n.className.split(" ");
    if (i.length > 0) {
      const r = i.indexOf("afterHiddenColumn");
      r > -1 && i.splice(r, 1), n.className = i.join(" ");
    }
  }
}
function Are(t) {
  if (Ri(so, this).copyPasteEnabled)
    return t;
  const e = [], n = (i, r, o, s) => {
    e.push({
      startRow: i,
      endRow: r,
      startCol: o,
      endCol: s
    });
  };
  return T(t, (i) => {
    let r = !0, o = 0;
    Ae(i.startCol, i.endCol, (s) => {
      this.isHidden(s) ? (r || n(i.startRow, i.endRow, o, s - 1), r = !0) : (r && (o = s), s === i.endCol && n(i.startRow, i.endRow, o, s), r = !1);
    });
  }), e;
}
function Cre(t, e) {
  if (!Ri(so, this).indicators || t < 0)
    return;
  const n = [];
  t >= 1 && this.isHidden(t - 1) && n.push("afterHiddenColumn"), t < this.hot.countCols() - 1 && this.isHidden(t + 1) && n.push("beforeHiddenColumn"), Q(e, n);
}
function vre(t) {
  t.items.push({
    name: $e
  }, hre(this), dre(this));
}
function yre() {
  Array.isArray(Ri(so, this).columns) && this.hideColumns(Ri(so, this).columns);
}
function Sre(t) {
  return {
    key: "hidden_rows_hide",
    name() {
      const e = this.getSelectedLast();
      let n = 0;
      if (Array.isArray(e)) {
        const [i, , r] = e;
        i - r !== 0 && (n = 1);
      }
      return this.getTranslatedPhrase(ww, n);
    },
    callback() {
      const {
        from: e,
        to: n
      } = this.getSelectedRangeLast(), i = Math.max(Math.min(e.row, n.row), 0), r = Math.max(e.row, n.row), o = [];
      for (let l = i; l <= r; l += 1)
        o.push(l);
      t.hideRows(o);
      const s = o[o.length - 1], a = this.rowIndexMapper.getNearestNotHiddenIndex(s, 1, !0);
      Number.isInteger(a) && a >= 0 ? this.selectRows(a) : this.deselectCell(), this.render(), this.view.adjustElementsSize();
    },
    disabled: !1,
    hidden() {
      return !(this.selection.isSelectedByRowHeader() || this.selection.isSelectedByCorner());
    }
  };
}
function bre(t) {
  const e = [];
  return {
    key: "hidden_rows_show",
    name() {
      const n = e.length > 1 ? 1 : 0;
      return this.getTranslatedPhrase(Aw, n);
    },
    callback() {
      var n, i;
      if (e.length === 0)
        return;
      let r = e[0], o = e[e.length - 1];
      r = (n = this.rowIndexMapper.getNearestNotHiddenIndex(r - 1, -1)) !== null && n !== void 0 ? n : 0, o = (i = this.rowIndexMapper.getNearestNotHiddenIndex(o + 1, 1)) !== null && i !== void 0 ? i : this.countRows() - 1, t.showRows(e), this.render(), this.view.adjustElementsSize(), o - r + 1 === this.countRows() || this.selectRows(r, o);
    },
    disabled: !1,
    hidden() {
      const n = Ke(t.getHiddenRows(), (h) => this.toPhysicalRow(h));
      if (!(this.selection.isSelectedByRowHeader() || this.selection.isSelectedByCorner()) || n.length < 1)
        return !0;
      e.length = 0;
      const i = this.getSelectedRangeLast(), r = i.getTopStartCorner().row, o = i.getBottomEndCorner().row, s = this.rowIndexMapper, a = s.getRenderableFromVisualIndex(r), l = s.getRenderableFromVisualIndex(o), u = s.getNotTrimmedIndexes(), c = [];
      if (r !== o) {
        const h = o - r + 1, d = l - a + 1;
        if (h > d) {
          const g = u.slice(r, o + 1);
          c.push(...g.filter((m) => n.includes(m)));
        }
      } else if (a === 0 && a < r)
        c.push(...u.slice(0, r));
      else if (a === null)
        c.push(...u.slice(0, this.countRows()));
      else {
        const h = this.countRows() - 1, d = s.getRenderableFromVisualIndex(s.getNearestNotHiddenIndex(h, -1));
        l === d && h > o && c.push(...u.slice(o + 1));
      }
      return T(c, (h) => {
        e.push(this.toVisualRow(h));
      }), e.length === 0;
    }
  };
}
function Rre(t, e) {
  A_(t, e), e.add(t);
}
function QM(t, e, n) {
  A_(t, e), e.set(t, n);
}
function A_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Wi(t, e) {
  return t.get(Es(t, e));
}
function Uc(t, e, n) {
  return t.set(Es(t, e), n), n;
}
function Es(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("beforeHideRows");
he.getSingleton().register("afterHideRows");
he.getSingleton().register("beforeUnhideRows");
he.getSingleton().register("afterUnhideRows");
const wv = "hiddenRows", Ere = 320;
var Go = /* @__PURE__ */ new WeakMap(), Wr = /* @__PURE__ */ new WeakMap(), Pa = /* @__PURE__ */ new WeakSet();
class Tre extends at {
  constructor() {
    super(...arguments), Rre(this, Pa), QM(this, Go, {}), QM(this, Wr, null);
  }
  static get PLUGIN_KEY() {
    return wv;
  }
  static get PLUGIN_PRIORITY() {
    return Ere;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link HiddenRows#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[wv];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    if (this.enabled)
      return;
    const n = this.hot.getSettings()[wv];
    Be(n) && (Uc(Go, this, n), Mt(n.copyPasteEnabled) && (n.copyPasteEnabled = !0)), Uc(Wr, this, new kp()), Wi(Wr, this).addLocalHook("init", () => Es(Pa, this, Hre).call(this)), this.hot.rowIndexMapper.registerMap(this.pluginName, Wi(Wr, this)), this.addHook("afterContextMenuDefaultOptions", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return Es(Pa, e, xre).call(e, ...r);
    }), this.addHook("afterGetCellMeta", (i, r, o) => Es(Pa, this, Ire).call(this, i, r, o)), this.addHook("modifyRowHeight", (i, r) => Es(Pa, this, Mre).call(this, i, r)), this.addHook("afterGetRowHeader", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return Es(Pa, e, Pre).call(e, ...r);
    }), this.addHook("modifyCopyableRange", (i) => Es(Pa, this, Ore).call(this, i)), super.enablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`hiddenRows`](@/api/options.md#hiddenrows)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.hot.rowIndexMapper.unregisterMap(this.pluginName), Uc(Go, this, {}), super.disablePlugin(), this.resetCellsMeta();
  }
  /**
   * Shows the rows provided in the array.
   *
   * @param {number[]} rows Array of visual row indexes.
   */
  showRows(e) {
    const n = this.getHiddenRows(), i = this.isValidConfig(e);
    let r = n;
    const o = Wi(Wr, this).getValues().slice(), s = e.length > 0;
    if (i && s) {
      const l = e.map((u) => this.hot.toPhysicalRow(u));
      T(l, (u) => {
        o[u] = !1;
      }), r = rn(o, (u, c, h) => (c && u.push(this.hot.toVisualRow(h)), u), []);
    }
    this.hot.runHooks("beforeUnhideRows", n, r, i && s) !== !1 && (i && s && Wi(Wr, this).setValues(o), this.hot.runHooks("afterUnhideRows", n, r, i && s, i && r.length < n.length));
  }
  /**
   * Shows the row provided as row index (counting from 0).
   *
   * @param {...number} row Visual row index.
   */
  showRow() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    this.showRows(n);
  }
  /**
   * Hides the rows provided in the array.
   *
   * @param {number[]} rows Array of visual row indexes.
   */
  hideRows(e) {
    const n = this.getHiddenRows(), i = this.isValidConfig(e);
    let r = n;
    i && (r = Array.from(new Set(n.concat(e)))), this.hot.runHooks("beforeHideRows", n, r, i) !== !1 && (i && this.hot.batchExecution(() => {
      T(e, (s) => {
        Wi(Wr, this).setValueAtIndex(this.hot.toPhysicalRow(s), !0);
      });
    }, !0), this.hot.runHooks("afterHideRows", n, r, i, i && r.length > n.length));
  }
  /**
   * Hides the row provided as row index (counting from 0).
   *
   * @param {...number} row Visual row index.
   */
  hideRow() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    this.hideRows(n);
  }
  /**
   * Returns an array of visual indexes of hidden rows.
   *
   * @returns {number[]}
   */
  getHiddenRows() {
    return Ke(Wi(Wr, this).getHiddenIndexes(), (e) => this.hot.toVisualRow(e));
  }
  /**
   * Checks if the provided row is hidden.
   *
   * @param {number} row Visual row index.
   * @returns {boolean}
   */
  isHidden(e) {
    return Wi(Wr, this).getValueAtIndex(this.hot.toPhysicalRow(e)) || !1;
  }
  /**
   * Checks whether all of the provided row indexes are within the bounds of the table.
   *
   * @param {Array} hiddenRows List of hidden visual row indexes.
   * @returns {boolean}
   */
  isValidConfig(e) {
    const n = this.hot.countRows();
    return Array.isArray(e) && e.length > 0 ? e.every((i) => Number.isInteger(i) && i >= 0 && i < n) : !1;
  }
  /**
   * Resets all rendered cells meta.
   *
   * @private
   */
  resetCellsMeta() {
    T(this.hot.getCellsMeta(), (e) => {
      e && (e.skipRowOnPaste = !1);
    });
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    Uc(Go, this, null), Uc(Wr, this, null), super.destroy();
  }
}
function Mre(t, e) {
  return this.isHidden(e) ? 0 : t;
}
function Ire(t, e, n) {
  if (Wi(Go, this).copyPasteEnabled === !1 && this.isHidden(t) && (n.skipRowOnPaste = !0), this.isHidden(t - 1))
    n.className = n.className || "", n.className.indexOf("afterHiddenRow") === -1 && (n.className += " afterHiddenRow");
  else if (n.className) {
    const i = n.className.split(" ");
    if (i.length > 0) {
      const r = i.indexOf("afterHiddenRow");
      r > -1 && i.splice(r, 1), n.className = i.join(" ");
    }
  }
}
function Ore(t) {
  if (Wi(Go, this).copyPasteEnabled)
    return t;
  const e = [], n = (i, r, o, s) => {
    e.push({
      startRow: i,
      endRow: r,
      startCol: o,
      endCol: s
    });
  };
  return T(t, (i) => {
    let r = !0, o = 0;
    Ae(i.startRow, i.endRow, (s) => {
      this.isHidden(s) ? (r || n(o, s - 1, i.startCol, i.endCol), r = !0) : (r && (o = s), s === i.endRow && n(o, s, i.startCol, i.endCol), r = !1);
    });
  }), e;
}
function Pre(t, e) {
  if (!Wi(Go, this).indicators || t < 0)
    return;
  const n = [];
  t >= 1 && this.isHidden(t - 1) && n.push("afterHiddenRow"), t < this.hot.countRows() - 1 && this.isHidden(t + 1) && n.push("beforeHiddenRow"), Q(e, n);
}
function xre(t) {
  t.items.push({
    name: $e
  }, Sre(this), bre(this));
}
function Hre() {
  Array.isArray(Wi(Go, this).rows) && this.hideRows(Wi(Go, this).rows);
}
function _re(t) {
  return {
    key: "freeze_column",
    name() {
      return this.getTranslatedPhrase(Zp);
    },
    callback(e, n) {
      const [{
        start: {
          col: i
        }
      }] = n;
      t.freezeColumn(i), this.render(), this.view.adjustElementsSize();
    },
    hidden() {
      const e = this.getSelectedRange();
      let n = !1;
      return (e === void 0 || e.length > 1 || e[0].from.col !== e[0].to.col || e[0].from.col <= this.getSettings().fixedColumnsStart - 1) && (n = !0), n;
    }
  };
}
function Nre(t) {
  return {
    key: "unfreeze_column",
    name() {
      return this.getTranslatedPhrase(Jp);
    },
    callback(e, n) {
      const [{
        start: {
          col: i
        }
      }] = n;
      t.unfreezeColumn(i), this.render(), this.view.adjustElementsSize();
    },
    hidden() {
      const e = this.getSelectedRange();
      let n = !1;
      return (e === void 0 || e.length > 1 || e[0].from.col !== e[0].to.col || e[0].from.col >= this.getSettings().fixedColumnsStart) && (n = !0), n;
    }
  };
}
function Dre(t, e) {
  C_(t, e), e.add(t);
}
function kre(t, e, n) {
  C_(t, e), e.set(t, n);
}
function C_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function tS(t, e) {
  return t.get(Bm(t, e));
}
function Av(t, e, n) {
  return t.set(Bm(t, e), n), n;
}
function Bm(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("beforeColumnFreeze");
he.getSingleton().register("afterColumnFreeze");
he.getSingleton().register("beforeColumnUnfreeze");
he.getSingleton().register("afterColumnUnfreeze");
const XM = "manualColumnFreeze", Lre = 110;
var Wa = /* @__PURE__ */ new WeakMap(), Cv = /* @__PURE__ */ new WeakSet();
class Bre extends at {
  constructor() {
    super(...arguments), Dre(this, Cv), kre(this, Wa, !1);
  }
  static get PLUGIN_KEY() {
    return XM;
  }
  static get PLUGIN_PRIORITY() {
    return Lre;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ManualColumnFreeze#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[XM];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled || (this.addHook("afterContextMenuDefaultOptions", (e) => Bm(Cv, this, Fre).call(this, e)), this.addHook("beforeColumnMove", (e, n) => Bm(Cv, this, Vre).call(this, e, n)), super.enablePlugin());
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    Av(Wa, this, !1), super.disablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`manualColumnFreeze`](@/api/options.md#manualcolumnfreeze)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Freezes the specified column (adds it to fixed columns).
   *
   * `freezeColumn()` doesn't re-render the table,
   * so you need to call the `render()` method afterward.
   *
   * @param {number} column Visual column index.
   */
  freezeColumn(e) {
    const n = this.hot.getSettings(), i = n.fixedColumnsStart < this.hot.countCols() && e > n.fixedColumnsStart - 1;
    tS(Wa, this) || Av(Wa, this, !0), this.hot.runHooks("beforeColumnFreeze", e, i) !== !1 && (i && (this.hot.columnIndexMapper.moveIndexes(e, n.fixedColumnsStart), n._fixedColumnsStart += 1), this.hot.runHooks("afterColumnFreeze", e, i));
  }
  /**
   * Unfreezes the given column (remove it from fixed columns and bring to it's previous position).
   *
   * @param {number} column Visual column index.
   */
  unfreezeColumn(e) {
    const n = this.hot.getSettings(), i = n.fixedColumnsStart > 0 && e <= n.fixedColumnsStart - 1;
    tS(Wa, this) || Av(Wa, this, !0), this.hot.runHooks("beforeColumnUnfreeze", e, i) !== !1 && (i && (n._fixedColumnsStart -= 1, this.hot.columnIndexMapper.moveIndexes(e, n.fixedColumnsStart)), this.hot.runHooks("afterColumnUnfreeze", e, i));
  }
}
function Fre(t) {
  t.items.push({
    name: "---------"
  }, _re(this), Nre(this));
}
function Vre(t, e) {
  if (tS(Wa, this)) {
    const n = this.hot.getSettings().fixedColumnsStart;
    if (e < n || t.some((i) => i < n))
      return !1;
  }
}
function fg(t, e, n) {
  return (e = Wre(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Wre(t) {
  var e = $re(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function $re(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const vv = 0, qM = 1, ZM = 2, Zl = "px";
let Ure = class {
  constructor(e) {
    fg(this, "hot", void 0), fg(this, "_element", null), fg(this, "state", vv), fg(this, "inlineProperty", void 0), this.hot = e, this.inlineProperty = e.isRtl() ? "right" : "left";
  }
  /**
   * Add created UI elements to table.
   *
   * @param {HTMLElement} wrapper Element which are parent for our UI element.
   */
  appendTo(e) {
    e.appendChild(this._element), this.state = ZM;
  }
  /**
   * Method for create UI element. Only create, without append to table.
   */
  build() {
    this.state === vv && (this._element = this.hot.rootDocument.createElement("div"), this.state = qM);
  }
  /**
   * Method for remove UI element.
   */
  destroy() {
    this.isAppended() && this._element.parentElement.removeChild(this._element), this._element = null, this.state = vv;
  }
  /**
   * Check if UI element are appended.
   *
   * @returns {boolean}
   */
  isAppended() {
    return this.state === ZM;
  }
  /**
   * Check if UI element are built.
   *
   * @returns {boolean}
   */
  isBuilt() {
    return this.state >= qM;
  }
  /**
   * Setter for position.
   *
   * @param {number} top New top position of the element.
   * @param {number} inlinePosition New left/right (depends on LTR/RTL document mode) position of the element.
   */
  setPosition(e, n) {
    Dn(e) && (this._element.style.top = e + Zl), Dn(n) && (this._element.style[this.inlineProperty] = n + Zl);
  }
  /**
   * Getter for the element position.
   *
   * @returns {object} Object contains left and top position of the element.
   */
  getPosition() {
    const e = this._element.style;
    return {
      top: e.top ? parseInt(e.top, 10) : 0,
      start: e[this.inlineProperty] ? parseInt(e[this.inlineProperty], 10) : 0
    };
  }
  /**
   * Setter for the element size.
   *
   * @param {number} width New width of the element.
   * @param {number} height New height of the element.
   */
  setSize(e, n) {
    Dn(e) && (this._element.style.width = e + Zl), Dn(n) && (this._element.style.height = n + Zl);
  }
  /**
   * Getter for the element position.
   *
   * @returns {object} Object contains height and width of the element.
   */
  getSize() {
    return {
      width: this._element.style.width ? parseInt(this._element.style.width, 10) : 0,
      height: this._element.style.height ? parseInt(this._element.style.height, 10) : 0
    };
  }
  /**
   * Setter for the element offset. Offset means marginTop and marginLeft of the element.
   *
   * @param {number} top New margin top of the element.
   * @param {number} inlineOffset New margin left/right (depends on LTR/RTL document mode) of the element.
   */
  setOffset(e, n) {
    Dn(e) && (this._element.style.marginTop = e + Zl), Dn(n) && (this._element.style[`margin${Bn(this.inlineProperty)}`] = n + Zl);
  }
  /**
   * Getter for the element offset.
   *
   * @returns {object} Object contains top and left offset of the element.
   */
  getOffset() {
    const e = this._element.style, n = `margin${Bn(this.inlineProperty)}`;
    return {
      top: e.marginTop ? parseInt(e.marginTop, 10) : 0,
      start: e[n] ? parseInt(e[n], 10) : 0
    };
  }
};
const v_ = Ure, Yre = "ht__manualColumnMove--backlight";
let jre = class extends v_ {
  /**
   * Custom className on build process.
   */
  build() {
    super.build(), Q(this._element, Yre);
  }
};
const Gre = jre, zre = "ht__manualColumnMove--guideline";
let Kre = class extends v_ {
  /**
   * Custom className on build process.
   */
  build() {
    super.build(), Q(this._element, zre);
  }
};
const Qre = Kre;
function Xre(t, e) {
  y_(t, e), e.add(t);
}
function lr(t, e, n) {
  y_(t, e), e.set(t, n);
}
function y_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function hi(t, e, n) {
  return t.set(Ts(t, e), n), n;
}
function ge(t, e) {
  return t.get(Ts(t, e));
}
function Ts(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("beforeColumnMove");
he.getSingleton().register("afterColumnMove");
const yv = "manualColumnMove", qre = 120, JM = "ht__manualColumnMove", Fm = "show-ui", nS = "on-moving--columns", iS = "after-selection--columns";
var Kn = /* @__PURE__ */ new WeakMap(), Ps = /* @__PURE__ */ new WeakMap(), zo = /* @__PURE__ */ new WeakMap(), nm = /* @__PURE__ */ new WeakMap(), Ls = /* @__PURE__ */ new WeakMap(), zn = /* @__PURE__ */ new WeakMap(), gg = /* @__PURE__ */ new WeakMap(), ki = /* @__PURE__ */ new WeakMap(), lu = /* @__PURE__ */ new WeakMap(), Vm = /* @__PURE__ */ new WeakMap(), Ah = /* @__PURE__ */ new WeakMap(), xa = /* @__PURE__ */ new WeakSet();
class Zre extends at {
  constructor() {
    super(...arguments), Xre(this, xa), lr(this, Kn, new Gre(this.hot)), lr(this, Ps, new Qre(this.hot)), lr(this, zo, []), lr(this, nm, 0), lr(this, Ls, !1), lr(this, zn, {}), lr(this, gg, void 0), lr(this, ki, void 0), lr(this, lu, void 0), lr(this, Vm, void 0), lr(this, Ah, void 0);
  }
  static get PLUGIN_KEY() {
    return yv;
  }
  static get PLUGIN_PRIORITY() {
    return qre;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ManualColumnMove#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[yv];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    this.enabled || (this.addHook("beforeOnCellMouseDown", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ts(xa, e, Jre).call(e, ...i);
    }), this.addHook("beforeOnCellMouseOver", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ts(xa, e, toe).call(e, ...i);
    }), this.addHook("afterScrollVertically", () => Ts(xa, this, ioe).call(this)), this.addHook("afterLoadData", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ts(xa, e, roe).call(e, ...i);
    }), this.buildPluginUI(), this.registerEvents(), Q(this.hot.rootElement, JM), super.enablePlugin());
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`manualColumnMove`](@/api/options.md#manualcolumnmove)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), this.moveBySettingsOrLoad(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    xe(this.hot.rootElement, JM), this.unregisterEvents(), ge(Kn, this).destroy(), ge(Ps, this).destroy(), super.disablePlugin();
  }
  /**
   * Moves a single column.
   *
   * @param {number} column Visual column index to be moved.
   * @param {number} finalIndex Visual column index, being a start index for the moved columns. Points to where the elements will be placed after the moving action.
   * To check the visualization of the final index, please take a look at [documentation](@/guides/columns/column-moving/column-moving.md#drag-and-move-actions-of-manualcolumnmove-plugin).
   * @fires Hooks#beforeColumnMove
   * @fires Hooks#afterColumnMove
   * @returns {boolean}
   */
  moveColumn(e, n) {
    return this.moveColumns([e], n);
  }
  /**
   * Moves a multiple columns.
   *
   * @param {Array} columns Array of visual column indexes to be moved.
   * @param {number} finalIndex Visual column index, being a start index for the moved columns. Points to where the elements will be placed after the moving action.
   * To check the visualization of the final index, please take a look at [documentation](@/guides/columns/column-moving/column-moving.md#drag-and-move-actions-of-manualcolumnmove-plugin).
   * @fires Hooks#beforeColumnMove
   * @fires Hooks#afterColumnMove
   * @returns {boolean}
   */
  moveColumns(e, n) {
    const i = ge(gg, this), r = this.isMovePossible(e, n), o = this.hot.runHooks("beforeColumnMove", e, n, i, r);
    if (hi(gg, this, void 0), o === !1)
      return;
    r && this.hot.columnIndexMapper.moveIndexes(e, n);
    const s = r && this.isColumnOrderChanged(e, n);
    return this.hot.runHooks("afterColumnMove", e, n, i, r, s), s;
  }
  /**
   * Drag a single column to drop index position.
   *
   * @param {number} column Visual column index to be dragged.
   * @param {number} dropIndex Visual column index, being a drop index for the moved columns. Points to where we are going to drop the moved elements.
   * To check visualization of drop index please take a look at [documentation](@/guides/columns/column-moving/column-moving.md#drag-and-move-actions-of-manualcolumnmove-plugin).
   * @fires Hooks#beforeColumnMove
   * @fires Hooks#afterColumnMove
   * @returns {boolean}
   */
  dragColumn(e, n) {
    return this.dragColumns([e], n);
  }
  /**
   * Drag multiple columns to drop index position.
   *
   * @param {Array} columns Array of visual column indexes to be dragged.
   * @param {number} dropIndex Visual column index, being a drop index for the moved columns. Points to where we are going to drop the moved elements.
   * To check visualization of drop index please take a look at [documentation](@/guides/columns/column-moving/column-moving.md#drag-and-move-actions-of-manualcolumnmove-plugin).
   * @fires Hooks#beforeColumnMove
   * @fires Hooks#afterColumnMove
   * @returns {boolean}
   */
  dragColumns(e, n) {
    const i = this.countFinalIndex(e, n);
    return hi(gg, this, n), this.moveColumns(e, i);
  }
  /**
   * Indicates if it's possible to move columns to the desired position. Some of the actions aren't
   * possible, i.e. You can’t move more than one element to the last position.
   *
   * @param {Array} movedColumns Array of visual column indexes to be moved.
   * @param {number} finalIndex Visual column index, being a start index for the moved columns. Points to where the elements will be placed after the moving action.
   * To check the visualization of the final index, please take a look at [documentation](@/guides/columns/column-moving/column-moving.md#drag-and-move-actions-of-manualcolumnmove-plugin).
   * @returns {boolean}
   */
  isMovePossible(e, n) {
    const i = this.hot.columnIndexMapper.getNotTrimmedIndexesLength(), r = e.length + n > i, o = n < 0, s = e.some((l) => l < 0), a = e.some((l) => l >= i);
    return !(r || o || s || a);
  }
  /**
   * Indicates if order of columns was changed.
   *
   * @private
   * @param {Array} movedColumns Array of visual column indexes to be moved.
   * @param {number} finalIndex Visual column index, being a start index for the moved columns. Points to where the elements will be placed after the moving action.
   * To check the visualization of the final index, please take a look at [documentation](@/guides/columns/column-moving/column-moving.md#drag-and-move-actions-of-manualcolumnmove-plugin).
   * @returns {boolean}
   */
  isColumnOrderChanged(e, n) {
    return e.some((i, r) => i - r !== n);
  }
  /**
   * Count the final column index from the drop index.
   *
   * @private
   * @param {Array} movedColumns Array of visual column indexes to be moved.
   * @param {number} dropIndex Visual column index, being a drop index for the moved columns.
   * @returns {number} Visual column index, being a start index for the moved columns.
   */
  countFinalIndex(e, n) {
    const i = rn(e, (r, o) => (o < n && (r += 1), r), 0);
    return n - i;
  }
  /**
   * Gets the sum of the widths of columns in the provided range.
   *
   * @private
   * @param {number} fromColumn Visual column index.
   * @param {number} toColumn Visual column index.
   * @returns {number}
   */
  getColumnsWidth(e, n) {
    const i = this.hot.columnIndexMapper;
    let r = 0;
    for (let o = e; o <= n; o += 1) {
      const s = i.getRenderableFromVisualIndex(o);
      o < 0 ? r += this.hot.view._wt.wtViewport.getRowHeaderWidth() || 0 : s !== null && (r += this.hot.view._wt.wtTable.getStretchedColumnWidth(s) || 0);
    }
    return r;
  }
  /**
   * Loads initial settings when persistent state is saved or when plugin was initialized as an array.
   *
   * @private
   */
  moveBySettingsOrLoad() {
    const e = this.hot.getSettings()[yv];
    if (Array.isArray(e))
      this.moveColumns(e, 0);
    else if (e !== void 0) {
      const n = this.persistentStateLoad();
      n.length && this.moveColumns(n, 0);
    }
  }
  /**
   * Checks if the provided column is in the fixedColumnsTop section.
   *
   * @private
   * @param {number} column Visual column index to check.
   * @returns {boolean}
   */
  isFixedColumnsStart(e) {
    return e < this.hot.getSettings().fixedColumnsStart;
  }
  /**
   * Saves the manual column positions to the persistent state (the {@link Options#persistentState} option has to be enabled).
   *
   * @private
   * @fires Hooks#persistentStateSave
   */
  persistentStateSave() {
    this.hot.runHooks("persistentStateSave", "manualColumnMove", this.hot.columnIndexMapper.getIndexesSequence());
  }
  /**
   * Loads the manual column positions from the persistent state (the {@link Options#persistentState} option has to be enabled).
   *
   * @private
   * @fires Hooks#persistentStateLoad
   * @returns {Array} Stored state.
   */
  persistentStateLoad() {
    const e = {};
    return this.hot.runHooks("persistentStateLoad", "manualColumnMove", e), e.value ? e.value : [];
  }
  /**
   * Prepares an array of indexes based on actual selection.
   *
   * @private
   * @param {number} start The start index.
   * @param {number} end The end index.
   * @returns {Array}
   */
  prepareColumnsToMoving(e, n) {
    const i = [];
    return Ae(e, n, (r) => {
      i.push(r);
    }), i;
  }
  /**
   * Update the UI visual position.
   *
   * @private
   */
  refreshPositions() {
    const e = this.hot.view.getFirstFullyVisibleColumn();
    this.isFixedColumnsStart(ge(ki, this)) && e > 0 && this.hot.scrollViewportTo({
      col: this.hot.columnIndexMapper.getNearestNotHiddenIndex(e - 1, -1)
    });
    const n = this.hot.view._wt.wtTable, i = this.hot.view._wt.wtOverlays.scrollableElement, r = typeof i.scrollX == "number" ? i.scrollX : i.scrollLeft;
    let o = this.hot.view.THEAD.offsetLeft + this.getColumnsWidth(0, ge(ki, this) - 1);
    const s = n.hider.offsetWidth, a = n.TBODY.offsetLeft, l = ge(Kn, this).getOffset().start, u = ge(Kn, this).getSize().width;
    let c = 0, h = 0;
    if (this.hot.isRtl()) {
      const m = this.hot.rootWindow, w = It(this.hot.rootElement), C = m.innerWidth - ge(lu, this) - w;
      h = m.innerWidth - ge(zn, this).eventPageX - C - (i.scrollX === void 0 ? r : 0);
    } else
      h = ge(zn, this).eventPageX - (ge(lu, this) - (i.scrollX === void 0 ? r : 0));
    if (ge(Vm, this) && (c = this.hot.view._wt.wtOverlays.inlineStartOverlay.clone.wtTable.getColumnHeader(-1).offsetWidth), this.isFixedColumnsStart(ge(ki, this)) && (o += r), o += c, ge(ki, this) < 0)
      ge(Ah, this) > 0 ? ge(zn, this).col = 0 : ge(zn, this).col = e > 0 ? e - 1 : e;
    else if (ge(zn, this).TD.offsetWidth / 2 + o <= h) {
      const m = ge(ki, this) >= ge(nm, this) ? ge(nm, this) - 1 : ge(ki, this);
      ge(zn, this).col = m + 1, o += ge(zn, this).TD.offsetWidth;
    } else
      ge(zn, this).col = ge(ki, this);
    let d = h, g = o;
    h + u + l >= s ? d = s - u - l : h + l < a + c && (d = a + c + Math.abs(l)), o >= s - 1 ? g = s - 1 : g === 0 ? g = 1 : i.scrollX !== void 0 && ge(ki, this) < ge(Ah, this) && (g -= ge(lu, this) <= i.scrollX ? ge(lu, this) : 0), ge(Kn, this).setPosition(null, d), ge(Ps, this).setPosition(null, g);
  }
  /**
   * Binds the events used by the plugin.
   *
   * @private
   */
  registerEvents() {
    const {
      documentElement: e
    } = this.hot.rootDocument;
    this.eventManager.addEventListener(e, "mousemove", (n) => Ts(xa, this, eoe).call(this, n)), this.eventManager.addEventListener(e, "mouseup", () => Ts(xa, this, noe).call(this));
  }
  /**
   * Unbinds the events used by the plugin.
   *
   * @private
   */
  unregisterEvents() {
    this.eventManager.clear();
  }
  /**
   * Builds the plugin's UI.
   *
   * @private
   */
  buildPluginUI() {
    ge(Kn, this).build(), ge(Ps, this).build();
  }
  /**
   * Callback for the `afterLoadData` hook.
   *
   * @private
   */
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    ge(Kn, this).destroy(), ge(Ps, this).destroy(), super.destroy();
  }
}
function Jre(t, e, n, i) {
  const r = this.hot.view._wt.wtTable, o = this.hot.selection.isSelectedByColumnHeader(), s = this.hot.getSelectedRangeLast(), a = Te(t.target, "sortAction");
  if (!s || !o || ge(Ls, this) || t.button !== 0 || a) {
    hi(Ls, this, !1), ge(zo, this).length = 0, xe(this.hot.rootElement, [nS, Fm]);
    return;
  }
  const l = ge(Ps, this).isBuilt() && !ge(Ps, this).isAppended(), u = ge(Kn, this).isBuilt() && !ge(Kn, this).isAppended();
  l && u && (ge(Ps, this).appendTo(r.hider), ge(Kn, this).appendTo(r.hider));
  const {
    from: c,
    to: h
  } = s, d = Math.min(c.col, h.col), g = Math.max(c.col, h.col);
  if (e.row < 0 && e.col >= d && e.col <= g) {
    i.column = !0, hi(Ls, this, !0);
    const m = n.firstChild ? dx(t, n.firstChild).x : t.offsetX;
    ge(zn, this).eventPageX = t.pageX, hi(ki, this, e.col), ge(zn, this).TD = n, ge(zn, this).col = e.col, hi(zo, this, this.prepareColumnsToMoving(d, g)), hi(Vm, this, !!this.hot.getSettings().rowHeaders), hi(nm, this, this.hot.countCols()), hi(Ah, this, this.hot.getSettings().fixedColumnsStart), hi(lu, this, gn(this.hot.rootElement).left);
    const w = ge(Vm, this) ? -1 : 0, C = r.holder.scrollTop + r.getColumnHeaderHeight(0) + 1, S = e.col < ge(Ah, this), b = this.hot.view._wt.wtOverlays.inlineStartOverlay.getOverlayOffset(), R = Math.abs(m - (this.hot.isRtl() ? n.offsetWidth : 0)), _ = this.getColumnsWidth(d, e.col - 1) + R, O = this.getColumnsWidth(w, d - 1) + (S ? b : 0) + _;
    ge(Kn, this).setPosition(C, O), ge(Kn, this).setSize(this.getColumnsWidth(d, g), r.hider.offsetHeight - C), ge(Kn, this).setOffset(null, -_), Q(this.hot.rootElement, nS);
  } else
    xe(this.hot.rootElement, iS), hi(Ls, this, !1), ge(zo, this).length = 0;
}
function eoe(t) {
  ge(Ls, this) && (ge(zn, this).eventPageX = t.pageX, this.refreshPositions());
}
function toe(t, e, n, i) {
  !this.hot.getSelectedRangeLast() || !ge(Ls, this) || (ge(zo, this).indexOf(e.col) > -1 ? xe(this.hot.rootElement, Fm) : Q(this.hot.rootElement, Fm), i.row = !0, i.column = !0, i.cell = !0, hi(ki, this, e.col), ge(zn, this).TD = n);
}
function noe() {
  const t = ge(zn, this).col, e = ge(zo, this).length;
  if (hi(ki, this, void 0), hi(Ls, this, !1), xe(this.hot.rootElement, [nS, Fm, iS]), this.hot.selection.isSelectedByColumnHeader() && Q(this.hot.rootElement, iS), e < 1 || t === void 0)
    return;
  const n = ge(zo, this)[0], i = this.hot.toPhysicalColumn(n), r = this.dragColumns(ge(zo, this), t);
  if (ge(zo, this).length = 0, r === !0) {
    this.persistentStateSave(), this.hot.render(), this.hot.view.adjustElementsSize();
    const o = this.hot.toVisualColumn(i), s = o + e - 1;
    this.hot.selectColumns(o, s);
  }
}
function ioe() {
  const t = this.hot.view._wt.wtTable, e = t.getColumnHeaderHeight(0) + 1, n = t.holder.scrollTop, i = e + n;
  ge(Kn, this).setPosition(i), ge(Kn, this).setSize(null, t.hider.offsetHeight - i);
}
function roe() {
  this.moveBySettingsOrLoad();
}
function ooe(t, e) {
  S_(t, e), e.add(t);
}
function Hn(t, e, n) {
  S_(t, e), e.set(t, n);
}
function S_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function qt(t, e, n) {
  return t.set(mr(t, e), n), n;
}
function se(t, e) {
  return t.get(mr(t, e));
}
function mr(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const Wm = "manualColumnResize", soe = 130, eI = "manualColumnWidths";
var Bo = /* @__PURE__ */ new WeakMap(), mg = /* @__PURE__ */ new WeakMap(), yi = /* @__PURE__ */ new WeakMap(), $m = /* @__PURE__ */ new WeakMap(), io = /* @__PURE__ */ new WeakMap(), aoe = /* @__PURE__ */ new WeakMap(), bu = /* @__PURE__ */ new WeakMap(), pg = /* @__PURE__ */ new WeakMap(), oi = /* @__PURE__ */ new WeakMap(), No = /* @__PURE__ */ new WeakMap(), ol = /* @__PURE__ */ new WeakMap(), Um = /* @__PURE__ */ new WeakMap(), Ch = /* @__PURE__ */ new WeakMap(), vh = /* @__PURE__ */ new WeakMap(), bi = /* @__PURE__ */ new WeakMap(), yh = /* @__PURE__ */ new WeakMap(), $r = /* @__PURE__ */ new WeakSet();
class loe extends at {
  static get PLUGIN_KEY() {
    return Wm;
  }
  static get PLUGIN_PRIORITY() {
    return soe;
  }
  /**
   * @type {HTMLTableHeaderCellElement}
   */
  constructor(e) {
    super(e), ooe(this, $r), Hn(this, Bo, null), Hn(this, mg, null), Hn(this, yi, []), Hn(this, $m, null), Hn(this, io, null), Hn(this, aoe, null), Hn(this, bu, null), Hn(this, pg, null), Hn(this, oi, this.hot.rootDocument.createElement("DIV")), Hn(this, No, this.hot.rootDocument.createElement("DIV")), Hn(this, ol, null), Hn(this, Um, !1), Hn(this, Ch, 0), Hn(this, vh, null), Hn(this, bi, void 0), Hn(this, yh, void 0), Q(se(oi, this), "manualColumnResizer"), Q(se(No, this), "manualColumnResizerGuide");
  }
  /**
   * @private
   * @returns {string}
   */
  get inlineDir() {
    return this.hot.isRtl() ? "right" : "left";
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ManualColumnResize#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return this.hot.getSettings()[Wm];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled || (qt(bi, this, new Zs()), se(bi, this).addLocalHook("init", () => mr($r, this, uoe).call(this)), this.hot.columnIndexMapper.registerMap(this.pluginName, se(bi, this)), this.addHook("modifyColWidth", (e, n) => mr($r, this, moe).call(this, e, n), 1), this.addHook("beforeStretchingColumnWidth", (e, n) => mr($r, this, poe).call(this, e, n)), this.addHook("beforeColumnResize", (e, n, i) => mr($r, this, woe).call(this, e, n, i)), this.bindEvents(), super.enablePlugin());
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`manualColumnResize`](@/api/options.md#manualcolumnresize)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    qt(yh, this, se(bi, this).getValues()), this.hot.columnIndexMapper.unregisterMap(this.pluginName), super.disablePlugin();
  }
  /**
   * Saves the current sizes using the persistentState plugin (the {@link Options#persistentState} option has to be enabled).
   *
   * @fires Hooks#persistentStateSave
   */
  saveManualColumnWidths() {
    this.hot.runHooks("persistentStateSave", eI, se(bi, this).getValues());
  }
  /**
   * Loads the previously saved sizes using the persistentState plugin (the {@link Options#persistentState} option has to be enabled).
   *
   * @returns {Array}
   * @fires Hooks#persistentStateLoad
   */
  loadManualColumnWidths() {
    const e = {};
    return this.hot.runHooks("persistentStateLoad", eI, e), e.value;
  }
  /**
   * Sets the new width for specified column index.
   *
   * @param {number} column Visual column index.
   * @param {number} width Column width (no less than 20px).
   * @returns {number} Returns new width.
   */
  setManualSize(e, n) {
    const i = Math.max(n, 20), r = this.hot.toPhysicalColumn(e);
    return se(bi, this).setValueAtIndex(r, i), i;
  }
  /**
   * Clears the cache for the specified column index.
   *
   * @param {number} column Visual column index.
   */
  clearManualSize(e) {
    const n = this.hot.toPhysicalColumn(e);
    se(bi, this).setValueAtIndex(n, null);
  }
  /**
   * Set the resize handle position.
   *
   * @private
   * @param {HTMLCellElement} TH TH HTML element.
   */
  setupHandlePosition(e) {
    if (!e.parentNode)
      return;
    qt(Bo, this, e);
    const {
      _wt: n
    } = this.hot.view, i = n.wtTable.getCoords(se(Bo, this)), r = i.col;
    if (r < 0)
      return;
    const o = Zt(se(Bo, this)), s = se(Bo, this).getBoundingClientRect(), a = r < n.getSetting("fixedColumnsStart");
    let l;
    a && (l = n.wtOverlays.topInlineStartCornerOverlay.getRelativeCellPosition(se(Bo, this), i.row, i.col)), l || (l = n.wtOverlays.topOverlay.getRelativeCellPosition(se(Bo, this), i.row, i.col)), qt(mg, this, this.hot.columnIndexMapper.getVisualFromRenderableIndex(r)), qt(yi, this, []);
    const u = this.hot.selection.isSelectedByCorner() || this.hot.selection.isSelectedByColumnHeader();
    if (this.hot.selection.isSelected() && u) {
      const c = this.hot.getSelectedRange();
      T(c, (h) => {
        const d = h.getTopStartCorner().col, g = h.getBottomEndCorner().col;
        Ae(d, g, (m) => {
          se(yi, this).includes(m) || se(yi, this).push(m);
        });
      });
    }
    se(yi, this).includes(se(mg, this)) || qt(yi, this, [se(mg, this)]), qt(pg, this, l.start - 6), qt(bu, this, parseInt(s.width, 10)), se(oi, this).style.top = `${l.top}px`, se(oi, this).style[this.inlineDir] = `${se(pg, this) + se(bu, this)}px`, se(oi, this).style.height = `${o}px`, this.hot.rootElement.appendChild(se(oi, this));
  }
  /**
   * Refresh the resize handle position.
   *
   * @private
   */
  refreshHandlePosition() {
    se(oi, this).style[this.inlineDir] = `${se(pg, this) + se($m, this)}px`;
  }
  /**
   * Sets the resize guide position.
   *
   * @private
   */
  setupGuidePosition() {
    const e = parseInt(Zt(se(oi, this)), 10), n = parseInt(se(oi, this).style.top, 10) + e, i = parseInt(this.hot.view.maximumVisibleElementHeight(0), 10);
    Q(se(oi, this), "active"), Q(se(No, this), "active"), se(No, this).style.top = `${n}px`, this.refreshGuidePosition(), se(No, this).style.height = `${i - e}px`, this.hot.rootElement.appendChild(se(No, this));
  }
  /**
   * Refresh the resize guide position.
   *
   * @private
   */
  refreshGuidePosition() {
    se(No, this).style[this.inlineDir] = se(oi, this).style[this.inlineDir];
  }
  /**
   * Hides both the resize handle and resize guide.
   *
   * @private
   */
  hideHandleAndGuide() {
    xe(se(oi, this), "active"), xe(se(No, this), "active");
  }
  /**
   * Checks if provided element is considered a column header.
   *
   * @private
   * @param {HTMLElement} element HTML element.
   * @returns {boolean}
   */
  checkIfColumnHeader(e) {
    const n = js(e, ["THEAD"], this.hot.rootElement), {
      topOverlay: i,
      topInlineStartCornerOverlay: r
    } = this.hot.view._wt.wtOverlays;
    return [i.clone.wtTable.THEAD, r.clone.wtTable.THEAD].includes(n);
  }
  /**
   * Gets the TH element from the provided element.
   *
   * @private
   * @param {HTMLElement} element HTML element.
   * @returns {HTMLElement}
   */
  getClosestTHParent(e) {
    return e.tagName !== "TABLE" ? e.tagName === "TH" ? e : this.getClosestTHParent(e.parentNode) : null;
  }
  /**
   * 'mouseover' event callback - set the handle position.
   *
   * @param {MouseEvent} event The mouse event.
   */
  /**
   * Auto-size row after doubleclick - callback.
   *
   * @private
   * @fires Hooks#beforeColumnResize
   * @fires Hooks#afterColumnResize
   */
  afterMouseDownTimeout() {
    const e = () => {
      this.hot.forceFullRender = !0, this.hot.view.render(), this.hot.view.adjustElementsSize();
    }, n = (i, r) => {
      const o = this.hot.runHooks("beforeColumnResize", se(io, this), i, !0);
      o !== void 0 && qt(io, this, o), this.hot.getSettings().stretchH === "all" ? this.clearManualSize(i) : this.setManualSize(i, se(io, this)), this.saveManualColumnWidths(), this.hot.runHooks("afterColumnResize", se(io, this), i, !0), r && e();
    };
    se(Ch, this) >= 2 && (se(yi, this).length > 1 ? (T(se(yi, this), (r) => {
      n(r);
    }), e()) : T(se(yi, this), (r) => {
      n(r, !0);
    })), qt(Ch, this, 0), qt(vh, this, null);
  }
  /**
   * 'mousedown' event callback.
   *
   * @param {MouseEvent} event The mouse event.
   */
  /**
   * Binds the mouse events.
   *
   * @private
   */
  bindEvents() {
    const {
      rootWindow: e,
      rootElement: n
    } = this.hot;
    this.eventManager.addEventListener(n, "mouseover", (i) => mr($r, this, coe).call(this, i)), this.eventManager.addEventListener(n, "mousedown", (i) => mr($r, this, hoe).call(this, i)), this.eventManager.addEventListener(e, "mousemove", (i) => mr($r, this, doe).call(this, i)), this.eventManager.addEventListener(e, "mouseup", () => mr($r, this, foe).call(this)), this.eventManager.addEventListener(se(oi, this), "contextmenu", () => mr($r, this, goe).call(this));
  }
  /**
   * Modifies the provided column width, based on the plugin settings.
   *
   * @param {number} width Column width.
   * @param {number} column Visual column index.
   * @returns {number}
   */
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function uoe() {
  const t = this.hot.getSettings()[Wm], e = this.loadManualColumnWidths();
  typeof e != "undefined" ? this.hot.batchExecution(() => {
    e.forEach((n, i) => {
      se(bi, this).setValueAtIndex(i, n);
    });
  }, !0) : Array.isArray(t) ? (this.hot.batchExecution(() => {
    t.forEach((n, i) => {
      se(bi, this).setValueAtIndex(i, n);
    });
  }, !0), qt(yh, this, t)) : t === !0 && Array.isArray(se(yh, this)) && this.hot.batchExecution(() => {
    se(yh, this).forEach((n, i) => {
      se(bi, this).setValueAtIndex(i, n);
    });
  }, !0);
}
function coe(t) {
  if (!ob(t.target) && se(Um, this) !== !0 && this.checkIfColumnHeader(t.target)) {
    const e = this.getClosestTHParent(t.target);
    if (!e)
      return;
    const n = e.getAttribute("colspan");
    e && (n === null || n === "1") && (se(ol, this) || this.setupHandlePosition(e));
  }
}
function hoe(t) {
  t.target.parentNode === this.hot.rootElement && Te(t.target, "manualColumnResizer") && (this.setupHandlePosition(se(Bo, this)), this.setupGuidePosition(), qt(ol, this, !0), se(vh, this) === null && (qt(vh, this, setTimeout(() => this.afterMouseDownTimeout(), 500)), this.hot._registerTimeout(se(vh, this))), qt(Ch, this, se(Ch, this) + 1), this.startX = t.pageX, qt(io, this, se(bu, this)));
}
function doe(t) {
  if (se(ol, this)) {
    const e = (t.pageX - this.startX) * this.hot.getDirectionFactor();
    qt($m, this, se(bu, this) + e), T(se(yi, this), (n) => {
      qt(io, this, this.setManualSize(n, se($m, this)));
    }), this.refreshHandlePosition(), this.refreshGuidePosition();
  }
}
function foe() {
  const t = () => {
    this.hot.forceFullRender = !0, this.hot.view.render(), this.hot.view.adjustElementsSize();
  }, e = (n, i) => {
    this.hot.runHooks("beforeColumnResize", se(io, this), n, !1), i && t(), this.saveManualColumnWidths(), this.hot.runHooks("afterColumnResize", se(io, this), n, !1);
  };
  se(ol, this) && (this.hideHandleAndGuide(), qt(ol, this, !1), se(io, this) !== se(bu, this) && (se(yi, this).length > 1 ? (T(se(yi, this), (i) => {
    e(i);
  }), t()) : T(se(yi, this), (i) => {
    e(i, !0);
  })), this.setupHandlePosition(se(Bo, this)));
}
function goe() {
  this.hideHandleAndGuide(), this.hot.rootElement.removeChild(se(oi, this)), this.hot.rootElement.removeChild(se(No, this)), qt(ol, this, !1), qt(Um, this, !0), this.hot._registerImmediate(() => {
    qt(Um, this, !1);
  });
}
function moe(t, e) {
  let n = t;
  if (this.enabled) {
    const i = this.hot.toPhysicalColumn(e), r = se(bi, this).getValueAtIndex(i);
    this.hot.getSettings()[Wm] && r && (n = r);
  }
  return n;
}
function poe(t, e) {
  let n = se(bi, this).getValueAtIndex(e);
  return n === null && (n = t), n;
}
function woe() {
  this.hot.view._wt.wtViewport.resetHasOversizedColumnHeadersMarked();
}
function Sv(t, e, n) {
  return (e = Aoe(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Aoe(t) {
  var e = Coe(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Coe(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const bv = 0, tI = 1, nI = 2, Jl = "px";
let voe = class {
  constructor(e) {
    Sv(this, "hot", void 0), Sv(this, "_element", null), Sv(this, "state", bv), this.hot = e;
  }
  /**
   * Add created UI elements to table.
   *
   * @param {HTMLElement} wrapper Element which are parent for our UI element.
   */
  appendTo(e) {
    e.appendChild(this._element), this.state = nI;
  }
  /**
   * Method for create UI element. Only create, without append to table.
   */
  build() {
    this.state === bv && (this._element = this.hot.rootDocument.createElement("div"), this.state = tI);
  }
  /**
   * Method for remove UI element.
   */
  destroy() {
    this.isAppended() && this._element.parentElement.removeChild(this._element), this._element = null, this.state = bv;
  }
  /**
   * Check if UI element are appended.
   *
   * @returns {boolean}
   */
  isAppended() {
    return this.state === nI;
  }
  /**
   * Check if UI element are built.
   *
   * @returns {boolean}
   */
  isBuilt() {
    return this.state >= tI;
  }
  /**
   * Setter for position.
   *
   * @param {number} top New top position of the element.
   * @param {number} left New left position of the element.
   */
  setPosition(e, n) {
    e !== void 0 && (this._element.style.top = e + Jl), n !== void 0 && (this._element.style.left = n + Jl);
  }
  /**
   * Getter for the element position.
   *
   * @returns {object} Object contains left and top position of the element.
   */
  getPosition() {
    return {
      top: this._element.style.top ? parseInt(this._element.style.top, 10) : 0,
      left: this._element.style.left ? parseInt(this._element.style.left, 10) : 0
    };
  }
  /**
   * Setter for the element size.
   *
   * @param {number} width New width of the element.
   * @param {number} height New height of the element.
   */
  setSize(e, n) {
    e && (this._element.style.width = e + Jl), n && (this._element.style.height = n + Jl);
  }
  /**
   * Getter for the element position.
   *
   * @returns {object} Object contains height and width of the element.
   */
  getSize() {
    return {
      width: this._element.style.width ? parseInt(this._element.style.width, 10) : 0,
      height: this._element.style.height ? parseInt(this._element.style.height, 10) : 0
    };
  }
  /**
   * Setter for the element offset. Offset means marginTop and marginLeft of the element.
   *
   * @param {number} top New margin top of the element.
   * @param {number} left New margin left of the element.
   */
  setOffset(e, n) {
    e && (this._element.style.marginTop = e + Jl), n && (this._element.style.marginLeft = n + Jl);
  }
  /**
   * Getter for the element offset.
   *
   * @returns {object} Object contains top and left offset of the element.
   */
  getOffset() {
    return {
      top: this._element.style.marginTop ? parseInt(this._element.style.marginTop, 10) : 0,
      left: this._element.style.marginLeft ? parseInt(this._element.style.marginLeft, 10) : 0
    };
  }
};
const b_ = voe, yoe = "ht__manualRowMove--backlight";
class Soe extends b_ {
  /**
   * Custom className on build process.
   */
  build() {
    super.build(), Q(this._element, yoe);
  }
}
const boe = Soe, Roe = "ht__manualRowMove--guideline";
class Eoe extends b_ {
  /**
   * Custom className on build process.
   */
  build() {
    super.build(), Q(this._element, Roe);
  }
}
const Toe = Eoe;
function Moe(t, e) {
  R_(t, e), e.add(t);
}
function eu(t, e, n) {
  R_(t, e), e.set(t, n);
}
function R_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Xa(t, e, n) {
  return t.set(Ms(t, e), n), n;
}
function _e(t, e) {
  return t.get(Ms(t, e));
}
function Ms(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("beforeRowMove");
he.getSingleton().register("afterRowMove");
const Rv = "manualRowMove", Ioe = 140, iI = "ht__manualRowMove", Ym = "show-ui", rS = "on-moving--rows", oS = "after-selection--rows";
var Qn = /* @__PURE__ */ new WeakMap(), xs = /* @__PURE__ */ new WeakMap(), Ko = /* @__PURE__ */ new WeakMap(), Bs = /* @__PURE__ */ new WeakMap(), di = /* @__PURE__ */ new WeakMap(), wg = /* @__PURE__ */ new WeakMap(), Ha = /* @__PURE__ */ new WeakSet();
class Ooe extends at {
  constructor() {
    super(...arguments), Moe(this, Ha), eu(this, Qn, new boe(this.hot)), eu(this, xs, new Toe(this.hot)), eu(this, Ko, []), eu(this, Bs, void 0), eu(this, di, {}), eu(this, wg, void 0);
  }
  static get PLUGIN_KEY() {
    return Rv;
  }
  static get PLUGIN_PRIORITY() {
    return Ioe;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ManualRowMove#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[Rv];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    this.enabled || (this.addHook("beforeOnCellMouseDown", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ms(Ha, e, Poe).call(e, ...i);
    }), this.addHook("beforeOnCellMouseOver", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ms(Ha, e, Hoe).call(e, ...i);
    }), this.addHook("afterScrollHorizontally", () => Ms(Ha, this, Noe).call(this)), this.addHook("afterLoadData", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return Ms(Ha, e, Doe).call(e, ...i);
    }), this.buildPluginUI(), this.registerEvents(), Q(this.hot.rootElement, iI), super.enablePlugin());
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`manualRowMove`](@/api/options.md#manualrowmove)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), this.moveBySettingsOrLoad(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    xe(this.hot.rootElement, iI), this.unregisterEvents(), _e(Qn, this).destroy(), _e(xs, this).destroy(), super.disablePlugin();
  }
  /**
   * Moves a single row.
   *
   * To see the outcome, rerender your grid by calling [`render()`](@/api/core.md#render).
   *
   * @param {number} row Visual row index to be moved.
   * @param {number} finalIndex Visual row index, being a start index for the moved rows. Points to where the elements will be placed after the moving action.
   * To check the visualization of the final index, please take a look at [documentation](@/guides/rows/row-moving/row-moving.md#drag-and-move-actions-of-manualrowmove-plugin).
   * @fires Hooks#beforeRowMove
   * @fires Hooks#afterRowMove
   * @returns {boolean}
   */
  moveRow(e, n) {
    return this.moveRows([e], n);
  }
  /**
   * Moves multiple rows.
   *
   * To see the outcome, rerender your grid by calling [`render()`](@/api/core.md#render).
   *
   * @param {Array} rows Array of visual row indexes to be moved.
   * @param {number} finalIndex Visual row index, being a start index for the moved rows. Points to where the elements will be placed after the moving action.
   * To check the visualization of the final index, please take a look at [documentation](@/guides/rows/row-moving/row-moving.md#drag-and-move-actions-of-manualrowmove-plugin).
   * @fires Hooks#beforeRowMove
   * @fires Hooks#afterRowMove
   * @returns {boolean}
   */
  moveRows(e, n) {
    const i = _e(wg, this), r = this.isMovePossible(e, n), o = this.hot.runHooks("beforeRowMove", e, n, i, r);
    if (Xa(wg, this, void 0), o === !1)
      return;
    r && this.hot.rowIndexMapper.moveIndexes(e, n);
    const s = r && this.isRowOrderChanged(e, n);
    return this.hot.runHooks("afterRowMove", e, n, i, r, s), s;
  }
  /**
   * Drag a single row to drop index position.
   *
   * @param {number} row Visual row index to be dragged.
   * @param {number} dropIndex Visual row index, being a drop index for the moved rows. Points to where we are going to drop the moved elements.
   * To check visualization of drop index please take a look at [documentation](@/guides/rows/row-moving/row-moving.md#drag-and-move-actions-of-manualrowmove-plugin).
   * @fires Hooks#beforeRowMove
   * @fires Hooks#afterRowMove
   * @returns {boolean}
   */
  dragRow(e, n) {
    return this.dragRows([e], n);
  }
  /**
   * Drag multiple rows to drop index position.
   *
   * @param {Array} rows Array of visual row indexes to be dragged.
   * @param {number} dropIndex Visual row index, being a drop index for the moved rows. Points to where we are going to drop the moved elements.
   * To check visualization of drop index please take a look at [documentation](@/guides/rows/row-moving/row-moving.md#drag-and-move-actions-of-manualrowmove-plugin).
   * @fires Hooks#beforeRowMove
   * @fires Hooks#afterRowMove
   * @returns {boolean}
   */
  dragRows(e, n) {
    const i = this.countFinalIndex(e, n);
    return Xa(wg, this, n), this.moveRows(e, i);
  }
  /**
   * Indicates if it's possible to move rows to the desired position. Some of the actions aren't possible, i.e. You can’t move more than one element to the last position.
   *
   * @param {Array} movedRows Array of visual row indexes to be moved.
   * @param {number} finalIndex Visual row index, being a start index for the moved rows. Points to where the elements will be placed after the moving action.
   * To check the visualization of the final index, please take a look at [documentation](@/guides/rows/row-moving/row-moving.md#drag-and-move-actions-of-manualrowmove-plugin).
   * @returns {boolean}
   */
  isMovePossible(e, n) {
    const i = this.hot.rowIndexMapper.getNotTrimmedIndexesLength(), r = e.length + n > i, o = n < 0, s = e.some((l) => l < 0), a = e.some((l) => l >= i);
    return !(r || o || s || a);
  }
  /**
   * Indicates if order of rows was changed.
   *
   * @private
   * @param {Array} movedRows Array of visual row indexes to be moved.
   * @param {number} finalIndex Visual row index, being a start index for the moved rows. Points to where the elements will be placed after the moving action.
   * To check the visualization of the final index, please take a look at [documentation](@/guides/rows/row-moving/row-moving.md#drag-and-move-actions-of-manualrowmove-plugin).
   * @returns {boolean}
   */
  isRowOrderChanged(e, n) {
    return e.some((i, r) => i - r !== n);
  }
  /**
   * Count the final row index from the drop index.
   *
   * @private
   * @param {Array} movedRows Array of visual row indexes to be moved.
   * @param {number} dropIndex Visual row index, being a drop index for the moved rows.
   * @returns {number} Visual row index, being a start index for the moved rows.
   */
  countFinalIndex(e, n) {
    const i = rn(e, (r, o) => (o < n && (r += 1), r), 0);
    return n - i;
  }
  /**
   * Gets the sum of the heights of rows in the provided range.
   *
   * @private
   * @param {number} fromRow Visual row index.
   * @param {number} toRow Visual row index.
   * @returns {number}
   */
  getRowsHeight(e, n) {
    const i = this.hot.rowIndexMapper;
    let r = 0;
    for (let o = e; o <= n; o++) {
      const s = i.getRenderableFromVisualIndex(o);
      s !== null && (r += this.hot.view._wt.wtTable.getRowHeight(s) || 23);
    }
    return r;
  }
  /**
   * Loads initial settings when persistent state is saved or when plugin was initialized as an array.
   *
   * @private
   */
  moveBySettingsOrLoad() {
    const e = this.hot.getSettings()[Rv];
    if (Array.isArray(e))
      this.moveRows(e, 0);
    else if (e !== void 0) {
      const n = this.persistentStateLoad();
      n.length && this.moveRows(n, 0);
    }
  }
  /**
   * Checks if the provided row is in the fixedRowsTop section.
   *
   * @private
   * @param {number} row Visual row index to check.
   * @returns {boolean}
   */
  isFixedRowTop(e) {
    return e < this.hot.getSettings().fixedRowsTop;
  }
  /**
   * Checks if the provided row is in the fixedRowsBottom section.
   *
   * @private
   * @param {number} row Visual row index to check.
   * @returns {boolean}
   */
  isFixedRowBottom(e) {
    return e > this.hot.countRows() - 1 - this.hot.getSettings().fixedRowsBottom;
  }
  /**
   * Saves the manual row positions to the persistent state (the {@link Options#persistentState} option has to be enabled).
   *
   * @private
   * @fires Hooks#persistentStateSave
   */
  persistentStateSave() {
    this.hot.runHooks("persistentStateSave", "manualRowMove", this.hot.rowIndexMapper.getIndexesSequence());
  }
  /**
   * Loads the manual row positions from the persistent state (the {@link Options#persistentState} option has to be enabled).
   *
   * @private
   * @fires Hooks#persistentStateLoad
   * @returns {Array} Stored state.
   */
  persistentStateLoad() {
    const e = {};
    return this.hot.runHooks("persistentStateLoad", "manualRowMove", e), e.value ? e.value : [];
  }
  /**
   * Prepares an array of indexes based on actual selection.
   *
   * @private
   * @returns {Array}
   */
  prepareRowsToMoving() {
    const e = this.hot.getSelectedRangeLast(), n = [];
    if (!e)
      return n;
    const {
      from: i,
      to: r
    } = e, o = Math.min(i.row, r.row), s = Math.max(i.row, r.row);
    return Ae(o, s, (a) => {
      n.push(a);
    }), n;
  }
  /**
   * Update the UI visual position.
   *
   * @private
   */
  refreshPositions() {
    const e = _e(di, this).coords, n = this.hot.view.getFirstFullyVisibleRow(), i = this.hot.view.getLastFullyVisibleRow(), r = this.hot.countRows();
    this.isFixedRowTop(e.row) && n > 0 && this.hot.scrollViewportTo(this.hot.rowIndexMapper.getNearestNotHiddenIndex(n - 1, -1)), this.isFixedRowBottom(e.row) && i < r && this.hot.scrollViewportTo(this.hot.rowIndexMapper.getNearestNotHiddenIndex(i + 1, 1), void 0, !0);
    const o = this.hot.view._wt.wtTable, s = _e(di, this).TD, a = this.hot.rootElement, l = gn(a), u = tl(a), c = o.holder.scrollTop, h = this.hot.rootWindow !== u ? u.scrollTop : 0, d = l.top - h, g = _e(di, this).eventPageY - d + c, m = o.hider.offsetHeight, w = o.TBODY.offsetTop, C = _e(Qn, this).getOffset().top, S = _e(Qn, this).getSize().height, b = s.offsetHeight / 2, R = s.offsetHeight;
    let _ = this.hot.view.THEAD.offsetHeight + this.getRowsHeight(0, e.row - 1);
    const O = g >= _ + b;
    this.isFixedRowTop(e.row) && (_ += this.hot.view._wt.wtOverlays.topOverlay.getOverlayOffset()), e.row < 0 ? _e(di, this).row = n > 0 ? n - 1 : n : O ? (_e(di, this).row = e.row + 1, _ += e.row === 0 ? R - 1 : R) : _e(di, this).row = e.row;
    let $ = g, U = _;
    g + S + C >= m ? $ = m - S - C : g + C < w && ($ = w + Math.abs(C)), _ >= m - 1 && (U = m - 1), _e(Qn, this).setPosition($), _e(xs, this).setPosition(U);
  }
  /**
   * Binds the events used by the plugin.
   *
   * @private
   */
  registerEvents() {
    const {
      documentElement: e
    } = this.hot.rootDocument;
    this.eventManager.addEventListener(e, "mousemove", (n) => Ms(Ha, this, xoe).call(this, n)), this.eventManager.addEventListener(e, "mouseup", () => Ms(Ha, this, _oe).call(this));
  }
  /**
   * Unbinds the events used by the plugin.
   *
   * @private
   */
  unregisterEvents() {
    this.eventManager.clear();
  }
  /**
   * Builds the plugin's UI.
   *
   * @private
   */
  buildPluginUI() {
    _e(Qn, this).build(), _e(xs, this).build();
  }
  /**
   * Callback for the `afterLoadData` hook.
   */
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    _e(Qn, this).destroy(), _e(xs, this).destroy(), super.destroy();
  }
}
function Poe(t, e, n, i) {
  const {
    wtTable: r,
    wtViewport: o
  } = this.hot.view._wt, s = this.hot.selection.isSelectedByRowHeader(), a = this.hot.getSelectedRangeLast();
  if (!a || !s || _e(Bs, this) || t.button !== 0) {
    Xa(Bs, this, !1), _e(Ko, this).length = 0, xe(this.hot.rootElement, [rS, Ym]);
    return;
  }
  const l = _e(xs, this).isBuilt() && !_e(xs, this).isAppended(), u = _e(Qn, this).isBuilt() && !_e(Qn, this).isAppended();
  l && u && (_e(xs, this).appendTo(r.hider), _e(Qn, this).appendTo(r.hider));
  const {
    from: c,
    to: h
  } = a, d = Math.min(c.row, h.row), g = Math.max(c.row, h.row);
  if (e.col < 0 && e.row >= d && e.row <= g) {
    i.row = !0, Xa(Bs, this, !0), _e(di, this).eventPageY = t.pageY, _e(di, this).coords = e, _e(di, this).TD = n, Xa(Ko, this, this.prepareRowsToMoving());
    const m = r.holder.scrollLeft + o.getRowHeaderWidth(), w = this.getRowsHeight(d, e.row - 1) + t.offsetY;
    _e(Qn, this).setPosition(null, m), _e(Qn, this).setSize(r.hider.offsetWidth - m, this.getRowsHeight(d, g)), _e(Qn, this).setOffset(-w, null), Q(this.hot.rootElement, rS), this.refreshPositions();
  } else
    xe(this.hot.rootElement, oS), Xa(Bs, this, !1), _e(Ko, this).length = 0;
}
function xoe(t) {
  _e(Bs, this) && (_e(di, this).eventPageY = t.pageY, this.refreshPositions());
}
function Hoe(t, e, n, i) {
  !this.hot.getSelectedRangeLast() || !_e(Bs, this) || (_e(Ko, this).indexOf(e.row) > -1 ? xe(this.hot.rootElement, Ym) : Q(this.hot.rootElement, Ym), i.row = !0, i.column = !0, i.cell = !0, _e(di, this).coords = e, _e(di, this).TD = n);
}
function _oe() {
  const t = _e(di, this).row, e = _e(Ko, this).length;
  if (Xa(Bs, this, !1), xe(this.hot.rootElement, [rS, Ym, oS]), this.hot.selection.isSelectedByRowHeader() && Q(this.hot.rootElement, oS), e < 1 || t === void 0)
    return;
  const n = _e(Ko, this)[0], i = this.hot.toPhysicalRow(n), r = this.dragRows(_e(Ko, this), t);
  if (_e(Ko, this).length = 0, r === !0) {
    this.persistentStateSave(), this.hot.render(), this.hot.view.adjustElementsSize();
    const o = this.hot.toVisualRow(i), s = o + e - 1;
    this.hot.selectRows(o, s);
  }
}
function Noe() {
  const t = this.hot.view._wt.wtTable, e = this.hot.view._wt.wtViewport.getRowHeaderWidth(), n = t.holder.scrollLeft, i = e + n;
  _e(Qn, this).setPosition(null, i), _e(Qn, this).setSize(t.hider.offsetWidth - i);
}
function Doe() {
  this.moveBySettingsOrLoad();
}
function koe(t, e) {
  E_(t, e), e.add(t);
}
function _n(t, e, n) {
  E_(t, e), e.set(t, n);
}
function E_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Ut(t, e, n) {
  return t.set(Fo(t, e), n), n;
}
function oe(t, e) {
  return t.get(Fo(t, e));
}
function Fo(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const jm = "manualRowResize", Loe = 30, rI = "manualRowHeights";
var qr = /* @__PURE__ */ new WeakMap(), Ag = /* @__PURE__ */ new WeakMap(), Si = /* @__PURE__ */ new WeakMap(), Sh = /* @__PURE__ */ new WeakMap(), Hs = /* @__PURE__ */ new WeakMap(), FR = /* @__PURE__ */ new WeakMap(), Ru = /* @__PURE__ */ new WeakMap(), Cg = /* @__PURE__ */ new WeakMap(), jn = /* @__PURE__ */ new WeakMap(), Qr = /* @__PURE__ */ new WeakMap(), sl = /* @__PURE__ */ new WeakMap(), Gm = /* @__PURE__ */ new WeakMap(), bh = /* @__PURE__ */ new WeakMap(), Rh = /* @__PURE__ */ new WeakMap(), pr = /* @__PURE__ */ new WeakMap(), Eh = /* @__PURE__ */ new WeakMap(), As = /* @__PURE__ */ new WeakSet();
class Boe extends at {
  static get PLUGIN_KEY() {
    return jm;
  }
  static get PLUGIN_PRIORITY() {
    return Loe;
  }
  /**
   * @type {HTMLTableCellElement}
   */
  constructor(e) {
    super(e), koe(this, As), _n(this, qr, null), _n(this, Ag, null), _n(this, Si, []), _n(this, Sh, null), _n(this, Hs, null), _n(this, FR, null), _n(this, Ru, null), _n(this, Cg, null), _n(this, jn, this.hot.rootDocument.createElement("DIV")), _n(this, Qr, this.hot.rootDocument.createElement("DIV")), _n(this, sl, !1), _n(this, Gm, !1), _n(this, bh, 0), _n(this, Rh, null), _n(this, pr, void 0), _n(this, Eh, void 0), Q(oe(jn, this), "manualRowResizer"), Q(oe(Qr, this), "manualRowResizerGuide");
  }
  /**
   * @private
   * @returns {string}
   */
  get inlineDir() {
    return this.hot.isRtl() ? "right" : "left";
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link ManualRowResize#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return this.hot.getSettings()[jm];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled || (Ut(pr, this, new Zs()), oe(pr, this).addLocalHook("init", () => Fo(As, this, joe).call(this)), this.hot.rowIndexMapper.registerMap(this.pluginName, oe(pr, this)), this.addHook("modifyRowHeight", (e, n) => Fo(As, this, Yoe).call(this, e, n)), this.bindEvents(), super.enablePlugin());
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`manualRowResize`](@/api/options.md#manualrowresize)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    Ut(Eh, this, oe(pr, this).getValues()), this.hot.rowIndexMapper.unregisterMap(this.pluginName), super.disablePlugin();
  }
  /**
   * Saves the current sizes using the persistentState plugin (the {@link Options#persistentState} option has to be
   * enabled).
   *
   * @fires Hooks#persistentStateSave
   */
  saveManualRowHeights() {
    this.hot.runHooks("persistentStateSave", rI, oe(pr, this).getValues());
  }
  /**
   * Loads the previously saved sizes using the persistentState plugin (the {@link Options#persistentState} option
   * has be enabled).
   *
   * @returns {Array}
   * @fires Hooks#persistentStateLoad
   */
  loadManualRowHeights() {
    const e = {};
    return this.hot.runHooks("persistentStateLoad", rI, e), e.value;
  }
  /**
   * Sets the new height for specified row index.
   *
   * @param {number} row Visual row index.
   * @param {number} height Row height.
   * @returns {number} Returns new height.
   */
  setManualSize(e, n) {
    const i = this.hot.toPhysicalRow(e), r = Math.max(n, xx);
    return oe(pr, this).setValueAtIndex(i, r), r;
  }
  /**
   * Returns the last desired row height set manually with the resize handle.
   *
   * @returns {number} The last desired row height.
   */
  getLastDesiredRowHeight() {
    return oe(Sh, this);
  }
  /**
   * Sets the resize handle position.
   *
   * @private
   * @param {HTMLCellElement} TH TH HTML element.
   */
  setupHandlePosition(e) {
    Ut(qr, this, e);
    const {
      view: n
    } = this.hot, {
      _wt: i
    } = n, r = i.wtTable.getCoords(oe(qr, this)), o = r.row;
    if (o < 0)
      return;
    const s = It(oe(qr, this)), a = oe(qr, this).getBoundingClientRect(), l = o < i.getSetting("fixedRowsTop"), u = o >= n.countNotHiddenRowIndexes(0, 1) - i.getSetting("fixedRowsBottom");
    let c;
    l ? c = i.wtOverlays.topInlineStartCornerOverlay.getRelativeCellPosition(oe(qr, this), r.row, r.col) : u && (c = i.wtOverlays.bottomInlineStartCornerOverlay.getRelativeCellPosition(oe(qr, this), r.row, r.col)), c || (c = i.wtOverlays.inlineStartOverlay.getRelativeCellPosition(oe(qr, this), r.row, r.col)), Ut(Ag, this, this.hot.rowIndexMapper.getVisualFromRenderableIndex(o)), Ut(Si, this, []);
    const h = this.hot.selection.isSelectedByCorner() || this.hot.selection.isSelectedByRowHeader();
    if (this.hot.selection.isSelected() && h) {
      const d = this.hot.getSelectedRange();
      T(d, (g) => {
        const m = g.getTopStartCorner().row, w = g.getBottomStartCorner().row;
        Ae(m, w, (C) => {
          oe(Si, this).includes(C) || oe(Si, this).push(C);
        });
      });
    }
    oe(Si, this).includes(oe(Ag, this)) || Ut(Si, this, [oe(Ag, this)]), Ut(Cg, this, c.top - 6), Ut(Ru, this, parseInt(a.height, 10)), oe(jn, this).style.top = `${oe(Cg, this) + oe(Ru, this)}px`, oe(jn, this).style[this.inlineDir] = `${c.start}px`, oe(jn, this).style.width = `${s}px`, this.hot.rootElement.appendChild(oe(jn, this));
  }
  /**
   * Refresh the resize handle position.
   *
   * @private
   */
  refreshHandlePosition() {
    oe(jn, this).style.top = `${oe(Cg, this) + oe(Sh, this)}px`;
  }
  /**
   * Sets the resize guide position.
   *
   * @private
   */
  setupGuidePosition() {
    const e = parseInt(It(oe(jn, this)), 10), n = parseInt(oe(jn, this).style[this.inlineDir], 10) + e, i = parseInt(this.hot.view.maximumVisibleElementWidth(0), 10);
    Q(oe(jn, this), "active"), Q(oe(Qr, this), "active"), oe(Qr, this).style.top = oe(jn, this).style.top, oe(Qr, this).style[this.inlineDir] = `${n}px`, oe(Qr, this).style.width = `${i - e}px`, this.hot.rootElement.appendChild(oe(Qr, this));
  }
  /**
   * Refresh the resize guide position.
   *
   * @private
   */
  refreshGuidePosition() {
    oe(Qr, this).style.top = oe(jn, this).style.top;
  }
  /**
   * Hides both the resize handle and resize guide.
   *
   * @private
   */
  hideHandleAndGuide() {
    xe(oe(jn, this), "active"), xe(oe(Qr, this), "active");
  }
  /**
   * Checks if provided element is considered as a row header.
   *
   * @private
   * @param {HTMLElement} element HTML element.
   * @returns {boolean}
   */
  checkIfRowHeader(e) {
    const n = js(e, ["TBODY"], this.hot.rootElement), {
      inlineStartOverlay: i,
      topInlineStartCornerOverlay: r,
      bottomInlineStartCornerOverlay: o
    } = this.hot.view._wt.wtOverlays;
    return [i.clone.wtTable.TBODY, r.clone.wtTable.TBODY, o.clone.wtTable.TBODY].includes(n);
  }
  /**
   * Gets the TH element from the provided element.
   *
   * @private
   * @param {HTMLElement} element HTML element.
   * @returns {HTMLElement}
   */
  getClosestTHParent(e) {
    return e.tagName !== "TABLE" ? e.tagName === "TH" ? e : this.getClosestTHParent(e.parentNode) : null;
  }
  /**
   * Returns the actual height for the provided row index.
   *
   * @private
   * @param {number} row Visual row index.
   * @returns {number} Actual row height.
   */
  getActualRowHeight(e) {
    const n = this.hot.view._wt.wtTable.getRowHeight(e);
    return n !== void 0 && oe(Hs, this) < n ? n : oe(Hs, this);
  }
  /**
   * Auto-size row after doubleclick - callback.
   *
   * @private
   * @fires Hooks#beforeRowResize
   * @fires Hooks#afterRowResize
   */
  afterMouseDownTimeout() {
    const e = () => {
      this.hot.forceFullRender = !0, this.hot.view.render(), this.hot.view.adjustElementsSize();
    }, n = (i, r) => {
      const o = this.hot.runHooks("beforeRowResize", this.getActualRowHeight(i), i, !0);
      o !== void 0 && Ut(Hs, this, o), this.setManualSize(i, oe(Hs, this)), this.hot.runHooks("afterRowResize", this.getActualRowHeight(i), i, !0), r && e();
    };
    oe(bh, this) >= 2 && (oe(Si, this).length > 1 ? (T(oe(Si, this), (r) => {
      n(r);
    }), e()) : T(oe(Si, this), (r) => {
      n(r, !0);
    })), Ut(bh, this, 0), Ut(Rh, this, null);
  }
  /**
   * 'mousedown' event callback.
   *
   * @param {MouseEvent} event The mouse event.
   */
  /**
   * Binds the mouse events.
   *
   * @private
   */
  bindEvents() {
    const {
      rootElement: e,
      rootWindow: n
    } = this.hot;
    this.eventManager.addEventListener(e, "mouseover", (i) => Fo(As, this, Foe).call(this, i)), this.eventManager.addEventListener(e, "mousedown", (i) => Fo(As, this, Voe).call(this, i)), this.eventManager.addEventListener(n, "mousemove", (i) => Fo(As, this, Woe).call(this, i)), this.eventManager.addEventListener(n, "mouseup", () => Fo(As, this, $oe).call(this)), this.eventManager.addEventListener(oe(jn, this), "contextmenu", () => Fo(As, this, Uoe).call(this));
  }
  /**
   * Modifies the provided row height, based on the plugin settings.
   *
   * @param {number} height Row height.
   * @param {number} row Visual row index.
   * @returns {number}
   */
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function Foe(t) {
  if (!ob(t.target) && oe(Gm, this) !== !0 && this.checkIfRowHeader(t.target)) {
    const e = this.getClosestTHParent(t.target);
    e && (oe(sl, this) || this.setupHandlePosition(e));
  }
}
function Voe(t) {
  Te(t.target, "manualRowResizer") && (this.setupHandlePosition(oe(qr, this)), this.setupGuidePosition(), Ut(sl, this, !0), oe(Rh, this) === null && (Ut(Rh, this, setTimeout(() => this.afterMouseDownTimeout(), 500)), this.hot._registerTimeout(oe(Rh, this))), Ut(bh, this, oe(bh, this) + 1), Ut(FR, this, t.pageY), Ut(Hs, this, oe(Ru, this)));
}
function Woe(t) {
  oe(sl, this) && (Ut(Sh, this, oe(Ru, this) + (t.pageY - oe(FR, this))), T(oe(Si, this), (e) => {
    Ut(Hs, this, this.setManualSize(e, oe(Sh, this)));
  }), this.refreshHandlePosition(), this.refreshGuidePosition());
}
function $oe() {
  const t = () => {
    this.hot.forceFullRender = !0, this.hot.view.render(), this.hot.view.adjustElementsSize();
  }, e = (n, i) => {
    this.hot.runHooks("beforeRowResize", this.getActualRowHeight(n), n, !1), i && t(), this.saveManualRowHeights(), this.hot.runHooks("afterRowResize", this.getActualRowHeight(n), n, !1);
  };
  oe(sl, this) && (this.hideHandleAndGuide(), Ut(sl, this, !1), oe(Hs, this) !== oe(Ru, this) && (oe(Si, this).length > 1 ? (T(oe(Si, this), (i) => {
    e(i);
  }), t()) : T(oe(Si, this), (i) => {
    e(i, !0);
  })), this.setupHandlePosition(oe(qr, this)));
}
function Uoe() {
  this.hideHandleAndGuide(), this.hot.rootElement.removeChild(oe(jn, this)), this.hot.rootElement.removeChild(oe(Qr, this)), Ut(sl, this, !1), Ut(Gm, this, !0), this.hot._registerImmediate(() => {
    Ut(Gm, this, !1);
  });
}
function Yoe(t, e) {
  let n = t;
  if (this.enabled) {
    const i = this.hot.toPhysicalRow(e), r = oe(pr, this).getValueAtIndex(i);
    this.hot.getSettings()[jm] && r && (n = r);
  }
  return n;
}
function joe() {
  const t = this.hot.getSettings()[jm], e = this.loadManualRowHeights();
  this.hot.batchExecution(() => {
    typeof e != "undefined" ? e.forEach((n, i) => {
      oe(pr, this).setValueAtIndex(i, n);
    }) : Array.isArray(t) ? (t.forEach((n, i) => {
      oe(pr, this).setValueAtIndex(i, n);
    }), Ut(Eh, this, t)) : t === !0 && Array.isArray(oe(Eh, this)) && oe(Eh, this).forEach((n, i) => {
      oe(pr, this).setValueAtIndex(i, n);
    });
  }, !0);
}
function Goe(t, e, n) {
  zoe(t, e), e.set(t, n);
}
function zoe(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function _a(t, e, n) {
  return (e = Koe(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Koe(t) {
  var e = Qoe(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Qoe(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function oI(t, e) {
  return t.get(T_(t, e));
}
function vg(t, e, n) {
  return t.set(T_(t, e), n), n;
}
function T_(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Na = /* @__PURE__ */ new WeakMap();
class Xoe {
  constructor(e, n, i, r, o, s) {
    _a(this, "row", void 0), _a(this, "col", void 0), _a(this, "rowspan", void 0), _a(this, "colspan", void 0), _a(this, "removed", !1), _a(this, "cellCoordsFactory", void 0), _a(this, "cellRangeFactory", void 0), Goe(this, Na, null), this.row = e, this.col = n, this.rowspan = i, this.colspan = r, this.cellCoordsFactory = o, this.cellRangeFactory = s;
  }
  /**
   * Get a warning message for when the declared merged cell data contains negative values.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */
  static NEGATIVE_VALUES_WARNING(e) {
    let {
      row: n,
      col: i,
      rowspan: r,
      colspan: o
    } = e;
    return mt`The merged cell declared with {row: ${n}, col: ${i},\x20
      rowspan: ${r}, colspan: ${o}} contains negative values, which is\x20
      not supported. It will not be added to the collection.`;
  }
  /**
   * Get a warning message for when the declared merged cell data contains values exceeding the table limits.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */
  static IS_OUT_OF_BOUNDS_WARNING(e) {
    let {
      row: n,
      col: i
    } = e;
    return mt`The merged cell declared at [${n}, ${i}] is positioned\x20
      (or positioned partially) outside of the table range. It was not added to the table, please fix your setup.`;
  }
  /**
   * Get a warning message for when the declared merged cell data represents a single cell.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */
  static IS_SINGLE_CELL(e) {
    let {
      row: n,
      col: i
    } = e;
    return mt`The merged cell declared at [${n}, ${i}] has both "rowspan"\x20
      and "colspan" declared as "1", which makes it a single cell. It cannot be added to the collection.`;
  }
  /**
   * Get a warning message for when the declared merged cell data contains "colspan" or "rowspan", that equals 0.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */
  static ZERO_SPAN_WARNING(e) {
    let {
      row: n,
      col: i
    } = e;
    return mt`The merged cell declared at [${n}, ${i}] has "rowspan"\x20
      or "colspan" declared as "0", which is not supported. It cannot be added to the collection.`;
  }
  /**
   * Check whether the values provided for a merged cell contain any negative values.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {boolean}
   */
  static containsNegativeValues(e) {
    let {
      row: n,
      col: i,
      rowspan: r,
      colspan: o
    } = e;
    return n < 0 || i < 0 || r < 0 || o < 0;
  }
  /**
   * Check whether the provided merged cell information object represents a single cell.
   *
   * @private
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {boolean}
   */
  static isSingleCell(e) {
    let {
      rowspan: n,
      colspan: i
    } = e;
    return i === 1 && n === 1;
  }
  /**
   * Check whether the provided merged cell information object contains a rowspan or colspan of 0.
   *
   * @private
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {boolean}
   */
  static containsZeroSpan(e) {
    let {
      rowspan: n,
      colspan: i
    } = e;
    return i === 0 || n === 0;
  }
  /**
   * Check whether the provided merged cell object is to be declared out of bounds of the table.
   *
   * @param {object} mergeCell Object containing the `row`, `col`, `rowspan` and `colspan` properties.
   * @param {number} rowCount Number of rows in the table.
   * @param {number} columnCount Number of rows in the table.
   * @returns {boolean}
   */
  static isOutOfBounds(e, n, i) {
    return e.row < 0 || e.col < 0 || e.row >= n || e.row + e.rowspan - 1 >= n || e.col >= i || e.col + e.colspan - 1 >= i;
  }
  /**
   * Sanitize (prevent from going outside the boundaries) the merged cell.
   *
   * @param {Core} hotInstance The Handsontable instance.
   */
  normalize(e) {
    const n = e.countRows(), i = e.countCols();
    this.row < 0 ? this.row = 0 : this.row > n - 1 && (this.row = n - 1), this.col < 0 ? this.col = 0 : this.col > i - 1 && (this.col = i - 1), this.row + this.rowspan > n - 1 && (this.rowspan = n - this.row), this.col + this.colspan > i - 1 && (this.colspan = i - this.col), vg(Na, this, null);
  }
  /**
   * Returns `true` if the provided coordinates are inside the merged cell.
   *
   * @param {number} row The row index.
   * @param {number} column The column index.
   * @returns {boolean}
   */
  includes(e, n) {
    return this.row <= e && this.col <= n && this.row + this.rowspan - 1 >= e && this.col + this.colspan - 1 >= n;
  }
  /**
   * Returns `true` if the provided `column` property is within the column span of the merged cell.
   *
   * @param {number} column The column index.
   * @returns {boolean}
   */
  includesHorizontally(e) {
    return this.col <= e && this.col + this.colspan - 1 >= e;
  }
  /**
   * Returns `true` if the provided `row` property is within the row span of the merged cell.
   *
   * @param {number} row Row index.
   * @returns {boolean}
   */
  includesVertically(e) {
    return this.row <= e && this.row + this.rowspan - 1 >= e;
  }
  /**
   * Shift (and possibly resize, if needed) the merged cell.
   *
   * @param {Array} shiftVector 2-element array containing the information on the shifting in the `x` and `y` axis.
   * @param {number} indexOfChange Index of the preceding change.
   * @returns {boolean} Returns `false` if the whole merged cell was removed.
   */
  shift(e, n) {
    const i = e[0] || e[1], r = n + Math.abs(e[0] || e[1]) - 1, o = e[0] ? "colspan" : "rowspan", s = e[0] ? "col" : "row", a = Math.min(n, r), l = Math.max(n, r), u = this[s], c = this[s] + this[o] - 1;
    if (u >= n && (this[s] += i), i > 0)
      n <= c && n > u && (this[o] += i);
    else if (i < 0) {
      if (a <= u && l >= c)
        return this.removed = !0, vg(Na, this, null), !1;
      if (u >= a && u <= l) {
        const h = l - u + 1, d = Math.abs(i) - h;
        this[s] -= d + i, this[o] -= h;
      } else if (u <= a && c >= l)
        this[o] += i;
      else if (u <= a && c >= a && c < l) {
        const h = c - a + 1;
        this[o] -= h;
      }
    }
    return vg(Na, this, null), !0;
  }
  /**
   * Check if the second provided merged cell is "farther" in the provided direction.
   *
   * @param {MergedCellCoords} mergedCell The merged cell to check.
   * @param {string} direction Drag direction.
   * @returns {boolean|null} `true` if the second provided merged cell is "farther".
   */
  isFarther(e, n) {
    return e ? n === "down" ? e.row + e.rowspan - 1 < this.row + this.rowspan - 1 : n === "up" ? e.row > this.row : n === "right" ? e.col + e.colspan - 1 < this.col + this.colspan - 1 : n === "left" ? e.col > this.col : null : !0;
  }
  /**
   * Get the bottom row index of the merged cell.
   *
   * @returns {number}
   */
  getLastRow() {
    return this.row + this.rowspan - 1;
  }
  /**
   * Get the rightmost column index of the merged cell.
   *
   * @returns {number}
   */
  getLastColumn() {
    return this.col + this.colspan - 1;
  }
  /**
   * Get the range coordinates of the merged cell.
   *
   * @returns {CellRange}
   */
  getRange() {
    return oI(Na, this) || vg(Na, this, this.cellRangeFactory(this.cellCoordsFactory(this.row, this.col), this.cellCoordsFactory(this.row, this.col), this.cellCoordsFactory(this.getLastRow(), this.getLastColumn()))), oI(Na, this);
  }
}
const Zr = Xoe;
function qoe(t, e) {
  Zoe(t, e), e.add(t);
}
function Zoe(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function yg(t, e, n) {
  return (e = Joe(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Joe(t) {
  var e = ese(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function ese(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Da(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Cs = /* @__PURE__ */ new WeakSet();
class zm {
  constructor(e) {
    qoe(this, Cs), yg(this, "plugin", void 0), yg(this, "mergedCells", []), yg(this, "mergedCellsMatrix", /* @__PURE__ */ new Map()), yg(this, "hot", void 0), this.plugin = e, this.hot = e.hot;
  }
  /**
   * Get a warning message for when the declared merged cell data overlaps already existing merged cells.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCell Object containing information
   * about the merged cells that was about to be added.
   * @returns {string}
   */
  static IS_OVERLAPPING_WARNING(e) {
    let {
      row: n,
      col: i
    } = e;
    return mt`The merged cell declared at [${n}, ${i}], overlaps\x20
      with the other declared merged cell. The overlapping merged cell was not added to the table, please\x20
      fix your setup.`;
  }
  /**
   * Get a merged cell from the container, based on the provided arguments. You can provide either the "starting coordinates"
   * of a merged cell, or any coordinates from the body of the merged cell.
   *
   * @param {number} row Row index.
   * @param {number} column Column index.
   * @returns {MergedCellCoords|boolean} Returns a wanted merged cell on success and `false` on failure.
   */
  get(e, n) {
    var i;
    return this.mergedCellsMatrix.has(e) && (i = this.mergedCellsMatrix.get(e).get(n)) !== null && i !== void 0 ? i : !1;
  }
  /**
   * Get the first-found merged cell containing the provided range.
   *
   * @param {CellRange|object} range The range to search merged cells for.
   * @returns {MergedCellCoords|boolean}
   */
  getByRange(e) {
    let n = !1;
    return T(this.mergedCells, (i) => i.row <= e.from.row && i.row + i.rowspan - 1 >= e.to.row && i.col <= e.from.col && i.col + i.colspan - 1 >= e.to.col ? (n = i, n) : !0), n;
  }
  /**
   * Filters merge cells objects provided by users from overlapping cells.
   *
   * @param {{ row: number, col: number, rowspan: number, colspan: number }} mergedCellsInfo The merged cell information object.
   * Has to contain `row`, `col`, `colspan` and `rowspan` properties.
   * @returns {Array<{ row: number, col: number, rowspan: number, colspan: number }>}
   */
  filterOverlappingMergeCells(e) {
    const n = /* @__PURE__ */ new Set();
    return this.mergedCells.forEach((r) => {
      const {
        row: o,
        col: s,
        colspan: a,
        rowspan: l
      } = r;
      for (let u = o; u < o + l; u++)
        for (let c = s; c < s + a; c++)
          n.add(`r${u},c${c}`);
    }), e.filter((r) => {
      const {
        row: o,
        col: s,
        colspan: a,
        rowspan: l
      } = r, u = /* @__PURE__ */ new Set();
      let c = !1;
      for (let h = o; h < o + l; h++) {
        for (let d = s; d < s + a; d++) {
          const g = `r${h},c${d}`;
          if (n.has(g)) {
            gt(zm.IS_OVERLAPPING_WARNING(r)), c = !0;
            break;
          }
          u.add(g);
        }
        if (c)
          break;
      }
      return c || n.add(...u), !c;
    });
  }
  /**
   * Get a merged cell contained in the provided range.
   *
   * @param {CellRange} range The range to search merged cells in.
   * @param {boolean} [countPartials=false] If set to `true`, all the merged cells overlapping the range will be taken into calculation.
   * @returns {MergedCellCoords[]} Array of found merged cells.
   */
  getWithinRange(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    const {
      row: i,
      col: r
    } = e.getTopStartCorner(), {
      row: o,
      col: s
    } = e.getBottomEndCorner(), a = [];
    for (let l = i; l <= o; l++)
      for (let u = r; u <= s; u++) {
        const c = this.get(l, u);
        c && (n || !n && c.row === l && c.col === u) && a.push(c);
      }
    return a;
  }
  /**
   * Add a merged cell to the container.
   *
   * @param {object} mergedCellInfo The merged cell information object. Has to contain `row`, `col`, `colspan` and `rowspan` properties.
   * @param {boolean} [auto=false] `true` if called internally by the plugin (usually in batch).
   * @returns {MergedCellCoords|boolean} Returns the new merged cell on success and `false` on failure.
   */
  add(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    const i = e.row, r = e.col, o = e.rowspan, s = e.colspan, a = new Zr(i, r, o, s, this.hot._createCellCoords, this.hot._createCellRange), l = this.get(i, r), u = n ? !1 : this.isOverlapping(a);
    return !l && !u ? (this.hot && a.normalize(this.hot), this.mergedCells.push(a), Da(Cs, this, sI).call(this, a), a) : (gt(zm.IS_OVERLAPPING_WARNING(a)), !1);
  }
  /**
   * Remove a merged cell from the container. You can provide either the "starting coordinates"
   * of a merged cell, or any coordinates from the body of the merged cell.
   *
   * @param {number} row Row index.
   * @param {number} column Column index.
   * @returns {MergedCellCoords|boolean} Returns the removed merged cell on success and `false` on failure.
   */
  remove(e, n) {
    const i = this.get(e, n), r = i ? this.mergedCells.indexOf(i) : -1;
    return i && r !== -1 ? (this.mergedCells.splice(r, 1), Da(Cs, this, tse).call(this, i), i) : !1;
  }
  /**
   * Clear all the merged cells.
   */
  clear() {
    T(this.mergedCells, (e) => {
      let {
        row: n,
        col: i,
        rowspan: r,
        colspan: o
      } = e;
      Ae(n, n + r, (s) => {
        Ae(i, i + o, (a) => {
          const l = this.hot.getCell(s, a);
          l && (l.removeAttribute("rowspan"), l.removeAttribute("colspan"), l.style.display = "");
        });
      });
    }), this.mergedCells.length = 0, this.mergedCellsMatrix = /* @__PURE__ */ new Map();
  }
  /**
   * Check if the provided merged cell overlaps with the others already added.
   *
   * @param {MergedCellCoords} mergedCell The merged cell to check against all others in the container.
   * @returns {boolean} `true` if the provided merged cell overlaps with the others, `false` otherwise.
   */
  isOverlapping(e) {
    const n = e.getRange();
    for (let i = 0; i < this.mergedCells.length; i++)
      if (this.mergedCells[i].getRange().overlaps(n))
        return !0;
    return !1;
  }
  /**
   * Check whether the provided row/col coordinates direct to a first not hidden cell within merge area.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @returns {boolean}
   */
  isFirstRenderableMergedCell(e, n) {
    const i = this.get(e, n);
    return i && this.hot.rowIndexMapper.getNearestNotHiddenIndex(i.row, 1) === e && this.hot.columnIndexMapper.getNearestNotHiddenIndex(i.col, 1) === n;
  }
  /**
   * Get the first renderable coords of the merged cell at the provided coordinates.
   *
   * @param {number} row Visual row index.
   * @param {number} column Visual column index.
   * @returns {CellCoords} A `CellCoords` object with the coordinates to the first renderable cell within the
   *                        merged cell.
   */
  getFirstRenderableCoords(e, n) {
    const i = this.get(e, n);
    if (!i || this.isFirstRenderableMergedCell(e, n))
      return this.hot._createCellCoords(e, n);
    const r = this.hot.rowIndexMapper.getNearestNotHiddenIndex(i.row, 1), o = this.hot.columnIndexMapper.getNearestNotHiddenIndex(i.col, 1);
    return this.hot._createCellCoords(r, o);
  }
  /**
   * Gets the start-most visual column index that do not intersect with other merged cells within the provided range.
   *
   * @param {CellRange} range The range to search within.
   * @param {number} visualColumnIndex The visual column index to start the search from.
   * @returns {number}
   */
  getStartMostColumnIndex(e, n) {
    const i = Da(Cs, this, Sg).call(this, e, "col", -1);
    let r = n;
    for (let o = 0; o < i.length; o++)
      if (i[o] <= n) {
        r = i[o];
        break;
      }
    return r;
  }
  /**
   * Gets the end-most visual column index that do not intersect with other merged cells within the provided range.
   *
   * @param {CellRange} range The range to search within.
   * @param {number} visualColumnIndex The visual column index to start the search from.
   * @returns {number}
   */
  getEndMostColumnIndex(e, n) {
    const i = Da(Cs, this, Sg).call(this, e, "col", 1);
    let r = n;
    for (let o = 0; o < i.length; o++)
      if (i[o] >= n) {
        r = i[o];
        break;
      }
    return r;
  }
  /**
   * Gets the top-most visual row index that do not intersect with other merged cells within the provided range.
   *
   * @param {CellRange} range The range to search within.
   * @param {number} visualRowIndex The visual row index to start the search from.
   * @returns {number}
   */
  getTopMostRowIndex(e, n) {
    const i = Da(Cs, this, Sg).call(this, e, "row", -1);
    let r = n;
    for (let o = 0; o < i.length; o++)
      if (i[o] <= n) {
        r = i[o];
        break;
      }
    return r;
  }
  /**
   * Gets the bottom-most visual row index that do not intersect with other merged cells within the provided range.
   *
   * @param {CellRange} range The range to search within.
   * @param {number} visualRowIndex The visual row index to start the search from.
   * @returns {number}
   */
  getBottomMostRowIndex(e, n) {
    const i = Da(Cs, this, Sg).call(this, e, "row", 1);
    let r = n;
    for (let o = 0; o < i.length; o++)
      if (i[o] >= n) {
        r = i[o];
        break;
      }
    return r;
  }
  /**
   * Shift the merged cell in the direction and by an offset defined in the arguments.
   *
   * @param {string} direction `right`, `left`, `up` or `down`.
   * @param {number} index Index where the change, which caused the shifting took place.
   * @param {number} count Number of rows/columns added/removed in the preceding action.
   */
  shiftCollections(e, n, i) {
    const r = [0, 0];
    switch (e) {
      case "right":
        r[0] += i;
        break;
      case "left":
        r[0] -= i;
        break;
      case "down":
        r[1] += i;
        break;
      case "up":
        r[1] -= i;
        break;
    }
    const o = [];
    this.mergedCells.forEach((s) => {
      s.shift(r, n), s.removed && o.push(s);
    }), o.forEach((s) => {
      this.mergedCells.splice(this.mergedCells.indexOf(s), 1);
    }), this.mergedCellsMatrix.clear(), this.mergedCells.forEach((s) => {
      Da(Cs, this, sI).call(this, s);
    });
  }
  /**
   * Adds a merged cell to the matrix.
   *
   * @param {MergedCellCoords} mergedCell The merged cell to add.
   */
}
function Sg(t, e) {
  let n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1;
  const i = /* @__PURE__ */ new Map(), r = n === 1 ? t.getTopStartCorner() : t.getBottomEndCorner(), o = n === 1 ? t.getBottomEndCorner() : t.getTopStartCorner();
  for (let s = r.row; n === 1 ? s <= o.row : s >= o.row; s += n)
    for (let a = r.col; n === 1 ? a <= o.col : a >= o.col; a += n) {
      const l = e === "row" ? s : a, u = this.get(s, a);
      let c = l;
      u && (c = n === 1 ? u[e] + u[`${e}span`] - 1 : u[e]), i.has(l) || i.set(l, /* @__PURE__ */ new Set()), i.get(l).add(c);
    }
  return Array.from(new Set(Array.from(i.entries()).filter((s) => {
    let [, a] = s;
    return a.size === 1;
  }).flatMap((s) => {
    let [, a] = s;
    return Array.from(a);
  })));
}
function sI(t) {
  for (let e = t.row; e < t.row + t.rowspan; e++)
    for (let n = t.col; n < t.col + t.colspan; n++)
      this.mergedCellsMatrix.has(e) || this.mergedCellsMatrix.set(e, /* @__PURE__ */ new Map()), this.mergedCellsMatrix.get(e).set(n, t);
}
function tse(t) {
  for (let e = t.row; e < t.row + t.rowspan; e++)
    for (let n = t.col; n < t.col + t.colspan; n++)
      this.mergedCellsMatrix.get(e).delete(n);
}
const nse = zm;
function Ev(t, e, n) {
  return (e = ise(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function ise(t) {
  var e = rse(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function rse(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class ose {
  constructor(e) {
    Ev(this, "plugin", void 0), Ev(this, "mergedCellsCollection", void 0), Ev(this, "currentFillData", null), this.plugin = e, this.mergedCellsCollection = this.plugin.mergedCellsCollection;
  }
  /**
   * Correct the provided selection area, so it's not selecting only a part of a merged cell.
   *
   * @param {Array} selectionArea The selection to correct.
   */
  correctSelectionAreaSize(e) {
    if (e[0] === e[2] && e[1] === e[3]) {
      const n = this.mergedCellsCollection.get(e[0], e[1]);
      n && (e[2] = e[0] + n.rowspan - 1, e[3] = e[1] + n.colspan - 1);
    }
  }
  /**
   * Get the direction of the autofill process.
   *
   * @param {Array} selectionArea The selection area.
   * @param {Array} finalArea The final area (base + drag).
   * @returns {string} `up`, `down`, `left` or `right`.
   */
  getDirection(e, n) {
    let i = null;
    return n[0] === e[0] && n[1] === e[1] && n[3] === e[3] ? i = "down" : n[2] === e[2] && n[1] === e[1] && n[3] === e[3] ? i = "up" : n[1] === e[1] && n[2] === e[2] ? i = "right" : i = "left", i;
  }
  /**
   * Snap the drag area to the farthest merged cell, so it won't clip any of the merged cells.
   *
   * @param {Array} baseArea The base selected area.
   * @param {Array} dragArea The drag area.
   * @param {string} dragDirection The autofill drag direction.
   * @param {Array} foundMergedCells MergeCellCoords found in the base selection area.
   * @returns {Array} The new drag area.
   */
  snapDragArea(e, n, i, r) {
    const o = n.slice(0), s = this.getAutofillSize(e, n, i), [a, l, u, c] = e, d = ["up", "down"].indexOf(i) > -1 ? u - a + 1 : c - l + 1, g = Math.floor(s / d) * d, m = s - g, w = this.getFarthestCollection(e, n, i, r);
    if (w) {
      if (i === "down") {
        const C = w.row + w.rowspan - a - m;
        o[2] + C >= this.plugin.hot.countRows() ? o[2] -= m : o[2] += m ? C : 0;
      } else if (i === "right") {
        const C = w.col + w.colspan - l - m;
        o[3] + C >= this.plugin.hot.countCols() ? o[3] -= m : o[3] += m ? C : 0;
      } else if (i === "up") {
        const C = u - m - w.row + 1;
        o[0] + C < 0 ? o[0] += m : o[0] -= m ? C : 0;
      } else if (i === "left") {
        const C = c - m - w.col + 1;
        o[1] + C < 0 ? o[1] += m : o[1] -= m ? C : 0;
      }
    }
    return this.updateCurrentFillCache({
      baseArea: e,
      dragDirection: i,
      foundMergedCells: r,
      fillSize: s,
      dragArea: o,
      cycleLength: d
    }), o;
  }
  /**
   * Update the current fill cache with the provided object.
   *
   * @private
   * @param {object} updateObject The current filled object cache.
   */
  updateCurrentFillCache(e) {
    this.currentFillData || (this.currentFillData = {}), nn(this.currentFillData, e);
  }
  /**
   * Get the "length" of the drag area.
   *
   * @private
   * @param {Array} baseArea The base selection area.
   * @param {Array} dragArea The drag area (containing the base area).
   * @param {string} direction The drag direction.
   * @returns {number|null} The "length" (height or width, depending on the direction) of the drag.
   */
  getAutofillSize(e, n, i) {
    const [r, o, s, a] = e, [l, u, c, h] = n;
    switch (i) {
      case "up":
        return r - l;
      case "down":
        return c - s;
      case "left":
        return o - u;
      case "right":
        return h - a;
      default:
        return null;
    }
  }
  /**
   * Trim the default drag area (containing the selection area) to the drag-only area.
   *
   * @private
   * @param {Array} baseArea The base selection area.
   * @param {Array} dragArea The base selection area extended by the drag area.
   * @param {string} direction Drag direction.
   * @returns {Array|null} Array representing the drag area coordinates.
   */
  getDragArea(e, n, i) {
    const [r, o, s, a] = e, [l, u, c, h] = n;
    switch (i) {
      case "up":
        return [l, u, r - 1, a];
      case "down":
        return [s + 1, o, c, a];
      case "left":
        return [l, u, s, o - 1];
      case "right":
        return [r, a + 1, c, h];
      default:
        return null;
    }
  }
  /**
   * Get the to-be-farthest merged cell in the newly filled area.
   *
   * @private
   * @param {Array} baseArea The base selection area.
   * @param {Array} dragArea The drag area (containing the base area).
   * @param {string} direction The drag direction.
   * @param {Array} mergedCellArray Array of the merged cells found in the base area.
   * @returns {MergedCellCoords|null}
   */
  getFarthestCollection(e, n, i, r) {
    const [o, s, a, l] = e, u = ["up", "down"].indexOf(i) > -1, c = u ? a : l, h = u ? o : s, d = this.getAutofillSize(e, n, i), g = u ? a - o + 1 : l - s + 1, m = Math.floor(d / g) * g, w = d - m;
    let C = null, S = null, b = null;
    switch (i) {
      case "up":
        C = "includesVertically", b = c - w + 1;
        break;
      case "left":
        C = "includesHorizontally", b = c - w + 1;
        break;
      case "down":
        C = "includesVertically", b = h + w - 1;
        break;
      case "right":
        C = "includesHorizontally", b = h + w - 1;
        break;
    }
    return T(r, (R) => {
      R[C](b) && R.isFarther(S, i) && (S = R);
    }), S;
  }
  /**
   * Recreate the merged cells after the autofill process.
   *
   * @param {Array} changes Changes made.
   */
  recreateAfterDataPopulation(e) {
    if (!this.currentFillData)
      return;
    const n = this.getRangeFromChanges(e), i = this.currentFillData.foundMergedCells, r = this.currentFillData.dragDirection, o = (u, c) => {
      switch (r) {
        case "up":
          return u.row - c >= n.from.row;
        case "down":
          return u.row + u.rowspan - 1 + c <= n.to.row;
        case "left":
          return u.col - c >= n.from.column;
        case "right":
          return u.col + u.colspan - 1 + c <= n.to.column;
        default:
          return null;
      }
    };
    let s = 0, a = null, l = 1;
    do
      for (let u = 0; u < i.length; u += 1) {
        if (a = i[u], s = l * this.currentFillData.cycleLength, o(a, s))
          switch (r) {
            case "up":
              this.plugin.mergedCellsCollection.add({
                row: a.row - s,
                rowspan: a.rowspan,
                col: a.col,
                colspan: a.colspan
              });
              break;
            case "down":
              this.plugin.mergedCellsCollection.add({
                row: a.row + s,
                rowspan: a.rowspan,
                col: a.col,
                colspan: a.colspan
              });
              break;
            case "left":
              this.plugin.mergedCellsCollection.add({
                row: a.row,
                rowspan: a.rowspan,
                col: a.col - s,
                colspan: a.colspan
              });
              break;
            case "right":
              this.plugin.mergedCellsCollection.add({
                row: a.row,
                rowspan: a.rowspan,
                col: a.col + s,
                colspan: a.colspan
              });
              break;
          }
        u === i.length - 1 && (l += 1);
      }
    while (o(a, s));
    this.currentFillData = null, this.plugin.hot.render(), i.length > 0 && this.plugin.ifChromeForceRepaint();
  }
  /**
   * Get the drag range from the changes made.
   *
   * @private
   * @param {Array} changes The changes made.
   * @returns {object} Object with `from` and `to` properties, both containing `row` and `column` keys.
   */
  getRangeFromChanges(e) {
    const n = {
      min: null,
      max: null
    }, i = {
      min: null,
      max: null
    };
    return T(e, (r) => {
      const o = r[0], s = this.plugin.hot.propToCol(r[1]);
      (n.min === null || o < n.min) && (n.min = o), (n.max === null || o > n.max) && (n.max = o), (i.min === null || s < i.min) && (i.min = s), (i.max === null || s > i.max) && (i.max = s);
    }), {
      from: {
        row: n.min,
        column: i.min
      },
      to: {
        row: n.max,
        column: i.max
      }
    };
  }
  /**
   * Check if the drag area contains any merged cells.
   *
   * @param {Array} baseArea The base selection area.
   * @param {Array} fullArea The base area extended by the drag area.
   * @param {string} direction Drag direction.
   * @returns {boolean}
   */
  dragAreaOverlapsCollections(e, n, i) {
    const r = this.getDragArea(e, n, i), [o, s, a, l] = r, u = this.plugin.hot._createCellCoords(o, s), c = this.plugin.hot._createCellCoords(a, l), h = this.plugin.hot._createCellRange(u, u, c);
    return this.mergedCellsCollection.getWithinRange(h, !0).length > 0;
  }
}
const sse = ose;
function Tv(t, e, n) {
  return (e = ase(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function ase(t) {
  var e = lse(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function lse(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class use {
  constructor(e) {
    Tv(this, "plugin", void 0), Tv(this, "hot", void 0), Tv(this, "fullySelectedMergedCellClassName", "fullySelectedMergedCell"), this.plugin = e, this.hot = e.hot;
  }
  /**
   * Generate an additional class name for the entirely-selected merged cells.
   *
   * @param {number} currentRow Visual row index of the currently processed cell.
   * @param {number} currentColumn Visual column index of the currently cell.
   * @param {Array} cornersOfSelection Array of the current selection in a form of `[startRow, startColumn, endRow, endColumn]`.
   * @param {number|undefined} layerLevel Number indicating which layer of selection is currently processed.
   * @returns {string|undefined} A `String`, which will act as an additional `className` to be added to the currently processed cell.
   */
  getSelectedMergedCellClassName(e, n, i, r) {
    const o = Math.min(i[0], i[2]), s = Math.min(i[1], i[3]), a = Math.max(i[0], i[2]), l = Math.max(i[1], i[3]);
    if (r === void 0 || !this.plugin.mergedCellsCollection.isFirstRenderableMergedCell(e, n))
      return;
    const c = this.plugin.mergedCellsCollection.get(e, n);
    if (!c)
      return;
    const h = c.getLastRow(), d = c.getLastColumn();
    if (o <= c.row && s <= c.col && a >= h && l >= d)
      return `${this.fullySelectedMergedCellClassName}-${r}`;
    if (this.isMergeCellFullySelected(c, this.plugin.hot.getSelectedRange()))
      return `${this.fullySelectedMergedCellClassName}-multiple`;
  }
  /**
   * Check if the provided merged cell is fully selected (by one or many layers of selection).
   *
   * @param {MergedCellCoords} mergedCell The merged cell to be processed.
   * @param {CellRange[]} selectionRangesArray Array of selection ranges.
   * @returns {boolean}
   */
  isMergeCellFullySelected(e, n) {
    const i = [];
    if (!n || !e)
      return !1;
    for (let r = 0; r < e.rowspan; r += 1)
      for (let o = 0; o < e.colspan; o += 1)
        i.push(this.hot._createCellCoords(e.row + r, e.col + o));
    for (let r = 0; r < i.length; r += 1) {
      const o = [];
      for (let s = 0; s < n.length; s += 1)
        o[s] = n[s].includes(i[r]);
      if (!o.includes(!0))
        return !1;
    }
    return !0;
  }
  /**
   * Generate an array of the entirely-selected merged cells' class names.
   *
   * @returns {string[]} An `Array` of `String`s. Each of these strings will act like class names to be removed from all the cells in the table.
   */
  getSelectedMergedCellClassNameToRemove() {
    const e = [];
    for (let n = 0; n <= 7; n += 1)
      e.push(`${this.fullySelectedMergedCellClassName}-${n}`);
    return e.push(`${this.fullySelectedMergedCellClassName}-multiple`), e;
  }
}
const cse = use;
function hse(t) {
  return {
    key: "mergeCells",
    name() {
      const e = this.getSelectedLast();
      if (e) {
        const n = t.mergedCellsCollection.get(e[0], e[1]);
        if (n.row === e[0] && n.col === e[1] && n.row + n.rowspan - 1 === e[2] && n.col + n.colspan - 1 === e[3])
          return this.getTranslatedPhrase(tw);
      }
      return this.getTranslatedPhrase(ew);
    },
    callback() {
      const e = this.getSelectedRangeLast();
      if (!e)
        return;
      e.setDirection(this.isRtl() ? "NE-SW" : "NW-SE");
      const {
        from: n,
        to: i
      } = e;
      t.toggleMerge(e), this.selectCell(n.row, n.col, i.row, i.col, !1);
    },
    disabled() {
      const e = this.getSelectedLast();
      return e ? Zr.isSingleCell({
        row: e[0],
        col: e[1],
        rowspan: e[2] - e[0] + 1,
        colspan: e[3] - e[1] + 1
      }) || this.selection.isSelectedByCorner() : !0;
    },
    hidden: !1
  };
}
function Th(t, e, n) {
  return (e = dse(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function dse(t) {
  var e = fse(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function fse(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class aI {
  constructor(e) {
    Th(this, "data", void 0), Th(this, "next", null), Th(this, "prev", null), this.data = e;
  }
}
class gse {
  constructor() {
    Th(this, "first", null), Th(this, "last", null);
  }
  /**
   * Add data to the end of linked list.
   *
   * @param {object} data Data which should be added.
   * @returns {NodeStructure} Returns the node which has been added.
   */
  push(e) {
    const n = new aI(e);
    if (this.first === null)
      this.first = n, this.last = n;
    else {
      const i = this.last;
      this.last = n, n.prev = i, i.next = n;
    }
    return n;
  }
  /**
   * Add data to the beginning of linked list.
   *
   * @param {object} data Data which should be added.
   */
  unshift(e) {
    const n = new aI(e);
    if (this.first === null)
      this.first = n, this.last = n;
    else {
      const i = this.first;
      this.first = n, n.next = i, i.prev = n;
    }
  }
  /**
   * In order traversal of the linked list.
   *
   * @param {Function} callback Callback which should be executed on each node.
   */
  inorder(e) {
    let n = this.first;
    for (; n; ) {
      const i = e(n);
      if (n === this.last || i === !0)
        break;
      n = n.next;
    }
  }
  /**
   * Remove data from the linked list.
   *
   * @param {object} data Data which should be removed.
   * @returns {boolean} Returns true if data has been removed.
   */
  remove(e) {
    if (this.first === null)
      return !1;
    let n = this.first, i, r;
    for (; n; ) {
      if (n.data === e)
        return i = n.next, r = n.prev, i && (i.prev = r), r && (r.next = i), n === this.first && (this.first = i), n === this.last && (this.last = r), !0;
      n = n.next;
    }
    return !1;
  }
  /**
   * Check if linked list contains cycle.
   *
   * @returns {boolean} Returns true if linked list contains cycle.
   */
  hasCycle() {
    let e = this.first, n = this.first;
    for (; ; ) {
      if (e === null || (e = e.next, e === null))
        return !1;
      if (e = e.next, n = n.next, e === n)
        return !0;
    }
  }
  /**
   * Return last node from the linked list.
   *
   * @returns {NodeStructure} Last node.
   */
  pop() {
    if (this.last === null)
      return null;
    const e = this.last;
    return this.last = this.last.prev, e;
  }
  /**
   * Return first node from the linked list.
   *
   * @returns {NodeStructure} First node.
   */
  shift() {
    if (this.first === null)
      return null;
    const e = this.first;
    return this.first = this.first.next, e;
  }
  /**
   * Reverses the linked list recursively.
   */
  recursiveReverse() {
    function e(i, r) {
      r && (e(r, r.next), r.next = i);
    }
    if (!this.first)
      return;
    e(this.first, this.first.next), this.first.next = null;
    const n = this.first;
    this.first = this.last, this.last = n;
  }
  /**
   * Reverses the linked list iteratively.
   */
  reverse() {
    if (!this.first || !this.first.next)
      return;
    let e = this.first.next, n = this.first, i;
    for (; e; )
      i = e.next, e.next = n, n.prev = e, n = e, e = i;
    this.first.next = null, this.last.prev = null, i = this.first, this.first = n, this.last = i;
  }
}
const bg = gse;
function mse(t, e) {
  M_(t, e), e.add(t);
}
function ka(t, e, n) {
  M_(t, e), e.set(t, n);
}
function M_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ht(t, e) {
  return t.get(Km(t, e));
}
function mi(t, e, n) {
  return t.set(Km(t, e), n), n;
}
function Km(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var Ur = /* @__PURE__ */ new WeakMap(), Yr = /* @__PURE__ */ new WeakMap(), jr = /* @__PURE__ */ new WeakMap(), Gr = /* @__PURE__ */ new WeakMap(), sS = /* @__PURE__ */ new WeakMap(), Rg = /* @__PURE__ */ new WeakMap(), Eg = /* @__PURE__ */ new WeakMap(), Mv = /* @__PURE__ */ new WeakSet();
class pse {
  constructor(e) {
    let {
      mergedCellsGetter: n,
      rowIndexMapper: i,
      columnIndexMapper: r
    } = e;
    mse(this, Mv), ka(this, Ur, new bg()), ka(this, Yr, new bg()), ka(this, jr, null), ka(this, Gr, null), ka(this, sS, null), ka(this, Rg, null), ka(this, Eg, null), mi(sS, this, n), mi(Rg, this, i), mi(Eg, this, r);
  }
  /**
   * Gets the currently selected node data from the vertical focus order list.
   *
   * @returns {NodeStructure}
   */
  getCurrentVerticalNode() {
    return ht(Gr, this).data;
  }
  /**
   * Gets the first node data from the vertical focus order list.
   *
   * @returns {NodeStructure}
   */
  getFirstVerticalNode() {
    return ht(Yr, this).first.data;
  }
  /**
   * Gets the next selected node data from the vertical focus order list.
   *
   * @returns {NodeStructure}
   */
  getNextVerticalNode() {
    return ht(Gr, this).next.data;
  }
  /**
   * Gets the previous selected node data from the vertical focus order list.
   *
   * @returns {NodeStructure}
   */
  getPrevVerticalNode() {
    return ht(Gr, this).prev.data;
  }
  /**
   * Gets the currently selected node data from the horizontal focus order list.
   *
   * @returns {NodeStructure}
   */
  getCurrentHorizontalNode() {
    return ht(jr, this).data;
  }
  /**
   * Gets the first node data from the horizontal focus order list.
   *
   * @returns {NodeStructure}
   */
  getFirstHorizontalNode() {
    return ht(Ur, this).first.data;
  }
  /**
   * Gets the next selected node data from the horizontal focus order list.
   *
   * @returns {NodeStructure}
   */
  getNextHorizontalNode() {
    return ht(jr, this).next.data;
  }
  /**
   * Gets the previous selected node data from the horizontal focus order list.
   *
   * @returns {NodeStructure}
   */
  getPrevHorizontalNode() {
    return ht(jr, this).prev.data;
  }
  /**
   * Sets the previous node from the vertical focus order list as active.
   */
  setPrevNodeAsActive() {
    mi(Gr, this, ht(Gr, this).prev), mi(jr, this, ht(jr, this).prev);
  }
  /**
   * Sets the previous node from the horizontal focus order list as active.
   */
  setNextNodeAsActive() {
    mi(Gr, this, ht(Gr, this).next), mi(jr, this, ht(jr, this).next);
  }
  /**
   * Rebuilds the focus order list based on the provided selection.
   *
   * @param {CellRange} selectedRange The selected range to build the focus order for.
   */
  buildFocusOrder(e) {
    const n = e.getTopStartCorner(), i = e.getBottomEndCorner(), r = /* @__PURE__ */ new WeakSet();
    mi(Ur, this, new bg());
    for (let s = n.row; s <= i.row; s++)
      if (!ht(Rg, this).isHidden(s))
        for (let a = n.col; a <= i.col; a++) {
          if (ht(Eg, this).isHidden(a))
            continue;
          const l = Km(Mv, this, lI).call(this, e, ht(Ur, this), r, s, a);
          l && mi(jr, this, l);
        }
    ht(Ur, this).first && (ht(Ur, this).first.prev = ht(Ur, this).last, ht(Ur, this).last.next = ht(Ur, this).first);
    const o = /* @__PURE__ */ new WeakSet();
    mi(Yr, this, new bg());
    for (let s = n.col; s <= i.col; s++)
      if (!ht(Eg, this).isHidden(s))
        for (let a = n.row; a <= i.row; a++) {
          if (ht(Rg, this).isHidden(a))
            continue;
          const l = Km(Mv, this, lI).call(this, e, ht(Yr, this), o, a, s);
          l && mi(Gr, this, l);
        }
    ht(Yr, this).first && (ht(Yr, this).first.prev = ht(Yr, this).last, ht(Yr, this).last.next = ht(Yr, this).first);
  }
  /**
   * Sets the active node based on the provided row and column.
   *
   * @param {number} row The visual row index.
   * @param {number} column The visual column index.
   * @returns {FocusOrder}
   */
  setActiveNode(e, n) {
    return ht(Ur, this).inorder((i) => {
      const {
        rowStart: r,
        rowEnd: o,
        colStart: s,
        colEnd: a
      } = i.data;
      if (e >= r && e <= o && n >= s && n <= a)
        return mi(jr, this, i), !1;
    }), ht(Yr, this).inorder((i) => {
      const {
        rowStart: r,
        rowEnd: o,
        colStart: s,
        colEnd: a
      } = i.data;
      if (e >= r && e <= o && n >= s && n <= a)
        return mi(Gr, this, i), !1;
    }), this;
  }
}
function lI(t, e, n, i, r) {
  const o = t.getTopStartCorner(), s = t.getBottomEndCorner(), a = t.highlight.clone().normalize(), l = ht(sS, this).call(this, i, r);
  if (l && n.has(l))
    return null;
  const u = {
    colStart: r,
    colEnd: r,
    rowStart: i,
    rowEnd: i
  };
  if (l) {
    if (n.add(l), l.row < o.row || l.row + l.rowspan - 1 > s.row || l.col < o.col || l.col + l.colspan - 1 > s.col)
      return null;
    u.colStart = l.col, u.colEnd = l.col + l.colspan - 1, u.rowStart = l.row, u.rowEnd = l.row + l.rowspan - 1;
  }
  const c = e.push(u);
  return i === a.row && r === a.col || l && a.row >= l.row && a.row <= l.row + l.rowspan - 1 && a.col >= l.col && a.col <= l.col + l.colspan - 1 ? c : null;
}
function wse(t) {
  const {
    hot: e
  } = t, {
    rowIndexMapper: n,
    columnIndexMapper: i
  } = e;
  function r() {
  }
  function o(s, a, l) {
    const u = t.mergedCellsCollection.get(a, l);
    if (!Be(u)) {
      s.removeAttribute("rowspan"), s.removeAttribute("colspan"), s.style.display = "";
      return;
    }
    const {
      row: c,
      col: h,
      colspan: d,
      rowspan: g
    } = u, [m, w] = t.translateMergedCellToRenderable(c, g, h, d), C = n.getRenderableFromVisualIndex(a), S = i.getRenderableFromVisualIndex(l), b = m - C + 1, R = w - S + 1, _ = n.getNearestNotHiddenIndex(c, 1), O = i.getNearestNotHiddenIndex(h, 1), $ = Math.min(g, b), U = Math.min(d, R);
    _ === a && O === l ? (s.setAttribute("rowspan", $), s.setAttribute("colspan", U)) : (s.removeAttribute("rowspan"), s.removeAttribute("colspan"), s.style.display = "none");
  }
  return {
    before: r,
    after: o
  };
}
function Ase(t, e) {
  I_(t, e), e.add(t);
}
function Tg(t, e, n) {
  I_(t, e), e.set(t, n);
}
function I_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Iv(t, e, n) {
  return (e = Cse(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Cse(t) {
  var e = vse(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function vse(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function od(t, e, n) {
  return t.set(dt(t, e), n), n;
}
function Tt(t, e) {
  return t.get(dt(t, e));
}
function dt(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
he.getSingleton().register("beforeMergeCells");
he.getSingleton().register("afterMergeCells");
he.getSingleton().register("beforeUnmergeCells");
he.getSingleton().register("afterUnmergeCells");
const Mh = "mergeCells", yse = 150, uI = Mh;
var Ui = /* @__PURE__ */ new WeakMap(), Yi = /* @__PURE__ */ new WeakMap(), ro = /* @__PURE__ */ new WeakMap(), cI = /* @__PURE__ */ new WeakMap(), At = /* @__PURE__ */ new WeakSet();
class Sse extends at {
  constructor() {
    super(...arguments), Ase(this, At), Iv(this, "mergedCellsCollection", null), Iv(this, "autofillCalculations", null), Iv(this, "selectionCalculations", null), Tg(this, Ui, null), Tg(this, Yi, {
      row: 0,
      col: 0
    }), Tg(this, ro, new pse({
      mergedCellsGetter: (e, n) => this.mergedCellsCollection.get(e, n),
      rowIndexMapper: this.hot.rowIndexMapper,
      columnIndexMapper: this.hot.columnIndexMapper
    })), Tg(this, cI, wse(this));
  }
  static get PLUGIN_KEY() {
    return Mh;
  }
  static get PLUGIN_PRIORITY() {
    return yse;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link MergeCells#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[Mh];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    this.enabled || (this.mergedCellsCollection = new nse(this), this.autofillCalculations = new sse(this), this.selectionCalculations = new cse(this), this.addHook("afterInit", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, bse).call(e, ...i);
    }), this.addHook("modifyTransformFocus", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Ese).call(e, ...i);
    }), this.addHook("modifyTransformStart", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Tse).call(e, ...i);
    }), this.addHook("modifyTransformEnd", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Mse).call(e, ...i);
    }), this.addHook("beforeSelectionHighlightSet", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Ise).call(e, ...i);
    }), this.addHook("beforeSetRangeStart", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, hI).call(e, ...i);
    }), this.addHook("beforeSetRangeStartOnly", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, hI).call(e, ...i);
    }), this.addHook("beforeSelectionFocusSet", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, xse).call(e, ...i);
    }), this.addHook("afterSelectionFocusSet", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Hse).call(e, ...i);
    }), this.addHook("afterSelectionEnd", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, _se).call(e, ...i);
    }), this.addHook("modifyGetCellCoords", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Ose).call(e, ...i);
    }), this.addHook("afterIsMultipleSelection", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Rse).call(e, ...i);
    }), this.addHook("afterRenderer", function() {
      return Tt(cI, e).after(...arguments);
    }), this.addHook("afterContextMenuDefaultOptions", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Pse).call(e, ...i);
    }), this.addHook("afterGetCellMeta", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Nse).call(e, ...i);
    }), this.addHook("afterViewportRowCalculatorOverride", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Dse).call(e, ...i);
    }), this.addHook("afterViewportColumnCalculatorOverride", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, kse).call(e, ...i);
    }), this.addHook("modifyAutofillRange", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Lse).call(e, ...i);
    }), this.addHook("afterCreateCol", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Bse).call(e, ...i);
    }), this.addHook("afterRemoveCol", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Fse).call(e, ...i);
    }), this.addHook("afterCreateRow", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Vse).call(e, ...i);
    }), this.addHook("afterRemoveRow", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Wse).call(e, ...i);
    }), this.addHook("afterChange", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, $se).call(e, ...i);
    }), this.addHook("beforeDrawBorders", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Use).call(e, ...i);
    }), this.addHook("afterDrawSelection", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Yse).call(e, ...i);
    }), this.addHook("beforeRemoveCellClassNames", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, jse).call(e, ...i);
    }), this.addHook("beforeBeginEditing", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, Gse).call(e, ...i);
    }), this.addHook("modifyRowHeightByOverlayName", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return dt(At, e, zse).call(e, ...i);
    }), this.addHook("beforeUndoStackChange", (n, i) => {
      if (i === "MergeCells")
        return !1;
    }), this.registerShortcuts(), super.enablePlugin());
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.clearCollections(), this.unregisterShortcuts(), this.hot.render(), super.disablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the
   * following configuration options:
   *  - [`mergeCells`](@/api/options.md#mergecells)
   */
  updatePlugin() {
    const e = this.hot.getSettings()[Mh];
    this.disablePlugin(), this.enablePlugin(), this.generateFromSettings(e), super.updatePlugin();
  }
  /**
   * If the browser is recognized as Chrome, force an additional repaint to prevent showing the effects of a Chrome bug.
   *
   * Issue described in https://github.com/handsontable/dev-handsontable/issues/521.
   *
   * @private
   */
  ifChromeForceRepaint() {
    if (!cb())
      return;
    const e = [];
    let n = [];
    this.mergedCellsCollection.mergedCells.forEach((i) => {
      const {
        row: r,
        rowspan: o
      } = i;
      for (let s = r + 1; s < r + o; s++)
        n.push(s);
    }), n = [...new Set(n)], n.forEach((i) => {
      const r = this.hot.rowIndexMapper.getRenderableFromVisualIndex(i);
      this.hot.view._wt.wtOverlays.getOverlays(!0).map((o) => (o == null ? void 0 : o.name) === "master" ? o : o.clone.wtTable).forEach((o) => {
        const s = o.getRow(r);
        s && (s.style.background = Eu(s, "backgroundColor").replace(")", ", 0.99)"), e.push(s));
      });
    }), this.hot._registerTimeout(() => {
      e.forEach((i) => {
        i.style.background = Eu(i, "backgroundColor").replace(", 0.99)", ")");
      });
    }, 1);
  }
  /**
   * Validates a single setting object, represented by a single merged cell information object.
   *
   * @private
   * @param {object} setting An object with `row`, `col`, `rowspan` and `colspan` properties.
   * @returns {boolean}
   */
  validateSetting(e) {
    return e ? Zr.containsNegativeValues(e) ? (gt(Zr.NEGATIVE_VALUES_WARNING(e)), !1) : Zr.isOutOfBounds(e, this.hot.countRows(), this.hot.countCols()) ? (gt(Zr.IS_OUT_OF_BOUNDS_WARNING(e)), !1) : Zr.isSingleCell(e) ? (gt(Zr.IS_SINGLE_CELL(e)), !1) : Zr.containsZeroSpan(e) ? (gt(Zr.ZERO_SPAN_WARNING(e)), !1) : !0 : !1;
  }
  /**
   * Generates the merged cells from the settings provided to the plugin.
   *
   * @private
   * @param {Array|boolean} settings The settings provided to the plugin.
   */
  generateFromSettings(e) {
    if (!Array.isArray(e))
      return;
    const n = e.filter((o) => this.validateSetting(o)), i = this.mergedCellsCollection.filterOverlappingMergeCells(n), r = [];
    i.forEach((o) => {
      const {
        row: s,
        col: a,
        rowspan: l,
        colspan: u
      } = o, c = this.hot._createCellCoords(s, a), h = this.hot._createCellCoords(s + l - 1, a + u - 1), d = this.hot._createCellRange(c, c, h);
      this.mergeRange(d, !0, !0);
      for (let g = s; g < s + l; g++)
        for (let m = a; m < a + u; m++)
          (g !== s || m !== a) && r.push([g, m, null]);
    }), r.length !== 0 && this.hot.setDataAtCell(r, void 0, void 0, this.pluginName);
  }
  /**
   * Clears the merged cells from the merged cell container.
   */
  clearCollections() {
    this.mergedCellsCollection.clear();
  }
  /**
   * Returns `true` if a range is mergeable.
   *
   * @private
   * @param {object} newMergedCellInfo Merged cell information object to test.
   * @param {boolean} [auto=false] `true` if triggered at initialization.
   * @returns {boolean}
   */
  canMergeRange(e) {
    return (arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1) ? !0 : this.validateSetting(e);
  }
  /**
   * Merges the selection provided as a cell range.
   *
   * @param {CellRange} [cellRange] Selection cell range.
   */
  mergeSelection() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.hot.getSelectedRangeLast();
    if (!e)
      return;
    e.setDirection(this.hot.isRtl() ? "NE-SW" : "NW-SE");
    const {
      from: n,
      to: i
    } = e;
    this.unmergeRange(e, !0), this.mergeRange(e), this.hot.selectCell(n.row, n.col, i.row, i.col, !1);
  }
  /**
   * Unmerges the selection provided as a cell range.
   *
   * @param {CellRange} [cellRange] Selection cell range.
   */
  unmergeSelection() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.hot.getSelectedRangeLast();
    if (!e)
      return;
    const {
      from: n,
      to: i
    } = e;
    this.unmergeRange(e, !0), this.hot.selectCell(n.row, n.col, i.row, i.col, !1);
  }
  /**
   * Merges cells in the provided cell range.
   *
   * @private
   * @param {CellRange} cellRange Cell range to merge.
   * @param {boolean} [auto=false] `true` if is called automatically, e.g. At initialization.
   * @param {boolean} [preventPopulation=false] `true`, if the method should not run `populateFromArray` at the end,
   *   but rather return its arguments.
   * @returns {Array|boolean} Returns an array of [row, column, dataUnderCollection] if preventPopulation is set to
   *   true. If the the merging process went successful, it returns `true`, otherwise - `false`.
   * @fires Hooks#beforeMergeCells
   * @fires Hooks#afterMergeCells
   */
  mergeRange(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    const r = e.getTopStartCorner(), o = e.getBottomEndCorner(), s = {
      row: r.row,
      col: r.col,
      rowspan: o.row - r.row + 1,
      colspan: o.col - r.col + 1
    }, a = [];
    let l = null;
    return this.canMergeRange(s, n) ? (this.hot.runHooks("beforeMergeCells", e, n), Ae(0, s.rowspan - 1, (c) => {
      Ae(0, s.colspan - 1, (h) => {
        let d = null;
        a[c] || (a[c] = []), c === 0 && h === 0 ? d = this.hot.getSourceDataAtCell(this.hot.toPhysicalRow(s.row), this.hot.toPhysicalColumn(s.col)) : this.hot.setCellMeta(s.row + c, s.col + h, "hidden", !0), a[c][h] = d;
      });
    }), this.hot.setCellMeta(s.row, s.col, "spanned", !0), this.mergedCellsCollection.add(s, n) ? (i ? l = [s.row, s.col, a] : this.hot.populateFromArray(s.row, s.col, a, void 0, void 0, this.pluginName), n || this.ifChromeForceRepaint(), this.hot.runHooks("afterMergeCells", e, s, n), l) : !0) : !1;
  }
  /**
   * Unmerges the selection provided as a cell range. If no cell range is provided, it uses the current selection.
   *
   * @private
   * @param {CellRange} cellRange Selection cell range.
   * @param {boolean} [auto=false] `true` if called automatically by the plugin.
   *
   * @fires Hooks#beforeUnmergeCells
   * @fires Hooks#afterUnmergeCells
   */
  unmergeRange(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    const i = this.mergedCellsCollection.getWithinRange(e);
    i.length !== 0 && (this.hot.runHooks("beforeUnmergeCells", e, n), T(i, (r) => {
      this.mergedCellsCollection.remove(r.row, r.col), Ae(0, r.rowspan - 1, (o) => {
        Ae(0, r.colspan - 1, (s) => {
          this.hot.removeCellMeta(r.row + o, r.col + s, "hidden"), this.hot.removeCellMeta(r.row + o, r.col + s, "copyable");
        });
      }), this.hot.removeCellMeta(r.row, r.col, "spanned");
    }), this.hot.runHooks("afterUnmergeCells", e, n), this.hot.render());
  }
  /**
   * Merges or unmerges, based on the cell range provided as `cellRange`.
   *
   * @private
   * @param {CellRange} cellRange The cell range to merge or unmerged.
   */
  toggleMerge(e) {
    const n = this.mergedCellsCollection.get(e.from.row, e.from.col);
    n.row === e.from.row && n.col === e.from.col && n.row + n.rowspan - 1 === e.to.row && n.col + n.colspan - 1 === e.to.col ? this.unmergeRange(e) : this.mergeSelection(e);
  }
  /**
   * Merges the specified range.
   *
   * @param {number} startRow Start row of the merged cell.
   * @param {number} startColumn Start column of the merged cell.
   * @param {number} endRow End row of the merged cell.
   * @param {number} endColumn End column of the merged cell.
   * @fires Hooks#beforeMergeCells
   * @fires Hooks#afterMergeCells
   */
  merge(e, n, i, r) {
    const o = this.hot._createCellCoords(e, n), s = this.hot._createCellCoords(i, r);
    this.mergeRange(this.hot._createCellRange(o, o, s));
  }
  /**
   * Unmerges the merged cell in the provided range.
   *
   * @param {number} startRow Start row of the merged cell.
   * @param {number} startColumn Start column of the merged cell.
   * @param {number} endRow End row of the merged cell.
   * @param {number} endColumn End column of the merged cell.
   * @fires Hooks#beforeUnmergeCells
   * @fires Hooks#afterUnmergeCells
   */
  unmerge(e, n, i, r) {
    const o = this.hot._createCellCoords(e, n), s = this.hot._createCellCoords(i, r);
    this.unmergeRange(this.hot._createCellRange(o, o, s));
  }
  /**
   * Register shortcuts responsible for toggling a merge.
   *
   * @private
   */
  registerShortcuts() {
    this.hot.getShortcutManager().getContext("grid").addShortcut({
      keys: [["Control", "m"]],
      callback: () => {
        const i = this.hot.getSelectedRangeLast();
        i && !i.isSingleHeader() && (this.toggleMerge(i), this.hot.render());
      },
      runOnlyIf: (i) => !i.altKey,
      // right ALT in some systems triggers ALT+CTRL
      group: uI
    });
  }
  /**
   * Unregister shortcuts responsible for toggling a merge.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("grid").removeShortcutsByGroup(uI);
  }
  /**
   * Modifies the information on whether the current selection contains multiple cells. The `afterIsMultipleSelection`
   * hook callback.
   *
   * @param {boolean} isMultiple Determines whether the current selection contains multiple cells.
   * @returns {boolean}
   */
  /**
   * Modify viewport start when needed. We extend viewport when merged cells aren't fully visible.
   *
   * @private
   * @param {object} calc The row calculator object.
   * @param {number} nrOfColumns Number of visual columns.
   */
  modifyViewportRowStart(e, n) {
    const i = this.hot.rowIndexMapper, r = i.getVisualFromRenderableIndex(e.startRow);
    for (let o = 0; o < n; o += 1) {
      const s = this.mergedCellsCollection.get(r, o);
      if (Be(s)) {
        const a = i.getRenderableFromVisualIndex(i.getNearestNotHiddenIndex(s.row, 1));
        if (a < e.startRow) {
          e.startRow = a, this.modifyViewportRowStart(e, n);
          return;
        }
      }
    }
  }
  /**
   *  Modify viewport end when needed. We extend viewport when merged cells aren't fully visible.
   *
   * @private
   * @param {object} calc The row calculator object.
   * @param {number} nrOfColumns Number of visual columns.
   */
  modifyViewportRowEnd(e, n) {
    const i = this.hot.rowIndexMapper, r = i.getVisualFromRenderableIndex(e.endRow);
    for (let o = 0; o < n; o += 1) {
      const s = this.mergedCellsCollection.get(r, o);
      if (Be(s)) {
        const a = s.row + s.rowspan - 1, l = i.getRenderableFromVisualIndex(i.getNearestNotHiddenIndex(a, -1));
        if (l > e.endRow) {
          e.endRow = l, this.modifyViewportRowEnd(e, n);
          return;
        }
      }
    }
  }
  /**
   * `afterViewportColumnCalculatorOverride` hook callback.
   *
   * @param {object} calc The column calculator object.
   */
  /**
   * Modify viewport start when needed. We extend viewport when merged cells aren't fully visible.
   *
   * @private
   * @param {object} calc The column calculator object.
   * @param {number} nrOfRows Number of visual rows.
   */
  modifyViewportColumnStart(e, n) {
    const i = this.hot.columnIndexMapper, r = i.getVisualFromRenderableIndex(e.startColumn);
    for (let o = 0; o < n; o += 1) {
      const s = this.mergedCellsCollection.get(o, r);
      if (Be(s)) {
        const a = i.getRenderableFromVisualIndex(i.getNearestNotHiddenIndex(s.col, 1));
        if (a < e.startColumn) {
          e.startColumn = a, this.modifyViewportColumnStart(e, n);
          return;
        }
      }
    }
  }
  /**
   *  Modify viewport end when needed. We extend viewport when merged cells aren't fully visible.
   *
   * @private
   * @param {object} calc The column calculator object.
   * @param {number} nrOfRows Number of visual rows.
   */
  modifyViewportColumnEnd(e, n) {
    const i = this.hot.columnIndexMapper, r = i.getVisualFromRenderableIndex(e.endColumn);
    for (let o = 0; o < n; o += 1) {
      const s = this.mergedCellsCollection.get(o, r);
      if (Be(s)) {
        const a = s.col + s.colspan - 1, l = i.getRenderableFromVisualIndex(i.getNearestNotHiddenIndex(a, -1));
        if (l > e.endColumn) {
          e.endColumn = l, this.modifyViewportColumnEnd(e, n);
          return;
        }
      }
    }
  }
  /**
   * Translates merged cell coordinates to renderable indexes.
   *
   * @private
   * @param {number} parentRow Visual row index.
   * @param {number} rowspan Rowspan which describes shift which will be applied to parent row
   *                         to calculate renderable index which points to the most bottom
   *                         index position. Pass rowspan as `0` to calculate the most top
   *                         index position.
   * @param {number} parentColumn Visual column index.
   * @param {number} colspan Colspan which describes shift which will be applied to parent column
   *                         to calculate renderable index which points to the most right
   *                         index position. Pass colspan as `0` to calculate the most left
   *                         index position.
   * @returns {number[]}
   */
  translateMergedCellToRenderable(e, n, i, r) {
    const {
      rowIndexMapper: o,
      columnIndexMapper: s
    } = this.hot;
    let a, l;
    n === 0 ? a = o.getNearestNotHiddenIndex(e, 1) : a = o.getNearestNotHiddenIndex(e + n - 1, -1), r === 0 ? l = s.getNearestNotHiddenIndex(i, 1) : l = s.getNearestNotHiddenIndex(i + r - 1, -1);
    const u = e >= 0 ? o.getRenderableFromVisualIndex(a) : e, c = i >= 0 ? s.getRenderableFromVisualIndex(l) : i;
    return [u, c];
  }
  /**
   * The `modifyAutofillRange` hook callback.
   *
   * @param {Array} drag The drag area coordinates.
   * @param {Array} select The selection information.
   * @returns {Array} The new drag area.
   */
}
function bse() {
  this.generateFromSettings(this.hot.getSettings()[Mh]), this.hot.render();
}
function Rse(t) {
  if (t) {
    const e = this.mergedCellsCollection.mergedCells, n = this.hot.getSelectedRangeLast(), i = n.getTopStartCorner(), r = n.getBottomEndCorner();
    for (let o = 0; o < e.length; o += 1)
      if (i.row === e[o].row && i.col === e[o].col && r.row === e[o].row + e[o].rowspan - 1 && r.col === e[o].col + e[o].colspan - 1)
        return !1;
  }
  return t;
}
function Ese(t) {
  Tt(Yi, this).row = t.row, Tt(Yi, this).col = t.col;
}
function Tse(t) {
  const e = this.hot.getSelectedRangeLast(), {
    highlight: n
  } = e, {
    columnIndexMapper: i,
    rowIndexMapper: r
  } = this.hot;
  Tt(Ui, this) && (r.getRenderableFromVisualIndex(Tt(Ui, this).row) !== null && (n.row = Tt(Ui, this).row), i.getRenderableFromVisualIndex(Tt(Ui, this).col) !== null && (n.col = Tt(Ui, this).col), od(Ui, this, null));
  const o = this.mergedCellsCollection.get(n.row, n.col);
  if (!o)
    return;
  const s = o.col, a = o.col + o.colspan - 1;
  if (t.col < 0) {
    const c = n.col >= s && n.col <= a ? s - 1 : a, h = i.getNearestNotHiddenIndex(c, -1);
    h === null ? t.col = -this.hot.view.countRenderableColumnsInRange(0, n.col) : t.col = -Math.max(this.hot.view.countRenderableColumnsInRange(h, n.col) - 1, 1);
  } else if (t.col > 0) {
    const c = n.col >= s && n.col <= a ? a + 1 : s, h = i.getNearestNotHiddenIndex(c, 1);
    h === null ? t.col = this.hot.view.countRenderableColumnsInRange(n.col, this.hot.countCols()) : t.col = Math.max(this.hot.view.countRenderableColumnsInRange(n.col, h) - 1, 1);
  }
  const l = o.row, u = o.row + o.rowspan - 1;
  if (t.row < 0) {
    const c = n.row >= l && n.row <= u ? l - 1 : u, h = r.getNearestNotHiddenIndex(c, -1);
    h === null ? t.row = -this.hot.view.countRenderableRowsInRange(0, n.row) : t.row = -Math.max(this.hot.view.countRenderableRowsInRange(h, n.row) - 1, 1);
  } else if (t.row > 0) {
    const c = n.row >= l && n.row <= u ? u + 1 : l, h = r.getNearestNotHiddenIndex(c, 1);
    h === null ? t.row = this.hot.view.countRenderableRowsInRange(n.row, this.hot.countRows()) : t.row = Math.max(this.hot.view.countRenderableRowsInRange(n.row, h) - 1, 1);
  }
}
function Mse(t) {
  const e = this.hot.getSelectedRangeLast(), n = e.clone(), {
    to: i
  } = e, {
    columnIndexMapper: r,
    rowIndexMapper: o
  } = this.hot, s = (a, l) => {
    n.expand(this.hot._createCellCoords(a, l));
    for (let u = 0; u < this.mergedCellsCollection.mergedCells.length; u += 1)
      n.expandByRange(this.mergedCellsCollection.mergedCells[u].getRange());
  };
  if (t.col < 0) {
    let a = this.mergedCellsCollection.getStartMostColumnIndex(e, i.col) + t.col;
    s(i.row, a), e.getHorizontalDirection() === "E-W" && n.getHorizontalDirection() === "E-W" && (a = n.getTopStartCorner().col);
    const l = r.getNearestNotHiddenIndex(a, 1);
    l !== null && (t.col = -Math.max(this.hot.view.countRenderableColumnsInRange(l, i.col) - 1, 1));
  } else if (t.col > 0) {
    let a = this.mergedCellsCollection.getEndMostColumnIndex(e, i.col) + t.col;
    s(i.row, a), e.getHorizontalDirection() === "W-E" && n.getHorizontalDirection() === "W-E" && (a = n.getBottomEndCorner().col);
    const l = r.getNearestNotHiddenIndex(a, -1);
    l !== null && (t.col = Math.max(this.hot.view.countRenderableColumnsInRange(i.col, l) - 1, 1));
  }
  if (t.row < 0) {
    let a = this.mergedCellsCollection.getTopMostRowIndex(e, i.row) + t.row;
    s(a, i.col), e.getVerticalDirection() === "S-N" && n.getVerticalDirection() === "S-N" && (a = n.getTopStartCorner().row);
    const l = o.getNearestNotHiddenIndex(a, 1);
    l !== null && (t.row = -Math.max(this.hot.view.countRenderableRowsInRange(l, i.row) - 1, 1));
  } else if (t.row > 0) {
    let a = this.mergedCellsCollection.getBottomMostRowIndex(e, i.row) + t.row;
    s(a, i.col), e.getVerticalDirection() === "N-S" && n.getVerticalDirection() === "N-S" && (a = n.getBottomStartCorner().row);
    const l = o.getNearestNotHiddenIndex(a, -1);
    l !== null && (t.row = Math.max(this.hot.view.countRenderableRowsInRange(i.row, l) - 1, 1));
  }
}
function Ise() {
  const t = this.hot.getSelectedRangeLast(), {
    highlight: e
  } = t;
  if (this.hot.selection.isSelectedByColumnHeader() || this.hot.selection.isSelectedByRowHeader()) {
    od(Ui, this, e.clone());
    return;
  }
  for (let i = 0; i < this.mergedCellsCollection.mergedCells.length; i += 1)
    t.expandByRange(this.mergedCellsCollection.mergedCells[i].getRange(), !1);
  for (let i = 0; i < this.mergedCellsCollection.mergedCells.length; i += 1)
    t.expandByRange(this.mergedCellsCollection.mergedCells[i].getRange(), !1);
  const n = this.mergedCellsCollection.get(e.row, e.col);
  od(Ui, this, e.clone()), n && e.assign(n);
}
function Ose(t, e) {
  if (t < 0 || e < 0)
    return;
  const n = this.mergedCellsCollection.get(t, e);
  if (!n)
    return;
  const {
    row: i,
    col: r,
    colspan: o,
    rowspan: s
  } = n;
  return [
    // Most top-left merged cell coords.
    i,
    r,
    // Most bottom-right merged cell coords.
    i + s - 1,
    r + o - 1
  ];
}
function Pse(t) {
  t.items.push({
    name: "---------"
  }, hse(this));
}
function hI() {
  od(Ui, this, null);
}
function xse() {
  if (Tt(Ui, this).isCell())
    return;
  const t = this.hot.getSelectedRangeLast(), e = t.getVerticalDirection(), n = t.getHorizontalDirection(), i = Tt(Ui, this).clone().normalize();
  Tt(ro, this).setActiveNode(i.row, i.col), Tt(Yi, this).row > 0 || Tt(Yi, this).col > 0 ? Tt(ro, this).setPrevNodeAsActive() : (n === "E-W" && Tt(Yi, this).col < 0 || e === "S-N" && Tt(Yi, this).row < 0) && Tt(ro, this).setNextNodeAsActive();
}
function Hse(t, e) {
  const n = this.hot.getSelectedRangeLast(), {
    columnIndexMapper: i,
    rowIndexMapper: r
  } = this.hot;
  let o = null, s = null;
  if (Tt(Yi, this).col < 0) {
    const {
      rowEnd: a,
      colEnd: l
    } = Tt(ro, this).getPrevHorizontalNode();
    s = i.getNearestNotHiddenIndex(l, -1), o = r.getNearestNotHiddenIndex(a, -1);
  } else if (Tt(Yi, this).col > 0) {
    const {
      rowStart: a,
      colStart: l
    } = Tt(ro, this).getNextHorizontalNode();
    s = i.getNearestNotHiddenIndex(l, 1), o = r.getNearestNotHiddenIndex(a, 1);
  } else if (Tt(Yi, this).row < 0) {
    const {
      rowEnd: a,
      colEnd: l
    } = Tt(ro, this).getPrevVerticalNode();
    s = i.getNearestNotHiddenIndex(l, -1), o = r.getNearestNotHiddenIndex(a, -1);
  } else if (Tt(Yi, this).row > 0) {
    const {
      rowStart: a,
      colStart: l
    } = Tt(ro, this).getNextVerticalNode();
    s = i.getNearestNotHiddenIndex(l, 1), o = r.getNearestNotHiddenIndex(a, 1);
  }
  if (o !== null || s !== null) {
    const a = this.hot._createCellCoords(o, s), l = this.mergedCellsCollection.get(a.row, a.col), u = this.hot.selection.highlight.getFocus();
    t = a.row, e = a.col, l ? n.highlight.assign({
      row: this.hot.rowIndexMapper.getNearestNotHiddenIndex(l.row, 1),
      col: this.hot.columnIndexMapper.getNearestNotHiddenIndex(l.col, 1)
    }) : n.highlight.assign(a), u.clear(), u.add(a).commit();
  }
  Tt(ro, this).setActiveNode(t, e), od(Yi, this, {
    row: 0,
    col: 0
  });
}
function _se() {
  this.hot.getSelectedRangeLast().isHeader() || Tt(ro, this).buildFocusOrder(this.hot.getSelectedRangeLast());
}
function Nse(t, e, n) {
  const i = this.mergedCellsCollection.get(t, e);
  i && (i.row !== t || i.col !== e ? n.copyable = !1 : (n.rowspan = i.rowspan, n.colspan = i.colspan));
}
function Dse(t) {
  const e = this.hot.countCols();
  this.modifyViewportRowStart(t, e), this.modifyViewportRowEnd(t, e);
}
function kse(t) {
  const e = this.hot.countRows();
  this.modifyViewportColumnStart(t, e), this.modifyViewportColumnEnd(t, e);
}
function Lse(t, e) {
  this.autofillCalculations.correctSelectionAreaSize(e);
  const n = this.autofillCalculations.getDirection(e, t);
  let i = t;
  if (this.autofillCalculations.dragAreaOverlapsCollections(e, i, n))
    return i = e, i;
  const r = this.hot._createCellCoords(e[0], e[1]), o = this.hot._createCellCoords(e[2], e[3]), s = this.hot._createCellRange(r, r, o), a = this.mergedCellsCollection.getWithinRange(s);
  return a.length === 0 || (i = this.autofillCalculations.snapDragArea(e, i, n, a)), i;
}
function Bse(t, e) {
  this.mergedCellsCollection.shiftCollections("right", t, e);
}
function Fse(t, e) {
  this.mergedCellsCollection.shiftCollections("left", t, e);
}
function Vse(t, e, n) {
  n !== "auto" && this.mergedCellsCollection.shiftCollections("down", t, e);
}
function Wse(t, e) {
  this.mergedCellsCollection.shiftCollections("up", t, e);
}
function $se(t, e) {
  e === "Autofill.fill" && this.autofillCalculations.recreateAfterDataPopulation(t);
}
function Use(t, e) {
  if (e && e === "area") {
    const n = this.hot.getSelectedRangeLast(), i = this.mergedCellsCollection.getWithinRange(n);
    T(i, (r) => {
      n.getBottomEndCorner().row === r.getLastRow() && n.getBottomEndCorner().col === r.getLastColumn() && (t[2] = r.row, t[3] = r.col);
    });
  }
}
function Yse(t, e, n, i) {
  if (n)
    return this.selectionCalculations.getSelectedMergedCellClassName(t, e, n, i);
}
function jse() {
  return this.selectionCalculations.getSelectedMergedCellClassNameToRemove();
}
function Gse(t, e, n, i) {
  if (!(i instanceof MouseEvent))
    return;
  const r = this.hot.getSelectedRangeLast(), o = this.mergedCellsCollection.getByRange(r);
  if (!o)
    return;
  const s = this.hot._createCellCoords(o.row, o.col), a = this.hot._createCellCoords(o.row + o.rowspan - 1, o.col + o.colspan - 1);
  return this.hot.selection.getLayerLevel() === 0 && r.isEqual(this.hot._createCellRange(s, s, a));
}
function zse(t, e, n) {
  if (this.hot.getSettings().rowHeaders || // merged cells do not work with the bottom overlays
  n === "bottom" || n === "bottom_inline_start_corner")
    return t;
  let i, r;
  if (n === "master")
    i = this.hot.view.getFirstRenderedVisibleColumn(), r = this.hot.view.getLastRenderedVisibleColumn();
  else {
    const d = this.hot.view.getOverlayByName(n);
    i = this.hot.columnIndexMapper.getVisualFromRenderableIndex(d.clone.wtTable.getFirstRenderedColumn()), r = this.hot.columnIndexMapper.getVisualFromRenderableIndex(d.clone.wtTable.getLastRenderedColumn());
  }
  if (!this.mergedCellsCollection.get(e, i))
    return t;
  const s = this.hot._createCellCoords(e, i), a = this.hot._createCellCoords(e, r), l = this.hot._createCellRange(s, s, a), u = this.mergedCellsCollection.getWithinRange(l), c = u.reduce((d, g) => {
    let {
      rowspan: m
    } = g;
    return Math.max(d, m);
  }, 1);
  let h = 0;
  return u.length > 1 && u[0].rowspan < c && (h = c - u[0].rowspan), u.forEach((d) => {
    var g;
    let {
      rowspan: m
    } = d, w = 0;
    n === "top" || n === "top_inline_start_corner" ? w = Math.min(c, this.hot.view.countNotHiddenFixedRowsTop() - e) : w = m - h, t = Math.max((g = t) !== null && g !== void 0 ? g : 0, dt(At, this, Kse).call(this, e, w));
  }), t;
}
function Kse(t, e) {
  const n = this.hot.view._wt.wtSettings.getSettingPure("defaultRowHeight"), i = this.hot.getPlugin("autoRowSize");
  let r = 0;
  for (let s = t; s < t + e; s++)
    if (!this.hot.rowIndexMapper.isHidden(s)) {
      var o;
      r += (o = i == null ? void 0 : i.getRowHeight(s)) !== null && o !== void 0 ? o : n;
    }
  return r;
}
function Qse(t, e) {
  return function(n, i) {
    const [, ...r] = n, [, ...o] = i;
    return function s(a) {
      const l = t[a], u = e[a], c = r[a], h = o[a], d = u.multiColumnSorting, m = (d.compareFunctionFactory ? d.compareFunctionFactory : iH(u.type))(l, u, d)(c, h);
      if (m === ao) {
        const w = a + 1;
        if (typeof e[w] != "undefined")
          return s(w);
      }
      return m;
    }(0);
  };
}
function dI() {
  gt(mt`Plugins \`columnSorting\` and \`multiColumnSorting\` should not be enabled simultaneously. 
    Only \`multiColumnSorting\` will work. The \`columnSorting\` plugin will be disabled.`);
}
const O_ = "sort";
function Xse(t, e, n) {
  const i = [];
  return n === !1 || t.isColumnSorted(e) && t.getNumberOfSortedColumns() > 1 && i.push(`${O_}-${t.getIndexOfColumnInSortQueue(e) + 1}`), i;
}
function qse(t) {
  const e = t.className.split(" "), n = new RegExp(`^${O_}-[0-9]{1,2}$`);
  return e.filter((i) => n.test(i));
}
function Zse(t, e, n) {
  return (e = Jse(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Jse(t) {
  var e = eae(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function eae(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const Qm = "multiColumnSorting", tae = 170, Ov = "columnSorting", fI = Qm;
nH(Qm, Qse);
class nae extends dH {
  constructor() {
    super(...arguments), Zse(this, "pluginKey", Qm);
  }
  static get PLUGIN_KEY() {
    return Qm;
  }
  static get PLUGIN_PRIORITY() {
    return tae;
  }
  /**
   * Checks if the plugin is enabled in the Handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link MultiColumnSorting#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return super.isEnabled();
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    !this.enabled && this.hot.getSettings()[this.pluginKey] && this.hot.getSettings()[Ov] && (dI(), this.hot.getPlugin(Ov).disablePlugin()), super.enablePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    super.disablePlugin();
  }
  /**
   * Register shortcuts responsible for toggling column sorting functionality.
   *
   * @private
   */
  registerShortcuts() {
    super.registerShortcuts(), this.hot.getShortcutManager().getContext("grid").addShortcut({
      keys: [["Shift", "Enter"]],
      callback: () => {
        const {
          highlight: e
        } = this.hot.getSelectedRangeLast();
        return e.row === -1 && e.col >= 0 && this.sort(this.getNextSortConfig(e.col, xm)), !1;
      },
      runOnlyIf: () => {
        var e, n;
        const i = (e = this.hot.getSelectedRangeLast()) === null || e === void 0 ? void 0 : e.highlight;
        return i && ((n = this.hot.getSelectedRangeLast()) === null || n === void 0 ? void 0 : n.isSingle()) && this.hot.selection.isCellVisible(i) && i.isHeader();
      },
      relativeToGroup: Js,
      position: "before",
      group: fI
    });
  }
  /**
   * Unregister shortcuts responsible for toggling column sorting functionality.
   *
   * @private
   */
  unregisterShortcuts() {
    super.unregisterShortcuts(), this.hot.getShortcutManager().getContext("grid").removeShortcutsByGroup(fI);
  }
  /**
   * Sorts the table by chosen columns and orders.
   *
   * @param {undefined|object|Array} sortConfig Single column sort configuration or full sort configuration (for all sorted columns).
   * The configuration object contains `column` and `sortOrder` properties. First of them contains visual column index, the second one contains
   * sort order (`asc` for ascending, `desc` for descending).
   *
   * **Note**: Please keep in mind that every call of `sort` function set an entirely new sort order. Previous sort configs aren't preserved.
   *
   * @example
   * ```js
   * // sort ascending first visual column
   * hot.getPlugin('multiColumnSorting').sort({ column: 0, sortOrder: 'asc' });
   *
   * // sort first two visual column in the defined sequence
   * hot.getPlugin('multiColumnSorting').sort([{
   *   column: 1, sortOrder: 'asc'
   * }, {
   *   column: 0, sortOrder: 'desc'
   * }]);
   * ```
   *
   * @fires Hooks#beforeColumnSort
   * @fires Hooks#afterColumnSort
   */
  sort(e) {
    super.sort(e);
  }
  /**
   * Clear the sort performed on the table.
   */
  clearSort() {
    super.clearSort();
  }
  /**
   * Checks if the table is sorted (any column have to be sorted).
   *
   * @returns {boolean}
   */
  isSorted() {
    return super.isSorted();
  }
  /**
   * Get sort configuration for particular column or for all sorted columns. Objects contain `column` and `sortOrder` properties.
   *
   * **Note**: Please keep in mind that returned objects expose **visual** column index under the `column` key. They are handled by the `sort` function.
   *
   * @param {number} [column] Visual column index.
   * @returns {undefined|object|Array}
   */
  getSortConfig(e) {
    return super.getSortConfig(e);
  }
  /**
   * @description
   * Warn: Useful mainly for providing server side sort implementation (see in the example below). It doesn't sort the data set. It just sets sort configuration for all sorted columns.
   * Note: Please keep in mind that this method doesn't re-render the table.
   *
   * @example
   * ```js
   * beforeColumnSort: function(currentSortConfig, destinationSortConfigs) {
   *   const columnSortPlugin = this.getPlugin('multiColumnSorting');
   *
   *   columnSortPlugin.setSortConfig(destinationSortConfigs);
   *
   *   // const newData = ... // Calculated data set, ie. from an AJAX call.
   *
   *   this.loadData(newData); // Load new data set and re-render the table.
   *
   *   return false; // The blockade for the default sort action.
   * }
   * ```
   *
   * @param {undefined|object|Array} sortConfig Single column sort configuration or full sort configuration (for all sorted columns).
   * The configuration object contains `column` and `sortOrder` properties. First of them contains visual column index, the second one contains
   * sort order (`asc` for ascending, `desc` for descending).
   */
  setSortConfig(e) {
    super.setSortConfig(e);
  }
  /**
   * Get normalized sort configs.
   *
   * @private
   * @param {object|Array} [sortConfig=[]] Single column sort configuration or full sort configuration (for all sorted columns).
   * The configuration object contains `column` and `sortOrder` properties. First of them contains visual column index, the second one contains
   * sort order (`asc` for ascending, `desc` for descending).
   * @returns {Array}
   */
  getNormalizedSortConfigs() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : [];
    return Array.isArray(e) ? e : [e];
  }
  /**
   * Update header classes.
   *
   * @private
   * @param {HTMLElement} headerSpanElement Header span element.
   * @param {...*} args Extra arguments for helpers.
   */
  updateHeaderClasses(e) {
    for (var n = arguments.length, i = new Array(n > 1 ? n - 1 : 0), r = 1; r < n; r++)
      i[r - 1] = arguments[r];
    super.updateHeaderClasses(e, ...i), xe(e, qse(e)), this.enabled !== !1 && Q(e, Xse(...i));
  }
  /**
   * Overwriting base plugin's `onUpdateSettings` method. Please keep in mind that `onAfterUpdateSettings` isn't called
   * for `updateSettings` in specific situations.
   *
   * @private
   * @param {object} newSettings New settings object.
   */
  onUpdateSettings(e) {
    this.hot.getSettings()[this.pluginKey] && this.hot.getSettings()[Ov] && dI(), super.onUpdateSettings(e);
  }
  /**
   * Callback for the `onAfterOnCellMouseDown` hook.
   *
   * @private
   * @param {Event} event Event which are provided by hook.
   * @param {CellCoords} coords Visual coords of the selected cell.
   */
  onAfterOnCellMouseDown(e, n) {
    TR(n.row, n.col, e) !== !1 && this.wasClickableHeaderClicked(e, n.col) && (this.hot.getShortcutManager().isCtrlPressed() ? (this.hot.deselectCell(), this.hot.selectColumns(n.col), this.sort(this.getNextSortConfig(n.col, xm))) : this.sort(this.getColumnNextConfig(n.col)));
  }
}
function gI(t, e, n) {
  return (e = iae(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function iae(t) {
  var e = rae(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function rae(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const oae = "multipleSelectionHandles", sae = 160;
class aae extends at {
  constructor() {
    super(...arguments), gI(this, "dragged", []), gI(this, "lastSetCell", null);
  }
  static get PLUGIN_KEY() {
    return oae;
  }
  static get PLUGIN_PRIORITY() {
    return sae;
  }
  /**
   * Check if the plugin is enabled in the handsontable settings.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return Yo();
  }
  /**
   * Enable plugin for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled || (this.registerListeners(), super.enablePlugin());
  }
  /**
   * Bind the touch events.
   *
   * @private
   */
  registerListeners() {
    const e = this, {
      rootElement: n
    } = this.hot;
    function i(r) {
      if (e.dragged.length === 1)
        return e.dragged.splice(0, e.dragged.length), !0;
      const o = e.dragged.indexOf(r);
      if (o === -1)
        return !1;
      o === 0 ? e.dragged = e.dragged.slice(0, 1) : o === 1 && (e.dragged = e.dragged.slice(-1));
    }
    this.eventManager.addEventListener(n, "touchstart", (r) => {
      let o;
      if (Te(r.target, "topSelectionHandle-HitArea"))
        return o = e.hot.getSelectedRangeLast(), e.dragged.push("top"), e.touchStartRange = {
          width: o.getWidth(),
          height: o.getHeight(),
          direction: o.getDirection()
        }, r.preventDefault(), !1;
      if (Te(r.target, "bottomSelectionHandle-HitArea"))
        return o = e.hot.getSelectedRangeLast(), e.dragged.push("bottom"), e.touchStartRange = {
          width: o.getWidth(),
          height: o.getHeight(),
          direction: o.getDirection()
        }, r.preventDefault(), !1;
    }), this.eventManager.addEventListener(n, "touchend", (r) => {
      if (Te(r.target, "topSelectionHandle-HitArea"))
        return i.call(e, "top"), e.touchStartRange = void 0, r.preventDefault(), !1;
      if (Te(r.target, "bottomSelectionHandle-HitArea"))
        return i.call(e, "bottom"), e.touchStartRange = void 0, r.preventDefault(), !1;
    }), this.eventManager.addEventListener(n, "touchmove", (r) => {
      const {
        rootDocument: o
      } = this.hot;
      let s, a, l, u, c, h;
      if (e.dragged.length === 0)
        return;
      const d = o.elementFromPoint(r.touches[0].clientX, r.touches[0].clientY);
      !d || d === e.lastSetCell || ((d.nodeName === "TD" || d.nodeName === "TH") && (s = e.hot.getCoords(d), s.col === -1 && (s.col = 0), a = e.hot.getSelectedRangeLast(), l = a.getWidth(), u = a.getHeight(), c = a.getDirection(), l === 1 && u === 1 && e.hot.selection.setRangeEnd(s), h = e.getCurrentRangeCoords(a, s, e.touchStartRange.direction, c, e.dragged[0]), h.start !== null && e.hot.selection.setRangeStart(h.start), e.hot.selection.setRangeEnd(h.end), e.lastSetCell = d), r.preventDefault());
    });
  }
  getCurrentRangeCoords(e, n, i, r, o) {
    const s = e.getTopStartCorner(), a = e.getBottomEndCorner(), l = e.getBottomStartCorner(), u = e.getTopEndCorner();
    let c = {
      start: null,
      end: null
    };
    switch (i) {
      case "NE-SW":
        switch (r) {
          case "NE-SW":
          case "NW-SE":
            o === "top" ? c = {
              start: this.hot._createCellCoords(n.row, e.highlight.col),
              end: this.hot._createCellCoords(l.row, n.col)
            } : c = {
              start: this.hot._createCellCoords(e.highlight.row, n.col),
              end: this.hot._createCellCoords(n.row, s.col)
            };
            break;
          case "SE-NW":
            o === "bottom" && (c = {
              start: this.hot._createCellCoords(a.row, n.col),
              end: this.hot._createCellCoords(n.row, s.col)
            });
            break;
        }
        break;
      case "NW-SE":
        switch (r) {
          case "NE-SW":
            o === "top" ? c = {
              start: n,
              end: l
            } : c.end = n;
            break;
          case "NW-SE":
            o === "top" ? c = {
              start: n,
              end: a
            } : c.end = n;
            break;
          case "SE-NW":
            o === "top" ? c = {
              start: n,
              end: s
            } : c.end = n;
            break;
          case "SW-NE":
            o === "top" ? c = {
              start: n,
              end: u
            } : c.end = n;
            break;
        }
        break;
      case "SW-NE":
        switch (r) {
          case "NW-SE":
            o === "bottom" ? c = {
              start: this.hot._createCellCoords(n.row, s.col),
              end: this.hot._createCellCoords(l.row, n.col)
            } : c = {
              start: this.hot._createCellCoords(s.row, n.col),
              end: this.hot._createCellCoords(n.row, a.col)
            };
            break;
          case "SW-NE":
            o === "top" ? c = {
              start: this.hot._createCellCoords(e.highlight.row, n.col),
              end: this.hot._createCellCoords(n.row, a.col)
            } : c = {
              start: this.hot._createCellCoords(n.row, s.col),
              end: this.hot._createCellCoords(s.row, n.col)
            };
            break;
          case "SE-NW":
            o === "bottom" ? c = {
              start: this.hot._createCellCoords(n.row, u.col),
              end: this.hot._createCellCoords(s.row, n.col)
            } : o === "top" && (c = {
              start: l,
              end: n
            });
            break;
        }
        break;
      case "SE-NW":
        switch (r) {
          case "NW-SE":
          case "NE-SW":
          case "SW-NE":
            o === "top" && (c.end = n);
            break;
          case "SE-NW":
            o === "top" ? c.end = n : c = {
              start: n,
              end: s
            };
            break;
        }
        break;
    }
    return c;
  }
  /**
   * Check if user is currently dragging the handle.
   *
   * @returns {boolean} Dragging state.
   */
  isDragged() {
    return this.dragged.length > 0;
  }
}
function aS() {
  let {
    label: t = "",
    colspan: e = 1,
    origColspan: n = 1,
    collapsible: i = !1,
    crossHiddenColumns: r = [],
    isCollapsed: o = !1,
    isHidden: s = !1,
    isRoot: a = !1,
    isPlaceholder: l = !1,
    headerClassNames: u = []
  } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
  return {
    label: t,
    colspan: e,
    origColspan: n,
    collapsible: i,
    isCollapsed: o,
    crossHiddenColumns: r,
    isHidden: s,
    isRoot: a,
    isPlaceholder: l,
    headerClassNames: u
  };
}
function P_() {
  return {
    label: "",
    isPlaceholder: !0
  };
}
function lae(t) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1 / 0;
  const n = [];
  if (e === 0)
    return n;
  T(t, (r) => {
    const o = [];
    let s = 0;
    n.push(o), T(r, (a) => {
      const l = aS();
      if (Be(a)) {
        const {
          label: c,
          colspan: h,
          headerClassName: d
        } = a;
        l.label = Gt(c), typeof h == "number" && h > 1 && (l.colspan = h, l.origColspan = h), typeof d == "string" && (l.headerClassNames = [...d.split(" ")]);
      } else
        l.label = Gt(a);
      s += l.origColspan;
      let u = !1;
      if (s >= e && (l.colspan = l.origColspan - (s - e), l.origColspan = l.colspan, u = !0), o.push(l), l.colspan > 1)
        for (let c = 0; c < l.colspan - 1; c++)
          o.push(P_());
      return !u;
    });
  });
  const i = Math.max(...Ke(n, (r) => r.length));
  return T(n, (r) => {
    if (r.length < i) {
      const o = Ke(new Array(i - r.length), () => aS());
      r.splice(r.length, 0, ...o);
    }
  }), n;
}
function Pv(t, e, n) {
  uae(t, e), e.set(t, n);
}
function uae(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ur(t, e) {
  return t.get(x_(t, e));
}
function Yc(t, e, n) {
  return t.set(x_(t, e), n), n;
}
function x_(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const mI = ["label", "collapsible"];
var xo = /* @__PURE__ */ new WeakMap(), La = /* @__PURE__ */ new WeakMap(), xv = /* @__PURE__ */ new WeakMap();
class cae {
  constructor() {
    Pv(this, xo, []), Pv(this, La, 0), Pv(this, xv, 1 / 0);
  }
  /**
   * Sets columns limit to the source settings will be trimmed. All headers which
   * overlap the column limit will be reduced to keep the structure solid.
   *
   * @param {number} columnsCount The number of columns to limit to.
   */
  setColumnsLimit(e) {
    Yc(xv, this, e);
  }
  /**
   * Sets a new nested header configuration.
   *
   * @param {Array[]} [nestedHeadersSettings=[]] The user-defined nested headers settings.
   */
  setData() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : [];
    Yc(xo, this, lae(e, ur(xv, this))), Yc(La, this, ur(xo, this).length);
  }
  /**
   * Gets normalized source settings.
   *
   * @returns {Array[]}
   */
  getData() {
    return ur(xo, this);
  }
  /**
   * Merges settings with current source settings.
   *
   * @param {object[]} additionalSettings An array of objects with `row`, `col` and additional
   *                                      properties to merge with current source settings.
   */
  mergeWith(e) {
    T(e, (n) => {
      let a = n, {
        row: i,
        col: r
      } = a, o = Wn(a, [
        "row",
        "col"
      ]);
      const s = this.getHeaderSettings(i, r);
      s !== null && nn(s, o, mI);
    });
  }
  /**
   * Maps the current state with a callback. For each source settings the callback function
   * is called. If the function returns value that value is merged with the source settings.
   *
   * @param {Function} callback A function that is called for every header settings.
   *                            Each time the callback is called, the returned value extends
   *                            header settings.
   */
  map(e) {
    T(ur(xo, this), (n) => {
      T(n, (i) => {
        const r = e(Xe({}, i));
        Be(r) && nn(i, r, mI);
      });
    });
  }
  /**
   * Gets source column header settings for a specified header. The returned
   * object contains information about the header label, its colspan length,
   * or if it is hidden in the header renderers.
   *
   * @param {number} headerLevel Header level (0 = most distant to the table).
   * @param {number} columnIndex A visual column index.
   * @returns {object|null}
   */
  getHeaderSettings(e, n) {
    var i;
    if (e >= ur(La, this) || e < 0)
      return null;
    const r = ur(xo, this)[e];
    return Array.isArray(r) === !1 || n >= r.length ? null : (i = r[n]) !== null && i !== void 0 ? i : null;
  }
  /**
   * Gets source of column headers settings for specified headers. If the retrieved column
   * settings overlap the range "box" determined by "columnIndex" and "columnsLength"
   * the exception will be thrown.
   *
   * @param {number} headerLevel Header level (0 = most distant to the table).
   * @param {number} columnIndex A visual column index from which the settings will be extracted.
   * @param {number} [columnsLength=1] The number of columns involved in the extraction of settings.
   * @returns {object}
   */
  getHeadersSettings(e, n) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1;
    const r = [];
    if (e >= ur(La, this) || e < 0)
      return r;
    const o = ur(xo, this)[e];
    let s = 0;
    for (let a = n; a < o.length; a++) {
      const l = o[a];
      if (l.isPlaceholder)
        throw new Error("The first column settings cannot overlap the other header layers");
      if (s += l.colspan, r.push(l), l.colspan > 1 && (a += l.colspan - 1), s === i)
        break;
      if (s > i)
        throw new Error("The last column settings cannot overlap the other header layers");
    }
    return r;
  }
  /**
   * Gets a total number of headers levels.
   *
   * @returns {number}
   */
  getLayersCount() {
    return ur(La, this);
  }
  /**
   * Gets a total number of columns count.
   *
   * @returns {number}
   */
  getColumnsCount() {
    return ur(La, this) > 0 ? ur(xo, this)[0].length : 0;
  }
  /**
   * Clears the data.
   */
  clear() {
    Yc(xo, this, []), Yc(La, this, 0);
  }
}
function Hv(t, e, n) {
  return (e = hae(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function hae(t) {
  var e = dae(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function dae(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
const H_ = "DF-pre-order";
function __(t, e) {
  let n = t.call(e, this);
  for (let i = 0; i < this.childs.length; i++) {
    if (n === !1)
      return !1;
    n = __.call(this.childs[i], t, e);
  }
  return n;
}
const fae = "DF-post-order";
function N_(t, e) {
  for (let n = 0; n < this.childs.length; n++)
    if (N_.call(this.childs[n], t, e) === !1)
      return !1;
  return t.call(e, this);
}
const D_ = "BF";
function gae(t, e) {
  const n = [this];
  function i() {
    if (n.length === 0)
      return;
    const r = n.shift();
    n.push(...r.childs), t.call(e, r) !== !1 && i();
  }
  i();
}
const mae = D_, pI = /* @__PURE__ */ new Map([[H_, __], [fae, N_], [D_, gae]]);
class Xm {
  constructor(e) {
    Hv(this, "data", {}), Hv(this, "parent", null), Hv(this, "childs", []), this.data = e;
  }
  /**
   * Adds a node to tree leaves. Added node is linked with the parent node through "parent" property.
   *
   * @param {TreeNode} node A TreeNode to add.
   */
  addChild(e) {
    e.parent = this, this.childs.push(e);
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * @memberof TreeNode#
   * @function cloneTree
   *
   * Clones a tree structure deeply.
   *
   * For example, for giving a tree structure:
   *      .--(B1)--.
   *   .-(C1)   .-(C2)-.----.
   *  (D1)     (D2)   (D3) (D4)
   *
   * Cloning a tree starting from C2 node creates a mirrored tree structure.
   *     .-(C2')-.-----.
   *    (D2')   (D3') (D4')
   *
   * The cloned tree can be safely modified without affecting the original structure.
   * After modification, the clone can be merged with a tree using the "replaceTreeWith" method.
   *
   * @param {TreeNode} [nodeTree=this] A TreeNode to clone.
   * @returns {TreeNode}
   */
  /* eslint-enable jsdoc/require-description-complete-sentence */
  cloneTree() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this;
    const n = new Xm(Xe({}, e.data));
    for (let i = 0; i < e.childs.length; i++)
      n.addChild(this.cloneTree(e.childs[i]));
    return n;
  }
  /**
   * Replaces the current node with a passed tree structure.
   *
   * @param {TreeNode} nodeTree A TreeNode to replace with.
   */
  replaceTreeWith(e) {
    this.data = Xe({}, e.data), this.childs = [];
    for (let n = 0; n < e.childs.length; n++)
      this.addChild(e.childs[n]);
  }
  /**
   * Traverses the tree structure through node childs. The walk down traversing supports
   * a three different strategies.
   *  - Depth-first pre-order strategy (https://en.wikipedia.org/wiki/Tree_traversal#Pre-order_(NLR));
   *  - Depth-first post-order strategy (https://en.wikipedia.org/wiki/Tree_traversal#Post-order_(NLR));
   *  - Breadth-first traversal strategy (https://en.wikipedia.org/wiki/Tree_traversal#Breadth-first_search_/_level_order).
   *
   * @param {Function} callback The callback function which will be called for each node.
   * @param {string} [traversalStrategy=DEFAULT_TRAVERSAL_STRATEGY] Traversing strategy.
   */
  walkDown(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : mae;
    if (!pI.has(n))
      throw new Error(`Traversal strategy "${n}" does not exist`);
    pI.get(n).call(this, e, this);
  }
  /**
   * Traverses the tree structure through node parents.
   *
   * @param {Function} callback The callback function which will be called for each node.
   */
  walkUp(e) {
    const n = this, i = (r) => {
      e.call(n, r) !== !1 && r.parent !== null && i(r.parent);
    };
    i(this);
  }
}
function _v(t, e, n) {
  pae(t, e), e.set(t, n);
}
function pae(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Yn(t, e) {
  return t.get(k_(t, e));
}
function wae(t, e, n) {
  return t.set(k_(t, e), n), n;
}
function k_(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var tu = /* @__PURE__ */ new WeakMap(), Ba = /* @__PURE__ */ new WeakMap(), nu = /* @__PURE__ */ new WeakMap();
class Aae {
  constructor(e) {
    _v(this, tu, /* @__PURE__ */ new Map()), _v(this, Ba, /* @__PURE__ */ new Map()), _v(this, nu, null), wae(nu, this, e);
  }
  /**
   * Gets an array of the all root nodes.
   *
   * @returns {TreeNode[]}
   */
  getRoots() {
    return Array.from(Yn(tu, this).values());
  }
  /**
   * Gets a root node by specified visual column index.
   *
   * @param {number} columnIndex A visual column index.
   * @returns {TreeNode|undefined}
   */
  getRootByColumn(e) {
    let n;
    return Yn(Ba, this).has(e) && (n = Yn(tu, this).get(Yn(Ba, this).get(e))), n;
  }
  /**
   * Gets a tree node by its position in the grid settings.
   *
   * @param {number} headerLevel Header level index (there is support only for positive values).
   * @param {number} columnIndex A visual column index.
   * @returns {TreeNode|undefined}
   */
  getNode(e, n) {
    const i = this.getRootByColumn(n);
    if (!i)
      return;
    const r = n - Yn(Ba, this).get(n);
    let o = 0, s;
    return i.walkDown((a) => {
      const {
        data: {
          origColspan: l,
          headerLevel: u
        }
      } = a;
      if (e === u) {
        if (r >= o && r <= o + l - 1)
          return s = a, s.data.isRoot = n === s.data.columnIndex, !1;
        o += l;
      }
    }), s;
  }
  /**
   * Builds (or rebuilds if called again) root nodes indexes.
   */
  rebuildTreeIndex() {
    let e = 0;
    Yn(Ba, this).clear(), T(Yn(tu, this), (n) => {
      let [, {
        data: {
          colspan: i
        }
      }] = n;
      for (let r = e; r < e + i; r++)
        Yn(Ba, this).set(r, e);
      e += i;
    });
  }
  /**
   * Builds trees based on SourceSettings class. Calling a method causes clearing the tree state built
   * from the previous call.
   */
  buildTree() {
    this.clear();
    const e = Yn(nu, this).getColumnsCount();
    let n = 0;
    for (; n < e; ) {
      const i = Yn(nu, this).getHeaderSettings(0, n), r = new Xm();
      Yn(tu, this).set(n, r), this.buildLeaves(r, n, 0, i.origColspan), n += i.origColspan;
    }
    this.rebuildTreeIndex();
  }
  /**
   * Builds leaves for specified tree node.
   *
   * @param {TreeNode} parentNode A node to which the leaves applies.
   * @param {number} columnIndex A visual column index.
   * @param {number} headerLevel Currently processed header level.
   * @param {number} [extractionLength=1] Determines column extraction length for node children.
   */
  buildLeaves(e, n, i) {
    let r = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 1;
    const o = Yn(nu, this).getHeadersSettings(i, n, r);
    i += 1, T(o, (s) => {
      const a = On(Xe({}, s), {
        /**
         * The header level (tree node depth level).
         *
         * @type {number}
         */
        headerLevel: i - 1,
        /**
         * A visual column index.
         *
         * @type {number}
         */
        columnIndex: n
      });
      let l;
      i === 1 ? (e.data = a, l = e) : (l = new Xm(a), e.addChild(l)), i < Yn(nu, this).getLayersCount() && this.buildLeaves(l, n, i, s.origColspan), n += s.origColspan;
    });
  }
  /**
   * Clears the tree to the initial state.
   */
  clear() {
    Yn(tu, this).clear(), Yn(Ba, this).clear();
  }
}
function L_(t, e) {
  t.walkDown((n) => {
    let {
      data: i,
      childs: r
    } = n;
    if (!i.isHidden && (e(i.columnIndex), r.length === 0))
      for (let o = 1; o < i.colspan; o++)
        e(i.columnIndex + o);
  });
}
function qm(t, e) {
  let {
    childs: n
  } = t;
  if (n.length !== 0)
    return n[0].data[e];
}
function Zm(t) {
  return qm(t, "origColspan") === t.data.origColspan;
}
function VR(t) {
  const {
    data: e,
    childs: n
  } = t;
  if (!e.isCollapsed || e.isHidden || e.origColspan <= 1)
    return {
      rollbackModification: () => {
      },
      affectedColumns: [],
      colspanCompensation: 0
    };
  if (Zm(t))
    return VR(n[0]);
  e.isCollapsed = !1;
  const r = n.slice(1), o = /* @__PURE__ */ new Set();
  let s = 0;
  if (r.length > 0)
    T(r, (a) => {
      a.replaceTreeWith(a.data.clonedTree), a.data.clonedTree = null;
      const l = a.data;
      s += l.colspan, L_(a, (u) => {
        o.add(u);
      });
    });
  else {
    const {
      colspan: a,
      origColspan: l,
      columnIndex: u
    } = e;
    s = l - a;
    for (let c = 1; c < l; c++)
      o.add(u + c);
  }
  return t.walkUp((a) => {
    const {
      data: l
    } = a;
    l.colspan += s, l.colspan >= l.origColspan ? (l.colspan = l.origColspan, l.isCollapsed = !1) : Zm(a) && (l.isCollapsed = qm(a, "isCollapsed"));
  }), {
    rollbackModification: () => WR(t),
    affectedColumns: Array.from(o),
    colspanCompensation: s
  };
}
function WR(t) {
  var e;
  const {
    data: n,
    childs: i
  } = t;
  if (n.isCollapsed || n.isHidden || n.origColspan <= 1)
    return {
      rollbackModification: () => {
      },
      affectedColumns: [],
      colspanCompensation: 0
    };
  if (Zm(t))
    return WR(i[0]);
  n.isCollapsed = !0;
  const o = i.slice(1), s = /* @__PURE__ */ new Set();
  if (o.length > 0)
    T(o, (l) => {
      L_(l, (u) => {
        s.add(u);
      }), l.data.clonedTree = l.cloneTree(), l.walkDown((u) => {
        let {
          data: c
        } = u;
        c.isHidden = !0;
      });
    });
  else {
    const {
      origColspan: l,
      columnIndex: u
    } = n;
    for (let c = 1; c < l; c++) {
      const h = u + c;
      s.add(h);
    }
  }
  const a = n.colspan - ((e = qm(t, "colspan")) !== null && e !== void 0 ? e : 1);
  return t.walkUp((l) => {
    const {
      data: u
    } = l;
    u.colspan -= a, u.colspan <= 1 ? (u.colspan = 1, u.isCollapsed = !0) : Zm(l) && (u.isCollapsed = qm(l, "isCollapsed"));
  }), {
    rollbackModification: () => VR(t),
    affectedColumns: Array.from(s),
    colspanCompensation: a
  };
}
function Cae(t, e) {
  if (!Number.isInteger(e))
    throw new Error("The passed gridColumnIndex argument has invalid type.");
  if (t.childs.length > 0)
    throw new Error(mt`The passed node is not the last node on the tree. Only for\x20
the last node, the hide column modification can be applied.`);
  const {
    crossHiddenColumns: n
  } = t.data;
  if (n.includes(e))
    return;
  let i = !1;
  t.walkUp((r) => {
    const {
      data: {
        collapsible: o
      }
    } = r;
    if (o)
      return i = !0, !1;
  }), !i && t.walkUp((r) => {
    const {
      data: o
    } = r;
    o.crossHiddenColumns.push(e), o.colspan > 1 ? o.colspan -= 1 : o.isHidden = !0;
  });
}
function vae(t, e) {
  if (!Number.isInteger(e))
    throw new Error("The passed gridColumnIndex argument has invalid type.");
  if (t.childs.length > 0)
    throw new Error(mt`The passed node is not the last node on the tree. Only for\x20
the last node, the show column modification can be applied.`);
  const {
    crossHiddenColumns: n
  } = t.data;
  if (!n.includes(e))
    return;
  let i = !1;
  t.walkUp((r) => {
    const {
      data: {
        collapsible: o
      }
    } = r;
    if (o)
      return i = !0, !1;
  }), !i && t.walkUp((r) => {
    const {
      data: o
    } = r;
    o.crossHiddenColumns.splice(o.crossHiddenColumns.indexOf(e), 1), !o.isHidden && o.colspan < o.origColspan && (o.colspan += 1), o.isHidden = !1;
  });
}
const wI = /* @__PURE__ */ new Map([["collapse", WR], ["expand", VR], ["hide-column", Cae], ["show-column", vae]]);
function yae(t, e, n) {
  if (!wI.has(t))
    throw new Error(`The node modifier action ("${t}") does not exist.`);
  return wI.get(t)(e, n);
}
function Mg(t) {
  const e = [];
  return T(t, (n) => {
    n.walkDown((i) => {
      const r = i.data, {
        origColspan: o,
        columnIndex: s,
        headerLevel: a,
        crossHiddenColumns: l
      } = r, u = bae(e, a);
      let c = !1;
      for (let h = s; h < s + o; h++)
        if (l.includes(h) || c)
          u.push(P_());
        else {
          const g = Sae(r);
          g.isRoot = !0, u.push(g), c = !0;
        }
    });
  }), e;
}
function Sae(t) {
  const i = aS(t), {
    crossHiddenColumns: e
  } = i;
  return Wn(i, [
    "crossHiddenColumns"
  ]);
}
function bae(t, e) {
  let n;
  return Array.isArray(t[e]) ? n = t[e] : (n = [], t[e] = n), n;
}
function Nv(t, e, n) {
  Rae(t, e), e.set(t, n);
}
function Rae(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function jc(t, e, n) {
  return t.set(B_(t, e), n), n;
}
function Wt(t, e) {
  return t.get(B_(t, e));
}
function B_(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var zr = /* @__PURE__ */ new WeakMap(), ii = /* @__PURE__ */ new WeakMap(), Fa = /* @__PURE__ */ new WeakMap();
class Eae {
  constructor() {
    Nv(this, zr, new cae()), Nv(this, ii, new Aae(Wt(zr, this))), Nv(this, Fa, [[]]);
  }
  /**
   * Sets a new state for the nested headers plugin based on settings passed
   * directly to the plugin.
   *
   * @param {Array[]} nestedHeadersSettings The user-defined settings.
   * @returns {boolean} Returns `true` if the settings are processed correctly, `false` otherwise.
   */
  setState(e) {
    Wt(zr, this).setData(e);
    let n = !1;
    try {
      Wt(ii, this).buildTree();
    } catch (i) {
      Wt(ii, this).clear(), Wt(zr, this).clear(), n = !0;
    }
    return jc(Fa, this, Mg(Wt(ii, this).getRoots())), n;
  }
  /**
   * Sets columns limit to the state will be trimmed. All headers (colspans) which
   * overlap the column limit will be reduced to keep the structure solid.
   *
   * @param {number} columnsCount The number of columns to limit to.
   */
  setColumnsLimit(e) {
    Wt(zr, this).setColumnsLimit(e);
  }
  /**
   * Merges settings with current plugin state.
   *
   * By default only foreign keys are merged with source state and passed to the tree. But only
   * known keys are exported to matrix.
   *
   * @param {object[]} settings An array of objects to merge with the current source settings.
   *                            It is a requirement that every object has `row` and `col` properties
   *                            which points to the specific header settings object.
   */
  mergeStateWith(e) {
    const n = Ke(e, (i) => {
      let s = i, {
        row: r
      } = s, o = Wn(s, [
        "row"
      ]);
      return Xe({
        row: r < 0 ? this.rowCoordsToLevel(r) : r
      }, o);
    });
    Wt(zr, this).mergeWith(n), Wt(ii, this).buildTree(), jc(Fa, this, Mg(Wt(ii, this).getRoots()));
  }
  /**
   * Maps the current state with a callback. For each header settings the callback function
   * is called. If the function returns value that value is merged with the state.
   *
   * By default only foreign keys are merged with source state and passed to the tree. But only
   * known keys are exported to matrix.
   *
   * @param {Function} callback A function that is called for every header source settings.
   *                            Each time the callback is called, the returned value extends
   *                            header settings.
   */
  mapState(e) {
    Wt(zr, this).map(e), Wt(ii, this).buildTree(), jc(Fa, this, Mg(Wt(ii, this).getRoots()));
  }
  /**
   * Maps the current tree nodes with a callback. For each node the callback function
   * is called. If the function returns value that value is added to returned array.
   *
   * @param {Function} callback A function that is called for every tree node.
   *                            Each time the callback is called, the returned value is
   *                            added to returned array.
   * @returns {Array}
   */
  mapNodes(e) {
    return rn(Wt(ii, this).getRoots(), (n, i) => (i.walkDown((r) => {
      const o = e(r.data);
      o !== void 0 && n.push(o);
    }), n), []);
  }
  /**
   * Triggers an action (e.g. "collapse") from the NodeModifiers module. The module
   * modifies a tree structure in such a way as to obtain the correct structure consistent with the
   * called action.
   *
   * @param {string} action An action name to trigger.
   * @param {number} headerLevel Header level index (there is support for negative and positive values).
   * @param {number} columnIndex A visual column index.
   * @returns {object|undefined}
   */
  triggerNodeModification(e, n, i) {
    n < 0 && (n = this.rowCoordsToLevel(n));
    const r = Wt(ii, this).getNode(n, i);
    let o;
    return r && (o = yae(e, r, i), jc(Fa, this, Mg(Wt(ii, this).getRoots()))), o;
  }
  /**
   * Triggers an action (e.g. "hide-column") from the NodeModifiers module. The action is
   * triggered starting from the lowest header. The module modifies a tree structure in
   * such a way as to obtain the correct structure consistent with the called action.
   *
   * @param {string} action An action name to trigger.
   * @param {number} columnIndex A visual column index.
   * @returns {object|undefined}
   */
  triggerColumnModification(e, n) {
    return this.triggerNodeModification(e, -1, n);
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * @memberof StateManager#
   * @function rowCoordsToLevel
   *
   * Translates row coordinates into header level. The row coordinates counts from -1 to -N
   * and describes headers counting from most closest to most distant from the table.
   * The header levels are counted from 0 to N where 0 describes most distant header
   * from the table.
   *
   *  Row coords             Header level
   *           +--------------+
   *       -3  │ A1 │ A1      │  0
   *           +--------------+
   *       -2  │ B1 │ B2 │ B3 │  1
   *           +--------------+
   *       -1  │ C1 │ C2 │ C3 │  2
   *           +==============+
   *           │    │    │    │
   *           +--------------+
   *           │    │    │    │
   *
   * @param {number} rowIndex A visual row index.
   * @returns {number|null} Returns unsigned number.
   */
  /* eslint-enable jsdoc/require-description-complete-sentence */
  rowCoordsToLevel(e) {
    if (e >= 0)
      return null;
    const n = e + Math.max(this.getLayersCount(), 1);
    return n < 0 ? null : n;
  }
  /* eslint-disable jsdoc/require-description-complete-sentence */
  /**
   * @memberof StateManager#
   * @function levelToRowCoords
   *
   * Translates header level into row coordinates. The row coordinates counts from -1 to -N
   * and describes headers counting from most closest to most distant from the table.
   * The header levels are counted from 0 to N where 0 describes most distant header
   * from the table.
   *
   *  Header level            Row coords
   *           +--------------+
   *        0  │ A1 │ A1      │  -3
   *           +--------------+
   *        1  │ B1 │ B2 │ B3 │  -2
   *           +--------------+
   *        2  │ C1 │ C2 │ C3 │  -1
   *           +==============+
   *           │    │    │    │
   *           +--------------+
   *           │    │    │    │
   *
   * @param {number} headerLevel Header level index.
   * @returns {number} Returns negative number.
   */
  /* eslint-enable jsdoc/require-description-complete-sentence */
  levelToRowCoords(e) {
    if (e < 0)
      return null;
    const n = e - Math.max(this.getLayersCount(), 1);
    return n >= 0 ? null : n;
  }
  /**
   * Gets column header settings for a specified column and header index. The returned object contains
   * all information necessary for header renderers. It contains header label, colspan length, or hidden
   * flag.
   *
   * @param {number} headerLevel Header level (there is support for negative and positive values).
   * @param {number} columnIndex A visual column index.
   * @returns {object|null}
   */
  getHeaderSettings(e, n) {
    var i, r;
    return e < 0 && (e = this.rowCoordsToLevel(e)), e === null || e >= this.getLayersCount() ? null : (i = (r = Wt(Fa, this)[e]) === null || r === void 0 ? void 0 : r[n]) !== null && i !== void 0 ? i : null;
  }
  /**
   * Gets tree data that is connected to the column header. The returned object contains all information
   * necessary for modifying tree structure (column collapsing, hiding, etc.). It contains a header
   * label, colspan length, or visual column index that indicates which column index the node is rendered from.
   *
   * @param {number} headerLevel Header level (there is support for negative and positive values).
   * @param {number} columnIndex A visual column index.
   * @returns {object|null}
   */
  getHeaderTreeNodeData(e, n) {
    const i = this.getHeaderTreeNode(e, n);
    return i ? Xe({}, i.data) : null;
  }
  /**
   * Gets tree node that is connected to the column header.
   *
   * @param {number} headerLevel Header level (there is support for negative and positive values).
   * @param {number} columnIndex A visual column index.
   * @returns {TreeNode|null}
   */
  getHeaderTreeNode(e, n) {
    if (e < 0 && (e = this.rowCoordsToLevel(e)), e === null || e >= this.getLayersCount())
      return null;
    const i = Wt(ii, this).getNode(e, n);
    return i || null;
  }
  /**
   * Finds the most top header level of the column header that is rendered entirely within
   * the passed visual columns range. If multiple columns headers are found within the range the
   * most top header level value will be returned.
   *
   * @param {number} columnIndexFrom A visual column index.
   * @param {number} [columnIndexTo] A visual column index.
   * @returns {number} Returns a header level in format -1 to -N.
   */
  findTopMostEntireHeaderLevel(e) {
    var n;
    let i = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : e;
    const r = i - e + 1;
    let o = !1, s = null;
    for (let a = e; a <= i; a++) {
      const l = Wt(ii, this).getRootByColumn(a);
      if (!l)
        break;
      o = !0, l.walkDown((u) => {
        const {
          columnIndex: c,
          headerLevel: h,
          origColspan: d,
          isHidden: g
        } = u.data;
        g || d <= r && c >= e && c + d - 1 <= i && (s === null || h < s) && (s = h);
      }, H_);
    }
    return o && s === null ? -1 : this.levelToRowCoords((n = s) !== null && n !== void 0 ? n : 0);
  }
  /**
   * The method is helpful in cases where the column index targets in-between currently
   * collapsed column. In that case, the method returns the left-most column index
   * where the nested header begins.
   *
   * @param {number} headerLevel Header level (there is support for negative and positive values).
   * @param {number} columnIndex A visual column index.
   * @returns {number}
   */
  findLeftMostColumnIndex(e, n) {
    var i;
    const {
      isRoot: r
    } = (i = this.getHeaderSettings(e, n)) !== null && i !== void 0 ? i : {
      isRoot: !0
    };
    if (r)
      return n;
    let o = n - 1;
    for (; o >= 0; ) {
      var s;
      const {
        isRoot: a
      } = (s = this.getHeaderSettings(e, o)) !== null && s !== void 0 ? s : {
        isRoot: !0
      };
      if (a)
        break;
      o -= 1;
    }
    return o;
  }
  /**
   * The method is helpful in cases where the column index targets in-between currently
   * collapsed column. In that case, the method returns the right-most column index
   * where the nested header ends.
   *
   * @param {number} headerLevel Header level (there is support for negative and positive values).
   * @param {number} columnIndex A visual column index.
   * @returns {number}
   */
  findRightMostColumnIndex(e, n) {
    var i;
    const {
      isRoot: r,
      origColspan: o
    } = (i = this.getHeaderSettings(e, n)) !== null && i !== void 0 ? i : {
      isRoot: !0,
      origColspan: 1
    };
    if (r)
      return n + o - 1;
    let s = n + 1;
    for (; s < this.getColumnsCount(); ) {
      var a;
      const {
        isRoot: l
      } = (a = this.getHeaderSettings(e, s)) !== null && a !== void 0 ? a : {
        isRoot: !0
      };
      if (l)
        break;
      s += 1;
    }
    return s - 1;
  }
  /**
   * Gets a total number of headers levels.
   *
   * @returns {number}
   */
  getLayersCount() {
    return Wt(zr, this).getLayersCount();
  }
  /**
   * Gets a total number of columns count.
   *
   * @returns {number}
   */
  getColumnsCount() {
    return Wt(zr, this).getColumnsCount();
  }
  /**
   * Clears the column state manager to the initial state.
   */
  clear() {
    jc(Fa, this, []), Wt(zr, this).clear(), Wt(ii, this).clear();
  }
}
function Gc(t, e, n) {
  return (e = Tae(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Tae(t) {
  var e = Mae(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Mae(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Iae {
  constructor(e, n) {
    Gc(this, "hot", void 0), Gc(this, "nestedHeaderSettingsGetter", void 0), Gc(this, "layersCount", 0), Gc(this, "container", void 0), Gc(this, "widthsMap", void 0), this.hot = e, this.nestedHeaderSettingsGetter = n, this.widthsMap = this.hot.columnIndexMapper.createAndRegisterIndexMap("nestedHeaders.widthsMap", "physicalIndexToValue");
  }
  /**
   * Sets the number of nested headers layers count.
   *
   * @param {number} layersCount Total number of headers levels.
   * @returns {GhostTable}
   */
  setLayersCount(e) {
    return this.layersCount = e, this;
  }
  /**
   * Gets the column width based on the visual column index.
   *
   * @param {number} visualColumn Visual column index.
   * @returns {number|null}
   */
  getWidth(e) {
    return this.widthsMap.getValueAtIndex(this.hot.toPhysicalColumn(e));
  }
  /**
   * Build cache of the headers widths.
   */
  buildWidthsMap() {
    this.container = this.hot.rootDocument.createElement("div"), this.container.classList.add("handsontable", "htGhostTable", "htAutoSize"), this._buildGhostTable(this.container), this.hot.rootDocument.body.appendChild(this.container);
    const e = this.container.querySelectorAll("tr:last-of-type th"), n = e.length;
    this.widthsMap.clear();
    for (let i = 0; i < n; i++) {
      const r = this.hot.columnIndexMapper.getVisualFromRenderableIndex(i), o = this.hot.toPhysicalColumn(r);
      this.widthsMap.setValueAtIndex(o, e[i].offsetWidth);
    }
    this.container.parentNode.removeChild(this.container), this.container = null;
  }
  /**
   * Build temporary table for getting minimal columns widths.
   *
   * @private
   * @param {HTMLElement} container The element where the DOM nodes are injected.
   */
  _buildGhostTable(e) {
    const {
      rootDocument: n,
      columnIndexMapper: i
    } = this.hot, r = n.createDocumentFragment(), o = n.createElement("table"), s = !!this.hot.getSettings().dropdownMenu, a = i.getRenderableIndexesLength();
    for (let l = 0; l < this.layersCount; l++) {
      const u = n.createElement("tr");
      for (let c = 0; c < a; c++) {
        let h = i.getVisualFromRenderableIndex(c);
        h === null && (h = c);
        const d = n.createElement("th"), g = this.nestedHeaderSettingsGetter(l, h);
        if (g && (!g.isPlaceholder || g.isHidden)) {
          let m = g.label;
          s && (m += '<button class="changeType"></button>'), pl(d, m), d.colSpan = g.colspan, u.appendChild(d);
        }
      }
      o.appendChild(u);
    }
    r.appendChild(o), e.appendChild(r);
  }
  /**
   * Clear the widths cache.
   */
  clear() {
    this.widthsMap.clear(), this.container = null;
  }
}
const Oae = Iae;
function Pae(t, e) {
  F_(t, e), e.add(t);
}
function AI(t, e, n) {
  return (e = xae(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function xae(t) {
  var e = Hae(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Hae(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Ig(t, e, n) {
  F_(t, e), e.set(t, n);
}
function F_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function qa(t, e, n) {
  return t.set($t(t, e), n), n;
}
function tt(t, e) {
  return t.get($t(t, e));
}
function $t(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const CI = "nestedHeaders", _ae = 280;
var vt = /* @__PURE__ */ new WeakMap(), vs = /* @__PURE__ */ new WeakMap(), Ih = /* @__PURE__ */ new WeakMap(), hA = /* @__PURE__ */ new WeakMap(), Qt = /* @__PURE__ */ new WeakSet();
class Nae extends at {
  constructor() {
    super(...arguments), Pae(this, Qt), Ig(this, vt, new Eae()), Ig(this, vs, null), Ig(this, Ih, null), Ig(this, hA, !1), AI(this, "ghostTable", new Oae(this.hot, (e, n) => this.getHeaderSettings(e, n))), AI(this, "detectedOverlappedHeaders", !1);
  }
  static get PLUGIN_KEY() {
    return CI;
  }
  static get PLUGIN_PRIORITY() {
    return _ae;
  }
  /**
   * Check if plugin is enabled.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[CI];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    if (this.enabled)
      return;
    const {
      nestedHeaders: n
    } = this.hot.getSettings();
    (!Array.isArray(n) || !Array.isArray(n[0])) && gt(mt`Your Nested Headers plugin configuration is invalid. The settings has to be\x20
                        passed as an array of arrays e.q. [['A1', { label: 'A2', colspan: 2 }]]`), this.addHook("init", () => $t(Qt, this, Xae).call(this)), this.addHook("afterLoadData", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, qae).call(e, ...r);
    }), this.addHook("beforeOnCellMouseDown", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Bae).call(e, ...r);
    }), this.addHook("afterOnCellMouseDown", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Fae).call(e, ...r);
    }), this.addHook("beforeOnCellMouseOver", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Vae).call(e, ...r);
    }), this.addHook("beforeOnCellMouseUp", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Wae).call(e, ...r);
    }), this.addHook("beforeSelectionHighlightSet", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, $ae).call(e, ...r);
    }), this.addHook("modifyTransformStart", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Uae).call(e, ...r);
    }), this.addHook("afterSelection", () => $t(Qt, this, Og).call(this)), this.addHook("afterSelectionFocusSet", () => $t(Qt, this, Og).call(this)), this.addHook("beforeViewportScrollHorizontally", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Dae).call(e, ...r);
    }), this.addHook("afterGetColumnHeaderRenderers", (i) => $t(Qt, this, jae).call(this, i)), this.addHook("modifyColWidth", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, zae).call(e, ...r);
    }), this.addHook("modifyColumnHeaderValue", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Kae).call(e, ...r);
    }), this.addHook("beforeHighlightingColumnHeader", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, kae).call(e, ...r);
    }), this.addHook("beforeCopy", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Lae).call(e, ...r);
    }), this.addHook("beforeSelectColumns", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Yae).call(e, ...r);
    }), this.addHook("afterViewportColumnCalculatorOverride", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Gae).call(e, ...r);
    }), this.addHook("modifyFocusedElement", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return $t(Qt, e, Qae).call(e, ...r);
    }), this.hot.columnIndexMapper.addLocalHook("cacheUpdated", () => $t(Qt, this, Og).call(this)), this.hot.rowIndexMapper.addLocalHook("cacheUpdated", () => $t(Qt, this, Og).call(this)), super.enablePlugin(), this.updatePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`nestedHeaders`](@/api/options.md#nestedheaders)
   */
  updatePlugin() {
    if (!this.hot.view)
      return;
    const {
      nestedHeaders: e
    } = this.hot.getSettings();
    tt(vt, this).setColumnsLimit(this.hot.countCols()), Array.isArray(e) && (this.detectedOverlappedHeaders = tt(vt, this).setState(e)), this.detectedOverlappedHeaders && gt(mt`Your Nested Headers plugin setup contains overlapping headers. This kind of configuration\x20
                        is currently not supported.`), this.enabled && this.hot.columnIndexMapper.hidingMapsCollection.getMergedValues().forEach((n, i) => {
      const r = n === !0 ? "hide-column" : "show-column";
      tt(vt, this).triggerColumnModification(r, i);
    }), !tt(vs, this) && this.enabled && qa(vs, this, this.hot.columnIndexMapper.createChangesObserver("hiding").subscribe((n) => {
      n.forEach((i) => {
        let {
          op: r,
          index: o,
          newValue: s
        } = i;
        if (r === "replace") {
          const a = s === !0 ? "hide-column" : "show-column";
          tt(vt, this).triggerColumnModification(a, o);
        }
      }), this.ghostTable.buildWidthsMap();
    })), this.ghostTable.setLayersCount(this.getLayersCount()).buildWidthsMap(), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.clearColspans(), tt(vt, this).clear(), tt(vs, this).unsubscribe(), qa(vs, this, null), this.ghostTable.clear(), super.disablePlugin();
  }
  /**
   * Returns an instance of the internal state manager of the plugin.
   *
   * @private
   * @returns {StateManager}
   */
  getStateManager() {
    return tt(vt, this);
  }
  /**
   * Gets a total number of headers levels.
   *
   * @private
   * @returns {number}
   */
  getLayersCount() {
    return tt(vt, this).getLayersCount();
  }
  /**
   * Gets column settings for a specified header. The returned object contains
   * information about the header label, its colspan length, or if it is hidden
   * in the header renderers.
   *
   * @private
   * @param {number} headerLevel Header level (0 = most distant to the table).
   * @param {number} columnIndex A visual column index.
   * @returns {object}
   */
  getHeaderSettings(e, n) {
    return tt(vt, this).getHeaderSettings(e, n);
  }
  /**
   * Clear the colspans remaining after plugin usage.
   *
   * @private
   */
  clearColspans() {
    if (!this.hot.view)
      return;
    const {
      _wt: e
    } = this.hot.view, n = e.getSetting("columnHeaders").length, i = e.wtTable.THEAD, r = e.wtOverlays.topOverlay.clone.wtTable.THEAD, o = e.wtOverlays.topInlineStartCornerOverlay ? e.wtOverlays.topInlineStartCornerOverlay.clone.wtTable.THEAD : null;
    for (let s = 0; s < n; s++) {
      const a = i.childNodes[s];
      if (!a)
        break;
      const l = r.childNodes[s], u = o ? o.childNodes[s] : null;
      for (let c = 0, h = a.childNodes.length; c < h; c++)
        a.childNodes[c].removeAttribute("colspan"), xe(a.childNodes[c], "hiddenHeader"), l && l.childNodes[c] && (l.childNodes[c].removeAttribute("colspan"), xe(l.childNodes[c], "hiddenHeader")), o && u && u.childNodes[c] && (u.childNodes[c].removeAttribute("colspan"), xe(u.childNodes[c], "hiddenHeader"));
    }
  }
  /**
   * Generates the appropriate header renderer for a header row.
   *
   * @private
   * @param {number} headerLevel The index of header level counting from the top (positive
   *                             values counting from 0 to N).
   * @returns {Function}
   * @fires Hooks#afterGetColHeader
   */
  headerRendererFactory(e) {
    var n = this;
    const i = this.hot.view._wt.getSetting("fixedColumnsStart");
    return (r, o) => {
      var s;
      const {
        columnIndexMapper: a,
        view: l
      } = this.hot;
      let u = a.getVisualFromRenderableIndex(r);
      u === null && (u = r), o.removeAttribute("colspan"), xe(o, "hiddenHeader");
      const {
        colspan: c,
        isHidden: h,
        isPlaceholder: d,
        headerClassNames: g
      } = (s = tt(vt, this).getHeaderSettings(e, u)) !== null && s !== void 0 ? s : {
        label: ""
      };
      if (d || h)
        Q(o, "hiddenHeader");
      else if (c > 1) {
        var m, w;
        const {
          wtOverlays: C
        } = l._wt, S = (m = C.topInlineStartCornerOverlay) === null || m === void 0 ? void 0 : m.clone.wtTable.THEAD.contains(o), b = (w = C.inlineStartOverlay) === null || w === void 0 ? void 0 : w.clone.wtTable.THEAD.contains(o), R = S || b ? Math.min(c, i - r) : c;
        R > 1 && o.setAttribute("colspan", R);
      }
      if (this.hot.view.appendColHeader(u, o, function() {
        return n.getColumnHeaderValue(...arguments);
      }, e), !d && !h) {
        const C = o.querySelector("div.relative");
        C && g && g.length > 0 && (xe(C, this.hot.getColumnMeta(u).headerClassName), Q(C, g));
      }
    };
  }
  /**
   * Returns the column header value for specified column and header level index.
   *
   * @private
   * @param {number} visualColumnIndex Visual column index.
   * @param {number} headerLevel The index of header level. The header level accepts positive (0 to N)
   *                             and negative (-1 to -N) values. For positive values, 0 points to the
   *                             top most header, and for negative direction, -1 points to the most bottom
   *                             header (the header closest to the cells).
   * @returns {string} Returns the column header value to update.
   */
  getColumnHeaderValue(e, n) {
    var i;
    const {
      isHidden: r,
      isPlaceholder: o
    } = (i = tt(vt, this).getHeaderSettings(n, e)) !== null && i !== void 0 ? i : {};
    return o || r ? "" : this.hot.getColHeader(e, n);
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    qa(vt, this, null), tt(vs, this) !== null && (tt(vs, this).unsubscribe(), qa(vs, this, null)), super.destroy();
  }
  /**
   * Gets the tree data that belongs to the column headers pointed by the passed coordinates.
   *
   * @private
   * @param {CellCoords} coords The CellCoords instance.
   * @returns {object|undefined}
   */
  _getHeaderTreeNodeDataByCoords(e) {
    if (!(e.row >= 0 || e.col < 0))
      return tt(vt, this).getHeaderTreeNodeData(e.row, e.col);
  }
}
function Og() {
  var t;
  const e = (t = this.hot) === null || t === void 0 ? void 0 : t.getSelectedRangeLast();
  if (!e)
    return;
  const {
    highlight: n
  } = e;
  if (n.isHeader() && n.col >= 0) {
    const r = tt(vt, this).findLeftMostColumnIndex(n.row, n.col), o = this.hot.selection.highlight.getFocus();
    o.visualCellRange.highlight.col = r, o.visualCellRange.from.col = r, o.visualCellRange.to.col = r, o.commit();
  }
}
function Dae(t) {
  const e = this.hot.getSelectedRangeLast();
  if (!e)
    return t;
  const {
    highlight: n
  } = e;
  if (!(n.isHeader() && n.col >= 0))
    return t;
  const r = this.hot.view.getFirstFullyVisibleColumn(), o = this.hot.view.getLastFullyVisibleColumn(), s = tt(vt, this).findLeftMostColumnIndex(n.row, n.col), a = tt(vt, this).findRightMostColumnIndex(n.row, n.col);
  return s < r && a > o ? t : s < r ? s : a;
}
function kae(t, e, n) {
  const i = tt(vt, this).getHeaderTreeNodeData(e, t);
  if (!i)
    return t;
  const {
    columnCursor: r,
    selectionType: o,
    selectionWidth: s
  } = n, {
    isRoot: a,
    colspan: l
  } = tt(vt, this).getHeaderSettings(e, t);
  if (o === Hb) {
    if (!a)
      return i.columnIndex;
  } else if (o === Kx && (l > s - r || !a))
    return null;
  return t;
}
function Lae(t, e, n) {
  let {
    columnHeadersCount: i
  } = n;
  if (i !== 0)
    for (let o = 0; o < e.length; o++) {
      const {
        startRow: s,
        startCol: a,
        endRow: l,
        endCol: u
      } = e[o], c = l - s + 1, h = a - u + 1;
      if (s >= 0 || h === 1)
        break;
      for (let d = a; d <= u; d++)
        for (let g = s; g <= l; g++) {
          var r;
          const m = c + g, w = d - a;
          if (w === 0)
            continue;
          ((r = tt(vt, this).getHeaderTreeNodeData(g, d)) === null || r === void 0 ? void 0 : r.isRoot) === !1 && (t[m][w] = "");
        }
    }
}
function Bae(t, e, n, i) {
  this._getHeaderTreeNodeDataByCoords(e) && (i.column = !0);
}
function Fae(t, e) {
  const n = this._getHeaderTreeNodeDataByCoords(e);
  if (!n)
    return;
  qa(Ih, this, e.clone()), qa(hA, this, !0);
  const {
    selection: i
  } = this.hot, r = i.isSelected() ? i.getSelectedRange().current() : null, o = [], {
    columnIndex: s,
    origColspan: a
  } = n, l = !i.inInSelection(e);
  t.shiftKey && r ? e.col < r.from.col ? o.push(r.getTopEndCorner().col, s, e.row) : e.col > r.from.col ? o.push(r.getTopStartCorner().col, s + a - 1, e.row) : o.push(s, s + a - 1, e.row) : (Hp(t) || zs(t) && l || hx(t)) && o.push(s, s + a - 1, e.row), i.selectColumns(...o);
}
function Vae(t, e, n, i) {
  if (!this.hot.view.isMouseDown())
    return;
  const r = this._getHeaderTreeNodeDataByCoords(e);
  if (!r)
    return;
  const {
    columnIndex: o,
    origColspan: s
  } = r, a = this.hot.getSelectedRangeLast(), l = a.getTopStartCorner(), u = a.getBottomEndCorner(), {
    from: c
  } = a;
  i.column = !0, i.cell = !0;
  const h = [], d = ft(e.row, -1 / 0, -1);
  e.col < c.col ? h.push(u.col, o, d) : e.col > c.col ? h.push(l.col, o + s - 1, d) : h.push(o, o + s - 1, d), this.hot.selection.selectColumns(...h);
}
function Wae() {
  qa(hA, this, !1);
}
function $ae() {
  const {
    navigableHeaders: t
  } = this.hot.getSettings();
  if (!this.hot.view.isMouseDown() || !tt(hA, this) || !t)
    return;
  const e = this.hot.getSelectedRangeLast(), n = e.getTopStartCorner().col, i = e.getBottomEndCorner().col, {
    columnIndex: r,
    origColspan: o
  } = tt(vt, this).getHeaderTreeNodeData(tt(Ih, this).row, tt(Ih, this).col);
  if (e.setHighlight(tt(Ih, this)), o > e.getWidth() || r < n || r + o - 1 > i) {
    const s = tt(vt, this).findTopMostEntireHeaderLevel(ft(n, r, r + o - 1), ft(i, r, r + o - 1));
    e.highlight.row = s, e.highlight.col = e.from.col;
  }
}
function Uae(t) {
  const {
    highlight: e
  } = this.hot.getSelectedRangeLast(), n = this.hot._createCellCoords(e.row + t.row, e.col + t.col);
  if (!(n.isHeader() && n.col >= 0))
    return;
  const r = tt(vt, this).findLeftMostColumnIndex(n.row, n.col), o = tt(vt, this).findRightMostColumnIndex(n.row, n.col);
  if (t.col < 0) {
    const s = e.col >= r && e.col <= o ? r - 1 : o, a = this.hot.columnIndexMapper.getNearestNotHiddenIndex(s, -1);
    a === null ? t.col = -this.hot.view.countRenderableColumnsInRange(0, e.col) : t.col = -Math.max(this.hot.view.countRenderableColumnsInRange(a, e.col) - 1, 1);
  } else if (t.col > 0) {
    const s = e.col >= r && e.col <= o ? o + 1 : r, a = this.hot.columnIndexMapper.getNearestNotHiddenIndex(s, 1);
    a === null ? t.col = this.hot.view.countRenderableColumnsInRange(e.col, this.hot.countCols()) : t.col = Math.max(this.hot.view.countRenderableColumnsInRange(e.col, a) - 1, 1);
  }
}
function Yae(t, e) {
  const n = t.row, i = this._getHeaderTreeNodeDataByCoords({
    row: n,
    col: t.col
  }), r = this._getHeaderTreeNodeDataByCoords({
    row: n,
    col: e.col
  });
  e.col < t.col ? (i && (t.col = i.columnIndex + i.origColspan - 1), r && (e.col = r.columnIndex)) : e.col >= t.col && (i && (t.col = i.columnIndex), r && (e.col = r.columnIndex + r.origColspan - 1));
}
function jae(t) {
  t.length = 0;
  for (let e = 0; e < tt(vt, this).getLayersCount(); e++)
    t.push(this.headerRendererFactory(e));
}
function Gae(t) {
  const e = tt(vt, this).getLayersCount();
  let n = t.startColumn, i = !!e;
  for (let r = 0; r < e; r++) {
    const o = tt(vt, this).findLeftMostColumnIndex(r, t.startColumn), s = this.hot.columnIndexMapper.getRenderableFromVisualIndex(o);
    if (o >= 0 && (i = !1), Dn(s) && s < t.startColumn) {
      n = s;
      break;
    }
  }
  t.startColumn = i ? tt(vt, this).getHeaderTreeNodeData(0, n).columnIndex : n;
}
function zae(t, e) {
  const n = this.ghostTable.getWidth(e);
  return t > n ? t : n;
}
function Kae(t, e, n) {
  var i;
  const {
    label: r
  } = (i = tt(vt, this).getHeaderTreeNodeData(n, e)) !== null && i !== void 0 ? i : {
    label: ""
  };
  return r;
}
function Qae(t, e) {
  if (t < 0)
    return this.hot.getCell(t, tt(vt, this).findLeftMostColumnIndex(t, e), !0);
}
function Xae() {
  this.updatePlugin();
}
function qae(t, e) {
  e || this.updatePlugin();
}
function zc(t, e, n) {
  return (e = Zae(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Zae(t) {
  var e = Jae(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Jae(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class ele {
  constructor(e, n) {
    zc(this, "hot", void 0), zc(this, "data", null), zc(this, "plugin", void 0), zc(this, "parentReference", /* @__PURE__ */ new WeakMap()), zc(this, "cache", {
      levels: [],
      levelCount: 0,
      rows: [],
      nodeInfo: /* @__PURE__ */ new WeakMap()
    }), this.hot = n, this.plugin = e;
  }
  /**
   * Set the data for the manager.
   *
   * @param {Handsontable.CellValue[][]|Handsontable.RowObject[]} data Data for the manager.
   */
  setData(e) {
    this.data = e;
  }
  /**
   * Get the data cached in the manager.
   *
   * @returns {Handsontable.CellValue[][]|Handsontable.RowObject[]}
   */
  getData() {
    return this.data;
  }
  /**
   * Load the "raw" source data, without NestedRows' modifications.
   *
   * @returns {Handsontable.CellValue[][]|Handsontable.RowObject[]}
   */
  getRawSourceData() {
    let e = null;
    return this.plugin.disableCoreAPIModifiers(), e = this.hot.getSourceData(), this.plugin.enableCoreAPIModifiers(), e;
  }
  /**
   * Update the Data Manager with new data and refresh cache.
   *
   * @param {Handsontable.CellValue[][]|Handsontable.RowObject[]} data Data for the manager.
   */
  updateWithData(e) {
    this.setData(e), this.rewriteCache();
  }
  /**
   * Rewrite the nested structure cache.
   *
   * @private
   */
  rewriteCache() {
    this.cache = {
      levels: [],
      levelCount: 0,
      rows: [],
      nodeInfo: /* @__PURE__ */ new WeakMap()
    }, Ae(0, this.data.length - 1, (e) => {
      this.cacheNode(this.data[e], 0, null);
    });
  }
  /**
   * Cache a data node.
   *
   * @private
   * @param {object} node Node to cache.
   * @param {number} level Level of the node.
   * @param {object} parent Parent of the node.
   */
  cacheNode(e, n, i) {
    this.cache.levels[n] || (this.cache.levels[n] = [], this.cache.levelCount += 1), this.cache.levels[n].push(e), this.cache.rows.push(e), this.cache.nodeInfo.set(e, {
      parent: i,
      row: this.cache.rows.length - 1,
      level: n
    }), this.hasChildren(e) && T(e.__children, (r) => {
      this.cacheNode(r, n + 1, e);
    });
  }
  /**
   * Get the date for the provided visual row number.
   *
   * @param {number} row Row index.
   * @returns {object}
   */
  getDataObject(e) {
    return e == null ? null : this.cache.rows[e];
  }
  /**
   * Read the row tree in search for a specific row index or row object.
   *
   * @private
   * @param {object} parent The initial parent object.
   * @param {number} readCount Number of read nodes.
   * @param {number} neededIndex The row index we search for.
   * @param {object} neededObject The row object we search for.
   * @returns {number|object}
   */
  readTreeNodes(e, n, i, r) {
    let o = !1, s = n;
    if (isNaN(s) && s.end)
      return s;
    let a = e;
    return a || (a = {
      __children: this.data
    }, o = !0, s -= 1), i != null && s === i ? {
      result: a,
      end: !0
    } : r != null && a === r ? {
      result: s,
      end: !0
    } : (s += 1, a.__children && T(a.__children, (l) => {
      if (this.parentReference.set(l, o ? null : a), s = this.readTreeNodes(l, s, i, r), isNaN(s) && s.end)
        return !1;
    }), s);
  }
  /**
   * Mock a parent node.
   *
   * @private
   * @returns {*}
   */
  mockParent() {
    const e = this.mockNode();
    return e.__children = this.data, e;
  }
  /**
   * Mock a data node.
   *
   * @private
   * @returns {{}}
   */
  mockNode() {
    const e = {};
    return Fe(this.data[0], (n, i) => {
      e[i] = null;
    }), e;
  }
  /**
   * Get the row index for the provided row object.
   *
   * @param {object} rowObj The row object.
   * @returns {number} Row index.
   */
  getRowIndex(e) {
    return e == null ? null : this.cache.nodeInfo.get(e).row;
  }
  /**
   * Get the index of the provided row index/row object within its parent.
   *
   * @param {number|object} row Row index / row object.
   * @returns {number}
   */
  getRowIndexWithinParent(e) {
    let n = null;
    isNaN(e) ? n = e : n = this.getDataObject(e);
    const i = this.getRowParent(e);
    return i == null ? this.data.indexOf(n) : i.__children.indexOf(n);
  }
  /**
   * Count all rows (including all parents and children).
   *
   * @returns {number}
   */
  countAllRows() {
    const e = {
      __children: this.data
    };
    return this.countChildren(e);
  }
  /**
   * Count children of the provided parent.
   *
   * @param {object|number} parent Parent node.
   * @returns {number} Children count.
   */
  countChildren(e) {
    let n = 0, i = e;
    return isNaN(i) || (i = this.getDataObject(i)), !i || !i.__children ? 0 : (T(i.__children, (r) => {
      n += 1, r.__children && (n += this.countChildren(r));
    }), n);
  }
  /**
   * Get the parent of the row at the provided index.
   *
   * @param {number|object} row Physical row index.
   * @returns {object}
   */
  getRowParent(e) {
    let n;
    return isNaN(e) ? n = e : n = this.getDataObject(e), this.getRowObjectParent(n);
  }
  /**
   * Get the parent of the provided row object.
   *
   * @private
   * @param {object} rowObject The row object (tree node).
   * @returns {object|null}
   */
  getRowObjectParent(e) {
    return !e || typeof e != "object" ? null : this.cache.nodeInfo.get(e).parent;
  }
  /**
   * Get the nesting level for the row with the provided row index.
   *
   * @param {number} row Row index.
   * @returns {number|null} Row level or null, when row doesn't exist.
   */
  getRowLevel(e) {
    let n = null;
    return isNaN(e) ? n = e : n = this.getDataObject(e), n ? this.getRowObjectLevel(n) : null;
  }
  /**
   * Get the nesting level for the row with the provided row index.
   *
   * @private
   * @param {object} rowObject Row object.
   * @returns {number} Row level.
   */
  getRowObjectLevel(e) {
    return e == null ? null : this.cache.nodeInfo.get(e).level;
  }
  /**
   * Check if the provided row/row element has children.
   *
   * @param {number|object} row Row number or row element.
   * @returns {boolean}
   */
  hasChildren(e) {
    let n = e;
    return isNaN(n) || (n = this.getDataObject(n)), !!(n.__children && n.__children.length);
  }
  /**
   * Returns `true` if the row at the provided index has a parent.
   *
   * @param {number} index Row index.
   * @returns {boolean} `true` if the row at the provided index has a parent, `false` otherwise.
   */
  isChild(e) {
    return this.getRowParent(e) !== null;
  }
  /**
   * Get child at a provided index from the parent element.
   *
   * @param {object} parent The parent row object.
   * @param {number} index Index of the child element to be retrieved.
   * @returns {object|null} The child element or `null` if the child doesn't exist.
   */
  getChild(e, n) {
    var i;
    return ((i = e.__children) === null || i === void 0 ? void 0 : i[n]) || null;
  }
  /**
   * Return `true` of the row at the provided index is located at the topmost level.
   *
   * @param {number} index Row index.
   * @returns {boolean} `true` of the row at the provided index is located at the topmost level, `false` otherwise.
   */
  isRowHighestLevel(e) {
    return !this.isChild(e);
  }
  /**
   * Return `true` if the provided row index / row object represents a parent in the nested structure.
   *
   * @param {number|object} row Row index / row object.
   * @returns {boolean} `true` if the row is a parent, `false` otherwise.
   */
  isParent(e) {
    var n;
    let i = e;
    return isNaN(i) || (i = this.getDataObject(i)), i && !!i.__children && ((n = i.__children) === null || n === void 0 ? void 0 : n.length) !== 0;
  }
  /**
   * Add a child to the provided parent. It's optional to add a row object as the "element".
   *
   * @param {object} parent The parent row object.
   * @param {object} [element] The element to add as a child.
   */
  addChild(e, n) {
    let i = n;
    this.hot.runHooks("beforeAddChild", e, i);
    let r = null;
    e && (r = this.getRowIndex(e)), this.hot.runHooks("beforeCreateRow", r + this.countChildren(e) + 1, 1);
    let o = e;
    e || (o = this.mockParent()), o.__children || (o.__children = []), i || (i = this.mockNode()), o.__children.push(i), this.rewriteCache();
    const s = this.getRowIndex(i);
    this.hot.rowIndexMapper.insertIndexes(s, 1), this.hot.runHooks("afterCreateRow", s, 1), this.hot.runHooks("afterAddChild", e, i);
  }
  /**
   * Add a child node to the provided parent at a specified index.
   *
   * @param {object} parent Parent node.
   * @param {number} index Index to insert the child element at.
   * @param {object} [element] Element (node) to insert.
   */
  addChildAtIndex(e, n, i) {
    let r = i, o;
    if (r || (r = this.mockNode()), this.hot.runHooks("beforeAddChild", e, r, n), e) {
      const a = this.getRowIndex(e) + n + 1;
      this.hot.runHooks("beforeCreateRow", a, 1), e.__children.splice(n, null, r), this.rewriteCache(), this.plugin.disableCoreAPIModifiers(), this.hot.setSourceDataAtCell(this.getRowIndexWithinParent(e), "__children", e.__children, "NestedRows.addChildAtIndex"), this.hot.rowIndexMapper.insertIndexes(a, 1), this.plugin.enableCoreAPIModifiers(), this.hot.runHooks("afterCreateRow", a, 1), o = a;
    } else
      this.plugin.disableCoreAPIModifiers(), this.hot.alter("insert_row_above", n, 1, "NestedRows.addChildAtIndex"), this.plugin.enableCoreAPIModifiers(), o = this.getRowIndex(this.data[n]);
    r = this.getDataObject(o), this.hot.runHooks("afterAddChild", e, r, n);
  }
  /**
   * Add a sibling element at the specified index.
   *
   * @param {number} index New element sibling's index.
   * @param {('above'|'below')} where Direction in which the sibling is to be created.
   */
  addSibling(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "below";
    const i = this.translateTrimmedRow(e), r = this.getRowParent(i), o = this.getRowIndexWithinParent(i);
    switch (n) {
      case "below":
        this.addChildAtIndex(r, o + 1, null);
        break;
      case "above":
        this.addChildAtIndex(r, o, null);
        break;
    }
  }
  /**
   * Detach the provided element from its parent and add it right after it.
   *
   * @param {object|Array} elements Row object or an array of selected coordinates.
   * @param {boolean} [forceRender=true] If true (default), it triggers render after finished.
   */
  detachFromParent(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, i = null;
    const r = [];
    Array.isArray(e) ? (Ae(e[0], e[2], (d) => {
      const g = this.translateTrimmedRow(d);
      r.push(this.getDataObject(g));
    }), Ae(0, r.length - 2, (d) => {
      this.detachFromParent(r[d], !1);
    }), i = r[r.length - 1]) : i = e;
    const o = this.getRowIndex(i), s = this.countChildren(i), a = this.getRowIndexWithinParent(i), l = this.getRowParent(i), u = this.getRowParent(l), c = this.getRowIndex(u);
    let h = null;
    if (this.hot.runHooks("beforeDetachChild", l, i), a != null) {
      const d = Array.from(new Array(o + s + 1).keys()).splice(-1 * (s + 1));
      if (this.hot.runHooks("beforeRemoveRow", o, s + 1, d, this.plugin.pluginName), l.__children.splice(a, 1), this.rewriteCache(), this.hot.runHooks("afterRemoveRow", o, s + 1, d, this.plugin.pluginName), u) {
        h = c + this.countChildren(u);
        const g = this.getChild(u, this.countChildren(u) - 1), m = this.getRowIndex(g);
        this.hot.runHooks("beforeCreateRow", m + 1, s + 1, this.plugin.pluginName), u.__children.push(i);
      } else
        h = this.hot.countRows() + 1, this.hot.runHooks("beforeCreateRow", h - 2, s + 1, this.plugin.pluginName), this.data.push(i);
    }
    this.rewriteCache(), this.hot.runHooks("afterCreateRow", h - 2, s + 1, this.plugin.pluginName), this.hot.runHooks("afterDetachChild", l, i, this.getRowIndex(i)), n && this.hot.render();
  }
  /**
   * Filter the data by the `logicRows` array.
   *
   * @private
   * @param {number} index Index of the first row to remove.
   * @param {number} amount Number of elements to remove.
   * @param {Array} logicRows Array of indexes to remove.
   */
  filterData(e, n, i) {
    const r = [];
    T(i, (o) => {
      r.push(this.getDataObject(o));
    }), T(r, (o) => {
      const s = this.getRowIndexWithinParent(o), a = this.getRowParent(o);
      a === null ? this.data.splice(s, 1) : a.__children.splice(s, 1);
    }), this.rewriteCache();
  }
  /**
   * Used to splice the source data. Needed to properly modify the nested structure, which wouldn't work with the
   * default script.
   *
   * @private
   * @param {number} index Physical index of the element at the splice beginning.
   * @param {number} amount Number of elements to be removed.
   * @param {object[]} elements Array of row objects to add.
   */
  spliceData(e, n, i) {
    const r = this.getDataObject(e - 1);
    let o = null, s = e;
    r && r.__children && r.__children.length === 0 ? (o = r, s = 0) : e < this.countAllRows() && (o = this.getRowParent(e), s = this.getRowIndexWithinParent(e)), o ? i ? o.__children.splice(s, n, ...i) : o.__children.splice(s, n) : i ? this.data.splice(s, n, ...i) : this.data.splice(s, n), this.rewriteCache();
  }
  /**
   * Update the `__children` key of the upmost parent of the provided row object.
   *
   * @private
   * @param {object} rowElement Row object.
   */
  syncRowWithRawSource(e) {
    let n = e, i = null;
    do
      i = this.getRowParent(i), i !== null && (n = i);
    while (i !== null);
    this.plugin.disableCoreAPIModifiers(), this.hot.setSourceDataAtCell(this.getRowIndex(n), "__children", n.__children, "NestedRows.syncRowWithRawSource"), this.plugin.enableCoreAPIModifiers();
  }
  /* eslint-disable jsdoc/require-param */
  /**
   * Move a single row.
   *
   * @param {number} fromIndex Index of the row to be moved.
   * @param {number} toIndex Index of the destination.
   * @param {boolean} moveToCollapsed `true` if moving a row to a collapsed parent.
   * @param {boolean} moveToLastChild `true` if moving a row to be a last child of the new parent.
   */
  /* eslint-enable jsdoc/require-param */
  moveRow(e, n, i, r) {
    const o = n === this.hot.countRows(), s = this.getRowParent(e), a = this.getRowIndexWithinParent(e), l = s.__children.slice(a, a + 1), u = e > n;
    let c = o ? this.getRowParent(n - 1) : this.getRowParent(n);
    c == null && (c = this.getRowParent(n - 1)), c == null && (c = this.getDataObject(n - 1)), c ? c.__children || (c.__children = []) : (c = this.getDataObject(n), c.__children = []);
    const h = o || i || r ? c.__children.length : this.getRowIndexWithinParent(n), d = s === c;
    c.__children.splice(h, 0, l[0]), s.__children.splice(a + (u && d ? 1 : 0), 1), this.syncRowWithRawSource(s), d || this.syncRowWithRawSource(c);
  }
  /**
   * Translate the visual row index to the physical index, taking into consideration the state of collapsed rows.
   *
   * @private
   * @param {number} row Row index.
   * @returns {number}
   */
  translateTrimmedRow(e) {
    return this.plugin.collapsingUI ? this.plugin.collapsingUI.translateTrimmedRow(e) : e;
  }
  /**
   * Translate the physical row index to the visual index, taking into consideration the state of collapsed rows.
   *
   * @private
   * @param {number} row Row index.
   * @returns {number}
   */
  untranslateTrimmedRow(e) {
    return this.plugin.collapsingUI ? this.plugin.collapsingUI.untranslateTrimmedRow(e) : e;
  }
}
const tle = ele;
function vI(t, e, n) {
  return (e = nle(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function nle(t) {
  var e = ile(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function ile(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class rle {
  constructor(e, n) {
    vI(this, "hot", void 0), vI(this, "plugin", void 0), this.hot = n, this.plugin = e;
  }
}
const $R = rle;
class Vo extends $R {
  /**
   * CSS classes used in the row headers.
   *
   * @type {object}
   */
  static get CSS_CLASSES() {
    return {
      indicatorContainer: "ht_nestingLevels",
      parent: "ht_nestingParent",
      indicator: "ht_nestingLevel",
      emptyIndicator: "ht_nestingLevel_empty",
      button: "ht_nestingButton",
      expandButton: "ht_nestingExpand",
      collapseButton: "ht_nestingCollapse"
    };
  }
  constructor(e, n) {
    super(e, n), this.dataManager = this.plugin.dataManager, this.collapsingUI = this.plugin.collapsingUI, this.rowHeaderWidthCache = null;
  }
  /**
   * Append nesting indicators and buttons to the row headers.
   *
   * @private
   * @param {number} row Row index.
   * @param {HTMLElement} TH TH 3element.
   */
  appendLevelIndicators(e, n) {
    const i = this.hot.toPhysicalRow(e), r = this.dataManager.getRowLevel(i), o = this.dataManager.getDataObject(i), s = n.getElementsByTagName("DIV")[0], a = s.querySelector("span.rowHeader"), l = s.querySelectorAll('[class^="ht_nesting"]'), u = this.hot.getSettings().ariaTags;
    if (T(l, (c) => {
      c && s.removeChild(c);
    }), Q(n, Vo.CSS_CLASSES.indicatorContainer), r) {
      const {
        rootDocument: c
      } = this.hot, h = a.cloneNode(!0);
      s.innerHTML = "", Ae(0, r - 1, () => {
        const d = c.createElement("SPAN");
        Q(d, Vo.CSS_CLASSES.emptyIndicator), s.appendChild(d);
      }), s.appendChild(h);
    }
    if (this.dataManager.hasChildren(o)) {
      const c = this.hot.rootDocument.createElement("DIV");
      u && Ee(c, [gl()]), Q(n, Vo.CSS_CLASSES.parent), this.collapsingUI.areChildrenCollapsed(i) ? (Q(c, `${Vo.CSS_CLASSES.button} ${Vo.CSS_CLASSES.expandButton}`), u && Ee(n, [Gi(!1)])) : (Q(c, `${Vo.CSS_CLASSES.button} ${Vo.CSS_CLASSES.collapseButton}`), u && Ee(n, [Gi(!0)])), s.appendChild(c);
    }
  }
  /**
   * Update the row header width according to number of levels in the dataset.
   *
   * @private
   * @param {number} deepestLevel Cached deepest level of nesting.
   */
  updateRowHeaderWidth(e) {
    let n = e;
    n || (n = this.dataManager.cache.levelCount), this.rowHeaderWidthCache = Math.max(50, 11 + 10 * n + 25), this.hot.render();
  }
}
const V_ = Vo;
class ole extends $R {
  constructor(e, n) {
    var i;
    super(e, n), i = this, this.dataManager = this.plugin.dataManager, this.collapsedRows = [], this.collapsedRowsStash = {
      stash: function() {
        let r = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
        i.lastCollapsedRows = i.collapsedRows.slice(0), i.expandMultipleChildren(i.lastCollapsedRows, r);
      },
      shiftStash: function(r, o) {
        let s = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1;
        o == null && (o = 1 / 0), T(i.lastCollapsedRows, (a, l) => {
          a >= r && a < o && (i.lastCollapsedRows[l] = a + s);
        });
      },
      applyStash: function() {
        let r = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
        i.collapseMultipleChildren(i.lastCollapsedRows, r), i.lastCollapsedRows = void 0;
      },
      trimStash: (r, o) => {
        Ae(r, r + o - 1, (s) => {
          const a = this.lastCollapsedRows.indexOf(s);
          a > -1 && this.lastCollapsedRows.splice(a, 1);
        });
      }
    };
  }
  /**
   * Collapse the children of the row passed as an argument.
   *
   * @param {number|object} row The parent row.
   * @param {boolean} [forceRender=true] Whether to render the table after the function ends.
   * @param {boolean} [doTrimming=true] I determine whether collapsing should envolve trimming rows.
   * @returns {Array}
   */
  collapseChildren(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
    const r = [];
    let o = null, s = null, a = null;
    return isNaN(e) ? (o = e, s = this.dataManager.getRowIndex(o)) : (o = this.dataManager.getDataObject(e), s = e), this.dataManager.hasChildren(o) && T(o.__children, (l) => {
      r.push(this.dataManager.getRowIndex(l));
    }), a = this.collapseRows(r, !0, !1), i && this.trimRows(a), n && this.renderAndAdjust(), this.collapsedRows.indexOf(s) === -1 && this.collapsedRows.push(s), a;
  }
  /**
   * Collapse multiple children.
   *
   * @param {Array} rows Rows to collapse (including their children).
   * @param {boolean} [forceRender=true] `true` if the table should be rendered after finishing the function.
   * @param {boolean} [doTrimming=true] I determine whether collapsing should envolve trimming rows.
   */
  collapseMultipleChildren(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
    const r = [];
    T(e, (o) => {
      r.push(...this.collapseChildren(o, !1, !1));
    }), i && this.trimRows(r), n && this.renderAndAdjust();
  }
  /**
   * Collapse a single row.
   *
   * @param {number} rowIndex Index of the row to collapse.
   * @param {boolean} [recursive=true] `true` if it should collapse the row's children.
   */
  collapseRow(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
    this.collapseRows([e], n);
  }
  /**
   * Collapse multiple rows.
   *
   * @param {Array} rowIndexes Array of row indexes to collapse.
   * @param {boolean} [recursive=true] `true` if it should collapse the rows' children.
   * @param {boolean} [doTrimming=true] I determine whether collapsing should envolve trimming rows.
   * @returns {Array} Rows prepared for trimming (or trimmed, if doTrimming == true).
   */
  collapseRows(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    const r = [];
    return T(e, (o) => {
      r.push(o), n && this.collapseChildRows(o, r);
    }), i && this.trimRows(r), r;
  }
  /**
   * Collapse child rows of the row at the provided index.
   *
   * @param {number} parentIndex Index of the parent node.
   * @param {Array} [rowsToTrim=[]] Array of rows to trim. Defaults to an empty array.
   * @param {boolean} [recursive] `true` if the collapsing process should be recursive.
   * @param {boolean} [doTrimming=true] I determine whether collapsing should envolve trimming rows.
   */
  collapseChildRows(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [], i = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1;
    if (this.dataManager.hasChildren(e)) {
      const r = this.dataManager.getDataObject(e);
      T(r.__children, (o) => {
        const s = this.dataManager.getRowIndex(o);
        n.push(s), this.collapseChildRows(s, n);
      });
    }
    i && this.trimRows(n);
  }
  /**
   * Expand a single row.
   *
   * @param {number} rowIndex Index of the row to expand.
   * @param {boolean} [recursive=true] `true` if it should expand the row's children recursively.
   */
  expandRow(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
    this.expandRows([e], n);
  }
  /**
   * Expand multiple rows.
   *
   * @param {Array} rowIndexes Array of indexes of the rows to expand.
   * @param {boolean} [recursive=true] `true` if it should expand the rows' children recursively.
   * @param {boolean} [doTrimming=true] I determine whether collapsing should envolve trimming rows.
   * @returns {Array} Array of row indexes to be untrimmed.
   */
  expandRows(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
    const r = [];
    return T(e, (o) => {
      r.push(o), n && this.expandChildRows(o, r);
    }), i && this.untrimRows(r), r;
  }
  /**
   * Expand child rows of the provided index.
   *
   * @param {number} parentIndex Index of the parent row.
   * @param {Array} [rowsToUntrim=[]] Array of the rows to be untrimmed.
   * @param {boolean} [recursive] `true` if it should expand the rows' children recursively.
   * @param {boolean} [doTrimming=false] I determine whether collapsing should envolve trimming rows.
   */
  expandChildRows(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [], i = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1;
    if (this.dataManager.hasChildren(e)) {
      const r = this.dataManager.getDataObject(e);
      T(r.__children, (o) => {
        if (!this.isAnyParentCollapsed(o)) {
          const s = this.dataManager.getRowIndex(o);
          n.push(s), this.expandChildRows(s, n);
        }
      });
    }
    i && this.untrimRows(n);
  }
  /**
   * Expand the children of the row passed as an argument.
   *
   * @param {number|object} row Parent row.
   * @param {boolean} [forceRender=true] Whether to render the table after the function ends.
   * @param {boolean} [doTrimming=true] If set to `true`, the trimming will be applied when the function finishes.
   * @returns {number[]}
   */
  expandChildren(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
    const r = [];
    let o = null, s = null, a = null;
    return isNaN(e) ? (o = e, s = this.dataManager.getRowIndex(e)) : (o = this.dataManager.getDataObject(e), s = e), this.collapsedRows.splice(this.collapsedRows.indexOf(s), 1), this.dataManager.hasChildren(o) && T(o.__children, (l) => {
      const u = this.dataManager.getRowIndex(l);
      r.push(u);
    }), a = this.expandRows(r, !0, !1), i && this.untrimRows(a), n && this.renderAndAdjust(), a;
  }
  /**
   * Expand multiple rows' children.
   *
   * @param {Array} rows Array of rows which children are about to be expanded.
   * @param {boolean} [forceRender=true] `true` if the table should render after finishing the function.
   * @param {boolean} [doTrimming=true] `true` if the rows should be untrimmed after finishing the function.
   */
  expandMultipleChildren(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
    const r = [];
    T(e, (o) => {
      r.push(...this.expandChildren(o, !1, !1));
    }), i && this.untrimRows(r), n && this.renderAndAdjust();
  }
  /**
   * Collapse all collapsable rows.
   */
  collapseAll() {
    const e = this.dataManager.getData(), n = [];
    T(e, (i) => {
      this.dataManager.hasChildren(i) && n.push(i);
    }), this.collapseMultipleChildren(n), this.renderAndAdjust();
  }
  /**
   * Expand all collapsable rows.
   */
  expandAll() {
    const e = this.dataManager.getData(), n = [];
    T(e, (i) => {
      this.dataManager.hasChildren(i) && n.push(i);
    }), this.expandMultipleChildren(n), this.renderAndAdjust();
  }
  /**
   * Trim rows.
   *
   * @param {Array} rows Physical row indexes.
   */
  trimRows(e) {
    this.hot.batchExecution(() => {
      T(e, (n) => {
        this.plugin.collapsedRowsMap.setValueAtIndex(n, !0);
      });
    }, !0);
  }
  /**
   * Untrim rows.
   *
   * @param {Array} rows Physical row indexes.
   */
  untrimRows(e) {
    this.hot.batchExecution(() => {
      T(e, (n) => {
        this.plugin.collapsedRowsMap.setValueAtIndex(n, !1);
      });
    }, !0);
  }
  /**
   * Check if all child rows are collapsed.
   *
   * @private
   * @param {number|object|null} row The parent row. `null` for the top level.
   * @returns {boolean}
   */
  areChildrenCollapsed(e) {
    let n = isNaN(e) ? e : this.dataManager.getDataObject(e), i = !0;
    return n === null && (n = {
      __children: this.dataManager.data
    }), this.dataManager.hasChildren(n) && T(n.__children, (r) => {
      const o = this.dataManager.getRowIndex(r);
      if (!this.plugin.collapsedRowsMap.getValueAtIndex(o))
        return i = !1, !1;
    }), i;
  }
  /**
   * Check if any of the row object parents are collapsed.
   *
   * @private
   * @param {object} rowObj Row object.
   * @returns {boolean}
   */
  isAnyParentCollapsed(e) {
    let n = e;
    for (; n !== null; ) {
      n = this.dataManager.getRowParent(n);
      const i = this.dataManager.getRowIndex(n);
      if (this.collapsedRows.indexOf(i) > -1)
        return !0;
    }
    return !1;
  }
  /**
   * Toggle collapsed state. Callback for the `beforeOnCellMousedown` hook.
   *
   * @private
   * @param {MouseEvent} event `mousedown` event.
   * @param {object} coords Coordinates of the clicked cell/header.
   */
  toggleState(e, n) {
    if (n.col >= 0)
      return;
    const i = this.translateTrimmedRow(n.row);
    Te(e.target, V_.CSS_CLASSES.button) && (this.areChildrenCollapsed(i) ? this.expandChildren(i) : this.collapseChildren(i), Zi(e));
  }
  /**
   * Translate visual row after trimming to physical base row index.
   *
   * @private
   * @param {number} row Row index.
   * @returns {number} Base row index.
   */
  translateTrimmedRow(e) {
    return this.hot.toPhysicalRow(e);
  }
  /**
   * Translate physical row after trimming to visual base row index.
   *
   * @private
   * @param {number} row Row index.
   * @returns {number} Base row index.
   */
  untranslateTrimmedRow(e) {
    return this.hot.toVisualRow(e);
  }
  /**
   * Helper function to render the table and call the `adjustElementsSize` method.
   *
   * @private
   */
  renderAndAdjust() {
    this.hot.render(), this.hot.view.adjustElementsSize();
  }
}
const sle = ole;
function ale(t, e, n) {
  lle(t, e), e.set(t, n);
}
function lle(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function ule(t, e, n) {
  return (e = cle(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function cle(t) {
  var e = hle(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function hle(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function dle(t, e) {
  return t.get(fle(t, e));
}
function fle(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
var yI = /* @__PURE__ */ new WeakMap();
class gle extends $R {
  constructor() {
    super(...arguments), ule(this, "dataManager", this.plugin.dataManager), ale(this, yI, {
      row_above: (e, n) => {
        const i = n[n.length - 1];
        this.dataManager.addSibling(i.start.row, "above");
      },
      row_below: (e, n) => {
        const i = n[n.length - 1];
        this.dataManager.addSibling(i.start.row, "below");
      }
    });
  }
  /**
   * Append options to the context menu. (Propagated from the `afterContextMenuDefaultOptions` hook callback)
   * f.
   *
   * @private
   * @param {object} defaultOptions Default context menu options.
   * @returns {*}
   */
  appendOptions(e) {
    const n = [{
      key: "add_child",
      name() {
        return this.getTranslatedPhrase(fw);
      },
      callback: () => {
        const i = this.dataManager.translateTrimmedRow(this.hot.getSelectedLast()[0]), r = this.dataManager.getDataObject(i);
        this.dataManager.addChild(r);
      },
      disabled: () => {
        const i = this.hot.getSelectedLast();
        return !i || i[0] < 0 || this.hot.selection.isSelectedByColumnHeader() || this.hot.countRows() >= this.hot.getSettings().maxRows;
      }
    }, {
      key: "detach_from_parent",
      name() {
        return this.getTranslatedPhrase(gw);
      },
      callback: () => {
        this.dataManager.detachFromParent(this.hot.getSelectedLast());
      },
      disabled: () => {
        const i = this.hot.getSelectedLast(), r = this.dataManager.translateTrimmedRow(i[0]);
        return !this.dataManager.getRowParent(r) || !i || i[0] < 0 || this.hot.selection.isSelectedByColumnHeader() || this.hot.countRows() >= this.hot.getSettings().maxRows;
      }
    }, {
      name: "---------"
    }];
    return Ae(0, e.items.length - 1, (i) => {
      if (i === 0)
        return T(n, (r, o) => {
          e.items.splice(i + o, 0, r);
        }), !1;
    }), this.modifyRowInsertingOptions(e);
  }
  /**
   * Modify how the row inserting options work.
   *
   * @private
   * @param {object} defaultOptions Default context menu items.
   * @returns {*}
   */
  modifyRowInsertingOptions(e) {
    return Ae(0, e.items.length - 1, (n) => {
      const i = dle(yI, this)[e.items[n].key];
      i != null && (e.items[n].callback = i);
    }), e;
  }
}
const mle = gle;
function Pg(t, e, n) {
  return (e = ple(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function ple(t) {
  var e = wle(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function wle(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class Ale {
  constructor(e) {
    Pg(this, "plugin", void 0), Pg(this, "hot", void 0), Pg(this, "dataManager", void 0), Pg(this, "collapsingUI", void 0), this.plugin = e, this.hot = e.hot, this.dataManager = e.dataManager, this.collapsingUI = e.collapsingUI;
  }
  /**
   * `beforeRowMove` hook callback.
   *
   * @param {Array} rows Array of visual row indexes to be moved.
   * @param {number} finalIndex Visual row index, being a start index for the moved rows. Points to where the elements
   *   will be placed after the moving action. To check the visualization of the final index, please take a look at
   *   [documentation](@/guides/rows/row-moving/row-moving.md).
   * @param {undefined|number} dropIndex Visual row index, being a drop index for the moved rows. Points to where we
   *   are going to drop the moved elements. To check visualization of drop index please take a look at
   *   [documentation](@/guides/rows/row-moving/row-moving.md).
   * @param {boolean} movePossible Indicates if it's possible to move rows to the desired position.
   * @fires Hooks#afterRowMove
   * @returns {boolean}
   */
  onBeforeRowMove(e, n, i, r) {
    if (this.displayAPICompatibilityWarning({
      rows: e,
      finalIndex: n,
      dropIndex: i,
      movePossible: r
    }))
      return !1;
    this.movedToCollapsed = !1;
    const s = i === this.hot.countRows(), a = s ? this.hot.countSourceRows() : this.dataManager.translateTrimmedRow(i);
    let l = !0;
    const u = e.map((m) => {
      if (!l)
        return !1;
      const w = this.dataManager.translateTrimmedRow(m);
      return l = this.shouldAllowMoving(w, a), w;
    }), c = u.indexOf(a) === -1;
    if (!l || !c)
      return !1;
    const h = this.getBaseParent(u), d = this.getTargetParent(s, a), g = h === d;
    return this.movedToCollapsed = this.collapsingUI.areChildrenCollapsed(d), this.collapsingUI.collapsedRowsStash.stash(), this.shiftCollapsibleParentsLocations(u, a, g), this.moveRows(u, a, d), this.dataManager.rewriteCache(), this.moveCellsMeta(u, a), this.collapsingUI.collapsedRowsStash.applyStash(!1), this.hot.runHooks("afterRowMove", e, n, i, r, r && this.isRowOrderChanged(e, n)), this.hot.render(), this.selectCells(e, i), !1;
  }
  /**
   * Display a `dragRows`/`moveRows` method compatibility warning if needed.
   *
   * @param {object} beforeMoveRowHookArgs A set of arguments from the `beforeMoveRow` hook.
   * @returns {boolean} `true` if is a result of an improper usage of the moving API.
   */
  displayAPICompatibilityWarning(e) {
    const {
      rows: n,
      finalIndex: i,
      dropIndex: r,
      movePossible: o
    } = e;
    let s = !1;
    return Mt(r) && (gt(mt`Since version 8.0.0 of the Handsontable the 'moveRows' method isn't used for moving rows\x20
      when the NestedRows plugin is enabled. Please use the 'dragRows' method instead.`), this.hot.runHooks("afterRowMove", n, i, r, o, !1), s = !0), s;
  }
  /**
   * Check if the moving action should be allowed.
   *
   * @param {number} physicalRowIndex Physical start row index.
   * @param {number} physicalDropIndex Physical drop index.
   * @returns {boolean} `true` if it should continue with the moving action.
   */
  shouldAllowMoving(e, n) {
    return !(this.dataManager.isParent(e) || this.dataManager.isRowHighestLevel(e) || e === n || n === 0);
  }
  /**
   * Get the base row parent.
   *
   * @param {number} physicalStartIndexes Physical start row index.
   * @returns {object|null} The base row parent.
   */
  getBaseParent(e) {
    return this.dataManager.getRowParent(e[0]);
  }
  /**
   * Get the target row parent.
   *
   * @param {boolean} dropToLastRow `true` if the row is moved to the last row of the table.
   * @param {number} physicalDropIndex Physical drop row index.
   * @returns {object|null} The target row parent.
   */
  getTargetParent(e, n) {
    let i = this.dataManager.getRowParent(e ? n - 1 : n);
    return i == null && (i = this.dataManager.getRowParent(n - 1)), i;
  }
  /**
   * Shift the cached collapsible rows position according to the move action.
   *
   * @param {number[]} physicalStartIndexes Physical start row indexes.
   * @param {number} physicalDropIndex Physical drop index.
   * @param {boolean} sameParent `true` if the row's being moved between siblings of the same parent.
   */
  shiftCollapsibleParentsLocations(e, n, i) {
    i || (Math.max(...e) <= n ? this.collapsingUI.collapsedRowsStash.shiftStash(e[0], n, -1 * e.length) : this.collapsingUI.collapsedRowsStash.shiftStash(n, e[0], e.length));
  }
  /**
   * Move the rows at the provided coordinates.
   *
   * @param {number[]} physicalStartIndexes Physical indexes of the rows about to be moved.
   * @param {number} physicalDropIndex Physical drop index.
   * @param {object} targetParent Parent of the destination row.
   */
  moveRows(e, n, i) {
    const r = n === this.dataManager.getRowIndex(i) + this.dataManager.countChildren(i) + 1;
    this.hot.batchRender(() => {
      e.forEach((o) => {
        this.dataManager.moveRow(o, n, this.movedToCollapsed, r);
      });
    });
  }
  /**
   * Move the cell meta for multiple rows.
   *
   * @param {number[]} baseIndexes Array of indexes for the rows being moved.
   * @param {number} targetIndex Index of the destination of the move.
   */
  moveCellsMeta(e, n) {
    const i = [], r = Math.max(...e) < n;
    e.forEach((o) => {
      i.push(this.hot.getCellMetaAtRow(o));
    }), this.hot.spliceCellsMeta(e[0], e.length), this.hot.spliceCellsMeta(n - (r ? i.length : 0), 0, ...i);
  }
  /**
   * Select cells after the move.
   *
   * @param {Array} rows Array of visual row indexes to be moved.
   * @param {undefined|number} dropIndex Visual row index, being a drop index for the moved rows. Points to where we
   *   are going to drop the moved elements. To check visualization of drop index please take a look at
   *   [documentation](@/guides/rows/row-moving/row-moving.md).
   */
  selectCells(e, n) {
    const i = e.length;
    let r = 0, o = 0, s = null, a = null;
    if (this.movedToCollapsed) {
      let l = null;
      e[i - 1] < n ? l = this.dataManager.translateTrimmedRow(n - i) : l = this.dataManager.translateTrimmedRow(n);
      const u = this.dataManager.getRowParent(l === null ? this.hot.countSourceRows() - 1 : l - 1), c = this.dataManager.getRowIndex(u);
      r = this.dataManager.untranslateTrimmedRow(c), o = r;
    } else
      e[i - 1] < n ? (o = n - 1, r = o - i + 1) : (r = n, o = r + i - 1);
    s = this.hot.selection, a = this.hot.countCols() - 1, s.setRangeStart(this.hot._createCellCoords(r, 0)), s.setRangeEnd(this.hot._createCellCoords(o, a), !0);
  }
  // TODO: Reimplementation of function which is inside the `ManualRowMove` plugin.
  /**
   * Indicates if order of rows was changed.
   *
   * @param {Array} movedRows Array of visual row indexes to be moved.
   * @param {number} finalIndex Visual row index, being a start index for the moved rows. Points to where the elements
   *   will be placed after the moving action. To check the visualization of the final index, please take a look at
   *   [documentation](@/guides/rows/row-moving/row-moving.md).
   * @returns {boolean}
   */
  isRowOrderChanged(e, n) {
    return e.some((i, r) => i - r !== n);
  }
}
function Cle(t, e) {
  W_(t, e), e.add(t);
}
function SI(t, e, n) {
  W_(t, e), e.set(t, n);
}
function W_(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Dv(t, e, n) {
  return (e = vle(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function vle(t) {
  var e = yle(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function yle(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function im(t, e) {
  return t.get(fn(t, e));
}
function Jm(t, e, n) {
  return t.set(fn(t, e), n), n;
}
function fn(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const ep = "nestedRows", Sle = 300, bI = ep, ble = "The Nested Rows plugin requires an Array of Objects as a dataset to be provided. The plugin has been disabled.";
var dA = /* @__PURE__ */ new WeakMap(), iu = /* @__PURE__ */ new WeakMap(), Cn = /* @__PURE__ */ new WeakSet();
class Rle extends at {
  constructor() {
    super(...arguments), Cle(this, Cn), Dv(this, "dataManager", null), Dv(this, "headersUI", null), Dv(this, "collapsedRowsMap", null), SI(this, dA, !1), SI(this, iu, !1);
  }
  static get PLUGIN_KEY() {
    return ep;
  }
  static get PLUGIN_PRIORITY() {
    return Sle;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link NestedRows#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[ep];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    this.enabled || (this.collapsedRowsMap = this.hot.rowIndexMapper.registerMap("nestedRows", new Td()), this.dataManager = new tle(this, this.hot), this.collapsingUI = new sle(this, this.hot), this.headersUI = new V_(this, this.hot), this.contextMenuUI = new mle(this, this.hot), this.rowMoveController = new Ale(this), this.addHook("afterInit", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Ble).call(e, ...i);
    }), this.addHook("beforeViewRender", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Fle).call(e, ...i);
    }), this.addHook("modifyRowData", function() {
      return e.onModifyRowData(...arguments);
    }), this.addHook("modifySourceLength", function() {
      return e.onModifySourceLength(...arguments);
    }), this.addHook("beforeDataSplice", function() {
      return e.onBeforeDataSplice(...arguments);
    }), this.addHook("filterData", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Mle).call(e, ...i);
    }), this.addHook("afterContextMenuDefaultOptions", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Ile).call(e, ...i);
    }), this.addHook("afterGetRowHeader", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Ole).call(e, ...i);
    }), this.addHook("beforeOnCellMouseDown", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Tle).call(e, ...i);
    }), this.addHook("beforeRemoveRow", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Hle).call(e, ...i);
    }), this.addHook("afterRemoveRow", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, xle).call(e, ...i);
    }), this.addHook("beforeAddChild", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, _le).call(e, ...i);
    }), this.addHook("afterAddChild", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Nle).call(e, ...i);
    }), this.addHook("beforeDetachChild", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Dle).call(e, ...i);
    }), this.addHook("afterDetachChild", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, kle).call(e, ...i);
    }), this.addHook("modifyRowHeaderWidth", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Ple).call(e, ...i);
    }), this.addHook("afterCreateRow", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Lle).call(e, ...i);
    }), this.addHook("beforeRowMove", function() {
      for (var n = arguments.length, i = new Array(n), r = 0; r < n; r++)
        i[r] = arguments[r];
      return fn(Cn, e, Ele).call(e, ...i);
    }), this.addHook("beforeLoadData", (n) => fn(Cn, this, RI).call(this, n)), this.addHook("beforeUpdateData", (n) => fn(Cn, this, RI).call(this, n)), this.registerShortcuts(), super.enablePlugin());
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.hot.rowIndexMapper.unregisterMap("nestedRows"), this.unregisterShortcuts(), super.disablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`nestedRows`](@/api/options.md#nestedrows)
   */
  updatePlugin() {
    this.disablePlugin();
    const e = this.dataManager.getData();
    this.enablePlugin(), this.dataManager.updateWithData(e), super.updatePlugin();
  }
  /**
   * Register shortcuts responsible for toggling collapsible columns.
   *
   * @private
   */
  registerShortcuts() {
    this.hot.getShortcutManager().getContext("grid").addShortcut({
      keys: [["Enter"]],
      callback: () => {
        const {
          highlight: e
        } = this.hot.getSelectedRangeLast(), n = this.collapsingUI.translateTrimmedRow(e.row);
        return this.collapsingUI.areChildrenCollapsed(n) ? this.collapsingUI.expandChildren(n) : this.collapsingUI.collapseChildren(n), !1;
      },
      runOnlyIf: () => {
        var e, n;
        const i = (e = this.hot.getSelectedRangeLast()) === null || e === void 0 ? void 0 : e.highlight;
        return i && ((n = this.hot.getSelectedRangeLast()) === null || n === void 0 ? void 0 : n.isSingle()) && this.hot.selection.isCellVisible(i) && i.col === -1 && i.row >= 0;
      },
      group: bI,
      relativeToGroup: Js,
      position: "before"
    });
  }
  /**
   * Unregister shortcuts responsible for toggling collapsible columns.
   *
   * @private
   */
  unregisterShortcuts() {
    this.hot.getShortcutManager().getContext("grid").removeShortcutsByGroup(bI);
  }
  /**
   * Enable the modify hook skipping flag - allows retrieving the data from Handsontable without this plugin's
   * modifications.
   *
   * @private
   */
  disableCoreAPIModifiers() {
    Jm(iu, this, !0);
  }
  /**
   * Disable the modify hook skipping flag.
   *
   * @private
   */
  enableCoreAPIModifiers() {
    Jm(iu, this, !1);
  }
  /**
   * `beforeOnCellMousedown` hook callback.
   *
   * @param {MouseEvent} event Mousedown event.
   * @param {object} coords Cell coords.
   * @param {HTMLElement} TD Clicked cell.
   */
  /**
   * The modifyRowData hook callback.
   *
   * @private
   * @param {number} row Visual row index.
   * @returns {boolean}
   */
  onModifyRowData(e) {
    if (!im(iu, this))
      return this.dataManager.getDataObject(e);
  }
  /**
   * Modify the source data length to match the length of the nested structure.
   *
   * @private
   * @returns {number}
   */
  onModifySourceLength() {
    if (!im(iu, this))
      return this.dataManager.countAllRows();
  }
  /**
   * @private
   * @param {number} index The index where the data was spliced.
   * @param {number} amount An amount of items to remove.
   * @param {object} element An element to add.
   * @returns {boolean}
   */
  onBeforeDataSplice(e, n, i) {
    return im(iu, this) || this.dataManager.isRowHighestLevel(e) ? !0 : (this.dataManager.spliceData(e, n, i), !1);
  }
  /**
   * Provide custom source data filtering. It's handled by core method and replaces the native filtering.
   *
   * @param {number} index The index where the data filtering starts.
   * @param {number} amount An amount of rows which filtering applies to.
   * @param {number} physicalRows Physical row indexes.
   * @returns {Array}
   */
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function Ele(t, e, n, i) {
  return this.rowMoveController.onBeforeRowMove(t, e, n, i);
}
function Tle(t, e, n) {
  this.collapsingUI.toggleState(t, e, n);
}
function Mle(t, e, n) {
  return this.collapsingUI.collapsedRowsStash.stash(), this.collapsingUI.collapsedRowsStash.trimStash(n[0], e), this.collapsingUI.collapsedRowsStash.shiftStash(n[0], null, -1 * e), this.dataManager.filterData(t, e, n), Jm(dA, this, !0), this.dataManager.getData().slice();
}
function Ile(t) {
  return this.contextMenuUI.appendOptions(t);
}
function Ole(t, e) {
  this.headersUI.appendLevelIndicators(t, e);
}
function Ple(t) {
  return Math.max(this.headersUI.rowHeaderWidthCache, t);
}
function xle(t, e, n, i) {
  i !== this.pluginName && this.hot._registerTimeout(() => {
    Jm(dA, this, !1), this.headersUI.updateRowHeaderWidth(), this.collapsingUI.collapsedRowsStash.applyStash();
  });
}
function Hle(t, e, n) {
  const i = Array.from(n.reduce((r, o) => {
    if (this.dataManager.isParent(o)) {
      const s = this.dataManager.getDataObject(o).__children;
      return r.add(o), Array.isArray(s) && s.forEach((a) => r.add(this.dataManager.getRowIndex(a))), r;
    }
    return r.add(o);
  }, /* @__PURE__ */ new Set()));
  n.length = 0, n.push(...i);
}
function _le() {
  this.collapsingUI.collapsedRowsStash.stash();
}
function Nle(t, e) {
  this.collapsingUI.collapsedRowsStash.shiftStash(this.dataManager.getRowIndex(e)), this.collapsingUI.collapsedRowsStash.applyStash(), this.headersUI.updateRowHeaderWidth();
}
function Dle() {
  this.collapsingUI.collapsedRowsStash.stash();
}
function kle(t, e, n) {
  this.collapsingUI.collapsedRowsStash.shiftStash(n, null, -1), this.collapsingUI.collapsedRowsStash.applyStash(), this.headersUI.updateRowHeaderWidth();
}
function Lle() {
  this.dataManager.rewriteCache();
}
function Ble() {
  this.headersUI.updateRowHeaderWidth();
}
function Fle(t, e) {
  im(dA, this) && (e.skipRender = !0);
}
function RI(t) {
  if (!o1(t)) {
    pb(ble), this.hot.getSettings()[ep] = !1, this.disablePlugin();
    return;
  }
  this.dataManager.setData(t), this.dataManager.rewriteCache();
}
function kv(t, e, n) {
  return (e = Vle(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function Vle(t) {
  var e = Wle(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Wle(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
class $le {
  // eslint-disable-next-line no-restricted-globals
  constructor(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : window;
    kv(this, "rootWindow", void 0), kv(this, "prefix", void 0), kv(this, "savedKeys", []), this.rootWindow = n, this.prefix = e, this.loadSavedKeys();
  }
  /**
   * Save data to localStorage.
   *
   * @param {string} key Key string.
   * @param {Mixed} value Value to save.
   */
  saveValue(e, n) {
    this.rootWindow.localStorage.setItem(`${this.prefix}_${e}`, JSON.stringify(n)), this.savedKeys.indexOf(e) === -1 && (this.savedKeys.push(e), this.saveSavedKeys());
  }
  /**
   * Load data from localStorage.
   *
   * @param {string} key Key string.
   * @param {object} defaultValue Object containing the loaded data.
   *
   * @returns {object|undefined}
   */
  loadValue(e, n) {
    const i = typeof e == "undefined" ? n : e, r = this.rootWindow.localStorage.getItem(`${this.prefix}_${i}`);
    return r === null ? void 0 : JSON.parse(r);
  }
  /**
   * Reset given data from localStorage.
   *
   * @param {string} key Key string.
   */
  reset(e) {
    this.rootWindow.localStorage.removeItem(`${this.prefix}_${e}`);
  }
  /**
   * Reset all data from localStorage.
   *
   */
  resetAll() {
    T(this.savedKeys, (e, n) => {
      this.rootWindow.localStorage.removeItem(`${this.prefix}_${this.savedKeys[n]}`);
    }), this.clearSavedKeys();
  }
  /**
   * Load and save all keys from localStorage.
   *
   * @private
   */
  loadSavedKeys() {
    const e = this.rootWindow.localStorage.getItem(`${this.prefix}__persistentStateKeys`), n = typeof e == "string" ? JSON.parse(e) : void 0;
    this.savedKeys = n || [];
  }
  /**
   * Save saved key in localStorage.
   *
   * @private
   */
  saveSavedKeys() {
    this.rootWindow.localStorage.setItem(`${this.prefix}__persistentStateKeys`, JSON.stringify(this.savedKeys));
  }
  /**
   * Clear saved key from localStorage.
   *
   * @private
   */
  clearSavedKeys() {
    this.savedKeys.length = 0, this.saveSavedKeys();
  }
}
const Ule = $le;
function Yle(t, e, n) {
  return (e = jle(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function jle(t) {
  var e = Gle(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Gle(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
he.getSingleton().register("persistentStateSave");
he.getSingleton().register("persistentStateLoad");
he.getSingleton().register("persistentStateReset");
const EI = "persistentState", zle = 0;
class Kle extends at {
  constructor() {
    super(...arguments), Yle(this, "storage", void 0);
  }
  static get PLUGIN_KEY() {
    return EI;
  }
  static get PLUGIN_PRIORITY() {
    return zle;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link PersistentState#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[EI];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled || (this.storage || (this.storage = new Ule(this.hot.rootElement.id, this.hot.rootWindow)), this.addHook("persistentStateSave", (e, n) => this.saveValue(e, n)), this.addHook("persistentStateLoad", (e, n) => this.loadValue(e, n)), this.addHook("persistentStateReset", () => this.resetValue()), super.enablePlugin());
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.storage = void 0, super.disablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`persistentState`](@/api/options.md#persistentstate)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Loads the value from local storage.
   *
   * @param {string} key Storage key.
   * @param {object} saveTo Saved value from local storage.
   */
  loadValue(e, n) {
    n.value = this.storage.loadValue(e);
  }
  /**
   * Saves the data to local storage.
   *
   * @param {string} key Storage key.
   * @param {Mixed} value Value to save.
   */
  saveValue(e, n) {
    this.storage.saveValue(e, n);
  }
  /**
   * Resets the data or all data from local storage.
   *
   * @param {string} key [optional] Storage key.
   */
  resetValue(e) {
    typeof e == "undefined" ? this.storage.resetAll() : this.storage.reset(e);
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function Qle(t, e) {
  Xle(t, e), e.add(t);
}
function Xle(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function Lv(t, e, n) {
  return (e = qle(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function qle(t) {
  var e = Zle(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Zle(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function TI(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const Bv = "search", Jle = 190, eue = "htSearchResult", tue = function(t, e, n, i, r) {
  t.getCellMeta(e, n).isSearchResult = r;
}, nue = function(t, e, n) {
  return Mt(t) || t === null || !t.toLocaleLowerCase || t.length === 0 || Mt(e) || e === null ? !1 : e.toString().toLocaleLowerCase(n.locale).indexOf(t.toLocaleLowerCase(n.locale)) !== -1;
};
var Fv = /* @__PURE__ */ new WeakSet();
class iue extends at {
  constructor() {
    super(...arguments), Qle(this, Fv), Lv(this, "callback", tue), Lv(this, "queryMethod", nue), Lv(this, "searchResultClass", eue);
  }
  static get PLUGIN_KEY() {
    return Bv;
  }
  static get PLUGIN_PRIORITY() {
    return Jle;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link AutoRowSize#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return this.hot.getSettings()[Bv];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    var e = this;
    if (this.enabled)
      return;
    const n = this.hot.getSettings()[Bv];
    this.updatePluginSettings(n), this.addHook("beforeRenderer", function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return TI(Fv, e, MI).call(e, ...r);
    }), super.enablePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    var e = this;
    const n = function() {
      for (var i = arguments.length, r = new Array(i), o = 0; o < i; o++)
        r[o] = arguments[o];
      return TI(Fv, e, MI).call(e, ...r);
    };
    this.hot.addHook("beforeRenderer", n), this.hot.addHookOnce("afterViewRender", () => {
      this.hot.removeHook("beforeRenderer", n);
    }), super.disablePlugin();
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`search`](@/api/options.md#search)
   */
  updatePlugin() {
    this.disablePlugin(), this.enablePlugin(), super.updatePlugin();
  }
  /**
   * Makes the query.
   *
   * @param {string} queryStr Value to be search.
   * @param {Function} [callback] Callback function performed on cells with values which matches to the searched query.
   * @param {Function} [queryMethod] Query function responsible for determining whether a query matches the value stored in a cell.
   * @returns {object[]} Return an array of objects with `row`, `col`, `data` properties or empty array.
   */
  query(e) {
    let n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.getCallback(), i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : this.getQueryMethod();
    const r = this.hot.countRows(), o = this.hot.countCols(), s = [], a = this.hot;
    return Ae(0, r - 1, (l) => {
      Ae(0, o - 1, (u) => {
        const c = this.hot.getDataAtCell(l, u), h = this.hot.getCellMeta(l, u), d = h.search.callback || n, m = (h.search.queryMethod || i)(e, c, h);
        if (m) {
          const w = {
            row: l,
            col: u,
            data: c
          };
          s.push(w);
        }
        d && d(a, l, u, c, m);
      });
    }), s;
  }
  /**
   * Gets the callback function.
   *
   * @returns {Function} Return the callback function.
   */
  getCallback() {
    return this.callback;
  }
  /**
   * Sets the callback function. This function will be called during querying for each cell.
   *
   * @param {Function} newCallback A callback function.
   */
  setCallback(e) {
    this.callback = e;
  }
  /**
   * Gets the query method function.
   *
   * @returns {Function} Return the query method.
   */
  getQueryMethod() {
    return this.queryMethod;
  }
  /**
   * Sets the query method function. The function is responsible for determining whether a query matches the value stored in a cell.
   *
   * @param {Function} newQueryMethod A function with specific match logic.
   */
  setQueryMethod(e) {
    this.queryMethod = e;
  }
  /**
   * Gets search result cells class name.
   *
   * @returns {string} Return the cell class name.
   */
  getSearchResultClass() {
    return this.searchResultClass;
  }
  /**
   * Sets search result cells class name. This class name will be added to each cell that belongs to the searched query.
   *
   * @param {string} newElementClass CSS class name.
   */
  setSearchResultClass(e) {
    this.searchResultClass = e;
  }
  /**
   * Updates the settings of the plugin.
   *
   * @param {object} searchSettings The plugin settings, taken from Handsontable configuration.
   * @private
   */
  updatePluginSettings(e) {
    Be(e) && (e.searchResultClass && this.setSearchResultClass(e.searchResultClass), e.queryMethod && this.setQueryMethod(e.queryMethod), e.callback && this.setCallback(e.callback));
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function MI(t, e, n, i, r, o) {
  const s = o.className || [];
  let a = [];
  typeof s == "string" ? a = s.split(" ") : a.push(...s), this.isEnabled() && o.isSearchResult ? a.includes(this.searchResultClass) || a.push(`${this.searchResultClass}`) : a.includes(this.searchResultClass) && a.splice(a.indexOf(this.searchResultClass), 1), o.className = a.join(" ");
}
function rue(t, e) {
  oue(t, e), e.add(t);
}
function oue(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function xg(t, e, n) {
  return (e = sue(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function sue(t) {
  var e = aue(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function aue(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Vv(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const lue = "touchScroll", uue = 200;
var Hg = /* @__PURE__ */ new WeakSet();
class cue extends at {
  constructor() {
    super(...arguments), rue(this, Hg), xg(this, "scrollbars", []), xg(this, "clones", []), xg(this, "lockedCollection", !1), xg(this, "freezeOverlays", !1);
  }
  static get PLUGIN_KEY() {
    return lue;
  }
  static get PLUGIN_PRIORITY() {
    return uue;
  }
  static get SETTING_KEYS() {
    return !0;
  }
  /**
   * Check if plugin is enabled.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return ub();
  }
  /**
   * Enable the plugin.
   */
  enablePlugin() {
    this.enabled || (this.addHook("afterViewRender", () => Vv(Hg, this, hue).call(this)), this.registerEvents(), super.enablePlugin());
  }
  /**
   * Updates the plugin to use the latest options you have specified.
   */
  updatePlugin() {
    this.lockedCollection = !1, super.updatePlugin();
  }
  /**
   * Disable plugin for this Handsontable instance.
   */
  disablePlugin() {
    super.disablePlugin();
  }
  /**
   * Register all necessary events.
   *
   * @private
   */
  registerEvents() {
    this.addHook("beforeTouchScroll", () => Vv(Hg, this, due).call(this)), this.addHook("afterMomentumScroll", () => Vv(Hg, this, fue).call(this));
  }
}
function hue() {
  if (this.lockedCollection)
    return;
  const {
    topOverlay: t,
    bottomOverlay: e,
    inlineStartOverlay: n,
    topInlineStartCornerOverlay: i,
    bottomInlineStartCornerOverlay: r
  } = this.hot.view._wt.wtOverlays;
  this.lockedCollection = !0, this.scrollbars.length = 0, this.scrollbars.push(t), e.clone && this.scrollbars.push(e), this.scrollbars.push(n), i && this.scrollbars.push(i), r && r.clone && this.scrollbars.push(r), this.clones = [], t.needFullRender && this.clones.push(t.clone.wtTable.holder.parentNode), e.needFullRender && this.clones.push(e.clone.wtTable.holder.parentNode), n.needFullRender && this.clones.push(n.clone.wtTable.holder.parentNode), i && this.clones.push(i.clone.wtTable.holder.parentNode), r && r.clone && this.clones.push(r.clone.wtTable.holder.parentNode);
}
function due() {
  this.freezeOverlays = !0, T(this.clones, (t) => {
    Q(t, "hide-tween");
  });
}
function fue() {
  this.freezeOverlays = !1, T(this.clones, (t) => {
    xe(t, "hide-tween"), Q(t, "show-tween");
  }), this.hot._registerTimeout(() => {
    T(this.clones, (t) => {
      xe(t, "show-tween");
    });
  }, 400), T(this.scrollbars, (t) => {
    t.refresh(), t.resetFixedPosition();
  }), this.hot.view._wt.wtOverlays.syncScrollWithMaster();
}
function gue(t, e) {
  mue(t, e), e.add(t);
}
function mue(t, e) {
  if (e.has(t))
    throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function pue(t, e, n) {
  return (e = wue(e)) in t ? Object.defineProperty(t, e, { value: n, enumerable: !0, configurable: !0, writable: !0 }) : t[e] = n, t;
}
function wue(t) {
  var e = Aue(t, "string");
  return typeof e == "symbol" ? e : e + "";
}
function Aue(t, e) {
  if (typeof t != "object" || !t)
    return t;
  var n = t[Symbol.toPrimitive];
  if (n !== void 0) {
    var i = n.call(t, e || "default");
    if (typeof i != "object")
      return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(t);
}
function Cue(t, e, n) {
  if (typeof t == "function" ? t === e : t.has(e))
    return arguments.length < 3 ? e : n;
  throw new TypeError("Private element is not present on this object");
}
const rm = "trimRows", vue = 330;
var II = /* @__PURE__ */ new WeakSet();
class yue extends at {
  constructor() {
    super(...arguments), gue(this, II), pue(this, "trimmedRowsMap", null);
  }
  static get PLUGIN_KEY() {
    return rm;
  }
  static get PLUGIN_PRIORITY() {
    return vue;
  }
  /**
   * Checks if the plugin is enabled in the handsontable settings. This method is executed in {@link Hooks#beforeInit}
   * hook and if it returns `true` then the {@link AutoRowSize#enablePlugin} method is called.
   *
   * @returns {boolean}
   */
  isEnabled() {
    return !!this.hot.getSettings()[rm];
  }
  /**
   * Enables the plugin functionality for this Handsontable instance.
   */
  enablePlugin() {
    this.enabled || (this.trimmedRowsMap = this.hot.rowIndexMapper.registerMap("trimRows", new Td()), this.trimmedRowsMap.addLocalHook("init", () => Cue(II, this, Sue).call(this)), super.enablePlugin());
  }
  /**
   * Updates the plugin's state.
   *
   * This method is executed when [`updateSettings()`](@/api/core.md#updatesettings) is invoked with any of the following configuration options:
   *  - [`trimRows`](@/api/options.md#trimrows)
   */
  updatePlugin() {
    const e = this.hot.getSettings()[rm];
    Array.isArray(e) && this.hot.batchExecution(() => {
      this.trimmedRowsMap.clear(), T(e, (n) => {
        this.trimmedRowsMap.setValueAtIndex(n, !0);
      });
    }, !0), super.updatePlugin();
  }
  /**
   * Disables the plugin functionality for this Handsontable instance.
   */
  disablePlugin() {
    this.hot.rowIndexMapper.unregisterMap("trimRows"), super.disablePlugin();
  }
  /**
   * Get list of trimmed rows.
   *
   * @returns {Array} Physical rows.
   */
  getTrimmedRows() {
    return this.trimmedRowsMap.getTrimmedIndexes();
  }
  /**
   * Trims the rows provided in the array.
   *
   * @param {number[]} rows Array of physical row indexes.
   * @fires Hooks#beforeTrimRow
   * @fires Hooks#afterTrimRow
   */
  trimRows(e) {
    const n = this.getTrimmedRows(), i = this.isValidConfig(e);
    let r = n;
    i && (r = Array.from(new Set(n.concat(e)))), this.hot.runHooks("beforeTrimRow", n, r, i) !== !1 && (i && this.hot.batchExecution(() => {
      T(e, (s) => {
        this.trimmedRowsMap.setValueAtIndex(s, !0);
      });
    }, !0), this.hot.runHooks("afterTrimRow", n, r, i, i && r.length > n.length));
  }
  /**
   * Trims the row provided as a physical row index (counting from 0).
   *
   * @param {...number} row Physical row index.
   */
  trimRow() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    this.trimRows(n);
  }
  /**
   * Untrims the rows provided in the array.
   *
   * @param {number[]} rows Array of physical row indexes.
   * @fires Hooks#beforeUntrimRow
   * @fires Hooks#afterUntrimRow
   */
  untrimRows(e) {
    const n = this.getTrimmedRows(), i = this.isValidConfig(e);
    let r = n;
    const o = this.trimmedRowsMap.getValues().slice(), s = e.length > 0;
    i && s && (T(e, (l) => {
      o[l] = !1;
    }), r = rn(o, (l, u, c) => (u && l.push(c), l), [])), this.hot.runHooks("beforeUntrimRow", n, r, i && s) !== !1 && (i && s && this.trimmedRowsMap.setValues(o), this.hot.runHooks("afterUntrimRow", n, r, i && s, i && r.length < n.length));
  }
  /**
   * Untrims the row provided as a physical row index (counting from 0).
   *
   * @param {...number} row Physical row index.
   */
  untrimRow() {
    for (var e = arguments.length, n = new Array(e), i = 0; i < e; i++)
      n[i] = arguments[i];
    this.untrimRows(n);
  }
  /**
   * Checks if given row is hidden.
   *
   * @param {number} physicalRow Physical row index.
   * @returns {boolean}
   */
  isTrimmed(e) {
    return this.trimmedRowsMap.getValueAtIndex(e) || !1;
  }
  /**
   * Untrims all trimmed rows.
   */
  untrimAll() {
    this.untrimRows(this.getTrimmedRows());
  }
  /**
   * Get if trim config is valid. Check whether all of the provided physical row indexes are within source data.
   *
   * @param {Array} trimmedRows List of physical row indexes.
   * @returns {boolean}
   */
  isValidConfig(e) {
    const n = this.hot.countSourceRows();
    return e.every((i) => Number.isInteger(i) && i >= 0 && i < n);
  }
  /**
   * Destroys the plugin instance.
   */
  destroy() {
    super.destroy();
  }
}
function Sue() {
  const t = this.hot.getSettings()[rm];
  Array.isArray(t) && this.hot.batchExecution(() => {
    T(t, (e) => {
      this.trimmedRowsMap.setValueAtIndex(e, !0);
    });
  }, !0);
}
var $_ = {}, bue = Xs, Rue = $_, Eue = bue("iterator"), Tue = Array.prototype, Mue = function(t) {
  return t !== void 0 && (Rue.Array === t || Tue[Eue] === t);
}, Iue = aO, OI = lp, Oue = ip, Pue = $_, xue = Xs, Hue = xue("iterator"), U_ = function(t) {
  if (!Oue(t))
    return OI(t, Hue) || OI(t, "@@iterator") || Pue[Iue(t)];
}, _ue = Tr, Nue = ad, Due = ts, kue = ap, Lue = U_, Bue = TypeError, Fue = function(t, e) {
  var n = arguments.length < 2 ? Lue(t) : e;
  if (Nue(n))
    return Due(_ue(n, t));
  throw new Bue(kue(t) + " is not iterable");
}, Vue = bO, Wue = Tr, $ue = ts, Uue = ap, Yue = Mue, jue = hd, PI = op, Gue = Fue, zue = U_, xI = SS, Kue = TypeError, om = function(t, e) {
  this.stopped = t, this.result = e;
}, HI = om.prototype, Que = function(t, e, n) {
  var i = n && n.that, r = !!(n && n.AS_ENTRIES), o = !!(n && n.IS_RECORD), s = !!(n && n.IS_ITERATOR), a = !!(n && n.INTERRUPTED), l = Vue(e, i), u, c, h, d, g, m, w, C = function(b) {
    return u && xI(u, "normal", b), new om(!0, b);
  }, S = function(b) {
    return r ? ($ue(b), a ? l(b[0], b[1], C) : l(b[0], b[1])) : a ? l(b, C) : l(b);
  };
  if (o)
    u = t.iterator;
  else if (s)
    u = t;
  else {
    if (c = zue(t), !c)
      throw new Kue(Uue(t) + " is not iterable");
    if (Yue(c)) {
      for (h = 0, d = jue(t); d > h; h++)
        if (g = S(t[h]), g && PI(HI, g))
          return g;
      return new om(!1);
    }
    u = Gue(t, c);
  }
  for (m = o ? t.next : u.next; !(w = Wue(m, u)).done; ) {
    try {
      g = S(w.value);
    } catch (b) {
      xI(u, "throw", b);
    }
    if (typeof g == "object" && g && PI(HI, g))
      return g;
  }
  return new om(!1);
}, Xue = Er, que = qs, Zue = np, Jue = function(t, e, n) {
  Xue ? que.f(t, e, Zue(0, n)) : t[e] = n;
}, ece = Mn, tce = Que, nce = Jue;
ece({ target: "Object", stat: !0 }, {
  fromEntries: function(e) {
    var n = {};
    return tce(e, function(i, r) {
      nce(n, i, r);
    }, { AS_ENTRIES: !0 }), n;
  }
});
const Y_ = "undoRedo", ice = "undoRedo";
function ie(t) {
  const e = this;
  this.instance = t, this.doneActions = [], this.undoneActions = [], this.ignoreNewActions = !1, this.enabled = !1, t.addHook("afterChange", function(i, r) {
    const o = i && i.length;
    if (!o || !i.find((l) => {
      const [, , u, c] = l;
      return u !== c;
    }))
      return;
    const a = () => {
      const l = i.reduce((c, h) => (c.push([...h]), c), []);
      T(l, (c) => {
        c[1] = t.propToCol(c[1]);
      });
      const u = o > 1 ? this.getSelected() : [[l[0][0], l[0][1]]];
      return new ie.ChangeAction(l, u);
    };
    e.done(a, r);
  }), t.addHook("afterCreateRow", (i, r, o) => {
    e.done(() => new ie.CreateRowAction(i, r), o);
  });
  const n = (i, r, o, s) => {
    const a = ["visualRow", "visualCol", "row", "col", "prop"], l = a.length, u = [];
    return Ae(o, s, (c) => {
      Ae(i, r, (h) => {
        const d = t.getCellMeta(h, c);
        if (Object.keys(d).length !== l) {
          const g = Object.fromEntries(Object.entries(d).filter((m) => {
            let [w] = m;
            return a.includes(w) === !1;
          }));
          u.push([d.visualRow, d.visualCol, g]);
        }
      });
    }), u;
  };
  t.addHook("beforeRemoveRow", (i, r, o, s) => {
    const a = () => {
      const l = t.toPhysicalRow(i), u = l + r - 1, c = Fn(e.instance.getSourceData(l, 0, l + r - 1, e.instance.countSourceCols() - 1));
      return new ie.RemoveRowAction(l, c, t.getSettings().fixedRowsBottom, t.getSettings().fixedRowsTop, t.rowIndexMapper.getIndexesSequence(), n(l, u, 0, t.countCols() - 1));
    };
    e.done(a, s);
  }), t.addHook("afterCreateCol", (i, r, o) => {
    e.done(() => new ie.CreateColumnAction(i, r), o);
  }), t.addHook("beforeRemoveCol", (i, r, o, s) => {
    const a = () => {
      const l = e.instance.getSourceDataArray(), u = (e.instance.countCols() + i) % e.instance.countCols(), c = u + r - 1, h = [], d = [], g = [];
      Ae(l.length - 1, (C) => {
        const S = [], b = l[C];
        Ae(u, c, (R) => {
          S.push(b[t.toPhysicalColumn(R)]);
        }), h.push(S);
      }), Ae(r - 1, (C) => {
        g.push(t.toPhysicalColumn(u + C));
      }), Array.isArray(t.getSettings().colHeaders) && Ae(r - 1, (C) => {
        d.push(t.getSettings().colHeaders[t.toPhysicalColumn(u + C)] || null);
      });
      const m = t.columnIndexMapper.getIndexesSequence(), w = t.rowIndexMapper.getIndexesSequence();
      return new ie.RemoveColumnAction(u, g, h, d, m, w, t.getSettings().fixedColumnsStart, n(0, t.countRows(), u, c));
    };
    e.done(a, s);
  }), t.addHook("beforeCellAlignment", (i, r, o, s) => {
    e.done(() => new ie.CellAlignmentAction(i, r, o, s));
  }), t.addHook("beforeFilter", (i, r) => {
    e.done(() => new ie.FiltersAction(i, r));
  }), t.addHook("beforeRowMove", (i, r) => {
    i !== !1 && e.done(() => new ie.RowMoveAction(i, r));
  }), t.addHook("beforeColumnMove", (i, r) => {
    i !== !1 && e.done(() => new ie.ColumnMoveAction(i, r));
  }), t.addHook("beforeMergeCells", (i, r) => {
    r || e.done(() => new ie.MergeCellsAction(t, i));
  }), t.addHook("afterUnmergeCells", (i, r) => {
    r || e.done(() => new ie.UnmergeCellsAction(t, i));
  }), t.addHook("beforeColumnSort", (i, r, o) => {
    o && e.done(() => new ie.ColumnSortAction(i, r));
  }), t.addHook("afterInit", () => {
    e.init();
  });
}
ie.prototype.done = function(t, e) {
  if (this.ignoreNewActions || e === "UndoRedo.undo" || e === "UndoRedo.redo" || e === "auto")
    return;
  const i = this.doneActions.slice();
  if (this.instance.runHooks("beforeUndoStackChange", i, e) === !1)
    return;
  const o = t(), s = this.undoneActions.slice();
  this.doneActions.push(o), this.instance.runHooks("afterUndoStackChange", i, this.doneActions.slice()), this.instance.runHooks("beforeRedoStackChange", s), this.undoneActions.length = 0, this.instance.runHooks("afterRedoStackChange", s, this.undoneActions.slice());
};
ie.prototype.undo = function() {
  if (this.isUndoAvailable()) {
    const t = this.doneActions.slice();
    this.instance.runHooks("beforeUndoStackChange", t);
    const e = this.doneActions.pop();
    this.instance.runHooks("afterUndoStackChange", t, this.doneActions.slice());
    const n = Fn(e);
    if (this.instance.runHooks("beforeUndo", n) === !1)
      return;
    this.ignoreNewActions = !0;
    const r = this, o = this.undoneActions.slice();
    this.instance.runHooks("beforeRedoStackChange", o), e.undo(this.instance, () => {
      r.ignoreNewActions = !1, r.undoneActions.push(e);
    }), this.instance.runHooks("afterRedoStackChange", o, this.undoneActions.slice()), this.instance.runHooks("afterUndo", n);
  }
};
ie.prototype.redo = function() {
  if (this.isRedoAvailable()) {
    const t = this.undoneActions.slice();
    this.instance.runHooks("beforeRedoStackChange", t);
    const e = this.undoneActions.pop();
    this.instance.runHooks("afterRedoStackChange", t, this.undoneActions.slice());
    const n = Fn(e);
    if (this.instance.runHooks("beforeRedo", n) === !1)
      return;
    this.ignoreNewActions = !0;
    const r = this, o = this.doneActions.slice();
    this.instance.runHooks("beforeUndoStackChange", o), e.redo(this.instance, () => {
      r.ignoreNewActions = !1, r.doneActions.push(e);
    }), this.instance.runHooks("afterUndoStackChange", o, this.doneActions.slice()), this.instance.runHooks("afterRedo", n);
  }
};
ie.prototype.isUndoAvailable = function() {
  return this.doneActions.length > 0;
};
ie.prototype.isRedoAvailable = function() {
  return this.undoneActions.length > 0;
};
ie.prototype.clear = function() {
  this.doneActions.length = 0, this.undoneActions.length = 0;
};
ie.prototype.isEnabled = function() {
  return this.enabled;
};
ie.prototype.enable = function() {
  if (this.isEnabled())
    return;
  const t = this.instance;
  this.enabled = !0, sce(t), this.registerShortcuts(), t.addHook("afterChange", j_);
};
ie.prototype.disable = function() {
  if (!this.isEnabled())
    return;
  const t = this.instance;
  this.enabled = !1, ace(t), this.unregisterShortcuts(), t.removeHook("afterChange", j_);
};
ie.prototype.destroy = function() {
  this.clear(), this.instance = null, this.doneActions = null, this.undoneActions = null;
};
ie.Action = function() {
};
ie.Action.prototype.undo = function() {
};
ie.Action.prototype.redo = function() {
};
ie.ChangeAction = function(t, e) {
  this.changes = t, this.selected = e, this.actionType = "change";
};
Ir(ie.ChangeAction, ie.Action);
ie.ChangeAction.prototype.undo = function(t, e) {
  const n = Fn(this.changes), i = t.countEmptyRows(!0), r = t.countEmptyCols(!0);
  for (let s = 0, a = n.length; s < a; s++)
    n[s].splice(3, 1);
  t.addHookOnce("afterChange", e), t.setDataAtCell(n, null, null, "UndoRedo.undo");
  for (let s = 0, a = n.length; s < a; s++) {
    const [l, u] = n[s];
    t.getSettings().minSpareRows && l + 1 + t.getSettings().minSpareRows === t.countRows() && i === t.getSettings().minSpareRows && (t.alter("remove_row", parseInt(l + 1, 10), t.getSettings().minSpareRows), t.undoRedo.doneActions.pop()), t.getSettings().minSpareCols && u + 1 + t.getSettings().minSpareCols === t.countCols() && r === t.getSettings().minSpareCols && (t.alter("remove_col", parseInt(u + 1, 10), t.getSettings().minSpareCols), t.undoRedo.doneActions.pop());
  }
  const o = t.getSelectedLast();
  if (o !== void 0) {
    const [s, a] = n[0], [l, u] = o, c = t.view.getFirstFullyVisibleRow(), h = t.view.getFirstFullyVisibleColumn(), d = s >= c, g = a >= h;
    if ((d && g) === !1 && (l !== s || u !== a) === !0) {
      const C = {
        row: s,
        col: a
      };
      d === !1 && (C.verticalSnap = "top"), g === !1 && (C.horizontalSnap = "start"), t.scrollViewportTo(C);
    }
  }
  t.selectCells(this.selected, !1, !1);
};
ie.ChangeAction.prototype.redo = function(t, e) {
  const n = Fn(this.changes);
  for (let i = 0, r = n.length; i < r; i++)
    n[i].splice(2, 1);
  t.addHookOnce("afterChange", e), t.setDataAtCell(n, null, null, "UndoRedo.redo"), this.selected && t.selectCells(this.selected, !1, !1);
};
ie.CreateRowAction = function(t, e) {
  this.index = t, this.amount = e, this.actionType = "insert_row";
};
Ir(ie.CreateRowAction, ie.Action);
ie.CreateRowAction.prototype.undo = function(t, e) {
  const n = t.countRows(), i = t.getSettings().minSpareRows;
  this.index >= n && this.index - i < n && (this.index -= i), t.addHookOnce("afterRemoveRow", e), t.alter("remove_row", this.index, this.amount, "UndoRedo.undo");
};
ie.CreateRowAction.prototype.redo = function(t, e) {
  t.addHookOnce("afterCreateRow", e), t.alter("insert_row_above", this.index, this.amount, "UndoRedo.redo");
};
ie.RemoveRowAction = function(t, e, n, i, r, o) {
  this.index = t, this.data = e, this.actionType = "remove_row", this.fixedRowsBottom = n, this.fixedRowsTop = i, this.rowIndexesSequence = r, this.removedCellMetas = o;
};
Ir(ie.RemoveRowAction, ie.Action);
ie.RemoveRowAction.prototype.undo = function(t, e) {
  const n = t.getSettings(), i = [];
  n.fixedRowsBottom = this.fixedRowsBottom, n.fixedRowsTop = this.fixedRowsTop, this.data.forEach((r, o) => {
    Object.keys(r).forEach((s) => {
      const a = parseInt(s, 10);
      i.push([this.index + o, isNaN(a) ? s : a, r[s]]);
    });
  }), t.alter("insert_row_above", this.index, this.data.length, "UndoRedo.undo"), this.removedCellMetas.forEach((r) => {
    let [o, s, a] = r;
    t.setCellMetaObject(o, s, a);
  }), t.addHookOnce("afterViewRender", e), t.setSourceDataAtCell(i, null, null, "UndoRedo.undo"), t.rowIndexMapper.setIndexesSequence(this.rowIndexesSequence);
};
ie.RemoveRowAction.prototype.redo = function(t, e) {
  t.addHookOnce("afterRemoveRow", e), t.alter("remove_row", this.index, this.data.length, "UndoRedo.redo");
};
ie.CreateColumnAction = function(t, e) {
  this.index = t, this.amount = e, this.actionType = "insert_col";
};
Ir(ie.CreateColumnAction, ie.Action);
ie.CreateColumnAction.prototype.undo = function(t, e) {
  t.addHookOnce("afterRemoveCol", e), t.alter("remove_col", this.index, this.amount, "UndoRedo.undo");
};
ie.CreateColumnAction.prototype.redo = function(t, e) {
  t.addHookOnce("afterCreateCol", e), t.alter("insert_col_start", this.index, this.amount, "UndoRedo.redo");
};
ie.RemoveColumnAction = function(t, e, n, i, r, o, s, a) {
  this.index = t, this.indexes = e, this.data = n, this.amount = this.data[0].length, this.headers = i, this.columnPositions = r.slice(0), this.rowPositions = o.slice(0), this.actionType = "remove_col", this.fixedColumnsStart = s, this.removedCellMetas = a;
};
Ir(ie.RemoveColumnAction, ie.Action);
ie.RemoveColumnAction.prototype.undo = function(t, e) {
  const n = t.getSettings();
  n.fixedColumnsStart = this.fixedColumnsStart;
  const i = this.indexes.slice(0).sort(), r = (u, c, h) => h[this.indexes.indexOf(i[c])], o = this.data.length, s = [];
  for (let u = 0; u < o; u++)
    s.push(Ke(this.data[u], r));
  const a = Ke(this.headers, r), l = [];
  t.alter("insert_col_start", this.indexes[0], this.indexes.length, "UndoRedo.undo"), T(t.getSourceDataArray(), (u, c) => {
    T(i, (h, d) => {
      u[h] = s[c][d], l.push([c, h, u[h]]);
    });
  }), t.setSourceDataAtCell(l, void 0, void 0, "UndoRedo.undo"), typeof this.headers != "undefined" && T(a, (u, c) => {
    t.getSettings().colHeaders[i[c]] = u;
  }), this.removedCellMetas.forEach((u) => {
    let [c, h, d] = u;
    t.setCellMetaObject(c, h, d);
  }), t.batchExecution(() => {
    t.rowIndexMapper.setIndexesSequence(this.rowPositions), t.columnIndexMapper.setIndexesSequence(this.columnPositions);
  }, !0), t.addHookOnce("afterViewRender", e), t.render();
};
ie.RemoveColumnAction.prototype.redo = function(t, e) {
  t.addHookOnce("afterRemoveCol", e), t.alter("remove_col", this.index, this.amount, "UndoRedo.redo");
};
ie.CellAlignmentAction = function(t, e, n, i) {
  this.stateBefore = t, this.range = e, this.type = n, this.alignment = i;
};
ie.CellAlignmentAction.prototype.undo = function(t, e) {
  T(this.range, (n) => {
    n.forAll((i, r) => {
      i >= 0 && r >= 0 && t.setCellMeta(i, r, "className", this.stateBefore[i][r] || " htLeft");
    });
  }), t.addHookOnce("afterViewRender", e), t.render();
};
ie.CellAlignmentAction.prototype.redo = function(t, e) {
  Ss(this.range, this.type, this.alignment, (n, i) => t.getCellMeta(n, i), (n, i, r, o) => t.setCellMeta(n, i, r, o)), t.addHookOnce("afterViewRender", e), t.render();
};
ie.FiltersAction = function(t, e) {
  this.previousConditionsStack = e, this.conditionsStack = t, this.actionType = "filter";
};
Ir(ie.FiltersAction, ie.Action);
ie.FiltersAction.prototype.undo = function(t, e) {
  const n = t.getPlugin("filters");
  t.addHookOnce("afterViewRender", e), this.previousConditionsStack && n.conditionCollection.importAllConditions(this.previousConditionsStack), n.filter();
};
ie.FiltersAction.prototype.redo = function(t, e) {
  const n = t.getPlugin("filters");
  t.addHookOnce("afterViewRender", e), n.conditionCollection.importAllConditions(this.conditionsStack), n.filter();
};
class rce extends ie.Action {
  constructor(e, n) {
    super(), this.cellRange = n;
    const i = this.cellRange.getTopStartCorner(), r = this.cellRange.getBottomEndCorner();
    this.rangeData = e.getData(i.row, i.col, r.row, r.col);
  }
  undo(e, n) {
    const i = e.getPlugin("mergeCells");
    e.addHookOnce("afterViewRender", n), i.unmergeRange(this.cellRange, !0);
    const r = this.cellRange.getTopStartCorner();
    e.populateFromArray(r.row, r.col, this.rangeData, void 0, void 0, "MergeCells");
  }
  redo(e, n) {
    const i = e.getPlugin("mergeCells");
    e.addHookOnce("afterViewRender", n), i.mergeRange(this.cellRange);
  }
}
ie.MergeCellsAction = rce;
class oce extends ie.Action {
  constructor(e, n) {
    super(), this.cellRange = n;
  }
  undo(e, n) {
    const i = e.getPlugin("mergeCells");
    e.addHookOnce("afterViewRender", n), i.mergeRange(this.cellRange, !0);
  }
  redo(e, n) {
    const i = e.getPlugin("mergeCells");
    e.addHookOnce("afterViewRender", n), i.unmergeRange(this.cellRange, !0), e.render();
  }
}
ie.UnmergeCellsAction = oce;
ie.RowMoveAction = function(t, e) {
  this.rows = t.slice(), this.finalRowIndex = e, this.actionType = "row_move";
};
Ir(ie.RowMoveAction, ie.Action);
ie.RowMoveAction.prototype.undo = function(t, e) {
  const n = t.getPlugin("manualRowMove");
  t.addHookOnce("afterViewRender", e), LR(this.rows, this.finalRowIndex, t.rowIndexMapper.getNumberOfIndexes()).reverse().forEach((r) => {
    let {
      from: o,
      to: s
    } = r;
    o < s && (s -= 1), n.moveRow(s, o);
  }), t.render(), t.deselectCell(), t.selectRows(this.rows[0], this.rows[0] + this.rows.length - 1);
};
ie.RowMoveAction.prototype.redo = function(t, e) {
  const n = t.getPlugin("manualRowMove");
  t.addHookOnce("afterViewRender", e), n.moveRows(this.rows.slice(), this.finalRowIndex), t.render(), t.deselectCell(), t.selectRows(this.finalRowIndex, this.finalRowIndex + this.rows.length - 1);
};
ie.ColumnMoveAction = function(t, e) {
  this.columns = t.slice(), this.finalColumnIndex = e, this.actionType = "col_move";
};
Ir(ie.ColumnMoveAction, ie.Action);
ie.ColumnMoveAction.prototype.undo = function(t, e) {
  const n = t.getPlugin("manualColumnMove");
  t.addHookOnce("afterViewRender", e), LR(this.columns, this.finalColumnIndex, t.columnIndexMapper.getNumberOfIndexes()).reverse().forEach((r) => {
    let {
      from: o,
      to: s
    } = r;
    o < s && (s -= 1), n.moveColumn(s, o);
  }), t.render(), t.deselectCell(), t.selectColumns(this.columns[0], this.columns[0] + this.columns.length - 1);
};
ie.ColumnMoveAction.prototype.redo = function(t, e) {
  const n = t.getPlugin("manualColumnMove");
  t.addHookOnce("afterViewRender", e), n.moveColumns(this.columns.slice(), this.finalColumnIndex), t.render(), t.deselectCell(), t.selectColumns(this.finalColumnIndex, this.finalColumnIndex + this.columns.length - 1);
};
ie.ColumnSortAction = function(t, e) {
  this.previousSortState = t, this.nextSortState = e;
};
Ir(ie.ColumnSortAction, ie.Action);
ie.ColumnSortAction.prototype.undo = function(t, e) {
  const n = t.getPlugin("columnSorting"), i = t.getPlugin("multiColumnSorting"), r = i.isEnabled() ? i : n;
  this.previousSortState.length ? r.sort(this.previousSortState) : r.clearSort(), e();
};
ie.ColumnSortAction.prototype.redo = function(t, e) {
  const n = t.getPlugin("columnSorting"), i = t.getPlugin("multiColumnSorting");
  (i.isEnabled() ? i : n).sort(this.nextSortState), e();
};
ie.prototype.init = function() {
  const t = this.instance.getSettings().undo, e = typeof t == "undefined" || t;
  this.instance.undoRedo || (this.instance.undoRedo = this), e ? this.instance.undoRedo.enable() : this.instance.undoRedo.disable();
};
ie.prototype.registerShortcuts = function() {
  const e = this.instance.getShortcutManager().getContext("grid"), i = {
    runOnlyIf: (r) => !r.altKey,
    group: Y_
  };
  e.addShortcuts([{
    keys: [["Control/Meta", "z"]],
    callback: () => {
      this.undo();
    }
  }, {
    keys: [["Control/Meta", "y"], ["Control/Meta", "Shift", "z"]],
    callback: () => {
      this.redo();
    }
  }], i);
};
ie.prototype.unregisterShortcuts = function() {
  this.instance.getShortcutManager().getContext("grid").removeShortcutsByGroup(Y_);
};
function j_(t, e) {
  const n = this;
  if (e === "loadData")
    return n.undoRedo.clear();
}
function sce(t) {
  t.undo = function() {
    return t.undoRedo.undo();
  }, t.redo = function() {
    return t.undoRedo.redo();
  }, t.isUndoAvailable = function() {
    return t.undoRedo.isUndoAvailable();
  }, t.isRedoAvailable = function() {
    return t.undoRedo.isRedoAvailable();
  }, t.clearUndo = function() {
    return t.undoRedo.clear();
  };
}
function ace(t) {
  delete t.undo, delete t.redo, delete t.isUndoAvailable, delete t.isRedoAvailable, delete t.clearUndo;
}
const Ld = he.getSingleton();
Ld.add("afterUpdateSettings", function() {
  var t;
  (t = this.getPlugin("undoRedo")) === null || t === void 0 || t.init();
});
Ld.register("beforeUndo");
Ld.register("afterUndo");
Ld.register("beforeRedo");
Ld.register("afterRedo");
ie.PLUGIN_KEY = ice;
ie.SETTING_KEYS = !0;
function lce() {
  rt(hh), rt(l8), rt(dh), rt(Y8), rt(Z8), rt(dH), rt(z9), rt(N5), rt(sA), rt(J7), rt(Eee), rt(_ee), rt(uA), rt(cte), rt(cie), rt(Uie), rt(mre), rt(Tre), rt(Bre), rt(Zre), rt(loe), rt(Ooe), rt(Boe), rt(Sse), rt(nae), rt(aae), rt(Nae), rt(Rle), rt(Kle), rt(iue), rt(cue), rt(yue), rt(ie);
}
function uce() {
  kJ(), e6(), u6(), O6(), lce();
}
function cce(t) {
  const e = typeof window == "undefined" ? !1 : window.jQuery;
  e && (e.fn.handsontable = function(n) {
    const i = this.first();
    let r = i.data("handsontable");
    if (typeof n != "string") {
      const u = n || {};
      return r ? r.updateSettings(u) : (r = new t.Core(i[0], u), i.data("handsontable", r), r.init()), i;
    }
    let o;
    if (r)
      if (typeof r[n] != "undefined") {
        for (var s = arguments.length, a = new Array(s > 1 ? s - 1 : 0), l = 1; l < s; l++)
          a[l - 1] = arguments[l];
        o = r[n].call(r, ...a), n === "destroy" && i.removeData();
      } else
        throw new Error(`Handsontable do not provide action: ${n}`);
    return o;
  });
}
var Wv, $v, Uv, Yv, jv;
uce();
cce(Ie);
Ie.__GhostTable = SR;
Ie._getListenersCounter = eK;
Ie._getRegisteredMapsCounter = WX;
Ie.EventManager = Qi;
const hce = [sG, gz, MX, n6, hz, KG, cG, jK, cz, dG, wz, YK], dce = [YG, Az];
Ie.helper = {};
Ie.dom = {};
T(hce, (t) => {
  T(Object.getOwnPropertyNames(t), (e) => {
    e.charAt(0) !== "_" && (Ie.helper[e] = t[e]);
  });
});
T(dce, (t) => {
  T(Object.getOwnPropertyNames(t), (e) => {
    e.charAt(0) !== "_" && (Ie.dom[e] = t[e]);
  });
});
Ie.cellTypes = (Wv = Ie.cellTypes) !== null && Wv !== void 0 ? Wv : {};
T(Z4(), (t) => {
  Ie.cellTypes[t] = iR(t);
});
Ie.cellTypes.registerCellType = Di;
Ie.cellTypes.getCellType = iR;
Ie.editors = ($v = Ie.editors) !== null && $v !== void 0 ? $v : {};
T(zz(), (t) => {
  Ie.editors[`${Bn(t)}Editor`] = Cm(t);
});
Ie.editors.registerEditor = ai;
Ie.editors.getEditor = Cm;
Ie.renderers = (Uv = Ie.renderers) !== null && Uv !== void 0 ? Uv : {};
T(s2(), (t) => {
  const e = vm(t);
  t === "base" && (Ie.renderers.cellDecorator = e), Ie.renderers[`${Bn(t)}Renderer`] = e;
});
Ie.renderers.registerRenderer = wi;
Ie.renderers.getRenderer = vm;
Ie.validators = (Yv = Ie.validators) !== null && Yv !== void 0 ? Yv : {};
T(u2(), (t) => {
  Ie.validators[`${Bn(t)}Validator`] = bb(t);
});
Ie.validators.registerValidator = $a;
Ie.validators.getValidator = bb;
Ie.plugins = (jv = Ie.plugins) !== null && jv !== void 0 ? jv : {};
T(Sb(), (t) => {
  Ie.plugins[t] = bd(t);
});
Ie.plugins[`${Bn(at.PLUGIN_KEY)}Plugin`] = at;
Ie.plugins.registerPlugin = rt;
Ie.plugins.getPlugin = bd;
/**
 * @preserve
 * Authors: soakit, HChenZi
 * Last updated: Mar 09, 2023
 *
 * Description: Definition file for Chinese - China language-country.
 */
const fce = {
  languageCode: "zh-CN",
  [Bp]: "上方插入行",
  [Fp]: "下方插入行",
  [Vp]: "左方插入列",
  [Wp]: "右方插入列",
  [$p]: ["移除该行", "移除多行"],
  [Up]: ["移除该列", "移除多列"],
  [Yp]: "撤销",
  [jp]: "恢复",
  [Vh]: "只读",
  [Gp]: "清空该列",
  [sw]: "对齐",
  [Wh]: "左对齐",
  [$h]: "水平居中",
  [Uh]: "右对齐",
  [Yh]: "两端对齐",
  [jh]: "顶端对齐",
  [Gh]: "垂直居中",
  [zh]: "底端对齐",
  [Zp]: "冻结该列",
  [Jp]: "取消冻结",
  [aw]: "边框",
  [lw]: "上",
  [uw]: "右",
  [cw]: "下",
  [hw]: "左",
  [dw]: "移除边框",
  [nw]: "插入批注",
  [iw]: "编辑批注",
  [rw]: "删除批注",
  [ow]: "只读批注",
  [ew]: "合并",
  [tw]: "取消合并",
  [zp]: "复制",
  [Kp]: "带标题的复制",
  [Qp]: "带标题组的复制",
  [Xp]: "仅复制标题",
  [qp]: "剪切",
  [fw]: "插入子行",
  [gw]: "与母行分离",
  [mw]: ["隐藏该列", "隐藏多列"],
  [pw]: ["显示该列", "显示多列"],
  [ww]: ["隐藏该行", "隐藏多行"],
  [Aw]: ["显示该行", "显示多行"],
  [Md]: "无",
  [Cw]: "为空",
  [vw]: "不为空",
  [yw]: "等于",
  [Sw]: "不等于",
  [bw]: "开头是",
  [Rw]: "结尾是",
  [Ew]: "包含",
  [Tw]: "不包含",
  [Mw]: "大于",
  [Iw]: "大于或等于",
  [Ow]: "小于",
  [Pw]: "小于或等于",
  [xw]: "在此范围",
  [Hw]: "不在此范围",
  [_w]: "之后",
  [Nw]: "之前",
  [Dw]: "今天",
  [kw]: "明天",
  [Lw]: "昨天",
  [Kh]: "空白单元格",
  [Bw]: "按条件过滤",
  [Fw]: "按值过滤",
  [Vw]: "且",
  [Id]: "或",
  [Ww]: "全选",
  [$w]: "清除",
  [Uw]: "确认",
  [Yw]: "取消",
  [jw]: "搜索",
  [Gw]: "值",
  [zw]: "第二值"
}, gce = fce;
eR(gce);
const mce = function(t, e, n, i, r, o, s) {
  if (Ie.renderers.getRenderer("text")(t, e, n, i, r, o, s), s.style) {
    const a = e.style;
    Object.assign(a, s.style);
  }
};
Ie.renderers.registerRenderer("styleRender", mce);
const pce = (t) => {
  const e = mu(!0), n = t(), i = {
    // 提交到worker
    emit: (a, l) => {
      e.value = !0, n == null || n.postMessage({
        type: a,
        payload: l
      });
    }
  }, r = {}, o = [], s = [];
  return n == null || n.addEventListener("message", (a) => {
    const { type: l, payload: u } = a.data, c = r[l];
    c != null && c.length && c.forEach((h) => h(u)), o.forEach((h) => h(a));
  }), n == null || n.addEventListener("error", (a) => {
    console.error(a), s.forEach((l) => l(a));
  }), eN(() => {
    n == null || n.terminate();
  }), {
    loading: e,
    // 一个worker的引用
    worker: i,
    // 消息钩子
    onWorkerMessage: (a) => o.push(a),
    // 异常钩子
    onWorkerError: (a) => s.push(a),
    // 处理器定义
    onWorkerEvent: (a, l) => {
      let u = r[a];
      u || (u = r[a] = []), u.push(l);
    },
    // 映射事件为方法
    mapEvents: (a) => Array.isArray(a) ? a.reduce((l, u) => (l[u] = (c) => i.emit(u, c), l), {}) : Object.keys(a).reduce((l, u) => {
      const c = a[u];
      return l[c] = (h) => i.emit(u, h), l;
    }, {})
  };
}, wce = { class: "excel-wrapper" }, Ace = {
  key: 0,
  class: "loading"
}, Cce = { class: "table-wrapper" }, vce = { class: "btn-group" }, yce = ["onClick"], Sce = /* @__PURE__ */ lS({
  __name: "XlsxTable",
  props: {
    data: {},
    type: {}
  },
  setup(t) {
    const e = t, n = mu(null), i = mu([]), r = mu(0);
    let o;
    const s = {
      language: "zh-CN",
      // columns: columns.value,
      colHeaders: !0,
      rowHeaders: !0,
      autoRowSize: !1,
      autoColumnSize: !1,
      height: "100%",
      // 静态设置，提高性能
      cells(m, w) {
        const C = o == null ? void 0 : o.cell;
        return C && Object.keys(C).length ? C[aJ(m, w)] : {};
      },
      columns(m) {
        const w = o == null ? void 0 : o.columns;
        return w != null && w.length ? w[m] : {};
      },
      colWidths(m) {
        const w = o == null ? void 0 : o.colWidths;
        return typeof w == "number" ? w : w && w[m] || (o == null ? void 0 : o.defaults.colWidth);
      },
      rowHeights(m) {
        const w = o == null ? void 0 : o.rowHeights;
        return typeof w == "number" ? w : w && w[m] || (o == null ? void 0 : o.defaults.rowHeight);
      },
      // contextMenu: true,
      // manualRowMove: true,
      // 关闭外部点击取消选中时间的行为
      outsideClickDeselects: !1,
      // fillHandle: {
      //   direction: 'vertical',
      //   autoInsertRow: true
      // },
      // afterSelectionEnd: this.afterSelectionEnd,
      // bindRowsWithHeaders: 'strict',
      licenseKey: "non-commercial-and-evaluation"
    }, a = tN(() => {
      var m;
      return (m = n.value) == null ? void 0 : m.hotInstance;
    }), l = () => e.type === "binary" ? uJ.create() : lJ.create(), { loading: u, worker: c, onWorkerEvent: h } = pce(l), d = (m) => {
      var w;
      r.value !== m && (r.value = m, c.emit("parseSheet", { sheet: (w = r.value) != null ? w : i.value[0].id }));
    }, g = () => hN(() => {
      setTimeout(() => {
        var m;
        (m = a.value) == null || m.updateSettings({
          data: o == null ? void 0 : o.data,
          mergeCells: o == null ? void 0 : o.merge
          // customBorders: sheetData?.border,
        });
      }, 0);
    });
    return h("sheets", ({ sheets: m }) => {
      if (i.value = m, m.length) {
        const [{ id: w }] = m;
        r.value = w, c.emit("parseSheet", { sheet: r.value });
      }
    }), h("parseSheet", ({ sheetData: m }) => {
      o = m, g();
    }), nN(() => e.data, () => {
      c.emit("parseWorkbook", { workbook: e.data });
    }), iN(() => t0(this, null, function* () {
      var m;
      (m = a.value) == null || m.addHook("afterUpdateSettings", () => u.value = !1), c.emit("parseWorkbook", { workbook: e.data });
    })), (m, w) => (Kc(), Qc("div", wce, [
      n0(u) ? (Kc(), Qc("div", Ace, w[0] || (w[0] = [
        $d("img", {
          class: "lg",
          src: gN,
          alt: "xlsx"
        }, null, -1),
        $d("img", {
          class: "sm",
          src: mN,
          alt: "loading"
        }, null, -1),
        rN(" 加载中，请耐心等待... ")
      ]))) : oN("", !0),
      $d("div", Cce, [
        sN(n0(rR), {
          ref_key: "table",
          ref: n,
          settings: s
        }, null, 512)
      ]),
      $d("div", vce, [
        (Kc(!0), Qc(aN, null, lN(i.value, (C) => (Kc(), Qc("button", {
          key: C.id,
          style: { padding: "0 30px" },
          class: uN({ active: r.value === C.id }),
          onClick: (S) => d(C.id)
        }, cN(C.name), 11, yce))), 128))
      ])
    ]));
  }
});
const Uce = /* @__PURE__ */ fN(Sce, [["__scopeId", "data-v-7789e35a"]]);
export {
  Uce as default
};
